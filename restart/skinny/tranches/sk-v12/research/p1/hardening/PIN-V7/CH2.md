# SK-V12 S-P1 PIN-V7 CH2 - Generality / Lock 14

Verdict: ACCEPT
Score: 98%

## Blocking Findings

None.

Review base was `HEAD d4a7e3e3f19482688fa42dd1be9cf584f6c3d19b`. CH2's contract remains Lock 14 generality: no grammar-name leak, no JSON-only evidence promoted into CSS L4 / Sheets / BBNF-self proof, and hot leaves must be described as grammar-neutral primitive families rather than JSON role names (`restart/prompts/ORCHESTRATOR.md:81-88`; `restart/prompts/skinny/PASS-1-PROFILE.md:129-135`). S-P1 convergence still needs two consecutive clean cycles with zero critical defects and no unresolved REVISE (`restart/prompts/ORCHESTRATOR.md:104-121`; `restart/prompts/skinny/PASS-1-PROFILE.md:166-183`).

The current fold does not promote JSON-only evidence into CSS L4 admission. The capture manifest keeps `skinny/RESULTS.md` as row authority, identifies the 458-row pin replay ledger, and explicitly records CSS L4 as unprofiled because the pin root has no generated CSS L4 Track 1 runtime, same-plane lightningcss comparator row, or strict equality oracle row (`restart/skinny/tranches/sk-v12/research/p1/skv12-p1-capture-manifest.md:28-29`, `:69-72`, `:171-177`). The P1 artifacts preserve the same boundary: P1-A rejects root CSS snippets, report fixtures, and lightningcss-only runs as substitutes (`restart/skinny/tranches/sk-v12/research/p1/p1a-samply-mode-1.md:145-158`, `:181-186`); P1-B keeps JSON product rows as guard/diagnostic evidence only (`restart/skinny/tranches/sk-v12/research/p1/p1b-samply-mode-2.md:46-50`, `:187-195`); P1-C records concrete CSS source blockers and routes CSS bring-up to S-P2/S-P3 (`restart/skinny/tranches/sk-v12/research/p1/p1c-samply-mode-3.md:89-110`); P1-D says PMU values move no row and create no CSS row (`restart/skinny/tranches/sk-v12/research/p1/p1d-pmu-cycles.md:67-80`, `:258-261`); P1-E keeps Track 2/oracle families out of generated Track 1 antecedents and marks CSS L4 as the only legitimate absent lane (`restart/skinny/tranches/sk-v12/research/p1/p1e-hot-leaf-attribution.md:32-50`, `:113-160`, `:175-200`); and P1-F extracts only the JSON row surface while stating JSON sonic/serde rows cannot populate the CSS lightningcss close bar (`restart/skinny/tranches/sk-v12/research/p1/p1f-results-delta.md:24-33`, `:80-93`, `:207-210`).

CSS L4 missing-baseline treatment remains a prerequisite, not a substitute. The user pin makes generated CSS L4 the first W1 target, raises the close bar to generated CSS L4 Track 1 beating `lightningcss_mbps + 1`, keeps parse-only diagnostic, and preserves Lock 14 plus the `GrammarConfig` prerequisite (`restart/skinny/tranches/sk-v12/USER-PIN-W1-CSS-L4-SOTA.md:18-35`, `:80-103`, `:141-150`). The handoff repeats that CSS L4 has no admitted row, Sheets/BBNF-self are fallback-only, ADMIT requires generated CSS L4 plus strict equality and lightningcss evidence, and S-P3 must select CSS first (`restart/skinny/tranches/sk-v12/HANDOFF.md:34-39`, `:53-65`, `:74-85`, `:110-128`).

Lock-14 / `GrammarConfig` leaks are bounded and routed. The value API audit names 5 major plus 2 embedded JSON-policy leaks and routes the legal shape through `GrammarConfig`, generated grammar metadata modules, and parameterized view generation (`restart/skinny/tranches/sk-v12/research/skv12-value-api-audit.md:63-107`, `:160-207`). The handoff requires `GrammarConfig` or equivalent metadata before CSS L4 emission can leave JSON-only templates, and it fails closed on grammar-name branches in generic crates (`restart/skinny/tranches/sk-v12/HANDOFF.md:119-123`, `:153-155`).

Generated-size and O(N) routing remains bounded. The handoff requires generated CSS runtime size before redress, including generated LOC, module byte size, regen/check command, and an O(N) grammar-size guard; overflow blocks W1b until traced (`restart/skinny/tranches/sk-v12/HANDOFF.md:121-123`). The CSS L4 gate/report must consume generated LOC, generated module byte size, O(N) grammar-size status, Lock 14 status, Lock 16 status where applicable, same-wave consumer class, JSON guard state, gate status, wave id, and REDRESS id (`restart/skinny/tranches/sk-v12/HANDOFF.md:144-151`).

Sheets and BBNF-self remain fallback-only. The user pin explicitly says they become fallbacks only after a CSS L4 redress attempt fails, not after preflight failure (`restart/skinny/tranches/sk-v12/USER-PIN-W1-CSS-L4-SOTA.md:18-27`). The handoff repeats the current state, priority rule, FIXPOINT condition, S-P3 requirement, and refusal condition (`restart/skinny/tranches/sk-v12/HANDOFF.md:37-38`, `:63-65`, `:87-95`, `:126-127`, `:157-163`). The old SPEC still contains pre-pin W1 fallback/preflight language (`restart/skinny/tranches/sk-v12/SPEC.md:400-408`), but that file declares itself pre-pin context until the pin-aware S-P1 -> S-P2 -> S-P3 sequence rewrites it (`restart/skinny/tranches/sk-v12/SPEC.md:5-14`), and the live handoff says SPEC is pre-pin context only where it does not conflict with the user pin (`restart/skinny/tranches/sk-v12/HANDOFF.md:5-10`, `:22-23`, `:172-177`).

The PIN-V6 clean-cycle record introduces no CH2 blocker for PIN-V7. PIN-V6 returned six ACCEPT, zero REVISE, zero REJECT and is recorded as the first all-ACCEPT cycle after the PIN-V5 reset (`restart/skinny/tranches/sk-v12/research/p1/hardening/PIN-V6/CONSOLIDATED.md:10-20`). Its rechecked evidence says the stale pre-pin root blocker is folded, the pin replay remains 458 rows, all PMU/samply/xctrace status rows pass, hot-leaf tables have resolved anchors, and no CSS L4/lightningcss/Sheets/BBNF-self artifacts exist in the pin root (`restart/skinny/tranches/sk-v12/research/p1/hardening/PIN-V6/CONSOLIDATED.md:22-38`). This CH2 ACCEPT can serve as the CH2 component of the second clean post-reset cycle; full S-P1 convergence still depends on the other PIN-V7 lenses and consolidation also returning clean ACCEPT.

## Nonblocking Notes

- ACCEPT here means the S-P1 profile packet remains grammar-safe and correctly fences CSS L4 absence. It does not mean CSS L4 is admitted.
- The file name `HARDENING-S-P1-CONVERGED.md` remains historically awkward, but its contents now say pre-pin convergence is superseded, pin convergence is in progress, and advancement requires two consecutive all-ACCEPT pin cycles (`restart/skinny/tranches/sk-v12/research/p1/hardening/HARDENING-S-P1-CONVERGED.md:1-6`, `:13-25`, `:78-82`).
- Commands run:

```bash
git status --short --branch && git rev-parse HEAD
# ## master...origin/master [ahead 2388]
# d4a7e3e3f19482688fa42dd1be9cf584f6c3d19b

awk -F '\t' 'NR>1{total++; if(tolower($2 FS $3 FS $4 FS $5) ~ /(css|lightningcss|sheets|bbnf)/) hits++; if($3=="parse") parse++; if($3=="direct") direct++; if($3=="typed") typed++} END{print "total=" total " semantic_nonjson_hits=" hits+0 " parse=" parse+0 " direct=" direct+0 " typed=" typed+0}' restart/skinny/tranches/sk-v12/research/p1/skv12-p1-pin-replay.tsv
# total=458 semantic_nonjson_hits=0 parse=170 direct=204 typed=84

awk -F '\t' 'NR>1 && ($5 !~ /^(track1|track2|real_typed_track1|real_typed_track2)$/ || $4=="update-center" || $3=="css" || $3=="sheets" || $3=="bbnf-self") {print NR ":" $0; bad++} END{print "bad",bad+0}' restart/skinny/tranches/sk-v12/research/p1/skv12-p1-pin-replay.tsv
# bad 0

rg -n "lightningcss|css_l4|CSS L4" skinny/RESULTS.md restart/skinny/tranches/sk-v12/research/p1/skv12-p1-pin-replay.tsv
# no output; exit 1

find skinny/crates/runtime/src/grammars -maxdepth 2 -type d | sort
# skinny/crates/runtime/src/grammars
# skinny/crates/runtime/src/grammars/json
# skinny/crates/runtime/src/grammars/sheets_witness

find /tmp/skv12-pin-p1 -maxdepth 3 \( -iname '*css*' -o -iname '*lightning*' -o -iname '*sheet*' -o -iname '*bbnf*' \) -print | sort
# no output

rg -n '(/tmp/skv12-p1/|/tmp/skv12-profile-target-50bd1648|skv12-p1-replay\.tsv)' restart/skinny/tranches/sk-v12/research/p1/hardening/HARDENING-S-P1-CONVERGED.md restart/skinny/tranches/sk-v12/SPEC.md restart/skinny/tranches/sk-v12/research/p1/p1*.md restart/skinny/tranches/sk-v12/research/p1/skv12-p1-capture-manifest.md
# no output; exit 1

awk -F '\t' 'NR>1{total++; if($7!="PASS") bad++} END{print "pmu_total=" total " bad=" bad+0}' /tmp/skv12-pin-p1/pmu/capture_status.tsv
# pmu_total=82 bad=0
awk -F '\t' 'NR>1{total++; if($7!="PASS") bad++} END{print "samply_total=" total " bad=" bad+0}' /tmp/skv12-pin-p1/samply/capture_status.tsv
# samply_total=82 bad=0
awk -F '\t' 'NR>1{total++; if($7!="PASS") bad++} END{print "xctrace_total=" total " bad=" bad+0}' /tmp/skv12-pin-p1/xctrace/capture_status.tsv
# xctrace_total=212 bad=0

awk -F '\t' 'NR>1{n++; if($16 ~ /:0([^0-9]|$)/ || $16 ~ /unknown/ || $15=="none") bad++} END{print "summary=" n " bad=" bad+0}' /tmp/skv12-pin-p1/time_profile_hot_leaf_summary.tsv
# summary=82 bad=0
awk -F '\t' 'NR>1{n++; if($9 ~ /:0([^0-9]|$)/ || $9 ~ /unknown/ || $8=="none") bad++} END{print "details=" n " bad=" bad+0}' /tmp/skv12-pin-p1/time_profile_hot_leaf_details.tsv
# details=410 bad=0

git diff --name-status HEAD -- skinny/RESULTS.md skinny/REDRESS.md restart/skinny/tranches/sk-v12/SPEC.md restart/skinny/tranches/sk-v12/HANDOFF.md restart/skinny/tranches/sk-v12/research/p1
# no output
```

## Exact Fold Edits If REVISE

Not applicable. Verdict is ACCEPT; no CH2 fold edits are required.
