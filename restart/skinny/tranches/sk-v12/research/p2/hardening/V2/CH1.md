# SK-V12 S-P2 CHALLENGE V2 — CH1 Correctness

Disposition: ACCEPT.

Lens: CH1 CORRECTNESS.

Scope: verify that every current S-P2 candidate primitive traces to a named
S-P1 hot leaf, that comparator claims use the correct strictness plane and
primary sources, that ISA claims cite architecture references, and that the
V1 CH1 revise items were folded.

## Basis

- PASS-2 binds CH1 to candidate antecedents, strictness-plane comparator
  sourcing, and ISA manual sourcing (`restart/prompts/skinny/PASS-2-RESEARCH.md:95`-`:100`).
- ORCHESTRATOR §3W binds CH1 to resolving file:line / commit / RESULTS /
  REDRESS evidence and measurable gates (`restart/prompts/ORCHESTRATOR.md:81`-`:84`).
- S-P1 convergence names the accepted hot-family set and keeps JSON-only
  telemetry as nomination evidence only (`restart/skinny/tranches/sk-v12/research/p1/hardening/HARDENING-S-P1-CONVERGED.md:47`-`:63`).
- RESULTS and REDRESS keep the SK-V12 surface at `N-direct / NoGo`, retire
  W3 union-substrate routes, and route SK-V12 to generated non-JSON baseline
  work before JSON residual retries (`skinny/RESULTS.md:143`;
  `skinny/REDRESS.md:2910`-`:2940`; `skinny/REDRESS.md:3529`-`:3553`).
- Locks 1, 14, and 16 remain the correctness boundary for substrate shape,
  grammar generality, and SIMD/ASM admissibility (`restart/locks/LOCKS.md:52`,
  `:78`, `:87`-`:112`).

## Findings

1. P2-A's V1 local-citation and scalar-sketch defects are closed. The stale
   S-P1 capture/replay paths now resolve under `research/p1/`
   (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:179`-`:180`),
   the out-of-range orchestrator references were replaced with live lines
   (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:169`-`:170`),
   and the SK-V11 historical P2 path includes `research/p2/hardening/`
   (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:184`).
   C1-C7 now carry scalar-reference status, parity expectation, and same-wave
   consumer accounting in one table, followed by scalar sketches for every
   candidate (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:29`-`:49`).

2. P2-A's comparator correctness remains sound. The artifact separates asmjson
   from the strict anchor lane because of its published conformance caveats,
   keeps sonic-rs unchecked/lossy APIs out of strict comparison, treats yyjson
   `YYJSON_READ_NOFLAG` as the strict lane, and uses simdjson's retained index
   only as comparator architecture evidence rather than a bbnf substrate import
   (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:14`-`:21`).
   The comparator source list is primary and commit-pinned for asmjson,
   sonic-rs, simdjson, and yyjson (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:186`-`:229`).
   Each P2-A candidate names accepted S-P1 antecedents before any bbnf delta
   or admission boundary (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:55`,
   `:67`, `:79`, `:91`, `:103`, `:115`, `:127`).

3. P2-B remains CH1-correct. Its process gates are not speculative kernels:
   every concrete gate in the table names a P1 antecedent, scalar status,
   strict parity/checkasm expectation, and same-wave consumer rule
   (`restart/skinny/tranches/sk-v12/research/p2/p2b-dav1d-process.md:36`-`:51`).
   The external process citations are primary VideoLAN, FFmpeg, and dav1d
   sources (`restart/skinny/tranches/sk-v12/research/p2/p2b-dav1d-process.md:83`-`:87`).

4. P2-C's V1 LD4 and SHA3 defects are closed. The artifact now states
   `Current candidate count: 6` and demotes LD4 interleaved classification
   plus SHA3 ternary boolean fold to ISA inventory because S-P1 does not name
   an existing interleaved byte stream or concrete three-input boolean fold
   source line (`restart/skinny/tranches/sk-v12/research/p2/p2c-arch-esoterica.md:38`-`:42`).
   I1 and I2 explicitly carry no sufficient P1 antecedent and are
   non-selectable until fresh profile evidence and scalar/checkasm/consumer
   proof exist (`restart/skinny/tranches/sk-v12/research/p2/p2c-arch-esoterica.md:55`-`:65`,
   `:122`-`:133`). The six remaining current candidates each trace to the
   accepted P1 hot families and have local source or scalar anchors
   (`restart/skinny/tranches/sk-v12/research/p2/p2c-arch-esoterica.md:44`-`:53`,
   `:67`-`:76`, `:78`-`:87`, `:89`-`:98`, `:100`-`:109`, `:111`-`:120`).
   The Arm ISA claims cite ACLE / Arm instruction references for TBL/TBX,
   DotProd, CSSC, PMULL, LD4, SHA3 EOR3/BCAX, EXT, shifts, reductions, and
   bit count (`restart/skinny/tranches/sk-v12/research/p2/p2c-arch-esoterica.md:168`-`:178`).

5. P2-D's V1 diagnostic-overcount defect is closed. It now declares zero
   selectable candidates from SK-V12 S-P1, three same-tape diagnostics, and one
   rejected parallel-substrate route (`restart/skinny/tranches/sk-v12/research/p2/p2d-substrate-tape.md:67`-`:72`).
   `offset_tape_capacity_policy`, `sparse_flag_lookup_policy`, and
   `retained_cursor_skip_projection` are each marked diagnostic/ineligible
   under current S-P1 with an explicit missing movement locus, while
   `structural_class_lane_union` is rejected with REDRESS 96/97/98 authority
   (`restart/skinny/tranches/sk-v12/research/p2/p2d-substrate-tape.md:74`-`:83`).
   This satisfies CH1 because no candidate primitive remains without a P1
   antecedent; the non-antecedent substrate items are not selectable candidates.

6. P2-E remains CH1-correct. It limits parse-that work to five candidates
   whose antecedents are accepted hot families and keeps the other five accepted
   leaves as constraints, not parse-that primitives
   (`restart/skinny/tranches/sk-v12/research/p2/p2e-parse-that-gaps.md:14`-`:32`).
   Each candidate includes a scalar reference sketch, accepted P1 antecedent,
   checkasm expectation where applicable, and same-wave consumer note
   (`restart/skinny/tranches/sk-v12/research/p2/p2e-parse-that-gaps.md:50`-`:93`,
   `:95`-`:151`, `:153`-`:220`, `:222`-`:282`, `:284`-`:358`).

7. P2-F remains CH1-correct as a grammar-neutral abstraction map. It introduces
   no new external comparator or ISA claim (`restart/skinny/tranches/sk-v12/research/p2/p2f-grammar-neutral.md:21`).
   Its table distinguishes six conditional parser/support families from one
   oracle-only and one accounting-only family, with P1 antecedents and
   eligibility stated per family (`restart/skinny/tranches/sk-v12/research/p2/p2f-grammar-neutral.md:25`-`:40`).
   F7 and F8 therefore cannot be misread as parser candidates under CH1.

## Revise List

None.

## Disposition

ACCEPT. The V1 CH1 revise items are folded, the current selectable candidate
pool traces to accepted S-P1 hot leaves, speculative LD4/SHA3 and same-tape
substrate items are correctly demoted out of candidate eligibility, comparator
strictness planes are sourced to primary comparator material, and Arm ISA claims
are cited against architecture references.
