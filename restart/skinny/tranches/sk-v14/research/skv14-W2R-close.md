# SK-V14 W2R Close

Date: 2026-05-26.

Disposition: **ADMITTED** for amended W2. W2 is skinny-side CSS L4 regen only;
`crates/core/src/runtime/css_l4/` remains excluded and is still W6.0 work.

## Scope

- Implementation commit: `45568e669` (`feat(sk-v14-w2): add skinny regen-css gate`).
- Gate command added: `cargo xtask regen-css`.
- Companion checks added:
  - `cargo xtask check-css-l4-at-rules-and-media`
  - `cargo xtask check-css-l4-declaration-values`
  - `cargo xtask check-css-l4-declaration-values-extended`
  - `cargo xtask check-css-l4-nested-layout`
  - `cargo xtask check-css-l4-stylesheet-selectors`
  - `cargo xtask check-css-l4-vendor-and-custom-atrules`
  - `cargo xtask check-css-l4-visual-functions`
- Source inputs: all 15 `grammar/css/l4/*.bbnf` files.
- Metadata inputs: root `Cargo.toml` and `skinny/Cargo.toml`.
- Output tree: `skinny/crates/runtime/src/grammars/css_l4_*`.

## Evidence

- `cargo xtask regen-css` PASS.
- Seven `cargo xtask check-css-l4-*` companions PASS.
- `cargo test -p xtask --bin xtask css_l4 -- --nocapture` PASS: 2 tests.
- `cargo test -p codegen css_l4_ -- --nocapture` PASS: 14 tests.
- `cargo test -p runtime css_l4 -- --nocapture` PASS: 7 tests.
- `cargo xtask gate-json --check-results --skv14-existing-results-capture` PASS.
- Destructive round-trip PASS after commit:
  `rm -rf skinny/crates/runtime/src/grammars/css_l4_* && (cd skinny && cargo xtask regen-css) && git diff --exit-code -- skinny/crates/runtime/src/grammars`.
- Root runtime exclusion PASS:
  `git diff --name-only -- crates/core/src/runtime/css_l4` produced no rows, and
  `git grep -n '@generated by skinny bbnf-codegen' -- crates/core/src/runtime/css_l4 || true`
  produced no rows.
- Invariants held at W2 close:
  - `grep -cE "^[0-9]+\. \*\*" restart/locks/LOCKS.md` -> `16`
  - `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l` -> `67`

## Remainder

W3 is unblocked. W4 remains gated by W3 admission. W8, W9, and W10 remain
blocked until PRUNE-1 through PRUNE-5 close per the SK-V14 prune-then-rebuild
chain.
