---
pass: T-P3-synthesis (SK-V17 totality fold of the SKINNY-proven unified-tape / lazy-view / NEON model)
artefact: HARDENING-CONSOLIDATED
cycle: V3
aggregator: T-P3-CHALLENGE-aggregator
generated_at: 2026-05-29T00:00:00Z
master_head: 2a76916ac1959ef027df4d28e09be2b0b0bbec7f
t_p1_locked_sha: 91b6893b0
t_p2_locked_sha: 2a76916ac
contract: restart/prompts/totality/PASS-3-SYNTHESIS.md §2 (3A-3F) + §2.1 frontmatter + §3 (CH1-CH6) + CH7 + §8 (§8.1-§8.6); ORCHESTRATOR §3W/§3Z
artefacts_reviewed: [3a-architecture-synthesis, 3b-master-plan-reconciliation, 3c-locks-crystallisation, 3c-locks-v+1-diff, 3d-skinny-fold, 3e-grammar-generalisation, 3f-migration-handoff]
lenses_run: [CH1, CH2, CH3, CH4, CH5, CH6, CH7]
cycle_accept_rate_pct: 98.5
cycle_history_accept_pct: [V1=87.4, V2=96.2, V3=98.5]
consecutive_ge95_cycles: 2          # V2 (96.2%) + V3 (98.5%)
orphan_revise: 0
v_ceiling: 5
v_current: 3
verdict: CONVERGED
g_omega_gate_object: restart/audit/totality/sk-v17/p3/3c-locks-v+1-diff.md
ready_for: Pass-Omega (G-Omega user gate)
g3_gate: ready (auto-passes on cohort lock under the active G-Omega-only pin; §3Z criteria met)
---

# HARDENING-T-P3-SKV17-V3-CONSOLIDATED — Totality Fold Synthesis, Converged

This is the V3 CHALLENGE consolidation for T-P3, the **totality-track synthesis
pass**. T-P3 crystallises the LOCKED T-P2 tape-fold designs into **PROPOSED V1
spec deltas** — READ-ONLY against the V1 surfaces (`ARCHITECTURE.md`,
`MASTER-PLAN.md`, `LOCKS.md`, `MIGRATION.md`, `HANDOFF.md`): T-P3 PROPOSES the
diffs; Pass Omega's CRUD wave applies them post-G-Omega (§8.6). This is the
SK-V17 totality fold of the SKINNY-proven **unified-tape / lazy-`ValueRef<G>` /
shared-NEON** model. Six parallel sub-agents (3A architecture, 3B master-plan
SK-V18 adoption waves, 3C LOCKS v+1 diff, 3D skinny-fold, 3E
grammar-generalisation, 3F migration/handoff) fan out per the §2 scope matrix;
the seven-lens CHALLENGE wave (CH1-CH6 + the monotonic CH7 OVERFIT-PRUNE)
adversarially reviews them. Every proposed delta is grammar-neutral (Lock 14),
substrate-union-preserving (Lock 1), 5-shape-canon-coherent (Lock 10),
scalar-ref + checkasm (Lock 16, aarch64-only NEON), file:line/SHA grounded,
master HEAD `2a76916ac`.

---

## §3Z — Convergence verdict

**CONVERGED. Ready for Pass Omega (G-Omega user gate).**

| cycle | ACCEPT | REVISE | REJECT | total | accept-% | character |
|---|---|---|---|---|---|---|
| V1 | 111 | 16 | 0 | 127 | **87.4%** | folding cycle (≥30% REVISE expected; every REVISE one-line/wording-class, zero load-bearing REJECT) |
| V2 | 126 | 5 | 0 | 131 | **96.2%** | V1-disposition fold + re-verify cycle (first ≥95% cycle) |
| V3 | 131 | 2 | 0 | 133 | **98.5%** | convergence cycle (all V2 REVISEs folded; two forward-path tightenings remain, both non-blocking) |

Per-lens V3 (all seven; CH7 is the monotonic-extension OVERFIT-PRUNE lens):

| lens | V1 | V2 | V3 | V3 verdict |
|---|---|---|---|---|
| CH1 CORRECTNESS | 18A/1R | 19A/0R | **21A/0R/0X** | gate object `git apply --check` EXIT 0; every delta cites a real T-P1 divergence / T-P2 LAC resolving at file:line; 14-candidate matrix complete, zero silent drop |
| CH2 GENERALITY | 8A/3R | 11A/0R | **11A/0R/0X** | Lock 14 holds across 3A/3B/3E; two-axis model (classifier by-exercise 8 grammars + value-plane by-construction JSON+CSS); 3C narrows no lock to JSON/CSS |
| CH3 REGRESSION | 16A/1R | 17A/0R | **17A/0R/0X** | zero re-opened REDRESS pre-block; AZ-IV-eager / StructRegistry-indirection / fact-stream-String / x86 fences hold; 3D promotes no rejected route |
| CH4 COST | 31A/3R | 33A/1R | **33A/1R/0X** | every delta carries LOC + propagation + risk + wave; 3B SK-V18 waves W0-W6 each carry same-wave consumer; one cost-precision REVISE (D07 scaffold-body band) |
| CH5 HIDDEN-COUPLING | 21A/4R | 26A/2R | **28A/0R/0X** | substrate-union ONE substrate; tape = substrate-manifest CATEGORY not silent 6th shape; StructRegistry/FieldSource fence intact; no Track-1≡Track-2 dishonesty |
| CH6 ANTI-PAPER-CLOSE | 6A/3R | 8A/1R | **8A/1R/0X** | 14/14 dispositioned, no bare "validated", every ORQ/DEFER triple-named; one forward-path silent-satisfy tightening (3E defer-word + 3C OQ clause) |
| CH7 OVERFIT-PRUNE | 11A/1R | 12A/1R | **13A/0R/0X** | 16-lock count + 5-shape canon verbatim; fold genuinely general; no fabricated speed; lightningcss the fair (UNMEASURED-PENDING) CSS bar; JSON 8349>4913 re-verified |

**§3Z criteria, each satisfied:** (1) ≥95% ACCEPT for **two consecutive cycles**
— V2 96.2%, V3 98.5%; (2) **zero orphan REVISE** — both V2 REVISEs (CH4-V2-01
22→40-file fold; CH5-V2-R01/R02 distribution-invariant + leak-census receiver)
are folded and re-verified at V3; no REVISE drew across three cycles; (3) **V ≤ 5**
— converged at V3; (4) zero open critical defects, zero REJECT in any cycle. The
two V3 residual REVISEs (CH4-V3-01, CH6-V3-7) are cost-precision /
silent-satisfy-aperture tightenings on a forward path — neither is a substantive
defect, both carry a concrete mechanical fix, neither blocks G3 (residual section
below).

---

## §3Z — The G-Omega gate object (`3c-locks-v+1-diff.md`)

The load-bearing artefact of this pass is the **line-level LOCKS v+1 diff**,
`restart/audit/totality/sk-v17/p3/3c-locks-v+1-diff.md` — the **G-Omega gate
object** (§8.6, PASS-OMEGA §6). It is an **SK-V17 T-P3 Crystallisation Addendum**
appended before the in-force `## v+1 Governance Boundary`
(`restart/locks/LOCKS.md:610`), immediately after the inherited SK-V15 addendum
(`:581`-`608`).

- **Applies clean.** Hunk header `@@ -606,7 +606,22 @@` (7 old-side context, 22
  new-side = 7 context + 15 added, no removed lines); `git apply --check` returns
  **EXIT 0** against the live `restart/locks/LOCKS.md` at master HEAD `2a76916ac`,
  re-verified independently by CH1, CH3, CH4, CH5, CH6, CH7 this cycle. The V1
  mis-counted `@@ -606,6 +606,52 @@` (EXIT 128 corrupt-patch) is folded out per
  CH1-V1.
- **16-lock count preserved verbatim** — addendum adds no numbered lock, retires
  none, renumbers none; all 16 lock headings resolve at
  `LOCKS.md:75,160,170,179,181,183,200,202,260,269,319,328,336,349,436,453` (§8.1).
- **5-shape canon verbatim** — `{EagerTape, OffsetTape, EventTape, SinkOnly,
  CollapsedStage}` (`LOCKS.md:107`-`108`); no sixth variant; a sixth stays
  G-Omega gated (`LOCKS.md:109`), SK-V17 §9-barred (`SPEC.md:808`) (§8.2).
- **No new directive / BIR variant / substrate / public substrate API / retained
  sidecar** — the tape is recorded as a substrate-manifest CATEGORY
  (`substrate_target = existing_tape`); the NEON classifier is
  `retention_lifetime = transient-single-call`; OnceCell carriers declare
  `existing_tape` / `local_temp_only` (§8.5).
- **Distribution invariant rides the gate object** (CH5-V2-R01 fold, `:103`): if
  Pass Omega distributes the addendum into Lock bodies, the Lock-10 tape-category
  clause MUST retain its inline cross-ref to the Lock-1 substrate manifest, else
  it re-opens the silent-6th-shape reading. The apply-time fence travels with the
  artefact Pass Omega CRUD applies.

---

## §3Z — The LOCKED proposed V1 deltas (the crystallised SK-V18 fold)

The synthesis distils the 5 LOCKED T-P2 LACs (`LAC-2F-FOLD-01..05`) + their 6
T-P1 antecedents (`LAC-1E-SKV17-01..06`) + 3 ORQs (`2F-FOLD-U1/U2/U3`) into
PROPOSED V1 deltas across the six surfaces. All carried clean V2→V3.

### 3A — ARCHITECTURE deltas (8: `ARCH-3A-S17-D01..D08`)

The architecture surface deltas crystallise the four T-P1 divergences into
`ARCHITECTURE.md` proposed-diff text:

- **D01 tape-as-unified-substrate** — retire the live eager `OpenFrame` builders
  (`css_l4/builder.rs:16` 817 LOC, `json/builder.rs:9`); converge AoS `TapeRec`
  (`tape/record.rs:103`) onto the PROVEN SoA `Tape<'input>`
  (`skinny/.../tape/mod.rs:94`) as the SINGLE post-fold encoding (dual AoS/SoA
  admissible ONLY as a transient fold-state).
- **D02 lazy `ValueRef<G>` value-plane** — the one grammar-parametric
  materialization plane (`skinny/.../tape/mod.rs:175`), `@generated`-allowed,
  preserve-rich-ast; scope-honest JSON+CSS-exercised, not falsely fleet-wide.
- **D03 shared NEON classifier manifest** — `retention_lifetime =
  transient-single-call`, `substrate_target = existing_tape`, same-wave consumer
  = the tape.
- **D04 BackendShape-category disposition** — tape = Lock-1 substrate-manifest
  CATEGORY per LAC-1E-14, NOT a 6th shape; two independent grounds (categorical
  precedent + `admits_collapsed_stage` x86-binding mechanically refusing on
  aarch64).
- **D05 StructRegistry/FieldSource fence** — per-leaf runtime `StructRegistry::layout(rule)`
  is REJECT (28-65×/983×/10583×); the SOLE live coupling `bbnf/arena.rs:47`
  severed by D01.
- **D06/D07/D08** — `StructLayout` reconcile (two priced paths), BackendShape
  selector wiring atop the existing `crates/egraph`+`crates/csp-solver` engine,
  and the 3-ORQ pre-gate conversion.

### 3B — MASTER-PLAN SK-V18 adoption waves (9 deltas + the W0-W6 receiver block)

The SK-V18 Fold Receiver Block proposes seven MASTER receiver rows realising the
five LOCKED LACs as F1-F9, each with a **same-wave consumer (no orphan
pre-gate)**:

- **W0** OnceCell `substrate_target` classification pre-gate (co-waved W1 IS the
  consumer; classification GATES the wiring; all 8 carriers);
- **W1** eager-`OpenFrame` retire → flat-tape commit (blast radius = 40 files via
  `grep -rl 'JsonStructBuilder\|CssStructBuilder' crates/` = 40);
- **W2** AoS `TapeRec` → SoA `Tape` exactly-one-encoding closure;
- **W3** lazy `ValueRef<G>` projection generator (JSON `value_from_ref`
  byte-equal re-emission gate; the leak-census receiver);
- **W4** StructRegistry/FieldSource compile-time projection fence (W3 generator
  resolves layout ONCE at codegen);
- **W5** shared NEON classifier Lock-16 manifest row + BackendShape selector
  wiring (the 5 real lowerers / SK-V15 W8/W9 all-five gate);
- **W6** Lock-2 `StructLayout` canonical-name reconcile (rename-vs-side-table
  routed to 3C/Pass Omega).

The 5-shape canon STAYS UNCHANGED across §13/§13.5/§13.1; the tape is a
substrate CATEGORY, not a 6th shape; no refuted wave revived.

### 3C — LOCKS v+1 diff with disposition matrix + 16-lock-preserved

The LOCKS SINGULARITY: every T-P1 1E + T-P2 2X amendment candidate converges into
**ONE v+1 diff** — five lock-addendum clauses on **Locks 1, 2, 10, 14, 16**:

| clause | LAC sources | fold |
|---|---|---|
| **D-SKV17-L01** tape-substrate-union | LAC-2F-FOLD-01/04, LAC-1E-SKV17-01/02/03, U1, U2 | eager-OpenFrame retire + AoS→SoA single-encoding + all-8 OnceCell `substrate_target` pre-gate + per-leaf-registry REJECT fence + `arena.rs:47` severed |
| **D-SKV17-L02** StructLayout-reconcile | LAC-2F-FOLD-05, LAC-1E-SKV17-04 | two priced paths (960-site rename vs `LayoutFacts.backend_shape` side-table, grep-zero in `crates/`); bars Lock-2 closure by `LayoutFacts` alone |
| **D-SKV17-L10** tape-category-not-sixth-shape | LAC-2F-FOLD-02, LAC-1E-SKV17-05, U3 | tape = substrate-manifest CATEGORY per LAC-1E-14 + `admits_collapsed_stage` aarch64-refusal; 5-shape verbatim; aarch64 CollapsedStage = UNKNOWN-2D-05 |
| **D-SKV17-L14** ValueRef/classifier-generalisation | LAC-2F-FOLD-03 (value half), LAC-1E-SKV17-03/04 | one grammar-agnostic `ValueRef<G>` generator; scope-honest JSON+CSS-only value-fold; classifier config-breadth a SEPARATE axis from value-plane proof; preserve-rich-ast |
| **D-SKV17-L16** NEON-classifier-manifest | LAC-2F-FOLD-03 (manifest half), LAC-1E-SKV17-06 | `select_classifier(alphabet)` Lock-16 manifest row; eq-set fan the one proven NEON body; table/prefix honest `scalar-delegate-non-ASM`; `crates/simd-scan` multi-arch reconcile WITHOUT x86 close path |

**Disposition matrix (14 candidates, zero silent drop):**

| disposition | count | candidates |
|---|---:|---|
| ACCEPT | 9 | LAC-2F-FOLD-01/02/03/04, LAC-1E-SKV17-01/02/03/05/06 |
| ACCEPT (ORQ crystallised) | 3 | 2F-FOLD-U1, 2F-FOLD-U2, 2F-FOLD-U3 |
| MODIFY | 2 | LAC-2F-FOLD-05, LAC-1E-SKV17-04 (the Lock-2 reconcile pair: record both priced routes, do not choose inside the lock) |
| REJECT | 0 | — |
| DEFER | 0 | — |

The 3 ORQs are ACCEPTed as crystallised disposition (NOT engineered-defers): U1
SoA-adopt → W2 substrate-union gate; U2 OnceCell classification → W2 §9-cond-1
pre-gate; U3 aarch64 CollapsedStage → the EXISTING 5-shape gate + G-Omega
6th-shape amendment (the 2E-source wave is the blocker precondition for a future
ADD, NOT a phantom receiver). The 0-REJECT/0-DEFER tally is LOCKED-input
provenance (the five §3Z refutation rows survive as REJECT-class clause text:
6th-shape / per-leaf-registry / AoS-SoA-dual / fleet-wide-value / x86-SVE), not
absent hostile content.

### 3D — skinny-fold (monotonic)

SKINNY-proven wins become V1-authoritative; rejections become locks-strengthening
evidence; the totality spec never dictates back to a live skinny iteration:
W1/W2/W3 wins (SoA `Tape` / `ValueRef<G>` / `select_classifier`) → V1-authoritative;
R1 eager-tree (118×) / R2 registry (28-65×/983×/10583×) / R3 fact-stream-String
→ locks-strengthening; the Sheets/BBNF-self generality gap routed to 3E by-construction.
The SCOPE-HONESTY BANNER marks every Sheets/BBNF AND CSS->SOTA claim
predicted/SK-V18-pending; the monotonic-direction clause (D08) forbids relocating
core into skinny.

### 3E — grammar-generalisation (Lock 14)

The tape/`ValueRef`/NEON generality across JSON/CSS/Sheets/BBNF-self: a two-axis
model — classifier config-breadth (alphabet-as-data, by-EXERCISE across 8 of 9
grammars, math=0) + value-plane proof (by-CONSTRUCTION JSON+CSS only). The
per-grammar BackendShape matrix tags every non-witnessed cell `predicted
(cost-model-pending)`; the future-grammar onboarding test carries P5a/P5b
(classifier-scan-measured vs tape-consumer-SK-V18-pending) + the P6 value-axis
firewall; the P1 leak-census (7 `strategy.rs` string-ident sites, NOT runtime
`match grammar {}`) is routed to MP.SK18.W3 with a fail-closed admitted Lock-14
baseline + re-entry trigger.

### 3F — MIGRATION / HANDOFF + next-cycle directive (8 deltas `3F17-MH-01..08`)

Migration receiver carries the eager retirement, AZ-IV registry fence,
fact-stream-String demoted to diagnostic-only, the single-encoding closure gate
(`grep` proves AoS XOR SoA retired), the no-second-substrate fence, and the
x86/cross-call-classifier-state must-NOT column. The next-cycle dispatch directive
is mechanical and measurable: T-P3 cohort lock → G3 (auto-passes under the active
G-Omega-only pin) → Pass Omega CHALLENGE + CRUD wave → G-Omega (LOCKS v+1 merge
applies the 3C matrix) → SK-V18 W0 only after CRUD-4 truth complete + G-Omega
authorised + DISPATCH-PROMPT routing. CRUD-4 cap handling records
blocked/extension remainder, never silent defer.

---

## §3Z — Residual REVISE

**ZERO orphan REVISE at V3.** Both V2 REVISEs folded and re-verified; the two V3
residuals are forward-path tightenings, neither blocking G3, each with a named
3X-author receiver:

- **CH4-V3-01 (cost precision, REVISE):** D07's 600-1400 LOC "wiring" envelope is
  honestly scoped to the wiring, but the LOCKED T-P2 source now sizes the lowerer
  set (`sink_only.rs` = 270 LOC the one real lowerer; four 17-LOC scaffolds), so
  the open-ended "the envelope grows" OQ can be tightened into a bounded named
  scaffold→body band (~4×270 ≈ 800-1100 LOC) in the D07 cost cell. **Fix → 3B/3A
  author:** add the third named line-item to the D07 envelope; one cost-row
  addition, no existing LOC band / risk class / wave allocation disturbed. The
  D07 *wiring* framing itself is honest; this is a precision residual, not a
  framing dishonesty.

- **CH6-V3-7 (silent-satisfy aperture, REVISE):** two forward-path surfaces —
  (a) the 3E math/EBNF matrix cell carries the full receiver/blocker/gate triple
  BUT leads with the bare word `DEFERRED`, which on a G3 skim reads as a soft
  defer before the qualifying triple; (b) the 3C CH6 Open Question asks whether a
  candidate can be "already satisfied by current `LOCKS.md` text" — if Pass Omega
  answers yes, a dispositioned candidate could become a silent no-op. **Fix → 3E
  author:** re-order the cell so the triple LEADS (`DEFERRED-WITH-TRIPLE`). **→ 3C
  author:** add the anti-silent-satisfy clause — "answering 'yes' is itself a
  logged ACCEPT-satisfied-by-prior-text disposition in the Pass Omega CRUD-3 LOG,
  never a silent satisfy." Both bodies already carry the substantive
  receiver/blocker/gate; this is a gate-object forward-path hardening, not a
  paper-close hole.

No REVISE drew across three consecutive cycles; no author paper-folded; the
orchestrator escalation trigger (same REVISE × 3, V4 ceiling, V5 BLOCKED) did not
fire. Both V3 residuals are wording/cost-cell precision the Pass Omega CRUD wave
can absorb at apply-time; neither perturbs the gate object's clean apply or any
disposition.

---

## §3Z — Refuted proposals (the REJECT-class fences, carried intact)

T-P3 refuses these by construction; each survives V1→V3 as a REJECT-class clause,
not a deferral:

1. **A 6th `BackendShape` variant — REFUTED** on two grounds (LAC-1E-14
   FactStream category precedent + `admits_collapsed_stage` x86-binding
   mechanically refusing on aarch64); a sixth stays G-Omega-gated, §9-barred.
2. **Per-leaf runtime `StructRegistry::layout(rule)` walk — REFUTED** (re-opens
   28-65×/983×/10583×); the walk MUST be compile-time resolved once at codegen.
3. **AoS/SoA dual end-state — REFUTED** as a Lock-1 closure; exactly ONE encoding
   survives; coexistence admissible ONLY as a transient fold-state.
4. **Fleet-wide value-plane proof — REFUTED** (JSON+CSS by-exercise only;
   Sheets/BBNF-self by-construction); the refutation does NOT narrow Lock 14.
5. **x86 / AVX-512 / SVE close route — REFUTED**; aarch64 primary, no SVE; the
   `crates/simd-scan` multi-arch retention binds WITHOUT admitting x86 as a close
   path. The `udot`/i8mm orphan kernel sits in the deferred appendix, REFUTED.
6. **CSS >SOTA "proven" — REFUTED/folded** (CH7-V2-R1): CSS >SOTA is the SK-V18
   proof obligation, UNMEASURED-PENDING; lightningcss held as the fair unmet bar.
   The >SOTA carrier re-grounds to the measured JSON fact (twitter parse_only
   8349.290 > sonic-rs strict 4913.095, +69.9%, aarch64 M5 Max, `RESULTS.md`).

---

## §3Z — Next move: ready-for-Pass-Omega (G-Omega user gate)

The fold synthesis is **sound and stable** against the live V1 surfaces + source
at master `2a76916ac`, each proposed delta grammar-neutral (Lock 14) /
substrate-union-preserving (Lock 1) / 5-shape-canon-coherent (Lock 10) /
scalar-ref + checkasm (Lock 16, aarch64-only):

- **3A** ARCHITECTURE deltas D01-D08 — tape-as-unified-substrate + `ValueRef<G>`
  value-plane + NEON manifest + BackendShape-category disposition;
- **3B** MASTER-PLAN SK-V18 adoption waves W0-W6 — each with a same-wave
  consumer, 5-shape canon unchanged;
- **3C** the LOCKS SINGULARITY — ONE v+1 diff, 14-candidate disposition matrix
  (9 ACCEPT + 3 ORQ-ACCEPT + 2 MODIFY + 0 REJECT + 0 DEFER), 16-lock count + 5
  shapes preserved verbatim, Lock 1/2/10/14/16 amendments;
- **3D** the monotonic skinny-fold — SKINNY wins V1-authoritative, never dictates
  back;
- **3E** grammar-generalisation — two-axis Lock-14 generality, concrete non-JSON
  story for CSS L4 / Sheets / BBNF-self, future-grammar onboarding test;
- **3F** MIGRATION/HANDOFF deltas + the mechanical next-cycle dispatch directive.

**The G-Omega gate object** `restart/audit/totality/sk-v17/p3/3c-locks-v+1-diff.md`
applies clean (`git apply --check` EXIT 0) and is the artefact the user reads at
the gate.

Per PASS-3-SYNTHESIS §6, T-P3 convergence reaches **G3 (mandatory)** — which,
under the active G-Omega-only user pin, **auto-passes on cohort lock**; the
synthesis then flows into **Pass Omega**, whose CHALLENGE + CRUD wave executes the
amendments as document operations on the V1 spec corpus, the LOCKS v+1 merge
itself **G-Omega-gated** (PASS-OMEGA §6). T-P3 produces the proposed diffs; Pass
Omega CRUD applies them, post-G-Omega. T-P3 does not amend a V1 spec surface
itself.

**VERDICT: T-P3 CONVERGED at V3 (98.5% ACCEPT, second consecutive ≥95% cycle —
V2 96.2% + V3 98.5%, zero orphan REVISE, V = 3 ≤ 5, zero REJECT in any cycle).
The totality fold is locked and ready for Pass Omega behind the mandatory G-Omega
user gate; the two V3 residual REVISEs are non-blocking forward-path tightenings
the CRUD wave absorbs at apply-time.**
