# SK-V16 S-P0 V1 CH7 - Overfit-Prune

Disposition: ACCEPT.

CH7 accepts the folded S-P0 packet:

- It does not permit behavior/admission waves.
- It blocks CSS proof replay through string literals, fact streams, full-parse
  summaries, wrong-plane comparators, sidecars, and FNV metadata.
- It blocks generated/codegen close proof on dirty generated files.
- It keeps Pattern H collapse as generator-owned template work, not header-only
  provenance.
- It keeps native SIMD conditional, profile-first, aarch64-only, and outside
  x86/AVX scope.

Required edits after fold: none.
