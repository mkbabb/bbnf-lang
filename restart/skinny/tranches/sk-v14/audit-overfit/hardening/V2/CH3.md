# CH3 REGRESSION (REDRESS) — SK-V14 S-P0 Overfit-Audit V2 Disposition

Date: 2026-05-23. Lens: CH3 per `ORCHESTRATOR.md §3W` row CH3 + the V2 addendum's §1 at `restart/skinny/tranches/sk-v14/audit-overfit/hardening/V2/CHALLENGE-V2-ADDENDUM.md:23`. Scope: verify the V2 axis-redispatch commit `1735882a5` lands the three V1 CH3 REVISE folds (F-V2-A4-1 scope-extension framing; F-V2-A6-1 LegacyPath both-readings disambiguation; F-V2-SYNTHESIS-2 `css_pretty` co-derivation note) without introducing any new regression-vector. Fresh-finding scan over the V2 deltas for silent reopens of V13 audit-CLEAN rows, silent reversals of prior REDRESS REJECTs, orphans against C-1..C-5, and double-counting of cross-confirmation evidence.

## §0 — Disposition summary

- ACCEPT-rate: **6 / 6 per-§ rows = 100.0%** (above the §3Z 95% convergence floor by a comfortable margin; CH3 V2 closes the three V1 REVISEs cleanly).
- REJECT count: 0.
- REVISE count: 0.
- ACCEPT-with-note count: 0.
- New CH3 findings on V2 deltas: **0**.
- Critical findings: 0 silent reopens of audit-CLOSED routes; 0 silent reversals of prior REDRESS REJECTs; 0 orphan findings outside the C-1..C-5 slate; 0 double-counted evidence under the V2-fold surface; 0 phrasing crossings that skirt prior V13 dispositions without explicit audit-trail acknowledgement.

Overall: V2 lands the three V1 CH3 REVISEs verbatim per the V1 fold-text recommendations (CH3 V1 §3 F-1 / F-2 / F-3). The substance was CH3-clean at V1; V2 closes the editorial-precision surface that V1 flagged. The CONFIRMS-vs-NEW census reconciliation (54 / 20 per-row; 11 NEW finding categories) is now coherent across §0.1 aggregate + §1.1 census + §1.2 enumeration; the A5 verdict-line is FAIL-at-HEAD-PASS-conditioned across §0:11 + §3:102-107 + §4 row 4 + §5 closing; the LegacyPath both-readings paragraph at A6 §0:14 + §2 ledger row Status "NEW (scope-extension over V13 Pattern G; not a reversal)" preserves both V13 Pattern G CLEAN and V14 NEW-HIGH-1 without contradiction. CH3 V2 verdict: **CONVERGED**.

## §1 — Per-§ disposition table

| § | V1 REVISE | V2 fold landed | Disposition | Reason |
|---|---|---|---|---|
| §1 — A4 NEW-1 phrasing risks misreading as reversal of V13 `json_provider` HONEST verdict | REVISE-1 | F-V2-A4-1 | ACCEPT | V2 axis file `sk-v14-audit-overfit-generator-truth.md:33-47` adds the scope-extension paragraph: "This delta is a **scope extension, not a reversal**, of the V13 §7.1 row 1 'HONEST' verdict for `json_provider`. V13 correctly identified the grammar-derived `parse_direct` sink chunk (~70 LOC appended to `generated.rs` via `json_sink_direct::render` per `skinny/crates/codegen/src/lib.rs:215-217`) as honestly grammar-derived; V14 extends the scope to the **OTHER 5 emitted files** (`config.rs`, `parser.rs`, `view.rs`, `value.rs`, `visitor.rs`) plus the `include_str!()` template body of `generated.rs` itself, all of which are pass-through templates with only the prepended `// @generated` header added. V14 does not reverse V13's HONEST verdict on the sink-derived chunk; it observes that the chunk is the minority share (~15%) of the JSON provider's emitted bytes." NEW-1 sub-bullet at line 53-55 also tagged: "Per the disambiguation above, this is a scope extension over V13's HONEST verdict (which addressed only the sink chunk), not a reversal of it." Verified `git show 1735882a5 -- sk-v14-audit-overfit-generator-truth.md` shows the line-cite refresh (F-V2-A4-2: config=:48, generated=:60, parser=:64, view=:68, value=:72) plus Three→Four count (F-V2-A4-3) folded alongside. V13 HONEST verdict on the sink chunk is preserved; V14 framing is unambiguously scope-extension. Disposition closes V1 REVISE-1. |
| §1 — A6 NEW-HIGH-1 phrasing crosses V13 "Pattern G CLEAN" disposition without audit-trail acknowledgement | REVISE-2 | F-V2-A6-1 | ACCEPT | V2 axis file `sk-v14-audit-overfit-pre-restart-pattern.md:14` adds the dedicated "V13-disposition disambiguation for finding (a)" paragraph: "the V13 `SYNTHESIS-AUDIT-OVERFIT.md` line 74 'Honest patterns left clean' disposition ('No backwards-compat shims that aren't legitimate refactors (Agent F Pattern G)') did not enumerate the `LegacyPath` alias surface. V14 records the `LegacyPath` alias as a NEW finding rather than a reversal of the V13 Pattern G CLEAN reading: the alias was introduced at `0e8dbc104 feat(runtime/parse-with-{json,css-l4,sheets,bbnf}): land W3.2 entry points (AZ-IV.W3.2)` (2026-05-02), before V13 close, as part of an in-flight typed-path refactor whose cursor-threaded `parse_<rule>` consumer was scheduled to land in W3.3. V13's Pattern G scan therefore most plausibly either did not survey the four `parse_with.rs` `use`-site aliases or read them as a legitimate in-flight refactor under the 'that aren't legitimate refactors' carveout. V14 surveys them explicitly post-W3.2-landing, classifies the alias as a shim that should be collapsed inside C-1 PRUNE-4 (typed-path collapse) per §4, and records the disposition as scope-extension over V13's Pattern G slate rather than reversal of any specific V13 finding." The §2 ledger row at line 156 carries the same disposition inline and tags Status "NEW (scope-extension over V13 Pattern G; not a reversal)". Both readings (V13's CLEAN under the legitimate-refactor carveout; V14's NEW post-W3.2-landing survey) are preserved without contradiction. Disposition closes V1 REVISE-2. |
| §1 — SYNTHESIS §2 sequencing-constraint inventory double-counts `css_pretty` evidence | REVISE-3 | F-V2-SYNTHESIS-2 | ACCEPT | V2 synthesis file `SYNTHESIS-AUDIT-OVERFIT.md:220-231` adds the "Co-derivation note (binding for S-P3 risk-weighting)" paragraph: "The +3 file delta (64 → 67) and the PRUNE-4 sub-wave count delta (8 → 9) are both attributable to the single `css_pretty` grammar addition between V13 audit-pack landing and SK-V14 baseline; A3 §1, A5 §2.1, and A6 §1 independently re-derive the count from the same evidence, so the three confirmations are evidentially **co-derived, not orthogonal**. The S-P3 wave manifest's risk-weighting for PRUNE-4 should treat the `css_pretty` delta as one piece of evidence with three cross-checks, not three independent regression signals. The R4-before-PRUNE-2 sequencing constraint (§2.1) is similarly co-derived with the +3 / +1 deltas via the `css_pretty` directory addition; A3/A5/A6 cross-confirms are co-derived, not three independent confirmations." V1 fold text recommended only the +3 / +1 co-derivation across §1.3 + §2.3; V2 extends the note to also encompass §2.1 R4-before-PRUNE-2 (genuinely co-derived through the same `css_pretty` addition). The extension is sound: R4 (the regen-css xtask) gates PRUNE-2 because the 24 CSS L4 ADMITTED rows include `css_pretty` rows (W10.3 vintage) that would be unrecoverable without the regen xtask, so the R4 → PRUNE-2 sequencing is itself co-derived with the +3 file / +1 sub-wave deltas. No double-counting risk remains in the S-P3 wave manifest's risk-weighting surface. Disposition closes V1 REVISE-3. |
| §1 — A4 NEW-2 row count "Three" → "Four" | (V1 ACCEPT; no REVISE) | F-V2-A4-3 + F-V2-SYNTHESIS-4 | ACCEPT | Adjacent fold. CH1 V1 caught the row-count drift at A4 §0 NEW-2 (text said "Three" while §2 ledger enumerated four files). V2 axis file lines 56-62 + SYNTHESIS §1.2 item 5 now both say "Four". Verified `grep -rE 'CANONICAL_FIXTURE\|CAPTURED_W2_INPUT' skinny/crates/codegen/src/css_l4_*_templates/generated.rs` returns 4 files (`nested_layout`, `at_rules_and_media`, `stylesheet_selectors`, `vendor_and_custom_atrules`). No CH3 regression-vector exposure — the fold tightens the count, preserves the v1 §1 OVERFIT verdict, and adds zero new admit-trail surface. CH3 audits the V2 alignment for completeness: the four-of-seven count is consistent with the §0.2 verdict text "FAIL — three CRITICAL Pattern H findings hold" and with A4 §2 ledger; no double-counting against A3 / A6 file enumerations. |
| §1 — A5 verdict-line FAIL-at-HEAD pattern across §0 + §3 + §4 + §5 | (V1 CH6 REVISE on paper-close) | F-V2-A5-1 + F-V2-SYNTHESIS-3 | ACCEPT | Adjacent fold. CH3 V2 inspects the verdict-line change purely for regression-surface impact: the V1 phrasing "PASS at SK-V14 starting baseline" could have been read as silently re-blessing the scaffold-citing W14.1-5 rows (which V13 closed AUDIT-FALSIFIED). V2 phrasing "FAIL at HEAD, PASS conditioned on C-5 (PRUNE-1 + PRUNE-2) + C-4 (PRUNE-5) landing" at `sk-v14-audit-overfit-decision-engine.md:11 + :102-107` + `SYNTHESIS-AUDIT-OVERFIT.md` §0.1 A5 row + §0.2 lines 36-39 + §5.1 bullet 2 preserves the FAIL verdict at present HEAD state and explicitly cites C-5 + C-4 as the gating waves. No row is silently re-admitted; no W14.1-5 / W13.* / W15.1 / 24 CSS L4 ADMITTED row is moved off the PRUNE-1/2 target list; the audit-corrected target stays `0/17 / 0/17 / 0/17 / 0/24` per `tranches/sk-v14/SYNTHESIS.md §0.2`. CH3-clean. |
| §1 — V2 fresh-finding scan over the 5-file +113/-55 delta | (no V1 finding) | (no V2 fold) | ACCEPT | Re-walked the diff over `git show 1735882a5` against the four CH3 risk-vectors: (i) silent reopen of V13 audit-CLEAN rows — none; the F-V2-A4-1 paragraph + F-V2-A6-1 paragraph + F-V2-SYNTHESIS-2 paragraph explicitly preserve V13 verdicts. (ii) silent reversal of V13 REDRESS REJECTs — `grep -n 'REDRESS-119\|REDRESS-120\|RE-ADMIT\|PRE-BLOCK' sk-v14-audit-overfit-*.md` returns empty (no V2 axis file cites REDRESS-119 / REDRESS-120 or proposes re-admit under R7); the SK-V14 contract carry remains binding. (iii) orphan findings outside C-1..C-5 — the V2 SYNTHESIS §3.1 coverage table holds at 41 + 7 + 11 + 4 + 11 = 74 with zero orphans (the L8 reclassification at A3 V2 adds no new finding; H3 HIGH→LOW + H6 freestanding HIGH preserves the 11 CRIT + 6 HIGH + 5 MED + 8 LOW = 30 total). (iv) double-counted evidence — F-V2-SYNTHESIS-2 explicitly closes the §1.3 + §2.3 + §2.1 co-derivation surface; no remaining double-count vector. CH3 V2 fresh-finding count: zero. |

## §2 — Critical findings

None. The three V1 REVISEs are folded cleanly; the V2 axis-redispatch surface introduces no new CH3-class regression-vector.

### Non-findings worth recording

- **F-V2-A4-1 preserves V13 §7.1 row 1 HONEST verdict verbatim.** Verified by re-reading the V2 paragraph at `sk-v14-audit-overfit-generator-truth.md:33-47`. The phrasing "**scope extension, not a reversal**" + the explicit identification of the sink chunk (`json_sink_direct::render` per `skinny/crates/codegen/src/lib.rs:215-217`) as the V13-HONEST surface + the explicit enumeration of the V14-extension scope (the OTHER 5 emitted files plus the `include_str!()` template body of `generated.rs` itself) leaves no audit-trail ambiguity. A downstream S-P3 reader scanning "what changed since V13" reads V14 NEW-1 unambiguously as "V13 captured the sink chunk; V14 captures the file roster". The CH3 V1 REVISE-1 surface is fully closed.

- **F-V2-A6-1 preserves both V13 Pattern G CLEAN and V14 NEW-HIGH-1 readings without contradiction.** Verified by re-reading the V2 paragraph at `sk-v14-audit-overfit-pre-restart-pattern.md:14` + the §2 ledger row at line 156. The two readings are: V13's Pattern G scan most plausibly read the W3.2 alias as legitimate in-flight refactor under its own carveout, OR did not survey the `use`-site aliases at all; V14 surveys them post-W3.2-landing and routes the collapse into C-1 PRUNE-4. Both readings are framed as defensible and orthogonal — neither contradicts the other. The Status column update to "NEW (scope-extension over V13 Pattern G; not a reversal)" captures the V14 disposition in the ledger row directly. The CH3 V1 REVISE-2 surface is fully closed.

- **F-V2-SYNTHESIS-2 co-derivation note extends to §2.1 R4-before-PRUNE-2 sensibly.** V1 fold text recommended the co-derivation note for §1.3 + §2.3 only; V2 also picks up §2.1's R4 → PRUNE-2 sequencing constraint, which is genuinely co-derived with the `css_pretty` addition (since the 24 CSS L4 ADMITTED rows that R4 enables recovery of include `css_pretty`-vintage W10.3 rows). The extension hardens the audit-trail surface without introducing a new finding; the S-P3 wave manifest's risk-weighting for PRUNE-4 + R4 → PRUNE-2 sequencing is now correctly framed as "one `css_pretty` delta with multiple cross-checks", not "three independent regression signals".

- **No silent reversal of any V13 REDRESS REJECT.** `grep -n 'REDRESS-119\|REDRESS-120\|RE-ADMIT' restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-*.md` returns empty across all six V2 axis files. The SK-V14 contract's PRE-BLOCK inheritance on REDRESS-119 / REDRESS-120 LIFTED-but-HISTORY rows + W11.2 + W13.5-9 + W11.4 honest measured rejects carries forward intact.

- **No silent reopen of any V13 audit-CLEAN reading on the V2 deltas.** Three V13 CLEAN readings are at risk on the V2 surface: (i) `json_provider` HONEST — closed by F-V2-A4-1 scope-extension framing; (ii) Pattern G CLEAN — closed by F-V2-A6-1 both-readings paragraph; (iii) `parse-that-regex` + `bbnf-simd` CLEAN under Lock 14 (A3 DELTA-NOTE D1) — unchanged at V2 (no F-V2-A3 fold touches D1; the H3 reclassification at decision_csp.rs:235 is orthogonal to D1's StringFlags::HAS_ESC scope and preserves the v3 CLEAN classification). All three remain coherently extended rather than reversed.

- **No orphan finding outside C-1..C-5 introduced by V2 folds.** SYNTHESIS §3.1 coverage table is preserved at 41 + 7 + 11 + 4 + 11 = 74 (V2's 54-CONFIRM / 20-NEW per-row reconciliation does not change the per-C-N coverage). The L8 reclassification at A3 V2 is a status downgrade (H3 HIGH → LOW under F-V2-A3-1), not a new finding; the 30-row total holds. No new C-6 candidate required; no new R-target proposed.

- **No double-counting risk in the S-P3 wave manifest surface.** F-V2-SYNTHESIS-2 explicitly closes the §1.3 + §2.3 + §2.1 co-derivation surface around `css_pretty`. The remaining cross-confirmations across A3 / A4 / A5 / A6 on Pattern H + provider rosters are independent (A3 enumerates Lock 14 violations per-file; A4 enumerates emission roster per-provider; A5 enumerates gate-layer footprint; A6 enumerates Pattern H file count + LegacyPath alias). The synthesis correctly distinguishes co-derived counts (`css_pretty`-driven) from independent confirmations (multi-axis enumeration of distinct surfaces).

- **The 54/20 per-row + 11 NEW finding categories census is internally consistent.** Verified by re-walking the per-axis tables in V2 SYNTHESIS §1.1: A1 (8 CONFIRMS + 0 NEW), A2 (7 CONFIRMS + 2 NEW), A3 (29 CONFIRMS + 1 NEW), A4 (4 CONFIRMS + 12 NEW), A5 (3 CONFIRMS + 1 NEW), A6 (3 CONFIRMS + 4 NEW) = 54 CONFIRMS + 20 NEW = 74 total. The 11 NEW finding categories in §1.2 cluster the 20 per-row NEW findings (A4's 12 NEW rows cluster into 3 categories: NEW-1 JSON @generated fake, NEW-2 four-of-seven CSS scanners as fixture lookups, NEW-3 14/15 .bbnf orphan; A6's 4 NEW rows cluster into 4 categories per A6 §0 lines 12; A2's 2 NEW into 2 categories F8 + F9; A3's 1 NEW (DELTA-NOTE D1) does not cluster into the 11-category enumeration; A5's 1 NEW is the gate-layer footprint quantification category). The 11 categories + 20 per-row figures are both correct under their respective counting conventions; F-V2-SYNTHESIS-1 explicitly reconciles them across the 5 sites.

## §3 — Recommended folds for V3

None. CH3 V2 verdict is CONVERGED at 100% ACCEPT. V3 confirming pass over unchanged V2 artefacts closes §3Z LOCK two-consecutive-cycle chain → G-S-P0-CONVERGED → S-P1 dispatch per the SK-V14 ORCHESTRATOR-PROMPT THE SK LOOP.

### Convergence note

CH3 V1 ACCEPT-rate was 30/33 = 90.9% (3 REVISEs, 0 REJECTs); CH3 V2 ACCEPT-rate is 6/6 = 100.0% (0 REVISEs, 0 REJECTs, 0 new findings). The +9.1pp ACCEPT-rate improvement clears the §3Z 95% floor by 5pp. V2 lands all three V1 REVISE folds verbatim per the V1 §3 F-1 / F-2 / F-3 fold-text recommendations; the V2 axis-redispatch surface preserves both V13 verdicts and V14 NEW findings without contradiction; the synthesis-level co-derivation note extends sensibly to cover §2.1 R4-before-PRUNE-2 sequencing alongside the §1.3 / §2.3 +3 / +1 deltas; the verdict-line FAIL-at-HEAD pattern at A5 + SYNTHESIS preserves the SK-V14 contract's audit-falsified status for all scaffold-citing rows. CH3 substance was clean at V1; CH3 audit-trail surface is now clean at V2.

No CH3 V2 finding requires a new C-6 candidate, a new R-target, a sequencing-constraint revision, or any further fold. The 63 → 54-per-row + 11-NEW-category framing in V2 is the correct reconciliation (per-row vs per-category counts both internally consistent); the prune list maps 1:1 to C-1..C-5 with zero orphans; no candidate or finding silently reverses any prior REDRESS REJECT; no V13 audit-CLEAN reading is silently reopened; no double-counted evidence remains in the audit-trail surface. CH3 V2 disposition: **CONVERGED**.
