# CH7 — OVERFIT-PRUNE — S-P1 V2 Profile Pass (Confirming)

Lens authority: `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87`
(CH7 Overfit-Prune lens definition); dispatch context
`restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CHALLENGE-CONTEXT.md:36`
(V1 lens-overlay focus, carried into V2); V1 disposition
`restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md`
(V1 ACCEPT 100 % / 7-of-7 subclauses, 9-grammar census verified).

V2 commit under review: `069ba203c` ("V2 light micro-redispatch — five
orphan REVISEs landed"); diff base: `a3dfcaf38`; V1 → V2 delta scope:
6 files, +86 / −26 lines, **no symbol re-record**.

Discipline: WRITE-ONLY (aggregator commits). HARD CAP 30 min.

## §0 — Disposition summary

**Verdict: ACCEPT (7/7 lens-overlay subclauses; unchanged from V1).**

V2 is a *light-fold* pass: methodology disclosure (`build_flags_regime`
row in P1-A/B/C/D), line-anchor refresh (P1-C NEON, P1-E typed-plane
file:lines, P1-A movemask `(fn @ N)` annotation), and CH5-V1-REVISE →
CH5-V2-CONTRACTED-DEFERRAL framing (P1-F §4.1). **None of the V2 fold
packets touch the CH7 audit-overlay surface.** The audit overlay
column population, PRUNE-1 W14.\* row consistency, and absence of
fake-`@generated` / scaffold-as-load-bearing / gate-relabel patterns
all remain at V1's 100 % posture, mechanically re-verified at HEAD.

| # | Subclause | V1 verdict | V2 verdict | Evidence (re-run at V2 HEAD) |
| ---: | --- | --- | --- | --- |
| 1 | P1-D Δ vs SK-V13 = −1.0 % c/B confirms zero-source-byte SK-V14 baseline | ACCEPT | **ACCEPT** | `p1d-pmu-cycles.md:478` (line drift +11 from V2 frontmatter insert; substance unchanged) mean Δ c/B = −0.063 (−1.0 %); 17 doc-only commits between SK-V13 close `00181742e` and SK-V14 dispatch seed `12ff0744e` unchanged at V2 HEAD |
| 2 | `audit_overlay_verdict` column populated per row across all 6 P1 outputs | ACCEPT (258 cells) | **ACCEPT (261 cells; +3 from F-V2-P1F-1 framing)** | `grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' p1*.md` → p1a=13, p1b=72, p1c=11, p1d=67, p1e=37, p1f=**61** (V1=58); the +3 cells land in P1-F §4.1 contracted-deferral framing paragraph (`p1f-results-delta.md:179-187`), explicitly naming the four NEW columns including `audit_overlay_verdict` |
| 3 | No S-P1 finding re-introduces fake `@generated` / scaffold-as-load-bearing / gate-relabel pattern | ACCEPT | **ACCEPT** | `git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/` returns hits ONLY in V1 hardening file's diagnostic citations (`hardening/V1/CH7.md`); zero hits in the six V2 P1 artefact bodies themselves; four textual mentions of "gate-relabel" / one of "scaffold" remain diagnostic carries of the audit-pack pathology, not new admit proposals |
| 4 | 5 PRUNE-1 AUDIT-FALSIFIED rows (W14.1-5: numbers, citm_catalog, canada, marine_ik, mesh) consistently marked across all P1 outputs | ACCEPT | **ACCEPT** | `grep -nE 'PRUNE-1\|W14\.[1-5]' p1{a,c,d,e,f}*.md` → 47 hits at V2 HEAD (identical count to V1); per-row PRUNE-1 attribution on all 5 W14.* rows in p1a, p1c, p1d, p1e, p1f unchanged; the V2 line-anchor refreshes on `citm_catalog` (movemask cell got `(fn @ 169)` annotation at p1a:147; AUDIT-FALSIFIED verdict literal unchanged) and `github_events` / `update_center` (P1-E typed-plane file:line refresh; AUDIT-FALSIFIED verdict literal unchanged) preserve verdict literally |

Aggregate ACCEPT-rate: **7/7 = 100 %** (4 explicit subclauses + 3
supporting executable verifications; unchanged from V1).
Zero CH7 REJECT triggers per `PASS-0-OVERFIT-AUDIT.md:86`.

V2 ΔACCEPT-rate vs V1: **0 pp** (both 100 %). V2 met §3Z ≥95 %; combined with V1, this
is the **second consecutive cycle** at ≥95 % on CH7 — the §3Z
"≥95 % × 2 cycles, zero orphan REVISEs" gate **CLOSES on CH7 at V2**
(V1=100 %, V2=100 %, no REVISE orphaned at either cycle on this lens).

## §1 — Per-artefact CH7 audit table (V1 → V2)

Each row evaluates one P1 artefact against the four CH7 lens
subclauses, with the V1 → V2 delta in the final column. ✓ = subclause
pass; n/a = subclause does not apply to this artefact's plane.

| Artefact | HEAD lines | Δ vs SK-V13 baseline cite | audit-overlay column | No fake-pattern recurrence | PRUNE-1 W14.* consistency | V2 verdict | Δ vs V1 |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| `p1a-samply-mode-1.md` | 343 (V1 340) | ✓ (§1.4 SK-V14 baseline cite unchanged) | ✓ (13 cells; per-row §2.1 table unchanged; cell-count identical) | ✓ (`:207` "gate-relabel, not parser improvement" diagnostic carry unchanged) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED; literal verdict cells unchanged; cell-count identical; movemask `(fn @ 169)` annotation added at table cells but PRUNE-1 attribution literal unchanged) | **ACCEPT** | unchanged |
| `p1b-samply-mode-2.md` | 323 (V1 320) | ✓ (§1 SK-V14 baseline cite + §3 verdict tables unchanged) | ✓ (72 cells; cell-count identical) | ✓ (`:321` "gate-relabel only; comparator misnamed" remains a **citation** of SYNTHESIS-AUDIT-OVERFIT §1, not a new admit) | n/a (JSON direct/typed plane; PRUNE-1 is parse_only; p1b direct + typed lanes' W14.* rows AUDIT-FALSIFIED literally unchanged) | **ACCEPT** | unchanged |
| `p1c-samply-mode-3.md` | 616 (V1 607) | ✓ (§3 Δ vs SK-V13 header unchanged at line shifted by +9 from V2 build_flags_regime insertion) | ✓ (11 cells; cell-count identical) | ✓ (`:409` "gate-relabel, not parser delta" diagnostic carry unchanged) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED; verdict literal unchanged; the 3 NEON line-anchor refreshes are cosmetic primitive lines, not W14.* admit cells) | **ACCEPT** | unchanged |
| `p1d-pmu-cycles.md` | 669 (V1 648) | ✓ (§3 Δ table at `:431+`; line drift +11 from V2 frontmatter insert; arithmetic `mean Δ c/B = −0.063 c/B (−1.0 %)` unchanged at lexical level) | ✓ (67 cells; cell-count identical) | ✓ (PMC `unavailable_from_current_export` cite unchanged) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED in §2.1 parse_only table; row literals unchanged) | **ACCEPT** | unchanged |
| `p1e-hot-leaf-attribution.md` | 321 (V1 306) | ✓ (V2 fold note at `:15` explicitly retains V14 baseline framing; §3 delta header unchanged) | ✓ (37 cells; cell-count identical) | ✓ (`:17` Lock-14 mis-attribution classification of envelope primitives unchanged; the F-V2-P1E-1 refresh repoints 5 file:line cites but preserves the same primitive identities) | ✓ (5/5 W14.* rows present in §2.1 table; verdict literal AUDIT-FALSIFIED unchanged; refreshed `:2949` / `:516` etc. are file-line repoints, not admit-status changes) | **ACCEPT** | unchanged |
| `p1f-results-delta.md` | 269 (V1 260) | ✓ (`:4` "Baseline: SK-V14-open (audit-corrected SK-V13 close state)" unchanged) | ✓ (**61 cells** = V1 58 + 3 new cells in §4.1 contracted-deferral framing paragraph; the +3 cells are *additional* per-row population evidence for the `audit_overlay_verdict` column, not a removal or relabel) | ✓ (`:144` "gate-relabelled" S-substrate-guard classification unchanged; `:247` A2 F8 single-lane comparator fanout cite unchanged) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED at `:269` closure summary unchanged from V1 `:397-398`; line drift only) | **ACCEPT** | unchanged (now stronger via +3 cells reinforcing column population) |

Per-artefact ACCEPT-rate: **6/6 = 100 %** (unchanged from V1).

## §2 — Executable verification (re-run at V2 HEAD `069ba203c`)

All five executable-verification commands from V1 CH7 §2 re-run
verbatim against V2 HEAD; outputs recorded below.

### §2.1 cargo metadata grammar census (dispatch §36 directive)

```
$ cd /Users/mkbabb/Programming/bbnf-lang
$ cargo metadata --format-version 1 --no-deps | \
    jq -r '.metadata.bbnf.grammars[].ident'
bbnf
json
css_l4
css_pretty
google_sheets
ebnf
bnf
csv
math
```

**Expected**: 9 grammars including `css_pretty`. **Observed**: 9
grammars; `css_pretty` present (5th line). **PASS (unchanged from V1).**

Quantitative cross-check: `cargo metadata ... | jq '.metadata.bbnf.grammars | length'` returns `9` at V2 HEAD (identical
to V1). The grammar enumeration source is `/Users/mkbabb/Programming/
bbnf-lang/Cargo.toml:18` `[workspace.metadata.bbnf]` `grammars =
[ ... ]` array (9 elements; verified by `grep -nE "^\[workspace\.metadata|grammars" Cargo.toml`).

**Caveat for downstream consumers (NEW V2 finding — methodology hygiene).**
The same jq command run from the inner `skinny/` workspace returns
`null` (not 9 grammars) because `skinny/Cargo.toml:18` declares
`[workspace.metadata.bbnf.grammars]` as a **map** (`.json` keyed
subtable), not an array, and only `json` is declared in the
skinny inner workspace. Consumers re-running the V1 / V2 CH7
verification command must `cd /Users/mkbabb/Programming/bbnf-lang`
(repo root) before invocation. This is a *command-portability* note,
not a CH7 violation: the V1 jq output (`bbnf, json, css_l4, css_pretty,
google_sheets, ebnf, bnf, csv, math`) is reproducible from repo root
verbatim at V2 HEAD. **Recommend** the V3 confirming or aggregator
file annotates the verbatim command with the working-directory
prefix to prevent future agent confusion. (Filed under §3.3 below.)

### §2.2 Fake-pattern recurrence scan (dispatch §36 directive)

```
$ git grep '@generated by skinny bbnf-codegen' \
    restart/skinny/tranches/sk-v14/research/p1/
restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md:| 3 | No S-P1 finding re-introduces fake `@generated` / scaffold-as-load-bearing / gate-relabel pattern | **ACCEPT** | `git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/` returns zero hits; ...
restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md:$ git grep '@generated by skinny bbnf-codegen' \
restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md:No P1 artefact body contains a fake `@generated by skinny bbnf-codegen`
restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md:git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/
```

**Expected (per dispatch directive)**: zero hits.
**Observed (excluding V1's own self-citation in hardening/V1/CH7.md)**:
**zero hits.** **PASS (unchanged from V1).**

Confirmation via:

```
$ git grep '@generated by skinny bbnf-codegen' \
    -- ':!restart/skinny/tranches/sk-v14/research/p1/hardening/V1/' \
       'restart/skinny/tranches/sk-v14/research/p1/'
(exit 1; zero hits)
```

The four matches above are all *inside* `hardening/V1/CH7.md` — the
V1 CH7 artefact itself documenting the absence of the pattern. These
are diagnostic citations of the fake-`@generated` recurrence vector
(the A4 NEW-1 + NEW-2 pattern enumerated at
`SYNTHESIS-AUDIT-OVERFIT.md:112-134`), not new admit recurrences.
**No V2 P1 artefact body contains the fake-`@generated` header.**

### §2.3 audit-overlay column cell-count census

```
$ grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' \
    restart/skinny/tranches/sk-v14/research/p1/p1*.md
restart/skinny/tranches/sk-v14/research/p1/p1a-samply-mode-1.md:13
restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md:72
restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md:11
restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md:67
restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:37
restart/skinny/tranches/sk-v14/research/p1/p1f-results-delta.md:61
```

Total: **261 audit-overlay cell-references across 6 P1 outputs at
V2 HEAD** (V1 was 258; **+3 cells in p1f from F-V2-P1F-1 §4.1
contracted-deferral framing paragraph at `:179-187`**, which
explicitly names the four NEW columns including
`audit_overlay_verdict`).

The +3 delta is a *reinforcement* of column population (the
contracted-deferral framing makes the `audit_overlay_verdict`
column requirement load-bearing for the C-2 wave), not a relabel or
removal. p1a, p1b, p1c, p1d, p1e cell-counts are identical to V1
(13, 72, 11, 67, 37 respectively). **PASS — column population is
maintained at V2 and slightly strengthened in p1f.**

### §2.4 PRUNE-1 W14.\* row consistency

```
$ grep -nE 'PRUNE-1|W14\.[1-5]' \
    restart/skinny/tranches/sk-v14/research/p1/p1{a,c,d,e,f}*.md | wc -l
47
```

**47 hits at V2 HEAD; identical to V1.** Every parse_only-plane
P1 artefact (p1a, p1c, p1d, p1e, p1f) flags all 5 W14.\* rows
AUDIT-FALSIFIED with PRUNE-1 attribution. p1b is JSON direct + typed
plane — PRUNE-1 is the parse_only revert; p1b correctly carries the
W14.\* corpora rows in the direct + typed lanes where the same
comparator-misbinding pathology fires (per
`SYNTHESIS-AUDIT-OVERFIT.md:91-97` A2 F8 single-lane fan-out
structural cause), with AUDIT-FALSIFIED verdict on every row.

V1 → V2 cross-artefact consistency is preserved verbatim. The V2
line-anchor refreshes on `citm_catalog` (movemask annotation),
`github_events` / `update_center` / `mesh` / `marine_ik`
(typed-plane file:line refresh in p1e §2.3) all repoint file:line
cites WITHOUT modifying the AUDIT-FALSIFIED verdict literal or the
PRUNE-1 / W14.\* attribution. **PASS (unchanged from V1).**

### §2.5 SK-V14 NEW schema column gap audit (re-verified)

```
$ grep -nE 'comparator_plane|per_iter_equality|track2_entry_point' \
    restart/skinny/tranches/sk-v14/research/p1/p1*.md
p1b-samply-mode-2.md:321:  (cites SYNTHESIS §0.2 + §93 R1 + §241 (comparator_plane) + §255 (audit_overlay_verdict))
p1f-results-delta.md:28:  grep -c 'comparator_plane\|per_iter_equality\|audit_overlay_verdict\|track2_entry_point' ...
p1f-results-delta.md:172: | `comparator_plane` | R1 (per-plane strict comparator) | ...
p1f-results-delta.md:173: | `per_iter_equality` | R2 (per-iter equality oracle inside timing region) | ...
p1f-results-delta.md:175: | `track2_entry_point` | CH5 hidden-coupling guard | ...
p1f-results-delta.md:179: **Contracted-deferral framing (V2 reclassification — F-V2-P1F-1).** ...
p1f-results-delta.md:184: **CH5 V1 REVISE → V2 ACCEPT-WITH-NOTE conversion.** ...
p1f-results-delta.md:198: | `comparator_plane` | **Absent** (SK-V14 NEW; per §4.1) | 0/76 | gap |
p1f-results-delta.md:247: A2 F8 — single-lane comparator fanout. ...
p1f-results-delta.md:269: 4 NEW SK-V14 schema columns absent from RESULTS.md ...
```

V2 strengthens the §2.5 cross-check via the F-V2-P1F-1 contracted-
deferral framing block at `p1f-results-delta.md:179-187`, which
explicitly assigns the four NEW SK-V14 schema columns
(`comparator_plane`, `per_iter_equality`, `audit_overlay_verdict`,
`track2_entry_point`) to the C-2 wave deliverable (SYNTHESIS §3
C-2:272). The schema-gap that V1 flagged as "76 rows × column gap"
is now framed as a load-bearing *deferral* to C-2 (R1 + R2 + audit-
overlay scribe), not a P1-F authoring failure.

The CH7 lens reads this exactly as V1 did: the gap is the **target**
of R1/R2 + the audit-overlay scribe (S-P3 wave manifest), not a
re-introduced overfit pattern. The V2 reclassification is a CH5-
domain disposition change (CH5 V1 REVISE → CH5 V2 ACCEPT-WITH-
CONTRACTED-DEFERRAL) that does *not* alter the CH7 verdict.
**PASS (no CH7 violation; CH7 carries the V1 flag forward as §3.2 of
this file).**

## §3 — Critical findings / observations

### §3.1 No new CH7 violations surfaced at V2

The six V2 P1 outputs preserve the audit overlay end-to-end; the
audit-pack prune list is honored on every per-row table verbatim;
no V2 fold packet proposes a new `@generated`-fake / scaffold-as-
load-bearing / gate-relabel admit. The dispatch context §36 four
subclauses all pass executable verification (re-run at V2 HEAD).
**CH7 fires CLEAN at V2.**

V1 → V2 delta scope was limited to:
1. Methodology disclosure (`build_flags_regime` row in 4 of 6 artefacts).
2. Line-anchor refresh (3 NEON in p1c; 5+ typed-plane file:lines
   in p1e; 12 `(fn @ N)` movemask annotations in p1a).
3. Contracted-deferral framing (P1-F §4.1; converts CH5 V1 REVISE
   to CH5 V2 ACCEPT-WITH-CONTRACTED-DEFERRAL).

**None of these touch the CH7 audit-overlay surface.** Cell counts:
p1a/b/c/d/e unchanged at 13/72/11/67/37; p1f **+3 cells** (V1=58,
V2=61) — strengthening, not weakening, the per-row column population.

### §3.2 V1 §3.2 observation (RESULTS.md schema gap) is CLOSED at V2 by F-V2-P1F-1

V1 CH7 §3.2 flagged the **RESULTS.md schema gap** (76 rows × 3
absent columns = 228 missing cells) as a non-CH7 finding worth the
aggregator's attention so the C-2 entry/exit gate is captured in the
SK-V14 SPEC wave manifest. **V2 F-V2-P1F-1 explicitly performs this
extraction** at `p1f-results-delta.md:179-187`, citing
`SYNTHESIS.md §3 C-2 (line 272)` as the wave deliverable and
naming the four NEW columns jointly. The V1 §3.2 observation is
**MECHANICALLY CLOSED by V2** — the aggregator no longer needs to
manually propagate it.

### §3.3 NEW V2 finding (methodology hygiene; non-blocking)

The dispatch directive's verbatim command
`cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'`
silently produces *different* output depending on the working
directory: from repo root (`/Users/mkbabb/Programming/bbnf-lang`)
it returns the 9-grammar list verbatim; from `skinny/` it returns
`null` because the inner workspace declares `grammars` as a map
with only `json`. The V1 CH7 author ran the command from repo root
and got the 9-grammar list, which is the authoritative answer.

This is a *command-portability* hygiene issue, not a CH7 violation.
The 9 grammars are reproducible at V2 HEAD. **Recommend** V3 or
aggregator annotate the verbatim command with the working-directory
prefix (e.g. `cd /Users/mkbabb/Programming/bbnf-lang && cargo
metadata ...`) so future agents do not misread the `null` and
conclude the grammar census has regressed. Filed as a §4 soft
enhancement; non-blocking on V2 ACCEPT.

### §3.4 Co-derivation note on +1 grammar (css_pretty) — carried unchanged from V1

The cargo-metadata grammar list at V2 HEAD confirms `css_pretty` as
the 5th grammar (line 5 of the jq output, identical position to V1).
A3, A5, A6, and the cargo metadata cross-confirm the +1 over the V13
audit-pack body's 8-directory enumeration. The S-P3 wave manifest's
PRUNE-4 sub-wave count of 9 (not 8) per
`SYNTHESIS-AUDIT-OVERFIT.md:282-291` rests on this co-derivation;
CH7 reads the four confirmations as **co-derived from the
css_pretty directory addition**, not four independent regression
signals. Risk weighting should treat the +1 delta accordingly.
**Carried verbatim from V1 §3.3; no change at V2.**

## §4 — V3 / aggregator fold recommendations

### §4.1 No CH7-driven revisions required at V2

The six V2 P1 artefacts are CH7-clean as landed. The aggregator may
consolidate the V2 hardening without per-artefact CH7 redress.

### §4.2 Two soft enhancements (non-blocking; carried + extended)

1. **§4.2.1 (carried from V1 §4.2.2; extended).** Lift the §2.2 fake-
   pattern grep into a CI pre-commit check for the
   `restart/skinny/tranches/sk-v{N}/research/` glob, per the §2.4
   CH7-companion lint already recommended by
   `SYNTHESIS-AUDIT-OVERFIT.md §2.4`. **V2 extension:** the V1 CH7
   itself now contains *diagnostic* citations of the fake-
   `@generated` header (4 hits in `hardening/V1/CH7.md`). A naive
   grep would fire on these citations; the lint rule must
   distinguish "fake-`@generated` header in a documentary citation"
   from "fake-`@generated` header in a load-bearing claim". One
   workable rule: allow the literal only when preceded by a code
   fence backtick on the same line or in a `### §N — Fake-pattern
   recurrence` titled subsection. Recommend folding into LOCKS.md.

2. **§4.2.2 (NEW; per §3.3 above).** Annotate the verbatim cargo
   metadata command with the working-directory prefix to prevent
   future agents from misreading the `null` from `skinny/` as a
   grammar regression. Two workable formulations:
   - `cd /Users/mkbabb/Programming/bbnf-lang && cargo metadata ...`
   - `cargo metadata --manifest-path /Users/mkbabb/Programming/bbnf-lang/Cargo.toml ...`

### §4.3 Convergence-gate impact (V2)

V2 CH7 ACCEPT-rate: **100 % (7/7 subclauses)**, identical to V1.

§3Z requires "≥95 % × 2 cycles, zero orphan REVISEs":
- **Cycle 1 (V1):** 100 % — MEETS threshold (zero orphan REVISEs).
- **Cycle 2 (V2):** 100 % — MEETS threshold (zero orphan REVISEs).

**CH7 convergence-gate CLOSES at V2** — two consecutive cycles at
≥95 % with zero orphan REVISEs. No V3 work required on this lens.

### §4.4 V3 / S-P2 readiness

The S-P1 V2 outputs are CH7-CONVERGED for §3Z on this lens. The
remaining six lenses (CH1-CH6) author their own V2 findings; the
aggregator consolidates all seven. **No CH7-driven blocker on S-P2
dispatch.**

The single soft enhancement in §4.2.1 (CI lint) and §4.2.2
(command-portability annotation) are S-P3 / LOCKS.md follow-ups,
not S-P2 entry-gate items.

## §5 — Bibliography / cite roster

Files referenced (all paths absolute from repo root):

- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87` (CH7 lens
  definition)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CHALLENGE-CONTEXT.md:36`
  (V1 lens-overlay focus — carried into V2)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md`
  (V1 disposition; 100 %; 7/7 subclauses ACCEPT)
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`
  (§0.1, §1.2, §2.4, §3.1, §3.3, lines 70-74, 91-97, 112-134,
  201-211, 282-291)
- `restart/skinny/tranches/sk-v14/research/p1/p1a-samply-mode-1.md`
  (V2 HEAD: 343 lines; :207 gate-relabel diagnostic carry)
- `restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md`
  (V2 HEAD: 323 lines; :321 gate-relabel citation; audit overlay basis)
- `restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md`
  (V2 HEAD: 616 lines; :409 gate-relabel diagnostic carry)
- `restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md`
  (V2 HEAD: 669 lines; Δ vs SK-V13 arithmetic preserved)
- `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md`
  (V2 HEAD: 321 lines; :17 Lock-14 mis-attribution classification
  unchanged; §2.3 typed-plane file:line refreshes do not alter
  AUDIT-FALSIFIED verdict literal)
- `restart/skinny/tranches/sk-v14/research/p1/p1f-results-delta.md`
  (V2 HEAD: 269 lines; :144 gate-relabelled S-substrate classification;
  :179-187 F-V2-P1F-1 contracted-deferral framing; :269 closure
  summary with 5 W14.* AUDIT-FALSIFIED unchanged)
- `/Users/mkbabb/Programming/bbnf-lang/Cargo.toml:18` (workspace
  metadata `bbnf.grammars` 9-element array)
- `/Users/mkbabb/Programming/bbnf-lang/skinny/Cargo.toml:54-69`
  (inner workspace metadata `bbnf.grammars` map with only `json`
  — methodology-hygiene caveat per §3.3)
- V1 → V2 commit delta: `git diff a3dfcaf38 069ba203c -- restart/
  skinny/tranches/sk-v14/research/p1/` (6 P1 files, +86/-26 lines,
  no symbol re-record)

Executable verification commands (re-run at V2 HEAD; outputs
quoted inline at §2.1-§2.5):

```
cd /Users/mkbabb/Programming/bbnf-lang
cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'
git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/
git grep '@generated by skinny bbnf-codegen' -- ':!restart/skinny/tranches/sk-v14/research/p1/hardening/V1/' 'restart/skinny/tranches/sk-v14/research/p1/'
grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' restart/skinny/tranches/sk-v14/research/p1/p1*.md
grep -nE 'PRUNE-1|W14\.[1-5]' restart/skinny/tranches/sk-v14/research/p1/p1{a,c,d,e,f}*.md | wc -l
grep -nE 'comparator_plane|per_iter_equality|track2_entry_point' restart/skinny/tranches/sk-v14/research/p1/p1*.md
grep -nE 'gate-relabel|scaffold' restart/skinny/tranches/sk-v14/research/p1/p1*.md
wc -l restart/skinny/tranches/sk-v14/research/p1/p1*.md
```

All seven re-runnable verifications executed at V2 HEAD
`069ba203c`. Outputs quoted inline at §0, §2.1-§2.5.
