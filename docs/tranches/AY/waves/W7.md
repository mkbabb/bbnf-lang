# AY.W7 — FINAL: Bench Matrix + FINAL.md + AY Handoff

**Opens after**: W6 close
**Agents**: 1 serial
**Hard gate**: `docs/benchmarks/post-AY.json` captured over the 18-entry parse matrix + eager-lane bench entries; `docs/tranches/AY/FINAL.md` authored per canonical template; `docs/tranches/AY/AY.md` review-ready with AY handoff dependencies documented; `cargo test --workspace` green; `nm` verifies invariants 22-24 closure.

## Scope

1. **Capture AY close bench matrix**: 18-entry parse bench + eager-lane `json_monolithic_value` bench (10 new entries). Cold per-parse, release profile, mimalloc. Save to `docs/benchmarks/post-AY.json`.
2. **Author `docs/tranches/AY/FINAL.md`**: tranche summary — outcomes per wave, invariant status (22-24 closed), handoff to AY, post-tranche review candidates.
3. **Update AY handoff**: `docs/tranches/AY/AY.md` §"Opens after" references AY close + AY-delivered substrate (stable shape emitter + tape hot-path inline + Value API + Named preservation). Document any AY-scope-reveal items that moved to AY.
4. **Commit handoff doc**: `docs/tranches/AY/PROGRESS.md` has final AY-close entry citing all waves.

## File bounds

| File | Access |
|------|--------|
| `docs/benchmarks/post-AY.json` | create |
| `docs/tranches/AY/FINAL.md` | create |
| `docs/tranches/AY/AY.md` | modify (update handoff dependencies + any absorbed-scope items) |
| `docs/tranches/AY/PROGRESS.md` | create (single final entry — PROGRESS log builds throughout AY execution) |

**Do NOT touch**: any `.bbnf` grammar, any code path. W7 is documentation + measurement only.

## Phase sub-items

### AY.W7.1 Bench capture

Run:
```bash
export CARGO_TARGET_DIR=/Users/mkbabb/Programming/bbnf-lang/.profile-target
find . -name .bbnf-cache -exec rm -rf {} + 2>/dev/null
# 18-entry parse matrix (4 grammar families × fixtures)
for bench in json_monolithic css_l4 google_sheets_monolithic bbnf_monolithic; do
  cargo bench -p bbnf --bench "$bench" --profile release 2>&1 | tee /tmp/post-AY-$bench.txt
done
# 10-entry eager matrix
cargo bench -p bbnf --bench json_monolithic_value --profile release 2>&1 | tee /tmp/post-AY-eager.txt
```

Aggregate numeric `ns_per_iter` + `bytes_per_ns` per entry into `docs/benchmarks/post-AY.json` using the same JSON schema as `post-AX-W0a-close.json`.

### AY.W7.2 FINAL.md authoring

Author `docs/tranches/AY/FINAL.md` per canonical tranche-FINAL template. Sections:

- **Scope recap**: what AY opened with (AX close state + 6 audit findings) and what AY delivered (W0–W6 wave outcomes).
- **Bench delta table**: post-AY.json vs post-AX-W1-close.json; per-entry ns/iter delta + MB/s + comparator ratio.
- **Invariant closure**: 22 (tape inline — `nm` + samply evidence), 23 (Named preservation — wire-contract test passing), 24 (Value API apples-to-apples — eager-lane bench entries populated).
- **Hot-path self-time shift**: before = `push_structural` + `finalise` at 50-70%; after = per-rule parse fns dominate.
- **JSON gap closure**: twitter bbnf/sonic 7.93× → target 3-4×; validated.
- **Post-tranche review candidates**: if any W4-W6 levers failed to hit targets, named here per SPEC.
- **AY handoff**: what AY delivers to AY as stable substrate (shape emitter, tape columns schema, Value API, Named preservation).

### AY.W7.3 AY handoff update

Edit `docs/tranches/AY/AY.md`:
- §"Opens after" references AY close + AY-delivered substrate.
- Invariant 1 ("No DTA carry-forward") unchanged.
- Any AY scope-reveal items that moved to AY documented in AY's §Archaeology.

### AY.W7.4 AY PROGRESS closure

Author `docs/tranches/AY/PROGRESS.md` as a single document appending entries throughout AY execution (per W0–W6 closing commits). W7 adds final AY-close entry.

## Hard gate

1. `docs/benchmarks/post-AY.json` exists with 18 parse entries + 10 eager entries, all with numeric `ns_per_iter`. Artefact: file.
2. `docs/tranches/AY/FINAL.md` authored, ≥ canonical sections. Artefact: file.
3. `docs/tranches/AY/AY.md` updated with handoff text. Artefact: git diff.
4. `cargo test --workspace --profile ax-iter` green (no `#[ignore]` except debug-assertion gates). Artefact: test log.
5. `nm` on 4 bench binaries: zero `push_structural` / `finalise::finalise` exports (invariant 22); `grep PROJECTION_DIRECT_TO_STRUCT generated.rs ≥ 4` (invariant 23); eager-lane entries populated in `post-AY.json` (invariant 24). Artefact: each.

## Verification artefacts

- `docs/benchmarks/post-AY.json` — canonical AY-close bench.
- `docs/tranches/AY/FINAL.md` — tranche summary.
- `docs/tranches/AY/AY.md` — updated handoff.
- `docs/tranches/AY/PROGRESS.md` — full AY progress log.
- Commit SHAs: one for bench capture, one for FINAL, one for AY update.

## Dependencies

- **Depends on**: W6 close.
- **Blocks**: AY tranche open.

## Archaeology

Each prior tranche has a FINAL.md summarising outcomes (AW-V, AW-IV, AW-III, AU, etc.). AY.W7 follows the established template. The first tranche to close with `FINAL.md` asserting invariants 22-24 at the tape + emitter + API layers.
