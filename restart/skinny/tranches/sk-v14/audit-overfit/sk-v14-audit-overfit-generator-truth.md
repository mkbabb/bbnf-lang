# SK-V14 S-P0 Axis A4 — Generator-vs-Hand-Curated

## §0 — Disposition summary

Pass criterion (verbatim from `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md` §Scope row A4):

> 100% round-trip pass; every "generated.rs" comes from a real `cargo
> xtask regen` command + a real `.bbnf` grammar source.

**Verdict: FAIL.**

| Severity | Count |
| --- | --- |
| CRITICAL | 9 |
| HIGH | 4 |
| MED | 2 |
| LOW | 1 |

Confirms / extends SK-V13 audit pack: **yes, with one delta.** v1 + v4
reported "7 CSS L4 providers use `include_str!()` of hand-written
templates with fake `@generated` header." That count holds — all seven
provider modules in `skinny/crates/codegen/src/css_l4_*_provider.rs`
ship a `render(include_str!(…))` pipeline whose only added byte is the
fake `// @generated by skinny bbnf-codegen; do not edit by hand.`
prefix. The delta this axis adds is that the **JSON** runtime profile
is no cleaner: `json_provider.rs` does the same `normalize(include_str!
("json_templates/X.rs"))` move with the same prepended fake header for
all eight roster files; only the trailing `json_sink_direct::render
(sink_only)` chunk appended to `generated.rs` is grammar-derived. The
hand-written / grammar-derived ratio in `generated.rs` is ~85% / ~15%
(391-line template vs ~70-line sink append).

This delta is a **scope extension, not a reversal**, of the V13 §7.1
row 1 'HONEST' verdict for `json_provider`. V13 correctly identified
the grammar-derived `parse_direct` sink chunk (~70 LOC appended to
`generated.rs` via `json_sink_direct::render` per
`skinny/crates/codegen/src/lib.rs:215-217`) as honestly grammar-
derived; V14 extends the scope to the **OTHER 5 emitted files**
(`config.rs`, `parser.rs`, `view.rs`, `value.rs`, `visitor.rs`) plus
the `include_str!()` template body of `generated.rs` itself, all of
which are pass-through templates with only the prepended `//
@generated` header added. V14 does not reverse V13's HONEST verdict on
the sink-derived chunk; it observes that the chunk is the minority
share (~15%) of the JSON provider's emitted bytes, while the pass-
through majority (5 of 6 emitted files + the template body of the
sixth) carries the same fake-`@generated` recurrence pattern as the
CSS L4 providers.

New findings (not enumerated in SK-V13 audit pack): **3.**

1. JSON also uses `include_str!()` of hand-written templates with the
   same fake `@generated` prefix (only the appended sink chunk is
   grammar-derived). Per the disambiguation above, this is a scope
   extension over V13's HONEST verdict (which addressed only the sink
   chunk), not a reversal of it.
2. Four of the seven CSS "scanners" short-circuit on a `const
   CANONICAL_FIXTURE` / `CAPTURED_W2_INPUT` byte-equality check and
   return a precomputed `CANONICAL_FACTS` blob (`nested_layout`,
   `at_rules_and_media`, `stylesheet_selectors`,
   `vendor_and_custom_atrules`). `css_l4_nested_layout
   _templates/generated.rs` is **49 lines total** — a hash-table
   lookup with `unsupported(0)` for every non-canonical input.
3. The 15 `grammar/css/l4/*.bbnf` files have **zero referents** from
   anywhere inside `skinny/` or root `xtask/` (only the totality-track
   root manifest cites `grammar/css/l4/stylesheet.bbnf` for the
   `css_l4` ident; the 14 other CSS L4 grammars are orphaned in both
   tracks).

## §1 — Methodology

Executable verification commands run + output quoted:

```
$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime crates/core/src/runtime
skinny/crates/runtime/src/grammars/css_l4_at_rules_and_media/config.rs
skinny/crates/runtime/src/grammars/css_l4_at_rules_and_media/generated.rs
skinny/crates/runtime/src/grammars/css_l4_at_rules_and_media/mod.rs
skinny/crates/runtime/src/grammars/css_l4_at_rules_and_media/parser.rs
skinny/crates/runtime/src/grammars/css_l4_at_rules_and_media/sink.rs
…
skinny/crates/runtime/src/grammars/json/config.rs
skinny/crates/runtime/src/grammars/json/generated.rs
skinny/crates/runtime/src/grammars/json/host.rs
skinny/crates/runtime/src/grammars/json/mod.rs
skinny/crates/runtime/src/grammars/json/parser.rs
skinny/crates/runtime/src/grammars/json/value.rs
skinny/crates/runtime/src/grammars/json/view.rs
skinny/crates/runtime/src/grammars/json/visitor.rs
```

42 files match. 35 belong to the seven CSS L4 providers (five files ×
seven providers, with `css_l4_declaration_values/` missing a `sink.rs`
since the declaration-values provider holds its sink in the parser
template). Eight belong to `json/`. Zero matches under
`crates/core/src/runtime` — the fake-`@generated` pattern is bounded
to the skinny tree.

```
$ ls -1 grammar/css/l4/*.bbnf | wc -l
      15
```

Expected 15, observed 15.

```
$ grep -n '"regen-' skinny/xtask/src/main.rs
18:        "regen-json" => regen_json(&root),
20:        "regen-real-typed" => regen_real_typed(&root),
```

Skinny xtask exposes exactly two regen entry points. There is no
`regen-css`, no `regen-sheets`, no `regen-css-l4`, no `regen-*` of any
kind for the seven CSS providers.

```
$ grep -rn "css/l4\|css_l4.*bbnf\|grammar/css" skinny xtask \
    | grep -v '/target/\|\.md:' | head
skinny/crates/bbnf-bench/src/lock14_baseline.rs:1615:  …diff fixture string…
skinny/crates/bbnf-bench/src/report.rs:6033:  || !matches!(row.grammar_id.as_str(), "css_l4" | "sheets" | "bbnf_self")
skinny/crates/bbnf-bench/src/report.rs:6279:  ("css_l4", "declaration_values") | …
skinny/crates/bbnf-bench/src/report.rs:6324:  if !matches!(grammar, "css_l4" | "sheets" | "bbnf_self") { … }
```

None of these are file-system references to `grammar/css/l4/*.bbnf`.
The bench/report hits are Lock 14 string-literal violations (out of
A4 scope, in A3 scope). The 15 `.bbnf` files at `grammar/css/l4/` are
load-bearing for **zero** code paths in `skinny/`.

```
$ git log --all --oneline --grep='regen.css\|regen-css'
(no output)
```

No commit in the repository has ever referenced a CSS regen command.

```
$ git log --all --oneline -- skinny/crates/runtime/src/grammars/css_l4_visual_functions/generated.rs
e7e3af22c feat(sk-v13-waveW4): admit CSS visual functions generated row
```

The CSS provider files were authored by hand inside the W4 admit
commit, not emitted by a regen pass. Identical provenance for the
template at `skinny/crates/codegen/src/css_l4_visual_functions_templates
/generated.rs`.

Files audited:

- `skinny/xtask/src/main.rs` (1303 lines; lines 1-176 + greps for
  `regen-` and `css`).
- `skinny/crates/codegen/src/lib.rs` (~780 lines; greps for
  `emit_runtime_profile`, `Json`, CSS provider names).
- `skinny/crates/codegen/src/json_provider.rs` (full, 101 lines).
- `skinny/crates/codegen/src/css_l4_visual_functions_provider.rs`
  (full, 51 lines).
- `skinny/crates/codegen/src/css_l4_*_templates/*.rs` (full
  enumeration; LOC table below).
- `skinny/crates/runtime/src/grammars/css_l4_at_rules_and_media/
  generated.rs` (lines 1-80).
- `skinny/crates/runtime/src/grammars/json/generated.rs` (lines 1-40).
- `Cargo.toml` (root; `[workspace.metadata.bbnf]` lines 18-29).
- `xtask/src/main.rs` (root; full, 65 lines).
- `xtask/src/regen.rs` (greps for `css`, `grammar/`).

## §2 — Per-finding ledger

### Round-trip table (per generated.rs file)

| Provider | runtime path | template path | grammar input | emission command | round-trip status |
| --- | --- | --- | --- | --- | --- |
| `json` | `skinny/crates/runtime/src/grammars/json/generated.rs` | `skinny/crates/codegen/src/json_templates/generated.rs` | `skinny/grammars/json.bbnf` (sink-only chunk only) | `cargo xtask regen-json` | template body NOT grammar-derived; only the tail `json_sink_direct::render(sink_only)` block is — partial round-trip |
| `css_l4_at_rules_and_media` | …/css_l4_at_rules_and_media/generated.rs | …/css_l4_at_rules_and_media_templates/generated.rs | none | none | NO ROUND-TRIP — `render(include_str!())` only |
| `css_l4_declaration_values` | …/css_l4_declaration_values/generated.rs | …/css_l4_declaration_values_templates/generated.rs | none | none | NO ROUND-TRIP |
| `css_l4_declaration_values_extended` | …/css_l4_declaration_values_extended/generated.rs | …/css_l4_declaration_values_extended_templates/generated.rs | none | none | NO ROUND-TRIP |
| `css_l4_nested_layout` | …/css_l4_nested_layout/generated.rs | …/css_l4_nested_layout_templates/generated.rs | none | none | NO ROUND-TRIP; `if input == CANONICAL_FIXTURE { return CANONICAL_FACTS }` shortcut |
| `css_l4_stylesheet_selectors` | …/css_l4_stylesheet_selectors/generated.rs | …/css_l4_stylesheet_selectors_templates/generated.rs | none | none | NO ROUND-TRIP; `if input == CAPTURED_W2_INPUT { return CAPTURED_W2_FACTS }` shortcut |
| `css_l4_vendor_and_custom_atrules` | …/css_l4_vendor_and_custom_atrules/generated.rs | …/css_l4_vendor_and_custom_atrules_templates/generated.rs | none | none | NO ROUND-TRIP; `if input == CANONICAL_FIXTURE { return CANONICAL_FACTS }` shortcut |
| `css_l4_visual_functions` | …/css_l4_visual_functions/generated.rs | …/css_l4_visual_functions_templates/generated.rs | none | none | NO ROUND-TRIP |

Template byte counts:

```
   3  css_l4_at_rules_and_media_templates/config.rs
 632  css_l4_at_rules_and_media_templates/generated.rs
   7  css_l4_at_rules_and_media_templates/mod.rs
  14  css_l4_at_rules_and_media_templates/parser.rs
 288  css_l4_at_rules_and_media_templates/sink.rs
…
  49  css_l4_nested_layout_templates/generated.rs
 264  css_l4_declaration_values_templates/generated.rs
 456  css_l4_declaration_values_extended_templates/generated.rs
 667  css_l4_stylesheet_selectors_templates/generated.rs
 675  css_l4_vendor_and_custom_atrules_templates/generated.rs
 433  css_l4_visual_functions_templates/generated.rs
3176  TOTAL css_l4_*_templates/generated.rs
```

### Per-finding ledger

| # | Severity | Finding | Citation | Status |
| --- | --- | --- | --- | --- |
| 1 | CRITICAL | All seven CSS L4 providers emit `generated.rs` by `render(include_str!("…_templates/generated.rs"))`, where `render` prepends `"// @generated by skinny bbnf-codegen; do not edit by hand.\n"` and emits the template body verbatim. The header is the only added byte; no IR / grammar / pattern lookup informs the output. | `skinny/crates/codegen/src/css_l4_visual_functions_provider.rs:42-51` (and the six sibling files) | CONFIRMS V13 (v1 §6 + v4 §3) |
| 2 | CRITICAL | The skinny xtask exposes no `regen-css*` subcommand. Subcommand list at `skinny/xtask/src/main.rs:8` is `<regen-json\|check-json\|regen-real-typed\|check-real-typed\|check-conformance\|lint-loc\|bench-json\|gate-json\|primitive-checkasm>`. The CSS providers cannot be regenerated; the round-trip test (delete + regen ⇒ byte-equivalent) is not even runnable. | `skinny/xtask/src/main.rs:8`, lines 16-30 dispatch table | CONFIRMS V13 (v4 §3) |
| 3 | CRITICAL | `css_l4_nested_layout_templates/generated.rs` is 49 lines total. The `emit_fact_stream` function is `if input == CANONICAL_FIXTURE { return Ok(CANONICAL_FACTS.to_string()); } let mut sink = FactSink::new(input); sink.unsupported(0)`. It is a single-key lookup table dressed as a parser. | `skinny/crates/codegen/src/css_l4_nested_layout_templates/generated.rs:1-49` | NEW (extension of V13 hand-written verdict) |
| 4 | CRITICAL | `css_l4_at_rules_and_media_templates/generated.rs:25-28` does the same `if input == CANONICAL_FIXTURE { return Ok(CANONICAL_FACTS.to_string()); }` shortcut before falling through to a hand-written `Scanner`. The fixture text is the exact 85-byte representative input the SK-V13 W10.1 ADMITTED row claims to parse at parity. | `skinny/crates/runtime/src/grammars/css_l4_at_rules_and_media/generated.rs:4-30` (mirror at `…/css_l4_at_rules_and_media_templates/generated.rs:1-30`) | NEW |
| 5 | CRITICAL | `css_l4_stylesheet_selectors_templates/generated.rs` uses `if input == CAPTURED_W2_INPUT { return Ok(CAPTURED_W2_FACTS.to_string()); }`. Same shortcut idiom under a "captured" rather than "canonical" name. | `skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/generated.rs:3-40` | NEW |
| 6 | CRITICAL | `css_l4_vendor_and_custom_atrules_templates/generated.rs:33-36` ditto: `if input == CANONICAL_FIXTURE { return Ok(CANONICAL_FACTS.to_string()); }`. | `skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_templates/generated.rs:3-36` | NEW |
| 7 | CRITICAL | Zero file-system references to `grammar/css/l4/*.bbnf` exist anywhere inside `skinny/` or root `xtask/`. The only reference in the repository is the totality-track root `Cargo.toml:22` line `{ ident = "css_l4", path = "grammar/css/l4/stylesheet.bbnf", … }`, which targets the totality root xtask + `crates/core/src/grammar/generated/` tree — orthogonal to the skinny providers under audit. 14 of 15 .bbnf files are orphaned from both tracks. | `Cargo.toml:18-29`; `grep -rn "grammar/css" skinny xtask` returns only string-literal / report comparator hits, no `include_str!` / `read_to_string` of the grammars | CONFIRMS V13 (v1 §6) |
| 8 | CRITICAL | The fake `@generated` header is added by `render` at `skinny/crates/codegen/src/css_l4_visual_functions_provider.rs:47-51` even though the template body is hand-authored and grows by hand. Each subsequent edit lands as a template body edit, then a re-emit prepends the same header, perpetuating the false claim. The mechanism is the same for `json_provider::normalize` at `skinny/crates/codegen/src/json_provider.rs:80-100` — only the prepended header line is added; the body is `include_str!()` verbatim. | `skinny/crates/codegen/src/json_provider.rs:80-100`; `skinny/crates/codegen/src/css_l4_visual_functions_provider.rs:47-51` | NEW (V13 noted CSS only; A4 finds JSON shares the pattern) |
| 9 | CRITICAL | The seven CSS provider modules in `skinny/crates/codegen/src/css_l4_*_provider.rs` are themselves a Lock-14 recurrence vector — each is a per-grammar provider inside a nominally-generic crate (`codegen`), wired via a per-grammar `RuntimeProvider::CssL4*` enum variant match at `skinny/crates/codegen/src/lib.rs:166-209`. This is the "no per-grammar code in shared crates" failure A3 + A6 also indict; here it co-causes the generator-truth failure because the providers ARE the only "emission" path and they emit only template bytes. | `skinny/crates/codegen/src/lib.rs:166-209`; 7 `css_l4_*_provider.rs` siblings + `json_provider.rs` | CONFIRMS V13 (v3 + audit pack body) |
| 10 | HIGH | JSON `generated.rs` is a mixed-source file: lines 1-(start-of-sink) come from `include_str!("json_templates/generated.rs")` (391 hand-written template lines), the trailing chunk comes from `json_sink_direct::render(sink_only)` consuming the grammar's lowered sink-only program. Only the trailing chunk passes the "grammar-derived" criterion. The whole file carries the `@generated` header, so the audit-grade truth is "partial round-trip" — round-trip would succeed only if the sink-emitter alone changed; any edit to template body would re-emit identically because nothing reads grammar for those lines. | `skinny/crates/codegen/src/lib.rs:211-227` (in particular line 215-217); `skinny/crates/codegen/src/json_provider.rs:60-78` | NEW |
| 11 | HIGH | `json_provider::generated_rs` (line 60), `parser_rs` (64), `view_rs` (68), `value_rs` (72) and `config_rs` (48) all do `include_str!("json_templates/X.rs")` and pass-through. None of these consult any grammar or IR. Only `host_rs` and `mod_rs` are inline string constants. | `skinny/crates/codegen/src/json_provider.rs:48-78` | NEW |
| 12 | HIGH | The 7 CSS `*_templates/generated.rs` files total 3176 LOC of hand-authored code carrying the fake `@generated` header after `render`. Plus the JSON `json_templates/generated.rs` (391 LOC) + `view.rs` (460) + `parser.rs` (69) + `value.rs` (172) + `visitor.rs` (38) + `config.rs` (19) = 1149 hand-authored JSON template LOC. **Total ~4325 LOC of hand-written code shipped under a fake `@generated` header inside the codegen crate alone**, before counting the 1909 LOC of identical-content twins re-emitted into `skinny/crates/runtime/src/grammars/{css_l4_*,json}/`. | `wc -l skinny/crates/codegen/src/{css_l4_*_templates,json_templates}/*.rs` | NEW (quantification of V13 finding) |
| 13 | HIGH | `cargo xtask regen-json` does run end-to-end and the codegen tests at `skinny/crates/codegen/src/lib.rs:338-349` assert determinism (`emission_is_deterministic`) and field-consumer overlap — but these tests verify only that the templates round-trip to themselves, not that any grammar input drives the template body. The "round-trip" thus passes trivially for JSON and **cannot pass at all for CSS** because no regen entry point exists. | `skinny/crates/codegen/src/lib.rs:338-349` | NEW |
| 14 | MED | The seven CSS `*_templates/` directories are sibling modules of the seven `css_l4_*_provider.rs` files inside the codegen crate root. The naming convention `<grammar>_provider.rs` + `<grammar>_templates/<roster>.rs` is itself the Pattern H recurrence vector projected into the codegen crate (separate dispatch issue from A3 which audits the runtime crate). PRUNE-3 needs to delete the per-grammar provider modules, not refactor them. | `find skinny/crates/codegen/src -maxdepth 1 -type d -name '*_templates'` returns 8 entries (7 CSS + 1 JSON) | NEW |
| 15 | MED | Identical-content twin files exist at `skinny/crates/runtime/src/grammars/<g>/generated.rs` + `skinny/crates/codegen/src/<g>_templates/generated.rs` for every grammar profile. The "template" + "runtime" pair is two copies of the same hand-written source — `render`/`normalize` adds one line. Git log shows both files in every CSS provider directory share the same authoring commit `e7e3af22c feat(sk-v13-waveW4): admit CSS visual functions generated row`. The "codegen" step is byte-for-byte mirroring + a comment prefix. | `git log --oneline -- skinny/crates/runtime/src/grammars/css_l4_visual_functions/generated.rs` vs `…/codegen/src/css_l4_visual_functions_templates/generated.rs` | NEW |
| 16 | LOW | `xtask` (root) `Cargo.toml:22` correctly wires the totality `css_l4` grammar to `grammar/css/l4/stylesheet.bbnf`, and `xtask::regen::run` at `xtask/src/regen.rs` reads that manifest. This is the only correct grammar-derived emission path in the repository touching CSS — but it lives in the **totality** crate tree (`crates/core/src/grammar/generated/`), not the skinny tree under audit. Noted to forestall a future redress that conflates the two regen pipelines under one `xtask`. | `xtask/src/main.rs:1-65`; `Cargo.toml:6-29` | CONFIRMS V13 (audit pack body; v3 noted boundary) |

## §3 — Pass criterion verdict

Quote of A4 row from `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md
§Scope`:

> **A4 Generator-vs-hand-curated** — For every generated module:
> round-trip test (delete + regen produces byte-equivalent). For every
> claimed grammar-derived parser: locate the grammar source + emission
> command. Pass criterion: 100% round-trip pass; every "generated.rs"
> comes from a real `cargo xtask regen` command + a real `.bbnf`
> grammar source.

**Declared: FAIL.**

- Round-trip pass rate: **0 / 7** for CSS L4 providers (no regen
  command exists); **1 / 1 partial** for JSON (regen runs; only the
  trailing sink chunk is grammar-derived).
- Real `.bbnf` source backing each `generated.rs`: **0 / 7** for CSS
  (the 15 grammars at `grammar/css/l4/*.bbnf` are orphaned from the
  skinny pipeline); **1 / 1** for JSON (`skinny/grammars/json.bbnf` is
  read by `regen_json` and consumed by sink-only lowering).

The criterion is violated on both halves for CSS and on the round-trip
half for JSON.

## §4 — Recommended prune actions

Cross-reference to SK-V14 contract (`SYNTHESIS.md` §3 C-1..C-5,
`HANDOFF.md` §7, `S-P0-DISPATCH-CONTEXT.md` §A4):

| Prune action | Maps to | Disposition |
| --- | --- | --- |
| **PRUNE-2** delete the 7 CSS hand-written template directories + their provider modules + their runtime twins. | Already enumerated in `S-P0-DISPATCH-CONTEXT.md §2` ("7 CSS hand-written template files with fake `@generated` header — to be addressed by PRUNE-2"). | CONFIRM scope; extend deletion target to include the parallel `json_templates/` directory's hand-written body if R4 (real CSS regen) lands first and validates the codegen pipeline can emit the JSON template body from `skinny/grammars/json.bbnf` end-to-end. |
| **R4** introduce real `regen-css-l4-<provider>` xtask subcommands that read `grammar/css/l4/*.bbnf`, lower through the IR + sink-only pipeline (extended to fact-stream emission), and emit `generated.rs` byte-deterministically. | `SYNTHESIS.md §3 C-4` ("R4 close CSS load-bearing gap"); `S-P0-DISPATCH-CONTEXT.md §A4` ("R4 + PRUNE-2 will close this"). | CONFIRM. Without R4, PRUNE-2 deletes the providers but leaves the 7 CSS ADMITTED rows unrecoverable; the order must be `R4 lands first → PRUNE-2 deletes hand-written twins`. |
| **PRUNE-3 + C-1** delete the per-grammar `<g>_provider.rs` modules and the `RuntimeProvider::*` enum arm match at `skinny/crates/codegen/src/lib.rs:166-209`; replace with a single grammar-driven dispatch keyed off the lowered IR profile. | `SYNTHESIS.md §3 C-1` ("delete per-grammar modules"); `S-P0-DISPATCH-CONTEXT.md §2` ("8 per-grammar provider modules in `skinny/crates/codegen/`"). | CONFIRM; this axis adds the codegen-crate per-grammar `<g>_templates/` sibling directories to the same deletion list (Finding 14). |
| **CH7 gating** every future wave-redress's claim of `// @generated` in a `generated.rs` must be backed by a regen subcommand + `.bbnf` source + a round-trip check in the matching `check-*` subcommand. | `PASS-0-OVERFIT-AUDIT.md §CH7` ("Every new code added is grammar-derived… never hand-written under a `// @generated` header"; "Every 'generated' output passes a round-trip test"). | NEW recommendation: extend `skinny/xtask/src/main.rs:8` USAGE line and CI to enforce the matched `regen-X` + `check-X` pair for every roster entry in every `GrammarProfile` (introduce a `check-all` aggregate). The current `check-json` + `check-real-typed` pair would extend to seven new `check-css-l4-<provider>` invocations after R4. |
| **Lock 14 reinforcement** add a clippy-lint or pre-commit grep that REJECTS any new `// @generated by skinny bbnf-codegen` header in `skinny/crates/runtime/src/grammars/**/*.rs` unless the matching path appears in a recognized regen subcommand's emission roster. | None yet; new lock-supporting lint. | NEW. The fake-header recurrence is the seam this axis indicts; without a mechanical guard the next SK-V{N+1} wave can re-introduce it. Recommend lifting to LOCKS.md as a Lock-14-companion lint. |
