# SK-V11 S-P2 V2 Challenge Consolidation

Pass: S-P2 Research CHALLENGE consolidation.
Cycle: V2.
Date: 2026-05-19.
Scope: consolidate the six S-P2 V2 challenge lenses.
Output: this file.

## Inputs

- Research cohort: `restart/skinny/tranches/sk-v11/research/p2/p2a-sota-teardown.md` through `p2f-grammar-neutral.md`, committed at `484a0617`.
- Challenge lenses: `restart/skinny/tranches/sk-v11/research/p2/hardening/V2/CH1.md` through `CH6.md`.
- Convergence authority: `restart/prompts/ORCHESTRATOR.md` §3Z and `restart/prompts/skinny/PASS-2-RESEARCH.md` §4.

## Disposition Matrix

| Lens | Disposition | Load-bearing accepted facts |
|---|---|---|
| CH1 correctness | ACCEPT | V2 candidates trace to S-P1 hot leaves; comparator strictness is bounded; ISA claims are AArch64-only and grounded. |
| CH2 generality / Lock 14 | ACCEPT | Every retained parser candidate carries a grammar-neutral, per-grammar-template, host-function, oracle-only, or accounting-only verdict; the live `json_provider` codegen shape remains an S-P3 gate before any CSS/Sheets/BBNF-self generality claim. |
| CH3 regression / REDRESS | ACCEPT | V1 reopenings are closed: x4 Unicode work is proof-only, string-block widening is caller-gated, W3/class-column/sidecar routes stay pre-blocked, and retained parse callers are guards only. |
| CH4 cost | ACCEPT | Retained candidates now carry scalar reference status, parity/checkasm expectation, micro-proof, same-wave consumer, feature/fallback, output plane, and reject boundary. |
| CH5 hidden coupling / Lock 1 | ACCEPT | V2 preserves the existing offset tape plus direct/typed consumer union; no parallel substrate, retained cursor, class lane, sidecar, or Track-1/Track-2 coupling is admitted. |
| CH6 anti-paper-close | ACCEPT | V2 grounds the candidate pool at S-P2 depth and routes incomplete SIMD, digest/hash, and x4 escape surfaces as proof/oracle-only rather than future-promise admissions. |

## Cycle Verdict

ACCEPT-rate: 6/6 = 100%.

REJECT list: none.

REVISE list: none.

Open critical defects: none.

Cycle verdict: ACCEPT, first consecutive accepting cycle for S-P2. This does
not converge the pass because V1 was 0/6; `ORCHESTRATOR.md` §3Z requires two
consecutive ≥95% ACCEPT cycles. V3 must therefore run as a stability fold and
second CHALLENGE cycle before S-P3 dispatch.

## Required V3 Fold

No substantive redress is required. The V3 research dispatch must preserve the
V2 candidate pool, update cycle markers, and explicitly carry forward these
facts:

1. C1-C7 are the parser primitive pool; C8 is benchmark/oracle or per-product
   host sink only; C9 is Lock-1/output-plane accounting only.
2. `HEX_QUARTET_X4_PROOF` is proof-only until a new source delta, scalar x4
   oracle, strict checkasm, and direct/typed/non-JSON same-wave consumer exist.
3. `output_digest_hash` is not parser vocabulary and cannot enter generic
   parser crates.
4. The W3 union/class-column/streaming-cursor family remains REDRESS-closed.
5. The live JSON-provider codegen path is a Lock 14 blocker for any claimed
   non-JSON generated parser wave unless the wave replaces it with a
   grammar-agnostic generated-runtime template.
