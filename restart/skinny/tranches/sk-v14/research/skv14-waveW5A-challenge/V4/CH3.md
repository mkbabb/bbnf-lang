# SK-V14 W5A CHALLENGE V4 CH3: Regression and Wave Graph
Disposition: ACCEPT
Acceptance score: 97/100
Findings:
- ACCEPT: REDRESS-184 is not reopened. REDRESS-184 rejected deletion of the seven CSS L4 providers/templates while `cargo xtask regen-css` still compiled through those provider modules and failed with `error[E0583]` before regeneration could run. W5A keeps CSS provider modules, CSS template directories, root CSS runtime, and grammar CSS source outside owner paths, requires provider/template counts to remain at 8 providers and 7 template directories, fails provider/template add/delete/rename diffs, and pre-blocks deleting or renaming CSS providers/templates before W5B.
- ACCEPT: REDRESS-209 is not reopened. REDRESS-209 rejected monolithic W5 because provider/template deletion and Lock 14 closure were paired with a non-existent source-consuming generator: `regen-css` still used `codegen::emit_runtime_profile(target.profile)`, source and metadata were freshness-only inputs, CSS L4 `->` and `@{...}` constructs were rejected, and static centralization would only hide hand-written bodies. W5A directly owns the missing capability by introducing one grammar-neutral `RuntimeGenerationRequest` path carrying grammar source plus workspace metadata, requiring CSS L4 runtime-generation constructs to parse without a CSS-specific generic branch, migrating `regen-css` plus the seven companions to the source-consuming path, and leaving deletion to W5B after W5A admits.
- ACCEPT: NEW-CH3-V4-01 deletion/rebuild ordering holds. The delete-target/rebuild pairs are ordered as rebuild capability before deletion: W5A proves source-consuming rebuild capability and explicitly forbids provider/template deletion; W5B deletes CSS provider/template clusters only after W5A admission and proof that all seven CSS profiles and companions already pass through the W5A path; W6 root-runtime collapse starts only after W5B admission and runs the destructive `rm -rf crates/core/src/runtime/css_l4 && cargo xtask regen-css && git diff --exit-code -- crates/core/src/runtime/css_l4` gate at W6.0.
- ACCEPT: The wave graph is coherent. W5A ADMIT unlocks W5B only, while W5A REJECT blocks W5B, W6, W7, W8, W9, and W10 until reroute. SPEC matches this chain: W5B is conditional on W5A, W6 is conditional on W5B, W7 is conditional on W5B plus W6, and W8/W9/W10 remain globally blocked until PRUNE-1 through PRUNE-5 close. W9 and W10 local prerequisites do not contradict the global PRUNE block.
- ACCEPT: V3 carries forward without CH3 regression. V3 already accepted the same graph: W5A proves rebuild capability, W5B owns deletion after W5A admission, W6 waits for W5B, and W8-W10 remain globally blocked until PRUNE-1 through PRUNE-5 close. V4 source checks found no new wave-graph contradiction.
Required folds:
- NONE
Evidence:
- `skinny/REDRESS.md:5105`-`5118`: REDRESS-184 rejection records provider/template deletion before live `regen-css` replacement, the `error[E0583]` failure, and the downstream PRUNE/new-admit block.
- `skinny/REDRESS.md:5173`-`5193`: REDRESS-209 rejection records the source-consuming generator gap, profile-only `emit_runtime_profile(target.profile)`, freshness-only source/metadata, CSS L4 parser gaps, static-centralization risk, and the W5A then W5B supersession route.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:18`: W5A intervention is one grammar-neutral `RuntimeGenerationRequest` path carrying grammar source plus workspace metadata and leaving CSS provider/template deletion to W5B.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:29`-`35`: W5A explicit non-owner paths include CSS provider modules, CSS template directories, skinny CSS runtime output except generated `regen-css`, root `crates/core/src/runtime/css_l4/`, and `grammar/css/l4/`.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:39`-`45`: W5A gates require `regen-css` and all seven companions to call the new request path, parse CSS L4 source facts without profile-specific branching, preserve JSON output, prove Sheets/BBNF-self through the same path, keep provider/template counts unchanged, and avoid W5B/W6 budget borrowing.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:87`-`106`: command/grep gates reject the old `emit_runtime_profile(target.profile)` boundary, require positive `RuntimeGenerationRequest|emit_runtime_from_request` evidence, keep provider count at 8 and CSS template count at 7, reject provider/template A/D/R diffs, cap W5A LOC, and require no diff under root CSS runtime or grammar CSS source.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:111`-`123`: W5A revert retains the provider/template mesh, same-wave consumers exercise the source-consuming request, W5A ADMIT unlocks only W5B, W5A REJECT blocks W5B through W10, and deletion/root-runtime edits remain pre-blocked.
- `restart/skinny/tranches/sk-v14/SPEC.md:241`-`248`: wave manifest orders W5A before W5B, W5B before W6, W6 before W7, and W8/W9/W10 after the PRUNE chain.
- `restart/skinny/tranches/sk-v14/SPEC.md:656`-`668`: W5A entry/tasks require source plus metadata, parser capability, `regen-css` migration, non-CSS proof, and a temporary guard forbidding provider/template deletion until W5B.
- `restart/skinny/tranches/sk-v14/SPEC.md:672`-`699`: W5A exit/pre-block/downstream gates require source-consuming emission, same-wave CSS consumers, provider/template retention, no W5B/W6 borrowing, and global block of new-admit waves on W5A rejection.
- `restart/skinny/tranches/sk-v14/SPEC.md:715`-`724`: W5B entry and tasks require W5A admission before deleting the seven CSS L4 provider modules and seven CSS L4 template directories.
- `restart/skinny/tranches/sk-v14/SPEC.md:729`-`753`: W5B exit/same-wave/revert/downstream gates require deleted provider/template counts to be zero, `regen-css` through the W5A path, all seven companions, and W6/W7 blocking on W5B rejection with W8/W9/W10 globally blocked.
- `restart/skinny/tranches/sk-v14/SPEC.md:775`-`807`: W6 starts only after W5B admits and owns the W6.0 CSS root-runtime destructive rebuild gate.
- `restart/skinny/tranches/sk-v14/SPEC.md:843`-`847`, `restart/skinny/tranches/sk-v14/SPEC.md:905`-`909`, `restart/skinny/tranches/sk-v14/SPEC.md:932`, `restart/skinny/tranches/sk-v14/SPEC.md:990`-`994`, `restart/skinny/tranches/sk-v14/SPEC.md:1049`-`1058`: W6/W7 downstream blocks and W8/W9/W10 entry gates preserve the global PRUNE-before-new-admit order while keeping W9/W10 local prerequisites non-conflicting.
- `restart/skinny/tranches/sk-v14/HANDOFF.md:11`-`16`: handoff records REDRESS-184/209 and the W5 split into W5A generator capability plus W5B provider/template deletion.
- `restart/skinny/tranches/sk-v14/HANDOFF.md:167`-`188`: handoff states W5A does not delete provider/template surfaces, W5B inherits deletion only after W5A admits, root CSS runtime remains W6.0, and W8/W9/W10 stay blocked until PRUNE-3/4/5 close.
- `restart/skinny/tranches/sk-v14/DISPATCH-PROMPT.md:173`-`179` and `restart/audit/totality/astral/V5/ΩB-skinny-lessons.md:44`-`52`: CH3 must grep delete-target/rebuild-capability pairs, and rebuild capability must precede deletion unless the same wave first proves the replacement load-bearing.
- `restart/skinny/tranches/sk-v14/research/skv14-waveW5A-challenge/V3/HARDENING-SKV14-W5A-V3-CONSOLIDATED.md:31`-`33`: prior clean cycle accepted the same CH3 wave graph and deletion ordering.
- Review commands run: `rg -n "W5A|W5B|W6|W8|W9|W10|REDRESS-184|REDRESS-209|NEW-CH3|delete|deletion|rebuild|blocker|unlock" ...`; `nl -ba ... | sed -n ...` for the cited plan, SPEC, HANDOFF, DISPATCH, lesson, and REDRESS ranges; `ls -la restart/skinny/tranches/sk-v14/research/skv14-waveW5A-challenge/V4`; `git status --short restart/skinny/tranches/sk-v14/research/skv14-waveW5A-challenge/V4`.
