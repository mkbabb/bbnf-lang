# T-P2 V3 Hardening Consolidated

Pass: T-P2 Research. Cycle: V3.
Date: 2026-05-21.
Scope: six-lens challenge verdict for the V3 folded totality research packet.
Output: this file.

## Verdict

`G-T-P2-V3-CHALLENGE`: REVISE.

V3 folded the V2 provenance and cost-shape defects into a much tighter packet:
the generic source roots are pinned or source-dated, the dossiers carry
`counted_source_ids`, the admission-state vocabulary is normalized, the shared
2B/2E/2F ledger exists, and the decision-engine caps are numeric or
dereferenceable. The packet still misses convergence on two executable details:
2E's counted source IDs do not match its local source registry, and the shared
ledger still leaves several command/consumer cells and REDRESS reopen slices
for S-P3 interpretation.

Acceptance rate: 4/6 = 66.7%.
Consecutive accepted cycles: 0.

| Lens | Disposition | Load-bearing finding |
|---|---|---|
| CH1 correctness / provenance | REVISE | V3 fixes the source-pin and count-arithmetic defects, but `2E-host-arch-esoterica.md` counts `SRC-INTEL-X86`, `SRC-FFMPEG`, and `SRC-DAV1D` while its source registry defines `SRC-X86-INTEL` and has no `SRC-FFMPEG` / `SRC-DAV1D` rows. V4 must align the counted IDs and mark support-only rows explicitly. |
| CH2 generality / Lock 14 | ACCEPT | The V3 fold preserves grammar-neutral transfer: executable rows remain generated-policy or caller-data owned, non-JSON CSS/Sheets pressure is retained, and the addendum does not narrow the research packet to JSON. |
| CH3 regression / REDRESS | ACCEPT | V3 keeps REDRESS-119, PMULL/CSSC, union, parse-that, and JSON/direct routes fenced as future candidates requiring fresh material differentials and measured gates; no historical rejection is silently reopened. |
| CH4 cost / executability | REVISE | The ledger schema and numeric caps are acceptable, but `tbl_tbx_escape_decode_batch` still defers the strict command, several rows name generic consumer families instead of first consumer paths or non-admit blockers, and REDRESS reopen ownership is still candidate-family-local rather than slice-local. |
| CH5 hidden coupling / substrate ownership | ACCEPT | `substrate_target`, `retention_lifetime`, and `policy_owner` fields prevent retained sidecars, public substrates, new BIR / `BackendShape`, and parser-owned cursor streams from entering through the research ledger. |
| CH6 anti-paper-close | ACCEPT | Source pins, source counts, microbench rows, and ledger entries are not treated as admissions; future gates still require strict equality, same-wave consumers, row movement or measured rejection, rollback, and G-Omega discipline. |

## Required V4 Fold

1. Repair 2E's provenance mapping. Either add exact registry rows for the
   counted IDs or change the frontmatter and V3 count convention to the actual
   registry IDs. Decide whether `SRC-REDRESS` and `SRC-BBNF-A64` are counted
   primary evidence or support-only evidence.
2. Replace deferred executable ledger cells with exact commands / first
   consumer paths, or mark the candidate non-shortlist until the cell exists.
   Required cells include `tbl_tbx_escape_decode_batch`,
   `escape_mask_64`, `digit_run_accumulate_udot`,
   `pmull_cssc_structural_union_emit64`, `string_context_64`, and
   `cache_hint_prefetch_store`.
3. Add a REDRESS-slice ownership table with:
   `redress_slice_id | prior REDRESS row(s) | candidate_id | owner |
   first_consumer_path | expected_row_gate | loc_budget | rollback_path |
   abrogate_threshold | blocker`. Every REDRESS-119 reopen row,
   PMULL/CSSC/union family, source-present primitive family, string/digit
   route, and parse-that import route must resolve to a candidate row or an
   explicit architectural block.
4. Keep `admissibility_state` reserved for the normalized enum and keep
   non-authoritative prose labels under `disposition`, `blocker`, or
   `summary_status`.

## Disposition

T-P2 does not converge on V3. Fold the required repairs into V4, commit the V4
research packet, then rerun the full six-lens challenge. The next accepted
cycle, if any, starts the two-cycle convergence count at one.
