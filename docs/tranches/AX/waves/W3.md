# AX.W3 — Subsystem Closures + W0b Investigation Queue Resolution

**Opens after**: W2 close
**Agents**: 3 parallel (closure tests + analysis, gorgeous + pprint-vm + imports, W0b investigation-queue resolution)
**Hard gate**: All ignored tests un-ignore OR delete with documented rationale; ignored count = 0; `cargo test --workspace` green.

## Scope

Inherited from the old AX's subsystem-closure ledger (AX-original-plan §Phase 11) plus W0b's investigation-queue items.

1. **Closure language-feature closure** (5 tests): `closure_lambda_param`, `closure_let_binding`, `closure_grammar_call`, `closure_recursive_param`, `closure_lower_expression`. Feature-completion of AT-era first-class closures + `GrammarCall` + `@host` directive work.
2. **Analysis structural-mode** (4 tests): cycle detection, alias detection, diagnostics surface under structural mode (post-AE). Legacy nominal-mode paths still work; structural-mode equivalents weren't ported when AE restructuring landed.
3. **Gorgeous fixture tests** (3 tests): reference non-checked-in snapshot files. Decide per test: commit snapshot OR delete test.
4. **pprint-vm hints** (2 tests): `softbreak_after_indent`, `indent_group_no_break` drifted post-AT pprint refactor. Re-snapshot OR fix.
5. **Imports** (`test_selective_transitive_unfurling`): conditional per AW-V.W5.5 disposition.
6. **W0b investigation queue**: resolve five items Agent 2 deferred to this wave:
   - (a) `emitter/visitor.rs` (~360 LOC): does any grammar use `@visitor` directive on master? If no → delete + `VisitorDescriptor` IR type.
   - (b) `passes/transform/{alias,fuse,inline,optimize,prune}` (~500-800 LOC): call-graph trace from `pipeline/compile.rs`. Orphans delete.
   - (c) `pattern_alphabet.rs:369,383 #[allow(dead_code)]` (~30 LOC): retire gated internals + tests if superseded by shape_dispatch.
   - (d) `crates/csp-solver/src/py.rs` + `feature = "py"`: workspace vs csc411 upstream — orchestrator decision.
   - (e) `parse-that/parsers/css/*` (1,625 LOC): out-of-workspace; flag for parse-that orchestrator (not in-wave work).

## File bounds

Modify / delete (per investigation outcome):

| File | Access |
|---|---|
| `crates/ir/src/lower/expression.rs` | modify (closure-captured value_expr lowering) |
| `crates/analysis/src/state/structural/` | modify (cycle/alias ports) |
| `crates/gorgeous/tests/fixtures/` | commit OR delete |
| `crates/pprint/src/vm/` | modify (softbreak/indent_group fix) |
| `crates/core/src/imports.rs` | modify (selective-transitive resolver, conditional) |
| `crates/core/src/backend/rust/emitter/visitor.rs` | investigate → likely delete |
| `crates/ir/src/passes/transform/{alias,fuse,inline,optimize,prune}.rs` | investigate → per-file retain/delete |
| `crates/ir/src/passes/recognizers/pattern_alphabet.rs` | modify (retire gated internals) |
| `crates/csp-solver/src/py.rs` + Cargo.toml | investigate |

## Phase sub-items

### W3.1 Closure + analysis

Un-ignore 5 closure tests + 4 analysis tests. Feature-complete the AT-era closure lowering path for the `lower::expression` gap; port AE nominal-mode analysis to structural-mode. Per-test: specific failure mode recorded in the un-ignore commit.

### W3.2 Gorgeous + pprint-vm + imports

Per-test commit-or-delete with documented rationale. Imports conditional: if AW-V.W5.5 fixed it, this sub-phase is no-op.

### W3.3 `emitter/visitor.rs` disposition

`nm target/release/deps/*_monolithic-*` search for `*_visitor_*` symbols. If absent → delete `emitter/visitor.rs` + `VisitorDescriptor` IR type + associated tests. If present → identify consumer, document, retain.

### W3.4 `transform/` orphan sweep

Call-graph trace: `pipeline/compile.rs` → ... → `passes/transform/*`. Any module reachable ONLY through an unreachable entry → delete. Likely candidates: `alias.rs`, `fuse.rs`, `fuse_token.rs`, `inline.rs`, `optimize.rs`, `prune.rs` (Agent 2 flagged these). Retain: `hoist_recurring_patterns` (live at `pipeline/compile.rs:581`).

### W3.5 `pattern_alphabet.rs` gated internals

The two `#[allow(dead_code)]` at `:369,383`. Retire if tests-only consumers are superseded by shape_dispatch tests. Document rationale.

### W3.6 csp-solver py feature

Workspace uses csp-solver without `feature = "py"`. Upstream csc411 uses it. Decision: if AX keeps the csc411 vendor, retain feature + file; if the workspace owns the crate, retire feature + file. Orchestrator question.

## Hard gate

1. Zero `#[ignore]` annotations remaining in workspace (`grep -rE '#\[ignore\]' crates/ --include '*.rs'` returns zero).
2. All 14+ inherited ignored tests un-ignored OR deleted with rationale.
3. W0b investigation queue resolved per item; each closes with either a delete-commit or an explicit retain-rationale.
4. `cargo test --workspace` green.
5. W2 parity harnesses still green.

## Verification artefacts

- Per-test commit log with rationale in `docs/tranches/AX/PROGRESS.md` §W3.
- `grep` output proving zero `#[ignore]`.
- `cargo test --workspace` output archive.

## Dependencies

- Depends on: W2 (parity harnesses gate subsystem-closure changes).
- Blocks: W4 (optimization waves presume clean test surface).

## Archaeology

Subsystem-closure items have histories spanning AE (analysis structural-mode), AT (closures), AV (gorgeous fixtures, pprint-vm). `aw5-n1-egraph-rewrite-codegen.md` and `V-to-X-learnings.md` both flag the five W0b investigation-queue items as deferred. This wave closes the ledger before optimizations compound the test surface.
