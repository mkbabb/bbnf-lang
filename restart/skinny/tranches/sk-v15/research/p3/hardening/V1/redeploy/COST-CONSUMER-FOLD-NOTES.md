# SK-V15 S-P3 V2 Fold Notes - CH4/CH6 Cost And Consumer Repair

Pass: S-P3 hardening V2 fold support.
Owned output: `restart/skinny/tranches/sk-v15/research/p3/hardening/V1/redeploy/COST-CONSUMER-FOLD-NOTES.md`.
Inputs read: `p3a-candidate-shortlist.md`, `p3b-wave-sequencing.md`, `p3c-falsifiability-gates.md`, `SPEC.md`, `DISPATCH-PROMPT.md`, `hardening/V1/CH4.md`, and `hardening/V1/CH6.md`.
Scope: concrete fold recipe for CH4 cost and CH6 anti-paper-close revisions.

## Verdict

Fold CH4 and CH6 by replacing the current W0-W9 executable packet with a final W0-W11 map. The packet has exactly two spare top-level wave slots under the 12-wave escalation ceiling. Use both slots to split W5 and W7. Do not add a thirteenth wave. If another split is needed, the affected receiver must record intrinsic block or one existing wave must be removed or merged without increasing redress scope.

Final map:

| Final wave | Receiver | V1 source |
|---|---|---|
| W0 | Baseline and telemetry lock | W0 |
| W1 | CSS admission honesty | W1 |
| W2 | Lock 14 / Lock 16 gate restoration | W2 |
| W3 | Codegen leak abrogation | W3 |
| W4 | Pattern H generated discipline | W4 |
| W5 | CSS typed Value provider | first half of W5 |
| W6 | CSS same-workload retiming and old-proof retirement | second half of W5 |
| W7 | Decision Engine spine | W6 |
| W8 | BackendShape lowerers A: fixture harness, EagerTape, OffsetTape | first half of W7 |
| W9 | BackendShape lowerers B: EventTape, SinkOnly, CollapsedStage, all-five gate | second half of W7 |
| W10 | FNV quarantine | W8 |
| W11 | Close reconciliation and PASS-IMPL V2 handoff | W9 |

## Cost Matrix To Fold Into P3-B, SPEC, And DISPATCH

Add these columns to the P3-B wave table and SPEC Section 2: `Risk class`, `Manual source/test LOC budget`, `Generated LOC status`, `Docs/ledger LOC budget`, `Phase caps`, `Split trigger`, and `Same-wave consumer`.

Manual LOC excludes generated output. Generated LOC may be large, but it must be deterministic, attributed to a named generator, and checked by a non-writing command or regenerated in the same wave. Docs/ledger LOC means wave research, plan, REDRESS rows, RESULTS/rolling delta/HANDOFF edits, and gate reports.

| Wave | Risk class | Manual source/test LOC budget | Generated LOC status | Docs/ledger LOC budget | Phase caps | Split trigger | Same-wave consumer |
|---|---|---:|---|---:|---|---|---|
| W0 | Medium ledger/gate | 60-160 | None; behavior output must be unchanged. | 80-180 | Research <=20m; plan <=15m; redress <=30m. | Any parser/codegen/runtime behavior diff, or telemetry schema work exceeding gate/report only. | `cargo xtask gate-json --check-results` or SK-V15 successor consumes all SK-V15 telemetry fields and rejects CSS broadcast. |
| W1 | Medium ledger/gate | 80-200 | None; CSS provider files remain live unless W5-grade typed proof lands. | 80-180 | Same caps. | Any independent CSS retiming, provider deletion, or typed output implementation. | CSS report/gate consumes `broadcast_group_id`, measurement identity, and diagnostic/non-admit status. |
| W2 | High gate repair | 120-280 | None unless gate fixtures are generated by a test helper; generated fixtures do not count as scan coverage. | 80-180 | Same caps. | More than one coherent scan/report repair family, or SIMD primitive rewiring beyond classify/delete/scalar-delegate status. | Lock 14/16 gates consume their own exclusion reports and primitive manifest; self-exempting scans fail. |
| W3 | High codegen prune | 150-320 | Generated runtime diffs allowed only as proof outputs, not manual source. | 80-180 | Same caps. | More than one leak family, JSON-adjacent generated behavior needing full 51-row rerun, or CSS provider deletion before W5. | `cargo xtask` regen/check path consumes changed generator, plus leak grep and JSON guard when JSON-adjacent. |
| W4 | High provenance/generator | 120-280 | 67 Pattern H files may be regenerated/provenance-bearing; generated LOC is uncapped but must round-trip. | 80-180 | Same caps. | Destructive root runtime deletion, inability to prove non-writing regen/check, or any behavior diff. | Pattern H gate consumes line-1 provenance and regeneration/check inventory for all 67 files. |
| W5 | High CSS provider build | 220-440 | CSS generated runtime/provider output allowed only from grammar/generator source; no `CSS_GENERATED_RS` string-literal body. | 80-180 | Same caps. | Same-workload retiming, RESULTS mutation, old-proof retirement, or >440 manual LOC. | CSS typed parser/value/document/view/visitor tests plus one gate-consumed aggregate diagnostic row consume the new provider. |
| W6 | High CSS measurement/retire | 160-340 | Optional generated CSS rows only if produced by W5 provider and uniquely measured. | 100-220 | Same caps. | New provider construction, more than aggregate plus bounded feature-row retime, or JSON guard drift requiring implementation repair. | CSS same-workload bench/report/gate consumes typed output; `cssparser` comparator plane and old-proof absence scans are gate inputs. |
| W7 | High Decision spine | 140-300 | Generated selection fixture diff is allowed as evidence; generated runtime bodies are not the main deliverable. | 80-180 | Same caps. | Any lowerer implementation beyond fixture hooks, or e-graph/CSP change not consumed by tests/gate in this wave. | Decision gate/tests consume e-graph rewrite and CSP output and fail against the old scaffold. Exact commands listed below. |
| W8 | High lowerer A | 180-360 | Fixture-generated output for EagerTape and OffsetTape allowed; each diff must be runtime-relevant. | 80-180 | Same caps. | Any EventTape/SinkOnly/CollapsedStage implementation, all-five gate closure, or >2 lowerers. | EagerTape and OffsetTape regenerate/test fixtures consume lowerers and fail against previous label-string scaffold. |
| W9 | High lowerer B | 220-420 | Fixture-generated output for EventTape, SinkOnly, and CollapsedStage allowed; all-five report generated by gate. | 100-220 | Same caps. | Any Decision spine rewrite/CSP work, FNV work, or missing all-five gate. | EventTape, SinkOnly, CollapsedStage fixtures plus all-five lowerer gate consume every lowerer or its rejected alternative. |
| W10 | Medium quarantine/gate | 100-240 | None unless adversarial fixtures are generated by tests. | 80-180 | Same caps. | Production runtime FNV migration or broad typed-product rewrite. | Strict-product gate consumes `fnv_quarantine=bench-only`, production FNV scan, and adversarial semantic fixtures. |
| W11 | High close/audit | 0-100 | None; generated diffs are evidence from prior waves only. | 200-420 | Same caps. | Any implementation fix, measurement repair, or unresolved dependency row. | PASS-IMPL V2 consumes close packet, HEAD command output, dependency table, generated diffs, strict manifests, and cold measurements. |

## W5/W6 Split Requirements

W5 is provider construction only:

- Build CSS typed value, document, view, and visitor surfaces.
- Keep old CSS provider/parser proof live but diagnostic unless same-wave W6-grade proof is also completed.
- Add tests that prove output is typed value/document output, not a fact stream, string, `CssFullParseSummary`, or brace counter.
- Add one gate-consumed aggregate diagnostic row if needed to prove the new provider is wired. This row is not an independent 24-feature admit.

W6 is measurement and retirement only:

- Retest Track 1 typed CSS output against same-workload `cssparser`.
- Retire `CSS_GENERATED_RS`, `CssFullParseSummary`, fact-stream-only `parse()`, and brace-counter live admission proof only after W5 provider proof is consumed.
- Update RESULTS/REDRESS/rolling delta/HANDOFF for aggregate or independently measured rows.
- Keep every optional per-feature CSS row distinct by `measurement_row_id`, `measurement_origin`, sample count, profile artifact, and comparator workload.

## Decision Spine Consumer Fold

After reindexing, the V1 W6 Decision spine becomes W7. Fold SPEC, DISPATCH, and P3-C to name these exact consumers.

Required same-wave consumers for W7:

| Consumer | Required shape | Existing or new command shape |
|---|---|---|
| E-graph rewrite test | A test proves `egraph_rewrite_count >= 1` and selected plan or generated fixture changes when the rewrite is enabled. It must fail against zero-rule scaffold. | `cargo test -p passes decision_egraph_rewrite_changes_selected_shape -- --exact` |
| CSP rejection test | Removing a required fact changes satisfiability or selected candidate. It must fail against the current preserve-only CSP. | `cargo test -p passes decision_csp_rejects_missing_required_fact -- --exact` |
| Grammar-neutral fact scan | Generic decision facts contain no `json_*` or `css_*` driver names. | `rg -n "json_|css_" skinny/crates/passes/src/backend_egraph.rs skinny/crates/passes/src/decision_csp.rs` must return no driver facts except test fixtures or explicit negative assertions. |
| Gate/report consumer | The bench gate consumes the Decision report and rejects scaffold status fields. | `cargo xtask gate-json --check-results --skv15-decision-spine-report <path>` or a committed successor flag consumed by `gate-json`. |
| Generated-selection fixture | A generated fixture records changed selection or emits a gate-consumed rejected alternative. | `cargo test -p codegen decision_spine_changes_generated_selection_fixture -- --exact` or equivalent fixture test in `passes` if codegen is untouched. |

Do not let W7 close on "can change generated behavior" prose. The test or gate must consume actual e-graph/CSP output in the same wave. If no generated/runtime selection can be affected inside W7, W7 must record intrinsic block and W8/W9 cannot cite Decision activation as admitted.

## Split Lowerer Consumer Fold

After reindexing, V1 W7 lowerers split into W8 and W9.

W8 owns fixture harness, EagerTape, and OffsetTape:

| Lowerer | Required same-wave consumer | Command shape |
|---|---|---|
| Fixture harness | A shared lowerer fixture that fails against the previous label-string scaffold. | `cargo test -p codegen backend_lowerer_fixture_rejects_label_string_scaffold -- --exact` |
| EagerTape | Generated runtime fixture contains eager payload writes or a gate-consumed rejected alternative explaining why EagerTape is not admissible. | `cargo test -p codegen lower_eager_tape_emits_runtime_relevant_diff -- --exact` |
| OffsetTape | Generated runtime fixture contains offset tape emission/traversal or a gate-consumed rejected alternative. | `cargo test -p codegen lower_offset_tape_emits_runtime_relevant_diff -- --exact` |

W9 owns EventTape, SinkOnly, CollapsedStage, and the all-five gate:

| Lowerer | Required same-wave consumer | Command shape |
|---|---|---|
| EventTape | Generated runtime fixture emits ordered events or a gate-consumed rejected alternative. | `cargo test -p codegen lower_event_tape_emits_runtime_relevant_diff -- --exact` |
| SinkOnly | Generated runtime fixture emits sink-only/direct output without tape payload, or a gate-consumed rejected alternative. | `cargo test -p codegen lower_sink_only_emits_runtime_relevant_diff -- --exact` |
| CollapsedStage | Generated runtime fixture collapses an admissible stage or records a gate-consumed rejected alternative. | `cargo test -p codegen lower_collapsed_stage_emits_runtime_relevant_diff -- --exact` |
| All-five gate | Gate reports all five BackendShape variants as implemented or explicitly rejected by executable alternative; no placeholder labels. | `cargo xtask gate-json --check-results --skv15-backend-lowerers-report <path>` or committed successor flag consumed by `gate-json`. |

Each lowerer wave must include a negative fixture proving the previous scaffold would pass compilation but fail the new gate. Row movement claims are forbidden unless generated runtime evidence and JSON/CSS maintain gates run in the same wave.

## W9 Anti-Deferral Close Wording Fold

After reindexing, close is W11. Replace the current close phrase "PASS-IMPL V2 accepts every axis or routes SK-V16 prune inputs" with:

```text
PASS-IMPL V2 closes SK-V15 only when each axis is ACCEPT at HEAD or is recorded as a row-level intrinsic block with HEAD command output, generated diffs/manifests where relevant, strict parity/checkasm where relevant, and cold measurements where behavior changed. SK-V16 routing is routed remainder after that proof; it is not close evidence and cannot substitute for an SK-V15 repair.
```

Also fold this into DISPATCH W11:

```text
Redress closes only when PASS-IMPL V2 consumes the close packet and every dependency row is admitted, reverted with REDRESS, or intrinsically blocked by row-level proof. Any implementation fix, measurement rerun, or unresolved dependency row discovered during W11 aborts close and spawns the owning repair wave; it is not deferred to SK-V16.
```

## P3-C Alignment Fold

Rewrite P3-C so it no longer says P3-B does not exist. The gate file must enumerate W0-W11 directly. Required receiver moves:

- Old W5 CSS API gate splits into W5 provider output and W6 same-workload retime/retirement.
- Old W6 combined Decision gate becomes W7 Decision spine only.
- Old W7 FNV gate becomes W10 FNV quarantine.
- Add W8 and W9 lowerer gates with the command-shaped consumers above.
- Add W11 close reconciliation with the anti-deferral wording above.

Do not rely on P3-F prose mapping. The dispatchable gate source itself must be folded before any wave redress.

## Hard Cap Enforcement Fold

Add this rule to P3-B, SPEC Section 2, and DISPATCH pre-dispatch verification:

```text
If plan research estimates exceed the wave LOC budget, generated-output status, or the 30-minute redress cap, the wave must split before redress or record intrinsic block. CHALLENGE time is review time only and cannot be used as implementation overflow. A wave may not continue by promising that the next wave will consume an unwired primitive, provider, lowerer, telemetry field, or gate report.
```

This keeps CH4 cost acceptance and CH6 anti-paper-close aligned: every source-present path either has a same-wave consumer, is scalar-delegated, is deleted with proof, is demoted to diagnostic, or is recorded as an intrinsic block.
