# AY.W0 — Legacy Prune + Stale Tests + ebnf_prettify + AX.FINAL

**Opens after**: tranche open
**Agents**: 1 serial (prune + AX.FINAL + housekeeping) + 1 parallel (ebnf_prettify diagnosis)
**Hard gate**: `cargo test --workspace` compiles clean (5 stale wire-contract + 2 emitter-shape tests retired; ebnf_prettify recognizer fixed or halt-diagnosed); `crates/tape/src/dta.rs` carved to ≤ 92 LOC; `shape_dict.rs` deleted; dead `GrammarProfile` fields removed; `post-AX-W1-close.json` + `AX/FINAL.md` committed; ~38 orphan worktrees cleaned.

## Scope

1. **Retire 5 stale W0a/W0b-era wire-contract test files** (invariant 14 discharge). Predicates retired in W0a.2.j / fields carved in W0b.A without corresponding test retirement.
2. **Retire 2 stale emitter-shape tests** (`classify_byte_dispatch.rs` + `ctns_lifter.rs`). Per A8: test shapes the emitter never emits (0 `__CLASSIFY_TABLE` / `ConsumeToNextStructural` in fresh expand artefacts).
3. **Diagnose + fix ebnf_prettify recognizer bug**: `EbnfParser::parse("digit = \"0\" ;")` fails at offset 0. Pre-existing. Not caused by W1r landings (bbnf_self_parity 56/0 parses same source via BbnfEmit).
4. **DTA kernel-dead prune (~458 LOC out of 550)** in `crates/tape/src/dta.rs`. Per A8 §Part 2: carve DtaState 13-variant enum (199 LOC), DtaTable+impls (69), DtaDiagnostic (55), LiteralPayload (91), DtaRuleEntry (8), DtaFrameKind (12), DtaCounterOptional (9), SeqPromote (15). **Retain only** DtaRuleId + DtaAssociativity + DtaPrecedenceEntry + DtaStateId-as-NONE-sentinel (~92 LOC; live Pratt pipeline consumers per A8 §Part 3).
5. **Dead GrammarProfile fields carve (~150 LOC)**: `list_rules` + `shape_dict` (+ `ShapeEntry` struct) + `__GRAMMAR_PROFILE_LIST_RULES` / `_SHAPE_DICT` statics + `push_*_count` triple — never read at runtime.
6. **Delete `crates/tape/src/shape_dict.rs`** (79 LOC): `BBNF_SHAPE_DICT` never emitted; `push_shape_ref` in builder.rs:634 has zero call sites.
7. **AX.FINAL artefacts**: capture `docs/benchmarks/post-AX-W1-close.json` (18-entry matrix at HEAD `411eabfd`); author `docs/tranches/AX/FINAL.md` summarizing AX outcomes; append `docs/tranches/AX/PROGRESS.md` with AX-closed entry.
8. **Housekeeping per A8 §Part 3**: clean ~38 of 54 orphan worktrees (2 `/private/tmp`, 17 `.claude/worktrees/agent-*`, 6 AW3 research, 2 AW reduce/walker, 8 AX.W1r, 1 AX.W1-c, 5 AU profile worktrees); prune 2,098 Apr 15-17 stale profile files + 2 redundant `-az-a5` dirs; verify `.bbnf-cache` single-copy under `target/`.

## File bounds

| File | Access |
|------|--------|
| `crates/core/tests/bbnf_profile_wire_contract.rs` | delete |
| `crates/core/tests/grammar_profile_wire_contract.rs` | delete |
| `crates/core/tests/json_parity_shape_emit.rs` | delete |
| `crates/core/tests/gate_predicate_wire_contract.rs` | delete |
| `crates/core/tests/aw_v_w5_2_per_ref_routing.rs` | delete |
| `crates/core/tests/classify_byte_dispatch.rs` | delete |
| `crates/core/tests/ctns_lifter.rs` | delete |
| `crates/tape/src/dta.rs` | modify-carve (550 → ≤ 92 LOC) |
| `crates/tape/src/shape_dict.rs` | delete |
| `crates/tape/src/lib.rs` | modify (re-export pruning) |
| `crates/tape/src/builder.rs` | modify (remove `push_shape_ref`) |
| `crates/tape/src/columns.rs` | modify (remove dead `push_*_count` + refs) |
| `crates/core/src/backend/rust/emitter/profile.rs` | modify (remove list_rules/shape_dict emit blocks) |
| `crates/ir/src/types/profile.rs` OR equivalent | modify (remove dead fields from GrammarProfile definition) |
| `crates/core/src/grammar/generated.rs` | modify (regen without removed emissions) |
| `docs/benchmarks/post-AX-W1-close.json` | create |
| `docs/tranches/AX/FINAL.md` | create |
| `docs/tranches/AX/PROGRESS.md` | modify (append close entry) |
| `docs/tranches/AY/audit/AYW0-ebnf-diag.md` | create |

**Do NOT touch**: any `.bbnf` grammar, any view layer (`crates/core/src/backend/rust/view/`), any serialize/prettify codegen, any shape-emitter (`crates/core/src/backend/rust/emitter/shapes/`). **Live-substrate survivors** (per A8 §Part 3) that MUST remain: DtaRuleId + DtaAssociativity + DtaPrecedenceEntry + DtaStateId sentinel + structural_alphabet/_digraphs/_mask/_quote_classes GrammarProfile fields + compounds_per_input_byte/leaves_per_input_byte/parallel_break_even_bytes.

## Phase sub-items

### AY.W0.1 Retire 7 stale test files

Mechanism: `git rm` 7 files in a single commit citing invariant-14 discharge. Commit body links each to its retired predicate / carved field per A8 §Part 2.

- `bbnf_profile_wire_contract.rs` → GrammarProfile fields carved in W0b.A (`b7aa41c0`).
- `grammar_profile_wire_contract.rs` → same root cause.
- `json_parity_shape_emit.rs` → `dta_run_JsonGrammar` walker symbol deleted in W0b.A (`a206b962`).
- `gate_predicate_wire_contract.rs` → shape predicates admission-widened in W0a.2.j (`5f0709fc`).
- `aw_v_w5_2_per_ref_routing.rs` → same predicates.
- `classify_byte_dispatch.rs` → `__CLASSIFY_TABLE` not emitted by current emitter.
- `ctns_lifter.rs` → `ConsumeToNextStructural` not emitted.

**Sub-gate**: `cargo test --workspace --no-run` compiles clean.

### AY.W0.2 ebnf_prettify diagnosis + fix (parallel agent)

Reproduce failure. Instrument `EbnfParser::parse` vs `BbnfEmit::parse(grammar/ebnf/ebnf.bbnf)`. Identify where EbnfParser rejects valid EBNF source at offset 0.

If fix is mechanical + within W0 scope (no grammar DSL changes, no IR pipeline changes): land.

If fix requires grammar regen or IR pipeline intervention: halt + diag at `docs/tranches/AY/audit/AYW0-ebnf-diag.md`; mark tests `#[ignore]` with diag citation; defer to a later AY sub-wave.

**Sub-gate**: `cargo test -p bbnf --test ebnf_prettify` green OR ignore+diag cited.

### AY.W0.3 DTA kernel-dead carve

Per A8 §Part 2 classification, retain only live-substrate types.

Mechanism:
- Identify every reference to the carved types via workspace grep. Delete or re-route each (all external refs should be in stale tests about to be deleted or in generated.rs which will regen).
- Export live types from `crates/tape/src/lib.rs`.
- Regen bootstrap. Verify cycle-1 = cycle-2 byte-identical post-regen.

**Sub-gate**: `cargo build --workspace` clean; 13 parity + canonical harnesses green; `wc -l crates/tape/src/dta.rs` ≤ 92.

### AY.W0.4 Dead GrammarProfile fields carve

Per A8 §Part 2: `list_rules` + `shape_dict` + `ShapeEntry` + `push_*_count` triple.
- Identify emit sites (`crates/core/src/backend/rust/emitter/profile.rs` or similar).
- Identify field definition sites (GrammarProfile struct in ir).
- Delete both. Regen.

**Sub-gate**: `cargo expand -p bbnf --lib | grep -cE 'LIST_RULES|SHAPE_DICT|push_compound_count|push_leaf_count'` returns zero.

### AY.W0.5 shape_dict.rs deletion

`git rm crates/tape/src/shape_dict.rs`; remove `pub mod shape_dict;` from `crates/tape/src/lib.rs`; remove `push_shape_ref` from `crates/tape/src/builder.rs` (zero call sites per A8).

**Sub-gate**: `cargo build --workspace` clean.

### AY.W0.6 AX.FINAL bench + close doc

Run 18-entry matrix at master HEAD `411eabfd` via `scripts/bench_regression.sh` (or equivalent). Save to `docs/benchmarks/post-AX-W1-close.json` with the schema from `post-AX-W0a-close.json` (`1241e7ac`).

Author `docs/tranches/AX/FINAL.md` per canonical tranche-FINAL template: scope recap, bench delta vs W0a.close, invariants 1-21 closure status, scope-reveals absorbed, handoff to AY.

Append `docs/tranches/AX/PROGRESS.md` with `## 2026-04-20 — AX closes; AY opens` entry.

**Sub-gate**: 3 files exist; `post-AX-W1-close.json` has 18 numeric entries.

### AY.W0.7 Housekeeping

Enumerate orphan worktrees via `git worktree list`; prune 38 of 54; delete stale profile directories; verify `.bbnf-cache` single-copy in `target/`. Destructive on local workspace; document actions in commit body.

**Sub-gate**: `git worktree list | wc -l` ≤ 20 (main + active feature-branch worktrees). `du -sh .profiles/` reduced by ≥ 30%.

## Hard gate

1. `cargo test --workspace --no-run --profile ax-iter` compiles clean.
2. `cargo test --workspace --profile ax-iter` runs clean (ebnf_prettify fixed OR ignore+diag cited).
3. `wc -l crates/tape/src/dta.rs` ≤ 92; `ls crates/tape/src/shape_dict.rs` errors.
4. `docs/benchmarks/post-AX-W1-close.json` exists with 18 numeric entries.
5. `docs/tranches/AX/FINAL.md` committed.
6. Bootstrap regen cycle-1 = cycle-2 byte-identical.
7. 13 parity + canonical harnesses green at their canonical counts.

## Verification artefacts

- `docs/benchmarks/post-AX-W1-close.json`, `docs/tranches/AX/FINAL.md`, `docs/tranches/AX/PROGRESS.md` close entry.
- `docs/tranches/AY/audit/AYW0-ebnf-diag.md` (if halted) or ebnf fix landing notes.
- `wc -l` before/after artefact at `docs/benchmarks/post-AY-W0-prune.txt` documenting LOC retired.
- Up to 7 commit SHAs: test retirement, ebnf fix/diag, dta carve, GrammarProfile carve, shape_dict delete, AX.FINAL, housekeeping.

## Dependencies

- **Depends on**: tranche open (AY.md + wave specs on master).
- **Blocks**: W1 (W0 clears compile blockers + removes ~2,300 LOC that otherwise fattens rustc work).

## Archaeology

W0b.D (`0d730c8f` + `6854f18b` + `e839378c`) deleted 8 DTA-coupled test suites but missed 5 wire-contract tests + 2 emitter-shape tests. W0b.A (`b7aa41c0`) carved 7 dead profile slots but left `list_rules` + `shape_dict` + `push_*_count` + `shape_dict.rs` + `dta.rs` kernel-dead carry-forward. A8 §Part 2-3 inventories the omission; AY.W0 completes the retirement cycle.
