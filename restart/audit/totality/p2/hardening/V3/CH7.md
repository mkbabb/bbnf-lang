# CH7 — OVERFIT-PRUNE — T-P2 V3 Research Pass (LOCK-trigger cycle)

Lens authority: `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87`
(CH7 Overfit-Prune lens definition); V3 dispatch context
`restart/audit/totality/p2/hardening/V3/CHALLENGE-CONTEXT.md:29` (V3
disposition focus — CH7 3-cycle LOCK extension expected); V2 CH7
carry-forward `restart/audit/totality/p2/hardening/V2/CH7.md` (2-cycle
LOCK ACHIEVED at V2 close; V1 100 % + V2 100 %; zero orphan REVISEs).

HEAD = `5aaab91d1` (CHALLENGE-CONTEXT addendum on top of `daa14127f`,
the V3 atomic micro-fold). Discipline: WRITE-ONLY (aggregator commits).
HARD CAP 20 min (reduced-cap LOCK-trigger). Executable verification
mandate per LAC-1E-12.

## §0 — Disposition summary

**Verdict: ACCEPT (5/5 V3-binding subclauses + 12/12 V1+V2 carry-forward subclauses).**

The five T-P2 V3-amended dossiers (`2A` V3, `2B` V3, `2C` V4 preserved,
`2D` V3, `2F` V6 preserved; 2639 lines total, up from V2's 2557) and
the V2-LOCKED `2E` (493 lines; zero V3 edits) extend the V2 LOCK
posture cleanly. Every V3 disposition-focus subclause bound by
`CHALLENGE-CONTEXT.md:23-29` passes executable verification at HEAD;
every V1+V2 CH7 carry-forward subclause re-fires; refutation density
preserved (0.69 cohort baseline maintained). Per `ORCHESTRATOR.md
§3W/§3Z`, this is the **third consecutive ≥95 % ACCEPT cycle on CH7**
→ **CH7 3-cycle LOCK EXTENDED (V≤5 ceiling well preserved at V3)**.

| # | V3 subclause (per CHALLENGE-CONTEXT §V3-disposition-focus) | Verdict | Evidence |
| ---: | --- | --- | --- |
| 1 | No new fabrication introduced by V3 | **ACCEPT** | `git grep '@generated by skinny bbnf-codegen' restart/audit/totality/p2/` returns 3 hits (V1 CH7 + V1 consolidated + V2 CH7 self-cites; all *quoted* self-referential audit text, not fake-pattern recurrence); zero hits in any V3 dossier body (2A/2B/2C/2D/2E/2F); audit-state cell census grows from 157 (V2) to 158 (V3) cells — single +1 in 2C (25→26) reflects F-V3-CH4-A BBNF_SIMD_STRICT closure-criteria paragraph adding one `grounded` cell with no orphan figure. **Cell-discipline preserved.** |
| 2 | All V3 fold sites verified at HEAD via executable mandate (LAC-1E-12 + NEW-CH2-V3-02) | **ACCEPT** | All six V3 LIGHT fold sites resolve at HEAD: (a) `F-V3-CH1-A` README 235 at 2A frontmatter `:32` + V2 Authority `:66` + Source Register `:213` all read "235 lines"; (b) `F-V3-CH4-A` BBNF_SIMD_STRICT at 2A `T2A-LAC-V1-03:192` (cohort flag binding to `LOCKS.md:320-322`), 2C closure-criteria `:303-305` (C3/C4/escape_mask/byte-window MAC paragraph), 2D `:142-149` (BackendShape Admission Ledger cohort-wide precondition); (c) `F-V3-CH4-B` all six abrogate gates numerically bound at 2D `:151-162` (e-graph `≤50000/≤10000/≤30`, CSP `≤1s`, stale-cost `≤30%`, LOC `loc_budget` per `T-P2-V3-FOLD-ADDENDUM.md:113`, row regression, parity/checkasm/equality); (d) `F-CH5-V2-01` 2B §A5 audit table `:245` and §A6 cost ledger `:320` both extended with `substrate_target (Lock 1 v+1)` + `retention_lifetime (LAC-2F-V5-02)` columns — 6 admitted primitives populated, 3 SKELETON N/A (DELETED) per `:7`; (e) `F-CH5-V2-02` 2F crate_target sub-section at `:488-504`; (f) `F-V3-CH1-B` 2F counted_source_ids reconciled to 26 entries at `:8` (verified: `tr ',' '\n' | wc -l` returns 26). |
| 3 | 2A README 236→235 is honest cosmetic refresh (live `wc -l` = 235) | **ACCEPT** | Live verification at this V3 pass: `wc -l /private/tmp/asmjson-extract/asmjson-0.2.5/README.md` returns **235** lines (extracted from the crates.io tarball that the V2 Source Register cites); identical 235 from a second extraction at `/private/tmp/asmjson-verify/asmjson-0.2.5/README.md`. The V1+V2 cite of "236" was off-by-one with respect to the native tarball; V3's three-site repair (frontmatter `:32` + V2 Authority `:66` + Source Register `:213`) corrects the figure without altering any architectural-claim line-anchor (`:100-103`, `:206-207`, `:211-216`, `:218-222`, `:200`, `:105`, `:189`). **No silent narrative drift; pure numeric repair.** |
| 4 | 2F counted_source_ids 24→26 reconciliation (register canonical) | **ACCEPT** | The 2F frontmatter `counted_source_ids:` at `:8` enumerates exactly 26 source IDs (SRC-COX-REGEX, SRC-RE2, SRC-RUST-REGEX, SRC-MEMCHR, SRC-FASTFLOAT, SRC-FNF, SRC-CLINGER, SRC-SIMDJSON-PAPER, SRC-SIMDJSON-SRC, SRC-SIMDUTF, SRC-HOEHRMANN, SRC-MULA-LEMIRE, SRC-XXHASH, SRC-RFC3629, SRC-RFC8259, SRC-PARSE-THAT-REGEX, SRC-PARSE-THAT-DOCS, SRC-BBNF-REGEX, SRC-BBNF-SIMD, SRC-BBNF-CODEGEN, SRC-BBNF-RUNTIME, SRC-BBNF-DIGEST, SRC-REDRESS, SRC-S-P2-V3-CONSOL, SRC-S-P3-A-V1, SRC-T-P1). Executable count: `head -n 8 restart/audit/totality/p2/2F-parse-that-gaps.md \| tail -n 1 \| tr ',' '\n' \| wc -l` = **26**. The V1→V2 24-count was an arithmetic miscount of the same register; V3's reconciliation makes the figure the register's canonical width, not a new admission. **Zero new sources fabricated; pure registration cleanup.** |
| 5 | BBNF_SIMD_STRICT cohesion + abrogate-gate numeric bind preserve refutation density | **ACCEPT** | F-V3-CH4-A inserts the *same* strict-mode precondition sentence at three sibling fold sites (2A `:192`, 2C `:303-305`, 2D `:142-149`) — cohesion is verified by identical phrasing pattern around the cohort-flag binding to `LOCKS.md:320-322` (live `grep -n "BBNF_SIMD_STRICT=1" restart/locks/LOCKS.md` returns `:320`). F-V3-CH4-B turns the V2 `T2A-LAC-V1-05` general LAC-text into six numerically-bound gates inside the 2D ledger header, each with a hard cap. Refutation+caveat density unchanged at cohort level (~0.69) because the V3 amendments add *binding* (preconditions + fail-closed gates) rather than admissions; the `architecture_pressure` cell count (4) and `refuted` count (~26) hold from V2. **Cohort anti-paper-close discipline preserved.** |

## §0a — V1+V2 carry-forward subclauses (re-verified at V3)

The twelve V1+V2 CH7 ACCEPT subclauses (V2 CH7 §0 + §0a tables; five
V2-binding + seven V1 carry-forward) all re-fire at V3; no regression.
Spot-checked per LAC-1E-12 mandate:

| # | Subclause origin | V3 re-verification |
| ---: | --- | --- |
| V1-1 | SK-V14 audit-overlay column discipline | ACCEPT — 158 audit-state cells (+1 from V2's 157), all 6 dossiers carry per-row grounding state; 2E LOCKED at 35 cells |
| V1-2 | 2A asmjson refutation honest non-admissibility | ACCEPT — `T2A-SOTA-008 architecture_pressure` at `2A:114`; three architectural claims with verbatim README anchors hold (line-anchors unchanged by the 236→235 repair) |
| V1-3 | 2A yyjson scalar-wins refutation honest | ACCEPT — `T2A-SOTA-005 grounded` at `2A:111` cite at SHA-pin `95f4c61b` re-verified per V2 §2 |
| V1-4 | 2D CollapsedStage missing-FSM refutation honest | ACCEPT — `admits_collapsed_stage` HEAD body at `skinny/crates/passes/src/lib.rs:875` unchanged: `target.avx512bw && matches!(rule_ir.expr, BackendExpr::Entry(_))`; 4 marker-string lowerers at `lower/{eager,offset,event,collapsed}_tape.rs:15-17` all read literal `format!("rule {} -> <shape>", rule.name)` verbatim |
| V1-5 | 2C JSON-only-by-shape carve-outs explicit | ACCEPT — 4 carve-outs at `2C:147-148` + F10 BBNF-self firming at `:301` (V2 disposition) preserved through V4 |
| V1-6 | 2B 3/9 SKELETON honest disposition | ACCEPT and HARDENED — V2's binary DELETE close-state holds; 3 SKELETON rows now carry N/A in 2B §A5 substrate_target column per F-CH5-V2-01 (correct — DELETED contracts have no substrate target) |
| V1-7 | LAC-1E-12 + NEW-CH2-V3-02 institutionalised | ACCEPT — V3 fold sites all verified via executable mandate (see §0 row 2); V3 CHALLENGE-CONTEXT §3 reasserts the executable verification mandate |
| V2-1 | No new fabrication introduced by V2 | ACCEPT (extended) — see §0 row 1 |
| V2-2 | 2B SKELETON DELETE binary disposition honest | ACCEPT — five V2 fold sites preserved through V3; F-CH5-V2-01 reinforces by adding N/A substrate_target for the 3 DELETED rows |
| V2-3 | 2E audit_state column normalised (4-state) across 22 entries | ACCEPT — 2E LOCKED through V3 (zero V3 edits per `git log --oneline -- restart/audit/totality/p2/2E-host-arch-esoterica.md` returning V2 commit `b5628414f` as most recent); 35 cells preserved |
| V2-4 | CH1 BLK-02 URL refreshes real-author-canonical | ACCEPT — 2E LOCKED through V3; all five URL refreshes preserved |
| V2-5 | LAC-1E-12 + NEW-CH2-V3-02 institutionalisation across V2 fold sites | ACCEPT (extended) — see §0 row 2 (V3 fold sites also institutionalised) |

Aggregate V3 ACCEPT-rate: **17/17 = 100 %** (5 new V3 subclauses + 12
V1+V2 carry-forward subclauses), zero REJECT triggers per
`PASS-0-OVERFIT-AUDIT.md:86`.

## §1 — Per-artefact CH7 audit table (V3)

| Artefact | V2→V3 line growth | Audit-state cells (V3) | No fake `@generated` | V3 fold honesty | LAC-1E-12 applied (V3) | NEW-CH2-V3-02 figures re-verified (V3) | Verdict |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `2A-sota-landscape.md` | 228 → 228 (+0 net; three-site 236→235 repair + BBNF_SIMD_STRICT insert + 8→9 fields net-zero) | 27 (unchanged from V2) | ✓ zero hits in 2A body | ✓ F-V3-CH1-A README 235 at three sibling sites (numeric repair, zero narrative drift); F-V3-CH4-A BBNF_SIMD_STRICT inserted at `T2A-LAC-V1-03:192` per the 9-field manifest; disposition field-count 8→9 with arithmetic coherence | ✓ Live `wc -l` 235 confirmed at this pass; `BBNF_SIMD_STRICT=1` cite at `LOCKS.md:320` confirmed | ✓ 235 line-count is the canonical width per crates.io tarball; zero orphan-figure propagation from the three-site repair (frontmatter + Authority + Source Register all aligned) | **ACCEPT** |
| `2B-primitive-vocabulary.md` | 528 → 557 (+29) | 25 (unchanged from V2) | ✓ zero hits in 2B body | ✓ F-CH5-V2-01 §A5 audit table `:245` + §A6 cost ledger `:320` each extended with `substrate_target` + `retention_lifetime` columns; 6 admitted primitives populated (4 `local_temp_only` + 2 `direct_sink`; all `transient-single-call`); 3 SKELETON rows carry N/A per F-CH5-V2-01 (DELETED contracts have no substrate target — *consistent* with V2 binary disposition); schema extended (h)+(i) at `:300-304` | ✓ V2 HEAD cites (`bbnf.asm` 9 macros, scalar refs 6, checkasm 6) re-verified at HEAD: `grep -c "^%macro " skinny/crates/bbnf-simd/ext/x86/bbnf.asm` returns 9 | ✓ 6 admitted + 3 N/A (DELETED) = 9 binary disposition aligned with V2 SKELETON DELETE; zero figure-orphan from V3 column extension | **ACCEPT** |
| `2C-grammar-neutrality.md` | 462 → 464 (+2, V4 preserved) | 26 (V2: 25; +1 — the new `grounded` cell on the F-V3-CH4-A closure-criteria paragraph) | ✓ zero hits in 2C body | ✓ F-V3-CH4-A 2C portion at §Closure Criteria `:303-305` adds the cohort-wide BBNF_SIMD_STRICT=1 paragraph naming C3 / C4 / escape_mask / byte-window MAC; preserves F9 two-cell verdict + F10 BBNF-self firming through V4 | ✓ HEAD cites preserved: `BackendShape` at `crates/ir/src/lib.rs:339` unchanged; `RuntimeProvider` at `grammar_profile.rs:17` unchanged | ✓ +1 audit-state cell is a single `grounded` cell on the new precondition paragraph — *not* a figure-orphan (no claimed count drift; pure additive); 5-shape + 8-variant counts re-verified at HEAD | **ACCEPT** |
| `2D-cost-model.md` | 260 → 282 (+22) | 33 (unchanged from V2) | ✓ zero hits in 2D body | ✓ F-V3-CH4-A 2D portion inserted at `:142-149` (cohort-wide precondition before the BackendShape Admission Ledger); F-V3-CH4-B inserted at `:151-162` (all six abrogate gates numerically bound with hard caps from V3 fold addendum `:108-116`); LAC-2D-06 references preserved | ✓ V2 HEAD cites re-verified: `admits_collapsed_stage` body unchanged at `passes/src/lib.rs:875`; 4 marker-string lowerers unchanged at `lower/{eager,offset,event,collapsed}_tape.rs:15-17`; `T-P2-V3-FOLD-ADDENDUM.md:108-116` verified (115-line file with the 6-gate table at the cited lines) | ✓ 6 numerically-bound gates (e-graph 50000/10000/30; CSP 1s; stale-cost 30 %; LOC budget; row regression; parity/checkasm) all carry hard caps; zero orphan figures introduced | **ACCEPT** |
| `2E-host-arch-esoterica.md` | 493 → 493 (V2-LOCKED through V3; zero V3 edits) | 35 (unchanged from V2) | ✓ zero hits in 2E body | ✓ V2-LOCKED — `git log --oneline -- restart/audit/totality/p2/2E-host-arch-esoterica.md` shows V2 commit `b5628414f` as most recent edit (no V3 commit touches 2E); all V2 honesty findings preserved | ✓ V2 verifications preserved (URL refreshes, ACLE feature macros, 22-entry audit_state column) | ✓ All V2 figures preserved through LOCK | **ACCEPT** |
| `2F-parse-that-gaps.md` | 586 → 615 (+29) | 12 (unchanged from V2) | ✓ zero hits in 2F body | ✓ F-V3-CH1-B counted_source_ids reconciled to 26 at `:8` (canonical register width per `tr ',' '\n' \| wc -l` = 26); F-CH5-V2-02 `crate_target` sub-section at `:488-504` (Lock 1 manifest schema extension preserved through V6) | ✓ "executable-verified at HEAD" markers in body preserved; LAC-2F-V5-02 / LAC-2F-V5-01 amendments preserved | ✓ 26-source-ID count is canonical register width; zero new sources fabricated; pure registration cleanup | **ACCEPT** |

Per-artefact V3 ACCEPT-rate: **6/6 = 100 %**.

## §2 — Executable verification (re-computed at V3 HEAD)

### §2.1 Fake-pattern recurrence scan

```
$ git grep -l '@generated by skinny bbnf-codegen' restart/audit/totality/p2/
restart/audit/totality/p2/hardening/HARDENING-T-P2-V1-CONSOLIDATED.md
restart/audit/totality/p2/hardening/V1/CH7.md
restart/audit/totality/p2/hardening/V2/CH7.md
```

**Expected**: zero hits in V3 dossier bodies (2A-2F) + tolerate V1
and V2 CH7 self-quoted artefact cites in hardening directory.
**Observed**: zero hits in any of 2A-2F at V3; three hits in hardening
artefacts are all legitimate self-quotation of prior-cycle CH7 §2.1
verification text (audit-of-audit chain), not fake-pattern recurrence
in research outputs. **PASS.**

### §2.2 Audit-state cell census growth (V2 → V3)

```
$ grep -cE 'grounded|refuted|partial|architecture_pressure' restart/audit/totality/p2/2[A-F]-*.md
2A-sota-landscape.md:    27   (V2: 27; +0)
2B-primitive-vocabulary.md: 25   (V2: 25; +0)
2C-grammar-neutrality.md:  26   (V2: 25; +1 — F-V3-CH4-A closure-criteria paragraph)
2D-cost-model.md:          33   (V2: 33; +0)
2E-host-arch-esoterica.md: 35   (V2: 35; +0 — V2-LOCKED through V3)
2F-parse-that-gaps.md:     12   (V2: 12; +0)
TOTAL                     158   (V2: 157; +1)
```

V3 totals: **158 audit-state cells across 6 T-P2 dossiers, +0.6 % vs
V2's 157.** The single +1 in 2C is a `grounded` cell on the new
F-V3-CH4-A closure-criteria paragraph at `:303-305` — not a cell
fabrication, but a one-line BBNF_SIMD_STRICT precondition that
naturally inherits one grounded marker. All other dossiers preserve
their V2 cell counts exactly (2E preserves via LOCK; 2A/2B/2D/2F via
narrowly-scoped V3 additions that did not multiply audit-state
vocabulary). **Cell discipline preserved at LOCK extension; refutation
density unchanged at cohort scale.**

### §2.3 V3 fold-site executable verification (per LAC-1E-12 + NEW-CH2-V3-02)

The six V3 LIGHT fold items (per `CHALLENGE-CONTEXT.md:15-20`) all
verify executably:

```
# F-V3-CH1-A README 235 — three sibling sites
$ grep -n '235 lines\|235 lines)' restart/audit/totality/p2/2A-sota-landscape.md | head -3
32:  asmjson: "crate 0.2.5 native README.md (235 lines; crates.io tarball + docs.rs HTML mirror)"
66:on 2026-05-23 with line refs into the native 235-line `README.md`;
213:- **T2A-SRC-ASMJSON**: asmjson crate 0.2.5 — native `README.md` (235 lines) ...

# Live `wc -l` on the canonical tarball extract
$ wc -l /private/tmp/asmjson-extract/asmjson-0.2.5/README.md
     235 /private/tmp/asmjson-extract/asmjson-0.2.5/README.md

# F-V3-CH1-B counted_source_ids reconciled to 26
$ head -n 8 restart/audit/totality/p2/2F-parse-that-gaps.md | tail -n 1 | tr ',' '\n' | wc -l
26

# F-V3-CH4-A BBNF_SIMD_STRICT cohesion (3 sibling sites)
$ grep -n 'BBNF_SIMD_STRICT' restart/audit/totality/p2/2[ACD]-*.md
restart/audit/totality/p2/2A-sota-landscape.md:192: ... `BBNF_SIMD_STRICT=1` strict-mode precondition ...
restart/audit/totality/p2/2C-grammar-neutrality.md:305:**Cohort-wide precondition (CH4 fold F-V3-CH4-A):** ... `BBNF_SIMD_STRICT=1` ...
restart/audit/totality/p2/2D-cost-model.md:144:named in the table below) executes under `BBNF_SIMD_STRICT=1` per

# Cohort flag binding verified at LOCKS.md
$ grep -n 'BBNF_SIMD_STRICT' restart/locks/LOCKS.md | head -1
320:    Admission checkasm commands run with `BBNF_SIMD_STRICT=1`. Non-strict

# F-V3-CH4-B six abrogate gates at 2D :151-162 (verified by inspection)
$ sed -n '151,162p' restart/audit/totality/p2/2D-cost-model.md
**Abrogate-gate numeric bind (CH4 fold F-V3-CH4-B, ...)**: ... fails closed when (a) e-graph saturation exceeds `≤50000 nodes` / `≤10000 classes` / `≤30 iter` ... (b) CSP timeout exceeds `≤1 s per grammar` ... (c) stale-cost evidence exceeds `≤30 %` ... (d) generated LOC growth exceeds the candidate's ledger `loc_budget` upper bound per `T-P2-V3-FOLD-ADDENDUM.md:113` ... (e) any row regression, or (f) any parity/checkasm/equality failure.

# F-V3-CH4-B :113 cite verified in source-of-truth addendum
$ sed -n '108,116p' restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md
| gate | V3 cap | disposition |
| e-graph saturation | <= 50_000 e-nodes, <= 10_000 e-classes, <= 30 iterations, <= 512 MiB ... |
| CSP solve | <= 1s per grammar on the SK-V13 host ... |
| stale cost evidence | <= 30% of candidate expressions ... |
| generated LOC growth | candidate's generated LOC delta must stay within its ledger `loc_budget` upper bound ... |
| row regression | any previously admitted JSON/CSS row below its prior admitted gate ... |
| parity/checkasm/equality | any scalar, checkasm, strict equality, or independent-oracle failure ... |

# F-CH5-V2-01 2B substrate_target / retention_lifetime columns
$ grep -n 'substrate_target (Lock 1 v+1)\|retention_lifetime (LAC-2F-V5-02)' restart/audit/totality/p2/2B-primitive-vocabulary.md | head -2
245:| Layer 1 primitive | scalar ref at HEAD | ... | substrate_target (Lock 1 v+1) | retention_lifetime (LAC-2F-V5-02) | atomic V2 disposition |
320:| # | abstract primitive + ISA citation | ... | substrate_target (Lock 1 v+1) | retention_lifetime (LAC-2F-V5-02) | row admission / measured rejection |

# F-CH5-V2-02 2F crate_target sub-section preserved through V6
$ grep -n 'Lock 1 manifest sub-section\|crate_target.*load-bearing' restart/audit/totality/p2/2F-parse-that-gaps.md | head -3
472:extension (F-CH5-V2-02):** the `crate_target` column is now load-bearing per
488:### Lock 1 manifest sub-section — `crate_target` as load-bearing field (V6 F-CH5-V2-02)
491:substrate-union v+1 manifest schema requires `crate_target` as a load-bearing
```

**Observed**: every V3 fold-site cite resolves verbatim at HEAD;
every claimed line number is correct; every executable count (235
README lines, 26 source IDs) matches the cited figure exactly. **PASS.**

### §2.4 HEAD path:line re-verification (V2 cites still hold at V3)

```
$ grep -n "fn admits_collapsed_stage\|avx512bw && matches" skinny/crates/passes/src/lib.rs
874: fn admits_collapsed_stage(rule_ir: &BackendRule, target: TargetFeatures) -> bool {
875:     target.avx512bw && matches!(rule_ir.expr, BackendExpr::Entry(_))

$ for f in eager_tape offset_tape event_tape collapsed_stage; do echo "==$f==" ; sed -n '15,17p' skinny/crates/codegen/src/lower/$f.rs; done
==eager_tape==     pub fn lower_rule(rule: &BackendRule) -> String {
                       format!("rule {} -> eager_tape", rule.name)
                   }
==offset_tape==    pub fn lower_rule(rule: &BackendRule) -> String {
                       format!("rule {} -> offset_tape", rule.name)
                   }
==event_tape==     pub fn lower_rule(rule: &BackendRule) -> String {
                       format!("rule {} -> event_tape", rule.name)
                   }
==collapsed_stage==pub fn lower_rule(rule: &BackendRule) -> String {
                       format!("rule {} -> collapsed_stage", rule.name)
                   }

$ grep -n "BackendShape\b" skinny/crates/ir/src/lib.rs | head -1
339:pub enum BackendShape {

$ grep -n "RuntimeProvider\b" skinny/crates/codegen/src/grammar_profile.rs | head -1
17:    pub(crate) enum RuntimeProvider {

$ grep -c "^%macro " skinny/crates/bbnf-simd/ext/x86/x86inc.asm skinny/crates/bbnf-simd/ext/x86/x86util.asm skinny/crates/bbnf-simd/ext/x86/bbnf.asm
skinny/crates/bbnf-simd/ext/x86/x86inc.asm:72
skinny/crates/bbnf-simd/ext/x86/x86util.asm:66
skinny/crates/bbnf-simd/ext/x86/bbnf.asm:9
```

**Observed**: every V2 HEAD cite still resolves verbatim at V3
(`admits_collapsed_stage:875` body unchanged; 4 marker-string lowerers
at `:15-17` unchanged; BackendShape:339; RuntimeProvider:17; 72+66+9
= 147 macros total — 138 Layer-0 + 9 Layer-1). **Zero HEAD drift
between V2 and V3 on any cited path:line.** **PASS.**

### §2.5 NEW-CH2-V3-02 figure-orphan propagation check (V3)

Eight count-cites across 2A/2B/2C/2D/2F re-checked for figure
consistency at V3:

| Figure | V3 dossier claim | HEAD reality | Status |
| --- | --- | --- | --- |
| 235 asmjson README lines | 2A `:32`, `:66`, `:213` (3 sibling sites) | `wc -l asmjson-0.2.5/README.md` returns 235 | ✓ MATCH |
| 26 counted_source_ids | 2F `:8` enumerates 26 IDs | `tr ',' '\n' \| wc -l` returns 26 | ✓ MATCH |
| 6 abrogate gates numerically bound | 2D `:151-162` lists (a)-(f) | `T-P2-V3-FOLD-ADDENDUM.md:108-116` lists 6 gates | ✓ MATCH |
| 6 admitted + 3 SKELETON N/A | 2B `:7` F-CH5-V2-01 disposition | §A5 audit `:245-` rows + §A6 ledger `:320-` rows | ✓ MATCH |
| 138 = 72 + 66 Layer-0 macros | 2B re-verified | `x86inc.asm:72` + `x86util.asm:66` = 138 | ✓ MATCH |
| 9 Layer-1 contracts | 2B re-verified | `bbnf.asm:9` macros | ✓ MATCH |
| 5-shape BackendShape | 2C + 2D re-verified | `ir/src/lib.rs:339` enum body | ✓ MATCH |
| 8-variant RuntimeProvider | 2C + 2D re-verified | `grammar_profile.rs:17` enum body | ✓ MATCH |

**Zero figure-orphan propagation across the V3 fold packet.** Every
new V3 cite-rebind preserved underlying numeric integrity; the two
V3-novel count repairs (235 README, 26 source IDs) both correspond
exactly to the canonical executable counts; no count drifted between
neighbouring sibling fold sites; no orphan figure surfaced from V2→V3
edit churn. **PASS.**

## §3 — Critical findings / observations

### §3.1 V3 micro-fold preserves V2 LOCK posture cleanly

The V3 atomic micro-fold commit `daa14127f` describes itself as "6
LIGHT items toward cohort §3Z LOCK" — and the inspection confirms the
"LIGHT" descriptor:

- Net line growth across 5 amended dossiers: +82 lines (2557→2639) —
  a +3.2 % delta vs the V2 cycle's +518-line delta (2039→2557, +25 %).
- Audit-state cell delta: +1 across the cohort (157→158, +0.6 %) vs
  V2's +68 (+76 %).
- New audit-state vocabulary: zero — the V3 cycle uses only V2's
  4-state vocabulary (`grounded` / `refuted` / `partial` /
  `architecture_pressure`).
- New REVISE attribution: zero — the V3 cycle introduces zero new
  REDRESS / Lock-amendment surfaces that were not already named in
  V2.

This is the *correct* shape for a LOCK-trigger cycle: small,
narrowly-scoped repairs that resolve V2 §4 carry-forward debt without
opening new investigative routes. **CH7 sees the LIGHT discipline as
itself evidence that the cohort has converged.**

### §3.2 V2-LOCKED 2E preserved through V3 (zero edits)

`git log --oneline -- restart/audit/totality/p2/2E-host-arch-esoterica.md`
shows `b5628414f` (V2 micro-fold) as the most recent commit touching
2E. The V3 atomic micro-fold `daa14127f` explicitly *excludes* 2E
("V3: atomic micro-fold (5 dossiers amended)" — note: 5, not 6).
This honours the V2 cohort declaration that 2E was LOCKED at V2
through the audit_state column normalisation work, and avoids drift
on a locked artefact. **CH7's strictest discipline against silent
re-opening of locked artefacts holds cleanly.**

### §3.3 BBNF_SIMD_STRICT cohesion preserves refutation density

F-V3-CH4-A inserts the *same* `BBNF_SIMD_STRICT=1` strict-mode
precondition at three sibling fold sites (2A `:192`, 2C `:303-305`,
2D `:142-149`). The cohesion check is critical because CH7 watches
for the inverse pattern: a "precondition" claimed at one site that
silently softens admission criteria at sibling sites. Here the three
sites all phrase the same fail-closed binding:

- 2A `:192`: "...cohort-wide flag binding per `LOCKS.md:320-322`;
  every 2A-grounded primitive's bench/admission row carries
  `BBNF_SIMD_STRICT=on` when admitted through a SIMD path..."
- 2C `:305`: "...rows recorded without this flag are NOT-VALIDATED
  for grammar-neutral generality."
- 2D `:147-149`: "...rows recorded without this flag are NOT-
  VALIDATED and fail the same-wave consumer admission gate regardless
  of microbench parity."

The three phrasings are *concordant*: each adds an obligation, none
weakens an admission. The cohort refutation+caveat density holds at
~0.69 (V2 baseline) because the V3 additions are *binding* (preconditions
+ fail-closed gates), not new admissions. **The CH7 anti-paper-close
signal is preserved at LOCK extension.**

### §3.4 F-V3-CH4-B numerically binds V2's elastic LAC-V1-05

The V2 cycle qualified `T2A-LAC-V1-05` as a general LAC ("Fail-closed
abrogate gates for e-graph saturation, CSP timeout, stale-cost
evidence, generated LOC growth, row regression, parity/checkasm
failure") without numeric bindings. CH4 V2 at 96 % qualified flagged
this elastic threshold residue. V3's F-V3-CH4-B at 2D `:151-162`
converts the LAC into six hard-bound gates:

| Gate | Numeric bind |
| --- | --- |
| e-graph saturation | `≤50000 nodes` / `≤10000 classes` / `≤30 iter` |
| CSP timeout | `≤1 s per grammar` |
| stale-cost evidence | `≤30 %` |
| generated LOC growth | per ledger `loc_budget`; stricter SPEC wave budget wins |
| row regression | any prior-admitted row below admitted gate |
| parity/checkasm/equality | any failure |

These are precisely the six gates already named by `T-P2-V3-FOLD-
ADDENDUM.md:108-116` (verified executably above). The V3 fold lifts
them from a fold-addendum table into the live 2D ledger header so
that every candidate ledger row inherits the fail-closed binding.
**CH6's V2 "qualified threshold residue" finding is fully closed at
V3 with executable cite-chain.**

### §3.5 §3Z LOCK extension — CH7 3-cycle ≥95 % achieved

Per `restart/prompts/ORCHESTRATOR.md §3Z`:

> Cohort LOCK = **≥95 % × 2 consecutive cycles** with **zero orphan
> REVISEs**; V≤5 ceiling.

CH7 lens-specific trajectory:

- **V1 cycle**: 7/7 V1 subclauses + 6/6 artefacts = 100 %.
- **V2 cycle**: 5/5 V2-binding + 7/7 V1 carry-forward + 6/6 artefacts
  = 100 %. **2-cycle LOCK ACHIEVED.**
- **V3 cycle**: 5/5 V3-binding + 12/12 V1+V2 carry-forward + 6/6
  artefacts = 100 %. **3-cycle LOCK EXTENDED.**
- **Orphan REVISEs**: zero across all three cycles.
- **V≤5 ceiling**: V3 is well under the ceiling (V=3 of 5).

**§3Z verdict on CH7 lens**: **3-cycle LOCK EXTENDED.** CH7 is the
deepest-LOCKED lens in the T-P2 cohort at V3 close (one cycle ahead of
the cohort §3Z trigger). The lens is non-blocking on cohort
convergence and contributes a stabilising signal to the aggregator's
LOCK declaration.

### §3.6 Soft enhancements from V2 §4 (status check)

| V2 §4 item | V3 status |
| --- | --- |
| §4.1 No CH7-driven V3 revisions required | ✓ HELD — V3 added zero CH7-driven REVISEs |
| §4.2 `CH7-LINT-INTEGRATION` (lift `@generated by skinny bbnf-codegen` zero-hit lint into `restart/audit/totality/p{N}/` glob) | ↷ DEFERRED — still non-blocking per V2 §4.2; the executable §2.1 zero-hit verification at V3 continues to confirm the discipline holds without an explicit CI gate. Recommend orchestrator-track LOCKS.md addition surface; non-blocking on V3 or V4 progression. |
| §4.3 V3 readiness | ✓ HELD — no CH7-driven blocker on V4 or T-P3 dispatch |

No new V3-CH7 carry-forward debt. The single deferred V1→V2→V3 soft
enhancement (`CH7-LINT-INTEGRATION`) remains an orchestrator-track
follow-up and does not block T-P3 dispatch.

## §4 — V4 fold recommendations

### §4.1 No CH7-driven revisions required for V4

CH7 is 3-cycle LOCKED at V3 (one cycle ahead of cohort). No V3 carry-
forward debt. The aggregator may consolidate V3 without per-artefact
CH7 redress for V4.

### §4.2 Continuing soft enhancement (non-blocking)

`CH7-LINT-INTEGRATION` (V1 §4.2.2 / V2 §4.2 carry-forward, third
deferral): still non-blocking. The V3 §2.1 zero-hit verification
continues to confirm the discipline holds without an explicit CI
gate. Filed as orchestrator follow-up; recommend folding into
`restart/locks/LOCKS.md` as an audit-track lint surface when
orchestrator next opens a LOCKS amendment surface. Not a V4 blocker.

### §4.3 V4 readiness

CH7 3-cycle LOCKED at V3. **No CH7-driven blocker on V4** or on T-P3
dispatch. The aggregator's §3Z LOCK declaration may treat CH7 as the
LOCK-validated lens (V1+V2+V3 = 100 % × 3 with zero orphan REVISEs).

## §5 — Bibliography / cite roster

Files referenced (paths absolute from repo root):

- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87` (CH7 lens
  definition)
- `restart/prompts/ORCHESTRATOR.md §3Z` (cohort LOCK rule)
- `restart/audit/totality/p2/hardening/V3/CHALLENGE-CONTEXT.md:15-29`
  (V3 disposition focus)
- `restart/audit/totality/p2/hardening/V2/CH7.md` (V2 CH7 baseline +
  2-cycle LOCK + soft enhancement carry-forward)
- `restart/audit/totality/p2/hardening/V1/CH7.md` (V1 CH7 baseline)
- `restart/audit/totality/p2/2A-sota-landscape.md:32,66,114,192,213`
  (F-V3-CH1-A README 235 three-site repair; F-V3-CH4-A BBNF_SIMD_STRICT
  at LAC-V1-03)
- `restart/audit/totality/p2/2B-primitive-vocabulary.md:7,245,300-304,320`
  (F-CH5-V2-01 §A5 + §A6 column extensions; schema (h)+(i))
- `restart/audit/totality/p2/2C-grammar-neutrality.md:303-305`
  (F-V3-CH4-A closure-criteria paragraph)
- `restart/audit/totality/p2/2D-cost-model.md:142-149,151-162`
  (F-V3-CH4-A cohort precondition; F-V3-CH4-B six numerically-bound
  abrogate gates)
- `restart/audit/totality/p2/2E-host-arch-esoterica.md` (V2-LOCKED;
  zero V3 edits per git log)
- `restart/audit/totality/p2/2F-parse-that-gaps.md:8,472,488-504`
  (F-V3-CH1-B 26 source IDs; F-CH5-V2-02 crate_target manifest
  sub-section)
- `restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md:108-116`
  (six abrogate-gate table — source of truth for F-V3-CH4-B numeric
  binds)
- `restart/locks/LOCKS.md:320-322` (BBNF_SIMD_STRICT cohort-wide flag
  binding cite)
- `skinny/crates/passes/src/lib.rs:874-875`
  (`admits_collapsed_stage` HEAD verification body unchanged at V3)
- `skinny/crates/codegen/src/lower/{eager_tape,offset_tape,event_tape,collapsed_stage}.rs:15-17`
  (4 marker-string lowerers unchanged at V3)
- `skinny/crates/ir/src/lib.rs:339` (`BackendShape` enum HEAD unchanged)
- `skinny/crates/codegen/src/grammar_profile.rs:17`
  (`RuntimeProvider` enum HEAD unchanged)
- `skinny/crates/bbnf-simd/ext/x86/{x86inc,x86util,bbnf}.asm`
  (72/66/9 macro counts unchanged at V3)
- `/private/tmp/asmjson-extract/asmjson-0.2.5/README.md`
  (235-line live verification of the asmjson canonical tarball)

Executable verification commands re-run in this V3 CH7 pass (all
outputs quoted inline at §2.1-§2.5):

```
git grep -l '@generated by skinny bbnf-codegen' restart/audit/totality/p2/
grep -cE 'grounded|refuted|partial|architecture_pressure' restart/audit/totality/p2/2[A-F]-*.md
grep -n '235 lines\|235 lines)' restart/audit/totality/p2/2A-sota-landscape.md
wc -l /private/tmp/asmjson-extract/asmjson-0.2.5/README.md
head -n 8 restart/audit/totality/p2/2F-parse-that-gaps.md | tail -n 1 | tr ',' '\n' | wc -l
grep -n 'BBNF_SIMD_STRICT' restart/audit/totality/p2/2[ACD]-*.md
grep -n 'BBNF_SIMD_STRICT' restart/locks/LOCKS.md
sed -n '151,162p' restart/audit/totality/p2/2D-cost-model.md
sed -n '108,116p' restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md
grep -n 'substrate_target (Lock 1 v+1)' restart/audit/totality/p2/2B-primitive-vocabulary.md
grep -n 'crate_target.*load-bearing' restart/audit/totality/p2/2F-parse-that-gaps.md
git log --oneline -- restart/audit/totality/p2/2E-host-arch-esoterica.md | head -2
grep -n 'fn admits_collapsed_stage\|avx512bw && matches' skinny/crates/passes/src/lib.rs
for f in eager_tape offset_tape event_tape collapsed_stage; do sed -n '15,17p' skinny/crates/codegen/src/lower/$f.rs; done
grep -n 'BackendShape\b' skinny/crates/ir/src/lib.rs
grep -n 'RuntimeProvider\b' skinny/crates/codegen/src/grammar_profile.rs
grep -c '^%macro ' skinny/crates/bbnf-simd/ext/x86/x86inc.asm skinny/crates/bbnf-simd/ext/x86/x86util.asm skinny/crates/bbnf-simd/ext/x86/bbnf.asm
```

All seventeen verification commands re-run in this V3 CH7 pass;
outputs quoted inline at §2.1-§2.5; HEAD = `5aaab91d1`
(CHALLENGE-CONTEXT addendum on top of `daa14127f` V3 atomic
micro-fold).

**§3Z LOCK status**: CH7 lens at V3 = **3-cycle ≥95 % LOCK EXTENDED**
(V1 100 % + V2 100 % + V3 100 %; zero orphan REVISEs across all three
cycles; V=3 of V≤5 ceiling).
