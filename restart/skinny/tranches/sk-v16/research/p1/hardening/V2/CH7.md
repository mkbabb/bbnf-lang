# S-P1 V2 CH7 Overfit-Prune

Disposition: ACCEPT.

The pass profiles the whole JSON corpus set and keeps CSS/native-SIMD/FNV
admission blocked. S-P2 may consume only the named hot leaves as empirical
antecedents, with harness leaves and generated JSON-specific leaves routed as
prune/generalization subjects.
