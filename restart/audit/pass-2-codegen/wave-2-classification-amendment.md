# Wave 2 Amendment Classification — PASS-2 Continuation

This note records the verify-then-patch classification for the nine consolidated punch-list items routed to the PASS-2 amendment surface (`HARDENING-CONSOLIDATED.md` §4 items 5, 7, 14, 24, 27, 28, 29, 39, 46). Each row captures the pre-fill verification result, the surgical disposition, and the acceptance gate that the second commit must satisfy.

| Item | Verification result on `PASS-2.md` | Disposition | Surgery |
|---|---|---|---|
| 5 — PASS-3 emission contract | Zero matches for `consumer acceptance gate`, `emitted parse signature`, or `materialisation cost` gate text. §4 carries the contract table at lines 319-328 but offers no acceptance gates. | ADD | Insert PASS-3 consumer acceptance-gate table at the close of §4 binding emitted parse signatures, document/view metadata, and materialisation cost emission to named verification commands. |
| 7 — Lookbehind co-amendment | `Lookbehind` BIR variant exists (line 74); detection threshold exists (line 164); diagnostic `BBNF-SEM040` exists (line 478). Codegen-side ratification of `\|<` finite-width legality + diagnostic emission routing absent. | AUGMENT | Add a finite-width legality + diagnostic-routing paragraph adjacent to the `Lookbehind` lines so PASS-1's `BBNF1004` reaches PASS-2 emission; co-amend the `Lookbehind` row to name `\|<` syntax and the diagnostic id. |
| 14 — Runtime emission proof | Runtime emission table at lines 429-442 carries `Runtime module` + `Required smoke` columns only; `generated.rs`, `parser.rs`, `host.rs`, host source, layout source, error source, Pratt/SIMD source rows absent. | EXPAND | Replace the existing runtime emission table with a per-grammar table whose columns are `generated.rs`, `parser.rs`, `host.rs`, host source, layout source, error source, Pratt/SIMD source. State the hand-written-runtime prohibition in the body. |
| 24 — Per-grammar generated LOC table | Per-grammar table at lines 363-374 names current LOC, PASS-2 max, disposition; xtask wall ceiling column and SYNTHESIS carry pointer absent. | AUGMENT | Add an `xtask wall ceiling` column (or a parallel ceiling row) referencing §6's regen-cycle wall budgets; record that SYNTHESIS carries this table into Master/Architecture via item 24 of `HARDENING-CONSOLIDATED.md` §4. |
| 27 — Non-generated LOC + child-count | Table at lines 380-386 covers four codegen + xtask areas; `runtime`, `host` budgets and child-count proof absent. | EXPAND | Add `runtime/src/*` and `host/src/*` budget rows; add a child-count proof row covering the 4-10 sibling target per Lock 13; reference enforcing command per area. |
| 28 — Xtask wall baseline | §6 states `≤ 22s` regen check, `≤ 5s` BIR snapshot, `≤ 4s/12s` per-grammar; baseline observed-vs-provisional labels and owner column absent. | AUGMENT | Annotate each row with `observed` or `provisional (owner: <agent>)`; record the baseline source for each ceiling. |
| 29 — SOTA table delta | §7 perf trajectory rows carry competitor, target, mechanism, evidence — but not `Platform` and not the row-complete shape `Competitor / Dataset / Platform / bbnf target / PASS-2 mechanism`. simd-json M1 numbers and lightning-css platform line absent. | REBUILD | Replace the §7 trajectory table with a row-complete table inlining twitter `≤ 380 µs` M1 Pro vs sonic-rs `436 µs` / simd-json `424 µs`; canada `≤ 2.8 ms` vs sonic-rs `3.144 ms`; citm `≤ 750 µs` vs sonic-rs `854 µs` / simd-json `831 µs`; bootstrap `≤ 3.0 ms` vs lightning-css `4.16 ms`; animate `≤ 1.6 ms` vs lightning-css `1.97 ms`; simdjson OD `≥ 5 GB/s` M-series, `≥ 7 GB/s` x86. Demote non-throughput rows to a sister mechanism-gate table. |
| 39 — Carry ledger | Table at lines 482-487 has 4 rows (PASS-1, PASS-3 metadata, WASM parity, yaml onboarding) and 4 columns. Missing rows: TS production, BD.W5/J parity, PASS-3 API docs (distinct from metadata), publication, fixtures, `path-ts`, WASM ABI; column `Receiving gate` not separated from `Gate`. | EXPAND | Replace the carry ledger with an 8-row table whose columns are `Item`, `Receiver`, `Blocker`, `Receiving gate`. Cover all 8 carries named in HARDENING punch item 39. |
| 46 — OpenFrame retirement | Five OpenFrame mentions: line 7 (DISCARD verdict), line 36 (samply pathology archaeology), line 88 (no-clone-stack invariant), line 405 (perf trajectory removal-cost row), line 494 (punch-list deletion). All five are deletion-pathology language; the §7 row name `Remove OpenFrame checkpoint cost` reads as preservation against quick reading. | AUGMENT | Reword the §7 row to make the deletion-pathology framing unambiguous (`Replace OpenFrame checkpoint clone with TapeBuilder + BIR builder-frame`); reword the punch-list line to `delete OpenFrame-style runtime builders before migration begins`; verify no preservation language survives in any added prose. |

Acceptance gates after the amendment commit:

- `rg -nC2 'consumer acceptance gate\|emitted parse signature\|materialisation cost' restart/audit/pass-2-codegen/PASS-2.md` returns matches at the §4 close.
- `rg -nC2 'lookbehind\|\|<\|finite-width' restart/audit/pass-2-codegen/PASS-2.md` returns matches binding `\|<` finite-width to `BBNF-SEM040` and `BBNF1004`.
- `rg -nC2 'runtime emission\|generated\.rs\|parser\.rs\|host\.rs' restart/audit/pass-2-codegen/PASS-2.md` returns 9 grammar rows + yaml smoke row in the runtime emission table.
- `rg -nC2 'generated LOC\|generated_loc' restart/audit/pass-2-codegen/PASS-2.md` returns 9 grammar rows plus the SYNTHESIS-carry mention.
- `rg -nC2 'non-generated LOC\|child-count\|500 LOC' restart/audit/pass-2-codegen/PASS-2.md` returns the expanded budget table including `runtime`, `host`, and child-count proof.
- `rg -nC2 'xtask wall\|BIR snapshot\|regen wall' restart/audit/pass-2-codegen/PASS-2.md` returns budget rows annotated `observed` or `provisional (owner: ...)`.
- `rg -nC2 'twitter\|canada\|citm\|bootstrap\|animate\|simd-json' restart/audit/pass-2-codegen/PASS-2.md` returns the row-complete SOTA table with `Platform` column.
- `rg -nC2 'carry ledger\|receiver.*blocker.*gate' restart/audit/pass-2-codegen/PASS-2.md` returns an 8-row ledger with the three columns named in HARDENING.
- `rg -nC2 'OpenFrame' restart/audit/pass-2-codegen/PASS-2.md` returns only deletion-pathology references; no preservation framing.

Wave 2 correction note: this classification is the predicate for the Wave 2 PASS-2 amendment commit; the amendment lands surgical edits on `PASS-2.md` only and leaves PASS-2 sub-agent reports untouched, save for this note plus matching pointers added below.
