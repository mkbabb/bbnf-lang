# Phase 7.2 PASS-3 fold classification

Pre-fill verifications run against `restart/audit/pass-3-runtime/PASS-3.md` post-Phase-7.1 (commits `adbaaaa0` + `9cb92284`). Each item per the orchestrator dispatch is classified ABSENT / PARTIAL / DONE; surgery scope is named where amendment is owed. The audit corpus binds the classification: V1 fold candidates at `restart/research/V1-FOLD-CANDIDATES.md`, the `pointer!` → `path!` rename ledger at `restart/research/deferral-audit-3-bbnf-surface-directives.md` §4-§5, runtime adjudication at `restart/research/deferral-audit-5-runtime-pass3.md` §3-§7, and the post-Phase-7.1 lock manifest at `restart/locks/14-LOCKS.md` (Locks 5, 7, 10, 11).

| # | Item | Source | Pre-fill verification | State | Surgery scope |
|---|---|---|---|---|---|
| A1 | `pointer!` → `path!` macro rename across PASS-3 (~22 sites) | audit #3 §4.7 | `rg -n 'pointer!' restart/audit/pass-3-runtime/PASS-3.md` returns 22 hits across §1 verdict ledger, §2 user runtime, §3 path/select worked path, §3 consumer acceptance gates, §6 query directory annotation, §6.fixture separation phase 1, §6b BBNF-POINTER001/002/003 verbatim strings, §7 bench rows, §8 cross-pass hand-offs, §9 KEEP/REINVENT/DISCARD, §10 unresolved punch-list | ABSENT | Rename every `pointer!` to `path!` verbatim per audit #3 §4.7 ledger. The audit lists the 22 sites column-by-column; surgery is mechanical. |
| A2 | `BBNF-POINTER*` → `BBNF-PATH*` diagnostic-code rename | audit #3 §5; audit #5 §3 F-table | `rg -n 'BBNF-POINTER' restart/audit/pass-3-runtime/PASS-3.md` returns 6 hits (lines 119, 120, 122, 446, 447, 448 — three failure-path prose mentions plus three §6b verbatim diagnostic strings) | ABSENT | Rename codes (`BBNF-POINTER001` → `BBNF-PATH001`; ditto 002, 003). Rewrite verbatim help-text inside §6b: "unknown pointer segment" → "unknown path segment"; "pointer grammar inference failed" → "path grammar inference failed"; "terminal type for pointer" → "terminal type for path"; help-line `pointer!(Json => "/...")` → `path!(Json => "/...")`. Rename §6b Pointer-cookbook receiver titles to Path-cookbook (§validation, §explicit-grammar, §regen). |
| A3 | API surface description: `path!(Json, [...])` + `select!(Css, "...")` | audit #3 §4.7 lines 162-167; audit #3 §3 directive ledger | §3 already names "path-core owns parsing, lowering, validation, diagnostics, metadata schema, and runtime plans" + "path owns Rust proc macros: pointer! and select!" | PARTIAL | Replace the macro-family description so the `path` crate names `path!` + `select!`; preserve the path-core/path/path-ts crate split (path-ts now deferred per A8). |
| B1 | Public `format()` method on typed roots / `DocumentView` | audit #5 §6 fold F4; V1-FOLD-CANDIDATES §2 conflict #5 | `rg -n 'format\(\)' restart/audit/pass-3-runtime/PASS-3.md` returns zero. The `bbnf/self_host/internal` bench row at line 498 commits a parse + format roundtrip, but no public `format()` surface lands on `DocumentView` | ABSENT | Add a `fn format(&self) -> String` row to the §2 `DocumentView<'input>` trait sketch; bind in §3 visitor commitments; add §3 cookbook hand-off "the formatter is `@layout`-driven and grammar-emitted; the legacy `gorgeous` engine is archived per Lock 12; the public method walks `LayoutFacts` against tape identity." Receiving gate: G.W2 round-trip parse+format on `bbnf/self_host/internal` ≤ 100 ms. |
| C1 | Match-expression + tuple-pattern examples in `@host fn` body | audit #2 user-facing surface; V1-FOLD-CANDIDATES Tier 2 #19 + #20 | `rg -n '@host fn' restart/audit/pass-3-runtime/PASS-3.md` returns three hits: line 16 settled-authority list (six BBNF extensions), line 429 yaml host-route cell, line 575 rare-host-adapter row. No worked-body example carries match or tuple shape | DONE-AS-CROSS-PASS | PASS-1's grammar surface owns the production rules for match + tuple per Phase 7.1 §B6 (PASS-1 §6 EBNF amendment). PASS-3 is the user-facing runtime; the match/tuple surface lands on PASS-1 fold's plate. PASS-3 amends only the §0 / §1 directive recital to keep the six-extension list aligned with the post-Phase-7.1 six-directive grammar (`@import`, `@host fn`, `@error`, `@layout`, `@pretty`, `@token`). |
| D1 | Function-value runtime contract: closure environment frames stack-allocated; `&'i` capture lifetime | audit #5 §7 cross-cut; Lock 4 amendment per Phase 7.1 | `rg -n 'closure' restart/audit/pass-3-runtime/PASS-3.md` returns zero hits. The four committed closure sites (host chain, map, predicate, recovery) are referenced obliquely through §1 visitors row + §3 worked path; no runtime obligations bound | ABSENT | Append to §4 (Tape/direct runtime architecture) a "closure environment" paragraph: closures capture by `&'i Tape<'i>` reference only; environment frames are stack-allocated, not heap; the four committed sites (host-chain closure, map closure, predicate closure, recovery closure) lower to fixed BIR variants (`HostChain`, `ValueProject`, predicate-bool, `ErrorRecover`); no runtime function-pointer table; broader function-values amend through Lock 1 reuse-map semantics (deferred). |
| E1 | `parse-that-regex` sibling crate anchor; remove `regex-automata` references; remove `bbnf-regex` references | audit #4; audit #6; Phase 7.1 §D1 | `rg -n 'regex-automata\|bbnf-regex' restart/audit/pass-3-runtime/PASS-3.md` returns zero. `rg -n 'parse-that' restart/audit/pass-3-runtime/PASS-3.md` returns zero. `rg -n 'RegexProgram' restart/audit/pass-3-runtime/PASS-3.md` returns 2 hits (lines 444, 462, 524 — already corrected) | PARTIAL | PASS-3 already names `RegexProgram` (post-PASS-2 fold); zero `regex-automata` / `bbnf-regex` references survive. Phase 7.1 already renamed legacy `bbnf-regex` corpus-wide. PASS-3 fold owes one teaching anchor: append to §0 settled-authority recital "rich regex literals route through `parse-that-regex` (the regex sub-crate of `parse-that`); BBNF does not expose Unicode class algebra." |
| F1 | `path-ts` defer post-V1 | user Q5 + Q6 (Phase 7.1 §A4); audit #5 §4 fold disposition | `rg -n 'path-ts' restart/audit/pass-3-runtime/PASS-3.md` returns 6 hits: line 84 (legacy `bbnf-path-ts` deletion archaeology), line 90 (path-ts owns TS template tags), line 128 (registry deletion `rg` command path list), line 360 + 381 (crate tree headers), line 526 + 573 (workspace naming hand-off rows) | ABSENT | Rewrite §3 path/select commitment row: `path-ts` defers post-V1; J.W3 ships `path-core` + `path` (Rust) only. Strike `path-ts` from the §6 crate tree; archive the `crates/path-ts/src/` block under a "deferred post-V1 (TS-native parse+runtime fork)" subsection per Lock 7 amendment. Update §8 hand-offs and §10 unresolved punch-list rows accordingly. The §3 registry deletion `rg` command still scans `crates/path-ts` because deletion archaeology survives in-tree; framing changes to "scans surviving deletion archaeology + post-V1 deferred crates". |
| F2 | WASM ABI defer; H.W3/J.W3 measurement rows defer to V2 | user Q5 + Q6 (Phase 7.1 §A2 + §A5); audit #5 §5 | `rg -n 'WASM\|wasm32' restart/audit/pass-3-runtime/PASS-3.md` returns 9 hits: line 186 (playground wasm API legacy citation), line 457 (BBNF-HOST003 verbatim), lines 464-470 (WASM host primitive route paragraph), line 523 (cross-pass hand-off row) | ABSENT | Reframe §6b BBNF-HOST003 verbatim text (TS/WASM author): the diagnostic survives because TS/WASM is a real future target; the diagnostic body now reads "the Rust backend continues to compile; **WASM lower-and-bench programme defers post-V1 alongside the V2 `WasmBackend: Backend` impl per Lock 5 amendment**; until then, keep the chain Rust-only or defer it post-V1." Reframe §5 WASM host primitive route paragraph: H.W3 ABI descriptor is part of the Rust-line trait surface that V2's `WasmBackend` will implement; no V1 measurement row binds here. Strike `WASM ABI descriptor` row from §8 cross-pass hand-offs as a V1-active obligation; carry to V2 punch-list. Update playground wasm citation to mark as "legacy artefact + deferred V2 surface." |
| G1 | Diagnostic ledger updates (BBNF-PATH-* + reserved local-equality + OPT001/002 retained) | audit #3 §5; audit #5 §6b survey | §6b at lines 437-460 carries `BBNF-LIFE`, `BBNF-LAYOUT`, `BBNF-OPT`, `BBNF-GRAMMAR`, `BBNF-POINTER`, `BBNF-VISIT`, `BBNF-RECOVERY`, `BBNF-LOOKBEHIND-WIDTH`, `BBNF-TYPE001`, `HostSignature`, `ChainStep`, `WasmHost`, `BBNF-GEN001` codes. `BBNF-LOCAL-EQUALITY-ANNOTATION` reserved-code row absent. `BBNF-OPT001/002` non-selection codes already present at lines 443-444. `BBNF-PATH*` rename pending per A2 | PARTIAL | Per A2 rename three POINTER codes to PATH. Add a reserved-code row at the end of §6b: `BBNF-LOCAL-EQUALITY-ANNOTATION` reserved (GADT V2; user-facing surface deferred to V2 amendment per Lock 4 amendment + ARCH §8.2). `BBNF-OPT001/002` rows survive as authored. |

## Surgery routing

The Phase 7.2 amendment commit will land:

1. §0 — settled-authority recital extended with `parse-that-regex` anchor (E1) and the six post-Phase-7.1 directives (`@import`, `@host fn`, `@error`, `@layout`, `@pretty`, `@token`) replacing the V6-era six-extension list (C1).
2. §1 verdict ledger — Path/select DSL row renamed `path!` (A1).
3. §2 user runtime commitments — `DocumentView<'input>` trait gains `fn format(&self) -> String` (B1).
4. §3 path/select commitments — macro names rebound to `path!` + `select!` (A1, A3); `path-ts` defer per F1; the worked path's macro callsites and failure-path prose rename (A1, A2).
5. §4 tape/direct runtime — closure environment paragraph appended (D1).
6. §5 error recovery — WASM host primitive route reframed for V2 defer (F2).
7. §6 crate tree — `path-ts` archived under "deferred post-V1" subsection (F1).
8. §6b diagnostic ledger — three `BBNF-POINTER*` rows renamed to `BBNF-PATH*` with verbatim help-text rewritten (A2); `BBNF-HOST003` (WASM) reframed for V2 defer (F2); `BBNF-LOCAL-EQUALITY-ANNOTATION` reserved row added (G1).
9. §7 benchmark gates — `json/citm/pointer` row name → `json/citm/path`; `pointer!` callsites in surface-under-test column → `path!` (A1).
10. §8 cross-pass hand-offs — `pointer!`/`select!` mentions rename (A1); `WASM ABI descriptor` carry retired from V1 active obligations (F2).
11. §9 KEEP / REINVENT / DISCARD — `pointer!` → `path!`; "explicit and implicit pointer forms" → "explicit and implicit path forms" (A1).
12. §10 unresolved punch-list — `pointer!`/`select!` mentions rename (A1); `path-ts` punch-list row reframes for V1 defer (F1).

## Closing posture

Three corpus-wide renames + one new public method + one runtime-contract paragraph + one crate-tree archive + one V2-defer reframe. Mechanical surgery on the rename axis; targeted authorial work on the format/closure/WASM axis. The cross-pass match/tuple surface stays with PASS-1 (no PASS-3 surgery owed beyond directive-list realignment).
