# Pass Omega V10 — CH5 HIDDEN-COUPLING — Cycle V4

Lens: CH5 HIDDEN-COUPLING. Does any Ω-C lock amendment imply a parallel substrate
/ renamed sidecar / Lock-1 violation / Track1≡Track2 dishonesty? Does the
single-substrate union hold across every staged delta?

Scope: the 11 staged lock-addendum clauses in `locks-diff.md` + the cross-artefact
CRUD operations (ΩC disposition, ΩD §13.6→SK-V19 re-key + §13.7 stand-up, ΩA
CF-01..CF-12 / CRUD-1..CRUD-6 / OA-V10-01..12, the SCANNER-UNIFY DEFER bundle, the
firewall tense, the retarget clause, the handoff/migration/architecture deltas).
Census = 14 amendment/CRUD items (matching the V3 census).

## V1→V2→V3 carry: every prior REVISE/REJECT is RESOLVED and re-verified at HEAD

The cumulative prior burn is V1 (3 REVISE: firewall tense, dangling SCANNER-UNIFY
owner, free-floating RED self-gate), V2 (1 REJECT byte-identity-to-3C + 1 REVISE
cursor-on-RED-roster), V3 (1 REVISE movemask-census mis-singularization). All seven
are folded into the staged tree and re-verified RESOLVED:

- **V1-01 (firewall tense) RESOLVED.** The relocated-seam clause reads "the un-fork
  is UNBUILT at HEAD (`RuntimeEmitterKind{CompiledLowering,RequestFacts}` still live,
  `runtime_generator.rs:17`-`25` still branches on `request.profile_contract.emitter`)
  … a PLANNED-at-G3 gate … NOT a present-tense fact." Verified live:
  `grammar_provider.rs:40` carries `enum RuntimeEmitterKind`, `:110` reads
  `request.profile_contract.emitter`, `runtime_generator.rs:17`/`:25` match on both
  variants. Tense matches reality.
- **V1-02 (SCANNER-UNIFY owner) RESOLVED.** The single-substrate clause cites the
  owning `MP-3B-SKV18-D07` and stages the reconcile into master-plan-diff Diff 4 §24
  row (c). Verified: `3B:177` is the `MP.SK19.SCANNER-UNIFY` row ("the
  renamed/parallel-scanner risk is ACTIVE … Decide UNIFY vs renamed-parallel-scanner;
  ≈+217 reconcile + 8/9 OnceCell re-route"); `3B:197` is D07; master-plan-diff `:261`
  row (c) carries it verbatim. DEFER no longer dangling.
- **V1-03 (RED self-gate) RESOLVED.** The Pattern-H clause carries the CF-09
  in-clause disposition ("do NOT bolt a 9-name regex widen as an SK-V18 patch — the
  R16 PartialEq full-row collapse … are a SK-V19 reconcile (D11b, ≈+217)").
- **V2-01 (cursor-on-RED-roster) RESOLVED.** The cursor-generality (b) config-breadth
  re-census is GATED on the Pattern-H 9-ident RED disposition "until the CF-09 / D11b
  SK-V19 reconcile lands."
- **V2-02 (byte-identity-to-3C REJECT) RESOLVED.** ΩC `:22`-`25` reads "NOT
  byte-identical to `3C-locks-v+1-diff.md` (md5 differs; 6 of 11 clauses changed);
  git apply --check exits 0; 11-clause / 9A-11M-0R-1D preserved." ΩA CRUD-3 (`:246`)
  reads "diff body differs from 3C by the V1–V3 hardening deltas." Honest
  hardened-FROM-3C framing everywhere.
- **V3-01 (movemask census) RESOLVED.** The single-substrate clause line 71 now reads
  "the `vaddv_u8` shift-add pack … is already replicated NON-delegating ≥three-fold at
  HEAD — `byte_class_from_eq_set_64.rs:79`-`87`, `bracket_depth_mask_64.rs:74`, and
  `comment_body_mask_64.rs:68` … plus a fourth non-delegating SHRN duplicate at
  `match_tiny_plain_string.rs:100`; the `bbnf_simd_single_mask_convention` co-gate's
  distinct-non-delegating-pack count is therefore ≥4 at G2 entry … a G2-entry author
  must gate against the full multi-pack census, not one named site." Verified live: all
  four sites carry a local `movemask_u8x16` — three byte-identical `vaddv_u8` shift-add
  bodies (`byte_class_from_eq_set_64.rs:83`-`84`, `bracket_depth_mask_64.rs:78`-`79`,
  `comment_body_mask_64.rs:72`-`73`) + a fourth SHRN duplicate
  (`match_tiny_plain_string.rs:101`), beside the canonical `movemask.rs:4`-`5` SHRN
  pack. The V3 evidence anchor is folded; the co-gate count is now stated ≥4. RESOLVED.

## Spot-Verifications (load-bearing items, re-run at HEAD)

- **`git apply --check` on staged `locks-diff.md`: exit 0** against live
  `restart/locks/LOCKS.md`. Hunk `@@ -622,6 +622,33 @@`; insertion AFTER the SK-V17
  Lock 16 clause (`LOCKS.md:622`) and BEFORE `## v+1 Governance Boundary` (`:625`).
  SK-V15 (`:581`-`607`) / SK-V17 (`:610`-`622`) addenda neither restated nor edited.
  APPLIES CLEANLY.
- **`git apply --check` on `architecture-delta.staged.md`: exit 0.** The two gated
  hunks (§7.4 title re-key `:1370`; §9.2 phantom-vehicle strike `:1997`-`:2000`) are
  byte-exact against live ARCHITECTURE.md.
- **16 numbered locks** (grep `^[0-9]+\. \*\*` = exactly 16); no Lock 17. **Five
  `BackendShape`** confirmed: `skinny/crates/codegen/src/lower/mod.rs:20`-`24` (the
  five LOWERING arms) + `ir/src/cost.rs:334` `all_backend_shapes() -> [BackendShape; 5]`.
  No sixth shape.
- **Both PLANNED co-gate symbols rg=0**: `runtime_target_rows_collapsed` and
  `bbnf_simd_single_mask_convention` return zero in `skinny/crates`+`skinny/xtask` —
  PLANNED, never live. HONEST.
- **§H wave resolved**: live MASTER-PLAN §13.6 header at `:974` is the SK-V18
  Tape-Fold receiver verbatim ("downstream of SK-V15; active after SK-V17 skinny
  W0-W5 + G-Omega"); master-plan-diff Diff 1 re-keys it to SK-V19 Totality-Fold
  (MP.SK18.W*→MP.SK19.W*, F1-F9 preserved verbatim), Diff 2 stands up a NEW §13.7
  SK-V18 GENERALIZATION 12-wave block. The tranche-IDENTITY pivot (certified SK-V18 =
  GENERALIZATION → §13.7; `crates/core/` tape-fold → SK-V19, re-keyed §13.6) is
  CONSISTENT across ΩD, master-plan-diff, ΩA CF-01/CF-07, ΩF, and 3B. The `crates/core/`
  fold is correctly NOT the certified SK-V18.
- **REDRESS reference resolved**: the CollapsedStage clause cites
  `skinny/REDRESS.md:2795`-`2944` finding `:2928`-`2933`. Verified: that span is the
  M5-Max scalar-cheaper-than-SIMD-cursor finding ("`G-W3-UNION-SUBSTRATE` is therefore
  retired, not merely blocked. No SK-V9 wave may force, amend, or split W3 to preserve
  the same union-substrate"). The clause GATES promotion-past-`diagnostic-only` against
  that RETIRED prior; it does NOT revive it. NOT a revived REDRESS route.
- **CollapsedStage cross-leg anchor split verified.** The C9 clause cites `:1289` as
  PRIMARY (demote-stable, NOT in the OA-V10-05 splice set) and `:1206` as SECONDARY (an
  OA-V10-05 splice target). Verified live: `ARCHITECTURE.md:1289` = "CollapsedStage =
  UNKNOWN-2D-05, no admission without a 2E source-backed [strategy]"; `:1206` = the
  ledger row carrying "aarch64 candidate is UNKNOWN-2D-05 (requires 2E source-backed
  aarch64 strategy before any aarch64 admission)" verbatim; `:1151` = the OA-V10-05
  anchor. The architecture-delta Anchor-2 HALT-NOTE explicitly preserves the
  `aarch64 candidate is UNKNOWN-2D-05` clause VERBATIM while demoting the x86 co-require
  wording, so the C9 SECONDARY cite is not stranded under the ARCHITECTURE leg. Clean
  cross-leg coupling handling.
- **e-graph regression-guard grounded.** The cursor-generality clause's
  decision-engine note cites `NormalizeDirectSinkCost` "live." Verified:
  `backend_egraph.rs:75` instantiates it (`let normalize = NormalizeDirectSinkCost;`),
  `:191` defines the struct, `:193` is its `impl Rewrite<DecisionNode, NoAnalysis>`.
  The "≥1 asserted e-graph rewrite, live" claim cites both definition and call site.
- **`css_provider_source` field collision DISCLOSED.** The firewall clause's PLANNED
  predicate `css_provider_source == generated` is flagged "distinct from the live
  `bbnf-bench/src/report.rs` bench-report field of the same name." Verified:
  `report.rs:1168` `pub css_provider_source: String`, used at `:1337`,`:1354` as a
  bench-report field. The diff DISCLOSES the collision. Not a renamed sidecar.
- **`simd-scan` second classifier substrate** (`crates/simd-scan/src/lib.rs:67`,
  `pub use alphabet::{KernelShape, NibbleLut, StructuralAlphabet, WideLut}`, distinct
  from the `:68` `{StructuralIndex, next_structural_at_or_after}` probe API): verified
  verbatim, disclosed, priced (≈+217 + 8/9 OnceCell re-route), DEFERRED to SK-V19 via
  the single-priced `(COH18-015 / D-SKV18-L01 / MP.SK19.SCANNER-UNIFY)` bundle owned by
  `MP-3B-SKV18-D07`, staged into master-plan-diff Diff 4 §24 row (c). Skinny `rg=0` NOT
  claimed as a totality single-substrate proof.

## The fresh V4 defect — the retarget clause C8 elides that the live recursive shell is test-only-reachable at HEAD

The retarget-not-author clause (C8) asserts, in present tense, that the eq-set kernel
"passes the checkasm differential plus the `neon_significant_skip_matches_scalar` guard
over the real corpora, and the skip stops at `([{'"/`." It gates ADMISSION on "a
non-`#[cfg(test)]` caller census over the P3-collapsed singular call site" and scopes
the speedup CLAIM to "the H1 quiet corpus-in-timer re-capture." The coupling MECHANISM
is sound — RETARGET (not author), grammar-neutral inner eq-set kernel carrying its
byte-set as CALLER DATA, no author-from-scratch loop, the ADMISSIBLE-vs-REJECTED
boundary matching the ΩB REDRESS-53 disposition (RETARGET NEON onto the EXISTING in-loop
scalar shell = ADMISSIBLE; a parser-local second scanner = REJECTED). The kernel and its
guard ARE live:

- `find_css_significant` is defined at `runtime_simd.rs:169`.
- `neon_significant_skip_matches_scalar` is the live `#[test]` at `lib.rs:562`, with the
  five-corpus array shown, calling `find_css_significant` at `lib.rs:574`.

BUT a full caller census at HEAD shows `find_css_significant`'s ONLY caller is that
`#[test]` (`lib.rs:574`); it does NOT appear in the generated CSS runtime
(`grammars/` rg=0) and has no non-test caller. The kernel is DEAD at admission on the
live path — the clause's OWN admission gate ("non-`#[cfg(test)]` caller census") is
PLANNED-at-G6, not a HEAD fact. The clause says "passes the guard over the real corpora"
present-tense without the parallel "UNBUILT at HEAD / test-only-reachable" tense marker
that the V1-01 REVISE forced onto the sibling relocated-seam clause ("the un-fork is
UNBUILT at HEAD … a PLANNED-at-G3 gate … NOT a present-tense fact"). C8 carries the
gate-side conditional ("the admissible G6 NEON acceleration RETARGETS"; "Any speedup
CLAIM DEFERS to H1") but elides the BUILT-state on the live path, where the firewall
clause discloses it explicitly. This is the same tense-eliding the firewall clause was
corrected for, on the substrate's other accelerated leaf.

CORRECTION (locks-diff retarget-not-author clause): mark the live-path state with the
firewall clause's tense — "at HEAD `find_css_significant` is reached ONLY from the
`#[cfg(test)] fn neon_significant_skip_matches_scalar` guard (`lib.rs:574`); the
non-`#[cfg(test)]` caller-census admission is PLANNED-at-G6, not a HEAD fact" — so the
present-tense "passes the guard over the real corpora" is not read as a built,
live-path acceleration when the retarget is unwired at HEAD. REVISE.

## Coupling Resolution (the core lens question)

The single-substrate union does NOT silently hold across the totality tree, and the
amendment is HONEST about every place it breaks — the correct CH5 disposition:

1. **Second classifier substrate** (`simd-scan` `NibbleLut`/`WideLut`): real,
   disclosed, PRICED, DEFERRED to SK-V19 via the single-priced bundle owned by
   `MP-3B-SKV18-D07`, staged into master-plan-diff Diff 4 §24 row (c). Skinny `rg=0`
   NOT claimed as a totality proof. NO coupling dishonesty.
2. **Relocated-seam firewall**: ONE `render(program)` reading shape only from
   `backend_shape`; `RuntimeEmitterKind`-DELETED is the post-G3 gate, verified
   still-live at HEAD. Skinny G2 scans the skinny CSS surface; totality
   `crates/core/src/runtime/css_l4/` is the SK-V19 seam (ΩF CH2 row). md5-distinctness
   named NECESSARY-NOT-SUFFICIENT; the `runtime_target_rows_collapsed` PartialEq co-gate
   mandatory. migration-delta `:87` and ΩB `:121` both state the 3-co-gate is a
   CONJUNCTION, not an md5 check — defends against Track1≡Track2 dishonesty.
3. **Lock-14 RED disclosed (OA-V10-10 / CF-09)**: self-gate asserts ZERO, returns 13;
   9-row grammar-named `idents` table live at `strategy.rs:137`-`185`. Clause states
   FALSIFIED/RED and tees the R16 collapse to SK-V19 (CF-09 in-clause). HONEST.
4. **Lock-1 intact; no sixth shape; no retained sidecar**: CollapsedStage is a SHAPE
   SLOT (diagnostic-only); the single-substrate clause re-homes `scan_balanced` into the
   ONE skinny `bbnf-simd` consuming EXISTING kernels + the ONE canonical SHRN movemask.
   No public substrate API; transient-single-call retention.
5. **architecture/migration/handoff deltas**: arch-delta = two `git apply`-gated hunks
   (exit 0) + four re-grep-HALT anchored splices, no 6th shape, CollapsedStage stays a
   SHAPE SLOT; migration-delta `:119` fences "an SK-V16/V17-rejected shape (second
   scanner / structural-stream driver / parser-local cursor) must not re-enter
   G2/G4/G6"; handoff-delta is prose (no apply-able fence), `generator_grammar_count ==
   3` framed as a PROVE-exit gate. No hidden coupling.

## Disposition Census (14 items)

| # | Item | Lens verdict |
|---|---|---|
| 1 | Named-primitive (a)-(d) gate clause (LAC-1E-V5-01) | ACCEPT — machine-checked four-conjunct gate (`*_abcd_pass` falsifier arms); no coupling. |
| 2 | Relocated-seam firewall + un-fork clause (LAC-1E-V5-02+2D) | ACCEPT — V1-01 RESOLVED; "UNBUILT at HEAD … PLANNED-at-G3" matches live `runtime_generator.rs:17`-`25` / `grammar_provider.rs:40`,`:110`; `css_provider_source` bench-field collision DISCLOSED; md5-distinct NECESSARY-NOT-SUFFICIENT. No renamed sidecar. |
| 3 | Neutrality-proof clause (LAC-1E-V5-03+2C) | ACCEPT — forced `balanced_component_scan`→`css_balanced_component_scan` demotion IS the discharge; `scoped non-JSON witness` per live MP.NW6 (`MASTER-PLAN.md:662`); third witness inherits the PROVE falsifier. |
| 4 | aarch64-ONLY clause (LAC-1E-V5-04+2E) | ACCEPT — x86 = P1 DELETION target; sharpens SK-V17 aarch64-PRIMARY to aarch64-ONLY; FEAT_SVE2 ABSENT honored; no x86 close path. |
| 5 | Verbatim-blob-courier clause (LAC-1E-V5-05) | ACCEPT — `CSS_GENERATED_RS:&str` courier (`runtime_generator.rs:701`) REJECT-as-grammar-driven; round-trip byte-equivalence binding. |
| 6 | Green-by-exclusion precondition clause (LAC-1E-V5-06) | ACCEPT — STRENGTHENS Lock-14 (weak `SKV15_W2_EXTRA_COVERAGE_ROOTS`→strict `GENERIC_SCAN_ROOTS`, extends `FORBIDDEN_GENERIC_TOKENS`); NOT a Lock-14 narrowing. |
| 7 | Single-SIMD-substrate + one-movemask clause (LAC-2F-V3-01) | ACCEPT — V3-01 RESOLVED; line 71 now cites all FOUR non-delegating packs (`byte_class_from_eq_set_64.rs:79`/`bracket_depth_mask_64.rs:74`/`comment_body_mask_64.rs:68` + `match_tiny_plain_string.rs:100`) and states the co-gate count is ≥4 at G2 entry (verified live); `simd-scan` second substrate disclosed+deferred; `D-SKV18-L01` bundle resolves to 3C:90. |
| 8 | Retarget-not-author clause (LAC-2E-V6-02+2F) | **REVISE — DEFECT-CH5-V4-01.** Coupling MECHANISM sound (RETARGET-not-author; grammar-neutral inner eq-set kernel; non-`#[cfg(test)]` caller census = the G6 gate; speedup DEFERS to H1). But C8 asserts present-tense "passes the … `neon_significant_skip_matches_scalar` guard over the real corpora" while at HEAD `find_css_significant` (`runtime_simd.rs:169`) is reached ONLY from the `#[test]` guard (`lib.rs:574`), absent from generated `grammars/` (rg=0) — DEAD at admission on the live path. The firewall clause (item 2) got the "UNBUILT at HEAD … PLANNED-at-G3" tense marker for the same condition; C8 did not. CORRECTION: add the live-path tense marker — "at HEAD reached ONLY from the `#[cfg(test)]` guard; the non-`#[cfg(test)]` caller-census admission is PLANNED-at-G6, not a HEAD fact." |
| 9 | CollapsedStage shape-slot clause (LAC-2D-V3-04) | ACCEPT — SHAPE SLOT, diagnostic-only; gates promotion against RETIRED REDRESS 96/97/98 (`REDRESS.md:2928`-`2933` G-W3-UNION-SUBSTRATE retired, verified); PRIMARY anchor `:1289` (demote-stable) + SECONDARY `:1206` (OA-V10-05 splice target with verbatim-preserve HALT-NOTE) verified; UNKNOWN-2D-05 discharge does not re-open the x86-refused row. No revived route, no sixth shape. |
| 10 | Cursor-generality re-anchor clause (1A-LOCK1-AMEND-001+2D) | ACCEPT — V2-01 RESOLVED; (b) config-breadth re-census GATED on the Pattern-H RED disposition (CF-09 / D11b); e-graph guard grounded (`NormalizeDirectSinkCost` live `backend_egraph.rs:75`,`:191`-`193`); `<G>`-strike sound; arch-delta §9.2 phantom strike applies clean. |
| 11 | Pattern-H re-census clause (LAC-1E-V5-07+2C) | ACCEPT — V1-03 RESOLVED; CF-09 in-clause bounds the RED self-gate; 9-row table live at `strategy.rs:137`-`185`; `css_types.rs` SK-V19-routed (`migration-delta:89`). |
| 12 | ΩC provenance framing + ΩA `:13` / CRUD-3 `:246` | ACCEPT — V2-02 REJECT RESOLVED; ΩC `:22`-`25` "NOT byte-identical (md5 differs; 6 of 11 clauses changed)"; CRUD-3 `:246` "diff body differs from 3C by the V1–V3 hardening deltas." Honest hardened-FROM-3C framing. |
| 13 | ΩD §13.6→SK-V19 re-key + new §13.7 + Diff 4 §24 SK-V19 tee-up (MP CRUD) | ACCEPT — tranche-identity pivot CONSISTENT across ΩD / master-plan-diff / ΩA CF-01,CF-07 / ΩF / 3B; live MP §13.6 header `:974` verified as the re-key target; the three totality-tree leaks (9-ident, `css_types.rs`, `simd-scan` asymmetry) staged as SK-V19 receivers, not bolted into SK-V18; staged-only. |
| 14 | ΩC disposition matrix (9A/11M/0R/1D, 21 candidates) + ΩA CRUD-1..6 / CF-01..12 | ACCEPT — ZERO silent drops, one disposition per candidate; lone DEFER (LAC-2F-V3-03) is AUDIT-SCOPE with a real `ls`-both-trees re-entry trigger; CF-12 preserves the 16-lock + 5-shape canon BYTE-VERBATIM across every CRUD leg. Not a coupling. |

## Notes for Orchestrator (out-of-lens)

- Live surfaces (`MASTER-PLAN.md`, `LOCKS.md`, `MIGRATION.md`, `HANDOFF.md`,
  `ARCHITECTURE.md`) are clean at HEAD — the staged-only invariant is honored across
  all six artefacts + the four `*.staged.md` deltas + the two `*-diff.md` files.
- The `D-SKV18-L01` token is the cursor-generality DEFER decision that 3C:90
  single-prices INTO the scanner-unification bundle; it resolves and is NOT a mislabel
  (confirmed against the converged 3C source over the V1→V3 chain). Out-of-lens.
- The CH5-DEFECT-V1-02 / -V1-03 SCOPE-NOTE markers inside the diff are PRIOR-cycle
  defect IDs carried as self-documenting scope notes; legitimate annotations.

## Verdict

The substrate-union story is COUPLING-HONEST across every staged delta: every place the
union does NOT hold on the totality tree (the `simd-scan` second `NibbleLut`/`WideLut`,
the unbuilt un-fork, the RED Lock-14 self-gate) is DISCLOSED and DEFERRED to SK-V19 —
priced, owned by `MP-3B-SKV18-D07`, staged into master-plan-diff Diff 4 §24 — never
claimed green and never bolted into an SK-V18 gate. `git apply --check` exits 0 on both
the locks-diff and the architecture-delta; 16 locks; five `BackendShape`; both co-gate
symbols rg=0. No revived REDRESS route (the CollapsedStage clause GATES against the
RETIRED streamed-cursor finding; the cross-leg `:1206` anchor is verbatim-preserved by
the arch-delta HALT-NOTE), no non-applying diff, no Lock-14 NARROWING (green-by-exclusion
TIGHTENS it), no Lock-1 violation, no sixth shape, no public substrate API, no retained
sidecar, no Track1≡Track2 dishonesty (md5-distinctness named insufficient, the 3-co-gate
CONJUNCTION mandatory). All seven prior-cycle defects (V1 firewall tense / SCANNER-UNIFY
owner / free-floating RED, V2 byte-identity REJECT / cursor-on-RED-roster, V3
movemask-census mis-singularization) are folded and re-verified RESOLVED at HEAD.

The lone fresh V4 defect (item 8) is second-order and is the EXACT sibling of the
V1-01 firewall-tense REVISE applied to the substrate's other accelerated leaf: the
retarget-not-author clause asserts present-tense "passes the guard over the real corpora"
for an eq-set kernel whose live recursive shell (`find_css_significant`) is reached ONLY
from a `#[cfg(test)]` guard at HEAD — DEAD at admission on the live path — without the
"UNBUILT at HEAD / PLANNED-at-G6" tense marker the firewall clause carries. The coupling
MECHANISM is honest; the tense is over-stated on the live-path build state. 1 REVISE /
14 = 7% — BELOW the ≥30% cycle-V1 floor, which is CORRECT and EXPECTED: V1 burned the
three load-bearing firewall/substrate/self-gate REVISEs, V2 burned the byte-identity
REJECT + the cursor-on-RED-roster REVISE, V3 burned the movemask-census REVISE; all
seven were folded into the staged tree, so V4 finds only the second-order retarget-clause
tense residue — the symmetric sibling of the already-burned V1-01 — not under-scrutiny.

TALLY accept=13 revise=1 reject=0
