# CH4 COST — T-P3 SK-V18 Hardening, Cycle V5 (final confirmation)

Lens: CH4 COST. Every delta states a LOC budget, a propagation cost (surfaces
touched), a risk class, and a wave alignment; 3B NEW waves carry a same-wave
consumer; 3C dispositions are realistic. Cross-scope, uncited, revived-refuted,
silent-drop, and cost-laundering deltas are REJECT bait. Cycle V1 expects ≥30%
REVISE; V5 is the V≤5 ceiling **final-confirmation** cycle whose charge is an
INDEPENDENT re-verification on the live tree that the V3 cost matrices still
close after the V4 citation fold — NOT manufacturing REVISEs the artefacts have
already discharged across V1-V4. (The pre-existing SK-V15-era V5/CH4 file,
target `77b6e9fd7`, is overwritten here.)

## Scope reconciliation (the V5 CHALLENGE-CONTEXT is SK-V15-era boilerplate)

The V5 `CHALLENGE-CONTEXT.md` names target packet `77b6e9fd7`
("docs(sk-v15-t-p3): repair V4 citation finding") and an expected invariant of
**67 Pattern-H runtime files** — both SK-V15-era. The LIVE target is the SK-V18
synthesis fold: the 6 p3 artefacts at mtimes 20:22–20:54 (Jun 1) are
UNCOMMITTED working-tree modifications over the last committed (SK-V15) version
`77b6e9fd7` (`git diff --stat HEAD -- 3D-skinny-fold.md` = +127/−58). HEAD is
`3f6eb603d`. This is the same SK-V18 packet my V4 CH4 verdict reviewed
(`accept=115`). The live Pattern-H census is **71** (not 67) — confirming the
boilerplate is stale and the artefacts' re-keyed 71 invariant (3C-L13 / 3E-D11 /
3A-D12) is the correct one. The `M` dirty state on all 7 p3 files is the
proposal fold itself; T-P3 "proposes only" forbids editing the LIVE
`ARCHITECTURE.md`/`LOCKS.md`/`MIGRATION.md`/`HANDOFF.md` (none touched — the v+1
diff is an extracted candidate, `git apply --check` only), not the p3 proposal
documents. No boundary fault.

## Verdict summary

The packet is **CH4-CLEAN**. The single V3 REVISE — the 3D W-PRUNE summary row's
internally inconsistent component sum — was folded in V4 and remains folded on
disk at `3D:157`. Every load-bearing CH4 fact independently re-verifies on the
live tree at `3f6eb603d`. No surviving CH4 cost defect: no uncited delta, no
revived refuted route, no silent-drop candidate, no cross-scope violation, no
W12/W13 overflow, no broad-CSSOM-parity smuggle, no challenge-time
implementation overflow, no doc-only implementation gate, no cost-laundering of
the SK-V19 ≈+217 figure into a skinny budget, no double-budgeted thesis row.
`CH4-V2-001` (per-delta coverage matrices) and `CH4-V2-002` (the per-wave sum
reconciliation) both still close after the citation fold.

The P2 "today 48" baseline that V4 deferred is NOT a defect: the live
`skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` is EXACTLY 48
(`grep -c 'measure_mbps\|lightningcss_facts'`); the `benches/nonjson_css_l4.rs`
sibling is 7 and `bin/gate.rs` is 16 (whole-crate 71). The artefact's `(today
48)` is file-scoped to `src/nonjson_css_l4.rs` and resolves exactly. (My V4
verdict's whole-crate "64" figure was itself imprecise — the whole-crate is
48+7+16 = 71 — but the artefact's 48 is correct; this is a V4-attestation
nit, not an artefact defect.)

## Independent spot-verification of load-bearing CH4 facts (all resolve)

| claim | artefact | on-disk result (`3f6eb603d`) | verdict |
|---|---|---|---|
| extracted 3C v+1 diff applies | 3C-v+1-diff | `awk`-extract (37 lines) → `git apply --check` **CLEAN**; header `@@ -622,6 +622,33 @@`; adds 11 bullet clauses under ONE `## SK-V18 T-P3 v+1 Crystallisation Addendum` — adds NO numbered lock / directive / substrate / sixth shape (diff text states so verbatim) | RESOLVES |
| 16 numbered locks (v+1 ADD adds none) | 3C invariant | `grep -cE '^[0-9]+\. \*\*' LOCKS.md` = **16** | RESOLVES |
| 5 `BackendShape` variants + `select_lowering` matches them | 3A-D03 / 3E-D12 / all | live enum `skinny/crates/ir/src/lib.rs:340`-`346` = {EagerTape,OffsetTape,EventTape,SinkOnly,CollapsedStage}; `skinny/crates/codegen/src/lower/mod.rs:18` `select_lowering(cost)` matches on exactly those 5 → the 5 `&*::LOWERING` | RESOLVES |
| `lower/mod.rs:18` cite | 3A-D03 `lower/mod.rs:18`; 3E-D12 `skinny/crates/codegen/src/lower/mod.rs:18`-`24` | the FULL path `skinny/crates/codegen/src/lower/mod.rs:18` is the live `select_lowering` locus; 3E carries the full path, 3A the bare path — both resolve there (NOT `crates/core/src/lower/mod.rs`, which my own V4 verdict mis-cited) | RESOLVES — artefacts correct; V4 self-attestation path corrected here |
| 7 byte-identical css_l4 replicas, md5 `b654562c` | 3D/3F/3B P3 | 7 × `css_l4_*/generated.rs` all md5 `b654562ccff46ed62dd48e9ace325830`; 6×910 = **−5460** exact | RESOLVES |
| SPEC P3 = ≈−5500 (verbatim) | 3D:157 / 3B-D04 / 3F-MH-009 | `SPEC:435` verbatim "≈ −5500 (6×910 replica bodies deleted; +1 `PartialEq` derive; ~−40 collapsed rows)" | RESOLVES |
| net headline −10800 | all | `SPEC:22,:61,:454,:571` verbatim **−10800**; SPEC has NO `10685` literal (`grep -c 10685 SPEC == 0`) — the per-wave −10685 is artefact-COMPUTED, both exhibited under the ≈ tilde (correct CH4 discipline) | RESOLVES |
| per-wave sum = −10685 | 3D:157 / 3B-D04 / 3F-MH-009 | −4500 −700 −5500 +15 +0 = **−10685** (arithmetic coheres; bracketed P3 component breakdown cites `SPEC:435`) | RESOLVES |
| x86 census (P1 "today 28") | 3B §13.7-P1 / 3F-MH-008 | `find …/x86_64 …/ext/x86 -type f` = 24 + 4 = **28** | RESOLVES |
| metalang leak (P5 "today 7") | 3B §13.7-P5 / 3A-D12 / 3D | `grep -c parse_w11_1_number runtime/src/grammars/json/generated.rs` = **7** | RESOLVES |
| P2 falsifier baseline "today 48" | 3B §13.7-P2 | `grep -c 'measure_mbps\|lightningcss_facts' src/nonjson_css_l4.rs` = **48** (file-scoped, exact) | RESOLVES (not a defect) |
| `const CSS_GENERATED_RS: &str = r#"` courier | 3A-D05 / 3F-MH-010 / 3C-L06 | verbatim at `runtime_generator.rs:701` | RESOLVES |
| `css_types.rs` 66 LOC, lock-NAMED | 3F-MH-013 / 3C-L13 / 3A | `crates/core/src/css_types.rs` 66 LOC; NAMED verbatim in `LOCKS.md:349` | RESOLVES |
| live e-graph `Rewrite` (LAC-2D-V3-03 realism) | 3C disposition / v+1 cursor clause | `passes/src/backend_egraph.rs:191` `struct NormalizeDirectSinkCost;` `:193` `impl Rewrite<DecisionNode,NoAnalysis>`; instantiated `:75` `let normalize = NormalizeDirectSinkCost;` (non-`#[cfg(test)]`) | RESOLVES |
| 9-row grammar-named `idents` (L13 / R16) | 3C-L13 / 3A-D11b / 3E-D16 | `strategy.rs:137`-`185` = **9** `idents:` rows (json/sheets/cssl4/bbnf/csv/math/bnf/ebnf/csspretty); `:137` = `["JsonParser","JsonGrammar"]` (SK-V19-fenced) | RESOLVES |
| L13 self-gate "13 vs ZERO" RED | 3C-L13 / v+1 clause | the narrow 4-name regex catches 4 of 9 idents; the self-gate is honestly disclosed FALSIFIED/RED ("asserts ZERO, the literal scan returns 13") — disclosed, not papered | RESOLVES |
| runtime Pattern-H census = 71 | 3C-L13 / 3A-D12 / 3E-D11 | `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs'` = **71** | RESOLVES |
| 3E-D17 CSS second-seam locus | 3E-D17 / v+1 unfork clause | `crates/core/src/runtime/css_l4/` EXISTS (the SK-V19-adoption seam, DEFER-bundled) | RESOLVES |
| PLANNED co-gate symbols (not cited live) | 3C / 3C-diff / 3A-D04/D09 | `runtime_target_rows_collapsed` and `bbnf_simd_single_mask_convention` both rg=0 live — honestly disclosed PLANNED | RESOLVES |
| stale-pattern scan | all 7 | `rg ORCHESTRATOR-PROMPT|why still open|follow-up docs-only cleanup` = **0 matches** | RESOLVES |

## The V3 REVISE (`3D:157`) — folded and re-verified, holds in V5

`3D:157` (the W-PRUNE summary row) reads: "net LOC ≈ −10800 headline (P1 x86 ≈
−4500, P2 warm bench ≈ −700, P3 ≈ −5500 [= −5460 replica bodies + ~−40 collapsed
rows + 1 `PartialEq` derive, per `…/SPEC.md:435`], P4 gate-fix ≈ +15, P5
metalang rename ≈ 0) — the P1+P2+P3+P4+P5 components sum to the SPEC-authoritative
per-wave −10685 under the ≈ tilde to the −10800 headline (matching 3B §13.7-P3 /
3B-D04 / 3F-MH-009)." The bare −5460-in-a-component-sum is GONE; the row carries
the SPEC −5500 and reconciles to −10685. The V3 fold row `3D:49` records the
repair. **CH4-V2-002 closes; the V3 REVISE is discharged and stays discharged.**

## Per-delta / disposition CH4 disposition (V5)

### 3A — ARCHITECTURE (14 SK-V18 + 12 carried = 26) — 26 ACCEPT
`proposed_deltas_count:26` = 12 carried `ARCH-3A-V1-D01..D12` + 14 SK-V18-new
`ARCH-3A-V4-SK18-D01..D14` (frontmatter `newly_added` lists exactly 14;
reconcile note `3A:60`). The V4 CH4 Coverage Matrix (`3A:251`-`263`) carries
doc-LOC / propagation / risk / wave / consumer-gate / fail-action for every
SK-V18 row INCLUDING D14 (≈+40 doc, 1 surface, pre-W0/Ω). The D11a/D11b split
(`3A:259`-`260`) is exemplary: D11a ≈+15 (SK-V18 P4 green-by-exclusion), D11b
≈+217 (SK-V19 R16 9-ident collapse, "charged to SK-V19, never laundered into
D11a's +15"). D07 x86 −4500, D05 −910-after-provider, D03 ≤450 un-fork honour
the 12-wave ceiling. Carried matrix re-stamped HISTORICAL. **26/26 ACCEPT.**

### 3B — MASTER-PLAN (10 new + 4 carried = 14) — 14 ACCEPT
§13.7 NEW-wave block (`3B:146`-`157`): all 12 waves
(P1,P2,P3,P4,P5,G1,G2,G3,G4,G5/G6,PROVE,H1) carry a same-wave consumer column
AND a RED exit-gate falsifier with a measurable baseline — the 3B-NEW-wave
consumer requirement is fully MET. D04 (`3B:213`) folded: ad-hoc −10700 dropped;
per-wave sum −10685 cites `SPEC:433`-`437`; P3 = −5500. The 12-wave manifest is
EXACTLY at the skinny ceiling (no W13; "no W12 spillover" guards present). The
short-prefix `FORBIDDEN ⊇ {...}` at `3B:149` (vs the full `FORBIDDEN_GENERIC_TOKENS
⊇ {...}` at 3A/3D/3C-diff) shares the byte-identical SET — a CH2-wording nuance,
NOT a CH4 cost discrepancy (P4 = ≈+15 everywhere). **14/14 ACCEPT.**

### 3C — LOCKS crystallisation (11 clauses + 21 dispositions + v+1 diff) — 33 ACCEPT
Per-clause cost matrix (`3C:103`-`115`) carries doc-LOC / risk / owning gate /
consumer / propagation / hard-cap fit / fail-action for ALL 11 clauses. 21/21
candidates disposed (8 T-P1: LAC-1E-V5-01..07 + 1A-LOCK1-AMEND-001; 13 T-P2:
3×2C + 4×2D + 3×2E + 3×2F); frontmatter `answered` lists exactly 21; **0 silent
drops** (LAC-2F-V3-03 DEFER carries a named re-entry trigger and is folded as a
one-line audit-scope note, not dropped). Dispositions realistic: LAC-2D-V3-03
carries the spot-verified live `NormalizeDirectSinkCost` Rewrite (`:191`-`193`,
instantiated `:75`); L13 cost row notes the ≈+217 9-row collapse is SK-V19-owned,
not implied in the 3-5 doc LOC; the two PLANNED co-gate symbols are disclosed
PLANNED (rg=0 confirmed). v+1 diff applies clean, doc-only, adds no
lock/directive/substrate/sixth shape (16 locks held; 5 shapes held).
**11 + 21 + 1 = 33/33 ACCEPT.**

### 3D — skinny-fold (12 deltas) — 12 ACCEPT
CH4 coverage matrix (`3D:139`-`151`) complete for all 12; monotonic-fold
direction preserved; D12 cost cell scoped to the skinny P3 +1-line
`RuntimeTarget:PartialEq` derive, the 9-row scaling cross-ref'd SK-V19 R16 ≈+217
(NOT charged to the +1-line). The W-PRUNE summary row (`3D:157`) — the sole V3
REVISE — is FOLDED and internally consistent. **12/12 ACCEPT.**

### 3E — grammar-generalisation (7 new D12-D18 + 11 carried) — 18 ACCEPT
7 new deltas carry LOC/risk/wave/propagation inline (`3E:270`-`276`); D12 "this
row is a cross-reference, no body charged" (bodies owned by 3E-D14/2D-V3-01) is
exemplary anti-double-budget; D14 "LOC ≈ +5 rename + the 2E/2F G6 retarget body
(≤150, owned there)" and D16 "3E owns docs/gate expectation only (180-320 LOC);
lowerer carriers inherit 2D W7/W8/W9 bands via 3B/G-Omega" never double-budget;
D12 cites the CORRECT `skinny/crates/codegen/src/lower/mod.rs:18`-`24`
select_lowering locus. Carried D01-D11 matrix re-stamped HISTORICAL + re-keyed
(67→71 per-file provenance). **18/18 ACCEPT.**

### 3F — MIGRATION + HANDOFF (6 SK-V18 active; MH-002 removed-with-supersession) — 12 ACCEPT
CH4 V6 coverage matrix (`3F:188`-`201`) complete for all six SK-V18 deltas
(MH-008..MH-013); five migration decisions priced (x86 −4500, courier −910,
replicas −5500 cited `SPEC:435`, phantom `<G>` DELETE, css_types 66 SK-V19).
MH-009 P3 −5500 cited verbatim, aligned across decisions table / collapse row /
3D's −5460. MH-013 defers css_types to SK-V19 (explicit, not dropped). MH-008
fail-action "if the deletion list is narrower than the verify grep, return REVISE
(RED-by-construction gate)" is a correctly self-priced cost falsifier. MH-002
removed-with-supersession (3F-MH-009 replaces it). Counting the six active
SK-V18 deltas + the five priced migration decisions + the MH-002 removal-record
at the granularity the lens enumerates = **12 ACCEPT.** **12/12 ACCEPT.**

## Cross-scope / refuted-route / silent-drop / laundering scan (REJECT bait — none found)

- **No SK-V19 cost laundered into a skinny figure**: every `≈+217` is uniformly
  scoped to SK-V19 R16 / MP.SK19.UNFORK / 3A-D11b / 3F-MH-013 across all loci;
  the skinny figures stay +15 (P4) / +1-line (P3 derive). The non-217 grep hits
  were unrelated `sk-v15/SPEC.md:217` line-number citations.
- **No double-budget**: 3E-D12 thesis row "no body charged"; 3E-D14/D16 bodies
  owned at G2/G3/2D rows; 3A-D11a/D11b split; no row charges a body owned
  elsewhere.
- **No sixth `BackendShape`**: 5 variants live at `skinny/crates/ir/src/lib.rs:340`;
  the v+1 diff explicitly "adds no … sixth shape"; FactStream is output-plane.
- **No revived refuted route**: REDRESS 96/97/98 fenced (3C-L10 / 3A-D08); G6 is
  RETARGET-not-author (3C-L16-retarget / 3A-D09); x86-closes-M5-row REFUTED
  (3A-D07/3F-MH-008); md5-alone-proves-un-fork REFUTED (3C-L05-L10 / 3E-D17 /
  3F-MH-011); CollapsedStage an inert REDRESS-fenced diagnostic-only slot.
- **No silent drop**: 3C disposes 21/21 (answered=21); 3B retires consumed
  SK-V15 deltas with supersession; 3F removes MH-002 with supersession; 3E
  L14-HC-13 / L13 defers (not drops) to SK-V19; LAC-2F-V3-03 DEFER carries a
  re-entry trigger.
- **No W12/W13 overflow**: manifest is exactly 12 waves at the skinny ceiling;
  every non-fit route is intrinsic-block / REDRESS / G-Omega amendment, never a
  13th wave; SPEC:431-447 wave count = 12, "≤12 ceiling, exactly at cap".
- **No broad-CSSOM-parity smuggle**: G2 gate is the 9-field cssparser oracle +
  `css_typed_summary_equal` (typed-summary equality), not whole-CSSOM parity;
  no challenge-time implementation overflow (every body charged to its owning
  G/P wave; the v+1 diff is doc-text; 3C clauses are "Pass Omega doc-only,
  consumed by [wave] gate").
- **No doc-only implementation gate**: every implementation body is charged to
  its owning G/P wave; the v+1 LOCKS addendum is doc text only.

## Tally

Counting at the delta/clause/disposition granularity the lens enumerates:

- 3A: 26 ACCEPT (14 SK-V18 incl. D14 + 12 carried; D11a/D11b split held)
- 3B: 14 ACCEPT (10 new + 4 carried; §13.7 12-wave same-wave-consumer MET)
- 3C: 33 ACCEPT (11 clauses + 21 dispositions + 1 v+1 diff; 0 silent drops)
- 3D: 12 ACCEPT (V3 REVISE on `3D:157` folded and re-verified, holds)
- 3E: 18 ACCEPT (7 new + 11 carried)
- 3F: 12 ACCEPT (6 SK-V18 active + 5 priced decisions + MH-002 supersession)

ACCEPT = 115, REVISE = 0, REJECT = 0. `CH4-V2-001` (per-delta coverage matrices)
and `CH4-V2-002` (per-wave sum −10685 reconciliation) both close after the V4
citation fold. Every load-bearing CH4 fact independently re-verified on the live
tree at `3f6eb603d` (diff applies; 6×910=−5460; SPEC P3=−5500 verbatim; no SPEC
10685 literal; x86=28; metalang=7; P2 src-file=48; css_types=66 lock-named;
5 shapes + `select_lowering` matches them; 16 locks; 71 Pattern-H; 9 `idents`
rows; 21/21 dispositions; live e-graph Rewrite; ≈+217 uniformly SK-V19-fenced;
no W13; no stale-pattern matches). The V5 CHALLENGE-CONTEXT's "67 Pattern-H" and
`77b6e9fd7` target are SK-V15-era boilerplate; the live SK-V18 fold under review
is the V4-reviewed packet, and it is CH4-CLEAN. This IS a clean V5 final
confirmation under the CH4 lens.

TALLY accept=115 revise=0 reject=0
