# BA.W3 — FINAL — Measurement Matrix + Parity Harness + Handoff to BB

**Opens after**: W2 close (host-binding isomorphism + e2e round-trip
green).
**Agents**: 2 parallel, disjoint file bounds.
**Hard gate**: Full measurement matrix committed (JSON / CSS L4 /
Sheets / BBNF × 3 / 10 / 30 fields × cold); pointer-parity harness
asserts bbnf matches sonic-rs / simdjson / cssparser / lightningcss
output for every compilable path; FINAL.md records deltas and any
work routed to BB.
**Status**: planned.

## Scope

1. Extend `benches/path_extract.rs` to the full grammar × depth
   matrix and commit the results alongside samply profiles.
2. Extend `tests/path_parity.rs` to the full compile-time path set
   across the four production grammars; assert pointer-output
   equivalence vs. each external reference.
3. Land `FINAL.md` summarising BA close: deltas, reversals taken,
   parent-pointer pick rationale, follow-on work routed to BB
   (egraph rewrite rule inference).

## File bounds

| File | Access |
|---|---|
| `benches/path_extract.rs` | modify (extend matrix) |
| `tests/path_parity.rs` | modify (extend harness) |
| `docs/tranches/BA/FINAL.md` | create |
| `docs/tranches/BA/PROGRESS.md` | modify (close) |

**Do NOT touch**: core path IR / executor / bindings (W0–W2
scope); AZ substrate.

## Phase sub-items

### BA.W3.1 — Full measurement matrix

`benches/path_extract.rs` runs JSON / CSS L4 / Sheets / BBNF each
at 3 / 10 / 30 field extraction depth, cold only per
`feedback_no-warm-benches`. Samply profiles committed under
`docs/benchmarks/profiles/BA/W3/`. Deltas recorded vs. sonic-rs /
simdjson / cssparser / lightningcss.

Sub-gate: every cell populated; every cell at or above the
hard-gate floor declared in BA.md.

### BA.W3.2 — Extended parity harness

`tests/path_parity.rs` enumerates every compile-time `path!`
site across the workspace and asserts pointer-output equivalence
vs. each external reference. `feedback_beat-lightningcss-target`
holds: CSS L4 parity first, exceedance measured second.

Sub-gate: parity harness green for every compile-time path.

### BA.W3.3 — FINAL.md

`docs/tranches/BA/FINAL.md` summarises BA close in the standard
tranche FINAL format: invariants held, gates met, reversals
taken, measurement deltas, handoff contract to BB.
`feedback_no-metalanguage-docs` holds: FINAL.md is standalone
prose, no reference to plans / commits / conversation history.

Sub-gate: FINAL.md lands; PROGRESS.md marks BA closed.

## Hard gate

1. Full grammar × depth matrix populated. Artefact:
   `docs/benchmarks/post-BA-W3-matrix.json`.
2. Every cell meets or exceeds the BA.md floor. Same artefact.
3. Parity harness green across sonic-rs / simdjson / cssparser /
   lightningcss. Artefact: `tests/path_parity.rs` green.
4. Zero heap on traversal across the full matrix (dhat).
   Artefact: `docs/benchmarks/post-BA-W3-dhat.txt`.
5. FINAL.md lands. Artefact: `docs/tranches/BA/FINAL.md`.

## Verification artefacts

- `docs/benchmarks/post-BA-W3-matrix.json`
- `docs/benchmarks/post-BA-W3-dhat.txt`
- `docs/benchmarks/profiles/BA/W3/`
- `docs/tranches/BA/FINAL.md`
- Commit hashes for each W3 sub-phase.

## Handoff to BB

BA closes when every item in BA.md's handoff contract is true:
full grammar coverage of `path!`, ≥ 20% 3-field-extraction win
over sonic-rs, compile-time grammar-aware error diagnostics, zero
heap on traversal, AU-baseline matrix at or above AZ close,
cross-binding isomorphism. BB then opens on the settled path IR
and layers egraph-inferred rewrite rules over it.

## Dependencies

- **Depends on**: W2 close.
- **Blocks**: BB open.
