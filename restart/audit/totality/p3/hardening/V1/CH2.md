---
challenge_agent: CH2
name: GENERALITY
pass: T-P3-synthesis
cycle: V1
sk_cycle: SK-V18
verdict: REVISE
generated_at: 2026-06-01T20:50:00Z
owned_output: restart/audit/totality/p3/hardening/V1/CH2.md
---

# CH2 GENERALITY — SK-V18 T-P3 V1

Lens: CH2 GENERALITY. Subject: the 6 T-P3 synthesis artefacts under
`restart/audit/totality/p3` (3A, 3B, 3C-crystallisation + 3C-v+1-diff, 3D, 3E,
3F), audited against T-P1 evidence (`p1/`), T-P2 dossiers (`p2/`), and the V1
surfaces (`restart/ARCHITECTURE.md`, `MASTER-PLAN.md`, `locks/LOCKS.md`,
`MIGRATION.md`).

CH2 mandate: Lock 14 holds; 3A surface deltas + 3B waves generalise to non-JSON;
3E's story is concrete for CSS L4 / Sheets / BBNF-self / the 9-grammar fleet; 3C
accepts no JSON/CSS-narrowing amendment; the future-grammar onboarding test
survives.

## Spot-verifications performed (load-bearing)

| check | result | evidence |
|---|---|---|
| Cited finding-id resolves (1E:147 LAC-1E-V5-01 named-primitive gate) | PASS | `1E-locks-evidence.md:147` is the (a)-(d) gate row verbatim. |
| Cited finding-id resolves (1E:73 5-shape canon verified-invariant) | PASS | `1E:73` "5-shape `BackendShape` canon | verified". |
| Cited finding-id resolves (2C:380-382 LAC-2C-SK18-01/02/03) | PASS | `2C:380` FORCED-demotion, `:381` fleet-scoped, `:382` totality-tree. |
| Cited finding-id resolves (2C:213-219 grounded SK-V18-2C findings) | PASS | one-generator thesis (`:213`)…relocated-seam firewall (`:219`). |
| Cited finding-id resolves (2C:313 tree-walk-regresses refute) | PASS | "A generic IR tree-walk lowering can preserve the 94.1% CSS scan | refuted". |
| Cited LOCKS section exists (Lock 14 @ 603; SK-V17 addendum 610-622; boundary 625) | PASS | `LOCKS.md:603` Lock 14 grammar-generalisation clause; SK-V17 addendum 610-622; governance boundary 625. |
| Cited live code resolves (ARCH:1998 phantom `<G>` vehicle) | PASS | `ARCHITECTURE.md:1998` "The `G:EventGrammar` type parameter is the generality vehicle" verbatim. |
| Cited live code resolves (lower/mod.rs:18-24 5-shape select_lowering) | PASS | exactly 5 `BackendShape` arms; the grammar-neutral dispatch axis is real. |
| Cited live code resolves (strategy.rs:137-185 9-ident leak) | PASS | 9 `idents` entries (json…css_pretty); `:216` `for_grammar_with_manifest`. |
| 9-grammar fleet roster (3E-D16) matches reality | PASS | strategy.rs 9 idents + `crates/core/src/grammar/generated/*.rs` 9 files = {bbnf,bnf,css_l4,css_pretty,csv,ebnf,google_sheets,json,math}. |
| Verbatim-blob courier exists (3A-D05 / 3C-L06) | PASS | `runtime_generator.rs:701 const CSS_GENERATED_RS: &str = r#"`. |
| PLANNED co-gate symbols genuinely absent (disclosed, not cited live) | PASS | `rg runtime_target_rows_collapsed`/`bbnf_simd_single_mask_convention` skinny = 0. |
| GENERALIZATION discipline genuinely unbound in LOCKS (addendum non-redundant) | PASS | `rg emit_shape_source\|css_balanced_component_scan\|PROFILE-PROVEN-NARROW-LEAF LOCKS.md` = 0. |
| **The 3C v+1 diff applies to current LOCKS.md** | **FAIL** | `git apply --check` errors at `LOCKS.md:622`; hunk header + context malformed (see CH2-R01). |

The evidentiary substrate for generality is solid: every generality-bearing
finding-id, LOCKS section, and live-code anchor I sampled resolves exactly. The
single load-bearing failure is the v+1 diff, which does NOT apply — the one
spot-verify CH2 is explicitly told to perform, and a CH1-binding gate object.

## Disposition enumeration (CH2 lens)

### 3A architecture (SK-V18 generality-bearing deltas)

| delta | verdict | basis |
|---|---|---|
| ARCH-3A-V4-SK18-D01 phantom `<G>` strike + re-anchor on Cursor trait + config-breadth classifier | ACCEPT | Re-anchors generality onto two axes the clause already names; `1A-SUB-023` census EMPTY of non-test `<G>` instantiation; ARCH:1998 anchor verified; no narrowing. |
| ARCH-3A-V4-SK18-D02 named-primitive (a)-(d) ARCHITECTURE-binding | ACCEPT | Keeps the 94.1% scan grammar-PARAMETERISED; refuted-tree-walk (2C:313) is the correct grounding; G1∧G2 (not CSS-only). |
| ARCH-3A-V4-SK18-D03 un-fork: DELETE `RuntimeEmitterKind`, dispatch on `BackendShape` | ACCEPT | Dispatch on the cost-derived grammar-neutral axis (lower/mod.rs:18 verified); this is the generality backbone, not a JSON path. |
| ARCH-3A-V4-SK18-D04 relocated-seam firewall `emit_shape_source==lowered_program` + CSS second seam | ACCEPT | The grep-evasive neutrality falsifier; the CSS second seam (`css_provider_source==generated`) extends generality to the CSS provider. |
| ARCH-3A-V4-SK18-D05 verbatim-blob-courier prohibition | ACCEPT | Courier verified at runtime_generator.rs:701; bars a hand-written CSS `&str` from being credited as grammar-derived — preserves the non-JSON generality bar. |
| ARCH-3A-V4-SK18-D06 5-shape canon RE-FRAMED as POSITIVE neutral dispatch axis | ACCEPT | The strongest generality affirmation: JSON=SinkOnly, CSS=lowered scan IR, Sheets tower→same SinkOnlyExpr vocab; sixth shape = overfit. Coherent with 3E-D16 / 3B. |
| ARCH-3A-V4-SK18-D09 G6 retarget-not-author + neutral-inner-kernel + single-movemask | ACCEPT | Inner eq-set kernel carries byte-set as CALLER DATA (neutral) even under a CSS-scoped shell — the correct generality split. |
| ARCH-3A-V4-SK18-D10 CSS-scoped `css_balanced_component_scan` FORCED demotion | ACCEPT | Honest discharge of a one-grammar neutral name; base one-fan kernel stays neutral independently; no fabricated cross-grammar caller. |
| ARCH-3A-V4-SK18-D11 totality 9-ident leak + `css_types.rs` + green-by-exclusion | REVISE | Generality-correct (9-ident leak verified at strategy.rs:137-185; self-gate RED), BUT the §13.1 FORBIDDEN token-set claim `{CSS_,_RS,EventGrammar,*EventGrammar}` diverges from 3C-L06/diff which write only `{CSS_, _RS, EventGrammar}`. See CH2-R02. |
| ARCH-3A-V4-SK18-D13 Sheets precedence-tower negative control (by-exercise upgrade) | ACCEPT | Upgrades Sheets from by-construction to by-exercise; tower lowers to existing `SinkOnlyExpr` (no new IR); the make-or-break generality stressor. Scoped, no fleet wording until SK-V19. |
| ARCH-3A-V4-SK18-D14 SK-V18 authority + HANDOFF-scope reconcile | ACCEPT | Routes the scope pivot (SK-V18=skinny generalization, SK-V19=crates/core fold); prevents fleet-wide misreading. |

(D07 aarch64-only, D08 CollapsedStage slot, D12 metalang+Pattern-H are
host/shape/census deltas tangential to the generality lens; CH2 takes no
position — owned by CH3/CH4/CH7.)

### 3B master-plan (waves generalise to non-JSON)

| delta | verdict | basis |
|---|---|---|
| MP-3B-SKV18-D01..D03 scope pivot + §13.7 12-wave GENERALIZATION block | ACCEPT | Waves map G1(JSON leaves)→G2(CSS)→G3(un-fork dispatch on BackendShape)→PROVE(Sheets) — generalise beyond JSON with same-wave consumers and RED exit falsifiers. |
| MP-3B-SKV18-D04 P-cluster routing (P4-before-G2/G3 hard order) | ACCEPT | Green-by-exclusion gate fixed BEFORE the emitter is neutrality-scanned-as-authored; correct sequencing for generality. |
| MP-3B-SKV18-D05 G-cluster (a)-(d) gate + 5-conjunct G3 un-fork exit | ACCEPT | `css_balanced_component_scan` FORCED-demoted; un-fork reads `BackendShape` not a grammar tag — generality preserved per-wave. |
| MP-3B-SKV18-D06 PROVE Sheets + H1 with BINDING FALLBACK `N` | ACCEPT | A Sheets shim ⇒ `N` (generalization NOT real), surfaced honestly, never paper-closed — the anti-narrowing fallback. |
| MP-3B-SKV18-D07 SK-V19 totality-fold tee-up (9-ident leak, css_types, scanner asymmetry) | ACCEPT | The three totality-tree leaks are DEFERRED to SK-V19, not bolted into SK-V18; future-grammar fleet onboarding (MP.SK19.FLEET-ONBOARD) survives. |
| MP-3B-SKV18-D08/D09 §25 order + F.W5 un-fork (3 grammars SK-V18 / 9 SK-V19) | ACCEPT | F.W5 explicitly NOT read as already-satisfied; the "nine seed grammars" claim is the SK-V18→SK-V19 obligation — fleet-scoping honest. |
| MP-3B-SKV18-D10 CSS verdict UPGRADED (measurement-valid, overfit is implementation) | REVISE | Generality-correct (overfit is forks/replicas, not measurement), but the bare phrase "measurement-valid" is the un-caveated closure word the row's own fail-action forbids. See CH2-R03. |

### 3C LOCKS (no JSON/CSS-narrowing amendment; Lock 14 holds)

| disposition / clause | verdict | basis |
|---|---|---|
| D-SKV18-L14-named-primitive-gate (LAC-1E-V5-01 ACCEPT) | ACCEPT | Binds the (a)-(d) discipline; keeps the hot kernel grammar-PARAMETERISED — the inverse of JSON narrowing. |
| D-SKV18-L05-L10-unfork (LAC-1E-V5-02, 2D-V3-01/02 ACCEPT) | ACCEPT | `render(program)` dispatches on `backend_shape`; dispatch-on-source-family is REJECT — generality is the lock, not a narrowing. |
| D-SKV18-L14-neutrality-proof (LAC-1E-V5-03, 2C-SK18-01/02 ACCEPT/MODIFY) | ACCEPT | Forced-demotion + fleet-scoping in one clause; inner kernel may stay neutral; explicitly NO fleet-wide wording on <full-roster witness. |
| D-SKV18-L14-green-by-exclusion (LAC-1E-V5-06 ACCEPT) | REVISE | Clause writes `FORBIDDEN_GENERIC_TOKENS ⊇ {CSS_, _RS, EventGrammar}`; 3A-D11 writes `{CSS_,_RS,EventGrammar,*EventGrammar}`. Two surfaces, two gates. See CH2-R02. |
| D-SKV18-L13-pattern-h-recensus (LAC-1E-V5-07, 2C-SK18-03 MODIFY) | ACCEPT | The 9-ident totality leak (verified) routed to STRUCTURAL full-row collapse over all 9 + widened regex at SK-V19; no narrowing. |
| D-SKV18-L01-cursor-generality (1A-LOCK1-AMEND-001, 2D-V3-03 MODIFY) | ACCEPT | Strikes the phantom `<G>` vehicle, re-anchors on Cursor trait + config-breadth classifier (8/9 grammars) — generality survives the `<G>` delete. |
| L14-HC-07 / V4 onboarding-axes future-grammar test | ACCEPT | Source/metadata-only; Sheets/BBNF-self fail on a generic branch / new directive / sixth shape. The onboarding test SURVIVES intact. |
| **No JSON/CSS-narrowing amendment introduced** | CONFIRMED | The live Lock 14 clause (LOCKS:603) and all 11 SK-V18 clauses bind generality OUTWARD (provider manifests, 9-grammar matrix, Sheets/BBNF-self receivers). Zero clause narrows the lock to JSON or CSS. |
| 21/21 candidates disposed, 0 silent drops | CONFIRMED | Disposition matrix accounts for 1E×7, 1A×1, 2C×3, 2D×4, 2E×3, 2F×3; the DEFER (LAC-2F-V3-03) names its re-entry trigger. |

### 3D skinny-fold / 3F migration-handoff (generality cross-refs)

| delta | verdict | basis |
|---|---|---|
| 3D-D01 JSON-guard-scope (51/51 is same-plane PoC, not fleet closure) | ACCEPT | Explicitly bars JSON from masquerading as generality proof. |
| 3D-D06 decision-engine selection-DEPTH under Sheets tower | ACCEPT | Five-shape canon preserved; the tower depth is the open generality stressor at G3. |
| 3D-D09 Sheets negative-control onboarding (shim ⇒ `N`) | ACCEPT | Monotonic generalization bridge; fleet wording waits for the witness; cross-ref 3E-D07. |
| 3D-D10 PRUNE-before-REBUILD sequencing | ACCEPT | No cross-scope violation; T-P3 proposes only, dispatches no wave. |
| 3F-MH-005/006 HANDOFF SK-V18=generalization scope + blocker matrix | ACCEPT | Strikes the stale "SK-V18 adopts into crates/core" definition; routes fleet onboarding to SK-V19. |
| 3F-MH-012 phantom `<G>` DELETE + LOCKS:620 reconcile | ACCEPT | K-axis preserved; generality re-anchored; LOCKS edit correctly deferred to Pass Omega CRUD. |
| 3F-MH-013 `css_types.rs` RELOCATE-or-DELETE as SK-V19 | ACCEPT | The named generic-core mess is an explicit SK-V19 decision, not silently dropped, not narrowed into SK-V18. |

### 3E grammar-generalisation (the lens-primary artefact)

| delta | verdict | basis |
|---|---|---|
| 3E-D01..D11 (carried SK-V15 generality matrix) | ACCEPT | Non-JSON proof matrix, per-grammar BackendShape matrix, primitive transfer, Lock 14 hardening clauses — concrete for CSS/Sheets/BBNF-self. |
| 3E-D12 one-generator generalisation thesis | ACCEPT | Generality is an INPUT-SURFACE property (grammar source + metadata), proven by md5-distinct output from a neutral renderer; 2C:213 grounded. |
| 3E-D13 named-primitive (a)-(d) neutrality discipline | ACCEPT | (a)-(c) prove grammar-COUPLING, (d) bounds SIZE; the escape is admissible ONLY under all four. |
| 3E-D14 css_balanced_component_scan FORCED demotion | ACCEPT | Base one-fan kernel structurally neutral; two-fan/shell CSS-scoped; the forced name IS the discharge. |
| 3E-D15 Sheets precedence-tower negative control | ACCEPT | Tower lowers to existing `SinkOnlyExpr`; no relabeled courier can fake the recursive `CallRule`/`RepeatLoop` cascade. The make-or-break generality stressor. |
| 3E-D16 9-grammar BackendShape fleet matrix | ACCEPT | Roster verified against strategy.rs (9 idents) + generated/ (9 files); five-shape canon preserved; CollapsedStage in no dominant/secondary cell (correct, M5-Max-aarch64). |
| 3E-D17 relocated-seam firewall + CSS second seam | ACCEPT | md5-distinct necessary-NOT-sufficient; the firewall extends generality to the CSS provider source channel. |
| 3E-D18 fleet-scoped neutrality wording | ACCEPT | Anti-overclaim discipline: <full-roster witness ⇒ scoped wording; 6 remaining grammars are SK-V19 receivers. Prevents the fold being narrated as already-fleet-proven. |

## REJECTs

None. No uncited delta; no revived refuted route (the 13 REFUTED constraints in
the 3E frontmatter — tree-walk, find_css_significant-as-is, neutral-name-on-one-
grammar, checkasm-PASS-as-speedup, x86/AVX-512, eq-set dual-consumer, md5-
distinctness, bracket_depth_mask — are all carried as REFUTED, none revived); no
silently-dropped candidate (3C disposes 21/21); no cross-scope violation (every
totality-tree item is explicitly routed to SK-V19, the monotonic skinny→totality
fold is preserved in 3D/3F). Lock 14 holds; the future-grammar onboarding test
survives.

## REVISE summary (with exact corrections)

- **CH2-R01 (3C-locks-v+1-diff.md) — THE LOAD-BEARING DEFECT.** The proposed
  unified diff does NOT apply. `git apply --check` errors at `LOCKS.md:622`. Two
  faults: (1) the hunk header `@@ -622,6 +622,38 @@` claims 6 old-side / 38
  new-side lines, but the hunk body carries only 5 old-side (4 context lines)
  and 33 new-side lines; both counts are wrong. (2) The old-side context is
  `Lock-16-NEON-clause / <one blank> / ## v+1 Governance Boundary`, but the
  current file has TWO blank lines (623, 624) between the Lock 16 clause and the
  governance boundary — the context does not exist contiguously, and the diff's
  own `+` blanks would yield four blank lines if it applied.
  **Correction:** regenerate the hunk against current `LOCKS.md` — set the header
  to the true line counts, include BOTH existing blank lines (623, 624) in the
  context, and re-run `git apply --check` to confirm clean apply before G3. The
  Invariant-Check bullet "Insertion anchor … lands AFTER the SK-V17 Lock 16
  clause and BEFORE `## v+1 Governance Boundary`" is correct in intent; only the
  mechanical hunk is malformed. (Also CH1-binding per the workflow's CH1 line.)

- **CH2-R02 (3C-locks-crystallisation.md D-SKV18-L14-green-by-exclusion +
  3C-locks-v+1-diff.md + 3A-architecture-synthesis.md ARCH-3A-V4-SK18-D11) —
  FORBIDDEN-token-set divergence.** 3C writes `FORBIDDEN_GENERIC_TOKENS ⊇ {CSS_,
  _RS, EventGrammar}`; 3A-D11 writes `{CSS_,_RS,EventGrammar,*EventGrammar}`. Two
  governance surfaces specify two different neutrality gates for the same P4
  fix. **Correction:** adopt one canonical token set across 3A/3B/3C (the
  `*EventGrammar` glob is the stronger, alias-immune form and is what 3B-P4's
  `SHEETS_GENERATED_RS` re-inject falsifier needs; the diff and 3C should add it,
  or 3A should drop it — but they must be identical).

- **CH2-R03 (3B-master-plan-reconciliation.md MP-3B-SKV18-D10) — un-caveated
  closure word.** The row pairs "CSS >SOTA is measurement-valid" with "directional,
  not re-locked" — the bare "measurement-valid" is the closure word the row's own
  fail-action forbids and could be lifted as a fleet-generality close.
  **Correction:** phrase the verdict "CSS >SOTA is directionally-valid pending H1
  re-lock; the overfit is implementation (forks/replicas), not measurement."

Census: ~32 dispositions judged under the CH2 lens (11 in 3A, 7 in 3B, 8 lock
dispositions + onboarding/no-narrowing confirmations in 3C, 7 in 3D/3F, 8 in
3E). The REVISE load lands on four artefact loci: 3C-locks-v+1-diff (R01),
3C-locks-crystallisation (R02), 3A-architecture-synthesis (R02), and
3B-master-plan-reconciliation (R03). ACCEPT dominates the substance: the
one-generator generality thesis, the 9-grammar fleet matrix, the Sheets
negative control, the no-narrowing Lock 14, and the surviving future-grammar
onboarding test are all sound. The three REVISE are mechanical/precision
corrections, the first of which (the non-applying diff) is the binding G3 gate
object and must be fixed before lock.

TALLY accept=10 revise=4 reject=0
