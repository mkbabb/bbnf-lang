# BB.W3 — Profiling Fleet + Workflow Proof

**Opens after**: W2 close
**Agents**: 3 parallel
**Hard gate**: The profiling and proof fleet is reusable, headless, and measured; post-BA workflow proof no longer depends on ad hoc one-off invocations.
**Status**: planned

## Scope

1. Consolidate reusable profiling and proof entrypoints.
2. Make headless capture and prebuild workflows first-class.
3. Prove lower proof-surface friction against post-W2.

## File bounds

| File | Access |
|---|---|
| `scripts/prebuild-benches.sh` | modify |
| `scripts/profile-bench-headless.sh` | modify |
| `scripts/prepare-profile-wave.sh` | modify |
| `docs/instructions/PROFILING.md` | modify |

**Do NOT touch**: parser semantics, replay/recovery/debug tooling, any
`.bbnf` grammar source text.

## Phase sub-items

### BB.W3.1 Reusable profiling fleet

Mechanism: consolidate prebuild and capture entrypoints so heavy proof
loops stop depending on ad hoc command invention.

Files touched: `scripts/prebuild-benches.sh`,
`scripts/prepare-profile-wave.sh`.

Sub-gate: the profiling fleet is reusable and documented.

### BB.W3.2 Headless proof path

Mechanism: make headless bench/profile capture a first-class workflow
for post-BA proof and regression analysis.

Files touched: `scripts/profile-bench-headless.sh`,
`docs/instructions/PROFILING.md`.

Sub-gate: headless proof runs are documented and reproducible.

### BB.W3.3 Workflow proof

Mechanism: capture timing and friction deltas after the profiling fleet
consolidation.

Files touched: `docs/instructions/PROFILING.md`.

Sub-gate: the targeted proof-surface metrics improve.

## Hard gate

1. The profiling and proof entrypoints are reusable and documented.
   Artefact:
   `docs/benchmarks/post-BB-W3-hotspots.txt`.
2. Headless proof workflow is reproducible. Artefact:
   `docs/benchmarks/post-BB-W3-mid.json`.
3. Measured proof-surface friction improves versus post-W2. Artefact:
   `docs/benchmarks/post-BB-W3-hotspots.txt`.

## Verification artefacts

- `docs/benchmarks/post-BB-W3-mid.json`
- `docs/benchmarks/post-BB-W3-hotspots.txt`
- commit hashes for each W3 sub-phase

## Dependencies

- **Depends on**: W2 close
- **Blocks**: W4
