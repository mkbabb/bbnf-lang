# CH4 COST

Lens name: CH4 COST.

Disposition: `ACCEPT`.

## Critical Findings

| id | severity | finding | evidence | required action |
|---|---|---|---|---|
| CH4-V3-00 | none | No blocking CH4 cost defect found. The live V2 dossier packet preserves the uniform row-shape/cost manifest and keeps Decision Engine plus lowerer work bounded across W7/W8/W9. | V3 requires CH4 to confirm the V2 manifest and W7/W8/W9 split at `restart/audit/totality/p2/hardening/V3/CHALLENGE-CONTEXT.md:48`-`49`. The manifest is defined at `restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md:27`-`68`. V2 consolidated CH4 already accepted the cost/admission manifest at `restart/audit/totality/p2/hardening/HARDENING-T-P2-V2-CONSOLIDATED.md:26` and fold closure at `:37`. Live dossier evidence remains present in `2A:51`-`:63`, `2B:101`-`:155`, `2C:59`-`:75`, `2D:57`-`:76`, `2E:67`-`:82`, and `2F:71`-`:80`. | None. |

## Evidence Inspected

- Challenge authority and convergence rule: `restart/audit/totality/p2/hardening/V3/CHALLENGE-CONTEXT.md:25`-`31`, `:39`-`:49`, and `:61`-`:70`; `restart/prompts/ORCHESTRATOR.md:118`-`:123`.
- CH4 cost contract: `restart/prompts/totality/PASS-2-RESEARCH.md:117`-`:119`, requiring scalar reference, checkasm parity, same-wave consumer, realistic LOC/risk, and no orphan-kernel research.
- V2 fold manifest: `restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md:22`, `:27`-`:68`, and dossier-specific cost obligations for 2A/2B at `:72`-`:90`.
- V2 clean baseline: `restart/audit/totality/p2/hardening/HARDENING-T-P2-V2-CONSOLIDATED.md:14`-`:17`, `:26`, `:37`, and V3 dispatch guidance at `:53`-`:60`.
- `2A-sota-landscape.md`: SOTA rows still carry transfer/admission/verification/close gates; the transferred sonic-rs primitive row also names scalar/parity/hardware/consumer/movement plus LOC/risk/owner/cap at `restart/audit/totality/p2/2A-sota-landscape.md:51`-`:63`.
- `2B-primitive-vocabulary.md`: the primitive manifest enumerates scalar oracle, differential, hardware gate, same-wave consumer, row movement, LOC, risk, owner, hard-cap fit, and close status at `restart/audit/totality/p2/2B-primitive-vocabulary.md:101`-`:115`; the route table preserves the scalar/checkasm/hardware/consumer/movement and cost columns at `:144`-`:155`.
- `2C-grammar-neutrality.md`: grammar-neutral rebuild routes preserve V2 row-shape fields at `restart/audit/totality/p2/2C-grammar-neutrality.md:59`-`:75`, with LAC cost/admission rows for generated manifest, CSS typed provider, broadcast gate, Pattern H, full-surface Lock 14, and future grammar onboarding at `:144`-`:149`.
- `2D-cost-model.md`: Decision Engine, cost-model, CSP, five-shape, SinkOnly, staged-tape, and CollapsedStage rows carry LOC/risk/owner/cap gates at `restart/audit/totality/p2/2D-cost-model.md:57`-`:68`; W7/W8/W9 are separately costed and bounded at `:70`-`:76`.
- `2E-host-arch-esoterica.md`: host primitive rows explicitly preserve scalar reference, parity/checkasm, hardware gate, same-wave consumer, row movement target, LOC, risk, owner, hard-cap fit, and close status at `restart/audit/totality/p2/2E-host-arch-esoterica.md:67`-`:82`.
- `2F-parse-that-gaps.md`: each parse-that gap row carries owner, scalar oracle, parity/checkasm command, hardware gate, same-wave consumer, row movement target, verification action, close status, LOC, risk, wave owner, and hard-cap fit at `restart/audit/totality/p2/2F-parse-that-gaps.md:71`-`:80`.

## Cost-Lens Assessment

The V2 row-shape/cost manifest survives unchanged in the live packet. Primitive and host rows still block admission on scalar reference, strict parity/checkasm or equivalent, host gate, same-wave consumer, and row-local movement. Rebuild rows still carry transfer reason, admission gate, verification action, close status, LOC estimate, risk class, wave owner, and hard-cap fit.

The W7/W8/W9 split remains bounded. W7 owns only the Decision Engine spine: e-graph rewrite activation, measurement-bearing cost extraction, and non-tautological CSP facts. W8 owns `EagerTape` and `OffsetTape`. W9 owns `EventTape`, `SinkOnly`, `CollapsedStage`, and the exact five-shape report. The dossier explicitly excludes lowerer output from W7, defers `EventTape` from W8, and keeps `CollapsedStage` diagnostic-only unless 2E supplies an aarch64 route.

No dossier admits an orphan kernel, bulk generated-provider rewrite, all-lowerer rewrite, wrong-host close route, or CSS broadcast movement as cost-close evidence. Large or speculative routes remain conditional, blocked, source-present-unwired, scalar-delegated, diagnostic-only, or refuted.

## Fold Requirements

None. CH4 has no `REVISE` or `REJECT` fold requirement for T-P2 V3.

## Convergence Impact

CH4 does not block T-P2 V3 convergence. If all other V3 lenses also return `ACCEPT`, with zero orphan `REVISE` items and no target-packet edits, T-P2 satisfies the two-consecutive-clean §3Z convergence rule described in `restart/audit/totality/p2/hardening/V3/CHALLENGE-CONTEXT.md:27`-`:31`.
