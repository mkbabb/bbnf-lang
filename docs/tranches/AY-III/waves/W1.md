# AY-III.W1 — CSS L4 Typed Parity + Sheets Self-Parity + AZ-I Baseline Absorption

**Opens after**: W0 close
**Agents**: 5 parallel (W1.a / W1.b / W1.c / W1.d / W1.e on disjoint file bounds)
**Hard gate**: Every lightningcss 1.0.0-alpha.71 AST surface admitted by the declared CSS L4 grammar projects through a grammar-derived `CssL4Parser<TypeCamel>Projection` plus `materialize_projection_*` emitted from CSP-inferred facts with zero hand-authored `RustNamedTypes` entries; `lightningcss_parity.rs` + `css_l4_canonical_parity.rs` + `typed_accessor_surface.rs` green at release with zero `#[ignore]`; bbnf CSS parse at or below cssparser and within 2× lightningcss; samply hot-path owner is the fused pipeline; Sheets parity tests green at fat-LTO; `sheets_parse_nested_no_panic` green; `make ay-bench-close WAVE=W1-close` clean across 5 bench binaries; `docs/benchmarks/post-AZ-I-W0-baseline.json` published as the MERGE absorption of AZ-I.W0 baseline-bench items.
**Status**: planned

## Scope

W0 closed JSON peer gates against the post-B5 substrate. W1
closes CSS L4 against lightningcss + cssparser on four axes
(admission parity, byte-identical canonical emission,
typed-accessor totality, competitor throughput) AND closes
Sheets self-parity (formula expression-tree parity, fat-LTO
panic verification, samply attribution) AND publishes the
AZ-I.W0 baseline-bench artefact (the MERGE absorption per the
AY-III audit consolidation §MERGES item 3).

Three concurrent surfaces, five parallel agents on disjoint file
bounds: W1.a + W1.b own CSS grammar plus IR plus emitter; W1.c
owns CSS parity tests; W1.d owns Sheets parity tests; W1.e owns
benches plus samply plus the AZ-I.W0 baseline absorption.

### Lightningcss AST → grammar mapping

Every row is a wave-gate item. State column: **EXISTS** =
annotation only; **PARTIAL** = extension + annotation; **MISSING**
= new rule + annotation; **OUT-OF-SCOPE** = stays under
`genericAtRule`, `grep`-evidenced zero occurrences in declared
fixtures.

| lightningcss surface | grammar rule (file:line) | state |
|---|---|---|
| `MediaRule`, `KeyframesRule`, `MediaQuery`, `MediaCondition`, `MediaFeature`, `MediaType`, `KeyframeSelector` | `stylesheet.bbnf:22/30`, `media.bbnf:36/32/21/12`, `stylesheet.bbnf:27-28` | EXISTS |
| `StyleRule` (qualified), `DeclarationBlock` | `stylesheet.bbnf:19/15` | EXISTS |
| `FontFaceRule`, `SupportsRule`, `ContainerRule`, `LayerStatementRule`, `LayerBlockRule`, `PageRule`, `ImportRule`, `NamespaceRule`, `PropertyRule` | all under `genericAtRule` @ `stylesheet.bbnf:34` | MISSING → new `grammar/css/l4/at-rules.bbnf` module |
| `CounterStyleRule`, `ScopeRule`, `StartingStyleRule`, `ViewTransitionRule`, `NestingRule`, `CustomMediaRule`, `FontFeatureValuesRule`, `FontPaletteValuesRule`, `MozDocumentRule`, `ViewportRule` | `genericAtRule` fallback | OUT-OF-SCOPE (zero matches in normalize + bootstrap + tailwind; admission via fallback; typed parity is BA-scope) |
| `Property` enum (~200 variants) with per-group `PropertyId` dispatch | 27 typed decls + `genericDecl` @ `properties.bbnf:161-223` | PARTIAL — each typed-decl group annotates `-> <Group>Property` with per-property u8 variant idx |
| `CustomProperty`, `Important` | `customPropertyDecl` @ `properties.bbnf:206`, `importantSuffix` @ `properties.bbnf:23` | EXISTS |
| `CssColor::RGBA` / `LAB` / `Predefined` / `Float` | `colorFunction` @ `color.bbnf:220`, `colorFn` @ `color.bbnf:253`, `colorMix` @ `color.bbnf:299`, `hex` @ `color.bbnf:189`, `namedColor` @ `color.bbnf:36-185` | EXISTS — `-> input : Color` on all three functional forms |
| `CssColor::CurrentColor` | `namedColor "currentcolor"` @ `color.bbnf:56` | EXISTS |
| `CssColor::LightDark`, `CssColor::System` | not in grammar | OUT-OF-SCOPE (zero matches) |
| `CssColor::Float` with `none` components | `colorValue` @ `color.bbnf:32` | PARTIAL — `"none" -> NaN` alternative |
| `Length` (48-unit `LengthValue` + `Calc`) | `length` @ `value-unit.bbnf:62`, `calcFunction` @ `func-body.bbnf:43` | EXISTS — 48 unit tags match lightningcss ordering |
| `Angle` / `Time` / `Resolution` / `Frequency` / `Percentage` / `FlexValue` / `NumberOrPercentage` | `angle`, `time`, `resolution`, `frequency`, `percentage`, `flex` @ `value-unit.bbnf:63-68`; `colorValue` @ `color.bbnf:32` | EXISTS |
| `Image`, `Gradient` family, `Url` | `urlFunction` @ `func-body.bbnf:55`, `gradients.bbnf` | EXISTS |
| `CSSString`, `CustomIdent`, `DashedIdent` | `string` @ `func-body.bbnf:10`, `ident` @ `properties.bbnf:19`, `dashIdent` @ `properties.bbnf:38`, `dashed` @ `func-body.bbnf:49` | EXISTS |
| `Selector`, `SelectorList`, `RelativeSelectorList`, `Combinator`, `AttrOperator`, `AttrSelector` | `complexSelector` / `selectorList` / `relativeSelectorList` / `combinator` / `attrMatcher` / `attrSelector` @ `selectors.bbnf:99/106/103/93/35/39` | EXISTS — u8 tags match lightningcss |
| `PseudoClass` hot subset (~20 keywords) | `simplePseudoClass` long-tail fallback @ `selectors.bbnf:69` | PARTIAL — `identifiedPseudoClass` u8-tagged alt before fallback |
| `PseudoClass::Nth/Lang/Dir/Is/Where/Not/Has` | `nthPseudo` / `langPseudo` / `dirPseudo` / `isPseudo` / `wherePseudo` / `notPseudo` / `hasPseudo` @ `selectors.bbnf:54-68` | EXISTS |
| `PseudoElement` hot subset (`::before` / `::after` / `::first-letter` / `::first-line` / `::placeholder` / `::marker` / `::backdrop` / `::selection`) | `simplePseudoElement` long-tail @ `selectors.bbnf:79` | PARTIAL — `identifiedPseudoElement` u8-tagged alt |
| `FontFamily`, `FontWeight`, `BackgroundPosition`, `TransformList`, `Filter`, `EasingFunction` | `fontDecl`, `bgDecl`, `transforms.bbnf`, `filters.bbnf`, `easing.bbnf` | PARTIAL — typed annotations per function |
| `MediaFeature` range form (`(width >= 768px)`) | `mediaFeature` @ `media.bbnf:21` | PARTIAL — range-form alternative |

### Wave scope bullets

1. **Audit enumeration.** Walk
   `~/.cargo/registry/src/index.crates.io-*/lightningcss-1.0.0-alpha.71/src/{rules,values,properties,selector,media_query}*.rs`;
   produce
   `docs/tranches/AY-III/waves/W1-lightningcss-audit.csv` with
   columns `lightningcss_type, lightningcss_path, grammar_rule,
   grammar_file_line, state`. Table above is authoring seed; CSV
   is committed ground truth.
2. **Emit `CssL4Parser<TypeCamel>Projection` plus
   `materialize_projection_*`** for every EXISTS plus PARTIAL
   row via the post-B5 emitter pipeline (IR `payload::layout`
   plus Rust backend `named_types`). No hand-coded mapping
   table. No `RustNamedTypes` hard-coded entries.
3. **Close MISSING rows** via new grammar rules under
   `grammar/css/l4/at-rules.bbnf` (new module per the
   `@import`-module discipline; no god module). Plumb into
   `atRule` alt before `genericAtRule`. Each new rule carries
   `-> TypeName` plus an adjacent comment citing the
   lightningcss target plus the fixture demonstrating it.
4. **Close OUT-OF-SCOPE rows** with `grep -c` evidence in the
   audit CSV; `genericAtRule` fallback admits token-for-token
   so admission parity holds; typed parity defers to BA per
   FINAL's deferred ledger.
5. **Retire `__named_type_shim_color`** at
   `view/named_types.rs`. The post-B5 `infer_named_shape`
   already synthesises the 5-field `(U8, F64, F64, F64, F64)`
   tuple for `colorFunction`'s Seq kernel; W1.b routes the
   emitter to consume that shape as the projection, not the
   shim. `RustNamedTypes::bindings` keeps `Color` as a
   CSP-inferred fact populated from `ir.types`; no hand-authored
   slice survives.
6. **Per-group `PropertyProjection` emission.** Each of the 27
   typed-declaration groups emits `CssL4Parser<Group>PropertyProjection
   { variant_idx: u8, value: arena_handle }`. Group dispatch
   lives in `declaration` Alt; per-property u8 tags already
   declared drive `variant_idx`. CSP facts flow via
   `ir.payload_layouts`.
7. **Author `crates/core/tests/css_admission_totality.rs`** —
   the CSS-scope wire-contract test. For every entry in
   `<CssL4Parser>::PROJECTION_DIRECT_TO_STRUCT`, assert a
   corresponding `materialize_projection_<rule>_CssL4Parser`
   symbol resolves through the emitted impl and produces a
   non-`None` result on a fixture where the rule fires
   (tailwind covers the wide AST surface). Asserts admission
   count == materialiser count == consumer count.
8. **Extend `css_l4_canonical_parity.rs` corpus.** Add ≥ 15
   hand-authored 5–30-line fixtures per targeted feature under
   `data/css/lightningcss-corpus/`; one
   `canonical_parity_<feature>` test each. Graduate bootstrap
   from `scale_interop_` to `canonical_parity_` as
   `token_normalize` rules land; tailwind stays on
   `scale_interop_` per the calc-evaluator gap (BA-scope).
9. **Tighten `lightningcss_parity.rs`.** Extend
   `color_channel_parity_rgb_family` to
   `color_channel_parity_all_families`; add `selector_parity`,
   `media_query_parity`, `property_parity`. Zero `#[ignore]`
   admitted.
10. **Tighten `typed_accessor_surface.rs`** floors to exact
    post-W1 numbers.
11. **CSS competitor bench publishing.**
    `crates/core/benches/css/competitors.rs` gains a
    `bench_bbnf_css` group (normalize + bootstrap + tailwind);
    one `cargo bench -p bbnf --bench competitors` captures all
    three parsers; ratios publish to
    `docs/benchmarks/post-AY-III-W1-competitors-css.txt`.
12. **Sheets parity-test tightening.** `sheets_parity.rs`,
    `sheets_expr_parity.rs`, `sheets_self_parity.rs` retighten
    for fused-pipeline truth on the post-B5 substrate; ≥ 9 new
    field-for-field `SheetsGValue` assertions in
    `sheets_expr_parity.rs`; zero new `#[ignore]`.
13. **`sheets_parse_nested_no_panic` sentry.** Direct-path test
    parsing every line of `data/sheets/nested.txt`; the deeply
    nested `IF(AND(...), VLOOKUP(...), IFERROR(...))` is the
    AUDIT-D §5 tripping shape; its green state is the
    transitive-fix evidence on the post-B5 substrate.
14. **Sheets projection totality** (post-W1.d): 10
    `PROJECTION_DIRECT_TO_STRUCT` entries == 10
    `materialize_projection_*_SheetsG` fns == 10 production
    consumer call sites (or the W1.d audited extension count).
15. **Samply per CSS fixture (interactive `samply record`,
    NOT `--save-only`)** under
    `.profiles/samply/AY-III-W1/css_{tailwind,bootstrap,normalize}/`.
    Hot-path owner is the fused pipeline plus `Tape<R>::*`;
    zero self-time on `materialize_projection_*` (the project
    runs once at `to_value()`, not on the hot parse path),
    zero retired-surface attribution.
16. **Samply per Sheets fixture** under
    `.profiles/samply/AY-III-W1/sheets_{simple,nested,stress}/`.
17. **Fat-LTO 5-bench matrix clean.**
    `make ay-bench-close WAVE=W1-close` runs across all 5
    bench binaries; no panic on any grammar; `compile_bbnf`
    median ≤ 2.806 ms × 1.05 (post-B5 baseline).
18. **AZ-I.W0 baseline-bench MERGE absorption.** Publish
    `docs/benchmarks/post-AZ-I-W0-baseline.json` capturing the
    5 fixture-baseline numbers AZ-I.W0 needs as its baseline
    (json_monolithic, css_l4, google_sheets_monolithic,
    bbnf_monolithic, compile_pipeline). The merge eliminates
    redundant bench cycles between AY-III.W1 close and
    AZ-I.W0 open; the artefact is published from W1.e in the
    same fat-LTO matrix run.

## File bounds

| File | Access | Owner |
|---|---|---|
| `grammar/css/l4/stylesheet.bbnf` | modify | W1.a |
| `grammar/css/l4/properties.bbnf` | modify | W1.a |
| `grammar/css/l4/color.bbnf` | modify | W1.a |
| `grammar/css/l4/selectors.bbnf` | modify | W1.a |
| `grammar/css/l4/media.bbnf` | modify | W1.a |
| `grammar/css/l4/value-unit.bbnf` | modify | W1.a |
| `grammar/css/l4/func-body.bbnf` | modify | W1.a |
| `grammar/css/l4/values.bbnf` | modify | W1.a |
| `grammar/css/l4/gradients.bbnf` | modify | W1.a |
| `grammar/css/l4/transforms.bbnf` | modify | W1.a |
| `grammar/css/l4/filters.bbnf` | modify | W1.a |
| `grammar/css/l4/easing.bbnf` | modify | W1.a |
| `grammar/css/l4/keywords.bbnf` | modify | W1.a |
| `grammar/css/l4/at-rules.bbnf` | create | W1.a |
| `crates/ir/src/passes/payload/layout.rs` | modify | W1.b |
| `crates/core/src/backend/rust/view/named_types.rs` | modify-carve | W1.b |
| `crates/core/src/backend/rust/emitter/shapes/value_materialize.rs` | modify | W1.b |
| `crates/core/tests/css_admission_totality.rs` | create | W1.b |
| `crates/core/tests/lightningcss_parity.rs` | modify | W1.c |
| `crates/core/tests/css_l4_canonical_parity.rs` | modify | W1.c |
| `crates/core/tests/typed_accessor_surface.rs` | modify | W1.c |
| `crates/core/tests/css_l4_parity.rs` | modify | W1.c |
| `crates/core/tests/css_l4_named_color_parity.rs` | modify | W1.c |
| `crates/core/tests/common/css_normalize.rs` | modify | W1.c |
| `data/css/lightningcss-corpus/` | create | W1.c |
| `crates/core/tests/sheets_parity.rs` | modify | W1.d |
| `crates/core/tests/sheets_expr_parity.rs` | modify | W1.d |
| `crates/core/tests/sheets_self_parity.rs` | modify | W1.d |
| `crates/core/tests/sheets_parse_nested_no_panic.rs` | create | W1.d |
| `crates/core/tests/sheets_admission_totality.rs` | create | W1.d |
| `crates/core/benches/css/competitors.rs` | modify | W1.e |
| `crates/core/benches/css/l4.rs` | modify | W1.e |
| `crates/core/benches/google_sheets/monolithic.rs` | modify | W1.e |
| `docs/tranches/AY-III/waves/W1-lightningcss-audit.csv` | create | W1.a |
| `docs/benchmarks/post-AY-III-W1-competitors-css.txt` | create | W1.e |
| `docs/benchmarks/post-AY-III-W1-monolithic.txt` | create | W1.e |
| `docs/benchmarks/post-AY-III-W1-nm.txt` | create | W1.e |
| `docs/benchmarks/post-AY-III-W1-samply-summary.txt` | create | W1.e |
| `docs/benchmarks/post-AZ-I-W0-baseline.json` | create | W1.e |
| `.profiles/samply/AY-III-W1/css_{tailwind,bootstrap,normalize}/` | populate | W1.e |
| `.profiles/samply/AY-III-W1/sheets_{simple,nested,stress}/` | populate | W1.e |
| `target/expand/ay-css-l4.rs` | regen | orchestrator |
| `target/expand/ay-sheets.rs` | regen | orchestrator |
| `crates/core/src/grammar/generated/{css_l4,google_sheets}.rs` | regen | orchestrator |

**Do NOT touch**: `grammar/json/**`, `grammar/bbnf/**`,
`grammar/ebnf/**`, `grammar/bnf/**`, `grammar/css/l2/**`,
`grammar/css/pretty.bbnf`; `crates/tape/**` (post-B5 closed
substrate); `crates/core/src/runtime/**` (post-B5 closed);
`crates/core/src/backend/rust/emitter/shapes/{object,array,wrap,
keyword,inline,alt_dispatch,flat,pratt}.rs` (B5 closed); JSON /
BBNF benches + tests; `docs/tranches/AY-III/AY-III.md` +
`PROGRESS.md` (orchestrator-owned); any crate outside
`crates/{core,ir}/`.

## Phase sub-items

### AY-III.W1.a — Grammar annotation audit + extension

Owner: CSS L4 grammar files.

Mechanism:

1. Produce `W1-lightningcss-audit.csv` by walking
   `~/.cargo/registry/src/index.crates.io-*/lightningcss-1.0.0-alpha.71/src/`.
   Inventory table in §Scope is authoring seed.
2. EXISTS rows: add `-> TypeName` annotation. Specific edits
   (non-exhaustive — CSV is ground truth):
   - `stylesheet.bbnf`: `qualifiedRule -> StyleRule`,
     `mediaRule -> MediaRule`, `keyframesRule -> KeyframesRule`,
     `keyframeSel -> KeyframeSelector`,
     `blockContent -> DeclarationBlock`.
   - `properties.bbnf`: 27 typed decls annotate with
     `-> <Group>Property`; `customPropertyDecl -> CustomProperty`;
     `importantSuffix -> Important`.
   - `color.bbnf`: `colorType -> ColorFunctionKind`,
     `colorSpace -> PredefinedColorSpace`,
     `mixSpace -> ColorSpace`,
     `hueMethodKeyword -> HueInterpolationMethod`. The
     `-> input : Color` on `colorFunction` / `colorFn` /
     `colorMix` remains.
   - `value-unit.bbnf`: `length -> Length`, `angle -> Angle`,
     `time -> Time`, `frequency -> Frequency`,
     `resolution -> Resolution`, `flex -> FlexValue`,
     `percentage -> Percentage`.
   - `selectors.bbnf`: `combinator -> Combinator`,
     `attrMatcher -> AttrOperator`,
     `attrSelector -> AttrSelector`,
     `complexSelector -> Selector`,
     `selectorList -> SelectorList`,
     `relativeSelectorList -> RelativeSelectorList`,
     `nthPseudo -> NthSelector`, `langPseudo -> LangSelector`,
     etc.
   - `media.bbnf`: `mediaQuery -> MediaQuery`,
     `mediaCondition -> MediaCondition`,
     `mediaFeature -> MediaFeature`,
     `mediaQueryList -> MediaQueryList`.
3. MISSING rows: new `grammar/css/l4/at-rules.bbnf` module
   `@import`'s `value-unit.bbnf` + `selectors.bbnf` +
   `properties.bbnf`; declares `fontFaceRule`, `supportsRule`
   + `supportsCondition`, `containerRule` + `containerCondition`,
   `layerStatementRule`, `layerBlockRule`, `pageRule`,
   `importRule`, `namespaceRule`, `propertyRule`; each with
   `-> TypeName`. `stylesheet.bbnf`'s `atRule` alt prepends
   the new rules before `genericAtRule`.
4. PARTIAL rows: `selectors.bbnf` adds `identifiedPseudoClass`
   u8-tagged alt over the 20 hot keywords plus
   `identifiedPseudoElement` for the 8 hot keywords.
   `color.bbnf` `colorValue` admits `"none" -> NaN`.
   `properties.bbnf` factors `fontFamilyList` for the
   font-family path. Gradient / transform / filter / easing
   rules gain per-function `-> TypeName`.
5. After each module edit,
   `cargo expand -p bbnf --bench css_l4 >
   target/expand/ay-css-l4.rs`; confirm the new annotation
   surfaces as a projection struct. Declined admission routes
   to W1.b per SPEC §Diagnostic-loop.

Files: `grammar/css/l4/{stylesheet,properties,color,selectors,
media,value-unit,func-body,values,gradients,transforms,
filters,easing,keywords}.bbnf` (modify);
`grammar/css/l4/at-rules.bbnf` (create);
`docs/tranches/AY-III/waves/W1-lightningcss-audit.csv`
(create).

Sub-gate:
- `W1-lightningcss-audit.csv` committed; every row has state
  + (OUT-OF-SCOPE) `grep` evidence.
- `cargo iter-check` exit 0.
- `grep -c 'pub struct CssL4Parser\w*Projection'
  target/expand/ay-css-l4.rs` ≥ 80 (pre-W1: 48 per AUDIT-B).
- `cargo test -p bbnf --test css_l4_parity --release` green.
- No `.bbnf` edit touches a rule outside `grammar/css/l4/**`.

### AY-III.W1.b — Projection emission completion

Owner: IR payload-layout pass + Rust backend view synthesis.

Mechanism:

1. Run `crates/core/tests/projection_totality.rs` against the
   post-W1.a grammar. Every `-> Type`-annotated rule failing
   to emit a projection feeds an extension to
   `crates/ir/src/passes/payload/layout.rs`. CSP facts drive
   admission; zero grammar-name dispatch added.
2. Layout-pass extensions (grammar-structure-driven, not
   name-driven):
   - `Selector` / `SelectorList` / `RelativeSelectorList`:
     repeat-of-complex → `CssL4ParserSelectorProjection {
     components: (arena_offset, arena_len) }`; materialiser
     walks the arena slab.
   - `MediaCondition` / `ContainerCondition` /
     `SupportsCondition` recursive Alt: variant-tag u8 +
     ColorRef-style arena-handle (mirrors `colorMix`'s
     ColorRef pattern @ `color.bbnf:266-268`). The ColorRef
     pattern generalises to every recursive Alt.
   - `Length::Calc` / `mathExpr` nesting: same arena-handle
     pattern applied to `calcFunction` / `mathExpr` /
     `mathProduct`.
   - `Property<'i>` per-group: each of the 27 typed-decl
     groups emits `CssL4Parser<Group>PropertyProjection {
     variant_idx: u8, value: arena_handle }`. Group dispatch
     via `declaration` Alt; `ir.payload_layouts` already
     carries the u8 width.
3. Retire `__named_type_shim_color` at
   `view/named_types.rs`. The B5-post `infer_named_shape`
   synthesises the 5-field `(U8, F64, F64, F64, F64)` tuple
   for `colorFunction`'s Seq kernel; W1.b routes the emitter
   to consume that shape as the projection, not the shim.
   `RustNamedTypes::bindings` keeps `Color` as a
   CSP-inferred fact populated from `ir.types`; no
   hand-authored slice survives.
4. Emit `CssL4Parser<Group>PropertyProjection` via extension
   to `shapes/value_materialize.rs`: when a rule's declared
   type is `Named(PropertyGroup)` and body is
   `Seq([propsAlt, value_list, …])`, route the `propsAlt`'s
   u8 tag into `variant_idx` rather than a generic
   `field_0`.
5. Author `crates/core/tests/css_admission_totality.rs` — the
   CSS-scope wire-contract test asserting 1:1:1 admission ↔
   materialiser ↔ consumer.

Sub-gate:
- `cargo iter-check` exit 0.
- `cargo test -p bbnf --test css_admission_totality --profile
  ax-iter` green.
- `cargo test -p bbnf --test named_type_preservation --profile
  ax-iter` green.
- `grep -c '__named_type_shim' target/expand/ay-css-l4.rs` →
  0.
- `grep -c 'pub struct CssL4Parser\w*Projection'
  target/expand/ay-css-l4.rs` ≥ 80;
  `grep -c 'fn materialize_projection_\w*_CssL4Parser'`
  equals projection count.
- `grep 'static.*NamedTypeBinding\|const.*NamedTypeBinding\|&\[NamedTypeBinding\b'
  crates/core/src/backend/rust/view/named_types.rs` empty.

### AY-III.W1.c — CSS canonical parity + lightningcss-parity tightening

Owner: CSS test surface
(`lightningcss_parity.rs`, `css_l4_canonical_parity.rs`,
`typed_accessor_surface.rs`, `css_l4_parity.rs`,
`css_l4_named_color_parity.rs`).

Mechanism:

1. `lightningcss_parity.rs`:
   - Extend `color_channel_parity_rgb_family` →
     `color_channel_parity_all_families`: RGBA, LAB / LCH /
     OKLab / OKLCH, Predefined (color() with space), Float
     (with `none` components), CurrentColor. Drop the
     pre-wiring empty-guard
     (`if bbnf_colors.is_empty() { return; }`); W1.b's
     projection landing makes it live.
   - New `selector_parity` test: 10 inline fixtures
     exercising each `Component` variant.
   - New `media_query_parity` test: 5 fixtures.
   - New `property_parity` test: 20-declaration inline CSS;
     assert lightningcss's `Property` variant ↔ bbnf's
     `CssL4Parser<Group>PropertyProjection.variant_idx` 1:1.
2. `typed_accessor_surface.rs`: `css_l4_accessor_surface`
   tightens from `>= 1` floors to exact post-W1 numbers
   (authoring seed: `r.aggregate >= 12`,
   `r.leaf_scalar >= 60`, `r.alt >= 25`; W1.c enshrines exact
   floors from the post-W1 expand).
3. `lightningcss_parity_{normalize,bootstrap,tailwind}`:
   tighten `bbnf > 1000` / `lc >= 500` to specific counts
   from the W1 close.
4. `css_l4_parity.rs` + `css_l4_named_color_parity.rs`:
   assertions track 148 named colors; grep-assert emitter
   lands a projection per branch.
5. `css_l4_canonical_parity.rs`: per-feature fixtures under
   `data/css/lightningcss-corpus/` (one per targeted surface;
   5–30 lines each); ≥ 15 fixtures.
6. Zero `#[ignore]` admitted in the CSS parity test family.
   Pre-existing `#[ignore]` tags either retire (their blocker
   is closed) or attribute to a W1-scope MISSING row
   (deferred ledger entry in PROGRESS.md).

Sub-gate:
- `cargo iter-check` exit 0.
- `cargo test -p bbnf --test lightningcss_parity --release`
  green; new tests green.
- `cargo test -p bbnf --test typed_accessor_surface --profile
  ax-iter` green; counts match post-W1 enshrined floors.
- `cargo test -p bbnf --test css_l4_parity --release` +
  `--test css_l4_named_color_parity --release` green.
- `grep -rn '#\[ignore\]'
  crates/core/tests/{lightningcss_parity,
  css_l4_canonical_parity,typed_accessor_surface,
  css_l4_parity,css_l4_named_color_parity}.rs` empty.

### AY-III.W1.d — Sheets parity + projection totality + panic verification

Owner: Sheets test surface.

Mechanism:

1. **Projection totality on Sheets.** Run the wave-contract
   test against the post-B5 substrate; confirm 10:10:10
   agreement (`PROJECTION_DIRECT_TO_STRUCT` entries :
   `materialize_projection_*_SheetsG` fns : production
   consumer reachability through
   `<Parsed<SheetsParser>>::to_value()`).
2. **Assertion-tightening in `sheets_parity.rs`.** Rewrite
   per-test documentation to reflect post-B5 fused-pipeline
   truth; drop obsolete pinned-to-fail framing on every
   assertion firing positively post-B5; tighten
   `≥ N occurrences` tolerances to exact-count where a
   structural invariant admits.
3. **Assertion-tightening in `sheets_expr_parity.rs`.** Add
   ≥ 9 `SheetsGValue` field-for-field assertions (one per
   primary variant: `FnCall`, `BinOp`, `Literal::Number`,
   `Literal::Str`, `Literal::Bool`, `Literal::Error`, `Cell`,
   `Ident`, `Paren`) asserting structural equality against
   the post-B5 constructed value enum. Oracle remains the
   hand-written recursive descent.
4. **Assertion-tightening in `sheets_self_parity.rs`.**
   Verify the 50+ serialise-roundtrip + prettify-idempotency
   cases green; three fixture-file corpus sweeps
   (`corpus_simple`, `corpus_nested`, `corpus_stress`).
5. **`sheets_parse_nested_no_panic` sentry.** Direct-path
   test parsing every line of `data/sheets/nested.txt` and
   asserting `SheetsParser::parse(line).is_ok()`. The first
   line — the deeply nested
   `IF(AND(...), VLOOKUP(...), IFERROR(...))` — is the
   AUDIT-D §5 tripping shape; its green state is the
   transitive-fix evidence on the post-B5 substrate.
6. **`sheets_admission_totality`** — Sheets-scope
   wire-contract test asserting 1:1:1.

Files: `crates/core/tests/sheets_parity.rs`,
`crates/core/tests/sheets_expr_parity.rs`,
`crates/core/tests/sheets_self_parity.rs`,
`crates/core/tests/sheets_parse_nested_no_panic.rs`,
`crates/core/tests/sheets_admission_totality.rs`.

Sub-gate:
- `cargo test -p bbnf --test sheets_parity --profile ax-iter`
  green; zero new `#[ignore]`.
- `cargo test -p bbnf --test sheets_expr_parity --profile
  ax-iter` green with ≥ 9 new field-for-field assertions.
- `cargo test -p bbnf --test sheets_self_parity --profile
  ax-iter` green on the full corpus.
- `cargo test -p bbnf --test sheets_parse_nested_no_panic
  --profile bench` green.
- `cargo test -p bbnf --test sheets_admission_totality
  --profile ax-iter` green.

### AY-III.W1.e — CSS competitor bench + Sheets bench + samply + AZ-I baseline

Owner: CSS + Sheets bench harness, samply per fixture, close
evidence, AZ-I.W0 baseline-bench MERGE absorption.

Mechanism:

1. `crates/core/benches/css/competitors.rs` gains
   `bench_bbnf_css` group (`bbnf_css_normalize`,
   `bbnf_css_bootstrap`, `bbnf_css_tailwind`), each driving
   `CssL4Parser::parse`; `b.bytes = input.len() as u64` so
   per-byte throughput publishes on the shared axis;
   `benchmark_main!` lists all three parser groups.
2. `crates/core/benches/css/l4.rs` audit: no
   `parse_with_visitor` visitor-lane probe survives (the
   emission path retired at B5.W1 — the fused parse IS the
   visitor lane).
3. `cargo bench --profile bench -p bbnf --bench competitors`
   + `--bench css_l4` + `--bench google_sheets_monolithic`
   run fat-LTO; publish throughput + ratios to
   `docs/benchmarks/post-AY-III-W1-competitors-css.txt`.
4. Samply per CSS fixture via interactive `samply record`
   (NOT `--save-only`); captures at
   `.profiles/samply/AY-III-W1/css_{tailwind,bootstrap,normalize}/profile.json.gz`;
   top-5 self-time to `post-AY-III-W1-samply-summary.txt`.
5. Samply per Sheets fixture under
   `.profiles/samply/AY-III-W1/sheets_{simple,nested,stress}/`.
6. Hot-path assertion: fused-pipeline parse fn +
   `Tape::push_leaf_*` / `Tape::begin_compound` /
   `Tape::end_compound` (post-B5 symbols) appear in top-5
   self-time per fixture; zero self-time on
   `Parsed::to_value` / `navigate_tape` /
   `ValueBuilder::*` / tape-reconstruction paths.
7. `nm` audit on each bench binary; capture
   `post-AY-III-W1-nm.txt`. Same retired-surface absence
   audit as W0.c; same post-B5 surface presence audit.
8. `make ay-bench-close WAVE=W1-close` clean across all 5
   bench binaries; aggregate to
   `post-AY-III-W1-monolithic.txt`.
9. Author `docs/benchmarks/post-AZ-I-W0-baseline.json` — the
   MERGE absorption per audit consolidation §MERGES item 3.
   The artefact captures the 5 fixture-baseline numbers
   AZ-I.W0 needs (json_monolithic, css_l4,
   google_sheets_monolithic, bbnf_monolithic,
   compile_pipeline) from the same fat-LTO matrix run.
   Eliminates redundant bench cycles between AY-III.W1 close
   and AZ-I.W0 open.

Sub-gate:
- `cargo bench --profile bench -p bbnf --bench css_l4` +
  `--bench competitors` + `--bench google_sheets_monolithic`
  complete; all fixtures publish throughput.
- `post-AY-III-W1-competitors-css.txt` committed; per-fixture
  ratios extracted.
- `post-AY-III-W1-samply-summary.txt` committed; top-5 per
  fixture named.
- `.profiles/samply/AY-III-W1/{css_*,sheets_*}/profile.json.gz`
  committed.
- `post-AY-III-W1-nm.txt`: retired-surface symbols absent on
  every bench binary; `Tape<R>::*` symbols present.
- `post-AZ-I-W0-baseline.json` valid JSON with 5
  fixture-baseline entries.

## Hard gate — peer-referenced (wave close)

### Admission parity

1. `cargo test -p bbnf --test lightningcss_parity --release`
   green with new `color_channel_parity_all_families`,
   `selector_parity`, `media_query_parity`, `property_parity`.
   Zero `#[ignore]`. Artefact: test pass log.
2. `cargo test -p bbnf --test css_l4_parity --release` green.
3. `cargo test -p bbnf --test css_l4_named_color_parity
   --release` green on 148 named colors.

### Byte-identical canonical parity

4. `cargo test -p bbnf --test css_l4_canonical_parity
   --release` green on the post-W1 corpus
   (`canonical_parity_normalize` plus per-feature fixtures).
   Artefact: `data/css/lightningcss-corpus/` ≥ 15 fixtures.

### Typed-accessor + projection invariants

5. `cargo test -p bbnf --test typed_accessor_surface
   --profile ax-iter` green; CSS L4 floors match post-W1
   exact counts.
6. `cargo test -p bbnf --test css_admission_totality
   --profile ax-iter` green on CSS L4 (1:1:1 wire contract).
7. `cargo test -p bbnf --test sheets_admission_totality
   --profile ax-iter` green on Sheets (1:1:1 wire contract).
8. `grep -c 'pub struct CssL4Parser\w*Projection'
   target/expand/ay-css-l4.rs` ≥ 80 (AUDIT-B pre-W1: 48).
9. `grep -c 'fn materialize_projection_\w*_CssL4Parser'
   target/expand/ay-css-l4.rs` equals projection count
   (totality).
10. `grep -c '__named_type_shim' target/expand/ay-css-l4.rs`
    → 0.
11. `grep 'static.*NamedTypeBinding\|const.*NamedTypeBinding\|&\[NamedTypeBinding\b'
    crates/core/src/backend/rust/view/named_types.rs` empty.
12. `grep -rn 'CssL4Parser' crates/core/src/backend/rust/
    crates/core/src/runtime/ crates/tape/src/` as dispatch
    predicate (`match` / `if` / `cfg`) → 0 (invariant 8
    preservation).
13. `grep -E '^fn (Color|Length|Angle|Time|Resolution|
    Frequency|Percentage|Selector|MediaQuery) \b'
    crates/core/src/backend/rust/**/*.rs` → 0 hand-written
    accessors.

### Sheets parity + panic verification

14. `cargo test -p bbnf --test sheets_parity --profile
    ax-iter` green; zero new `#[ignore]`.
15. `cargo test -p bbnf --test sheets_expr_parity --profile
    ax-iter` green with ≥ 9 new field-for-field assertions.
16. `cargo test -p bbnf --test sheets_self_parity --profile
    ax-iter` green on the full corpus.
17. `cargo test -p bbnf --test sheets_parse_nested_no_panic
    --profile bench` green.
18. `cargo bench --profile bench -p bbnf --bench
    google_sheets_monolithic parse_nested` exit 0 (no panic
    at fat-LTO).

### Competitor + performance

19. `bbnf_css_<fixture> / cssparser_<fixture> ≤ 1.00` for
    normalize + bootstrap + tailwind. Artefact:
    `post-AY-III-W1-competitors-css.txt`.
20. `bbnf_css_<fixture> / lightningcss_<fixture> ≤ 2.00` for
    normalize + bootstrap + tailwind (lightningcss's broader
    typed AST is the peer target; AY-III's target is parity-
    within-2× budget; exceedance is BA-scope).
21. Samply hot-path owner per CSS fixture: fused-pipeline
    parse fn + `Tape<R>::*` in top-5 self-time; zero
    self-time on `navigate_tape` / generic child-walk /
    tape-reconstruction. Artefact:
    `post-AY-III-W1-samply-summary.txt`.
22. Samply hot-path owner per Sheets fixture: same
    fused-pipeline attribution; zero
    `materialize_projection_*_SheetsG` self-time on the
    parse path (the project runs once at `to_value()`, not
    on the hot parse path); zero retired-surface symbols.

### Fat-LTO + substrate stability

23. `make ay-bench-close WAVE=W1-close` clean across all 5
    bench binaries. Artefact:
    `post-AY-III-W1-monolithic.txt`.
24. `nm` on bench binaries: retired-surface symbols absent
    (post-B5 surface map per AY-III.W0.c full list); post-B5
    surface symbols present. Artefact:
    `post-AY-III-W1-nm.txt`.

### AZ-I.W0 baseline absorption

25. `docs/benchmarks/post-AZ-I-W0-baseline.json` exists with
    5 fixture-baseline entries; valid JSON; carries the
    same fat-LTO numbers the W1-close matrix run captured.
    Artefact: `post-AZ-I-W0-baseline.json`.

## Verification artefacts

- `target/expand/ay-css-l4.rs` — post-W1 regen.
- `target/expand/ay-sheets.rs` — post-W1 regen.
- `docs/tranches/AY-III/waves/W1-lightningcss-audit.csv` —
  ground truth.
- `docs/benchmarks/post-AY-III-W1-competitors-css.txt` — bbnf
  vs cssparser vs lightningcss ratios.
- `docs/benchmarks/post-AY-III-W1-monolithic.txt` — full
  5-bench fat-LTO capture.
- `docs/benchmarks/post-AY-III-W1-samply-summary.txt` — top-5
  self-time per fixture (CSS + Sheets).
- `docs/benchmarks/post-AY-III-W1-nm.txt` — `nm` audit.
- `.profiles/samply/AY-III-W1/css_{tailwind,bootstrap,normalize}/profile.json.gz`.
- `.profiles/samply/AY-III-W1/sheets_{simple,nested,stress}/profile.json.gz`.
- `data/css/lightningcss-corpus/*.css` — per-feature parity
  fixtures (≥ 15 files).
- `docs/benchmarks/post-AZ-I-W0-baseline.json` — AZ-I.W0
  baseline-bench MERGE absorption.
- Per-sub-agent commit SHAs (W1.a / W1.b / W1.c / W1.d /
  W1.e).
- Test pass logs:
  `lightningcss_parity --release`,
  `css_l4_canonical_parity --release`,
  `typed_accessor_surface --profile ax-iter`,
  `css_admission_totality --profile ax-iter`,
  `css_l4_parity --release`,
  `css_l4_named_color_parity --release`,
  `named_type_preservation --profile ax-iter`,
  `sheets_parity --profile ax-iter`,
  `sheets_expr_parity --profile ax-iter`,
  `sheets_self_parity --profile ax-iter`,
  `sheets_parse_nested_no_panic --profile bench`,
  `sheets_admission_totality --profile ax-iter`.

## Dependencies

- **Depends on**: W0 close (JSON peer gates plus admission
  totality wire-contract plus parsecount green).
- **Blocks**: W2 (BBNF self-host plus close ceremony).

## Archaeology

- AY-I.W2 (`docs/tranches/AY-I/audit/AYW2-named-collapse-probe.md`)
  attempted CSS named-type preservation via a hand-authored
  `RustNamedTypes` static slice carrying `Color` plus
  `ColorMix`. AX.W1r.1 retired the static slice in favour of
  an `ir.types`-derived builder, landing the `Color` family
  via CSP inference. AY-I's typed-CSS work did not complete
  the full lightningcss AST surface — only `Color` landed,
  leaving `Length` / `Angle` / `Time` / `Selector` /
  `MediaQuery` / the 27-group `Property` dispatch / all
  at-rules (except `@media` / `@keyframes`) unannotated. W1
  closes that surface, guided by AUDIT-B §4's 49-vs-48-vs-1
  split (49 const entries, 48 materialisers, 1
  `__named_type_shim_color`). AY-I.W8's projected CSS close
  gate was a single `lightningcss_parity.rs` corpus-admission
  pass; AY-III.W1's peer-referenced gate set is strictly
  broader (admission + byte-identical canonical + typed-
  accessor totality + competitor ratio + samply attribution).
- AUDIT-D §5 (`docs/tranches/AY-II-I/audit/AY-II-AUDIT-D-predecessor-successor.md`)
  documented the `columns.rs:409` panic on `css_l4/bootstrap`
  + `google_sheets_monolithic/parse_nested` at AY-II-I
  pre-close pause. Root cause: W6.c Pratt outer-compound
  retarget reopened the rollback-under-open-frame hazard that
  W5.b's Shape-2 rationale was supposed to prevent. The
  B-series predecessor sequence (B4.W1 unified atomic-
  rollback path; B5.W1 substrate dissolution; B5.W4 cousin-
  leak migration; B5.W6 + W6b depth-stamp invariant
  inversion) repaired this transitively. W1.e verifies repair
  held across the post-W1 CSS annotations + Sheets fixtures;
  panic recurrence triggers diagnostic-loop relinquish per
  SPEC §Diagnostic-loop relinquish.
- AX.W1r.3 (`docs/tranches/AX/audit/W1r3a-diag.md`) documented
  the `scale_interop_<fixture>` vs `canonical_parity_<fixture>`
  split: byte-identical canonical parity for bootstrap /
  tailwind exceeds the harness because lightningcss performs
  semantic simplifications (calc folding, longhand-to-
  shorthand collapse) that no symmetric `token_normalize` can
  invert. W1.c preserves the split: bootstrap graduates from
  `scale_interop_` to `canonical_parity_` only if the post-W1
  `token_normalize` additions close every divergence class;
  tailwind remains on `scale_interop_` per the calc-evaluator
  gap (BA-scope).
- AY-II-I.W2 + W3 (this wave's pass-II predecessors) authored
  the same CSS plus Sheets close surface against the welded
  `FusedBuilder` substrate; the audit consolidation §MERGES
  item 1 + item 2 fold the two waves into AY-III.W1. The
  post-B5 substrate retired the welded surface; AY-III.W1
  inherits the AY-II-I.W2 + W3 scope verbatim, updates symbol
  references to the post-B5 surface, and consolidates the
  agent count (8 across two waves → 5 across one wave).
- AZ-I.W0 baseline-bench MERGE: per audit consolidation
  §MERGES item 3, AZ-I.W0's baseline-bench artefact absorbs
  into the AY-III.W1 fat-LTO matrix run. AZ-I.W0 carries
  load-bearing classifier-unification + IR audit scope; the
  baseline-bench was redundant with AY-III's close-matrix
  capture. The MERGE eliminates one bench cycle without
  touching AZ-I.W0's load-bearing scope.
