# B2.W0.c — Status snapshot (2026-04-25 04h)

Master at `21881591` after the W0.c partial-close cherry-pick. Autonomous
loop iteration paused; recording the current state so the next session
can resume without re-tracing.

## What landed cleanly

- `dec67806` — W0.a: xtask substrate scaffold + 9-grammar workspace
  manifest at `[workspace.metadata.bbnf]`.
- `3c68e8c4` — W0.b: per-grammar boundary spec + the BBNF surface
  snapshot. Crucial finding: pre-B2 `crates/core/src/grammar/
  generated.rs` (33 293 lines) IS one grammar — the whole file is the
  BBNF self-host emission. No other grammars have ever been on disk;
  every other consumer relies on `bbnf_derive` expanding at compile
  time.
- `21881591` — W0.c partial: `xtask::regen` body fully implemented;
  `crates/bootstrap/src/lib.rs` migrated from `#[derive(Parser)]` to
  `pub use ::bbnf::grammar::generated::BbnfBootstrap;` (the cleaner
  unification per W0.b §3.3); `crates/bootstrap/Cargo.toml` deps
  purged (only `bbnf` remains).

## What blocks W0.c close

`cargo xtask regen --grammar bbnf` (and its underlying invocation of
`bbnf::pipeline::compile_paths_request` on `grammar/bbnf/bbnf.bbnf`)
hangs > 5 min in the W0.c agent's environment. The agent's diagnostic
narrowed the wall to the parse step itself — `BbnfBootstrap::parse`
on `bbnf.bbnf` — not the IR-pass loop. Even the simpler `json` grammar
(537 bytes) hung > 39 s without completing.

This is **not** what the four 2026-04-25 deep audits inferred. The
audits attributed the 80+ min cold wall to "AY-II IR-pipeline
accumulation running inside rustc's expand phase". The W0.c probe
disproves that thesis: the wall reproduces in **release-profile direct
parse** with the IR pipeline never invoked. The actual regression is
runtime parser performance on `bbnf.bbnf`-shaped corpora.

## Most recent state of the orchestrator's own probes

After cherry-picking W0.c partial, the orchestrator attempted a probe
of `cargo test --profile ax-iter -p bbnf --test bbnf_parity
bbnf_parses_its_own_grammar` to establish a baseline + bisect.
Cargo hung in test-binary compile (no rustc spawned) for > 10 min;
killed at orchestrator level. `cargo metadata --offline` still
responds in < 1 s, so cargo itself is healthy — the hang is in the
test-binary's compile chain (which pulls `bbnf_derive` expansion of
`bbnf-bootstrap` and other test consumers, hitting the 80-min cold
wall).

The orchestrator chose to halt the autonomous loop rather than
continue burning compute. Resumption requires either:

1. A long-running probe in a fresh shell (one-time cold compile of the
   test binary; subsequent runs of the binary fast).
2. A targeted bisect against a SMALLER consumer than `bbnf-bootstrap`
   that doesn't pull the full proc-macro chain (e.g., a tiny custom
   binary that links only `bbnf` lib + reads `bbnf.bbnf` + calls
   `BbnfBootstrap::parse` directly — no other consumers, no other
   derive sites).
3. Running the bisect in a separate worktree that's already cached
   the heavy proc-macro expansions (so each bisect-step's `cargo test`
   doesn't re-pay the 80-min wall).

## Recalibrated plan

The four audits identified the right architectural transposition (T3 —
move codegen to xtask + checked-in source files), and the W0.a/W0.b
substrate work is mature. But T3 alone doesn't dissolve the parser
regression — it relocates the wall from rustc-expand-time to
xtask-runtime, but the wall persists.

Two paths from here:

### (R1) Diagnose-and-fix as a B3 prelude annex

Open B3 as a focused prelude annex that bisects + fixes the
`BbnfBootstrap::parse` regression. Likely scope: 1-3 commits in the
ax-w0a → master range. Bisect candidates:

- `bd563c1d` — W0'.a FusedBuilder collapse (replaces
  TapeBuilder/ValueBuilder split). Most likely culprit: the parse
  emits BOTH columns; a per-record extra-write might compound on
  large grammars.
- `f8ac2cd7` — W0-fix begin_compound migration (variant_idx + meta_idx
  packing). Affects every compound write.
- `f768f50d` — W0'.d3 `value_end_compound` O(1) refactor. Was supposed
  to FIX an O(N²); regression risk if it broke a stamping path.

After fix lands, re-run `cargo xtask regen --grammar bbnf` — should
complete in seconds (the IR pipeline alone is fast per `compile_pipeline`
bench's 911 µs measurement).

### (R2) Revert AY-II.W0' source landings

W0'.a/b/c are on master but `generated.rs` was never regenerated
post-W0'. The cherry-picked source landings affect runtime types
(FusedBuilder, materializer routing, scan-policy splice) without the
emitter ever updating its output. Master is in a half-migrated state.

Reverting W0'.a/b/c returns the workspace to a known-good substrate
(AY-I close at `e777a68d`-ish) where:
- `BbnfBootstrap::parse` runs fast.
- `cargo expand -p bbnf-bootstrap` completes in 5-10 min historically.
- `compile_pipeline` bench's `compile_bbnf` runs in 911 µs.

Then B2 lands cleanly on the AY-I substrate, and W0'.a/b/c re-land as
a fresh tranche AFTER B2 closes — under the post-B2 substrate (xtask
+ checked-in per-grammar files), the parser regression diagnosis is
straightforward (run + bisect against the BBNF parse test directly,
no proc-macro re-compile cost).

### (R3) Hybrid

Land R2's revert as B3.W0; run B2 (W0.c-W4) cleanly on the post-revert
substrate; then re-land W0'.a/b/c as B4 (the new tranche) using B2's
cargo xtask regen to close cleanly.

## Forward decision points (for the next session)

1. Which path: R1 (bisect-and-fix) vs R2 (revert + re-tranche) vs R3
   (hybrid).
2. If R1: dispatch a focused bisect agent in a fresh shell where the
   test binary can compile once (paying the 80-min wall once).
3. If R2/R3: open B3 as the revert tranche; B3 is single-commit:
   `git revert` the W0'.a/b/c commits + cherry-pick onto master. Then
   B2 resumes from the post-revert substrate.

## State check + handoff invariants

- Master HEAD: `21881591`.
- Working tree: clean.
- Worktrees: only the main `bbnf-lang` checkout. No active worktrees.
- Background processes: none. All cargo/rustc killed.
- `target/` state: warm with B2.W0.a/b xtask substrate compiled.
- xtask substrate: builds clean (`cargo build -p xtask --release` =
  75 s cold, < 5 s warm). Calling `cargo xtask regen --grammar bbnf`
  hangs in the parse step.
- bbnf-bootstrap: migrated to `pub use` re-export; compiles via
  `bbnf::grammar::generated::BbnfBootstrap` once that path resolves.
  Currently `generated::BbnfBootstrap` resolves to the pre-B2
  monolithic `generated.rs` content (not yet split; W0.c didn't land
  the split because regen hung). The bbnf-bootstrap migration is a
  no-op operationally but lands the contract.
- AY-II: still blocked on B2 close; W0' source landings on master
  unchanged.

## Key insight (for the next session)

The four 2026-04-25 deep audits (`docs/audit/2026-04-25-deep-audit/
AUDIT-{A,B,C,D}-*.md`) are still load-bearing analyses BUT one
inferred wrong root cause:

- AUDIT-A correctly identified two B1 bugs (rustflags warm regression
  + bench-json alias drift). Both fixed at `81195656`.
- AUDIT-B's ceremony-compression analysis is correct.
- AUDIT-C's "AZ-I.W0 doesn't fix the cold wall" finding is correct.
- AUDIT-D's T3 recommendation (xtask + checked-in generation) is
  correct as architectural endpoint.

But: all four assumed the wall was IR-pipeline-at-expand-time
accumulation. The W0.c probe disproves that. The wall is in
`BbnfBootstrap::parse` at runtime, almost certainly introduced by
W0'.a/b/c's source landings on master without a corresponding
generated.rs regen.

The correct next-session move is the bisect (R1) OR the revert-and-
re-tranche (R2/R3). Without that, B2 alone won't close — T3 relocates
the wall but doesn't dissolve it.

## Honest closing assessment

This session burned compute on a wall whose actual root cause was
masked by 4 audits' shared assumption. The audits' architectural
recommendation (T3) is sound; the tactical execution path (B2 alone)
is insufficient because it presumes the IR-pipeline-at-expand-time
attribution. The next session needs the parser regression diagnosed
first (R1) or worked around (R2/R3).

State preserved at master `21881591`. Awaiting user direction on
sequencing.
