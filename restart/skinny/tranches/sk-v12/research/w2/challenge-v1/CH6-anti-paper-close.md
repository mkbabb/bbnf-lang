# SK-V12 W2 CH6 - Anti-Paper-Close

Disposition: ACCEPT.

W2 can honestly be proof-only. The plan does not retire the stale
`CHECKASM-REPORT.md` divergence by prose or by the existing green harness
alone: it requires new direct `escape_mask_64` scalar-reference parity,
caller-level scanner adversarial parity, strict checkasm/corpus evidence, and
only then permits report and REDRESS updates.

No-throughput/no-row admission is recorded honestly. W2 is framed as a
correctness prerequisite, not a row mover. Throughput waiver, parse-only SOTA,
new SIMD primitive admission, x86 work, and orphan admission are blocked.

Reject redress only if it updates the report without new proof cells passing,
claims `primitive-checkasm` alone while the new W2 test is not captured
elsewhere, admits throughput/row movement from W2, or edits runtime scanner
source without owner-path expansion.
