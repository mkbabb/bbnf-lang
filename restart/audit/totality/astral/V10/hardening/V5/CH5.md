# Pass Omega V10 — CH5 HIDDEN-COUPLING — Cycle V5

Lens: CH5 HIDDEN-COUPLING. Does any Ω-C lock amendment imply a parallel substrate
/ renamed sidecar / Lock-1 violation / Track1≡Track2 dishonesty? Does the
single-substrate union hold across every staged delta?

Scope: the 11 staged lock-addendum clauses in `locks-diff.md` (the V10-hardened
consolidation OF converged T-P3 `3C-locks-v+1-diff.md`) + the cross-artefact CRUD
operations (ΩC disposition, ΩD §13.6→SK-V19 re-key + §13.7 stand-up, ΩA
CF-01..CF-12 / CRUD-1..CRUD-6 / OA-V10-01..12, the SCANNER-UNIFY DEFER bundle, the
firewall tense, the retarget clause, the handoff/migration/architecture deltas,
the ΩB/ΩE corpus). Census = 15 amendment/CRUD items (the V4 14 + the
handoff-delta PROVE-row md5-gate, freshly under-scrutiny this cycle).

## V1→V4 carry: all eight prior REVISE/REJECT folded and re-verified RESOLVED at HEAD

The cumulative prior burn is V1 (3 REVISE: firewall tense / dangling
SCANNER-UNIFY owner / free-floating RED self-gate), V2 (1 REJECT byte-identity-to-3C
+ 1 REVISE cursor-on-RED-roster), V3 (1 REVISE movemask-census mis-singularization),
V4 (1 REVISE retarget-clause live-path tense). All eight are folded into the
staged tree and re-verified at HEAD:

- **V1-01 / V4-01 (firewall + retarget tense) RESOLVED.** The relocated-seam
  clause reads "the un-fork is UNBUILT at HEAD (`RuntimeEmitterKind{CompiledLowering,
  RequestFacts}` still live, `runtime_generator.rs:17`-`25` still branches on
  `request.profile_contract.emitter`)." Verified live: `runtime_generator.rs:16`
  matches `request.profile_contract.emitter`, `:17`/`:25` match both variants
  `CompiledLowering`/`RequestFacts`. The retarget clause now carries the V4-forced
  "LIVE-PATH TENSE" marker: "the recursive shell `find_css_significant`
  (`runtime_simd.rs:169`) is, at HEAD, reached ONLY from the `#[cfg(test)] fn
  neon_significant_skip_matches_scalar` guard (`lib.rs:574`) and is ABSENT from
  the generated CSS runtime (`grammars/` rg=0) — DEAD at admission on the live
  path." Verified live: `find_css_significant` defined `runtime_simd.rs:169`, sole
  caller `lib.rs:574` inside the `#[cfg(test)]` guard, `rg -c find_css_significant
  grammars/` exits 1 (zero matches). Both accelerated-leaf tenses now match reality.
- **V1-02 (SCANNER-UNIFY owner) RESOLVED.** The single-substrate clause cites the
  owning `MP-3B-SKV18-D07` and stages the reconcile into master-plan-diff Diff 4
  §24 row (c). Verified: `3B:177` is the `MP.SK19.SCANNER-UNIFY` row ("the
  renamed/parallel-scanner risk is ACTIVE … Decide UNIFY vs renamed-parallel-scanner;
  ≈+217 reconcile + 8/9 OnceCell re-route"); master-plan-diff `:261` row (c) carries
  it verbatim ("decide UNIFY vs renamed-parallel-scanner + 8/9 OnceCell re-route").
  DEFER no longer dangling.
- **V1-03 (RED self-gate) RESOLVED.** The Pattern-H clause carries the CF-09
  in-clause disposition ("do NOT bolt a 9-name regex widen as an SK-V18 patch — the
  R16 PartialEq full-row collapse … are a SK-V19 reconcile (D11b, ≈+217)"). Verified
  live: 9-row grammar-named `idents` table at `strategy.rs:137`-`185`
  (`JsonParser`/`JsonGrammar`, `GoogleSheetsParser`, `CssL4Parser`, `BbnfBootstrap`/
  `BbnfParser`, …); the self-gate is FALSIFIED/RED as disclosed.
- **V2-01 (cursor-on-RED-roster) RESOLVED.** The cursor-generality (b)
  config-breadth re-census is GATED on the Pattern-H RED disposition "until the
  CF-09 / D11b SK-V19 reconcile … lands."
- **V2-02 (byte-identity-to-3C REJECT) RESOLVED.** ΩC `:22`-`25` reads "NOT
  byte-identical … (md5 differs; 6 of 11 clauses changed); git apply --check exits
  0; 11-clause / 9A-11M-0R-1D preserved." ΩA CRUD-3 (`:246`) reads "diff body differs
  from 3C by the V1–V3 hardening deltas." Honest hardened-FROM-3C framing everywhere.
- **V3-01 (movemask census) RESOLVED.** The single-substrate clause cites all FOUR
  non-delegating packs (`byte_class_from_eq_set_64.rs:79`-`87`, `bracket_depth_mask_64.rs:74`,
  `comment_body_mask_64.rs:68`, `match_tiny_plain_string.rs:100`) and states the
  `bbnf_simd_single_mask_convention` co-gate count is ≥4 at G2 entry.

## Spot-Verifications (load-bearing items, re-run at HEAD)

- **`git apply --check` on staged `locks-diff.md`: exit 0** against live
  `restart/locks/LOCKS.md`. Hunk `@@ -622,6 +622,33 @@`; insertion AFTER the SK-V17
  Lock 16 clause (`LOCKS.md:622`) and BEFORE `## v+1 Governance Boundary` (`:625`).
  SK-V15 (`:581`-`607`) / SK-V17 (`:610`-`622`) addenda neither restated nor edited.
  APPLIES CLEANLY.
- **`git apply --check` on `architecture-delta.staged.md`: exit 0.** Gated Hunk 1
  (§7.4 title re-key SK-V15→SK-V18) + Gated Hunk 2 (§9.2 phantom-vehicle strike,
  header `@@ -1997,4 +1997,7 @@`) are byte-exact against live ARCHITECTURE.md.
- **16 numbered locks** at `:75,160,170,179,181,183,200,202,260,269,319,328,336,349,436,453`;
  no Lock 17. **Five `BackendShape`** confirmed in code:
  `skinny/crates/codegen/src/lower/mod.rs:18`-`24` + `ir/src/cost.rs:334`
  (`all_backend_shapes() -> [BackendShape; 5]`). No sixth shape.
- **Both PLANNED co-gate symbols rg=0**: `runtime_target_rows_collapsed` and
  `bbnf_simd_single_mask_convention` return zero in `skinny/crates`+`skinny/xtask` —
  PLANNED, never live. HONEST.
- **§H wave resolved**: master-plan-diff Diff 1 re-keys live §13.6 (`MASTER-PLAN.md:974`,
  the "SK-V18 Tape-Fold … active after SK-V17 skinny W0-W5 + G-Omega" header) →
  §13.6 SK-V19 Totality-Fold (F1-F9 preserved verbatim); Diff 2 stands up a NEW
  §13.7 SK-V18 GENERALIZATION 12-wave block; Diff 4 §24 (`:259`-`262`) re-keys the
  carry ledger + stages the three totality-tree leaks (9-ident, `css_types.rs`,
  `simd-scan`) as SK-V19 receivers. The tranche-identity pivot (certified SK-V18 =
  GENERALIZATION → §13.7; `crates/core/` tape-fold → SK-V19, re-keyed §13.6) is
  CONSISTENT across ΩD `:14`-`15`/`:40`-`41`, master-plan-diff, ΩA CF-01/CF-07, ΩF,
  3B D01/D02/D07. The `crates/core/` fold is correctly NOT the certified SK-V18.
- **REDRESS reference resolved**: the CollapsedStage clause cites `skinny/REDRESS.md:2795`-`2944`
  finding `:2928`-`2933` — the M5-Max scalar-cheaper-than-SIMD-cursor finding
  (`G-W3-UNION-SUBSTRATE` RETIRED, streamed-cursor REDRESS 96/97/98). The clause
  GATES promotion-past-`diagnostic-only` against that RETIRED prior; it does NOT
  revive it. NOT a revived REDRESS route. The retarget clause's REDRESS-53 disposition
  (ΩB `:200`: RETARGET NEON onto the EXISTING in-loop scalar shell = ADMISSIBLE; a
  parser-local second scanner = REJECTED) holds.
- **CollapsedStage cross-leg anchor split verified.** C9 cites `:1289` as PRIMARY
  (demote-stable, NOT in the OA-V10-05 splice set) and `:1206` as SECONDARY (an
  OA-V10-05 splice target). Verified live: `ARCHITECTURE.md:1289` = "CollapsedStage
  = UNKNOWN-2D-05, no admission without a 2E source-backed [strategy]"; `:1206` = the
  x86-pinned ledger row carrying "aarch64 candidate is UNKNOWN-2D-05 (requires 2E
  source-backed aarch64 strategy before any aarch64 admission)" verbatim. The
  architecture-delta Anchor-2 HALT-NOTE preserves that `aarch64 candidate is
  UNKNOWN-2D-05` clause VERBATIM while demoting the x86 co-require wording, so the
  C9 SECONDARY cite is not stranded under the ARCHITECTURE leg.
- **C10 cursor-generality cross-leg anchor verified.** C10 cites `:1990`,`:1997`
  as the §9.2 prose carrier struck EARLIER at CRUD-1 by arch-delta OA-V10-06 Gated
  Hunk 2. Verified live: the "The `G:EventGrammar` type parameter is the generality
  vehicle" sentence resolves at `ARCHITECTURE.md:1997`-`1998` at HEAD; Gated Hunk 2
  (`@@ -1997,4 +1997,7 @@`) strikes it and re-anchors onto the `Cursor` micro-trait +
  config-breadth classifier; the Anchor-3 HALT-NOTE orders a pre-strike re-grep of
  `:1997` and re-anchors `LOCKS.md:620` onto the post-strike §9.2 state WITHOUT
  re-grepping the gone sentence. The C10 cite is the PRE-strike carrier, honestly
  framed. (Minor: C10 cites the sentence-start line `:1997`; the struck load-bearing
  phrase is at `:1998` — the Anchor-3 HALT-NOTE names `:1997`/`:1998` together and
  re-greps the phrase string, so the one-line cite slack is harmless, not a defect.)
- **e-graph regression-guard grounded.** The cursor-generality decision-engine note
  cites `NormalizeDirectSinkCost` "live." Verified: `backend_egraph.rs:75`
  instantiates it, `:191`-`193` define the struct + its `impl Rewrite`. ≥1 asserted
  e-graph rewrite, live.
- **`css_provider_source` field-collision DISCLOSED.** The firewall clause's PLANNED
  predicate `css_provider_source == generated` is flagged "distinct from the live
  `bbnf-bench/src/report.rs` bench-report field of the same name." Verified:
  `report.rs:1168` `pub css_provider_source: String`, used `:1337`/`:1354`. The diff
  DISCLOSES the collision. Not a renamed sidecar.
- **`simd-scan` second classifier substrate**: `crates/simd-scan/src/lib.rs:67`
  exports `pub use alphabet::{KernelShape, NibbleLut, StructuralAlphabet, WideLut}`,
  distinct from the `:68` `{StructuralIndex, next_structural_at_or_after}` probe API
  — verified verbatim. Disclosed, priced (≈+217 + 8/9 OnceCell re-route), DEFERRED
  to SK-V19 via the single-priced `(COH18-015 / D-SKV18-L01 / MP.SK19.SCANNER-UNIFY)`
  bundle owned by `MP-3B-SKV18-D07`, staged into master-plan-diff Diff 4 §24 row (c).
  Skinny `rg=0` NOT claimed as a totality single-substrate proof.

## The fresh V5 defect — handoff-delta Sheets PROVE row treats md5-distinctness as SUFFICIENT, asymmetric with its own sibling P3 row and SPEC:333-334

The fresh under-scrutiny item this cycle is the handoff-delta SK-V18 Blocker Matrix
(`handoff-delta.staged.md:124`-`134`), which the prior cycles touched only as a
prose-handoff item, not at the per-row md5-gate grain. Two rows of the SAME table
use md5-distinctness with OPPOSITE rigor:

- **P3 row** (7 byte-identical css_l4 replicas), `:126`: "`md5 …/{json,css_l4}/generated.rs`
  no-identical-pair **∧ `runtime_target_rows_collapsed == true`** (the md5-distinctness
  half is NECESSARY-not-sufficient, the structural row-collapse co-gate completes it;
  matches master-plan-diff `:171` + `SPEC.md:435`)." COUPLING-HONEST — the bare md5
  is explicitly named insufficient and conjoined with the structural co-gate.
- **PROVE row** (Sheets 24-LOC stub), `:132`: "`sheets_grammar_shape == pratt-operator`;
  **md5-distinct from JSON ∧ CSS**." Here md5-distinctness stands as a standalone PROVE
  proof, with NO "NECESSARY-not-sufficient" marker and NO conjunction with
  `generator_grammar_count == 3` / `emit_shape_source == lowered_program`.

This is the Track1≡Track2-adjacent asymmetry my lens hunts. The artefact suite's own
discipline binds md5-distinctness into a CONJUNCTION everywhere else: SPEC `:333`-`334`
("md5-distinctness is NECESSARY-NOT-SUFFICIENT — the 3-co-gate CONJUNCTION is
{md5-distinct ∧ … ∧ `emit_shape_source==lowered_program`}"), SPEC `:253`-`254` (the
PROVE gate is `sheets_grammar_shape == pratt-operator` ∧ `generator_grammar_count == 3`,
md5 among the co-gates), the neutrality-proof clause in `locks-diff.md` ("md5-distinctness
proves the OUTPUT differs per grammar but NOT that the SELECTION is grammar-derived …
NECESSARY-NOT-SUFFICIENT"), and ΩB `:121` ("the 3-co-gate is a CONJUNCTION, not an md5
check"). A Sheets `generated.rs` can be md5-distinct from JSON/CSS yet still be produced
by a relocated per-grammar branch — the exact insufficiency the relocated-seam firewall
clause names. Letting the PROVE row read bare-md5-distinct as a sufficient generalization
proof contradicts the firewall it is meant to verify.

The coupling MECHANISM upstream is honest — SPEC `:78` PROVE gate carries
`emit_shape_source == lowered_program`, and the binding `N`-fallback ("Sheets cannot
emit via the generator ONLY → generalization NOT real") is the real negative control —
so this is a handoff-row transcription under-statement of the SPEC conjunction, not a
new coupling. CORRECTION (handoff-delta Blocker Matrix, Sheets PROVE row): mark the
Sheets `md5-distinct from JSON ∧ CSS` with the same "NECESSARY-not-sufficient" framing
its sibling P3 row carries, conjoined with `generator_grammar_count == 3` ∧
`emit_shape_source == lowered_program` (per SPEC `:253`-`254`/`:333`-`334`), so the
PROVE row does not read bare-md5-distinctness as a sufficient generalization proof.
REVISE.

## Coupling Resolution (the core lens question)

The single-substrate union does NOT silently hold across the totality tree, and the
amendment is HONEST about every place it breaks — the correct CH5 disposition:

1. **Second classifier substrate** (`simd-scan` `NibbleLut`/`WideLut`): real,
   disclosed, PRICED, DEFERRED to SK-V19 via the single-priced bundle owned by
   `MP-3B-SKV18-D07`, staged into master-plan-diff Diff 4 §24 row (c). Skinny `rg=0`
   NOT claimed as a totality proof. NO coupling dishonesty.
2. **Relocated-seam firewall**: ONE `render(program)` reading shape only from
   `backend_shape`; `RuntimeEmitterKind`-DELETED is the post-G3 gate, verified
   still-live at HEAD (`runtime_generator.rs:16`-`25`). Skinny G2 scans the skinny
   CSS surface; totality `crates/core/src/runtime/css_l4/` is the SK-V19 seam.
   md5-distinctness named NECESSARY-NOT-SUFFICIENT; the `runtime_target_rows_collapsed`
   PartialEq co-gate mandatory. Defends Track1≡Track2 — EXCEPT the handoff-delta PROVE
   row, which is the V5 REVISE.
3. **Lock-14 RED disclosed (OA-V10-10 / CF-09)**: self-gate asserts ZERO, returns 13;
   9-row grammar-named `idents` table live at `strategy.rs:137`-`185`. Clause states
   FALSIFIED/RED and tees the R16 collapse to SK-V19 (CF-09 in-clause). HONEST.
4. **Lock-1 intact; no sixth shape; no retained sidecar**: CollapsedStage is a SHAPE
   SLOT (diagnostic-only); the single-substrate clause re-homes `scan_balanced` into
   the ONE skinny `bbnf-simd` consuming EXISTING kernels + the ONE canonical SHRN
   movemask (`movemask.rs:5`). No public substrate API; transient-single-call retention.
5. **architecture/migration/handoff deltas**: arch-delta = two `git apply`-gated hunks
   (exit 0) + four re-grep-HALT anchored splices, no 6th shape, CollapsedStage stays a
   SHAPE SLOT; migration-delta `:119`-`120` fences "an SK-V16/V17-rejected shape
   (second scanner / structural-stream driver / parser-local cursor) must not re-enter
   G2/G4/G6", `css_types.rs` routes to SK-V19 (`:89`,`:156`); handoff-delta is prose
   with `generator_grammar_count == 3` as a PROVE-exit gate. ΩB/ΩE corpus: REDRESS-53
   disposition preserved (ΩB `:200`), 5-shape/16-lock canon by ADDITION (ΩE `:54`-`55`),
   no parallel substrate, no cross-call retained classifier state (ΩE `:353`).

## Disposition Census (15 items)

| # | Item | Lens verdict |
|---|---|---|
| 1 | Named-primitive (a)-(d) gate clause (LAC-1E-V5-01) | ACCEPT — machine-checked four-conjunct gate (`*_abcd_pass` falsifier arms); no coupling. |
| 2 | Relocated-seam firewall + un-fork clause (LAC-1E-V5-02+2D) | ACCEPT — V1-01 RESOLVED; "UNBUILT at HEAD … PLANNED-at-G3" matches live `runtime_generator.rs:16`-`25`; `css_provider_source` bench-field collision DISCLOSED; md5-distinct NECESSARY-NOT-SUFFICIENT + `runtime_target_rows_collapsed` co-gate. No renamed sidecar. |
| 3 | Neutrality-proof clause (LAC-1E-V5-03+2C) | ACCEPT — forced `balanced_component_scan`→`css_balanced_component_scan` demotion IS the discharge; `scoped non-JSON witness` per live MP.NW6 (`MASTER-PLAN.md:662`); third witness inherits the PROVE falsifier (no presupposed success). |
| 4 | aarch64-ONLY clause (LAC-1E-V5-04+2E) | ACCEPT — x86 = P1 DELETION target; sharpens SK-V17 aarch64-PRIMARY to aarch64-ONLY; FEAT_SVE2 ABSENT honored; no x86 close path. |
| 5 | Verbatim-blob-courier clause (LAC-1E-V5-05) | ACCEPT — `CSS_GENERATED_RS:&str` courier (`runtime_generator.rs:701`) REJECT-as-grammar-driven; round-trip byte-equivalence binding. |
| 6 | Green-by-exclusion precondition clause (LAC-1E-V5-06) | ACCEPT — STRENGTHENS Lock-14 (weak `SKV15_W2_EXTRA_COVERAGE_ROOTS`→strict `GENERIC_SCAN_ROOTS`, extends `FORBIDDEN_GENERIC_TOKENS`); NOT a Lock-14 narrowing. |
| 7 | Single-SIMD-substrate + one-movemask clause (LAC-2F-V3-01) | ACCEPT — V3-01 RESOLVED; cites all FOUR non-delegating packs, co-gate count ≥4 at G2 entry; `simd-scan` second substrate disclosed+deferred (`D-SKV18-L01` → 3C bundle). |
| 8 | Retarget-not-author clause (LAC-2E-V6-02+2F) | ACCEPT — V4-01 RESOLVED; carries the "LIVE-PATH TENSE" marker ("`find_css_significant` reached ONLY from the `#[cfg(test)]` guard `lib.rs:574`, ABSENT from generated `grammars/` — DEAD at admission"; non-`#[cfg(test)]` caller-census admission PLANNED-at-G6); verified live. RETARGET-not-author; grammar-neutral inner kernel; speedup DEFERS to H1. |
| 9 | CollapsedStage shape-slot clause (LAC-2D-V3-04) | ACCEPT — SHAPE SLOT, diagnostic-only; gates promotion against RETIRED REDRESS 96/97/98 (`REDRESS.md:2928`-`2933`); PRIMARY `:1289` (demote-stable) + SECONDARY `:1206` (OA-V10-05 splice target, verbatim-preserve HALT-NOTE) verified. No revived route, no sixth shape. |
| 10 | Cursor-generality re-anchor clause (1A-LOCK1-AMEND-001+2D) | ACCEPT — V2-01 RESOLVED; (b) re-census GATED on Pattern-H RED (CF-09/D11b); e-graph guard grounded (`NormalizeDirectSinkCost` `backend_egraph.rs:75`,`:191`-`193`); `<G>`-strike sound; arch-delta §9.2 phantom strike applies clean; C10 `:1997` carrier verified at HEAD. |
| 11 | Pattern-H re-census clause (LAC-1E-V5-07+2C) | ACCEPT — V1-03 RESOLVED; CF-09 in-clause bounds the RED self-gate; 9-row table live at `strategy.rs:137`-`185`; `css_types.rs` SK-V19-routed (`migration-delta:89`). |
| 12 | ΩC provenance framing + ΩA `:13` / CRUD-3 `:246` | ACCEPT — V2-02 REJECT RESOLVED; "NOT byte-identical (md5 differs; 6 of 11 clauses changed)"; honest hardened-FROM-3C framing. |
| 13 | ΩD §13.6→SK-V19 re-key + new §13.7 + Diff 4 §24 SK-V19 tee-up (MP CRUD) | ACCEPT — tranche-identity pivot CONSISTENT across ΩD/master-plan-diff/ΩA CF-01,CF-07/ΩF/3B; live MP §13.6 header `:974` verified as re-key target; the three totality-tree leaks staged as SK-V19 receivers, not bolted into SK-V18; staged-only. |
| 14 | ΩC disposition matrix (9A/11M/0R/1D, 21 candidates) + ΩA CRUD-1..6 / CF-01..12 | ACCEPT — ZERO silent drops, one disposition per candidate; lone DEFER (LAC-2F-V3-03) is AUDIT-SCOPE with a real `ls`-both-trees re-entry trigger; CF-12 preserves the 16-lock + 5-shape canon BYTE-VERBATIM across every CRUD leg. Not a coupling. |
| 15 | handoff-delta SK-V18 Blocker Matrix — Sheets PROVE row md5-gate (`handoff-delta.staged.md:132`) | **REVISE — DEFECT-CH5-V5-01.** Coupling upstream honest (SPEC `:78` PROVE carries `emit_shape_source==lowered_program`; binding `N`-fallback is the real negative control), BUT the row states `md5-distinct from JSON ∧ CSS` as a STANDALONE PROVE proof — no "NECESSARY-not-sufficient" marker and no conjunction with `generator_grammar_count==3` / `emit_shape_source==lowered_program`, asymmetric with the SAME table's P3 row `:126` (which DOES mark md5 NECESSARY-not-sufficient ∧ `runtime_target_rows_collapsed`) and contradicting SPEC `:333`-`334`/`:253`-`254` + ΩB `:121` (the 3-co-gate is a CONJUNCTION, not an md5 check). CORRECTION: mark the Sheets PROVE md5 NECESSARY-not-sufficient, conjoined with `generator_grammar_count==3` ∧ `emit_shape_source==lowered_program`, so bare md5-distinctness is not read as a sufficient generalization proof. |

## Notes for Orchestrator (out-of-lens)

- Live surfaces (`MASTER-PLAN.md`, `LOCKS.md`, `MIGRATION.md`, `HANDOFF.md`,
  `ARCHITECTURE.md`) are clean at HEAD — the staged-only invariant is honored across
  all six Ω artefacts + the four `*.staged.md` deltas + the two `*-diff.md` files.
  `git apply --check` exits 0 on both the locks-diff and the architecture-delta.
- The CH5-DEFECT-V1-02 / -V1-03 SCOPE-NOTE markers inside the diff are PRIOR-cycle
  defect IDs carried as self-documenting scope notes; legitimate annotations, not
  forward-references. Out-of-lens.
- The C10 `:1997`-vs-`:1998` sentence-line slack (cite names the sentence-start line,
  struck phrase is one line down) is harmless — the Anchor-3 HALT-NOTE names both lines
  and re-greps the phrase string, not a line number. Not a defect; logged for completeness.

## Verdict

The substrate-union story is COUPLING-HONEST across every staged delta: every place the
union does NOT hold on the totality tree (the `simd-scan` second `NibbleLut`/`WideLut`,
the unbuilt un-fork, the RED Lock-14 self-gate, the test-only-reachable retarget leaf)
is DISCLOSED and DEFERRED to SK-V19 — priced, owned by `MP-3B-SKV18-D07`, staged into
master-plan-diff Diff 4 §24 — never claimed green and never bolted into an SK-V18 gate.
`git apply --check` exits 0 on both the locks-diff and the architecture-delta; 16 locks;
five `BackendShape`; both co-gate symbols rg=0. No revived REDRESS route (CollapsedStage
GATES against the RETIRED streamed-cursor finding; the `:1206` anchor is verbatim-preserved
by the arch-delta HALT-NOTE), no non-applying diff, no Lock-14 NARROWING (green-by-exclusion
TIGHTENS it), no Lock-1 violation, no sixth shape, no public substrate API, no retained
sidecar.

All eight prior-cycle defects (V1 firewall tense / SCANNER-UNIFY owner / free-floating RED,
V2 byte-identity REJECT / cursor-on-RED-roster, V3 movemask-census, V4 retarget live-path
tense) are folded and re-verified RESOLVED at HEAD. The lone fresh V5 defect (item 15) is
the Track1≡Track2-adjacent residue this lens exists to find: the handoff-delta Sheets PROVE
row treats md5-distinctness as a SUFFICIENT generalization proof, asymmetric with its own
sibling P3 row and contradicting the artefact suite's NECESSARY-NOT-SUFFICIENT discipline
(SPEC `:333`-`334`, ΩB `:121`, the neutrality-proof clause). The coupling MECHANISM is
honest upstream (SPEC PROVE conjunction + binding-`N` negative control); the handoff ROW
under-states the conjunction. 1 REVISE / 15 = 7% — BELOW the ≥30% cycle-V1 floor, which is
CORRECT and EXPECTED at cycle V5: V1 burned the three load-bearing firewall/substrate/self-gate
REVISEs, V2 the byte-identity REJECT + cursor-on-RED-roster, V3 the movemask census, V4 the
retarget tense; all eight folded into the staged tree, so V5 finds only the second-order
md5-sufficiency residue on the one handoff row not yet examined at per-gate grain — the
symmetric sibling of the already-burned md5/co-gate discipline applied one row over — not
under-scrutiny.

TALLY accept=14 revise=1 reject=0
