# S-P2 V3 CHALLENGE — CH3 REGRESSION (REDRESS) Confirming-Cycle Lens

Authored: 2026-05-23 (post-S-P2 V3 atomic micro-fold commit `ebe84954b`,
"docs(sk-v14-p2-V3): atomic micro-fold (P2-F verb-tense 2 cells) + V3
dispatch context"). Six artefacts under review at V3 HEAD: p2a (367
lines; V2-LOCKED, V1-original; zero V3 edits), p2b (217; V2-LOCKED,
V2-amended; zero V3 edits), p2c (164; V2-LOCKED, V2-amended; zero V3
edits), p2d (254; V2-LOCKED, V2-amended; zero V3 edits), p2e (342;
V2-LOCKED, V1-original; zero V3 edits), p2f (360; V3-amended with 2
verb-tense cells at §2.10 line 164 + §2.13 line 197). Authorities re-
read end-to-end: `restart/prompts/skinny/PASS-2-RESEARCH.md` §3 CH3
(specialised to S-P2), `restart/prompts/ORCHESTRATOR.md` §3W + §3Z (≥
95% × 2 consecutive cycles),
`restart/skinny/tranches/sk-v14/research/p2/hardening/V3/CHALLENGE-CONTEXT.md`
§0–§4, `restart/skinny/tranches/sk-v14/research/p2/hardening/V2/CH3.md`
(V2 CH3 100% ACCEPT + V1→V2 2-cycle LOCK + 3 REDRESS-strengthened
families authority), and the V2→V3 diff via
`git diff 4c70b6f19..ebe84954b` against all six axis files plus the V2
consolidated.

CH3 binding restated (PASS-2-RESEARCH §3 CH3): no candidate re-opens
REDRESS routes (watch-list per V1 dispatch §2 + V2 dispatch §2 + V3
dispatch §2 unchanged across all three cycles): **28+33**, **50–55**,
**60–72**, **80**, **82–84**, **88**, **89**, **96/97/98**, **119/120**,
**126**.

V3 disposition focus (per V3 dispatch context §2 CH3 bullet):
1. No V3 edit re-opens REDRESS routes (28+33, 50-55, 60-72, 80, 82-84,
   88, 89, 96/97/98, 119/120, 126).
2. Three V2-strengthened REDRESS families (126 via C-P2C-7 demotion;
   88 via NF-CH6-3 C2 scalar-ref evidence upgrade; 60-72 via NF-CH6-4
   cross-axis tracking note) carry through V3 unchanged.
3. V2-LOCKED axes (P2-A + P2-B + P2-C + P2-D + P2-E): confirm zero V3
   drift via diff against V2 commit `4c70b6f19`.
4. V3-amended P2-F: 2 verb-tense cells at §2.10 + §2.13 are CH1/CH4
   convergent polish (F-V2-CH1-1 + F-V2-CH4-1 discharge); no CH3
   surface change — re-verify against the per-cell REDRESS-regression
   audit V2 CH3 §2.4 performed.

## §1 — V2 → V3 drift audit (V2-LOCKED axes)

Per V3 dispatch context special-attention clause: V3 is verification-
only for 5 of 7 axes (P2-A/B/C/D/E). The diff command
`git diff 4c70b6f19..ebe84954b -- <axis>` was executed against each of
the five V2-LOCKED axis files; in all five cases the output is empty
(zero drift).

| Axis | V2 lines | V3 lines | V2→V3 diff size | Drift verdict |
|---|---:|---:|---|---|
| p2a-sota-teardown | 367 | 367 | `git diff` returns empty for this file | **ZERO drift confirmed** |
| p2b-dav1d-process | 217 | 217 | `git diff` returns empty for this file | **ZERO drift confirmed** |
| p2c-arch-esoterica | 164 | 164 | `git diff` returns empty for this file | **ZERO drift confirmed** |
| p2d-substrate-tape | 254 | 254 | `git diff` returns empty for this file | **ZERO drift confirmed** |
| p2e-parse-that-gaps | 342 | 342 | `git diff` returns empty for this file | **ZERO drift confirmed** |

All five V2-LOCKED axes are byte-identical at V3 HEAD. CH3 carry-
forward from V2 (V2 CH3 §2.1 p2b SHA-pinning ACCEPT; §2.2 p2c 8→5
active + 3 demoted + REDRESS-126 strengthened; §2.3 p2d C-P2D-3 §1.6(d)
demotion + Lock 1 v+1 preservation; §1 p2a + p2e V1-LOCKED zero drift)
applies verbatim at V3. The V1 carry-forwards on p2a (§2.1 7-candidate
disposition) and p2e (§2.5 8-gap disposition incl. Gap 5 REDRESS-80
ACCEPT-WITH-NOTE) flow through both V2 and V3 unchanged. **F-V2-CH3-1
through F-V2-CH3-5** (V2 CH3 §3) carry through V3 unchanged.

## §2 — V3 amended axis CH3 audit (P2-F only)

### §2.1 — p2f-grammar-neutral (V3 amended; 2 verb-tense cells)

V3 diff (verified via `git diff 4c70b6f19..ebe84954b -- p2f-grammar-
neutral.md`): two single-line edits at §2.10 (C10 line 164) and §2.13
(C13 line 197). Both edits change the trailing-clause verb-tense:

| Cell | V2 trailing clause | V3 trailing clause |
|---|---|---|
| §2.10 C10 line 164 | "Authoring landed as Fold-4 V2 deliverable per HARDENING-S-P2-V1-CONSOLIDATED §3.4." | "Authoring queued for S-P3 same-wave Lock 16 same-commit admission per HARDENING-S-P2-V1-CONSOLIDATED §3.4 Fold-4; function body lands same-commit with SIMD body at S-P3." |
| §2.13 C13 line 197 | "Authoring landed as Fold-5 V2 deliverable per HARDENING-S-P2-V1-CONSOLIDATED §3.4." | "Authoring queued for S-P3 same-wave Lock 16 same-commit admission per HARDENING-S-P2-V1-CONSOLIDATED §3.4 Fold-5; function body lands same-commit with SIMD body at S-P3." |

The remainder of each §2.10 and §2.13 cell is byte-identical to V2:
- §2.10 lines 160-163 (heading + P1 antecedent + shape) preserved
- §2.10 line 164 lead clause "Scalar-ref status: required (the scalar
  reference is trivially the byte-by-byte loop with no chunk boundary;
  the SIMD form is the candidate). **Scalar-reference target
  path:line** (Stage-A authoring under same-wave Lock 16 same-commit
  discipline): `crates/bbnf-simd/src/scalar/byte_context_64.rs` —
  `byte_context_64_scalar(...)` ... (sibling of existing
  `crates/bbnf-simd/src/scalar/byte_class_from_eq_set_64.rs:1` shape)."
  preserved
- §2.10 lines 165-169 (Arch / P1 antecedent / Source anchor / Lock 16
  manifest tie / Substrate target) byte-identical
- §2.13 lines 193-196 (heading + P1 antecedent + shape) preserved
- §2.13 line 197 lead clause "Scalar-ref status: required (scalar
  reference is the trivial 2-op form `(a & !b) ^ c`).
  **Scalar-reference target path:line** ...
  `crates/bbnf-simd/src/scalar/bcax_64.rs` —
  `bcax_64_scalar(...)` ... (sibling of existing
  `crates/bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:1` shape)."
  preserved
- §2.13 lines 198-202 (Arch / P1 antecedent / Source anchor / Lock 16
  manifest tie / Substrate target) byte-identical

**Per-V3-edit REDRESS-regression audit:**

| V3 edit | Candidate touched | V1 / V2 REDRESS adjacency | V3 impact | Re-open risk? |
|---|---|---|---|---|
| §2.10 C10 verb-tense (Fold-4 wording) | C10 cross-chunk byte-context | none directly (Lock 16 :285 admits abstract primitive verbatim); V1 CH3 §2.6 + V2 CH3 §2.4 Fold-4: n/a, accepted, CH4-flagged; V2 CH4 reads C10 scalar-ref as a Stage-A authoring deliverable, not as a yet-existing body | Verb-tense correction: V2 wording "Authoring landed as Fold-4 V2 deliverable" was process-history asymmetric (the V2 fold *named the target path:line*; it did not author the body — the body lands at S-P3 same-commit with the SIMD body per Lock 16 same-commit discipline). V3 wording disambiguates: target path:line NAMED at V2, function body QUEUED for S-P3 same-commit admission. | **NO** — verb-tense polish on the same scalar-ref status sentence. The scalar-ref target path `crates/bbnf-simd/src/scalar/byte_context_64.rs` preserved; the substrate-target `local_temp_only` preserved; the Lock 16 :285 binding preserved; no REDRESS family is implicated. The §3.4 "same-commit-with-SIMD-body" discipline is named verbatim by both V2 and V3; V3 makes the timing of body landing (S-P3 wave, not V2 fold) explicit. |
| §2.13 C13 verb-tense (Fold-5 wording) | C13 BCAX 3-way XOR | none (Lock 16 :289 admits BCAX/EOR3 abstractly); V1 CH3 §2.6 + V2 CH3 §2.4 Fold-5: n/a, accepted, CH4-flagged; same shape as C10 | Verb-tense correction: same as §2.10 (V2 said "landed as Fold-5 V2 deliverable"; V3 says "queued for S-P3 same-wave Lock 16 same-commit admission"). | **NO** — same shape as §2.10 C10. Scalar-ref target path `crates/bbnf-simd/src/scalar/bcax_64.rs` preserved; substrate-target `local_temp_only` preserved; Lock 16 :289 binding preserved; no REDRESS family touched. |

**Cross-validation with V2 CH3 §2.4 F-2 (Fold-4 + Fold-5 scalar-ref
targets are REDRESS-neutral Layer-0 sibling shapes):** the V2 CH3 §3
F-2 finding documented that the scalar-ref targets `crates/bbnf-simd/
src/scalar/byte_context_64.rs` and `crates/bbnf-simd/src/scalar/bcax_
64.rs` did NOT yet exist at V2 HEAD ("both are Stage-A authoring
deliverables per the V2 hardening consolidated §3.4 same-commit-with-
SIMD-body discipline"). This finding made the V2 verb-tense wording
("Authoring landed as Fold-N V2 deliverable") inconsistent with the
V2 file-system state: the target paths were NAMED in V2 but the bodies
did NOT yet exist. V3 verb-tense polish reconciles the cell-level
wording with the V2 hardening §3.4 binding and the F-2 finding. **The
V3 polish is the verbal expression of what V2 CH3 F-2 already documented
factually; it is not a new REDRESS surface change.**

**Re-verified V3 HEAD file-system state** (same as V2 HEAD):
- `skinny/crates/bbnf-simd/src/scalar/byte_context_64.rs` — **NOT YET
  EXISTS** (Fold-4 Stage-A authoring deliverable; same-commit with
  SIMD body per V3 verb-tense fix "function body lands same-commit
  with SIMD body at S-P3")
- `skinny/crates/bbnf-simd/src/scalar/bcax_64.rs` — **NOT YET EXISTS**
  (Fold-5 Stage-A authoring deliverable; same shape)
- `skinny/crates/bbnf-simd/src/aarch64/string_block.rs:31` →
  `scan_string_special_block_scalar` **CONFIRMED** (NF-CH6-3 C2 Gap 6
  composition root — V2 CH3 F-1 path:line verification carries through
  V3)
- `skinny/crates/bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:2` →
  `bitmap_prefix_xor_64_scalar` **CONFIRMED**
- `skinny/crates/bbnf-simd/src/lib.rs:175` → `escape_mask_64`
  **CONFIRMED**
- `skinny/crates/runtime/src/grammars/json/scan.rs:32` →
  `scan_structurals_scalar` **CONFIRMED** (per-byte `is_member` /
  `match` inner at lines 68-72; C12 CF-1 reframe + C1/C2 Gap 6
  composition references unchanged)

**CH3 disposition: ACCEPT.** Two verb-tense cells; zero REDRESS routes
re-opened. The V3 polish reconciles cell-level wording with V2 §3.4
"same-commit-with-SIMD-body" binding and with the V2 CH3 F-2 finding
that the scalar-ref target files do not yet exist at V2 / V3 HEAD;
no REDRESS family is implicated.

## §3 — Three V2-strengthened REDRESS families carry through V3 (focus item 2)

V3 dispatch context §2 CH3 explicitly names the carry-through obligation
for the three V2-strengthened REDRESS families. Per-family verification:

### §3.1 — REDRESS 126 (PEXT mask plan; aarch64 has no PEXT) — V2 STRENGTHENED carries through V3

V2 strengthening path: C-P2C-7 (`byte_context_orphan_resolution`)
demotion to §2.X non-candidate inventory was the canonical REDRESS-126
close-hygiene path per V1 CH3 F-4 ("may wire OR delete/demote with
REDRESS evidence"). V2 CH3 F-3 verified the strengthening: "the orphan
inventory ceiling is enforced more strictly at V2 than at V1."

V3 carry-through verification:
- p2c-arch-esoterica.md V3 = V2 byte-identical (ZERO drift per §1
  table); §2.X non-candidate inventory at p2c:65-67 preserves V1
  disposition + V2 demotion stamp verbatim.
- p2c:71 C-P2C-7 row preserves "**`NOT-S-P3-ELIGIBLE` at SK-V14 V1** as
  a standalone wave. Close-hygiene only; may be folded into C-P2C-5
  consumer or deletion / demotion evidence under a hygiene wave.
  **Demoted V2: zero S-P1 hot-leaf antecedent at SK-V14; re-evaluate
  if F-V2-P1ABC-RERECORD surfaces antecedent.**" verbatim.
- p2c:98 §4 REDRESS-126 enumeration row "five aarch64 orphans were
  demoted with evidence. SK-V14 may wire or delete / demote, but cannot
  add or retain another orphan. `cache_hints` has no SK-V14 P1 hot-
  leaf antecedent and remains non-selectable." preserved verbatim.
- p2f-grammar-neutral.md V3 §4 REDRESS-126 row at p2f:295 (C9 x86_64
  BMI2 form pre-block) preserved verbatim (the V3 verb-tense edits
  are at §2.10 line 164 + §2.13 line 197; §4 starts at p2f:286).
- p2c §4 11-REDRESS-family enumeration at p2c:86-99 byte-identical to
  V2.

**V3 verdict: REDRESS-126 V2 strengthening carries through V3 unchanged.**

### §3.2 — REDRESS 88 (PMULL prefix-XOR as hot body) — V2 STRENGTHENED carries through V3

V2 strengthening path: P2-F NF-CH6-3 C2 scalar-ref evidence upgrade
from "required" to "EXISTS via composition per P2-E Gap 6" naming
three composing scalar functions. V2 CH3 F-1 (load-bearing) verified
the strengthening: "the C2 upgrade strengthens scalar-ref evidence
(REDRESS-88 binding requires 'fresh material differential'; having a
scalar oracle that composes Gap 6 + escape_mask_64 + bitmap_prefix_
xor_64 IS material evidence the differential is structurally grounded,
not aspirational). The upgrade does NOT re-introduce PMULL-hot-body
framing; the §4 row 'must cite REDRESS 88's exact prior failure mode
and prove the split-lane framing avoids it; checkasm-parity must hold
for both lanes against the C1 baseline' is unchanged."

V3 carry-through verification:
- p2f-grammar-neutral.md V3 §2.2 C2 scalar-ref status row at p2f:81
  (the NF-CH6-3 upgrade cell) preserved verbatim (V3 verb-tense
  edits are at §2.10 line 164 + §2.13 line 197; §2.2 is at p2f:78-88).
- p2f:81 preserves "scalar oracle EXISTS via composition per P2-E
  Gap 6 (`p2e-parse-that-gaps.md:177`): compose
  `scan_string_special_block_sweep_64` (P2-E Gap 1's scalar in
  `bbnf-simd::aarch64::string_block`) with `bitmap_prefix_xor_64_
  scalar` at `crates/bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:1`
  + the `escape_mask_64` body at `crates/bbnf-simd/src/lib.rs:
  175-206` for the even/odd backslash carry — bit-identical to the
  simdjson `prev_in_string` carry shape" verbatim.
- p2f:292 §4 REDRESS-88 row for C2 PMULL prefix-XOR — "Lock 16 :294
  AVX-512 VPCLMULQDQ at 512-bit lane (NEW 2026-05-12) admits the 4×-
  width form; framing is 'split structural classifier into ordinary +
  quote-aware lanes' not 'use PMULL wholesale as hot body' | S-P3
  must cite REDRESS 88's exact prior failure mode and prove the split-
  lane framing avoids it; checkasm-parity must hold for both lanes
  against the C1 baseline." preserved verbatim.
- p2c:88 §4 REDRESS-88 row "PMULL as the default `bitmap_prefix_
  xor_64` body is not admissible for escape-heavy and narrow parse-
  only JSON rows on this host." preserved verbatim.
- p2c-arch-esoterica.md V3 = V2 byte-identical (ZERO drift); C-P2C-2's
  PRE-BLOCKED-by-REDRESS-88+89+96-98 disposition preserved.
- All three scalar-ref composition path:line claims re-verified at
  V3 HEAD (§2.1 of this report).

**V3 verdict: REDRESS-88 V2 strengthening carries through V3 unchanged.**

### §3.3 — REDRESS 60-72 (SK-V6 retained-parse + sidecar producers + digest cap-16) — V2 STRENGTHENED carries through V3

V2 strengthening path: P2-F NF-CH6-4 cross-axis tracking note §2.Y
explicitly binds S-P3 to single-canonical-primitive admission,
preventing the three-orthogonal-SIMD-bodies recurrence shape (which is
exactly the REDRESS 60-72 sidecar-producer fake-pattern incarnation
where SK-V6 admitted multiple sidecar consumers for one classifier
output). V2 CH3 F-5 verified: "NF-CH6-4 is a meta-binding for S-P3
admission discipline that explicitly prevents the REDRESS 60-72-
shaped recurrence; the note exists in P2-F's §2.Y, not in any
candidate's §2 row, and so does not re-open any REDRESS family."

V3 carry-through verification:
- p2f-grammar-neutral.md V3 §2.Y cross-axis tracking note at p2f:
  231-239 preserved verbatim (V3 verb-tense edits are at §2.10 line
  164 + §2.13 line 197; §2.Y is at p2f:231-239 which is well outside
  the edit lines).
- p2f:239 preserves "**S-P3 consolidator binding:** the C2/Gap1/C1+C2
  alignment is the same primitive under three names; S-P3 must
  produce ONE canonical primitive name + ONE canonical scalar
  reference function rather than admitting three near-duplicates."
  verbatim.
- p2f:297 §4 REDRESS 60-72 row for C1 + C2 + C4 + C9 combined "None
  of the candidates retain a per-parse sidecar; all are local-loop
  or existing-tape-only per the substrate-target column in §2 | S-P3
  must verify substrate-target manifest per Lock 1 v+1
  (`restart/locks/LOCKS.md:76-82`) for every shortlisted candidate;
  any retained mask/list/cursor REJECTs the candidate." preserved
  verbatim.
- p2c:93 §4 REDRESS 60-72 row "NO sidecar event vector, parser-owned
  cursor, or decoded-string stats sink. C-P2C-2 and C-P2C-5 must
  preserve Lock 1 and generated-grammar ownership." preserved
  verbatim.
- p2c-arch-esoterica.md V3 = V2 byte-identical (ZERO drift).
- Cross-references in §2.Y to p2a C2 + p2e Gap 1 + Gap 6 are V2-LOCKED
  axes confirmed ZERO drift (per §1 table).

**V3 verdict: REDRESS-60-72 V2 strengthening carries through V3 unchanged.**

## §4 — Critical findings (CH3 lens, V3-new)

### F-V3-CH3-1 — V3 verb-tense polish reconciles V2 wording with V2 §3.4 + V2 CH3 F-2 (no new CH3 surface)

The V3 verb-tense polish on §2.10 line 164 + §2.13 line 197 reconciles
two pieces of V2 truth that were verbally asymmetric:

1. V2 hardening consolidated §3.4 binds the Fold-4 + Fold-5 scalar-
   reference authoring under "Lock 16 same-commit discipline" — i.e.
   the scalar-ref function body must land in the SAME commit as the
   corresponding SIMD body.
2. V2 CH3 §3 F-2 verified that the scalar-ref target files at
   `crates/bbnf-simd/src/scalar/byte_context_64.rs` and `crates/bbnf-
   simd/src/scalar/bcax_64.rs` **did NOT yet exist at V2 HEAD** and
   are "Stage-A authoring deliverables per the V2 hardening
   consolidated §3.4 same-commit-with-SIMD-body discipline."

The V2 cell-level wording at §2.10 + §2.13 said "Authoring landed as
Fold-N V2 deliverable", which read as if the bodies had landed at V2.
The V2 fold ACTUALLY named the target path:line (which is the V2
deliverable proper); body landing is queued for S-P3 same-wave Lock 16
same-commit admission. The V3 verb-tense fix says exactly this.

**REDRESS-regression check:** the V3 polish does NOT change any of:
- Scalar-reference target path (`crates/bbnf-simd/src/scalar/byte_
  context_64.rs` for C10; `crates/bbnf-simd/src/scalar/bcax_64.rs`
  for C13).
- Substrate-target column (`local_temp_only` for both C10 and C13).
- Lock 16 manifest tie (:285 for C10; :289 for C13).
- Source-anchor row (N/A new primitive for both; scalar-reference
  target named per Fold-N).
- The §4 risks section §304-305 wording "DISCHARGED V2 via Fold-N
  Stage-A scalar-reference authoring" (the risks are discharged via
  the V2 NAMING of the target path:line per §3.4 same-commit
  discipline; the body landing is the S-P3 admission step, not the
  V2 discharge step — V2 + V3 wording consistent here).

**CH3 disposition: ACCEPT.** The polish is process-truth disambiguation
at the cell level; no REDRESS family is touched.

### F-V3-CH3-2 — V2 CH3 F-1 (NF-CH6-3 C2 REDRESS-88 split-lane preservation) carries through V3 unchanged

V2 CH3 F-1 was the load-bearing audit that NF-CH6-3 C2 scalar-ref
upgrade preserves REDRESS-88 split-lane differential. The V3 verb-
tense polish on §2.10 + §2.13 is on C10 and C13, NOT on §2.2 C2 (the
NF-CH6-3 cell). The §2.2 C2 cell at p2f:78-88 is byte-identical V2→V3.

V3 re-verification of the three scalar-ref composition path:line claims:
- `skinny/crates/bbnf-simd/src/aarch64/string_block.rs:31` →
  `pub fn scan_string_special_block_scalar(` — **CONFIRMED at V3 HEAD**
- `skinny/crates/bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:2` →
  `pub fn bitmap_prefix_xor_64_scalar(mut mask: u64, carry_in: bool)
  -> u64 {` — **CONFIRMED at V3 HEAD**
- `skinny/crates/bbnf-simd/src/lib.rs:175` →
  `pub fn escape_mask_64(bs_mask: u64, bs_carry_in: bool) -> (u64,
  bool) {` — **CONFIRMED at V3 HEAD**

The §4 REDRESS-88 row at p2f:292 (C2 PMULL prefix-XOR split-lane
framing differential + S-P3 verification clause) is byte-identical
V2→V3. The Lock 16 :294 AVX-512 VPCLMULQDQ NEW 2026-05-12 admission
binding is unchanged.

**CH3 disposition: ACCEPT.** V2 CH3 F-1 finding carries through V3
verbatim; REDRESS-88 split-lane framing differential preserved.

### F-V3-CH3-3 — V2 CH3 F-2 + F-3 + F-4 + F-5 all carry through V3 unchanged

The remaining four V2 CH3 §3 findings carry through V3 verbatim:

- **F-V2-CH3-2** (Fold-4 + Fold-5 scalar-ref targets are REDRESS-
  neutral Layer-0 sibling shapes): the V3 verb-tense polish does NOT
  change the target paths or the Layer-0 sibling status. The V2
  finding that "neither file exists at HEAD yet; both are Stage-A
  authoring deliverables per the V2 hardening consolidated §3.4
  same-commit-with-SIMD-body discipline" is precisely what the V3
  verb-tense polish makes verbally explicit at the cell level (§2.10
  + §2.13). **F-V2-CH3-2 carries through V3 unchanged; the V3 polish
  is the cell-level mirror of F-V2-CH3-2's hardening-doc-level
  finding.**
- **F-V2-CH3-3** (P2-C C-P2C-7 demotion is the canonical REDRESS-126
  close-hygiene path): P2-C V3 = V2 byte-identical. **F-V2-CH3-3
  carries through V3 unchanged.**
- **F-V2-CH3-4** (P2-D §1.6(d) substrate-side demotion preserves
  Lock 1 v+1 manifest): P2-D V3 = V2 byte-identical. **F-V2-CH3-4
  carries through V3 unchanged.**
- **F-V2-CH3-5** (NF-CH6-4 cross-axis tracking note is anti-REDRESS-
  recurrence by construction): p2f §2.Y at p2f:231-239 byte-
  identical V2→V3 (outside V3 edit lines 164 + 197). **F-V2-CH3-5
  carries through V3 unchanged.**

## §5 — Demoted candidates remain pre-blocked (V3 carry-through)

V2 dispatch context §2 named 5 demoted candidates (P2-C C-P2C-1, P2-C
C-P2C-6, P2-C C-P2C-7, P2-D C-P2D-3, P2-F C8); V2 CH3 §5 verified all
5 remain pre-blocked at V2 with re-admission gates documented. V3
verification:

| Demoted candidate | Demotion location | V2 disposition preserved at V3? | Re-admission gate at V3? | Silently re-opens? |
|---|---|---|---|---|
| **P2-C C-P2C-1** `ascii_set_member64_css_delimiter` | `p2c:65` §2.X | YES (P2-C V3 = V2 byte-identical) | YES (F-V2-P1ABC-RERECORD antecedent gate) | NO |
| **P2-C C-P2C-6** `eor3_string_mask_fusion` | `p2c:66` §2.X | YES (P2-C V3 = V2 byte-identical) | YES (F-V2-P1ABC-RERECORD antecedent gate; CH6 paper-close risk discharged via demotion) | NO |
| **P2-C C-P2C-7** `byte_context_orphan_resolution` | `p2c:67` §2.X + p2c:71 row | YES (P2-C V3 = V2 byte-identical) | YES (F-V2-P1ABC-RERECORD antecedent gate + REDRESS-126 close-hygiene) | NO |
| **P2-D C-P2D-3** sparse-flag-band gating | `p2d:104-105, :128-130` §1.6(d) + gap-note | YES (P2-D V3 = V2 byte-identical) | YES (same-wave consumer gate) | NO |
| **P2-F C8** comment-skip primitive | `p2f:218-229` §2.X.1 | YES (P2-F §2.X.1 unchanged by V3 verb-tense edits at §2.10 + §2.13; lines 218-229 byte-identical V2→V3) | YES (§2.X.1 re-promotion gate at p2f:229: fresh-material differential or same-wave consumer commit) | NO |

**V3 demotion-as-pre-block-strengthening verdict:** all 5 demoted
candidates have their V1 + V2 dispositions preserved verbatim at V3.
None of the 5 demoted candidates is admitted at V3; all 5 remain
explicitly non-S-P3-shortlist-eligible at V3 with documented re-
admission gates. **Zero silent REDRESS re-openings at V3.**

## §6 — P2-B 5-stage gate REDRESS-pre-block-safe by construction (V3 re-verification)

V2 CH3 §4 verified the P2-B claim "the 5-stage admission gate is
structurally REDRESS-pre-block-safe by construction" against the 5+8+
7+13+2 = 35 active candidate enumeration at V2. V3 verification:

- P2-B V3 = V2 byte-identical (ZERO drift; the 5-stage gate text at
  p2b is unchanged).
- P2-F active candidate count at V3: 13 active (unchanged from V2 —
  the verb-tense edits do NOT touch the active candidate enumeration,
  they polish trailing-clause wording in two cells of the 13 active
  rows). §3 verdict-tally at p2f:266-285 is byte-identical V2→V3
  (verified by inspecting the file at those lines; lines 164 + 197
  are the only V3 edits per `git diff --stat`).
- P2-A + P2-C + P2-D + P2-E active counts unchanged (all V2-LOCKED).
- Total V3 active candidate surface: **35 active** (same as V2).
- Total V3 demoted-to-inventory entries: 5 (same as V2).
- Total V3 pre-blocked-anti-pattern entries: 1 (C-P2D-4, same as V2).

**V3 REDRESS-family coverage matrix** (re-deriving V1 + V2 CH3 §4
matrix at V3 HEAD; only rows that differ from V2 are shown):

| REDRESS family | V2 coverage | V3 changes | V3 coverage |
|---|---|---|---|
| **all 10 REDRESS families** (28+33, 50-55, 60-72, 80, 82-84, 88, 89, 96/97/98, 119/120, 126) | per V2 CH3 §4 table (3 STRENGTHENED at V2: 126, 88, 60-72; 7 unchanged) | no V3 edit touches any REDRESS family enumeration row; V3 verb-tense polish on §2.10 + §2.13 is cell-level wording reconciliation, not a §4 REDRESS-table edit | **unchanged from V2** (3 STRENGTHENED carry through V3; 7 unchanged) |

**Verdict on P2-B claim at V3**: the 5-stage admission gate is **still
structurally REDRESS-pre-block-safe by construction at V3 HEAD**. The
three V2-STRENGTHENED REDRESS families (126, 88, 60-72) carry through
V3 unchanged. All other REDRESS-family coverage rows unchanged from V2
+ V1. The Lock 1 v+1 substrate-manifest enforcement at Stage E
unchanged. The strict-row-movement clause at Stage D unchanged. **No
coverage gap surfaces at V3.**

## §7 — Sources verified (executable-verification mandate)

V3 HEAD verifications (`git diff` + `git diff --stat` + `wc -l` +
`grep -n` per `[read-size-preflight]`):

- `restart/skinny/tranches/sk-v14/research/p2/p2a-sota-teardown.md` —
  367 lines; `git diff 4c70b6f19 ebe84954b -- $file` returns empty
  (V2-LOCKED zero drift confirmed; V1 carry-forward via V2 still
  applies).
- `restart/skinny/tranches/sk-v14/research/p2/p2b-dav1d-process.md` —
  217 lines; `git diff 4c70b6f19 ebe84954b -- $file` returns empty
  (V2-LOCKED zero drift confirmed).
- `restart/skinny/tranches/sk-v14/research/p2/p2c-arch-esoterica.md` —
  164 lines; `git diff 4c70b6f19 ebe84954b -- $file` returns empty
  (V2-LOCKED zero drift confirmed).
- `restart/skinny/tranches/sk-v14/research/p2/p2d-substrate-tape.md` —
  254 lines; `git diff 4c70b6f19 ebe84954b -- $file` returns empty
  (V2-LOCKED zero drift confirmed).
- `restart/skinny/tranches/sk-v14/research/p2/p2e-parse-that-gaps.md` —
  342 lines; `git diff 4c70b6f19 ebe84954b -- $file` returns empty
  (V2-LOCKED zero drift confirmed; V1 carry-forward via V2 still
  applies).
- `restart/skinny/tranches/sk-v14/research/p2/p2f-grammar-neutral.md` —
  360 lines; V2→V3 diff: 2 single-line edits at lines 164 (§2.10 C10)
  + 197 (§2.13 C13), both trailing-clause verb-tense polish. Lines
  78-88 (§2.2 C2 NF-CH6-3 cell), 218-229 (§2.X.1 C8 demoted cell),
  231-239 (§2.Y NF-CH6-4 cross-axis tracking), 266-285 (§3 verdict
  tally), 286-307 (§4 risks) all byte-identical V2→V3.

**Scalar-reference path:line verification at V3 HEAD (NF-CH6-3 + Fold-
4/Fold-5 + C12 CF-1 re-verified):**
- `skinny/crates/bbnf-simd/src/aarch64/string_block.rs:31` →
  `scan_string_special_block_scalar` **CONFIRMED at V3 HEAD**
- `skinny/crates/bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:2` →
  `bitmap_prefix_xor_64_scalar` **CONFIRMED at V3 HEAD**
- `skinny/crates/bbnf-simd/src/lib.rs:175` → `escape_mask_64`
  **CONFIRMED at V3 HEAD**
- `skinny/crates/runtime/src/grammars/json/scan.rs:32` →
  `scan_structurals_scalar` **CONFIRMED at V3 HEAD**
- `skinny/crates/bbnf-simd/src/scalar/byte_context_64.rs` — **NOT YET
  EXISTS at V3 HEAD** (Fold-4 Stage-A authoring deliverable; per V3
  verb-tense fix "function body lands same-commit with SIMD body at
  S-P3")
- `skinny/crates/bbnf-simd/src/scalar/bcax_64.rs` — **NOT YET EXISTS
  at V3 HEAD** (Fold-5 Stage-A authoring deliverable; same shape)

Note on the latter two: still not-yet-existing at V3 HEAD; V3 verb-
tense fix makes the queued status explicit at the cell level. CH3
concern is REDRESS-regression, not authoring gap; this remains a CH4
flag (consistent with V2 CH3 §6 footnote).

**V3 commit verification:** `git show ebe84954b --stat` confirms 2
files changed: V3 dispatch context (40 insertions, new file) + P2-F
(4 insertions / 2 deletions, the 2 verb-tense cells). P2-A + P2-B +
P2-C + P2-D + P2-E are not in the V3 changeset (V2-LOCKED, confirmed).

## §8 — CH3 disposition (final, V3 confirming cycle)

**ACCEPT 6/6 artefacts at V3.** Per-axis rate: **100% ACCEPT** at V3,
matching V2 (V2: 5 ACCEPT + 1 ACCEPT-WITH-NOTE on P2-E REDRESS-80 = 100%).
V3 is the **second consecutive ≥95% cycle on CH3** (V2 was first; V1
was preceding 100%).

- **P2-A**: ACCEPT (V2-LOCKED zero drift; V1 + V2 CH3 §2.1 carry-
  forward applies verbatim)
- **P2-B**: ACCEPT (V2-LOCKED zero drift; V2 CH3 §2.1 SHA-pinning
  ACCEPT carry-forward; §4 catchall preserved)
- **P2-C**: ACCEPT (V2-LOCKED zero drift; V2 CH3 §2.2 3-demotions-
  strengthen-pre-blocks carry-forward; REDRESS-126 close-hygiene
  strengthened-at-V2 unchanged)
- **P2-D**: ACCEPT (V2-LOCKED zero drift; V2 CH3 §2.3 C-P2D-3 §1.6(d)
  demotion carry-forward; C-P2D-4 pre-blocked-anti-pattern reference
  preserved; substrate-union YES preserved)
- **P2-E**: ACCEPT-WITH-NOTE (V2-LOCKED zero drift; V1 + V2 CH3 F-1
  Gap 5 REDRESS-80 differential carries through V3 unchanged; S-P3
  dispatch must carry the REDRESS-80 re-evaluation checklist per V1
  CH3 §5 item 2)
- **P2-F**: ACCEPT (2 verb-tense cells; zero REDRESS re-openings; V2
  NF-CH6-3 C2 scalar-ref upgrade STRENGTHENING preserved; Fold-4 +
  Fold-5 scalar-ref targets remain REDRESS-neutral Layer-0 sibling
  shapes — V3 polish reconciles cell-level wording with V2 §3.4 same-
  commit discipline; NF-CH6-4 cross-axis tracking note REDUCES
  REDRESS 60-72 recurrence risk; C8 demotion DISCHARGES CH6 paper-
  close risk)

**Three NEW V3 findings logged: F-V3-CH3-1 through F-V3-CH3-3 in §4.**
**Zero REVISE; zero REJECT.** **The V2 CH3 F-1 through F-5 findings
all carry through V3 unchanged** (per §4 F-V3-CH3-2 + F-V3-CH3-3).
**The V1 CH3 F-1 through F-5 findings flow through V2 to V3 unchanged.**

The V3 dispatch context §2 V3-attention items (V3 verb-tense fix
discharges F-V2-CH1-1/F-V2-CH4-1; no V3 edit re-opens REDRESS routes;
three V2-strengthened families [126, 88, 60-72] carry through V3) are
**all satisfied** at V3.

**§3Z gate evaluation:** V3 CH3 is the **second consecutive ≥95%
cycle on CH3** with **100% ACCEPT** (V1: 100%; V2: 100%; V3: 100%).
The §3Z criterion "≥95% × 2 consecutive cycles" is met on CH3 grounds
at V3.

**Cycle disposition:** **LOCK at V3 on CH3 grounds (2-cycle
confirmation: V2 + V3 both 100% ACCEPT, plus V1 preceding 100%).** No
CH3-grounded blocker to S-P3 dispatch. The dispatch §2 watch-list
(REDRESS 28+33, 50-55, 60-72, 80, 82-84, 88, 89, 96/97/98, 119/120,
126) is satisfied with quadruple canonical coverage (p2b admission-
process + p2c arch-instruction + p2d substrate + p2a SOTA-comparator)
at V3 — three families STRENGTHENED at V2 (126, 88, 60-72) carry
through V3, all others unchanged across all three cycles.

**LOCK CONFIRMATION (CH3):** V1 100% + V2 100% + V3 100% = **CH3
2-cycle §3Z LOCK CONFIRMED at V3 HEAD `ebe84954b`.** The CH3 lens is
post-§3Z; no further CH3 cycles required for S-P2 cohort §3Z LOCK
evaluation (the CH3 lens contributes 1 of 7 to cohort §3Z; gate opens
when all 7 lenses achieve 2-cycle 100% per §3Z).
