# Omega-E Skinny Corpus Alignment - Pass Omega V9 SK-V15

Date: 2026-05-28.
Scope: `restart/skinny/{BENCH,COMPILER,HARDENING,INDEX,SUBSTRATE,WORKSPACE}.md`
plus `restart/skinny/tranches/sk-v15/{SYNTHESIS,HANDOFF,SPEC,DISPATCH-PROMPT}.md`.
Disposition: ACCEPT-WITH-CRUD-5-UPDATES.

## Verdict

The live skinny corpus is still anchored on Pass Omega V8 / SK-V14
W5B-FRONTENDR. That was correct for REDRESS-212, but it is now stale after
PASS-IMPL V1, SK-V15 S-P3 V4 lock, and T-P1/T-P2/T-P3 totality closure.

The SK-V15 totality packet changes the active skinny story:

- SK-V14 row-ledger full admit remains historical evidence, but PASS-IMPL V1
  demotes the CSS leg as a generalisation claim.
- SK-V15 is the active PRUNE-then-REBUILD implementation contract.
- W0 is the next legal implementation wave after Pass Omega/G-Omega, not
  SK-V14 W5B.0.
- CSS live admits based on the W8R 24-row broadcast are diagnostic or NO-GO,
  not close evidence.
- Pattern H is still 67 root runtime files, but the close requirement is true
  line-1 generator provenance plus regeneration/check proof.
- Decision Engine is scaffold until W7 proves a real e-graph rewrite,
  non-tautological CSP, grammar-neutral facts, and generated selection impact.
- Lock 14 / Lock 16 status is currently too strong in several skinny surfaces:
  gates must scan/report exclusions and reject self-exempting close.
- FNV closed-enum products are bench-only quarantine inputs for W10; production
  FNV arbiter or hash correctness proof remains blocked.

## Current Authority

Read order for CRUD-5 should be:

1. `restart/skinny/tranches/sk-v15/SPEC.md`.
2. `restart/skinny/tranches/sk-v15/DISPATCH-PROMPT.md`.
3. `restart/skinny/tranches/sk-v15/SYNTHESIS.md`.
4. `restart/skinny/tranches/sk-v15/HANDOFF.md`.
5. `restart/audit/skinny-impl-overfit/V1/CONSOLIDATED-AUDIT.md`.
6. `restart/audit/totality/p1/hardening/HARDENING-T-P1-V5-CONSOLIDATED.md`.
7. `restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md`.
8. `restart/audit/totality/p3/hardening/HARDENING-T-P3-V5-CONSOLIDATED.md`.

Governance note to preserve: T-P1 V5 advanced by clean-final G1 auto-pin rather
than normal two-clean-cycle lock; T-P2 V3 is normal §3Z lock; T-P3 V5 is final
confirmation after a citation-only V4 repair. G-Omega is the next mandatory
user gate before live skinny implementation waves.

## Surface Disposition

| Surface | CRUD-5 operation | Reason |
|---|---|---|
| `restart/skinny/INDEX.md` | Update | Active dispatch, measured split, decision protocol, and authority list still name SK-V14/SK-V13 as current. |
| `restart/skinny/WORKSPACE.md` | Update | Workspace receiver still routes through SK-V14 W5B/W5C/W5D and treats SK-V15 as conditional; replace with W0-W11 SK-V15 envelope. |
| `restart/skinny/HARDENING.md` | Update | Required reading and refusal posture must move from SK-V14 W5B.0 to SK-V15 W0-W11, seven-lens challenge triggers, and PASS-IMPL findings. |
| `restart/skinny/COMPILER.md` | Update | Compiler receiver must replace W5B/W5C/W5D execution with SK-V15 W3/W5/W7/W8/W9 obligations and weaken Decision Engine/load-bearing claims. |
| `restart/skinny/BENCH.md` | Update | BENCH is not read/no-op: CSS comparator/admission language is too strong and misses SK-V15 telemetry, broadcast demotion, native-host, and FNV quarantine. |
| `restart/skinny/SUBSTRATE.md` | Limited update | Substrate mechanics remain mostly valid, but authority/status must anchor SK-V15 and avoid treating FactStream/Lock 16/BackendShape state as closed evidence. |

## CRUD-5 Update Plan By Surface

### INDEX

Recommended operation: Update the opening authority block, the current measured
split, the decision protocol, and authority cross-references.

Defects:

- Lines 5-23 still make Pass Omega V8 / SK-V14 W5B-FRONTENDR the current
  dispatch authority.
- Lines 84-89 still call SK-V13 active dispatch anchor.
- Lines 168-180 still dispatch build/bench flow and SK-V13 close logic rather
  than SK-V15 W0-W11 after G-Omega.
- Lines 186-190 list SK-V14 as active after V8 CRUD and W5B.0 next.

Required replacement posture:

```text
Pass Omega V9 / SK-V15 totality alignment supersedes the active SK-V14
W5B-FRONTENDR dispatch text. SK-V14 closed the row ledger, but PASS-IMPL V1
reclassified CSS L4 as contrived and reopened it as SK-V15 prune/rebuild work.
The active skinny implementation contract is
`restart/skinny/tranches/sk-v15/SPEC.md` plus `DISPATCH-PROMPT.md`, W0-W11.
W0 Baseline and Telemetry Lock is the first legal implementation wave after
Pass Omega/G-Omega authorization.
```

Required state table:

```text
JSON parse_only/direct_to_struct/real_typed_struct remain 51/51 strict
same-plane guard rows. CSS L4's 24 SK-V14 rows are audit-demoted until W1
demotes/collapses the broadcast evidence and W5/W6 provide typed CSS value
output plus same-workload cssparser retiming. SK-V16 routing is remainder only;
it cannot substitute for SK-V15 close evidence.
```

Keep the five-shape `BackendShape` canon and Lock 1 substrate-union text. Do
not add a sixth shape, new public syntax, or a production FNV route.

### WORKSPACE

Recommended operation: Update lines 29-77 and any downstream active-dispatch
phrasing that still treats SK-V15 as conditional.

Defects:

- Lines 29-54 bind active workspace to SK-V14 12-wave plan and old cohort SHAs.
- Lines 56-77 continue the REDRESS-183..212 W5B/W5C/W5D graph and say SK-V15
  entry is conditional if SK-V14 R10 does not close it first.
- The workspace does not name SK-V15 W0-W11 caps, dependency rows, native-host
  admission, or new telemetry fields.

Required replacement posture:

```text
SK-V15 is no longer conditional. The active workspace receiver is the locked
SK-V15 W0-W11 contract: W0 telemetry baseline, W1 CSS admission honesty, W2
Lock 14/16 gate restoration, W3 codegen leak abrogation, W4 Pattern H generated
discipline, W5 CSS typed Value provider, W6 CSS same-workload retime, W7
Decision Engine spine, W8/W9 BackendShape lowerers, W10 FNV quarantine, and W11
close/PASS-IMPL V2 handoff.
```

Add workspace-owned telemetry obligations:

```text
`bbnf-bench` / gate/report code must consume SK-V15 fields:
`measurement_row_id`, `measurement_origin`, `value_plane`,
`css_comparator_workload`, `generator_source`, `lock14_scan_scope`,
`lock16_status`, `checkasm_or_parity_status`, `gate_exclusion_report`, and
`broadcast_group_id`. Producer-only telemetry and self-exempting exclusions
reject close.
```

Keep crate set and LOC budget unless another CRUD agent explicitly authorizes
budget edits. This Omega-E does not propose a crate addition.

### HARDENING

Recommended operation: Update the opening receiver and required-reading blocks.

Defects:

- Lines 7-71 are a dense SK-V14/V8 receiver with W5B.0 as the active refusal
  shape.
- Lines 29-35 require older SK-V14 hardening consolidators and G-Omega before
  treating SK-V14 as dispatch authority, but do not name SK-V15 S-P3 V4,
  totality T-P1/T-P2/T-P3 closure, or Pass Omega V9.
- The prompt still says it defines lenses but not source/gate edits; keep that
  distinction, but update the refusal list to SK-V15.

Required replacement posture:

```text
Pass Omega V9 / SK-V15 receiver: hardening reads SK-V15 SPEC,
DISPATCH-PROMPT, SYNTHESIS, HANDOFF, PASS-IMPL V1 consolidated, T-P1 V5,
T-P2 V3, and T-P3 V5 before treating any skinny surface as current. Refuse
implementation before G-Omega. Refuse SK-V15 close from documentation-only
evidence, warm benches, x86/AVX-512 anchors, W8R CSS metric floors, hidden
broadcast measurements, self-exempting Lock 14/16 exclusions, header-only
Pattern H provenance, source-present unwired primitives, Decision Engine
advisory-only facts, label-string BackendShape lowerers, and production FNV
arbiter/hash-correctness routes.
```

Add the SK-V15 CHALLENGE trigger:

```text
W2-W10 are mandatory seven-lens CHALLENGE candidates unless the plan proves the
redress is ledger-only and non-behavioral. Lenses are CH1 correctness, CH2
generality, CH3 regression, CH4 cost, CH5 hidden coupling, CH6 anti-paper-close,
and CH7 overfit-prune/gate-exclusion.
```

### COMPILER

Recommended operation: Update the SK-V14 receiver block at lines 41-93 and
cross-check skip-impact language around Decision Engine/cost/e-graph.

Defects:

- Lines 73-93 are still the W5B/W5C/W5D compiler graph. That graph is now
  historical and should not dispatch.
- Lines 42-50 mention SK-V14 absorbing decision-engine fold and 5x15 CSS matrix,
  but PASS-IMPL V1 and SK-V15 classify Decision Engine as scaffold and CSS
  provider/generator truth as unproven.
- Lines 914-916 correctly call cost-model/egraph/csp stubbed, but should now
  cross-link to SK-V15 W7/W8/W9 rather than V1 H-only receiver language.

Required replacement posture:

```text
SK-V15 compiler obligations are W3, W5, W7, W8, and W9. W3 removes one
coherent generic codegen leak family with same-wave generator/check consumer.
W5 builds typed CSS value/document/view/visitor output before old CSS parser
proof retires. W7 makes Decision Engine executable: at least one asserted
e-graph rewrite, non-tautological CSP, grammar-neutral facts, and generated
selection impact. W8 implements or gate-rejects EagerTape/OffsetTape lowerers;
W9 implements or gate-rejects EventTape/SinkOnly/CollapsedStage and proves the
all-five BackendShape gate.
```

Terminology guard:

```text
CSS fact streams may be diagnostic evidence or historical substrate-manifest
classification, but fact-stream-only `parse()` and `CssFullParseSummary` are
not live CSS admission after SK-V15 W6. FactStream is not a sixth
`BackendShape`; EventTape is not a sidecar stream.
```

### BENCH

Recommended operation: Update, not read/no-op.

Defects:

- Lines 29-43 anchor SK-V14 telemetry and say CSS L4 rows extend the envelope
  with lightningcss/cssparser, without the PASS-IMPL demotion.
- Lines 317-319 call lightning-css an active CSS comparator without the SK-V15
  limitation that lightningcss counts only after comparable CSSOM/value output.
- Lines 1635-1672 say SK-V13 CSS rows are close targets that must beat
  lightningcss or block; this is now too strong and wrong-plane for current CSS.
- Lines 1875-1879 still frame a CSS prior probe table; that can remain
  historical but should not look like active CSS admission.
- Lines 2238-2239 say CSS generalisation is gate-owned by SK-V13 rows and CSS
  gates are SK-V13 close targets.

Required replacement posture:

```text
BENCH consumes SK-V15 W0/W1/W5/W6/W10. CSS W8R 24-row evidence is diagnostic
or NO-GO until W1 demotes/collapses it and W5/W6 provide typed CSS value output
with fresh same-workload cssparser comparison. `lightningcss` is a diagnostic
planning signal before comparable CSSOM/value output; it is not an admission
floor for fact-stream or brace-counter output.
```

Required schema update:

```text
Rows require `measurement_row_id`, `measurement_origin`, `value_plane`,
`css_comparator_workload`, `generator_source`, `lock14_scan_scope`,
`lock16_status`, `checkasm_or_parity_status`, `gate_exclusion_report`, and
`broadcast_group_id`. Missing fields, hidden one-to-N measurement stamps,
self-exempting gate exclusions, source-present unwired primitives, and
non-native admission hosts reject close.
```

Required FNV update:

```text
W11L/W11N/W11O FNV closed-enum products are bench-only quarantine inputs.
Strict-product comparators must catch closed-enum sidecar coupling. No
production runtime/generic-codegen FNV arbiter, selector, or hash correctness
proof is admissible in SK-V15.
```

### SUBSTRATE

Recommended operation: Limited update.

Substrate content mostly remains valid: Lock 1 substrate union, the five-shape
`BackendShape` canon, no sidecar structural stream, and Lock 16 scalar-oracle
plus checkasm posture all survive SK-V15 totality closure.

Defects:

- Lines 33-64 still frame active substrate obligations as Pass Omega V2 /
  SK-V14 after CRUD-3.
- Lines 48-56 classify FactStream as a substrate-manifest category. That is
  still a useful historical classification, but SK-V15 says fact-stream-only
  CSS `parse()` cannot remain live admission after W6.
- Lines 57-64 state the SIMD/ASM obligation but do not mention SK-V15 W2 gate
  exclusion reporting or native Apple M5 Max / aarch64 admission-only status.

Required replacement posture:

```text
Pass Omega V9 / SK-V15 substrate receiver: no substrate amendment follows from
SK-V15 totality closure. Lock 1 substrate union, no parallel substrate, no
cross-call retained classifier state, and the five-shape `BackendShape` canon
remain binding. SK-V15 W2 restores Lock 14/16 gate coverage and exclusion
reporting; W8/W9 prove lowerer implementations for the existing five shapes.
Fact-stream-only CSS output is diagnostic after W1 and must retire from live
admission by W6 typed CSS proof.
```

Do not change substrate data structures from Omega-E. Do not add a retained
EventTape sidecar, sixth `BackendShape`, public `UnionTape`, alternate document
projection, or production FNV route.

## Cross-Surface CRUD Notes

- Replace "current authority is SK-V14" with "current authority is SK-V15
  SPEC/DISPATCH after Pass Omega/G-Omega".
- Preserve SK-V14 as historical evidence, especially REDRESS-183/184/209-215
  and W5B-FRONTENDR, but do not let it dispatch.
- Where surfaces say SK-V13 CSS rows are active close targets, replace with
  SK-V15 CSS demotion/rebuild language.
- Where surfaces say CSS must beat lightningcss, qualify: `cssparser` is the
  near-term same-workload comparator; `lightningcss` counts only after Track 1
  emits comparable CSSOM/value output.
- Where Pattern H says "67 hand-written runtime files", add the SK-V15 close
  requirement: exactly 67 root runtime files plus true line-1 generator
  provenance and non-writing regen/check proof.
- Where Decision Engine language implies folded/absorbed status, weaken it to
  scaffold until W7 proof.
- Where Lock 14/16 language implies gates are already authoritative, add that
  W2 must restore omitted roots, report exclusions, and make self-exempting
  scans reject.
- Where FNV appears, route to W10 quarantine only.

## Non-Goals

This Omega-E artefact proposes CRUD-5 updates only. It does not edit live
surfaces, does not change locks, does not modify `skinny/RESULTS.md` or
`skinny/REDRESS.md`, does not stage or commit, and does not reopen substrate
mechanics beyond authority/status alignment.
