---
agent: 1B
pass: T-P1-excavation
cycle: V5
generated_at: 2026-05-29T19:10:00Z
spec_surfaces_audited:
  - restart/ARCHITECTURE.md
  - restart/MASTER-PLAN.md
  - restart/locks/LOCKS.md
  - restart/skinny/tranches/sk-v17/SPEC.md
files_audited_count: 23
live_truth_method: "wc -l + grep -n/-c + sed path:line, re-verified against master 445925167154de73540e3ea3283d0170371de790. Spec: ARCHITECTURE.md §7.3 BackendShape enum :1091-1114, derive_backend_shape steps :1135-1176, per-shape lowering table :1180-1186, admission ledger :1188-1206 (UNKNOWN-2D-05 :1206), bifurcation/grammar-neutral primitive signatures :1284; LOCKS.md Lock 10 five-shape :107-109/:272-273/:599, substrate_target :119/:528-530, Lock 14 :603; MASTER-PLAN.md §H.W1 :620 (BackendShape 'LANDED' claim), §13.5 :854; SPEC.md §0.1 JSON-guard :42, W1 §4 :390-440, pre-block :793-808, W1 row :824. Core: grep -rn 'enum BackendShape'/'derive_backend_shape' crates/ = ZERO (exit 1); skinny enum BackendShape skinny/crates/ir/src/lib.rs:340; skinny derive_backend_shape skinny/crates/passes/src/lib.rs:392/:401; wc -l skinny/crates/passes/src/{backend_egraph=311,decision_csp=273}; backend_egraph::select lib.rs:498, decision_csp::finalize_rule :499, choose_backend_shape :473; wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs=17 each, sink_only=270; root Cargo.toml:2 (crates/egraph + crates/csp-solver present, NO passes); EmitStrategy::StructDirect crates/ir/src/registry/strategy.rs:104-119, PRODUCTION_MANIFEST_TABLE :134 (9 ManifestStrategyEntry rows verified), for_grammar :215, StructDirect-only matches! :224; FieldSource enum crates/ir/src/registry/struct.rs:84-115, StructLayout :202, StructRegistry :313, lookup method = StructRegistry::layout :331 / layout_by_name :337 (NOT 'lookup'); begin_compound(&StructLayout) crates/core/src/runtime/tape/mod.rs:185; substrate.rs builder_path :41/document_path :53/substrate_path :76 panic! :79; simd-scan: StructuralAlphabet crates/simd-scan/src/alphabet.rs:19, KernelShape::select :118, scan_structural lib.rs:80, neon::scan neon.rs:47, avx2/avx512/wasm cfg-gated to non-aarch64; GRAMMAR_STRUCTURAL_ALPHABET emitter crates/core/src/backend/rust/emitter/profile.rs:43/:100, runtime alphabet ensure_structural_index dispatcher/support.rs:472-485; grep -rln scan_structural over crates/core/src/grammar/generated = 8 grammars (ebnf,bbnf,bnf,json,csv,google_sheets,css_pretty,css_l4). No cargo/build mutation."
prior_cycle_dispositions_folded:
  accepted:
    - BSHAPE17-001-no-backendshape-enum-in-core
    - BSHAPE17-002-derive-selector-skinny-only-deps-present-in-core
    - BSHAPE17-003-single-emitstrategy-structdirect-vs-5-shape-canon
    - BSHAPE17-004-four-lowerers-scaffold-one-substantive-skinny-only
    - BSHAPE17-005-collapsedstage-x86-pinned-unknown-2d-05-aarch64
    - BSHAPE17-006-structregistry-arena-builder-hot-path-pre-block
    - BSHAPE17-007-fieldsource-walk-is-compile-time-projection-emission
    - BSHAPE17-008-emitter-data-bound-manifest-lock14-honoured
  rejected: []
  revised: []
  first_cycle_additions:
    - BSHAPE17-009-neon-shared-classifier-grammar-general-already-in-core
  symbol_anchor_corrections:
    - BSHAPE17-006 StructRegistry lookup method is `StructRegistry::layout` (crates/ir/src/registry/struct.rs:331) / `layout_by_name` (:337); V4 cited symbol `lookup` at the struct-decl line :313 (struct decl, not a method) — same symbol-vs-decl class CH1 corrected elsewhere this pass; the pre-block fact (no per-leaf registry indirection) is unchanged.
divergence_count:
  spec_claims_implemented: 1
  spec_claims_unimplemented: 4
  impl_exceeds_spec: 2
  unknown: 2
locks_amendment_candidates: 0
---

## Executive Summary

1B excavates the codegen layer of the TOTALITY V1 spec (§7.3 `BackendShape`
5-shape canon, `derive_backend_shape`, Lock 10) plus the NEON shared-classifier
generalization against the crates/core fold target and the SKINNY-proven
codegen, master `445925167`. Central fact: the whole 5-shape selector — enum,
`derive_backend_shape`, e-graph→CSP→cost pipeline, five lowerers — lives ONLY in
skinny (`skinny/crates/ir/src/lib.rs:340`, `skinny/crates/passes/src/lib.rs:392`,
`.../codegen/src/lower/`). crates/core has NO `BackendShape` and NO
`derive_backend_shape` (grep-zero, exit 1); its codegen selects via single-variant
`EmitStrategy::StructDirect` (`crates/ir/src/registry/strategy.rs:104-119`) from a
9-row manifest (`:134`), not the per-rule cost selector §7.3 mandates — even
though MASTER-PLAN §H.W1 (`:620`) asserts these "LANDED for Rust-state substrate
in SK-V5 Wave 1" (a spec-vs-fold-target divergence). Four of five skinny lowerers
are 17-LOC scaffolds; only `sink_only` (270 LOC) is substantive; `CollapsedStage`
is x86-pinned, aarch64 = spec-named UNKNOWN-2D-05 (`ARCHITECTURE.md:1206`). The
load-bearing pre-block is StructRegistry/Arena<G>/Builder<G> hot-path indirection
(SPEC `:793-794`): `begin_compound(&StructLayout)` (`crates/core/src/runtime/tape/mod.rs:185`)
takes a pre-resolved layout — the projection generator MUST be compile-time, never
a per-leaf walk, or it re-opens 28-65×/983×/10583×. The selector fold's dependent
crates pre-exist in core (egraph+csp-solver, `Cargo.toml:2`), so SK-V18 WIRES them
(311+273-LOC passes + `CostFacts`), co-dependent with the StructDirect resolver
re-key (combined 600-1400 LOC, non-additive). NEW V5: the NEON shared classifier
`select_classifier(alphabet)` is ALREADY grammar-general in core
(`simd_scan::scan_structural(input,&StructuralAlphabet)` lib.rs:80; `KernelShape::select`
alphabet.rs:118; all 8 generated grammars emit `GRAMMAR_STRUCTURAL_ALPHABET`) —
NOT JSON-only; impl-exceeds-spec. No new shape, directive, or substrate.

## Spec-Claim ↔ Implementation Table

| ID | Spec / corpus claim (path:line) | Live / counter-surface evidence (path:line) | Verdict | Divergence class | Note |
|---|---|---|---|---|---|
| BSHAPE17-001 | §7.3 mandates a 5-variant `BackendShape` enum `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` as the codegen substrate-projection selector (`restart/ARCHITECTURE.md:1091-1114`); Lock 10 fixes the 5-shape search domain (`restart/locks/LOCKS.md:107-109`). | `enum BackendShape` exists ONLY in skinny: `skinny/crates/ir/src/lib.rs:340`. `grep -rn 'enum BackendShape' crates/` returns ZERO (exit 1); the crates/core/crates/ir tree has no BackendShape type. | spec-canon-vs-core-absent | unimplemented | The §7.3 codegen selector is unbuilt in the totality fold target; its codegen carries no 5-shape vocabulary. SKINNY proves it; SK-V18 folds. |
| BSHAPE17-002 | `passes::recognizers::derive_backend_shape(grammar_ir, rule_id) -> BackendShape` is the (now e-graph→CSP→cost) per-rule selector (`restart/ARCHITECTURE.md:1135-1176`); Lock 10 binds it (`restart/locks/LOCKS.md:272-273`). | `derive_backend_shape` exists ONLY in skinny: `skinny/crates/passes/src/lib.rs:392` (+`_with_diagnostics:401`), routing `backend_egraph::select`(`:498`)/`decision_csp::finalize_rule`(`:499`) under `choose_backend_shape`(`:473`). `grep -rn 'derive_backend_shape' crates/` = ZERO (exit 1). crates/core has no cost-model shape selector. | spec-selector-vs-core-absent | unimplemented | Cost-model derivation is skinny-only. Dependent crates `egraph`/`csp-solver` ALREADY in core (`Cargo.toml:2`; skinny consumes via `path` `skinny/Cargo.toml:36-37`); the fold WIRES them + the `backend_egraph`(311 LOC)/`decision_csp`(273 LOC) passes + `CostFacts` — co-dependent with BSHAPE17-003 (combined 600-1400 LOC, see Divergences). |
| BSHAPE17-003 | §7.3: the cost model picks one of five shapes per rule; "no BBNF directive carries the choice" (`restart/ARCHITECTURE.md:1088`). | crates/core codegen carries ONE strategy variant: `pub enum EmitStrategy { StructDirect { rust, ts, wasm } }` (`crates/ir/src/registry/strategy.rs:104-119`), resolved per-grammar from `PRODUCTION_MANIFEST_TABLE` (9 `ManifestStrategyEntry` rows, `:134-189`) by `for_grammar` (`:215`). `is_struct_direct` is the lone variant test (`matches!(self, EmitStrategy::StructDirect{..})` `:224`). It is grammar-keyed substrate-binding DATA, not a per-rule 5-shape cost selection. | single-strategy-vs-5-shape-per-rule | unimplemented | The fold target's codegen is whole-grammar StructDirect, not per-rule shape. The 5-shape canon must absorb the StructDirect path (the SinkOnly/struct-builder lineage) without re-introducing a directive. |
| BSHAPE17-004 | §7.3 per-shape lowering: each `BackendShape` resolves to a concrete artefact triple the codegen template emits (`restart/ARCHITECTURE.md:1180-1186`); the all-five-lowerer gate is a Lock-10 closure condition (`restart/locks/LOCKS.md:599`). | Live lowerer surface (skinny-only): `skinny/crates/codegen/src/lower/{eager_tape,offset_tape,event_tape,collapsed_stage}.rs` = 17 LOC each (verified `wc -l`); `sink_only.rs` = 270 LOC (substantive). The `ARCHITECTURE.md:1202-1206` admission ledger marks all five NOT-ADMITTED / OPEN-AUDIT-DEMOTED. | spec-five-real-lowerers-vs-four-scaffold | unimplemented | Only `sink_only` is substantive; four are marker scaffolds. crates/core has no lowerer hierarchy — its emitter is the StructDirect per-shape emitter set under `backend/rust/emitter/shapes/`. |
| BSHAPE17-005 | §7.3 `CollapsedStage` "fuses mask-state and emission for AVX-512-class hardware" (`restart/ARCHITECTURE.md:1088,1186`); CSP binds it to `target.arch == x86` + `target.avx512bw` + `Entry(_)` (`:1171,:1206`; LAC-2D-06). | `ARCHITECTURE.md:1206` itself records the aarch64 candidate as spec-named **UNKNOWN-2D-05** ("requires 2E source-backed aarch64 strategy before any aarch64 admission; aarch64 mechanically refused", reiterated `:1277-1281`). SK-V17 is aarch64-only and bars x86/AVX/SVE + a 6th shape (`restart/skinny/tranches/sk-v17/SPEC.md:806`, `:808`). Skinny `collapsed_stage.rs:15-17` is a marker lowerer. | spec-x86-shape vs spec-named-aarch64-unknown | unknown | NOT a fresh undiscovered gap: the aarch64-into-canon question is already catalogued at `ARCHITECTURE.md:1206` as UNKNOWN-2D-05. SK-V17's NEON union sits under the four LLVM shapes' scan-leaf FFI (`ARCHITECTURE.md:1284`); the canon holds at Lock 10. T-P2 maps how aarch64-NEON absorbs without a 6th shape. |
| BSHAPE17-006 | SK-V17 pre-blocks "**StructRegistry / Arena<G> / Builder<G> hot-path indirection** (28-65× bbnf/sheets, 983× css bootstrap, 10583× WATCHDOG tailwind). No registry lookup in the per-leaf hot path." (`restart/skinny/tranches/sk-v17/SPEC.md:793-794`); W1 row carries it (`:824`). | `StructRegistry` is `BTreeMap<RuleId, StructLayout>` (`crates/ir/src/registry/struct.rs:313-315`); its lookup method is `StructRegistry::layout(rule_id)` (`:331`) / `layout_by_name` (`:337`), populated via `insert` (`:326`) at `project_types`. The live consumer `begin_compound(&mut self, layout: &StructLayout)` (`crates/core/src/runtime/tape/mod.rs:185`) takes a **pre-resolved** `&StructLayout` — no per-leaf `StructRegistry::layout` in the runtime hot path. | pre-block-honoured-in-current-shape | implemented (pre-block honoured) | The do-not-redrive fence: registry lookup is compile-time (project_types population), runtime carries the resolved layout by reference. SK-V18 fold MUST preserve this — no `StructRegistry::layout`/`Arena<G>`/`Builder<G>` per-leaf, or 28-65×/983×/10583× re-opens. **V5 anchor fix:** the lookup symbol is `layout`/`layout_by_name` (`:331`/`:337`), not `lookup` (V4 mis-cited the struct-decl line `:313`). |
| BSHAPE17-007 | §7.3 Value-API / Layout-shape projection walks `BackendRule`/`FieldSource` to emit `document/value/view/visitor` (SK-V17 W2). FieldSource is the BackendRule-walk recipe. | `FieldSource{TypedLeaf, BranchTag, SeqPosition, RepeatElement, RuleReference}` (`crates/ir/src/registry/struct.rs:84-115`) lives INSIDE `StructLayout`(`:202`)/`StructRegistry`(`:313`). A naive per-leaf FieldSource walk at runtime IS a per-leaf StructRegistry indirection (BSHAPE17-006 regression). | projection-walk-must-be-compile-time | implemented (with fence) | The FieldSource walk is **compile-time projection-emission**, resolved once at codegen and baked into the generated parser body — never a runtime per-leaf registry walk. The emitter (`backend/rust/emitter/shapes/`) consumes the layout at generation time, not parse time (SPEC `:794`). |
| BSHAPE17-008 | Lock 14: generic codegen crates carry ZERO grammar-named modules; the strategy binding is DATA, not a Rust arm-list (`restart/locks/LOCKS.md:603`; SPEC `:824` W1 W5C-array RETIRE). | The emitter resolves `builder_path`/`document_path` from `EmitStrategy::StructDirect.rust` (`crates/core/src/backend/rust/emitter/shapes/substrate.rs:41,53`); the per-grammar binding is `PRODUCTION_MANIFEST_TABLE` DATA (`crates/ir/src/registry/strategy.rs:134-189`) — "adding a new grammar adds a row, not a match arm in for_grammar" (`:125-127`). `substrate_path` HARD-FAILs (`panic!` `:79`) on malformed binding rather than defaulting to `JsonStructBuilder`. | impl-honours-lock14-data-binding | impl-exceeds-spec | crates/core codegen already enforces the W5C-array RETIRE discipline SK-V17 W1 mandates: routing is grammar/manifest DATA, not per-rule branching. The grammar-named `*StructBuilder` are per-grammar runtime surfaces (Lock 14 ALLOWED), consumed as path strings. |
| **BSHAPE17-009 (NEW V5)** | §13.1 SIMD primitives are grammar-neutral (signatures `BYTE_CLASS_FROM_TABLE_64`, `BYTE_CLASS_FROM_EQ_SET_64`, … `ARCHITECTURE.md:1284`); §0.1 SK-V17 mandates the shared NEON byte-class classifier `select_classifier(alphabet)`, aarch64-only, NEON-and-grammar-general (`SPEC.md:42,:102,:258`). The prompt subject names "NEON JSON-only vs the shared-classifier generalization" as a divergence to excavate. | The shared classifier is ALREADY grammar-general in crates/core. `simd_scan::scan_structural(input, &StructuralAlphabet)` (`crates/simd-scan/src/lib.rs:80`) is the grammar-neutral public entry; the alphabet is a per-grammar `StructuralAlphabet` (`crates/simd-scan/src/alphabet.rs:19`, doc: "per-grammar configuration … no per-grammar code branch — the Shape value picks the lowering, the data drives it", quote-class example covers JSON AND CSS `:35`); `KernelShape::select(alphabet)` (`:118`) is the cardinality-driven classifier selector (= `select_classifier`); the NEON kernel `neon::scan` (`crates/simd-scan/src/neon.rs:47`) is aarch64-baseline; x86 (`avx2.rs:25`/`avx512.rs:20`) + wasm (`wasm.rs:36`) are `#![cfg]`-gated to non-aarch64. The IR pass `crates/ir/src/passes/sets/structural_alphabet.rs` mines the alphabet per-grammar; codegen emits `GRAMMAR_STRUCTURAL_ALPHABET` (`crates/core/src/backend/rust/emitter/profile.rs:43,:100`); the runtime constructs the alphabet per-call inside `ensure_structural_index` (`crates/core/src/backend/rust/emitter/shapes/dispatcher/support.rs:472-485`). **All 8 generated grammars** (ebnf,bbnf,bnf,json,csv,google_sheets,css_pretty,css_l4) consume `scan_structural` (`grep -rln scan_structural crates/core/src/grammar/generated` = 8). | spec-shared-classifier vs impl-already-general | **impl-exceeds-spec** | The NEON classifier is NOT JSON-only in the fold target — it is the grammar-general `select_classifier(alphabet)` the SKINNY proof generalizes, already landed in core. The SK-V18 fold does NOT re-build it; the divergence is that the spec's narrative still frames NEON as the JSON scanner while crates/core already carries the alphabet-parametrised shared classifier across 8 grammars. CH5-S3/S2 hold: the runtime alphabet is constructed per-call (`support.rs:477`) inside a `get_or_init` scan cache (retained scan cache, NOT tape projection nor renamed-scanner cross-call state). |

## Cross-Tree Codegen Map (SK-V17 fold subject)

| Concern | SKINNY-proven (skinny/crates) | TOTALITY fold target (crates/core) | Fold reconciliation |
|---|---|---|---|
| Shape selector | 5-variant `BackendShape` enum (`skinny/crates/ir/src/lib.rs:340`); `derive_backend_shape` cost pipeline (`skinny/crates/passes/src/lib.rs:392`) routing `backend_egraph::select`(`:498`)/`decision_csp::finalize_rule`(`:499`) over egraph+csp-solver crates (`skinny/Cargo.toml:36-37`) | NO BackendShape enum; NO `passes` crate (root `Cargo.toml:2`); single `EmitStrategy::StructDirect` (`crates/ir/src/registry/strategy.rs:104`) resolved from 9-row manifest (`:134`). egraph + csp-solver crates PRESENT in core (`Cargo.toml:2`) | SK-V18 folds the 5-shape selector into core (wiring the present egraph/csp-solver, adding the 311+273-LOC passes); StructDirect IS the SinkOnly/struct-builder shape, absorbed not deleted; co-dependent with the resolver re-key (600-1400 LOC joint) |
| Per-shape lowering | 5 lowerers: `sink_only.rs` 270 LOC substantive; 4×17-LOC scaffolds delegating `tape_plan` (`skinny/crates/codegen/src/lower/`) | per-shape EMITTERS keyed off StructDirect (`crates/core/src/backend/rust/emitter/shapes/`) | the core emitter shapes are the StructDirect lowering; SK-V18 maps them under the 5-shape canon's SinkOnly/OffsetTape arms |
| CollapsedStage | x86/AVX-512 only (`ARCHITECTURE.md:1206`); marker lowerer `collapsed_stage.rs:15-17` | absent (no BackendShape) | x86-pinned, aarch64 = UNKNOWN-2D-05 (`ARCHITECTURE.md:1206`); NEON union under 4 LLVM shapes, NOT CollapsedStage |
| Strategy binding | `BackendRule` + `LayoutFacts.backend_shape` | `EmitStrategy` + `SubstrateBinding` + `PRODUCTION_MANIFEST_TABLE` DATA (`crates/ir/src/registry/strategy.rs:134-189`) | both are grammar-as-data (Lock 14); fold unifies BackendRule-walk with the StructDirect manifest |
| Layout recipe | `BackendRule`-walk projection generator (SK-V17 W2) | `StructLayout` + `FieldSource` inside `StructRegistry` (`crates/ir/src/registry/struct.rs:84,202,313`); resolved compile-time, consumed `begin_compound(&StructLayout)` (`crates/core/src/runtime/tape/mod.rs:185`) | FieldSource walk = compile-time projection-emission ONCE (BSHAPE17-007); NO per-leaf registry lookup (BSHAPE17-006 fence) |
| **NEON byte-class classifier** | shared `select_classifier(alphabet)` (SK-V17 SUBSTRATE) — JSON-proven, generalized to CSS-and-grammar-general | `simd_scan::scan_structural(input,&StructuralAlphabet)` (`crates/simd-scan/src/lib.rs:80`); `KernelShape::select(alphabet)` (`alphabet.rs:118`); per-grammar `GRAMMAR_STRUCTURAL_ALPHABET` emitted (`profile.rs:43`); consumed by all 8 generated grammars | **already grammar-general in core** (impl-exceeds-spec, BSHAPE17-009); SK-V18 does NOT re-build the classifier; the spec narrative (still JSON-framed) is what must absorb the alphabet-parametrised shared form. NEON aarch64-baseline; x86/wasm cfg-gated out (aarch64-only honoured) |

## Divergences Catalogued

| ID | Divergence | Evidence | Files-touched surface (fold cost) | LOC / risk |
|---|---|---|---|---|
| BSHAPE17-001 | No `BackendShape` enum in crates/core/crates/ir; lives only in skinny. | `skinny/crates/ir/src/lib.rs:340`; `grep -rn 'enum BackendShape' crates/` = ZERO (exit 1) | generator-side: a new core IR enum + its emitter dispatch; 0 generated parsers touched at definition (selector lands before emit) | 60-200 LOC SK-V18; medium (definition only; wiring is BSHAPE17-002/003) |
| BSHAPE17-002 | No `derive_backend_shape` cost selector in crates/core. | `skinny/crates/passes/src/lib.rs:392`; `grep -rn 'derive_backend_shape' crates/` = ZERO (exit 1) | generator-side: core gains a `passes`-equivalent shape-selection layer (NO `passes` crate in root `Cargo.toml:2`). Dependent-crate surface KNOWABLE and already-present: `crates/egraph` + `crates/csp-solver` ALREADY in core (`Cargo.toml:2`; skinny consumes via `path` `skinny/Cargo.toml:36-37`) — the fold WIRES them, does not build them. New surface = `backend_egraph` routing (311 LOC, `select` `lib.rs:498`) + `decision_csp` finalize (273 LOC, `finalize_rule` `:499`) + `choose_backend_shape`/`CostFacts` plumbing (`lib.rs:473`, `CostFacts` skinny ir). No generated-parser hand-edit at selector-land. **Co-dependent with BSHAPE17-003**: the selector is inert without the `EmitStrategy` resolver consuming it, so the LOC bands are NOT additive-independent. | selector alone 400-900 LOC SK-V18 (backend_egraph 311 + decision_csp 273 + plumbing); high. **Combined BSHAPE17-002 ⊕ BSHAPE17-003 envelope: 600-1400 LOC** (selector + resolver re-key, NOT 400-900 + 200-500 summed — the resolver re-key is the consumption half of the same fold). Dependent crates egraph + csp-solver pre-exist (`Cargo.toml:2`). |
| BSHAPE17-003 | crates/core codegen is single-variant `EmitStrategy::StructDirect`, not per-rule 5-shape. | `crates/ir/src/registry/strategy.rs:104-119`, manifest `:134-189` (9 rows), `is_struct_direct` `:224` | generator-side: `EmitStrategy` gains arms OR the resolver consumes derive_backend_shape; the 9-manifest rows re-keyed; regen-gated (all generated parsers re-emit) | 200-500 LOC; high (touches the strategy resolver + every generated parser via regen). **Co-dependent with BSHAPE17-002** (this is the consumption half of the selector fold); do NOT sum onto BSHAPE17-002 — joint envelope 600-1400 LOC (BSHAPE17-002 row). |
| BSHAPE17-004 | Four of five skinny lowerers are 17-LOC scaffolds; only sink_only substantive. | `wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs` = 17 each; `sink_only.rs` = 270; `ARCHITECTURE.md:1202-1206` NOT-ADMITTED/OPEN | generator-side: the all-five lowerer gate (`LOCKS.md:599`) is a skinny-close obligation, not a core fold; core has no lowerer hierarchy | 0 LOC to catalogue; high to close all-five (skinny W8/W9 owns) |
| BSHAPE17-005 | CollapsedStage x86-pinned; aarch64 = spec-named UNKNOWN-2D-05. | `restart/ARCHITECTURE.md:1206,1277-1281`; SPEC `:806,:808` | 0 LOC (canon holds); aarch64 absorbs into 4 LLVM shapes via scan-leaf FFI (`ARCHITECTURE.md:1284`) | 0 LOC; medium (T-P2 maps aarch64-NEON into canon without a 6th shape) |
| BSHAPE17-006 | StructRegistry/Arena<G>/Builder<G> hot-path indirection is a load-bearing pre-block; current core honours it (begin_compound takes resolved &StructLayout). | SPEC `:793-794`, W1 `:824`; `crates/ir/src/registry/struct.rs:331` `StructRegistry::layout`; `crates/core/src/runtime/tape/mod.rs:185` `begin_compound(&StructLayout)` | DO-NOT-REDRIVE fence: SK-V18 fold must NOT introduce per-leaf `StructRegistry::layout`/`Arena<G>`/`Builder<G>` in the runtime hot path | 0 LOC (catalogue); CRITICAL if violated (28-65×/983×/10583× regression) |
| BSHAPE17-009 (NEW V5) | Spec narrative frames NEON as the JSON scanner; crates/core already carries the grammar-general alphabet-parametrised shared classifier across 8 grammars. | `crates/simd-scan/src/lib.rs:80` (`scan_structural(input,&StructuralAlphabet)`); `alphabet.rs:19,:118` (`StructuralAlphabet`, `KernelShape::select`); `crates/core/src/backend/rust/emitter/profile.rs:43,:100` (per-grammar `GRAMMAR_STRUCTURAL_ALPHABET`); `grep -rln scan_structural crates/core/src/grammar/generated` = 8 grammars; x86/wasm cfg-gated (`avx2.rs:25`/`avx512.rs:20`/`wasm.rs:36`) | 0 LOC fold (classifier already general); narrative-only: the V1 spec's JSON-scanner framing must absorb the alphabet-parametrised shared form. No re-build. | 0 LOC (impl-exceeds-spec); low — fence: keep the alphabet grammar-mined (`structural_alphabet.rs`), per-call constructed (`support.rs:477`), no retained cross-call classifier state (CH5-S3) and no second substrate. |

## Gaps / Missing Primitives

| Gap | Evidence | Receiver |
|---|---|---|
| No `BackendShape` enum / `derive_backend_shape` / per-shape lowerer hierarchy in crates/core; the §7.3 codegen canon is skinny-only — despite MASTER-PLAN §H.W1 (`:620`) asserting these "LANDED for Rust-state substrate in SK-V5 Wave 1". | `skinny/crates/ir/src/lib.rs:340`, `skinny/crates/passes/src/lib.rs:392`, `skinny/crates/codegen/src/lower/`; `grep` over `crates/` = ZERO; MASTER-PLAN `:620` | SK-V18 folds the 5-shape selector + lowerers into core (skinny proves all-five gate first, `LOCKS.md:599`). T-P2 reconciles the §H.W1 "LANDED" claim against the fold-target grep-zero (the claim is true of skinny, false of crates/core). |
| The crates/core StructDirect emitter has no per-rule shape decision; whole-grammar manifest binding only. | `crates/ir/src/registry/strategy.rs:104-189` (one variant, 9-row table) | SK-V18: the cost-model derive selects per rule; StructDirect becomes the SinkOnly/struct-builder arm |
| Four lowerers (eager/offset/event/collapsed) are 17-LOC marker scaffolds; the all-five-real-lowerer Lock-10 gate is unclosed. | `wc -l skinny/.../lower/*.rs`; `ARCHITECTURE.md:1202-1206` | skinny W8/W9 (real lowerers or gate-consumed rejection) — NOT a core fold gap |
| aarch64 CollapsedStage admission is spec-named UNKNOWN-2D-05, not a built path. | `restart/ARCHITECTURE.md:1206,1277-1281` | T-P2 research: aarch64-NEON union into the canon without a 6th shape; no aarch64 CollapsedStage re-derivation |
| NO missing classifier primitive: the shared `select_classifier(alphabet)` generalization the spec mandates is ALREADY landed grammar-general in core (NOT a gap). | `crates/simd-scan/src/lib.rs:80`; `alphabet.rs:118`; 8 generated grammars consume it | none — impl-exceeds-spec (BSHAPE17-009). T-P3 folds the spec narrative, not code. |

## Do-Not-Redrive Ledger (CH3 pre-block, codegen layer)

| Pre-block | Source | Fence (no-redrive rule) |
|---|---|---|
| StructRegistry / Arena<G> / Builder<G> hot-path indirection (28-65× bbnf/sheets, 983× css bootstrap, 10583× WATCHDOG tailwind) | SPEC `:793-794`, W1 `:824` | NO per-leaf `StructRegistry::layout`/`Arena<G>`/`Builder<G>` in any runtime hot path. The live `begin_compound` (`crates/core/src/runtime/tape/mod.rs:185`) takes a pre-resolved `&StructLayout`; SK-V18 preserves compile-time layout resolution, runtime by-reference. |
| FieldSource per-leaf walk at runtime | `crates/ir/src/registry/struct.rs:84,313` (FieldSource inside StructRegistry) | The BackendRule/FieldSource projection walk is **compile-time projection-emission** — resolved ONCE at codegen, baked into the generated body. A naive per-leaf FieldSource walk re-opens BSHAPE17-006. |
| Sixth BackendShape / x86 / AVX-512 / SVE collapsed-stage | SPEC `:806,:808`; `ARCHITECTURE.md:1206` | aarch64 only; no aarch64 CollapsedStage re-derivation; no 6th shape (Lock 10 `:107-109`). |
| W5C_REQUEST_FACT_PROFILES array / per-rule branching as routing | SPEC `:798` (`codegen/src/lib.rs:336`), W1 `:824` (RETIRE) | routing stays grammar/manifest DATA (`crates/ir/src/registry/strategy.rs:134-189` already honours this); do not relocate per-rule branching into projection DATA or flag form. |
| Retained cross-call classifier state / parser-local structural-mask cursor (REDRESS-53) | SPEC `:577,:824`; CH5-S3 | the NEON alphabet is per-call constructed (`support.rs:477`) inside a `get_or_init` scan cache (retained scan cache, NOT tape projection, NOT renamed-scanner cross-call state); SK-V18 fold of the shared classifier MUST NOT retain alphabet/classifier state across calls as a parallel substrate. |

## Open Questions

| UNKNOWN | Blocking question | verify_action |
|---|---|---|
| U-BSHAPE17-001 (BSHAPE17-005) | Does aarch64-NEON absorb into the existing four LLVM `BackendShape` shapes via scan-leaf FFI, or does UNKNOWN-2D-05 require a distinct aarch64-canon mapping when SK-V18 folds the selector into core? | T-P2 read `ARCHITECTURE.md:1206,1277-1284` + SPEC `:806,:808`; map the NEON `KernelShape::select` leaf onto OffsetTape/SinkOnly scan arms; record whether any new admission row is needed (no 6th shape per Lock 10 `:107-109`). |
| U-BSHAPE17-002 (BSHAPE17-003) | When SK-V18 folds `derive_backend_shape` into core, does the single `EmitStrategy::StructDirect` become one arm of a per-rule shape selector (StructDirect = SinkOnly/struct-builder), or is StructDirect retired in favour of the BackendShape enum? | T-P2/T-P3 compare `crates/ir/src/registry/strategy.rs:104-119` against `skinny/crates/ir/src/lib.rs:340`; state whether StructDirect is absorbed as a shape arm or replaced; no IR edit by T-P1. |
