# CH3 REGRESSION — SK-V18 T-P2 V5 (cycle V5)

Lens: **CH3 REGRESSION.** No dossier grounds a route already refuted in
`skinny/REDRESS.md` as viable; the rejected-route ledger is honoured; a
"promising" direction REDRESS has already falsified is a REJECT; a grounding
that ignores a directly-relevant ledger prior (refuting *or* admitting) — or
asserts "no relevant ledger prior" without the verifiable negative-witness — is a
REVISE. Spot-verify the most load-bearing citations against the live ledger and
the live (post-cutoff) external sources.

Target packet (the SK-V18-regenerated dossiers, `generated_at` 2026-06-01,
re-read this pass — 2A modified 19:35, AFTER the V4 hardening dir mtime 19:32, so
the prior V4 verification is NOT trusted):

- `restart/audit/totality/p2/2A-sota-landscape.md`
- `restart/audit/totality/p2/2B-primitive-vocabulary.md`
- `restart/audit/totality/p2/2C-grammar-neutrality.md`
- `restart/audit/totality/p2/2D-cost-model.md`
- `restart/audit/totality/p2/2E-host-arch-esoterica.md`
- `restart/audit/totality/p2/2F-parse-that-gaps.md`

Disposition: **REVISE** (does not block clean convergence on its own — the one
residual defect is ledger-anchoring DRIFT, NOT a refuted-route revival: no route
REDRESS has falsified is grounded as viable; every load-bearing REDRESS + external
citation verifies verbatim).

## What this cycle turns on — independent re-verification from scratch

Per the CH3 confabulation contract I did NOT trust the V4 cycle's verification.
Every load-bearing REDRESS line was re-read verbatim against the live ledger,
every load-bearing local route claim re-checked in-tree, and the two NEW /
post-cutoff external citations re-fetched live. The headline result: **every
load-bearing REDRESS citation, every local route claim, and both external
citations (Pratt-1973, Lemire-2026) verify faithful — and NO dossier revives a
REDRESS-refuted route.** One genuine residual ledger-anchoring drift surfaces on a
fresh adversarial read (CH3-V5-S1, on 2D), described below; it is the V4 CH3
disposition row #13 over-attributing a negative-witness that is ABSENT from the
regenerated 2D.

Per-dossier `REDRESS N` census (this regen, first-digit grep): 2A=9, 2B=8, 2C=0,
2D=0, 2E=14, 2F=10. **2D dropped to 0 `REDRESS N` tokens** — the R-A relocated-seam
un-fork carries its anti-regression grounding entirely against iburg LOPLAS 1992 +
Aho Ch.8 + SPEC §5-risk-1, with the firewall/R16 co-gate as the binding distinction.
This is correct as far as it goes (the un-fork has no genuine REDRESS prior), BUT
the V4 CH3 verdict claimed "2D states grep == 0 for a relocated-seam/
RuntimeEmitterKind/un-fork ledger entry" — that inline negative-witness is NOT in
the regenerated 2D (CH3-V5-S1). 2C carries its REDRESS coverage as prose carry-forward
(refutation-heavy neutrality dossier; it refutes — does not revive — CSS-only /
generator-sidecar / generic-grammar-switch / 9-ident-leak routes).

## Spot-verification of the load-bearing citations (CH3 confabulation check)

I read the cited REDRESS sections verbatim and re-fetched the post-cutoff + NEW
external citations live. **No confabulated line number, no misrepresented verdict,
no fabricated metric.** A route grounded on a misread ledger verdict (citing a
RETIRED route as a PASS-ADMIT precedent, or inventing a DOI) would be a REJECT.
None found.

REDRESS ledger (read verbatim against `skinny/REDRESS.md` this pass — exact
line numbers confirmed by grep):

- **REDRESS 96/97/98** — VERIFIED. Section "SK-V9 Wave 3 Union Event-Model
  Class-Column Redress" starts at `:2795` (dossiers cite `:2795`-`2940` ✓);
  Item 98 retires `G-W3-UNION-SUBSTRATE` at `:2910`; the load-bearing finding
  "on the M5 Max wide-issue core, the scalar `consume_structural`/delimiter path
  … is cheaper than materializing or streaming a SIMD structural cursor through
  retained parsing" is at `:2928` (dossiers cite `:2928`-`2933` ✓); "`G-W3-UNION-
  SUBSTRATE` is therefore retired, not merely blocked" at `:2934`. Both faithful
  implementations were correctness-green and missed every must-improve row. Verbatim
  faithful to what 2A/2B/2D/2E/2F quote.
- **REDRESS 126** — VERIFIED. Item 126 at `:3768`, `G-W4-ASM-GEN-CONSUMER` =
  `ROUTE-PRODUCTION-SPLIT` (`:3769`); the `find_ascii_set_member64(…, b"{};")`
  microbench (`:3775`) speedup ratio is `4.718279341` (`:3798`,`:3866`) (dossiers
  say "4.7×" — faithful); a microbench PASS is NOT a production-row move. Cited
  `:3766`-`3805` ✓.
- **REDRESS 144** — VERIFIED. Item 144 at `:4420` (cited `:4418`-`4438` ✓),
  `G-W12-SIMD-ASM-PRODUCTION` = `PASS-ADMIT`; `find_ascii_set_member64` wired into
  generated CSS L4 `Scanner::scan_block` delimiter search; Track 1 `444.208` Mbps
  versus prior `434.1316520595916` Mbps; Criterion `+109.87%`; "strict equality
  versus cssparser and lightningcss remained green." Dossiers say "444.2 vs 434.1,
  +109.87%" — faithful.
- **REDRESS 50/51/53** — VERIFIED. The admissible-side line is verbatim at
  `:808`-`812`: "structural projection must be the parser's single substrate, not a
  second scanner bolted onto source-byte recursive descent … or a `CollapsedStage`
  / `SinkOnly` lowering consumes live masks in the same loop. A `ParserState`-owned
  structural cursor over source bytes is non-canonical." This is the exact
  admissible-transient (same-loop mask) vs inadmissible-retained (cursor) line that
  2A JSON-001 / LAC-04 / 2B-A2 / 2C inner-kernel-split / 2F all cite (`:807`-`813` ✓).
- **Rejected-route keep-intact list** (`:1214`-`1220`) — VERIFIED. The ledger
  binds as non-canonical: "structural-index typed parser prepass, NEON no-escape
  string matcher, separator elision, generic SWAR whitespace, … parse-time
  projection side tables, byte-class whitespace EventCursor wrappers,
  parser-local structural-mask cursors, and sink-local decoded hash helpers."
  Grep of all six dossiers for "NEON no-escape string matcher / separator elision /
  generic SWAR whitespace" == 0 — NONE is grounded as viable.

External citations (live re-fetch this pass — CH3 confabulation guard, especially
on the NEW-this-cycle and post-cutoff sources):

- **Pratt "Top Down Operator Precedence", POPL 1973, DOI 10.1145/512927.512931**
  (2C `SK-V18-2C-SHEETS-PRECEDENCE-TOWER-NEGATIVE-CONTROL`, `2C:216`,`:269`) —
  VERIFIED real and faithful. WebSearch confirms the DOI resolves to "Top down
  operator precedence" in the Proceedings of the 1st annual ACM SIGACT-SIGPLAN
  Symposium on Principles of Programming Languages (POPL 1973), Vaughan Pratt — the
  technique "combines the best properties of Recursive Descent and Floyd's Operator
  Precedence." Grounds the Sheets precedence-tower negative control faithfully. Not
  confabulated.
- **Lemire-2026-MATCH** (2A/2E svmatch refutation + CH4-V3-03 attribution fold,
  `https://lemire.me/blog/2026/04/19/the-fastest-way-to-match-characters-on-arm-processors/`)
  — VERIFIED real and faithful (WebFetch, post-cutoff). Confirms: "It might be that
  the SVE2 function `match` is the fastest way to match characters on ARM processors"
  (16.0 GB/s); "Apple has not yet adopted SVE2"; the conventional NEON route is the
  table-driven Langdale/Lemire (2019) classifier; the `vceqq_u8` eq-fan is "presented
  as an alternative … not as the original established technique" (the commenter route).
  Grounds 2E's NEON-svmatch refutation on the SVE2-absent M5 Max AND the CH4-V3-03
  attribution on solid ground.

(The canonical citations — simdjson/Langdale-Lemire VLDBJ 2019 arXiv:1902.08318,
iburg LOPLAS 1992 DOI 10.1145/151640.151642, egg POPL 2021 DOI 10.1145/3434304,
Mison VLDB 2017 DOI 10.14778/3115404.3115416, FFmpeg/dav1d checkasm — are
textbook-canonical and were verified faithful across V1/V2/V3/V4; no change this cycle.)

Local route-reality (CH3 cares whether the grounded route stands on real ground vs
a phantom — re-checked in-tree this pass):

- The eq-set / bracket-depth kernels are LIVE-consumed as TRANSIENT per-call carries
  by `count_top_level_commas` (`runtime_simd.rs:29`,`:44`,`:47`,`:56`): the comment at
  `:27` is "Bit-identical to the scalar reference … one call"; the i32 depth threads
  within one call, never a retained side-array — the **admissible same-loop side** of
  REDRESS 50/51/53, NOT the retired retained-cursor side. The 2F/2B "live-wired
  transient, never a retained side array" framing is real.
- `find_css_significant` is dead/test-only (`runtime_simd.rs:169`; sole caller
  `lib.rs:574` under `#[cfg(test)]`) — the R7 "retarget not wire-as-is" framing is real.
- `find_ascii_set_member64` has NO non-test runtime/codegen caller (grep over
  `runtime/src` + `codegen/src` == 0) — the 2B/2C/2F "wrapper, NOT a live JSON
  consumer; the JSON `scan_dispatch` rides `byte_class_from_table_64`" framing is real.
- The eq-set NEON body's movemask attribution ("Lemire + Mula … AArch64 movemask
  spill", `byte_class_from_eq_set_64.rs:63`-`64`) is VERIFIED in-tree — 2B's
  `neonmovemask_addv` grounding is faithful, not confabulated.
- Upstream `parse-that` is absent from `skinny/Cargo.toml` (only `parse-that-regex`
  is a member, `:10`/`:31`) — 2F's provenance claim is real.
- The stale `runtime_simd.rs:6`-`7` source comment IS present ("is the same kernel
  JSON's `scan_structurals` rides") — and the dossiers (2B/2C/2F) themselves REFUTE
  it and route it to a same-wave G6 source-fix. The eq-set neutrality rests on the
  STRUCTURAL caller-data argument, not the refuted dual-consumer one — a refuted claim
  being refuted, NOT grounded. Not a CH3 defect (CH2/CH4 own source-comment hygiene).
- 2D HEAD-supersession is real and correctly framed: `NormalizeDirectSinkCost` live,
  `collapsed_stage.rs` delegates to `tape_plan::render_rule(.., Collapsed)`, the
  `RuntimeEmitterKind{CompiledLowering,RequestFacts}` fork live — the R-A un-fork
  RELOCATES that fork's consumer; it is a SK-V18-NOVEL relocated-seam move. **HOWEVER**
  the regenerated 2D no longer carries the inline ledger-negative-witness V4 attributed
  to it (CH3-V5-S1 below).

## Critical findings (residual CH3 defects)

| id | severity | dossier | finding | fold requirement |
|---|---:|---|---|---|
| CH3-V5-S1 | low | 2D | **The R-A relocated-seam un-fork is framed "DERIVED-not-new / RELOCATES the discriminator / no REDRESS id owed" but the regenerated 2D carries NO inline ledger-negative-witness for that claim.** The V4 CH3 disposition (row #13) asserted "2D states grep == 0 for a relocated-seam/RuntimeEmitterKind/un-fork ledger entry; the V3 cycle independently confirmed 0 ledger hits" — but a fresh grep of the regenerated 2D for `grep == 0 \| no REDRESS prior \| SK-V18-novel \| 0 ledger hits` returns **0 occurrences** (2D `REDRESS N` census == 0 entirely). 2D grounds R-A solely against iburg LOPLAS 1992 + Aho Ch.8 + SPEC §5-risk-1 (rows `:55`,`:67`,`:69`,`:79`,`:96`). The un-fork is the single SK-V18 route most ADJACENT to the substrate/discriminator ledger family (REDRESS 96/97/98 retires a substrate cursor; R-A relocates an *emitter* discriminator — genuinely a different object, which I independently confirmed is NOT a refuted-route revival). But under the CH3 lens, a grounding that claims "no relevant ledger prior" for a route adjacent to a retired ledger family must carry the verifiable negative-witness ON THE DOSSIER, not rely on a prose "DERIVED-not-new" assertion the prior cycle's verdict mis-attributed. As written, a downstream consumer reading 2D alone cannot confirm the un-fork owes no REDRESS id — the negative-witness lives only in the (now-stale) V4 CH3 verdict text. | Carry an inline one-clause witness on the R-A grounding row (or the LAC-2D-V3-01/02 rows): the relocated-seam/un-fork/`RuntimeEmitterKind` route has ZERO REDRESS ledger entry (`rg -n 'relocated.seam\|RuntimeEmitterKind\|un.fork' skinny/REDRESS.md == 0`), so R-A is SK-V18-NOVEL and owes no ledger id — and distinguish it explicitly from REDRESS 96/97/98 (which retires a *substrate cursor*, a different object than an *emitter discriminator*). Cosmetic ledger-anchoring; does not change the route's viability — R-A remains genuinely novel and correctly grounded against iburg + §5-risk-1. |

## Per-item CH3 disposition ledger

Enumeration of every grounding/refutation under the CH3 lens (route-viability vs the
REDRESS ledger). 16 items; 1 REVISE.

The V1-cycle ≥30% REVISE expectation does NOT bind a re-converged cycle whose
predecessor (V4) CH3 defects are folded-and-present and whose every load-bearing
REDRESS citation + local route claim + external citation (Pratt-1973, Lemire-2026)
verifies faithful. CH3's contract is explicit: a REVISE must name a REAL missing/wrong
ledger prior (here: a missing inline negative-witness the prior verdict falsely claimed
was present); a REJECT must name a confabulated citation or a refuted-route revival.
Forcing the count to ≥30% would require manufacturing defects against
faithfully-folded, citation-verified groundings — which the lens forbids. A fresh
adversarial read surfaces exactly ONE genuine ledger-anchoring drift (CH3-V5-S1) and
NO refuted-route revival; the honest count is 1 REVISE, consistent with the converging
V1→V2→V3→V4→V5 REVISE sequence (5 → 3 → 2 → 0 → 1, the V5 uptick being the regen's
2D-census drop from 4 to 0 surfacing a witness the V4 verdict over-attributed).

| # | dossier · item | route grounded/refuted | CH3 verdict |
|---|---|---|---|
| 1 | 2A · T2A-V1-SOTA-JSON-001 (transient structural projection → same-loop masks into ONE substrate) | admissible side of REDRESS 50/51/53; names `:807-813` by id on the grounding row itself (V3-S1 fold present in regen) | ACCEPT |
| 2 | 2A · LAC-04 (retained cursor/list/class-column/sidecar refuted) | names REDRESS 96/97/98 `G-W3-UNION-SUBSTRATE` + 50/51/53 by id + SK-V18 SPEC `:397-402` | ACCEPT |
| 3 | 2A · refutation #5 (simdjson stage1 ≠ retained class columns) | cross-references LAC-04's REDRESS 96/97/98 + 50/51/53 by id | ACCEPT |
| 4 | 2A · T2A-V18-ASMJSON-001 (NO JSON SIMD classifier; G5 neutralizes `json/scan.rs`, scan-free product path) | profile-first; honours scan-free JSON product path; no route revival | ACCEPT |
| 5 | 2A · T2A-V18-DAV1D-001/002 (checkasm PROCESS, PASS≠speedup) | process-discipline grounding; no route revival | ACCEPT |
| 6 | 2A · CSS broadcast / fact-stream / `CssFullParseSummary` REFUTE-CSS-001..004 | refutes contrived CSS parity; consistent with overfit audit | ACCEPT |
| 7 | 2B · six scalar-backed families; LD4/PMULL/CSSC/SVE2/FSM citation-only refuted | mirrors Lock-16 orphan-kernel discipline | ACCEPT |
| 8 | 2B · SKV18-A2 find_component_delim retarget + OQ-2B-SKV18-01 | viable + REDRESS-144-precedented; OQ + A2 LEDGER-FRAMED (96/97/98/126 + 50/51/53 admissible side) inline | ACCEPT |
| 9 | 2B · SKV18-A5 FSM/frame-stack DELETE-only reconcile | catches the latent retained-frame-stack (refuted sidecar) reintroduction; routes to DELETE; carries the Lock-1 transient-per-call-FSM fence (2D LAC-2D-V3-04) | ACCEPT |
| 10 | 2B · `balanced_component_scan` → `css_balanced_component_scan` forced demotion | neutrality fence; no ledger regression | ACCEPT |
| 11 | 2C · JSON-only / CSS-only / `CSS_GENERATED_RS` / generator-sidecar / generic-grammar-switch / 9-ident-leak / css_types-shim refuted; delete-before-provider (NEW-CH3-V5-01 wave-graph honoured: deletion blocked until same-wave provider lands) | honours wave-graph fence + refuted-route ledger; no revival | ACCEPT |
| 12 | 2C · typed CSS value/document provider as grammar-derived receiver; Sheets/BBNF-self anti-JSON falsifiers; eq-set neutrality SPLIT (base one-fan structural-neutral, two-fan composition CSS-only forced-demote) | admissible-after-gate; the stale dual-consumer JUSTIFICATION line is REFUTED + routed to same-wave source-fix, not grounded; consistent with REDRESS CSS provider route | ACCEPT |
| 13 | 2D · R-A un-fork on cost-derived BackendShape; STALE V2 zero-rule/marker verdicts superseded at HEAD | HEAD-verified supersession (NormalizeDirectSinkCost live, lowerers real); relocated-seam is a SPEC §5-risk-1 NOVEL move correctly grounded against iburg — BUT the regenerated 2D carries NO inline ledger-negative-witness for the "no REDRESS id owed" claim the V4 verdict over-attributed | **REVISE** (CH3-V5-S1) |
| 14 | 2D · CollapsedStage as branchless staged FSM (UNKNOWN-2D-V3-04 / LAC-2D-V3-04) | the streamed-cursor class REDRESS 96/97/98 retired; LEDGER-FENCED inline by id (`:2795-2944`,`:2928-2933`) | ACCEPT |
| 15 | 2E · G6 two-fan eq-set skip (WIRE) + AA + OQ-4 + svmatch refute + Movemask64Shrn/Interleave4Classify net-win fences | viable + ledger-anchored (144 precedent, 96/97/98/126 cautionary); svmatch refuted on verified Lemire-2026; both NEW upgrade rows carry the inline net-win fence (V3-S2 fold present in regen) | ACCEPT |
| 16 | 2F · PTG-2F-11 `bracket_depth_mask_64` + PTG-2F-13 G6 retarget + bracket_depth-replaces-recursion OQ | all carry the INLINE LEDGER FENCE (REDRESS 96/97/98 + 144 + 126) (V1-S2 + V2-S2 folds present in regen) | ACCEPT |

## Evidence inspected

- Lens contract: `restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md`;
  prior CH3 verdicts `…/V1/CH3.md`, `…/V2/CH3.md`, `…/V3/CH3.md` (accept=14 revise=2),
  `…/V4/CH3.md` (accept=16 revise=0); `…/T-P2-DISPATCH-CONTEXT.md` (NEW-CH3-V5-01
  wave-graph / deletion-before-rebuild).
- Rejected-route ledger read verbatim: `skinny/REDRESS.md` — REDRESS 96/97/98
  (`:2795` section start, Item 98 retirement `:2910`, finding `:2928`, retired-not-
  blocked `:2934`); REDRESS 126 (Item 126 `:3768`, `ROUTE-PRODUCTION-SPLIT` `:3769`,
  ratio `4.718279341` `:3798`/`:3866`, `find_ascii_set_member64(…, b"{};")` `:3775`);
  REDRESS 144 (Item 144 `:4420`, `PASS-ADMIT`, `444.208`/`434.1316520595916`,
  `+109.87%`, `Scanner::scan_block` `:4431`); REDRESS 50/51/53 (admissible single-
  substrate / same-loop-mask line `:808`-`812`); keep-intact rejected-route list `:1214`.
- All six dossiers read in full (2A 281L, 2B 437L, 2C 377L, 2D 145L, 2E 252L, 2F 197L).
- Local route-reality re-checked in-tree: `runtime_simd.rs:6,27,29,44,47,56,169,199`
  (transient eq-set/bracket carry + stale comment + dead `find_css_significant`);
  `runtime/src/lib.rs:574` (test-only caller); `find_ascii_set_member64` 0 runtime/codegen
  callers; `byte_class_from_eq_set_64.rs:63-64` (Lemire+Mula movemask attribution);
  `skinny/Cargo.toml:10,31` (parse-that-regex member, upstream parse-that absent);
  2D grep for inline ledger-negative-witness == 0 (CH3-V5-S1).
- Citation spot-verification (WebSearch/WebFetch, this pass): Pratt "Top Down
  Operator Precedence" POPL 1973 DOI 10.1145/512927.512931 confirmed real (NEW 2C
  Sheets negative-control citation); Lemire-2026-MATCH confirmed live and faithful
  (SVE2 `match` fastest 16.0 GB/s, Apple lacks SVE2, conventional route is the
  table-driven classifier, eq-fan is the commenter route).
- Per-dossier REDRESS `REDRESS N` census (this regen): 2A=9, 2B=8, 2C=0, 2D=0, 2E=14,
  2F=10 (2D dropped to 0 — the CH3-V5-S1 trigger).

## Convergence impact

This issue **does not block clean convergence on its own.** No dossier revives a
REDRESS-refuted route: the G6/R-F eq-set retarget is the REDRESS-144-precedented,
transient-same-loop ADMISSIBLE side of the REDRESS 50/51/53/96/97/98 line (NOT the
retired retained-cursor side); 2C refutes (does not revive) the `CSS_GENERATED_RS`/
generator-sidecar/generic-grammar-switch/9-ident-leak routes and honours the
delete-before-provider wave-graph (NEW-CH3-V5-01); 2B SKV18-A5 catches and
routes-to-DELETE the latent retained-frame-stack (refuted sidecar) reintroduction;
2E refutes NEON-svmatch on the verified Lemire-2026 SVE2-absent finding; 2D's R-A is
a SK-V18-novel un-fork with no REDRESS prior to honour (relocated-seam fenced by the
firewall + R16 co-gate), and its supersession is an HEAD-evidence update of a prior
*finding*, not a ledger-retired route.

Every load-bearing REDRESS citation, external citation (including the NEW Pratt-1973
DOI and the post-cutoff Lemire-2026 post), and local route claim verified faithful.

The single residual REVISE (CH3-V5-S1) is ledger-anchoring DRIFT, not a regression:
the regenerated 2D grounds the R-A relocated-seam un-fork as "DERIVED-not-new, no
REDRESS id owed" but no longer carries the inline negative-witness (`rg
relocated-seam|RuntimeEmitterKind skinny/REDRESS.md == 0`) the V4 CH3 verdict
attributed to it — so a reader of 2D alone cannot confirm the un-fork (a route adjacent
to the retired REDRESS 96/97/98 substrate family) owes no ledger id. Folding S1 attaches
the verifiable zero-ledger-hit witness to the R-A grounding and explicitly distinguishes
an *emitter discriminator* relocation from a *substrate cursor* retirement, closing the
last path by which a downstream consumer could mistake the novel un-fork for an
under-examined adjacent-to-refuted route. It does not retract a viable route — R-A
remains genuinely novel and correctly grounded against iburg + SPEC §5-risk-1.

TALLY accept=15 revise=1 reject=0
