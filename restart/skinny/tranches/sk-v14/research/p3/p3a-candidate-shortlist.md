# SK-V14 P3-A: Candidate Shortlist

Pass: S-P3 Synthesis-Plan. Cycle: V1.
Date: 2026-05-23.
Scope: distil the S-P2 §3Z LOCK candidate pool (V3 HEAD `ebe84954b`) into ≤8 SK-V14 candidate interventions; each carries owner path + scalar-ref status + checkasm-parity status + same-wave consumer NAMED + falsifiability gate sketch + P2 ID + S-P1 antecedent + CF-3 3-gate admission cell + F-V2-P1ABC-RERECORD dependency flag.
Output: this file.
Pass Alpha goalset: SK-V14 SYNTHESIS §0.1 R10 close-condition (every JSON cell 51 = 17×3 AND every CSS L4 feature 24 non-OUT_OF_SCOPE ADMITs > strict-vs-strict on same plane/corpus/equality semantics — or carries architectural-block proof); SYNTHESIS §0.2 audit-zero baseline (JSON parse_only 0/17, JSON direct 0/17, JSON typed 0/17, CSS L4 0/24); SYNTHESIS §0.3 R1-R10 acceptance criteria; SYNTHESIS §0.4 P-1..P-7 pre-blocks.
Candidate pool: research/p2/ post-V3 LOCK survivors per `hardening/HARDENING-S-P2-V3-CONSOLIDATED.md §6` carry-forward; demotions P2-C C-P2C-1/-6/-7 + P2-D C-P2D-3 + P2-F C8 excluded per §0.3 §3Z LOCK ratification.

## §1 — Synthesis (concrete; cites P1 row, P2 candidate, REDRESS entry, goalset line)

### §1.1 — Distillation discipline (binding)

The S-P2 LOCKED candidate pool (post-V3) carries 39 active candidate-slots across the six axis files plus 6 demoted-to-§2.X non-candidate inventory entries:

- **P2-A (V1-LOCKED, 7/7 grammar-neutral):** C1 `lazy_field_skip_with_index`; C2 `long_string_body_simd_scan`; C3 `digit_block_simd_accumulate`; C4 `force_inline_lto_envelope_discipline`; C5 `structural_index_singular_substrate_consumer`; C6 `parse_attribution_envelope_cracker`; C7 `unicode_escape_neon_nibble_decode` (per `research/p2/p2a-sota-teardown.md:184-196 §2.1`).
- **P2-C (V2-amended, 5 active + 3 demoted):** C-P2C-2 `pmull_cssc_structural_union_emit64` (PRE-BLOCKED REDRESS-88/89/96-98); C-P2C-3 `udot_digit_span_x4` (NOT-S-P3-ELIGIBLE V1 — re-eval gate on F-V2-P1ABC-RERECORD); C-P2C-4 `tbl_tbx_escape_decode_batch` (S-P3-ELIGIBLE JSON-fixed-width route); C-P2C-5 `string_special_64_context` (conditional support); C-P2C-8 `parse_attribution_profile_rebuild_gate` (process-gate prerequisite). Demoted §2.X: C-P2C-1/-6/-7.
- **P2-D (V2-amended, 2 active + 1 demoted + 1 pre-blocked):** C-P2D-1 `BackendShape::SinkOnly` activation; C-P2D-2 `OffsetTapeStats` column extension. Demoted §1.6(d): C-P2D-3; pre-blocked: C-P2D-4 (EventTape — REDRESS 96/97/98).
- **P2-E (V1-LOCKED, 9 Layer-1 primitive gaps):** Gap 1 `scan_string_special_block_sweep_64`; Gap 2 `unescape_uxxxx_x8_neon`; Gap 3 `ascii_whitespace_skip_64`; Gap 4 `validate_block_streaming`; Gap 5 `parse_16_digits_dotprod`; Gap 6 `scan_string_with_carry_64`; Gap 7 `scan_digit_run_simd_64`; Gap 7.5 `byte_class_from_range_64`; Gap 8 `utf8_codepoint_scan_64`.
- **P2-F (V3-amended, 13 active + 1 demoted):** C1 structural-byte SIMD classify; C2 quoted-string boundary scan (PMULL prefix-XOR); C3 escape canonicalisation; C4 tiny-keyword-set match; C5 digit-block number decode; C6 branch-on-first-byte dispatch; C7 leading-whitespace prefix skip; C9 offset-tape bulk emit; C10 cross-chunk byte-context propagation; C11 substrate-walk-with-shape-validation; C12 keyword-set 16-byte alphabet membership; C13 branchless-3-way XOR (BCAX); C14 i-cache-resident force-inline ceiling. Demoted §2.X.1: C8 (comment-skip).

The PASS-3-SYNTHESIS-PLAN.md §2 P3-A row caps shortlist at **≤8 candidate interventions**. Distillation strategy per the carry-forward packets at HARDENING-S-P2-V3-CONSOLIDATED §6:

1. **NF-CH6-4 canonical-name binding (§6.2):** P2-A C2 + P2-E Gap 1 + P2-F C1+C2 consolidate to ONE canonical primitive (long-string-body SIMD scan) per the cross-axis tracking note at `p2f:231-239` — NOT three orthogonal SIMD bodies for one primitive.
2. **CF-3 3-gate admission discipline (§6.1):** every shortlist entry carries explicit (scalar-ref status / checkasm-parity expectation / same-wave-consumer NAMED) cell — promoted from S-P2 §4 documentation-discipline to S-P3 wave-program admission-gate.
3. **F-V2-P1ABC-RERECORD Stage-0 binding (§6.3):** 12 consumer-dependency primitives (P2-A C6 + P2-C C-P2C-3/-8 + P2-E Gap 1/3/4/5 + P2-F C6/C7/C10/C12/C13) must ship F-V2-P1ABC-RERECORD as Stage 0 of any admitting wave — the dependency flag column below identifies dispatch-envelope-internal candidates.
4. **No-deferrals (per `[no-deferrals]`):** every primitive lands its hot-path consumer in the same commit; P3-A names the consumer per candidate; P3-C gates on it.
5. **Substrate-union honesty (Lock 1 + CH5):** every candidate carries explicit substrate-target = `existing_tape` / `local_temp_only` / `direct_sink` / `admitted_fact_output` (not a new substrate, not a parallel scan, not a retained sidecar).

### §1.2 — Hot-leaf antecedent map (S-P1 → P2 candidate convergence)

Per `research/p1/p1e-hot-leaf-attribution.md §2.1-§2.4` the SK-V14 dominant hot leaves cluster around six envelopes/primitives:

| S-P1 hot leaf | Rank / % self-time | P2 candidates that serve it | Shortlist consolidation target |
|---|---|---|---|
| `dispatch_value` (`generated.rs:45`) | parse_only rank-1 on 13/17 at 95-100 % | P2-A C6 + C4; P2-C C-P2C-8; P2-F C6 + C14 | C5 (F-V2-P1ABC-RERECORD Stage-0) + C6 (force-inline LTO) |
| `parse_object_value_at_direct::<S>` (`generated.rs:466`) | direct rank-1 on 8/17 at 19.5-85.3 % | P2-A C1 + C5; P2-D C-P2D-1; P2-F C11 | C2 (substrate-union typed-skip) + C8 (SinkOnly activation) |
| `parse_array_element_at_direct::<S>` (`generated.rs:506`) | direct rank-1 on 6/17 at 37-85 % | same set | C2 + C8 |
| `DirectParser::skip_value` (`generated_real_typed.rs:2949`) | typed rank-1 on 5/7 at 39-76 % | P2-A C1 + C5; P2-F C11 | C2 (substrate-union typed-skip) |
| `parse_that_regex::unescape_string` (`parse-that-regex/src/lib.rs:718`) | direct rank-1 on `unicode_escapes` at 46.7 % | P2-A C2; P2-E Gap 1 + Gap 6; P2-F C1+C2 | C1 (long-string SIMD canonical) |
| `parse_that_regex::read_hex_unit_scalar` (`parse-that-regex/src/lib.rs:945`) | parse-only rank-1 on `y_string_unicode` at 100 % | P2-A C7; P2-C C-P2C-4; P2-E Gap 2 | C4 (unicode \uXXXX decode) |
| `match_tiny_plain_string_with_cap::<16>` (`generated.rs:169`) | parse-only rank-1 on `distinct_values` at 96.3 % | P2-F C4 | (folded into C1 long-string SIMD canonical — same primitive shape with smaller alphabet) |
| `materialize_f64`/`u64` (`parse-that-regex/src/number/mod.rs:261, 247`) | direct rank-2 on canada (14.32 %), mesh (6.09 %), marine_ik (5.54 %); serde_json `parse_decimal` 28-59 % typed Track 2 | P2-A C3; P2-C C-P2C-3; P2-E Gap 5 + Gap 7 + Gap 7.5; P2-F C5 | C3 (digit-block SIMD accumulate) |
| `skip_ascii_whitespace` (`parse-that-regex/src/lib.rs:113`) | every JSON value-position prelude; envelope-masked | P2-E Gap 3; P2-F C7 | C7 (ASCII whitespace SIMD skip) |
| `OffsetTapeStats` substrate-measurement plumbing | substrate-regression guard | P2-D C-P2D-2 | C8 (folded — substrate measurement IS the SinkOnly admission gate) |

Eight consolidated candidates emerge. The S-P1 hot-leaf coverage is total: every rank-1 leaf on every plane has at least one shortlist candidate naming it as antecedent.

### §1.3 — Pre-blocked-route discipline (CH3 binding)

Per SYNTHESIS §0.4 P-1..P-7 + S-P0 SYNTHESIS-AUDIT-OVERFIT.md, each shortlist entry below carries a REDRESS-pre-block check. The pre-block surface inherited from S-P2 cohort: REDRESS 28+33 (Class A NEON tiny-string), 50-55 (SK-V5 UTF-8 fusion), 60-72 (SK-V6 retained-parse + sidecar producers + digest cap-16), 80 (canada mantissa-widen), 82-84 (single-quartet unicode classifier / StringBlock16 tiny probe / object-pair compaction), 88 (PMULL prefix-XOR as hot body), 89 (CSSC CTZ next-bit bulk consumer), 96/97/98 (retained class-column / streaming structural cursor / class-lane-only), 126 (PEXT mask plan; aarch64 has no PEXT). Each shortlist entry carries the explicit fresh material differential where it abuts a pre-block.

### §1.4 — Strictness-plane discipline (R1 binding)

Per SK-V14 R1 + P-2: every candidate's SOTA-beat target is the **plane-correct** strict comparator. `sonic_rs::from_slice::<Value>` (eager DOM) is NEVER an admission anchor on parse_only / direct / typed. Per `skinny/RESULTS.md:5-46` row schema: parse_only anchors against sonic-rs Skipper-class (Skipper-class API; serde_json `IgnoredAny` is the in-tree strict baseline); direct against `sonic_rs::from_slice::<MyCorpusStruct>`; typed against per-corpus typed struct deser. Each shortlist's falsifiability gate names the comparator column from RESULTS.md.

## §2 — Deliverable (the ≤8 candidate shortlist)

Eight candidate interventions, each with the CH4 3-gate admission cell + the §6 carry-forward bindings.

### Candidate C1 — `long_string_body_simd_scan` (canonical name; NF-CH6-4 consolidation)

- **P2 candidate IDs (consolidated per §6.2):** P2-A C2 `long_string_body_simd_scan` (`p2a:184` row) ∪ P2-E Gap 1 `scan_string_special_block_sweep_64` (`p2e:97-110`) ∪ P2-F C1 structural-byte SIMD classify + C2 quoted-string boundary scan (`p2f:68-88`). One canonical primitive across three convergent identifiers per the §2.Y cross-axis tracking note at `p2f:231-239`.
- **Canonical owner path:** `crates/bbnf-simd/src/aarch64/string_block.rs` (existing `scan_string_special_block` at `:57` extends with a 64-byte sweep wrapper); composes existing `bbnf-simd/src/aarch64/classify_tbl4.rs:22` + `bbnf-simd/src/lib.rs:175` `escape_mask_64` + `bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:1`.
- **Scalar-reference status (3-gate cell a):** PRESENT — `scan_string_special_block_scalar` at `crates/bbnf-simd/src/aarch64/string_block.rs:31` (executable-verified at HEAD via `grep -n "fn scan_string_special_block" skinny/crates/bbnf-simd/src/aarch64/string_block.rs`); the 64-byte sweep scalar reference is a 4-iteration OR-fold of the existing 16-byte scalar per P2-E Gap 1 reference sketch at `p2e:101`. Quote-aware composition scalar reference is `scan_structurals_scalar` at `crates/runtime/src/grammars/json/scan.rs:32` (executable-verified at HEAD) composed with the existing `bitmap_prefix_xor_64_scalar` at `crates/bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:1` (executable-verified at HEAD).
- **Checkasm-parity status (3-gate cell b):** EXTENSION required — checkasm matrix at `crates/bbnf-simd/tests/` adds `checkasm_scan_string_special_block_sweep_64.rs` (sibling shape to existing `checkasm_bitmap_prefix_xor_64.rs`, `checkasm_bulk_emit_positions_64.rs` at HEAD). Matrix coverage: all alignments, escape positions, control positions, tails 0-63, long ASCII, high-bit bytes, grammar-specific terminators (JSON `"` vs CSS `'`/`"` vs BBNF `'`/`"`/`` ` ``).
- **Same-wave consumer NAMED (3-gate cell c):** `parse_that_regex::skip_string_plain_trusted` at `crates/parse-that-regex/src/lib.rs:547` (long-string body inside `parse_object_value_at_direct` envelope at `crates/runtime/src/grammars/json/generated.rs:466` and `parse_array_element_at_direct` at `:506`); same-wave row movement on `unicode_escapes` direct + `twitter` direct + `gsoc-2018` direct + `mesh` direct + `github_events` direct.
- **S-P1 antecedent:** `parse_that_regex::unescape_string` direct rank-1 on `unicode_escapes` at 46.7 % self-time (`research/p1/p1e-hot-leaf-attribution.md §2.2`); 17/56 rank-2/3 leaves in P1-B direct-plane table. Also serves `match_tiny_plain_string_with_cap::<16>` parse-only rank-1 on `distinct_values` at 96.3 % (P1-E §2.1) via small-alphabet specialisation.
- **Falsifiability gate sketch:** `unicode_escapes` direct_to_struct Mbps ≥ `SK-V14-open` Track 1 + 15 % AND sonic-rs strict + 1 % (current `unicode_escapes` direct_to_struct: Track 1 5523, Track 2 5431, sonic-rs strict 14298, serde_json 5467 per `skinny/RESULTS.md:42`); `twitter` direct_to_struct Mbps ≥ `SK-V14-open` + 10 % (current Track 1 11908, sonic-rs strict 15110); full-table maintain ±1 % of `SK-V14-open` on the 17 corpus × 3 plane set.
- **Substrate target (Lock 1):** `local_temp_only` (the SIMD masks are transient producers; the offset emit folds into existing `compact_mask` at `crates/bbnf-simd/src/lib.rs:227`).
- **REDRESS pre-block check:** REDRESS 28+33 (Class A NEON tiny-string wiring) — fresh material differential: this primitive is the **long-string envelope consumer**, not a tiny-string probe replay (the 16-byte body at `crates/bbnf-simd/src/aarch64/string_block.rs:57` was Wave-1 admitted; the gap is the 64-byte sweep wrapper consumed by `parse_that_regex::skip_string_plain_trusted`). REDRESS 82-84 (single-quartet unicode classifier, StringBlock16 tiny probe, object-pair compaction) — fresh material differential: this is the long-string body scan, not a single-quartet classifier replay and not StringBlock16. REDRESS 88 (PMULL prefix-XOR hot body) — composes the existing scalar `bitmap_prefix_xor_64` whatever its internal body; no PMULL substitution.
- **F-V2-P1ABC-RERECORD dependency flag:** **YES** — dispatch-envelope-internal primitive per HARDENING-S-P2-V3-CONSOLIDATED §6.3 (P2-E Gap 1 in the 12-consumer-dependency list); the admitting wave MUST ship F-V2-P1ABC-RERECORD as Stage 0 (cargo build --release -p bbnf-bench --features runtime/parse-attribution; interactive samply record; cfg_attr flip verification at `generated.rs:33-237` 8 sites).

### Candidate C2 — `structural_index_singular_substrate_consumer` (substrate-union typed-skip)

- **P2 candidate IDs (consolidated):** P2-A C1 `lazy_field_skip_with_index` (`p2a:190 row`) ∪ P2-A C5 `structural_index_singular_substrate_consumer` (`p2a:193`) ∪ P2-D C-P2D-1 `BackendShape::SinkOnly` activation (`p2d:112-119`) — partially; the SinkOnly piece carved out into separate Candidate C8 below for direct-plane envelope vs typed-plane envelope ∪ P2-F C11 `substrate-walk-with-shape-validation` (`p2f:171-180`).
- **Owner path:** primary rewrite in `crates/bbnf-bench/src/generated_real_typed.rs:2949-3003` (the `DirectParser::skip_value` + `skip_object` + `skip_array` body); secondary consumer wiring in `crates/runtime/src/grammars/json/generated.rs:466,506` (the `parse_object_value_at_direct` + `parse_array_element_at_direct` direct envelopes); `StructuralIndex` producer is the existing `scan_structurals` at `crates/runtime/src/grammars/json/scan.rs:22` (single substrate; no new producer).
- **Scalar-reference status (3-gate cell a):** PRESENT — `scan_structurals_scalar` at `crates/runtime/src/grammars/json/scan.rs:32` (executable-verified at HEAD); the new consumer scalar reference is a positions-Vec walk advancing-past-current-cursor returning first position > cursor whose marker is in `{,`, `]`, `}}` at object-nest-depth 0. Equivalence proof: parse output (sink callbacks + tape positions emitted) byte-equivalent under both index-consuming vs byte-rescanning consumer.
- **Checkasm-parity status (3-gate cell b):** EXTENSION required — this is a substrate-consumer correctness gate, not a SIMD-body parity gate. Test surface: `crates/bbnf-bench/tests/` adds `skip_value_index_consumer_parity.rs` (byte-equivalent diff-test of sink callbacks + tape positions emitted under index-consuming `skip_value` vs current byte-rescanning `skip_value`); coverage matrix: all 17 corpora × {twitter shape, citm shape, canada shape, marine_ik shape}.
- **Same-wave consumer NAMED (3-gate cell c):** direct + typed envelopes in `parse_object_value_at_direct` (`generated.rs:466`), `parse_array_element_at_direct` (`generated.rs:506`), `DirectParser::skip_value` (`generated_real_typed.rs:2949`). Same wave, same commit (Lock 1 v+1 substrate-union manifest — substrate-target `existing_tape`, retention_lifetime `output_row`, policy_owner `generated_grammar`).
- **S-P1 antecedent:** `DirectParser::skip_value` typed rank-1 on 5/7 corpora at 39.5-76.1 % self-time (twitter 72.50 %, citm_catalog 76.12 %, marine_ik 41.70 %, github_events 41.70 %, mesh 39.5 % per `research/p1/p1e-hot-leaf-attribution.md §2.3`). Single largest typed-plane gap. Substrate-union finding `p1e §4.4` — DirectParser::cursor + parse_object_value_at_direct cursor are two structurally independent state machines; this candidate operationalises the union.
- **Falsifiability gate sketch:** `twitter` real_typed_struct Track 1 ≥ `SK-V14-open` + 20 % AND sonic-rs strict + 1 % (current Track 1 17898, sonic-rs strict 15502 per `skinny/RESULTS.md:7`); `citm_catalog` real_typed_struct Track 1 ≥ `SK-V14-open` + 15 % (current Track 1 36719, sonic-rs strict 22857 per `skinny/RESULTS.md:10`); full-table maintain ±1 % on the 17 corpus × 3 plane set; substrate-union honesty gate: parse output (sink callbacks + tape positions) byte-equivalent under both consumers.
- **Substrate target (Lock 1):** `existing_tape` (per Lock 1 v+1 declaration triple at `restart/locks/LOCKS.md:73-82`); retention_lifetime `output_row`; policy_owner `generated_grammar`. **OPPOSITE of REDRESS 96/97/98** — removes parallel-substrate consumers by re-routing existing consumers to existing producer.
- **REDRESS pre-block check:** REDRESS 96/97/98 (retained class-column / streaming structural cursor / class-lane-only replay) — material differential: "no new retained data; only re-route existing consumer to existing producer". REDRESS 50-55 (SK-V5 UTF-8 fusion) — no fusion proposed. REDRESS 60-72 (SK-V6 retained-parse + sidecar producers + digest cap-16) — consumes existing single substrate; no sidecar.
- **F-V2-P1ABC-RERECORD dependency flag:** NO (typed-plane primitive; the typed envelope is already cracked by the `DirectParser::skip_value` non-`generated.rs` callee chain).

### Candidate C3 — `digit_block_simd_accumulate` (UDOT / Eisel-Lemire mantissa)

- **P2 candidate IDs (consolidated):** P2-A C3 `digit_block_simd_accumulate` (`p2a:191`) ∪ P2-C C-P2C-3 `udot_digit_span_x4` (`p2c:43`) ∪ P2-E Gap 5 `parse_16_digits_dotprod` (`p2e:157-171`) ∪ P2-E Gap 7 `scan_digit_run_simd_64` (`p2e:189-201`) ∪ P2-E Gap 7.5 `byte_class_from_range_64` (`p2e:203-215`) ∪ P2-F C5 digit-block number decode (`p2f:110-119`).
- **Owner path:** `crates/bbnf-simd/src/aarch64/digit_mac.rs` (extend existing `parse_4_digits_dotprod` at `:27` to 16-digit `parse_16_digits_dotprod`); new `crates/bbnf-simd/src/aarch64/byte_class_from_range_64.rs` (Gap 7.5 sibling of existing `byte_class_from_eq_set_64.rs`); consumer in `crates/parse-that-regex/src/number/scan_digit_run_simd.rs` (new file) replacing the 8/4/2/1 SWAR ladder at `crates/parse-that-regex/src/number/mod.rs:106-123`.
- **Scalar-reference status (3-gate cell a):** PRESENT — scalar reference for `parse_4_digits_dotprod` is at `crates/bbnf-simd/src/aarch64/digit_mac.rs:5-22` (`bytes.iter().all(u8::is_ascii_digit)` + Horner); for `parse_16_digits_dotprod` is `parse_eight_digits` called twice with `* 10_000_000_000` between, at `crates/parse-that-regex/src/number/mod.rs:214` (executable-verified); for `byte_class_from_range_64` is `crates/bbnf-simd/src/scalar/byte_class_from_eq_set_64.rs:1` shape generalisation (sibling required at `crates/bbnf-simd/src/scalar/byte_class_from_range_64.rs` — Stage-A authoring target queued for S-P3 same-commit admission per Lock 16, sibling-shape template PRESENT at HEAD).
- **Checkasm-parity status (3-gate cell b):** EXTENSION required — `crates/bbnf-simd/tests/checkasm_digit_mac_x16.rs` (new) covering: 0/1/4/8/16/19/20-digit runs, overflow at u64 boundary, signs, decimal/exponent transition, invalid stops, CSS dimension/percentage samples, all alignments; sibling `checkasm_byte_class_from_range_64.rs` modeling existing `checkasm_byte_class_from_eq_set_64.rs` shape at HEAD.
- **Same-wave consumer NAMED (3-gate cell c):** direct-plane number kernel in `parse_array_element_at_direct` (`generated.rs:506`) on canada / mesh / marine_ik / numbers (number-heavy corpora); typed-plane `parse_vec_cap_10800_scalar_f64` on mesh-typed Track 1; CSS L4 `<number>` consumer per S-P3 R6 (post-R4 `cargo xtask regen-css`).
- **S-P1 antecedent:** `materialize_f64` rank-2 on canada-direct-Track1 (14.32 %), mesh-direct-Track1 (6.09 %), marine_ik-direct-Track1 (5.54 %); `materialize_u64` rank-3 on citm_catalog (2.04 %), random (0.83 %), instruments (2.60 %); serde_json `parse_decimal` rank-1 on mesh-typed-Track2 28.17 %, numbers-typed-Track2 58.68 %, marine_ik-typed-Track2 13.03 % (per `research/p1/p1b-samply-mode-2.md` direct + typed tables; `p1e §2.4` mode-III SIMD/scalar ratios 4.96x-5.04x on float-heavy corpora).
- **Falsifiability gate sketch:** `canada` direct_to_struct Track 1 Mbps ≥ `SK-V14-open` + 15 % AND sonic-rs strict + 1 % (current Track 1 10962, sonic-rs strict 12205 per `skinny/RESULTS.md:12`); `mesh` direct_to_struct Track 1 ≥ `SK-V14-open` + 12 %; `numbers` direct_to_struct Track 1 ≥ `SK-V14-open` + 10 %; `marine_ik` direct_to_struct Track 1 ≥ `SK-V14-open` + 8 %; full-table maintain ±1 % on the 17 corpus × 3 plane set.
- **Substrate target (Lock 1):** `direct_sink` for f64/i64 emit; `local_temp_only` for per-chunk digit accumulator.
- **REDRESS pre-block check:** REDRESS 80 (canada mantissa-widen) — material differential: this primitive is the **generic digit-block accumulate** (input → raw mantissa lanes) for ANY grammar's number primitive (Lock 16 abstract-primitive declaration at `restart/locks/LOCKS.md:287`); not the canada-specific f64 widening pass (raw mantissa → f64). Cross-grammar consumer (CSS dimension values, Sheets formulas, BBNF regex `{n,m}` counts) is new framing REDRESS 80 never carried.
- **F-V2-P1ABC-RERECORD dependency flag:** **YES** — P2-C C-P2C-3 + P2-E Gap 5 are both in the 12-consumer-dependency list per §6.3. The admitting wave MUST ship F-V2-P1ABC-RERECORD as Stage 0.

### Candidate C4 — `unicode_escape_neon_nibble_decode` (`\uXXXX` x4/x8 batch)

- **P2 candidate IDs (consolidated):** P2-A C7 `unicode_escape_neon_nibble_decode` (`p2a:195`) ∪ P2-C C-P2C-4 `tbl_tbx_escape_decode_batch` (`p2c:44`) ∪ P2-E Gap 2 `unescape_uxxxx_x8_neon` (`p2e:111-126`).
- **Owner path:** `crates/bbnf-simd/src/aarch64/unescape_uxxxx.rs` (existing scalar at `:40`, existing `_neon` single at `:74`, existing `_x4_neon` at `:125`; Gap 2 adds `_x8_neon` doubling). Consumer in `crates/parse-that-regex/src/lib.rs:718` `unescape_string` extends the existing `_x4` driver at `:386` to an `_x8` first-pass.
- **Scalar-reference status (3-gate cell a):** PRESENT — `unescape_uxxxx_scalar` at `crates/bbnf-simd/src/aarch64/unescape_uxxxx.rs:40` (executable-verified at HEAD); `read_hex_unit_scalar` at `crates/parse-that-regex/src/lib.rs:945` (executable-verified at HEAD). The `_x8` scalar reference is a loop calling `unescape_uxxxx_scalar` 8 times (bit-identical to the SIMD body).
- **Checkasm-parity status (3-gate cell b):** EXTENSION required — `crates/bbnf-simd/tests/checkasm_unescape_uxxxx_x8.rs` (new); existing `_x4` checkasm at `crates/bbnf-simd/tests/` already covers single + x4 (per `p2e:62` Layer-1 inventory). Matrix coverage: valid hex, invalid hex (non-hex chars), low/high surrogate pair, lone surrogate, mixed raw + escaped, tails, alignment, xorshift adversarial.
- **Same-wave consumer NAMED (3-gate cell c):** parse-only `y_string_unicode` row + direct-plane `unicode_escapes` + `unicode_mixed` rows; CSS L4 escaped-ident row per S-P3 R6 (CSS Syntax L3 §4.3.7 variable-width hex escape `\HEXHEX`).
- **S-P1 antecedent:** `read_hex_unit_scalar` parse-only rank-1 on `y_string_unicode` at 100 % self-time (`research/p1/p1e-hot-leaf-attribution.md §2.1`); `unescape_string` direct rank-1 on `unicode_escapes` at 46.7 % (`p1e §2.2`); 22.5 % rank-2 on `unicode_mixed` direct (per P1-B §2 direct table).
- **Falsifiability gate sketch:** `unicode_escapes` direct_to_struct Track 1 ≥ `SK-V14-open` + 12 % AND sonic-rs strict + 1 % (current Track 1 5523, sonic-rs strict 14298 per `skinny/RESULTS.md:42`); `y_string_unicode` parse_only Track 1 ≥ `SK-V14-open` + 10 %; `unicode_mixed` direct_to_struct Track 1 ≥ `SK-V14-open` + 6 %; full-table maintain ±1 % on the 17 corpus × 3 plane set.
- **Substrate target (Lock 1):** `local_temp_only` for the decoded codepoint buffer.
- **REDRESS pre-block check:** REDRESS 82 (single-quartet unicode classifier) — material differential: this is the **windowed nibble decode** (`\\` + `u` + 4 nibbles, possibly chained to a second 6-byte surrogate window), not the single-quartet classifier (which classifies one quartet against a fixed table). REDRESS 83 (StringBlock16 tiny probe) — different shape, different consumer.
- **F-V2-P1ABC-RERECORD dependency flag:** NO (the `read_hex_unit_scalar` and `unescape_string` leaves are already cleanly attributed in P1-E §2.1-§2.2; the envelope-mask CH2 census does not touch these primitives).

### Candidate C5 — `parse_attribution_envelope_cracker` (F-V2-P1ABC-RERECORD Stage-0 wave commitment)

- **P2 candidate IDs (consolidated):** P2-A C6 `parse_attribution_envelope_cracker` (`p2a:194`) ∪ P2-C C-P2C-8 `parse_attribution_profile_rebuild_gate` (`p2c:46`) ∪ P2-F C6 branch-on-first-byte dispatch CH2 binding (`p2f:121-131`). NOT a kernel primitive — a Stage-0 wave commitment per §6.3.
- **Owner path:** N/A (process / measurement). The toggle target is `crates/runtime/src/grammars/json/generated.rs:33-237` (8 `cfg_attr(feature = "parse-attribution", inline(never))` sites at lines 33-34, 43-44, 58-59, 79-80, 86-87, 117-118, 138-139, 157-158 per HARDENING-S-P2-V3-CONSOLIDATED §6.3). Build invocation: `cargo build --release -p bbnf-bench --features runtime/parse-attribution`; profile invocation: interactive `samply record` (NOT `--save-only` per `[samply-symbol-resolution]` feedback).
- **Scalar-reference status (3-gate cell a):** N/A — this is a profiling discipline, not a kernel. Scalar reference exists by construction (the envelope IS scalar).
- **Checkasm-parity status (3-gate cell b):** N/A — process discipline. Verification is `cfg_attr` flip at 8 sites + interactive `samply record` symbol-resolution + first-pass attribution of inner primitives.
- **Same-wave consumer NAMED (3-gate cell c):** the 12 consumer-dependency primitives per §6.3 verbatim: P2-A C6 + P2-C C-P2C-3 + P2-C C-P2C-8 + P2-E Gap 1 + Gap 3 + Gap 4 + Gap 5 + P2-F C6 + C7 + C10 + C12 + C13. Maps to shortlist candidates C1 (long-string SIMD; Gap 1) + C3 (digit-block; Gap 5 + C-P2C-3) + C7 (whitespace skip; Gap 3 + C7).
- **S-P1 antecedent:** every dispatch-envelope row in P1-E §2.1-§2.3 (27 of 34 parse-only + direct rank-1 leaves at 95-100 % self-time on `dispatch_value`).
- **Falsifiability gate sketch:** post-rerecord samply traces show inner-primitive attribution > 0 % on at least one of {`match_tiny_plain_string`, `match_number_at_digit`, `parse_number_direct`, `skip_ascii_whitespace`} per the cfg_attr-flipped 8 sites; first-pass attribution table emitted as a per-corpus column in the bench harness Notes block (sibling shape to existing `OffsetTapeStats` Notes per `skinny/RESULTS.md:137-180`); CH2 measurability gate verified (the inner primitives become measurable separately).
- **Substrate target (Lock 1):** N/A (build invariant; no new substrate, no sidecar).
- **REDRESS pre-block check:** none directly — process discipline; adjacent to REDRESS 50-55 (SK-V5 dispatch-table / function-pointer alternates) but explicitly uses parse-attribution `inline(never)` rather than dispatch-table substitution.
- **F-V2-P1ABC-RERECORD dependency flag:** **N/A — THIS IS F-V2-P1ABC-RERECORD ITSELF.** Per HARDENING-S-P2-V3-CONSOLIDATED §6.3: "Stage 0 of the first SK-V14 implementation wave admitting any dispatch-envelope-internal primitive" — C5 IS the Stage-0 commitment. Per `[no-deferrals]`, C5 ships in any wave admitting C1/C3/C7 (the three F-V2-P1ABC-RERECORD-dependent shortlist entries).

### Candidate C6 — `force_inline_lto_envelope_discipline` (Lock 15 enforcement + scalar discipline)

- **P2 candidate IDs (consolidated):** P2-A C4 `force_inline_lto_envelope_discipline` (`p2a:192`) ∪ P2-F C14 `i-cache-resident force-inline ceiling` (`p2f:204-212`).
- **Owner path:** codegen template in `crates/codegen/src/lower/rust.rs` (force-inline emission); LTO + codegen-units configuration in `skinny/Cargo.toml` (`lto = "fat"`, `codegen-units = 1` for the bench profile); verification via `cargo asm` for fused-function size; verification via samply with `parse-attribution` OFF for hot-function residency.
- **Scalar-reference status (3-gate cell a):** N/A — build invariant. Scalar reference exists by construction (the envelope IS scalar — the existing `dispatch_value` at `crates/runtime/src/grammars/json/generated.rs:45` is the scalar shape).
- **Checkasm-parity status (3-gate cell b):** N/A — build invariant. Verification: `cargo asm parse_value_at` shows fused-function size ≤ 20 KiB (Lock 15 binding); samply re-record post-`parse-attribution`-flip confirms i-cache residency for the dispatch envelope hot-function + per-leaf attribution unmasked.
- **Same-wave consumer NAMED (3-gate cell c):** the codegen template itself; same-wave verification is `cargo asm` + samply re-record paired with C5 (F-V2-P1ABC-RERECORD Stage 0). Per-grammar diagnostics: `BBNF-FORCE-INLINE-MISSED` (fires when a rule mined as hot-path lacks `#[inline(always)]`) + `BBNF-ICACHE-BUDGET-EXCEEDED` (fires when fused-function size exceeds 20 KiB budget; Lock 15 binding).
- **S-P1 antecedent:** `dispatch_value` 13/17 parse-only rank-1 at 95-100 % (`research/p1/p1e-hot-leaf-attribution.md §2.1`); c/B headroom gap vs yyjson (bbnf SK-V14 V1 ~1.14-5.67 c/B per P1-E §3 range; yyjson 0.91 c/B twitter per `restart/locks/LOCKS.md:268` Lock 15 evidence) — the scalar-discipline gap, not a SIMD gap. Per Lock 15 v+1 the JSON parser is already a 7,304-byte hot function (sub-budget per `skinny/profile/wave2-asm/PROFILE-REPORT.md` Appendix C).
- **Falsifiability gate sketch:** `twitter` parse_only c/B ≤ 0.95 c/B (yyjson reference 0.91 c/B per Lock 15); `citm_catalog` parse_only c/B ≤ 0.30 c/B (current ns_per_byte=0.265 per `skinny/RESULTS.md:58`; convert to c/B at 3.2 GHz M5 Max); fused dispatch-envelope hot-function size ≤ 20 KiB (Lock 15 binding); full-table maintain ±1 % on the 17 corpus × 3 plane set.
- **Substrate target (Lock 1):** N/A (build invariant; no new substrate, no sidecar). Lock 1 compliant by construction.
- **REDRESS pre-block check:** none — build-discipline + codegen, not a new kernel. Lock 14 compliant by construction (grammar-neutral diagnostics).
- **F-V2-P1ABC-RERECORD dependency flag:** **PAIRED with C5** — the verification (cargo asm + samply re-record) requires the `parse-attribution` feature ON for the per-primitive attribution; ships in the same wave as C5.

### Candidate C7 — `ascii_whitespace_skip_64` (NEON whitespace prefix skip)

- **P2 candidate IDs (consolidated):** P2-E Gap 3 `ascii_whitespace_skip_64` (`p2e:127-140`) ∪ P2-F C7 leading-whitespace prefix skip (`p2f:133-143`).
- **Owner path:** `crates/bbnf-simd/src/aarch64/string_block.rs` (new sibling fn `ascii_whitespace_skip_64` to existing `scan_string_special_block` at `:57`); consumer in `crates/parse-that-regex/src/lib.rs:113-125` (collapses to one Layer-1 call); driver to `crates/runtime/src/grammars/json/scan.rs:107` `scan_tail` whitespace inner.
- **Scalar-reference status (3-gate cell a):** PRESENT — `byte_class_from_eq_set_64_scalar` at `crates/bbnf-simd/src/scalar/byte_class_from_eq_set_64.rs:1` (executable-verified at HEAD; reused via the 4-byte whitespace set `[0x20, 0x09, 0x0A, 0x0D]` per RFC 8259); inversion returns offset of first non-member via `(!mask).trailing_zeros()`. Wire as thin Layer-1 wrapper per Gap 3 reference sketch at `p2e:131`.
- **Checkasm-parity status (3-gate cell b):** EXTENSION via the existing `checkasm_ascii_set_member_find_64.rs` at `crates/bbnf-simd/tests/` (executable-verified at HEAD) — the whitespace-set inversion is a config-instantiation of the existing test surface; new `checkasm_ascii_whitespace_skip_64.rs` covers: 64-byte all-whitespace, 64-byte no-whitespace, tail 0..63, mixed alignments, CSS-form-feed `0x0C` (5-byte set for CSS L4 consumer per CSS Syntax §4.2).
- **Same-wave consumer NAMED (3-gate cell c):** every JSON value-position prelude (`runtime/src/grammars/json/scan.rs:107`, `parse-that-regex/src/lib.rs:113`); CSS L4 declaration-value whitespace consumer per S-P3 R6 (set `{0x20, 0x09, 0x0A, 0x0D, 0x0C}`); Sheets whitespace consumer; BBNF-self whitespace consumer.
- **S-P1 antecedent:** `skip_ascii_whitespace` (`parse-that-regex/src/lib.rs:113`) called at every JSON value position; per P1-E §1.3 the JSON value-position prelude routes through `dispatch_value` which inlines `skip_ascii_whitespace` per the cfg_attr at `runtime/src/grammars/json/generated.rs:43-44`. Envelope-masked at SK-V14 dispatch; F-V2-P1ABC-RERECORD will surface this site.
- **Falsifiability gate sketch:** `gsoc-2018` parse_only Track 1 ≥ `SK-V14-open` + 8 % (gsoc-2018 is the sparsest object-of-strings shape per P2-D §1.2 — whitespace-heavy); `apache_builds` parse_only Track 1 ≥ `SK-V14-open` + 5 %; full-table maintain ±1 % on the 17 corpus × 3 plane set; CSS L4 maintain on `declaration_values` (post-R4 corpus capture).
- **Substrate target (Lock 1):** `local_temp_only` (returns offset; no positions emitted).
- **REDRESS pre-block check:** REDRESS 50-55 (SK-V5 UTF-8 fusion) — material differential: standalone whitespace primitive, NOT fused with string decode; framing is *separate* primitive (whitespace standalone).
- **F-V2-P1ABC-RERECORD dependency flag:** **YES** — P2-E Gap 3 + P2-F C7 both in the 12-consumer-dependency list per §6.3.

### Candidate C8 — `BackendShape::SinkOnly` activation (substrate-side hot-path demolition)

- **P2 candidate IDs (consolidated):** P2-D C-P2D-1 `BackendShape::SinkOnly` activation on JSON `parse_direct` direct-to-struct path (`p2d:112-119`); peer to P2-D C-P2D-2 `OffsetTapeStats` column extension (`p2d:120-126`) as the measurement-guard for the activation.
- **Owner path:** `crates/codegen/src/lower/rust.rs` (lowering-pass: emit `parse_object_direct` + `parse_array_direct` at `crates/runtime/src/grammars/json/generated.rs:436,437` WITHOUT constructing `TapeBuilder` / calling `attach_structural_index` when the rule's `LayoutFacts.backend_shape[rule_id] = SinkOnly`); `crates/runtime/src/grammars/json/parser.rs:7-12` `ParserState<'i>` carries `tape: TapeBuilder<'i>` field becomes optional (replaced by `()` under `SinkOnly`); `crates/runtime/src/tape/offsets.rs:1-6` `OffsetTapeStats` becomes a per-row schema column in `skinny/RESULTS.md` Notes block.
- **Scalar-reference status (3-gate cell a):** PRESENT — the scalar reference IS the current `parse_value_direct` at `crates/runtime/src/grammars/json/generated.rs:425-462` (it already takes `(input, bytes, cursor, sink)` with no tape parameter). The reference is the existing direct-emit body MINUS the (currently still constructed-but-unused) `TapeBuilder` in `ParserState`. Substrate-measurement scalar reference: `Tape::offset_bytes()` / `flag_bytes()` / `offset_capacity_bytes()` at `crates/runtime/src/tape/mod.rs:152-164` already computed at parse-end.
- **Checkasm-parity status (3-gate cell b):** EXTENSION required — substrate-elision correctness gate, not a SIMD-body parity gate. Test surface: `crates/bbnf-bench/tests/sink_only_elision_parity.rs` (new) diff-tests sink callbacks emitted under SinkOnly-activated vs SinkOnly-OFF for the 8 P1-B direct-plane rows where the envelope is 70 %+ top-1 (twitter 81.13 %, update_center 86.64 %, gsoc-2018 80.78 %, github_events 87.16 %); substrate-measurement gate: `OffsetTapeStats` column added to `xtask gate-json` per SYNTHESIS.md §3 — mechanical refusal of any row whose A/input crosses configurable threshold or A/L exceeds 2× empirical band (per P2-D §1.2 census 1.11×-1.87× band).
- **Same-wave consumer NAMED (3-gate cell c):** the 8 P1-B direct-plane rows where the envelope is 70 %+ top-1: `twitter` direct (81.13 %), `update_center` direct (86.64 %), `gsoc-2018` direct (80.78 %), `github_events` direct (87.16 %), plus apache_builds + unicode_mixed + canada + mesh direct rows; consumer is `JsonDigestSink` per `crates/bbnf-bench/src/direct_struct.rs:48-110`; substrate-measurement consumer is `xtask gate-json` per SYNTHESIS.md §3.
- **S-P1 antecedent:** P1-B `parse_object_value_at_direct::<JsonDigestSink>` (81.13 % twitter direct Track 1 per `research/p1/p1b-samply-mode-2.md`; 86.64 % update_center; 80.78 % gsoc-2018; 87.16 % github_events). The 7 P1-B direct rows where the envelope is 70 %+ top-1 carry the elidable tape-init cost. Per P1-E §4.4 substrate-union — the typed-plane `DirectParser::skip_value` already operates without a tape; the Track 1 direct path constructs a `TapeBuilder` even when `JsonDigestSink` never consumes the resulting document.
- **Falsifiability gate sketch:** `twitter` direct_to_struct Track 1 ≥ `SK-V14-open` + 10 % AND sonic-rs strict + 1 % (current Track 1 11908, sonic-rs strict 15110 per `skinny/RESULTS.md:6`); `update_center` direct_to_struct Track 1 ≥ `SK-V14-open` + 8 %; `gsoc-2018` direct_to_struct Track 1 ≥ `SK-V14-open` + 8 %; `github_events` direct_to_struct Track 1 ≥ `SK-V14-open` + 8 %; substrate-measurement gate: `OffsetTapeStats` column populates for every row; A/L within 2× empirical band; full-table maintain ±1 % on the 17 corpus × 3 plane set.
- **Substrate target (Lock 1):** `direct_sink` (the SinkOnly activation REMOVES a retained-substrate path — elides `TapeBuilder` construction — rather than adding one). **OPPOSITE of REDRESS 96/97/98 family** — closes a retention path, does not open one.
- **REDRESS pre-block check:** REDRESS 96/97/98 — material differential: removes substrate construction, does not add a parallel substrate. REDRESS 50-55 (SK-V5 UTF-8 fusion) — no new cursor introduced; elides existing TapeBuilder construction.
- **F-V2-P1ABC-RERECORD dependency flag:** NO (the `parse_object_value_at_direct` envelope is already attributable in P1-B §2 direct-plane table at 81.13 % rank-1; the tape-init elision is a substrate-edit, not a primitive whose measurability depends on rerecord).

### §2.1 — Shortlist count + carry-forward census

**8 candidate interventions** (at the §2 cap of ≤8 per PASS-3-SYNTHESIS-PLAN.md §2):

| # | Candidate | P2 ID consolidation | F-V2-P1ABC-RERECORD dep | Substrate target | CF-3 3-gate cell |
|---|---|---|---|---|---|
| C1 | `long_string_body_simd_scan` (canonical) | P2-A C2 ∪ P2-E Gap 1 ∪ P2-F C1+C2 | YES | `local_temp_only` | (scalar-ref PRESENT; checkasm EXTENSION; consumer NAMED) |
| C2 | `structural_index_singular_substrate_consumer` | P2-A C1 + C5 ∪ P2-F C11 | NO | `existing_tape` | (scalar-ref PRESENT; checkasm EXTENSION; consumer NAMED) |
| C3 | `digit_block_simd_accumulate` | P2-A C3 ∪ P2-C C-P2C-3 ∪ P2-E Gap 5 + Gap 7 + Gap 7.5 ∪ P2-F C5 | YES | `direct_sink` + `local_temp_only` | (scalar-ref PRESENT; checkasm EXTENSION; consumer NAMED) |
| C4 | `unicode_escape_neon_nibble_decode` | P2-A C7 ∪ P2-C C-P2C-4 ∪ P2-E Gap 2 | NO | `local_temp_only` | (scalar-ref PRESENT; checkasm EXTENSION; consumer NAMED) |
| C5 | `parse_attribution_envelope_cracker` | P2-A C6 ∪ P2-C C-P2C-8 ∪ P2-F C6 | **IS** F-V2-P1ABC-RERECORD | N/A | (scalar-ref N/A; checkasm N/A; consumer = 12 dep primitives NAMED) |
| C6 | `force_inline_lto_envelope_discipline` | P2-A C4 ∪ P2-F C14 | PAIRED with C5 | N/A | (scalar-ref N/A; checkasm N/A; consumer = codegen template + cargo asm + samply NAMED) |
| C7 | `ascii_whitespace_skip_64` | P2-E Gap 3 ∪ P2-F C7 | YES | `local_temp_only` | (scalar-ref PRESENT; checkasm EXTENSION; consumer NAMED) |
| C8 | `BackendShape::SinkOnly` activation | P2-D C-P2D-1 + C-P2D-2 | NO | `direct_sink` | (scalar-ref PRESENT; checkasm EXTENSION; consumer NAMED) |

**F-V2-P1ABC-RERECORD dependency census:** 3 candidates carry the Stage-0 dependency flag (C1 long-string SIMD via Gap 1 / C3 digit-block via Gap 5 + C-P2C-3 / C7 whitespace via Gap 3); C5 IS the F-V2-P1ABC-RERECORD packet itself; C6 ships paired with C5. The first SK-V14 implementation wave admitting any of {C1, C3, C7} MUST ship C5 as Stage 0 (per `[no-deferrals]`).

**Carry-forward CF-3 3-gate completeness:** 8/8 candidates carry the 3-gate cell explicitly. Per CH4 V3 §2.2 + HARDENING-S-P2-V3-CONSOLIDATED §6.1: "Every shortlisted candidate's admission manifest carries the 3-gate CH4 cell explicitly: (scalar-ref status / checkasm-parity expectation / same-wave-consumer NAMED) per S-P2 CH4 V2 CF-3."

**§2.Y NF-CH6-4 canonical-name binding compliance:** C1 is the ONE canonical primitive name across P2-A C2 + P2-E Gap 1 + P2-F C1+C2 per HARDENING-S-P2-V3-CONSOLIDATED §6.2 verbatim binding. Zero candidates admit three orthogonal SIMD bodies for one primitive.

**Demotions honoured (V2 + V3 cycle dispositions):**
- P2-C C-P2C-1 (CSS delimiter member-find) — demoted §2.X; NOT shortlisted at SK-V14 V1 absent real CSS L4 parser + corpus per `p2c:69`.
- P2-C C-P2C-6 (EOR3 string-mask fusion) — demoted §2.X; NOT shortlisted at SK-V14 V1 absent named three-input boolean hot expression per `p2c:70`.
- P2-C C-P2C-7 (byte_context orphan resolution) — demoted §2.X; NOT shortlisted as standalone wave per `p2c:71`.
- P2-D C-P2D-3 (sparse-flag-band gating) — demoted §1.6(d); zero hot-leaf consumer at SK-V14 per `p2d:128-130`.
- P2-F C8 (comment-skip primitive) — demoted §2.X.1; zero JSON P1 antecedent + no same-wave consumer committed per `p2f:147-148`.

### §2.2 — Architectural sequencing carried forward (binding to P3-B wave sequencing)

Per dispatch context §1 three architectural sequencing constraints, the shortlist surfaces sequencing-relevant pre-commitments that P3-B must honor:

1. **R4 (regen-css xtask) must land BEFORE PRUNE-2** — C7 (whitespace SIMD skip) is the first SK-V14 candidate whose CSS L4 consumer (declaration-value whitespace, set widening to include `0x0C` form-feed) needs the R4 regen-css xtask. C1 + C3 + C4 also have CSS L4 consumer claims that depend on R4.
2. **C-1 (PRUNE-3 + PRUNE-4) must land BEFORE C-4 (PRUNE-5 W8/W9 wire-up)** — C2 (substrate-union typed-skip) requires the trait-dispatch + grammar-agnostic codegen template per SYNTHESIS R3 + R4 to land before the typed-plane consumer wiring can converge.
3. **PRUNE-4 sub-wave count is 9 not 8** — P3-B per-wave sequencing must reflect this updated sub-wave count (per S-P0 A6).

## §3 — Falsifiability binding (named corpus rows + Mbps thresholds)

Per `[no-deferrals]` + `[no-warm-benches]` + `[no-orphan-redress]`: every shortlist candidate carries named corpus rows + Mbps thresholds vs `SK-V14-open` baseline + full-table maintain budget + exit gate + revert protocol. **Mbps thresholds quoted are V1 sketches; P3-C's per-wave falsifiability gate is authoritative.** Baseline `SK-V14-open` Mbps values from `skinny/RESULTS.md` Track 1 columns at HEAD; per-row baseline numbers in §2 candidate entries.

| Candidate | Named row(s) | Threshold (Track 1 Mbps delta vs `SK-V14-open`) | Strict comparator anchor | Full-table maintain |
|---|---|---|---|---|
| C1 long-string SIMD | unicode_escapes direct + twitter direct + gsoc-2018 direct + mesh direct + github_events direct | unicode_escapes direct ≥ +15 % AND sonic-rs strict + 1 %; twitter direct ≥ +10 % | sonic-rs strict struct deser per corpus (direct plane); 5 rows | ±1 % on 17 corpus × 3 plane |
| C2 substrate-union typed-skip | twitter typed + citm_catalog typed + marine_ik typed + github_events typed + mesh typed | twitter typed ≥ +20 % AND sonic-rs strict + 1 %; citm_catalog typed ≥ +15 % | per-corpus typed struct deser (typed plane); 5 rows | ±1 % on 17 corpus × 3 plane |
| C3 digit-block SIMD | canada direct + mesh direct + numbers direct + marine_ik direct | canada direct ≥ +15 %; mesh direct ≥ +12 %; numbers direct ≥ +10 %; marine_ik direct ≥ +8 % | sonic-rs strict struct deser (direct plane); 4 rows | ±1 % on 17 corpus × 3 plane |
| C4 unicode \uXXXX SIMD | unicode_escapes direct + y_string_unicode parse_only + unicode_mixed direct | unicode_escapes direct ≥ +12 %; y_string_unicode parse_only ≥ +10 %; unicode_mixed direct ≥ +6 % | sonic-rs strict (direct); sonic-rs Skipper-class (parse_only); 3 rows | ±1 % on 17 corpus × 3 plane |
| C5 parse-attribution Stage 0 | (process — no row threshold) | inner-primitive attribution > 0 % on at least one of {match_tiny_plain_string, match_number_at_digit, parse_number_direct, skip_ascii_whitespace}; CH2 measurability gate verified | N/A (process discipline) | ±1 % on 17 corpus × 3 plane (must not regress) |
| C6 force-inline LTO | twitter parse_only + citm_catalog parse_only c/B | twitter parse_only c/B ≤ 0.95 c/B (yyjson ref 0.91); citm parse_only c/B ≤ 0.30 c/B; fused hot-function size ≤ 20 KiB (Lock 15) | yyjson scalar 0.91 c/B (Lock 15 reference) | ±1 % on 17 corpus × 3 plane |
| C7 whitespace SIMD skip | gsoc-2018 parse_only + apache_builds parse_only + CSS declaration_values | gsoc-2018 parse_only ≥ +8 %; apache_builds parse_only ≥ +5 %; CSS declaration_values maintain (post-R4) | sonic-rs Skipper-class (parse_only); 2 JSON rows + 1 CSS row | ±1 % on 17 corpus × 3 plane |
| C8 SinkOnly activation | twitter direct + update_center direct + gsoc-2018 direct + github_events direct | twitter direct ≥ +10 %; update_center direct ≥ +8 %; gsoc-2018 direct ≥ +8 %; github_events direct ≥ +8 %; A/L within 2× empirical band; OffsetTapeStats column populates for every row | sonic-rs strict struct deser (direct plane); 4 rows | ±1 % on 17 corpus × 3 plane |

**Revert protocol (binding to every candidate):** any wave that fails its exit gate (named row threshold AND full-table maintain) reverts the entire candidate's commit; the candidate becomes a REDRESS entry per `[no-orphan-redress]`; the wave does NOT close with partial admission per SYNTHESIS §0.4 + `[no-deferrals]`.

**Per-iteration equality oracle (R2 binding):** every gate cell is verified inside the timing region per iteration; the comparator output plane matches the bbnf plane (parse_only consumes bytes without materialising values; direct deserialises into the same struct shape; typed deserialises into the same typed-struct shape). Per SYNTHESIS §0.2 reconciliation block: any cell citing `sonic_rs::from_slice::<Value>` as anchor is AUDIT-FALSIFIED (P-2 anti-pattern) and the wave fails closed.

## §4 — Pre-blocked routes (REDRESS entries each wave must NOT re-open)

Per CH3 binding + SYNTHESIS §0.4 P-1..P-7. The eight pre-blocked REDRESS families per dispatch context §1 + S-P0 SYNTHESIS-AUDIT-OVERFIT.md, per-candidate disposition:

| REDRESS family | Description | Shortlist candidates that abut | Material differential carried |
|---|---|---|---|
| REDRESS 28+33 | Class A NEON tiny-string `match_tiny_plain_string` wiring | C1 (long-string SIMD) | Long-string envelope consumer, NOT tiny-string probe replay; 16-byte body at `string_block.rs:57` already Wave-1-admitted; gap is 64-byte sweep wrapper consumed by `parse_that_regex::skip_string_plain_trusted` |
| REDRESS 50-55 | SK-V5 UTF-8 fusion routes (sink-local decoded stats, source-method digest folds) | C7 (whitespace SIMD skip); C2 (substrate-union typed-skip) | C7 framing is STANDALONE primitive (not fused with string decode); C2 introduces no new cursor (elides where applicable) |
| REDRESS 60-72 | SK-V6 retained-parse + sidecar producers + digest cap-16 | C2 (substrate-union typed-skip); C8 (SinkOnly activation) | Both candidates consume existing single substrate; no sidecar producer; C8 ELIDES retention rather than adding it |
| REDRESS 80 | canada mantissa-widen plan | C3 (digit-block SIMD) | Generic digit-block accumulate (Lock 16 abstract-primitive declaration line 287); cross-grammar consumer (CSS dimension values, Sheets formulas) is new framing REDRESS 80 never carried |
| REDRESS 82-84 | single-quartet unicode classifier + StringBlock16 tiny probe + object-pair compaction | C4 (unicode \uXXXX SIMD) | Windowed nibble decode (\\ + u + 4 nibbles, possibly chained to surrogate window), NOT single-quartet classifier replay; NOT StringBlock16 |
| REDRESS 88 | PMULL prefix-XOR as hot body | C1 (long-string SIMD) | Composes existing scalar `bitmap_prefix_xor_64` whatever its internal body; NO PMULL substitution; framing is "compose classifier + escape-mask + prefix-XOR" not "replace prefix-XOR body with PMULL" |
| REDRESS 89 | CSSC CTZ next-bit bulk consumer | (none in shortlist) | No shortlist candidate proposes CSSC CTZ bulk consumer |
| REDRESS 96/97/98 | retained class-column / streaming structural cursor / class-lane-only replay | C2 (substrate-union typed-skip); C8 (SinkOnly activation) | OPPOSITE direction — C2 removes parallel-substrate consumers by re-routing existing consumers to existing producer; C8 elides retention path entirely. Substrate-target `existing_tape` (C2) + `direct_sink` (C8); no new retention; no class column |
| REDRESS 126 | PEXT mask plan; aarch64 has no PEXT | (none in shortlist; aarch64 scope) | aarch64 NEON form is REDRESS-126-orthogonal; no x86_64 BMI2 form in shortlist |

**Substrate-union risks (Lock 1 + CH5):** the substrate-union holds at HEAD per HARDENING-S-P2-V3-CONSOLIDATED §4.5 seven-witness corroboration. Every shortlist candidate carries explicit substrate-target per the Lock 1 v+1 declaration triple at `restart/locks/LOCKS.md:73-82`; zero shortlist candidates propose a parallel substrate, a sidecar producer, a renamed scanner (Lock 1), or a Track 1 ≡ Track 2 dishonesty.

**Strictness-plane risks (R1 + P-2):** every shortlist candidate's SOTA-beat target is the plane-correct strict comparator per SK-V14 R1 (sonic-rs Skipper-class for parse_only; sonic-rs strict struct deser per corpus for direct; per-corpus typed struct deser for typed); zero candidates anchor against `sonic_rs::from_slice::<Value>` (P-2 anti-pattern).

**Pattern-level pre-blocks (SYNTHESIS §0.4 P-1..P-7):** every wave's exit gate compares against the `SK-V14-open` baseline on the strict plane; per-iter equality oracle (R2) verified inside the timing region per iteration; no candidate re-opens the audit-falsified row family (40 audit-falsified rows per SYNTHESIS §0.2 reconciliation block).

## §5 — Sources (every upstream artefact cited)

### §5.1 — S-P2 LOCKED candidate pool authority (V3 HEAD `ebe84954b`)

- `restart/skinny/tranches/sk-v14/research/p2/p2a-sota-teardown.md` (367 lines; V1+V2-LOCKED — zero V3 drift) — primary P2-A source for C1, C2 (via C1+C5), C3, C4, C5, C6, C7 inheritance.
- `restart/skinny/tranches/sk-v14/research/p2/p2c-arch-esoterica.md` (164 lines; V2-LOCKED) — primary P2-C source for C3 (via C-P2C-3), C4 (via C-P2C-4), C5 (via C-P2C-8) inheritance; demotion ratification for C-P2C-1/-6/-7.
- `restart/skinny/tranches/sk-v14/research/p2/p2d-substrate-tape.md` (254 lines; V2-LOCKED) — primary P2-D source for C2 (via C-P2D-1 carved out into C8), C8 (via C-P2D-1 + C-P2D-2 substrate-measurement) inheritance; demotion ratification for C-P2D-3.
- `restart/skinny/tranches/sk-v14/research/p2/p2e-parse-that-gaps.md` (342 lines; V1+V2-LOCKED) — primary P2-E source for C1 (via Gap 1), C3 (via Gap 5 + Gap 7 + Gap 7.5), C4 (via Gap 2), C7 (via Gap 3) inheritance.
- `restart/skinny/tranches/sk-v14/research/p2/p2f-grammar-neutral.md` (360 lines; V3-amended) — primary P2-F source for C1 (via §2.Y NF-CH6-4 canonical-name binding + C1/C2), C2 (via C11), C3 (via C5), C5 (via C6 dispatch envelope CH2 binding), C6 (via C14), C7 (via C7), C8 (via C-P2D-1 generality verdict) inheritance; demotion ratification for C8 comment-skip.
- `restart/skinny/tranches/sk-v14/research/p2/hardening/HARDENING-S-P2-V3-CONSOLIDATED.md` (668 lines; §3Z COHORT LOCK aggregator) — primary carry-forward authority for §6.1 CF-3 3-gate admission discipline, §6.2 NF-CH6-4 canonical-name binding, §6.3 F-V2-P1ABC-RERECORD Stage-0 wave commitment.

### §5.2 — S-P1 hot-leaf antecedent authority

- `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md` (per-cycle V3-LOCKED at `hardening/HARDENING-S-P1-V1-CONSOLIDATED.md`) — primary P1-E source for every shortlist candidate's S-P1 antecedent claim.
- `restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md` — direct + typed plane hot-leaf tables for C2, C3, C8 antecedents.
- `restart/skinny/tranches/sk-v14/research/p1/p1a-samply-mode-1.md` — parse-only plane hot-leaf tables for C1, C4, C6, C7 antecedents.
- `restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md` — mode-III SIMD/scalar probe data for C1, C3, C7 antecedents.
- `restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md` — PMU c/B data for C6 (force-inline LTO) antecedent (yyjson 0.91 c/B reference).
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V3/CH5.md:78-83` — substrate-union two-cursor independence verification for C2 substrate-target binding.

### §5.3 — Pass Alpha goalset + binding authority

- `restart/skinny/tranches/sk-v14/SYNTHESIS.md §0.1` — R10 close-condition (binding S-P3 target).
- `restart/skinny/tranches/sk-v14/SYNTHESIS.md §0.2` — audit-zero goalset (binding baseline).
- `restart/skinny/tranches/sk-v14/SYNTHESIS.md §0.3` — R1-R10 acceptance criteria.
- `restart/skinny/tranches/sk-v14/SYNTHESIS.md §0.4` — P-1..P-7 pattern-level pre-blocks.
- `restart/skinny/tranches/sk-v14/SYNTHESIS.md §2` — telemetry binding (`comparator_plane`, `per_iter_equality`, `audit_overlay_verdict`, `track2_entry_point` per SK-V14 SYNTHESIS).
- `restart/skinny/tranches/sk-v14/HANDOFF.md §3` — honest baseline.
- `restart/skinny/tranches/sk-v14/HANDOFF.md §7` — 41-element refusal conditions.
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md` — S-P0 74 findings + 3 architectural sequencing constraints.
- `restart/skinny/tranches/sk-v14/ORCHESTRATOR-PROMPT.md` — R-target acceptance criteria.

### §5.4 — Contract authority (PASS contracts + lens registries)

- `restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md` — S-P3 contract; §2 scope matrix row P3-A; §2.1 frontmatter; §3 CH1-CH6 lens overlay; §7 hard caps; §8 bbnf-lang specifics.
- `restart/prompts/ORCHESTRATOR.md §3W` (universal lens registry) + `§3Z` (convergence rule) + `§8` non-negotiables (Lock 1 substrate union; Lock 14 grammar-neutrality; scalar-reference + checkasm + same-wave consumer).
- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §CH7` — Overfit-Prune lens (carry-forward).
- `restart/prompts/pass-contracts/SKINNY-TRIUMVIRATE.md` — wave-execution contract S-P3's waves must conform to.
- `restart/prompts/pass-contracts/PASS-ALPHA.md` — SK-V{N} bracket + G-Alpha(N-1→N) goalset authority.
- `restart/skinny/tranches/sk-v14/research/p3/S-P3-DISPATCH-CONTEXT.md` — this dispatch's binding context.

### §5.5 — Locks + SPEC shape mirror

- `restart/locks/LOCKS.md:48-90` — Lock 1 verbatim + 2026-05-04 reframe + 2026-05-21 v+1 substrate-ceiling fold + substrate_target/retention_lifetime/policy_owner manifest requirement.
- `restart/locks/LOCKS.md:220-263` — Lock 14 + v+1 amendments (grammar-neutrality binding; primary CH2 authority).
- `restart/locks/LOCKS.md:265-281` — Lock 15 (LTO + force-inline + i-cache ≤ 20 KiB; yyjson 0.91 c/B reference for C6).
- `restart/locks/LOCKS.md:282-340` — Lock 16 (SIMD/ASM allowlist + abstract-primitive declarations; binding for C1/C3/C4/C7 admission).
- `restart/skinny/tranches/sk-v8/SPEC.md` — the SPEC shape P3-F mirrors verbatim; §0 close-condition + comparator classes + outcome enum + required telemetry + opening-row goalset binding shape for the SK-V14 SPEC the shortlist informs.

### §5.6 — Empirical floor (the bench-bound truth)

- `skinny/RESULTS.md` (185 lines) — per-row baseline Mbps + corpus row identifiers; cited per candidate falsifiability gate; the bench-gate authority all §3 thresholds compare against.
- `skinny/REDRESS.md` (~5041 lines; grep + offset per `[read-size-preflight]`) — REDRESS 28+33, 50-55, 60-72, 80, 82-84, 88, 89, 96-98, 119/120, 126 pre-block surface; cited per §4 candidate disposition.

### §5.7 — Bbnf source-of-truth (path:line at V3 HEAD `ebe84954b`; executable-verified at write-time)

- `skinny/crates/runtime/src/grammars/json/generated.rs:33-237` — parse-attribution `cfg_attr` plumbing (8 sites; C5 toggle target); `:45` `dispatch_value` envelope; `:159, 169` `match_tiny_plain_string`; `:213` `match_number_at_digit`; `:466` `parse_object_value_at_direct`; `:506` `parse_array_element_at_direct`; `:650` `parse_number_direct`. Executable-verified via `grep -n "fn dispatch_value\|fn match_tiny_plain_string\|fn parse_object_value_at_direct\|fn parse_array_element_at_direct\|fn match_number_at_digit\|fn parse_number_direct\|cfg_attr.*parse-attribution" skinny/crates/runtime/src/grammars/json/generated.rs`.
- `skinny/crates/runtime/src/grammars/json/scan.rs:22` `scan_structurals`; `:32` `scan_structurals_scalar` (executable-verified live; C1 + C2 + C7 scalar-reference target); `:107` `scan_tail`; `:131` `scan_tail_byte`.
- `skinny/crates/runtime/src/grammars/json/parser.rs:7-12` `ParserState<'i>` (4 fields: input, bytes, cursor at `:10`, tape: TapeBuilder); `:48` `state = ParserState::new(input)` consumer site.
- `skinny/crates/runtime/src/tape/mod.rs:92` `TapeId`; `:94` `Tape<'input>` (substrate-union single-substrate verification per HARDENING-S-P2-V3-CONSOLIDATED §4.5).
- `skinny/crates/runtime/src/tape/assembler.rs:42` `TapeBuilder<'input>` (substrate-union single-substrate verification).
- `skinny/crates/runtime/src/tape/offsets.rs:1-6` `OffsetTapeStats` (C8 substrate-measurement plumbing).
- `skinny/crates/bbnf-bench/src/generated_real_typed.rs:2742-2746` `DirectParser<'i>` (Track 2 cursor; two-cursor independence per HARDENING-S-P2-V3-CONSOLIDATED §4.5); `:2949` `DirectParser::skip_value` (C2 rewrite target); `:2966` `skip_object`; `:2987` `skip_array`.
- `skinny/crates/parse-that-regex/src/lib.rs:113` `skip_ascii_whitespace` (C7 consumer); `:547` `skip_string_plain_trusted` (C1 consumer); `:718` `unescape_string` (C4 consumer + Lock 14 violation site per `parse-that-regex::StringFlags::HAS_ESC` DELTA-NOTE); `:945` `read_hex_unit_scalar` (C4 scalar reference).
- `skinny/crates/parse-that-regex/src/number/mod.rs:106` `scan_digit_run` (C3 consumer); `:214` `parse_eight_digits` (C3 scalar reference component); `:261` `materialize_f64`.
- `skinny/crates/bbnf-simd/src/aarch64/bulk_emit_positions_64.rs:2` `bulk_emit_positions_64_neon`.
- `skinny/crates/bbnf-simd/src/aarch64/string_block.rs:31` `scan_string_special_block_scalar` (C1 scalar-reference; NF-CH6-3 composition primitive per §6.2 cross-witness); `:57` `scan_string_special_block` (C1 SIMD body + C7 sibling host).
- `skinny/crates/bbnf-simd/src/aarch64/unescape_uxxxx.rs:40` `unescape_uxxxx_scalar` (C4 scalar reference); `:74` `unescape_uxxxx_neon`; `:125` `unescape_uxxxx_x4_neon` (C4 Gap 2 doubling target).
- `skinny/crates/bbnf-simd/src/aarch64/digit_mac.rs:5` scalar reference (C3); `:25-49` `parse_4_digits_dotprod` (C3 SIMD body — UDOT 4-digit; Gap 5 extends to 16-digit).
- `skinny/crates/bbnf-simd/src/aarch64/classify_tbl4.rs:22` `classify_chunk_from_table` (C1 composition primitive).
- `skinny/crates/bbnf-simd/src/lib.rs:175` `escape_mask_64` (C1 NF-CH6-3 composition primitive); `:227` `compact_mask` (C1 + C7 substrate-emit consumer).
- `skinny/crates/bbnf-simd/src/scalar/bitmap_prefix_xor_64.rs:1` `bitmap_prefix_xor_64_scalar` (C1 composition primitive + sibling-shape template for queued `bcax_64.rs` per §2.Y NF-CH6-4 V3 cross-witness).
- `skinny/crates/bbnf-simd/src/scalar/byte_class_from_eq_set_64.rs:1` (C7 scalar reference + sibling-shape template for queued `byte_class_from_range_64.rs` per C3 Stage-A authoring target).
- `skinny/crates/bbnf-simd/tests/checkasm_*.rs` — existing checkasm matrix (15 files at HEAD per `ls`): `checkasm_ascii_set_member_find_64.rs` + `checkasm_bitmap_next_set_bit.rs` + `checkasm_bitmap_prefix_xor_64.rs` + `checkasm_bulk_emit_positions_64.rs` + `checkasm_byte_class_from_eq_set_64.rs` + `checkasm_byte_class_from_table_64.rs` + `checkasm_common.rs` + `checkasm_eob_pad_clamp.rs` + `checkasm_escape_mask_64.rs` + `checkasm_parity.rs` + `checkasm_structural_terminator_64.rs` + `checkasm_utf8_block.rs` (sibling-shape templates for extensions per C1/C3/C4/C7 checkasm cells); `aarch64_primitives.rs` + `classifier_parity.rs` + `corpus_parity.rs`.
- `skinny/crates/bbnf-simd/src/scalar/` (8-file directory listing at V3 HEAD per `ls`): `bitmap_next_set_bit.rs`, `bitmap_prefix_xor_64.rs`, `bulk_emit_positions_64.rs`, `byte_class_from_eq_set_64.rs`, `byte_class_from_table_64.rs`, `eob_pad_clamp.rs`, `mod.rs`, `swar_8byte.rs`; `byte_context_64.rs` + `bcax_64.rs` correctly NOT-PRESENT at HEAD (Stage-A authoring targets for queued S-P3 same-wave Lock 16 same-commit admission per HARDENING-S-P2-V3-CONSOLIDATED §4.4 + V3 verb-tense polish; C3 sibling shape via `byte_class_from_range_64.rs` similarly queued).
