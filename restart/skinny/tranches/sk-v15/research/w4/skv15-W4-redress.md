# SK-V15 W4 Redress

Verdict: ADMIT.

Landed:

- Added root `xtask check-runtime` with no-writing full-universe check.
- Split root runtime emitters into render/write/check paths.
- Added generator-owned line-1 provenance to all 67 Pattern H runtime files.
- Validated projection set, output dirs, expected-vs-actual Pattern H path set,
  and final generated bytes.

Budget:

- Manual source insertions: `266` across `xtask/src/{main.rs,regen_css.rs,regen_simple_runtime.rs}`.
- Generated output: `67` header lines, one per Pattern H file.
- Active W4 plan/challenge/redress docs: within the `80-180` line budget.

Evidence:

```sh
cargo fmt --check --package xtask
RUSTFLAGS="-C target-cpu=native" cargo run --profile ax-iter -p xtask -- check-runtime
find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l # 67
grep -cE "^[0-9]+\\. \\*\\*" restart/locks/LOCKS.md # 16
git diff --check
```

External line-1 scan accepted all 67 directory-specific headers. BackendShape
canon remains `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`.

`LegacyPath` / `LegacySegment` hits remain limited to CSS L4 runtime output and
`xtask/src/regen_css.rs`; deletion stays blocked under
`DEP-W4-W6-CSS-LEGACY-RUNTIME-SHIM`.
