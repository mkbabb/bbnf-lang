# S-P1 V1 CH6 Anti-Paper-Close

Disposition: ACCEPT.

The flame profile files exist on disk: 51 baseline JSON profile artifacts under
`/tmp/skv16-p1/profiles` and 68 mode-III artifacts under
`/tmp/skv16-p1-mode3/profiles`. Raw Samply JSON reports `symbolicated=false`,
so the artefacts cite offline `atos -inlineFrames` output and the archived
profile binary instead of pretending Samply pre-symbolicated the files.
