# SK-V13 S-P2 V2 CH3 Regression / REDRESS

Verdict: ACCEPT.

## Evidence

- V2 preserves the S-P2 CH3 bar: candidates that touch REDRESS-blocked
  territory are either materially reframed, conditional on same-wave row
  movement, or stamped out of S-P3 eligibility. The prompt requires CH3 to
  reject candidates that reopen REDRESS 28/33, 82-84, 88/89, historical
  blocked routes, or related pre-blocks without fresh P1 evidence plus new
  framing (`restart/prompts/skinny/PASS-2-RESEARCH.md:109`-`:117`).

- V1's accepted CH3 limits were carried forward. V1 already accepted the
  REDRESS treatment while the consolidated V1 packet failed on other lenses:
  correctness, generality, and anti-paper-close
  (`restart/skinny/tranches/sk-v13/research/p2/hardening/V1/CH3.md:1`-`:73`;
  `restart/skinny/tranches/sk-v13/research/p2/hardening/HARDENING-S-P2-V1-CONSOLIDATED.md:9`-`:46`).
  The V2 files fold those non-CH3 blockers without loosening CH3: P2-C now
  stamps unsupported inventory rows as `NOT-S-P3-ELIGIBLE`
  (`restart/skinny/tranches/sk-v13/research/p2/p2c-arch-esoterica.md:30`-`:44`),
  P2-D demotes D1 lazy capacity to a measurement question
  (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:146`-`:185`),
  and P2-F makes inventory/drop decisions binding for S-P3
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:146`-`:157`,
  `:238`-`:243`).

- REDRESS 28/33 and 82-84 are not reopened under renamed string/unicode
  candidates. P2-A's string and escape candidates require grammar-owned policy,
  row-moving consumers, and reject naive tiny-string or single-quartet replays
  (`restart/skinny/tranches/sk-v13/research/p2/p2a-sota-teardown.md:75`-`:76`,
  `:136`-`:139`). P2-E repeats the same fence for NEON tiny string,
  StringBlock16, single-quartet unicode, direct materialization, and object-pair
  compaction (`restart/skinny/tranches/sk-v13/research/p2/p2e-parse-that-gaps.md:136`-`:146`).
  This matches the measured histories: active 16-byte tiny-string dispatch
  regressed `twitter` in REDRESS 28/33 (`skinny/REDRESS.md:324`-`:421`);
  single-quartet unicode, StringBlock16 tiny probe, and object-pair compaction
  were rejected in REDRESS 82-84 (`skinny/REDRESS.md:2285`-`:2395`).

- REDRESS 88/89/90 and 126 are preserved. P2-C frames PMULL/CSSC only as a
  new SIMD-first union route, not a local default hot-body replacement
  (`restart/skinny/tranches/sk-v13/research/p2/p2c-arch-esoterica.md:16`,
  `:37`-`:43`, `:59`-`:65`). P2-B carries the same boundary for prefix-XOR,
  next-bit, bulk emit, byte-class, and CSS ASCII run-skip, requiring scalar
  reference, strict checkasm, production consumer, and row gate
  (`restart/skinny/tranches/sk-v13/research/p2/p2b-dav1d-process.md:64`-`:76`,
  `:92`-`:118`, `:137`-`:148`). That is materially different from the rejected
  REDRESS 88 PMULL hot body and REDRESS 89 CTZ/bulk consumer; REDRESS 90 admits
  only canary hardening (`skinny/REDRESS.md:2510`-`:2618`). For REDRESS-126,
  V2 correctly treats `a64_ascii_set_run_skip` as route-production split /
  production wiring candidate, not retroactive production admission
  (`skinny/REDRESS.md:3766`-`:3814`, `:3864`-`:3872`).

- REDRESS 96/97/98 union failures remain live pre-blocks. P2-D states that
  structural SIMD is scanner micro-evidence only and does not authorize a
  retained SIMD-position vector, streaming cursor, class column, or parser-owned
  structural cursor (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:62`-`:85`).
  The surviving D2/D3 shapes name material differentials: codegen-time
  same-tape event projection with no retained class side vector, and mask-to-tape
  writeback where the retained projection is the tape
  (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:187`-`:227`,
  `:229`-`:276`). P2-F keeps union attempts conditional on cited REDRESS
  history, material differential, and no sidecar substrate
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:100`-`:102`,
  `:165`-`:169`). That preserves REDRESS 96/97/98, which record two
  correctness-green measured failures and retire the old union-substrate gate
  (`skinny/REDRESS.md:2795`-`:2949`).

- REDRESS 119/120 direct fixpoints are reopened only by the user pin's
  material-differential rule, not by renaming profile envelopes as admissions.
  P2-A scopes direct/digest candidates to strict same-plane row movement and
  prior-fixpoint citation (`restart/skinny/tranches/sk-v13/research/p2/p2a-sota-teardown.md:81`,
  `:124`-`:128`, `:149`-`:151`). P2-D and P2-F repeat that direct reopen waves
  need a fresh material differential and strict comparator evidence
  (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:400`-`:404`;
  `restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:178`-`:182`).
  REDRESS 119/120 remain measured fixpoint/no-source-close authority, not row
  admission (`skinny/REDRESS.md:3497`-`:3555`).

## Blockers / Fold Requirements

No CH3 blocker for V2.

Carry these requirements into S-P3 selection:

1. `a64_ascii_set_run_skip` / `ByteSetRunSkip64` may be shortlisted only as CSS
   production wiring with fresh narrow CSS parser evidence or same-wave
   scan-block row movement and strict lightningcss equality. A second
   microbench-only landing is a REDRESS-126 replay.
2. Union / structural SIMD candidates must cite REDRESS 96/97/98 and make the
   projection the existing tape or sink-only fact stream. Class columns,
   retained structural vectors, streaming cursors, parser-owned cursors, and
   class-lane-only variants remain rejected.
3. PMULL, CSSC CTZ, prefix-XOR, next-bit, bulk emit, `byte_context`, cache
   hints, EOR3, LD4/TBX/SMIN/SMAX, and D1 lazy capacity cannot enter S-P3 as
   standalone waves without a later accepted research cycle naming fresh P1
   evidence, scalar oracle, parity/checkasm, and same-wave measured consumer.
4. String/unicode/number candidates must stay policy-driven and grammar-neutral.
   JSON-shaped tiny-string, single-quartet unicode, direct materialization, and
   one-row numeric patches are rejected unless V3 materially reframes them with
   strict row movement.

## Disposition

CH3 accepts V2. The current V2 packet preserves material differentials and does
not reopen rejected REDRESS routes under renamed candidates. Route to the V2
aggregator with the fold requirements above as S-P3 preconditions, not as V2
revision blockers.
