# SK-V13 S-P2 V3 CH6: Anti-Paper-Close

## Verdict

ACCEPT.

## Evidence

- The CH6 bar remains the PASS-2 anti-paper-close rule: comparator claims need
  source anchors, ISA claims need manual sections, primitive claims need scalar
  sketches, and future-wave deferral is not evidence
  (`restart/prompts/skinny/PASS-2-RESEARCH.md:133`-`:138`). V3 is judged
  against the V2 consolidated fold requirement that P2-F's CSS rows be made
  non-primitive or conditional route-production scopes
  (`HARDENING-S-P2-V2-CONSOLIDATED.md:29`-`:38`).
- The remaining CSS paper-close risk is removed. P2-F V3 adds
  `CSS-ROW-SCOPE-CONDITIONAL` as a verdict meaning "not a primitive admission,"
  requiring a fresh narrow CSS parser profile or same-wave strict
  lightningcss/cssparser row movement (`p2f-grammar-neutral.md:44`-`:47`).
  Each CSS row scope now carries that boundary: stylesheet/selectors,
  declaration-value extension, visual functions, at-rules/media, nesting, and
  vendor/custom at-rule taxonomy all require scalar oracle/equality evidence and
  fresh parser profile or same-wave strict row movement
  (`p2f-grammar-neutral.md:69`-`:74`).
- P2-F also states the rule outside the row table, so S-P3 cannot count the CSS
  scopes as primitive validation or as a future-phase promise. CSS L4 row scopes
  are only row-production work, need strict equality against lightningcss and
  cssparser plus fresh narrow parser profile or same-wave strict row movement,
  and are neither generic runtime primitives nor CH1 primitive hot-leaf evidence
  (`p2f-grammar-neutral.md:123`-`:132`). The carry-forward list repeats that
  CSS rows 1-6 are conditional row-production scopes, not primitive eligibility,
  and any SIMD primitive inside such a row inherits its own scalar/checkasm/
  consumer gate (`p2f-grammar-neutral.md:151`-`:166`).
- The V2 accepted dav1d boundary did not regress. P2-B still names the process
  authority as FFmpeg / VideoLAN checkasm, keeps dav1d to project-level lineage
  context, and forbids dav1d-specific S-P3 gate text unless exact dav1d
  source-file anchors are added (`p2b-dav1d-process.md:1`-`:25`,
  `:143`-`:148`). Its admission table still requires P1 antecedent, primitive
  contract, executable scalar reference, differential checkasm, microbench,
  same-wave consumer, grammar policy, and strict row gate
  (`p2b-dav1d-process.md:37`-`:48`).
- The Arm boundary did not regress. P2-C still ties CSSC CTZ, PMULL, UDOT,
  TBL/TBX, EXT/shift, and EOR3/SHA3 to named ACLE/Neon features or intrinsics
  (`p2c-arch-esoterica.md:16`-`:24`, `:75`-`:93`). P2-F preserves the same
  REDRESS 88/89/126 guard: PMULL, CSSC CTZ, prefix-XOR, bulk emission, and
  orphan SIMD inventory require scalar reference, checkasm parity, material
  differential, and same-wave consumer before any new attempt
  (`p2f-grammar-neutral.md:180`-`:185`).
- Inventory and scalar/checkasm/consumer boundaries remain binding. P2-A's
  C1-C8 table still names shape, scalar-reference status, checkasm expectation,
  P1 antecedent, and same-wave consumer/reject boundary for every comparator-led
  primitive (`p2a-sota-teardown.md:72`-`:87`). P2-C stamps
  `NOT-S-P3-ELIGIBLE` rows as inventory only unless a later fold adds a named
  S-P1 hot expression, scalar reference, checkasm/parity, and same-wave row
  consumer (`p2c-arch-esoterica.md:30`-`:43`). P2-F preserves that disposition
  for EOR3/BCAX, cache hints, standalone prefix/next/bulk bitmap primitives,
  standalone `byte_context`, LD4/TBX/SMIN/SMAX refinements, and standalone D1
  lazy capacity (`p2f-grammar-neutral.md:162`-`:166`, `:255`-`:260`).
- The substrate and parse-that boundaries still ground their candidates instead
  of deferring details. P2-D's load-bearing conclusion remains that legal work
  changes how generated code emits or consumes the single tape/sink event
  projection with a same-wave consumer and row-moving gate, not by retaining
  scanner output (`p2d-substrate-tape.md:125`-`:137`); D1-D5 carry scalar,
  parity/checkasm, and consumer language (`p2d-substrate-tape.md:146`-`:351`).
  P2-E keeps P2E-1 through P2E-8 in scalar-reference-sketch form with checkasm or
  unit parity expectations and same-wave consumer notes
  (`p2e-parse-that-gaps.md:35`-`:121`).

## Blockers / Fold Requirements

None for CH6.

## Disposition

V3 removes the remaining paper-close risk for CH6. The CSS row scopes are now
conditional row-production scopes requiring strict equality plus either fresh
narrow parser evidence or same-wave strict row movement, not primitive
validation and not a deferred promise. The prior accepted boundaries for
dav1d/FFmpeg/VideoLAN authority, Arm ISA citation, SIMD inventory exclusion,
Lock 1 substrate shape, and scalar/checkasm/same-wave-consumer admission remain
intact.
