[package]
name = "bbnf-ir"
version = "0.1.3"
edition = "2024"
license = "MIT"
description = "Canonical Grammar IR for the BBNF compiler pipeline"
repository = "https://github.com/mkbabb/bbnf-lang"

[dependencies]
serde = { version = "1", features = ["derive", "rc"] }
serde_json = "1"
rmp-serde = "1"
regex = { version = "1.11", features = ["perf"] }
