# SK-V15 W6 Challenge: Typed Retime Plan

Status: ACCEPT.
Date: 2026-05-28.
Plan: `skv15-W6-plan.md`.

## CH1 Correctness

ACCEPT. The plan names one measurable command and defines both Track 1 and
comparator workloads. It does not claim that CSS is admitted before measurement.

## CH2 Generality

ACCEPT. The plan uses the root typed CSS provider already admitted in W5 and
does not add a new substrate, sidecar event vector, or sixth BackendShape.
Generic surface edits are limited to bench/report/gate consumption.

## CH3 Regression

ACCEPT. The plan explicitly blocks W8R floors, broadcast rows, fact-stream
string admission, `CssFullParseSummary`, and `lightningcss` live admission. A
measured Track 1 loss routes W6 instead of preserving the old proof.

## CH4 Cost

ACCEPT WITH BOUND. The implementation is plausible only if W6 avoids rebuilding
the seven skinny CSS generated runtimes in this first redress. If the typed
report exposes a larger retirement cascade, record a routed W6 reject rather
than exceeding the wave budget.

## CH5 Hidden Coupling

ACCEPT. The plan's same-run command prevents one measurement tuple from being
broadcast into 24 feature rows. The gate must reject `full_parse_summary`,
`fact_stream`, and W8R origins in live W6 typed telemetry.

## CH6 Next-Wave Impact

ACCEPT. W7 can proceed on W6 admitted or routed. The redress packet must record
whether CSS got a fresh typed floor or remained blocked by measured Track 1
loss.

## CH7 Overfit-Prune

ACCEPT. The intervention does not tune toward a named CSS corpus row or reuse
historical numbers. It treats a losing typed run as a successful old-proof
retirement route, which is the non-contrived outcome if the measurement fails.
