# CH4 COST — S-P0 Overfit Audit Hardening V4 Disposition

Lens binding: `restart/prompts/ORCHESTRATOR.md §3W CH4` ("LOC budget,
risk class, wave alignment, and hard cap are stated and realistic;
same-wave consumer present per kernel/primitive"). Dispatch scope
implicit from the V4 micro-redispatch commit `f8e279877` (single-file
atomic, +9/-4 on SYNTHESIS-AUDIT-OVERFIT.md alone; A1/A2/A3/A4/A5/A6
STAND verbatim per `git diff 007624849..f8e279877 --stat` returning
prose only on the SYNTHESIS file). V3 disposition (`audit-overfit/
hardening/V3/CH4.md`) closed at 100 % ACCEPT with the four-version
unbroken envelope-stability chain (V5 alpha-hardening → V1 → V2 → V3);
V4 confirming pass verifies that the two V4 micro-fold packets close
both V3 NEW orphans (CH2 N-V3-CH2-1 inclusion-relation defect; CH7
§1.5 inheritance-miss cross-flag) with zero LOC/risk/cap drift, that
SK-V14 SYNTHESIS C-1..C-5 envelopes hold verbatim across now five
consecutive lens cycles, and that the V4 fold pattern extends rather
than perturbs the institutional cost surface established under V1+V2+V3.

The V4 micro-fold packet inventory (verified against commit
`f8e279877` tail diff):

1. **F-V4-SYNTHESIS-1** — `SYNTHESIS-AUDIT-OVERFIT.md §2.4:311`
   inclusion-relation defect closure. Prior V3 prose read "42 files
   carry the fake header including 14 codegen-side files (8
   providers+templates + 6 ancillary)" — arithmetically wrong because
   the 42 (runtime-side mirror under `runtime/src/grammars/`) and 14
   (codegen-side under `codegen/src/`) sets are **disjoint** under the
   lint glob brace-expansion `{runtime/src/grammars,codegen/src}/**/
   *.rs`, not subset-related. V4 replaces with "56 files carry the
   fake header (42 runtime-side mirror + 14 codegen-side (8
   providers+templates + 6 ancillary)) — disjoint sets per the lint
   glob brace-expansion `{runtime/src/grammars,codegen/src}/**/*.rs`;
   `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/
   runtime/src/grammars/ | wc -l = 42` and `git grep -l '@generated
   by skinny bbnf-codegen' skinny/crates/codegen/src/ | wc -l = 14`,
   summing to the 56 total with zero overlap (the two path roots are
   siblings)." Net +5 / -1 lines in §2.4.
2. **F-V4-SYNTHESIS-2** — `SYNTHESIS-AUDIT-OVERFIT.md §3.1:347` C-3
   row prose cell `3 scanners are fixture lookups` → `4 scanners are
   fixture lookups`. Final residual cell from the V2 F-V2-SYNTHESIS-4
   + V3 F-V3-SYNTHESIS-3 arithmetic-correction sweep that propagated
   the 3→4 count update across §0.1 + §1.1:81 + §1.2:122 + §5.1 +
   `generator-truth.md:56` but did not enumerate the §3.1 C-3 row.
   Net +0 / -0 (in-place single-character edit; 3 → 4).

## §0 — Disposition summary

- **V4 ACCEPT-rate: 100 % (18 / 18 sectioned dispositions).**
- **V4 REJECT count: 0.**
- **V4 REVISE count: 0.**
- **Critical findings: 0.**
- **CH4 V4 verdict: ACCEPT.** Both V4 micro-fold packets are
  envelope-neutral on the CH4 cost surface. The §3.1 coverage table
  arithmetic remains 41 + 7 + 11 + 4 + 11 = 74 verified by direct read
  of `SYNTHESIS-AUDIT-OVERFIT.md:345-350`; the SK-V14 SYNTHESIS
  C-1..C-5 LOC envelopes (2.8k–3.4k / 600–1.08k / 1.2k–2.0k /
  800–1.4k / 250–500; total 5.65k–8.38k) hold verbatim verified by
  direct read of `restart/skinny/tranches/sk-v14/SYNTHESIS.md:271-275`;
  the PRUNE-4 sub-wave count remains 9 with the same 9 × 30 = 270-min
  cluster wall-clock; the V5 alpha-hardening F-V5-α-E-1 roster-count-
  agnostic phrasing absorbs the V4 inclusion-relation closure (42
  runtime + 14 codegen = 56 total replaces "42 including 14" without
  re-arithmetic on the §3.1 finding-count column) and the V4
  arithmetic-correction final cell (3 → 4 in §3.1 C-3 row) without
  shifting any C-N count column or envelope.

### §0.1 — V4 cost-axis verification points

The V4 micro-fold dispatch carries two explicit verification points;
both ratified by independent re-execution at HEAD `f8e279877`:

**Point (1): F-V4-SYNTHESIS-1 closes the inclusion-relation defect
with the disjoint 42 + 14 = 56 arithmetic.** VERIFIED via independent
re-execution:

```
$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ | wc -l
      42

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/ | wc -l
      14

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ \
    skinny/crates/codegen/src/ | wc -l
      56
```

The 42 + 14 = 56 sum equals the combined-grep count exactly, proving
zero overlap between the two roots. The V4 prose at §2.4:311-318
recites both individual counts inline plus the 56 aggregate plus the
disjointness rationale. Cost-axis impact: the lint glob mechanism is
unchanged from V3 (the brace-expansion glob `{runtime/src/grammars,
codegen/src}/**/*.rs` was already cited at §2.4:307 in the V3 prose
as the binding lint scope); the V4 edit only refines the count-and-
relation prose on lines 311-318 to match the disjoint reality. The
+5-10 LOC absorption inside the C-3 200-LOC validator slice OR
LOCKS.md companion-lint amendment both HELD verbatim from V1+V2+V3
attribution paths. **C-3 envelope HELD at 1.2k–2.0k**; **LOCKS.md
zero-source alternative HELD**; no C-6 candidate required.

**Point (2): F-V4-SYNTHESIS-2 closes the final arithmetic-correction
residual cell.** VERIFIED via independent re-execution:

```
$ grep -lE 'CANONICAL_FIXTURE|CAPTURED_W2_INPUT' \
    skinny/crates/codegen/src/css_l4_*_templates/generated.rs \
    | sort -u
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/generated.rs
skinny/crates/codegen/src/css_l4_nested_layout_templates/generated.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/generated.rs
skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_templates/generated.rs
```

Four distinct files = 3 CANONICAL_FIXTURE-bearing files
(`at_rules_and_media`, `nested_layout`, `vendor_and_custom_atrules`)
+ 1 CAPTURED_W2_INPUT-bearing file (`stylesheet_selectors`); the 4-
of-7 fixture-lookup count is the correct enumeration matching every
other site in SYNTHESIS (§0.1, §1.1:81, §1.2:122, §5.1) and
`generator-truth.md:56`. V4 closes the §3.1:347 final residual cell.
Cost-axis impact: enumeration precision only. The four fixture-lookup
templates fold into PRUNE-2 (C-5) for revert + R4 (C-3) for
generative replacement; the +1 template over the V1 count delta is
+0 LOC delta in the deletion target because PRUNE-2 already deletes
all 7 CSS template directories wholesale and R4 already generates all
7 from the 15 `.bbnf` files. **C-3 envelope HELD at 1.2k–2.0k**;
**C-5 envelope HELD at 250–500**.

**Point (3, ratification): A1, A2, A3, A4, A5, A6 STAND verbatim
under V4 — V4 introduces zero axis-file edits.** VERIFIED via direct
diff:

```
$ git diff 007624849..f8e279877 --stat \
    -- restart/skinny/tranches/sk-v14/audit-overfit/
       sk-v14-audit-overfit-{css-measurement,admit-mechanism,\
       lock14-scan,generator-truth,decision-engine,\
       pre-restart-pattern}.md
(empty — zero bytes touched on all six axis files)

$ git diff 007624849..f8e279877 --stat \
    -- restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md
 .../audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md | 13 +++++++++----
 1 file changed, 9 insertions(+), 4 deletions(-)
```

V4 commit roster is **SYNTHESIS-only** (zero axis touches), per the
V3 CONSOLIDATED §2.3 + §3 forecast ("Single SYNTHESIS V4 agent. Two
atomic prose edits at SYNTHESIS-AUDIT-OVERFIT.md lines 311 + 342.
~5 minutes total edit time. A1 + A2 + A3 + A4 + A5 + A6 STAND
verbatim at V4"). The A1 8-finding ledger, A2 9-finding ledger,
A3 30-finding ledger (11C/6H/5M/8L after V2 H3→L8), A4 16-finding
ledger (with the V3 F-V3-A4-1 line-cite refresh holding), A5 4-
finding ledger (with the V3 F-V3-A5-1 LOW action-class restoration
holding), and A6 7-finding ledger all map to the same C-1..C-5 cells
they mapped to under V1+V2+V3 ACCEPT.

### §0.2 — Five-cycle envelope stability now established

The SK-V14 SYNTHESIS C-1..C-5 LOC envelopes are now verified verbatim
across five consecutive cycles:

| Cycle | Verdict | Cross-cycle delta on C-1..C-5 |
| --- | --- | --- |
| **V5 alpha-hardening** | ACCEPT | α-E §2 ratified 5.65k–8.38k total |
| **V1** (S-P0 challenge) | ACCEPT | synthesis §3 C-1..C-5 verbatim from SK-V14 SYNTHESIS |
| **V2** | ACCEPT | 11 V2 fold packets envelope-neutral; co-derivation note REDUCES risk-weighting on PRUNE-4 |
| **V3** | ACCEPT | 5 V3 micro-folds envelope-neutral; two cross-flag closures (CH1 line-count, CH5 codegen prose precision) |
| **V4** | ACCEPT | 2 V4 micro-folds envelope-neutral; two orphan-REVISE closures (CH2 inclusion-relation, CH7 §1.5 inheritance-miss) |

The five-cycle chain operates as institutional evidence that the
SK-V14 prune-list cost surface is structurally stable — every
micro-fold cycle since V5 alpha-hardening has refined audit-document
precision without perturbing the C-1..C-5 envelopes, the §3.1
coverage arithmetic, the PRUNE-4 sub-wave count, the wall-clock
caps, or the §2.4 CH7-companion attribution paths. The S-P3 plan
work that consumes this surface can rely on the 5.65k–8.38k total
LOC budget, the 9-sub-wave PRUNE-4 size, the 30-min sub-wave wall-
clock cap, and the C-3 OR LOCKS.md attribution split as the
binding-stable inputs.

## §1 — Per-fold disposition table

| Fold | V3 → V4 change | V4 disposition | Cost-axis notes |
|---|---|---|---|
| **F-V4-SYNTHESIS-1** | SYNTHESIS §2.4:311 inclusion-relation defect: `42 files … including 14 codegen-side (8 providers+templates + 6 ancillary)` → `56 files (42 runtime-side mirror + 14 codegen-side (8 providers+templates + 6 ancillary)) — disjoint sets per the lint glob brace-expansion`, plus inline executable-verification quote of both individual counts plus aggregate | ACCEPT | Closes CH2 V3 §4.1 N-V3-CH2-1 LOW orphan; the disjointness arithmetic is verified by `git grep` returning 42 + 14 = 56 with zero overlap; lint glob mechanism unchanged (brace-expansion glob already cited at §2.4:307 in V3 prose); **C-3 envelope HELD**; **LOCKS.md attribution HELD**; +5-10 LOC absorption path either way unchanged. |
| **F-V4-SYNTHESIS-2** | SYNTHESIS §3.1:347 C-3 row prose cell `3 scanners are fixture lookups` → `4 scanners are fixture lookups` | ACCEPT | Closes CH7 V3 §1.5 informational cross-flag (final residual cell from F-V2-SYNTHESIS-4 + F-V3-SYNTHESIS-3 arithmetic-correction sweep); independent `grep -lE 'CANONICAL_FIXTURE\|CAPTURED_W2_INPUT'` returns 4 distinct files; PRUNE-2 deletes all 7 CSS template directories wholesale, R4 regenerates all 7; **+0 LOC delta in deletion target**; **C-3 + C-5 envelopes HELD**. |
| **§3.1 coverage table arithmetic** | 41 + 7 + 11 + 4 + 11 = 74 | ACCEPT | Unchanged from V1 + V2 + V3; per-finding mapping intact; zero orphan; F-V4-SYNTHESIS-2 changes prose only inside C-3 row, not the count column (still 11). |
| **SK-V14 SYNTHESIS C-1..C-5 envelopes** | 2.8k–3.4k / 600–1.08k / 1.2k–2.0k / 800–1.4k / 250–500 | ACCEPT | Verified by direct read of `restart/skinny/tranches/sk-v14/SYNTHESIS.md:271-275`; total ≈ 5.65k–8.38k preserved; **five-version stable now** (V5 alpha → V1 → V2 → V3 → V4). |
| **C-1 PRUNE-4 sub-wave count** | 9 sub-waves (css_pretty included) | ACCEPT | F-V2-SYNTHESIS-2 co-derivation note continues to reduce risk-weighting (one evidence + three observer confirmations); 9 × 30 = 270-min cluster wall-clock HELD; **C-1 envelope HELD at 2.8k–3.4k**. |
| **§2.1 R4 → PRUNE-2 sequencing** | Unchanged | ACCEPT | Binding text intact; no V4 fold touched the A4 §4 quote or the SYNTHESIS §2.1 verbatim re-citation; envelope-neutral. |
| **§2.2 C-1 → C-4 sequencing** | Unchanged | ACCEPT | A5 §4.1 quote intact; F-V3-A5-1 + F-V3-SYNTHESIS-1 C-4 entry-gate invariant phrasing carries forward into V4 without revision; envelope-neutral. |
| **§2.3 PRUNE-4 sub-wave count = 9 not 8** | Unchanged | ACCEPT | A3 / A5 / A6 cross-confirms intact; F-V2-SYNTHESIS-2 co-derivation rationale preserved; 270-min cluster wall-clock unchanged. |
| **§2.4 CH7-companion extensions** | F-V4-SYNTHESIS-1 closes inclusion-relation prose drift; brace-expansion lint glob mechanism unchanged | ACCEPT | Both attribution paths (C-3 200-LOC validator slice OR LOCKS.md amendment) HELD; the brace-expansion glob already declared at §2.4:307 in V3 prose covers both roots; **C-3 envelope HELD**; **LOCKS.md zero-source alternative HELD**. |
| **A1 / A2 / A3 / A4 / A5 / A6 unchanged** | STAND verbatim per V4 commit roster | ACCEPT | Verified via `git diff 007624849..f8e279877 --stat` returning **SYNTHESIS-only**; all six axis files zero-byte diff; all 8+9+30+16+4+7 = 74 findings map to same C-N cells as V1+V2+V3 ACCEPT; envelope-neutral. |
| **C-5 → C-4 sequencing edge** | Unchanged from V3 reinforcement | ACCEPT | Sequencing edge binding through SK-V14 SYNTHESIS §0.3 R3 ("PRUNE waves before any new admit attempt"); V4 prose touches the §3.1 C-3 row only at the scanner-count cell, not the sequencing manifest; envelope-neutral. |
| **Risk-weighting on PRUNE-4** | Preserved through V4 | ACCEPT | Three observer confirmations of one evidence (`css_pretty` addition); higher-confidence single delta; S-P3 can plan against without defensive over-allocation; **+30-min cluster wall-clock HELD**. |
| **V3 cross-flag closures (CH1 + CH5)** | Both closed under V3; V4 does not re-open | ACCEPT | F-V3-A4-1 (CH1 line-count) and F-V3-SYNTHESIS-2 (CH5 codegen prose precision) both verified RETIRED in V3 CH4 §3.2; V4 makes no edits that would re-open either; envelope-neutral. |
| **V3 → V4 orphan REVISE closures (CH2 + CH7)** | F-V4-SYNTHESIS-1 closes N-V3-CH2-1; F-V4-SYNTHESIS-2 closes CH7 §1.5 cross-flag | ACCEPT | Both V3 NEW orphans were single-cell SYNTHESIS prose touches classified by surfacing lens as below fatal threshold (CH2 LOW; CH7 informational); both close cleanly under the §3.1 finding-count column unchanged and the §2.4 lint glob mechanism unchanged; envelope-neutral. |
| **Lint glob brace-expansion `{runtime/src/grammars,codegen/src}/**/*.rs`** | Already cited at §2.4:307 in V3; V4 re-cites at §2.4:314 inside the verification quote | ACCEPT | Mechanism unchanged; +5-10 LOC absorption inside C-3 200-LOC validator slice OR LOCKS.md companion-lint amendment continues unchanged under both attribution paths; the brace-expansion is correctly disjoint-covering for the 42 + 14 = 56 file set. |
| **§3.1 §3.2 §3.3 no C-6 candidate** | Re-affirmed at line 363 ("No C-6 candidate is required") | ACCEPT | V4 introduces neither a new C-N candidate nor a new wave; the two micro-folds are entirely within the SYNTHESIS prose surface; sub-wave count summary at §3.3 (PRUNE-1 / PRUNE-2 / PRUNE-3 / PRUNE-4 = 9 / PRUNE-5) unchanged. |
| **F-V5-α-E-1 roster-count-agnostic phrasing** | Continues to absorb V4 census refinements | ACCEPT | V4 introduces no count-column re-arithmetic on the §3.1 finding totals; the inclusion-relation refresh (42 + 14 = 56 vs 42 "including" 14) and the fixture-lookup scanner refresh (3 → 4 in C-3 row prose) are both decompositions of pre-existing aggregates that the F-V5-α-E-1 phrasing already absorbs as documentary precision. |
| **Total V4 LOC change** | +9 / -4 (single file: SYNTHESIS-AUDIT-OVERFIT.md) | ACCEPT | Zero source LOC touched; 100 % audit-documentation prose; concentrated entirely within two paragraphs (§2.4:311-318 and §3.1:347); no spread-out scribbling pattern; minimal-surface fold roster. |

## §2 — Critical findings

**None.** No CH4-axis CRITICAL surfaces among the two V4 micro-fold
packets.

The cost surface is intact under V4: both V4 folds are documentary-
precision refinements (inclusion-relation rewrite from "42 including
14" to "42 + 14 = 56 disjoint"; enumeration alignment from "3 scanners"
to "4 scanners"). Neither fold perturbs any C-N envelope, the §3.1
coverage arithmetic, the PRUNE-4 sub-wave count, the 30-min wall-clock
cap per sub-wave, the §2.4 CH7-companion attribution paths, or the
lint glob brace-expansion mechanism. The V4 commit roster is
**SYNTHESIS-only** per the V3 CONSOLIDATED §3 forecast; the V4 axis
roster predicted "A1 + A2 + A3 + A4 + A5 + A6 STAND verbatim at V4"
landed exactly as forecast.

Five-version unbroken envelope-stability chain:

- **V5 alpha-hardening** ACCEPT — α-E §2 ratified 5.65k–8.38k total.
- **V1** ACCEPT — synthesis §3 C-1..C-5 verbatim from SK-V14
  SYNTHESIS.
- **V2** ACCEPT — 11 V2 fold packets envelope-neutral; co-derivation
  note REDUCES risk-weighting on PRUNE-4.
- **V3** ACCEPT — 5 V3 micro-fold packets envelope-neutral; two
  cross-flag closures retire V2 hand-off concerns (CH1 + CH5).
- **V4** ACCEPT — 2 V4 micro-fold packets envelope-neutral; two
  orphan-REVISE closures retire V3 hand-off concerns (CH2 inclusion-
  relation + CH7 §1.5 inheritance-miss).

## §3 — V5 fold recommendations

**None required.** CH4 V4 returns 100 % ACCEPT; neither V4 fold
introduces LOC/risk/cap drift; SK-V14 SYNTHESIS C-1..C-5 hold
verbatim across five lens cycles; both V3 orphan concerns are closed
by F-V4-SYNTHESIS-1 and F-V4-SYNTHESIS-2 respectively. No fold
candidates surface for V5.

### §3.1 — Informational notes (below action threshold; carried from V3)

The two documentary propagations from V3 CH4 §3.1 remain unchanged
under V4 — neither is a cost expansion, both are S-P3 plan-time
propagations:

1. **SK-V14 SYNTHESIS §3 line 271 + §2 line 95** continues to cite
   "8 sub-waves" + "64 hand-written" (verified via `grep -n "8 sub-
   waves\|64 hand-written" restart/skinny/tranches/sk-v14/
   SYNTHESIS.md` returning lines 95 + 271 verbatim). The audit-
   overfit SYNTHESIS at §2.3 + §3.3 already updates this to 9 / 67
   for the `css_pretty` addition; S-P3 propagates the update with
   the +30-min wall-clock delta acknowledged within the existing
   C-1 2.8k–3.4k envelope. V4 introduces no new documentary
   propagation requirement.

2. **§2.4 CH7-companion attribution** remains open between C-3
   sub-task (1.2k–2.0k headroom absorbs the +5-10 LOC lint glob
   walker extension) OR LOCKS.md amendment (zero source LOC for
   the companion lint; check-X subcommand pairing stays in C-3
   either way). CH4 V1 §3.1 + V2 §0.1 point 2 + V3 §0.1 point 3
   + V4 §0.1 point 1 all preserve both attribution paths as
   envelope-neutral; S-P3 chooses at plan time.

### §3.2 — Cross-lens hand-off (V3 carry-overs closed; V4 introduces zero)

V4 closes both V3 NEW orphan REVISEs inline:

- **CH2 (GENERALITY):** F-V4-SYNTHESIS-1 retires the V3 §4.1
  N-V3-CH2-1 LOW inclusion-relation defect; the disjointness
  arithmetic (42 + 14 = 56) is verified inline-cited in the V4
  prose at §2.4:311-318. CH2 V4 may dispose the concern as CLOSED.

- **CH7 (OVERFIT-PRUNE):** F-V4-SYNTHESIS-2 retires the V3 §1.5
  inheritance-miss informational cross-flag; the §3.1 C-3 row prose
  cell now reads "4 scanners are fixture lookups" matching the
  five other sites SYNTHESIS uses to cite this count. CH7 V4 may
  dispose the concern as CLOSED.

Remaining V3 cross-lens bindings (CH1, CH3, CH5, CH6) unchanged
under V4 — each was envelope-neutral at V3 and remains so:

- **CH1 (CORRECTNESS):** F-V3-A4-1 line-count refresh holds; V4
  introduces zero new citations or counts that would require CH1
  re-verification beyond the two V4 verification quotes (already
  re-executed under §0.1 Points 1 + 2 above).
- **CH3 (REGRESSION):** No V4 fold reclassifies a finding severity,
  re-opens a V13 audit-CLEAN route, or silently reverses a V13
  REDRESS REJECT; the 74-finding aggregate and severity distribution
  (31C / 20H / 12M / 11L) hold verbatim through five cycles.
- **CH5 (HIDDEN COUPLING):** F-V3-SYNTHESIS-2 prose precision closed;
  the V4 inclusion-relation rewrite reinforces (not perturbs) the
  parallel-substrate boundary by making the disjointness of the
  runtime-side and codegen-side roots explicit; the identical-
  content round-tripping vector A4 finding 15 remains bounded by
  the brace-expansion lint glob.
- **CH6 (ANTI-PAPER-CLOSE):** V3 F-V3-A5-1 + F-V3-SYNTHESIS-1
  paired action-class propagation holds; V4 introduces no new
  verdict-line, action-class, or scaffold-as-load-bearing surface;
  CH6 V4 should re-confirm the now-fully-coherent verdict-line
  pattern at the same 100 % rate.

### §3.3 — Five-cycle pattern observation (informational; CH4 has no fold action)

The V2 → V3 → V4 sequence has institutionalised one inheritance-
remediation pattern (action-class column edits and count-column
edits must be simultaneously propagated into all per-paragraph
closing summaries within the same axis file AND all cross-axis
summary tables that cite the same finding); CH6 V3 §4 rec 2
explicitly named this pattern as a forward-looking §3W companion
gate addition for future S-P0 cycles. From the CH4 cost-axis
perspective, this pattern is **envelope-neutral by construction** —
every instance surfaced so far (V2 action-class; V3 8→14 codegen
count + 3→4 scanner count; V4 inclusion-relation + 3→4 C-3 cell)
is a documentary-precision refinement, not a count-column re-
arithmetic. The §3.1 finding-count column (41 / 7 / 11 / 4 / 11 = 74)
has held verbatim through five cycles regardless of these prose
refinements. The CH4 forward-binding for SK-V{N+1} S-P0 cycles is
that the institutional pattern stays cost-neutral so long as fold
rosters address prose-cell drift only and never spill into count-
column re-arithmetic; if a future cycle ever surfaces a count-column
inheritance miss (i.e. a §3.1 cell whose number itself is wrong),
CH4's envelope-neutrality posture would need re-evaluation.

## §4 — Fresh-finding scan

Per V4 dispatch scope ("Fresh-finding scan"), the V4 micro-fold
inventory was scanned for any new CH4-axis concern not surfaced
under V1, V2, or V3:

- **LOC drift:** None. Both V4 folds are single-paragraph in-place
  edits (F-V4-SYNTHESIS-1 = +5 / -1 inside §2.4:311-318; F-V4-
  SYNTHESIS-2 = +0 / -0 single-character `3 → 4` in §3.1:347 C-3
  row prose cell). The +9 / -4 commit-stat total is concentrated
  in two paragraphs; zero source LOC touched.
- **Risk-class drift:** None. No V4 fold reclassifies a finding
  severity. The V2 F-V2-A3-1 H3 HIGH→LOW reclassification remains;
  no V3 or V4 reclassifications exist. No new VERY-HIGH risk surface
  introduced. The V4 inclusion-relation closure refines the lint
  glob coverage prose without expanding the glob's actual scope.
- **Hard-cap drift:** None. The 30-min sub-wave wall-clock cap
  remains bound to the C-1 PRUNE-4 9-sub-wave cluster at 270-min
  total. No V4 fold proposes a new wave; the 2 micro-folds are
  entirely within the audit-documentation prose surface, not the
  SPEC wave manifest.
- **CH7-companion attribution drift:** None. Both attribution paths
  (C-3 sub-task or LOCKS.md amendment) preserved verbatim; F-V4-
  SYNTHESIS-1 strengthens the prose precision around the lint glob's
  brace-expansion coverage without changing the glob mechanism or
  LOC absorption envelope.
- **Same-wave consumer presence:** Unchanged. C-1 → C-4 sequencing
  edge ensures W8/W9 consumers (per SK-V14 SYNTHESIS §3 C-4
  falsifiability gate) land in the same wave as the W8/W9 scaffold-
  to-load-bearing wiring; F-V3-A5-1 + F-V3-SYNTHESIS-1 gate-rejection
  invariant inside C-4 entry-gates carries through V4 unchanged.
- **C-1..C-5 envelope arithmetic:** Unchanged. The 2.8k–3.4k /
  600–1.08k / 1.2k–2.0k / 800–1.4k / 250–500 budget verifies
  byte-identically against `SYNTHESIS.md:271-275` at HEAD
  `f8e279877` (no source touched; the SYNTHESIS file itself was
  not modified by V4).
- **§3.1 coverage arithmetic:** Unchanged. 41 + 7 + 11 + 4 + 11 = 74
  verified by direct read of `SYNTHESIS-AUDIT-OVERFIT.md:345-350`;
  F-V4-SYNTHESIS-2 modifies the prose-cell content of the C-3 row's
  finding-list cell ("3 scanners" → "4 scanners") but does not
  modify the C-3 row's count column (still 11).
- **Brace-expansion glob mechanism:** Unchanged. `{runtime/src/
  grammars,codegen/src}/**/*.rs` was already declared at §2.4:307
  in the V3 prose as the binding lint scope; V4 re-cites the
  brace-expansion at §2.4:314 inside the verification quote, but
  introduces no new glob, no new lint mechanism, and no new
  attribution path.

**One CH1-class minor informational note (sub-fatal threshold; below
CH4 action threshold; surfaced for CH1 V4 attention if any):**
The V4 prose at §2.4:318 closes with "(the two path roots are
siblings)." Strictly the path roots are `runtime/src/grammars`
(3-deep from `skinny/crates/`) and `codegen/src` (2-deep from
`skinny/crates/`); their structural relation is co-descended-from-
`skinny/crates/` (i.e. `runtime/` and `codegen/` ARE direct siblings
at depth 1, but the lint glob roots themselves are at different
sub-depths). The disjointness claim that this parenthetical supports
holds true and is independently verified (combined `git grep` returns
56 = 42 + 14 with zero overlap); the "siblings" framing is loose
prose decoration around an arithmetic claim that does not depend on
it. CH4 records this as informational only (zero cost-axis impact;
the lint glob brace-expansion mechanism is path-pattern-based not
structural-relation-based, so the siblings framing is documentary
not load-bearing). CH1 V4 may dispose this as ACCEPT-with-NOTE
or no-op per its own threshold; it does NOT gate §3Z LOCK either
way (the disjointness arithmetic underlying the 56 count is correct
and independently re-verified).

**Zero fresh CH4-axis findings.**

## §5 — Summary

CH4 V4 verdict: **ACCEPT** at 100 % (18 / 18 sectioned dispositions).

Both V4 micro-fold packets (SYNTHESIS × 2) are envelope-neutral on
the CH4 cost surface. The §3.1 coverage table arithmetic preserves
41 + 7 + 11 + 4 + 11 = 74; the SK-V14 SYNTHESIS C-1..C-5 LOC
envelopes (2.8k–3.4k / 600–1.08k / 1.2k–2.0k / 800–1.4k / 250–500;
total 5.65k–8.38k) hold verbatim verified by direct read of
`restart/skinny/tranches/sk-v14/SYNTHESIS.md:271-275`; the PRUNE-4
sub-wave count remains 9 with the same 9 × 30 = 270-min cluster
wall-clock; the V5 alpha-hardening F-V5-α-E-1 roster-count-agnostic
phrasing continues to absorb every documentary refinement (V2 per-
row vs per-category, V3 fixture-count 3→4 in §1.1 + codegen prose
precision 8→14, V4 inclusion-relation 42+14=56 + final C-3 cell 3→4)
without re-arithmetic.

Two V3 orphan REVISEs close cleanly under V4:

- **CH2 inclusion-relation defect** (V3 §4.1 N-V3-CH2-1 LOW; "42
  including 14" arithmetically wrong since the 42-runtime and 14-
  codegen sets are disjoint under the brace-expansion lint glob) —
  closed by F-V4-SYNTHESIS-1 amending the prose to "56 files (42
  runtime-side mirror + 14 codegen-side …) — disjoint sets per the
  lint glob brace-expansion `{runtime/src/grammars,codegen/src}/**/
  *.rs`" with executable-verification `git grep -l … runtime = 42`
  + `git grep -l … codegen = 14` quoted inline at SYNTHESIS §2.4.
- **CH7 §1.5 inheritance-miss cross-flag** (V3 §3.1:347 C-3 row
  prose cell "3 scanners are fixture lookups" — final residual cell
  from the F-V2-SYNTHESIS-4 + F-V3-SYNTHESIS-3 arithmetic-correction
  sweep) — closed by F-V4-SYNTHESIS-2 amending the cell to "4
  scanners are fixture lookups" matching the five other sites that
  cite this count.

The V4 commit roster is **SYNTHESIS-only** (zero axis touches; A1
+ A2 + A3 + A4 + A5 + A6 STAND verbatim per `git diff 007624849..
f8e279877 --stat`). The five-version unbroken envelope-stability
chain (V5 alpha-hardening → V1 → V2 → V3 → V4) operates as
institutional evidence that the SK-V14 prune-list cost surface is
structurally stable; S-P3 plan work consuming this surface can rely
on the 5.65k–8.38k total LOC budget, the 9-sub-wave PRUNE-4 size,
the 30-min sub-wave wall-clock cap, and the C-3 OR LOCKS.md
attribution split as binding-stable inputs.

No V5 fold required; the audit's cost surface is sound through five
consecutive lens cycles (V5 alpha-hardening → V1 → V2 → V3 → V4),
all 100 % ACCEPT on the same envelope arithmetic. Per the V3
CONSOLIDATED §3 forecast ("V5 confirming pass per `ORCHESTRATOR.md
§3Z` then closes the two-consecutive-cycle convergence chain →
G-S-P0-CONVERGED → S-P1 dispatch"), V4 CH4 returns the expected
confirming ACCEPT under the strict-reading interpretation
(V3 carried 2 orphans → V4 = first clean cycle → V5 = second clean
cycle → §3Z LOCK), and contributes to the §3Z convergence chain.
