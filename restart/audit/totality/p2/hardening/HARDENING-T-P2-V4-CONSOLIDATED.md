# T-P2 V4 Hardening Consolidated

Pass: T-P2 Research. Cycle: V4.
Date: 2026-05-21.
Scope: six-lens challenge verdict for the V4 folded totality research packet.
Output: this file.

## Verdict

`G-T-P2-V4-CHALLENGE`: ACCEPT.

V4 resolves the V3 revise set. The 2E counted-source IDs now match its local
Source Registry, all 2A-2F `primary_sources_cited` counts are reproducible from
`counted_source_ids`, deferred executable cells are either replaced with exact
commands / first consumer paths or marked non-shortlist, and REDRESS reopen
families map to concrete owners, budgets, rollback paths, abrogate thresholds,
and blockers. The V4 packet remains research authority only: non-shortlist
blockers, command shapes, and REDRESS-slice mappings are not admissions.

Acceptance rate: 6/6 = 100%.
Consecutive accepted cycles: 1 (V4).

| Lens | Disposition | Confirmation reason |
|---|---|---|
| CH1 correctness / provenance | ACCEPT | V4 repaired 2E's counted IDs to `SRC-X86-INTEL`, `SRC-REDRESS`, and `SRC-BBNF-A64` as locally registered rows, kept inherited FFmpeg/dav1d support under `SRC-V2-FOLD`, and preserved reproducible source counts across 2A-2F. |
| CH2 generality / Lock 14 | ACCEPT | Non-shortlist blockers preserve generated-policy and caller-data ownership across CSS/JSON/Sheets pressure; V4 adds no JSON-only generic-crate policy. |
| CH3 regression / REDRESS | ACCEPT | The REDRESS-slice ownership table preserves material-differential requirements and keeps prior REDRESS rejections fenced behind blockers rather than treating them as reopened admissions. |
| CH4 cost / executability | ACCEPT | V4 replaces the V3 deferred cells with exact checkasm/parity commands, concrete first-consumer paths, or explicit non-shortlist blockers; the REDRESS-slice table covers JSON direct rows, PMULL/CSSC/union, source-present primitives, string/digit routes, and parse-that import routes. |
| CH5 hidden coupling / substrate ownership | ACCEPT | V4's slice and non-shortlist rows keep Lock 1 ownership fields authoritative and avoid retained sidecars, public substrates, new BIR / `BackendShape`, and parser-owned runtime streams. |
| CH6 anti-paper-close | ACCEPT | V4 treats command shapes and ownership mappings as routing evidence only; future gates still require strict equality, same-wave consumers, row movement or measured rejection, rollback, and G-Omega discipline. |

## Carry Forward To V5 Confirmation

Run one unchanged-packet confirmation challenge. V5 must challenge the V4
research packet as-is; no dossier or addendum edits are expected before V5. A
second full ACCEPT cycle converges T-P2 and authorizes T-P3 after
`restart/HANDOFF.md` is updated to ready-for-T-P3.

If V5 revises, the consecutive accepted cycle count resets to zero and a V6
fold is required, subject to the pass ceiling / user escalation rules.
