# SK-V14 S-P0 Axis A3 — Lock 14 Generic-Crate Scan

## §0 — Disposition summary

Pass criterion (verbatim from `PASS-0-OVERFIT-AUDIT.md §Scope` row A3): *"Zero CRITICAL or HIGH violations in skinny generic crates."*

- Findings: **CRITICAL = 11**, **HIGH = 6**, **MED = 5**, **LOW = 8** (total **30**).
- Verdict: **FAIL** — the SK-V13 v3 enumeration of 30 violations is reproduced verbatim at SK-V14 baseline; not a single line has moved.
- Confirms / extends SK-V13 audit pack: **CONFIRMS in full**; one adjacent observation surfaced (`parse-that-regex::StringFlags::HAS_ESC` retains JSON-flavored naming, but the v3 audit explicitly classified `bbnf-simd` + `parse-that-regex` as CLEAN once the byte semantics were lifted to the generated config — treated below as DELTA-NOTE, not a new violation).
- New findings (not in SK-V13 audit pack): **1 DELTA-NOTE** (see §2.E); a 9th `crates/core/src/runtime/` directory (`css_pretty`) is present alongside the 8 documented in §1 of the v13 scan — adds Pattern-H surface but A6 owns Pattern-H tally.

## §1 — Methodology

Executable verification per dispatch §A3:

```
$ git grep -n 'RuntimeProvider::Json\|JsonGrammar\|parse_json_grammar\|"json"\|"css_l4"\|css_pretty' skinny/crates/ | wc -l
66
```

Per-file distribution (`git grep -c …`):

| File | Hits |
|------|------|
| `skinny/crates/bbnf-bench/src/bin/gate.rs` | 1 |
| `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` | 7 |
| `skinny/crates/bbnf-bench/src/report.rs` | 20 |
| `skinny/crates/bbnf/src/lib.rs` | 4 |
| `skinny/crates/codegen/src/json_provider.rs` | 2 |
| `skinny/crates/codegen/src/lib.rs` | 18 |
| `skinny/crates/grammar/src/lib.rs` | 5 |
| `skinny/crates/passes/src/decision_csp.rs` | 1 |
| `skinny/crates/passes/src/lib.rs` | 8 |

Sample 5 hits (verbatim):

```
skinny/crates/bbnf/src/lib.rs:47:pub struct JsonGrammar;
skinny/crates/codegen/src/json_provider.rs:4:    "json",
skinny/crates/codegen/src/lib.rs:209:        grammar_profile::RuntimeProvider::Json => {}
skinny/crates/grammar/src/lib.rs:16:pub fn parse_json_grammar(source: &str) -> Result<GrammarIr, GrammarError> {
skinny/crates/passes/src/decision_csp.rs:235:        let resolved = finalize_rule("json", RuleId(0), candidates, active);
```

Per-grammar runtime directory roster:

```
$ find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d
crates/core/src/runtime/google_sheets
crates/core/src/runtime/bnf
crates/core/src/runtime/css_l4
crates/core/src/runtime/ebnf
crates/core/src/runtime/math
crates/core/src/runtime/bbnf
crates/core/src/runtime/json
crates/core/src/runtime/csv
crates/core/src/runtime/css_pretty
```

9 directories present (the v13 audit pack §1 documented 8 grammar dirs; `css_pretty` is the 9th — relevant to A6 Pattern-H but counted here as ambient context).

Skinny-side per-grammar runtime roster:

```
$ find skinny/crates/runtime/src/grammars -mindepth 1 -maxdepth 1 -type d
… 8 grammar dirs (7 css_l4_* + json) + sheets_witness (test-only).
```

Codegen-side provider modules (hand-written backends):

```
$ ls skinny/crates/codegen/src/*_provider.rs
css_l4_at_rules_and_media_provider.rs
css_l4_declaration_values_extended_provider.rs
css_l4_declaration_values_provider.rs
css_l4_nested_layout_provider.rs
css_l4_stylesheet_selectors_provider.rs
css_l4_vendor_and_custom_atrules_provider.rs
css_l4_visual_functions_provider.rs
json_provider.rs
```

8 hand-written per-grammar provider files (Lock-14-forbidden in nominally-generic `codegen` crate root).

RuntimeProvider enum match-arms (`git grep -n 'grammar_profile::RuntimeProvider' skinny/crates/codegen/src/lib.rs`):

```
skinny/crates/codegen/src/lib.rs:167:        grammar_profile::RuntimeProvider::CssL4DeclarationValues => {
skinny/crates/codegen/src/lib.rs:173:        grammar_profile::RuntimeProvider::CssL4DeclarationValuesExtended => {
skinny/crates/codegen/src/lib.rs:179:        grammar_profile::RuntimeProvider::CssL4StylesheetSelectors => {
skinny/crates/codegen/src/lib.rs:185:        grammar_profile::RuntimeProvider::CssL4VisualFunctions => {
skinny/crates/codegen/src/lib.rs:191:        grammar_profile::RuntimeProvider::CssL4AtRulesAndMedia => {
skinny/crates/codegen/src/lib.rs:197:        grammar_profile::RuntimeProvider::CssL4VendorAndCustomAtRules => {
skinny/crates/codegen/src/lib.rs:203:        grammar_profile::RuntimeProvider::CssL4NestedLayout => {
skinny/crates/codegen/src/lib.rs:209:        grammar_profile::RuntimeProvider::Json => {}
```

8 hardcoded match arms over the grammar-named enum — the v3 audit's CRITICAL dispatch reproduction.

Generated-header file count (`git grep -n '@generated by skinny bbnf-codegen' skinny/crates/runtime crates/core/src/runtime | wc -l`):

```
42
```

(Cross-axis: A4 owns the round-trip verification; this number is logged here purely to bound the Lock-14 surface area.)

History verification (no SK-V14 commits land yet): ROLLING-SOTA-DELTA `git log --oneline` head is `7ec4a474c feat(sk-v13-waveW15.1): admit UpdateCenter typed plugin fast path` — confirms SK-V14 starting state == SK-V13 close state.

## §2 — Per-finding ledger

### §2.A — CRITICAL (11)

| # | Severity | Finding | Citation | Status |
|---|---|---|---|---|
| C1 | CRITICAL | Hardcoded `#[path = "grammars/json/mod.rs"]` + 7 CSS L4 siblings at generic runtime root | `skinny/crates/runtime/src/lib.rs:3-26` | CONFIRMS V13 v3 C1 |
| C2 | CRITICAL | `pub mod grammars { pub use crate::generated_json as json; … }` namespace re-export carrying grammar names | `skinny/crates/runtime/src/lib.rs:35-44` | CONFIRMS V13 v3 C1 (sub-arm) |
| C3 | CRITICAL | `RuntimeProvider` enum bakes 8 grammar names into the type system | `skinny/crates/codegen/src/grammar_profile.rs:17-26` | CONFIRMS V13 v3 C2 |
| C4 | CRITICAL | `match` dispatch over `RuntimeProvider` variants in nominally-generic codegen entry-point | `skinny/crates/codegen/src/lib.rs:167-209` | CONFIRMS V13 v3 C3 |
| C5 | CRITICAL | Hand-written `json_provider.rs` in generic `codegen/src/` crate root (renders JSON runtime files; not generated) | `skinny/crates/codegen/src/json_provider.rs:1-20` | CONFIRMS V13 v3 §7 Gap-1 |
| C6 | CRITICAL | Hand-written `css_l4_declaration_values_provider.rs` (one of 7 CSS L4 backends) | `skinny/crates/codegen/src/css_l4_declaration_values_provider.rs` | CONFIRMS V13 v3 §7 Gap-1 |
| C7 | CRITICAL | Hand-written `css_l4_declaration_values_extended_provider.rs` | `skinny/crates/codegen/src/css_l4_declaration_values_extended_provider.rs` | CONFIRMS V13 v3 §7 Gap-1 |
| C8 | CRITICAL | Hand-written `css_l4_stylesheet_selectors_provider.rs` | `skinny/crates/codegen/src/css_l4_stylesheet_selectors_provider.rs` | CONFIRMS V13 v3 §7 Gap-1 |
| C9 | CRITICAL | Hand-written `css_l4_visual_functions_provider.rs` | `skinny/crates/codegen/src/css_l4_visual_functions_provider.rs` | CONFIRMS V13 v3 §7 Gap-1 |
| C10 | CRITICAL | Hand-written `css_l4_at_rules_and_media_provider.rs` | `skinny/crates/codegen/src/css_l4_at_rules_and_media_provider.rs` | CONFIRMS V13 v3 §7 Gap-1 |
| C11 | CRITICAL | Hand-written `css_l4_vendor_and_custom_atrules_provider.rs` + `css_l4_nested_layout_provider.rs` (collapsed to one row — 7th + 8th provider file) | `skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_provider.rs`, `…/css_l4_nested_layout_provider.rs` | CONFIRMS V13 v3 §7 Gap-1 |

### §2.B — HIGH (6)

| # | Severity | Finding | Citation | Status |
|---|---|---|---|---|
| H1 | HIGH | `pub struct JsonGrammar` + `pub fn compile_json_source` + `pub fn compile_json_file` at generic crate root | `skinny/crates/bbnf/src/lib.rs:47-65` | CONFIRMS V13 v3 H1 |
| H2 | HIGH | `pub fn parse_json_grammar` + `pub fn load_json_grammar` at generic crate root | `skinny/crates/grammar/src/lib.rs:16-27` | CONFIRMS V13 v3 H2 |
| H4 | HIGH | JSON codegen template hardcoded as `include_str!`-style hand-written module (`json_templates/{config,generated,parser,value,view,visitor}.rs`) | `skinny/crates/codegen/src/json_templates/` | CONFIRMS V13 v3 §7 Gap-2 |
| H5 | HIGH | CSS L4 codegen templates hand-written (`css_l4_declaration_values_templates/{config,generated,mod,parser}.rs` + 6 sibling template dirs) | `skinny/crates/codegen/src/css_l4_*_templates/` | CONFIRMS V13 v3 §7 Gap-2 |
| H6 | HIGH | CSS L4 entry-rule absence from acceptance-test surface of the decision engine (no CSS L4 entry-rule cited under `decision_csp` constraint set) — structural absence at the acceptance-test boundary; freestanding HIGH on its own merits (no longer derivative of H3, which has been LOW-reclassified per V2 fold F-V2-A3-1) | `skinny/crates/passes/src/decision_csp.rs` (file-wide grep absence) + `skinny/crates/passes/src/lib.rs:478` production call site `finalize_rule(&grammar.name, …)` is generic over grammar name; the acceptance-test surface is the gap | CONFIRMS V13 v3 §7 Gap-3 |
| H7 | HIGH | `bbnf/src/lib.rs:67-89` re-exports `pub mod json { … }` namespace at generic crate root (JSON-named public module beside JsonGrammar) | `skinny/crates/bbnf/src/lib.rs:67-89` | CONFIRMS V13 v3 §7 Pre-Restart Pattern recurrence (#4 PARTIAL) |

### §2.C — MEDIUM (5)

| # | Severity | Finding | Citation | Status |
|---|---|---|---|---|
| M1 | MEDIUM | Test assertions hardcoded to JSON entry rule (`assert_eq!(profile.id(), "json")`) | `skinny/crates/codegen/src/lib.rs:384, 575` | CONFIRMS V13 |
| M2 | MEDIUM | Test asserts CSS L4 explicitly unsupported (`select_runtime_profile_for_name("css_l4").is_err()`) | `skinny/crates/codegen/src/lib.rs:386` | CONFIRMS V13 |
| M3 | MEDIUM | `parse_number_span_from_first` consumed only by JSON; number policy implicitly JSON-only at consumer side (no grammar-config gate) | `skinny/crates/runtime/src/grammars/json/generated.rs:213-215` (via v3 §6) | CONFIRMS V13 v3 §6 number-coupling |
| M4 | MEDIUM | `STRUCTURAL_BYTES: &[u8] = b"{}[],:\""` crate-scoped JSON alphabet in codegen | `skinny/crates/codegen/src/json_templates/config.rs:3` | CONFIRMS V13 |
| M5 | MEDIUM | `RUNTIME_PROFILE: GrammarProfile = GrammarProfile::new("json", …)` static at private-but-resident scope in generic codegen | `skinny/crates/codegen/src/json_provider.rs:3-16` | CONFIRMS V13 |

### §2.D — LOW (8)

| # | Severity | Finding | Citation | Status |
|---|---|---|---|---|
| L1 | LOW | Test fn `json_config_policy_fields_are_consumed` JSON-named | `skinny/crates/codegen/src/lib.rs:351` | CONFIRMS V13 |
| L2 | LOW | Test fn `facade_compiles_json_source` JSON-named | `skinny/crates/bbnf/src/lib.rs:98` | CONFIRMS V13 |
| L3 | LOW | Test fn `facade_parses_json_with_runtime_tape` JSON-named | `skinny/crates/bbnf/src/lib.rs:104` | CONFIRMS V13 |
| L4 | LOW | Test fn `parses_skinny_json_rules` JSON-named | `skinny/crates/grammar/src/lib.rs:389` | CONFIRMS V13 |
| L5 | LOW | Test fn `compiles_json_to_single_plan_bir` JSON-named | `skinny/crates/passes/src/lib.rs:1558` | CONFIRMS V13 |
| L6 | LOW | Test fns `…1684, …1692` JSON-rooted | `skinny/crates/passes/src/lib.rs:1684, 1692` | CONFIRMS V13 |
| L7 | LOW | `JSON_GRAMMAR` const + `parse_json_grammar(JSON_GRAMMAR)` test plumbing recurs across `passes/src/lib.rs:1559, 1588, 1643, 1673, 1685, 1693, 1797` (one ledger row for the file-wide pattern) | `skinny/crates/passes/src/lib.rs:1559-1797` | CONFIRMS V13 |
| L8 | LOW | `finalize_rule("json", RuleId(0), …)` JSON-named test fixture inside `#[cfg(test)] mod tests { … }` block exercising the solver in isolation; production call site at `skinny/crates/passes/src/lib.rs:478` reads `finalize_rule(&grammar.name, …)` and is generic over grammar identity. Reclassified from HIGH (V1 H3) to LOW per V2 fold F-V2-A3-1: violation class is test-fixture name leak in generic crate's test surface (severity tier L6/L7), not production-path overfit; the production call site is grammar-neutral. H6 (CSS L4 entry-rule absence from the decision engine's acceptance-test surface) takes the HIGH bar on its own structural merits, with no derivation chain from H3. | `skinny/crates/passes/src/decision_csp.rs:235` (test-fixture call site, inside `#[cfg(test)] mod tests`); contrast with production call site `skinny/crates/passes/src/lib.rs:478` `finalize_rule(&grammar.name, …)` | CONFIRMS V13 v3 H3 (severity recalibrated HIGH → LOW per V2 fold F-V2-A3-1) |

### §2.E — DELTA-NOTE (new context, not a new violation)

| # | Severity | Observation | Citation |
|---|---|---|---|
| D1 | NOTE | `parse-that-regex` (a generic regex-engine crate, classified CLEAN in v3 §1) defines `StringFlags::HAS_ESC` / `HAS_CONTROL` / `NEEDS_DECODE` as public-API flag aliases. Bit semantics are grammar-neutral (any quoted-string grammar can populate them); naming retains JSON heritage. Treat as **future-rename concern**, NOT a new Lock-14 violation: the bits themselves are opaque and the trait sits in a regex primitive, not in `runtime`/`codegen`. | `skinny/crates/parse-that-regex/src/lib.rs:56-60, 179, 254, 262` |

DELTA-NOTE is reported per the SK-V13 v3 "deep-scan ⇒ deeper-scan" mandate; orchestrator may fold into PRUNE-3 cosmetic rename or carry as documentary-only.

## §3 — Pass criterion verdict

Pass criterion (verbatim): *"Zero CRITICAL or HIGH violations in skinny generic crates."*

Actual count: **11 CRITICAL + 6 HIGH = 17** violations in nominally-generic skinny crates (`runtime`, `codegen`, `bbnf`, `grammar`, `passes`) — every CRITICAL and HIGH from the v13 v3 enumeration reproduces at the same `file:line` coordinates (V1 H3 reclassified HIGH → LOW per V2 fold F-V2-A3-1; H6 retains HIGH on its own structural merits; aggregate finding count unchanged at 30). No new SK-V14 commits have touched these surfaces (verified via `git log --oneline restart/skinny/ROLLING-SOTA-DELTA.md` head = `7ec4a474c`, a SK-V13 W15.1 commit; nor have any of the cited files been re-edited post-`00181742e`).

**VERDICT: FAIL.**

The fail is expected per `S-P0-DISPATCH-CONTEXT.md §2`: "Pattern recurrence vectors STILL PRESENT in source (PRUNE waves not yet dispatched)". The S-P0 charter is to *confirm* the falsified state pre-PRUNE, not to find remediation; remediation is owned by C-1 = PRUNE-3 + PRUNE-4 per SK-V14 SYNTHESIS §3.

## §4 — Recommended prune actions

Cross-reference to SK-V14 SYNTHESIS §3 candidate shortlist C-1..C-5 and SYNTHESIS §0.4 P-1..P-7 pre-blocks (per dispatch context):

### Tier-1 (CRITICAL — 11 violations)

| Finding(s) | Recommended PRUNE | Mechanism |
|---|---|---|
| C1 + C2 (`runtime/src/lib.rs:3-44`) | **PRUNE-4** under C-1 | Replace the 8 `#[path = …]` declarations + the `pub mod grammars { … }` re-export with a metadata-driven registry emitted by the codegen pipeline; grammar paths must come from grammar metadata, not from hand-typed root declarations. |
| C3 (`grammar_profile.rs:17-26`) | **PRUNE-3** under C-1 | Replace `enum RuntimeProvider { Json, CssL4… }` with a trait `EmitProvider { fn emit_runtime_files(…) -> EmittedSource; }` keyed by grammar metadata; the enum is the type-system encoding of overfit. |
| C4 (`lib.rs:167-209`) | **PRUNE-3** under C-1 | Collapse the 8 match arms into a single `profile.provider().emit_runtime_files(…)` virtual dispatch; the JSON `=> {}` empty arm at :209 is itself the residue of pre-restart hand-rolled JSON-special-case. |
| C5..C11 (8 provider modules) | **PRUNE-3** + **PRUNE-2** under C-1 + C-2 | Each `*_provider.rs` is a hand-written grammar-specific backend in a generic crate. Convert to a **single grammar-agnostic provider generator** parameterised by grammar metadata + `.bbnf` schema. Delete all 8 files; the generator authors them. R4 (CSS-L4 grammar reactivation) is the natural pair to PRUNE-2 here — the 15 `.bbnf` grammars at `/grammar/css/l4/` become the SOLE source of CSS L4 codegen. |

### Tier-2 (HIGH — 6 violations)

| Finding(s) | Recommended PRUNE | Mechanism |
|---|---|---|
| H1 + H7 (`bbnf/src/lib.rs:47-89`) | **PRUNE-3** | Move `JsonGrammar`, `compile_json_source`, `compile_json_file`, and `pub mod json` into a per-grammar facade crate (`bbnf-json` OR `bbnf/json/`). The generic `bbnf` crate keeps only `compile_from_source(grammar_name, source)` and `Grammar` trait. |
| H2 (`grammar/src/lib.rs:16-27`) | **PRUNE-3** | Move `parse_json_grammar` + `load_json_grammar` to `grammar/json/` submodule OR delete and call `parse_grammar("json", …)` directly at consumer sites. The generic crate exports only `parse_grammar(name, source)`. |
| H6 (`decision_csp.rs` file-wide grep absence; cf. production call at `passes/src/lib.rs:478`) | **PRUNE-3** under C-1 | Add a CSS L4 entry-rule constraint test under R4 to close the acceptance-test-surface absence; the production call site `finalize_rule(&grammar.name, …)` already accepts any grammar's entry rule generically, so the gap is in test coverage, not solver dispatch. (Companion L8 test-fixture rename — formerly H3 — folds into the bulk L1..L8 rename pass; see Tier-4.) |
| H4 + H5 (`codegen/src/{json_templates,css_l4_*_templates}/`) | **PRUNE-2** | Delete the 6 JSON template files + 28 CSS L4 template files; generate them from `.bbnf` grammar metadata + a single shared template skeleton. The `@generated` headers must be *real* — produced by a `cargo xtask regen` command that takes `.bbnf` input and emits all per-grammar files. (A4 owns the round-trip enforcement.) |

### Tier-3 (MEDIUM — 5; cosmetic / W8-deferred)

M1 + M2: test-only; convert to generic-entry-rule assertions OR move to per-grammar test modules under PRUNE-3.
M3: number policy generalisation — fold into PRUNE-3 once `EmitProvider` trait lands (grammar-config gate at the consumer side).
M4 + M5: crate-scoped JSON constants — acceptable post-PRUNE-3 IF they ride along with the JSON provider migration into a per-grammar location.

### Tier-4 (LOW — 8; cleanup, no architectural cost)

L1..L8: test function renames + test-fixture string-literal renames (L8 `finalize_rule("json", …)` test-fixture call site at `decision_csp.rs:235`); bulk-rename PR after PRUNE-3 lands the directory restructure. No standalone wave required.

### Cross-axis interlock

- **A4 (Generator-vs-Hand-Curated)** owns the round-trip test for the 42 `@generated`-headered files; A3 enumerates the surface, A4 proves they're not actually generated.
- **A6 (Pre-Restart Pattern Recurrence)** owns the 9-directory `crates/core/src/runtime/` Pattern-H tally (we noted `css_pretty` adds a 9th directory beyond the v13 baseline's 8); A3 cites it as ambient context only.
- **CH7 Overfit-Prune lens** binds Pass Alpha SK-V14 → SK-V15: no behaviour wave admits until PRUNE-3 + PRUNE-4 land C1..C11 + H1..H7. The 18-row CRITICAL+HIGH count is the gate.

---

**Scan authored:** 2026-05-23 (SK-V14 S-P0 axis A3).
**Authority:** `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §Scope` row A3 + `restart/skinny/tranches/sk-v14/audit-overfit/S-P0-DISPATCH-CONTEXT.md §A3` + `restart/locks/LOCKS.md §14`.
**Status:** WRITE-ONLY (untracked); orchestrator commits the six A1-A6 outputs atomically per dispatch §3.
**Next gate:** SK-V14 synthesis dispatch consolidates A1-A6; CH1-CH7 challenge over the synthesis; G-S-P0-CONVERGED fires PRUNE-1..5 + S-P1.
