# Pass Omega V10 Locks Diff — SK-V18 GENERALIZATION Discipline

Disposition: proposed amendment, STAGED ONLY, G-Omega gated.

This is the FINAL staged locks diff for the SK-V18 GENERALIZATION cycle. It is
the consolidation of the converged T-P3 `3C-locks-v+1-diff.md` (the 11-clause
SK-V18 addendum disposing all 21 amendment candidates: 9 ACCEPT, 11 MODIFY,
0 REJECT, 1 DEFER). It adds one `## SK-V18 T-P3 v+1 Crystallisation Addendum`
immediately after the existing SK-V17 tape-fold addendum (the Lock 16
NEON-classifier-manifest clause ending at `restart/locks/LOCKS.md:622`) and
before the existing `## v+1 Governance Boundary` (`restart/locks/LOCKS.md:625`).

The diff preserves the 16 numbered locks, preserves the five `BackendShape`
variants `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`, and
adds no directive, BIR variant, substrate, public substrate API, retained
sidecar, lock, lock retirement, or sixth shape. The two co-gate symbols
(`runtime_target_rows_collapsed`, `bbnf_simd_single_mask_convention`) are written
as PLANNED SK-V18-SPEC gates, never as live symbols.

STAGED ONLY: this file does not edit the live `restart/locks/LOCKS.md`. The CRUD
merge executes POST-G-Omega, after user sign-off.

## Verification

```sh
# 16 numbered locks preserved
grep -nE '^[0-9]+\. \*\*' restart/locks/LOCKS.md | head -16   # 1..16 at :75,160,170,179,181,183,200,202,260,269,319,328,336,349,436,453

# Five BackendShape variants in code (no sixth)
rg -n 'BackendShape::(EagerTape|OffsetTape|EventTape|SinkOnly|CollapsedStage)' skinny/crates/codegen/src/lower/mod.rs:18-24
rg -n 'all_backend_shapes\(\) -> \[BackendShape; 5\]' skinny/crates/ir/src/cost.rs:334

# Two PLANNED co-gate symbols are absent (PLANNED, not live)
rg -c runtime_target_rows_collapsed skinny/crates skinny/xtask    # 0
rg -c bbnf_simd_single_mask_convention skinny/crates             # 0

# Diff applies cleanly against live LOCKS.md at HEAD
awk '/^```diff$/{flag=1;next} /^```$/{flag=0} flag{print}' \
  restart/audit/totality/astral/V10/locks-diff.md | git apply --check -
```

All four checks pass at HEAD: 16 locks, five variants, both symbols rg=0,
`git apply --check` exits 0.

## Proposed Unified Diff

```diff
diff --git a/restart/locks/LOCKS.md b/restart/locks/LOCKS.md
--- a/restart/locks/LOCKS.md
+++ b/restart/locks/LOCKS.md
@@ -622,6 +622,33 @@
 - Lock 16 NEON-classifier-manifest clause: the shared NEON `select_classifier(alphabet)` / `scan_structural(input, &StructuralAlphabet)` classifier is registered as a Lock-16 primitive-manifest ROW: abstract primitive = alphabet-parametrised byte classification; scalar reference `scalar/byte_class_from_eq_set_64.rs`; checkasm parity under `BBNF_SIMD_STRICT=1`; `substrate_target = existing_tape`; `retention_lifetime = transient-single-call` (no cross-call retained classifier state per the Lock 1 v+1 ELEVATION, `restart/locks/LOCKS.md:137`-`149`); same-wave consumer = the tape. The eq-set fan is the one proven NEON Layer-1 body (87 LOC, 8 distinct NEON intrinsics); `byte_class_from_table_64` and `bitmap_prefix_xor_64` are honestly-declared `scalar-delegate-non-ASM` passthroughs, not SIMD row-movers. The JSON-first NEON narrative folds to the alphabet-as-data form (`restart/skinny/tranches/sk-v17/SPEC.md:314`-`317`, the alphabet is the only grammar datum). The multi-arch `crates/simd-scan` scope-reconcile (narrow-to-aarch64 vs retain x86/avx2/wasm/scalar kernels) binds WITHOUT admitting x86 as a close path; aarch64 is primary, no SVE (`restart/skinny/tranches/sk-v17/SPEC.md:806`). Evidence: `restart/audit/totality/sk-v17/p1/1E-locks-evidence.md:120`,`:131`,`:183`, `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:582`.
 
 
+## SK-V18 T-P3 v+1 Crystallisation Addendum
+
+This addendum is the SK-V18 T-P3 3C GENERALIZATION-discipline fold. It preserves the 16 numbered locks and the exact five BackendShape variants `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`; it adds no directive, BIR variant, substrate, public substrate API, retained sidecar, lock, lock retirement, or sixth shape. The SK-V17 addendum (`restart/locks/LOCKS.md:610`-`622`) already folds SK-V18's TAPE-substrate and CLASSIFIER discipline; this addendum binds ONLY the SK-V18 GENERALIZATION discipline not yet bound. T-P1 SK-V18 is a near-converged NON-normal-§3Z input (V7 lone clean r=1.000, V8 broke the streak; consec=0, converged=false), not a normal two-clean lock; T-P2 SK-V18 is a near-converged NON-normal-§3Z research input (V1-V5; converged=false, consec=0; only single-cell citation-precision qualifiers from V4, no surviving REJECT). The monotonic direction is skinny→totality. Evidence: `restart/audit/totality/p1/hardening/HARDENING-T-P1-CONSOLIDATED.md:44`-`52`, `restart/audit/totality/p2/hardening/HARDENING-T-P2-CONSOLIDATED.md:17`-`25`, `restart/audit/totality/p1/1E-locks-evidence.md:38`,`:53`-`60`.
+
+- Lock 14/16/8 named-primitive (a)-(d) gate clause: every hand-shaped kernel admitted under the §6 honest-finding escape MUST pass a machine-checked four-conjunct gate — (a) grammar-INVOKED-by-name, (b) emitted-output VARIES under invoking-rule mutation, (c) `verbatim_blob_present == false`, (d) PROFILE-PROVEN-NARROW-LEAF (a single profiled hot leaf with a walk-derived surrounding skeleton). A primitive failing any one of the four is a relabeled hand-written blob and is REJECT. The gate is the per-(a)-(d) `*_abcd_pass` falsifier arms (`named_primitive_falsifier_pass` at G1/G2), not a prose review. Evidence: `restart/skinny/tranches/sk-v18/SPEC.md:358`-`390`,`:284`; `restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md:257`-`266`; `restart/audit/totality/p1/1E-locks-evidence.md:147`.
+
+- Lock 5/14/1 relocated-seam firewall + un-fork clause: the un-forked emitter is ONE `render(program)` body that dispatches on the cost-model-derived `program.policy_summary.backend_shape` (`cost.chosen`); the un-fork is UNBUILT at HEAD (`RuntimeEmitterKind{CompiledLowering,RequestFacts}` still live, `runtime_generator.rs:17`-`25` still branches on `request.profile_contract.emitter`; 1E D-1E-V5-02), and the post-G3 admission END-STATE — `RuntimeEmitterKind` and both variants DELETED and `generator_grammar_branch_count == generator_grammar_type_count == 0` — is a PLANNED-at-G3 gate (`emitter_fork_present` MUST be false at G3, SPEC `:247`), NOT a present-tense fact. `render(program)` reads its output-shape ONLY from `backend_shape` and NEVER from any `RuntimeTarget`/`RuntimeProfileContract`/`profile`/`emitter`/`output_labels` field (`emit_shape_source == lowered_program`; falsifier: grep the render body for any `target.*` read == 0). Dispatch-on-source-family is REJECT; dispatch-on-cost-selected-shape is the iburg-faithful architecture (iburg LOPLAS 1992, DOI 10.1145/151640.151642; back-end stage separation, Aho et al. Ch. 8). md5-distinctness proves the OUTPUT differs per grammar but NOT that the SELECTION is grammar-derived and is NECESSARY-NOT-SUFFICIENT; the structural full-row `PartialEq` co-gate `runtime_target_rows_collapsed` (a PLANNED SK-V18 gate at `restart/skinny/tranches/sk-v18/SPEC.md:247`, not yet a live symbol) is mandatory because the arm-census grep is syntactically incapable of seeing a per-grammar branch relocated into a neutral data table. The firewall extends to the CSS-typed side channel via `css_provider_source == generated` (a PLANNED SK-V18 firewall predicate, distinct from the live `bbnf-bench/src/report.rs` bench-report field of the same name; not yet a codegen/firewall gate symbol): for the SK-V18 G2 SKINNY firewall this scans the skinny CSS surface (`skinny/crates/runtime/src/grammars/css_l4_*` + `runtime_simd.rs`); the totality `crates/core/src/runtime/css_l4/` surface is the SK-V19-adoption seam carried under the same DEFER bundle as the cursor-generality/scanner-unification reconcile (D-SKV18-L01 / MP.SK19.SCANNER-UNIFY), NOT a skinny-cycle scan target (CH5-DEFECT-V1-02; SK-V18 = skinny generalization per D14). If the un-fork cannot dispatch all witnessed grammars on `BackendShape` without a `match grammar` arm, DELETE the un-fork and carry the SPEC-declared two-path unworkability; never ship a shim. Evidence: `restart/skinny/tranches/sk-v18/SPEC.md:179`-`185`,`:247`,`:1100`-`1145`; `restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md:272`-`279`; `restart/audit/totality/p1/1E-locks-evidence.md:148`,`:106`.
+
+- Lock 14/16 neutrality-proof clause: a primitive named neutrally but exercised by ONE grammar in a campaign MUST be PROVEN neutral by ≥1 structurally-compatible non-that-grammar invocation OR demoted to an honestly grammar-scoped name. The gate REJECTS a neutral name with zero structurally-compatible non-that-grammar caller; the `balanced_component_scan` → `css_balanced_component_scan` FORCED demotion (with honest disclosure) IS the discharge, NOT a fabricated cross-grammar caller. The INNER caller-data sub-kernel may remain neutral independently of the recognizer SHELL. A generalisation claim witnessed by fewer than the full 9-grammar roster is SCOPED to the witnessed grammars and may NOT use fleet-wide grammar-neutral wording. Per the live MP.NW6 (`restart/MASTER-PLAN.md:662`) / H.W4.LOCK14 (`:605`) single-negative-control standard, with only ONE non-CSS-non-JSON negative control the result is labelled a `scoped non-JSON witness`, NOT fleet-wide or grammar-neutral closure: SK-V18's un-fork generality is a `scoped non-JSON witness` (CSS + a single Sheets control), and fleet-wide / grammar-neutral wording requires SK-V19 adoption OR both Sheets AND BBNF-self negative-control witnesses in the same wave. SK-V18's PROVE wave PROMOTES Sheets from by-construction (the SK-V17 `:620` clause: Sheets/BBNF-self are by-construction under SK-V18, `sheets_witness` 24-LOC stub) to a by-EXERCISE negative control IF AND ONLY IF PROVE returns non-`N` (binding fallback `N` = "Sheets cannot emit via the generator ONLY → generalization NOT real", surfaced honestly); the third exercised witness DOES NOT YET EXIST at this HEAD (PROVE undispatched) and materialises only on a non-`N` PROVE verdict. The exercised witness set is JSON+CSS exercised at G1/G2 + Sheets-exercised-IF-PROVE-passes, with BBNF-self staying by-construction → SK-V19, and remains a `scoped non-JSON witness` either way — the third-witness claim inherits the PROVE wave's own falsifier rather than presupposing its success. Evidence: `restart/skinny/tranches/sk-v18/SPEC.md:972`-`983`,`:390`,`:1034`,`:7`-`12`; `restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md:231`-`237`; `restart/audit/totality/p1/1E-locks-evidence.md:149`; `restart/audit/totality/p2/2C-grammar-neutrality.md:380`-`381`.
+
+- Lock 16/8 aarch64-ONLY clause: the host is Apple M5 Max / aarch64 ONLY. The whole x86 surface (`skinny/crates/bbnf-simd/src/x86_64/` + `ext/x86/` + the nasm driver + the `diagnostic-x86` gate exclusion) is a P1 DELETION target, not a measured plane; x86/AVX-512/GFNI literature is totality-SECONDARY architecture pressure that closes NO skinny row. This sharpens the SK-V17 aarch64-PRIMARY clause (`restart/locks/LOCKS.md:622`) to aarch64-ONLY. Every aarch64 primitive reports `wired` / `scalar-delegated` / `source-present-blocked` / `strict-checkasm-admitted` / `deleted`, with its exact `hw.optional.arm.FEAT_*` gate and emitted-asm proof for non-baseline features. The M5 Max probe shows FEAT_PMULL/DotProd/I8MM/CSSC/SHA3 = 1 and FEAT_SVE2 ABSENT; SVE/SVE2 primitives must not be filed as NEON/AdvSIMD, and a future `svmatch_u8` requires an SVE2 host and scalable-vector dispatch. Evidence: `restart/skinny/tranches/sk-v18/SPEC.md:51`-`52`,`:130`; `skinny/crates/bbnf-simd/src/lib.rs:5`; `skinny/crates/bbnf-bench/src/lock14_baseline.rs:2463`; `restart/audit/totality/p1/1E-locks-evidence.md:150`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:244`.
+
+- Lock 6/14 verbatim-blob-courier clause: a `@generated` file that is a verbatim `&str` literal in codegen (e.g. `const CSS_GENERATED_RS: &str = r#"`) is hand-written, NOT generator-derived, and is REJECT as "grammar-driven". The binding proof of a real projection is round-trip BYTE-EQUIVALENCE against the deletable oracle — never a ±5% line-count delta and never a header-only `@generated` template. `verbatim_blob_present` MUST be false at the G1/G2 close. This co-binds the Lock-6 v+1 byte-equivalent-regen rule to the SK-V18 verbatim-blob prohibition. Evidence: `skinny/crates/codegen/src/runtime_generator.rs:701`; `restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:50`,`:122`-`124`; `restart/skinny/tranches/sk-v18/SPEC.md:329`-`339`,`:244`; `restart/audit/totality/p1/1E-locks-evidence.md:151`,`:105`.
+
+- Lock 14 green-by-exclusion precondition clause: the Lock-14 gate is currently green-by-exclusion — leak surfaces are parked in the weak `SKV15_W2_EXTRA_COVERAGE_ROOTS`, not the strict `GENERIC_SCAN_ROOTS`. Any wave authoring a new emitter MUST FIRST (P4, which lands BEFORE G2/G3) move the codegen surfaces (`runtime_generator.rs`, JSON sink/typed/template) into strict `GENERIC_SCAN_ROOTS`, extend `FORBIDDEN_GENERIC_TOKENS ⊇ {GENERATED_RS,CSS_GENERATED_RS,EventGrammar,*EventGrammar}` (the certified SK-V18 SPEC form, `restart/skinny/tranches/sk-v18/SPEC.md:711`-`712`: the `GENERATED_RS` courier suffix refines the broad `_RS` substring antecedent at `1E:152`, catching BOTH `CSS_GENERATED_RS` and `JSON_PARSE_ONLY_GENERATED_RS` without the false-positive `_RS` substring catch, and the `*EventGrammar` glob is alias-immune — catching `SHEETS_EventGrammar`/`JsonEventGrammar`-style re-injects the bare token misses; this single form is written byte-identically across 3A-D11/3C-green-by-exclusion/3B-P4/3D-D04), and drop the `diagnostic-x86` exclusion — proven by a re-inject-then-revert RED falsifier (`lock14_gate_scans_codegen == true`). Evidence: `skinny/crates/bbnf-bench/src/lock14_baseline.rs:2420`,`:2442`,`:2463`; `restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:170`-`175`; `restart/skinny/tranches/sk-v18/SPEC.md:690`,`:436`; `restart/audit/totality/p1/1E-locks-evidence.md:152`.
+
+- Lock 16 single-SIMD-substrate + one-movemask clause: the SK-V18 close uses ONE scan substrate (skinny `bbnf-simd`), NOT both `bbnf-simd` AND the upstream `parse-that` `scan/` crate. The `scan_balanced` shell is vendored (re-homed) into `bbnf-simd` and MUST consume the EXISTING `byte_class_from_eq_set_64`/`bracket_depth_mask_64` kernels plus the project's ONE canonical SHRN movemask pack (`vshrn_n_u16::<4>`, `aarch64/movemask.rs:5`) — it must NOT plant a second nibble-LUT classifier or a second `vaddv_u8` shift-add pack convention. A path-dep absence alone is insufficient: a vendor copying the upstream nibble-LUT classifier verbatim passes a path-dep-absence test while planting a second mask substrate. The PLANNED structural co-gate `bbnf_simd_single_mask_convention` (the `bbnf-simd` analog of `runtime_target_rows_collapsed`; not yet a live symbol) asserts every 64-byte→64-bit pack DELEGATES to the one canonical pack, counting DISTINCT non-delegating pack implementations (alias-immune), enforced as an xtask/CI check at G2 entry. NOTE (audit-scope): the balanced-delimiter scan is present in BOTH `parse-that/rust/parse_that/src/parsers/scan/` AND skinny `bbnf-simd` — any future audit must `ls` both trees before declaring a "balanced-scan gap". SCOPE NOTE (CH5-DEFECT-V1-03): this single-movemask falsifier is SKINNY-scoped (`rg build_nibble_luts|find_first_of_nibble_lut skinny/crates/bbnf-simd == 0`); the totality `crates/simd-scan` exports a second `NibbleLut`/`WideLut` classifier convention (`crates/simd-scan/src/lib.rs:67`, the `pub use alphabet::{KernelShape, NibbleLut, StructuralAlphabet, WideLut}` re-export — distinct from the `:68` `{StructuralIndex, next_structural_at_or_after}` probe API), folded into the SK-V19 scanner-unification single-priced disposition owned by `MP-3B-SKV18-D07` (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:177`, the `MP.SK19.SCANNER-UNIFY` row: "the renamed/parallel-scanner risk is ACTIVE … Decide UNIFY vs renamed-parallel-scanner; ≈+217 reconcile + 8/9 OnceCell re-route"), staged into master-plan-diff Diff 4 (the §24 "SK-V19 totality-tree leaks" row, sub-item (c) the `simd-scan` vs skinny `bbnf-simd` probe-API asymmetry: "decide UNIFY vs renamed-parallel-scanner + 8/9 OnceCell re-route") so the DEFER is not a dangling token (COH18-015 / D-SKV18-L01) — the skinny `rg = 0` green is NOT a totality single-substrate proof. Evidence: `skinny/crates/bbnf-simd/src/aarch64/movemask.rs:5` (the ONE canonical SHRN pack); the `vaddv_u8` shift-add pack the one-movemask rule forbids is already replicated NON-delegating ≥three-fold at HEAD — `byte_class_from_eq_set_64.rs:79`-`87`, `bracket_depth_mask_64.rs:74`, and `comment_body_mask_64.rs:68` carry byte-identical `vaddv_u8` shift-add bodies, plus a fourth non-delegating SHRN duplicate at `match_tiny_plain_string.rs:100`; the `bbnf_simd_single_mask_convention` co-gate's distinct-non-delegating-pack count is therefore ≥4 at G2 entry (NOT a single "second" pack), and a G2-entry author must gate against the full multi-pack census, not one named site (the `scalar/` twins carry no such pack); `restart/audit/totality/p2/2F-parse-that-gaps.md:194`,`:196`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:246`.
+
+- Lock 16/14 retarget-not-author clause: the admissible G6 NEON acceleration RETARGETS an already-checkasm-gated kernel (`byte_class_from_eq_set_64`) onto the LIVE generated recursive shell (not a dead/flat sibling), proven by a non-`#[cfg(test)]` caller census over the P3-collapsed singular call site — it does NOT author a new intrinsic loop (an author-from-scratch NEON loop on the CSS path has no scalar-oracle precedence and re-litigates Lock 16). The kernel carries its byte-set as CALLER DATA so the inner eq-set kernel is grammar-neutral even when the recognizer SHELL is CSS-scoped, passes the checkasm differential plus the `neon_significant_skip_matches_scalar` guard over the real corpora, and the skip stops at `([{'"/` so the scalar shell still handles recursion/strings/error-positions. LIVE-PATH TENSE (mirroring the relocated-seam firewall clause's "UNBUILT at HEAD" marker on the substrate's other accelerated leaf): the recursive shell `find_css_significant` (`runtime_simd.rs:169`) is, at HEAD, reached ONLY from the `#[cfg(test)] fn neon_significant_skip_matches_scalar` guard (`lib.rs:574`) and is ABSENT from the generated CSS runtime (`grammars/` rg=0) — DEAD at admission on the live path; so the "passes the guard over the real corpora" assertion is a checkasm/test-corpus FACT, not a built live-path acceleration. The non-`#[cfg(test)]` caller-census admission (the generated singular call site reaching the kernel) is PLANNED-at-G6, NOT a HEAD fact. Any speedup CLAIM DEFERS to the H1 quiet corpus-in-timer re-capture. Evidence: `skinny/crates/runtime/src/runtime_simd.rs:169`,`:180`-`204`; `restart/skinny/tranches/sk-v18/SPEC.md:25`-`35`,`:480`-`482`; `restart/audit/totality/p2/2F-parse-that-gaps.md:195`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:245`.
+
+- Lock 10/16 CollapsedStage shape-slot clause: CollapsedStage occupies the five-shape canon as a SHAPE SLOT ONLY; its lowerer body is `diagnostic-only / author-declared` until BOTH (a) a transient-mask proof — the `ParserState + CollapsedStagePlan` carries no retained per-grammar side stream, only a per-call staged FSM state — AND (b) the aarch64 TBL-classify-class scalar oracle + strict checkasm parity + `collapsed_stage_author_declared` + a same-wave consumer land. This is a BINDING admission gate, not an open question: an undocumented hand-tuned `Collapsed` loop is Lock-16 INADMISSIBLE. A `Collapsed` lowerer threading a per-call FSM mask through retained parsing is the streamed-cursor shape REDRESS 96/97/98 RETIRED (the M5 Max scalar-cheaper-than-SIMD-cursor finding); promotion past `diagnostic-only` must clear that retired prior, not merely the generic transient-mask gate. x86 AVX-512 staged-FSM literature is architecture pressure that cannot close any row. The aarch64 TBL-classify scalar-oracle grounding (2E:73,`:81`) DISCHARGES UNKNOWN-2D-05. The PRIMARY discharge anchor is the U3 directive at `restart/ARCHITECTURE.md:1289` ("CollapsedStage = UNKNOWN-2D-05, no admission without a 2E source-backed … strategy") — chosen as primary because it is demote-STABLE: the companion `:1206` CollapsedStage ledger row (which records CollapsedStage as `target.arch == x86 + target.avx512bw`-bound with the aarch64 candidate "requires a 2E source-backed aarch64 strategy before any aarch64 admission") is CONCURRENTLY a demote-to-diagnostic splice target of the sibling staged `architecture-delta.staged.md` OA-V10-05 (`:1151`/`:1171`/`:1186`/`:1206`), so the SAME-line WORDING may move under the ARCHITECTURE leg; the `:1289` U3 directive carries the same "no admission without a 2E source-backed strategy" bar verbatim and is NOT in the OA-V10-05 splice set. `:1206` is retained as the SECONDARY ledger reference, but it is APPLY-ORDER-DEPENDENT: at the LIVE HEAD this addendum amends, `:1206` is the x86-pinned ledger ROW (`target.arch == x86 + target.avx512bw`, aarch64 candidate annotation inline), so a reader resolving `:1206` AT HEAD lands on the pre-demotion x86 row — the `aarch64 candidate is UNKNOWN-2D-05 (requires 2E source-backed aarch64 strategy before any aarch64 admission)` clause is ALREADY present inline at `:1206` within that x86-pinned ledger ROW, and OA-V10-05 PRESERVES that existing clause verbatim while DEMOTING only the surrounding `target.arch == x86 + target.avx512bw` co-require wording; so a reader at HEAD DOES resolve the discharge clause at `:1206` (embedded in the x86 row), and the SECONDARY reference is apply-order-dependent only as to the SURROUNDING x86 wording, not the discharge clause itself. The AT-HEAD-resolvable bar for this clause is therefore the PRIMARY `:1289` U3 directive (which resolves verbatim at the live HEAD with no apply-order dependency); the `:1206` SECONDARY discharge clause ALSO resolves at HEAD inline, and only its SURROUNDING x86 co-require wording is contingent on the companion `architecture-delta.staged.md` OA-V10-05 demotion landing — see the architecture-delta OA-V10-05 HALT-anchor note, which orders a re-grep of `:1206` before/after the splice so the preserved clause is re-anchored, never stranded. The SK-V17 "no aarch64 CollapsedStage admission without a 2E source-backed strategy" bar is MET, not relaxed — the x86-mechanically-refused row is not quietly re-opened. The committed cost is the inert slot (≈0 LOC, no consumer); any conditional rebuild (≤450 LOC scalar twin + checkasm) is G5/G6-GATED on a profiled CollapsedStage hot leaf, not a committed build. Evidence: `skinny/crates/codegen/src/lower/collapsed_stage.rs:16`; `skinny/crates/passes/src/lib.rs:658`; `skinny/REDRESS.md:2795`-`2944` (finding `:2928`-`2933`); `restart/audit/totality/p2/2D-cost-model.md:98`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:73`,`:81`.
+
+- Lock 14/1/10 cursor-generality re-anchor clause: the SK-V17 Lock-14 clause names "The `G:EventGrammar` type parameter is the generality vehicle" (`restart/locks/LOCKS.md:620`), but the certified SK-V18 plan DELETES the `<G>` axis (no non-test production animator exists). The generality claim re-anchors onto the TWO axes the clause itself already names: (a) the shared value-API `Cursor` micro-trait (≥2 non-collapsible impls) and (b) the config-breadth classifier (`alphabet: &'static [u8; 64]`, alphabet-as-data across the generated grammars that consume the shared classifier — the SK-V17 8-of-9 figure is NOT re-verified at the SK-V18 HEAD census post-P3-collapse, so this re-census is owed at SK-V18 G4/G5 with the census command cited, not carried as a settled count). The (b) config-breadth re-census at G4/G5 is GATED on the Pattern-H 9-ident RED self-gate disposition (the cursor-generality witness rides the SAME generic-crate roster `crates/ir/registry/strategy.rs:137`-`185` whose grammar-name leak is the OPEN Lock-14 RED that clause declares FALSIFIED at HEAD): the (b) witness-count may NOT be settled on that known-leaking roster until the CF-09 / D11b SK-V19 reconcile (R16 PartialEq full-row collapse + roster-wide regex widen) lands, so the generality re-anchor is never silently witnessed on a falsified roster. The `@generated` per-grammar emission remains the grammar-neutrality guarantor; `preserve-rich-ast` and the K-axis preservation hold verbatim; no lock count, shape, directive, or substrate change. (Decision-engine soundness note, carried for the Lock-4/Lock-10 decision-engine clause and NOT a grammar-generality invariant: the Decision Engine MUST keep ≥1 asserted e-graph rewrite (`NormalizeDirectSinkCost`, live) and a feasibility-bearing CSP across SK-V18; a regression to a zero-rule e-graph re-opens the V2 scaffold finding and blocks the wave.) This is a SK-V19 LOCKS reconcile (a one-clause strike + re-anchor at `LOCKS.md:620`, with the companion `restart/ARCHITECTURE.md:1990`,`:1997` §9.2 prose carrier — which is STRUCK EARLIER at CRUD-1 by the sibling `architecture-delta.staged.md` OA-V10-06 Gated Hunk 2: the `:1997`/`:1998` "generality vehicle" sentence and the `:1990` lazy-`ValueRef` header are demoted/re-annotated at CRUD-1, so this C10 cite is the PRE-strike carrier; the SK-V19/CRUD-3 reconcile inherits the already-struck §9.2 post-state and re-anchors `LOCKS.md:620` WITHOUT re-grepping `:1997` for a live "generality vehicle" sentence — mirroring the demote-stable framing the C9 clause received for its `:1206` secondary cite; see the architecture-delta OA-V10-06 HALT-NOTE). Evidence: `restart/skinny/tranches/sk-v18/SPEC.md:1202`-`1207`; `skinny/crates/runtime/src/tape/mod.rs:175`; `skinny/crates/passes/src/backend_egraph.rs:75` (the `NormalizeDirectSinkCost` instantiation in `select_with_rewrite_policy`),`:191`-`193` (the `struct` definition + its `impl Rewrite<DecisionNode, NoAnalysis>`, so the "≥1 asserted e-graph rewrite, live" claim cites the symbol's definition, not only its call site); `restart/audit/totality/p1/1A-substrate-evidence.md:180`; `restart/audit/totality/p2/2D-cost-model.md:97`.
+
+- Lock 13/14 Pattern-H re-census clause: the Pattern H invariant re-keys from the absolute SK-V15-era 67-file baseline (`restart/locks/LOCKS.md:408`-`409`) to per-file provenance coverage over the LIVE census; `crates/core/src/runtime` is 71 at HEAD, and the +4 (`tape/{mod,cursor,arena,record}.rs`) is a tape-fold roster trace, not an O(N) generator regression — but an unattributable +N opens an O(N) generator-regression scan per the generated-size-budget. Genericity additionally requires STRUCTURAL full-row collapse over the 9-row grammar-named `idents` table in the generic `ir` crate and a widened leak regex covering the full registered roster: the live table at `strategy.rs:137`-`185` registers 9 grammars (Json, GoogleSheets, CssL4, Bbnf, Csv, Math, Bnf, Ebnf, CssPretty — 17 ident strings, CssL4 carrying only `CssL4Parser`), but the narrow 4-name leak regex catches only 4 of those 9 grammar names while the literal 13-crate scan returns 13, so the Lock-14 self-gate is currently FALSIFIED/RED (asserts ZERO, returns 13); the leak regex must widen from the 4 names to the full 9-grammar roster and the R16 `PartialEq` collapse is over the 9 rows. DISPOSITION (CF-09, in-clause): do NOT bolt a 9-name regex widen as an SK-V18 patch — the R16 PartialEq full-row collapse + the roster-wide regex widen + `css_types.rs` relocation/deletion are a SK-V19 reconcile (D11b, ≈+217, SK-V19-owned), not an SK-V18 bolt-on; `css_types.rs` must relocate to a declaration crate or be deleted at SK-V19. Evidence: `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l` = 71; `crates/ir/src/registry/strategy.rs:137`-`185`,`:216`; `crates/core/src/css_types.rs:1`; `restart/audit/totality/p1/1E-locks-evidence.md:153`; `restart/audit/totality/p2/2C-grammar-neutrality.md:382`.
+
+
 ## v+1 Governance Boundary
 
 The v+1 text above is active only because Pass Omega CHALLENGE converged and
```

## Invariant Check

- 16 numbered locks unchanged: live `restart/locks/LOCKS.md` carries 16 numbered
  lock headings at `:75,160,170,179,181,183,200,202,260,269,319,328,336,349,436,453`
  (verified `grep -nE '^[0-9]+\. \*\*'`); the addendum adds no Lock 17.
- BackendShape canon remains exactly five variants in code:
  `skinny/crates/codegen/src/lower/mod.rs:18`-`24` dispatches
  `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` and
  `skinny/crates/ir/src/cost.rs:334` returns `[BackendShape; 5]`; no sixth shape.
- The hunk adds no new directive, BIR variant, public substrate API, retained
  sidecar, lock number, lock retirement, or sixth `BackendShape`; the governance
  boundary remains in force at `restart/locks/LOCKS.md:625`-`634`.
- The two PLANNED co-gate symbols are written as PLANNED, not live:
  `rg -c runtime_target_rows_collapsed skinny/crates skinny/xtask` = 0 and
  `rg -c bbnf_simd_single_mask_convention skinny/crates` = 0 (1E LAC-1E-V5-02;
  2F LAC-2F-V3-01).
- Insertion anchor: the new addendum lands AFTER the SK-V17 Lock 16 clause
  (`LOCKS.md:622`) and BEFORE the `## v+1 Governance Boundary` heading
  (`LOCKS.md:625`); it does not restate or edit the SK-V15 (`:581`-`607`) or
  SK-V17 (`:610`-`622`) addenda.
- Hunk applies cleanly: header `@@ -622,6 +622,33 @@` (6 old-side context lines:
  the Lock 16 clause + the TWO existing blank lines at `:623`,`:624` + the
  trailing `## v+1 Governance Boundary` heading region; 33 new-side = 6 context
  + 27 added). Verified `git apply --check` exits 0 against the live
  `restart/locks/LOCKS.md` at HEAD.
