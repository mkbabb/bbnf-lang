---
agent: CH6
pass: T-P1-excavation
cycle: V3
lens: ANTI-PAPER-CLOSE
disposition: REVISE
generated_at: 2026-05-28T05:55:24Z
inventory_fold_commit_claimed_by_context: 7e32eddaa
audited_head: 5a9ef7f43
inputs_audited:
  - restart/prompts/totality/PASS-1-EXCAVATION.md
  - restart/prompts/ORCHESTRATOR.md sections 3W and 3Z
  - restart/audit/totality/p1/hardening/V3/CHALLENGE-CONTEXT.md
  - restart/audit/totality/p1/hardening/HARDENING-T-P1-V2-CONSOLIDATED.md
  - restart/audit/totality/p1/hardening/V2/CH6.md
  - restart/audit/totality/p1/1A-substrate-evidence.md
  - restart/audit/totality/p1/1B-codegen-evidence.md
  - restart/audit/totality/p1/1C-runtime-evidence.md
  - restart/audit/totality/p1/1D-skinny-lessons.md
  - restart/audit/totality/p1/1E-locks-evidence.md
  - restart/audit/totality/p1/1F-coherence-scan.md
  - restart/audit/totality/p1/1F-anti-pattern.md
  - restart/audit/totality/p1/1F-past-corpora.md
---

# CH6 Anti-Paper-Close - SK-V15 T-P1 V3

## Verdict

REVISE.

Score: 4 ACCEPT / 2 REVISE / 0 REJECT.

V3 discharges the two V2 CH6 blockers that were explicitly rostered for fold:
`1A-SUB-001` is no longer broad runtime closure, and Lock 1 in `1E` is no
longer broad `honoured`. The packet also keeps generated provenance open and
routes UNKNOWN rows through verify actions.

The remaining CH6 problem is bounded but material: two `implemented` /
`honoured` cells still claim more closure than their own sibling evidence
supports. These are paper-close wording issues, not evidence loss.

## Evidence

Selected commands run from `/Users/mkbabb/Programming/bbnf-lang`:

```sh
git rev-parse --short HEAD
# 5a9ef7f43

find skinny/crates/runtime/src/grammars -type f -name '*.rs' | wc -l
# 48
find skinny/crates/runtime/src/grammars -type f -name '*.rs' -exec sh -c 'head -n 1 "$1" | grep -q "@generated" && printf x' sh {} \; | wc -c
# 43
find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l
# 67
rg '@generated|DO NOT EDIT|autogenerated' crates/core/src/runtime -g '*.rs' | wc -l
# 0

rg -n 'W7_POLICY_BACKEND_SHAPE|W7_SUBSTRATE_TARGET|admitted_fact_output|CSS_GENERATED_RS|fixture_sidecar_facts|same-plane-source-sidecar' \
  skinny/crates/runtime/src/grammars/css_l4_declaration_values/config.rs \
  skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs \
  skinny/crates/codegen/src/runtime_generator.rs \
  skinny/crates/bbnf-bench/src/nonjson_css_l4.rs
```

Material grep hits:

- `skinny/crates/runtime/src/grammars/css_l4_declaration_values/config.rs:5`
  sets `W7_POLICY_BACKEND_SHAPE = "admitted_fact_output"`, while `:6` sets
  `W7_SUBSTRATE_TARGET = "admitted_fact_output"`.
- `skinny/crates/codegen/src/runtime_generator.rs:97` emits
  `normalize(CSS_GENERATED_RS)`, and `:713` begins the embedded
  `CSS_GENERATED_RS` parser body.
- `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:648` calls
  `fixture_sidecar_facts(input)`, with same-plane source-sidecar literals at
  `:1082`, `:1203`, `:1354`, `:1511`, `:1661`, `:1815`, and `:1964`.

Prompt authority:

- PASS-1 CH6 requires no self-report of resolved/wired/verified without live
  evidence, no later-inventory deferral, and every UNKNOWN to carry
  `verify_action` (`restart/prompts/totality/PASS-1-EXCAVATION.md:130`-`133`).
- ORCHESTRATOR CH6 requires no complete/wired/verified claim without live
  evidence and no future-phase deferral (`restart/prompts/ORCHESTRATOR.md:86`-`88`).
- V3 context routes CH6 to broad `honoured` / `implemented` downgrades, UNKNOWN
  routing, and generated provenance (`restart/audit/totality/p1/hardening/V3/CHALLENGE-CONTEXT.md:79`-`81`).

## Findings

| disposition | target | evidence | required fold |
|---|---|---|---|
| ACCEPT | V2 CH6 blockers folded | `1A-SUB-001` now reads `partial / JSON-example implemented` and says CSS lacks full retained value/view/visitor parity (`restart/audit/totality/p1/1A-substrate-evidence.md:64`). `1E` Lock 1 now reads `partial / JSON-tape-only` and the V3 fold table records `CH6-V2-F10` (`restart/audit/totality/p1/1E-locks-evidence.md:89`, `restart/audit/totality/p1/1E-locks-evidence.md:191`). | None. |
| REVISE | `1E-locks-evidence.md` L05 | L05 says `honoured` for `IR + per-backend lower` (`restart/audit/totality/p1/1E-locks-evidence.md:93`). But Lock 5 itself includes the formal `Backend` trait/Rust backend boundary (`restart/locks/LOCKS.md:181`), and sibling 1B evidence says no `trait Backend`, `RustBackend`, `WasmBackend`, or `TsBackend` symbols were found (`restart/audit/totality/p1/1B-codegen-evidence.md:39`). 1B also records four of five shape lowerers as marker strings (`restart/audit/totality/p1/1B-codegen-evidence.md:49`, `restart/audit/totality/p1/1B-codegen-evidence.md:57`-`63`), while 1E L10 already says the five-shape evidence is not load-bearing (`restart/audit/totality/p1/1E-locks-evidence.md:98`). | Downgrade L05 from `honoured` to `partial / Rust-only IR boundary present` or equivalent. State that the formal `Backend` trait and concrete per-shape lowerer depth remain open under L10/P1-1B-D1/P1-1B-D7. |
| REVISE | `1C-runtime-evidence.md` CSS fact-stream row | 1C marks the CSS fact-stream claim `Implemented` (`restart/audit/totality/p1/1C-runtime-evidence.md:65`), but the cited config uses `W7_POLICY_BACKEND_SHAPE = "admitted_fact_output"` while also setting the substrate target (`skinny/crates/runtime/src/grammars/css_l4_declaration_values/config.rs:5`-`6`). 1A already classifies this as metadata drift because admitted fact output is a substrate target/category, not a backend shape (`restart/audit/totality/p1/1A-substrate-evidence.md:79`). 1E keeps the schema/value-API issue open through LAC-1E-V1-01 (`restart/audit/totality/p1/1E-locks-evidence.md:129`). | Change the row verdict to `partial / admitted output plane present; metadata/schema unresolved` or similar. Preserve the point that no sixth `BackendShape` enum variant exists, but do not call the CSS fact-stream classification fully implemented until the policy key/schema issue is disposed. |
| ACCEPT | Generated provenance remains unclosed | 1A keeps generated comments as file-state evidence only (`restart/audit/totality/p1/1A-substrate-evidence.md:85`). 1C says headers are present but provenance is unverified (`restart/audit/totality/p1/1C-runtime-evidence.md:59`), repeats that a regen/check transcript or manifest proof is required (`restart/audit/totality/p1/1C-runtime-evidence.md:100`), and records that generated round-trip was not run (`restart/audit/totality/p1/1C-runtime-evidence.md:139`). 1D and 1F likewise require Pattern H generated ownership/round-trip proof (`restart/audit/totality/p1/1D-skinny-lessons.md:195`, `restart/audit/totality/p1/1F-coherence-scan.md:137`, `restart/audit/totality/p1/1F-coherence-scan.md:149`). | None. |
| ACCEPT | UNKNOWN rows have verify_action routes | UNKNOWN/verify-action tables exist for 1A (`restart/audit/totality/p1/1A-substrate-evidence.md:171`-`177`), 1B (`restart/audit/totality/p1/1B-codegen-evidence.md:122`-`125`), 1C (`restart/audit/totality/p1/1C-runtime-evidence.md:143`-`146`), 1D (`restart/audit/totality/p1/1D-skinny-lessons.md:190`-`212`), 1E (`restart/audit/totality/p1/1E-locks-evidence.md:200`-`208`), and 1F (`restart/audit/totality/p1/1F-coherence-scan.md:164`-`169`). COH-012's unknown governance conflict at `1F:83` is covered by U-COH-004 at `1F:169`. | None. |
| ACCEPT | Superseded 1F auxiliaries are not live proof | `1F-anti-pattern.md` and `1F-past-corpora.md` both declare `status: superseded-historical-auxiliary` and point to `1F-coherence-scan.md` as the authoritative live inventory (`restart/audit/totality/p1/1F-anti-pattern.md:6`-`14`, `restart/audit/totality/p1/1F-past-corpora.md:6`-`19`). Current 1F repeats that these files may not be used for current LOC/provider/module claims (`restart/audit/totality/p1/1F-coherence-scan.md:88`-`93`). | None. |

## Required Fold

1. `restart/audit/totality/p1/1E-locks-evidence.md`: downgrade L05 from broad
   `honoured` to scoped partial wording. The safe wording is
   `partial / Rust-only IR boundary present`; mention that the formal Backend
   trait and concrete per-shape lowerer depth remain open.

2. `restart/audit/totality/p1/1C-runtime-evidence.md`: downgrade the CSS
   fact-stream row from `Implemented` to a partial wording that separates:
   no sixth live `BackendShape` enum variant, admitted-output-plane file state,
   and unresolved `W7_POLICY_BACKEND_SHAPE`/schema evidence.

After those two wording folds, CH6 should be able to ACCEPT if no new closure
language is introduced.
