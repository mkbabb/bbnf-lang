# BB.W6 - Measurement And Close

**Name**: W6 - Measurement And Close
**Opens after**: W5 - Review Ledger + CI
**Agents**: 3 parallel
**Hard gate**: `docs/benchmarks/post-BB.json` per SPEC.md; substrate-audit GREEN; samply 7-artefact contract per perf-citing claim; FINAL.md cites resolving artefact for every Hard Gate; close-honesty checklist passes
**Status**: planned

## Scope

1. Run the full benchmark matrix under fat-LTO `[profile.bench]` and `[profile.bench-iter]`. Produce `docs/benchmarks/post-BB.json` per `docs/benchmarks/SPEC.md` schema. The `floors` block compares row-by-row against `post-BA.json` (BB inherits the BA floor) and `post-AU.json` (BA's AU-floor closure persists). The `competitors` block carries same-harness sonic-rs / lightningcss / simdjson rows.
2. Verify zero regressions vs `post-BA.json`. Verify ≥ 1 grammar shows measurable throughput gain (the BB invariant from W5).
3. Run samply against every Hard Gate citing performance: corpus hit-rate evidence, throughput-gain row, generated-LOC shrink. Save the 7-artefact contract per claim under `.profiles/samply/post-BB/`.
4. Run the close-honesty checklist: every claim in FINAL.md is grounded; every gate marked MET has an evidence path that resolves; every cross-tranche debt entry names BC explicitly; every rule-discovery claim cites the rule id in the registry.
5. Write FINAL.md citing resolving artefact for every Hard Gate, miss, deletion, and handoff. The cross-tranche debt table names BC (cleanup) explicitly.
6. Update PROGRESS.md to match reality at close.
7. Refresh `docs/codegen-paths.md` and `docs/GESTALT.md` to reflect the post-BB rule-discovery substrate; the e-graph saturation pipeline now consumes discovered rules from `crates/ir/src/rewrites/` + `grammar/<name>/rewrites/`.

## Triumvirate Dispatch

Dispatch a triumvirate if any of these occurs:

- `post-BB.json` has any row regressing vs `post-BA.json` (BB invariant 6 violation; BB cannot close);
- the close-honesty checklist surfaces a stale status word, an evidence path that does not resolve, or a cross-tranche debt entry that names a generic placeholder rather than BC;
- the samply 7-artefact contract cannot be produced for one of the perf claims (environmental gating regression);
- the substrate-audit denominator surfaces a BB substrate with no production caller (the discovery substrate has a phantom feature);
- three diagnostic-loop iterations fail to isolate why a perf row regresses;
- JSONL transcript quiet >15 minutes;
- first-pass return with no commit and no evidence (after one verbatim redispatch).

## File Bounds

| File | Access |
|---|---|
| `docs/benchmarks/post-BB.json` | create |
| `docs/benchmarks/profiles/post-BB/**` | create |
| `docs/tranches/BB/FINAL.md` | create |
| `docs/tranches/BB/PROGRESS.md` | modify (close ledger) |
| `docs/tranches/BB/audit/W6-*` | create |
| `.profiles/samply/post-BB/**` | create |
| `docs/codegen-paths.md` | modify-carve (post-BB refresh) |
| `docs/GESTALT.md` | modify-carve (post-BB refresh) |

Do NOT touch: any source code (W6 is measurement + docs only); enumerator (W1 owns); oracle (W2 owns); ranker/tiering (W3 owns); per-grammar RON (W4 owns); CI ledger (W5 owns).

## Disjointness

W6.1 (benchmark matrix + post-BB.json) writes `docs/benchmarks/post-BB.json` + `docs/benchmarks/profiles/post-BB/**`. W6.2 (samply 7-artefact contracts) writes `.profiles/samply/post-BB/**` + the canonicalisation doc. W6.3 (FINAL.md + PROGRESS.md + GESTALT/codegen-paths refresh + close-honesty checklist) writes `docs/tranches/BB/FINAL.md` + `docs/tranches/BB/PROGRESS.md` + `docs/codegen-paths.md` + `docs/GESTALT.md` + `docs/tranches/BB/audit/W6-*`. The three sub-units have disjoint primary write paths.

## Worktree Plan

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| BB.W6.1 Benchmark Matrix | `/Users/mkbabb/Programming/bbnf-wt-bb-w6-bench` | `/Users/mkbabb/Programming/bbnf-wt-bb-w6-bench/target/w6-bench` |
| BB.W6.2 Samply | `/Users/mkbabb/Programming/bbnf-wt-bb-w6-samply` | `/Users/mkbabb/Programming/bbnf-wt-bb-w6-samply/target/w6-samply` |
| BB.W6.3 FINAL + Close Honesty | `/Users/mkbabb/Programming/bbnf-wt-bb-w6-final` | `/Users/mkbabb/Programming/bbnf-wt-bb-w6-final/target/w6-final` |

## Agent Units

### BB.W6.1 Benchmark Matrix

- Mechanism: run the full bench matrix under fat-LTO; produce `docs/benchmarks/post-BB.json` per `docs/benchmarks/SPEC.md`. The `floors` block compares row-by-row against `post-BA.json` and `post-AU.json`. Verify zero regressions and ≥ 1 grammar with throughput gain.
- Files: `docs/benchmarks/post-BB.json`, `docs/benchmarks/profiles/post-BB/**`.
- Sub-gate: zero regressions; ≥ 1 grammar with gain.

### BB.W6.2 Samply

- Mechanism: run samply against every perf-citing Hard Gate; save the 7-artefact contract per claim under `.profiles/samply/post-BB/`. The contract continues the BA-canonicalised close discipline.
- Files: `.profiles/samply/post-BB/**`, `docs/tranches/BB/audit/W6-samply.md`.
- Sub-gate: every perf-citing Hard Gate has a saved 7-artefact contract.

### BB.W6.3 FINAL + Close Honesty

- Mechanism: write `docs/tranches/BB/FINAL.md` citing resolving artefact for every Hard Gate, miss, deletion, and handoff. Update `PROGRESS.md`. Refresh `docs/codegen-paths.md` and `docs/GESTALT.md` to reflect the post-BB rule-discovery substrate. Run the close-honesty checklist.
- Files: `docs/tranches/BB/FINAL.md`, `docs/tranches/BB/PROGRESS.md`, `docs/codegen-paths.md`, `docs/GESTALT.md`, `docs/tranches/BB/audit/W6-close-honesty.md`.
- Sub-gate: close-honesty checklist passes; FINAL.md cites artefact for every gate; cross-tranche debt names BC explicitly.

## Hard Gate

1. `docs/benchmarks/post-BB.json` per SPEC.md schema; `floors.post-BA.rows_at_or_above` = 100% (zero regressions); `floors.post-AU.rows_at_or_above` ≥ BA close (BA-floor closure persists); ≥ 1 grammar with measurable throughput gain.
2. Every perf-citing Hard Gate has saved 7-artefact contract under `.profiles/samply/post-BB/`.
3. `crates/ir/tests/substrate_audit.rs` GREEN against the post-BB denominator (every BB substrate has a production caller).
4. `docs/tranches/BB/FINAL.md` cites resolving artefact for every Hard Gate, miss, deletion, and handoff.
5. Close-honesty checklist passes.
6. Cross-tranche debt names BC (cleanup) explicitly; F5 (TS Node-execute) routes to BD with named successor letter (inheriting the BA cross-tranche debt routing).
7. `docs/codegen-paths.md` and `docs/GESTALT.md` refreshed to reflect the post-BB substrate.
8. `git diff --check` passes; FINAL.md commit body is present.

## Format And Lint Cadence

Docs-only wave: `git diff --check`, `markdownlint` (if available), and the repository's local document checks.

## Verification Artefacts

- `docs/benchmarks/post-BB.json`
- `docs/benchmarks/profiles/post-BB/**`
- `.profiles/samply/post-BB/**`
- `docs/tranches/BB/audit/W6-samply.md`
- `docs/tranches/BB/audit/W6-close-honesty.md`
- `docs/tranches/BB/FINAL.md`
- `docs/tranches/BB/PROGRESS.md`

## Commit Plan

Expected scopes: `benchmarks/post-BB`, `samply/post-BB-canonicalisation`, `docs/tranches/BB/W6-audit`, `docs/tranches/BB/FINAL`, `docs/tranches/BB/PROGRESS-close`, `docs/codegen-paths-post-BB`, `docs/GESTALT-post-BB`. Commit bodies required for the post-BB.json commit, FINAL commit, and GESTALT/codegen-paths refresh.

Every dispatch carries `HARD CAP: N min. At 0.9N commit, at N halt.`

## Dependencies

- **Depends on**: W5 - Review Ledger + CI; W4, W3, W2, W1, W0 (every prior wave's evidence is the input to W6).
- **Blocks**: BC - Cleanup Pass (BC opens after BB close).
