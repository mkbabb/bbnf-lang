# CH1 — CORRECTNESS lens disposition (SK-V14 S-P0 Overfit Audit V4)

Authority: `restart/prompts/ORCHESTRATOR.md §3W CH1` + dispatch
`restart/skinny/tranches/sk-v14/audit-overfit/hardening/V1/CHALLENGE-
CONTEXT.md` §3 + V2 overlay `…/V2/CHALLENGE-V2-ADDENDUM.md` §1 + V3
consolidated prescription `…/V3/HARDENING-S-P0-V3-CONSOLIDATED.md §2`
+ V4 micro-fold commit `f8e279877` (1 file / +9 / −4) which folds the
two V3 NEW orphan REVISEs the V3 CONSOLIDATED enumerated (`F-V4-
SYNTHESIS-{1,2}`).

V3 disposition (`…/V3/CH1.md`, 100 % outright ACCEPT with zero
carry-overs from CH1 itself; the two V3 NEW orphans were surfaced
independently by CH2 §4.1 + CH7 §1.5):

1. CH2 V3 §4.1 N-V3-CH2-1 (LOW) — SYNTHESIS §2.4:311 inclusion-
   relation defect: "42 files carry the fake header including 14
   codegen-side files" is arithmetically wrong because the 42
   (runtime mirror) and 14 (codegen) sets are disjoint under the
   lint glob brace-expansion `{runtime/src/grammars,codegen/src}/
   **/*.rs`; correct reading is 42 + 14 = 56 (zero overlap).
2. CH7 V3 §1.5 informational cross-flag — SYNTHESIS §3.1:342 C-3
   row prose cell still read "3 scanners are fixture lookups" while
   every other site (§0.1, §1.1:81, §1.2:122, §5.1, `generator-
   truth.md:56`) had folded to "4" under the F-V2-SYNTHESIS-4 +
   F-V3-SYNTHESIS-3 sweep.

V4 micro-fold packets (per `f8e279877` commit body, re-quoted here
so the disposition is self-contained):

- **F-V4-SYNTHESIS-1** — SYNTHESIS §2.4:311 "42 files carry the fake
  header including 14 codegen-side files (8 providers+templates + 6
  ancillary; …)" → "56 files carry the fake header (42 runtime-side
  mirror + 14 codegen-side (8 providers+templates + 6 ancillary)) —
  disjoint sets per the lint glob brace-expansion `{runtime/src/
  grammars,codegen/src}/**/*.rs`; `git grep -l '@generated by skinny
  bbnf-codegen' skinny/crates/runtime/src/grammars/ | wc -l = 42` and
  `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/
  codegen/src/ | wc -l = 14`, summing to the 56 total with zero
  overlap (the two path roots are siblings)." Adopts the V3 §2.1
  "Option B compound" reading.
- **F-V4-SYNTHESIS-2** — SYNTHESIS §3.1:342 C-3 row "3 scanners are
  fixture lookups" → "4 scanners are fixture lookups". Closes the
  CH7 V3 §1.5 inheritance-miss orphan and aligns §3.1:342 with the
  already-folded §0.1 + §1.1:81 + §1.2:122 + §5.1 + `generator-
  truth.md:56` sites.

A1, A2, A3, A4, A5, A6 STAND verbatim under V4 (zero axis edits;
the V3 axis folds at A4 §1:153 + A5 §5:133 carry forward unchanged).

## §0 — V4 Verdict + disposition summary

**Verdict: ACCEPT (100 %).** Both V3 NEW orphans landed at the
exact line-coordinates the V3 CONSOLIDATED §2 prescription enumerated;
all V3 baseline folds (F-V3-A4-1 + F-V3-A5-1 + F-V3-SYNTHESIS-{1,2,3})
preserved; A1 / A2 / A3 / A4 / A5 / A6 byte-identity from V3 confirmed
by `git diff 007624849..f8e279877 -- …{six-axis files} | wc -l = 0`;
no fresh CH1-grade defect surfaced under the V4 confirming pass.

| Disposition | Count | Notes |
| --- | ---: | --- |
| ACCEPT | 7 of 7 artefacts | A1 / A2 / A3 / A4 / A5 / A6 STAND verbatim from V3 (zero diff bytes per `git diff 007624849..f8e279877 -- …`); SYNTHESIS V4 carries the two prescribed micro-fold edits at the two prescribed line-coordinates (§2.4:311 + §3.1:342) and preserves every V3 axis/SYNTHESIS fold below those edits. |
| REVISE | 0 | No residual orphans; both V3 NEW orphans cleared at source by the prescribed two-edit micro-fold. |
| REJECT | 0 | No source-side factual error in either V4 fold; both new prose readings re-verify against `git grep` ground-truth. |

ACCEPT-rate: **7/7 artefacts = 100 %**. V3 forecast in `…/V3/
HARDENING-S-P0-V3-CONSOLIDATED.md §3` ("expected 7 × 100 % = 94/94
with zero orphans under V4") confirmed for CH1.

## §1 — V3 NEW-orphan confirming pass

### §1.1 N-V3-CH2-1 (SYNTHESIS §2.4:311 inclusion-relation defect) — RESOLVED

Executable verification of the corrected counts:

```
git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ | wc -l   → 42
git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/         | wc -l   → 14
```

The two path roots (`runtime/src/grammars/` and `codegen/src/`) are
sibling directories under `skinny/crates/`; their `git grep` yields
are necessarily disjoint by the file-system tree structure. 42 + 14
= 56 ✓ with provable zero overlap.

Spot-check of the SYNTHESIS §2.4 prose now in place
(`sed -n '305,328p' SYNTHESIS-AUDIT-OVERFIT.md`-equivalent reading):

```
311:   56 files carry the fake header (42 runtime-side mirror + 14
312:   codegen-side (8 providers+templates + 6 ancillary)) — disjoint
313:   sets per the lint glob brace-expansion
314:   `{runtime/src/grammars,codegen/src}/**/*.rs`; `git grep -l
315:   '@generated by skinny bbnf-codegen' skinny/crates/runtime/src/grammars/
316:   | wc -l = 42` and `git grep -l '@generated by skinny bbnf-codegen'
317:   skinny/crates/codegen/src/ | wc -l = 14`, summing to the 56 total
318:   with zero overlap (the two path roots are siblings).
```

The V3 fold's "Option B compound" reading was chosen per the V4 fold
agent's local prose-flow judgement (V3 CONSOLIDATED §2.1 offered both
options as equivalent). Compound reading preserves the original prose
spine ("CH2 §3.5 verified …") while restating the inclusion relation
as a parenthetical decomposition; the trailing executable verification
quotes both grep commands inline (instead of one) which is the more
defensible CH1-grade citation form. The "8 providers+templates + 6
ancillary" decomposition holds verbatim from the V3 fold and remains
correct by the V3 CH1 §2.3 enumeration (7 CSS providers + 1 JSON
provider = 8 providers; 4 `json_templates/*` + `json_typed_direct.rs`
+ `lib.rs` = 6 ancillary; 8 + 6 = 14 ✓).

Residual prose hygiene: a `grep -n "including" SYNTHESIS-AUDIT-
OVERFIT.md` returns zero hits across the whole file — the offending
inclusion-relation framing is fully retired (not merely overwritten
in the targeted cell). The V3 CH1 §4 below-threshold observation
about the soft paragraph break at the original V3 insertion ("The
lint glob `codegen/src/**/*.rs` catches all 14 regardless … / A
runtime-only lint would let the / codegen-side twin re-introduce
…") is preserved verbatim under V4 because the V4 edit only touches
lines 311-318 inside the same paragraph; the soft-break is the same
typographical artefact V3 already classified as below-threshold,
unchanged at V4.

**Disposition**: RESOLVED. The CH2 V3 N-V3-CH2-1 LOW orphan is
closed at source — SYNTHESIS now both correctly arithmetics the 56
total and explicitly names the disjoint-set relation; the lint glob
recommendation factually holds (`codegen/src/**/*.rs` still catches
all 14 regardless of the 8-vs-14 prose distinction).

### §1.2 CH7 V3 §1.5 informational cross-flag (SYNTHESIS §3.1:342 "3 → 4 scanners") — RESOLVED

Executable verification of the four-scanner ground-truth count:

```
grep -rE 'CANONICAL_FIXTURE|CAPTURED_W2_INPUT' \
    skinny/crates/codegen/src/css_l4_*_templates/generated.rs \
    | cut -d: -f1 | sort -u
```

returns four distinct files:

```
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/generated.rs
skinny/crates/codegen/src/css_l4_nested_layout_templates/generated.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/generated.rs
skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_templates/generated.rs
```

Per-mechanism decomposition (`grep -rE 'CANONICAL_FIXTURE|CAPTURED_
W2_INPUT' skinny/crates/codegen/src/css_l4_*_templates/generated.rs`):
three files use `CANONICAL_FIXTURE` byte-equality (`at_rules_and_
media`, `nested_layout`, `vendor_and_custom_atrules`) plus one file
uses `CAPTURED_W2_INPUT` byte-equality (`stylesheet_selectors`); 3 +
1 = 4 ✓. The §1.2:125 prose decomposition "(3 CSS L4 `CANONICAL_
FIXTURE` short-circuits + 1 `CAPTURED_W2_INPUT` short-circuit)" was
already correct at V2 and remains correct at V4 — it is the per-
mechanism breakdown of the four files, not a stale "3" count.

Spot-verification across the now-fully-aligned 4-of-7 sites:

```
grep -n "Four\|4 of 7\|fixture-lookup\|fixture lookup" \
    restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md
```

returns lines 22 ("CSS scanners as fixture lookups" — A4 NEW-2 col
mention), 81 ("4 of 7 CSS scanners short-circuit" — V3 F-V3-
SYNTHESIS-3 site), 122 ("Four / of the seven CSS L4 template
generators" — V2 enumeration), 125 ("(3 + 1 decomposition)" — see
above), 347 ("4 scanners are fixture lookups" — V4 F-V4-SYNTHESIS-2
site), 469 + 483 (summary mentions, no count). Zero residual "3 of 7"
or "3 scanners" framings anywhere under `audit-overfit/`.

**Disposition**: RESOLVED. The §3.1:342 C-3 row prose cell is now
aligned with every other 4-of-7 framing site across SYNTHESIS + A4;
the inheritance-miss the V3 fold roster under-enumerated is closed
at exactly the cell V3 §1.5 cross-flagged; the V4 fold roster
correctly added the §3.1:342 cell that the F-V3-SYNTHESIS-3 sweep
missed.

## §2 — V3 baseline preservation under V4

Critical V3 baseline cites re-executed and reproduce verbatim under
V4 (the V3→V4 boundary is `git diff 007624849..f8e279877`):

1. `git diff 007624849..f8e279877 -- restart/skinny/tranches/sk-v14/
   audit-overfit/sk-v14-audit-overfit-{css-measurement,admit-
   mechanism,lock14-scan,generator-truth,decision-engine,pre-restart-
   pattern}.md | wc -l` returns **0** — A1, A2, A3, A4, A5, A6 STAND
   byte-identical V3 → V4 (matches V4 commit body's "A1-A6 STAND
   verbatim" claim).
2. `git diff 1735882a5..f8e279877 -- restart/skinny/tranches/sk-v14/
   audit-overfit/sk-v14-audit-overfit-{css-measurement,admit-
   mechanism,lock14-scan,pre-restart-pattern}.md | wc -l` returns
   **0** — A1, A2, A3, A6 STAND byte-identical across the full V2 →
   V3 → V4 span (3-cycle invariant chain on these four axes).
3. F-V3-A4-1 carry-forward: `grep -n "json_provider.rs" sk-v14-
   audit-overfit-generator-truth.md` returns line 153 reading
   `- skinny/crates/codegen/src/json_provider.rs (full, 100 lines).`
   verbatim; `wc -l skinny/crates/codegen/src/json_provider.rs`
   returns **100** ✓.
4. F-V3-A5-1 carry-forward: `grep -n "preserved through PRUNE-5"
   sk-v14-audit-overfit-decision-engine.md` returns line 133 reading
   "the LOW finding (honest self-labelling) is preserved through
   PRUNE-5 as a gate-rejection invariant inside C-4 entry-gates so
   any admit attempting to cite W8 / W9 pre-runtime-consumer is
   denied at admit time" ✓; `grep -n "no-op pre-C-4" restart/skinny/
   tranches/sk-v14/audit-overfit/` returns zero matches (the framing
   stays fully retired under V4) ✓.
5. F-V3-SYNTHESIS-1 carry-forward: `grep -n "preserved through PRUNE-5
   as C-4 entry-gate invariant" SYNTHESIS-AUDIT-OVERFIT.md` returns
   line 348 (was 343 in V3 before the §2.4 insertion grew the file
   by 5 lines; the C-4 row offset shifts but the cell content is
   verbatim) ✓.
6. F-V3-SYNTHESIS-2 carry-forward: SYNTHESIS §2.4 prose now extends
   the V3 codegen-count refinement with the V4 inclusion-relation
   correction; the `git grep -l … codegen/src/ | wc -l = 14`
   verification quoted inline at lines 316-317 ✓; the 14-file
   enumeration from V3 CH1 §2.3 still resolves to the same 14 files
   under `git grep -l '@generated by skinny bbnf-codegen' skinny/
   crates/codegen/src/`.
7. F-V3-SYNTHESIS-3 carry-forward: `grep -n "3 of 7\|4 of 7"
   SYNTHESIS-AUDIT-OVERFIT.md` returns line 81 only, reading "4 of
   7 CSS scanners short-circuit" ✓; the stale "3 of 7" framing
   remains fully retired.
8. SYNTHESIS census preservation: `grep -n "54 of 74\|74 finding\|
   31 CRIT\|20 HIGH\|12 MED\|11 LOW" SYNTHESIS-AUDIT-OVERFIT.md`
   returns lines 45 (CRIT+HIGH aggregate trigger), 64 ("54 of 74
   findings (73 %) CONFIRM"), 336 (§3.1 coverage header), 340
   ("**73 of 74 findings**"), 360 (zero-orphan attestation), 495
   ("**Aggregate: 74 findings (31 CRIT + 20 HIGH + 12 MED + 11 LOW
   )**"), 507 (covered-by-C-1..C-5 closing) — 74-finding census +
   severity distribution preserved verbatim ✓.
9. Per-axis severity column sums (V2 §3 / V3 §3): A1 8 (4+2+2+0),
   A2 9 (4+3+1+1), A3 30 (11+7+5+7), A4 16 (9+4+2+1), A5 4 (0+2+
   1+1), A6 7 (3+2+1+1) → 74 ✓; CRIT 31 + HIGH 20 + MED 12 + LOW
   11 = 74 ✓ (re-attested at V4 byte-identically).
10. PRUNE-4 sub-wave count + 9-grammar census: `find crates/core/src/
    runtime -mindepth 1 -maxdepth 1 -type d | wc -l` returns **9**;
    `cargo metadata --format-version 1 --no-deps | jq -r '.metadata.
    bbnf.grammars[].ident' | wc -l` returns **9** — the PRUNE-4
    sub-wave binding (9 not 8; css_pretty addition) holds verbatim
    at V4 ✓.

## §3 — V4 fresh-finding scan

Scanned the V4 diff (`git show f8e279877`) end-to-end and re-greped
the modified file for any incidental drift introduced by the two
folds. No new CH1-grade defect:

- **F-V4-SYNTHESIS-1**: 11-line block replacement at §2.4:311-318
  (was 308-312 in V3) — extends the paragraph by 5 lines net (was
  −4 / +9 per commit stat). The new prose introduces no fresh
  off-by-one, inversion, or absent-cite: the additive count claim
  (42 + 14 = 56) re-arithmetics correctly, the disjoint-set claim
  is structurally true (sibling directories), the lint-glob brace-
  expansion notation `{runtime/src/grammars,codegen/src}/**/*.rs`
  uses correct POSIX-glob brace-expand syntax, and the two inline
  `git grep` verifications match shell output exactly. The 8 + 6
  decomposition carries forward from V3 unchanged. Zero residual
  "including 14 codegen" or "42 files carry … including" framings
  anywhere under `audit-overfit/` (full-tree grep returns zero hits).
- **F-V4-SYNTHESIS-2**: single-character replacement at §3.1:342
  C-3 row ("3 scanners are fixture lookups" → "4 scanners are
  fixture lookups"). The four-scanner ground-truth count is verified
  by `grep -rE 'CANONICAL_FIXTURE|CAPTURED_W2_INPUT' skinny/crates/
  codegen/src/css_l4_*_templates/generated.rs | cut -d: -f1 | sort
  -u | wc -l = 4` (returns 4 distinct files). The "14/15 .bbnf
  orphan" co-cited count remains verifiable by V13 + SK-V14 baseline
  (`find skinny/grammars-side -name '*.bbnf' | wc -l` paths the
  count; carried forward verbatim from V1 + V2 + V3). The "no
  regen-css subcommand" claim re-verifies by `grep -nE 'regen-css'
  skinny/xtask/src/main.rs` returning zero matches.
- **Cross-document propagation**: the V4 edits do not perturb any
  cross-axis cite. `grep -n "fixture-lookup" sk-v14-audit-overfit-
  generator-truth.md` returns line 56 ("Four of the seven CSS
  scanners") which matches the V4-aligned SYNTHESIS framing; no
  inheritance miss surfaces to a third site under V4.
- **Soft paragraph break preserved (below-threshold)**: the same
  typographical artefact V3 CH1 §4 noted at SYNTHESIS §2.4:320-322
  ("The lint glob … catches all 14 regardless; only the prose
  count needs the 8-vs-14 distinction. A runtime-only lint would
  let the / codegen-side twin re-introduce …") carries forward
  under V4 because the V4 edit only modifies lines 311-318 — lines
  319-324 are byte-identical to V3. This is the same below-CH1-
  threshold cosmetic V3 declined to fold; the V4 confirming pass
  declines for the same reason (no factual error; renders as
  continuous prose in HTML).

One **observation (below CH1 threshold, not a finding)**: V4 has
introduced two consecutive em-dash phrases inside the same sentence
at §2.4:312-313 ("(8 providers+templates + 6 ancillary)) — disjoint
sets per the lint glob brace-expansion `{runtime/src/grammars,
codegen/src}/**/*.rs`; …"). The double-em-dash construct is
grammatically defensible (the dash introduces a sentence-level
qualifier, the parenthetical preceding it scopes the 14-codegen
sub-decomposition) but the resulting prose contains a triple-nested
parenthetical "(42 runtime-side mirror + 14 codegen-side (8
providers+templates + 6 ancillary))" which is denser than the V3
single-paren reading. This is **style-grade**, not correctness-
grade — every factual claim re-arithmetics correctly, the disjoint-
set proof remains airtight, and the V3 CONSOLIDATED §2.1 explicitly
authorised either "Option A" (additive flat) or "Option B" (nested
compound) per local prose-flow judgement. No fold recommended.

## §4 — Spot-verified cites (V4 sample of 10)

1. `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/
   runtime/src/grammars/ | wc -l` → **42** ✓.
2. `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/
   codegen/src/ | wc -l` → **14** ✓.
3. `grep -rE 'CANONICAL_FIXTURE|CAPTURED_W2_INPUT' skinny/crates/
   codegen/src/css_l4_*_templates/generated.rs | cut -d: -f1 | sort
   -u | wc -l` → **4** ✓.
4. `wc -l skinny/crates/codegen/src/json_provider.rs` → **100** ✓
   (F-V3-A4-1 carry-forward).
5. `grep -n "json_provider.rs" sk-v14-audit-overfit-generator-truth.md`
   → line 153 reads "(full, 100 lines)" ✓ (F-V3-A4-1 carry-forward).
6. `grep -n "no-op pre-C-4" restart/skinny/tranches/sk-v14/audit-
   overfit/` → zero matches across SYNTHESIS + A5 (F-V3-A5-1 carry-
   forward; framing stays fully retired under V4).
7. `grep -n "3 of 7\|3 scanners" SYNTHESIS-AUDIT-OVERFIT.md` → zero
   matches ✓ (both V3 F-V3-SYNTHESIS-3 + V4 F-V4-SYNTHESIS-2 close
   the last residuals).
8. `grep -n "including 14 codegen\|42 files carry the fake header
   including" SYNTHESIS-AUDIT-OVERFIT.md` → zero matches ✓
   (F-V4-SYNTHESIS-1 retires the inclusion-relation framing).
9. `grep -n "56 files\|4 scanners are fixture lookups"
   SYNTHESIS-AUDIT-OVERFIT.md` → line 311 (V4 F-V4-SYNTHESIS-1 site)
   + line 347 (V4 F-V4-SYNTHESIS-2 site) ✓.
10. `git diff 007624849..f8e279877 -- restart/skinny/tranches/sk-v14/
    audit-overfit/sk-v14-audit-overfit-{css-measurement,admit-mechanism,
    lock14-scan,generator-truth,decision-engine,pre-restart-pattern}.md
    | wc -l` → **0** ✓ (A1, A2, A3, A4, A5, A6 STAND verbatim V3 →
    V4; matches commit body).

---

**V4 ACCEPT-rate: 7/7 artefacts = 100 %.**

Both V3 NEW orphans are RESOLVED at the exact line-coordinates the
V3 CONSOLIDATED §2 prescription enumerated (SYNTHESIS §2.4:311
inclusion-relation correction; SYNTHESIS §3.1:342 C-3 row "3 → 4
scanners"). The V4 fold roster correctly enumerated **both** the
prescribed primary anchors with zero secondary inheritance misses —
the V3 §0.2 + §3.3 "inheritance-miss class on SYNTHESIS surface"
institutional pattern is closed for the V4 cycle (the V4 commit
body's explicit two-line-coordinate enumeration "311 + 342"
demonstrates the discipline V3 prescribed). A1 / A2 / A3 / A4 / A5
/ A6 STAND byte-identical V3 → V4, preserving every previously-
verified V3 baseline cite plus every V2 → V3 → V4 three-cycle
invariant chain on A1 / A2 / A3 / A6.

**No CH1-grade source-side correctness defects identified in V4.**
Every file:line cite re-executes to the quoted output; the 74-
finding aggregate + severity distribution remain arithmetically
self-consistent; the 54 CONFIRMS / 20 NEW per-row + 11 NEW
conceptual category census reconciles across §0.1 + §1.1 + §1.2 +
§3.2 + §4.3 + §5.1; A5 verdict-line alignment preserved across all
four V2-targeted sites + the V3 action-class fold at §4 row 4 + §5
close; the 14-file codegen-side fake-`@generated` count is source-
verified and the 8 + 6 breakdown reconciles by direct enumeration;
the V4 56-file disjoint-set claim is source-verified by two
independent `git grep` invocations summing to 56 with structural
zero-overlap (sibling-directory proof); the four-scanner CSS L4
fixture-lookup count is source-verified by `grep -rE … | sort -u`
returning exactly 4 distinct files.

CH1 V4 contributes **100 %** toward the §3Z convergence chain. With
V3 CH1 at 100 % outright and V4 CH1 at 100 % outright, the two-
consecutive-cycle ≥ 95 % CH1 condition is met from the CH1 side
through the V3 → V4 boundary under both the strict reading (V3
clears the floor; V4 clears the floor with zero orphans → V5 closes
§3Z LOCK as second clean cycle) and the pragmatic reading (V4 = first
post-orphan clean cycle; CH1 has been at 100 % for the V1 + V2 + V3
+ V4 chain on the substantive disposition surface). The aggregate
G-S-P0-CONVERGED gating remains conditional on the remaining six
lens dispositions at V4; CH1 itself stands clean and ready to
contribute its 100 % weight to the V5 confirming pass without
further fold prescription.
