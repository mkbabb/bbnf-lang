# SK-V11 S-P3 CHALLENGE V1 CH4 Cost / Wave Budget

Pass: S-P3 Synthesis-Plan CHALLENGE. Cycle: V1.
Date: 2026-05-20.
Scope: cost, hard-cap, sub-wave, same-wave consumer, checkasm/micro-proof, and close-wave budget review for SK-V11.
Output: this file.
Disposition: REVISE.
Accept rate contribution: 0.

## Findings

1. Critical - The bracket count is inside the formal ceiling, but the packet spends nearly all slack before the risky work starts. P3-B correctly states that W0-W9 is 10 waves and therefore inside the `> 12` escalation ceiling (`restart/skinny/tranches/sk-v11/research/p3/p3b-wave-sequencing.md:42` through `:49`; `restart/prompts/pass-contracts/SKINNY-TRIUMVIRATE.md:100` through `:110`). That topology is acceptable only if at most two sub-waves are needed. The current cost shape makes at least one sub-wave likely: W1 combines non-JSON generated parser harness, independent oracle, gate/report consumer, and `json_provider` leakage proof in one 220-460 LOC / <=90 minute redress budget (`restart/skinny/tranches/sk-v11/SPEC.md:184`, `:273` through `:321`). If W1 is split and W8 also needs a final source route, the bracket reaches 12 waves. Any additional W2/W5/W6 rescue sub-wave exceeds the skinny ceiling and must escalate rather than silently expand.

2. Critical - W1 should be split unless an existing generated non-JSON parser row and oracle are already runnable at plan time. W1 is not just telemetry: it must stand up generated Track 1, add or name independent Track 2/oracle, teach gate/report to consume non-JSON evidence, prove strict equality, render throughput provenance, and prove no generic JSON policy leak (`restart/skinny/tranches/sk-v11/SPEC.md:293` through `:311`). P3-C likewise requires the W1 gate to record grammar id, output plane, run id, feature mask, same-wave consumer class, independent oracle, and strict equality while admitting no behavior row (`restart/skinny/tranches/sk-v11/research/p3/p3c-falsifiability-gates.md:77` through `:79`). That is too much for one first-of-class <=90 minute redress unless the bench lane is already close to working. Required fold: define W1a as gate/report/non-JSON schema lane and W1b as exactly one generated CSS/Sheets/BBNF-self baseline row, or make W1 dispatch conditional on a plan artifact proving those pieces already exist.

3. Major - The LOC budgets mix handwritten source, tests, gate/report changes, and generated output without a stable accounting rule. SPEC lists W2-W6 at 300-650, 220-500, 260-650, 220-550, and 220-550 "source/test/generated LOC" under <=90 minutes (`restart/skinny/tranches/sk-v11/SPEC.md:185` through `:189`), while P3-B separately describes several waves as source/test LOC "plus generated output" (`restart/skinny/tranches/sk-v11/research/p3/p3b-wave-sequencing.md:62` through `:66`). CH4 cannot evaluate cost if generated output sometimes counts against the cap and sometimes sits outside it. Required fold: budget handwritten source/test/gate LOC separately from regenerated output LOC, cap generated output by named generator input, and require CHALLENGE to reject any wave whose generated diff prevents review inside the redress cap.

4. Major - Same-wave consumer, checkasm, and micro-proof expectations are sound but do not fit broad row sweeps. The packet correctly requires scalar reference, strict checkasm/differential when SIMD/ASM is used, same-host caller microbench, feature/fallback plan, same-wave consumer, and row gate before W2-W7 redress (`restart/skinny/tranches/sk-v11/SPEC.md:203` through `:217`). The dispatch prompt also requires strict scalar differential/checkasm before row measurements count (`restart/skinny/tranches/sk-v11/DISPATCH-PROMPT.md:113` through `:124`). P3-C sets useful minimums: SIMD caller microbench median >=1.08x with no selected slice below 0.99x, scalar-only refactor >=1.0% on at least one selected row, and reject-before-redress when scalar reference, checkasm plan, consumer, thresholds, gate consumer, or revert protocol are missing (`restart/skinny/tranches/sk-v11/research/p3/p3c-falsifiability-gates.md:175` through `:204`). This fits a wave only when the plan selects one primitive family, one consumer, and a small row subset. It does not fit W5's six string-heavy rows plus Unicode guards or W7's "every post-W6 N-direct row" phrasing under one 90-minute redress (`restart/skinny/tranches/sk-v11/SPEC.md:510` through `:519`; `restart/skinny/tranches/sk-v11/research/p3/p3b-wave-sequencing.md:101`).

5. Major - W2's baseline rule undermines its own cost cap. W2 may generate and benchmark CSS L4 intervention work, wire one primitive family, and, if W1 did not supply a baseline, capture that baseline in the same wave and set the 1% improvement floor (`restart/skinny/tranches/sk-v11/SPEC.md:341` through `:362`). P3-B is stricter: if W1 cannot produce the CSS baseline row, W2 is REVISE before redress (`restart/skinny/tranches/sk-v11/research/p3/p3b-wave-sequencing.md:96`). CH4 accepts the stricter P3-B rule and rejects the SPEC fallback. Required fold: W2 cannot both create the first measurable non-JSON baseline and admit the intervention in the same redress wave; that is W1/W1b work.

6. Major - W8 close work is bounded only if it remains docs/gate/result accounting. SPEC gives W8 a default docs/gate budget plus optional <=250 source LOC if CHALLENGE accepts one final narrow candidate (`restart/skinny/tranches/sk-v11/SPEC.md:191`, `:628` through `:665`). The W8 tasks also require every remaining direct residual to be re-evaluated and either admitted or assigned a measured REDRESS proof (`restart/skinny/tranches/sk-v11/SPEC.md:648` through `:661`). That is plausible as a close accounting wave after W3-W7 have real dispositions. It is not plausible if W8 also implements a new source route, re-runs all residuals, updates RESULTS/REDRESS, and preserves guard floors inside <=90 minutes. Required fold: if W8 touches source, split into W8a source redress for one narrow residual route and W8b direct-fixpoint accounting; if the bracket has already spent both spare waves, W8 source work must be BLOCKED or deferred.

7. Minor - The packet disagrees on whether W1 requires CHALLENGE. SPEC says W2-W7 require CHALLENGE and W8 requires it only for source work (`restart/skinny/tranches/sk-v11/SPEC.md:199` through `:201`). The dispatch prompt makes CHALLENGE mandatory for W1 through W7 (`restart/skinny/tranches/sk-v11/DISPATCH-PROMPT.md:94` through `:111`). CH4 sides with the dispatch prompt: W1 is first-of-class gate/report/non-JSON infrastructure and should be CHALLENGE-gated before redress. Required fold: align SPEC and P3-B/DISPATCH so W1 challenge cost is visible in the wave budget.

8. Pass - W9 close is bounded if W8 has already produced complete dispositions. W9 owns close documents, RESULTS/REDRESS reconciliation, and no new row movement (`restart/skinny/tranches/sk-v11/SPEC.md:673` through `:703`). The dispatch prompt similarly states W9 is docs/gate with 80-180 LOC and closes only after W8 (`restart/skinny/tranches/sk-v11/DISPATCH-PROMPT.md:61` through `:68`). CH4 accepts W9's budget on the condition that unresolved row proofs, non-JSON admission gaps, and guard-floor repairs remain W8 or earlier work, not W9 work.

## Required Folds

1. Make the bracket budget explicit: W0-W9 is 10 waves, with only two available sub-wave slots before the `> 12` escalation rule. Reserve one slot for W1 split unless plan evidence proves the non-JSON harness/oracle is already runnable, and reserve the other for either W8 source-last-mile work or one failed behavior-wave rescue, not both.
2. Split W1 into W1a/W1b or make W1 entry require an already-runnable generated non-JSON baseline row plus independent oracle. W2 must not create the first baseline and admit the intervention in one redress wave.
3. Separate handwritten LOC caps from regenerated output caps in the manifest. Generated diffs need named inputs and a reviewable generated-output budget; handwritten source/test/gate LOC should remain the redress cost cap.
4. Add per-wave row-count caps before redress: W3 at one to two numeric rows unless existing microbench data proves four; W4 already caps at three and should keep it; W5 should cap at one string/key consumer plus at most two target rows; W6 should cap at one escaped-segment consumer plus at most the three Unicode rows; W7 should cap to rows where a fresh post-W6 profile still names output digest/hash as limiting.
5. For any W2-W7 SIMD/ASM route lacking existing strict checkasm coverage, default to scalar production first or split the SIMD overlay into a later same-consumer wave. No primitive-only or checkasm-only source patch may count as redress success.
6. Make W8 docs/gate-only by default. If a final source route is needed, it becomes a CHALLENGE-accepted W8a with one candidate and one row subset, followed by W8b close accounting, provided the bracket remains <=12 waves.
7. Align SPEC with DISPATCH so W1 CHALLENGE is mandatory and costed.

## Accepted Facts

- The proposed sequence is topologically valid and remains within <=12 brackets as written: W0-W9 is 10 waves.
- The same-wave consumer and micro-prove-first rules are the right cost-control mechanism.
- W8/W9 close can be bounded when W8 is proof/accounting only and W9 is documentation reconciliation only.

These facts are not enough for ACCEPT because V1 lacks the split triggers and accounting precision needed to keep the likely execution within the skinny wave ceiling and <=90 minute redress cap.
