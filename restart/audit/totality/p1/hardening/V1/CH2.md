---
lens: CH2
name: GENERALITY
pass: T-P1-excavation
cycle: V1
disposition: REVISE
reviewed_artifacts:
  - restart/audit/totality/p1/1A-substrate-evidence.md
  - restart/audit/totality/p1/1B-codegen-evidence.md
  - restart/audit/totality/p1/1C-runtime-evidence.md
  - restart/audit/totality/p1/1D-skinny-lessons.md
  - restart/audit/totality/p1/1E-locks-evidence.md
  - restart/audit/totality/p1/1F-anti-pattern.md
  - restart/audit/totality/p1/1F-coherence-scan.md
  - restart/audit/totality/p1/1F-past-corpora.md
lens_obligation: "PASS-1-EXCAVATION.md:110-114 — Lock 14 holds across inventories; no divergence catalogued as JSON-only when grammar-neutral substrate fact; 1C runtime census flags every grammar-named module in a generic crate; 1D separates JSON-empirical from grammar-neutral findings; no grammar-name leak passes uncited."
authority: restart/locks/LOCKS.md:220
verification_method: "live re-run of Lock 14 verification commands at HEAD against 1C citations; mechanical extraction of grammar-named reexports from crates/core/src/runtime/mod.rs:25-71; rg census across crates/core/src/runtime/; line-resolved re-read of 1A-SUB-018 + 1B-D7/D8/D10/D13 + 1D rows 100-115 + 125 + 1E LAC-1E-08/15 against the lens obligation; cross-check that JSON-empirical (1D:101) and grammar-neutral (1D:100,113) labels are explicit"
verdict_summary:
  accept: 6
  revise: 4
  reject: 0
  accept_rate: "6/10 = 60%"
---

## Verdict

REVISE. The eight T-P1 V1 inventories carry the Lock 14 / generality findings the lens demands: 1C produces a complete 67/67 Pattern H per-grammar runtime census against `crates/core/src/runtime/` with all five Lock 14 verification commands re-run at HEAD; 1B confirms the 8-provider codegen recurrence vector + the grammar-shape leaks in `passes` role-mining and recognizer-mining; 1D explicitly labels JSON-empirical lessons (e.g., payload-arena-cold at line 101) distinctly from grammar-neutral substrate findings (line 100 substrate cardinality, line 113 bbnf-simd); 1E LAC-1E-08/13/15 escalates Pattern H as the category-scale recurrence vector with proposed per-tranche census rule; 1A-SUB-018 names the Lock 14 mis-attribution of `dispatch_value` envelopes as the explicit generality-failure surface. No JSON-only finding is mis-catalogued as grammar-neutral, and no catalogued substrate finding is wrongly narrowed to JSON. The lens finds four REVISE items: (a) two LOC undercounts in 1C that materially weaken its census authority (parser-name leaks counted as "19+" when live re-run returns 30 matches across 15 files; grammar-named reexports counted as "60+" when the actual count of distinct exported names from `crates/core/src/runtime/mod.rs:25-71` is 126 grammar-named symbols, ~2× the cited floor); (b) 1B carries `P1-1B-D8` recognizer mining and `P1-1B-D10` role mining as grammar-shape-leak rows but does not push them into 1D's JSON-empirical-vs-grammar-neutral matrix, leaving the Sheets / BBNF-self generalization risk implicit rather than catalogued; (c) the 1D row at line 113 marks `bbnf-simd` "grammar-neutral substrate" using SK-V14 axis A3 as the authority — true today — but does not carry forward the PC-008 SK-V5 pending finding flagged by 1F-past-corpora as still requiring verify-before-rederive, so the grammar-neutral verdict is not yet falsifiability-gated against the older corpus; (d) 1C-D11 (skinny CSS L4 7-cluster vs main monolithic css_l4/) is flagged as a generality finding but is uncited in 1D and 1F-coherence-scan, so the layout-asymmetry-as-generality-failure pattern is not yet a tracked obligation.

REJECT: none. No inventory fails Lock 14 grammar-name fencing or mis-attributes JSON-only when grammar-neutral.

ACCEPT-rate: 6 / 10 = 60%. CH2 fires REVISE on the four items above.

## Findings

| Disposition | Finding | Evidence |
|---|---|---|
| ACCEPT | 1C Pattern H 67/67 census reproduces verbatim against live workspace HEAD. `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d` returns 9 (`bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json, math`); per-grammar file counts `bbnf=8, bnf=7, css_l4=7, css_pretty=7, csv=7, ebnf=7, google_sheets=10, json=7, math=7` sum to 67. The 1C row `1C-D1` and the Lock 14 verification command `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d` returning 9 (vs `LOCKS.md:220` required ZERO) are correctly cited at path:line. | Live re-run on 2026-05-23 at HEAD: per-grammar table at `restart/audit/totality/p1/1C-runtime-evidence.md:42-55`; verified against `find` output for each of the 9 directories. Census matches the SK-V14 S-P0 A6 Pattern H baseline at `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:200-231`. |
| ACCEPT | 1C row at `restart/audit/totality/p1/1C-runtime-evidence.md:121-122` correctly identifies the `crates/core/src/runtime/mod.rs:9-21` grammar-module declarations (9 `pub mod <grammar>;` lines: bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json, math — interleaved with grammar-neutral modules `arena_template`, `builder`, `builder_template`, `error`, `handle`, `path`, `view`) AND the grammar-named type reexports at `crates/core/src/runtime/mod.rs:25-71` as the Lock 14 root-leak. Re-read of `mod.rs` confirms the 9 `pub mod <grammar>;` declarations at lines 9, 10, 13, 14, 15, 16, 18, 20, 21. | `crates/core/src/runtime/mod.rs:8-23` per direct Read; 1C citation matches. |
| ACCEPT | 1D correctly separates JSON-empirical from grammar-neutral findings. The row at `restart/audit/totality/p1/1D-skinny-lessons.md:101` tags "JSON-empirical lazy-materialization rule" and explicitly notes "Generalization to non-JSON grammars remains pending — no live non-JSON payload-counter evidence at SK-V14 audit-zero." The grammar-neutral counterparts are tagged at `1D:100` (substrate cardinality), `1D:113` (`bbnf-simd` grammar-neutral substrate), and `1D:107-109` (W5 bbnf-regex, W6 e-graph, W7 CSP solver — generic-crate primitives). No row mis-labels a JSON-only fact as grammar-neutral, and no row mis-labels a grammar-neutral substrate finding as JSON-only. | `restart/audit/totality/p1/1D-skinny-lessons.md:100-101,107-115,125`. |
| ACCEPT | 1A-SUB-018 at `restart/audit/totality/p1/1A-substrate-evidence.md:70` correctly catalogues the S-P1 CH2 envelope mis-attribution as a Lock 14 generality failure ("the JSON-grammar names hide the grammar-neutral `dispatch` primitive") and 1D row at `restart/audit/totality/p1/1D-skinny-lessons.md:125` carries the same finding with the 13/17 + 14/17 census forward. The mis-attribution is named at the envelope level (`dispatch_value`, `parse_object_value_at_direct`, `parse_array_element_at_direct`) with explicit grammar-neutral primitive identification (`dispatch` per `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:234`). | `restart/audit/totality/p1/1A-substrate-evidence.md:69-70`; `restart/audit/totality/p1/1D-skinny-lessons.md:125`. |
| ACCEPT | 1F-anti-pattern AP-016 + AP-019 + AP-018 catch the live grammar-name couplings: the 67 hand-written per-grammar runtime files (AP-016), the `builder_template.rs` / `arena_template.rs` doc-comments that enshrine the Pattern H hot-grammar opt-out as design-of-record (AP-019), and the `LegacyPath` rename shims in 4 `parse_with.rs` files (AP-018). Each carries a path:line citation and a wave assignment. | `restart/audit/totality/p1/1F-anti-pattern.md:75-78`. |
| ACCEPT | 1E LAC-1E-08 + LAC-1E-15 propose the right amendment vector: per-tranche Pattern H census rule + ban on substrate-template doc-comments enshrining hot-grammar opt-outs as design-of-record. LAC-1E-13 adds the round-trip clean check on `cargo xtask regen-{grammar}` as the falsifiability gate distinguishing real codegen from fake `@generated` `include_str!` templates. All three name grammar-neutral enforcement rules, not grammar-specific carveouts. | `restart/audit/totality/p1/1E-locks-evidence.md:116-123`. |
| REVISE | 1C undercounts the Lock 14 parser-name leak by ~37%. 1C row at `restart/audit/totality/p1/1C-runtime-evidence.md:91` claims "at minimum 19 matches" for `rg -n 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/core/src/runtime/`. Live re-run on 2026-05-23 at HEAD returns **30 matches across 15 files**, not 19. The "at minimum" hedge is correct but the floor is too low to defend the Lock 14 census as authoritative for T-P3 disposition; the row should be tightened to the live count or replaced with a captured `rg` artefact. Same defect at `1C:123` ("at minimum 19+ matches" in the Lock 14 leak table). | Live `rg -n 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/core/src/runtime/ \| wc -l` returns 30; `rg -nc ... \| wc -l` returns 15 files. Affected files include `bbnf/{mod,parse_with,serialize}.rs`, `bbnf/builder.rs` (not in 1C's enumeration at :91), `css_l4/{mod,parse_with}.rs`, `google_sheets/{mod,parse_with,document/mod}.rs` (the `google_sheets/document/mod.rs:43,142` site is also missed by 1C's enumeration), `json/{mod,parse_with,document,builder}.rs`. |
| REVISE | 1C understates the grammar-named-reexport count by ~50%. 1C rows at `restart/audit/totality/p1/1C-runtime-evidence.md:90` and `restart/audit/totality/p1/1C-runtime-evidence.md:122` state "60+ grammar-named types" reexported from `crates/core/src/runtime/mod.rs:25-71`. Mechanical extraction of the externally-bound names from those lines (treating `pub use … as Foo` as exporting `Foo`) returns **126 distinct grammar-named symbols** (after subtracting the 10 grammar-neutral exports `DtaError, ParseErr, CompoundHandle, StringHandle, RuntimeView, StructBuilder, IntoPathSegment, Path, PathSegment, GenericAtRule`). The "60+" floor is correct but understates the surface by ~2×. The repair-LOC estimate at `1C:122` ("~50 LOC of reexports to delete") is consequently undersized; `mod.rs:25-71` is 47 lines holding 126 names, and downstream consumer rewires will be proportionally larger. | `crates/core/src/runtime/mod.rs:25-71` per direct Read; mechanical extraction yields 126 grammar-named exports (per-grammar breakdown approximate: bbnf 10, bnf 10, css_l4 ~38, css_pretty 10, csv 10, ebnf 10, google_sheets 12, json 13, math 10, plus 3 css aliases `CssDeclaration`, `CssSelector`, `CssRule` at mod.rs:34-35). |
| REVISE | 1B carries the grammar-shape leak rows `P1-1B-D8` (recognizer mining JSON-byte whitelist at `skinny/crates/passes/src/lib.rs:331`) and `P1-1B-D10` (role mining JSON literals `{`,`}`,`[`,`]`,`:`,`true`,`false`,`null` at `skinny/crates/passes/src/lib.rs:1300-1391`), and 1B's Sheets / BBNF-Self Implications section at `restart/audit/totality/p1/1B-codegen-evidence.md:69-73` correctly names the Sheets and BBNF-self generalization risk. But 1D's JSON-empirical-vs-grammar-neutral matrix does not carry a row for this: every JSON payload arena, dispatch, and SinkOnly row in 1D is labeled JSON-empirical, but the upstream pass-layer grammar-shape leak that BLOCKS non-JSON role mining from working is uncited in 1D. The lens obligation "1D separates JSON-empirical findings from grammar-neutral findings" reads as honoured for what is present, but the upstream blocker (pass-layer role-mining is JSON-shape-coded) is not in 1D's matrix, so the disproved row at `1D:106` ("Generic non-JSON grammar generation is proven") collapses codegen-layer grammar-name leaks and pass-layer grammar-shape leaks into one row. | `restart/audit/totality/p1/1B-codegen-evidence.md:50-51,63-64,71-73,86-87`; `restart/audit/totality/p1/1D-skinny-lessons.md:106` (cites 30 Lock 14 violations + Pattern H but not the role-mining shape leak). |
| REVISE | 1D row at `restart/audit/totality/p1/1D-skinny-lessons.md:113` marks `bbnf-simd` "grammar-neutral substrate" PROVED on SK-V14 axis A3 v3 §4 authority, but 1F-past-corpora PC-008 at `restart/audit/totality/p1/1F-past-corpora.md:74` records the SK-V5 pending Lock 14 `bbnf-simd` finding as "revised/partially closed" with a verify-before-rederive obligation. The unresolved question `U-PC-002` at `1F-past-corpora.md:158` explicitly demands "Run targeted `rg -n 'JSON_STRUCTURAL\|scan_json\|JsonParseIndex' skinny/crates/bbnf-simd skinny/crates/runtime` + cite every remaining hit". 1D's "proved" verdict is correct on the SK-V14 evidence but is not yet falsifiability-gated against the older SK-V5 corpus that PC-008 still tracks. The row should cross-cite PC-008 + `U-PC-002` so the grammar-neutral verdict carries its open-question pointer. | `restart/audit/totality/p1/1D-skinny-lessons.md:113`; `restart/audit/totality/p1/1F-past-corpora.md:74,150,158`. |
| REVISE | 1C-D11 at `restart/audit/totality/p1/1C-runtime-evidence.md:167` flags the layout asymmetry between skinny CSS L4 7-cluster (`css_l4_at_rules_and_media`, `css_l4_declaration_values`, `css_l4_declaration_values_extended`, `css_l4_nested_layout`, `css_l4_stylesheet_selectors`, `css_l4_vendor_and_custom_atrules`, `css_l4_visual_functions`) and main workspace's monolithic `css_l4/` + sibling `css_pretty/` pair as a generality finding requiring T-P3 disposition. But this layout-asymmetry-as-generality-failure pattern is uncited in 1D's matrix and uncited in 1F-coherence-scan's drift rows COH-001..COH-012. Whichever layout is canonical for V1, the asymmetry between the empirical floor (skinny) and the main workspace is a generality risk: SK-V14 R4 mandates a `regen-css` xtask first instance of the `regen-{grammar}` family; if main-workspace `css_l4/` is the V1 canonical shape, the skinny 7-cluster fragmentation IS the over-fragmentation Lock 14 was authored to prevent, and 1D should carry the row; if skinny is canonical, 1F-coherence-scan should carry a COH-N row naming `crates/core/src/runtime/css_l4/` as drift from the CSS L4 sub-grammar census. Neither inventory currently does. | `restart/audit/totality/p1/1C-runtime-evidence.md:167`; `restart/audit/totality/p1/1D-skinny-lessons.md` (no row); `restart/audit/totality/p1/1F-coherence-scan.md:51-64` (no COH row). |

## Lock 14 Verification Re-run At HEAD (2026-05-23)

The lens obligation requires every Lock 14 verification command from `restart/locks/LOCKS.md:220` to re-run at HEAD against the inventories' citations. All five run cleanly; results materially confirm 1C's verdicts and surface the two undercount REVISE rows above.

| Lock 14 verification command (LOCKS.md:220) | Required result | Live result at HEAD | 1C citation | Verdict |
|---|---|---|---|---|
| `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d \| wc -l` | 0 (all generated from template) | 9 (bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json, math) | `1C:193` re-derives the same 9 | ACCEPT — 1C census matches live |
| `rg -n 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/{ir,parse,codegen,runtime,path,path-core,egraph,csp-solver,parse-that-regex,parse-that,bbnf-simd,analysis,lsp}/src/` | 0 matches | 30 matches in 15 files under `crates/core/src/runtime/` (the only generic-crate sub-tree of those listed that exists at HEAD; the workspace flattened to `crates/core/src/`) | `1C:91,123` cites "19+ matches" | REVISE — 1C floor undercount |
| `rg -nE 'match\s+\w+\s*\{[^}]*Json\s*=>\|CssL4\s*=>\|Bbnf\w*\s*=>\|GoogleSheets\w*\s*=>' crates/` | 0 matches | Lives in `skinny/crates/codegen/src/grammar_profile.rs:17-26` per 1B-D7 / 1B-D13 / AP-003 / AP-012 — 8 `RuntimeProvider` variants + corresponding match arms in `skinny/crates/codegen/src/lib.rs:167-209` (per 1B census) | `1B:48,60,90` enumerate verbatim | ACCEPT — 1B census matches live |
| Pattern H file census | 0 hand-written per-grammar runtime files | 67 hand-written, 0 generated markers (`rg -l '@generated\|AUTO-GENERATED\|THIS FILE IS GENERATED' crates/core/src/runtime/` returns empty per 1C:197) | `1C:42-55,124,197` | ACCEPT — 1C census matches live |
| Grammar-named module / type reexport census in generic-crate roots | 0 grammar-named modules / types in public APIs | 9 `pub mod <grammar>;` + 126 grammar-named reexports at `crates/core/src/runtime/mod.rs:8-71` | `1C:90,121,122` cites "60+" | REVISE — 1C floor undercount (126 vs cited 60+) |

## Cross-Inventory Generality Coverage Matrix

| Generality lens demand | 1A | 1B | 1C | 1D | 1E | 1F-anti | 1F-coh | 1F-past | Coverage |
|---|---|---|---|---|---|---|---|---|---|
| Lock 14 holds | — | D7/D13 | D1,D4,D6,D10 | rows 106,112 | LAC-08,15 | AP-003,012,016 | COH-005,008,011 | PC-006,008 | full |
| No JSON-only cataloguing of grammar-neutral facts | SUB-018 | D8,D10 | mod.rs cit | rows 100,113 | — | AP-005 | COH-005 | — | full |
| 1C runtime census flags every grammar-named module | — | — | 1C-D1..D10 | — | — | AP-016 | COH-011 | — | full (1C is primary) |
| 1D separates JSON-empirical from grammar-neutral | — | — | — | rows 100,101,113 | — | — | — | — | partial — 1D matrix lacks an upstream "pass-layer role-mining is JSON-shape-coded" row that 1B/D8/D10 both name |
| No grammar-name leak passes uncited | SUB-016,017,018 | D7,D8,D10,D13 | D1-D10 | rows 106,112,125 | LAC-08,15 | AP-012..016 | COH-005,011 | PC-008 | full |

Coverage: full on four of five lens demands; one partial (1D matrix incomplete on upstream pass-layer role-mining as generality blocker).

## Required Revisions

1. **Replace "19+ matches" / "60+ grammar-named types" with live counts.** 1C rows at `restart/audit/totality/p1/1C-runtime-evidence.md:91,122,123` should be updated with the captured `rg` / `mod.rs` counts at HEAD: 30 matches across 15 files for the parser-name rg, and 126 distinct grammar-named reexports for `mod.rs:25-71`. Either commit a captured `rg` transcript artefact and cite it, or replace the floor wording with the exact live count (preferred). The Lock 14 repair-LOC band at `1C:122` ("~50 LOC of reexports to delete") should be widened proportionally; mod.rs:25-71 is 47 lines holding 126 names, and downstream consumer rewires extend the bill.

2. **Add a 1D row mapping `P1-1B-D8` + `P1-1B-D10` into the JSON-empirical-vs-grammar-neutral matrix.** The current 1D row at `restart/audit/totality/p1/1D-skinny-lessons.md:106` ("Generic non-JSON grammar generation is proven") collapses codegen and pass-layer leaks into one disproved row. Split into: (a) codegen-layer grammar-name leak (the 8 `RuntimeProvider` variants / 8 per-grammar provider modules / Pattern H 67 files, already cited); (b) pass-layer grammar-shape leak (recognizer mining JSON-byte whitelist + role mining JSON literals — currently uncited in 1D), with verify_action pointing at `skinny/crates/passes/src/lib.rs:331,1300-1391` and same-wave consumer "Sheets or BBNF-self grammar fixture whose role facts arise without code change in passes".

3. **Cross-cite PC-008 + `U-PC-002` from 1D row 113.** The `bbnf-simd` grammar-neutral substrate verdict at 1D:113 is proved on SK-V14 axis A3 v3 §4 authority, but the SK-V5 pending Lock 14 finding tracked in 1F-past-corpora PC-008 with `U-PC-002` verify-before-rederive obligation should be cross-cited so the proved row carries its open-question pointer. Recommended insertion at 1D:113 note column: "Survives SK-V14 axis A3 audit; 1F-past-corpora PC-008 + `U-PC-002` retain SK-V5 verify-before-rederive obligation pending captured `rg -n 'JSON_STRUCTURAL\|scan_json\|JsonParseIndex' skinny/crates/bbnf-simd skinny/crates/runtime` artefact."

4. **Catalogue the CSS L4 layout asymmetry (1C-D11) in 1D or 1F-coherence-scan.** Either 1D adds a row reading "skinny CSS L4 7-cluster vs main monolithic css_l4/ + css_pretty/ layout asymmetry — neither layout is canonical for V1" with T-P3 disposition obligation, or 1F-coherence-scan adds a COH-N row naming the same drift. Choice depends on whether the asymmetry is treated as a skinny-lessons digest item (1D) or a spec-surface drift (1F). The lens preference is 1D, because the SK-V14 R4 `regen-css` xtask obligation per `restart/skinny/tranches/sk-v14/SYNTHESIS.md:96` is what determines which layout is canonical, and 1D is the dispatch surface for SK-V14-binding obligations.

## New Findings Surfaced

NEW-CH2-01: The `crates/core/src/runtime/google_sheets/document/` sub-module (4 files at `mod, canonical, view, path_query`) is the only nested per-grammar sub-module at HEAD. 1C-D8 flags it correctly for T-P3 disposition. The `google_sheets/document/mod.rs:43,142` site adds two `GoogleSheetsParser` references not enumerated in 1C's parser-name leak row at `1C:91`; this is part of the count-undercount REVISE row 7 above, but worth surfacing as a structural fact: the nested sub-module pattern, if generalised across other grammars, multiplies the per-grammar leak surface.

NEW-CH2-02: Live `mod.rs` at `crates/core/src/runtime/mod.rs:25-71` carries three aliased CSS imports (`CssRule`, `CssDeclaration` aliased from `Declaration`, `CssSelector` aliased from `Selector`) — the `as` rewrites at `mod.rs:34-35` are the only grammar-name-disambiguation aliases at HEAD. The Lock 14 leak surface includes these aliases: deleting the reexports requires the downstream consumers either to import the original short names (`Declaration`, `Selector`) directly, which conflicts with the Lock 14 "ZERO grammar-named types in public APIs" mandate, OR to be rewired through a generated trait method. This is implicit in 1C's census but worth surfacing explicitly for T-P2 to research.

NEW-CH2-03: 1C row at `restart/audit/totality/p1/1C-runtime-evidence.md:99` records the JSON mod.rs comment "the tape substrate is severed on the JSON parse path" and the CSV mod.rs identical wording — these are evidence that the tape-substrate severance is documented in PROSE rather than enforced in TYPE, which is a separate Lock 1 / Lock 14 generality concern from the file census. The 1A inventory at `1A-SUB-016` carries the substrate-union finding; the 1C prose-evidence row should be cross-cited from 1A so the substrate-union finding has both type-evidence (1A) and prose-evidence (1C) cites.
