---
lens: CH1-CORRECTNESS
pass: T-P1-excavation
cycle: V4
subject: SK-V17 T-P1 EXCAVATION
generated_at: 2026-05-29T19:10:00Z
master_head: 445925167154de73540e3ea3283d0170371de790
reviewer_method: "Read PASS-1-EXCAVATION.md §3 CH1 + ORCHESTRATOR §3W; Read prior V3/CH1.md to carry continuity + verify the three V3 REVISE dispositions (CH1-V3-001/002/003) were folded; Read the three CH1-scope V4 inventories (1a-substrate, 1c-runtime, 1d-skinny-lessons) end-to-end; re-anchored every load-bearing file:line/SHA via grep -n/-c + sed -n over crates/core/src/{runtime,backend,grammar/generated}, crates/ir/src/registry/struct.rs, crates/simd-scan/src/alphabet.rs, skinny/crates/{runtime,ir,codegen,bbnf-simd}, restart/{ARCHITECTURE,MASTER-PLAN}.md, restart/locks/LOCKS.md, restart/skinny/tranches/sk-v17/{SPEC.md,research/alpha/*}, skinny/RESULTS.md; no recalled LOC, every claim re-anchored at source"
inventories_reviewed:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
prior_cycle_dispositions_carried:
  - CH1-V3-001 (fabricated *ChildrenIter view.rs:39/44 + substrate.rs:60-not-62) — RESOLVED: 1C now cites JsonChildrenIter at json/view.rs:39, CssChildrenIter at css_l4/view.rs:44 (both verbatim-confirmed live); CssStructBuilder doc at substrate.rs:60 (confirmed)
  - CH1-V3-002 (HAND-WRITTEN→generated, inverts Lock-14 cause) — RESOLVED: 1C says "GENERATED ... NOT hand-written" throughout; all six @generated headers confirmed live
  - CH1-V3-003 (shared HAND-WRITTEN in 1D SK17L-002/Exec) — RESOLVED: 1D says "EAGER-generated vs LAZY-generated", "Lock-14 HONOURED not violated"
disposition_counts:
  accept: 4
  revise: 2
  reject: 0
sections_dispositioned: 6
---

## CH1 CORRECTNESS — Scope + Method

CH1 verifies, for V4: every spec-claim ↔ implementation row resolves at the
cited file:line/SHA; the tape-substrate / eager-OpenFrame divergence is
accurately mapped; SKINNY-proven facts are cited correctly. No recalled LOC,
no recalled symbol path. V4's primary obligation is to confirm the three V3
REVISE dispositions (CH1-V3-001/002/003) were folded, then re-scan the three
correctness-floor inventories — **1A substrate**, **1C runtime census**,
**1D skinny-lessons** — for any residual or newly-introduced citation defect.

Master HEAD confirmed `445925167154de73540e3ea3283d0170371de790`.

**Verdict in brief.** The three V3 REVISE dispositions are folded **and
live-verified** (§6 ACCEPT): `JsonChildrenIter` resolves at `json/view.rs:39`,
`CssChildrenIter` at `css_l4/view.rs:44`, the `CssStructBuilder` doc-example at
`substrate.rs:60`, and all six per-grammar value/builder/view modules carry the
`// @generated by xtask regen-*` header — so the "GENERATED not hand-written"
relabel is correct and the Lock-14 cause now reads consistently. Of ~70 distinct
file:line/SHA citations re-anchored across 1A/1C/1D, the overwhelming majority
resolve verbatim — the SoA `Tape<'input>` six-member layout, `ValueRef<…,K,G>`,
`value_from_ref:143`, `select_classifier:42`, the AoS `TapeRec`+16-byte asserts,
`begin_compound(&StructLayout)`, StructLayout=960, the CSS `OpenFrame`+six-Vec+
one-Option count, the 8 scan call-sites at exact lines, RESULTS Track-1>sonic
rows, the 24-row broadcast, cost.rs `for_backend_shape`, and every alphaC/alphaD/
SPEC/LOCKS corpus anchor. **One new defect requiring REVISE** surfaced, shared by
1C and 1D: both assert `math.rs` "carries the `OnceCell<StructuralIndex>` FIELD"
— it does not. `math.rs`'s only `OnceCell`/`StructuralIndex` occurrences (lines
281, 285) are **doc-comment text** inside `__shape_support_MathParser::ScanState`'s
doc-block; the actual `ScanState` struct carries only `nospace_bits: u64` +
`nospace_start: isize`, no `structural_index` field at all. This is the same
symbol-vs-doc-comment class CH1 caught in V3 (the `*ChildrenIter` case), now
recurring in a different file. The substantive census (8 scan-wired, math NOT
wired) is correct and unaffected; only the "math carries the field" sub-claim is
false. 1A is clean on this point — it claims only "8 wired + math.rs=0" and never
asserts the field. No REJECT: no inverted impl verdict, no unresolved load-bearing
recall.

---

## §1 — 1A substrate-evidence.md spec-claim↔impl table + V4 fold — ACCEPT

All eight SUB17 rows + the Cross-Tree map + the Firewall table + the Open-Question
rows were re-anchored. Every load-bearing citation resolves verbatim:

- **SUB17-001** SoA `Tape<'input>` six members — `skinny/crates/runtime/src/tape/mod.rs:94-100`
  (`source: &'input [u8]`, `offsets: Vec<u32>`, `flag_cursors: Vec<u32>`,
  `flag_values: Vec<u8>`, `payloads: PayloadArena`, `id: TapeId`) — **verbatim-confirmed**.
  Lock 1 `LOCKS.md:75`, ARCH `:1088` — confirmed.
- **SUB17-002** core AoS `TapeRec` — `record.rs:103` (`pub struct TapeRec`),
  16-byte+align-4 const-asserts at `:120-121`, mod-doc "kept AoS first … the same
  `TapeCursor` API rides a later SoA split" at `mod.rs:5-9` — **all verbatim-confirmed**.
  `TapeStructBuilder` at `tape/mod.rs:58` — confirmed.
- **SUB17-003** UNWIRED: `TapeStructBuilder` grep-zero outside `tape/`
  (`grep -rln TapeStructBuilder crates/core/src/ | grep -v /tape/` = **empty**,
  re-ran) — **confirmed**. Live JSON `JsonStructBuilder::new()` at
  `json/parse_with.rs:34`, CSS `CssStructBuilder::new()` at `css_l4/parse_with.rs:34`
  — confirmed.
- **SUB17-004** core has no `value_from_ref`/`ValueRef` in `json/value.rs` (grep
  empty); `CssTypedValue<'p>` at `css_l4/value.rs:414` — confirmed.
- **SUB17-005 (V4 fold)** x86 bar now cited at SPEC `:806` ("**x86 / AVX-512 / SVE:**
  aarch64 only (Apple cores have no SVE)") — **verbatim-confirmed**; the second-substrate
  block is the separate `:807-811` (confirmed: skinny StructLayout/TapeStructBuilder/…
  list). D6 REJECT at `:852-854` ("D6 second substrate") — confirmed. ARCH `:1206`
  UNKNOWN-2D-05 ("aarch64 mechanically refused", "aarch64 candidate is UNKNOWN-2D-05")
  — confirmed. The CH3-R4 off-by-one (x86-bar :806-not-807) is correctly folded.
- **SUB17-006** `StructLayout` Lock-2-retired (`LOCKS.md:160` "replaces … *StructLayout*
  … everywhere") yet live at `crates/ir/src/registry/struct.rs:202`;
  `grep -rn StructLayout crates/` = **960** (re-ran, exact) — **confirmed**.
- **SUB17-007** richer `StructuralAlphabet` (`singletons`/`digraph_mask:[u64;4]`/
  `digraph_pairs`/`quote_classes`) at `crates/simd-scan/src/alphabet.rs:19-37`;
  quote_classes JSON/CSS-motivated doc `:33-37` ("`b'"'` for JSON, `b'\''` and
  `b'"'` for CSS") — **verbatim-confirmed**. "config-breadth not proof-breadth" sound.
- **SUB17-008/009** two simd crates + the per-leaf-registry fence (`struct.rs:84,313,331`
  — FieldSource:84, StructRegistry:313, `layout(rule_id) -> Option<&StructLayout>`:331;
  `begin_compound` reads `layout.rule_id & 0x1F` at `tape/mod.rs:186`) — **confirmed**.

**V4-fold integrity (CH3-R4, CH5-S8):** the Firewall Sidecar row re-anchors REDRESS-53
to SPEC `:825` (W2 row, "L1/L4 index as parallel retained vector (REDRESS-53)") +
`:839` ("A retained parallel index collapses into REDRESS-53") — **both verbatim-confirmed**.

**Math-field check:** 1A's frontmatter states only "8 wired + math.rs=0" — it does
**not** claim math carries the `OnceCell` field. 1A is clean on the defect that
afflicts 1C/1D (§3/§4). Counts (implemented=2, unimplemented=4, impl-exceeds-spec=2,
unknown=2) tally against the verdict column.

DISPOSITION (§1): **ACCEPT** — every row resolves; V4 off-by-one folds confirmed;
1A does not carry the math-field mischaracterisation.

---

## §2 — 1A SKINNY-proven citations + tape↔OpenFrame map — ACCEPT

CH1's named focus, against 1A. The SKINNY-proven inputs all resolve at source:

- `Tape<'input>` SoA + `offsets: Vec<u32>` — `skinny/.../tape/mod.rs:94-100`. ✓
- `ValueRef<'doc, 'input: 'doc, K = AnyKind, G: EventGrammar = AnyGrammar>` —
  `skinny/.../tape/mod.rs:175` (1A's `ValueRef<'doc,'input>` shorthand is a faithful
  elision of the full generic list; symbol + line exact). ✓
- `value_from_ref` — `skinny/crates/runtime/src/grammars/json/value.rs:143`
  (`pub(crate) fn value_from_ref<'doc, 'input: 'doc>(`); the `grammars/` segment is
  carried throughout (CH1-V2-002 stays RESOLVED). ✓
- `select_classifier(alphabet: &'static [u8; 64])` —
  `skinny/crates/bbnf-simd/src/dispatch.rs:42`. ✓

The tape-substrate ↔ eager-OpenFrame divergence map (Cross-Tree table + Substrate-Union
Firewall) is accurate end-to-end: the dead AoS `TapeStructBuilder` vs the live eager
`OpenFrame` builders (`json/builder.rs:9`, `css_l4/builder.rs:16`), the
`OnceCell<StructuralIndex>` scan-cache, the no-cross-call-classifier-state Lock-1-v+1
check (`LOCKS.md:137-149`), and the per-leaf-registry fence (`SPEC:794-795`) are all
rendered correctly. No Track-1≡Track-2 dishonesty; the tape is correctly catalogued
dormant, not a sidecar.

DISPOSITION (§2): **ACCEPT**.

---

## §3 — 1C runtime-evidence.md — REVISE

**V3 dispositions folded + live-verified:**

- **CH1-V3-001 (fabricated `*ChildrenIter`)** RESOLVED. RT17-004 now cites
  `JsonChildrenIter` at `json/view.rs:39` and `CssChildrenIter` at `css_l4/view.rs:44`.
  Both **live-verified**: `grep -n ChildrenIter crates/core/src/runtime/json/view.rs`
  returns `39:pub struct JsonChildrenIter<'a, 'p: 'a> {`; `css_l4/view.rs` returns
  `44:pub struct CssChildrenIter<'a, 'p: 'a> {`. The symbols moved from the (clean)
  top-level `view.rs` to the correct per-grammar `runtime/<g>/view.rs` — exactly
  right. The `substrate.rs:60` doc-example (`"::bbnf::runtime::css_l4::CssStructBuilder"`)
  — confirmed at line 60.
- **CH1-V3-002 (HAND-WRITTEN→generated)** RESOLVED. Exec Summary + RT17-002 + RT17-004
  now say "GENERATED … NOT hand-written". All six headers live-verified:
  `json/value.rs:1` / `json/builder.rs:1` / `json/view.rs:1` = `@generated by xtask
  regen-json`; `css_l4/{value,builder,view}.rs:1` = `@generated by xtask regen-css`.
  The Lock-14 verdict now reads consistently (generated ⇒ honoured).

**Other base citations re-anchored and confirmed:** the 8 scan call-sites
(json.rs:732, css_l4.rs:15982, bbnf.rs:4843, bnf.rs:848, csv.rs:566, ebnf.rs:1381,
css_pretty.rs:1905, google_sheets.rs:3559 — each `::simd_scan::scan_structural(input,
&alphabet)`, grep `scan_structural(`=1 per file); the real `structural_index:
::core::cell::OnceCell<…>` FIELD in json.rs at **:701** (confirmed a genuine struct
field, not a doc-comment); `begin_compound(&StructLayout)` at `tape/mod.rs:185`;
`tape/mod.rs:55` "never on per-grammar route strings"; `substrate.rs:43,55`
builder_path/document_path data-bind; BackendShape canon `ARCH:31` (`5-shape canon
{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`); RT17-007 grep-empty
shape-symbol finding.

**One defect requiring REVISE:**

**CH1-V4-001 — RT17-005 + Exec Summary assert `math.rs` "holds the
`OnceCell<StructuralIndex>` FIELD"; it does not.** The Exec Summary states "`math.rs`
is the lone generated dir with the `OnceCell` field (math.rs:OnceCell=1) but ZERO
scan_structural calls"; RT17-005's evidence column says "`math.rs` holds the field
but no scan call." Both are false on two counts:
- `grep -c 'OnceCell' crates/core/src/grammar/generated/math.rs` = **2** (not 1),
  at lines **281 and 285**.
- Both occurrences are **doc-comment text**, not a struct field. They sit inside the
  doc-block of `__shape_support_MathParser::ScanState` (line 281: "… ScanState
  additionally carries a lazy `OnceCell<StructuralIndex>` consumed by CTNS-style"; the
  comment is explicitly conditional — "for grammars whose `structural_alphabet` is
  non-empty"). The actual `ScanState` struct (math.rs:286+) declares only
  `nospace_bits: u64` and `nospace_start: isize` — **no `structural_index` /
  `OnceCell<StructuralIndex>` field exists in math.rs**.

This is the same recalled-symbol-vs-doc-comment failure class CH1 caught in V3
(`*ChildrenIter` at the top-level `view.rs` doc lines). The substantive census is
correct — math IS the lone generated grammar with **zero** `scan_structural` calls
(8 wired, not 9), and that conclusion stands. Only the "math carries the field"
sub-claim is wrong, and the `OnceCell=1` grep count is wrong (it is 2 doc-comment
lines). No verdict is inverted; the U-RT17-001 / RT17-005 substrate_target finding is
about the **eight** scan-wired grammars' real fields, which are genuine
(json.rs:701 verbatim) — so the load-bearing finding survives.
- **file:line**: `1c-runtime-evidence.md` Exec Summary ("math.rs … OnceCell field
  (math.rs:OnceCell=1)") + RT17-005 ("`math.rs` holds the field") + RT17-005 evidence
  column ("`math.rs` holds the field but no scan call") + the Cross-Tree scan row
  ("`math.rs` holds the OnceCell field but scan_structural=0"), all contradicted by
  `crates/core/src/grammar/generated/math.rs` (grep -c OnceCell = 2, both doc-comments
  at :281/:285; `ScanState` struct carries only `nospace_bits`/`nospace_start`).
- **Fix**: replace "math.rs holds the `OnceCell` field but no scan call" with "`math.rs`
  is the lone generated grammar with **zero** `scan_structural` calls; its only
  `OnceCell<StructuralIndex>` mentions (math.rs:281,285) are doc-comment text in the
  `ScanState` doc-block — `math.rs`'s `ScanState` struct carries no `structural_index`
  field." Strike "(math.rs:OnceCell=1)" (grep count is 2, both doc-comments). The
  8-wired census and the "8 not 9" conclusion are unchanged.

DISPOSITION (§3): **REVISE** — V3 defects fully folded + live-verified; the 8-grammar
scan census, the json.rs:701 real field, the @generated relabel, and every line
citation are sound; one new defect (CH1-V4-001) mischaracterises a `math.rs`
doc-comment as a struct field and mis-states the grep count.

---

## §4 — 1D skinny-lessons.md RESULTS/REDRESS + corpus citations — REVISE

CH1's named focus per §3 ("RESULTS-row and REDRESS-entry citations in 1D resolve to
real entries"). **V3 disposition folded:** CH1-V3-003 RESOLVED — SK17L-002 + Exec now
say "EAGER-generated vs LAZY-generated, not generated-vs-hand-written", "Lock-14
HONOURED not violated", with both `@generated` headers cited. The eager-vs-lazy
divergence is intact.

Re-anchored and **confirmed**:

- **RESULTS Track-1>sonic** `skinny/RESULTS.md:5-55` — confirmed: twitter parse_only
  Track-1 8349.290 > sonic 4913.095 (Track 2 4558.264, per-iter equality PASS); twitter
  direct_to_struct Track-1 17585.679 > strict sonic 14857.624 — verbatim. ✓
- **24-row broadcast** `skinny/RESULTS.md:112-135` — the slice resolves (the
  AUDIT-FALSIFIED css_l4 full-parse diagnostic rows, `not_admitted:SK-V15-W0-broadcast-
  diagnostic`); the broadcast pre-block framing is accurate. ✓
- `TapeBuilder` `assembler.rs:42`, `push_plain_offset` `assembler.rs:71`,
  `PayloadArena` `tape/mod.rs:38` — **all confirmed**. ✓
- skinny `cost.rs:119-121` `for_backend_shape` → `EagerTape | OffsetTape | EventTape`
  — confirmed. ✓
- `W5C_REQUEST_FACT_PROFILES` `skinny/crates/codegen/src/lib.rs:336` — confirmed. ✓
- **Corpus refs**: alphaC §1 (`:50` "AZ-IV eager-value-tree materialization (118x)"),
  §2 (`:98` "StructRegistry / Arena<G> / Builder<G> hot-path indirection (28-65x; 983x;
  10583x WATCHDOG)"), §6 (`:307` "x86 / AVX paths"); alphaD I5 (`:104`, PRE-BLOCKED),
  I7 (`:106`, W6 single-sample INVALIDATED); SPEC `:794-795` ("No registry lookup in the
  per-leaf hot path"); LOCKS:100 ("v+1 FactStream 5th substrate category"), :349 (Lock 14
  full generalisation), :160 (Lock 2 StructLayout retired) — **all resolve verbatim**. ✓

The JSON-empirical vs grammar-neutral split is accurate; the do-not-redrive ledger maps
each pre-block to a real corpus anchor. Counts (implemented=1, unimplemented=4,
impl-exceeds-spec=2, unknown=2) tally.

**One defect requiring REVISE (shared root with CH1-V4-001):**

**CH1-V4-002 — SK17L-008 + frontmatter assert `math.rs` carries the
`OnceCell<StructuralIndex>` "index FIELD".** SK17L-008's impl column says "`math.rs`
is the lone generated grammar carrying the `OnceCell<StructuralIndex>` index FIELD but
NO `scan_structural` call … the only `OnceCell`/`StructuralIndex` hits are doc-comments
at `math.rs:281`,`:285`." The two clauses **contradict each other**: the same sentence
calls it a FIELD and then admits the hits are doc-comments. The frontmatter
`live_truth_method` likewise says "math is field-only at :281/:285 doc-comments" — a
self-contradiction ("field-only" + "doc-comments"). Ground truth: math.rs's `ScanState`
struct carries only `nospace_bits`/`nospace_start`; lines 281/285 are doc-comment text.
There is no `structural_index` field in math.rs.
- **file:line**: `1d-skinny-lessons.md` SK17L-008 impl column ("carrying the
  `OnceCell<StructuralIndex>` index FIELD") + frontmatter `live_truth_method`
  ("math is field-only at :281/:285 doc-comments"), contradicted by
  `crates/core/src/grammar/generated/math.rs` (ScanState struct fields
  `nospace_bits`/`nospace_start`; :281/:285 doc-comments only).
- **Fix**: change "carrying the `OnceCell<StructuralIndex>` index FIELD" → "carrying NO
  `structural_index` field; its only `OnceCell<StructuralIndex>` mentions (math.rs:281,
  285) are doc-comment text in the `ScanState` doc-block." Restate the frontmatter as
  "math is the lone generated grammar with `scan_structural`=0 and no `structural_index`
  field — only doc-comments at :281/285." The verified-EIGHT-wired census and the
  impl-exceeds-spec verdict are unchanged.

Note: CH1-V4-001 (1C) and CH1-V4-002 (1D) share one root — the `math.rs` doc-comment
mistaken for a struct field. Folding requires both authors to correct in lockstep; not
an orphan (both carriers named).

DISPOSITION (§4): **REVISE** — RESULTS/REDRESS/corpus citations all resolve; CH1-V3-003
folded; correct the shared `math.rs` doc-comment-as-field mischaracterisation in
SK17L-008 + frontmatter.

---

## §5 — Tape-substrate ↔ eager-OpenFrame divergence map accuracy (cross-inventory) — ACCEPT

CH1's named focus, audited across 1A + 1C + 1D as one coherent map:

- The proven SoA substrate (`skinny/.../tape/mod.rs:94-100` `Tape<'input>` + `offsets:
  Vec<u32>` + sparse flags + `PayloadArena`) vs the core AoS `TapeRec` (16-byte
  const-asserted, `record.rs:103,120-121`) — rendered identically and correctly in
  1A SUB17-001/002, 1C RT17-001, 1D SK17L-001.
- The UNWIRED-tape / live-eager-OpenFrame split: `TapeStructBuilder` grep-zero outside
  `tape/`; live JSON `JsonStructBuilder` (`json/parse_with.rs:34`); live CSS
  `CssStructBuilder` + `OpenFrame` (`css_l4/builder.rs:16`) + the **corrected** six
  `pending_*` Vec (`:74-79`: pending_rules / pending_decls / pending_selectors /
  pending_values / pending_blocks / pending_components) + one `pending_value: Option`
  (`:71`) — live-verified across all three inventories (CH1-V2-005 stays RESOLVED).
- The scan-wired-all-8 / tape-unwired asymmetry — confirmed (8 scan call-sites at exact
  lines; css_l4 IS scan-wired with the rich alphabet at `css_l4.rs:15976-15982`).

The load-bearing fold finding (eager `OpenFrame` builders are the live substrate; the
tape is dormant SK-V18 fold-target code) is accurately rendered everywhere it appears.
The CH1-V4-001/002 math-field defect does not touch this map (it concerns the
scan-cache census of a non-scanning grammar, not the substrate shape).

DISPOSITION (§5): **ACCEPT**.

---

## §6 — Prior-cycle (V3) disposition fold integrity — ACCEPT

All three V3 REVISE dispositions are folded **and live-re-verified** at source:

- **CH1-V3-001** RESOLVED + live-verified: `JsonChildrenIter` at `json/view.rs:39`
  (`grep -n` confirmed), `CssChildrenIter` at `css_l4/view.rs:44` (confirmed); the
  fabricated top-level-`view.rs` citations are gone, replaced by the real per-grammar
  symbols. `substrate.rs:60` CssStructBuilder doc-example confirmed (off-by-two fixed).
- **CH1-V3-002 / CH1-V3-003** RESOLVED + live-verified: all six per-grammar
  value/builder/view modules carry `// @generated by xtask regen-{json,css}; do not
  edit by hand.` at line 1 (`head -1` confirmed on all six); both 1C and 1D relabel
  "HAND-WRITTEN" → "GENERATED" and reconcile the Lock-14 cause (generated ⇒ honoured).

The frontmatter `prior_cycle_dispositions_folded.revised` lists in both 1C and 1D map
to the V3 disposition IDs with accurate fix descriptions. No prior disposition is
mis-folded or left orphan; the fold is source-anchored, not paper-folded.

DISPOSITION (§6): **ACCEPT**.

---

## Disposition Summary

| § | Subject | Disposition | Finding |
|---|---|---|---|
| §1 | 1A spec↔impl table + V4 off-by-one folds + counts | **ACCEPT** | — |
| §2 | 1A SKINNY citations + tape↔OpenFrame map | **ACCEPT** | — |
| §3 | 1C runtime census | **REVISE** | CH1-V4-001 (math.rs `OnceCell<StructuralIndex>` is doc-comment text at :281/285, NOT a struct field; grep count is 2 not 1) |
| §4 | 1D skinny-lessons RESULTS/REDRESS/corpus | **REVISE** | CH1-V4-002 (shared root: SK17L-008 + frontmatter call the math.rs doc-comment an "index FIELD"; self-contradictory "field-only … doc-comments") |
| §5 | Tape↔OpenFrame divergence map (cross-inventory) | **ACCEPT** | — |
| §6 | Prior-cycle (V3) disposition fold integrity | **ACCEPT** | — |

**Counts**: ACCEPT 4 · REVISE 2 · REJECT 0.

**Verdict**: V4 folds all three V3 REVISE dispositions correctly, and each fold is
live-re-verified at source (§6): the `*ChildrenIter` symbols now resolve at their real
per-grammar `view.rs:39/44`, `substrate.rs:60` is correct, and all six per-grammar
runtime modules carry the `@generated` header that makes the Lock-14-honoured verdict
read consistently. The tape-substrate ↔ eager-OpenFrame divergence map and every
SKINNY-proven citation resolve verbatim (§1, §2, §5, §6 ACCEPT). One new defect remains,
shared by 1C and 1D under a single root (CH1-V4-001 / CH1-V4-002): both inventories
assert `math.rs` carries an `OnceCell<StructuralIndex>` struct field, when the file's
only such occurrences (lines 281, 285) are **doc-comment text** in the
`__shape_support_MathParser::ScanState` doc-block — `ScanState` itself declares only
`nospace_bits`/`nospace_start`, no `structural_index` field; the `OnceCell=1` grep count
is also wrong (it is 2). This is the same symbol-vs-doc-comment class CH1 caught in V3.
The substantive census ("8 scan-wired, math is the lone non-scanning generated grammar,
8 not 9") is correct and unaffected, and 1A is clean (it claims only "math.rs=0", never
the field). No REJECT: no inverted impl claim, no unresolved load-bearing recall. With
CH1-V4-001/002 folded, CH1's correctness floor is met for SK-V17 T-P1 V4 — the lens
is one defect short of a clean ACCEPT-all, and that defect is a doc-comment misread, not
a substrate-shape or SKINNY-proof error.
