# SK-V16 Alpha V3 CH3 - Regression

Disposition: REVISE-FOLDED -> ACCEPT.

CH3 found that the first V3 packet preserved SK-V15 evidence locks but did not
carry every inherited SK-V15 REDRESS semantic by name.

Folded edits:

- `SYNTHESIS.md` now carries retained cursor/list, aux density/projection
  tables, retained sidecar tables, cursor streams, parser-owned structural
  projections or streams, wrong-plane comparator admission, BBNF-side hot-leaf
  wording, and decoded-string / structural-stream / string64 / fixed-shape
  Unicode retry blocks.
- `alpha-C-redress-digest.md` mirrors the same full Section 15 semantics.
- `alpha-E-candidate-shortlist.md` cites `SYNTHESIS.md` and `alpha-C` as
  binding and names the high-risk inherited blockers directly.
- `alpha-F-contract-draft.md` makes the full inherited REDRESS semantics binding
  on S-P0 through S-P3.

After fold, CH3 re-ran and returned ACCEPT.

Required edits: none after fold.
