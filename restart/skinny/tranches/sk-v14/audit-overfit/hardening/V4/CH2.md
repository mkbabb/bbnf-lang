# CH2 — GENERALITY (V4)

S-P0 CHALLENGE V4 confirming-pass lens disposition (SK-V14 Overfit
Audit Pass; Lock 14 generality posture).

Authority: `restart/prompts/ORCHESTRATOR.md §3W` row CH2 ("Lock 14
holds: no grammar-name leak; every proposed intervention is grammar-
neutral and works for CSS L4 / Sheets / BBNF-self, not only JSON.")
+ `restart/skinny/tranches/sk-v14/audit-overfit/hardening/V3/CH2.md`
§5 V4 fold recommendation (close N-V3-CH2-1 inclusion-relation
defect at SYNTHESIS §2.4:311) + V4 micro-redispatch commit
`f8e279877` (1 file modified, +9 / -4; F-V4-SYNTHESIS-{1,2}).

V4 artefacts under review (HEAD == `f8e279877`):

- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`
  (V4; F-V4-SYNTHESIS-1 §2.4:311-318 inclusion-relation + F-V4-SYNTHESIS-2
  §3.1:347 C-3 row "3 → 4 scanners are fixture lookups")

A1, A2, A3, A4, A5, A6 STAND verbatim (per V4 commit message + commit
stat: only SYNTHESIS-AUDIT-OVERFIT.md modified).

## §0 — Disposition summary

| Total dispositions | ACCEPT | ACCEPT-with-NOTE | REVISE | REJECT |
| ---: | ---: | ---: | ---: | ---: |
| **8** | **8** | **0** | **0** | **0** |

ACCEPT-rate **8/8 = 100 %**. F-V4-SYNTHESIS-1 closes N-V3-CH2-1
cleanly: §2.4:311-318 now reads correctly as "56 files carry the
fake header (42 runtime-side mirror + 14 codegen-side (8 providers+
templates + 6 ancillary)) — disjoint sets per the lint glob brace-
expansion". The 42 + 14 = 56 arithmetic is now explicit; the
disjointness is named directly; the lint glob's grammar-neutral
binding to both runtime + codegen sides is preserved as load-bearing.

V3 baseline (per V3 CH2.md §6 — 7 ACCEPT + 1 ACCEPT-with-NOTE) holds
under V4: F-V3-A4-1 + F-V3-A5-1 + F-V3-SYNTHESIS-{1,3} all carry
forward unchanged (V4 commit stat confirms zero touches to A4 / A5
/ axis files). The single V3 ACCEPT-with-NOTE upgrades to ACCEPT
under V4 by the closing fold; the V3-deferred editorial sub-defect
is retired.

The CH2 GENERALITY posture remains sound end-to-end after the V4
micro-fold. Lock 14 baseline (A3 30 violations) holds verbatim
under V4 — A3 carried no V4 fold; V4 commit stat confirms A3 file
unchanged. The 9-grammar census re-attests byte-identically at V4
HEAD. Primitive-crate generic posture (zero grammar-name leaks
across `bbnf-regex`, `bbnf-simd`, `simd-scan`, `parse-that-regex`,
`ir`) preserved through V4.

## §1 — Per-artefact V4 disposition

| Artefact | V3 → V4 fold(s) | V4 Disposition | CH2 verification |
| --- | --- | --- | --- |
| `SYNTHESIS-AUDIT-OVERFIT.md` (V4) | F-V4-SYNTHESIS-1 (§2.4:311-318 "42 files … including 14 codegen-side" → "56 files (42 runtime-side mirror + 14 codegen-side (8 providers+templates + 6 ancillary)) — disjoint sets per the lint glob brace-expansion") + F-V4-SYNTHESIS-2 (§3.1:347 C-3 row prose cell "3 scanners are fixture lookups" → "4 scanners are fixture lookups") | **ACCEPT** | §3.1 below; F-V4-SYNTHESIS-1 closes V3 N-V3-CH2-1 inclusion-relation defect cleanly with Option B (compound) reading from V3 §5 rec 1; F-V4-SYNTHESIS-2 closes V3 CH7 §1.5 cross-flag (CH2-coincident only on grammar-neutrality of fixture-lookup violation class — F-V4-SYNTHESIS-2 is primary CH7 territory but the count refresh keeps the fixture-lookup grammar-neutrality reading consistent across all six SYNTHESIS surfaces). |
| `sk-v14-audit-overfit-generator-truth.md` (A4) | STAND (no V4 folds; V3 F-V3-A4-1 line-count refresh carries forward) | **ACCEPT** | V3 CH2 §1 A4 row carries forward; V4 commit stat confirms A4 file unchanged |
| `sk-v14-audit-overfit-decision-engine.md` (A5) | STAND (no V4 folds; V3 F-V3-A5-1 action-class propagation carries forward) | **ACCEPT** | V3 CH2 §1 A5 row carries forward; V4 commit stat confirms A5 file unchanged |
| `sk-v14-audit-overfit-css-measurement.md` (A1) | STAND (no V4 folds; V1+V2+V3 STAND) | **ACCEPT** | V3 CH2 §1 A1 row carries forward |
| `sk-v14-audit-overfit-admit-mechanism.md` (A2) | STAND (no V4 folds; V1+V2+V3 STAND) | **ACCEPT** | V3 CH2 §1 A2 row carries forward |
| `sk-v14-audit-overfit-lock14-scan.md` (A3) | STAND (no V4 folds; V1+V2+V3 STAND) | **ACCEPT** | V3 CH2 §1 A3 row carries forward; 30 violations re-verified at V4 HEAD (§3.2 below) |
| `sk-v14-audit-overfit-pre-restart-pattern.md` (A6) | STAND (no V4 folds; V1+V2+V3 STAND) | **ACCEPT** | V3 CH2 §1 A6 row carries forward; Pattern H 9-grammar census preserved at V4 HEAD |
| **Aggregate** | | **ACCEPT** | — |

## §2 — Per-§ ACCEPT-rate (V4 cycle)

Per V3 CH2 §5 V4 fold recommendation, the V4 lens has one specific
fold-verification clause (N-V3-CH2-1 closure) plus the standard
fresh-finding scan + carry-forward bookkeeping:

| CH2 V4 sub-clause | ACCEPT | ACCEPT-with-NOTE | REVISE | REJECT |
| --- | ---: | ---: | ---: | ---: |
| (i) **F-V4-SYNTHESIS-1 closes V3 N-V3-CH2-1 inclusion-relation defect at §2.4:311 (42 + 14 = 56 disjoint, not "42 including 14")** | 1 | 0 | 0 | 0 |
| (ii) **F-V4-SYNTHESIS-2 closes V3 CH7 §1.5 cross-flag at §3.1:347 (3 → 4 scanners are fixture lookups)** | 1 | 0 | 0 | 0 |
| (iii) **Fresh-finding scan over V4 deltas + Lock 14 baseline re-verification + primitive-crate posture re-check** | 1 | 0 | 0 | 0 |
| (iv) **V4 carry-forward of V3 ACCEPTs (A1, A2, A3, A4, A5, A6 STAND)** | 5 | 0 | 0 | 0 |
| **All** | **8** | **0** | **0** | **0** |

Note: F-V4-SYNTHESIS-2 is enumerated under CH2 because the fixture-
lookup violation class is grammar-neutral (would catch any future
grammar attempting the same overfit), and the cross-surface count
consistency keeps the GENERALITY scope coherent — but it is primary
CH7 territory; CH2 confirms only that the refresh does not perturb
the grammar-neutrality reading.

## §3 — Executable verification (CH2 V4 evidence)

Per ADDENDUM §3 executable-verification mandate (carried through to
V4 per §3Z confirming-pass discipline), the following were re-run
against the working tree at HEAD == `f8e279877` (V4 micro-redispatch
landing commit).

### §3.1 F-V4-SYNTHESIS-1 — inclusion-relation closure

V3 CH2 §4.1 N-V3-CH2-1 flagged SYNTHESIS §2.4:311 prose "42 files
carry the fake header including 14 codegen-side" as an arithmetic
inclusion-relation defect (the 42-runtime and 14-codegen sets are
disjoint, not subset-related). V4 verification of the closing fold:

```
$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ | wc -l
42

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/ | wc -l
14

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ skinny/crates/codegen/src/ | wc -l
56
```

42 + 14 = 56 confirmed with zero overlap (the two path roots
`runtime/src/grammars/` and `codegen/src/` are siblings, not nested).
The V4 prose at SYNTHESIS §2.4:311-318 now reads:

> "CH2 §3.5 verified 56 files carry the fake header (42 runtime-
> side mirror + 14 codegen-side (8 providers+templates + 6
> ancillary)) — disjoint sets per the lint glob brace-expansion
> `{runtime/src/grammars,codegen/src}/**/*.rs`; `git grep -l
> '@generated by skinny bbnf-codegen' skinny/crates/runtime/src/grammars/
> | wc -l = 42` and `git grep -l '@generated by skinny bbnf-codegen'
> skinny/crates/codegen/src/ | wc -l = 14`, summing to the 56 total
> with zero overlap (the two path roots are siblings)."

The V4 fold landed Option B (compound reading) from V3 CH2 §5 rec 1
verbatim. The disjointness is named directly; the executable
verification of both halves is inline-cited; the 42 + 14 = 56
arithmetic is explicit; the lint glob's brace-expansion mechanism
is named as the disjoint-set unifier. N-V3-CH2-1 CLOSED.

### §3.2 Lock 14 baseline re-verification — A3 unchanged at V4 HEAD

V4 commit `f8e279877` stat confirms A3 (`sk-v14-audit-overfit-lock14-
scan.md`) carries no V4 fold:

```
$ git show f8e279877 --stat
 .../audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md | 13 +++++++++----
 1 file changed, 9 insertions(+), 4 deletions(-)
```

Only one file touched at V4. A3 ledger holds at 30 at V4 HEAD:

```
$ grep -cE "^\| (C[0-9]+|H[0-9]+|M[0-9]+|L[0-9]+) \|" \
    restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-lock14-scan.md
30
```

Per-severity distribution preserved from V1+V2+V3: 11 CRIT + 6 HIGH
+ 5 MED + 8 LOW = 30. H3→L8 reclassification carries through. The
production call site `passes/src/lib.rs:478` (`finalize_rule(&grammar.
name, …)`) remains the grammar-generic threading anchor; the test-
fixture `decision_csp.rs:230-240` (under `#[cfg(test)] mod tests`)
remains the L8-reclassified violation surface.

### §3.3 Primitive-crate generic posture re-check at V4 HEAD

Re-verification at V4 HEAD == `f8e279877`:

```
$ grep -rn -E '\b(Json|Css|Sheets|JsonValue|CssValue|SheetsValue|CssL4|CssPretty)\b' \
    skinny/crates/bbnf-regex/src/ \
    skinny/crates/bbnf-simd/src/ \
    skinny/crates/simd-scan/src/ \
    skinny/crates/parse-that-regex/src/ \
    skinny/crates/ir/src/
[no output]
```

Still clean across all 5 primitive crates. The V4 micro-redispatch
was a single-file SYNTHESIS prose edit (commit stat confirms 1 file
modified; zero source touched). No SK-V14 commit through V4 has
introduced grammar-name leaks into the primitive layer.

### §3.4 F-V4-SYNTHESIS-2 fixture-scanner count cross-surface coherence

V4 fold corrects "3 scanners are fixture lookups" → "4 scanners are
fixture lookups" at SYNTHESIS §3.1:347 C-3 row prose cell — the
final residual cell from the F-V2-SYNTHESIS-4 + F-V3-SYNTHESIS-3
arithmetic-correction sweep. Executable verification of the "4"
count:

```
$ grep -rln 'CANONICAL_FIXTURE\|CAPTURED_W2_INPUT' \
    skinny/crates/codegen/src/
skinny/crates/codegen/src/css_l4_nested_layout_templates/generated.rs
skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_templates/generated.rs
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/generated.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/generated.rs
```

4 templates confirmed (3 use CANONICAL_FIXTURE + 1 uses CAPTURED_W2_
INPUT per V2 CH2 §3.2 decomposition; identical to V3 §3.4 evidence,
no change at V4 HEAD). Cross-surface count consistency check at V4
HEAD:

```
$ grep -nE "(scanners are fixture|of 7 CSS scanners|Four of the seven|fixture lookups dressed)" \
    restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md
81: A4 | 4 | 12 | … (4 of 7 CSS scanners short-circuit …); …
122: 5. **A4 NEW-2 (CRIT, 4 findings: rows 3, 4, 5, 6 in A4 §2)** — Four
347: | **C-3** = R4 + R5 | … A4 findings 2 + 3 + 4 + 5 + 6 + 7 (no regen-css subcommand, 4 scanners are fixture lookups, 14/15 .bbnf orphan); …
```

All three SYNTHESIS surfaces now agree on the "4" count (§1.1:81
in-table NEW-2 cell, §1.2:122 in-prose decomposition, §3.1:347 C-3
row prose cell). The fixture-lookup-table-dressed-as-parser violation
class is grammar-neutral (would catch any future grammar attempting
the same overfit); cross-surface count coherence preserves the
GENERALITY scope reading.

### §3.5 Total fake-`@generated` surface re-verification at V4 HEAD

```
$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ skinny/crates/codegen/src/ | wc -l
56

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ | wc -l
42

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/ | wc -l
14
```

Aggregate: 42 runtime-side + 14 codegen-side = 56 files (zero
overlap, sibling path roots). The lint glob `{runtime/src/grammars,
codegen/src}/**/*.rs` brace-expansion binds both halves of the
round-tripping vector. V4 SYNTHESIS §2.4:311-318 now describes this
disjoint binding correctly. GENERALITY scope preserved end-to-end.

### §3.6 Per-grammar census re-verification at V4 HEAD

```
$ cargo metadata --format-version 1 --no-deps 2>/dev/null | \
    jq -r '.metadata.bbnf.grammars[].ident' 2>/dev/null
bbnf
json
css_l4
css_pretty
google_sheets
ebnf
bnf
csv
math
```

9 grammars confirmed at V4 HEAD (identical to V1+V2+V3 enumeration).
PRUNE-4 sub-wave count = 9 across A3 + A6 + SYNTHESIS §2.3 + §3.3.
No V4 fold touched the census or the co-derivation note; 9-grammar
census preserved.

## §4 — Fresh-finding scan (V4 cycle)

V4 axis-redispatch surface is minimal: 1 file modified, +9 / -4
LOC, 2 atomic prose edits inside `SYNTHESIS-AUDIT-OVERFIT.md`. The
fresh-finding scan focuses on (a) the V4-edited prose surface itself
and (b) the broader Lock-14 / primitive-crate / per-grammar
generality posture for any drift since V3.

### §4.1 V4-edited prose surface — no new CH2 findings

The V4 fold at SYNTHESIS §2.4:311-318 (F-V4-SYNTHESIS-1) replaces
the V3 "42 files … including 14 codegen-side" inclusion-relation
phrasing with the explicit disjoint-set reading "56 files (42
runtime-side mirror + 14 codegen-side (8 providers+templates + 6
ancillary)) — disjoint sets per the lint glob brace-expansion".
The replacement carries:

- explicit 42 + 14 = 56 arithmetic (rather than V3's implicit
  "including" relation);
- named disjointness ("disjoint sets per the lint glob brace-
  expansion");
- the brace-expansion mechanism named directly (`{runtime/src/
  grammars,codegen/src}/**/*.rs`);
- both half-counts re-executable-verified inline (`runtime/src/
  grammars/ | wc -l = 42` and `codegen/src/ | wc -l = 14`);
- explicit "summing to the 56 total with zero overlap (the two
  path roots are siblings)" closing clause.

CH2 GENERALITY scope: the lint glob's grammar-neutral binding to
both runtime + codegen sides is preserved as load-bearing. The
prose now correctly describes the disjoint-set arithmetic. No new
CH2 finding surfaces on the V4 prose edit.

The V4 fold at SYNTHESIS §3.1:347 (F-V4-SYNTHESIS-2) corrects the
C-3 row prose cell scanner count from "3" → "4". The fixture-lookup
violation class is grammar-neutral; the count correction aligns
§3.1:347 with the V2+V3 fold sweep that already corrected §1.1:81,
§1.2:122, §0.1, §5.1, and `generator-truth.md:56`. No new CH2
finding surfaces on the V4 prose edit.

### §4.2 §2.4 closing sentence editorial cohesion (sub-fatal NOTE)

The §2.4 closing sentence at lines 319-321 reads:

> "The lint glob `codegen/src/**/*.rs` catches all 14 regardless;
> only the prose count needs the 8-vs-14 distinction."

This sentence was originally added during F-V3-SYNTHESIS-2 to address
the V2 §4.1 NOTE on the 8 → 14 prose precision. After F-V4-SYNTHESIS-1
replaced the V3 "42 … including 14" phrasing with the explicit
disjoint-set "56 (42 + 14)" reading, the "8-vs-14 distinction"
reference reads slightly disjointly — the V4 prose above it already
spells out "14 codegen-side (8 providers+templates + 6 ancillary)"
explicitly, so the closing clause's "only the prose count needs the
8-vs-14 distinction" carries forward a distinction that the V4 prose
no longer leaves to interpretation.

This is a **cohesion-only editorial wrinkle**, not a CH2 GENERALITY
finding. The semantic content of the sentence is still correct (the
codegen-side glob `codegen/src/**/*.rs` does catch all 14 regardless
of how the prose enumerates the 8+6 decomposition); the only issue
is that the framing reads as a residual from a no-longer-applicable
V2 → V3 precision repair. The lint mechanism's grammar-neutral
binding to both halves is unaffected.

**Severity:** sub-CH2-fatal (cohesion editorial residue from the
V2 → V3 → V4 fold chain; the V4 fold itself is correct). Not a NEW
orphan REVISE under the strict §3Z reading because the V4 prose
remains arithmetically + mechanically correct; the issue is purely
prose-flow aesthetics, not a substantive defect.

**Optional V5 confirming-pass micro-fold (non-blocking):** §2.4:319-
321 could be tightened to either drop the residual "8-vs-14
distinction" clause OR rewrite it as a forward-looking statement
about future glob-narrowing decisions. The V4 prose stands as
correct; this is purely a polish item that does not gate §3Z LOCK
closure.

### §4.3 Lock 14 broader scan re-verification at V4 HEAD

Beyond the A3 ledger re-check at §3.2, the V4 fresh-finding scan
re-confirms the broader Lock-14 posture invariants:

- **Per-grammar dispatch generality:** the production call site
  `skinny/crates/passes/src/lib.rs:478` (`finalize_rule(&grammar.name,
  …)`) remains the grammar-generic threading anchor at V4 HEAD; A3
  C1..C11 cluster carries forward as PRUNE-3 + PRUNE-4 inputs.
- **Test-fixture posture:** `decision_csp.rs:230-240` under
  `#[cfg(test)] mod tests` remains the L8-reclassified violation
  surface (per V2 F-V2-A3-1 reclassification).
- **9-grammar census:** preserved at V4 HEAD per §3.6 above.
- **Pattern H 67-file census (A6 CRIT-1):** preserved at V4 HEAD
  per V3 CH2 §4.2 + V4 commit stat (A6 file unchanged).
- **48-file Pattern H sub-census (A6 CRIT-2):** preserved at V4
  HEAD per V4 commit stat (A6 file unchanged).
- **Fake-`@generated` 56-file surface:** preserved at V4 HEAD per
  §3.5 above (42 + 14 disjoint).
- **Primitive-crate generic posture:** preserved at V4 HEAD per
  §3.3 above (zero leaks across 5 crates).

No new CH2 GENERALITY findings surface under the V4 fresh-finding
scan.

## §5 — V5 confirming-pass forecast

V4 closes the V3 N-V3-CH2-1 inclusion-relation defect cleanly via
F-V4-SYNTHESIS-1 (+ F-V4-SYNTHESIS-2 closing the CH7 cross-flag);
V4 CH2 disposition is 100 % ACCEPT (8 ACCEPT + 0 ACCEPT-with-NOTE
/ 8 = 100 %); the §3Z two-consecutive-cycle convergence chain
expects V5 confirming pass at 100 % ACCEPT (V3 100 % + V4 100 %
+ V5 100 % → ≥ 95 % twice with the strict-orphan-REVISE-blocking
reading per V3 CONSOLIDATED §3.1 option 1 → G-S-P0-CONVERGED → S-P1
dispatch per the SK-V14 ORCHESTRATOR-PROMPT THE SK LOOP).

If S-P0 V5 dispatches as the confirming-pass closer, the optional
§4.2 cohesion polish at SYNTHESIS §2.4:319-321 could fold; it is
non-blocking for §3Z LOCK and does not perturb the 74-finding
aggregate, the C-1..C-5 envelopes, the 9-grammar census, the 56-
file fake-`@generated` coverage, or the per-axis disposition
matrix. CH2 V5 disposition expected at 100 % under either fold
landing or carry-forward.

## §6 — Disposition

**CH2 V4 VERDICT: ACCEPT.** F-V4-SYNTHESIS-1 closes V3 N-V3-CH2-1
inclusion-relation defect at SYNTHESIS §2.4:311-318 cleanly: the
42 + 14 = 56 disjoint-set arithmetic is now explicit; the lint
glob's brace-expansion mechanism is named directly; both half-counts
are inline-cited with executable verification; the "summing to the
56 total with zero overlap" closing clause names the disjointness
unambiguously. The V3 single ACCEPT-with-NOTE upgrades to ACCEPT
under V4.

F-V4-SYNTHESIS-2 closes V3 CH7 §1.5 cross-flag at SYNTHESIS §3.1:347
C-3 row prose cell ("3 → 4 scanners are fixture lookups"). The
fixture-lookup violation class is grammar-neutral and now
arithmetically consistent across all three SYNTHESIS surfaces
(§1.1:81 + §1.2:122 + §3.1:347). The cross-surface count coherence
preserves the GENERALITY scope reading.

Lock 14 baseline holds — A3 ledger preserved at 30 violations
verbatim (V4 commit stat confirms A3 file unchanged); per-grammar
census (9 grammars) preserved at V4 HEAD; primitive-crate generic
posture preserved (zero name leaks across 5 crates); Pattern H 67-
and 48-file censuses preserved (V4 commit stat confirms A6 file
unchanged); fake-`@generated` 56-file surface preserved with the
disjoint-set arithmetic now explicit in the prose.

One sub-CH2-fatal editorial wrinkle surfaces at §2.4:319-321 — the
closing "8-vs-14 distinction" clause reads as a residual from the
V2 → V3 precision repair after F-V4-SYNTHESIS-1 rewrote the
preceding prose. This is cohesion-only polish, not a substantive
defect; the lint mechanism's grammar-neutral binding is unaffected;
the V4 prose remains arithmetically + mechanically correct. Non-
blocking for §3Z LOCK; optional V5 micro-fold per §4.2.

V4 CH2 ACCEPT-rate: **8/8 = 100 %** (8 ACCEPT + 0 ACCEPT-with-NOTE
+ 0 REVISE + 0 REJECT). §3Z V4 ≥ 95 % satisfied for CH2; V3 + V4
both at 100 % satisfies the two-consecutive-cycle ≥ 95 % criterion
for CH2 specifically (G-S-P0-CONVERGED aggregate gating depends on
the other six lenses' V4 disposition + the V5 confirming pass per
V3 CONSOLIDATED §3.1 strict-reading institution).

---

**Authored:** 2026-05-23 (SK-V14 S-P0 CHALLENGE V4 lens CH2 —
GENERALITY).
**Status:** WRITE-ONLY; aggregator commits all 8 V4 hardening files
atomically with `docs(sk-v14-audit-overfit-hardening-V4): challenge
V4 + consolidated`.
**Authority:** `restart/prompts/ORCHESTRATOR.md §3W` CH2 row;
V3 CH2 base at `restart/skinny/tranches/sk-v14/audit-overfit/
hardening/V3/CH2.md` §5 V4 fold recommendation; V1 CHALLENGE-CONTEXT
`restart/skinny/tranches/sk-v14/audit-overfit/hardening/V1/CHALLENGE-
CONTEXT.md §3` per-lens focus row; V3 CONSOLIDATED §2.1 F-V4-
SYNTHESIS-1 prescription; V4 micro-redispatch commit `f8e279877`.
