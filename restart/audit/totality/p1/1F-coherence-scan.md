---
agent: 1F
pass: T-P1-excavation
cycle: V5-SKV18-totality
generated_at: 2026-06-01T00:00:00Z
spec_surfaces_audited:
  - restart/ARCHITECTURE.md
  - restart/MASTER-PLAN.md
  - restart/locks/LOCKS.md
  - restart/HANDOFF.md
  - restart/skinny/tranches/sk-v18/SPEC.md
  - restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md
  - restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md
  - restart/skinny/tranches/sk-v18/research/p1/SYNTHESIS-PROFILE.md
files_audited_count: 41
live_truth_method: "sed/Read path:line over the four V1 governance surfaces + the three SK-V18 plan docs; rg + md5 + find + wc -l over skinny/crates and crates/core/src; no cargo, no build mutation; every divergence row re-grounded at path:line at the dirty tree HEAD this pass"
prior_cycle_dispositions_folded:
  accepted:
    - SKV17-COH17-004-x86-CollapsedStage-aarch64-drift (carried; SK-V18 P1 deletes x86 in skinny)
    - SKV15-PATTERN-H-67 (re-verified 67 per-grammar in crates/core, 71 with tape/)
    - SKV15-LOCK14-ROOT-LEAKS (re-verified; relocated to ir/registry/strategy.rs table)
  superseded:
    - SKV15-V4-COH-001..016 (SK-V15 PRUNE/REBUILD coherence packet; this V5 re-roots on SK-V18 generalization)
    - 1F-anti-pattern.md / 1F-past-corpora.md SK-V15-V2 superseded stubs (rewritten this pass)
  first_cycle_additions:
    - COH18-001-HANDOFF-scope-drift (HANDOFF SK-V18 = totality-adopt, certified SK-V18 = skinny generalization)
    - COH18-002-ARCH-sheets-by-construction-drift
    - COH18-003-spec-claims-unforked-generator-impl-forks
    - COH18-004-MASTER-F.W5-nine-seed-template-vs-fork
    - COH18-005-relocated-seam-totality-analog (ir/registry/strategy.rs)
    - COH18-006-css_types.rs-Lock14-named-mess-still-live
    - COH18-007-Pattern-H-71-vs-67-baseline
    - COH18-008-LOCKS-620-unforked-generator-claim-vs-skinny-fork
    - COH18-009-x86-skinny-vs-totality-asymmetry
    - COH18-010-sheets_witness-stub-coheres
divergence_count:
  spec_claims_implemented: 1
  spec_claims_unimplemented: 9
  impl_exceeds_spec: 0
  unknown: 1
  plan_coherent: 1
  spec_defect: 1
  split_implemented_json_directional_css: 1
  enumeration_note: "Re-keyed to the 14-row COH18-001..014 Spec-Claim table per CH1-V4-F8 (the prior 3/13/2 did not reconcile to the table). Per-row bucket map: unimplemented(9) = COH18-001,002,003,004,005,006,007,008,009; implemented(1) = COH18-010; plan_coherent(1) = COH18-011 (executed-surface absent per CH6-V3-F3, NOT counted implemented); spec_defect(1) = COH18-012 (spec-defect-on-:1643); split_implemented_json_directional_css(1) = COH18-013 (JSON 51/51 implemented / CSS directional-not-re-locked); unknown(1) = COH18-014. Sum = 9+1+1+1+1+1 = 14 = the 14 COH18 rows. (COH18-015 is a Divergences-Catalogued row, not a Spec-Claim row, so it is not in this count.)"
locks_amendment_candidates: 0
---

## Executive Summary

The certified SK-V18 plan (GENERALIZATION cycle: un-fork JSON+CSS into ONE
grammar-driven generator emitting JSON+CSS+Sheets from `.bbnf`, on the SKINNY
tree, aarch64-only, `crates/core/` deferred to SK-V19) does NOT cohere with the
V1 governance surfaces — it surfaces material spec-drift the totality fold must
reconcile. The sharpest drift: `restart/HANDOFF.md:17-19` defines SK-V18 as
"adopts the SKINNY-proven unified-tape / lazy-`ValueRef` / shared-NEON model
into the totality `crates/core/` tree" — a DIFFERENT SK-V18 than the certified
SPEC (`sk-v18/SPEC.md:19-21`). `restart/ARCHITECTURE.md:2003-2005` predicts a
weaker "Sheets by-construction under SK-V18" posture than the certified
EXERCISED Sheets negative control (PROVE wave, `sk-v18/SPEC.md:143-152`). The
spec ALREADY claims an un-forked generator (`MASTER-PLAN.md:519` F.W5 "nine seed
grammars build through new template"; `LOCKS.md:349`/`:620` "single
grammar-agnostic generator"), but BOTH impl trees fork: the skinny generator on
`RuntimeEmitterKind{CompiledLowering,RequestFacts}`
(`grammar_provider.rs:40-42`), the totality `ir` crate on a grammar-named lookup
table (`registry/strategy.rs:137-155`, the relocated-seam analog). The CSS const
courier (`runtime_generator.rs:701`), 7 byte-identical css_l4 replicas (md5
`b654562c`), phantom `<G>` (`tape/mod.rs:175`), x86 surface, and `css_types.rs`
all live. Pattern H is 67 per-grammar (71 with `tape/`). The skinny→totality fork
asymmetry IS the SK-V18→SK-V19 thesis.

## Spec-Claim ↔ Implementation Table

| ID | Spec / corpus claim (path:line) | Live / counter-surface evidence (path:line) | Verdict | Class | Note |
|---|---|---|---|---|---|
| COH18-001 | `restart/HANDOFF.md:17-19`: "The next IMPLEMENTATION tranche is **SK-V18**: it adopts the SKINNY-proven unified-tape / lazy-`ValueRef` / shared-NEON model into the totality `crates/core/` tree, per the five LOCKED fold designs." | Certified SK-V18 is the GENERALIZATION cycle: un-fork JSON+CSS into ONE generator emitting JSON+CSS+Sheets, on the SKINNY tree (`restart/skinny/tranches/sk-v18/SPEC.md:19-21`, `:58-61` "each generator/gate verifiable by grepping `skinny/crates/`, NOT `crates/core/` (the TOTALITY tree is the SK-V19 adoption target)"). | spec-surface drift | unimplemented | HANDOFF defines a DIFFERENT SK-V18 than what was certified — totality-adopt vs skinny-generalize. The totality fold MUST re-author HANDOFF's SK-V18 paragraph; the `crates/core/` adoption is SK-V19. |
| COH18-002 | `restart/ARCHITECTURE.md:2003-2005`: "Sheets/BBNF-self are by-construction under SK-V18 (the `sheets_witness` 24-LOC stub cannot serve as exercise), NOT claimed fleet-wide." | Certified SK-V18 makes Sheets an EXERCISED negative control through the un-forked generator (`sk-v18/SPEC.md:143-152` "Sheets proves the generalization is REAL"; PROVE wave entry-gates G3∧G4, `SYNTHESIS-RESEARCH.md:200-205`). | spec-surface drift | unimplemented | ARCH predicts a weaker SK-V18 Sheets posture (by-construction) than certified (by-exercise via generator). The 24-LOC `sheets_witness` stub is what SK-V18 PROVE retires. |
| COH18-003 | `restart/LOCKS.md:349`: "Per-grammar runtime modules … are emitted from a single grammar-agnostic generator template … hand-written per-grammar runtime files are forbidden." `:620`: "re-emitted by a single grammar-agnostic generator (`restart/locks/LOCKS.md:349`) that resolves the layout once at codegen". | Skinny generator FORKS on `RuntimeEmitterKind{CompiledLowering,RequestFacts}` (`skinny/crates/codegen/src/grammar_provider.rs:40-42`); `runtime_generator.rs:17` matches `CompiledLowering`, `:25` `RequestFacts`; CSS is a const courier `normalize(CSS_GENERATED_RS)` (`runtime_generator.rs:91`, `:701`). | spec-to-live drift | unimplemented | The spec ALREADY claims the un-forked generator the impl forks — this IS the SK-V18 generalization thesis. The lock text is correct; the skinny impl violates it. |
| COH18-004 | `restart/MASTER-PLAN.md:519`: "F.W5 \| Current nine grammar regeneration. \| Nine seed grammars build through new template." `:196` F gate "Rust lowerer emits equal generated runtime for seed grammars". | Skinny: 2 forked emitter arms + CSS const courier + 7 byte-identical replicas. Totality: `crates/core/src/grammar/generated/{bbnf,bnf,css_l4,css_pretty,csv,ebnf,google_sheets,json,math}.rs` exist (9) but `ir/registry/strategy.rs:137-155` carries a grammar-named ident lookup table feeding the template. | spec-to-live drift | unimplemented | "Nine seed grammars build through new template" is the spec's un-forked claim; the skinny benched tree does NOT realize it (the SK-V18 target). |
| COH18-005 | `restart/LOCKS.md:349`: generic crate `bbnf-ir` carries "ZERO grammar-named modules; ZERO grammar-specific types in their public APIs". The relocated-seam risk: a per-grammar branch moved into a neutral-identifier data table (`sk-v18/SYNTHESIS-RESEARCH.md:272-279` §5-risk-1). | `crates/ir/src/registry/strategy.rs:137`: `PRODUCTION_MANIFEST_TABLE` carries **NINE** grammar-named `idents` rows: `["JsonParser","JsonGrammar"]` (`:137`), `["GoogleSheetsParser","GoogleSheetsGrammar"]` (`:143`), `["CssL4Parser"]` (`:149`), `["BbnfBootstrap","BbnfParser"]` (`:155`), `["CsvParser","CsvGrammar"]` (`:161`), `["MathParser","MathGrammar"]` (`:167`), `["BnfParser","BnfGrammar"]` (`:173`), `["EbnfParser","EbnfGrammar"]` (`:179`), `["CssPrettyParser","CssPrettyGrammar"]` (`:185`) — a 9-grammar-named ident table in the generic `ir` crate. | spec-to-live drift | unimplemented | The totality-tree relocated-seam analog of the skinny `RuntimeTarget` columns SK-V18 R16 collapses. The leak is 9-grammar-WIDE (all `:137,:143,:149,:155,:161,:167,:173,:179,:185`): the strict 4-name leak regex `JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser` catches only **4** of the 9 idents rows (`:137,:143,:149,:155` — Json/GoogleSheets/CssL4/Bbnf); the other **5** idents rows (`:161,:167,:173,:179,:185` — Csv/Math/Bnf/Ebnf/CssPretty) escape it (untransposed per CH2-V3-008, replacing the carried CH2-V2-009 "catches only 5 ident sites" wording). R16 structural row-collapse must collapse ALL 9. The names ARE live-consumed by the generator: `for_grammar_with_manifest(grammar_ident, registry, PRODUCTION_MANIFEST_TABLE)` at `crates/ir/src/registry/strategy.rs:216` (consumer anchor added per CH1-V3-F11; whether that consumer is the generator path or only `regen --check` is itself routed to `U-COH18-001`). Grammar names live in a neutral-identifier data table the arm-grep cannot see; only structural row-collapse catches it. |
| COH18-006 | `restart/LOCKS.md:349`: "The current overfitting mess — … `crates/core/src/css_types.rs` … is the failure mode this lock prevents from recurring." | `crates/core/src/css_types.rs` STILL LIVE (66 LOC, `:1` "Host shims for the CSS L4 grammar's `-> parse_hex_color(...)` map") — in `crates/core/src/`, NOT a `crates/<grammar>/` declaration crate. | spec-to-live drift | unimplemented | The lock NAMES this file verbatim as the mess; it persists in the totality tree. Lock 14 (c) admits ONLY "a per-grammar declaration crate (`crates/<grammar>/`)" — a SEPARATE crate; `css_types.rs` lives in the generic core crate, so Lock 14 (c) does NOT apply (CH2-007). The admissibility note is scoped strictly to a relocate-to-`crates/css/`-declaration-crate condition (SK-V19 receiver). |
| COH18-007 | `restart/LOCKS.md:402-419`: Lock 14 v+1 Pattern H baseline = 67 via `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l`; "Pattern H 67-file recurrence is the category-scale failure". `restart/ARCHITECTURE.md:1932` "**67 hand-written files across 9 grammar dirs**". | Live `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l` = **71**; per-grammar-only (excluding the generic `tape/` dir) = **67**; the +4 are `tape/{mod,cursor,arena,record}.rs` (generic substrate, NO `@generated`). 67/71 carry `@generated`. | spec-to-live drift | unimplemented | The LOCKS-bound command now returns 71, not 67 — the `tape/` substrate dir was added after the baseline. The per-grammar Pattern H is still 67 (no collapse). The baseline command needs a `tape/` exclusion or a +4 substrate-trace per Lock 14's "+N MUST trace" rule. |
| COH18-008 | `restart/LOCKS.md:620`: the SK-V17 fold clause cites `skinny/crates/runtime/src/tape/mod.rs:175` `ValueRef<…G:EventGrammar>` as "the ONE materialization plane, re-emitted by a single grammar-agnostic generator". | `skinny/.../tape/mod.rs:175` confirmed (`ValueRef<'doc,'input,K=AnyKind,G:EventGrammar=AnyGrammar>`, `_grammar:PhantomData` `:179`); but the `<G>` axis has ZERO non-test production consumers (`sk-v18/SYNTHESIS-RESEARCH.md:26-30`) and SK-V18 G4 DELETES it (`sk-v18/SPEC.md:99-102`). | spec-to-live drift | unimplemented | LOCKS:620 calls `G:EventGrammar` "the generality vehicle"; the certified SK-V18 resolves the phantom by DELETE. The lock leans on a phantom the plan retires — a future LOCKS reconcile, NOT a T-P1 amendment. |
| COH18-009 | `restart/ARCHITECTURE.md:1088`/`:1186`/`:1206` (per SKV17-COH17-004) bind `CollapsedStage` to x86/AVX-512 asm; SK-V18 is aarch64-only, x86 is a PRUNE target (`sk-v18/SPEC.md:42`, `:130-134`). | x86 LIVE in skinny: `skinny/crates/bbnf-simd/{src/x86_64/, ext/x86/bbnf.asm, ext/x86/x86util.asm}`. NO x86 in `crates/core/` (`find crates/core -type d -name x86_64` = empty). | spec-to-live drift / asymmetry | unimplemented | The benched skinny tree carries x86 (SK-V18 P1 deletes it); the totality tree is already x86-free. The ARCH x86-CollapsedStage canon contradicts the aarch64-only SK-V18 plane (carried SKV17 finding). |
| COH18-010 | `restart/ARCHITECTURE.md:2004`: "the `sheets_witness` 24-LOC stub cannot serve as exercise". | `skinny/crates/runtime/src/grammars/sheets_witness/event_grammar_witness.rs` = 24 LOC; `mod.rs` = 1 LOC. EXACT match. | implemented (coherent) | implemented | The 24-LOC figure is current and coherent; ARCH and the live tree agree the stub is non-exercise. SK-V18 PROVE retires it. |
| COH18-011 | `restart/ARCHITECTURE.md:2007-2009`: "a CSS-only generator that never re-emits JSON FAILS the single-plane claim and is REDRESS" — the JSON `value_from_ref` byte-equal re-emit pre-gate. | SK-V18 G1 binds exactly this: JSON projection byte-equivalence against the `json_templates/` oracle BEFORE oracle deletion (`sk-v18/SPEC.md` G1; `SYNTHESIS-RESEARCH.md:160-166`); `skinny/crates/codegen/src/json_templates/` exists. | coherent (plan-bound) | plan-coherent | The ARCH single-plane pre-gate COHERES with SK-V18 G1's diff-control gate. No drift; the certified plan honors this lock. Verdict token is `coherent (plan-bound)`, NOT `implemented` (per CH6-V3-F3): the only live surface is that `json_templates/` exists — the G1 byte-equivalence gate is a PLANNED wave, NOT executed; reserve `implemented` for rows with an executed live surface (cf. COH18-010's live 24/1-LOC stub). |
| COH18-012 | `restart/ARCHITECTURE.md:1643` (§12 Backend-impl-table row): Lock 14 leak scan `rg -nE 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/codegen/src/` returns ZERO. | The `:1643` command scopes ONLY `crates/codegen/src/` (clean), but the grammar-named idents live in `crates/ir/src/registry/strategy.rs:137-185` — NINE `idents` rows (`:137,:143,:149,:155,:161,:167,:173,:179,:185`), a 9-grammar-wide leak in a DIFFERENT generic crate Lock 14:349 also names (untransposed per CH2-V3-008 — the narrow 4-name leak regex `JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser` catches only **4** of the 9 idents rows; the breadth is all 9 totality grammars). Live: `rg 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/ir/src/` = 11 sites (**4** are `idents` rows — `:137,:143,:149,:155`, Json/GoogleSheets/CssL4/Bbnf; the other **5** grammar names — Csv/Math/Bnf/Ebnf/CssPretty at `:161,:167,:173,:179,:185` — escape that narrow regex entirely; the remaining 7 narrow-regex hits are doc-comment/sibling lines). The 9 idents are live-consumed via `for_grammar_with_manifest(grammar_ident, registry, PRODUCTION_MANIFEST_TABLE)` at `crates/ir/src/registry/strategy.rs:216` (consumer anchor per CH1-V3-F11; generator-vs-`regen --check` routed to `U-COH18-001`). | gate-scope gap (§12 `:1643` only) + LOCKS:349 gate RED | spec-defect-on-`:1643` | CORRECTION (CH2-003): the §9 gate command `ARCHITECTURE.md:2215` and `LOCKS.md:349` BOTH scan `crates/{ir,parse,codegen,runtime,...}/src/` and reach `strategy.rs` — so the narrow command is the §12 row `:1643` ALONE, NOT the totality leak scan. SHARPER (CH2-V4-007): `LOCKS.md:349` ASSERTS its verification command "returns ZERO", but the live command returns **13 sites** (11 `crates/ir/src/` + 2 `crates/analysis/src/` — `rg 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/ir/src/ crates/analysis/src/` = 13), so Lock 14's OWN verification gate is FALSIFIED / RED — this is distinct from "the wide command benignly catches a leak." The real gap is one §12 sub-command narrower than "gate too narrow", AND the LOCKS:349 self-gate is red. Reclassified OFF impl-exceeds-spec. |
| COH18-013 | `restart/MASTER-PLAN.md:923`: "refuted CSS L4 admission as a generalization claim: JSON remains honest". | JSON half: 51/51 strict cold HONEST — bench-row-backed (`RESULTS.md:5-25` measured rows, twitter 8349>4913 etc., per-iter PASS). CSS half: canonical cold N≥200 1.9-3.3× is a synthesis-doc ASSERTION (`SYNTHESIS-AUDIT-OVERFIT.md:36` prose, NOT a bench table; `:138`); the residual is hand-written/forked/replicated, NOT a fake admit. | implemented (coherent) [JSON 51/51] / directional, not re-locked [CSS] | implemented (JSON) / directional (CSS) | SPLIT per CH6-F1 (the V1 CH6-F3 fold named this row by ID but missed it). JSON 51/51 half COHERES with the MASTER refutation — bench-backed, implemented. CSS half: INLINE DIRECTIONAL CAVEAT (per 1D U-4 `1D:199-203,228-232`) — the 1.9–3.3× ratios (bootstrap 2.190, tailwind 3.375, material 1.658, animate 2.101, `SPEC.md:113-118`) ran under loadavg 4.35 and are DIRECTIONAL, NOT re-locked; the H1 `css_canon_bench` re-lock gate (NOT `SYNTHESIS-AUDIT-OVERFIT.md:36`) is the falsifier. SK-V18 UPGRADES the MASTER refutation: the >SOTA is now measurement-valid, the overfit is implementation (forks/replicas) not measurement. Do NOT re-derive CSS as "audit-demoted/contrived" (SK-V15) AND do NOT carry the un-caveated "MEASUREMENT-VALID" coherent-closure word on the CSS ratio until the bench row is re-locked. |
| COH18-014 | `restart/MASTER-PLAN.md:307` A.W2 "Metadata validation accepts current nine grammars"; `:519` "Nine seed grammars". | 9 grammar sources exist under `grammar/` (json, css/l4/*, google-sheets, bbnf, bnf, ebnf, css/pretty, misc/csv, misc/math) + 9 generated at `crates/core/src/grammar/generated/`. Skinny benches only 3 (json + css + sheets-witness). | partial | unknown | The 9-grammar census holds in the totality tree; the skinny tree is a 3-grammar (JSON+CSS+Sheets) witness. The SK-V18→SK-V19 boundary: SK-V18 proves the un-fork on 3, SK-V19 adopts to 9. UNKNOWN: whether the totality 9 already un-fork (see Open Questions). |

## Divergences Catalogued

| ID | Divergence | Evidence | LOC / risk |
|---|---|---|---|
| COH18-001 | HANDOFF defines SK-V18 as totality-adopt; certified SK-V18 is skinny-generalize. The single most material drift. | `restart/HANDOFF.md:17-19` vs `restart/skinny/tranches/sk-v18/SPEC.md:19-21`,`:58-61` | 40-120 LOC docs; CRITICAL routing risk — downstream waves route from a stale SK-V18 definition |
| COH18-002 | ARCH predicts Sheets by-construction under SK-V18; certified SK-V18 exercises Sheets through the generator (PROVE). | `restart/ARCHITECTURE.md:2003-2005` vs `restart/skinny/tranches/sk-v18/SPEC.md:143-152` | 30-80 LOC docs; high |
| COH18-003 | Spec claims a un-forked single grammar-agnostic generator; skinny impl forks on `RuntimeEmitterKind` + CSS const courier. | `restart/LOCKS.md:349`,`:620` vs `skinny/crates/codegen/src/grammar_provider.rs:40-42`, `runtime_generator.rs:17`,`:25`,`:91`,`:701` | ≈910 LOC CSS courier + fork arms; CRITICAL — the SK-V18 G1/G2/G3 core |
| COH18-004 | F.W5 "nine seed grammars build through new template" un-realized on the benched skinny tree. | `restart/MASTER-PLAN.md:196`,`:519` vs forked skinny generator | 300-900 LOC; critical |
| COH18-005 | Relocated-seam analog in the totality `ir` crate: grammar-named ident lookup table. | `crates/ir/src/registry/strategy.rs:137`,`:143`,`:149`,`:155` | 60-200 LOC; high (the SK-V19 R16 receiver) |
| COH18-006 | `css_types.rs` — the file Lock 14:349 names verbatim as the mess — still live in `crates/core/src/` (NOT a `crates/<grammar>/` declaration crate; Lock 14 (c) does NOT apply). | `crates/core/src/css_types.rs:1` (66 LOC) | 66 LOC; medium (the lock-NAMED mess; admissible ONLY if relocated to a `crates/css/` declaration crate) |
| COH18-007 | Pattern H census command returns 71 (vs LOCKS-asserted 67); per-grammar 67 unchanged; +4 = generic `tape/`. | `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs'` = 71 vs `restart/LOCKS.md:408` | baseline-command discipline; medium |
| COH18-008 | LOCKS:620 leans on `G:EventGrammar` phantom as "generality vehicle"; SK-V18 G4 deletes it. | `restart/LOCKS.md:620` vs `skinny/.../tape/mod.rs:175`,`:179` + `sk-v18/SPEC.md:99-102` | 1 line LOCKS reconcile (SK-V19); medium |
| COH18-009 | x86 live in skinny (SK-V18 P1 deletes); x86-free in totality; ARCH CollapsedStage x86-pinned vs aarch64-only plane. | `skinny/crates/bbnf-simd/src/x86_64/` + `ext/x86/` vs `restart/ARCHITECTURE.md:1186`,`:1206` | ≈4500 LOC skinny delete (P1); carried SKV17-COH17-004 |
| COH18-012 | The §12 Backend-impl-table leak command (`ARCHITECTURE.md:1643`, `crates/codegen/src/`-scoped) misses the real `ir/registry/strategy.rs` leak; the §9 gate `:2215` and `LOCKS.md:349` (`crates/{ir,...}/src/`-scoped) DO catch it (11 live sites). Census COMPLETENESS (CH2-V3-010): the `analysis` generic crate (also Lock-14-listed) additionally carries `BbnfBootstrap`-named doc-comment occurrences at `crates/analysis/src/state/ast_utils/mod.rs:4` (`BbnfBootstrapNodeView`) + `:11` (`BbnfBootstrapRuleKind`), caught by the same Lock 14 verification regex yet previously uncited — these are doc-comment (not code) hits, of the same code-vs-doc class as the `ir` `crates/ir/src/passes/recognizers/grammar_facts.rs:799` / `crates/ir/src/passes/recognizers/shape_dispatch/scalar.rs:17` siblings counted toward the 11 (full `crates/ir/src/passes/recognizers/...` paths per CH1-V4-F10 — `scalar.rs` is AMBIGUOUS across three disk locations: `crates/simd-scan/src/scalar.rs`, `crates/ir/src/passes/recognizers/shape_dispatch/scalar.rs`, `crates/core/src/backend/rust/emitter/shapes/scalar.rs`; the leak hit is the `ir` recognizers copy). | `restart/ARCHITECTURE.md:1643` (narrow §12) vs `crates/ir/src/registry/strategy.rs:137-185` (9 idents rows, 4 caught / 5 escape); `rg ... crates/ir/src/` = 11; `rg ... crates/analysis/src` = `ast_utils/mod.rs:4,11` (BbnfBootstrap* doc-comments) | gate-scope gap on `:1643` only + LOCKS:349 self-gate RED (13 live); high |
| COH18-015 | Scanner-crate asymmetry (CH5-V1-04): the totality `crates/simd-scan` carries a random-access probe API (`next_structural_at_or_after`) and the totality generated grammars retain it across the parse via `OnceCell<StructuralIndex>` on `ScanState` (`crates/core/src/grammar/generated/json.rs:701`) — the `OnceCell` lives in the `crates/core` CONSUMER, NOT in `simd-scan` (crate attribution fixed per CH5-V4-010; `simd-scan/src/lib.rs:68` exports ONLY `{StructuralIndex, next_structural_at_or_after}`, no `OnceCell` anywhere in `simd-scan`). The asymmetry is NOT "skinny lacks both" (CH5-V4-011): skinny `bbnf-simd` DOES expose a full `StructuralIndex` (`from_positions`/`positions`/`parity_hash`) + `scan_dispatch`/`scan_scalar` (`lib.rs:72,78,82,94,106,126`), consumed in `json/scan.rs:22`; what it lacks is the `next_structural_at_or_after` random-access API and the cross-parse `OnceCell` retention. The two scanners are FUNCTIONALLY PARALLEL (same `StructuralIndex` concept, divergent APIs + a skinny-only `parity_hash`) — the renamed/parallel-scanner risk is ACTIVE, NOT one-sided. SK-V19 adoption must decide whether it UNIFIES the two scanners or carries a renamed parallel scanner. | `crates/simd-scan/src/lib.rs:68` (`pub use index::{StructuralIndex, next_structural_at_or_after}`), `crates/simd-scan/src/index.rs:74`,`:101`; `OnceCell` field `crates/core/src/grammar/generated/json.rs:701`,`:719`,`:732` + emitter `crates/core/src/backend/rust/emitter/shapes/dispatcher/support.rs:67`; skinny live scanner `skinny/crates/bbnf-simd/src/lib.rs:72,78,82,94,106,126`, consumed `skinny/crates/runtime/src/grammars/json/scan.rs:22`; `rg next_structural_at_or_after skinny/crates/bbnf-simd/src` = 0. | ≈ +20..+217 (SK-V19 scanner-unify = a ≈217-LOC probe-API reconcile — `wc -l crates/simd-scan/src/{index.rs,lib.rs} = 217` — + the 8/9 generated-grammar `OnceCell<StructuralIndex>` emission-site re-route; band per CH4-V4-007, cross-linked to the one shared SK-V19 scanner-unification disposition per CH4-V4-008); renamed/parallel-scanner check; SK-V19 scanner-unification decision; medium-high |

## Gaps / Missing Primitives

| Gap | Evidence | Receiver |
|---|---|---|
| No re-authored HANDOFF SK-V18 paragraph matching the certified generalization scope. | `restart/HANDOFF.md:17-19` (totality-adopt) ≠ `sk-v18/SPEC.md:19-21` (skinny-generalize) | T-P3/Omega docs fold |
| No un-forked single emitter on the benched skinny tree (the SK-V18 deliverable). | `grammar_provider.rs:40-42` fork; `runtime_generator.rs:701` courier | SK-V18 G1/G2/G3 |
| No structural row-collapse over the totality `ir/registry/strategy.rs` grammar-named table (totality R16 analog). | `crates/ir/src/registry/strategy.rs:137-155` | SK-V19 totality fold |
| No `tape/` exclusion (or +4 trace) in the Lock 14 v+1 Pattern H baseline command. | LOCKS:408 returns 71 not 67 | T-P3 LOCKS reconcile |
| The §12 Backend-impl-table leak sub-command (`ARCHITECTURE.md:1643`) covers only `crates/codegen/src/`, missing the `crates/ir/src/registry/strategy.rs` leak; the §9 gate `:2215` + `LOCKS.md:349` already scope `crates/{ir,...}/src/` and catch it (11 sites). | `restart/ARCHITECTURE.md:1643` (narrow) vs `:2215`/`LOCKS.md:349` (wide, catch) vs `ir/registry/strategy.rs:137-155` | SK-V19 / §12 `:1643` command hardening |

## Open Questions

| UNKNOWN | Blocking question | verify_action |
|---|---|---|
| U-COH18-001 | Do the totality `crates/core/src/grammar/generated/{9}.rs` already emit through a un-forked generator, or do they fork on the `ir/registry/strategy.rs` grammar-named table (making SK-V19's totality un-fork a real obligation, not a relabel)? | `rg -n 'RuntimeEmitterKind\|match.*grammar.*\{\|strategy_for\|PRODUCTION_MANIFEST_TABLE' crates/core/src/backend crates/core/src/generate crates/ir/src/`; trace whether `PRODUCTION_MANIFEST_TABLE` is consumed by the generator or only by `regen --check`. |
| U-COH18-002 | `crates/core/src/css_types.rs` is in `crates/core/src/` (NOT a `crates/<grammar>/` declaration crate), so Lock 14 (c) does NOT apply — it is the lock-NAMED mess (CH2-007). The ONLY open question is whether SK-V19 relocates it to a `crates/css/` declaration crate (which WOULD make it Lock 14 (c) admissible) or deletes it. | Confirm `css_types.rs` path is `crates/core/src/css_types.rs` (it is), NOT `crates/css/`; at SK-V19 adoption decide relocate-to-`crates/css/`-declaration-crate (admissible) vs delete; the in-`crates/core/src/` host-shim is the named mess, not a relocated-admissible shim, AS-IS. |
