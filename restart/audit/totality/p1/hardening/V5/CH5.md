---
lens: CH5
name: HIDDEN COUPLING
pass: T-P1-excavation
cycle: V5
disposition: ACCEPT
generated_at: 2026-05-23T21:15:00-04:00
head_commit: 9833295d5
files_audited:
  - restart/audit/totality/p1/1A-substrate-evidence.md
  - restart/audit/totality/p1/1B-codegen-evidence.md
  - restart/audit/totality/p1/1C-runtime-evidence.md
  - restart/audit/totality/p1/1D-skinny-lessons.md
  - restart/audit/totality/p1/1E-locks-evidence.md
  - restart/audit/totality/p1/1F-coherence-scan.md
  - restart/audit/totality/p1/1F-anti-pattern.md
  - restart/audit/totality/p1/1F-past-corpora.md
  - restart/audit/totality/p1/hardening/V2/CH5.md
  - restart/audit/totality/p1/hardening/V3/CH5.md
  - restart/audit/totality/p1/hardening/V4/CH5.md
  - restart/audit/totality/p1/hardening/HARDENING-T-P1-V4-CONSOLIDATED.md
  - restart/audit/totality/p1/hardening/V5/CHALLENGE-CONTEXT.md
prior_cycle_review:
  - "V2 CH5 ACCEPT (12/12, 100 %; CH5-V2-008 ACCEPT-with-caveat cite-staleness across three sub-cases: Track 2 off-by-2, proof-witness drift, CSS source-sidecar fabricated)"
  - "V3 CH5 ACCEPT (9/9, 100 %; CH5-V3-008 housekeeping caveat for 1D row 117 sub-case enumeration carrying pre-rebind cite list verbatim)"
  - "V4 CH5 ACCEPT (8/8, 100 %; F-V4-CH5-1 atomic micro-fold packet — 1D row 117 sub-case cite refresh + 1A row-100→117 cross-cite refresh — discharged V3 housekeeping caveat; zero residual caveats carried forward; 3-cycle LOCK extension declared at V4)"
v5_disposition_focus: "Substrate-union T-P3 §3C disposition rule preserved; AP-009/AP-010/AP-011/AP-020 classifications byte-identical to V4 per CHALLENGE-CONTEXT §2 CH5 clause. V5 is the LOCK-TRIGGER cohort cycle — second consecutive ≥95 % triggers §3Z LOCK at V≤5 ceiling EXACTLY."
v5_fold_packet: "F-V5-CH6-1 single-cell trivial cosmetic anchor refresh at 1E:35 (`:126-128` → `:128-130`; self-cite to §1.5 LAC-1E-12 promotion-candidacy block). No CH5 substantive surface touched. Per CHALLENGE-CONTEXT §1, 1E is the only inventory amended at V5; all other 7 inventories are V≤4-LOCKED with no V5 edits."
live_truth_method: "grep + line-anchored Read at V5 HEAD commit 9833295d5; (1) verification of V4 F-V4-CH5-1 cite refreshes still intact post-V5 1E:35 cosmetic; (2) substantive cite re-verification at HEAD per LAC-1E-12 + NEW-CH2-V3-02 institutionalized executable-verification mandate (Track 2 substrate-helper cluster `skinny/crates/bbnf-bench/src/track2/json.rs:7,26,34,45`; CSS sidecar cluster `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:648, 1082, 1203, 1354, 1511, 1661, 1815, 1964, 2691`; proof-witness exports `skinny/crates/runtime/src/lib.rs:27,29,31,33`); (3) residual orphan grep for pre-rebind cites returns zero hits at V5 HEAD across 1A/1D/1F-anti-pattern (`row 100`, `:100`, `row-100`, `track2/json.rs:5,24,43`, `nonjson_css_l4.rs:222`, `:234`, `:299`, `:504`); (4) cross-cite check of V4 row-100→117 refresh against 1A amended inventory holds verbatim at V5 (1A:10 frontmatter `t_p1_v1_hardening_fold_note` clause (c) carries `1D `:117`` cross-cite; 1A:84 1A-DIV-008 evidence column carries substrate-union nuance pending T-P3 §3C disposition with `1D `:117`` cites in four positions); (5) AP-009 hit-count re-verification (live `grep -c 'lightningcss_facts' skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` returns 27 hits at HEAD, matching V4 F-V4-CH1-1 cosmetic refresh narrative); (6) 1E:35 anchor refresh verification — line 35 now carries `Cross-reference to §1.5 LAC-1E-12 promotion candidacy block at `1E-locks-evidence.md:128-130`` (refreshed from V4 `:126-128` per V5 F-V5-CH6-1 atomic cosmetic); (7) 1F-anti-pattern AP-009/AP-010/AP-011/AP-020 classifications + cite clusters re-read byte-identical to V4 (no V5 edits per CHALLENGE-CONTEXT §1)."
---

## Executive Summary

CH5 returns **ACCEPT** on this V5 LOCK-TRIGGER cohort cycle. V4 closed first cohort-wide ≥95% LOCK-eligible cycle at 100 % × 7 lenses; V5 is the **second consecutive ≥95 % cycle** that triggers cohort §3Z LOCK at V≤5 ceiling EXACTLY per ORCHESTRATOR §3Z dual-cycle rule. V5 is the **4-cycle LOCK extension** for CH5 specifically (V2/V3/V4/V5 each landed 100 % ACCEPT on the V2 amendment baseline). CH5 is the only T-P1 lens whose substantive cite cluster touches the V5 fold inventory (1E:35 anchor refresh sits in the V4 sustained-UNKNOWN paragraph that CH5 verifies for the substrate-coupling firewall — but the refresh itself is a self-cite anchor target update, not a CH5 surface edit), so this CH5 V5 report carries two duties: (a) verify F-V5-CH6-1 cosmetic does not perturb any CH5 substantive cite, and (b) verify the V4 substrate-coupling firewall posture holds byte-identical at V5 HEAD per CHALLENGE-CONTEXT §2 CH5 clause.

**V5 fold no-touch verification of CH5 surface.** The V5 atomic cosmetic fold (F-V5-CH6-1) edits a single cell at `restart/audit/totality/p1/1E-locks-evidence.md:35` — the self-cite anchor reference in the sustained-UNKNOWN paragraph is refreshed from `1E-locks-evidence.md:126-128` to `1E-locks-evidence.md:128-130` (the LAC-1E-12 promotion-candidacy block's true line range at V5 HEAD after intervening LAC table growth between V4 and V5). The substantive content of the 1E:35 paragraph — the four sustained-UNKNOWNs (L03 cursor elision, L16 full allowlist coverage, audit-overlay column gap, Lock 1 fact-stream taxonomy) with their `verify_action` attributes — is preserved byte-identical from V4. Critically, the (iv) NEW SK-V14 UNKNOWN clause carries the Lock 1 fact-stream taxonomy question that T-P3 §3C must dispose between explicit `FactStream` taxonomy extension vs `admitted_fact_output` substrate_target — this is the substrate-union taxonomy hinge that CH5 V5 verifies still routes to T-P3 §3C per the substrate-union T-P3 §3C disposition rule preservation mandate in CHALLENGE-CONTEXT §2 CH5 clause.

**Substrate-coupling firewall preservation at V5 HEAD (9833295d5).** Direct Read verification confirms the four substrate-coupling classifications hold byte-identical to V4:
1. **AP-009 (CSS L4 hit-count)** at `1F-anti-pattern.md:39` + `:56` carries 27-hit narrative verbatim; live `grep -c 'lightningcss_facts' skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` returns 27 hits at V5 HEAD — V4 F-V4-CH1-1 cosmetic refresh remains accurate, no further drift.
2. **AP-010 (proof-witness Lock 14 leak)** at `1F-anti-pattern.md:40` + `:57` + `:73` carries "runtime root proof/test leakage" classification + `runtime/src/lib.rs:29-33` cite cluster verbatim from V4; no V5 edit.
3. **AP-011 (Track 2 independent parser authority with shared substrate helpers)** at `1F-anti-pattern.md:41` + `:58` + `:74` carries "independent parser authority with shared runtime substrate helpers" verdict + 0 LOC tax + `bbnf-bench/src/track2/json.rs:7, 26, 34, 45` cite cluster verbatim from V4 (which preserved verbatim from V3); CH5-V2-005 Lock 1 union sub-case framing carries through unchanged.
4. **AP-020 (CSS comparator-sidecar fence required)** at `1F-anti-pattern.md:80` + `:105` carries "comparator-sidecar coupling; fence required (non-runtime-authoritative; not retained document identity)" verdict + V3 rebind note with explicit fabrication disclosure verbatim from V4; cite cluster `:648, :2691, :1082, 1203, 1354, 1511, 1661, 1815, 1964` preserved verbatim.

**Substantive cite re-verification at V5 HEAD per institutionalized executable-verification mandate.** Per LAC-1E-12 + NEW-CH2-V3-02 (institutionalized at V4, sustained at V5), every CH5-bearing cite was re-grep/Read-verified at HEAD = 9833295d5:
1. **Track 2 substrate-helper sharing** — `skinny/crates/bbnf-bench/src/track2/json.rs:7` carries `tape::{CapacityPlan, OffsetFlags, TapeBuilder}` import (verified); `:26` carries `runtime::grammars::json::scan::structural_capacity_for(` call (verified); `:34` carries `tape: TapeBuilder::new(input.as_bytes(), capacity)` construction (verified); `:45` carries `Ok(JsonRoot::from_tape(self.input, self.tape.finish()))` seal (verified) — four lines match exactly at V5 HEAD; zero drift from V4.
2. **CSS source-sidecar** — `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:648` carries `fixture_sidecar_facts(input)` callsite; `:2691` carries `fn fixture_sidecar_facts(input: &str) -> Result<String, CssOracleError>` definition; `:1082, 1203, 1354, 1511, 1661, 1815, 1964` carry seven `comparator=lightningcss-1.0.0-alpha.71:same-plane-source-sidecar` writer literals (one per CSS L4 sub-grammar wave) — nine lines match exactly at V5 HEAD; zero drift from V4.
3. **Proof-witness exports** — `skinny/crates/runtime/src/lib.rs:27` carries `#[cfg(any(test, feature = "proof"))]` gate; `:29` carries `pub mod json_event_grammar_witness`; `:31` carries `#[cfg(any(test, feature = "proof"))]` gate; `:33` carries `pub mod sheets_witness` — four lines match exactly at V5 HEAD; zero drift from V4.

**Residual-orphan grep verification at V5 HEAD.** Live grep across 1A/1D/1F-anti-pattern returns zero hits for residual orphan patterns:
- `row 100` / `row-100` / `1D \`:100\`` / `1D :100` — zero hits (V4 1A row-100→117 refresh holds at V5; F-V5-CH6-1 1E:35 cosmetic touches a different anchor type and does not regress this row).
- `track2/json.rs:5,24,43` — zero hits (V3 Track 2 pre-rebind cite eliminated, preserved through V4 and V5).
- `nonjson_css_l4.rs:222` / `:234` / `:299` / `:504` as routing cites — zero hits at V5; the V3 explicit-fabrication-disclosure narrative at AP-009 :69, AP-020 :80, and 1A-SUB-014 `t_p1_v1_hardening_fold_note` :10 preserves these in-text only as fabrication disclosure (the CH6 ANTI-PAPER-CLOSE attestation), not as live cites.

**1A row-100→117 cross-cite refresh continues to hold at V5.** V4 F-V4-CH5-1 fold landed two related edits: (1) 1D row 117 sub-case enumeration refresh, (2) 1A row-100→117 cross-cite refresh. Both remain intact at V5 HEAD per direct Read verification:
- `1A-substrate-evidence.md:10` (frontmatter `t_p1_v1_hardening_fold_note`) clause (c) carries `Cross-fold to 1D `:117` added per T-P1 V1 CONSOLIDATED §1.2 substrate-coupling convergence` (verified verbatim from V4).
- `1A-substrate-evidence.md:84` (1A-DIV-008 row evidence column) carries `**Substrate-union nuance pending T-P3 §3C disposition:** 1D `:117` records 'Single substrate proved as substrate cardinality' (REDRESS attribution); 1A-DIV-008 records two structurally independent cursors at HEAD. Both can hold under different definitions — T-P3 §3C must either ratify the two-cursor shape as the V1 substrate-union (1D `:117` reads correctly under ratified definition) or mandate unification (1D `:117` downgrades to 'disproved at HEAD; obligation deferred to T-P2 unification').` — four `:117` cites in the row preserved verbatim from V4.

**Substrate-union T-P3 §3C disposition rule preservation (CHALLENGE-CONTEXT §2 CH5 mandate).** Per V5 dispatch context CH5 clause: "substrate-union T-P3 §3C disposition rule preserved; AP-009/AP-010/AP-011/AP-020 classifications byte-identical to V4." Verified live at V5 HEAD:
- The substrate-union nuance disposition rule at 1A:84 reads verbatim from V4 with both forks (ratify two-cursor shape AS V1 substrate-union OR mandate unification) explicit; `1A-UNK-005` verify_action route preserved; T-P3 §3C ratification rule binding intact.
- 1D row 117 substrate-cardinality verdict + T-P3 §3C PENDING flag + CH5-002/004/005/007 sub-case enumeration (Track 2 + CSS sidecar + StructuralIndex scanner + proof-witness) preserved verbatim with all V3/V4-rebound cites.
- 1E:35 sustained-UNKNOWN paragraph (iv) clause carries the Lock 1 fact-stream taxonomy T-P3 §3C disposition question with `verify_action: T-P3 §3C dispose between (a) explicit `FactStream` taxonomy extending BackendShape to 5 variants vs (b) fact-stream remaining `admitted_fact_output` substrate_target without taxonomy promotion (routes to T-P3 §3C)` — preserved verbatim from V4; the V5 anchor refresh updates only the cross-reference line number, not the substantive routing.

**1A-SUB-016/017/018 SK-V14 binding rows.** All three rows preserve CH5 firewall reading through V5. 1A-SUB-016 (`:69`) carries "partial / diverged — substrate-union is two parallel cursor types" (preserved); 1A-SUB-017 (`:70`) carries `dispatch_value` LTO-fused envelope as Lock-14-mis-attributed by name (preserved); 1A-SUB-018 (`:71`) carries S-P1 CH2 13/17 parse_only + 14/17 direct rank-1 envelope mis-attribution census (preserved). CH5 firewall reading intact: the JSON-named envelope hides the grammar-neutral `dispatch` primitive across offset and direct paths — exactly the hidden coupling CH5 is authored to surface. V5 fold made no edit to these rows (1A is V4-LOCKED per CHALLENGE-CONTEXT §1).

The CH5 hidden-coupling firewall holds at V5. V4 ACCEPT (8/8, 100 %) is preserved unchanged in substantive surface; V5 introduces only the F-V5-CH6-1 cosmetic anchor refresh which does not perturb CH5 cite clusters. The substrate-coupling classifications (AP-011 independent-parser-with-shared-helpers; AP-020 CSS comparator-sidecar coupling fence-required; AP-010 Lock 14 leak under unverified proof gate; AP-009 CSS L4 hit-count 27; 1A-SUB-014 transient scanner side plane live) are all preserved byte-identical to V4. V5 introduces no new hidden-coupling violations. The substrate-union T-P3 §3C ratify-or-unify disposition rule is preserved verbatim across both forks and routes through both 1A-DIV-008 + 1D row 117 + 1E:35 sustained-UNKNOWN paragraph clauses.

## Dispositions

| ID | Disposition | Finding | Evidence |
|---|---|---|---|
| CH5-V5-001 | ACCEPT | V4 F-V4-CH5-1 1D row 117 sub-case enumeration refresh remains intact at V5 HEAD; V5 F-V5-CH6-1 cosmetic does not touch 1D (V4-LOCKED per CHALLENGE-CONTEXT §1). | V5 HEAD direct Read at `restart/audit/totality/p1/1D-skinny-lessons.md:117` confirms the row's note column carries verbatim from V4: "Renamed `StructuralIndex` scanner (CH5-002 / `scan.rs:1,22,51` — transient capacity/proof input, fenced), Track 2 substrate-helper sharing (CH5-005 / `bbnf-bench/src/track2/json.rs:7,26,34,45`), CSS source-sidecar (CH5-004 / `nonjson_css_l4.rs:648, 1082, 1203, 1354, 1511, 1661, 1815, 1964, 2691`), and proof-witness generic-runtime exports (CH5-007 / `runtime/src/lib.rs:29-33`) are all sub-cases of the same Lock 1 union-vs-split disposition T-P3 §3C must close. Until then, this row carries an open `T-P3 §3C PENDING` flag." All four sub-case cites carry V3/V4-rebound values; T-P3 §3C PENDING flag preserved; substrate-cardinality verdict preserved; cross-cite-to-1A-DIV-008 preserved. Live grep at V5 HEAD returns zero hits for pre-rebind orphans `track2/json.rs:5,24,43` and `nonjson_css_l4.rs:222,234,299,504` as routing cites. |
| CH5-V5-002 | ACCEPT | V4 F-V4-CH5-1 1A row-100→117 cross-cite refresh remains intact at V5 HEAD (6 substitutions across 2 lines preserved verbatim from V4); 1A is V4-LOCKED with no V5 edits. | V5 HEAD direct Read at `restart/audit/totality/p1/1A-substrate-evidence.md:10` (frontmatter `t_p1_v1_hardening_fold_note` clause (c)) carries "1D `:117` records 'Single substrate proved as substrate cardinality' (REDRESS attribution); 1A-DIV-008 records two structurally independent cursors at HEAD. Both can hold under different definitions — T-P3 §3C must either ratify two-cursor shape as V1 substrate-union or mandate unification" + "Cross-fold to 1D `:117` added per T-P1 V1 CONSOLIDATED §1.2 substrate-coupling convergence" — both `:117` cites preserved verbatim from V4. Direct Read at `1A-substrate-evidence.md:84` (1A-DIV-008 evidence column) carries the substrate-union nuance paragraph with all four `1D `:117`` cites preserved verbatim from V4 (substrate-union nuance pending clause + ratify-or-unify rule + same-wave consumer clause). Total six substitutions across two lines preserved exactly. Live grep returns zero residual `row 100`/`:100` orphans at V5 HEAD across 1A/1D/1F-anti-pattern. |
| CH5-V5-003 | ACCEPT | Substrate-union nuance + T-P3 §3C ratify-or-unify disposition rule preserved verbatim through V5 (CHALLENGE-CONTEXT §2 CH5 mandate satisfied). | 1A-DIV-008 row at `1A-substrate-evidence.md:84` carries the two-cursor structural split at V5 HEAD verbatim from V4: `ParserState.cursor` over `TapeBuilder<'i>` at `runtime/src/grammars/json/parser.rs:7-12` vs `DirectParser.cursor` with raw `usize` only at `codegen/src/json_typed_direct.rs:518-522`. The substrate-union nuance disposition rule reads exactly as V4 (the prose, T-P3 §3C ratify-or-unify framing, two-reading carry-forward, `1A-UNK-005` verify_action route are all preserved byte-identical). 1D row 117 carries the V2 "proved historically; SK-V14 1A-DIV-008 records two-cursor structural split at HEAD pending T-P3 §3C disposition" verdict + T-P3 §3C PENDING flag + CH5-002/004/005/007 sub-case enumeration in the note column + cross-cite to 1A-DIV-008. 1E:35 sustained-UNKNOWN paragraph (iv) clause carries the Lock 1 fact-stream taxonomy T-P3 §3C disposition question verbatim from V4 with verify_action routing to T-P3 §3C preserved. No paper-close; both ratify/unify readings carry forward with binding T-P3 §3C ratification rule. CHALLENGE-CONTEXT §2 CH5 clause "substrate-union T-P3 §3C disposition rule preserved" satisfied exactly. |
| CH5-V5-004 | ACCEPT | Track 2 + CSS sidecar + proof-witness cites all re-verified executable-valid at V5 HEAD per institutionalized LAC-1E-12 + NEW-CH2-V3-02 mandate. | Track 2: `skinny/crates/bbnf-bench/src/track2/json.rs:7` carries `tape::{CapacityPlan, OffsetFlags, TapeBuilder}` import (verified live at V5 HEAD); `:26` carries `runtime::grammars::json::scan::structural_capacity_for(` call (verified); `:34` carries `tape: TapeBuilder::new(input.as_bytes(), capacity)` construction (verified); `:45` carries `Ok(JsonRoot::from_tape(self.input, self.tape.finish()))` seal (verified). CSS sidecar: `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:648` carries `fixture_sidecar_facts(input)` callsite (verified); `:2691` carries `fn fixture_sidecar_facts(input: &str) -> Result<String, CssOracleError>` definition (verified); `:1082, 1203, 1354, 1511, 1661, 1815, 1964` carry seven `comparator=lightningcss-1.0.0-alpha.71:same-plane-source-sidecar` writer literals (one per CSS L4 sub-grammar wave, verified). Proof-witness: `skinny/crates/runtime/src/lib.rs:27` carries `#[cfg(any(test, feature = "proof"))]`; `:29` carries `pub mod json_event_grammar_witness`; `:31` carries `#[cfg(any(test, feature = "proof"))]`; `:33` carries `pub mod sheets_witness`. All cites match exactly at V5 HEAD; zero drift introduced by V5 cosmetic fold. |
| CH5-V5-005 | ACCEPT | AP-011 substrate-coupling classification preserved byte-identical to V4 (CHALLENGE-CONTEXT §2 CH5 mandate explicit on AP-011). | 1F-anti-pattern AP-011 evidence column at `restart/audit/totality/p1/1F-anti-pattern.md:41` reads "independent parser authority with shared runtime substrate helpers" verdict + "0 LOC classification; low" risk + "Preserve classification while keeping helper sharing visible" note — preserved byte-identical to V4 framing (which was identical V3 framing, which was identical V2 framing). Cross-cite row at `:58` carries identical verdict + cite cluster. Planning metadata row at `:74` carries "0 LOC classification | low | Track 2 evidence-accounting wave | 0 LOC | benchmark report classification | `skinny/crates/bbnf-bench/src/track2/json.rs:7, 26, 34, 45` (V3 cite-rebind; V2 `:5,24,43` off-by-2 per HARDENING-T-P1-V2-CONSOLIDATED §3.1 F-V3-CH5-1)" — preserved byte-identical from V4. The V5 dispatch context confirms 1F-anti-pattern is V4-LOCKED (no V5 edits). The CH5-V2-005 Lock 1 union sub-case framing carries through unchanged through V5: "Independence holds for parser implementation; substrate helpers are deliberately shared and that sharing is Lock 1 union sub-case, not Track-independence violation." |
| CH5-V5-006 | ACCEPT | AP-020 CSS comparator-sidecar fence + AP-010 proof-witness Lock 14 leak + AP-009 CSS L4 hit-count classifications preserved byte-identical to V4 (CHALLENGE-CONTEXT §2 CH5 mandate explicit on AP-009/AP-010/AP-020). | AP-020 at `1F-anti-pattern.md:80` carries "comparator-sidecar coupling; fence required (non-runtime-authoritative; not retained document identity)" verdict + V3 rebind note with explicit fabrication disclosure ("V2 cite cluster `:222,234,299,504` was fabricated (provenance V1 CH5-004; HEAD verifies `:222, :234` are CSS token hex literals inside an `EXPECTED_FACTS` fixture array, `:299` is a `decl…property_hex=…` fixture literal, `:504` is `impl fmt::Display for CssOracleError`)") + the explicit verification command — all preserved byte-identical from V4. AP-020 planning metadata at `:105` carries identical V4 framing. AP-010 carries the proof-witness cite `runtime/src/lib.rs:29-33` under proof gates + "Lock 14 leak under unverified proof gate" verdict — preserved byte-identical from V4 (1F-anti-pattern V4-LOCKED at V5). AP-009 hit-count narrative ("returns 27 hits") preserved byte-identical from V4; live `grep -c 'lightningcss_facts' skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` returns 27 hits at V5 HEAD — V4 F-V4-CH1-1 refresh remains accurate; cite cluster `:648, :2691, :1082, 1203, 1354, 1511, 1661, 1815, 1964` preserved verbatim. |
| CH5-V5-007 | ACCEPT | 1A-SUB-016/017/018 SK-V14 binding rows preserve CH5 firewall reading through V5 (JSON-named dispatch envelopes hide grammar-neutral `dispatch` primitive across two cursor types). | 1A-SUB-016 at `1A-substrate-evidence.md:69` carries verdict "partial / diverged — substrate-union is two parallel cursor types" (preserved byte-identical from V4); 1A-SUB-017 at `:70` carries `dispatch_value`/`parse_object_value_at_direct`/`parse_array_element_at_direct` envelope census + `Alt { Dispatch }` shape attribution + LTO-fuse production-build observation (preserved byte-identical); 1A-SUB-018 at `:71` carries S-P1 CH2 13/17 parse_only + 14/17 direct rank-1 + 5/7 typed-product rank-1 to `DirectParser::skip_value` census (preserved byte-identical). CH5 firewall reading intact: the JSON-named envelopes hide the grammar-neutral `dispatch` primitive across offset and direct paths — exactly the hidden coupling CH5 is authored to surface. V5 fold made no edit to these rows (1A V4-LOCKED per CHALLENGE-CONTEXT §1). |
| CH5-V5-008 | ACCEPT | V5 F-V5-CH6-1 cosmetic anchor refresh at 1E:35 does not perturb CH5 substantive cite clusters; 1E paragraph substantive content preserved byte-identical from V4 (including Lock 1 fact-stream taxonomy T-P3 §3C disposition routing). | V5 HEAD direct Read at `restart/audit/totality/p1/1E-locks-evidence.md:35` confirms the sustained-UNKNOWN paragraph carries the four UNKNOWNs (L03 cursor elision, L16 full allowlist coverage, audit-overlay column gap, Lock 1 fact-stream taxonomy) byte-identical to V4 with their respective `verify_action` attributes. The only change is the cross-reference anchor at end of paragraph: "Cross-reference to §1.5 LAC-1E-12 promotion candidacy block at `1E-locks-evidence.md:128-130`" (V5) vs V4 `:126-128`. The refreshed anchor target at V5 `:128-130` resolves to the LAC-1E-12 promotion candidacy block (verified live: §1.5 block begins after the LAC table which grew by 2 lines between V4 and V5 due to intervening hardening). The substantive Lock 1 fact-stream taxonomy clause (iv) routes correctly to T-P3 §3C per the substrate-union disposition rule — preserved verbatim. No CH5 surface cite was touched. F-V5-CH6-1 cosmetic is documentation cohesion truth-up (anchor target line-number freshness), not a substrate admission or classification change. |

## Convergence Check

| Cycle | Total findings | ACCEPT | REVISE | REJECT | ACCEPT-rate |
|---|---:|---:|---:|---:|---:|
| V1 | 7 | 3 | 4 | 0 | 42.9 % |
| V2 | 12 | 12 (1 ACCEPT-with-caveat for documentation drift) | 0 | 0 | 100.0 % |
| V3 | 9 | 9 (1 ACCEPT carrying residual-housekeeping caveat for V4 micro-fold) | 0 | 0 | 100.0 % |
| V4 | 8 | 8 (V3 housekeeping caveat discharged; zero orphans) | 0 | 0 | 100.0 % |
| V5 | 8 | 8 (V4 surface preserved byte-identical; F-V5-CH6-1 cosmetic does not perturb CH5) | 0 | 0 | 100.0 % |

V5 CH5 ACCEPT-rate is **100 % (8/8)**. V2 + V3 + V4 + V5 represent **four consecutive ≥95 % cycles** for CH5 on the V2 amendment baseline — the §4 convergence criterion (≥95 % × 2 consecutive cycles) was satisfied at V3; V4 extended to three; V5 extends to four. The CH5-V3-008 housekeeping caveat was discharged at V4 and remains discharged at V5; no carry-forward caveat exists. V5 CH5 reads CLOSED for cohort §3Z LOCK at the V≤5 ceiling.

## §3Z Gate Evaluation

Per V5 CHALLENGE-CONTEXT §0 §2 binding: V5 is the **LOCK-TRIGGER cohort cycle for T-P1**. V4 closed first cohort-wide ≥95 % LOCK-eligible cycle (100 % × 7 lenses; sub-axis 78/78; per-lens 700/7). V5 is the second consecutive ≥95 % cycle that triggers cohort §3Z LOCK at V≤5 ceiling EXACTLY per ORCHESTRATOR §3Z dual-cycle ≥95 % rule.

CH5 trajectory through V5:
- **V2 → V3:** two consecutive ≥95 % cycles satisfied for CH5 on the V2 amendment baseline.
- **V3 → V4:** three consecutive ≥95 % cycles for CH5; V3 housekeeping caveat DISCHARGED via F-V4-CH5-1 atomic micro-fold (1D row 117 sub-case cite refresh + 1A row-100→117 cross-cite refresh).
- **V4 → V5:** **four consecutive ≥95 % cycles for CH5 — this is the 4-cycle LOCK extension declared by this V5 cycle.** V5 F-V5-CH6-1 cosmetic anchor refresh at 1E:35 does not perturb CH5 surface (CH5-V5-008 verifies). Substrate-coupling firewall (AP-009/AP-010/AP-011/AP-020) preserved byte-identical to V4 per CHALLENGE-CONTEXT §2 CH5 mandate. Substrate-union T-P3 §3C ratify-or-unify disposition rule preserved verbatim across 1A-DIV-008 + 1D row 117 + 1E:35 sustained-UNKNOWN paragraph.

CH5 is **LOCK-READY at V5**. Cohort §3Z LOCK gate triggers on V5 cohort close per §3Z dual-cycle ≥95 % rule + V≤5 ceiling. **This V5 CH5 cycle is the 4-cycle LOCK extension** the V5 dispatch context anticipated. CH5 is the canonical substrate-coupling firewall lens; its 4-cycle LOCK extension at V5 with zero substrate admissions and full substrate-union T-P3 §3C disposition rule preservation reads as the strongest possible LOCK candidate among the seven T-P1 lenses.

## Anti-Paper-Close Self-Audit

V5 CH5 explicitly tests for the CH7 V2 failure mode (V→V+1 cite-carry without re-verification) and the meta-CH7 anti-pattern (asserting prose into a counter-surface that does not contain it). All cite preservations and substrate-coupling classification verifications in this V5 LOCK-TRIGGER cycle were re-verified at V5 HEAD via direct grep/Read on the cited files. This V5 self-audit honours the **institutionalized executable-verification mandate** per LAC-1E-12 + NEW-CH2-V3-02 at the V5 cycle:

1. **Track 2 cites `:7,26,34,45`** — re-verified at V5 HEAD by direct grep of `skinny/crates/bbnf-bench/src/track2/json.rs`; the four lines carry the imported tape types, `structural_capacity_for` call, `TapeBuilder::new` construction, and `JsonRoot::from_tape` seal respectively.
2. **CSS sidecar cites `:648, 1082, 1203, 1354, 1511, 1661, 1815, 1964, 2691`** — re-verified at V5 HEAD by direct grep of `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` for `fixture_sidecar_facts` + `same-plane-source-sidecar` patterns; all nine lines carry the expected content; the `lightningcss_facts` hit-count narrative at AP-009 (27 hits) matches live `grep -c` at V5 HEAD.
3. **Proof-witness `:29, 33` under cfg gates `:27, 31`** — re-verified at V5 HEAD by direct grep of `skinny/crates/runtime/src/lib.rs`; the four lines carry the proof-cfg gate + `json_event_grammar_witness` module declaration + proof-cfg gate + `sheets_witness` module declaration.
4. **1A row-100→117 cross-cite refresh** — re-verified at V5 HEAD by direct Read of 1A-substrate-evidence.md frontmatter (line 10) + 1A-DIV-008 row (line 84); both lines carry the refreshed `:117` cross-cite to 1D byte-identical to V4; live grep returns zero residual `row 100`/`:100` orphans at V5 HEAD across 1A/1D/1F-anti-pattern.
5. **1D row 117 sub-case enumeration refresh** — re-verified at V5 HEAD by direct Read of 1D-skinny-lessons.md row 117; the note column carries the four V3/V4-rebound sub-case cites byte-identical to V4; live grep returns zero residual pre-rebind orphan patterns at V5 HEAD.
6. **F-V5-CH6-1 anchor refresh discharge verification** — re-verified at V5 HEAD by direct Read of 1E-locks-evidence.md line 35: the sustained-UNKNOWN paragraph's cross-reference anchor now reads `1E-locks-evidence.md:128-130` (V5) refreshed from V4 `:126-128`. Cross-Read of 1E:128-130 confirms the LAC-1E-12 promotion candidacy block §1.5 begins at the refreshed range. The 4 cited UNKNOWNs in the paragraph (L03 cursor elision, L16 allowlist, audit-overlay column gap, Lock 1 fact-stream taxonomy) remain executable-verifiable at their respective Open Questions table line references — substrate-union T-P3 §3C disposition routing preserved verbatim.
7. **AP-009/AP-010/AP-011/AP-020 byte-identity verification** — direct Read of 1F-anti-pattern.md confirms all four anti-pattern rows carry V4 verdicts, cite clusters, V3 rebind notes, explicit fabrication disclosure narratives, and planning metadata byte-identical to V4. 1F-anti-pattern is V4-LOCKED per CHALLENGE-CONTEXT §1 — no V5 edits attempted, none observed.

V5 CH5 carries this self-audit because CH6 ANTI-PAPER-CLOSE explicitly flagged the CH7-V2-failure-mode discipline at maximum strength for the V5 LOCK-TRIGGER cycle, AND because the V5 CHALLENGE-CONTEXT §3 elevated executable verification to "institutionalized mandate" per LAC-1E-12 + NEW-CH2-V3-02. The V5 fold packet honoured the discipline (single cosmetic cell with executable verification of refreshed anchor target); this CH5 report attests it. The V3 explicit-fabrication-disclosure narrative (AP-009 / AP-020 / 1A-SUB-014 `t_p1_v1_hardening_fold_note`) is preserved verbatim through V5 — V5 does not paper over V3's disclosure.

## Closing Posture

CH5 hidden-coupling firewall holds at V5. The V4 substantive surface is preserved byte-identical at V5; the F-V5-CH6-1 cosmetic anchor refresh at 1E:35 is documentation cohesion truth-up (anchor target line-number freshness due to LAC-table growth between V4 and V5), not a CH5 surface edit. The substrate-coupling classifications (AP-011 independent-parser-with-shared-helpers; AP-020 CSS comparator-sidecar coupling fence-required; AP-010 Lock 14 leak under unverified proof gate; AP-009 CSS L4 hit-count 27; 1A-SUB-014 transient scanner side plane live) are all preserved byte-identical to V4 per CHALLENGE-CONTEXT §2 CH5 mandate — verified live at V5 HEAD. The substrate-union nuance (1A-DIV-008 two-cursor structural split + 1D row 117 substrate-cardinality T-P3 §3C pending + 1E:35 Lock 1 fact-stream taxonomy T-P3 §3C question) carries through unchanged with binding ratification rule intact across all three carrier surfaces. The SK-V14 binding (1A-SUB-016/017/018 dispatch-envelope mis-attribution census) reads correctly under CH5 — the JSON-named envelopes hide the grammar-neutral `dispatch` primitive across two cursor types.

Zero residual housekeeping caveats carry forward to V6 (none expected — V5 is LOCK-trigger; T-P1 cohort closes at V5 ceiling per §3Z). The 1D row 117 sub-case enumeration cites match the substantive binding rows on the cite-of-record inventories (1A-SUB-014, 1F-anti-pattern AP-009/010/011/020) byte-for-byte at V5 HEAD. The 1A row-100→117 cross-cite refresh from V4 holds verbatim at V5.

V5 CH5 is **ACCEPT (8/8, 100 %)**. V2 + V3 + V4 + V5 represent **four consecutive ≥95 % cycles** for CH5 on the V2 amendment baseline; the §4 convergence criterion is satisfied with a 2-cycle margin beyond minimum. **Cohort §3Z LOCK candidacy for CH5 reads CLOSED at V5** as part of the cohort-wide §3Z LOCK declaration at V5 ceiling — this is the **4-cycle LOCK extension** the V5 dispatch context anticipated, sustaining the substrate-coupling firewall through the LOCK-TRIGGER cycle without admission, weakening, or fabrication. Substrate-union T-P3 §3C disposition rule preserved verbatim; AP-009/AP-010/AP-011/AP-020 classifications byte-identical to V4; CHALLENGE-CONTEXT §2 CH5 mandate satisfied exactly.
