---
challenge: CH2
pass: T-P2-research
cycle: V5
lens: generality / Lock 14
generated_at: 2026-05-21T15:07:16-04:00
verdict: ACCEPT
---

# T-P2 V5 CH2 - Generality / Lock 14

## Verdict

**ACCEPT.** The unchanged V4 packet remains acceptable for CH2. It does not
narrow the research packet to JSON-only proof, does not transfer grammar policy
ownership into generic crates, and does not turn source-backed primitives into
fleet-wide admissions. Generated-policy and caller-data ownership remain the
binding route for shared primitives; CSS plus Sheets/BBNF-self pressure remains
load-bearing for Lock 14; generic-crate policy leaks are still explicit blockers.

## Confirmation Evidence

- The pass prompt defines CH2 as a Lock 14 generality audit: grounded techniques
  must transfer through CSS L4 / Sheets / BBNF-self, not only JSON, and a
  JSON-only fleet-wide technique is a REVISE
  (`restart/prompts/totality/PASS-2-RESEARCH.md:106`-`110`). The V4 consolidated
  result carries CH2 as ACCEPT specifically because non-shortlist blockers
  preserve generated-policy and caller-data ownership across CSS/JSON/Sheets
  pressure and add no JSON-only generic-crate policy
  (`restart/audit/totality/p2/hardening/HARDENING-T-P2-V4-CONSOLIDATED.md:23`-`30`).

- V4 is an additive fold over V2/V3 and explicitly does not weaken their Lock 14,
  Lock 1, REDRESS, material-differential, anti-paper-close, state-machine, or
  abrogate contracts (`restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:9`-`22`).
  Its executable delta marks weak primitives non-shortlist until strict commands,
  policy splits, and first consumers exist, including JSON plus CSS escape parity
  for `tbl_tbx_escape_decode_batch`, scalar policy plus first consumer for DOTPROD
  digit parsing, and a combined matrix plus exact consumer for PMULL/CSSC union
  (`restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:47`-`58`).

- The inherited Lock 14 transfer contract remains broader than JSON. It requires
  a generated provider registry, grammar-shape leak scan, generated
  sink/fact/value/flag surfaces, primitive policy from generated grammar data or
  caller data, CSS plus Sheets/BBNF-self negative-control transfer, and generated
  decision-engine facts (`restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md:53`-`75`).
  V3 keeps those rows gate-consumable through normalized admission states and a
  shared ledger whose `policy_owner` is `generated_grammar`, `caller_data`, or
  `none` (`restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md:56`-`101`).

- The owner dossiers still preserve non-JSON pressure. 2A says SOTA techniques
  transfer only through generated grammar facts or caller-supplied policy, and
  that one CSS row is evidence but not fleet-wide grammar closure
  (`restart/audit/totality/p2/2A-sota-landscape.md:56`-`64`). 2C makes CSS L4 and
  Sheets the load-bearing falsifiers, requires generated registry/config/fact
  tables with no generic crate edits, and keeps Sheets/BBNF-self as negative
  controls against JSON-shaped role mining
  (`restart/audit/totality/p2/2C-grammar-neutrality.md:36`-`63`,
  `restart/audit/totality/p2/2C-grammar-neutrality.md:82`-`127`,
  `restart/audit/totality/p2/2C-grammar-neutrality.md:157`-`168`).

- Generated-policy and caller-data ownership are preserved at primitive and
  hardware boundaries. 2B requires shared SIMD primitives to receive byte
  alphabets, delimiter, quote/escape/control, number, string, and no-string policy
  from generated grammar data or caller data, and downgrades proof-only routes
  until a JSON/CSS/Sheets/BBNF-self row moves or is measured rejected
  (`restart/audit/totality/p2/2B-primitive-vocabulary.md:30`-`46`,
  `restart/audit/totality/p2/2B-primitive-vocabulary.md:53`-`60`). 2E requires
  every hardware route to record `substrate_target`, `retention_lifetime`, and
  `policy_owner`, and treats caller-provided alphabets as the transfer condition
  for byte-class primitives (`restart/audit/totality/p2/2E-host-arch-esoterica.md:115`-`139`,
  `restart/audit/totality/p2/2E-host-arch-esoterica.md:243`-`253`).

- Generic-crate policy leaks remain blocked. 2C identifies hard-coded
  `RuntimeProvider`, `JsonSink`, JSON flag meanings, and JSON role mining as Lock
  14 drift, not closure (`restart/audit/totality/p2/2C-grammar-neutrality.md:128`-`155`).
  2F fences future parse-that / `bbnf-regex` imports to grammar-neutral HIR,
  byte-class, scanner-plan, and policy facts; runtime scanner outputs must be
  local temporaries consumed by generated grammar code, while hard-coded
  JSON/CSS alphabets, `JsonSink`, JSON flag names, and `\uXXXX`-only semantics are
  non-admissible shared APIs (`restart/audit/totality/p2/2F-parse-that-gaps.md:54`-`61`,
  `restart/audit/totality/p2/2F-parse-that-gaps.md:162`-`187`,
  `restart/audit/totality/p2/2F-parse-that-gaps.md:245`-`252`).

## Required Repairs

None for CH2.

## Carry-Forward

This V5 ACCEPT is unchanged-packet confirmation only. T-P3/G-Omega must still
gate-consume the generated manifests, Lock 14 leak scans, strict CSS/JSON oracles,
Sheets or BBNF-self witnesses, same-wave consumers, and non-shortlist blocker
clearance before any fleet-wide Lock 14 closure claim.
