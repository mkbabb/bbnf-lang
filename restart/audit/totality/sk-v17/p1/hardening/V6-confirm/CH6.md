# CH6 — ANTI-PAPER-CLOSE (V6-confirm) — SK-V17 T-P1 EXCAVATION

```yaml
lens: CH6-ANTI-PAPER-CLOSE
pass: T-P1-SKV17-excavation
cycle: V6-confirm
generated_at: 2026-05-29T00:00:00Z
reviewer: CH6
master_head: 445925167154de73540e3ea3283d0170371de790
mode: confirming   # ACCEPT where V5-folded artefact is correct+complete; flag only genuine residual defects
subjects_reviewed:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1b-codegen-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1e-locks-evidence.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
  - restart/audit/totality/sk-v17/p1/1f-past-corpora.md
live_truth_method: "git rev-parse HEAD = 445925167 (matches all 8 frontmatters). Independent live re-resolution this cycle at master 445925167: (1) grep -c scan_structural over the NINE core generated grammars crates/core/src/grammar/generated/{json,ebnf,bnf,csv,css_l4,css_pretty,google_sheets,bbnf}.rs = 1 each, math.rs = 0 — EIGHT-name census exact. (2) math.rs structural_index field = ABSENT (grep structural_index = 0 field hits); doc-comment `OnceCell<StructuralIndex>` at :281; ScanState struct :287, body holds only nospace_bits:u64 :288 + nospace_start:isize :289 (no field). json.rs genuine field structural_index: OnceCell at :701, init :711, scan_structural call :732. (3) scan call-site lines all eight exact: json:732, css_l4:15982, bbnf:4843, bnf:848, csv:566, ebnf:1381, css_pretty:1905, google_sheets:3559. (4) file LOC json.rs=3505, css_l4.rs=108406. (5) core TapeRec #[repr(C, align(4))] record.rs:102, struct :103, const-assert size==16 :120 / align==4 :121; begin_compound(&StructLayout) reads layout.rule_id & 0x1F mod.rs:185-186. (6) skinny SoA Tape<'input> mod.rs:94 (offsets:Vec<u32>:96, flag_cursors:Vec<u32>:97, flag_values:Vec<u8>:98), from_offsets :104; ValueRef struct :175; value_from_ref json/value.rs:143. (7) core value-API absence: grep value_from_ref|ValueRef in crates/core/src/runtime/json/value.rs = 0. (8) @generated headers (V5 1A Gaps-row fold) LIVE: json/value.rs:1 `// @generated by xtask regen-json; do not edit by hand.`, css_l4/value.rs:1 `regen-css`. (9) enum BackendShape grep crates/ = 0; skinny ir/lib.rs:340. derive_backend_shape crates/ = 0; skinny passes/lib.rs:392,401. (10) LayoutFacts crates/ = 0 (V5 1E LAC-04 fold); skinny passes/lib.rs:85,91. (11) StructLayout crates/ = 960. (12) EmitStrategy enum strategy.rs:104, StructDirect variant :107, is_struct_direct :223-224. FieldSource enum struct.rs:84, StructLayout :202, StructRegistry :313, insert :326, layout :331, layout_by_name :337. (13) TapeStructBuilder grep outside runtime/tape/ = 0. (14) css_l4 builder: pending_value:Option :71 + 6 pending_* Vec :74-79 = 7 pending fields, none Vec<Vec>; builder LOC 817/231, view.rs 76. (15) SPEC REDRESS-53 :577/:657/:825/:839 (V5 :578->:577 fold; :578 = continuation clause); SPEC :791 AZ-IV, :793-795 StructRegistry 28-65x/983x/10583x, :806 x86 bar, :807-811 second-substrate block, :854 D6. (16) LOCKS:75 (offset-tape + columnar SoA buried + parallel substrates dead), :107-108 (5-shape canon), :160 (Lock-2 StructLayout retire). ARCH:1088 (Lock-1 union, structural projection IS the tape), :1206 (CollapsedStage NOT-ADMITTED x86-only/UNKNOWN-2D-05/collapsed_stage.rs:15-17), :1803 (FactStream output-plane (3), NOT 6th BackendShape). (17) Deferral-pattern grep (will find|later inventory|defer to|resolved later|T-P2 will|T-P3 will) over all 8 artefacts outside verify_action/receiver = 0. RESIDUAL DEFECT (§3, 1b): the live_truth_method frontmatter (1b:12) AND body BSHAPE17-004 (1b:97) cite `wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs=17 each`; the brace-glob expands `collapsed`->collapsed_tape.rs which DOES NOT EXIST (real file collapsed_stage.rs). Live execution: exit 1, `collapsed_tape.rs: No such file or directory`, returns 3 files/51 total, NEVER `17 each` for four. This is the V5-recorded CH1-V5-001 REVISE, DEFERRED (not folded) to T-P2/T-P3 carry per HARDENING-V5-CONSOLIDATED:212-213. The 17-LOC fact is true and body line 72 was corrected to collapsed_stage; line 12 + line 97 brace-globs were NOT."
counts:
  sections_dispositioned: 8
  accept: 7
  revise: 1
  reject: 0
accept_rate: 0.875
```

## Lens charge

CH6 ANTI-PAPER-CLOSE: no "excavated / resolved / wired" claim stands
without a live-evidence citation (cargo asm symbol, bench row, REDRESS
admit, file:line/SHA); no divergence is deferred to "a later inventory"
or "T-P2 will find" in place of present ground truth; every UNKNOWN
carries a verify_action; every cited *command* must execute as written.
Ground truth is concrete, resolved to file:line / SHA.

This is a CONFIRMING cycle: ACCEPT where the V5-folded artefact is
correct + complete; flag only genuine residual defects.

## Posture entering V6-confirm

V5 CH6 (`hardening/V5/CH6.md`) returned 8/8 ACCEPT (100%). The V5
consolidated (`HARDENING-T-P1-SKV17-V5-CONSOLIDATED.md`) recorded the
cycle at 98.7% with **one residual REVISE, CH1-V5-001**, which it
declared "non-load-bearing, isolated to a frontmatter shorthand string"
and **explicitly deferred** as a "Fix (for T-P2/T-P3 carry)"
(`HARDENING-…-V5-CONSOLIDATED.md:204-217`).

CH6's confirming mandate is precisely *"nothing deferred to T-P2 will
find."* A defect that the prior aggregate chose to carry forward rather
than fold is the exact paper-close shape CH6 must re-test against live
ground truth — not inherit on the aggregate's say-so. CH6 therefore
re-executed the cited command string verbatim this cycle. It does not
execute. The defect is live in the current on-disk 1b. **§3 is REVISE.**

The other seven sections re-resolved verbatim clean (every load-bearing
substrate / value-API / BackendShape / NEON / SPEC-anchor citation
re-run this cycle); they ACCEPT.

## Verification posture — every load-bearing citation re-resolved live at master `445925167`

CH6 accepts no self-reported figure on recall. Each row was re-run live.

- **Scan census.** `grep -c scan_structural` over the nine *core*
  generated grammars (`crates/core/src/grammar/generated/*.rs`):
  json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf = **1 each;
  math = 0**. EIGHT-name census exact. `math.rs` carries **no**
  `structural_index` field — `ScanState` (`:287`) body holds only
  `nospace_bits: u64` (`:288`) + `nospace_start: isize` (`:289`); the
  `OnceCell<StructuralIndex>` mention (`:281`) is doc-comment. Contrast
  the genuine `json.rs:701` field (init `:711`, scan `:732`). The
  doc-comment-vs-field correction holds tree-wide.
- **Scan call-site lines.** json:732, css_l4:15982, bbnf:4843, bnf:848,
  csv:566, ebnf:1381, css_pretty:1905, google_sheets:3559 — **all eight
  resolve**. File LOC json.rs=3505, css_l4.rs=108406 (the call-site-line
  vs file-LOC disambiguation is honest).
- **Tape AoS (core).** `TapeRec` `#[repr(C, align(4))]` (`record.rs:102`),
  struct `:103`, const-asserted 16-byte (`:120`) / align-4 (`:121`);
  `begin_compound(&StructLayout)` reads `(layout.rule_id & 0x1F)`
  (`mod.rs:185-186`) — **resolve.**
- **Tape SoA (skinny).** `Tape<'input>` (`mod.rs:94`), `offsets:Vec<u32>`
  (`:96`), `flag_cursors:Vec<u32>` (`:97`), `flag_values:Vec<u8>` (`:98`),
  `from_offsets` (`:104`); `ValueRef` struct (`:175`); `value_from_ref`
  (`json/value.rs:143`) — **resolve.** Sparse position-keyed side-vectors,
  not a dense class-column.
- **Value-API absence (core) / presence (skinny).** `value_from_ref|
  ValueRef` grep-**zero** in core `json/value.rs`; the V5 1A Gaps-row
  fold is live: both core value modules carry `// @generated by xtask
  regen-{json,css}; do not edit by hand.` at line 1 — **EAGER, not
  hand-written; Lock-14 ALLOWED** holds.
- **BackendShape / selector absence in core.** `enum BackendShape`
  grep-**zero** in `crates/`, present skinny `ir/lib.rs:340`;
  `derive_backend_shape` grep-zero in `crates/`, skinny `passes/lib.rs:
  392,401`; `EmitStrategy` enum `strategy.rs:104`, `StructDirect` variant
  `:107`, `is_struct_direct` `:223-224` — **resolve.**
- **StructRegistry / FieldSource fence.** `FieldSource` enum
  (`struct.rs:84`), `StructLayout` (`:202`), `StructRegistry` (`:313`),
  `insert` (`:326`), `layout` (`:331`), `layout_by_name` (`:337`); `grep
  -rn StructLayout crates/` = **960** — exact. The V5 1B anchor fix
  (lookup symbol = `StructRegistry::layout`@:331 / `layout_by_name`@:337,
  NOT `lookup`@:313) holds.
- **LayoutFacts (V5 1E LAC-04 fold).** `grep -rn LayoutFacts crates/` =
  **0**; present skinny `passes/lib.rs:85,91` — **resolve.**
- **Live tape UNWIRED.** `TapeStructBuilder` grep-**zero** outside
  `runtime/tape/`. CSS builder pending shape: `pending_value: Option`
  (`:71`) + six `pending_*` Vecs (`:74-79`) = SEVEN pending fields,
  **none `Vec<Vec>`** — exact. CSS builder 817 LOC, json 231 LOC,
  `view.rs` 76 LOC — all exact.
- **Spec / LOCKS / ARCH surfaces.** SPEC REDRESS-53 `:577/:657/:825/:839`
  (V5 `:578→:577` fold verified; `:578` is the continuation clause "…
  retained cursor / aux density / sidecar event vector"); SPEC `:791`
  (AZ-IV), `:793-795` (StructRegistry 28-65×/983×/10583×), `:806` (x86
  bar), `:807-811` (second-substrate block naming StructLayout/
  TapeStructBuilder/TapeCursor + sixth BackendShape + cross-call
  classifier-state retention), `:854` (D6). LOCKS `:75` (offset-tape +
  "Columnar SoA stays buried" + "parallel substrates are dead"),
  `:107-108` (5-shape canon), `:160` (Lock-2 StructLayout retire). ARCH
  `:1088` (Lock-1 union, "structural projection IS the tape (no second
  sidecar)"), `:1206` (CollapsedStage NOT-ADMITTED x86-only/UNKNOWN-2D-05/
  `collapsed_stage.rs:15-17`), `:1803` (FactStream output-plane (3), "NOT
  a 6th BackendShape") — **all resolve verbatim.**

A deferral-pattern grep (`will find | later inventory | defer to |
resolved later | T-P2 will | T-P3 will`) over all eight artefacts
returns **zero** hits outside verify_action / receiver columns.

---

## Dispositions

### §1 — 1d-skinny-lessons SK17L-001..010 + Do-Not-Redrive ledger + UNKNOWNs → **ACCEPT**

No paper-close. SK17L rows resolve at skinny + core file:lines re-run
this cycle: SK17L-001 (skinny SoA `Tape` mod.rs:94 vs core AoS `TapeRec`
record.rs:103), SK17L-002 (value-API eager-generated vs lazy `ValueRef`,
Lock-14 EAGER-vs-hand-written distinction correct, `@generated` headers
live), SK17L-008 (EIGHT-name census + math.rs field-absence via
`ScanState` body :288-289). SK17L-009 honestly separates by-construction
generality from by-exercise proof (JSON tape witness only). The
Do-Not-Redrive ledger rows carry SPEC/alphaC anchors + fold fences; the
StructRegistry no-per-leaf-lookup fence is stated present-tense at
`struct.rs:313,331` + `tape/mod.rs:185`. UNKNOWNs carry verify_actions;
"T-P2/T-P3" sits only in those columns. ACCEPT.

---

### §2 — 1a-substrate SUB17-001..009 + Substrate-Union Firewall + UNKNOWNs → **ACCEPT**

The V5 Gaps-row REVISE is fully discharged and re-verified live: the row
states `@generated … but EAGER` with Lock-14 ALLOWED framing; both
`@generated` headers confirmed at `value.rs:1` this cycle. Every SUB17
row resolves (record.rs:102,120; skinny tape/mod.rs:94,96-98,175;
struct.rs:84,202,313,331; alphabet richer-form; ARCH:1206; LOCKS:75,160).
SUB17-003 confirms tape UNWIRED (`TapeStructBuilder` grep-zero) with NO
claimed fold. The per-leaf-lookup fence is present-tense. Two UNKNOWNs
carry verify_actions. ACCEPT.

---

### §3 — 1b-codegen BSHAPE17-001..009 + do-not-redrive ledger + UNKNOWNs → **REVISE**

**The load-bearing codegen facts all resolve hard** — `enum BackendShape`
= 0 in `crates/` (BSHAPE17-001), `derive_backend_shape` skinny-only
passes/lib.rs:392,401 (BSHAPE17-002), single `EmitStrategy::StructDirect`
strategy.rs:104/:107/:223-224 (BSHAPE17-003), the V5 anchor fix
(`StructRegistry::layout`@:331 / `layout_by_name`@:337, not :313)
verified, and BSHAPE17-009's shared-classifier impl-exceeds-spec grounded
at every cited line. **No substrate/value-API/BackendShape/NEON
divergence claim is false.**

**But a genuine residual defect persists, un-folded, in the V5 artefact.**
The `live_truth_method` frontmatter (`1b-codegen-evidence.md:12`) AND the
body BSHAPE17-004 do-not-redrive row (`1b-codegen-evidence.md:97`) both
cite the evidence command:

```
wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs = 17 each
```

The brace-glob expands `collapsed` → `collapsed_tape.rs`, which **does
not exist** — the real lowerer file is `collapsed_stage.rs`. Executed
verbatim this cycle, the command **errors** (exit 1,
`collapsed_tape.rs: open: No such file or directory`) and returns only
THREE files / 51 total — it **never** yields "17 each" for four files.

This is the exact CH1-V5-001 REVISE the V5 consolidated recorded and
then **deferred** as a "Fix (for T-P2/T-P3 carry)"
(`HARDENING-…-V5-CONSOLIDATED.md:204-217`). On the CH6 lens this is
paper-close by deferral: a cited evidence command that does not execute
as written, parked for a "later inventory" instead of folded. The V5
fold corrected only the body line 72 (`collapsed_stage.rs:15-17`); the
frontmatter (`:12`) and the second body row (`:97`) still carry the
non-executing brace-glob. The underlying FACT (17 LOC each, four
scaffolds + 270-LOC `sink_only`) is independently true — the real files
`{eager_tape,offset_tape,event_tape,collapsed_stage}.rs` each read 17 —
so this is a citation-string defect, not a false divergence; hence
**REVISE, not REJECT.**

**Concrete fix (must fold, not carry):** replace both `:12` and `:97`
brace-globs with the executing form
`{eager_tape,offset_tape,event_tape,collapsed_stage}.rs = 17 each;
sink_only.rs = 270` (enumerated real filenames, no `_tape` suffix on
`collapsed_stage`).

The remaining 1b content (two UNKNOWNs with verify_actions; BSHAPE17-005
CollapsedStage → UNKNOWN-2D-05 at ARCH:1206 as a recorded open, not a
fresh gap) is clean. REVISE is localized to the one non-executing
citation-command in `:12` + `:97`.

---

### §4 — 1c-runtime RT17-001..007 + per-grammar census + UNKNOWNs → **ACCEPT**

RT17-003 is the strongest anti-paper-close row: tape UNWIRED (grep-zero)
AND scan wired across all 8, both live-cited, labelled "UNWIRED confirmed
as stated" — no claimed fold. The math.rs field-absence carry is verified
live (`ScanState` body :288-289 holds only nospace_bits/nospace_start,
no `structural_index` field; the two OnceCell mentions are doc-comment).
Per-grammar scan call-site lines all resolve; the file-LOC disambiguation
(json.rs=3505, css_l4.rs=108406) is honest. RT17-002/006 carry the
value-API + per-leaf-fence divergences present-tense. RT17-004 confirms
Lock 14 (substrate data-binding + hard-fail panic). Three UNKNOWNs carry
verify_actions scoped to all 8 (math excluded). ACCEPT.

---

### §5 — 1e-locks Verified Invariants + L01/L02/L10/L14/L16 + COH-014 + LACs + UNKNOWNs → **ACCEPT**

1E re-resolves the 16-lock count, 5-shape canon, all-8 scan census, and
one-tape-construct invariant. The V5 folds are clean: LAC-04 path-(b)
re-pricing grounded at `grep -rn LayoutFacts crates/` = **0** (live, this
cycle), present only skinny passes/lib.rs:85,91; CH5-S8 REDRESS-53 anchor
at :577/:825/:839 (live grep confirms; :578 continuation). Per-lock
verdicts honest (L01 partial/core-UNWIRED-scan-WIRED; L02 drifted with
960-site evidence; L10 canon-holds/CollapsedStage-aarch64-refused →
UNKNOWN-2D-05; L16 NEON-proven/x86-diagnostic). The six LACs carry
path:line + loc/risk/wave_hint with the explicit "disposition is T-P3 3C"
guard — no premature amendment. Two UNKNOWNs carry verify_actions. ACCEPT.

---

### §6 — 1f-coherence COH17-001..008 + Gaps row + UNKNOWNs → **ACCEPT**

The all-8 scan census holds. COH17-001..008 resolve; COH17-007 FactStream
citation (ARCH:1803 output-plane (3)) verified verbatim, the lone
`unknown` carrying U-COH17-001 with a verify_action. U-COH17-002 is the
load-bearing CATALOGUED DIVERGENCE: it states the Lock-1
exactly-one-encoding closure present-tense and distinguishes the
admissible offset-tape-SoA (skinny `Tape.offsets`, LOCKS:75 + :86 +
ARCH:1088) from the dead AV.04 class-column-SoA (LOCKS:75 "Columnar SoA
stays buried") — resolves at skinny tape/mod.rs:96-98. The forward
disposition sits only in the verify_action column. ACCEPT.

---

### §7 — 1f-anti-pattern AP17-001..005 + CH5 verdict + UNKNOWN → **ACCEPT**

AP17-002 enumerates all 8 scan/OnceCell carriers, scopes the firewall to
8 never 9, with the math.rs field-absence reasoning carried. AP17-003
states "six pending_* Vecs (:74-79) plus one pending_value: Option (:71)
= SEVEN pending_ fields, NONE Vec<Vec>" — verified exact this cycle. The
817-LOC CSS builder is named the SK-V18 fold-DELETION target citing
tape/mod.rs:16-20, not a permanent surface. AP17-005 (StructRegistry
hot-path indirection) is the present-tense fence. AP17-001/004/006 hedge
un-proven absences with verify_actions. U-AP17-001 carries a
verify_action. No deferral. ACCEPT.

---

### §8 — 1f-past-corpora ledger + COH-014 flag + monotonicity + carrier enumeration → **ACCEPT**

The Do-Not-Redrive ledger resolves verbatim: SPEC :577/:825/:839
(REDRESS-53), :791-793 (AZ-IV + StructRegistry), :807-811
(second-substrate block), alphaC grep-zero. The V5 PC17-001
REDRESS-53 :578→:577 fold is clean against live grep. The
Do-Not-Carry-Undercount flag records the all-8 live census with
per-grammar line anchors; prior-totality COH-014 resolves at
`p1/1F-coherence-scan.md:87`. The Second-Substrate Carrier Enumeration
lists all 8 carrier field/doc lines. The skinny→totality
direction-monotonicity note is correct and load-bearing. ACCEPT.

---

## Cross-cutting CH6 observations

1. **One genuine residual defect found — the deferred CH1-V5-001 is a
   live non-executing citation, not a benign frontmatter string.** The V5
   consolidated declared it "non-load-bearing … recorded with a concrete
   fix" and carried it to T-P2/T-P3. On the ANTI-PAPER-CLOSE lens, a
   cited `wc -l` command that errors as written (exit 1, missing
   `collapsed_tape.rs`) is precisely the deferral CH6 must refuse. The
   fact it asserts is true, so this is **REVISE** (fold the citation),
   not REJECT. The fix is mechanical: enumerate the real filenames
   (`collapsed_stage.rs`, not `collapsed_tape.rs`) at `1b:12` and `1b:97`.

2. **No tape claim is paper-closed as "wired."** Every artefact states the
   crates/core tape is UNWIRED dead code (`TapeStructBuilder` grep-zero
   re-confirmed). The SK-V18 fold is named FUTURE with the proven skinny
   shape as source; the monotonic skinny→totality direction holds.

3. **The seven other sections re-resolved verbatim clean.** Scan census,
   AoS/SoA tape shapes, value-API absence/presence (+ `@generated`
   headers), BackendShape grep-zero, 960-site StructLayout, LayoutFacts
   grep-zero, REDRESS-53 named-lines, the alphabet/classifier surface, the
   CSS-builder seven-pending-field count, all eight scan call-site +
   OnceCell doc-comment lines — every load-bearing citation re-run live.

4. **The load-bearing dual-substrate divergence remains catalogued
   present-tense, not deferred.** U-COH17-002 states the Lock-1
   exactly-one-encoding closure with the offset-tape-vs-class-column
   distinction resolved at file:line; the forward disposition sits only
   in the verify_action column.

5. **Every UNKNOWN carries a verify_action; zero deferral-pattern hits in
   prose.** The "T-P2/T-P3" language is confined to
   reconciliation/verify_action columns. The single deferral CH6 charges
   is the §3 citation-command carry, recorded in the V5 consolidated
   itself, not a prose substitution.

## Disposition summary

| § | Subject | Disposition |
|---|---|---|
| 1 | 1d SK17L-001..010 + ledger | ACCEPT |
| 2 | 1a substrate SUB17-001..009 + firewall + UNKNOWNs | ACCEPT |
| 3 | 1b codegen BSHAPE17-001..009 + ledger + UNKNOWNs | **REVISE** — `1b:12` + `1b:97` cite a non-executing brace-glob `{…,collapsed}_tape.rs` (real file `collapsed_stage.rs`); the deferred CH1-V5-001, fact-true, citation-false |
| 4 | 1c runtime RT17-001..007 + census + UNKNOWNs | ACCEPT |
| 5 | 1e locks invariants + L01/02/10/14/16 + COH-014 + LACs | ACCEPT |
| 6 | 1f-coherence COH17-001..008 + Gaps + U-COH17-002 | ACCEPT |
| 7 | 1f-anti-pattern AP17-001..005 + CH5 verdict | ACCEPT |
| 8 | 1f-past-corpora ledger + COH-014 + monotonicity | ACCEPT |

**Counts:** 8 dispositioned · 7 ACCEPT · 1 REVISE · 0 REJECT · ACCEPT-rate 87.5%.

**Verdict:** seven of eight sections are correct + complete and re-resolve
verbatim at master `445925167` — the substrate / value-API / BackendShape
/ NEON spine is well-grounded with near-zero recalled LOC, and the V5
folds (1A `@generated`, 1E LayoutFacts grep-0 + REDRESS-53 :577/:825/:839,
1C/1F math.rs field-absence, 1f-past-corpora :578→:577) are each verified
clean. **§3 carries one genuine residual defect: the CH1-V5-001
non-executing brace-glob (`{…,collapsed}_tape.rs`, real file
`collapsed_stage.rs`) is still live in the 1b frontmatter `:12` and body
`:97`, deferred — not folded — by the V5 consolidated.** On the
ANTI-PAPER-CLOSE lens a cited evidence command that errors as written and
is parked for "T-P2/T-P3 carry" must be folded, not inherited; CH6 cannot
issue the second consecutive 100% while it stands. The fact it asserts
(17 LOC × 4 scaffolds + 270-LOC sink_only) is independently true, so the
defect is REVISE (fold the two citation strings to the real filenames),
not REJECT. CH6 returns **7/8 ACCEPT (87.5%)** — above the §3Z ≥95%? No:
87.5% < 95%. This confirming cycle does NOT clear the bar on the CH6 lens;
the lone REVISE is concrete, mechanical, and load-bearing only as
citation hygiene, but it blocks a clean CH6 ACCEPT until folded.
