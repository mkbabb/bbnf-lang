# BC.W5 - Cross-Repo Discipline

**Name**: W5 - Cross-Repo Discipline
**Opens after**: W4 - Audit-A TRANSPOSE Residue Absorption
**Agents**: 5 parallel
**Hard gate**: csp-solver canonical-source split refreshed (bbnf-lang in-tree vs csc411 sibling diff-clean); bbnf-regex relocated to parse-that sub-crate (`parse-that/rust/bbnf-regex/`); bbnf-lang consumes via path-dep; cross-repo bench parity; csc411 + bbnf-lang both run bench harness against canonical sources
**Status**: planned

## Scope

1. csp-solver canonical-source split refresh: bbnf-lang in-tree (at `crates/csp-solver/`) is bench-authoritative; csc411 sibling (at `/Users/mkbabb/Programming/csc411/`) is algorithm-evolution-authoritative. After BB's e-graph saturation work touched the cost extractor (which uses csp-solver internally), the two repos may have drifted. W5 verifies diff-clean and refreshes the canonical-source policy doc.
2. bbnf-regex sub-crate-of-parse-that resolution: relocate `crates/bbnf-regex/` to `parse-that/rust/bbnf-regex/`. bbnf-lang consumes via Cargo.toml path-dep. The relocation is per AZ-IV §Cross-Repo Future Work and `feedback_general-infra-crates`.
3. Cross-repo bench parity: csc411 + bbnf-lang both run their respective canonical-source bench harnesses. The bench rows align (where both repos benchmark the same algorithm).
4. Update workspace `Cargo.toml`, `.cargo/config.toml`, and CI workflows to reflect the new locations.
5. Document the post-W5 cross-repo topology in `docs/tranches/BC/audit/W5-cross-repo.md`.

## Triumvirate Dispatch

Dispatch a triumvirate if any of these occurs:

- the csp-solver diff between bbnf-lang in-tree and csc411 sibling is > 50 lines (the algorithm-evolution drift is non-trivial; route to triumvirate to decide whether csc411 is the authoritative source for the new state, or whether bbnf-lang's bench-authoritative copy stays as-is);
- the bbnf-regex relocation surfaces a parse-that submodule pin incompatibility (the parent repo's submodule pin must update; cross-repo coordination required);
- the cross-repo bench parity reveals that csc411's bench harness shape diverges from bbnf-lang's (alignment requires changing one harness; outside W5 file bounds);
- the relocation breaks a workspace lockfile invariant (`cargo deny` rule rejects the path-dep);
- three diagnostic-loop iterations fail to isolate the cross-repo divergence;
- JSONL transcript quiet >15 minutes;
- first-pass return with no commit and no evidence (after one verbatim redispatch).

## File Bounds

| File | Access |
|---|---|
| `crates/csp-solver/**` | modify-carve (refresh from csc411 if drift; diff-clean) |
| `/Users/mkbabb/Programming/csc411/**` | modify-carve (sibling refresh; csp-solver canonical content) |
| `crates/bbnf-regex/**` | delete (after relocation lands) |
| `/Users/mkbabb/Programming/parse-that/rust/bbnf-regex/**` | create (sub-crate location) |
| `Cargo.toml` | modify-carve (path-dep update) |
| `.cargo/config.toml` | modify-carve |
| `parse-that/Cargo.toml` | modify-carve (sub-crate registration) |
| `docs/tranches/BC/audit/W5-*` | create |

Do NOT touch: AUDIT-B splits (W1 owns); fixture contract (W2 owns); samply CI (W3 owns); Audit-A residue (W4 owns); BD-bound TS/WASM work.

## Disjointness

W5.1 (csp-solver diff + refresh) writes `crates/csp-solver/**` + `csc411/**`. W5.2 (bbnf-regex relocation) writes `crates/bbnf-regex/**` (delete) + `parse-that/rust/bbnf-regex/**` (create). W5.3 (workspace path-dep update) writes `Cargo.toml`, `.cargo/config.toml`, `parse-that/Cargo.toml`. W5.4 (cross-repo bench parity) writes bench harness alignment evidence. W5.5 (audit doc) writes `docs/tranches/BC/audit/W5-*`. The five sub-units have disjoint primary write paths.

## Worktree Plan

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| BC.W5.1 csp-solver Refresh | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-csp` | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-csp/target/w5-csp` |
| BC.W5.2 bbnf-regex Relocation | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-rgx` | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-rgx/target/w5-rgx` |
| BC.W5.3 Workspace Path-Dep | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-pd` | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-pd/target/w5-pd` |
| BC.W5.4 Cross-Repo Bench Parity | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-bench` | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-bench/target/w5-bench` |
| BC.W5.5 Cross-Repo Audit | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-doc` | `/Users/mkbabb/Programming/bbnf-wt-bc-w5-doc/target/w5-doc` |

## Agent Units

### BC.W5.1 csp-solver Refresh

- Mechanism: diff `crates/csp-solver/` against `/Users/mkbabb/Programming/csc411/` canonical source. Refresh whichever side is stale per the canonical-source policy. Document the diff resolution.
- Files: `crates/csp-solver/**`, `/Users/mkbabb/Programming/csc411/**` (sibling).
- Sub-gate: diff-clean; canonical-source policy honoured.

### BC.W5.2 bbnf-regex Relocation

- Mechanism: move `crates/bbnf-regex/` content to `parse-that/rust/bbnf-regex/`. Delete the bbnf-lang in-tree copy. Update parse-that's Cargo workspace to register the new sub-crate.
- Files: `crates/bbnf-regex/**` (delete), `parse-that/rust/bbnf-regex/**` (create), `parse-that/Cargo.toml` (modify-carve).
- Sub-gate: relocation lands; parse-that builds; bbnf-lang's path-dep resolves.

### BC.W5.3 Workspace Path-Dep

- Mechanism: bbnf-lang's `Cargo.toml` consumes `bbnf-regex` via path-dep at `parse-that/rust/bbnf-regex/`. Update `.cargo/config.toml` if needed.
- Files: `Cargo.toml`, `.cargo/config.toml`.
- Sub-gate: `cargo build` GREEN; `cargo nextest run --workspace --cargo-profile ax-iter` 100% pass.

### BC.W5.4 Cross-Repo Bench Parity

- Mechanism: run csc411's bench harness against its csp-solver canonical source; run bbnf-lang's bench harness against the in-tree (bench-authoritative) copy. The rows align where both repos benchmark the same algorithm.
- Files: bench evidence in `docs/tranches/BC/audit/W5-bench-parity.md`.
- Sub-gate: parity verified.

### BC.W5.5 Cross-Repo Audit

- Mechanism: document the post-W5 cross-repo topology in `docs/tranches/BC/audit/W5-cross-repo.md`. Update `docs/codegen-paths.md` for the new bbnf-regex location.
- Files: `docs/tranches/BC/audit/W5-cross-repo.md`, `docs/codegen-paths.md` (modify-carve).
- Sub-gate: docs land.

## Hard Gate

1. csp-solver bbnf-lang in-tree vs csc411 sibling diff-clean.
2. bbnf-regex relocated to `parse-that/rust/bbnf-regex/`; bbnf-lang consumes via path-dep.
3. Cross-repo bench parity verified.
4. `cargo build` GREEN; `cargo nextest run --workspace --cargo-profile ax-iter` 100% pass.
5. `cargo xtask regen --check` 9/9 green.
6. `crates/ir/tests/substrate_audit.rs` GREEN.
7. `cargo fmt --all -- --check`, focused `cargo clippy --profile ax-iter`, and `git diff --check` pass.

## Format And Lint Cadence

`cargo fmt --all -- --check`, focused `cargo clippy --profile ax-iter`, full nextest at integration, `git diff --check`.

## Verification Artefacts

- `docs/tranches/BC/audit/W5-cross-repo.md`
- `docs/tranches/BC/audit/W5-bench-parity.md`
- `docs/tranches/BC/audit/W5-csp-diff.txt`

## Commit Plan

Expected scopes: `crates/csp-solver-refresh`, `crates/bbnf-regex-delete`, `parse-that/rust/bbnf-regex-create`, `Cargo/path-dep-bbnf-regex`, `docs/tranches/BC/W5-audit`. Commit bodies required for the relocation commit (rationale + cross-repo coordination) and the W5 close status commit.

Every dispatch carries `HARD CAP: N min. At 0.9N commit, at N halt.`

## Dependencies

- **Depends on**: W4 (Audit-A residue closed; cross-repo work doesn't compete).
- **Blocks**: W6 (close cites cross-repo discipline as evidence).
