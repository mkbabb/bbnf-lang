# BB.W0 — Sister Crate Emigration (Backbone; Split Into W0a + W0b)

**Note**: W0 splits into BB.W0a (sister-crate path-deps + workspace metadata) and BB.W0b (same-wave consumer minimal optimiser smoke pass) per BB02-1 of `audit/HARDENING-PLAN-2026-05-03-02-sequencing-discipline.md:39-41`. See `docs/tranches/BB/waves/W0a.md` and `docs/tranches/BB/waves/W0b.md` for executable sub-wave specs. This document is the backbone reference.

**Thesis** Hereupon five sister crates emigrate to path-dep status so the optimiser pipeline that BB.W3c lands has a path-dep boundary at every seam, per Lock 11. **Closer-gate** `cargo check --workspace` green; path-deps resolve; `Cargo.toml` declares no in-tree workspace-member duplicates; no `crates/ir/src/rewrites/rank.rs` or `tiering.rs` files exist (rank/tier rewrites land at BB.W3c with consumer per `audit/HARDENING-PLAN-SYNTHESIS-2026-05-03.md:118-125`).

## §1 Deliverable

Five crates emigrate from in-tree workspace members to path-dep'd incubators per Lock 11 of `docs/HARDENING-PLAN-PROMPT.md:54`: `crates/egraph/`, `crates/egraph-derive/`, `crates/csp-solver/`, the regex crate at `parse-that/rust/regex` (renamed to `parse-that/rust/bbnf-regex` per the endpoint reconciliation in `audit/HARDENING-SYNTHESIS-2026-05-03.md:166-175`), and `parse-that` itself. Each is documented as "incubating" in workspace metadata. simd-scan + bootstrap + analysis + lsp stay workspace-internal because they are bbnf-specific by construction (per `audit/CENSUS-2026-05-03.md:124-141`, the analysis crate is BBNF-grammar-specific by design).

The csp-solver crate at 532 LOC in lib.rs (`audit/MODULES-2026-05-03.md:81`) is a god-module per Lock 13; W0 does NOT split it (that landed in BA.W2's god-module sweep) but does record the path-dep relocation. The egraph crate at 12 files totalling ~1,800 LOC (`audit/MODULES-2026-05-03.md:136-152`) emigrates as a unit alongside egraph-derive (343 LOC `lib.rs`, `audit/MODULES-2026-05-03.md:158`). The regex endpoint reconciliation is precondition: per `audit/HARDENING-SYNTHESIS-2026-05-03.md:170-175`, the BC.W5 endpoint pre-flight commands check that exactly one of `parse-that/rust/regex` or `parse-that/rust/bbnf-regex` exists; W0 chooses the bbnf-regex name and renames the parse-that workspace member accordingly.

Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:107-115` and the amendment block at lines 118-127, **rank and tier rewrites are NOT created in W0**. The previously-flagged BB.W0 substrate-forward concern (rank.rs + tiering.rs without consumer) is resolved by moving creation INTO BB.W3 alongside the consumer. W0's deliverable EXCLUDES `crates/ir/src/rewrites/rank.rs` and `crates/ir/src/rewrites/tiering.rs` — both files must NOT exist at W0 close. The W3 dispatch consumes them in the same wave they are created. W0 thereby honours the Era V abrogation per `docs/tranches/meta-audit/archaeology/era-V-dta-psi-rut.md:8-12`.

W0 also lands the workspace metadata table that records each path-dep's canonical endpoint, so BB.W3's optimiser pipeline can reference each sister crate by `&str` identifier through `[workspace.metadata.bbnf-incubators]`. Estimated edit surface: `Cargo.toml` workspace `members` array shrinks by ~5 entries; per-incubator `[patch.crates-io]` block lands; ~50 LOC of metadata.

The five emigrated crates each carry a precise role at the BB.W3 optimiser pipeline:

| Sister crate | Role at BB.W3 | Anchor file |
|---|---|---|
| `crates/csp-solver/` | Type inference (CSP-solved over `bbnf-ir`'s payload-coverage facts) + strategy selection (branch-and-bound over CallStrategy / AltStrategy / WrapMode) | `audit/MODULES-2026-05-03.md:81-122` |
| `crates/egraph/` | E-graph saturation (rewrite rules applied to the grammar IR's hash-consed DAG) + cost-model extraction | `audit/MODULES-2026-05-03.md:136-152` |
| `crates/egraph-derive/` | The `#[derive(Language)]` proc-macro auto-detecting `Id` / `Vec<Id>` / `Box<[Id]>` / `[Id; N]` fields per `audit/MODULES-2026-05-03.md:158` | same |
| `parse-that/rust/bbnf-regex/` | Bespoke HIR replacing regex-syntax; the explicit-negated-flag, hand-written parser per the canon at `feedback_bespoke_regex` | `audit/MODULES-2026-05-03.md:166-175` |
| `parse-that` (root) | The combinator + adapter substrate that all five sister crates load through | same |

The path-dep relocation does NOT change the public API; downstream consumers in `bbnf-ir`, `bbnf`, `bbnf-analysis`, and `bbnf-path` continue to import via `use egraph::*` etc. The change is purely structural — the workspace `members` array shrinks by 5 entries, and the workspace `[patch.crates-io]` table re-routes each name to its emigrated path.

The "incubating" status in `[workspace.metadata.bbnf-incubators]` records the BC.W4 freeze target per `audit/HARDENING-SYNTHESIS-2026-05-03.md:166-175`. At BC.W4 close, each incubator candidates for crates.io publication; the metadata records the API freeze date; the workspace continues path-dep'ing until the publication lands. This is the substrate-without-consumer mitigation by design: the consumer (BB.W3 + BC.W4) is documented in workspace metadata, not deferred to a future tranche.

The pre-W0 vs post-W0 workspace topology:

| Pre-W0 (BA close) | Post-W0 |
|---|---|
| `crates/egraph/` (workspace member) | path-dep at `crates/egraph/` (NOT workspace member) |
| `crates/egraph-derive/` (workspace member) | path-dep at `crates/egraph-derive/` (NOT workspace member) |
| `crates/csp-solver/` (workspace member) | path-dep at `crates/csp-solver/` (NOT workspace member) |
| `parse-that/rust/regex/` (path-dep, in parse-that workspace) | `parse-that/rust/bbnf-regex/` (renamed; path-dep, in parse-that workspace) |
| `parse-that/` (root crate, path-dep) | `parse-that/` (path-dep, unchanged path; metadata records as incubator) |
| `crates/{simd-scan,bootstrap,analysis,lsp}/` (workspace members) | `crates/{simd-scan,bootstrap,analysis,lsp}/` (workspace members; unchanged) |
| `crates/{core,ir,bbnf-path,bbnf-path-ts}/` (workspace members) | `crates/{core,ir,path,path-core,path-ts}/` (workspace members; renamed at BA.W3 carry BA→BB.C4) |

The `bbnf-path` → `path` and `bbnf-path-ts` → `path-ts` renames happened at BA.W3 (per `docs/tranches/BA/BA.md:33`); BB.W0 inherits the renamed crates via carry BA→BB.C4 and does not re-rename. The `path-core` crate exists at BA.W3 close.

## §2 Milestones

| ID | Surface | Action | Gate | Exit-criteria |
|---|---|---|---|---|
| M0 | `Cargo.toml` workspace | Strike `crates/egraph`, `crates/egraph-derive`, `crates/csp-solver` from workspace `members`; add path-dep entries under `[workspace.dependencies]` | `cargo metadata --format-version 1 \| jq '.workspace_members \| length'` decreases by 3 | The three names disappear from the `workspace_members` array; `cargo check --workspace` succeeds. |
| M1 | `parse-that/rust/regex/` | Rename to `parse-that/rust/bbnf-regex/`; update parse-that workspace `members`; verify path-dep root path matches per `audit/HARDENING-SYNTHESIS-2026-05-03.md:170-175` | `test -d /Users/mkbabb/Programming/parse-that/rust/bbnf-regex && test ! -d /Users/mkbabb/Programming/parse-that/rust/regex` | The endpoint reconciliation pre-flight from `audit/HARDENING-SYNTHESIS-2026-05-03.md:170-175` passes verbatim. |
| M2 | Workspace metadata | Add `[workspace.metadata.bbnf-incubators]` block listing each path-dep with its canonical-path + incubation-status. Each entry: `{ path = "...", status = "incubating", api_freeze_target = "BC.W4" }` | `rg -n 'bbnf-incubators' Cargo.toml` returns exactly one block; `cargo metadata` parses cleanly | The metadata block lands; downstream consumers (BB.W3 optimiser pipeline) read each sister crate by metadata key, not by hardcoded path. |
| M3 | Workspace verification | Run `cargo check --workspace` + `cargo nextest run -p bbnf-ir -p bbnf` to confirm path-deps resolve identically to in-tree builds | `cargo check --workspace 2>&1 \| grep -E 'error\|warning: unused' \| wc -l` returns 0 | Green workspace check; nextest pass-rate matches BA close baseline (no regression). |
| M4 | Substrate-audit guard | Verify no `crates/ir/src/rewrites/rank.rs` and no `crates/ir/src/rewrites/tiering.rs` exist; the W0 commit body asserts the absence per `audit/HARDENING-SYNTHESIS-2026-05-03.md:107-115` | `test ! -f crates/ir/src/rewrites/rank.rs && test ! -f crates/ir/src/rewrites/tiering.rs` | The two files do not exist; W3 will create them with the consumer in the same wave. |
| M5 | Worktree fixture preflight | Verify `xtask worktree-init` materialises `data/{json,css,bbnf,sheets}` per BA.W0's contract (`docs/tranches/BA/waves/W0.md` per `audit/HARDENING-SYNTHESIS-2026-05-03.md:71-78`); BB.W0 does not extend the contract — it is preflight only | `xtask worktree-init --check` runs cleanly | The BA-installed fixture symlink contract holds; no fleet-wide fixture changes land here (BC.W3 owns the fleet-wide post-BB closure). |
| M6 | Generated-LOC baseline | Capture pre-W0 LOC for every `crates/core/src/grammar/generated/<g>.rs`; record in `docs/tranches/BB/audit/W0-generated-baseline.md` per `audit/HARDENING-SYNTHESIS-2026-05-03.md:90-94` | `find crates/core/src/grammar/generated -name '*.rs' -exec wc -l {} +` matches the BB.md table |  The baseline lands so BB.W1, W2, W4 can compute deltas. |
| M7 | Sister-crate API surface inventory | For each emigrated crate, capture pre-W0 public API surface (`pub fn`, `pub struct`, `pub enum`, `pub trait`); record in `docs/tranches/BB/audit/W0-sister-api-surface.md`; downstream waves (BB.W3, BC.W4) verify the surface is unchanged through their work | `cargo doc -p egraph -p egraph-derive -p csp-solver` produces output that matches recorded surface | API stability through path-dep relocation; BC.W4 freeze gate has the baseline. |
| M8 | Workspace `[patch.crates-io]` table | Add explicit patch entries for each emigrated sister crate; the patch routes `egraph = "..."` references from crates.io fallback to path-dep | `cargo tree -p bbnf-ir` shows path-dep'd egraph, not crates.io | The patch table works; downstream cargo invocations resolve cleanly through the workspace patch. |

## §3 Closer gate

Per the wave-summary closer-gate column in `docs/tranches/BB/BB.md`:

```sh
cargo check --workspace                                                    # green
cargo metadata --format-version 1 | jq '.workspace_members | length'        # decreases by 3 from BA close
test ! -f crates/ir/src/rewrites/rank.rs                                   # absent
test ! -f crates/ir/src/rewrites/tiering.rs                                # absent
test -d /Users/mkbabb/Programming/parse-that/rust/bbnf-regex               # endpoint chosen
test ! -d /Users/mkbabb/Programming/parse-that/rust/regex                  # old endpoint gone
rg -n 'bbnf-incubators' Cargo.toml | wc -l                                 # exactly one block
cargo nextest run -p bbnf-ir -p bbnf -p bbnf-analysis                      # 100% pass for BB-owned surfaces
```

All eight conditions must pass; any failure halts W1 dispatch.

## §4 Invariants

§I1. **Lock 11** (`docs/HARDENING-PLAN-PROMPT.md:54`) — five sister crates emigrate to path-dep status; simd-scan + bootstrap + analysis + lsp stay workspace-internal. Wave-specific contribution: `[workspace.metadata.bbnf-incubators]` records each canonical endpoint; downstream waves reference incubators by metadata key.

§I2. **Lock 4** (`docs/HARDENING-PLAN-PROMPT.md:40`) precursor — each optimiser sub-system (CSP, e-graph, miners, cost model) is a path-dep boundary, ready for BB.W3's output-piping. Wave-specific contribution: the path-dep relocation is the structural seam W3 consumes.

§I3. **Lock 13** (`docs/HARDENING-PLAN-PROMPT.md:58`) — sister-crate emigration removes ~3,000 LOC from the workspace cargo-check surface; god-modules at the in-tree level (csp-solver lib.rs at 532 LOC) become external responsibilities under their own crate's BC.W4 freeze, not BB-wave debt.

§I4. **Lock 12** (precondition; closed pre-BA) — verifies `archive/{ser,gorgeous}/` exist; ser+gorgeous emigration ALREADY happened per `docs/tranches/BA/BA.md:37-49`. BB.W0 does not re-archive; the pre-BA ceremony is upstream.

§I5. **Era V abrogation** (`docs/tranches/meta-audit/archaeology/era-V-dta-psi-rut.md:8-12`) — rank.rs + tiering.rs are NOT created in this wave; they land in W3 with same-wave consumer per the amendment in `audit/HARDENING-SYNTHESIS-2026-05-03.md:107-127`. Wave-specific contribution: the absence is gate-checked at M4.

## §5 Risks

| Risk | Likelihood | Detection | Mitigation |
|---|---|---|---|
| Path-dep resolution fails because the relative path from `crates/core` to a sister crate changes during emigration | Medium | `cargo check --workspace` errors with "no matching package found" | Use `path = "../../../parse-that/rust/bbnf-regex"` resolved relative to `bbnf-lang`'s root, not relative to each crate's `Cargo.toml`; verify with `cargo metadata`. |
| The endpoint reconciliation rename of `parse-that/rust/regex` → `bbnf-regex` breaks parse-that workspace at the parse-that side | Medium | `cd /Users/mkbabb/Programming/parse-that && cargo check` errors | Update parse-that's workspace `members` array in the same atomic commit as the rename; verify both bbnf-lang and parse-that compile. |
| The BB.W0 commit body fails to assert the absence of rank.rs / tiering.rs and the substrate-first concern resurfaces at W3 | Low | substrate_audit fails with "rank.rs found in W0 commit but no consumer in same wave" | M4 exit-criteria is a literal `test ! -f` shell predicate; the commit body must include the M4 verification command output. |
| Sister crates have transitive deps (e.g., egraph depends on bbnf-regex via path-dep) that don't resolve through the workspace patch table | Low | `cargo update --dry-run` flags incompatible-version errors | Each sister crate's `Cargo.toml` references workspace deps via `bbnf-regex.workspace = true`; the workspace `[patch.crates-io]` table resolves all sister crates uniformly. |
| The BB.W0 commit body is too verbose because of the metadata + verification artefacts | Low | Commit message exceeds 100 lines | Per voice lock §V3: state the deliverable, state the gate, move on; the commit body is structured: (1) one-line summary; (2) per-milestone gate-result table; (3) M6 generated-LOC baseline table; (4) closer-gate verification output. Total commit body ≤ 60 lines. |

## §6 Cross-references

- **BB-G gates this wave is on the path to closing**: BB-G10 (optimiser composition output-piped); BB-G11 (generated-LOC baseline). Closure proper lands at BB.W3 (BB-G10) and BB.W6 (BB-G11).
- **Carry-tags this wave consumes**: BA→BB.C5 — grammar-agnostic `bbnf-ir` substrate so sister crate emigration does not break grammar-specific imports (per `docs/tranches/BA/BA.md:62`).
- **Carry-tags this wave produces**: BB→BC.C4 — sister crates as path-deps, ready for BC.W4 freeze and crates.io candidacy.
- **Preceding wave dependency**: BA.W6 — BA close verified; pre-W0 baseline LOC table emitted; substrate_audit green.
- **Following wave consumer**: BB.W1 — direct-to-struct generalisation consumes the path-dep'd sister crates (especially bbnf-regex for grammar-specific regex compilation through the typed alphabet).

The data flow from W0 to consumer waves:

| W0 deliverable | Consumed by |
|---|---|
| Path-dep'd egraph + egraph-derive | BB.W3 (e-graph saturation step [11]); BC.W4 (API freeze candidate) |
| Path-dep'd csp-solver | BB.W3 (CSP type inference step [8] + CSP strategy selection step [12]); BC.W4 (API freeze candidate) |
| Path-dep'd bbnf-regex | BB.W1 (grammar-specific regex compilation via typed alphabet); BB.W3 (regex engine selection per cost model); BC.W4 + BC.W5 (endpoint reconciliation closure per `audit/HARDENING-SYNTHESIS-2026-05-03.md:166-175`) |
| Path-dep'd parse-that | BB.W3 (combinator substrate for IR composition); BC.W4 (API freeze candidate) |
| Workspace `[bbnf-incubators]` metadata | BB.W3 (each optimiser stage references its incubator by metadata key); BC.W4 (freeze planning) |
| Pre-W0 generated-LOC baseline | BB.W1, W2, W4, W5 (compute deltas); BB.W6 (close aggregate) |
| Sister-crate API surface inventory | BC.W4 (verify API stability through path-dep work); BC.W4 publication readiness |
| Rank/tier absence verification | BB.W3 (verify the abrogation held until same-wave consumer landed at W3) |

## §7 Iter-time check

Per `docs/HARDENING-PLAN-PROMPT.md:120-123` Lane 06 budget enforcement: each cargo invocation respects the iter-time gate of `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`.

| Cargo Command | Expected Duration | Pass-Rate Target | Notes |
|---|---|---|---|
| `cargo check --workspace --profile ax-iter` | ≤ 18 s on M1 Pro | n/a | Per `feedback_iter_profile_always`; bare `cargo check` heavy-surface on workspace size. |
| `cargo nextest run -p bbnf-ir --profile ax-iter` | ≤ 35 s | 100% | bbnf-ir is the smallest BB-touched test surface. |
| `cargo metadata --format-version 1` | ≤ 2 s | n/a | Workspace metadata sanity. |
| `xtask regen --check` (BA close baseline) | ≤ 30 s; W0 holds | n/a | Per BA-G3 from `docs/tranches/BA/BA.md:17`; BB target at close is ≤ 25 s but W0 does not regress against BA. |
| `cargo nextest run -p bbnf -p bbnf-analysis --profile ax-iter` | ≤ 90 s | 100% for BB-owned surfaces | Verifies path-dep relocation does not regress test pass-rate. |
| `cargo build --workspace --profile ax-iter` | ≤ 60 s | n/a | Confirms workspace links cleanly through path-deps. |

The W0 close commit body MUST include the actual measured times for each row above; per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`, regression beyond +20% blocks the wave.

## §8 Verification artefacts

Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:128-136`, every BB wave emits per-wave audit artefacts the close commit body cites. W0's artefacts:

| Artefact | Path | Purpose | Consumer |
|---|---|---|---|
| `W0-toolchain-baseline.md` | `docs/tranches/BB/audit/` | Records `cargo xtask regen --check`, full `cargo nextest`, `cargo bench` baseline times | All later BB waves cite for regression-detection |
| `W0-generated-baseline.md` | same | Per-grammar pre-W0 LOC; the start of the BB-G11 budget table | BB.W1, W2, W4 compute deltas from this baseline |
| `W0-sister-api-surface.md` | same | Pre-W0 public API of each emigrated crate (egraph, csp-solver, bbnf-regex) | BC.W4 freeze gate verifies API didn't drift |
| `W0-incubator-metadata.md` | same | The `[workspace.metadata.bbnf-incubators]` block snapshot + each entry's freeze target | BC.W4 reads the metadata to plan crates.io publication |
| `W0-rank-tier-absence.md` | same | Verification that `crates/ir/src/rewrites/rank.rs` and `tiering.rs` do NOT exist; the M4 gate's evidence | BB.W3 verifies the W0 abrogation held until same-wave consumer landed |

Each artefact is read-only after wave close; subsequent waves cite by path:line. Per `feedback_no_metalanguage_docs`, no artefact references commits, conversation history, or the plan's draft history; all citations are path:line to source files.

## §9 Audit lane forecast

Per `docs/HARDENING-PLAN-PROMPT.md:60-153` Phase 3 hardening lanes, W0 anticipates challenges from each lane:

| Lane | Anticipated challenge | W0 response |
|---|---|---|
| Lane 1 (Lock-Adherence) | "BB.W0 substrate-forward skeletons" per `audit/HARDENING-SYNTHESIS-2026-05-03.md:107-115` | M4 explicitly verifies absence of rank.rs / tiering.rs; the W0 commit body asserts the absence; the artefact `W0-rank-tier-absence.md` records the verification |
| Lane 2 (Sequencing-Discipline) | "BB.W0 produces sister-crate emigration; consumer is BB.W3 — multi-wave gap" | Same-wave consumer is workspace cargo check (`cargo check --workspace`); the path-dep relocation is itself the gate; W3's optimiser pipeline consumption is a strict next-wave consumer; per `docs/tranches/PLAN-INPUT-2026-05-03.md:289`, the verdict is OK |
| Lane 4 (SOTA-Anchoring) | W0 has no perf gate (none required) | The wave-summary closer-gate cites `cargo check --workspace` + path-dep verification; perf gates trajectory at W3 + W6 |
| Lane 5 (Grammar-Authoritative) | "Does W0 introduce per-grammar leaks in supposedly-generic crates?" | The path-dep relocation does NOT introduce per-grammar code; the workspace metadata `[workspace.metadata.bbnf-incubators]` enumerates incubators by crate name, not by grammar; the metadata follows the BA.W1 grammar-agnostic pattern (carry BA→BB.C5) |
| Lane 6 (Generated-Code Budget) | "Is W0 silent on generated-code impact?" | M6 captures the pre-W0 baseline; W0 itself has no generated-code delta (it's a workspace structural change, not a regen change); the artefact `W0-generated-baseline.md` is the BB-G11 input |
| Lane 7 (Friction-Forecast) | W0 has no API surface friction (it's a workspace-internal change) | n/a |
| Lane 8 (Carry-Audit) | "Are BB→BC.C4 carries explicit?" | The artefact `W0-incubator-metadata.md` records each incubator's BC.W4 freeze target; the metadata is the carry's source-of-truth |
| Lane 9 (Encapsulation-Audit) | "Does W0 violate cohesive encapsulation?" | The path-dep relocation enforces cohesion (each sister crate is a path-dep boundary); Lock 13 honoured by mechanism |

## §10 Implementation reference

The workspace `Cargo.toml` shape post-W0:

```toml
# Cargo.toml (workspace root, post-W0)
[workspace]
members = [
    "crates/core",
    "crates/ir",
    "crates/path",
    "crates/path-core",
    "crates/path-ts",
    "crates/simd-scan",
    "crates/bootstrap",
    "crates/analysis",
    "crates/lsp",
    "xtask",
    # NOT members: crates/egraph, crates/egraph-derive, crates/csp-solver
]
resolver = "2"

[workspace.dependencies]
# Path-dep'd incubators (per Lock 11)
egraph = { path = "crates/egraph" }
egraph-derive = { path = "crates/egraph-derive" }
csp-solver = { path = "crates/csp-solver" }
bbnf-regex = { path = "../parse-that/rust/bbnf-regex" }
parse-that = { path = "../parse-that" }

# Workspace-internal
bbnf = { path = "crates/core" }
bbnf-ir = { path = "crates/ir" }
bbnf-path = { path = "crates/path" }
bbnf-path-core = { path = "crates/path-core" }
bbnf-path-ts = { path = "crates/path-ts" }
simd-scan = { path = "crates/simd-scan" }
bbnf-bootstrap = { path = "crates/bootstrap" }
bbnf-analysis = { path = "crates/analysis" }
bbnf-lsp = { path = "crates/lsp" }

[workspace.metadata.bbnf-incubators]
egraph = { path = "crates/egraph", status = "incubating", api_freeze_target = "BC.W4" }
egraph-derive = { path = "crates/egraph-derive", status = "incubating", api_freeze_target = "BC.W4" }
csp-solver = { path = "crates/csp-solver", status = "incubating", api_freeze_target = "BC.W4" }
bbnf-regex = { path = "../parse-that/rust/bbnf-regex", status = "incubating", api_freeze_target = "BC.W4" }
parse-that = { path = "../parse-that", status = "incubating", api_freeze_target = "BC.W4" }

[patch.crates-io]
egraph = { path = "crates/egraph" }
egraph-derive = { path = "crates/egraph-derive" }
csp-solver = { path = "crates/csp-solver" }
bbnf-regex = { path = "../parse-that/rust/bbnf-regex" }
parse-that = { path = "../parse-that" }
```

The `[workspace.metadata.bbnf-incubators]` block is a workspace-level metadata table that Cargo accepts as opaque vendor metadata; downstream xtask invocations read it to identify each incubator's path + freeze target. The `[patch.crates-io]` table re-routes any `crate.io` reference to the path-dep; this is the standard cargo pattern for pre-publication crates.
