# S-P3 V1 CH2 Generality

Disposition: ACCEPT.

The SPEC carries a Lock 14/generality table for generic and generated edits.
Primitive candidates are byte, mask, offset, carry, generated-policy, or sealed
tape operations. CSS provider work is CSS-owned until generic surfaces are
touched; generic claims require CSS plus Sheets or BBNF-self receiver proof or
an intrinsic block.
