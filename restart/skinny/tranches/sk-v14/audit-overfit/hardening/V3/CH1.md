# CH1 — CORRECTNESS lens disposition (SK-V14 S-P0 Overfit Audit V3)

Authority: `restart/prompts/ORCHESTRATOR.md §3W CH1` + dispatch
`restart/skinny/tranches/sk-v14/audit-overfit/hardening/V1/CHALLENGE-
CONTEXT.md` §3 + V2 overlay `…/V2/CHALLENGE-V2-ADDENDUM.md` §1 + V3
micro-fold commit `007624849` (3 files / +10 / −6) which folds the
two V2 below-threshold carry-overs CH1 flagged at `…/V2/CH1.md §5`
plus three SYNTHESIS coherence packets (`F-V3-SYNTHESIS-{1,2,3}`).

V2 disposition (`…/V2/CH1.md`, 100 % substantive ACCEPT with two
hygiene carry-overs):

1. SYNTHESIS §1.1 line 81 NEW-2 cell still read "3 of 7 CSS scanners"
   while §1.2 enumeration was already correctly "Four of the seven".
2. A4 §1 methodology line 153 read "(full, 101 lines)" while `wc -l
   skinny/crates/codegen/src/json_provider.rs` returns 100.

V3 micro-fold packets (per `007624849` commit body, re-quoted here so
the disposition is self-contained):

- F-V3-A4-1 — A4 §1:153 "101 lines" → "100 lines".
- F-V3-A5-1 — A5 §5:133 LOW-finding action-class "no-op pre-C-4" →
  "preserved through PRUNE-5 as a gate-rejection invariant inside
  C-4 entry-gates so any admit attempting to cite W8 / W9 pre-
  runtime-consumer is denied at admit time".
- F-V3-SYNTHESIS-1 — SYNTHESIS §3.1 C-4 row A5 LOW cell "no-op
  pre-C-4" → "preserved through PRUNE-5 as C-4 entry-gate invariant".
- F-V3-SYNTHESIS-2 — SYNTHESIS §2.4 "8 codegen-side template+provider
  files" → "14 codegen-side files (8 providers+templates + 6
  ancillary)" per `git grep -l '@generated by skinny bbnf-codegen'
  skinny/crates/codegen/src/ | wc -l = 14`.
- F-V3-SYNTHESIS-3 — SYNTHESIS §1.1:81 NEW-2 cell "3 of 7" → "4 of 7".

A1, A2, A3, A6 STAND verbatim under V3.

## §0 — V3 Verdict + disposition summary

**Verdict: ACCEPT (100 %).** Both V2 CH1 carry-overs landed at the
exact line-coordinates V2 prescribed; the three V3 SYNTHESIS coherence
folds are all source-verified; A1 / A2 / A3 / A6 byte-identity from V2
preserved; no fresh CH1-grade defect surfaced under the V3 confirming
pass.

| Disposition | Count | Notes |
| --- | ---: | --- |
| ACCEPT | 7 of 7 artefacts | A1 / A2 / A3 / A6 STAND verbatim (zero V3 diff verified by `git diff 1735882a5..007624849`); A4 V3 F-V3-A4-1 lands at line 153 ("100 lines"); A5 V3 F-V3-A5-1 lands at §5:133 (action-class propagation); SYNTHESIS V3 F-V3-SYNTHESIS-{1,2,3} all three land at the targeted sites (§3.1:343, §2.4:311-315, §1.1:81). |
| REVISE | 0 | No residual carry-overs; both V2 hygiene flags cleared. |
| REJECT | 0 | No source-side factual error in any V3 fold. |

ACCEPT-rate: **7/7 artefacts = 100 %**. V3 forecast in commit body
("expected 7 × 100 % = 114/114 with zero orphans") confirmed for CH1.

## §1 — V2 CH1 carry-over confirming pass

### §1.1 V2 CH1 §5 item 1 (SYNTHESIS §1.1:81 "3 of 7" → "4 of 7") — RESOLVED

Executable verification:

`grep -n "3 of 7\|4 of 7" restart/skinny/tranches/sk-v14/audit-
overfit/SYNTHESIS-AUDIT-OVERFIT.md` returns line 81 only:

```
81:| A4 | 4 | 12 | NEW-1 (…); NEW-2 (4 of 7 CSS scanners short-circuit
   on `CANONICAL_FIXTURE`/`CAPTURED_W2_INPUT` byte-equality → fixture-
   lookup tables dressed as parsers); NEW-3 (…); plus 9 quantification
   extensions of V13 findings |
```

The old "3 of 7" wording is gone from the SYNTHESIS file entirely (no
other line carries it). §1.2:122 enumeration "**Four** of the seven
CSS L4 template generators" already aligned in V2 and remains aligned
in V3:

`grep -n "Four\|Three" restart/skinny/tranches/sk-v14/audit-overfit/
SYNTHESIS-AUDIT-OVERFIT.md | head` returns line 122 ("Four") and line
505 (an unrelated "Three architectural sequencing constraints"
sentence about the S-P3 plan).

**Disposition**: RESOLVED. §1.1 in-table NEW cluster cell, §1.2 prose
enumeration, §0.1 NEW column count (12), §5.1 closing, and A4 §0
narrative all now read the "Four / 4 of 7" framing consistently.

### §1.2 V2 CH1 §5 item 2 (A4 §1:153 "101 lines" → "100 lines") — RESOLVED

Executable verification:

`wc -l skinny/crates/codegen/src/json_provider.rs` returns **100**.

`grep -n "json_provider.rs" restart/skinny/tranches/sk-v14/audit-
overfit/sk-v14-audit-overfit-generator-truth.md`:

```
153:- `skinny/crates/codegen/src/json_provider.rs` (full, 100 lines).
211:| 10 | HIGH | … `skinny/crates/codegen/src/json_provider.rs:60-78`
     | NEW |
```

Methodology line 153 now reads "(full, 100 lines)" matching `wc -l`
ground-truth. The A4 ledger row 10 line-range `:60-78` remains intact
(verified in V2 CH1 §1.2 via `sed -n '60p;64p;68p;72p;48p;80,100p'`;
the V3 fold touches only line 153 of the methodology and leaves the
ledger coordinates undisturbed).

**Disposition**: RESOLVED. The off-by-one between methodology summary
and actual file is closed; A4 V3 is now internally consistent across
methodology, ledger rows 8 / 10 / 11, and source coordinates.

## §2 — V3 fresh-fold verification (F-V3-A5-1, F-V3-SYNTHESIS-{1,2,3})

### §2.1 F-V3-A5-1 — A5 §5:133 action-class propagation

Executable verification:

`grep -n "no-op pre-C-4\|preserved through PRUNE-5\|gate-rejection
invariant" restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-
overfit-decision-engine.md`:

```
118:| **Preserve the resolver's honest self-labelling** (…) | … |
     LOW | Preserve through PRUNE-5; gate-rejection invariant inside
     C-4 entry-gates |
133:The two HIGH findings (W8 / W9 SCAFFOLD persistence) are addressed
     by C-4; the MED finding (gate-layer-only footprint) is a
     quantification of the SCAFFOLD verdict and clears as C-4 wires
     the runtime consumer; the LOW finding (honest self-labelling) is
     preserved through PRUNE-5 as a gate-rejection invariant inside
     C-4 entry-gates so any admit attempting to cite W8 / W9 pre-
     runtime-consumer is denied at admit time. No CRITICAL violation
     in the decision-engine layer at SK-V14 starting state.
```

The "no-op pre-C-4" framing is gone from the file entirely. §5:133
narrative-paragraph action-class now matches §4 row 4 ledger-cell
action-class verbatim. Cross-document consistency with SYNTHESIS
§3.1:343 C-4 row A5 LOW cell ("preserved through PRUNE-5 as C-4
entry-gate invariant" — see §2.2 below) confirmed.

**Disposition**: ACCEPT. The CH6 V2 critique that a LOW finding tagged
"no-op pre-C-4" risked paper-close drift is closed at source — the
finding is now load-bearing through PRUNE-5 (admit-gate denial
mechanism), not a sit-and-wait artifact.

### §2.2 F-V3-SYNTHESIS-1 — §3.1 C-4 A5 LOW cell propagation

Executable verification:

`grep -n "no-op pre-C-4\|preserved through PRUNE-5" restart/skinny/
tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`:

```
343:| **C-4** = PRUNE-5 | Wire W8 per-grammar policy + W9 same-
     substrate union from gate-only into compile / lower / runtime |
     A5 finding 1 (W8 SCAFFOLD persists); A5 finding 2 (W9 SCAFFOLD
     persists); A5 NEW-MED (gate-layer-only footprint quantified); A5
     LOW (resolver honest self-labelling — preserved through PRUNE-5
     as C-4 entry-gate invariant) | 4 |
```

The C-4 row A5 LOW cell now reads "preserved through PRUNE-5 as C-4
entry-gate invariant", consistent with A5 §4 row 4 + A5 §5:133. The
"no-op pre-C-4" wording is gone from SYNTHESIS.

**Disposition**: ACCEPT.

### §2.3 F-V3-SYNTHESIS-2 — §2.4 codegen-file count refinement

Executable verification:

`git grep -l '@generated by skinny bbnf-codegen' skinny/crates/
codegen/src/ | wc -l` returns **14**.

Enumerated (verified by `git grep -l '@generated by skinny bbnf-
codegen' skinny/crates/codegen/src/`):

```
css_l4_at_rules_and_media_provider.rs              <- provider
css_l4_declaration_values_extended_provider.rs     <- provider
css_l4_declaration_values_provider.rs              <- provider
css_l4_nested_layout_provider.rs                   <- provider
css_l4_stylesheet_selectors_provider.rs            <- provider
css_l4_vendor_and_custom_atrules_provider.rs       <- provider
css_l4_visual_functions_provider.rs                <- provider
json_provider.rs                                   <- provider (8th)
json_templates/generated.rs                        <- ancillary
json_templates/parser.rs                           <- ancillary
json_templates/value.rs                            <- ancillary
json_templates/view.rs                             <- ancillary
json_typed_direct.rs                               <- ancillary
lib.rs                                             <- ancillary (6th)
```

8 providers (7 CSS + 1 JSON) + 6 ancillary (4 `json_templates/*` +
`json_typed_direct.rs` + `lib.rs`) = **14** ✓.

`sed -n '305,323p' restart/skinny/tranches/sk-v14/audit-overfit/
SYNTHESIS-AUDIT-OVERFIT.md`:

```
2. **Lock-14-companion lint.** Add a clippy-lint or pre-commit grep
   that REJECTS any new `// @generated by skinny bbnf-codegen` header
   in `skinny/crates/{runtime/src/grammars,codegen/src}/**/*.rs`
   unless the matching path appears in a recognized regen subcommand's
   emission roster. The glob MUST scope BOTH the runtime-side mirror
   AND the codegen-side template/provider files — CH2 §3.5 verified
   42 files carry the fake header including 14 codegen-side files
   (8 providers+templates + 6 ancillary; `git grep -l '@generated by
   skinny bbnf-codegen' skinny/crates/codegen/src/ | wc -l = 14`).
   The lint glob `codegen/src/**/*.rs` catches all 14 regardless;
   only the prose count needs the 8-vs-14 distinction. A runtime-only
   lint would let the
   codegen-side twin re-introduce the fake header silently …
```

Roster breakdown reconciles. Lint-glob scope ("`codegen/src/**/*.rs`
catches all 14 regardless") factually correct — a glob over
`codegen/src/**/*.rs` will pick up providers + json_templates/* +
json_typed_direct.rs + lib.rs uniformly.

**Disposition**: ACCEPT. The CH2 V2 concern about the "8 codegen-side
template+provider files" count understating the lint surface is
closed; SYNTHESIS now both quantifies the true count (14) and clarifies
that the lint-glob recommendation does not depend on the 8-vs-14 split.

### §2.4 F-V3-SYNTHESIS-3 — §1.1:81 NEW-2 in-table cell

Already verified in §1.1 above (overlaps V2 CH1 §5 item 1 closure).

## §3 — V2 baseline preservation

Critical V2 baseline cites re-executed and reproduce verbatim under
V3:

1. `git diff 1735882a5..007624849 -- restart/skinny/tranches/sk-v14/
   audit-overfit/sk-v14-audit-overfit-{css-measurement,admit-
   mechanism,lock14-scan,pre-restart-pattern}.md | wc -l` returns **0**
   — A1, A2, A3, A6 STAND byte-identical from V2 (matches V3 commit
   body).
2. SYNTHESIS census (V2 §1.1): `grep -n "54 of 74\|20 of 74\|11 NEW
   conceptual\|11 NEW finding" SYNTHESIS-AUDIT-OVERFIT.md` returns
   lines 64-65 ("54 of 74 findings (73 %) CONFIRM …; 20 of 74 are
   NEW (27 %)"), 67 ("11 NEW conceptual"), 87 ("11 NEW finding
   *categories* (= 20 NEW *rows*)"), 356, 462, 495. Per-row /
   per-category distinction intact.
3. Per-axis severity column sums (V2 §3): A1 8 (4+2+2+0), A2 9
   (4+3+1+1), A3 30 (11+7+5+7), A4 16 (9+4+2+1), A5 4 (0+2+1+1), A6 7
   (3+2+1+1) → 74 ✓; CRIT 31 + HIGH 20 + MED 12 + LOW 11 = 74 ✓.
4. A5 verdict-line alignment: `grep -n "FAIL at HEAD" sk-v14-audit-
   overfit-decision-engine.md` returns lines 11, 102, 105 all reading
   "FAIL at HEAD, PASS conditioned on C-5 (PRUNE-1 + PRUNE-2) + C-4
   (PRUNE-5) landing" — verbatim cross-site alignment preserved.
5. A6 LegacyPath both-readings disambiguation: `grep -n "scope-
   extension over V13 Pattern G" sk-v14-audit-overfit-pre-restart-
   pattern.md` returns the §0:14 disambiguation paragraph + the §2
   ledger row Status cell, both intact.
6. CH7 grammar census: `find crates/core/src/runtime -mindepth 1
   -maxdepth 1 -type d | wc -l` returns **9**; `cargo metadata
   --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].
   ident' | wc -l` returns **9**. PRUNE-4 sub-wave binding (9 not 8)
   holds.
7. Coverage table SYNTHESIS §3.1: C-1 41 + C-2 7 + C-3 11 + C-4 4 +
   C-5 11 = **73** with the §3.2 orphan note ("the slate covers 73 of
   74 findings; the one not covered" — re-read at line 335 cites the
   A6 NEW-LOW asm bibliographic as KEEP no-action). Aggregate
   arithmetic preserved.

## §4 — V3 fresh-finding scan

Scanned the V3 diff (`git show 007624849`) end-to-end and re-greped
the three modified files for any incidental drift introduced by the
folds. No new CH1-grade defect:

- F-V3-A4-1 single-line replacement at A4 §1:153 introduces no
  ledger-coordinate / methodology-roster contradiction (the 100-line
  count agrees with `wc -l` and with every previously-cited per-line
  range in A4 §2 rows 8 / 10 / 11).
- F-V3-A5-1 replaces one parenthetical at A5 §5:133; no other A5
  paragraph references the old "no-op pre-C-4" framing (grep returns
  zero matches for that string anywhere under `audit-overfit/`).
- F-V3-SYNTHESIS-1 replaces one C-4 row LOW cell at §3.1:343; same
  zero-residual grep confirmation.
- F-V3-SYNTHESIS-2 extends one prose paragraph at §2.4:305-323; the
  added executable-verification line (`git grep -l … | wc -l = 14`)
  re-runs to 14 and the 8 + 6 breakdown reconciles to 14 by file
  enumeration. The trailing "A runtime-only lint would let the" line
  is slightly fragmented by paragraph flow (the original sentence was
  broken across the insertion point) but reads correctly as continuous
  prose; it is a cosmetic paragraph-wrap, not a substantive defect.
- F-V3-SYNTHESIS-3 single-cell replacement at §1.1:81; the §1.2
  enumeration, §0.1 NEW column count (12), and §5.1 closing all
  remain consistent with the now-aligned "4 of 7" framing.

One **observation (below CH1 threshold, not a finding)**: the V3
insertion at SYNTHESIS §2.4:314-316 produces a soft paragraph break
where the original prose flowed continuously — "The lint glob
`codegen/src/**/*.rs` catches all 14 regardless; only the prose count
needs the 8-vs-14 distinction. A runtime-only lint would let the /
codegen-side twin re-introduce …" The slash here marks a hard
newline-then-indent inside what reads as a single sentence in HTML
render. This is purely typographical; the assertion-level content is
correct and the lint-glob recommendation factually holds. No fold
recommended.

## §5 — Spot-verified cites (V3 sample of 9)

1. `wc -l skinny/crates/codegen/src/json_provider.rs` → **100** ✓.
2. `grep -n "json_provider.rs" sk-v14-audit-overfit-generator-truth.md`
   → line 153 reads "(full, 100 lines)" ✓.
3. `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/
   codegen/src/ | wc -l` → **14** ✓ (matches SYNTHESIS §2.4 prose).
4. `grep -n "3 of 7\|4 of 7" SYNTHESIS-AUDIT-OVERFIT.md` → line 81
   only, reading "4 of 7" ✓.
5. `grep -n "no-op pre-C-4" restart/skinny/tranches/sk-v14/audit-
   overfit/` → zero matches across SYNTHESIS + A5 (the framing is
   fully retired).
6. `grep -n "preserved through PRUNE-5\|gate-rejection invariant"
   restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-
   decision-engine.md` → lines 118 (§4 row 4) + 133 (§5 close) both
   carry the new action-class.
7. `grep -n "preserved through PRUNE-5 as C-4 entry-gate invariant"
   SYNTHESIS-AUDIT-OVERFIT.md` → line 343 (§3.1 C-4 row) ✓.
8. `git diff 1735882a5..007624849 -- restart/skinny/tranches/sk-v14/
   audit-overfit/sk-v14-audit-overfit-{css-measurement,admit-mechanism,
   lock14-scan,pre-restart-pattern}.md | wc -l` → **0** ✓ (A1, A2,
   A3, A6 STAND verbatim).
9. `git show --stat 007624849 | grep "files changed"` → "3 files
   changed, 10 insertions(+), 6 deletions(-)" ✓ (matches commit
   stat as cited in dispatch context).

---

**V3 ACCEPT-rate: 7/7 artefacts = 100 %.**

Both V2 CH1 hygiene carry-overs are RESOLVED at the exact line-
coordinates V2 §5 prescribed (SYNTHESIS §1.1:81 "4 of 7", A4 §1:153
"100 lines"). The three V3 SYNTHESIS coherence folds (`F-V3-
SYNTHESIS-{1,2,3}`) all land at the targeted sites with consistent
cross-document propagation; F-V3-A5-1 closes the V2 CH6 critique on
action-class propagation. A1 / A2 / A3 / A6 STAND byte-identical from
V2 → V3, preserving every previously-verified V2 baseline cite.

**No CH1-grade source-side correctness defects identified in V3.**
Every file:line cite re-executes to the quoted output; the 74-finding
aggregate + severity distribution remain arithmetically self-
consistent; the 54 CONFIRMS / 20 NEW per-row + 11 NEW conceptual
category census reconciles across §0.1 + §1.1 + §1.2 + §3.2 + §4.3 +
§5.1; A5 verdict-line alignment preserved across all four V2-targeted
sites and now extended with the V3 action-class fold at §4 row 4 +
§5 close; the 14-file codegen-side fake-`@generated` count is source-
verified and the 8+6 breakdown reconciles by direct enumeration.

CH1 V3 contributes **100 %** toward the §3Z convergence chain. With
V2 CH1 at 100 % substantive and V3 CH1 at 100 % outright, the two-
consecutive-cycle ≥95 % CH1 condition is met from the CH1 side; the
aggregate G-S-P0-CONVERGED gating remains conditional on the
remaining six lens dispositions.
