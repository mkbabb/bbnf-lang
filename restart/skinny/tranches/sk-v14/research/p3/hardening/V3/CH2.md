# SK-V14 P3 CHALLENGE V3 — CH2 GENERALITY (Lock 14 LOCK-TRIGGER)

Pass: S-P3 Synthesis-Plan CHALLENGE. Cycle: V3 (LOCK-trigger).
Date: 2026-05-23.
Lens: CH2 GENERALITY per `restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md §3`
+ `restart/prompts/ORCHESTRATOR.md §3W` (universal lens registry) + §3Z
(cohort LOCK at ≥95% × 2 consecutive cycles; V≤5 ceiling). WRITE-ONLY.
HEAD pin: `867b0cd0b` (V3 atomic cosmetic-fold commit = HEAD; matches V3
CHALLENGE-CONTEXT §0 authority pin verbatim).
Authority: V3 CHALLENGE-CONTEXT.md (43 lines) + V2 CH2.md (697 lines;
100.0 % ACCEPT first-cycle LOCK-eligible baseline) +
HARDENING-S-P3-V2-CONSOLIDATED.md (594 lines; §2.4 F-V2-CH2-1 +
F-V2-CH2-2 discharge ratification; §3.3 C3/C4 same-wave-consumer
convergence; §4.1 F-V3-CH6-3 LIGHT cosmetic non-blocking residual) +
V3-amended P3-C (537 lines; `:36` + `:423` mirror SPEC §13:982 verbatim
UNCONDITIONAL Stage-0 binding) + V2-LOCKED SPEC.md (1187 lines;
byte-identical at V3 HEAD) + V2-LOCKED P3-A/P3-B/P3-D/P3-E/P3-F +
V1-LOCKED DISPATCH-PROMPT.md (zero V3 edits across all 7 LOCKED
artefacts) + Lock 14 v+1 strict-read at `LOCKS.md:259` ("must exercise
at least one non-JSON consumer or record a measured deletion/
rejection") + Lock 16 v+1 abstract-primitive cross-grammar declarations
at `LOCKS.md:282-340`.

## §0 — Disposition headline

**ACCEPT-rate: 11 / 11 cells = 100.0 % ACCEPT, 0 REVISE, 0 REJECT.**

V3 is the **second consecutive ≥ 95 % cycle for CH2** (V2 100.0 % +
V3 100.0 %). Per `ORCHESTRATOR.md §3Z` cohort LOCK rule ("≥ 95 % ACCEPT
× 2 consecutive cycles + zero orphan REVISE"), CH2 **LOCKs at V3
close** as a 2-cycle confirmed lens.

The V3 cosmetic fold (F-V3-CH6-3 LIGHT cosmetic; CH6 surface) does NOT
touch CH2 disposition surface. Per `git diff f25c3af2e..867b0cd0b` on
the 7 V2-LOCKED CH2-bearing artefacts (`p3a-candidate-shortlist.md`,
`p3b-wave-sequencing.md`, `p3d-telemetry-schema.md`,
`p3e-preblocked-ledger.md`, `p3f-spec-draft.md`, `SPEC.md`,
`DISPATCH-PROMPT.md`) the diff is **0 lines** — byte-identical V2→V3.
The V3 cosmetic fold touches ONLY P3-C `:36` (§1.2 W10 wave manifest)
and P3-C `:423` (§2.10 W10 exit-gate item 8); both edits add the phrase
"UNCONDITIONALLY per S-P2 V3 §6.3 (SPEC §13:982 binding)" to mirror SPEC
§13:982 unconditional Stage-0 wording (a CH6 surface refinement, not a
CH2 surface refinement). Lock 14 v+1 strict-read posture is preserved
byte-identical across all 8 P3-A candidate cells.

All 11 CH2 cells from V2 (eight per-candidate verdict traces + three
SPEC-level binding cells) hold at V3 HEAD by construction
(byte-identical text on the load-bearing surfaces); confirmation
verifications are re-executed at V3 HEAD per LAC-1E-12 executable-cite
mandate (§2).

§3Z status: V2 100.0 % → V3 100.0 % = second consecutive ≥ 95 % cycle
for CH2 + zero open REVISE + zero orphan disposition + zero critical
defect. CH2 LOCKs at V3 close.

## §1 — CH2 disposition cells (11 cells; carry-forward from V2 100 %)

The eleven CH2 cells from V2 CH2 §1 are carried forward at V3 HEAD; the
V3 cosmetic fold's textual mirror at P3-C `:36` + `:423` does not edit
any of the cell-load-bearing text. Each cell holds by V2 ACCEPT
ratification plus V3-HEAD byte-identical preservation.

### Cell §1.1 — C1 `long_string_body_simd_scan` grammar-neutral verdict — **ACCEPT** (V2 ACCEPT carried; zero V3 edit)

P3-A §2 C1 entry at `p3a:61-72` (V2-LOCKED; byte-identical at V3 HEAD;
verified via `git diff f25c3af2e..867b0cd0b -- p3a-candidate-shortlist.md`
returns 0 lines). The P2-F NEUTRAL-CONFIG-DRIVEN verdict at `p2f:247`
(C1 structural-byte SIMD classify) + `p2f:248` (C2 quoted-string
boundary scan PMULL prefix-XOR) preserved. Same-wave consumer at
`p3a:67`: `parse_that_regex::skip_string_plain_trusted` at
`skinny/crates/parse-that-regex/src/lib.rs:547`. C1 checkasm matrix
at `p3a:66` covers grammar-specific terminators (JSON `"` vs CSS
`'`/`"` vs BBNF `'`/`"`/`` ` ``) — the same-shape non-JSON exercise.
Lock 14 v+1 disjunctive read (LOCKS.md:259) holds via NEUTRAL-CONFIG-
DRIVEN config-substrate alphabet + checkasm-row non-JSON exercise.
NF-CH6-4 canonical-name binding (SPEC §15 line 1118; p3a:184 "Zero
candidates admit three orthogonal SIMD bodies for one primitive")
preserved at V3 HEAD. CH2 cell holds.

### Cell §1.2 — C2 `structural_index_singular_substrate_consumer` verdict — **ACCEPT** (V2 ACCEPT carried; zero V3 edit)

P3-A §2 C2 entry at `p3a:74-85` (V2-LOCKED; byte-identical at V3 HEAD).
P2-F C11 verdict NEUTRAL-CONFIG-DRIVEN at `p2f:257`; substrate-target
`existing_tape` per Lock 1 v+1 declaration triple at `p3a:83`. The
CSS L4 / Sheets / BBNF-self consumer surface is the same
`passes::layout`-emitted per-rule validator (`p2f:280-282`). SPEC §15
line 1112 REDRESS 126 carry-through binding ("per V3 §1.4 CH3 NF-CH6-3
C2 scalar-ref evidence upgrade carry-through; primitive consumption
requires post-W7 runtime divergence") preserved at V3. CH2 cell holds.

### Cell §1.3 — C3 `digit_block_simd_accumulate` + F-V2-CH2-1 discharge preservation — **ACCEPT** (V2 DISCHARGED; zero V3 edit; LOCK-tight)

P3-A §2 C3 entry at `p3a:87-98` (V2-LOCKED; byte-identical at V3 HEAD —
verified via `git diff` 0 lines on `p3a-candidate-shortlist.md`). The
F-V2-CH2-1 discharge at `p3a:93` (V1 §1.3 REVISE → V2 option (iii) fold)
remains byte-identical at V3:

> same-wave non-JSON consumer is the `bbnf-simd` checkasm row
> exercising the CSS-permissive `byte_class_from_range_64` (Gap 7.5)
> sibling — new `crates/bbnf-simd/tests/checkasm_byte_class_from_range_64.rs`
> modelling the sibling-shape template at
> `skinny/crates/bbnf-simd/tests/checkasm_byte_class_from_eq_set_64.rs:1`
> (executable-verified at HEAD), with the CSS L4 `<number>` byte-class
> config `[0x30..=0x39, 0x2E, 0x2B, 0x2D, 0x65, 0x45]` instantiated as
> the non-JSON row.

V3-HEAD executable cite re-verification (LAC-1E-12 mandate):

1. **Sibling-shape template present at HEAD `867b0cd0b`**: `ls
   skinny/crates/bbnf-simd/tests/checkasm_byte_class_from_eq_set_64.rs`
   returns the file (V2 verification preserved; V3 commit modifies only
   `p3c-falsifiability-gates.md` + adds new `V3/CHALLENGE-CONTEXT.md` —
   the `bbnf-simd/tests/` tree is untouched at V3).
2. **Stage-A authoring target correctly NOT-PRESENT**:
   `checkasm_byte_class_from_range_64.rs` correctly absent at HEAD
   (queued for S-P3 same-commit admission per Lock 16 same-commit
   discipline).
3. **CSS-permissive byte-class config legitimacy** at P2-E line 244
   ratifies the CSS dimension/percentage row's range exercise; the
   `[0x30..=0x39, 0x2E, 0x2B, 0x2D, 0x65, 0x45]` byte-set differs from
   the JSON `number` byte-class config (JSON does not admit leading
   `+`/`-` per RFC 8259 §6; CSS L4 `<number>` does per CSS Syntax
   §4.3.4) — a real non-JSON exercise, option-1 of LOCKS.md:259
   disjunction.

Lock 16 line 287 (`LOCKS.md:287`) abstract-primitive declaration
preserved verbatim at V3: "abstract primitive: byte-window multiply-
accumulate, lifted from dav1d's FIR filter — applies to ANY grammar's
digit-block decode, not just JSON".

V3 cosmetic fold note: P3-C `:36` (§1.2 W10 wave manifest) now reads
"Stage-0 F-V2-P1ABC-RERECORD UNCONDITIONALLY per S-P2 V3 §6.3 (SPEC
§13:982 binding)" — mirroring SPEC §13:982 wording. C3 admission at
W10 (the first wave consuming Gap 5 + Gap 7.5 per SPEC §13 + p3a:180
unconditional binding language) is preserved; the V3 wording refinement
strengthens the textual link between P3-C and SPEC §13:982 without
altering the wave-id or the Lock 14 v+1 discharge posture. CH2 cell
holds.

### Cell §1.4 — C4 `unicode_escape_neon_nibble_decode` + F-V2-CH2-2 discharge preservation — **ACCEPT** (V2 DISCHARGED; zero V3 edit; LOCK-tight)

P3-A §2 C4 entry at `p3a:100-111` (V2-LOCKED; byte-identical at V3 HEAD).
The F-V2-CH2-2 discharge at `p3a:106` (V1 §1.4 REVISE → V2 combination
(i) + (ii) fold) remains byte-identical at V3:

> same-wave non-JSON consumer is the **BBNF-self string-escape
> consumer** — BBNF-self uses JSON-shape escape alphabet per P2-F §2.7
> + §3 note 1 (`grammar/bbnf/bbnf.bbnf:11-13` defines `literal = ( "\""
> , /(\\.|[^"\\])*/ , "\"" | "'" , … | "`" , … ) -> Span` with the same
> `\\.` escape pattern as JSON; the `\u`+4-nibble form is shape-
> identical), driving through `parse_that_regex::unescape_string` at
> `skinny/crates/parse-that-regex/src/lib.rs:718` with the BBNF-self
> triple-quote-set config — the SAME SIMD body
> (`unescape_uxxxx_x8_neon` fixed-4-nibble decode) is exercised by the
> BBNF-self literal-unescape consumer at `grammar/bbnf/bbnf.bbnf:11-13`.
> The CSS L4 escaped-ident `\HEXHEX` (CSS Syntax §4.3.7 variable 1-6
> hex digits) is SHAPE-ORTHOGONAL to the fixed-4-nibble SIMD body and
> does NOT exercise this primitive — carved out as a separate-primitive
> concern per Lock 14 v+1 "measured deletion/rejection" record.

V3-HEAD executable cite re-verification (LAC-1E-12 mandate):

1. **`grammar/bbnf/bbnf.bbnf:11-13`**: literal-with-backslash-escape
   rule preserved at V3 HEAD (V3 commit modifies only P3-C + adds
   V3/CHALLENGE-CONTEXT; the `grammar/` tree is untouched).
2. **`parse-that-regex/src/lib.rs:718`** `unescape_string` function
   entry preserved at V3 HEAD (V3 commit does not touch
   `parse-that-regex/`).
3. **`parse-that-regex/src/lib.rs:945`** `read_hex_unit_scalar`
   preserved at V3 HEAD.
4. **`unescape_uxxxx_scalar` at `aarch64/unescape_uxxxx.rs:40`**
   preserved at V3 HEAD per P2-A C7 V1-LOCKED 7/7 status.
5. **P2-F §2.7 + §3 note 1** at `p2f:46` + `p2f:248` + `p2f:262-264`
   preserved at V3 HEAD (V3 commit does not touch P2 artefacts;
   `research/p2/` is V3-LOCKED-by-baseline since S-P2 V3 §3Z cohort
   LOCK at HEAD `4db55d284`).

The BBNF-self consumer exercises the SAME SIMD body
(`unescape_uxxxx_x8_neon` fixed-4-nibble decode) because the
`\\.`-pattern admits backslash-prefixed `u`+4-nibble forms identically
to JSON. Sheets correctly EXCLUDED by shape (doubled-quote escape per
`p2f:41`; no `\u` form); CSS L4 variable-width `\HEXHEX` correctly
carved out as separate-primitive measured-rejection per Lock 14 v+1
option (ii). Two-route closure of both V1 REVISE families preserved
byte-identical at V3.

**REDRESS 126 carry-through** (SPEC §15 line 1112) preserved at V3.
The `parse-that-regex::StringFlags::HAS_ESC` JSON-flavored naming
carry-over (per `sk-v14-audit-overfit-lock14-scan.md:9` cited at P2-F
§3 note 1 = `p2f:264`) remains a NON-BLOCKING CH2 observation
forwarded to W10 implementation per V2 CH2 §5 disposition; V2's
non-blocking carry-forward is honoured at V3 (cohort LOCK is not
gated on this granularity per V2 CH2 §5 explicit non-blocking
clause). CH2 cell holds.

### Cell §1.5 — C5 `parse_attribution_envelope_cracker` verdict — **ACCEPT** (V2 ACCEPT carried; zero V3 edit on P3-A C5)

P3-A §2 C5 entry at `p3a:113-124` (V2-LOCKED; byte-identical at V3 HEAD).
N/A on substrate (process discipline, not a kernel). The `parse-
attribution` cargo feature at `runtime` crate is grammar-agnostic
infrastructure per HARDENING-S-P2-V3-CONSOLIDATED §6.3; the 12
consumer-dependency primitives the rerecord serves are themselves
grammar-neutral verdicts inherited through C1/C3/C7 admission cells.

V2 unconditional-binding amendment at SPEC §11/§12/§13 (per F-V2-CH6-1
LOAD-BEARING fold) preserved at V3 HEAD byte-identical. `p3a:180`
preserves the unconditional binding language verbatim. The V3 cosmetic
fold to P3-C `:36` + `:423` mirrors the SPEC §13:982 unconditional
wording at the P3-C artefact (textual consistency closure of CH6's V2
non-blocking F-V3-CH6-3 residual); this STRENGTHENS the C5 / W10
unconditional binding across artefacts without altering the CH2
disposition. CH2 cell holds.

### Cell §1.6 — C6 `force_inline_lto_envelope_discipline` verdict — **ACCEPT** (V2 ACCEPT carried; zero V3 edit)

P3-A §2 C6 entry at `p3a:126-137` (V2-LOCKED; byte-identical at V3 HEAD).
P2-F C14 verdict NEUTRAL-WIRED at `p2f:260` ("cross-grammar by
construction") preserved. Lock 15 hardware-only LTO + force-inline +
i-cache budget discipline (LOCKS.md:265-281) preserved at V3 HEAD;
BBNF-FORCE-INLINE-MISSED + BBNF-ICACHE-BUDGET-EXCEEDED diagnostics
emit per-grammar uniformly. NEUTRAL-WIRED verdict at `p2f:270`
preserved. CH2 cell holds.

### Cell §1.7 — C7 `ascii_whitespace_skip_64` verdict — **ACCEPT** (V2 ACCEPT carried; zero V3 edit; gold-standard)

P3-A §2 C7 entry at `p3a:139-150` (V2-LOCKED; byte-identical at V3 HEAD).
P2-F C7 verdict NEUTRAL-CONFIG-DRIVEN via `WhitespaceByteSet: [bool;
256]` from generated grammar config (per-grammar `@ws` directive) at
`p2f:253` preserved. P3-A C7 entry cell (c) at `p3a:145` names the
cross-grammar same-wave consumer trio explicitly: "CSS L4 declaration-
value whitespace consumer per S-P3 R6 (set `{0x20, 0x09, 0x0A, 0x0D,
0x0C}`); Sheets whitespace consumer; BBNF-self whitespace consumer."
The C7 checkasm cell at `p3a:144` explicitly names CSS-form-feed `0x0C`
in matrix coverage. Gold-standard CF-3 same-wave-non-JSON-consumer
naming preserved at V3 — three non-JSON grammars by explicit byte-set
difference (CSS adds `0x0C` form-feed; Sheets + BBNF-self use the
JSON whitespace set sub-modulo). Lock 14 v+1 holds cleanly; canonical
CH2-clean entry. CH2 cell holds.

### Cell §1.8 — C8 `BackendShape::SinkOnly` activation verdict — **ACCEPT** (V2 ACCEPT carried; zero V3 edit)

P3-A §2 C8 entry at `p3a:152-163` (V2-LOCKED; byte-identical at V3 HEAD).
P2-D V2-LOCKED status with C-P2D-1 active inherits the SinkOnly verdict
— a codegen-lowering decision parametrised on
`LayoutFacts.backend_shape[rule_id]` (per-rule from grammar source, not
per-grammar). The `OffsetTapeStats` substrate-measurement column
applies uniformly to all grammars. C8's substrate target is
`direct_sink` (Lock 1 v+1); no JSON-specific policy in the lowering
pass. CH2 cell holds.

### Cell §1.9 — SPEC §2.1 generality gate presence — **ACCEPT** (V2 ACCEPT carried; zero V3 edit on SPEC.md)

`SPEC.md:294-313` carries §2.1 "Generality And Lock 14 Gate" verbatim
from SK-V8 + SK-V14 R3 PRUNE-3 extension. The six gate checks (public
API scan, grammar branch scan, primitive/table scan, role/fact
boundary, template/provider boundary, non-JSON proof, forward
invariant) preserved at V3 HEAD. V3 SPEC.md is V2-LOCKED at 1187 lines
(byte-identical per `git diff` 0 lines on SPEC.md). Cell holds.

### Cell §1.10 — SPEC §2.1 non-JSON proof requirement per generic-crate edit — **ACCEPT** (V2 ACCEPT carried; zero V3 edit on SPEC.md:305)

`SPEC.md:305` reads verbatim (preserved at V3 HEAD byte-identical):

> **Non-JSON proof**: CSS L4, Sheets, and BBNF-self must compile,
> lower, cost, or run without JSON structural roles for any generic
> CostFacts, codegen, runtime, SIMD, or parser-template edit.
> Acceptable proof is a named no-op dry run, focused test, or
> unchanged-output audit.

The clause covers `bbnf-simd` (where C1/C3/C4/C7 land), `passes::layout`
(where C2/C8 land), and `codegen` (where C2/C6/C8 land). The V2 fold's
per-candidate cell-level strengthening on C3 + C4 (F-V2-CH2-1 +
F-V2-CH2-2) preserved byte-identical at V3 (zero P3-A edit V2→V3).
Cell holds.

### Cell §1.11 — No P3-B wave lets JSON policy into a generic crate — **ACCEPT** (V2 ACCEPT carried; zero V3 edit on P3-B)

The per-wave audit from V2 CH2 §1.11 carries forward byte-identical at
V3 HEAD (P3-B is V2-LOCKED, zero V3 edits). All 12 waves W0..W11
preserved: ZERO admit JSON policy into a generic crate. The V3 fold to
P3-C `:36` + `:423` does not alter the wave-id sequence or the W5
template-collapse + W6 hand-written-runtime collapse + W7 BackendShape-
only-dispatch + W8 CSS L4 re-admit / W10 JSON parse_only admit
sequence; only the W10 Stage-0 binding wording mirrors SPEC §13:982
unconditional language. The wave-id ledger at V2 CH2 §1.11 holds
byte-identical at V3. Cell holds.

## §2 — V3-HEAD verifications + V2-discharge preservation audit

### §2.1 — V3 cosmetic fold scope verification

The V3 commit `867b0cd0b` stat (per `git show --stat 867b0cd0b`):

```
.../research/p3/hardening/V3/CHALLENGE-CONTEXT.md   | 43 ++++++++++++++++++++++
.../sk-v14/research/p3/p3c-falsifiability-gates.md  |  4 +-
 2 files changed, 45 insertions(+), 2 deletions(-)
```

Total surface = 2 files: (a) NEW `V3/CHALLENGE-CONTEXT.md` (43 lines,
seed-context document; not a CH2 disposition surface); (b)
`p3c-falsifiability-gates.md` 4-line edit (`:36` wave-manifest row +
`:423` exit-gate item 8 — both add the phrase "UNCONDITIONALLY per
S-P2 V3 §6.3 (SPEC §13:982 binding)"). **Zero edits to** P3-A, P3-B,
P3-D, P3-E, P3-F, SPEC.md, DISPATCH-PROMPT.md — verified via `git diff
f25c3af2e..867b0cd0b -- p3a-candidate-shortlist.md p3b-wave-sequencing.md
p3d-telemetry-schema.md p3e-preblocked-ledger.md p3f-spec-draft.md
SPEC.md DISPATCH-PROMPT.md` returning 0 lines.

### §2.2 — F-V2-CH2-1 discharge byte-identical preservation at V3

The C3 same-wave non-JSON consumer cell at `p3a:93` (V2 LIGHT-fold)
preserved at V3 HEAD byte-identical (V3 git-diff 0 lines on p3a).
Lock 14 v+1 option-1 discharge (checkasm row IS the non-JSON consumer
exercise) holds at V3:

1. CSS-permissive byte-class config `[0x30..=0x39, 0x2E, 0x2B, 0x2D,
   0x65, 0x45]` legitimate per P2-E line 244 (CSS L4 `<number>` per
   CSS Syntax §4.3.4).
2. Sibling-shape template at
   `skinny/crates/bbnf-simd/tests/checkasm_byte_class_from_eq_set_64.rs`
   present at V3 HEAD (V3 commit does not touch `bbnf-simd/`).
3. Stage-A authoring target `checkasm_byte_class_from_range_64.rs`
   correctly NOT-PRESENT at V3 HEAD (per Lock 16 same-commit
   discipline; queued for S-P3 same-commit admission).
4. No cross-wave deferral to W8: the checkasm parity row lands in the
   SAME W10 wave that admits the SIMD body (per V2 SPEC §13 + V3 P3-C
   `:36` unconditional Stage-0 binding wording mirror).
5. Lock 14 v+1 disjunctive read (LOCKS.md:259) satisfied via option 1
   (non-JSON consumer exercise via CSS-permissive checkasm row).

### §2.3 — F-V2-CH2-2 discharge byte-identical preservation at V3

The C4 same-wave non-JSON consumer cell at `p3a:106` (V2 LIGHT-fold)
preserved at V3 HEAD byte-identical (V3 git-diff 0 lines on p3a).
Lock 14 v+1 disjunctive read satisfied via BOTH options at V3:

1. **Option 1 (non-JSON consumer exercise)** via BBNF-self string-
   escape consumer — BBNF-self literal at `grammar/bbnf/bbnf.bbnf:11-13`
   exercises the SAME `unescape_uxxxx_x8_neon` fixed-4-nibble SIMD body
   through `parse_that_regex::unescape_string` at
   `skinny/crates/parse-that-regex/src/lib.rs:718`. All cited source
   files preserved at V3 HEAD (V3 commit touches neither `grammar/`
   nor `parse-that-regex/`).
2. **Option 2 (measured deletion/rejection record)** via CSS L4
   variable-width `\HEXHEX` carve-out (CSS Syntax §4.3.7) — shape-
   orthogonal to fixed-4-nibble SIMD body; admitted/rejected
   separately, not via C4. The V2 fold text at `p3a:106` makes the
   measured-rejection explicit ("variable-width CSS escape requires a
   different primitive; admitted/rejected separately, not via C4");
   preserved at V3.

Sheets correctly EXCLUDED by shape per `p2f:41` (doubled-quote escape
`""` → `"`; no `\u` form); V2's choice of BBNF-self (not Sheets)
honours the shape-match requirement. Preserved at V3.

### §2.4 — Lock 14 v+1 strict read at V3 HEAD for all 8 P3-A candidates

| Candidate | P2-F verdict | Lock 14 v+1 discharge route | V2 status | V3 status |
|---|---|---|---|---|
| C1 | NEUTRAL-CONFIG-DRIVEN | checkasm matrix covering grammar-specific terminators (JSON `"`/CSS `'`+`"`/BBNF `'`+`"`+`` ` ``) per `p3a:66` | HOLD | HOLD (byte-identical) |
| C2 | NEUTRAL-CONFIG-DRIVEN | per-rule TypedShape enum config-free; CSS L4/Sheets/BBNF-self via same `passes::layout` validator | HOLD | HOLD (byte-identical) |
| C3 | NEUTRAL-CONFIG-DRIVEN | F-V2-CH2-1: `bbnf-simd` checkasm row exercising CSS-permissive `byte_class_from_range_64` (same wave as SIMD body admission) | DISCHARGED at V2 | DISCHARGED (byte-identical at V3) |
| C4 | NEUTRAL-CONFIG-DRIVEN (P2-A V1-LOCKED 7/7) | F-V2-CH2-2: BBNF-self string-escape consumer (same SIMD body exercise) + variable-width CSS measured-rejection record | DISCHARGED at V2 | DISCHARGED (byte-identical at V3) |
| C5 | NEUTRAL-CONFIG-DRIVEN (cargo-feature infra) | grammar-agnostic profiling discipline; 12 dep primitives inherit individual verdicts | HOLD | HOLD (byte-identical; V3 cosmetic mirror strengthens textual link to SPEC §13:982 unconditional binding for the W10 host wave) |
| C6 | NEUTRAL-WIRED | hardware-only Lock 15 discipline (LTO + force-inline + i-cache); per-grammar diagnostics uniformly | HOLD | HOLD (byte-identical) |
| C7 | NEUTRAL-CONFIG-DRIVEN | gold-standard: CSS L4 + Sheets + BBNF-self consumer trio explicitly named at `p3a:145`; CSS-form-feed in checkasm matrix | HOLD | HOLD (byte-identical) |
| C8 | NEUTRAL-CONFIG-DRIVEN | per-rule `LayoutFacts.backend_shape` is grammar-source-derived; codegen template emits SinkOnly for every grammar uniformly | HOLD | HOLD (byte-identical) |

Lock 14 v+1 strict read holds across all 8 candidates at V3 HEAD. The
V2 F-V2-CH2-1 + F-V2-CH2-2 discharges (C3 + C4) preserved BYTE-
IDENTICAL across the V2→V3 cycle transition. No new disposition cells
opened; no V2 disposition closed re-opens. The V3 cycle is the
**second confirming cycle** required by `ORCHESTRATOR.md §3Z` for
2-cycle cohort LOCK on CH2.

### §2.5 — V3 cosmetic-fold consistency with Lock 14 v+1

The V3 P3-C cosmetic fold (`:36` + `:423`) adds the phrase
"UNCONDITIONALLY per S-P2 V3 §6.3 (SPEC §13:982 binding)" to mirror SPEC
§13:982. Verified via grep at V3 HEAD:

```
p3c:36:  | W10 | R8 | … Stage-0 F-V2-P1ABC-RERECORD UNCONDITIONALLY
         per S-P2 V3 §6.3 (SPEC §13:982 binding) | R8 | 17 (per corpus) |
p3c:423: 8. Stage-0 F-V2-P1ABC-RERECORD shipped UNCONDITIONALLY per
         S-P2 V3 §6.3 (SPEC §13:982 binding — W10 is the bound wave per
         the 5-step inheritance chain): cargo build + interactive
         samply record + cfg_attr flip at `generated.rs:33-237` 8
         sites, in this wave's commit slice, BEFORE any parse_only
         admit lands.
SPEC.md:982: W10 plan MUST include Stage-0 F-V2-P1ABC-RERECORD
         UNCONDITIONALLY per S-P2 V3 §6.3 verbatim: W10 is the bound
         wave for Stage-0 …
SPEC.md:990: Ship F-V2-P1ABC-RERECORD Stage-0 UNCONDITIONALLY per S-P2
         V3 §6.3 (W10 is the bound wave per §13 entry-gate inheritance
         chain) …
SPEC.md:1000: F-V2-P1ABC-RERECORD Stage-0 SHIPPED UNCONDITIONALLY per
         S-P2 V3 §6.3 …
```

The V3 P3-C mirror text aligns with SPEC §13:982 + §13:990 + §13:1000
unconditional Stage-0 wording. This is a **CH6 ANTI-PAPER-CLOSE** /
textual-consistency closure (F-V3-CH6-3 per V2 CONSOLIDATED §4.1), not
a CH2 GENERALITY edit; the Lock 14 v+1 strict-read posture is
preserved byte-identical across the V2→V3 transition for CH2's
disposition surface. The CH2 implication is purely positive
(textual reinforcement of unconditional Stage-0 binding strengthens the
W10 same-wave-consumer landing that hosts the F-V2-CH2-1 + F-V2-CH2-2
non-JSON consumer rows). CH2 LOCK posture unaffected; only
strengthened by adjacent CH6 closure.

## §3 — Inter-axis convergence audit at V3 HEAD (CH2-relevant)

### §3.1 — S-P2 V3 carry-forward §6.2 NF-CH6-4 canonical-name binding

V3 SPEC §15 line 1118 preserves the binding ("Three orthogonal SIMD
bodies for one primitive … admit under ONE canonical primitive name +
ONE canonical scalar-ref function at admission time"). P3-A C1 entry
(`p3a:61`) honours the canonical-name binding at V3 HEAD byte-
identical. V3 fold does not re-open. CH2 binding holds.

### §3.2 — S-P2 V3 carry-forward §6.1 CF-3 admission 3-gate

V3 P3-A §2.1 footer at `p3a:182` preserves "8/8 candidates carry the
3-gate cell explicitly" at V3 HEAD byte-identical. The V2 fold-
strengthened cell (c) on C3 + C4 (per V1 §1.3 + §1.4 REVISE
prescription discharge) preserved at V3. CH2 binding holds.

### §3.3 — S-P2 V3 carry-forward §6.3 F-V2-P1ABC-RERECORD unconditional binding

V3 SPEC §11/§12/§13 amendment (V2 F-V2-CH6-1 LOAD-BEARING fold) makes
Stage-0 binding UNCONDITIONAL to W10 (per SPEC.md:923 5-step
inheritance chain) — preserved byte-identical at V3. The V3 cosmetic
fold to P3-C `:36` + `:423` STRENGTHENS the textual mirror across
artefacts (P3-C language now reads the SPEC's unconditional wording
verbatim), eliminating the V2 non-blocking F-V3-CH6-3 textual
inconsistency. CH2 axis is not directly affected; the rerecord enables
measurability of inner primitives whose CH2 verdicts (C1 / C3 / C7)
are already grammar-neutral by P2-F verdict. CH2 binding holds.

### §3.4 — Wave-numbering reconcile preserves CH2 measurement-based gates at V3

The V2 F-V2-CH6-2 wave-numbering reconcile is preserved at V3
byte-identical (P3-B is V2-LOCKED; SPEC.md is V2-LOCKED). The §1.11
per-wave audit at V3 uses the V2-locked W0..W11 numbering verbatim.
CH2 gate measurability via Lock 14 baseline gate greps at each wave's
exit remains gate-json-enforceable per SPEC §0.4 + §2.1. CH2 binding
holds.

## §4 — Falsifiability binding (CH2-axis specific at V3 LOCK)

CH2 measurability remains two-fold at V3 (preserved byte-identical
from V2):

1. **Compile-time / source-level** — Lock 14 baseline gate greps (per
   SPEC §8 W5 + §9 W6 + §10 W7 + §11 W8 + §12 W9 + §13 W10 exit
   gates) measure JSON-name presence in generic crates; ZERO is the
   bound. Executable from the shell and gate-json-enforceable per the
   SPEC §0.4 non-negotiable. V3 has no new wave-exit gate edits
   (SPEC.md V2-LOCKED byte-identical at V3 HEAD).

2. **Bench-row / runtime-level** — Per Lock 14 v+1 ("must exercise at
   least one non-JSON consumer or record a measured deletion/
   rejection") — the non-JSON consumer materialises as a CSS L4 /
   Sheets / BBNF-self bench row or checkasm parity row in the SAME
   wave as the SIMD body admission:
   - C1 carries grammar-specific terminator checkasm coverage
     (`p3a:66`) — byte-identical at V3.
   - C3 carries F-V2-CH2-1 (`p3a:93`) — checkasm row exercising CSS-
     permissive `byte_class_from_range_64` — byte-identical at V3.
   - C4 carries F-V2-CH2-2 (`p3a:106`) — BBNF-self string-escape
     consumer (same-shape) + variable-width CSS measured-rejection
     record — byte-identical at V3.
   - C7 carries the gold-standard CSS L4 + Sheets + BBNF-self trio
     (`p3a:145`) — byte-identical at V3.

The V3 cosmetic fold to P3-C `:36` + `:423` does not edit any of the
above bench-row / checkasm consumer cells; CH2 falsifiability binding
is **gate-enforceable byte-identical at V3 HEAD**. The W10 wave is the
host wave for the C3 + C4 same-wave non-JSON consumers AND for Stage-0
F-V2-P1ABC-RERECORD UNCONDITIONALLY — a single wave bundling the CH2-
strengthening consumer rows with the CH6-strengthening unconditional
Stage-0 ship, mutually reinforcing without conflict.

## §5 — Pre-blocked routes (REDRESS entries CH2 enforces at V3 LOCK)

The V2 CH2 §5 pre-block census carries forward byte-identical at V3
HEAD (no V3 edit on P3-E):

- **REDRESS 36-38, 85-86**: Lock 14 residue + old JSON helpers +
  generic JSON branches + `StructuralAlphabet::json`. SPEC §15 line
  1102 binds at V3 HEAD. W5 PRUNE-3 (SPEC §8) collapses the 8 per-
  grammar providers — directly closes 36-38 cluster. W7 BackendShape-
  only dispatch closes 85-86 cluster. V3 fold does not re-open these.
  CH2 binding holds.
- **NF-CH6-4 three-orthogonal-SIMD-bodies pre-block**: SPEC §15 line
  1118 binds at V3 HEAD. C1 consolidation honours the bind (§1.1). V3
  fold does not re-open. CH2 binding holds.
- **Lock 14 v+1 generated-output allowance scope** at LOCKS.md:222-229:
  generated files under `runtime/src/grammars/<name>/` may contain
  grammar names ONLY when emitted from the rostered generator. SPEC §1
  binds at V3; W6 PRUNE-4 (SPEC §9) operationalises (collapse 67
  hand-written per-grammar runtime files onto W5 template). V3 fold
  does not modify this. CH2 binding holds.
- **`parse-that-regex::StringFlags::HAS_ESC` JSON-flavored naming
  carry-over** at `sk-v14-audit-overfit-lock14-scan.md:9` (cited at
  P2-F §3 note 1 = `p2f:264`): V2's NON-BLOCKING V2-CH2 observation
  carry-forward continues at V3 (Lock 14 closure obligation surfaces
  at W10 implementation time inside C4's owner-path `lib.rs:718`
  `unescape_string` rewrite per V2 §5 + V3 carry-forward). V3 cohort
  LOCK is not gated on this granularity (V2 explicit non-blocking
  clause + V3 byte-identical preservation). CH2 binding holds.
- **REDRESS 126 carry-through** at SPEC §15 line 1112: V3 §1.4 CH3
  NF-CH6-3 C2 scalar-ref evidence upgrade carry-through preserved at
  V3 SPEC byte-identical; primitive consumption requires post-W7
  runtime divergence. No V3 fold edit re-opens. CH2 binding holds.

## §6 — V3 LOCK declaration (CH2-axis)

Per `ORCHESTRATOR.md §3Z` cohort LOCK rule:
**"≥ 95 % ACCEPT × 2 consecutive cycles + zero orphan REVISE + zero
critical defect → cohort LOCK."**

CH2 trajectory:

| Cycle | ACCEPT-rate | Disposition vs LOCK rule | LOCK status |
|---|---|---|---|
| V1 | 81.8 % (9/11) — 2 REVISE on C3 + C4 | below 95 % floor | not LOCK-eligible |
| V2 | 100.0 % (11/11) — 2 V1 REVISEs discharged via F-V2-CH2-1 + F-V2-CH2-2 | first ≥ 95 % cycle; LOCK-eligible if V3 confirms | LOCK-pending |
| **V3** | **100.0 % (11/11) — byte-identical preservation across V2→V3 transition; F-V3-CH6-3 cosmetic fold does NOT touch CH2 surface** | **second consecutive ≥ 95 % cycle; zero open REVISE; zero critical defect** | **LOCK (2-cycle confirmed)** |

**CH2 LOCKs at V3 close**. Per `PASS-3-SYNTHESIS-PLAN.md §3` CH2
specialised wording ("Lock 14 holds: the SPEC's §2.1 generality gate
must be present and must require, for every generic-crate edit, a non-
JSON proof — CSS L4 / Sheets / BBNF-self"), the V3 SPEC §2.1 at
SPEC.md:294-313 preserves the verbatim Lock 14 gate text (V2-LOCKED
byte-identical at V3 HEAD). All 8 P3-A candidates carry the grammar-
neutral verdict trace through P2-F NEUTRAL-CONFIG-DRIVEN /
NEUTRAL-WIRED at V3. C3 + C4 — the two V1 REVISE candidates — carry
both V2 discharge routes (F-V2-CH2-1 + F-V2-CH2-2) byte-identical at
V3. Zero P3-B wave admits JSON policy into a generic crate at V3
HEAD; the per-wave audit ledger holds byte-identical. The CH2 lens
satisfies the 2-cycle cohort LOCK rule.

## §7 — Sources

### §7.1 — Authority

- `restart/skinny/tranches/sk-v14/research/p3/hardening/V3/CHALLENGE-CONTEXT.md`
  (43 lines; HEAD pin `867b0cd0b` at execution = HEAD verbatim;
  V3-LOCK-trigger seed).
- `restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md §3` (CH2
  specialised to S-P3 per V3 CHALLENGE-CONTEXT §2).
- `restart/prompts/ORCHESTRATOR.md §3W` (universal lens registry) +
  §3Z (cohort LOCK rule; V≤5 ceiling).
- `restart/locks/LOCKS.md:220-263` Lock 14 + v+1 amendments (primary
  CH2 authority; line 259 strict-read disjunction).
- `restart/locks/LOCKS.md:282-340` Lock 16 v+1 SIMD/ASM allowlist +
  abstract-primitive cross-grammar declarations (line 287 binds C3's
  digit-block abstract-primitive verbatim).

### §7.2 — V3 artefacts under review (HEAD pin: 867b0cd0b)

- `restart/skinny/tranches/sk-v14/SPEC.md` (V2-LOCKED at V3; 1187
  lines; byte-identical at V3 HEAD per `git diff` 0 lines on SPEC.md).
- `restart/skinny/tranches/sk-v14/DISPATCH-PROMPT.md` (V1-LOCKED; 344
  lines; zero V3 edits; CH2 N/A — process discipline).
- `restart/skinny/tranches/sk-v14/research/p3/p3a-candidate-shortlist.md`
  (V2-LOCKED at V3; 316 lines; byte-identical at V3 HEAD;
  F-V2-CH2-1 at line 93 + F-V2-CH2-2 at line 106 preserved verbatim).
- `restart/skinny/tranches/sk-v14/research/p3/p3b-wave-sequencing.md`
  (V2-LOCKED at V3; 410 lines; byte-identical at V3 HEAD).
- `restart/skinny/tranches/sk-v14/research/p3/p3c-falsifiability-gates.md`
  (V3-amended; 537 lines; F-V3-CH6-3 cosmetic fold at lines 36 + 423
  mirroring SPEC §13:982 unconditional Stage-0 binding wording).
- `restart/skinny/tranches/sk-v14/research/p3/p3d-telemetry-schema.md`
  (V1-LOCKED; 168 lines; zero V3 edits).
- `restart/skinny/tranches/sk-v14/research/p3/p3e-preblocked-ledger.md`
  (V1-LOCKED; 903 lines; zero V3 edits; REDRESS 36-38, 85-86 + Lock
  14 residue census per §5).
- `restart/skinny/tranches/sk-v14/research/p3/p3f-spec-draft.md`
  (V1-LOCKED; 245 lines; zero V3 edits).
- `restart/skinny/tranches/sk-v14/research/p3/hardening/HARDENING-S-P3-V2-CONSOLIDATED.md`
  (V2 aggregator; 594 lines; §2.4 F-V2-CH2-1 + F-V2-CH2-2 discharge
  ratification at lines 243-273; §3.3 C3/C4 same-wave-consumer
  convergence at lines 380-396; §4.1 F-V3-CH6-3 LIGHT cosmetic
  non-blocking residual at lines 418-444).

### §7.3 — V2 carry-forward + V1 retrospect

- `restart/skinny/tranches/sk-v14/research/p3/hardening/V2/CH2.md`
  (697 lines; V2 100.0 % ACCEPT 11/11; first ≥ 95 % cycle for CH2;
  V3 confirming-cycle template).
- `restart/skinny/tranches/sk-v14/research/p3/hardening/V1/CH2.md`
  (V1 81.8 % ACCEPT, 2 REVISE on C3 + C4; the REVISE prescription at
  V1 §1.3 + §1.4 actions discharged via V2 micro-fold).

### §7.4 — S-P2 V3 grammar-neutral verdict authority (LOCKED upstream)

- `restart/skinny/tranches/sk-v14/research/p2/p2f-grammar-neutral.md`
  (360 lines; §1.1 admission categories; §2.1-§2.14 per-candidate
  entries; §3 table at lines 247-260; §3 note 1 Sheets-doubled-quote-
  vs-backslash divergence at line 264; §4 inter-axis convergence at
  lines 269-272). S-P2 V3 §3Z cohort LOCK at HEAD `4db55d284`
  preserves CH2's upstream authority byte-identical.
- `restart/skinny/tranches/sk-v14/research/p2/p2a-sota-teardown.md`
  (367 lines; V1-LOCKED 7/7 grammar-neutral status per §2.1).
- `restart/skinny/tranches/sk-v14/research/p2/p2e-parse-that-gaps.md`
  (342 lines; Gap 7.5 `byte_class_from_range_64` lineage at line 203
  + grammar-neutrality table at line 244).

### §7.5 — Lock authority + audit-overfit residue

- `restart/locks/LOCKS.md:220-263` Lock 14 + v+1 generated-output
  allowance + per-wave gate enforcement + non-JSON-consumer admission
  gate; line 259 strict-read disjunction ("must exercise at least one
  non-JSON consumer or record a measured deletion/rejection") is the
  load-bearing CH2 LOCK criterion.
- `restart/locks/LOCKS.md:282-340` Lock 16 v+1 SIMD/ASM allowlist +
  abstract-primitive cross-grammar declarations (line 287 binds C3's
  digit-block abstract-primitive verbatim).
- `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-lock14-scan.md:9`
  `parse-that-regex::StringFlags::HAS_ESC` JSON-flavored naming
  carry-over (NON-BLOCKING CH2 observation; carry-forward to W10
  implementation time per V2 §5 + V3 §5).

### §7.6 — Empirical floor + REDRESS

- `skinny/RESULTS.md` (185 lines; bench-row evidence floor; CH2's
  bench-row non-JSON-consumer materialisation lands as checkasm parity
  row (C3) + BBNF-self consumer row (C4) + CSS L4 + Sheets + BBNF-self
  trio (C7) at W10).
- `skinny/REDRESS.md` (~5041 lines; REDRESS 36-38, 85-86 Lock 14
  residue per §5; REDRESS 126 carry-through per §5).

### §7.7 — Source-file verifications at V3 HEAD (executable cite log)

Five source-file verifications re-executed at V3 HEAD `867b0cd0b`
during V3 CH2 disposition per LAC-1E-12 executable-cite mandate:

1. `grammar/bbnf/bbnf.bbnf:11-13` — preserved at V3 HEAD (V3 commit
   does not touch `grammar/`). **VERIFIED**.
2. `skinny/crates/parse-that-regex/src/lib.rs:718` `unescape_string` —
   preserved at V3 HEAD (V3 commit does not touch `parse-that-regex/`).
   **VERIFIED**.
3. `skinny/crates/parse-that-regex/src/lib.rs:945` `read_hex_unit_scalar`
   — preserved at V3 HEAD. **VERIFIED**.
4. `skinny/crates/bbnf-simd/tests/checkasm_byte_class_from_eq_set_64.rs`
   — sibling-shape template PRESENT at V3 HEAD (V3 commit does not
   touch `bbnf-simd/`). **VERIFIED**.
5. `skinny/crates/bbnf-simd/tests/checkasm_byte_class_from_range_64.rs`
   — Stage-A authoring target correctly NOT-PRESENT at V3 HEAD per
   Lock 16 same-commit discipline. **VERIFIED**.

Plus V3-specific verifications:

6. `git diff f25c3af2e..867b0cd0b -- p3a-candidate-shortlist.md
   p3b-wave-sequencing.md p3d-telemetry-schema.md p3e-preblocked-
   ledger.md p3f-spec-draft.md SPEC.md DISPATCH-PROMPT.md` returns
   **0 lines** — 7 V2-LOCKED artefacts byte-identical V2→V3.
   **VERIFIED**.
7. `git show --stat 867b0cd0b` — V3 commit touches exactly 2 files
   (`V3/CHALLENGE-CONTEXT.md` + `p3c-falsifiability-gates.md` 4-line
   edit). **VERIFIED**.
8. `grep -n "UNCONDITIONALLY per S-P2 V3 §6.3" p3c-falsifiability-gates.md`
   returns matches at `:36` + `:423` (V3 fold sites). **VERIFIED**.
9. `grep -n "UNCONDITIONALLY per S-P2 V3 §6.3" SPEC.md` returns matches
   at `:982` + `:990` + `:1000` (binding artefacts; V3 P3-C mirrors
   these). **VERIFIED**.
10. `grep -n "must exercise at least one non-JSON consumer" LOCKS.md`
    returns Lock 14 v+1 strict-read at line 259. **VERIFIED**.

All ten citations resolve at V3 HEAD; CH2 disposition is executable-
cite-clean for the 2-cycle LOCK confirmation cycle.
