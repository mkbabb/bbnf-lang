# Alpha-F Contract Draft For SK-V10

Date: 2026-05-19.

Role: PASS-ALPHA alpha-F contract draft. This slice materializes the initial
SK-V10 contract surface:

- `restart/skinny/tranches/sk-v10/SYNTHESIS.md`
- `restart/skinny/tranches/sk-v10/HANDOFF.md`

It does not create `SPEC.md` or `DISPATCH-PROMPT.md`. Downstream S-P3 owns the
detailed wave manifest after Alpha challenge convergence.

## Corrected Diagnosis

SK-V9 proved that the typed product plane is where bbnf currently beats or
matches SOTA. W1 admitted Apache/CITM typed rows with fresh measurement. W2
admitted the retained grammar proof. W3 was falsified: two faithful
union-substrate implementations regressed uniformly and REDRESS 98 retired the
gate.

The SK-V10 contract therefore removes W3 from the candidate set, demotes W4 to
existing-substrate kernels only, retires parse-only SOTA from §0, makes
`direct_to_struct` the first JSON frontier, and requires micro-prove-first
before any substrate/kernel wave reaches S-P3.

## Contract Materialized

`SYNTHESIS.md` carries:

- Alpha close condition.
- SK-V10 cycle close posture.
- Current measured state.
- Goalset centered on direct-plane diagnosis plus typed product-plane extension.
- Pre-blocked routes from REDRESS 94-98.
- Candidate shortlist and telemetry binding.
- Micro-prove-first as a non-negotiable for substrate/kernel candidates.
- Pass Omega routing for the substrate-ceiling lock amendment and totality T-P1
  routing for non-JSON generalization.

`HANDOFF.md` carries:

- Read-first order.
- Current state and candidate boundaries.
- Next move through Alpha CHALLENGE, G-Alpha, then S-P1/S-P2/S-P3.
- Explicit refusal conditions for W3 or stale W4 cascade dispatch.

## Challenge Notes

Alpha CHALLENGE should reject this contract if it finds:

- any W3 implementation dispatch authority;
- any W4 wave that cites the retired W3 substrate;
- any parse-only row scored as SOTA admission under `S / NO-GO`;
- any kernel/substrate candidate scoped without same-host micro proof;
- any Canada typed shortcut;
- any typed candidate lacking generated/serde/sonic parity and same-run typed
  comparator evidence;
- any sidecar freshness claim that becomes behavior movement by implication.

After challenge convergence, the orchestrator may present `G-ALPHA-SK-V10`.
