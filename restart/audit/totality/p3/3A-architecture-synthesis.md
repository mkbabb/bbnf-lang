---
agent: 3A
pass: T-P3-synthesis
cycle: V1
generated_at: 2026-05-23T22:00:00Z
t_p1_inventories_consumed: [1A, 1B, 1C, 1D, 1E, 1F-coherence-scan, 1F-anti-pattern, 1F-past-corpora]
t_p2_dossiers_consumed: [2A, 2B, 2C, 2D, 2E, 2F]
v1_surface_targeted: ARCHITECTURE.md
proposed_deltas_count: 12
delta_summary:
  carried_from_prior_cycle: []
  removed: []
  answered: []
  newly_added: [ARCH-3A-D01, ARCH-3A-D02, ARCH-3A-D03, ARCH-3A-D04, ARCH-3A-D05, ARCH-3A-D06, ARCH-3A-D07, ARCH-3A-D08, ARCH-3A-D09, ARCH-3A-D10, ARCH-3A-D11, ARCH-3A-D12]
prior_cycle_dispositions_folded:
  accepted: []
  rejected: []
  revised: []
---

## Executive Summary

T-P3 proposes architecture deltas only; Pass Omega CRUD writes them post-G-Omega. The V1 ARCHITECTURE.md surface diverges materially from T-P1 V6 truth and T-P2 V2-V7 grounding along five vectors. (i) The §7.3 substrate-union narrative still reads as one tape unioned with direct-to-struct (`restart/ARCHITECTURE.md:1060`), but at HEAD substrate-union resolves as two structurally independent cursor types — `ParserState.cursor` over offset-tape and `DirectParser.cursor` over raw bytes — sharing no `Tape` abstraction per 1A-DIV-008. (ii) §7.3's eight-step `derive_backend_shape` cascade (`restart/ARCHITECTURE.md:1090`) is refuted as a literature-grounded optimizer; the live path routes through `backend_egraph::select` + `decision_csp::finalize_rule` per P1-1B-D3 and SOTA grounds search-and-extraction, not a fixed P1-P8 priority. (iii) Four of five `BackendShape` lowerers emit marker strings (`restart/ARCHITECTURE.md:1102`); only `SinkOnly` is admitted per 2D's admission ledger. (iv) §9 templates `generated.rs`/`visitor.rs`/`host.rs` exist in 0 of 9 main-workspace grammars (`restart/ARCHITECTURE.md:1648`); Pattern H = 67 hand-written runtime files across 9 grammar dirs, with 127 grammar-named reexports at `crates/core/src/runtime/mod.rs:25-71`. (v) `CollapsedStage` is x86-AVX-512-only per T2A-REF-002 and the live `admits_collapsed_stage` predicate leaks aarch64 admission per LAC-2D-06. Twelve proposed deltas align ARCHITECTURE.md with that truth while preserving the 5-shape canon, the substrate-union fence, and the no-new-directive/no-new-BIR/no-new-substrate gate per §8 PASS-3-SYNTHESIS.

## V1 Delta Summary

This is the first T-P3 cycle against the new T-P1 V5/V6 LOCK and T-P2 V5 CONVERGED inventories. No prior accepted T-P3 cycle carries forward. All twelve items are newly-added proposed deltas for `restart/ARCHITECTURE.md`. They are deliberately additive (status blocks, taxonomies, admission ledgers, evidence schemas, hardening clauses) rather than structural rewrites; the 5-shape `BackendShape` canon at `restart/ARCHITECTURE.md:1063-1087` stays intact per PASS-3-SYNTHESIS §8.2, and no delta proposes a new directive, new BIR variant, new public substrate, or BackendShape expansion (PASS-3-SYNTHESIS §8.5). Refuted-technique consequences carry forward: SKELETON triple (`FSM_DISPATCH_THREADED`/`FRAME_PUSH_BOUNDED`/`FRAME_POP_BOUNDED`) DELETED per 2B §R3; asmjson AVX-512BW NON-ADMISSIBLE for aarch64 per T2A-REF-002 + LAC-2D-06; CollapsedStage cited as paper-architecture per CH4-F2 ledger.

## Proposed Delta Table

| proposed delta | source T-P1/T-P2 finding-id cited | affected V1-surface section | rationale |
|---|---|---|---|
| ARCH-3A-D01 — Replace the §0 conflict-ledger SK-V6 fold-back framing with an SK-V14 totality-status block: T-P1 V5/V6 LOCKED inventories + T-P2 V5 CONVERGED dossiers are current authority; T-P3 proposes deltas; G-Omega user gate is the only mandatory relinquish; any new directive/BIR variant/public substrate/BackendShape expansion remains G-Omega-gated. | T-P3 dispatch §1 (`restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:22-30`); PASS-3-SYNTHESIS §8.5 no-new-directive (`restart/prompts/totality/PASS-3-SYNTHESIS.md:214`); SK-V14 binding `restart/skinny/tranches/sk-v14/SYNTHESIS.md:54-60` audit-zero baseline. | `restart/ARCHITECTURE.md:10-48` (§0 Authority And Conflict Ledger), currently SK-V6-oriented (`restart/ARCHITECTURE.md:19`). | The §0 ledger frames fold-back as SK-V6 while handoff and converged T-P1/T-P2 made SK-V14 the active synthesis authority; the §0 block also needs the explicit "no T-P3 surface edit; Pass Omega CRUD only" boundary. |
| ARCH-3A-D02 — Add a §7.2 "Live BIR Coverage Status" table separating the architectural 20-variant alphabet from the live 13-variant `BackendExpr` plus `Recognizer::SimdScan`; mark `PrattSpine`/`CallHost`/`LayoutScope`/`ErrorRecover`/`PathEval`/`DebugMark` as unimplemented in skinny; preserve the no-new-BIR-variant gate. | 1A-SUB-004 (`restart/audit/totality/p1/1A-substrate-evidence.md:57`); 1A-DIV-001 (`restart/audit/totality/p1/1A-substrate-evidence.md:77`); P1-1B-D1 (`restart/audit/totality/p1/1B-codegen-evidence.md:79`); PASS-3-SYNTHESIS §8.5 (`restart/prompts/totality/PASS-3-SYNTHESIS.md:214`). | `restart/ARCHITECTURE.md:911-1031` (§7.2 Backend IR; 20-variant alphabet at `:922-944`); `restart/ARCHITECTURE.md:1017-1031` (BIR invariants). | V1 claims 20 BIR variants as target alphabet, but live `BackendExpr` covers 13 variants (`skinny/crates/ir/src/lib.rs:354-389`) and `SimdScan` is a separate `Recognizer` (`:391-398`); the reader cannot distinguish target from live without a status table. |
| ARCH-3A-D03 — Rewrite §7.3 cost-model derivation: preserve the 5-shape enum, but replace the hardcoded 8-step P1-P8 cascade (`restart/ARCHITECTURE.md:1090-1098`) with a "candidate generation → equality saturation (bounded) → CSP feasibility filter → cost extraction with active CostFacts" pipeline; demote the priority steps to diagnostic vocabulary; bind every `BackendExpr` node and extraction result to declare `substrate_target ∈ {local_temp_only, existing_tape, direct_sink, admitted_fact_output}`. | P1-1B-D3 (`restart/audit/totality/p1/1B-codegen-evidence.md:81`); T2D-EGRAPH-EXTRACTION + T2D-BURG-FINITE-ALTERNATIVES + T2D-CSP-FEASIBILITY-LAYER (`restart/audit/totality/p2/2D-cost-model.md:114-119`); LAC-2D-01 + LAC-2D-06 (`restart/audit/totality/p2/2D-cost-model.md:260,265`); T-P2 V4 ledger F-CH5-V1-03 (`restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:42-58`). | `restart/ARCHITECTURE.md:1047` (CostFacts row); `restart/ARCHITECTURE.md:1060-1110` (BackendShape + derivation algorithm + per-shape outputs); `restart/ARCHITECTURE.md:1700` (`cost-driven-rewrites` budget pool in §10.1). | T-P2 grounds equality saturation + BURG + CSP as the SOTA decision-engine class; the P1-P8 cascade is bbnf-local heuristic with no published authority. Live skinny already routes through `backend_egraph::select` + `decision_csp::finalize_rule` (`skinny/crates/passes/src/lib.rs:476-478`); ARCHITECTURE.md should reflect that. |
| ARCH-3A-D04 — Extend the §7.3 CostFacts row to register `ActiveCostFacts` (egraph candidate counts, selection trace hash, rewrite-order variance, cascade-fallback) and `DecisionCspFacts` (CSP solver status/budget/parity, choose-backend-shape status), and add an "active fields required" subsection: objective vectors, frontier/dominance, scalarization profile, extraction method, evidence freshness, stale/static fallback marker per LAC-2D-02. | P1-1B-D2 revised + P1-1B-D11 + P1-1B-D12 (`restart/audit/totality/p1/1B-codegen-evidence.md:80,88,89`); LAC-2D-02 (`restart/audit/totality/p2/2D-cost-model.md:261`); 1B amendment candidate (`restart/audit/totality/p1/1B-codegen-evidence.md:114`). | `restart/ARCHITECTURE.md:1053` (CostFacts row); `restart/ARCHITECTURE.md:1184-1260` (§7.5 Diagnostic Vocabulary); `restart/ARCHITECTURE.md:1262-1328` (§7.6 Backend Trait). | V1 names CostFacts with objective/frontier/scalarization but live struct lacks them at top level; W7 lower-to-rust is fail-closed on active-cost + sat-CSP (`skinny/crates/codegen/src/lower/rust.rs:27-77`), an admission gate ARCHITECTURE §7.6 must register. |
| ARCH-3A-D05 — Add a "Live BackendShape Admission Ledger" subsection to §7.3: per-shape rows naming abstract primitive + published citation + hardware gate + scalar oracle status + checkasm cell + corpus parity + same-wave consumer + admission disposition; SinkOnly ADMITTED via CSS L4 declaration-values, EagerTape/OffsetTape/EventTape/CollapsedStage NOT-ADMITTED (marker-string lowerers); each non-admitted shape resolves to either kernel implementation per LAC-2D-04 or Lock 10 amendment retiring it. | P1-1B-D6 (`restart/audit/totality/p1/1B-codegen-evidence.md:84`); 2D BackendShape Admission Ledger (`restart/audit/totality/p2/2D-cost-model.md:164-179`); 2B Executive Summary §R3 (`restart/audit/totality/p2/2B-primitive-vocabulary.md:74-76`); T2A-REF-002 asmjson AVX-512 x86-only (refuted in `restart/audit/totality/p2/2A-sota-landscape.md` Executive Summary); LAC-2D-04 (`restart/audit/totality/p2/2D-cost-model.md:263`). | `restart/ARCHITECTURE.md:1100-1108` (§7.3 per-shape lowering output table); `restart/ARCHITECTURE.md:1107-1108` (`CollapsedStage` artefact triple); `restart/ARCHITECTURE.md:1662-1681` (§10 Codegen And Lowerers). | V1 lists artefact triples for all 5 shapes as if all five emit kernels; T-P2 admission ledger shows 1/5 admitted at HEAD. ARCHITECTURE.md must distinguish architectural intent from live admission so readers and Pass Omega CRUD do not paper-close on the four marker-string shapes. |
| ARCH-3A-D06 — Add a §7.3 "Substrate-Union Resolution Disposition" block with two disposed parts. **Part (a) — cross-call retention (DISPOSED at 3C V1):** the no-cross-call-retained-classifier-state rule is elevated to Lock 1 substrate-union v+1 per 3C-L01-substrate-union-v+1-elevation (LAC-2F-V5-02 ELEVATED at `restart/audit/totality/p3/3C-locks-crystallisation.md:31`); ARCHITECTURE.md §9.2 text mirrors that elevation. **Part (b) — two-cursor structural split (ROUTED to Pass Omega Ω-A, NOT to 3C):** 1A-DIV-008 records `ParserState.cursor` (offset-tape, `runtime/src/grammars/json/parser.rs:7-12`) and `DirectParser.cursor` (raw bytes, `codegen/src/json_typed_direct.rs:518-522`) as two structurally independent cursor types at HEAD; LAC-2F-V5-02 elevation addresses cross-call retention only, NOT the cursor-shape ratify-or-unify question. T-P3 V2 reroutes Part (b) to Pass Omega Ω-A architecture intake with receiver = Ω-A ARCH-CRUD-1 fold, blocker = `1A-DIV-008 records two structurally independent cursor types at HEAD`, gate = `Ω-A selects ratify-two-cursor OR mandate-unification before CRUD-1 §9.2 fold`. ARCHITECTURE.md §9.2 text mirrors the Ω-A-selected disposition once landed; until then §9.2 prose carries an explicit `cursor-shape ratify-or-unify pending Ω-A` carrier note rather than asserting a unified event cursor. | 1A-DIV-008 (`restart/audit/totality/p1/1A-substrate-evidence.md:84`); 1A-SUB-016 (`restart/audit/totality/p1/1A-substrate-evidence.md:69`); 1A-UNK-005 (`restart/audit/totality/p1/1A-substrate-evidence.md:106`); 1A-LOCK1-AMEND-001 (`restart/audit/totality/p1/1A-substrate-evidence.md:113`); 3C-L01-substrate-union-v+1-elevation (`restart/audit/totality/p3/3C-locks-crystallisation.md:31`); LAC-2F-V5-02 ELEVATED (T-P3 dispatch §1.3 + `restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:26,30`); 2D LAC-2D-06 (`restart/audit/totality/p2/2D-cost-model.md:265`); PASS-3-SYNTHESIS §8.2 5-shape coherence (`restart/prompts/totality/PASS-3-SYNTHESIS.md:211`). | `restart/ARCHITECTURE.md:1547-1577` (§9 Runtime Architecture + §9.1 Tape); `restart/ARCHITECTURE.md:1577-1660` (§9.2 Direct-To-Struct Union). | V1 §9.2 reads "direct builders do not bypass the substrate event stream" but live evidence is two parallel cursor surfaces with no shared event cursor; ARCHITECTURE.md must reflect (a) the LAC-2F-V5-02 cross-call retention elevation already disposed at 3C AND (b) the Ω-A-routed two-cursor structural decision as a separate open item, so the §9.2 union claim becomes auditable without paper-closing the cursor-shape question on a routing target that does not address it. |
| ARCH-3A-D07 — Add a §9 "Output-Plane Substrate Taxonomy": four planes — (1) retained tape (Eager/Offset/Event), (2) direct-sink (SinkOnly), (3) admitted same-plane fact-stream output (CSS L4 declaration-values), (4) transient scanner/capacity plane (StructuralIndex, comparator sidecars). The fact-stream plane is admitted-evidence with `substrate_target = admitted_fact_output`; it is NOT a sixth BackendShape, NOT retained substrate, NOT full CSS closure. | 1A-SUB-015 + 1A-DIV-006 + 1A-DIV-007 (`restart/audit/totality/p1/1A-substrate-evidence.md:68,82,83`); 1C-D7 (`restart/audit/totality/p1/1C-runtime-evidence.md:165`); LAC-1E-14 CSS L4 substrate-classification (`restart/audit/totality/p1/1E-locks-evidence.md:59`); LAC-2F-V5-02 elevated Lock 1 substrate-union v+1 (T-P3 dispatch §1.3 + `restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:26,30`); 2C grammar-neutrality (`restart/audit/totality/p2/2C-grammar-neutrality.md:74-89`). | `restart/ARCHITECTURE.md:1547-1660` (§9 Runtime Architecture); `restart/ARCHITECTURE.md:1716-1764` (§11 Performance Targets). | V1 rejects parallel substrates correctly but lacks a category for admitted CSS fact-stream evidence and comparator-plane sidecars; without taxonomy these become invisible to Pass Omega and risk later promotion to a sixth shape. |
| ARCH-3A-D08 — Replace §9 hand-wired-runtime narrative with a "Live Pattern H Status" table: 67 hand-written .rs files across 9 main-workspace grammar dirs (bbnf 8 / bnf 7 / css_l4 7 / css_pretty 7 / csv 7 / ebnf 7 / google_sheets 10 / json 7 / math 7); 0/9 carry `@generated` markers; 0/9 emit the §9 template files `generated.rs`/`visitor.rs`/`host.rs`; the live roster is `{mod, arena, builder, document, value, view} ± {parse_with, kind, serialize}`; per-grammar declaration crates absent. Lock 14 verification `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d` returns 9 (must return 0). | 1C executive summary + Pattern H census (`restart/audit/totality/p1/1C-runtime-evidence.md:40,46-65`); 1C-D1/D2/D6/D10/D11 (`restart/audit/totality/p1/1C-runtime-evidence.md:159-169`); LAC-1E-15 Pattern H 67-file recurrence vector (`restart/audit/totality/p1/1E-locks-evidence.md:55`); ARCH §9 generated-template clause (`restart/ARCHITECTURE.md:1646-1660`). | `restart/ARCHITECTURE.md:1646-1660` (§9 generated runtime template); `restart/ARCHITECTURE.md:1862-1890` (§13 File And Directory Discipline); `restart/ARCHITECTURE.md:1803-1862` (§12 Future Grammar Onboarding Test). | V1 says generated runtime modules under `runtime/src/grammars/<grammar>/`; reality at HEAD has zero generated runtime files and the literal substring "runtime/grammars" does not appear in `crates/`. Pattern H must be named so Lock 14 PRUNE-3/PRUNE-4 wave has a defined surface. |
| ARCH-3A-D09 — Add §7.4 "Generic-Crate Grammar-Name Leak Surface" subsection: enumerate the 4 leak classes — (a) `RuntimeProvider` enum 8 variants in `skinny/crates/codegen/src/grammar_profile.rs:17-26` + 8 `runtime_profiles()` entries + 7 CSS L4 match arms; (b) 8 per-grammar provider modules in codegen/; (c) 30 grammar-named symbol references across 15 files (Pattern H runtime per `crates/core/src/runtime/`); (d) 127 grammar-named reexports across 47 lines at `crates/core/src/runtime/mod.rs:25-71`. State Lock 14 invariant: any new grammar adds ZERO new .rs files in generic crates; manifest/registry must own per-grammar names. | P1-1B-D7 revised + P1-1B-D8 + P1-1B-D10 + P1-1B-D13 (`restart/audit/totality/p1/1B-codegen-evidence.md:85,86,87,90`); 1B Generic-Crate Census (`restart/audit/totality/p1/1B-codegen-evidence.md:60-65`); 1C Lock 14 Leak Audit (`restart/audit/totality/p1/1C-runtime-evidence.md:120-128`); D-1E-08 + LAC-1E-15 (`restart/audit/totality/p1/1E-locks-evidence.md:55,89`); 2C grammar-neutrality transfer contract (`restart/audit/totality/p2/2C-grammar-neutrality.md:89,157`). | `restart/ARCHITECTURE.md:1112-1183` (§7.4 SK-V5 Through SK-V13 Implementation Status); `restart/ARCHITECTURE.md:1862-1910` (§13 + §13.1 Lint Manifest); `restart/ARCHITECTURE.md:1812-1862` (§12.2 Per-Grammar Authority Table). | T-P1 1B + 1C document precise leak surfaces with line-anchored evidence; V1's generic-crate neutrality narrative reads as already-honoured but reality at HEAD has 30 reproducible Lock 14 violations from SK-V14 audit pack. The lint manifest at §13.1 must consume this enumeration to make leak detection executable. |
| ARCH-3A-D10 — Add a §7.3 + §10 PrimitiveFacts admission manifest: every Lock 16 primitive carries (1) abstract primitive name + published citation (2) hardware gate (ISA/feature macro) (3) scalar oracle path (4) checkasm parity command (5) corpus-parity evidence (6) same-wave consumer (path + measured row) (7) substrate target ∈ {local_temp_only, existing_tape, direct_sink, admitted_fact_output} (8) close state ∈ {wired, deleted, scalar-delegate-non-ASM, architectural-block-with-REDRESS}. Record the V1 SKELETON triple (`FSM_DISPATCH_THREADED`/`FRAME_PUSH_BOUNDED`/`FRAME_POP_BOUNDED`) as DELETED per 2B §R3. | LAC-1E-12 procedural addendum (T-P3 dispatch §1.1); 2B Layer-0/Layer-1 contract + SKELETON DELETE disposition (`restart/audit/totality/p2/2B-primitive-vocabulary.md:38-72`); 2E aarch64 PRIMARY 13 entries (`restart/audit/totality/p2/2E-host-arch-esoterica.md` Executive Summary); 2F 9 primitive gaps (`restart/audit/totality/p2/2F-parse-that-gaps.md:42`); LAC-2D-04 (`restart/audit/totality/p2/2D-cost-model.md:263`); 2A T2A-LAC-V1-03 eight-cell manifest (`restart/audit/totality/p2/2A-sota-landscape.md` LAC table); Lock 16 close-state vocabulary (`restart/locks/LOCKS.md:335-342`). | `restart/ARCHITECTURE.md:1055` (PrimitiveFacts row); `restart/ARCHITECTURE.md:1184-1260` (§7.5 Diagnostic Vocabulary); `restart/ARCHITECTURE.md:1662-1716` (§10 Codegen And Lowerers); `restart/ARCHITECTURE.md:1716-1764` (§11 Performance Targets). | V1 names PrimitiveFacts but lacks the 8-cell schema and SKELETON close-state evidence; absent the manifest the SK-V14 SIMD pre-block has no architectural anchor and primitives can paper-close without scalar oracle + checkasm + same-wave consumer. |
| ARCH-3A-D11 — Add §7.3 + §11 admission-state vocabulary block: `CollapsedStage` predicate at `passes/src/lib.rs:874-876` must co-require `target.arch == x86` alongside `target.avx512bw` + `Entry(_)` so aarch64 admission is mechanically refused per LAC-2D-06 / CH5 F-CH5-V1-03; AVX-512 literature (asmjson, Sneller) is x86 architecture-pressure ONLY and cannot close M5/aarch64 rows per Lock 16 v+1 close-state vocabulary; aarch64 path requires source-backed candidate from 2E (UNKNOWN-2D-05). | T2A-REF-002 asmjson AVX-512BW x86-only NON-ADMISSIBLE (`restart/audit/totality/p2/2A-sota-landscape.md` Executive Summary + refuted table); 2D T2D-COLLAPSEDSTAGE-X86-ONLY + LAC-2D-06 (`restart/audit/totality/p2/2D-cost-model.md:123,265`); 2D Refuted Assertions table CollapsedStage row (`restart/audit/totality/p2/2D-cost-model.md:236`); P1-1B-D4 (`restart/audit/totality/p1/1B-codegen-evidence.md:82`); 2E aarch64 PRIMARY 13 entries + S-P2 V3 P2C candidates (`restart/audit/totality/p2/2E-host-arch-esoterica.md` Executive Summary). | `restart/ARCHITECTURE.md:1081-1086` (`CollapsedStage` enum variant); `restart/ARCHITECTURE.md:1108` (per-shape lowering output for `CollapsedStage`); `restart/ARCHITECTURE.md:1716-1764` (§11 Performance Targets). | V1 narrates `CollapsedStage` as a fifth shape selected by CPUID/cost model but live predicate is aarch64-leaky and admits via cross-build target inheritance; ARCHITECTURE.md must document architecture pressure boundary so the M5 Max host context (the primary skinny target) does not silently inherit x86 admissions. |
| ARCH-3A-D12 — Add §7.5 + §10 "Parse-That / Regex Import Boundary" subsection: regex/HIR facts (Cox NFA→DFA, regex-automata lazy-DFA per SRC-05/SRC-06 in 2D) may feed compile-time CostFacts and the decision engine ONLY through `bbnf-regex` import gates and grammar-derived regex-info-to-`BackendExpr` facts; opaque pattern strings, local worktree-only evidence, and `regex-automata` as a runtime dependency are inadmissible per `docs/parse-that/regex-engine.md:9`; SIMD scanner outputs remain local-temp-only or feed existing tape / direct-sink — no new public substrate or BIR surface without G-Omega. | T2D-REGEX-NFA-DFA-PLAN (`restart/audit/totality/p2/2D-cost-model.md:120`); 2F Executive Summary 2 regex/HIR gaps + 4 SIMD-scan gaps + 2 string gaps + 1 float gap (`restart/audit/totality/p2/2F-parse-that-gaps.md:41-49`); 2F three load-bearing refutations (`restart/audit/totality/p2/2F-parse-that-gaps.md:59-68`); 2F V5 LAC-2F-V5-01 `bbnf-regex` absorption decision; T-P3 dispatch §1.3 LAC-2F-V5-02 elevated Lock 1 substrate-union v+1 (`restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:26`); PASS-3-SYNTHESIS §8.5 no-new-substrate (`restart/prompts/totality/PASS-3-SYNTHESIS.md:214`). | `restart/ARCHITECTURE.md:911-1031` (§7.2 BIR operands list — `RegexProgram`); `restart/ARCHITECTURE.md:1053-1055` (CostFacts + PrimitiveFacts); `restart/ARCHITECTURE.md:1184-1260` (§7.5 Diagnostic Vocabulary); `restart/ARCHITECTURE.md:1662-1716` (§10 Codegen And Lowerers). | T-P2 2F shows live `parse-that-regex` is JSON-oriented (8-byte SWAR for `unescape_string`, no bespoke Thompson-NFA→DFA pipeline at HEAD) and the SIMD bodies live in `bbnf-simd`; ARCHITECTURE.md must define the import boundary so regex/HIR facts feed costs without smuggling a new runtime substrate. |

## Consequences

| delta | positive consequence | cost / risk consequence | propagation surface count |
|---|---|---|---:|
| ARCH-3A-D01 | Aligns §0 authority block with SK-V14 + handoff + converged T-P1/T-P2; closes SK-V6 vs SK-V14 contradiction (`restart/ARCHITECTURE.md:19`). | Downstream lock/status docs must stop citing stale SK-V6 framing; small wording risk in HANDOFF.md. | 3 surfaces — ARCHITECTURE §0, HANDOFF, future G-Omega narrative. |
| ARCH-3A-D02 | Reader can distinguish architectural BIR target from live coverage; closes paper-close risk around the 20-variant alphabet (1A-DIV-001). | Forces VM/lowerer status updates to track per-variant coverage. | 4 surfaces — BIR spec §7.2, lowerers §10, VM replay tests, diagnostics §7.5. |
| ARCH-3A-D03 | Makes BackendShape selection executable and evidence-backed while preserving the 5-shape fence; aligns with grounded SOTA (egg/BURG/CSP). | Adds schema and freshness burden to CostFacts; invalidates P1-P8 cascade as canonical reference. | 5 surfaces — §7.3 derivation algorithm, §7.5 diagnostics, §10.1 rewrite-budget pools, decision-engine waves, SK-V14 C-4 falsifiability gate. |
| ARCH-3A-D04 | Registers the impl-exceeds-spec `ActiveCostFacts`/`DecisionCspFacts` evidence vectors and the W7 admission gate already enforced by `lower_to_rust`. | Doc-only delta but binds future CostFacts schema changes to the V1 frontier-and-extraction discipline. | 4 surfaces — §7.3 CostFacts row, §7.5 diagnostics, §7.6 Backend trait, decision-engine wave. |
| ARCH-3A-D05 | Truthful per-shape implementation map; prevents EventTape/CollapsedStage from closing by assertion; surfaces the 1/5 admission rate as load-bearing fact. | §10 lowerers must declare marker-string-or-kernel state each tranche; raises visibility of architectural-block decisions. | 5 surfaces — §7.3 shape table, §10 lowerers, §11 perf rows, Lock 10/16 amendments, BackendShape admission ledger. |
| ARCH-3A-D06 | Forces explicit resolution of the two-cursor vs one-cursor question (1A-DIV-008) rather than leaving the §9.2 union claim ambiguous; separates the cross-call retention carrier (DISPOSED at 3C V1 via LAC-2F-V5-02 ELEVATED) from the cursor-shape ratify-or-unify carrier (rerouted to Pass Omega Ω-A architecture intake) so neither paper-closes on the other's routing target. | Either ratifies a substrate shape that diverges from `Tape`+cursor wording, or schedules a 400-900 LOC unification wave; until Ω-A selects, §9.2 carries an explicit `cursor-shape ratify-or-unify pending Ω-A` carrier note. | 4 surfaces — §9.1 tape, §9.2 union, Lock 1, Pass Omega Ω-A architecture intake. |
| ARCH-3A-D07 | Gives CSS fact-stream evidence a defined home without expanding BackendShape; categorises comparator sidecars + scanner planes correctly. | Future CSS/SIMD rows must classify outputs explicitly; raises bar for "admitted-evidence" wording. | 5 surfaces — §9 substrate taxonomy, §11 benchmarks, Lock 1, LAC-1E-14, T-P3 §3C. |
| ARCH-3A-D08 | Names Pattern H = 67 as the Lock 14 PRUNE-3/PRUNE-4 work surface; closes the SK-V14 §0 audit-zero gap. | Requires §9 template prose to be rewritten as live-vs-target; multi-tranche generator-template wave implied. | 4 surfaces — §9 template, §12 onboarding test, §13 directory discipline, Lock 14 enforcement. |
| ARCH-3A-D09 | Converts the abstract Lock 14 obligation into an enumerated leak surface with line-anchored evidence; the SK-V14 30-violation audit becomes auditable architecture. | Forces §13.1 lint manifest to consume the enumeration; surfaces the per-grammar declaration-crate (1C-D10) decision. | 5 surfaces — §7.4 status, §12 onboarding, §13.1 lint manifest, Lock 14, future grammar-onboarding test. |
| ARCH-3A-D10 | Makes PrimitiveFacts admission an 8-cell manifest with executable verification per LAC-1E-12; records SKELETON triple deletion (2B §R3) as architecture truth. | Adds telemetry burden to every SIMD/ASM primitive route; tightens admission so primitives without same-wave consumer cannot land. | 5 surfaces — §7.3 PrimitiveFacts, §7.5 diagnostics, §10 codegen, §11 SIMD/ASM rows, Lock 16. |
| ARCH-3A-D11 | Closes the live `admits_collapsed_stage` aarch64 leak; documents T2A-REF-002 architecture pressure boundary so M5 Max evidence cannot inherit x86 admissions. | Disables `CollapsedStage` admission on aarch64 until 2E source-backed candidate lands; tightens predicate to x86-only. | 4 surfaces — §7.3 `CollapsedStage` variant, §7.3 lowering output table, §11 performance rows, Lock 16. |
| ARCH-3A-D12 | Defines the regex/HIR import boundary so 2F's NFA/DFA/lazy-DFA pipeline can feed costs without smuggling a runtime substrate; preserves no-new-BIR gate. | Blocks opaque-pattern shortcuts and `regex-automata`-as-runtime-dep; tightens `bbnf-regex` absorption discipline. | 4 surfaces — §7.2 BIR `RegexProgram`, §7.3 CostFacts/PrimitiveFacts, §7.5 diagnostics, §10 codegen. |

## Cost And Routing Ledger

Per PASS-3-SYNTHESIS §3 CH4 lens (`restart/prompts/totality/PASS-3-SYNTHESIS.md:119`) and `restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:32-46`: every delta declares LOC budget, propagation count, risk class, wave alignment, same-wave consumer, and hard cap or abrogate gate. Budgets are doc-only LOC for ARCHITECTURE.md; downstream surface budgets flow through their authoring tranches.

| delta | LOC budget (docs) | propagation surfaces | risk class | wave alignment | same-wave consumer / receiver | hard cap or abrogate gate |
|---|---:|---:|---|---|---|---|
| ARCH-3A-D01 | 30-80 | 3 | Medium | Pass Omega CRUD-1 authority/status | §0 ledger + HANDOFF current-override + G-Omega presentation. | Abrogate if T-P1/T-P2 LOCKED authority cannot be cited from converged artefacts. |
| ARCH-3A-D02 | 80-180 | 4 | Medium | Pass Omega CRUD-1 BIR status | §7.2 status table + lowerer/VM coverage backlog. | Block close unless live (13) and target (20) BIR variants are separated and no new BIR variant is implied. |
| ARCH-3A-D03 | 140-300 | 5 | High | Pass Omega CRUD-1 + S-P3 decision-engine wave (skinny W7) | Active CostFacts schema + `backend_egraph::select` + `decision_csp::finalize_rule` consumers. | Abrogate if resolver still admits through static P1-P8 priority or stale cost; F-CH5-V1-03 substrate_target gate binds. |
| ARCH-3A-D04 | 80-160 | 4 | Medium | Pass Omega CRUD-1 + 3C Lock 10 alignment | `ActiveCostFacts` + `DecisionCspFacts` + W7 admission gate consumers. | Block if `lower_to_rust` admission gate is omitted from §7.6 trait obligations. |
| ARCH-3A-D05 | 100-220 | 5 | High | Pass Omega CRUD-1 + S-P3 BackendShape admission wave | Per-shape admission ledger + LAC-2D-04 disposition + Lock 10 retirement gate. | Block if EventTape/CollapsedStage described as live without same-wave consumer evidence. |
| ARCH-3A-D06 | 80-160 | 4 | High | Part (a) 3C V1 substrate-union elevation (3C-L01-substrate-union-v+1-elevation, DISPOSED) + Pass Omega CRUD-1; Part (b) Pass Omega Ω-A architecture intake (cursor-shape ratify-or-unify) + Pass Omega CRUD-1 | Lock 1 v+1 amendment owner + §9.2 union prose; Ω-A ARCH-CRUD-1 fold for Part (b) cursor-shape carrier. | Block Part (b) admission if Ω-A does not select ratify-two-cursor OR mandate-unification; Pass Omega cannot silently merge §9.2 prose until the cursor-shape disposition lands. Part (a) cross-call retention carrier already disposed at 3C V1; no further gate. |
| ARCH-3A-D07 | 100-220 | 5 | Medium-high | Pass Omega CRUD-1 + LAC-1E-14 CSS L4 classification | §9 substrate taxonomy + §11 perf rows + Lock 1 v+1. | Block if CSS fact stream becomes retained substrate or a sixth BackendShape. |
| ARCH-3A-D08 | 120-260 | 4 | High | SK-V14 C-1 PRUNE-3/PRUNE-4 wave + Pass Omega CRUD-1 | §9 template owner + §12 onboarding test + Lock 14 PRUNE wave receiver. | Block if §9 template prose claims `runtime/src/grammars/<g>/` while live HEAD is `crates/core/src/runtime/<g>/`. |
| ARCH-3A-D09 | 140-320 | 5 | High | Lock 14 PRUNE wave + Pass Omega CRUD-1 | §13.1 lint manifest + Lock 14 leak scan + future grammar onboarding. | Abrogate if generic crates require hand-coded grammar branches or JSON-shape role mining. |
| ARCH-3A-D10 | 120-260 | 5 | High | Lock 16 primitive manifest wave + Pass Omega CRUD-1 | PrimitiveFacts 8-cell schema + SKELETON triple DELETED close-state. | Abrogate any primitive without scalar oracle + checkasm + same-wave consumer + close-state. |
| ARCH-3A-D11 | 60-140 | 4 | Medium-high | LAC-2D-06 predicate hardening wave + Pass Omega CRUD-1 | `admits_collapsed_stage` predicate + 2E source-backed aarch64 candidate. | Block aarch64 `CollapsedStage` admission until 2E candidate lands AND F-CH5-V1-03 substrate_target manifest binds. |
| ARCH-3A-D12 | 100-200 | 4 | Medium-high | 2F V5 `bbnf-regex` absorption (skinny W11) + Pass Omega CRUD-1 | Regex fact import boundary + `bbnf-regex` HIR + SIMD scanner output classifier. | Block opaque-pattern or `regex-automata`-as-runtime-dep shortcuts; F-CH5-V1-03 substrate_target manifest binds. |

## Open Questions

| lens | question | receiver | blocker | gate |
|---|---|---|---|---|
| CH1 correctness / source-map hygiene | Should the §0 conflict-ledger keep the historical SK-V6 fold-back as labelled "historical divergence" or remove entirely? | Pass Omega CRUD-1 for `ARCHITECTURE.md:19`. | T-P3 cannot edit the authoritative surface; ARCH-3A-D01 proposes replacement, not historical preservation. | CRUD diff must reconcile §0 against SK-V14 audit-zero baseline + converged T-P1/T-P2 authority. |
| CH2 generality / Lock 14 | Where should the generated registry/manifest contract live: §7.4 status, §10 generated artefacts, §12.2 per-grammar authority table, or a new §13 manifest subsection? | Pass Omega CRUD-1 + 3C Lock 14 disposition. | ARCH-3A-D08 + D09 propose the contract surface; the location is a layout question pending G-Omega. | G-Omega accepts one canonical manifest location; Lock 14 leak-scan command consumes it. |
| CH3 substrate / evidence plane | Should CSS L4 fact-stream evidence appear in §9 substrate taxonomy AND §11 performance rows, or only in §9 with §11 cross-reference? | Pass Omega CRUD-1 + BENCH/CRUD surface authors. | LAC-1E-14 requires explicit classification; 1A-DIV-008 two-cursor structural ratification rerouted per ARCH-3A-D06 V2 to Pass Omega Ω-A (cross-call retention component already disposed at 3C V1 via LAC-2F-V5-02 ELEVATED). | Wording must preserve 5 BackendShape values and emit non-JSON telemetry provenance; ARCH-3A-D07 enforces. |
| CH4 evidence / cost discipline | Does ARCHITECTURE.md need one shared evidence-schema table for `CostFacts` + `PrimitiveFacts`, or two separate manifests as in ARCH-3A-D04 and ARCH-3A-D10? | Pass Omega CRUD-1 + 3C Lock 10/Lock 16 alignment. | CostFacts and PrimitiveFacts have different admission fields (objective-vector-and-frontier vs scalar-oracle-and-checkasm). | CRUD diff must keep CostFacts objective/freshness fields distinct from PrimitiveFacts source-state/first-consumer fields. |
| CH5 hidden coupling / substrate-union | Does Pass Omega ratify the two-cursor `ParserState` vs `DirectParser` shape as V1 substrate-union (1A-UNK-005), or schedule unification into one shared event cursor as a T-P2 wave? Note: cross-call retention component DISPOSED at 3C V1 (LAC-2F-V5-02 ELEVATED → 3C-L01-substrate-union-v+1-elevation at `restart/audit/totality/p3/3C-locks-crystallisation.md:31`); the open question that remains is the 1A-DIV-008 two-cursor structural split, rerouted per ARCH-3A-D06 V2 to Pass Omega Ω-A architecture intake. | Pass Omega Ω-A ARCH-CRUD-1 fold (then Lock 1 v+1 merge); NOT T-P3 §3C — LAC-2F-V5-02 elevation addresses cross-call retention only, not cursor-shape ratification. | 1A-DIV-008 records two structurally independent cursor types at HEAD; 1D `:117` records "Single substrate proved as substrate cardinality". Both can hold under different definitions. | Ω-A selects ratify-two-cursor OR mandate-unification before CRUD-1 §9.2 fold; the chosen disposition propagates to §9.2 prose + Lock 1 v+1 (cursor-shape carrier, separate from the LAC-2F-V5-02 cross-call retention carrier already merged). |
| CH6 anti-paper-close / primitive admission | Which Lock 16 primitive becomes the first SK-V14-horizon admission test for the 8-cell PrimitiveFacts manifest (ARCH-3A-D10), and which non-admitted primitives carry the SKELETON-triple precedent into deletion? | S-P3 + Lock 16 primitive admission wave after G-Omega. | T-P3 cannot pick production source changes; 2D/2E candidate ledgers propose options (`escape_mask_64`, `digit_run_accumulate_udot`, `pmull_cssc_structural_union_emit64`, `string_context_64`, etc., per `restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:42-58`). | SPEC wave must name scalar oracle + first consumer + row gate OR architectural-block-with-REDRESS before any admission. |
