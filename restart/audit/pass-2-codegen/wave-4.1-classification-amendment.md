# Wave 4.1 Amendment Classification — PASS-2 V3 Punch List

This note records the verify-then-patch classification for the nine surgical items routed to the PASS-2 surface by `HARDENING-PASS-2-V3.md` §14 (8 mandatory + 1 optional). Every row captures the pre-fill verification result, the surgical disposition, and the acceptance gate that the amendment commit must satisfy.

| # | Verification result on `PASS-2.md` | Disposition | Surgery |
|---|---|---|---|
| P2-1 — Lock 2 layout-canon vocabulary | Adjacent surfaces at lines 67, 459, 475 carry the `Layout` BIR variant row + `@layout` skip-policy mentions but never name `passes::layout` (the canonical IR pass), `LayoutFacts` (the public side-table), or `LayoutSink` (the consumer trait). Lock 2 vocabulary is silent. | ADD | Insert a one-line clause naming `passes::layout` as the producer, `LayoutFacts` as the side-table, and `LayoutSink` as the consumer trait, adjacent to the BIR `Layout` row at line 67. |
| P2-2 — Lock 3 unified cursor + byte-skip obligation | §3 close (line 168) ratifies lookbehind co-amendment but never declares the Rust V1 / WASM V1 obligation that one parse implementation services both eager fast path and cursor-driven path, with byte-skip generated when consult returns `Skip` and cursor calls elided on `__EAGER_EMPTY_PATH`. Lock 3 surface is silent at the codegen-side. | ADD | Insert a paragraph at §3 close declaring the unified parse-implementation obligation, naming `Ref` / `Lit` / `RegexDfa` / `Scanner` BIR variants as the realisation surface and noting that `PrattSpine` and `SimdScan` carry their own dispatch and elide cursor consultation in the inner loop. |
| P2-3 — Deny-gate path widening | Verbatim deny command at line 239 scans `crates/codegen/src/lower crates/codegen/src/runtime_template`; `crates/codegen/src/backend_ir/README.md` (line 195) is documentation-only but lives outside the deny scope. A lowerer that imports `GrammarIR` from anywhere else under `crates/codegen/src/` slips through. | TIGHTEN | Replace the deny path pair with the whole `crates/codegen/src/` tree; add an exception comment naming `crates/codegen/src/backend_ir/README.md` as documentation. |
| P2-4 — Runtime template parameter schema rows | Schema at lines 127-145 carries 13 rows (`grammar_ident` through `budget`); `visitor_bitflags`, `bump_arena`, and `incremental_marker` rows are absent. Visitor-bitflag derivation, bumpalo lifetime obligation (Lock 9), and source-map sidecar wiring are not bound to schema source/consumer. | EXPAND | Add three rows: `visitor_bitflags` (source: BIR view shapes; consumer: generated `visitor.rs` impl); `bump_arena` (source: PASS-3 API contract; consumer: `parse_in` signature lowering); `incremental_marker` (source: cost model; consumer: optional source-map sidecar). |
| P2-5 — yaml two-surface invariant | Yaml proof table at lines 368-374 carries grammar source, metadata, runtime emission, registry, gate rows. Lock 14's two-surface input invariant — exactly two added paths in `git diff HEAD~1`, namely `grammars/yaml.bbnf` and one `[workspace.metadata.bbnf.grammars.yaml]` block — is not bound to a verification command. | ADD | Append a `Two-surface invariant` row to the proof table, binding the input-side invariant to the `git diff HEAD~1` shape plus `rg`/`find` verification commands. |
| P2-6 — Diagnostic ledger verbatim strings | Ledger at lines 520-527 carries `Code | Trigger | PASS-2 producer` columns for six codes (`BBNF-GEN001`, `BBNF-GEN014`, `BBNF-CODEGEN021`, `BBNF-CODEGEN033`, `BBNF-LIFE009`, `BBNF-SEM040`). Verbatim user-facing strings are absent — Lane 7 standard requires the verbatim string column. | EXPAND | Add a fourth column `Verbatim string`; populate the six verbatim strings prescribed by the V3 directive. |
| P2-7 — Pratt/SIMD misfire diagnostics | Detection thresholds at lines 159-164 cover Pratt/SIMD/PHF/Lookbehind selection, but no diagnostic surface fires when the optimizer rejects an apparent operator-chain or apparent SIMD candidate. Authors hit silent no-op classification with no actionable string. | ADD | Add `BBNF-OPT001` and `BBNF-OPT002` rows to the diagnostic ledger with verbatim strings for the operator-chain and SIMD-misfire surfaces. |
| P2-8 — swc citation surface | Cardinality defence at line 78 cites the swc `Stmt` and `Expr` rustdoc URLs (`https://rustdoc.swc.rs/swc_ecma_ast/enum.Stmt.html`, `…enum.Expr.html`). Path:line citation discipline (per HARDENING.md §3) requires corpus references, not external URLs. | REPLACE | Replace the rustdoc URL pair with a corpus citation path (`restart/corpora/SOTA.md:186`, the parol typed-AST cardinality reference — closest available auditable line for the AST-cardinality argument). |
| P2-9 (optional) — yaml smoke regen receiver | Regen-cycle wall budget at line 421 names the yaml smoke regen receiver as `Tranche A/G/F yaml onboarding gate` (three candidates). Tranche G runtime publication is the natural single receiver — A is bootstrap, F is parity. | TIGHTEN | Pin the receiver to `Tranche G yaml onboarding gate`, removing the A/F alternatives. |

Acceptance gates after the amendment commit:

- `rg -nC2 'passes::layout|LayoutFacts|LayoutSink' restart/audit/pass-2-codegen/PASS-2.md` returns matches adjacent to the BIR `Layout` row at line 67.
- `rg -nC2 'unified|byte-skip|__EAGER_EMPTY_PATH' restart/audit/pass-2-codegen/PASS-2.md` returns the §3-close paragraph binding the Rust V1 / WASM V1 obligation.
- `rg -n 'GrammarIR' restart/audit/pass-2-codegen/PASS-2.md` shows the deny command tightened to `crates/codegen/src/` (whole tree) with the documentation exception.
- `rg -nC2 'visitor_bitflags|bump_arena|incremental_marker' restart/audit/pass-2-codegen/PASS-2.md` returns the three new rows in the runtime template parameter schema.
- `rg -nC2 'Two-surface invariant|workspace.metadata.bbnf.grammars.yaml' restart/audit/pass-2-codegen/PASS-2.md` returns the yaml proof's input-invariant row.
- `rg -nC2 'Verbatim string' restart/audit/pass-2-codegen/PASS-2.md` returns the diagnostic ledger header with the fourth column.
- `rg -nC2 'BBNF-OPT001|BBNF-OPT002' restart/audit/pass-2-codegen/PASS-2.md` returns the Pratt/SIMD misfire rows.
- `rg -n 'rustdoc.swc.rs' restart/audit/pass-2-codegen/PASS-2.md` returns zero; `rg -n 'restart/corpora/SOTA.md' restart/audit/pass-2-codegen/PASS-2.md` returns the swc-replacement citation.
- `rg -nC2 'yaml smoke regen|Tranche G yaml onboarding' restart/audit/pass-2-codegen/PASS-2.md` returns the pinned receiver row.

Wave 4.1 correction note: this classification is the predicate for the Wave 4.1 narrow-scope amendment commit; the amendment lands surgical edits on `PASS-2.md` only and leaves PASS-2 sub-agent reports untouched, save for this note alongside.
