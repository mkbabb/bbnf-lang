# T-P2 V1 Hardening Consolidated

Pass: T-P2 Research. Cycle: V1.
Date: 2026-05-21.
Scope: consolidated CH1-CH6 challenge verdict for the totality T-P2 V1 research cohort.
Output: this file.

## Verdict

`G-T-P2-V1-CONVERGED`: FAIL.

V1 is useful and directionally grounded, but it is not provenance-clean or
mechanical enough for T-P3. All six lenses return REVISE. No dossier is rejected:
the underlying routes are mostly supportable, but V2 must convert parallel
recommendations into stable citations, explicit Lock 14/Lock 1 contracts,
row-specific REDRESS reopen rules, and executable admission ledgers.

| Lens | Disposition | Load-bearing reason |
|---|---|---|
| CH1 correctness / provenance | REVISE | Several standards/source citations are brittle or wrong: W3C line citations do not support the claimed sections, OpenFormula PDF line refs are not reproducible, Sneller source repo URL is dead, moving-branch anchors need SHAs, and the external parse-that worktree needs revision/dirty-state provenance. |
| CH2 generality / Lock 14 | REVISE | The dossiers identify the right grammar-neutrality hazards, but V2 needs one enforceable Lock 14 transfer contract covering generated provider registry, grammar-shape leak scanning, generated sink/flag surfaces, primitive policy manifests, and CSS plus Sheets/BBNF-self transfer proof. |
| CH3 regression / REDRESS | REVISE | REDRESS-119 reopening is too broad without a row-specific material-differential matrix; PMULL+CSSC/Union-C shorthand must distinguish itself from REDRESS 88/89/96/97/98; REDRESS 121-127 need prerequisite/admission taxonomy. |
| CH4 cost / admission discipline | REVISE | Cost remains prose-level; V2 needs a per-technique admission ledger with scalar reference, checkasm/parity, strict oracle, same-wave consumer, row gate, LOC/risk, rollback, abrogate criteria, and admissibility state. |
| CH5 hidden coupling / Lock 1 | REVISE | V2 must make substrate boundaries mechanical with substrate-kind annotations and retention-lifetime/policy-owner fields for e-graph nodes, backend rewrites, imported scanner plans, union candidates, and SIMD consumers. |
| CH6 anti-paper-close | REVISE | The packet still risks treating citations as validation. V2 needs transfer ledgers for CSS/decision engine, a pinned-or-downgraded parse-that import claim, normalized orphan states, and a common material-differential checklist for union/ASM-gen routes. |

Acceptance rate: 0/6 = 0%.
Consecutive accepted cycles: 0.

## Fold Actions For V2

1. Repair provenance:
   - replace brittle W3C line citations with stable section anchors or checked
     source snapshots;
   - replace OpenFormula PDF line refs with page/section anchors or a checked
     text-extract artifact with checksum;
   - remove or replace the dead Sneller source repository citation;
   - pin moving upstream source anchors to SHAs/tags or record verified upstream
     HEADs;
   - record the external parse-that HEAD and dirty/untracked state, or vendor a
     snapshot before treating it as primary evidence;
   - normalize `primary_sources_cited` counting.
2. Add a canonical Lock 14 transfer contract covering generated provider
   registry, grammar-shape leak scanning, generated sink/fact/value/flag
   interpretation, primitive policy manifests, and CSS plus Sheets/BBNF-self
   transfer/witness requirements.
3. Add a per-technique grammar-transfer table:
   `technique | CSS L4 transfer | Sheets transfer | BBNF-self transfer |
   required generated facts | failure mode if absent`.
4. Add a REDRESS-119 reopen matrix for all 13 residual direct rows, naming prior
   REDRESS proof, proposed material differential, same-wave consumer, strict
   comparator/oracle, and old-route-block condition per row.
5. Expand PMULL+CSSC / Union-C into an admissibility checklist: old REDRESS
   route cited, old cost source deleted or bypassed, retained substrate replay
   forbidden, scalar/checkasm cell named, isolated microbench required, strict
   row gate named, and abort criteria stated.
6. Add a REDRESS 121-127 taxonomy distinguishing prerequisite, comparator,
   scaffold, microbench split, admitted CSS row, and future material-differential
   route.
7. Add a per-technique admission ledger with owner dossier, source paths, scalar
   reference, checkasm/parity command, `BBNF_SIMD_STRICT` status, corpus/equality
   oracle, hardware gate, same-wave consumer path, expected row/feature gate,
   LOC budget, risk class, rollback path, abrogate thresholds, and admissibility
   state.
8. Add a source-present SIMD/ASM orphan state machine: `wired`, `deleted`,
   `scalar-delegate-non-ASM`, or `architectural-block-with-REDRESS`.
   Historical `inventory_demoted_with_evidence` is evidence only, not close.
9. Add Lock 1 substrate-kind fields for resolver/scanner/primitive routes:
   `local_temp_only`, `existing_tape`, `direct_sink`, or `admitted_fact_output`.
   Anything else is rejected without user amendment.
10. Pin or downgrade the `bbnf-regex` / parse-that import authority: V2 must
    either record revision/license/import boundaries and HIR-to-bbnf mapping, or
    mark extraction conditional with blockers.

## Cycle Disposition

T-P2 returns to V2 research fold. The expected V2 shape is a revised six-dossier
packet, or a shared V2 addendum cited by all six dossiers, that turns V1's
source grounding into mechanical contracts T-P3 can synthesize without paper
closure.
