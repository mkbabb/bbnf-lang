---
lens: CH4
lens_name: COST / admission discipline
pass: T-P2-research
cycle: V3
hardening_authority: restart/audit/totality/p2/hardening/V3/CHALLENGE-CONTEXT.md
spec_authority: restart/prompts/totality/PASS-2-RESEARCH.md §3 CH4
agent_role: write-only adversary (LOCK-TRIGGER lens)
generated_at: 2026-05-23
head_sha: 5aaab91d11389dc26ed8e6263c1a640cc9c28035
verdict: ACCEPT (V2 qualifications discharged; F-V3-CH4-A cohort-wide BBNF_SIMD_STRICT inline cohesion enacted at 2A:192 + 2C:303-305 + 2D:142-149; F-V3-CH4-B six abrogate gates numerically bound at 2D:151-162; cohort §3Z LOCK triggers on V3 close)
accept_rate: 100% (6/6 dossiers ACCEPT)
findings_count: 0 carry-forward
load_bearing_findings: []
artefacts_reviewed: [2A-sota-landscape.md, 2B-primitive-vocabulary.md, 2C-grammar-neutrality.md, 2D-cost-model.md, 2E-host-arch-esoterica.md, 2F-parse-that-gaps.md, T-P2-V2-FOLD-ADDENDUM.md, T-P2-V3-FOLD-ADDENDUM.md, T-P2-V4-FOLD-ADDENDUM.md]
prior_cycle: restart/audit/totality/p2/hardening/V2/CH4.md (V2 ACCEPT qualified — 96%; load-bearing CH4-F1/F2/F4/F7 discharged; two micro-folds tracked for V3)
lock_state: 2-CYCLE LOCK (V2 96% qualified + V3 100%; cohort §3Z LOCK triggers on V3 close)
---

# T-P2 V3 CH4 — COST / Admission Discipline (LOCK-TRIGGER)

Lens: CH4 COST per PASS-2-RESEARCH.md §3.
Authority: V3 CHALLENGE-CONTEXT.md §2 CH4 binding — verify F-V3-CH4-A
cohort-wide `BBNF_SIMD_STRICT=1` inline cohesion at 2A T2A-LAC-V1-03
`:192` + 2C closure-criteria header `:303-305` + 2D BackendShape ledger
preamble `:142-149`; verify F-V3-CH4-B all six T2A-LAC-V1-05 abrogate
gates numerically bound at 2D `:151-162`. WRITE-ONLY; aggregator commits
eight V3 hardening files atomically as the cohort §3Z LOCK declaration.

## §0 — Verdict

**ACCEPT (100% — second consecutive ≥95% cycle → cohort §3Z LOCK trigger).**

V3 discharges the two V2 micro-fold qualifications operationally and at
the exact line ranges the V2 dispatch context named. Specifically:

- **F-V3-CH4-A (BBNF_SIMD_STRICT cohort-wide inline cohesion).** V2
  carried the strict-mode precondition through 2B prose + 2E manifest
  column + V3/V4 addendum executable ledger but did not name the flag
  inline at 2A T2A-LAC-V1-03 nor at 2C nor at 2D. V3 enacts the three-
  site fold:
  - 2A T2A-LAC-V1-03 amendment row at `2A-sota-landscape.md:192` adds
    `BBNF_SIMD_STRICT=1` strict-mode precondition explicitly into the
    eight-cell manifest schema ("checkasm differential cell,
    `BBNF_SIMD_STRICT=1` strict-mode precondition (cohort-wide flag
    binding per `LOCKS.md:320-322`; every 2A-grounded primitive's
    bench/admission row carries `BBNF_SIMD_STRICT=on` when admitted
    through a SIMD path), corpus parity test, …") with cross-reference
    to `LOCKS.md:320-322` Lock 16 v+1 surface and to 2B §A4 #2 +
    2E `:328` `BBNF_SIMD_STRICT_status` manifest column.
  - 2C closure-criteria header at `2C-grammar-neutrality.md:303-305`
    binds the cohort-wide precondition explicitly: "**Cohort-wide
    precondition (CH4 fold F-V3-CH4-A):** every bench/admission row
    binding a 2C-grounded primitive (C3 byte-class checkasm, C4
    fixed-4-nibble decode, escape_mask, byte-window MAC) executes
    under `BBNF_SIMD_STRICT=1`, which fails fast on any silent scalar
    fallback and binds same-wave consumer measurement to the SIMD path
    actually claimed; rows recorded without this flag are NOT-VALIDATED
    for grammar-neutral generality."
  - 2D BackendShape Admission Ledger preamble at `2D-cost-model.md:142-149`
    binds the same precondition with explicit cross-reference into 2A
    and 2C: "**Cohort-wide precondition (CH4 fold F-V3-CH4-A):** every
    bench / admission row populating this ledger (and the per-shape
    consumer rows named in the table below) executes under
    `BBNF_SIMD_STRICT=1` per `LOCKS.md:320-322` (cohort-wide flag
    binding propagated from 2A T2A-LAC-V1-03 at
    `2A-sota-landscape.md:192` and 2C closure-criteria header at
    `2C-grammar-neutrality.md:305`); rows recorded without this flag
    are NOT-VALIDATED and fail the same-wave consumer admission gate
    regardless of microbench parity."

  The three-site cohesion is mutually cross-referenced: 2D names 2A
  `:192` and 2C `:305` explicitly; 2C names CH4-F-V3-CH4-A; 2A's
  amendment row cites `LOCKS.md:320-322` + 2B §A4 #2 + 2E `:328` as
  the binding cohort surface. The flag is no longer "named only in
  addendum" — it is inline at the three prose anchors the V2 CH4
  qualification named.

- **F-V3-CH4-B (all six abrogate gates numerically bound at 2D inline).**
  V2 carried four of six abrogate gates numerically (CSP ≤1 s/grammar;
  stale ≤30 %; row regression; parity/checkasm) and two elastically
  (e-graph cap deferred to V2 addendum; generated LOC growth deferred
  to "SPEC wave budget"). V3 enacts the inline numeric bind at 2D
  `:151-162`:
  - Gate (a) e-graph saturation: `≤50000 nodes` / `≤10000 classes`
    / `≤30 iter` (cross-referenced to UNKNOWN-2D-03 at `:229`
    bounded-scheduler caps, and to 2A T2A-LAC-V1-05 at `:194`).
  - Gate (b) CSP timeout: `≤1 s per grammar`.
  - Gate (c) stale-cost evidence: `≤30 %`.
  - Gate (d) generated LOC growth: ledger `loc_budget` upper bound
    per `T-P2-V3-FOLD-ADDENDUM.md:113` ("no row may exceed its declared
    budget without traced O(N) regression — stricter SPEC wave budget
    wins when later named"). The elastic "SPEC wave budget" reference
    is now anchored to a concrete row-local upper bound that exists
    on every populated ledger row, with the SPEC reference reserved
    as the stricter-binding option, not as the discretionary fallback.
  - Gate (e) any row regression.
  - Gate (f) any parity/checkasm/equality failure.

  All six gates are named inline as the BackendShape Admission Ledger
  preamble; the closing sentence carries the schema into Lock 10 v+1
  via LAC-2D-06 below. The V2 qualification "(a) e-graph node/iteration
  cap is named in 2A but 2D still defers to T-P2-V2-FOLD-ADDENDUM.md;
  (b) generated LOC growth defers to 'SPEC wave budget' without naming
  the budget file/section" is fully discharged.

The V2 ACCEPT-rate of 96% (qualified) rises to 100% at V3 (unqualified).
Both consecutive cycles meet the §3Z ≥95% threshold → cohort §3Z LOCK
trigger condition is satisfied at V3 close. V≤5 ceiling preserved (V3
is the LOCK landing; no V4/V5 anticipated for CH4).

## §1 — Per-dossier disposition

| dossier | V2 disposition | V3 disposition | rationale (one line) |
|---|---|---|---|
| 2A-sota-landscape.md | ACCEPT (qualified) | ACCEPT | F-V3-CH4-A enacted: T2A-LAC-V1-03 at `:192` now names `BBNF_SIMD_STRICT=1` strict-mode precondition inline in the eight-cell manifest schema with cross-reference to `LOCKS.md:320-322` + 2B §A4 #2 + 2E `:328`. UNKNOWN-3 discharge anchor (`:181`) preserved. Counted source IDs 26 (F-V3-CH1-B fold) preserved. Zero CH4 drift. |
| 2B-primitive-vocabulary.md | ACCEPT | ACCEPT | F-V3-CH4-A inline propagation already at HEAD (V2 carried `BBNF_SIMD_STRICT=1` at `:91, :179, :280, :293-298, :317-318`); §A6 ledger preserved with 6 admitted rows populated (§A5/§A6 substrate_target + retention_lifetime columns added via F-CH5-V2-01 fold; 3 SKELETON N/A rows DELETED per V2 §R3 enactment). §R5 marker-string refutation preserved. Cohort-wide propagation surface intact. |
| 2C-grammar-neutrality.md | ACCEPT | ACCEPT | F-V3-CH4-A enacted: closure-criteria header at `:303-305` adds the cohort-wide precondition prose explicitly ("every bench/admission row binding a 2C-grounded primitive … executes under `BBNF_SIMD_STRICT=1`, which fails fast on any silent scalar fallback"). 7-step onboarding test + generator-stub gap closure cells + CSS L4 byte-class row preserved. BBNF-self literal-escape consumer at `bbnf.bbnf:11-13` admits C4 `unescape_uxxxx_x8_neon` directly per V2. Zero generality regression. |
| 2D-cost-model.md | ACCEPT (qualified) | ACCEPT | F-V3-CH4-A enacted at `:142-149` (cohort-wide precondition preamble with cross-reference to 2A `:192` and 2C `:305`); F-V3-CH4-B enacted at `:151-162` (all six abrogate gates numerically bound: e-graph 50000/10000/30, CSP 1s, stale 30%, LOC growth via ledger `loc_budget` upper bound, row regression, parity/checkasm). LAC-2D-06 carries the schema into Lock 10 v+1. BackendShape Admission Ledger 5 rows (1 ADMITTED + 4 NOT-ADMITTED) preserved. Per-technique transfer coverage table preserved. |
| 2E-host-arch-esoterica.md | ACCEPT | ACCEPT | V2-LOCKED through V3 per §0 (zero V3 edits). Lock 16 hardware-gate manifest 13-cell schema at `:311-336` preserved; per-candidate adoption-cost rows `:295-301` preserved; inventory entries `:307-309` preserved; Source-Present Primitive State `:343-367` preserved; Material-Differential Gate `:369-389` preserved. `BBNF_SIMD_STRICT_status` manifest column at `:328` is the cohort cross-reference anchor for F-V3-CH4-A at 2A. |
| 2F-parse-that-gaps.md | ACCEPT | ACCEPT | V4 admission ledger format + 9 gap rows preserved; F-CH5-V2-02 crate_target column elevated. F-V3-CH4-A inline propagation already at HEAD via V4 ledger delta at `T-P2-V4-FOLD-ADDENDUM.md:49-53` (every candidate row carries `BBNF_SIMD_STRICT=1` checkasm command). REDRESS Slice Ownership 12 rows preserved (`loc_budget` numeric cells populated). F-V3-CH1-B counted_source_ids 26 fold preserved. |

## §2 — V3 fold verification (F-V3-CH4-A + F-V3-CH4-B)

### F-V3-CH4-A verification — cohort-wide BBNF_SIMD_STRICT=1 inline cohesion at 2A:192 + 2C:303-305 + 2D:142-149

**V2 qualification.** V2 §4 micro-fold candidate #1: "2A T2A-LAC-V1-03
inline `BBNF_SIMD_STRICT=1`. At `:192`, add `BBNF_SIMD_STRICT=1` to the
eight-cell manifest schema between 'checkasm differential cell' and
'corpus parity test' so the cohort schema anchor names the strict-mode
precondition in prose. The flag is already canonical via V3/V4 addendum
executable ledger; this is prose cohesion only."

**V3 evidence — three-site enactment.**

- **Site 1: 2A T2A-LAC-V1-03 at `2A-sota-landscape.md:192`.** Direct
  citation from HEAD:
  > "Require a published per-primitive manifest with at minimum: abstract
  > primitive name, published citation, hardware gate, scalar reference
  > path, checkasm differential cell, `BBNF_SIMD_STRICT=1` strict-mode
  > precondition (cohort-wide flag binding per `LOCKS.md:320-322`;
  > every 2A-grounded primitive's bench/admission row carries
  > `BBNF_SIMD_STRICT=on` when admitted through a SIMD path), corpus
  > parity test, same-wave production consumer, row admission *or*
  > measured rejection. Hand-tuned intrinsic loops without this manifest
  > are inadmissible."

  The flag is now named between "checkasm differential cell" and "corpus
  parity test" exactly as V2 §4 #1 prescribed. The amendment row's
  supporting-evidence column cites `LOCKS.md:320-322` + T-P1 1E Lock 16
  partial-UNKNOWN + 2B §A4 #2 `BBNF_SIMD_STRICT=1` admission contract +
  2E manifest column `BBNF_SIMD_STRICT_status` at `2E:328` as the
  cohort surface. The proposed-amendment field is now nine-cell
  (eight cells + strict-mode precondition) per the closing sentence.

- **Site 2: 2C closure-criteria header at `2C-grammar-neutrality.md:303-305`.**
  Direct citation from HEAD:
  > "**Cohort-wide precondition (CH4 fold F-V3-CH4-A):** every
  > bench/admission row binding a 2C-grounded primitive (C3 byte-class
  > checkasm, C4 fixed-4-nibble decode, escape_mask, byte-window MAC)
  > executes under `BBNF_SIMD_STRICT=1`, which fails fast on any silent
  > scalar fallback and binds same-wave consumer measurement to the
  > SIMD path actually claimed; rows recorded without this flag are
  > NOT-VALIDATED for grammar-neutral generality."

  The fold tag `CH4 fold F-V3-CH4-A` is explicit at the header. The four
  2C-grounded primitives named (C3, C4, escape_mask, byte-window MAC)
  match the V2/V3/V4 addendum candidate ledger and 2B §A6 ledger rows
  exactly. The fail-fast semantics ("fails fast on any silent scalar
  fallback") binds the cohort-wide flag to executable consumer
  measurement, not to discretionary prose.

- **Site 3: 2D BackendShape Admission Ledger preamble at `2D-cost-model.md:142-149`.**
  Direct citation from HEAD:
  > "**Cohort-wide precondition (CH4 fold F-V3-CH4-A):** every bench /
  > admission row populating this ledger (and the per-shape consumer
  > rows named in the table below) executes under `BBNF_SIMD_STRICT=1`
  > per `LOCKS.md:320-322` (cohort-wide flag binding propagated from
  > 2A T2A-LAC-V1-03 at `2A-sota-landscape.md:192` and 2C closure-
  > criteria header at `2C-grammar-neutrality.md:305`); rows recorded
  > without this flag are NOT-VALIDATED and fail the same-wave consumer
  > admission gate regardless of microbench parity."

  The cross-reference graph is fully closed: 2D explicitly names 2A
  `:192` and 2C `:305`; 2C explicitly names CH4 fold F-V3-CH4-A;
  2A explicitly names `LOCKS.md:320-322` + 2B `:91, :179, :280, :293-298,
  :317-318` (via §A4 #2 cohort surface) + 2E `:328` (manifest column).
  Per-primitive coverage cascade is intact:
  2A T2A-LAC-V1-03 amendment row → 2B §A4 #2 + §A6 ledger rows 1-6 →
  2C closure-criteria header → 2D BackendShape ledger preamble + table
  rows → 2E `BBNF_SIMD_STRICT_status` manifest column → V3/V4 addendum
  executable ledger checkasm commands.

**Executable verification.** Local re-verification at HEAD
(`5aaab91d11389dc26ed8e6263c1a640cc9c28035`):

- `grep -n "BBNF_SIMD_STRICT" restart/audit/totality/p2/2A-sota-landscape.md`
  confirms inline citation at the T2A-LAC-V1-03 amendment row.
- `grep -n "F-V3-CH4-A" restart/audit/totality/p2/2C-grammar-neutrality.md`
  confirms fold tag at `:303-305`.
- `grep -n "F-V3-CH4-A" restart/audit/totality/p2/2D-cost-model.md`
  confirms fold tag at `:142-149`.
- Cross-references resolve at the named line ranges; no broken anchors.

**V3 verification status.** PASS. The V2 qualification #1 is fully
discharged. The strict-mode precondition is now named inline at three
mutually cross-referenced sites that span the SOTA-landscape anchor (2A),
the grammar-neutrality closure header (2C), and the cost-model admission
ledger (2D). F-V3-CH4-A is V3-grade.

### F-V3-CH4-B verification — all six T2A-LAC-V1-05 abrogate gates numerically bound at 2D:151-162

**V2 qualification.** V2 §4 micro-fold candidate #2: "2D abrogate-gate
numeric bind for e-graph cap + LOC growth. At `:136-146`, replace 'node
or iteration cap exceeded' with 2A T2A-LAC-V1-05's numeric set
(`≤50000 nodes / ≤10000 classes / ≤30 iter`) and replace 'exceeds SPEC
wave budget' with either the row-local upper `loc_budget` bound or a
named SPEC §X.Y reference. Both numbers exist in 2A T2A-LAC-V1-05 at
`:194`; this is a cross-dossier inline copy, not a new derivation."

**V3 evidence — six-gate inline numeric bind.**

Direct citation from 2D HEAD at `2D-cost-model.md:151-162`:

> "**Abrogate-gate numeric bind (CH4 fold F-V3-CH4-B, from 2A
> T2A-LAC-V1-05 at `2A-sota-landscape.md:194`):** every candidate
> admitted via this ledger fails closed when (a) e-graph saturation
> exceeds `≤50000 nodes` / `≤10000 classes` / `≤30 iter` (the
> bounded-scheduler caps already referenced by UNKNOWN-2D-03 at `:229`),
> (b) CSP timeout exceeds `≤1 s per grammar`, (c) stale-cost evidence
> exceeds `≤30 %`, (d) generated LOC growth exceeds the candidate's
> ledger `loc_budget` upper bound per `T-P2-V3-FOLD-ADDENDUM.md:113`
> (no row may exceed its declared budget without traced O(N) regression
> — stricter SPEC wave budget wins when later named), (e) any row
> regression, or (f) any parity/checkasm/equality failure. All six
> gates are numerically bound at this ledger; LAC-2D-06 below carries
> the schema into Lock 10 v+1."

**Per-gate verification.**

| gate | V2 elasticity | V3 numeric bind | source authority |
|---|---|---|---|
| (a) e-graph saturation | elastic; deferred to V2 addendum | `≤50000 nodes` / `≤10000 classes` / `≤30 iter` inline | 2A T2A-LAC-V1-05 `:194` + UNKNOWN-2D-03 `:229` |
| (b) CSP timeout | bound | `≤1 s per grammar` inline | 2A T2A-LAC-V1-05 `:194` |
| (c) stale-cost evidence | bound | `≤30 %` inline | 2A T2A-LAC-V1-05 `:194` |
| (d) generated LOC growth | elastic; "SPEC wave budget" unnamed | candidate's ledger `loc_budget` upper bound (row-local) per `T-P2-V3-FOLD-ADDENDUM.md:113`; SPEC wave budget reserved as stricter-binding option | T-P2-V3-FOLD-ADDENDUM.md:113 + per-candidate ledger rows |
| (e) row regression | bound | "any row regression" (any-row fail-closed) | LAC-2D-04 admission contract |
| (f) parity/checkasm | bound | "any parity/checkasm/equality failure" | LAC-2D-04 admission contract + 2B §A4 #2 admission contract |

All six gates are now numerically bound inline. The two V2-elastic
gates (e-graph cap and LOC growth) are discharged:

- **e-graph cap** is now a three-cell numeric specification (`≤50000 nodes`
  / `≤10000 classes` / `≤30 iter`) lifted directly from 2A T2A-LAC-V1-05
  `:194` with cross-reference to UNKNOWN-2D-03 `:229` bounded-scheduler
  caps — no "node or iteration cap exceeded" prose elasticity.
- **LOC growth** is now anchored to the candidate's row-local
  `loc_budget` upper bound (every populated ledger row carries this
  cell — verified at 2F V4 addendum `:67-79` REDRESS slices 80-180,
  120-240, 80-140, 280-520, 160-300, 40-100 LOC ranges + 2B §A6
  per-kernel LOC envelopes `:303-315` + V3 addendum per-candidate
  `loc_budget` cells `T-P2-V3-FOLD-ADDENDUM.md:91-97`). The "stricter
  SPEC wave budget" is reserved as the stricter-binding option, not
  the discretionary fallback — every row has a concrete upper bound
  even without SPEC §X.Y naming.

**LAC-2D-06 schema propagation.** The closing sentence of the
abrogate-gate bind ("LAC-2D-06 below carries the schema into Lock 10
v+1") anchors the six-gate set as a Lock 10 v+1 amendment surface,
ensuring the V3 numeric bind is not isolated prose but a
LOCK-AMENDMENT-CANDIDATE row consumable by T-P3 §3C.

**Executable verification.** Local re-verification at HEAD:

- `grep -n "F-V3-CH4-B" restart/audit/totality/p2/2D-cost-model.md`
  confirms fold tag at `:151-162`.
- Six gates enumerable in the cited block: (a) e-graph saturation
  (three-cell numeric); (b) CSP timeout (numeric); (c) stale-cost
  evidence (numeric); (d) generated LOC growth (row-local
  `loc_budget`); (e) row regression (any); (f) parity/checkasm/equality
  failure (any). Count = 6.
- Cross-references resolve: 2A `:194` carries the abrogate-cap source
  set; T-P2-V3-FOLD-ADDENDUM.md `:113` carries the row-local
  `loc_budget` discipline; UNKNOWN-2D-03 `:229` carries the
  bounded-scheduler caps.

**V3 verification status.** PASS. The V2 qualification #2 is fully
discharged. All six abrogate gates from 2A T2A-LAC-V1-05 `:194` are
now numerically bound inline at 2D `:151-162` with no elastic
deferrals. F-V3-CH4-B is V3-grade.

### F-V3-CH4 cohort coherence — V2 four load-bearing findings preserved

V2's four load-bearing findings (CH4-F1, F2, F4, F7) were already
discharged at V2 ACCEPT. V3 preserves all four enactments:

- **CH4-F1 (SKELETON triple binary DELETE).** 2B §R3 at `:356-410`
  preserved. Executable-verified zero-consumer evidence preserved.
  Lock 16 v+1 close-state vocabulary `deleted` invoked at `:393-395`.
  §A6 ledger 3 SKELETON N/A rows DELETED per V3 amendment.
- **CH4-F2 (marker-string lowerer candidate refutation).** 2B §R5 at
  `:424-481` preserved. 2D BackendShape Admission Ledger at `:128-148`
  preserved (1 ADMITTED `SinkOnly` + 4 NOT-ADMITTED marker-string
  lowerers).
- **CH4-F4 (per-candidate adoption-cost ledger).** ≈37 ledger rows
  across cohort preserved: 6 admitted (2B §A6, was 9, reduced to 6
  per V3 SKELETON DELETE) + 5 (2D) + 5 (2E) + 6 (2F V4 addendum) +
  12 (2F V4 REDRESS slices) = 34 rows. Coverage still exceeds V1's
  ≈28-row floor.
- **CH4-F7 (LOC/risk realism).** 2B §A6 `:303-315` LOC envelopes
  preserved. 2F V4 REDRESS slice `loc_budget` cells preserved. 2E
  manifest schema `:332` preserved. V3 addendum per-candidate cells
  preserved.

The four load-bearing findings remain V2-grade at V3 close; V3 adds
only the two micro-fold cohesion edits without disturbing the
load-bearing enactment surface.

## §3 — Cycle disposition (per §3Z LOCK-trigger trajectory)

| metric | V1 | V2 | V3 | §3Z target | gap |
|---|---|---|---|---|---|
| CH4 ACCEPT-rate per dossier | 33% (2/6) | 100% qualified (6/6, 96% overall) | 100% (6/6) | ≥95% | met (LOCK-trigger) |
| Open critical defects (CH4-load-bearing) | 4 | 0 | 0 | 0 | met |
| Open carry-forward micro-folds | n/a | 2 (F-V3-CH4-A, F-V3-CH4-B) | 0 | 0 | met |
| Cohort-wide BBNF_SIMD_STRICT inline cohesion | 2B only | 2B + 2E + V3/V4 addendum; 2A/2C/2D pending | 2A `:192` + 2C `:303-305` + 2D `:142-149` (three-site cross-referenced) | full inline cohort | met |
| Abrogate-gate numeric thresholds bound | 1 | 4 (CSP/stale/row-regression/parity); 2 elastic (e-graph cap / LOC growth) | 6 (e-graph 3-cell numeric + CSP + stale + LOC budget row-local + row-regression + parity/checkasm) | ≥6 | met |
| Per-candidate admission ledger rows | 0 / ≈28 | 37 / ≈28 | 34 / ≈28 (SKELETON DELETE reduced 2B rows 9 → 6) | ≈28 | exceeded |
| Admission state vocabulary normalised | heterogeneous | 8-state machine defined; cohort prose pending | normalised at addendum authority; dossiers reference back | normalised | met (V3 addendum §A enacts) |

**Cycle disposition: ACCEPT (V3 100%).** V3 is the second consecutive
≥95% cycle following V2 96%. The §3Z 2-cycle LOCK condition is
satisfied. V3 carries zero open carry-forward; no V4 micro-fold is
anticipated for CH4.

**Cycle progression.**
- V1: REVISE 33% (load-bearing CH4-F1/F2/F4/F7).
- V2: ACCEPT qualified 96% (load-bearing discharged; F-V3-CH4-A +
  F-V3-CH4-B tracked).
- V3: ACCEPT 100% (F-V3-CH4-A + F-V3-CH4-B discharged).
- LOCK status: 2-cycle LOCK eligible (V2 + V3 both ≥95%); cohort §3Z
  LOCK triggers on V3 close.

V≤5 ceiling: V3 lands at LOCK; no V4/V5 anticipated for CH4. The CH4
lens converges at V3.

## §4 — Cohort LOCK confirmation

Per V3 CHALLENGE-CONTEXT.md §0 + ORCHESTRATOR.md §3W + §3Z (cohort LOCK
= ≥95% × 2 consecutive cycles; V≤5 ceiling):

- **V2 cycle:** CH4 ACCEPT qualified 96% (≥95%). ✓
- **V3 cycle:** CH4 ACCEPT 100% (≥95%). ✓
- **Consecutive cycles ≥95%:** 2. ✓
- **V≤5 ceiling:** V3 lands at LOCK (well under V5). ✓
- **CH4 LOCK condition met at V3 close.**

Per V3 CHALLENGE-CONTEXT.md §2 CH4 ("CH4 96% qualified → 100% expected
at V3"): the V3 enactment matches the dispatch context's expected
LOCK-trigger outcome exactly. The two V3 micro-folds (F-V3-CH4-A,
F-V3-CH4-B) discharge the V2 qualifications without re-opening any V1
load-bearing finding.

Per V3 CHALLENGE-CONTEXT.md §3Z ("V3 is second consecutive cohort-wide
≥95% cycle → cohort §3Z LOCK triggers on V3 close"): the CH4 lens
contribution to the cohort LOCK is firm. The aggregator
`HARDENING-T-P2-V3-CONSOLIDATED.md` is the cohort §3Z LOCK declaration
document; this CH4 V3 report contributes the CH4 LOCK row.

## §5 — Accepted carry-forward (preserved through V3)

V3 preserves the V2 load-bearing enactment surface in full. The
following V2 carry-forward items remain V3-canonical:

- **SKELETON triple binary DELETE** (CH4-F1 V2 enactment): 2B §R3 +
  §A6 rows 7-9 (DELETED at V3) + Lock 16 v+1 `deleted` close state.
  The V3 SKELETON-triple-DELETE reduction (9 → 6 admitted §A6 rows)
  reflects V3 cleanup of the DELETED rows, not a regression.
- **Marker-string lowerer candidate refutation** (CH4-F2 V2 enactment):
  2B §R5 + LAC-2B-06 + 2D BackendShape Admission Ledger (1 ADMITTED
  + 4 NOT-ADMITTED). V3 zero drift.
- **Per-candidate adoption-cost ledger ≈34 rows** (CH4-F4 V2
  enactment, reduced from 37 by V3 SKELETON DELETE): 6 (2B §A6) +
  5 (2D) + 5 (2E) + 6 (2F V4 candidate) + 12 (2F V4 REDRESS slices).
  Coverage still exceeds V1's ≈28-row floor.
- **LOC envelopes per kernel + per slice** (CH4-F7 V2 enactment):
  2B §A6 + 2F V4 REDRESS slices + V3 addendum per-candidate cells.
- **Lock 16 manifest schema cohort propagation**: 2E `:311-336`
  13-cell template + 2A `:192` cohort-wide 8-cell + `BBNF_SIMD_STRICT=1`
  9th cell (V3 fold) + 2B §A6 schema preamble adoption.
- **Cross-dossier cross-references**: 2D `:14, 87, 125` cross-ref
  to 2B + 2D `:142-149` cross-ref to 2A `:192` + 2C `:305` + 2E
  `:328` (CH4-F2 + F-V3-CH4-A) preserved.

## §6 — Closing

V3 discharges the V2 cohort's two micro-fold qualifications operationally
and at the exact line ranges the V3 dispatch context named. The CH4
lens achieves 100% ACCEPT-rate at V3 — the second consecutive ≥95%
cycle — triggering the §3Z cohort LOCK condition.

Summary of V3 enactments:
- F-V3-CH4-A: cohort-wide `BBNF_SIMD_STRICT=1` named inline at 2A
  T2A-LAC-V1-03 `:192` + 2C closure-criteria header `:303-305` + 2D
  BackendShape Admission Ledger preamble `:142-149`, with mutual
  cross-references closing the cohort surface.
- F-V3-CH4-B: all six T2A-LAC-V1-05 abrogate gates numerically bound
  at 2D `:151-162` (e-graph 50000/10000/30 + CSP 1s + stale 30% +
  LOC growth row-local `loc_budget` + row regression + parity/checkasm).

The V2 four load-bearing finding enactments (CH4-F1 SKELETON DELETE,
CH4-F2 marker-string refutation, CH4-F4 per-candidate ledger, CH4-F7
LOC envelopes) are preserved without regression. The CH4 lens has zero
open carry-forward at V3 close.

**CH4 verdict for V3: ACCEPT (100%).**
**Cycle disposition: §3Z 2-cycle LOCK confirmed.**
**LOCK confirmation: CH4 lens LOCKED at V3 close (V2 96% + V3 100%
consecutive ≥95% cycles; V≤5 ceiling preserved).**

The aggregator `HARDENING-T-P2-V3-CONSOLIDATED.md` collects CH4 into
the cohort §3Z LOCK declaration. T-P2 LOCK unblocks T-P3 dispatch per
PASS-3-SYNTHESIS.md §3C (T-P1 LOCK already at `0a9c0fe65` + T-P2 LOCK
at V3 close).

## §7 — Authority register

- `restart/audit/totality/p2/hardening/V3/CHALLENGE-CONTEXT.md` (HEAD =
  `daa14127f` per dispatch context §0; current HEAD =
  `5aaab91d11389dc26ed8e6263c1a640cc9c28035`)
- `restart/audit/totality/p2/hardening/V2/CH4.md` — V2 ACCEPT qualified
  96% (load-bearing CH4-F1/F2/F4/F7 discharged; F-V3-CH4-A + F-V3-CH4-B
  tracked as V3 micro-folds)
- `restart/audit/totality/p2/hardening/V1/CH4.md` — V1 REVISE 33%
  (4 load-bearing findings: CH4-F1, F2, F4, F7)
- `restart/prompts/totality/PASS-2-RESEARCH.md` §3 CH4
- `restart/prompts/ORCHESTRATOR.md` §3W + §3Z (cohort LOCK = ≥95% × 2
  consecutive cycles; V≤5 ceiling)
- `restart/locks/LOCKS.md` Lock 16 v+1 (lines 282-360, especially
  320-322 `BBNF_SIMD_STRICT=1` precondition + 335-342 four close-state
  vocabulary)
- `restart/audit/totality/p2/2A-sota-landscape.md`
  (T2A-LAC-V1-03 `:192` F-V3-CH4-A enactment; T2A-LAC-V1-05 `:194`
  abrogate-cap source; UNKNOWN-3 `:181`; counted_source_ids `:79`
  F-V3-CH1-B fold)
- `restart/audit/totality/p2/2B-primitive-vocabulary.md`
  (§A4 #2 `:91` admission contract; §A5/§A6 substrate_target +
  retention_lifetime F-CH5-V2-01 fold; §A6 `:293-298` 6 admitted rows;
  §R3 `:356-410` SKELETON DELETE; §R5 `:424-481` marker-string
  refutation; LAC-2B-06 `:503` Lock 10 ban; OQ discharged `:486-487`)
- `restart/audit/totality/p2/2C-grammar-neutrality.md`
  (closure-criteria header `:303-305` F-V3-CH4-A enactment; 7-step
  onboarding test `:99-105`; generator-stub gap closure cells `:111-115`)
- `restart/audit/totality/p2/2D-cost-model.md`
  (BackendShape Admission Ledger preamble `:128-148` + F-V3-CH4-A
  cohort-wide precondition `:142-149` + F-V3-CH4-B abrogate-gate
  numeric bind `:151-162`; ledger rows `:164-179`; CH4-F2 cross-
  references `:14, 87, 125`; per-technique transfer coverage `:177-184`;
  UNKNOWN-2D-03 `:229`)
- `restart/audit/totality/p2/2E-host-arch-esoterica.md` (V2-LOCKED;
  Lock 16 hardware-gate manifest 13-cell schema `:311-336`; manifest
  column `BBNF_SIMD_STRICT_status` `:328`; per-candidate adoption-cost
  rows `:295-301`; Source-Present Primitive State `:343-367`;
  Material-Differential Gate `:369-389`)
- `restart/audit/totality/p2/2F-parse-that-gaps.md`
  (V5 frontmatter binding V4 admission ledger format `:81`; F-CH5-V2-02
  crate_target column; F-V3-CH1-B counted_source_ids 26 fold)
- `restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md`
  (per-technique admission ledger schema `:159-180`; state machine
  `:152-157`; abrogate ledger `:212-219`)
- `restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md`
  (per-candidate executable ledger `:84-101` with `BBNF_SIMD_STRICT=1`
  + LOC + risk + rollback + abrogate cells; abrogate caps `:103-115`
  including row-local `loc_budget` discipline at `:113`; vocabulary
  normalisation `:56-82`)
- `restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md`
  (V4 executable ledger delta 6 rows `:47-54`; REDRESS Slice Ownership
  12 rows `:67-79` with `loc_budget` populated; summary-state wording
  `:83-86`)
- `restart/audit/totality/p2/hardening/HARDENING-T-P2-V2-CONSOLIDATED.md`
  (V2 aggregator + V3 fold-packet authority + cohort LOCK trajectory
  binding)
