# CH2 GENERALITY (Lock 14) — Pass Alpha V5 Disposition

Lens: CH2 per `restart/prompts/ORCHESTRATOR.md §3W` — "Lock 14 holds:
no grammar-name leak; every proposed intervention is grammar-neutral
and works for CSS L4 / Sheets / BBNF-self, not only JSON."

Authority binding: `restart/locks/LOCKS.md:220-238` (Lock 14 text);
V1 dispatch context
`restart/skinny/tranches/sk-v14/research/alpha-hardening/V1/CHALLENGE-CONTEXT.md:99-110`
(§CH-2 scope); V4 disposition
`restart/skinny/tranches/sk-v14/research/alpha-hardening/V4/CH2.md`
(100 % ACCEPT; 0 REJECT; 1 non-finding observation against
`alpha-E:756` stale "8 grammars" budgeting cell; CONVERGED-FINAL on
the CH2 plane with the non-finding flagged for V5 belt-and-braces);
V5 cycle bind dispatching CH2 over the V4 baseline (`1bc9380b8`)
overlaid with the V5 micro-fold commit `87ee874f0` (one-file atomic
alpha-E ±11 lines repairing the V4 CH2 non-finding observation at
:756 + recomputing the cluster-total wall-clock at :770).

This V5 disposition is the **§3Z confirming pass at the V≤5 ceiling**.
The V5 mandate is threefold: (1) verify the V4 CH2 non-finding
observation is now resolved (alpha-E:756 + :770 roster-count-
agnostic); (2) verify Lock 14 baseline still holds with no new
grammar-name leak; the derived-enumeration pattern is now consistent
across the C-1 forward-invariant gate (§3), the C-3 round-trip gate
(§5), and the cost/cap table (§10); (3) fresh-finding scan across all
V5 artefacts. CH2 V5 executes the V4-folded gate command in the live
tree to confirm substrate continuity and reads the V5-folded budgeting
cell + wall-clock paragraph in tree to confirm the fold landed.

## §0 — Disposition summary

- ACCEPT-rate: 100 % (35 / 35 § lines disposed — one §10 cost/caps
  line gains explicit ACCEPT status as the V5 fold target)
- REJECT count: 0
- REVISE count: 0
- FOLD-LANDED count: 1 / 1 V4-folded surface (F-V5-α-E-1 repairs
  CH2 V4 non-finding observation §2.1; one-file atomic; ±11 lines)
- FOLD-PARTIAL count: 0
- FOLD-MISSING count: 0
- Critical findings: 0
- New findings: 0
- Escalation flag: none. CH2 converges at 100 % ACCEPT on V5. The
  V3 → V4 → V5 chain holds the percentage floor three times (V3 100 %
  + V4 100 % + V5 100 %), satisfies the §3Z two-consecutive-cycle
  close rule (V4 + V5 both ≥95 %; both at the ceiling), and closes
  the residual non-finding observation V4 surfaced. CH2 reports
  **CONVERGED-FINAL at the §3Z confirming-pass terminus**.

The V5 α-E micro-fold lands the precise belt-and-braces edit the V4
CH2 non-finding observation §2.1 prescribed: the cost/cap table row
at `alpha-E:756` now reads "C-1 sub-waves (per rostered grammar; per
sub-wave)" (was "8 grammars"); the wall-clock total paragraph at
`alpha-E:770-774` now reads "N × 30 min of redress windows where N
is the live rostered-grammar enumeration (`cargo metadata | jq` over
the grammar roster at HEAD)" (was "8 × 30 = 240 min"); the C-4 row's
shape-count variable is correspondingly renamed `N → M` ("M × 45 min
where M is the number of shapes the wiring exercises") to avoid the
collision the V5 fold introduces by binding `N` to the grammar-roster
count. The fold is minimal, mechanically precise, and addresses every
axis of the V4 non-finding observation.

## §1 — Per-artefact disposition table

| Artefact | § | Disposition | Reason |
|---|---|---|---|
| `SYNTHESIS.md` | §0.1 close condition | ACCEPT | STAND per V5 commit body (single-file commit on alpha-E; SYNTHESIS unchanged); R10 verbatim; close criterion treats JSON cells + CSS features symmetrically. |
| `SYNTHESIS.md` | §0.2 goalset enumeration | ACCEPT | Per-grammar row counts plane-neutral; F-1 numeric divergence reconciliation paragraph carries forward; STAND. |
| `SYNTHESIS.md` | §0.3 R-target goalset (R4 row) | ACCEPT | F-11 mirror clause "first instance of the `regen-{grammar}` family; the xtask binary parametrises a grammar-neutral generator" preserved at `SYNTHESIS.md:96`; STAND. |
| `SYNTHESIS.md` | §0.4 P-1…P-7 pre-blocks | ACCEPT | P-6 names per-grammar provider modules as recurrence vector; grammar-neutral. STAND. |
| `SYNTHESIS.md` | §0.5 wave-by-wave deferral | ACCEPT | Contracted deferral unchanged; deferred work remains grammar-neutral in surface. STAND. |
| `SYNTHESIS.md` | §1 corrected diagnosis | ACCEPT | Pillars marked `grammar-neutral` for bbnf-simd / OffsetFlags + Tape. STAND. |
| `SYNTHESIS.md` | §2 telemetry binding | ACCEPT | Column rule wording treats every plane uniformly; `track2_entry_point` column plane-keyed not grammar-keyed. STAND. |
| `SYNTHESIS.md` | §3 candidate C-1 row | ACCEPT | Forward-invariant clause at `SYNTHESIS.md:271` verbatim — "any new grammar added under `workspace.metadata.bbnf.grammars.{name}` produces ZERO new `.rs` files…ZERO new directories in `crates/core/src/runtime/`". F-12 + E-7 binding preserved. STAND. |
| `SYNTHESIS.md` | §3 candidate C-2 row | ACCEPT | Per-plane comparator rebind has no grammar branch. STAND. |
| `SYNTHESIS.md` | §3 candidate C-3 row | ACCEPT | F-11 family-shape clause "first instance of the `regen-{grammar}` family — the xtask binary parametrises a grammar-neutral generator" preserved at `SYNTHESIS.md:273`. STAND. |
| `SYNTHESIS.md` | §3 candidate C-4 row | ACCEPT | C-4 row inherits F-13's S-P3 dispatch-discipline clause; no grammar branch baked. STAND. |
| `SYNTHESIS.md` | §3 candidate C-5 row | ACCEPT | Revert + REDRESS; row-keyed not grammar-keyed. STAND. |
| `SYNTHESIS.md` | §4 S-P3 constraints (C-1 forward invariant clause) | ACCEPT | `SYNTHESIS.md:330-334` F-12 clause verbatim preserved. STAND. |
| `SYNTHESIS.md` | §4 S-P3 constraints (C-4 dispatch-discipline clause) | ACCEPT | `SYNTHESIS.md:335-341` F-13 clause verbatim preserved (including the "no `match grammar { Json => ..., CssL4 => ... }`" prohibition at line 340). STAND. |
| `SYNTHESIS.md` | §4 S-P3 constraints (other lines) | ACCEPT | G-SIMD-GRAMMAR-POLICY triad (F-14), LOC ceiling (F-6), triumvirate (F-9) all grammar-neutral. STAND. |
| `SYNTHESIS.md` | §5 pre-blocked routes | ACCEPT | "grammar-name branches in generic crates" enumerated as pre-blocked. STAND. |
| `SYNTHESIS.md` | §6 close posture | ACCEPT | Bracket framing symmetric. STAND. |
| `HANDOFF.md` | §1 bracket verdict | ACCEPT | Pillar list mirrors SYNTHESIS §1.1. STAND. |
| `HANDOFF.md` | §3 honest baseline | ACCEPT | JSON-CSS parity in admit-count framing preserved. STAND. |
| `HANDOFF.md` | §4 authorship declaration | ACCEPT | F-2(b) lands α-F as sole author; no grammar implications. STAND. |
| `HANDOFF.md` | §6 next-move | ACCEPT | "CH2 verifies Lock 14 grammar-neutrality" pin preserved; F-7 + F-8 grammar-neutral. STAND. |
| `HANDOFF.md` | §7 refusal conditions (broadened) | ACCEPT | F-V3-α-F-1 broadening preserved; lines 208-218 "any grammar-specific generic behaviour through SPEC-local wording (Lock 14 binding)" refusal + `G-SIMD-GRAMMAR-POLICY` clause + cascade-silently-serve refusal all stand. STAND. |
| `HANDOFF.md` | §8 disposition | ACCEPT | V4 PENDING-V5 wording carries forward; V5 close updates the disposition surface but no grammar implications. STAND. |
| `alpha-A-results-extraction.md` | §1 / §3 / §6 | ACCEPT | A-1/A-2/A-3 folds carry forward; row-keyed, plane-keyed, grammar-neutral. STAND per V5 commit body (alpha-A unchanged). |
| `alpha-B-competitor-deltas.md` | comparator overlay | ACCEPT | STANDs per V2 + V3 + V4 commit bodies + V5 commit body (alpha-B unchanged); CSS / JSON overlay symmetric. |
| `alpha-C-redress-digest.md` | §2 P-7 falsifiability + other P-1..P-6 | ACCEPT | C-1 fold triple-check gate preserved; substrate-keyed not grammar-keyed. STAND. |
| `alpha-D-validated-invalidated.md` | §S-3 Lock 14 + §V-4 + §V-5 | ACCEPT | 30 violations cited; bbnf-simd + Tape + OffsetFlags grammar-neutral. STAND. |
| `alpha-E-candidate-shortlist.md` | §2 shortlist table | ACCEPT | C-3 row pointer "see §5 + hardening V1 CH7 §3.1"; C-1 row "see §3"; grammar-neutral. STAND. |
| `alpha-E-candidate-shortlist.md` | §3 C-1 detail (forward invariant + owner paths) | ACCEPT | `alpha-E:170-176` E-7 forward-invariant clause verbatim preserved. Owner-paths section at lines 108, 136 still enumerates current-state PRUNE targets using the `{json, css_l4, google_sheets, bbnf, csv, ebnf, bnf, math}/` literal — V3/V4/V5 CH2 §2 category-3 audit-narrative naming (the symbols PRUNE removes; deleted from prose when PRUNE lands). STAND. |
| `alpha-E-candidate-shortlist.md` | §4 C-2 detail | ACCEPT | Per-plane comparator rebind preserved; no grammar branch. STAND. |
| `alpha-E-candidate-shortlist.md` | §5 C-3 detail (Purpose + family binding) | ACCEPT | E-6 family-shape clause at `alpha-E:303-309` verbatim preserved. STAND. |
| `alpha-E-candidate-shortlist.md` | §5 C-3 detail (round-trip gate) | ACCEPT | F-V4-α-E-1 fold at `alpha-E:362-387` preserved verbatim through V5 (V5 fold is single-file but touches only §10 rows :756 + :770-774). The V4 executable substrate (`cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'`) re-verified live below — 9 idents enumerate; gate text remains grammar-list-free. STAND. |
| `alpha-E-candidate-shortlist.md` | §6 C-4 detail (Pre-blocked routes + 2-grammar) | ACCEPT | E-8 clauses at `alpha-E:559-571` verbatim preserved (`BackendShape` enum dispatch + "no `match grammar { Json => ..., CssL4 => ... }`" prohibition + two-grammar-family exercise requirement). STAND. |
| `alpha-E-candidate-shortlist.md` | §7 C-5 detail | ACCEPT | Revert covers JSON parse_only + CSS rows symmetrically; E-10 scribe-count clarification row-keyed not grammar-keyed. STAND. |
| `alpha-E-candidate-shortlist.md` | §8 consolidated pre-blocks | ACCEPT | P-6 verbatim; consolidated list grammar-neutral. STAND. |
| `alpha-E-candidate-shortlist.md` | §9 concurrency matrix | ACCEPT | E-13 fold preserved; ledger-serialisation discipline, not grammar privilege. STAND. |
| `alpha-E-candidate-shortlist.md` | §10 cost / caps / telemetry (V5 fold target) | ACCEPT — **FOLD-LANDED (F-V5-α-E-1; closes CH2 V4 non-finding observation §2.1)** | `alpha-E:756` now reads (V5 diff): `\| C-1 sub-waves (per rostered grammar; per sub-wave) \| 20 min \| 15 min \| 30 min \|` — hardcoded "8 grammars" entirely removed; `alpha-E:770-774` paragraph now reads "The C-1 cluster total is N × 30 min of redress windows where N is the live rostered-grammar enumeration (`cargo metadata | jq` over the grammar roster at HEAD), run serialised per §9; the C-4 cluster total is M × 45 min where M is the number of shapes the wiring exercises (≥ 2 per E-8's two-grammar-family requirement)". The N → M rename for the C-4 variable avoids the collision the fold introduces by binding N to grammar-roster count (V5 commit body notes this explicitly). The "8 × 30 = 240 min" literal that V4 CH2 §2.1 flagged is now derived: `N × 30 min` where `N` is the substrate-bound enumeration that the same `cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'` invocation that drives the C-3 round-trip gate at `alpha-E:362-387` produces. Substrate-cross-binding holds: the cost cell, the C-1 forward-invariant gate at `alpha-E:170-176`, the C-3 round-trip gate, and the workspace metadata clause `LOCKS.md:220` ("workspace metadata declaring its strategy") now share one substrate. The CH2 V4 non-finding observation is closed; the V5 fold lands the exact change the V4 §2.1 prescription named ("8 grammars" → "rostered grammars; currently 9" or similar; the V5 chose "per rostered grammar" + variable-bound total — equally roster-count-agnostic, and structurally cleaner because the variable is derived live rather than re-stated as a current count). |
| `alpha-E-candidate-shortlist.md` | §11 convergence + escalation | ACCEPT | Escalation triggers measurement-keyed, not grammar-keyed. STAND. |
| `DISPATCH-CONTEXT.md` | §0–§3 + per-agent | ACCEPT | STANDs per V2 addendum §0 + V3 + V4 + V5 commit bodies (DISPATCH-CONTEXT unchanged); α-agent scope spec unchanged. |

## §2 — Critical findings + V5 fold verification + executable verification

Zero critical findings in V5. The V4 CH2 non-finding observation §2.1
has been closed by F-V5-α-E-1, and no new Lock 14 leak surfaces under
fresh-finding scan across the V5 artefacts.

### Fold verification: F-V5-α-E-1 — LANDED (documentary + substrate-cross-binding + executable)

CH2 V4 non-finding observation §2.1 flagged that the cost/cap table
row at `alpha-E:756` read "C-1 sub-waves (8 grammars; per sub-wave)"
and the subsequent wall-clock paragraph at `alpha-E:770` read "The
C-1 cluster total is 8 × 30 = 240 min", both naming a literal `8`
grammar count that had been stale since `css_pretty` joined the
roster (live count: 9 per the V4 executable verification). V4 CH2
classified this as a non-finding under CH2 scope (the cell describes
sub-wave budgeting scope, not a Lock 14 gate enumeration substrate),
flagged it for the V5 belt-and-braces fold the V4 aggregator
prescribed.

**Documentary verification.** The V5 diff (commit `87ee874f0`, one
file, ±11 lines) lands three changes at `alpha-E:756, 770-774`:

1. **Cost/cap table row.** `\| C-1 sub-waves (8 grammars; per
   sub-wave) \|` → `\| C-1 sub-waves (per rostered grammar; per
   sub-wave) \|`. The hardcoded "8 grammars" literal is entirely
   removed. The cell now reads as roster-count-agnostic: it states
   the cap discipline per sub-wave, and the sub-wave structure is
   implicit (one sub-wave per grammar in the roster, per the C-1
   `Sub-divide by grammar` clause at `alpha-E:109-110`).

2. **Cluster-total wall-clock.** "8 × 30 = 240 min" → "N × 30 min
   of redress windows where N is the live rostered-grammar
   enumeration (`cargo metadata | jq` over the grammar roster at
   HEAD)". The literal `8 × 30 = 240` is derived through a
   substrate-bound variable `N` whose binding clause names the
   exact `cargo metadata | jq` invocation that drives the C-3
   round-trip gate at `alpha-E:362-387`. Substrate-cross-binding
   holds: the cost cell, the C-1 forward-invariant gate at
   `alpha-E:170-176`, the C-3 round-trip gate, and `LOCKS.md:220`
   share one substrate — workspace metadata.

3. **C-4 variable rename N → M.** The neighbouring C-4 cluster
   total previously read "N × 45 min where N is the number of
   shapes the wiring exercises" — `N` collided with the new
   grammar-roster `N`. V5 renames the C-4 shape-count to `M`
   ("M × 45 min where M is the number of shapes the wiring
   exercises (≥ 2 per E-8's two-grammar-family requirement)") to
   keep the two variables disjoint. The rename is precise (V5
   commit body notes it explicitly: "M x 45 min for C-4 where M
   = exercised shapes (N->M rename avoids variable collision)").
   The ≥ 2 lower bound from E-8 is preserved verbatim.

The V4 CH2 §2.1 prescription ("8 grammars" → "rostered grammars;
currently 9" or similar, with the wall-clock total correspondingly
re-derived) is **fully met** — the V5 fold chose the cleaner
roster-count-agnostic surface (no literal current count anywhere in
the cell; the live count derives from the substrate at gate-authoring
time) rather than the alternative current-count phrasing the V4 §2.1
proposed as one option. Both forms are roster-count-agnostic; the V5
choice is structurally stronger because it eliminates the inherited-
staleness vector entirely (a future grammar admission to the roster
re-binds `N` without prose re-edit; the alternative "currently 9"
phrasing would have required a prose edit at every grammar
admission).

**Executable verification.** Re-run in `/Users/mkbabb/Programming/bbnf-lang`:

```
$ cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'
bbnf
json
css_l4
css_pretty
google_sheets
ebnf
bnf
csv
math
```

Nine idents enumerate. The V5 cost-cell `N` binding now resolves to
`N = 9` at plan-authoring time; the C-1 cluster total computes to
`9 × 30 = 270 min` of redress windows (the figure V4 CH2 §2.1
predicted as the correct number under the live roster). The C-4
`M` binding remains shape-count-bound (≥ 2 per E-8); no collision
between `N` and `M` in the §10 prose. Both variables are derived
live at plan-authoring time, not literal in the artefact.

### Lock 14 baseline holds at V5 — substrate-cross-binding triple

The V5 fold cements a structural property the V4 fold inaugurated
but only partially achieved: the V4 fold bound the C-3 round-trip
gate's grammar enumeration to `workspace.metadata.bbnf.grammars` via
`cargo metadata | jq`; the V5 fold extends the same substrate
binding to the §10 cost cluster total. The three Lock 14
load-bearing surfaces of the α-E candidate shortlist now all share
one substrate:

| Surface | Citation | Substrate clause |
|---|---|---|
| C-1 forward-invariant gate | `alpha-E:170-176` | "any new grammar added under `workspace.metadata.bbnf.grammars.{name}` produces ZERO new `.rs` files…" |
| C-3 round-trip gate | `alpha-E:362-387` | `for g in $(cargo metadata --format-version 1 --no-deps \| jq -r '.metadata.bbnf.grammars[].ident'); do …` |
| §10 cost cluster total | `alpha-E:770-771` | "N × 30 min of redress windows where N is the live rostered-grammar enumeration (`cargo metadata \| jq` over the grammar roster at HEAD)" |

This triple satisfies the V4 CH2 §2 conclusion that "the gate
substrate is correct and Lock 14 holds" by extending substrate
correctness from the gate axis to the budgeting axis. The V5 fold
closes the residual ambiguity by removing the last hardcoded count.
Per `LOCKS.md:220` ("workspace metadata declaring its strategy") the
substrate the three surfaces share is canonically the Lock 14 source
language — the substrate-cross-binding is structurally aligned with
Lock 14's own substrate clause.

### Fresh-finding scan: V5 artefacts under Lock 14

Scanned all V5 artefacts (SYNTHESIS, HANDOFF, α-A, α-B, α-C, α-D,
α-E, DISPATCH-CONTEXT) for grammar-name leaks via:

```
grep -nE 'JsonGrammar|RuntimeProvider::Json|parse_json_grammar|
match grammar|if grammar ==|8 grammars|9 grammars|
json.*css_l4.*google_sheets|google_sheets.*bbnf|csv.*ebnf.*bnf'
```

Every match resolves to one of the three benign categories
enumerated in V3 / V4 CH2 §2 (fresh-finding scan):

1. **Negative-grep targets inside falsifiability gates** (the gates
   *detect and reject* the leaks). Examples: `SYNTHESIS.md:271`,
   `alpha-E:83`, `alpha-E:159`, `alpha-C:322`, `DISPATCH-CONTEXT.md:165`.
   Unchanged from V3/V4.

2. **Pre-block patterns explicitly prohibited** (named as the
   forbidden form so S-P3 constraints reject them). Examples:
   `SYNTHESIS.md:340`, `alpha-E:577`. Unchanged from V3/V4.

3. **Current-state references inside the audit narrative** (to-be-
   PRUNED symbols + files + counts). Examples: `alpha-E:108, 129,
   132, 136` — the C-1 "Owner paths" + per-grammar runtime
   directory enumeration that PRUNE-3 + PRUNE-4 collapse;
   `alpha-E:105, 109-110, 178` and `SYNTHESIS.md:95, 271` and
   `DISPATCH-CONTEXT.md:165` — the "8 per-grammar provider modules"
   / "64 hand-written per-grammar files" / "8 sub-waves" current-
   state counts that describe what PRUNE-3 + PRUNE-4 removes from
   the codegen tree (8 hand-written modules in `codegen/`, distinct
   from the grammar-roster count which is the cost cell's scope).
   These literals exist *because* the PRUNE operates on them; they
   would be deleted from the prose only when the PRUNE itself
   lands and the audit narrative is superseded. Unchanged from
   V3/V4.

Importantly the "8" hits in this third category name the **module
count** (8 hand-written codegen modules at `crates/codegen/src/*.rs`
naming the per-grammar providers; 64 = 8 modules × 8 average files
per module) — NOT the grammar-roster count which the V5 fold
just removed from the cost cell. The two counts coincide at the
current cycle but are semantically distinct surfaces: codegen-
module count is a fixed enumeration of the historical per-grammar
provider files that PRUNE consolidates; grammar-roster count is the
live `cargo metadata` enumeration. The V5 fold correctly addresses
the second axis (the live-roster surface) and leaves the first axis
(the historical-codegen-module audit narrative) intact, which is
the correct disposition because the audit narrative names what
PRUNE deletes, not what the gate enumerates.

No new Lock 14 leak surfaces in V5. The CH2 lens holds with zero
critical findings and zero fresh observations beyond the V5 fold
target.

#### §2.1 — Observation status: V4 §2.1 non-finding CLOSED

The V4 CH2 §2.1 non-finding observation (cost/cap table cell
`alpha-E:756` reading "8 grammars" stale) is **CLOSED** by
F-V5-α-E-1. No new observation surfaces in V5. The V5 fresh-finding
scan yields zero new flags.

## §3 — Recommended folds for post-V5 (if any)

None from CH2. The lens converges at 100 % ACCEPT on V5. The
three-consecutive-cycle close chain (V3 100 % + V4 100 % + V5
100 %; all three with zero CH2 orphan REVISEs at close) far exceeds
the §3Z two-consecutive-cycle close rule on CH2's plane. The V5
confirming pass is the campaign-wide §3Z terminus at the V≤5
ceiling; CH2 contributes no fold to any post-V5 surface and
reports CONVERGED-FINAL at the §3Z terminus.

The CH2 V2 NF-1 + CH1 V3 REV-1 + CH2 V4 §2.1 lineage is now
PERMANENTLY closed across the three-fold chain:

- F-V3-α-E-1 (V3 fold; V2 NF-1 closure on documentary grounds) —
  superseded by F-V4-α-E-1.
- F-V4-α-E-1 (V4 fold; CH1 V3 REV-1 closure on executable grounds;
  CH2 V2 NF-1 closure on substrate-correctness grounds) — landed
  at `alpha-E:362-387`; substrate `workspace.metadata.bbnf.grammars`
  bound to the C-3 round-trip gate.
- F-V5-α-E-1 (V5 fold; CH2 V4 §2.1 non-finding closure on
  budgeting-axis substrate-binding grounds) — landed at
  `alpha-E:756, 770-774`; same substrate bound to the §10 cost
  cluster total; N → M variable rename keeps C-4 disjoint;
  three-surface substrate-cross-binding now holds across §3 + §5
  + §10.

CH2 V5 reports **CONVERGED-FINAL at the §3Z confirming-pass
terminus**. The Lock 14 baseline holds at the V5 baseline without
qualification. The substrate-cross-binding triple cements the
structural property that any future grammar admission to
`workspace.metadata.bbnf.grammars` flows through one substrate to
three Lock 14-load-bearing surfaces (gate, round-trip, budgeting)
without prose re-edit — exactly the roster-count-agnostic forward
posture the §CH-2 lens scope demands. SK-V14 Pass Alpha bracket is
ready to lock at the V5 convergence on the CH2 plane; the
aggregator's §3Z verdict awaits the parallel V5 CH1–CH7
dispositions.
