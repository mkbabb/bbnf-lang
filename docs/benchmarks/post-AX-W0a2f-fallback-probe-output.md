## JSON

Entry rule: `value`

Entry-reachable classified rules with fallback-position body: **4**

| Rule | Outer fallback kind | ShapeTag | Native-handling |
|---|---|---|---|
| `bool` | Alt | Keyword | yes |
| `number` | Regex | Number | yes |
| `string` | Regex | String | yes |
| `value` | Alt | Wrap | yes |

Note: sub-variant type collision between `globalKeyword::globalKeyword_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `borderWidthDecl::borderWidthDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `anPlusB::anPlusB_1` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `valueUnit::valueUnit_0` and `mathValue::mathValue_0` (F64). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__flexWrapDecl_cont_174::__flexWrapDecl_cont_174_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `cursorKeyword::cursorKeyword_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `nthFunctionName::nthFunctionName_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `colorSpace::colorSpace_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `colorSpace::colorSpace_1` and `cursorKeyword::cursorKeyword_1` (Tuple([Span, Tuple([Span, Tuple([Span, U8])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `colorSpace::colorSpace_2` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__fontWeightDecl_cont_178::__fontWeightDecl_cont_178_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `flexDirDecl::flexDirDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `borderStyleKeyword::borderStyleKeyword_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `borderStyleKeyword::borderStyleKeyword_1` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `viewportLengthUnit::viewportLengthUnit_0` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `viewportLengthUnit::viewportLengthUnit_1` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `viewportLengthUnit::viewportLengthUnit_3` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `containerLengthUnit::containerLengthUnit_0` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `containerLengthUnit::containerLengthUnit_1` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__textAlignDecl_cont_184::__textAlignDecl_cont_184_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__positionDecl_cont_170::__positionDecl_cont_170_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `borderStyleDecl::borderStyleDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__pattern_db1e96a7fb6ccf5d::__pattern_db1e96a7fb6ccf5d_0` and `mathValue::mathValue_0` (F64). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__flexDirDecl_cont_173::__flexDirDecl_cont_173_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `length::length_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__borderStyleDecl_cont_181::__borderStyleDecl_cont_181_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `alignKeyword::alignKeyword_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `alignKeyword::alignKeyword_1` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `alignKeyword::alignKeyword_2` and `nthFunctionName::nthFunctionName_1` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__fontDecl_cont_164::__fontDecl_cont_164_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__visibilityDecl_cont_172::__visibilityDecl_cont_172_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `cursorDecl::cursorDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `bgProps::bgProps_0` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `bgProps::bgProps_1` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `mediaType::mediaType_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `mediaType::mediaType_1` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `spacingProps::spacingProps_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `spacingProps::spacingProps_1` and `nthFunctionName::nthFunctionName_1` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `spacingProps::spacingProps_3` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontWeightDecl::fontWeightDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__cursorDecl_cont_186::__cursorDecl_cont_186_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `value::value_1` and `mathValue::mathValue_0` (F64). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `value::value_2` and `namedColor::namedColor_0` (U32). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `value::value_3` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `flexWrapDecl::flexWrapDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `transitionProps::transitionProps_2` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `transitionProps::transitionProps_3` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `listTableProps::listTableProps_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `listTableProps::listTableProps_1` and `transitionProps::transitionProps_0` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `listTableProps::listTableProps_3` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `positionDecl::positionDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__alignDecl_cont_175::__alignDecl_cont_175_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__boxSizingDecl_cont_185::__boxSizingDecl_cont_185_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__borderWidthDecl_cont_180::__borderWidthDecl_cont_180_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `visibilityDecl::visibilityDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `relativeLengthUnit::relativeLengthUnit_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__displayDecl_cont_169::__displayDecl_cont_169_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `transformProps::transformProps_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `transformProps::transformProps_1` and `transitionProps::transitionProps_1` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `transformProps::transformProps_3` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `absoluteLengthUnit::absoluteLengthUnit_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `absoluteLengthUnit::absoluteLengthUnit_1` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontDecl::fontDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `alignDecl::alignDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontProps::fontProps_0` and `nthFunctionName::nthFunctionName_1` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontProps::fontProps_1` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontProps::fontProps_2` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontProps::fontProps_3` and `cursorKeyword::cursorKeyword_1` (Tuple([Span, Tuple([Span, Tuple([Span, U8])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontProps::fontProps_5` and `transitionProps::transitionProps_0` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontProps::fontProps_6` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `overflowDecl::overflowDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `displayKeyword::displayKeyword_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `displayKeyword::displayKeyword_1` and `nthFunctionName::nthFunctionName_1` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `displayKeyword::displayKeyword_3` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `flexDirKeyword::flexDirKeyword_0` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `flexDirKeyword::flexDirKeyword_1` and `nthFunctionName::nthFunctionName_1` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontLengthUnit::fontLengthUnit_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `fontLengthUnit::fontLengthUnit_1` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `textAlignDecl::textAlignDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `flexWrapKeyword::flexWrapKeyword_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `flexWrapKeyword::flexWrapKeyword_1` and `cursorKeyword::cursorKeyword_1` (Tuple([Span, Tuple([Span, Tuple([Span, U8])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `resolutionUnit::resolutionUnit_0` and `globalKeyword::globalKeyword_1` (Tuple([Span, Tuple([Span, U8])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `resolutionUnit::resolutionUnit_1` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `__overflowDecl_cont_171::__overflowDecl_cont_171_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `colorProps::colorProps_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `colorProps::colorProps_1` and `sizeProps::sizeProps_1` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `colorProps::colorProps_3` and `spacingProps::spacingProps_2` (Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, Tuple([Span, U8])])])])])])). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `boxSizingDecl::boxSizingDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `displayDecl::displayDecl_0` and `sizeProps::sizeProps_0` (U8). Codegen will resolve by rule-scoped lookup.
## CSS L4

Entry rule: `stylesheet`

Entry-reachable classified rules with fallback-position body: **98**

| Rule | Outer fallback kind | ShapeTag | Native-handling |
|---|---|---|---|
| `__pattern_db1e96a7fb6ccf5d` | Alt | Wrap | yes |
| `absoluteLengthUnit` | Alt | Keyword | yes |
| `alignDecl` | Alt | Flat | **RISK** |
| `alignKeyword` | Alt | Keyword | yes |
| `anPlusB` | Alt | AltDispatch | yes |
| `angleUnit` | Alt | Keyword | yes |
| `atRule` | Alt | Wrap | yes |
| `atRuleBody` | Alt | Keyword | yes |
| `attrMatcher` | Alt | Keyword | yes |
| `attrSelector` | Alt | Flat | **RISK** |
| `bgProps` | Alt | Flat | **RISK** |
| `blockContent` | Alt | Flat | **RISK** |
| `borderRadiusDecl` | Alt | Flat | **RISK** |
| `borderStyleDecl` | Alt | Flat | **RISK** |
| `borderStyleKeyword` | Alt | Keyword | yes |
| `borderWidthDecl` | Alt | Flat | **RISK** |
| `borderWidthKeyword` | Alt | Keyword | yes |
| `boxSizingDecl` | Alt | Flat | **RISK** |
| `boxSizingKeyword` | Alt | Keyword | yes |
| `colonSelector` | Alt | Wrap | yes |
| `colorProps` | Alt | Keyword | yes |
| `colorSpace` | Alt | Keyword | yes |
| `combinator` | Alt | Wrap | yes |
| `compoundSelector` | Alt | Unordered | **RISK** |
| `containerLengthUnit` | Alt | Flat | **RISK** |
| `cssString` | Regex | String | yes |
| `cursorDecl` | Alt | Flat | **RISK** |
| `cursorKeyword` | Alt | Keyword | yes |
| `customPropertyDecl` | Regex | Flat | **RISK** |
| `dashIdent` | Regex | HRegex | yes |
| `declaration` | Alt | Wrap | yes |
| `dirKeyword` | Alt | Keyword | yes |
| `displayDecl` | Alt | Flat | **RISK** |
| `displayKeyword` | Alt | Keyword | yes |
| `envFunction` | Regex | ArgList | **RISK** |
| `flexDirDecl` | Alt | Flat | **RISK** |
| `flexDirKeyword` | Alt | Keyword | yes |
| `flexNumDecl` | Alt | Flat | **RISK** |
| `flexWrapDecl` | Alt | Flat | **RISK** |
| `flexWrapKeyword` | Alt | Keyword | yes |
| `fontDecl` | Alt | Flat | **RISK** |
| `fontLengthUnit` | Alt | Keyword | yes |
| `fontProps` | Alt | Keyword | yes |
| `fontWeightDecl` | Alt | Flat | **RISK** |
| `fontWeightKeyword` | Alt | Keyword | yes |
| `frequencyUnit` | Alt | Keyword | yes |
| `funcBody` | Alt | Flat | **RISK** |
| `genericAtRule` | Regex | Flat | **RISK** |
| `genericDecl` | Regex | Flat | **RISK** |
| `genericFunction` | Regex | ArgList | **RISK** |
| `globalKeyword` | Alt | Keyword | yes |
| `hash` | Regex | HRegex | yes |
| `hex` | Regex | Flat | **RISK** |
| `ident` | Regex | HRegex | yes |
| `keyframeSel` | Regex | Pratt | **RISK** |
| `keyframeStop` | Alt | AltDispatch | yes |
| `length` | Alt | Flat | **RISK** |
| `listTableProps` | Alt | Keyword | yes |
| `mathProductOp` | Alt | Keyword | yes |
| `mathSumOp` | Alt | Keyword | yes |
| `mathValue` | Alt | AltDispatch | yes |
| `mediaCondition` | Alt | Wrap | yes |
| `mediaFeature` | Regex | Flat | **RISK** |
| `mediaInParens` | Alt | Keyword | yes |
| `mediaQualifier` | Alt | Keyword | yes |
| `mediaType` | Alt | Keyword | yes |
| `namedColor` | Alt | AltDispatch | yes |
| `nsPrefix` | Alt | Flat | **RISK** |
| `nthFunctionName` | Alt | Flat | **RISK** |
| `number` | Regex | Number | yes |
| `overflowDecl` | Alt | Flat | **RISK** |
| `overflowKeyword` | Alt | Keyword | yes |
| `positionDecl` | Alt | Flat | **RISK** |
| `positionKeyword` | Alt | Keyword | yes |
| `pseudoClass` | Alt | Keyword | yes |
| `pseudoElement` | Alt | Keyword | yes |
| `relativeLengthUnit` | Alt | Wrap | yes |
| `relativeSelectorList` | Regex | Pratt | **RISK** |
| `resolutionUnit` | Alt | Keyword | yes |
| `ruleItem` | Alt | Wrap | yes |
| `selectorIdent` | Regex | HRegex | yes |
| `selectorList` | Regex | Pratt | **RISK** |
| `sizeProps` | Alt | Keyword | yes |
| `spacingProps` | Alt | Keyword | yes |
| `string` | Regex | String | yes |
| `textAlignDecl` | Alt | Flat | **RISK** |
| `textAlignKeyword` | Alt | Keyword | yes |
| `timeUnit` | Alt | Keyword | yes |
| `transformProps` | Alt | Keyword | yes |
| `transitionProps` | Alt | Keyword | yes |
| `typeSelector` | Alt | AltDispatch | yes |
| `urlFunction` | Alt | ArgList | **RISK** |
| `value` | Alt | AltDispatch | yes |
| `valueUnit` | Alt | Wrap | yes |
| `varFunction` | Regex | ArgList | **RISK** |
| `viewportLengthUnit` | Alt | Keyword | yes |
| `visibilityDecl` | Alt | Flat | **RISK** |
| `visibilityKeyword` | Alt | Keyword | yes |

Note: sub-variant type collision between `error_literal::error_literal_0` and `compare_op::compare_op_1` (U8). Codegen will resolve by rule-scoped lookup.
Note: sub-variant type collision between `error_literal::error_literal_1` and `compare_op::compare_op_0` (Tuple([Span, U8])). Codegen will resolve by rule-scoped lookup.
## Sheets

Entry rule: `formula`

Entry-reachable classified rules with fallback-position body: **17**

| Rule | Outer fallback kind | ShapeTag | Native-handling |
|---|---|---|---|
| `add_op` | Alt | Keyword | yes |
| `boolean` | Alt | Wrap | yes |
| `cell_or_range` | Alt | Wrap | yes |
| `cell_ref` | Regex | HRegex | yes |
| `compare_op` | Alt | Keyword | yes |
| `error_literal` | Alt | Flat | **RISK** |
| `formula` | Regex | Flat | **RISK** |
| `identifier` | Regex | HRegex | yes |
| `lambda_call` | Regex | ArgList | **RISK** |
| `let_call` | Regex | ArgList | **RISK** |
| `mul_op` | Alt | Keyword | yes |
| `number` | Regex | HRegex | yes |
| `primary` | Alt | Wrap | yes |
| `range_ref` | Alt | Flat | **RISK** |
| `sheet_prefix` | Alt | Wrap | yes |
| `string` | Regex | String | yes |
| `unary_prefix` | Alt | Keyword | yes |

Note: sub-variant type collision between `term::term_2` and `value_atom::value_atom_3` (Tuple([Span, BoxedEnum, Span])). Codegen will resolve by rule-scoped lookup.
## BBNF

Entry rule: `grammar`

Entry-reachable classified rules with fallback-position body: **30**

| Rule | Outer fallback kind | ShapeTag | Native-handling |
|---|---|---|---|
| `__pattern_6454fdf5a63906f1` | Alt | Keyword | yes |
| `big_comment` | Regex | Flat | **RISK** |
| `binary_factor` | Alt | Pratt | **RISK** |
| `bool_lit` | Alt | Keyword | yes |
| `comment` | Regex | Flat | **RISK** |
| `debug_directive` | Alt | Flat | **RISK** |
| `directive` | Alt | Keyword | yes |
| `factor` | Alt | Flat | **RISK** |
| `float_lit` | Regex | HRegex | yes |
| `grammar` | Alt | Array | **RISK** |
| `identifier` | Regex | HRegex | yes |
| `import_directive` | Alt | Flat | **RISK** |
| `import_path` | Regex | Flat | **RISK** |
| `int_lit` | Regex | HRegex | yes |
| `literal` | Alt | Keyword | yes |
| `pretty_directive` | Alt | Flat | **RISK** |
| `pretty_hint` | Regex | Flat | **RISK** |
| `regex` | Regex | Flat | **RISK** |
| `rhs` | Alt | Wrap | yes |
| `term` | Alt | AltDispatch | yes |
| `type_name` | Alt | AltDispatch | yes |
| `value_add` | Alt | Pratt | **RISK** |
| `value_atom` | Alt | AltDispatch | yes |
| `value_closure` | Regex | Flat | **RISK** |
| `value_cmp` | Alt | Pratt | **RISK** |
| `value_expr` | Alt | Wrap | yes |
| `value_input` | Regex | Pratt | **RISK** |
| `value_mul` | Alt | Pratt | **RISK** |
| `value_path` | Regex | Pratt | **RISK** |
| `value_unary` | Alt | AltDispatch | yes |

## EBNF

Entry rule: `grammar`

Entry-reachable classified rules with fallback-position body: **12**

| Rule | Outer fallback kind | ShapeTag | Native-handling |
|---|---|---|---|
| `alternation` | Regex | Flat | **RISK** |
| `character` | Alt | AltDispatch | yes |
| `concatenation` | Regex | Flat | **RISK** |
| `digit` | Alt | Keyword | yes |
| `factor` | Regex | Flat | **RISK** |
| `grammar` | Regex | Array | **RISK** |
| `identifier` | Alt | Flat | **RISK** |
| `letter` | Alt | AltDispatch | yes |
| `rule` | Regex | Flat | **RISK** |
| `symbol` | Alt | AltDispatch | yes |
| `term` | Alt | AltDispatch | yes |
| `terminal` | Alt | Keyword | yes |

## BNF

Entry rule: `grammar`

Entry-reachable classified rules with fallback-position body: **4**

| Rule | Outer fallback kind | ShapeTag | Native-handling |
|---|---|---|---|
| `alternation` | Alt | Flat | **RISK** |
| `nonterminal` | Regex | Flat | **RISK** |
| `rule` | Regex | Flat | **RISK** |
| `terminal` | Regex | Flat | **RISK** |

Note: sub-variant type collision between `value_atom::value_atom_3` and `term::term_2` (Tuple([Span, BoxedEnum, Span])). Codegen will resolve by rule-scoped lookup.
## BbnfBootstrap

Entry rule: `grammar`

Entry-reachable classified rules with fallback-position body: **33**

| Rule | Outer fallback kind | ShapeTag | Native-handling |
|---|---|---|---|
| `add_op` | Alt | Keyword | yes |
| `big_comment` | Regex | Flat | **RISK** |
| `binary_operators` | Alt | Keyword | yes |
| `bool_lit` | Alt | Keyword | yes |
| `cmp_op` | Alt | Keyword | yes |
| `comment` | Regex | Flat | **RISK** |
| `debug_directive` | Alt | Flat | **RISK** |
| `directive` | Alt | Keyword | yes |
| `float_lit` | Regex | HRegex | yes |
| `grammar_item` | Alt | Wrap | yes |
| `host_directive` | Alt | Flat | **RISK** |
| `identifier` | Regex | HRegex | yes |
| `import_directive` | Alt | Flat | **RISK** |
| `import_path` | Regex | Flat | **RISK** |
| `int_lit` | Regex | HRegex | yes |
| `literal` | Alt | Keyword | yes |
| `modifier` | Alt | Keyword | yes |
| `mul_op` | Alt | Keyword | yes |
| `pretty_directive` | Alt | Flat | **RISK** |
| `pretty_hint` | Regex | Flat | **RISK** |
| `recover_directive` | Alt | Flat | **RISK** |
| `regex` | Regex | Flat | **RISK** |
| `rhs` | Alt | Wrap | yes |
| `rule` | Alt | Flat | **RISK** |
| `string_lit` | Regex | Flat | **RISK** |
| `term` | Alt | AltDispatch | yes |
| `token_directive` | Alt | Flat | **RISK** |
| `type_name` | Alt | AltDispatch | yes |
| `value_atom` | Alt | AltDispatch | yes |
| `value_expr` | Alt | Wrap | yes |
| `value_ident` | Regex | HRegex | yes |
| `value_unary` | Alt | AltDispatch | yes |
| `ws_directive` | Alt | Flat | **RISK** |

test probe_all_grammars ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.19s

