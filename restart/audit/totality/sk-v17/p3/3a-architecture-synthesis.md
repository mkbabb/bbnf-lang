---
agent: 3A
pass: T-P3-synthesis
cycle: V3
generated_at: 2026-05-29T00:00:00Z
t_p1_inventories_consumed: [1a, 1b, 1c, 1d, 1e, 1f]
t_p2_dossiers_consumed: [2a, 2b, 2c, 2d, 2e, 2f]
v1_surface_targeted: ARCHITECTURE.md
proposed_deltas_count: 8
delta_summary:
  carried_from_prior_cycle: [ARCH-3A-S17-D01, ARCH-3A-S17-D02, ARCH-3A-S17-D03, ARCH-3A-S17-D04, ARCH-3A-S17-D05, ARCH-3A-S17-D06, ARCH-3A-S17-D07, ARCH-3A-S17-D08]
  removed: []
  answered: []
  newly_added: []
prior_cycle_dispositions_folded:
  accepted: [CH1-V2-3A, CH2-V2-3A-D02, CH3-V2-3A-D01-D05-OQ, CH4-V2-3A-D01-D08, CH5-V2-3A-D01-D08, CH6-V2-3A, CH7-V2-3A]
  rejected: []
  revised: []
master_head: 2a76916ac
t_p2_locked_commit: 91b6893b0
---

## Executive Summary

This V3 packet proposes `ARCHITECTURE.md` deltas only. T-P3 does not edit V1
surfaces; Pass Omega CRUD applies them post-G-Omega
(`restart/prompts/totality/PASS-3-SYNTHESIS.md:21`-`24`, `:215`). The subject is the
SK-V17 totality fold of the SKINNY-proven unified-tape / lazy-`ValueRef<G>` /
aarch64-NEON model into the V1 spec — the SK-V18 implementation the spec must
direct (`restart/audit/totality/sk-v17/p2/hardening/HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:26`-`37`).
The eight deltas crystallise the five LOCKED T-P2 LACs (LAC-2F-FOLD-01..05) plus
the three Open Research Questions (2F-FOLD-U1..U3) into architecture text. The
load-bearing posture is conservative against the V1 surface: ARCH §7.3 already
frames the five `BackendShape` shapes as projections of the tape substrate
(`restart/ARCHITECTURE.md:1088`), and Lock 1's FactStream precedent already carves
the substrate-manifest category (`restart/locks/LOCKS.md:100`-`116`); the fold
makes the tape's placement at that manifest explicit and directs the *impl* to
fold to the spec, never the reverse. The five-shape canon holds verbatim; the tape
is a substrate-manifest CATEGORY, not a silent sixth shape (§8.1/§8.2). Every
delta is grammar-neutral (Lock 14), substrate-union-preserving (Lock 1),
5-shape-coherent (Lock 10), and NEON-scalar-ref+checkasm (Lock 16, aarch64-only).
No new directive, no new BIR variant, no new substrate, no public substrate API.

## V3 Delta Summary

This is cycle V3. The eight deltas carry forward unchanged in count and body;
no delta is added, removed, or answered. The V2 CHALLENGE wave returned all-ACCEPT
across CH1–CH7 against 3A (no REVISE, no REJECT raised on any 3A delta); the V3
fold is therefore a convergence ratification — the seven V2 ACCEPT dispositions
are recorded as folded and the per-cycle Delta Summary block is regenerated per
`restart/prompts/totality/PASS-3-SYNTHESIS.md:147`. The two V1 REVISEs (CH4-01
40-file blast radius, CH5-V1-R01 `arena.rs:47` unique-caller cite) folded at V2
remain folded and were re-verified at HEAD 2a76916ac (`grep -rl
'JsonStructBuilder\|CssStructBuilder' crates/`=40; `grep -rn
compound_kind_for_layout crates/` = defn `struct.rs:388` + sole caller
`arena.rs:47`). Zero orphan REVISE against 3A.

| class | disposition | evidence |
|---|---|---|
| Carried from prior cycle | All eight: `ARCH-3A-S17-D01`..`D08` carry from V2 with body + citation base unchanged. The V2 wave ACCEPTed every 3A delta on every lens. | `restart/audit/totality/sk-v17/p3/hardening/V2/CH1.md:113`-`117`; `.../CH2.md:126`; `.../CH3.md:84`; `.../CH4.md:73`-`77`; `.../CH5.md:73`-`76`; `.../CH6.md:128`; `.../CH7.md:59`. |
| Removed | None. T-P3 is proposal-only and may not silently drop a candidate (`restart/prompts/totality/PASS-3-SYNTHESIS.md:228`). | n/a |
| Answered | None. No new answer landed at V3; the V1 cite/figure REVISEs were already answered at V2. | n/a |
| Newly added | None at V3. The cycle folds prior dispositions only; no fresh coupling vector entered the packet (`newly_added: []`). | n/a |
| Revised (folded prior dispositions) | None at V3 — zero REVISE was raised against 3A in the V2 wave. The V2-folded V1 REVISEs (CH4-01, CH5-V1-R01) stand FOLDED and re-verified: D01 Consequences reads "blast radius = 40 files" (Consequences row, "D01"); D05 names the bbnf caller path `arena.rs:47`←`builder.rs:102` with the unique-caller property (D05 Proposed-Delta row). The "22+" string does not appear in the proposed body. | `restart/audit/totality/sk-v17/p3/hardening/V2/CH4.md:37`; `.../CH5.md:41`,`:16`; re-verified `grep -rl ... crates/`=40, `grep -rn compound_kind_for_layout crates/`= `struct.rs:388` defn + `arena.rs:47` caller (HEAD 2a76916ac). |

### V2 disposition fold ledger (all ACCEPT — convergence)

| V2 lens | disposition on 3A | folded as |
|---|---|---|
| CH1 correctness | ACCEPT — every delta cites a real T-P1 divergence + T-P2 LAC; all cited ARCH §7.3/§9.1/§9.2/§13 anchors resolve at path:line (`CH1.md:49`,`:113`-`117`). | ratified; no body change. |
| CH2 generality | ACCEPT — D02 names the grammar-parametric `ValueRef<G>` as the materialization plane; fail action REJECTs the CSS-only generator that never re-emits JSON; scope-honest "not falsely claimed fleet-wide" (`CH2.md:126`,`3a:76`,`3a:93`). | ratified; no body change. |
| CH3 regression | ACCEPT — D01 retires eager `OpenFrame` builders + AoS→SoA single-encoding (dual end-state transient-only); D05 holds the StructRegistry fence as REJECT naming the sole `arena.rs:47` coupling D01 severs; CH3 OQ tags REDRESS-53 correctly; no pre-block re-opened (`CH3.md:84`,`3a:62`,`3a:66`,`3a:107`). | ratified; no body change. |
| CH4 cost | ACCEPT — per-delta CH4 Coverage Matrix carries LOC/propagation/risk/wave/consumer/fail for all 8; 0-LOC deltas priced at 0 with risk on the violation; D01 live rows read "40 files" (`CH4.md:73`-`77`). | ratified; no body change. |
| CH5 hidden coupling | ACCEPT — no parallel substrate / sidecar / renamed-scanner across any 3A delta; `arena.rs:47` unique-caller property re-verified; `newly_added: []` confirms no fresh coupling vector (`CH5.md:73`-`76`,`:64`). | ratified; no body change. |
| CH6 anti-paper-close | ACCEPT — no delta claimed "validated" without evidence chain; D08 converts the 3 ORQs into named pre-gates with receiver+blocker+gate, "not open-ended deferrals" (`CH6.md:128`,`3a:69`,`3a:82`). | ratified; no body change. |
| CH7 overfit-prune | ACCEPT — `udot`/i8mm orphan kernel REFUTED (no CSS antecedent, no live consumer) and confined to the deferred appendix (`CH7.md:59`,`:67`,`3a:111`). | ratified; no body change. |

## Proposed Delta Table

| proposed delta | source T-P1/T-P2 finding cited | affected V1-surface section | rationale | cost / propagation |
|---|---|---|---|---|
| `ARCH-3A-S17-D01` Tape-as-unified-substrate directive: ARCH §7.3 substrate-manifest prose must DIRECT the SK-V18 fold — retire the live eager `OpenFrame` builders (`CssStructBuilder` 817 LOC, `JsonStructBuilder` 231 LOC) and converge AoS `TapeRec` onto the PROVEN SoA `Tape<'input>` as the single post-fold encoding, with the all-8 `OnceCell<StructuralIndex>` `substrate_target` declaration as the pre-gate. Exactly ONE encoding survives; a dual AoS/SoA end-state is admissible ONLY as a transient fold-state. | LAC-2F-FOLD-01 (F1/F3/F7); Divergences A+B. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:580`; `restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md:92`-`94` (SUB17-002/003); `restart/audit/totality/sk-v17/p1/1e-locks-evidence.md:126` (D-1E-SKV17-01); `restart/locks/LOCKS.md:75` (parallel substrates dead); `crates/core/src/runtime/css_l4/builder.rs:16`; `skinny/crates/runtime/src/tape/mod.rs:94`-`100`; `restart/skinny/tranches/sk-v17/SPEC.md:110`-`114`. | §7.3 `LayoutFacts.backend_shape` substrate-manifest prose (`restart/ARCHITECTURE.md:1088`); §9.1 Tape (`:1840`-`1862`). | ARCH §7.3 already states the 5 shapes ARE the tape's projections; the spec describes the *target* but not the *retirement of the live eager builders* as the fold step (`restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:106`-`109`). The delta writes the fold-directive: skinny→core, monotonic. | 200-700 LOC SK-V18 fold; high (eager retirement) + medium (encoding); propagation touches 2 surfaces (ARCH §7.3, §9.1) + the 3C LAC + 3B wave. Wave: SK-V18 fold (3B W1). |
| `ARCH-3A-S17-D02` Lazy `ValueRef<G>` value-plane: ARCH §9.2 must name the one grammar-parametric `ValueRef<'doc,'input,K,G>` projection as the unified materialization plane, re-emitting all 8 per-grammar value surfaces from a single `@generated` accessor generator, replacing the per-grammar EAGER value enums (`CssTypedValue<'p>`). The generator resolves `StructLayout`/`FieldSource` ONCE at codegen; preserve-rich-ast holds (lazy view, not flattened). | LAC-2F-FOLD-03-home (F2); Divergence C. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:158`-`192`; `restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md:95` (SUB17-004); `restart/audit/totality/sk-v17/p1/1e-locks-evidence.md:127` (D-1E-SKV17-02); `skinny/crates/runtime/src/tape/mod.rs:175`; `skinny/crates/runtime/src/grammars/json/value.rs:143`; `crates/core/src/runtime/css_l4/value.rs:414`; `restart/skinny/tranches/sk-v17/SPEC.md:172` (`css_rich_ast_preserved`). | §9.2 Direct-To-Struct Union (`restart/ARCHITECTURE.md:1863`-`1936`); §9.1 `ValueRef` identity (`:1861`). | Core's value layer is eager-per-grammar (`@generated` but EAGER — Lock 14 ALLOWED; hand-authoring would be the VIOLATION); the fold acts on the GENERATOR to emit one lazy projection (`restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md:140`). Scope-honest: exercised JSON+CSS only (Sheets/BBNF-self by-construction under SK-V18). | 300-700 LOC (one accessor generator distinguished from per-grammar regen ×8); high; propagation touches 2 surfaces (ARCH §9.2, §9.1). Wave: SK-V18 W2. |
| `ARCH-3A-S17-D03` Shared NEON classifier manifest narrative: §13 SIMD prose + §7.3 primitive narrative must fold from JSON-first to the alphabet-parametrised shared `select_classifier(alphabet)` / `scan_structural(&StructuralAlphabet)` form, and register the classifier as a Lock-16 primitive-manifest ROW: abstract primitive = alphabet-parametrised byte classification; scalar-ref `byte_class_from_eq_set_64`; checkasm parity; `substrate_target=existing_tape`; `retention_lifetime=transient-single-call`; same-wave consumer = the tape. | LAC-2F-FOLD-03 (F5); Divergence E. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:284`-`327`, `:582`; `restart/audit/totality/sk-v17/p1/1e-locks-evidence.md:131` (D-1E-SKV17-06); `restart/locks/LOCKS.md:453`-`489` (manifest); `restart/locks/LOCKS.md:137`-`149` (no cross-call carry); SK-V17 `SPEC.md:314`-`317` (alphabet-as-data); `skinny/crates/bbnf-simd/src/dispatch.rs:42`; `crates/simd-scan/src/lib.rs:80`; `crates/simd-scan/src/alphabet.rs:19`-`37`. | §13 SIMD vocabulary; §7.3 PrimitiveFacts manifest (`restart/ARCHITECTURE.md:1083`); §11 performance targets. | The classifier ALREADY exceeds the spec (alphabet-as-data, wired across 8-of-9 grammars; impl-exceeds-spec). The narrative still frames it JSON-first. The fold is a 0-LOC narrative fold + a Lock-16 manifest row; eq-set fan is the one real NEON body, table/prefix are honest scalar-delegate-non-ASM. | 0-LOC narrative + manifest row; 100-400 LOC `crates/simd-scan` scope reconcile (no x86 close path); medium; propagation touches 3 surfaces (§13, §7.3, §11). Wave: SK-V18 W3. |
| `ARCH-3A-S17-D04` BackendShape-category disposition: ARCH §7.3 + §9 must record the tape as the substrate-manifest CATEGORY the 5 shapes project from (`substrate_target=existing_tape`), per the LAC-1E-14 FactStream precedent — NOT a 6th `BackendShape` variant. The 5-shape Lock-10 domain `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` holds verbatim; a 6th variant remains G-Omega gated. | LAC-2F-FOLD-02 (F4, primary D-fold); Divergence D. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:233`-`281`, `:581`; `restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md:96` (SUB17-005); `restart/audit/totality/sk-v17/p1/1e-locks-evidence.md:129` (D-1E-SKV17-04); `restart/locks/LOCKS.md:100`-`116` (LAC-1E-14), `:107`-`108` (5-shape domain); `restart/ARCHITECTURE.md:1088` (5 shapes ARE projections), `:1151`/`:1282` (`admits_collapsed_stage` x86-bound); SK-V17 `SPEC.md:808`. | §7.3 `BackendShape` enum (`restart/ARCHITECTURE.md:1090`-`1116`); §9 Output-Plane Taxonomy (`:1790`-`1804`). | The dispatch's "propose, do NOT silently add a 6th" is discharged in the negative on TWO independent grounds: (i) the categorical precedent LAC-1E-14; (ii) the mechanical arch-refusal — `admits_collapsed_stage` binds the only arch-gated shape to x86+avx512bw, mechanically refusing on the aarch64 M5 Max target, leaving no mechanism for a 6th aarch64 shape. | 0 LOC (canon + precedent); medium prose; propagation touches 2 surfaces (§7.3, §9) + the 3C LAC. Wave: T-P3 §3C / Pass Omega CRUD. |
| `ARCH-3A-S17-D05` StructRegistry/FieldSource fence: ARCH §7.3 substrate-manifest must add the no-per-leaf-registry-lookup fence — the `FieldSource` projection walk inside the live `StructRegistry` is compile-time emission resolved once at codegen; ANY per-leaf runtime `StructRegistry::layout(rule)` indirection in the tape/projection hot path re-opens the 28-65× / 983× / 10583× regression and is REJECT. The SOLE live-runtime `StructRegistry`-method coupling is `crates/core/src/runtime/bbnf/arena.rs:47` (`StructRegistry::compound_kind_for_layout`), reached only via the eager bbnf builder `crates/core/src/runtime/bbnf/builder.rs:102` (`BbnfCompoundKind::from_layout(layout)` inside `begin_compound`'s `OpenFrame` push); it is severed by the eager-OpenFrame retirement (D01). The css_l4 + json builders carry ZERO `StructRegistry` coupling (`grep -rln StructRegistry crates/core/src/runtime/{css_l4,json}/` = empty); the other 6 grammars resolve compound kind from a local `match layout.rule_id` (e.g. `crates/core/src/runtime/bnf/kind.rs:20`), not a registry method. | LAC-2F-FOLD-04 (F6, regression firewall); Divergence F. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:331`-`381`, `:583`; `restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md:120` (SUB17-009 fence); `restart/audit/totality/sk-v17/p1/1e-locks-evidence.md:128` (D-1E-SKV17-03), `:140` (CH3 ledger); `restart/skinny/tranches/sk-v17/SPEC.md:793`-`795`; `crates/ir/src/registry/struct.rs:84`,`:313`,`:331`,`:388` (`compound_kind_for_layout` defn); `crates/core/src/runtime/tape/mod.rs:185`-`186`; `crates/core/src/runtime/bbnf/arena.rs:47`; `crates/core/src/runtime/bbnf/builder.rs:102`; `crates/core/src/runtime/bnf/kind.rs:20`. | §7.3 cost-model derivation pipeline + Lock 1 v+1 manifest binding (`restart/ARCHITECTURE.md:1128`-`1133`). | `begin_compound` reads `layout.rule_id & 0x1F` only (grep-zero `StructRegistry`) — fence-clean. The present-tense coupling is `arena.rs:47` `StructRegistry::compound_kind_for_layout`, the UNIQUE live `StructRegistry`-method call in `crates/` runtime (`grep -rn compound_kind_for_layout crates/` = defn at `struct.rs:388` + this one caller), inside the eager arena path; D01's eager retirement severs it precisely there. Keeps the AZ-IV-IV indirection pre-blocked. | 0 LOC (fence); high/regression-class if violated; propagation touches 1 surface (§7.3) + the 3C LAC. Wave: SK-V18 fold gate. |
| `ARCH-3A-S17-D06` Lock-2 `StructLayout` reconcile note: ARCH §7.3/§7.4 must carry the directed Lock-2 `StructLayout`→`Layout` reconcile by two disjoint priced paths — (a) full rename across 960 sites, generator-side, regenerating 8 parsers + ~16 tests; (b) lock re-scope to a `LayoutFacts.backend_shape` side-table, sized as the 0→N introduce-site delta (`grep StructLayout crates/`=960 vs `grep 'backend_shape\|LayoutFacts' crates/`=0). | LAC-2F-FOLD-05 (F9, Lock-2 sub-surface); Divergence F sub-surface. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:460`-`503`, `:584`; `restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md:97` (SUB17-006); `restart/audit/totality/sk-v17/p1/1e-locks-evidence.md:130` (D-1E-SKV17-05), `:181` (LAC-04 re-price); `restart/locks/LOCKS.md:160`,`:162`-`166`; `crates/ir/src/registry/struct.rs:202`. | §7.4 SK-V5–SK-V15 status (`restart/ARCHITECTURE.md:1286`); §7.3 `LayoutFacts` table (`:1075`). | `StructLayout` is Lock-2-RETIRED yet LIVE at 960 sites; `LayoutFacts`/`backend_shape` are skinny/prior-totality-only (grep-zero in crates/), so path-(b)'s core realisation is non-zero, not ~0 LOC. The v+1 note bars Lock-2 closure by `LayoutFacts` alone while public `Layout`/`LayoutSink` are absent. | path-(a) 960-site/medium; path-(b) text-only low + core materialisation sizable; propagation touches 2 surfaces (§7.3, §7.4) + the 3C Lock-2 LAC. Wave: T-P3 §3C + SK-V18 regen. |
| `ARCH-3A-S17-D07` BackendShape selector wiring narrative: ARCH §7.3 cost-model pipeline prose must record that the `BackendShape` enum + `derive_backend_shape` selector (skinny-only today; grep-zero in `crates/`) WIRES into core atop the `EmitStrategy::StructDirect` lineage, consuming the already-present `crates/egraph` + `crates/csp-solver` decision engine; the fold WIRES, does not build. `backend_shape` is a side-table field, not a surface annotation; no grammar author annotates the shape. | F8 (BackendShape selector wiring); Divergence D. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:424`-`457`; `restart/audit/totality/sk-v17/p1/1b-codegen-evidence.md` (BSHAPE17-002/004); `restart/ARCHITECTURE.md:1118`-`1176` (pipeline); `restart/locks/LOCKS.md:269`-`274` (5-shape domain + cost-evidence). | §7.3 cost-model derivation pipeline (`restart/ARCHITECTURE.md:1118`-`1176`); Live BackendShape Admission Ledger (`:1188`-`1206`). | The selector + 5-shape enum live ONLY in skinny; core carries single-variant `StructDirect`. The fold wires the published SOTA pipeline class (equality-saturation → bounded saturation → CSP feasibility → cost extraction) into core. No P1-P8 fixed cascade. | 60-200 LOC selector + 600-1400 LOC joint decision-engine wiring envelope; medium; propagation touches 2 surfaces (§7.3 pipeline, ledger). Wave: SK-V18 (3B). |
| `ARCH-3A-S17-D08` Three-ORQ disposition note: ARCH §9.2 carrier note must record the three T-P2 ORQs as SK-V18 pre-gates — 2F-FOLD-U1 (SoA `Tape` is the declared convergence-target encoding; parity-vs-adopt is the T-P3 call, 2F recommends SoA the proven-and-benched encoding); 2F-FOLD-U2 (each of the 8 `OnceCell<StructuralIndex>` carriers classified `existing_tape` vs `local_temp_only` BEFORE wiring, else REDRESS-53 re-entry); 2F-FOLD-U3 (aarch64 CollapsedStage = UNKNOWN-2D-05, no admission without a 2E source-backed strategy; no x86 close path, no D6 second substrate). | ORQs 2F-FOLD-U1/U2/U3; U-SUB17-001/002; 1E-SKV17-U1/U2. Evidence: `restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:561`-`565`; `restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md:148`-`149` (U-SUB17-001/002); `restart/audit/totality/sk-v17/p1/1e-locks-evidence.md:189`-`190`; `restart/skinny/tranches/sk-v17/SPEC.md:577`/`:825`/`:839` (REDRESS-53); `restart/ARCHITECTURE.md:1282` (UNKNOWN-2D-05). | §9.2 cursor-shape carrier note (`restart/ARCHITECTURE.md:1877`-`1888`); §7.3 Admission Ledger CollapsedStage row (`:1206`). | The three ORQs are SK-V18 pre-gates, not open-ended deferrals. Each names a concrete receiver + blocker + receiving gate. U2's mis-declaration re-opens REDRESS-53; U3's premature admission re-opens the x86 close path. | 0 LOC (disposition note); medium; propagation touches 2 surfaces (§9.2, §7.3 ledger). Wave: SK-V18 pre-gate (3B). |

## Consequences

| delta | positive consequence | cost / risk | propagation |
|---|---|---|---|
| `ARCH-3A-S17-D01` | Directs the impl to fold to the spec (skinny→core, monotonic); eliminates the `Vec<OpenFrame>::clone` 86.07% samply parallel-substrate pathology Lock 1 forbids (`restart/locks/LOCKS.md:75`). | 200-700 LOC; high (eager retirement blast radius = 40 files via `grep -rl 'JsonStructBuilder\|CssStructBuilder' crates/`; the LOC band 200-700 is the fold-edit envelope, not the touched-file count). | 2 ARCH surfaces (§7.3, §9.1) + 3C LAC-2F-FOLD-01 + 3B W1. |
| `ARCH-3A-S17-D02` | One lazy materialization plane replaces per-grammar eager enums; preserve-rich-ast intact; the value-fold is JSON+CSS-exercised, not falsely claimed fleet-wide (`restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:316`-`318`). | 300-700 LOC; high (generator + per-grammar regen ×8). | 2 ARCH surfaces (§9.2, §9.1). |
| `ARCH-3A-S17-D03` | Folds the classifier narrative to the proven grammar-general form; one Lock-16 manifest row makes the primitive admission process-based; no x86 close path admitted. | 0-LOC narrative + manifest row; 100-400 LOC scope reconcile; medium. | 3 ARCH surfaces (§13, §7.3, §11) + 3C LAC-2F-FOLD-03. |
| `ARCH-3A-S17-D04` | Preserves the five-shape canon; places the tape at the substrate manifest CATEGORY, not a silent 6th shape; no JSON-narrowing. | 0 LOC (canon + precedent); medium prose. | 2 ARCH surfaces (§7.3, §9) + 3C LAC-2F-FOLD-02; coherent with 3B §13 + 3E per-grammar matrix (§8.2). |
| `ARCH-3A-S17-D05` | Keeps the AZ-IV-IV StructRegistry indirection pre-blocked; names the live `arena.rs:47` coupling D01 severs; the fence is concrete, not abstract. | 0 LOC fence; CRITICAL if violated (worst measured regression). | 1 ARCH surface (§7.3) + 3C LAC-2F-FOLD-04. |
| `ARCH-3A-S17-D06` | Prices the Lock-2 reconcile honestly by two disjoint paths; bars Lock-2 closure by `LayoutFacts` alone while public names are absent. | path-(a) medium (960 sites); path-(b) sizable core materialisation. | 2 ARCH surfaces (§7.3, §7.4) + 3C Lock-2 LAC. |
| `ARCH-3A-S17-D07` | Records the selector as a WIRE of the existing decision engine, not a fresh build; keeps `backend_shape` a side-table field, grammar-author-blind. | 60-200 LOC selector + 600-1400 LOC joint wiring; medium. | 2 ARCH surfaces (§7.3 pipeline, ledger). |
| `ARCH-3A-S17-D08` | Converts the three ORQs into named SK-V18 pre-gates with receiver+blocker+gate; no engineered-defer (§8.5/CH6). | 0 LOC; medium. | 2 ARCH surfaces (§9.2, §7.3 ledger). |

## CH4 Coverage Matrix

For all rows, hard-cap fit excludes any silent 6th-shape route, any parallel
substrate, and any per-leaf registry walk; non-fit work follows the row's fail
action.

| delta | LOC | propagation count | risk | wave alignment | consumer/gate | hard-cap fit | fail action |
|---|---:|---:|---|---|---|---|---|
| `ARCH-3A-S17-D01` | 200-700 | 2 + LAC + wave | High | SK-V18 W1 | F1/F3 tape-wiring same-wave; Lock 1 one-encoding gate | Fits as eager-retirement + SoA-convergence with transient dual fold-state. | Dual AoS/SoA end-state is a Lock-1 violation; intrinsic-block or G-Omega Lock-1 amendment. |
| `ARCH-3A-S17-D02` | 300-700 | 2 | High | SK-V18 W2 | accessor generator; JSON `value_from_ref` byte-equal re-emit gate | Fits if one generator re-emits JSON byte-equal AND CSS lazy projection. | CSS-only generator that never re-emits JSON FAILS (CH2 generic-named-CSS-generator mode); REDRESS/revert. |
| `ARCH-3A-S17-D03` | 0 narrative + 100-400 reconcile | 3 + LAC | Medium | SK-V18 W3 | Lock-16 manifest row; checkasm parity; tape same-wave consumer | Fits as narrative fold + manifest row + aarch64-only reconcile. | Any x86 close path is REJECT; scope reconcile routes to intrinsic-block. |
| `ARCH-3A-S17-D04` | 0 | 2 + LAC | Medium | T-P3 §3C | Pass Omega CRUD; Lock 1 substrate-manifest + Lock 10 5-shape review | Fits as category prose; no 6th shape. | A 6th variant is G-Omega gated; G-Omega blocks CRUD until canon coherent. |
| `ARCH-3A-S17-D05` | 0 fence | 1 + LAC | High (regression) | SK-V18 fold gate | substrate-manifest fence; `begin_compound` registry-free property | Fits as fence (0 LOC). | Per-leaf `StructRegistry::layout(rule)` walk re-opens 28-65×/983×/10583×; REJECT. |
| `ARCH-3A-S17-D06` | path-(a) 960-site; path-(b) sizable | 2 + LAC | Medium | T-P3 §3C + SK-V18 regen | 3C Lock-2 disposition; regen byte-equal | Fits per chosen path. | Claiming Lock-2 closure by `LayoutFacts` alone is barred; revert. |
| `ARCH-3A-S17-D07` | 60-200 + 600-1400 | 2 | Medium | SK-V18 | 5-shape domain + cost-evidence gate; substrate_target binding | Fits as selector + decision-engine wiring envelope. | Non-fit decision-engine build is out of scope; intrinsic-block or G-Omega wave amendment. |
| `ARCH-3A-S17-D08` | 0 | 2 | Medium | SK-V18 pre-gate | U1/U2/U3 receiving gates (Lock 1 manifest, REDRESS-53 scan, `admits_collapsed_stage`) | Fits as disposition note. | Mis-declared `substrate_target` re-opens REDRESS-53; premature aarch64 CollapsedStage re-opens x86 path; REJECT. |

## Open Questions

| CH lens | question | receiver | blocker | gate |
|---|---|---|---|---|
| CH1 correctness | Should ARCH §7.3 retain the existing `LayoutFacts.backend_shape` substrate-manifest paragraph (`restart/ARCHITECTURE.md:1088`) verbatim and append the fold-directive, or rewrite it to lead with the SK-V18 fold step? | Pass Omega ARCH CRUD owner; 3C LOCKS-crystallisation owner. | The §7.3 paragraph already frames the 5 shapes as tape projections — a rewrite could over-touch the canon; an append could read as bolt-on (`feedback_doc_style`). | G-Omega ARCH CRUD review; 3C consistency check against LAC-2F-FOLD-01/02. |
| CH2 generality | Which non-CSS receiver does ARCH §9.2 name first for the `ValueRef<G>` value-plane generality proof beyond the JSON+CSS exercise — Sheets or BBNF-self? | 3E grammar-generalisation owner; SK-V18 W2 generator owner. | 2F scopes the value-fold JSON+CSS only; Sheets/BBNF-self are by-construction under SK-V18 (`sheets_witness` 24-LOC stub cannot serve as exercise, SK-V17 `SPEC.md:113`-`114`). | Lock 14 single-generator scan requiring at least one non-CSS, non-JSON receiver row (3E deliverable). |
| CH3 regression | Can the eager-OpenFrame retirement (D01) fully precede tape-wiring without re-opening REDRESS-53 via a retained `OnceCell<StructuralIndex>` running parallel to the now-wired tape? | SK-V18 W1 tape-wiring owner; Lock 1 substrate-manifest owner. | F7's all-8 `substrate_target` classification (U2) must resolve to `existing_tape` BEFORE F1/F3 wire the tape (`restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:406`-`414`). | Lock 1 substrate manifest + REDRESS-53 parallel-index scan (`restart/skinny/tranches/sk-v17/SPEC.md:577`/`:825`/`:839`). |
| CH4 cost | Is the D07 decision-engine wiring envelope (600-1400 LOC) bounded if the 4 skinny lowerers (17-LOC scaffolds) require real per-shape lowering rather than wiring the existing engine? | 3B master wave-map owner; SK-V18 lowerer owners. | 2F frames F8 as a WIRE of the already-present `crates/egraph`+`crates/csp-solver`; if the lowerers need real bodies the envelope grows (`restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:442`). | 3B cost matrix + 5-shape lowerer gate; non-fit is intrinsic-blocked or G-Omega wave amendment, no overflow wave. |
| CH5 hidden coupling | Is the retained `PayloadArena` member of the SoA `Tape` a substrate-manifest-declared member (`substrate_target=existing_tape`) or an implicit-retention sidecar hole under Lock 1? | SK-V18 W1 tape owner; Lock 1 substrate-manifest owner. | 2F declares `PayloadArena` `substrate_target=existing_tape` / `retention_lifetime=output_row` / `policy_owner=generated_grammar` (`restart/audit/totality/sk-v17/p2/2f-fold-gaps.md:516`-`524`); implicit retention would be the parallel-substrate hole. | Lock 1 v+1 manifest declaration scan; `PayloadArena.write_count==0` invariant on re-readable leaves. |
| CH6 anti-paper-close | Does ARCH §13's NEON narrative fold (D03) state the eq-set fan is the ONE real NEON body and table/prefix are honest scalar-delegate-non-ASM, not claim full NEON coverage? | SK-V18 W3 NEON owner; Pass Omega §13 CRUD owner. | 2F declares `byte_class_from_table_64` and `bitmap_prefix_xor_64` as verified scalar passthroughs (line-3 delegates) — claiming them as NEON bodies is paper-close (`restart/audit/totality/sk-v17/p2/2f-fold-gaps.md` F5 body). | Lock 16 manifest close-state vocabulary requiring `scalar-delegate-non-ASM` disposition where true. |
| CH7 overfit-prune | Should the `udot`/i8mm digit-MAC orphan kernel (FOLD-L9 Alt-mode) be mentioned in ARCH §13 at all, or stay in the deferred appendix only? | Pass Omega §13 CRUD owner; CH7 OVERFIT-PRUNE lens. | 2F REFUTES the orphan kernel (no CSS antecedent, no live consumer; `restart/audit/totality/sk-v17/p2/hardening/HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:250`-`252`). | CH7 overfit scan; the kernel stays in the deferred appendix, not the wired set. |
