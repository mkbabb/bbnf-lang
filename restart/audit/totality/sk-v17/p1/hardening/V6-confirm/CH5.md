---
lens: CH5 HIDDEN-COUPLING
pass: T-P1-excavation
cycle: V6-confirm
subject: SK-V17 T-P1 excavation artefacts (restart/audit/totality/sk-v17/p1/*.md)
generated_at: 2026-05-29T00:00:00Z
firewall: Lock 1 substrate-union + Lock 10 5-shape BackendShape + Track-1≡Track-2 honesty
cycle_kind: CONFIRMING (2nd-consecutive ≥95% obtain per ORCHESTRATOR §3Z)
artefacts_present: [1a-substrate-evidence.md, 1b-codegen-evidence.md, 1c-runtime-evidence.md, 1d-skinny-lessons.md, 1e-locks-evidence.md, 1f-coherence-scan.md, 1f-anti-pattern.md, 1f-past-corpora.md]
artefacts_absent: []
prior_cycle: V5 CH5 (ACCEPT 8 / REVISE 0 / REJECT 0; 100% — the V4 sole REVISE CH5-S8 [1E REDRESS-53 re-entry anchor] was folded + live-verified at 1e:190 → SPEC :577/:825/:839)
live_truth_method: "nl/sed + grep over restart/{ARCHITECTURE.md,locks/LOCKS.md,skinny/tranches/sk-v17/SPEC.md} and the eight p1 artefacts; Read/sed over crates/core/src/runtime/tape/{mod,record}.rs (mod.rs:54-58/185, record.rs:103/120/121), crates/core/src/runtime/{json,css_l4}/value.rs:1 (@generated headers), crates/ir/src/registry/struct.rs (:84/:314/:331), crates/simd-scan/src/lib.rs:80, crates/core/src/grammar/generated/math.rs:281/285 (doc-comment); grep -rn TapeStructBuilder crates/core/src | grep -v runtime/tape/ = ZERO (re-verified); grep -c scan_structural over crates/core/src/grammar/generated/*.rs (bbnf/bnf/css_l4/css_pretty/csv/ebnf/google_sheets/json = 1 each; math=0, mod=0 — 8-carrier census re-confirmed); grep -n REDRESS-53 SPEC = :577/:657/:825/:839; LOCKS :75/:107-109/:119-126/:137-149 re-read; ARCH :1088/:1206 re-read; SPEC :806-811/:839 re-read; no cargo/build mutation. Master HEAD 445925167154de73540e3ea3283d0170371de790 (re-confirmed via git rev-parse)."
counts:
  accept: 8
  revise: 0
  reject: 0
---

## Lens Charge

CH5 HIDDEN-COUPLING is the Lock-1 substrate-union firewall over the SK-V17 T-P1
excavation. This is the **CONFIRMING** cycle (V6): V5 landed at 100% ACCEPT with
the last V4 REVISE (CH5-S8) folded; this cycle re-verifies every section against
live file:line at master HEAD `445925167` and ACCEPTs where the V5-folded artefact
is correct + complete, flagging only genuine residual defects. The charge scans
for: (a) catalogued state implying a **parallel substrate**, **sidecar producer**,
or **renamed-scanner / cross-call classifier-state** violation; (b) whether the
**5-shape BackendShape canon** (Lock 10) is excavated whole and not mis-stated as
6th-shape pressure; (c) **Track-1 ≡ Track-2 dishonesty**; (d) whether the
tape-as-unified-substrate question is framed without implying a parallel or second
substrate beside the live one.

## Confirming-Cycle Method

I re-ran the full live-truth battery this cycle. Every anchor the V5 CH5 cited
resolves at master HEAD `445925167` (re-confirmed via `git rev-parse`):

- `grep -rn TapeStructBuilder crates/core/src | grep -v runtime/tape/` → **ZERO**.
  ONE tape construct, unwired.
- `grep -c scan_structural` over the generated grammars → `1` each for
  `bbnf/bnf/css_l4/css_pretty/csv/ebnf/google_sheets/json`; `0` for `math.rs` and
  `mod.rs`. **8-carrier census re-confirmed**, math.rs non-carrier.
- `grep -n REDRESS-53 SPEC.md` → `:577` (parallel-vector naming), `:657`, `:825`
  (W2 row), `:839` (shortlist condition 1). The four canonical naming sites hold.
- `LOCKS.md:107-108` → `{EagerTape, OffsetTape, EventTape, SinkOnly,
  CollapsedStage}` verbatim; `:108-109` "Adding a 6th `BackendShape` variant
  remains G-Omega gated".
- `ARCHITECTURE.md:1206` → CollapsedStage `target.arch == x86 + target.avx512bw`,
  "aarch64 mechanically refused", "NOT-ADMITTED: x86-only; aarch64 candidate is
  UNKNOWN-2D-05".
- `SPEC.md:807-811` → second-substrate block names skinny
  `StructLayout`/`TapeStructBuilder`/`TapeCursor`, public `UnionTape`, "sixth
  `BackendShape`", "parallel source passes", "cross-call classifier-state
  retention" as FORBIDDEN.
- Impl: `tape/mod.rs:185` `begin_compound(&mut self, layout: &StructLayout)`;
  `tape/mod.rs:54-56` "dispatches on the `StructLayout` … never on per-grammar
  route strings"; `struct.rs:84` `pub enum FieldSource`, `:314`
  `BTreeMap<RuleId, StructLayout>`, `:331` `layout(rule_id) -> Option<&StructLayout>`;
  `simd-scan/lib.rs:80` `scan_structural(input: &[u8], alphabet:
  &StructuralAlphabet) -> StructuralIndex`; `record.rs:103/120/121` `TapeRec`,
  `size_of==16`, `align_of==4`; `math.rs:281/285` `OnceCell<StructuralIndex>` in
  doc-comment text only; `value.rs:1` `@generated by xtask regen-{json,css}`
  headers.

All anchors live. No artefact text drift. The V5 fold of CH5-S8 (1e:190 →
:577/:825/:839) holds with the `:578` adjacent sidecar-vector clause absent from
every 1E surface.

## Section Dispositions

### CH5-S1 — Parallel-substrate firewall: ONE unwired tape in crates/core
**Subject:** `1a-substrate-evidence.md:117` (Firewall row 1) + SUB17-001/002/003 (`:92-94`); `1f-anti-pattern.md:69`.
**Disposition: ACCEPT.**
**Evidence:** crates/core holds exactly ONE tape construct (`TapeStructBuilder`,
`tape/mod.rs:58`) and it is UNWIRED — `grep -rn TapeStructBuilder crates/core/src |
grep -v runtime/tape/` returns ZERO (re-verified this cycle). The live substrate is
the eager `JsonStructBuilder`/`CssStructBuilder` `OpenFrame` machine
(`json/builder.rs:9`, `css_l4/builder.rs:16`). The 16-byte AoS `TapeRec` is
const-asserted (`record.rs:120,121`). 1A's verdict (`:117`) — "NO same-tree parallel
substrate; the unwired tape is the SK-V18 fold target sitting dormant" — is the
correct Lock-1 reading: a dead construct is not a second substrate. No
tape-as-second-plane framing anywhere; `1f-anti-pattern.md:48-53` states it
explicitly ("the tape is not a sidecar producer … it is dead code awaiting the
SK-V18 fold"). Correct + complete.

### CH5-S2 — Sidecar producer / retained structural projection (OnceCell across all 8 carriers)
**Subject:** `1a-substrate-evidence.md:118` (Firewall row 2) + U-SUB17-002 (`:149`); `1f-anti-pattern.md:70`.
**Disposition: ACCEPT.**
**Evidence:** `scan_structural` is wired in ALL 8 generated grammars (grep -c = 1
each; 0 for `math.rs`/`mod.rs` — re-verified). The 8-carrier (NOT 9) census holds:
`math.rs:281,285` carry `OnceCell<StructuralIndex>` only in doc-comment text
("`OnceCell<StructuralIndex>` consumed by CTNS-style …", confirmed), not a field —
exactly as `1f-anti-pattern.md:70` states. The Lock-1 prose "if structural offsets
are retained, the structural projection IS the tape (no second sidecar)" is verbatim
at `ARCHITECTURE.md:1088`. 1A's distinction — the retained index feeds the eager
`OpenFrame` builders, so it is a **retained scan cache, not the tape projection** —
is the precise Lock-1 reading; the firewall refuses to pre-classify it, pinning it to
the four `substrate_target` manifest values (`LOCKS.md:119-126`) and REDRESS-53 at
the `:825`/`:839` naming anchors. No parallel-substrate assertion. The strongest
hidden-coupling catch in the set, held correct.

### CH5-S3 — Renamed-scanner / cross-call classifier-state (Lock 1 v+1)
**Subject:** `1a-substrate-evidence.md:119` (Firewall row 3); `1f-anti-pattern.md:74`.
**Disposition: ACCEPT.**
**Evidence:** The signature `pub fn scan_structural(input: &[u8], alphabet:
&StructuralAlphabet) -> StructuralIndex` is at `simd-scan/lib.rs:80` (re-verified —
by-reference inputs, fresh index returned, no `&mut self` carry). The Lock 1 v+1
ELEVATION ("no cross-call retained classifier state. Period. … carry MUST stay
within a single chunk-call boundary"; `retention_lifetime ∈ {transient-single-call,
retained-within-chunk, retained-across-call-boundary}`, third = REJECT) resolves
verbatim at `LOCKS.md:137-149`. 1A and `1f-anti-pattern.md:74` both correctly hold
the REJECT class as NOT tripped and give the right verify_action (grep
`prev_state|carry|prefix_xor|retained` over `crates/simd-scan/src/`). The
`OnceCell<StructuralIndex>` retains the OUTPUT index per-parse, correctly
distinguished from classifier carry state. Held open, not falsely cleared.

### CH5-S4 — 5-shape BackendShape canon framing (Lock 10) + aarch64-NEON absorption
**Subject:** `1a-substrate-evidence.md:96` (SUB17-005), `:111` (BackendShape map row), `:129`, `:141`; `1e-locks-evidence.md:182` (LAC-1E-SKV17-05).
**Disposition: ACCEPT.**
**Evidence:** Lock 10's 5-shape search domain `{EagerTape, OffsetTape, EventTape,
SinkOnly, CollapsedStage}` resolves verbatim at `LOCKS.md:107-108`, with "Adding a
6th `BackendShape` variant remains G-Omega gated" at `:108-109`. The
`CollapsedStage` admission gate — `target.arch == x86 + target.avx512bw`, "aarch64
mechanically refused", "NOT-ADMITTED: x86-only; aarch64 candidate is UNKNOWN-2D-05"
— resolves verbatim at `ARCHITECTURE.md:1206`. ARCH:1088 carries the canonical
5-shape description ("`CollapsedStage` fuses mask-state and emission for
AVX-512-class hardware"). 1A's framing (`:96`) — "the 5-shape canon holds at Lock 10;
CollapsedStage admission is x86-pinned, mechanically refused on aarch64, and the
aarch64 candidate is the spec-named open unknown UNKNOWN-2D-05 — NOT a fresh
undiscovered gap; SK-V17's NEON union sits under the four LLVM shapes' scan-leaf
FFI" — is the exactly-correct Lock-10 framing, mirrored in 1E (LAC-1E-SKV17-05,
`:182` — "the 5-shape canon holds and absorbs aarch64-NEON without a 6th shape; no
LOCKS edit, no x86 close path"). The tape-as-unified-substrate question is posed as
**absorption into the existing five shapes**, never as a 6th shape or a parallel
plane. The lens's core BackendShape charge holds at every cited line; 1A ↔ 1E agree.

### CH5-S5 — Lock-1 closure obligation: exactly-one-encoding post-fold
**Subject:** `1a-substrate-evidence.md:93` (SUB17-002 Note), `:105` (Cross-Tree map row 1), `:148` (U-SUB17-001); `1e-locks-evidence.md:178` (LAC-1E-SKV17-01).
**Disposition: ACCEPT.**
**Evidence:** The exactly-one-encoding-post-fold invariant is intact: SUB17-002's
Note (`:93`) reads "**Lock-1 closure obligation:** post-SK-V18 exactly ONE encoding
survives across both trees (`LOCKS.md:75` 'parallel substrates are dead'); AoS/SoA
coexistence is admissible ONLY as a transient fold-state." The Cross-Tree map row 1
(`:105`) and U-SUB17-001 (`:148`) carry the same. Lock 1's "columnar SoA is dead …
parallel substrates are dead" is verbatim at `LOCKS.md:75`; the core mod-doc's "kept
AoS first … the same `TapeCursor` API rides a later SoA split" is at `tape/mod.rs:6-9`.
1E's LAC-1E-SKV17-01 (`:178`) carries the same invariant as an explicit-divergence
candidate ("a dual AoS/SoA end-state is NOT a permissible Lock-1 end-state"). The
latent two-substrate end-state is named as an explicit invariant in three places in
1A and re-stated in 1E; no license for a post-fold dual substrate. Correct + complete.

### CH5-S6 — Second-substrate / monotonic-direction / forbidden-name collision firewall
**Subject:** `1a-substrate-evidence.md:96` (SUB17-005), `:94` (SUB17-003 direction guard); SPEC `:806-811`, `:814-815`, `:110-114`.
**Disposition: ACCEPT.**
**Evidence:** The SPEC §9 second-substrate block names skinny
`StructLayout`/`TapeStructBuilder`/`TapeCursor`, public `UnionTape`, "sixth
`BackendShape`", "parallel source passes", "cross-call classifier-state retention"
as FORBIDDEN — resolves verbatim at SPEC `:807-811` (re-verified). These are
precisely the crates/core fold-target names. 1A correctly catalogues the direction
guard: SK-V17 frames the core tape as the SK-V18 **adoption target** (SPEC
`:110-114`), monotonic skinny→totality — the proven skinny `Tape`/`ValueRef` shape
is adopted INTO crates/core, never the forbidden-in-skinny names relocated back into
skinny as a second substrate. SUB17-003 (`:94`) notes the gate `tape_activated` is
"NOT a crates/core grep". The forbidden-name collision (the §9-forbidden-in-skinny
names ARE the crates/core fold-target names) is the single most important
direction-reversal hidden coupling, and 1A draws it correctly — adoption INTO core,
never relocation INTO skinny. No Track-1 ≡ Track-2 dishonesty.

### CH5-S7 — StructRegistry/FieldSource per-leaf hot-path indirection (do-not-redrive fence)
**Subject:** `1a-substrate-evidence.md:120` (Firewall row 4); `1e-locks-evidence.md:179` (LAC-1E-SKV17-02); `1f-anti-pattern.md` StructRegistry row.
**Disposition: ACCEPT.**
**Evidence:** `begin_compound(&mut self, layout: &StructLayout) -> CompoundHandle`
is at `tape/mod.rs:185` (re-verified, pre-resolved `&StructLayout` by reference).
`StructLayout` originates from `StructRegistry`, a `BTreeMap<RuleId, StructLayout>`
(`struct.rs:314`) queried via `layout(rule_id) -> Option<&StructLayout>`
(`struct.rs:331`); `FieldSource` is the same registry's `pub enum FieldSource`
(`struct.rs:84`). 1A's fence (`:120`) — "a naive per-leaf `StructRegistry` BTreeMap
lookup on the hot path re-opens the 28-65×/983×/10583× regression; the
`FieldSource`/`StructLayout` walk MUST be compile-time projection-emission … NOT a
per-leaf runtime registry lookup" — is the correct do-not-redrive coupling, priced
as a 0-LOC fence in 1E's LAC-1E-SKV17-02 (`:179`). The tape "dispatches on the
`StructLayout` … never on per-grammar route strings" (`tape/mod.rs:54-56`,
re-verified) — Lock-14-clean. A genuine hidden coupling the firewall surfaces at
file:line with the correct compile-time-resolution invariant.

### CH5-S8 — V4 REVISE closure: 1E REDRESS-53 re-entry anchor (SPEC :578 → :577/:825/:839)
**Subject:** `1e-locks-evidence.md:190` (Open Question 1E-SKV17-U2), `:171` (LAC closure prose), `:178-182` (LAC table).
**Disposition: ACCEPT.**
**Evidence:** The V4 CH5-S8 REVISE remains closed in V6. The REDRESS-53 re-entry
citation in 1E Open Question 1E-SKV17-U2 (`:190`) reads "… classify against the four
`substrate_target` values before wiring the tape, **else REDRESS-53 re-entry
(`restart/skinny/tranches/sk-v17/SPEC.md:577` [parallel-vector naming]; W2 row
`:825`; shortlist condition 1 `:839`)**" — verbatim the V4 concrete fix. The `:578`
adjacent sidecar-vector clause is absent from every 1E surface (re-verified). The
LAC closure prose (`:171`) records "CH5-S8 re-anchors the REDRESS-53 re-entry
citation from the adjacent sidecar-vector clause to the naming lines
`:577`/`:825`/`:839`." Independent re-verification: `grep -n REDRESS-53 SPEC.md` =
`:577`, `:657`, `:825`, `:839`; `:577` names REDRESS-53 against the parallel-vector
shape ("the L1/L4 index retained as a parallel vector (REDRESS-53)"); `:578` does NOT
carry the token. 1E is synced with sibling 1A (`:118`, `:825`/`:839`) and
`1f-anti-pattern.md:70` (`:825`). Cross-artefact REDRESS-53-anchor consistency holds
across all three substrate-firewall surfaces. The single V4 REVISE is folded,
live-verified, and orphan-free.

## CH5 Firewall Verdict (Track 1 ≡ Track 2)

The V5-folded V6 excavation passes the Lock-1 firewall on a confirming re-read.
With 1A holding the dedicated substrate-union spine and the V4 CH5-S8 REVISE folded
+ live-re-verified across all three firewall artefacts (1A / 1E / 1f-anti-pattern),
NO catalogued state implies a live parallel substrate, sidecar producer, or
renamed-scanner / cross-call-classifier-state violation, and NO Track-1 ≡ Track-2
dishonesty surfaces:

- **One substrate, unwired** — `TapeStructBuilder` grep-zero outside `tape/`
  (re-verified); the eager `OpenFrame` builders are the live substrate; the dead
  tape is the SK-V18 fold target, not a second plane (CH5-S1).
- **The only live retained projection** — the `OnceCell<StructuralIndex>` scan
  cache across all 8 carriers (math.rs confirmed non-carrier, doc-comment-only) — is
  correctly held as a retained scan cache, pinned to the four-value
  `substrate_target` manifest + REDRESS-53 at `:825`/`:839` (CH5-S2).
- **No cross-call classifier carry** under Lock 1 v+1; the `retention_lifetime`
  REJECT class is held open, not falsely cleared (CH5-S3).
- **The 5-shape BackendShape canon is excavated whole**; CollapsedStage is
  x86-pinned and the aarch64 candidate is the spec-named UNKNOWN-2D-05; aarch64-NEON
  is framed as absorption under the four LLVM shapes, never a 6th shape; 1A ↔ 1E
  agree (CH5-S4).
- **The Lock-1 closure obligation** (exactly-one-encoding post-fold; AoS/SoA
  coexistence transient-only) is intact as an explicit invariant in three places in
  1A and re-stated in 1E (CH5-S5).
- **The direction guard** (monotonic skinny→totality; the §9-forbidden-in-skinny
  names ARE the fold-target names; adoption INTO core, never relocation INTO skinny)
  holds (CH5-S6).
- **The do-not-redrive fence** (FieldSource/StructLayout walk is compile-time
  projection-emission, never a per-leaf registry lookup) is surfaced;
  `begin_compound(&StructLayout)` takes a pre-resolved reference (CH5-S7).
- **The V4 REVISE (CH5-S8)** — the 1E REDRESS-53 re-entry anchor — remains
  **closed**: `1e-locks-evidence.md:190` anchors REDRESS-53 at `:577`/`:825`/`:839`,
  the `:578` sidecar-vector clause is removed, 1E synced with 1A and
  1f-anti-pattern (CH5-S8).

## Disposition Summary

| Section | Subject | Disposition |
|---|---|---|
| CH5-S1 | Parallel-substrate firewall: one unwired tape (SUB17-001/002/003, AP17-001) | ACCEPT |
| CH5-S2 | Sidecar / retained projection across all 8 carriers (Firewall row 2, AP17-004) | ACCEPT |
| CH5-S3 | Renamed-scanner / cross-call classifier-state (Lock 1 v+1) | ACCEPT |
| CH5-S4 | 5-shape BackendShape canon + aarch64-NEON absorption (Lock 10, SUB17-005, 1E parity) | ACCEPT |
| CH5-S5 | Lock-1 closure obligation: exactly-one-encoding post-fold | ACCEPT |
| CH5-S6 | Second-substrate / monotonic-direction / forbidden-name collision (SUB17-005, §9) | ACCEPT |
| CH5-S7 | StructRegistry/FieldSource per-leaf hot-path do-not-redrive fence (SUB17-009, LAC-02) | ACCEPT |
| CH5-S8 | V4 REVISE closure: 1E REDRESS-53 re-entry anchor :578 → :577/:825/:839 (1e:190) | ACCEPT |

**Counts: ACCEPT 8 · REVISE 0 · REJECT 0 (8 sections).**
ACCEPT rate 100%. This lens meets the §4 ≥95% criterion for the second consecutive
cycle (V5 100% → V6 100%). Zero open critical defects from this lens; zero
REVISE/REJECT; zero orphan REVISE. The substrate-union (Lock 1) is accurate and
complete — ONE dormant unwired tape, the only live retained projection correctly
held as a scan cache, no parallel or second substrate implied anywhere. The 5-shape
BackendShape canon (Lock 10) is excavated whole with no 6th-shape pressure and
aarch64-NEON framed as absorption. The tape-as-unified-substrate question is framed
throughout as the ONE dormant fold target awaiting SK-V18 wiring.
