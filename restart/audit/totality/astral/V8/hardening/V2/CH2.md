# Pass Omega V8 CHALLENGE V2 CH2 Generality

Date: 2026-05-26.
Lens: CH2 generality after CH1 exact-test-table fold.
Scope: Omega-A through Omega-F, `locks-diff.md`, `master-plan-diff.md`, prior
CH2, REDRESS-212, and the SK-V14 W5B-FRONTEND V2 challenge archive.
Disposition: ACCEPT.

## Verdict

ACCEPT. The CH1 exactness fold does not narrow W5B into a CSS-only policy path or
introduce a grammar-name branch. The amended V8 surface now carries the exact
construct table and named Lock 14 tests in
`restart/audit/totality/astral/V8/master-plan-diff.md:68`-`100`,
`restart/audit/totality/astral/V8/ΩD-master-plan-reconciliation.md:58`-`95`,
and `restart/audit/totality/astral/V8/ΩF-migration-handoff.md:51`-`85`; those
rows bind every compatibility construct to
`skinny/crates/grammar/src/lib.rs::FrontendClosure`, request-local facts, exact
positive tests, and exact fail-closed tests.

The prior CH2 ACCEPT still holds, with the CH1 fold making the public-retirement
and Lock 14 parts stricter rather than less general. W5B remains generic BBNF
grammar-source frontend/import/IR closure, CSS L4 remains only the strict
positive witness, JSON/Sheets/BBNF-self proof carry remains required at W5B.4,
and W5C-GEN remains blocked until aggregate W5B-FRONTEND close.

## Checks

| Check | Result | Evidence |
|---|---:|---|
| No CSS-only policy branch | ACCEPT | W5B.1-W5B.3 lower imports, layout/discard, pretty/span/projection, and typed projections into `FrontendClosure` request-local facts, not CSS-specific generic policy (`master-plan-diff.md:58`-`64`, `:72`-`:81`; `ΩD-master-plan-reconciliation.md:52`-`56`). CSS appears only in W5B.4 as `regen-css` plus seven companions, beside JSON/Sheets/BBNF proof carry (`master-plan-diff.md:64`-`67`). |
| Full public-retirement tests | ACCEPT | The V2 CH2 defect was public retirement only for `@ws` (`skv14-waveW5B-FRONTEND-challenge/V2/CH2.md:17`-`24`, `:37`-`:43`). The CH1 fold now names fail-closed tests for `@import`, `@ws`, `?w`, `>>`/`<<`, `@pretty`, `@{...}`, `->` projection, and typed projection (`master-plan-diff.md:72`-`81`; `ΩF-migration-handoff.md:70`-`79`). |
| All-template guards | ACCEPT | V2 CH5 rejected CSS-only template guards because eight `*_templates` directories exist, including `json_templates` (`skv14-waveW5B-FRONTEND-challenge/V2/CH5.md:9`-`17`). V8 folds `all-template guard`, modified template rejection, and `w5b_lock14_frontend_all_templates_guard_counts_8` into W5B.0 (`master-plan-diff.md:55`-`57`, `:83`-`:91`; local census returned 8). |
| Non-JSON proof carry | ACCEPT | W5B.4 remains the same-commit request-consumer close and requires JSON unchanged-output proof plus Sheets/BBNF-self proof carry alongside CSS (`master-plan-diff.md:64`-`67`; `ΩB-skinny-lessons.md:44`-`46`; `ΩF-migration-handoff.md:35`-`36`). The V2 consolidated archive already classified non-JSON proof carry as useful and not a required fold (`HARDENING-SKV14-W5B-FRONTEND-V2-CONSOLIDATED.md:60`-`64`). |
| No grammar-name branches | ACCEPT | `locks-diff.md` is zero delta: no public syntax, BIR variant, BackendShape, substrate, or lock is added (`locks-diff.md:3`-`9`). V8 explicitly preserves "no grammar-name branches in generic crates" (`master-plan-diff.md:109`-`114`; `ΩD-master-plan-reconciliation.md:149`-`157`), and W5B.0 includes `w5b_lock14_frontend_generic_owner_leak_census` before frontend source work (`master-plan-diff.md:83`-`91`). |
| W5C remains blocked | ACCEPT | REDRESS-212 rejects the one-wave shape and routes formal W5B.0 through W5B.4 before W5C (`skv14-W5B-FRONTEND-redress.md:68`-`83`). V8 keeps W5C-GEN blocked until aggregate W5B close (`master-plan-diff.md:25`-`26`, `:40`; `ΩF-migration-handoff.md:87`-`99`). |

## Local Verification

- W5B source-owner and ledger diff check returned no paths for:
  `skinny/crates/bbnf-bench/src/lock14_baseline.rs`,
  `skinny/crates/grammar/src/lib.rs`,
  `skinny/crates/codegen/src/grammar_provider.rs`,
  `skinny/crates/codegen/src/lib.rs`, `skinny/xtask/src/main.rs`,
  `skinny/xtask/src/regen.rs`, `skinny/xtask/src/regen_css.rs`,
  `skinny/RESULTS.md`, and `restart/skinny/ROLLING-SOTA-DELTA.md`.
- `grep -cE '^[0-9]+\\. \\*\\*' restart/locks/LOCKS.md` returned `16`.
- `find skinny/crates/codegen/src -type d -name '*_templates'` returned 8
  directories: seven CSS L4 template dirs plus `json_templates`.

## Exact Folds

1. Keep W5B.0 as Lock14-only: owner-path roster, parent-diff routing, W5C/W5D
   subject rejection, modified-provider/template rejection tests, all
   `_templates` path guard, `w5b_lock14_frontend_all_templates_guard_counts_8`,
   and `w5b_lock14_frontend_generic_owner_leak_census` before any
   grammar/codegen/xtask frontend source work.
2. Preserve the CH1 exact construct table. Every row must retain owner
   file/type, request-local target representation, exact positive test, and exact
   fail-closed test for `@import`, `@ws`, `?w`, `>>`/`<<`, `@pretty`,
   `@{...}`, `->` projection, and typed projection.
3. Treat CSS L4 only as the strict positive witness. Do not add a CSS-only
   generic-crate branch, CSS-only template guard, grammar-name match arm,
   per-grammar feature flag, public syntax revival, BIR variant, BackendShape, or
   substrate surface.
4. Keep W5B.4 as the first W5B close point: `emit_runtime_from_request` must
   consume the frontend closure with JSON unchanged-output proof,
   Sheets/BBNF-self proof carry, `regen-css`, all seven CSS companions,
   provider/template no-change proof, and the W5B maintain proof in the same
   close.
5. Keep W5C-GEN blocked until W5B.4 admits aggregate W5B-FRONTEND. Keep
   provider-free generator-body replacement in W5C-GEN and provider/template
   deletion in W5D-DELETE.

Omitting any full public-retirement test, all-template guard, generic owner-path
leak census, non-JSON proof carry, no-grammar-name prohibition, or W5C block
reopens CH2 as REVISE.
