---
agent: 1D
pass: T-P1-excavation
cycle: V5
generated_at: 2026-05-29T00:00:00Z
spec_surfaces_audited:
  - restart/skinny/tranches/sk-v17/SPEC.md
  - restart/skinny/tranches/sk-v17/research/alpha/alphaC-redress-digest.md
  - restart/skinny/tranches/sk-v17/research/alpha/alphaD-validated-invalidated.md
  - restart/skinny/tranches/sk-v17/research/alpha/alphaE-candidate-shortlist.md
  - skinny/RESULTS.md
  - skinny/REDRESS.md
  - restart/locks/LOCKS.md
  - restart/ARCHITECTURE.md
files_audited_count: 24
live_truth_method: "wc -l + grep path:line over restart/skinny/tranches/sk-v17/{SPEC.md, research/alpha/*}; Read over skinny/RESULTS.md, restart/locks/LOCKS.md; grep over skinny/crates/runtime/src/tape/{mod,assembler}.rs + skinny/crates/runtime/src/grammars/json/value.rs + skinny/crates/bbnf-simd/src/dispatch.rs; cross-tree grep over crates/core/src/runtime/{tape,json,css_l4}/, crates/ir/src/registry/struct.rs, crates/simd-scan/src/lib.rs, crates/core/src/grammar/generated/*.rs; V4: `grep -c scan_structural` per generated file = {json,ebnf,bnf,csv,css_l4,css_pretty,google_sheets,bbnf}:1, math.rs:0, mod.rs:0 (EIGHT wired; math.rs carries NO `structural_index` field — its `ScanState` struct (math.rs:287-290) declares only `nospace_bits`/`nospace_start`; the two `OnceCell<StructuralIndex>` mentions at :281/:285 are doc-comment text (`grep -c OnceCell math.rs`=2, both doc-comments), contrasting json.rs:701 which DOES carry the genuine `structural_index: OnceCell<StructuralIndex>` field); `head -3` of crates/core/src/runtime/{json,css_l4}/{value,builder}.rs confirms '@generated by xtask regen-*; do not edit by hand.' (generated, not hand-written); value_from_ref/ValueRef grep-empty in both core value.rs; no cargo/build mutation"
prior_cycle_dispositions_folded:
  accepted:
    - CH5-S3-renamed-scanner-cross-call (Lock 1 v+1 ELEVATION LOCKS:137-149 verified; REJECT class not tripped)
    - V3-CH1-V3-003-SK17L-002-eager-vs-lazy-divergence-real (the eager-vs-lazy divergence survives; only the 'hand-written' framing was corrected)
  rejected:
    - CH1-V2-001-1D-absent (this inventory authored to close the ORPHAN REVISE / CH3-R3 missing-1D charge)
    - CH1-V2-003-css_l4-scan-unwired-FALSE (scan_structural IS wired in css_l4.rs:15982 and all 8 grammars; corrected in §SK17L-GEN)
    - 1f-coherence-Gaps-row-107-css_l4-alphabet-absent-FALSE
  revised:
    - CH1-V2-002-value_from_ref-path-defect (corrected to skinny/crates/runtime/src/grammars/json/value.rs:143)
    - CH3-R1-StructRegistry-Arena-Builder-preblock-absent (added as do-not-redrive ledger row L-SK17-02 with no-per-leaf-registry-lookup fence)
    - CH3-R2-FieldSource-walk-compile-time (annotated: FieldSource walk is compile-time projection-emission resolved once)
    - CH5-S4-css-builder-pending-count (corrected to six pending_ Vecs + pending_value:Option)
    - S1.4-COH17-004-aarch64-CollapsedStage-UNKNOWN-2D-05 (reclassified as spec-flagged open unknown)
    - S1.8-COH17-008-StructuralAlphabet-JSON-CSS-exercised-only (breadth-of-config not breadth-of-proof)
    - V3-CH1-V3-003-SK17L-002-hand-written-mischaracterisation (FOLD §3-B/§1: json/value.rs:1 + css_l4/value.rs:1 carry '@generated by xtask regen-*'; relabel EAGER-generated vs LAZY-generated; Lock-14 HONOURED not violated — grep-verified regen headers; SK17L-002 row + Gaps row corrected)
    - V3-SK17L-008-plus-math.rs-false-ninth-carrier (FOLD §1/§3-B/1d:91: grep -c scan_structural math.rs = 0; math.rs carries NO structural_index field — its ScanState struct (math.rs:287-290) holds only nospace_bits/nospace_start; the :281/:285 OnceCell<StructuralIndex> mentions are doc-comments; struck '+math.rs', restated verified EIGHT-name wired census, named math as the lone NO-scan-call generated grammar)
    - CH1-V4-002-SK17L-008-doc-comment-as-index-FIELD-mischaracterisation (FOLD CH1-V4-002, shared root w/ CH1-V4-001: SK17L-008 impl column + frontmatter called the math.rs :281/:285 doc-comments an 'index FIELD' and self-contradicted 'field-only … doc-comments'; corrected to 'math.rs scan_structural=0; OnceCell<StructuralIndex> mentions are doc-comments, no structural_index field'; contrasted with json.rs:701 genuine field; grep -c OnceCell math.rs=2 both doc-comments live-verified)
  first_cycle_additions:
    - SK17L-001-tape-substrate-proven-SoA-unwired-totality
    - SK17L-002-value-api-lazy-ValueRefG-isomorphic
    - SK17L-003-eager-value-tree-118x-pre-block
    - SK17L-004-struct-registry-indirection-pre-block
    - SK17L-005-fact-stream-String-pre-block
    - SK17L-006-NEON-aarch64-only-x86-pre-block
    - SK17L-007-StructLayout-name-retired-Lock2
    - SK17L-008-scan-generality-wiring-state-not-design
    - SK17L-009-projection-generality-JSON-CSS-only-sheets-SK18
    - SK17L-010-broadcast-telemetry-honesty
divergence_count:
  spec_claims_implemented: 1
  spec_claims_unimplemented: 4
  impl_exceeds_spec: 2
  unknown: 2
locks_amendment_candidates: 0
---

## Executive Summary

1D digests what the SK-V1…SK-V17 skinny track empirically proved or disproved that
the TOTALITY V1 spec must absorb, for the tape / substrate / value-API / BackendShape
/ NEON surfaces. The skinny path is proven-in-plan (Alpha V4 + S-P1 V4 + S-P2 V3 +
S-P3 V3, §3Z-LOCKED) at master `445925167`. The single load-bearing proved fact: the
flat lazy-offset tape (`skinny/crates/runtime/src/tape/`: `Tape<'input>` SoA with
`offsets: Vec<u32>` + sparse `flag_cursors`/`flag_values` + `PayloadArena`,
`mod.rs:94`,`:175`,`:38`) + the lazy `ValueRef`-cursor projection (JSON's
`value_from_ref` rider, `skinny/crates/runtime/src/grammars/json/value.rs:143`, zero
per-node heap alloc) IS the >SOTA carrier — JSON 51/51 strict A/GO Track 1 > sonic
strict same-plane (`skinny/RESULTS.md:5-55`) is the standing proof and regression
tripwire. The disproved routes the totality fold MUST NOT re-derive: the AZ-IV eager
value tree (118x, canada 1.83ms→215.7ms), the StructRegistry/Arena<G>/Builder<G>
per-leaf hot-path indirection (28-65x / 983x / 10583x WATCHDOG), the CSS fact-stream
String as admission plane, and any x86/AVX/SVE path (aarch64 M5 Max only; no SVE).
The crates/core fold-target carries the disproved-route SHAPES (eager `OpenFrame`
builders, AoS `TapeRec`, `StructLayout`/`StructRegistry` registry indirection) as
UNWIRED dead code awaiting SK-V18 fold — they are the fold TARGET, not skinny
artefacts; the monotonic direction is skinny-proven → crates/core. The
JSON-empirical vs grammar-neutral split is explicit per row: JSON is the only tape
WITNESS today; CSS is the SK-V17 first-mover; Sheets/BBNF-self projection generality
is by-construction-not-by-exercise, an SK-V18 fold target. preserve-rich-ast and the
N≥50-cold-median telemetry honesty are non-negotiable.

## Spec-Claim ↔ Implementation Table

The "spec" column here is the SK-V17 SPEC + skinny corpus (what the proven path
establishes); the "impl / fold-target" column is what crates/core + the V1 spec
currently carry. Every divergence is a fold obligation for SK-V18, NOT an SK-V17
owner-path edit (the monotonic direction, SPEC §0.1.11 `:110-114`).

| ID | Skinny-proven lesson (path:line) | Totality fold-target / V1-spec state (path:line) | Verdict | Empirical class | Note |
|---|---|---|---|---|---|
| SK17L-001 | Proven substrate is the SoA `Tape<'input>` — `offsets: Vec<u32>` + sparse `flag_cursors`/`flag_values` + `PayloadArena` (`skinny/crates/runtime/src/tape/mod.rs:94`,`:96-99`,`:38`); `TapeBuilder` `push_plain_offset` = one branchless u32 write (`assembler.rs:42`,`:71`). JSON rides it >SOTA (`skinny/RESULTS.md:5-55`, V6 `alphaD:82`). | crates/core fold-target is `TapeStructBuilder` over a 16-byte AoS `TapeRec` (`crates/core/src/runtime/tape/mod.rs`, `record.rs:103`), UNWIRED; mod-doc says "AoS first … later SoA split". V1 ARCH Lock-1 union says retained offsets ARE the tape (`ARCHITECTURE.md:1088`). | grammar-neutral | unimplemented (cross-tree shape drift) | SK-V18 converges to ONE substrate (Lock 1, LOCKS:75). AoS-vs-SoA is a fold-shape question, NOT a parallel substrate within either tree. |
| SK17L-002 | Value API is the lazy grammar-parametric `ValueRef<'doc,'input,K,G>` (`skinny/crates/runtime/src/tape/mod.rs:175`) read by `value_from_ref` per grammar (JSON witness `skinny/crates/runtime/src/grammars/json/value.rs:143` — zero per-node heap alloc; `alphaC:69-72`). ONE `BackendRule`-walking generator emits document/value/view/visitor (SPEC §0.1 row 3 `:54-72`). | crates/core has per-grammar **generated** EAGER value enums (NOT hand-written — `crates/core/src/runtime/json/value.rs:1` + `css_l4/value.rs:1` both carry `// @generated by xtask regen-*; do not edit by hand.`); NO `value_from_ref`/`ValueRef` in `crates/core/src/runtime/json/value.rs` (grep empty); CSS `CssTypedValue` eager (`crates/core/src/runtime/css_l4/value.rs:414`). | grammar-neutral | unimplemented | The divergence is EAGER-generated vs LAZY-generated, not generated-vs-hand-written: the value API is already a regen output (Lock-14 HONOURED — a generic crate carrying a per-grammar generator, not hand-written per-grammar runtime). The fold target is to make that generator emit the lazy `ValueRef<G>` projection; the `FieldSource` walk (struct.rs:84) IS the BackendRule recipe but must be COMPILE-TIME projection-emission (see SK17L-004 fence). |
| SK17L-003 | AZ-IV eager value tree REFUTED: 118x (canada 1.83ms→215.7ms, `cb14970f`; `alphaC:50-66`, `alphaD:I5 :104`). Eager per-leaf payload / f64-alloc-per-number / `Box<CssColor>` is the pre-block; materialization stays lazy-by-default (SPEC §1 `:241-242`, K-block `:139`). | crates/core CSS builder IS the eager-value-tree shape: `CssTypedValue` eager enum + six `pending_*` Vecs + `pending_value:Option` (`crates/core/src/runtime/css_l4/builder.rs:71-79`, `value.rs:414`). UNWIRED dead code (fold target). | grammar-neutral (CSS-witnessed cost) | unimplemented (pre-blocked shape present as fold target) | Fold must REPLACE the eager tree with lazy projection, NOT carry it forward (SK17L-002). Pre-block is anchored to the construct, not a symbol list (`alphaC:78-82`). |
| SK17L-004 | StructRegistry/Arena<G>/Builder<G> per-leaf hot-path indirection REFUTED: 28-65x bbnf/sheets, 983x css bootstrap (606.4ms), 10583x WATCHDOG tailwind (77.6s) (`alphaC:98-118`, SPEC §9 global block `:793-795`, W1 row `:824`). No registry lookup in the per-leaf hot path. | LIVE in crates/core/crates/ir fold-target: `StructRegistry::layout(rule_id)` lookup (`crates/ir/src/registry/struct.rs:313`,`:331`) resolving a `StructLayout` (`:202`), handed by `&StructLayout` into `begin_compound` (`crates/core/src/runtime/tape/mod.rs:185`). | grammar-neutral | unimplemented (pre-blocked indirection present as fold target) | FENCE (CH3-R1): the `StructRegistry::layout` lookup must be resolved ONCE at compile-time / emission, never per-leaf. `begin_compound` already takes a resolved `&StructLayout` (no per-leaf lookup inside it — reads only `layout.rule_id & 0x1F`), so the fold inherits the correct shape ONLY if the caller resolves the layout once. SPEC `:794-795`. |
| SK17L-005 | CSS fact-stream String REFUTED as admission plane: ~34% self-time `emit_*`; benched ~63.9 Mbps is String serialization not typed CSSOM (`alphaC:162-211`, SPEC §1 `:243-244`, global block `:796-797`). Live skinny route is `W5C_REQUEST_FACT_PROFILES` (`skinny/crates/codegen/src/lib.rs:336`) + 7 `RequestFacts` registrations (`regen_css.rs:45-153`). | V1 Lock 1 v+1 admits `FactStream` as the 5th substrate-manifest category with `substrate_target=admitted_fact_output` (`LOCKS.md:100-116`). NOT a contradiction: Lock 1's FactStream is typed-schema/provenance output-plane; SK-V17 bars fact-stream-String-as-CSS-admission specifically. | grammar-neutral (skinny CSS-empirical eager analogue) | unknown (reconcilable framing) | The V1 FactStream category survives for non-CSS typed-output planes; SK-V17 retires the String CSS admission plane. Upgrade to catalogued divergence U-SK17L-002 (CH5-S8: one-encoding closure obligation). |
| SK17L-006 | NEON is aarch64-only, profile-first, scalar+checkasm-gated, same-wave-consumed; produces only a `Vec<u32>` structural index via `select_classifier(alphabet: &[u8;64])` (`skinny/crates/bbnf-simd/src/dispatch.rs:42`); x86/AVX-512/SVE PRE-BLOCKED (Apple M5 Max no SVE) (SPEC §1 `:258`, §0.1 NEON `:99-106`, `alphaC:307-335`). | V1 §7.3 BackendShape `CollapsedStage` is x86/AVX-512-pinned (`ARCHITECTURE.md:1088`,`:1186`); `admits_collapsed_stage` binds `target.arch==x86`+`avx512bw` (`:1206`, LAC-2D-06); aarch64 mechanically refused. crates/core scan crate `crates/simd-scan` carries neon+avx2+avx512+wasm+scalar (`lib.rs:80`). | impl-exceeds-spec (multi-arch scan) + spec-x86-pinned (CollapsedStage) | impl-exceeds-spec | aarch64 CollapsedStage is the SPEC-NAMED open unknown UNKNOWN-2D-05 (`ARCHITECTURE.md:1206`,`:1278-1282`), NOT a fresh gap. SK-V17 NEON sits under the 4 LLVM shapes' scan-leaf FFI, NOT CollapsedStage. T-P2 maps absorb-without-6th-shape (Lock 10 5-shape canon, `LOCKS.md:107-108`). |
| SK17L-007 | Lock 2 RETIRED the term `StructLayout`; canonical name is `Layout`/`LayoutFacts` (`LOCKS.md:160`; `alphaC:29`,`:380`). Skinny uses `BackendRule` + `LayoutFacts.backend_shape ∈ {EagerTape,OffsetTape,EventTape,SinkOnly,CollapsedStage}` (`skinny/crates/ir/src/cost.rs:119-121`). | crates/core/crates/ir carries `StructLayout` LIVE (`crates/ir/src/registry/struct.rs:202`); the retired name is the live core API consumed by `begin_compound` (`crates/core/src/runtime/tape/mod.rs:26`,`:185`). | grammar-neutral | unimplemented (name not migrated in core) | Lock-2 naming reconciliation surface for T-P3; the totality tree has not migrated `StructLayout`→`Layout`. ~960 `StructLayout` references across crates/ir + codegen emitter + ~16 tests (generator-side rename regenerating all 8 parsers). |
| SK17L-008 | SK-V17 NEON scan is JSON-only-WIRED today in skinny; CSS scan unwired (SPEC §0.1 row 9 `:99-106`, W3 `:588-636`); generality is JSON-witnessed (`alphaD:53-65`). The scan generality is a WIRING property, not a design property. | crates/core `scan_structural(&StructuralAlphabet)` is WIRED into EXACTLY 8 generated grammars: json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf each carry exactly one `scan_structural` call (`crates/core/src/grammar/generated/*.rs`, grep-verified `grep -c scan_structural` = 1 per file). `math.rs` is the lone generated grammar with `scan_structural`=0 and no `structural_index` field: its `ScanState` struct (`math.rs:287-290`) declares only `nospace_bits: u64` + `nospace_start: isize`, and its only `OnceCell<StructuralIndex>` mentions (`math.rs:281`,`:285`) are doc-comment text in the `ScanState` doc-block (`grep -c OnceCell math.rs` = 2, both doc-comments) — NOT a struct field. (Contrast json.rs:701, which DOES carry a genuine `structural_index: OnceCell<StructuralIndex>` field.) So the wired census is EIGHT, not nine. css_l4.rs builds the full `StructuralAlphabet` and calls it (css_l4.rs:15976-15982). | grammar-neutral | impl-exceeds-spec (core scan IS grammar-general across 8) | CORRECTS the 1f-coherence Gaps row (CH1-V2-003 REJECT): core CSS scan IS wired with the rich alphabet across all 8 grammars; the residual gap is the missing TAPE CONSUMER, not the scan. The skinny-side CSS scan (W3) is the unwired surface, in the skinny tree. |
| SK17L-009 | Projection generality is exercised by-construction on JSON + CSS ONLY; `sheets_witness` is a 24-LOC `EventGrammar` stub with NO `.bbnf`/`BackendRule` and CANNOT serve as a projection exercise; BBNF-self has no tape witness; Sheets/BBNF generality is SK-V18 (SPEC §0.1.11 `:110-114`, `alphaD:53-65`, `:151`). | V1 Lock 14 claims full grammar generalization across JSON/CSS/Sheets/BBNF (`LOCKS.md:349`). True by-construction; proven by-exercise only on JSON (tape) + CSS (SK-V17 first-mover). | JSON-empirical + grammar-neutral-by-construction split | spec-claims-implemented (generality is by-construction; SK-V17 closes JSON+CSS proof) | The split is load-bearing for Lock 14: scan/projection generality is breadth-of-config (alphabet-as-data), not breadth-of-proof. SK-V18 proves Sheets/BBNF-self by-exercise. |
| SK17L-010 | Telemetry honesty: N≥50 cold + MEDIAN per corpus (SPEC §0.4 `:151-181`); the 24-row broadcast (one tuple ×24 row-ids, `skinny/RESULTS.md:112-135`) and W6 single-sample are REFUTED (`alphaC:215-260`, `alphaD:I7 :106`). | crates/core has no benched-CSS rows; the V1 spec carries the broadcast pre-block (Lock 8, `LOCKS.md`). | grammar-neutral measurement discipline | spec-claims-implemented (discipline codified) | Not a tape/substrate shape lesson but binds the close gate; carried for completeness so the fold does not re-derive the broadcast. |

## JSON-Empirical vs Grammar-Neutral Split (CH2 GENERALITY firewall)

The skinny lessons separate into the durable grammar-neutral substrate facts the
totality spec must absorb, vs the JSON-empirical witness facts that prove them but do
not by themselves generalize. T-P3 folds only the durable grammar-neutral findings.

| Lesson | JSON-empirical (witness) | Grammar-neutral (durable, fold) | Generality status |
|---|---|---|---|
| Lazy-offset tape | JSON 51/51 strict A/GO Track 1 > sonic, per-iter equality PASS (`skinny/RESULTS.md:5-55`) — the only tape WITNESS today | `Tape`/`ValueRef`/`TapeBuilder` is grammar-agnostic substrate; `push_plain_offset` carries no grammar policy | tape: JSON-WITNESSED; CSS is SK-V17 first-mover; Sheets/BBNF by-construction (SK-V18) |
| ValueRef projection | JSON `value_from_ref` (`json/value.rs:143`) — the byte-equal re-emission target (W2 R-CH2-1, SPEC `:552-557`) | one `BackendRule`-walking generator emits document/value/view/visitor; alphabet/layout are DATA | projection: JSON+CSS only; a CSS-only generator that never re-emits JSON FAILS CH2 (SPEC `:62`,`:557`) |
| Structural scan | JSON `scan_structurals` (`json/scan.rs:22`) the isomorphism anchor | `select_classifier(alphabet:&[u8;64])` — alphabet is the only grammar datum (Lock-14 vehicle, SPEC §2.1 `:315`) | core: WIRED across all 8 grammars (SK17L-008); skinny: JSON-wired, CSS is W3 |
| Eager-tree pre-block | (none — JSON never eager) | per-leaf typed/f64/Box heap alloc at parse time refuted (118x); applies to ANY grammar | grammar-neutral pre-block (CSS was the witness corpus) |
| Registry indirection pre-block | (none) | per-leaf StructRegistry/Arena<G>/Builder<G> dereference refuted (28-65x/983x/10583x) | grammar-neutral pre-block |

## Past-Corpora Do-Not-Redrive Ledger (CH3 REGRESSION + CH3-R1 fence)

Every re-open test greps the **skinny benched tree** for skinny gates; for the
totality fold the same construct is the crates/core fold-target shape. The fold must
not RE-DERIVE these into either tree.

| # | Pre-block | Measured refutation | Class | Fold fence (CH3-R1 / do-not-redrive) |
|---|---|---|---|---|
| L-SK17-01 | AZ-IV eager value tree | 118x (canada 1.83ms→215.7ms, `cb14970f`; `alphaC:50-66`) | ADMIT-UNDER-FRAMING | lazy view over sealed tape; zero per-leaf alloc; PayloadArena (`mod.rs:38`) only for irreducible decode. crates/core `CssTypedValue`+six pending Vecs (`css_l4/builder.rs:71-79`) is the fold-deletion target. |
| L-SK17-02 | StructRegistry / Arena<G> / Builder<G> hot-path indirection | 28-65x bbnf/sheets; 983x css bootstrap; 10583x WATCHDOG tailwind (`alphaC:98-118`; SPEC `:793-795`,`:824`) | PERMANENT PRE-BLOCK | **FENCE: no per-leaf `StructRegistry::layout(rule_id)` lookup in the hot path.** Live at `crates/ir/src/registry/struct.rs:313`,`:331`, consumed by `begin_compound` (`crates/core/src/runtime/tape/mod.rs:185`). The layout MUST be resolved once at compile-time/emission; `begin_compound` correctly takes a resolved `&StructLayout` (no internal lookup). SPEC `:794-795`. |
| L-SK17-02b | FieldSource per-leaf walk | (the BackendRule recipe lives inside the live StructRegistry, `struct.rs:84`,`:313`) | PRE-BLOCK seam (CH3-R2) | **FENCE: the `FieldSource{TypedLeaf,BranchTag,SeqPosition,RepeatElement,RuleReference}` walk is COMPILE-TIME projection-emission, resolved once, NOT a per-leaf StructRegistry indirection.** A naive per-leaf walk re-opens the 28-65x/983x/10583x regression. SPEC `:794-795`. |
| L-SK17-03 | CSS fact-stream String | ~34% self-time `emit_*`; benched String not typed (`alphaC:162-211`) | PERMANENT PRE-BLOCK (as admission plane) | typed CSSOM via lazy projection; fact-stream + `W5C_REQUEST_FACT_PROFILES` (`codegen/src/lib.rs:336`) retired to diagnostic-only. V1 FactStream category (`LOCKS.md:100`) survives only for typed-schema output planes. |
| L-SK17-04 | 24-row broadcast | one tuple ×24 row-ids (`skinny/RESULTS.md:112-135`, grep count=24) | PERMANENT PRE-BLOCK | per-corpus independent N≥50 cold median; distinct measurement_row_id + broadcast_group_id (Lock 8). |
| L-SK17-05 | FNV / fixture overfit | bench-only; 148 fixture parse fns (`alphaC:264-303`) | PERMANENT PRE-BLOCK (as runtime) | scratch sizes from `input.len()` + BackendRule shape, never per-corpus; FNV diagnostic-only. |
| L-SK17-06 | x86 / AVX / SVE | out-of-scope; Apple M5 Max no-SVE (`alphaC:307-335`, SPEC `:258`,`:806`) | PERMANENT PRE-BLOCK (this pass) | aarch64 NEON intrinsics-first; the V1 §7.3 x86 CollapsedStage is UNKNOWN-2D-05 (`ARCHITECTURE.md:1206`), NOT the SK-V17 target. crates/simd-scan multi-arch kernels are a fold-scope decision, not admission. |
| L-SK17-07 | Second substrate (D6) | SK-V17 §9 REJECTed `:854`; second-substrate block enumerates `StructLayout`/`TapeStructBuilder`/`TapeCursor` `:807-811` | PERMANENT PRE-BLOCK | the §9 names are FORBIDDEN-IN-SKINNY; SK-V18 adopts the PROVEN skinny `Tape`/`ValueRef` INTO crates/core, never relocates crates/core `TapeStructBuilder` into skinny (monotonic skinny→totality, SPEC `:110-114`). |

## Divergences Catalogued

| ID | Divergence | Evidence | LOC / risk (files-touched surface) |
|---|---|---|---|
| SK17L-001 | Tape encoding: proven SoA `Tape` vs fold-target AoS `TapeRec`. | `skinny/crates/runtime/src/tape/mod.rs:94`; `crates/core/src/runtime/tape/record.rs:103` | 200-600 LOC SK-V18 fold; medium. Propagation surface: eager builders + tape wiring touch 22+ files (all 8 generated parsers + emitter shape hierarchy + runtime/mod.rs + parse_with.rs); re-emit is generator-side, regen-gated. |
| SK17L-002 | Value API per-grammar eager vs lazy `ValueRef<G>`. | `skinny/crates/runtime/src/grammars/json/value.rs:143`; `crates/core/src/runtime/json/value.rs` (no value_from_ref) | 300-700 LOC projection generator; high. Surface: emitter generator (hand-written) + per-grammar value.rs/view.rs (regenerated emitted-LOC). |
| SK17L-003 | Eager value tree present as crates/core fold-target. | `crates/core/src/runtime/css_l4/builder.rs:71-79`,`value.rs:414` | 817-LOC CSS builder god-module is fold-deletion target; high. |
| SK17L-004 | StructRegistry per-leaf indirection live in fold-target. | `crates/ir/src/registry/struct.rs:313`,`:331`; `crates/core/src/runtime/tape/mod.rs:185` | 0 LOC to catalogue; HIGH to fold (no-per-leaf-lookup fence is load-bearing). |
| SK17L-006 | §7.3 CollapsedStage x86-pinned; SK-V17 aarch64-NEON under 4 LLVM shapes. | `ARCHITECTURE.md:1206` (UNKNOWN-2D-05); SPEC `:258`,`:806` | 0 LOC (canon holds); medium (absorb without 6th shape). |
| SK17L-007 | `StructLayout` Lock-2-retired but live in crates/ir/crates/core. | `LOCKS.md:160`; `crates/ir/src/registry/struct.rs:202` | ~960 `StructLayout` references across crates/; medium (generator-side rename regenerating 8 parsers + ~16 tests). |
| SK17L-008 | core CSS scan IS wired (all 8 grammars); the 1f Gaps-row claim was FALSE. | `crates/core/src/grammar/generated/css_l4.rs:15982` + 7 siblings; `crates/simd-scan/src/lib.rs:80` | 0 LOC (already wired); residual gap is the tape consumer not the scan. |

## Gaps / Missing Primitives

| Gap | Evidence | Receiver |
|---|---|---|
| No live tape CONSUMER in crates/core: `TapeStructBuilder` + the wired `scan_structural` index exist but no parse path threads the index INTO a tape; eager `OpenFrame` builders are the live substrate. | `crates/core/src/runtime/json/parse_with.rs` uses `JsonStructBuilder`; scan WIRED (`css_l4.rs:15982`) but feeds eager builders. | SK-V18 fold (T-P2/T-P3 map; SK-V17 proves the consumer in skinny first). |
| No `ValueRef<G>` lazy-projection generator in crates/core; the per-grammar value APIs are GENERATED but EAGER (`@generated by xtask regen-*`, `json/value.rs:1` + `css_l4/value.rs:1`), not hand-written and not lazy. | `crates/core/src/runtime/{json,css_l4}/value.rs:1` (regen headers; no value_from_ref) | SK-V17 W2 proves the lazy projection in skinny (SPEC §5); SK-V18 retargets the existing regen path to emit `ValueRef<G>`. |
| No aarch64 CollapsedStage admission path; §7.3 5th shape x86-only (UNKNOWN-2D-05). | `ARCHITECTURE.md:1206`,`:1278-1282`; SPEC §9 `:854` | T-P2 research: how aarch64-NEON union maps into canon without a 6th shape (Lock 10). |
| Skinny-side CSS structural scan unwired (the real W3 gap — distinct from core's wired scan). | SPEC §0.1 row 9 `:99-106`, W3 `:588-636`; skinny CSS `generated.rs` has 0 SIMD (`alphaD:152`) | SK-V17 W3 wires CSS scan via `select_classifier` in skinny. |

## Open Questions

| UNKNOWN | Blocking question | verify_action |
|---|---|---|
| U-SK17L-001 | Does the proven SoA `Tape` shape replace the fold-target AoS `TapeRec` in SK-V18, or does crates/core keep AoS and prove parity? | T-P2 re-read `crates/core/src/runtime/tape/mod.rs` ("AoS first … later SoA split") against the proven `skinny/.../tape/mod.rs:94`; name the convergence target tape shape. |
| U-SK17L-002 (CH5-S8 closure obligation) | Post-SK-V18, is there EXACTLY ONE substrate encoding across both trees, or may a dual AoS/SoA encoding read as a permissible end-state? | T-P3/Omega assert the Lock-1 post-fold one-substrate closure (`LOCKS.md:75` "parallel substrates are dead") as a CATALOGUED divergence, not merely an open question; a dual encoding is NOT a permissible end-state. No LOCKS edit by T-P1. |
| U-SK17L-003 | Does the totality scan layer (`crates/simd-scan`, multi-arch) narrow to the proven aarch64 set, or retain x86/avx512/wasm kernels post-fold? | T-P2 compare `crates/simd-scan/src/lib.rs:80-113` against SK-V17 aarch64-only mandate (SPEC `:258`); architecture-scope decision, not a defect. |
