# BB.W6 — BB Close

**Thesis** Hereupon BB closes: the perf gates BB-G1..G4 are met; the cohort-grammar generated-LOC budget is verified; the carry ledger to BC.W0 is named explicitly; the 13-lock cross-reference closes with no empty cells. **Closer-gate** `cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis -p bbnf-path` 100% pass; bench harness produces post-BB.json archetype; PROGRESS.md and FINAL.md land.

## §1 Deliverable

W6 is the close wave. The deliverables are:

1. **PROGRESS.md** — captures the current per-wave status with each closer-gate's verification (W0 path-deps, W1 OpenFrame retired, W2 cohort templated, W3 optimiser pipeline + perf gates, W4 three-surface API, W5 path + visitor surfaces).
2. **FINAL.md** — the BB close artefact carrying the 13-lock verification, the per-grammar generated-LOC summary table, the perf-gate measurements against SOTA anchors, and the carry ledger to BC.W0.
3. **post-BB.json bench archetype** — a benchmark snapshot file that BC.W0 entry preflight reads to confirm the perf trajectory; per `audit/HARDENING-SYNTHESIS-2026-05-03.md:128-136` the archetype includes BB-G1, BB-G2, BB-G3, BB-G4 measurements + the cohort grammar bench rows for regression-detection at BC.
4. **Carry ledger to BC.W0** — the four carry-tags BB→BC.C1 through BB→BC.C4 are documented at the wave-level exit-criteria; BC.W0 entry preflight verifies each carry's pre-condition.
5. **Lock cross-reference table closure** — the BB.md 13-lock table closes with every cell populated; no empty cells; the Notes column flags any carried-forward concerns (e.g., L7's BC.W4 `bbnf-regex` endpoint reconciliation).

The perf-gate verification is the dominant content of FINAL.md. Each gate names competitor + dataset + platform per Lock 8 (`docs/HARDENING-PLAN-PROMPT.md:48`):

| Gate | Target | Competitor | Dataset | Platform | BB-close measurement |
|---|---|---|---|---|---|
| BB-G1 | ≤ 3.5 ms | lightningcss 4.16 ms | bootstrap.css | M1 Pro | (filled at W6) |
| BB-G2 | ≤ 35 ms | lightningcss 43.37 ms | tailwind.css | M1 Pro | (filled at W6) |
| BB-G3 | ≤ 800 µs | sonic-rs 854 µs | citm_catalog.json | M1 Pro | (filled at W6) |
| BB-G4 | ≤ 3.0 ms | sonic-rs 3.144 ms | canada.json | M1 Pro | (filled at W6) |

Per `audit/SOTA-2026-05-03.md:131-136` (lightningcss bench table) and `audit/SOTA-2026-05-03.md:50-58` (sonic-rs benchmark_aarch64). If any gate misses by mechanism, FINAL.md routes the gap to BC.W5 with a named rewrite hypothesis per `audit/HARDENING-SYNTHESIS-2026-05-03.md:55-58` (the F4 Tailwind regex_scan amendment).

The cohort-grammar LOC budget verification: BB-G5 requires ≥ 1,500 LOC saved across the 5-grammar cohort. The W2 close already verified the cohort runtime/<g>/ shrinkage; W6 emits the aggregate table:

| Grammar | Pre-BB LOC | Post-BB LOC | Net Delta | BB-G5 contribution |
|---|---:|---:|---:|---|
| `runtime/bnf/` | ~440 | ≤ 50 | −89% | ~390 LOC saved |
| `runtime/csv/` | ~458 | ≤ 50 | −89% | ~408 LOC saved |
| `runtime/ebnf/` | ~445 | ≤ 50 | −89% | ~395 LOC saved |
| `runtime/css_pretty/` | ~455 | ≤ 50 | −89% | ~405 LOC saved |
| `runtime/math/` | ~467 | ≤ 50 | −89% | ~417 LOC saved |
| **TOTAL cohort** | **~2,265** | **≤ 250** | **−89%** | **~2,015 LOC saved** |

Per `audit/CENSUS-2026-05-03.md:511-515`, the pre-BB cohort baseline is ~2,000 LOC across 35 files. BB-G5 (≥ 1,500 LOC saved) closes with margin.

The 13-lock cross-reference closure: every cell in `docs/tranches/BB/BB.md` 13-lock table populates with the wave that addressed it. Empty cells are faults. BB.W6 verifies the table closes per `docs/HARDENING-PLAN-PROMPT.md:64-68` Lane 1.

Estimated edit surface: PROGRESS.md ~120 LOC; FINAL.md ~200 LOC; post-BB.json ~ a few KB of bench measurements. Source code: zero (W6 is a documentation + verification close).

## §2 Milestones

| ID | Surface | Action | Gate | Exit-criteria |
|---|---|---|---|---|
| M0 | Pre-W6 verification | Verify W0-W5 closer-gates each closed; the wave-level audit artefacts (W0-toolchain-baseline, W2-cohort-budget, W3-perf-gates, W4-generated-size-budget, W5-visitor-coverage) all exist | `find docs/tranches/BB/audit -name '*.md' \| wc -l` returns ≥ 5 (one per W) | All wave artefacts in place; the close has evidence to cite. |
| M1 | Perf gate measurement | Run bench harness `cargo bench -p bbnf -- css_l4_bootstrap css_l4_tailwind json_citm json_canada`; record measurements; verify each is at-or-below the BB-G target | Each measurement at-or-below target; `cargo bench` output captured | Perf gates met; SOTA anchors satisfied. |
| M2 | post-BB.json archetype | Emit `docs/tranches/BB/post-BB.json` with the bench measurements + cohort LOC summary + visitor coverage; format matches the BC.W0 entry preflight expectation | `test -f docs/tranches/BB/post-BB.json && jq '.gates' docs/tranches/BB/post-BB.json` parses cleanly | Archetype lands; BC.W0 entry preflight has its read-source. |
| M3 | PROGRESS.md | Land `docs/tranches/BB/PROGRESS.md` with per-wave summary; each W's deliverable + closer-gate + actual-measurement; Wn-status column at exactly "CLOSED" for W0-W5 | `test -f docs/tranches/BB/PROGRESS.md`; `grep -c 'CLOSED' docs/tranches/BB/PROGRESS.md` returns 6 (W0-W5 + the close itself) | Progress tracking complete. |
| M4 | FINAL.md | Land `docs/tranches/BB/FINAL.md` with: 13-lock verification (every cell populated); per-grammar LOC summary; perf-gate measurements; carry ledger to BC.W0 | `test -f docs/tranches/BB/FINAL.md`; `grep -c '\| L[0-9]\+\.' docs/tranches/BB/FINAL.md` returns 13 (13 locks rows) | The BB close artefact lands. |
| M5 | Carry ledger to BC.W0 | Document each of BB→BC.C1, BB→BC.C2, BB→BC.C3, BB→BC.C4 with: owner-wave-in-BB, what-BC-consumes, BC.W0 entry preflight check | `grep -c 'BB→BC.C' docs/tranches/BB/FINAL.md` returns 4 (one per carry-tag) | Carries are explicit; no dangling deferrals. |
| M6 | Workspace verification | Run `cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis -p bbnf-path` 100% pass; the four-crate test surface gates the close | All four crates pass | Workspace-green at BB close; BC.W0 inherits a stable substrate. |
| M7 | 13-lock cross-reference | Verify every lock cell in BB.md is populated; the Notes column flags carried-forward concerns explicitly; no empty cells | `grep -A 13 '^| Lock ' docs/tranches/BB/BB.md \| grep -E '\|\s*\|\s*\|' \| wc -l` returns 0 (no empty cells) | Lane 1 (Lock-Adherence) gate green. |

## §3 Closer gate

Per `docs/tranches/BB/BB.md` wave-summary closer-gate column:

```sh
cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis -p bbnf-path --profile ax-iter   # 100% pass
test -f docs/tranches/BB/PROGRESS.md                                                    # PROGRESS.md lands
test -f docs/tranches/BB/FINAL.md                                                       # FINAL.md lands
test -f docs/tranches/BB/post-BB.json                                                   # bench archetype lands
jq '.gates.BB-G1, .gates.BB-G2, .gates.BB-G3, .gates.BB-G4' docs/tranches/BB/post-BB.json  # all four gates met
grep -c 'BB→BC.C' docs/tranches/BB/FINAL.md                                            # 4 (one per carry-tag)
xtask regen --check                                                                       # ≤ 25 s on M1 Pro
cargo bench -p bbnf -- css_l4_bootstrap                                                  # ≤ 3.5 ms (BB-G1 close)
cargo bench -p bbnf -- css_l4_tailwind                                                   # ≤ 35 ms (BB-G2 close)
cargo bench -p bbnf -- json_citm                                                         # ≤ 800 µs (BB-G3 close)
cargo bench -p bbnf -- json_canada                                                       # ≤ 3.0 ms (BB-G4 close)
```

All eleven conditions must pass; BC.W0 cannot dispatch without all closing.

## §4 Invariants

§I1. **All 13 locks** (`docs/HARDENING-PLAN-PROMPT.md:34-58`) — every lock cell in BB.md is populated. Wave-specific contribution: M7 verifies the cross-reference table closes; the Notes column flags carried-forward concerns (e.g., L7's BC.W4 `bbnf-regex` endpoint reconciliation, L5's BC.W0 IR contract codification).

§I2. **Lock 8** (`docs/HARDENING-PLAN-PROMPT.md:48`) — surpass sonic-rs / simdjson / lightning-css. Wave-specific contribution: BB-G1, BB-G2 surpass lightningcss bootstrap + tailwind; BB-G3, BB-G4 surpass sonic-rs citm + canada. Each gate names competitor + dataset + platform.

§I3. **Era V abrogation** (`docs/tranches/meta-audit/archaeology/era-V-dta-psi-rut.md:8-12`) — BB closes with no substrate-without-consumer; every wave's deliverable consumed in same-wave or next-wave per `docs/tranches/PLAN-INPUT-2026-05-03.md:285-297`. Wave-specific contribution: M0 verifies all wave-level closer-gates closed; FINAL.md records the same-wave / next-wave consumer pattern explicitly.

§I4. **Lane 6 generated-code budget** (`docs/HARDENING-PLAN-PROMPT.md:114-123`) — per-wave LOC delta tables emitted; aggregate net delta within forecast (-10% to -15%). Wave-specific contribution: M4 emits the per-grammar summary; the cohort -89% shrinkage dominates; specialised grammar +0% to +5% deltas are within budget.

§I5. **Carry-tag discipline** (`docs/HARDENING-PLAN-PROMPT.md:142-153` Lane 8) — every BB→BC carry names: receiving tranche, what blocks it, gate in receiving tranche. Wave-specific contribution: M5 documents each carry with owner-wave + consumer + BC entry-preflight check; no dangling carries.

## §5 Risks

| Risk | Likelihood | Detection | Mitigation |
|---|---|---|---|
| Perf gate BB-G2 (lightningcss tailwind) misses at BB close because tailwind is a 4-MB stress test that the W3 optimiser did not stabilise | Medium | M1 measurement reports > 35 ms | Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:55-58`, F4 routes to BC.W5 as a grammar-specific rule-discovery candidate; FINAL.md records the routing with named rewrite hypothesis; BC.W5 has a wave to close it. |
| The 13-lock cross-reference has an empty cell that M7 misses (a wave that touched a lock but didn't update the table) | Low | Manual inspection finds an empty cell | M7 uses a regex-driven detector (`grep -E '\|\s*\|\s*\|'`) to find empty cells; the gate is mechanical. |
| The post-BB.json archetype format differs from BC.W0 entry preflight expectation | Low | BC.W0 entry preflight fails to parse the archetype | Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:128-136`, the archetype format is documented in BB.md's build/iter time gate section; BC.W0 entry preflight checks `jq '.gates'` parse + structural conformance; the format is a stable contract. |
| FINAL.md grows beyond 200 LOC (scope creep into a verbose narrative) | Low | `wc -l docs/tranches/BB/FINAL.md` reports > 250 | Per voice lock §V3 of BB.md: state the deliverable, state the gate, move on. FINAL.md is a verification artefact, not a narrative. |
| Workspace nextest pass-rate regresses at M6 because a downstream consumer of `pointer!` macro broke after BB.W5's macro extension | Low | `cargo nextest run -p bbnf-path` reports failures | M6 runs the full four-crate suite; any failure halts the close; remediation is a same-wave fix-and-retry, not a deferred carry. |

## §6 Cross-references

- **BB-G gates this wave is on the path to closing**: ALL of BB-G1 through BB-G11 (the close verifies each).
- **Carry-tags this wave consumes**: ALL of BA→BB.C1 through BA→BB.C5 (the verification confirms each carry's consumer landed).
- **Carry-tags this wave produces**: BB→BC.C1, BB→BC.C2, BB→BC.C3, BB→BC.C4 (documented at M5).
- **Preceding wave dependency**: BB.W5 — sonic-class path API + visitor surface; the W5 close verifies macro + visitor, M0 verifies all prior wave artefacts.
- **Following wave consumer**: BC.W0 — BC opens; entry preflight reads `docs/tranches/BB/post-BB.json` + `docs/tranches/BB/FINAL.md` + the BB→BC carry-ledger from M5.

The aggregate carry-tag closure summary:

| Carry-tag | Owner-wave-in-BA | Consumer-wave-in-BB | Status at BB close |
|---|---|---|---|
| BA→BB.C1 | BA.W5 (JSON direct-to-struct) | BB.W1 (CSS L4 + BBNF + Sheets); BB.W2 (cohort) | CLOSED |
| BA→BB.C2 | BA.W2 (Layout/LayoutSink rename) | BB.W3 (referenced throughout optimiser pipeline) | CLOSED |
| BA→BB.C3 | BA.W4 (cursor + byte-skip unification) | BB.W2 (extends to all cohort) | CLOSED |
| BA→BB.C4 | BA.W3 (path-core crate) | BB.W5 (`pointer!` macro consumes path-core directly) | CLOSED |
| BA→BB.C5 | BA.W1 (grammar-agnostic bbnf-ir) | BB.W3 (optimiser pipeline references grammars by metadata key) | CLOSED |

The new carry-tags BB produces:

| Carry-tag | Owner-wave-in-BB | Consumer-wave-in-BC | Pre-condition |
|---|---|---|---|
| BB→BC.C1 | BB.W3 (output-piped optimiser pipeline) | BC.W0 (IR contract spec) | `W3-cost-model-pipe.md` lands |
| BB→BC.C2 | BB.W1 (direct-to-struct emit shape grammar-agnostic) | BC.W0 (IR contract spec) | post-BB.json shows 9-grammar uniform emit |
| BB→BC.C3 | BB.W5 (Visitor + VisitTypes bitflag pattern) | BC.W2 (TS + WASM emitter stubs implement the same Visitor surface) | `W5-visitor-coverage.md` lands |
| BB→BC.C4 | BB.W0 (sister crates as path-deps) | BC.W4 (sister crate API freeze) | `W0-incubator-metadata.md` lands |

## §7 Iter-time check

| Cargo Command | Expected Duration | Pass-Rate Target | Notes |
|---|---|---|---|
| `cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis -p bbnf-path --profile ax-iter` | ≤ 90 s | 100% | Workspace verification at M6. |
| `cargo bench -p bbnf -- css_l4_bootstrap css_l4_tailwind json_citm json_canada` | ≤ 12 s wall (combined) | gates met | Perf gate measurement at M1. |
| `xtask regen --check` | ≤ 25 s on M1 Pro | n/a | BB-close iteration-time gate. |
| `jq '.gates' docs/tranches/BB/post-BB.json` | ≤ 1 s | n/a | Archetype format verification. |
| `grep -c 'CLOSED' docs/tranches/BB/PROGRESS.md` | ≤ 1 s | returns 6 | Per-wave status verification. |

The W6 close commit body MUST include: actual perf-gate measurements; the 13-lock verification; the carry-tag enumeration; the LOC budget summary table. Per Lane 06, the W6 commit message is `tranche(BB): CLOSE — sonic-class generality, lightningcss surpassed`.

## §8 Verification artefacts

Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:128-136`, every BB wave emits per-wave audit artefacts the close commit body cites. W6's artefacts:

| Artefact | Path | Purpose | Consumer |
|---|---|---|---|
| `PROGRESS.md` | `docs/tranches/BB/` | Per-wave summary of W0-W5 + W6 close; each wave's status column at "CLOSED" with closer-gate verification + actual-measurement | BC.W0 entry preflight; the BB-tranche audit trail |
| `FINAL.md` | `docs/tranches/BB/` | The BB close artefact; 13-lock verification table; per-grammar LOC summary; perf-gate measurements; carry ledger to BC.W0 | BC.W0 + future tranche-level audits |
| `post-BB.json` | `docs/tranches/BB/` | Bench archetype with the four BB-G measurements + cohort grammar bench rows + visitor traversal measurements | BC.W0 entry preflight reads to confirm perf trajectory; BC.W5 final perf gates trajectory baseline |
| `W6-13-lock-cross-reference.md` | `docs/tranches/BB/audit/` | The expanded 13-lock table; every cell with citation to the wave that addressed it; Lane 1 (Lock-Adherence) gate evidence | Future Lane 1 audits |
| `W6-bc-carry-ledger.md` | same | The four BB→BC carry-tags with: owner-wave-in-BB, what-BC-consumes, BC.W0 entry preflight check; no dangling carries | BC.W0 entry preflight verifies each carry's pre-condition |
| `W6-perf-gate-narrative.md` | same | Per-gate narrative: target, measurement, SOTA anchor, mechanism by which the gate closed (or routed if missed); for any routed-to-BC.W5 gap, the named rewrite hypothesis | BC.W5 final perf gates inherit the routed gaps with explicit hypotheses |

## §9 Audit lane forecast

Per `docs/HARDENING-PLAN-PROMPT.md:60-153` Phase 3 hardening lanes, W6 anticipates:

| Lane | Anticipated challenge | W6 response |
|---|---|---|
| Lane 1 (Lock-Adherence) | "Is every lock cell populated in BB.md?" | M7 verifies the cross-reference table closes; the artefact `W6-13-lock-cross-reference.md` records each cell with citation to the wave that addressed it |
| Lane 2 (Sequencing-Discipline) | "Are there any waves with substrate-without-consumer at BB close?" | The §1.2 §3.2 sequencing check in `docs/tranches/PLAN-INPUT-2026-05-03.md:285-297` enumerates each wave's verdict as OK; W6 verifies the rank/tier same-wave consumer rule held at W3 |
| Lane 4 (SOTA-Anchoring) | "Are all four BB-G perf gates met?" | M1 measurements verify; if any gate misses, the artefact `W6-perf-gate-narrative.md` records the routing to BC.W5 with named rewrite hypothesis |
| Lane 5 (Grammar-Authoritative) | "Did BB introduce per-grammar leaks in supposedly-generic crates?" | M4 verifies the 13-lock table; L5 (IR + per-backend lower) cell cites W1 (generalises across 9 grammars) + W3 (rank/tier consumer) + W5 (visitor codegen); per-grammar code lives at the grammar-host layer or `generated/<g>.rs` |
| Lane 6 (Generated-Code Budget) | "Aggregate generated/ delta within forecast?" | M4 emits the per-grammar summary; cohort -89% shrinkage dominates; specialised +0% to +5% deltas are within budget; aggregate net delta is -10% to -15% per BB.md forecast |
| Lane 7 (Friction-Forecast) | "Are friction-forecast cookbooks committed?" | M0 verifies the W4 lifetime-surfaces cookbook + the W5 path-macro cookbook exist; per-grammar docstrings cite by path:line |
| Lane 8 (Carry-Audit) | "Are all BB→BC carries explicit?" | M5 verifies the four BB→BC carry-tags are documented; the artefact `W6-bc-carry-ledger.md` records each carry's owner-wave + consumer + BC entry-preflight check; no dangling carries |
| Lane 9 (Encapsulation-Audit) | "Did BB violate cohesive encapsulation?" | The per-wave Lane 9 responses verified cohesion at each step; W6 verifies aggregate cohesion: sister-crate path-deps (W0); 9-grammar uniform direct-to-struct emit (W1); cohort template emission (W2); per-domain optimiser crates (W3); three-surface API (W4); path-crate triplet + visitor codegen (W5) |

## §10 BC.W0 entry preflight

The BB.W6 close hands BC.W0 a precise entry preflight:

```sh
# BC.W0 entry preflight (run before BC.W0 dispatch)
test -f docs/tranches/BB/PROGRESS.md                                  # BB PROGRESS lands
test -f docs/tranches/BB/FINAL.md                                     # BB FINAL lands
test -f docs/tranches/BB/post-BB.json                                 # bench archetype lands
jq '.gates.BB-G1, .gates.BB-G2, .gates.BB-G3, .gates.BB-G4' \
   docs/tranches/BB/post-BB.json                                       # all four gates met (or routed to BC.W5)
test -f docs/tranches/BB/audit/W6-bc-carry-ledger.md                  # carry ledger lands
grep -c 'BB→BC.C' docs/tranches/BB/audit/W6-bc-carry-ledger.md        # 4 (one per carry-tag)
cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis -p bbnf-path     # 100% pass at BB close
xtask regen --check                                                     # ≤ 25 s on M1 Pro
test -f crates/ir/src/rewrites/rank.rs                                  # rank.rs landed at W3
test -f crates/ir/src/rewrites/tiering.rs                               # tiering.rs landed at W3
rg -n 'enum OpenFrame' crates/core/src/runtime/                         # zero hits (all retired)
find crates/core/src/runtime/{bnf,csv,ebnf,css_pretty,math} \
     -name '*.rs' | wc -l                                                # 10 (cohort templated)
test -f crates/path/src/path_macro.rs                                   # pointer! macro lands
```

All thirteen conditions must pass before BC.W0 dispatch. Per the BC tranche's entry-preflight discipline (sister-tranche concern; documented at BC.W0 not here), the BB→BC carry-tags are mechanically verified.

## §11 Implementation reference

The `post-BB.json` archetype shape:

```json
{
  "tranche": "BB",
  "close_date": "2026-MM-DD",
  "gates": {
    "BB-G1": {
      "target": "≤ 3.5 ms on bootstrap.css",
      "measurement": "<filled at W6>",
      "competitor": "lightningcss 4.16 ms",
      "anchor": "audit/SOTA-2026-05-03.md:131-136",
      "platform": "M1 Pro",
      "verdict": "MET / ROUTED-TO-BC.W5"
    },
    "BB-G2": { /* ... */ },
    "BB-G3": { /* ... */ },
    "BB-G4": { /* ... */ },
    "BB-G5": {
      "target": "≥ 1500 LOC saved across 5-grammar cohort",
      "measurement": "<filled at W6>",
      "anchor": "audit/CENSUS-2026-05-03.md:507-528"
    },
    "BB-G6": { /* ... */ },
    "BB-G7": { /* ... */ },
    "BB-G8": { /* ... */ },
    "BB-G9": { /* ... */ },
    "BB-G10": { /* ... */ },
    "BB-G11": { /* ... */ }
  },
  "loc_summary": {
    "json.rs": { "pre_BB": 2100, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "bbnf.rs": { "pre_BB": 20860, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "css_l4.rs": { "pre_BB": 104800, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "google_sheets.rs": { "pre_BB": 13800, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "css_pretty.rs": { "pre_BB": 8930, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "ebnf.rs": { "pre_BB": 7570, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "bnf.rs": { "pre_BB": 3257, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "csv.rs": { "pre_BB": 1676, "post_BB": "<filled>", "delta_pct": "<filled>" },
    "math.rs": { "pre_BB": 862, "post_BB": "<filled>", "delta_pct": "<filled>" }
  },
  "carry_tags_to_BC": [
    "BB→BC.C1", "BB→BC.C2", "BB→BC.C3", "BB→BC.C4"
  ],
  "lock_cross_reference_complete": true
}
```

BC.W0 entry preflight reads this archetype via `jq` to verify the gate measurements + the carry-tag enumeration. Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:128-136`, the format is a stable contract; the structural conformance is verified at BC.W0 dispatch.
