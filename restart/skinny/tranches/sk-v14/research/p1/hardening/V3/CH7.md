# CH7 — OVERFIT-PRUNE — S-P1 V3 Profile Pass (Pure Confirming)

Lens authority: `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87`
(CH7 Overfit-Prune lens definition); dispatch context
`restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CHALLENGE-CONTEXT.md:36`
(lens-overlay focus inherited V1 → V2 → V3); V1 disposition
`restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md`
(V1 ACCEPT 100 % / 7-of-7 subclauses); V2 disposition
`restart/skinny/tranches/sk-v14/research/p1/hardening/V2/CH7.md`
(V2 ACCEPT 100 % / 7-of-7 subclauses; **§3Z convergence-gate CLOSES on
CH7 standalone at V2**, recorded at V2 §0:42-43 and V2 §4.3:319-326).

V2 → V3 baseline commits:
- V2 light micro-redispatch: `069ba203c` (six P1 artefacts; +86 / −26).
- V2 hardening atomic commit: `4ad8f1949` (`docs(sk-v14-p1-hardening-V2):
  challenge V2 + consolidated`); current HEAD at V3 dispatch.
- V2 → V3 P1 artefact diff: `git diff 069ba203c 4ad8f1949 --
  restart/skinny/tranches/sk-v14/research/p1/p1{a,b,c,d,e,f}*.md`
  → **zero bytes**. The six P1 artefacts under CH7 review are
  bit-identical between V2 commit and V3 HEAD; only V2 hardening lens
  files (CH1..CH7 + V2 consolidated) were added between V2 and V3.

Discipline: WRITE-ONLY (aggregator commits). HARD CAP 30 min.

## §0 — Disposition summary

**Verdict: ACCEPT (7/7 lens-overlay subclauses; unchanged from V1 and
V2).**

V3 is a *pure-confirming* pass over unchanged V2 substrate. The five
V2 light fold packets (F-V2-METHODOLOGY-1, F-V2-P1A-MOVEMASK,
F-V2-P1C-LINEDRIFT, F-V2-P1E-1, F-V2-P1F-1) landed at commit
`069ba203c`; no further P1 artefact edits occurred at the V2
aggregator commit `4ad8f1949`. The four CH7 lens-overlay subclauses
plus the three supporting executable verifications are mechanically
re-runnable at V3 HEAD and produce **identical** outputs to V2.

CH7 already discharged the §3Z "≥95 % × 2 cycles, zero orphan
REVISEs" gate **standalone at V2** (V1=100 %, V2=100 %, zero REVISE
either cycle). V3 is supererogatory for CH7's own convergence-gate
discharge; this V3 pass exists to provide a third independent
confirming signal as the orchestrator runs the V3 cycle on the five
lenses (CH1, CH2, CH3, CH4, CH5) that still need their second ≥95 %
cycle. CH7 at V3 = **ACCEPT-UNCHANGED**, with **zero new findings**
beyond the §3.3 cargo-metadata cd-prefix hygiene note (already
flagged in V2 §3.3, carried below as §3.3 unchanged).

| # | Subclause | V1 verdict | V2 verdict | V3 verdict | Evidence (re-run at V3 HEAD `4ad8f1949`) |
| ---: | --- | --- | --- | --- | --- |
| 1 | P1-D Δ vs SK-V13 = −1.0 % c/B confirms zero-source-byte SK-V14 baseline | ACCEPT | ACCEPT | **ACCEPT** | `p1d-pmu-cycles.md:478` mean Δ c/B = −0.063 (−1.0 %); P1-D file unchanged between V2 commit `069ba203c` and V3 HEAD `4ad8f1949` (zero-byte diff). |
| 2 | `audit_overlay_verdict` column populated per row across all 6 P1 outputs | ACCEPT (258 cells) | ACCEPT (261 cells; +3 from F-V2-P1F-1) | **ACCEPT (261 cells)** | `grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' p1*.md` at V3 HEAD → p1a=13, p1b=72, p1c=11, p1d=67, p1e=37, p1f=61. **Sum = 13+72+11+67+37+61 = 261.** Identical to V2; no cell count drift. |
| 3 | No S-P1 finding re-introduces fake `@generated` / scaffold-as-load-bearing / gate-relabel pattern | ACCEPT | ACCEPT | **ACCEPT** | `git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/` returns 13 hits at V3 HEAD, **ALL inside `hardening/V{1,2}/CH7.md`** (V1 CH7 = 4 hits, V2 CH7 = 9 hits — both are diagnostic citations of the audit-pack pathology, not new admits). Excluding hardening dir: `git grep '@generated by skinny bbnf-codegen' -- ':!restart/skinny/tranches/sk-v14/research/p1/hardening/' 'restart/skinny/tranches/sk-v14/research/p1/'` returns **zero hits**. |
| 4 | 5 PRUNE-1 AUDIT-FALSIFIED rows (W14.1-5: numbers, citm_catalog, canada, marine_ik, mesh) consistently marked across all P1 outputs | ACCEPT | ACCEPT | **ACCEPT** | `grep -nE 'PRUNE-1\|W14\.[1-5]' p1{a,c,d,e,f}*.md \| wc -l` returns **47** at V3 HEAD (identical to V1 and V2). |

Aggregate ACCEPT-rate: **7/7 = 100 %** (4 explicit subclauses + 3
supporting executable verifications; unchanged from V1 and V2).
Zero CH7 REJECT triggers per `PASS-0-OVERFIT-AUDIT.md:86`.

V3 ΔACCEPT-rate vs V2: **0 pp** (both 100 %). V3 is the **third
consecutive cycle** at ≥95 % on CH7. The §3Z "≥95 % × 2 cycles, zero
orphan REVISEs" gate **remains closed at V3** (V1=100 %, V2=100 %,
V3=100 %, no REVISE orphaned at any cycle on this lens).

## §1 — Per-artefact CH7 audit table (V2 → V3 confirming delta)

Each row evaluates one P1 artefact against the four CH7 lens
subclauses at V3 HEAD. ✓ = subclause pass; n/a = subclause does not
apply to this artefact's plane. The "Δ vs V2" column reports the
byte-level change between V2 commit `069ba203c` and V3 HEAD
`4ad8f1949` on the artefact's own contents.

| Artefact | HEAD lines | Δ vs SK-V13 baseline cite | audit-overlay column | No fake-pattern recurrence | PRUNE-1 W14.* consistency | V3 verdict | Δ vs V2 |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| `p1a-samply-mode-1.md` | 343 | ✓ (§1.4 SK-V14 baseline cite unchanged at V3 HEAD) | ✓ (13 cells; identical to V2) | ✓ (`:207` "gate-relabel, not parser improvement" diagnostic carry preserved verbatim) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED; cells unchanged; movemask `(fn @ 169)` annotation from V2 packet preserved) | **ACCEPT** | unchanged (zero-byte diff) |
| `p1b-samply-mode-2.md` | 323 | ✓ (§1 SK-V14 baseline cite + §3 verdict tables unchanged) | ✓ (72 cells; identical to V2) | ✓ (`:321` "gate-relabel only; comparator misnamed" remains a citation of SYNTHESIS-AUDIT-OVERFIT §1) | n/a (JSON direct/typed plane; PRUNE-1 is parse_only; p1b direct + typed lanes' W14.* rows AUDIT-FALSIFIED literally unchanged) | **ACCEPT** | unchanged (zero-byte diff) |
| `p1c-samply-mode-3.md` | 616 | ✓ (§3 Δ vs SK-V13 header unchanged at V2-folded line position) | ✓ (11 cells; identical to V2) | ✓ (`:409` "gate-relabel, not parser delta" diagnostic carry preserved) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED; verdict literal unchanged; the 3 NEON line-anchor refreshes from F-V2-P1C-LINEDRIFT preserved at V3 HEAD) | **ACCEPT** | unchanged (zero-byte diff) |
| `p1d-pmu-cycles.md` | 669 | ✓ (§3 Δ table at `:431+`; `mean Δ c/B = −0.063 c/B (−1.0 %)` lexically unchanged) | ✓ (67 cells; identical to V2) | ✓ (PMC `unavailable_from_current_export` cite unchanged) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED in §2.1 parse_only table; row literals unchanged) | **ACCEPT** | unchanged (zero-byte diff) |
| `p1e-hot-leaf-attribution.md` | 321 | ✓ (V2 fold note at `:15` retains V14 baseline framing; §3 delta header unchanged) | ✓ (37 cells; identical to V2) | ✓ (`:17` Lock-14 mis-attribution classification preserved; F-V2-P1E-1 repointed 5 file:line cites but same primitive identities) | ✓ (5/5 W14.* rows present in §2.1 table; verdict literal AUDIT-FALSIFIED unchanged) | **ACCEPT** | unchanged (zero-byte diff) |
| `p1f-results-delta.md` | 269 | ✓ (`:4` "Baseline: SK-V14-open (audit-corrected SK-V13 close state)" unchanged) | ✓ (**61 cells**; identical to V2; +3 over V1 from F-V2-P1F-1 contracted-deferral framing at `:179-187` preserved) | ✓ (`:144` "gate-relabelled" S-substrate-guard classification unchanged; `:247` A2 F8 single-lane comparator fanout cite unchanged) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED at `:269` closure summary unchanged) | **ACCEPT** | unchanged (zero-byte diff) |

Per-artefact ACCEPT-rate: **6/6 = 100 %** (unchanged from V1 and V2).

V2 → V3 substrate delta: `git diff 069ba203c 4ad8f1949 --
restart/skinny/tranches/sk-v14/research/p1/p1{a,b,c,d,e,f}*.md` →
**zero bytes across all six artefacts**. Line counts in the table
above are HEAD `wc -l` (identical to V2 §1 HEAD line counts):
343 / 323 / 616 / 669 / 321 / 269 = 2541 total.

## §2 — Executable verification (re-run at V3 HEAD `4ad8f1949`)

All five executable-verification commands from V2 CH7 §2 (and the
four foundational commands from V1 CH7 §2) re-run verbatim at V3
HEAD; outputs recorded below.

### §2.1 cargo metadata grammar census (dispatch §36 directive)

```
$ cd /Users/mkbabb/Programming/bbnf-lang
$ cargo metadata --format-version 1 --no-deps | \
    jq -r '.metadata.bbnf.grammars[].ident'
bbnf
json
css_l4
css_pretty
google_sheets
ebnf
bnf
csv
math
```

**Expected**: 9 grammars including `css_pretty`. **Observed**: 9
grammars; `css_pretty` present (5th line). **PASS (identical to V1
and V2).**

Quantitative cross-check at V3 HEAD: `cargo metadata
--format-version 1 --no-deps | jq '.metadata.bbnf.grammars |
length'` returns **`9`** at V3 HEAD (identical to V1 and V2). The
grammar enumeration source is `Cargo.toml:18`
`[workspace.metadata.bbnf]` `grammars = [ ... ]` array (9 elements).

**Working-directory caveat (carried verbatim from V2 §2.1
methodology hygiene).** The same jq command run from inner
`skinny/` workspace returns `null` (not 9 grammars) because
`skinny/Cargo.toml` declares `[workspace.metadata.bbnf.grammars]` as
a **map** with only `json` keyed. The dispatch directive in the V3
dispatch packet explicitly anticipates this and instructs the V3 lens
agent to "run from repo root (NOT skinny/ subdir)". V3 reproduces
the 9-grammar list from repo root as instructed; the hygiene
recommendation from V2 §3.3 (annotate the verbatim command with
`cd /Users/mkbabb/Programming/bbnf-lang` prefix) remains a soft
enhancement for S-P3 / LOCKS.md adoption — not a CH7 violation, not
a V3 blocker.

### §2.2 Fake-pattern recurrence scan (dispatch §36 directive)

```
$ git grep '@generated by skinny bbnf-codegen' \
    restart/skinny/tranches/sk-v14/research/p1/ | wc -l
13
$ git grep -l '@generated by skinny bbnf-codegen' \
    restart/skinny/tranches/sk-v14/research/p1/
restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md
restart/skinny/tranches/sk-v14/research/p1/hardening/V2/CH7.md
```

**Expected (per dispatch directive)**: zero hits in P1 artefact
bodies. **Observed (excluding `hardening/` documentary citations)**:
**zero hits.** **PASS (logically identical to V1 and V2; the +9
count vs V2 is entirely attributable to V2 CH7 itself becoming a
new diagnostic-citation carrier in the hardening tree).**

Confirmation via:

```
$ git grep '@generated by skinny bbnf-codegen' \
    -- ':!restart/skinny/tranches/sk-v14/research/p1/hardening/' \
       'restart/skinny/tranches/sk-v14/research/p1/'
(exit 1; zero hits)
```

The 13 hits across `hardening/V1/CH7.md` (4 hits) and
`hardening/V2/CH7.md` (9 hits) are diagnostic citations of the A4
NEW-1 + NEW-2 fake-`@generated` recurrence vector enumerated at
`SYNTHESIS-AUDIT-OVERFIT.md:112-134`. **No V2 P1 artefact body
contains the fake-`@generated` header.** This V3 CH7 file
deliberately avoids the literal `@generated` token in code-fence
form to keep the V3 hits count from drifting; the textual reference
above is the necessary minimum diagnostic citation.

(NB: the V2 §4.2.1 "CI lint distinguishes documentary citation from
load-bearing claim" recommendation is reinforced here — both V1 CH7
and V2 CH7 documents now carry the literal string in code-fence
positions, and a naive CI grep would fire on either. The lint must
gate on "literal appears in `hardening/V{N}/CH{M}.md` documentary
context vs literal appears in any other research-tree position".)

### §2.3 audit-overlay column cell-count census

```
$ grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' \
    restart/skinny/tranches/sk-v14/research/p1/p1*.md
restart/skinny/tranches/sk-v14/research/p1/p1a-samply-mode-1.md:13
restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md:72
restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md:11
restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md:67
restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:37
restart/skinny/tranches/sk-v14/research/p1/p1f-results-delta.md:61
```

Total: **261 audit-overlay cell-references across 6 P1 outputs at
V3 HEAD** (identical to V2; V1 was 258 + 3 from F-V2-P1F-1
contracted-deferral framing paragraph at `p1f-results-delta.md:179-187`).

Cell counts identical V2 → V3 across all six artefacts:
13 / 72 / 11 / 67 / 37 / 61 → sum 261. **PASS — column population
preserved verbatim at V3.**

### §2.4 PRUNE-1 W14.\* row consistency

```
$ grep -nE 'PRUNE-1|W14\.[1-5]' \
    restart/skinny/tranches/sk-v14/research/p1/p1{a,c,d,e,f}*.md | wc -l
47
```

**47 hits at V3 HEAD; identical to V1 and V2.** Every parse_only-plane
P1 artefact (p1a, p1c, p1d, p1e, p1f) flags all 5 W14.\* rows
AUDIT-FALSIFIED with PRUNE-1 attribution. p1b carries the W14.\*
corpora rows in the direct + typed lanes where the same
comparator-misbinding pathology fires (per
`SYNTHESIS-AUDIT-OVERFIT.md:91-97` A2 F8 single-lane fan-out
structural cause), with AUDIT-FALSIFIED verdict on every row.

V2 → V3 cross-artefact consistency preserved verbatim (zero-byte
diff on every artefact body). **PASS (unchanged).**

### §2.5 SK-V14 NEW schema column gap audit (re-verified)

```
$ grep -nE 'comparator_plane|per_iter_equality|track2_entry_point' \
    restart/skinny/tranches/sk-v14/research/p1/p1*.md
p1b-samply-mode-2.md:321:  (cites SYNTHESIS §0.2 + §93 R1 + §241 (comparator_plane) + §255 (audit_overlay_verdict))
p1f-results-delta.md:28:  grep -c 'comparator_plane\|per_iter_equality\|audit_overlay_verdict\|track2_entry_point' ...
p1f-results-delta.md:172: | `comparator_plane` | R1 (per-plane strict comparator) | ...
p1f-results-delta.md:173: | `per_iter_equality` | R2 (per-iter equality oracle inside timing region) | ...
p1f-results-delta.md:175: | `track2_entry_point` | CH5 hidden-coupling guard | ...
p1f-results-delta.md:179: **Contracted-deferral framing (V2 reclassification — F-V2-P1F-1).** ...
p1f-results-delta.md:184: **CH5 V1 REVISE → V2 ACCEPT-WITH-NOTE conversion.** ...
p1f-results-delta.md:198: | `comparator_plane` | **Absent** (SK-V14 NEW; per §4.1) | 0/76 | gap |
p1f-results-delta.md:247: A2 F8 — single-lane comparator fanout. ...
p1f-results-delta.md:269: 4 NEW SK-V14 schema columns absent from RESULTS.md ...
```

Identical match set to V2 §2.5 (10 hits across 2 files: 1 in p1b,
9 in p1f). The F-V2-P1F-1 contracted-deferral framing block at
`p1f-results-delta.md:179-187` is preserved verbatim at V3 HEAD;
the four NEW SK-V14 schema columns remain assigned to the C-2 wave
deliverable (SYNTHESIS §3 C-2:272). **CH7 reads this exactly as V1
and V2 did: the gap is the target of R1/R2 + the audit-overlay
scribe (S-P3 wave manifest), not a re-introduced overfit pattern.
PASS (no CH7 violation).**

### §2.6 V2 → V3 substrate-stability verification (NEW V3 confirming check)

```
$ git diff 069ba203c 4ad8f1949 -- \
    restart/skinny/tranches/sk-v14/research/p1/p1{a,b,c,d,e,f}*.md \
    | wc -l
0
```

**Zero-byte diff across all six P1 artefacts** between V2 commit
`069ba203c` and V3 HEAD `4ad8f1949`. The CH7 substrate is
bit-identical V2 → V3; this V3 confirming pass is mathematically
guaranteed to reproduce V2 conclusions on every executable
verification, because the underlying files are byte-for-byte the
same.

V2 → V3 diff scope (8 files, +2415 / −0): exclusively the V2
hardening lens files themselves —

```
$ git diff --stat 069ba203c 4ad8f1949 -- \
    restart/skinny/tranches/sk-v14/research/p1/ | tail -10
 .../HARDENING-S-P1-V2-CONSOLIDATED.md | 294 +++++++++++++++
 .../hardening/V2/CH1.md               | 300 ++++++++++++++++
 .../hardening/V2/CH2.md               | 253 +++++++++++++
 .../hardening/V2/CH3.md               | 320 +++++++++++++++++
 .../hardening/V2/CH4.md               | 286 +++++++++++++++
 .../hardening/V2/CH5.md               | 243 +++++++++++++
 .../hardening/V2/CH6.md               | 326 +++++++++++++++++
 .../hardening/V2/CH7.md               | 393 +++++++++++++++++++++
 8 files changed, 2415 insertions(+)
```

**PASS — substrate stability confirms V3 is a faithful confirming
re-evaluation of V2, not a redress of new substrate edits.**

## §3 — Critical findings / observations

### §3.1 No new CH7 violations at V3 (zero-byte substrate)

The six V3 P1 outputs are byte-identical to V2 (§2.6 verification);
the audit overlay column population, PRUNE-1 W14.\* row consistency,
fake-`@generated` recurrence absence, and SK-V13 baseline cite are
all preserved verbatim. The dispatch context §36 four subclauses
re-run cleanly at V3 HEAD with identical outputs to V2.
**CH7 fires CLEAN at V3.**

### §3.2 V1 §3.2 RESULTS.md schema gap (mechanically closed at V2; verified preserved at V3)

V1 CH7 §3.2 flagged the **RESULTS.md schema gap** (76 rows × 3
absent columns = 228 missing cells); V2 CH7 §3.2 recorded the
mechanical closure via F-V2-P1F-1 (which extracted the gap into
`p1f-results-delta.md:179-187` as a C-2 wave deliverable). V3 re-runs
§2.5 against the unchanged P1-F file at V3 HEAD; the
contracted-deferral framing block remains in place verbatim with the
four NEW columns explicitly named. **V1 §3.2 closure preserved at
V3; no V3 work required.**

### §3.3 V2 §3.3 cargo-metadata cd-prefix hygiene (carried unchanged)

The dispatch directive in this V3 dispatch packet explicitly
addresses the V2 §3.3 hygiene observation by instructing the V3
lens agent to run "`cargo metadata --format-version 1 --no-deps |
jq -r '.metadata.bbnf.grammars[].ident'` from repo root (NOT skinny/
subdir)". V3 reproduces the 9-grammar list as instructed (§2.1
above). The underlying recommendation — that the verbatim
documentary form of the command in V1 / V2 CH7 §2.1 should be
prefixed with `cd /Users/mkbabb/Programming/bbnf-lang` or
`--manifest-path /Users/mkbabb/Programming/bbnf-lang/Cargo.toml` —
remains a soft enhancement for S-P3 / LOCKS.md adoption. The V3
dispatch packet operationalizes the workaround via instruction; the
documentary fix is still recommended for `LOCKS.md`. **Non-blocking;
carried verbatim from V2 §3.3.**

### §3.4 Co-derivation note on +1 grammar (css_pretty) — carried unchanged from V1 and V2

The cargo-metadata grammar list at V3 HEAD confirms `css_pretty` as
the 5th grammar (line 5 of jq output; identical position to V1 and
V2). A3, A5, A6, and cargo metadata cross-confirm the +1 over the
V13 audit-pack body's 8-directory enumeration. The S-P3 wave
manifest's PRUNE-4 sub-wave count of 9 (not 8) per
`SYNTHESIS-AUDIT-OVERFIT.md:282-291` rests on this co-derivation;
CH7 reads the four confirmations as **co-derived from the
css_pretty directory addition**, not four independent regression
signals. **Carried verbatim from V1 §3.3 and V2 §3.4; no change at
V3.**

### §3.5 NEW V3 observation (documentary hygiene; non-blocking)

The cumulative count of `@generated by skinny bbnf-codegen` literal
occurrences in `restart/skinny/tranches/sk-v14/research/p1/` has
grown from V1 (4 hits in `hardening/V1/CH7.md`) to V2 (13 hits;
+9 in `hardening/V2/CH7.md` documenting the V1 citations + new V2
diagnostic citations). This V3 CH7 file is authored to **avoid
adding further code-fence occurrences of the literal** so that V3
HEAD does not bump the count further; the §2.2 verification command
output above documents the cumulative state via wc -l + git grep -l
rather than re-quoting the literal inline.

This is the same hygiene point V2 §4.2.1 raises (CI lint must
distinguish documentary citation from load-bearing claim), now
empirically reinforced — each CH7 cycle that quotes the literal
verbatim in a code fence will mechanically grow the cumulative grep
count, creating a "wolf cried" pattern where the CI signal becomes
noisier over cycles. V3 demonstrates one workable mitigation:
**describe the literal in narrative prose; use code fences only for
the grep commands that produce the count, not for the literal itself
as bare content**. Future hardening cycles should adopt this
convention so the CI lint can gate on "code-fence content equals the
literal, *outside* a `grep`/`git grep` command line" as the precise
firing rule. Filed under §4.2 below; non-blocking on V3 ACCEPT.

## §4 — Aggregator / S-P2 fold recommendations

### §4.1 No CH7-driven revisions required at V3

The six V3 P1 artefacts are bit-identical to V2 and CH7-clean. The
V3 aggregator may consolidate the V3 hardening without per-artefact
CH7 redress. **CH7 V3 ACCEPT-UNCHANGED.**

### §4.2 Soft enhancements (non-blocking; carried + extended)

1. **§4.2.1 (carried from V1 §4.2.2 and V2 §4.2.1; reinforced).**
   Lift the §2.2 fake-pattern grep into a CI pre-commit check for
   the `restart/skinny/tranches/sk-v{N}/research/` glob, per the
   §2.4 CH7-companion lint already recommended by
   `SYNTHESIS-AUDIT-OVERFIT.md §2.4`. **V3 extension:** the V3 CH7
   author convention (describe the literal in narrative prose; use
   code fences only for grep commands, not for the literal as bare
   content) provides a precise firing rule for the lint: "literal
   appears in code-fence content that is NOT a `grep` / `git grep`
   command line, AND the file path is NOT
   `hardening/V{N}/CH{M}.md`". Recommend folding into LOCKS.md as
   the §2.4 companion lint already targets.

2. **§4.2.2 (carried from V2 §4.2.2).** Annotate the verbatim cargo
   metadata command with the working-directory prefix to prevent
   future agents from misreading the `null` from `skinny/` as a
   grammar regression. Two workable formulations:
   - `cd /Users/mkbabb/Programming/bbnf-lang && cargo metadata ...`
   - `cargo metadata --manifest-path /Users/mkbabb/Programming/bbnf-lang/Cargo.toml ...`

   The V3 dispatch packet operationalized this as a dispatch
   instruction; the documentary annotation in V1 / V2 / V3 CH7 §2.1
   should be updated in S-P3's LOCKS.md propagation step.

### §4.3 Convergence-gate impact (V3)

V3 CH7 ACCEPT-rate: **100 % (7/7 subclauses)**, identical to V1 and V2.

§3Z requires "≥95 % × 2 cycles, zero orphan REVISEs":
- **Cycle 1 (V1):** 100 % — MEETS threshold (zero orphan REVISEs).
- **Cycle 2 (V2):** 100 % — MEETS threshold (zero orphan REVISEs).
- **Cycle 3 (V3):** 100 % — MEETS threshold (zero orphan REVISEs).

**CH7 convergence-gate REMAINS CLOSED at V3** — three consecutive
cycles at ≥95 % with zero orphan REVISEs. V3 provides a redundant
third confirming signal; no V4 work required on this lens.

### §4.4 V3 / S-P2 readiness

The S-P1 V3 outputs are CH7-CONVERGED for §3Z on this lens (third
cycle ACCEPT-UNCHANGED). The remaining lenses' V3 confirming passes
(CH1, CH2, CH3, CH4, CH5) provide the second ≥95 % cycle they need
for their own §3Z discharge; CH6 and CH7 are already two-cycle-
discharged from V2 and are providing supererogatory third-cycle
confirmation at V3. **No CH7-driven blocker on S-P2 dispatch.**

The two soft enhancements in §4.2 (CI lint + command-portability
annotation) remain S-P3 / LOCKS.md follow-ups, not S-P2 entry-gate
items.

## §5 — Bibliography / cite roster

Files referenced (all paths absolute from repo root):

- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87` (CH7 lens
  definition)
- `restart/prompts/ORCHESTRATOR.md §3Z` (≥95 % × 2 cycles, zero
  orphan REVISEs; V max=5 ceiling)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CHALLENGE-CONTEXT.md:36`
  (V1 lens-overlay focus — inherited V1 → V2 → V3)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md`
  (V1 disposition; 100 %; 7/7 subclauses ACCEPT)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V2/CH7.md`
  (V2 disposition; 100 %; 7/7 subclauses ACCEPT; §3Z CLOSES on CH7
  standalone at V2 per V2 §0:42-43 + V2 §4.3:319-326)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/HARDENING-S-P1-V2-CONSOLIDATED.md`
  (V2 consolidated; records CH7 §3Z standalone closure at
  `:90`, `:204`)
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`
  (§0.1, §1.2, §2.4, §3.1, §3.3, lines 70-74, 91-97, 112-134,
  201-211, 282-291)
- `restart/skinny/tranches/sk-v14/research/p1/p1a-samply-mode-1.md`
  (V3 HEAD: 343 lines; zero-byte diff vs V2 commit `069ba203c`)
- `restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md`
  (V3 HEAD: 323 lines; zero-byte diff vs V2)
- `restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md`
  (V3 HEAD: 616 lines; zero-byte diff vs V2)
- `restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md`
  (V3 HEAD: 669 lines; zero-byte diff vs V2)
- `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md`
  (V3 HEAD: 321 lines; zero-byte diff vs V2)
- `restart/skinny/tranches/sk-v14/research/p1/p1f-results-delta.md`
  (V3 HEAD: 269 lines; zero-byte diff vs V2;
  `:179-187` F-V2-P1F-1 contracted-deferral framing preserved)
- `Cargo.toml:18` (`[workspace.metadata.bbnf]` `grammars`
  9-element array at repo root)
- V1 commit: `3510c1de5` (P1 six-axis); V1 hardening: `a3dfcaf38`.
- V2 commit: `069ba203c` (P1 light micro-redispatch; 6 files, +86/-26);
  V2 hardening: `4ad8f1949` (current HEAD).
- V2 → V3 P1 substrate diff: zero bytes across all six artefacts.

Executable verification commands (re-run at V3 HEAD; outputs quoted
inline at §2.1-§2.6):

```
cd /Users/mkbabb/Programming/bbnf-lang
cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'
cargo metadata --format-version 1 --no-deps | jq '.metadata.bbnf.grammars | length'
git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/ | wc -l
git grep -l '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/
git grep '@generated by skinny bbnf-codegen' -- ':!restart/skinny/tranches/sk-v14/research/p1/hardening/' 'restart/skinny/tranches/sk-v14/research/p1/'
grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' restart/skinny/tranches/sk-v14/research/p1/p1*.md
grep -nE 'PRUNE-1|W14\.[1-5]' restart/skinny/tranches/sk-v14/research/p1/p1{a,c,d,e,f}*.md | wc -l
grep -nE 'comparator_plane|per_iter_equality|track2_entry_point' restart/skinny/tranches/sk-v14/research/p1/p1*.md
git diff 069ba203c 4ad8f1949 -- restart/skinny/tranches/sk-v14/research/p1/p1{a,b,c,d,e,f}*.md | wc -l
git diff --stat 069ba203c 4ad8f1949 -- restart/skinny/tranches/sk-v14/research/p1/
wc -l restart/skinny/tranches/sk-v14/research/p1/p1*.md
```

All ten re-runnable verifications executed at V3 HEAD `4ad8f1949`.
Outputs quoted inline at §0, §2.1-§2.6, §3.5. V3 verdict:
**ACCEPT-UNCHANGED (7/7 subclauses; third consecutive ≥95 % cycle;
§3Z gate remains CLOSED on CH7 standalone).**
