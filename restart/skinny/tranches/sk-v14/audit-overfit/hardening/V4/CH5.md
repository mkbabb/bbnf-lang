# S-P0 CHALLENGE V4 — CH5 HIDDEN COUPLING

Lens: CH5 — HIDDEN COUPLING (per `restart/prompts/ORCHESTRATOR.md:87`).
Scope: the seven S-P0 artefacts at HEAD `f8e279877` — six STAND
artefacts unchanged from V3 (A1 `…css-measurement.md`, A2 `…admit-
mechanism.md`, A3 `…lock14-scan.md`, A4 `…generator-truth.md`, A5
`…decision-engine.md`, A6 `…pre-restart-pattern.md`) plus
`SYNTHESIS-AUDIT-OVERFIT.md` carrying the V4 micro-fold packet
(F-V4-SYNTHESIS-{1,2}). Charge per V1 `CHALLENGE-CONTEXT.md §CH-5`,
carried verbatim through V2 addendum §1 line 25 and V3 confirming-pass
into V4 as the *fourth consecutive* confirming pass under
`ORCHESTRATOR.md §3Z` two-consecutive-cycle rule (V2→V3 already
satisfied; V3→V4 reinforces): verify no parallel substrate, no Track
1 ≡ Track 2 collapse, no renamed-scanner Lock 1 violation; verify the
F-V2-A6-1 LegacyPath disambiguation still classifies the shim
correctly; fresh-finding scan against the V4 artefact deltas.

V3 baseline: 7 / 7 = 100 % ACCEPT (two ACCEPT-with-COUPLING-NOTE; zero
REVISE), matching V1 + V2 for three consecutive 100 % cycles. V4
expected ≥ 100 % — the V4 micro-fold packet's two edits
(F-V4-SYNTHESIS-1 inclusion-relation arithmetic correction
"42 including 14" → "56 (42 + 14) disjoint sets"; F-V4-SYNTHESIS-2
SYNTHESIS §3.1 C-3 row in-table cell "3 scanners" → "4 scanners")
target CH2 / CH7 carry-overs surfaced by V3 lens cross-flags, neither
of which is CH5-surface. Forward V3 binding from V2 CH5 §4 note 3
(totality-track lint glob extension post-C-1 PRUNE-4) carries through
V4 to S-P3 unchanged.

UTC dispatch start: 2026-05-23T09:14Z. Hard cap: 30 min.

## §0 — Disposition summary

| Per-artefact | V1 | V2 | V3 | V4 | Δ V3 → V4 |
| --- | --- | --- | --- | --- | --- |
| `SYNTHESIS-AUDIT-OVERFIT.md` | ACCEPT | ACCEPT | ACCEPT | ACCEPT | F-V4-SYNTHESIS-{1,2} are arithmetic precision (set-theoretic union correction + final residual in-table cell). Zero CH5 surface. |
| A1 `…css-measurement.md` | ACCEPT | ACCEPT (STAND) | ACCEPT (STAND) | ACCEPT (STAND) | No V4 fold |
| A2 `…admit-mechanism.md` | ACCEPT | ACCEPT (STAND) | ACCEPT (STAND) | ACCEPT (STAND) | No V4 fold |
| A3 `…lock14-scan.md` | ACCEPT | ACCEPT | ACCEPT (STAND) | ACCEPT (STAND) | No V4 fold |
| A4 `…generator-truth.md` | ACCEPT-with-COUPLING-NOTE | ACCEPT-with-COUPLING-NOTE | ACCEPT-with-COUPLING-NOTE | ACCEPT-with-COUPLING-NOTE | No V4 fold; A4 STAND verbatim from V3 |
| A5 `…decision-engine.md` | ACCEPT | ACCEPT | ACCEPT | ACCEPT (STAND) | No V4 fold |
| A6 `…pre-restart-pattern.md` | ACCEPT-with-COUPLING-NOTE | ACCEPT-with-COUPLING-NOTE | ACCEPT-with-COUPLING-NOTE | ACCEPT-with-COUPLING-NOTE | No V4 fold; A6 STAND verbatim |

**V4 ACCEPT-rate: 7 / 7 = 100 %.** Zero CH5 firings introduced by the
V4 micro-fold packet. The two ACCEPT-with-COUPLING-NOTE dispositions
(A4 §3.1 Track 1 / Track 2; A6 §3.3 substrate-doc opt-out
enshrinement) carry forward unchanged from V3 with the underlying
A4 / A6 artefacts byte-identical at HEAD `f8e279877` (V4 commit
modifies only SYNTHESIS-AUDIT-OVERFIT.md per `git show f8e279877
--stat` returning a single-file delta of +9/-4).

## §1 — V4 micro-fold contamination scan (CH5 charge-specific)

The V4 micro-fold packet at commit `f8e279877` lands two prose touches
in one file (per `git show f8e279877 --stat`: SYNTHESIS-AUDIT-
OVERFIT.md, +9 insertions / -4 deletions). CH5 verification per-fold
below.

### §1.1 V4 micro-fold per-fold verification table

Verified via `git diff f65d4dc78..f8e279877 -- restart/skinny/tranches/
sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`. One file modified,
+9 / -4 hunk-pair across §2.4 lines 308-318 and §3.1 line 344 (C-3 row
in-table cell).

| V4 fold | Edit summary | Substrate impact | CH5 firing? |
| --- | --- | --- | --- |
| **F-V4-SYNTHESIS-1** | SYNTHESIS §2.4 second-item parenthetical lines 308-318: replaces "42 files carry the fake header **including** 14 codegen-side files (8 providers+templates + 6 ancillary; `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/codegen/src/ \| wc -l = 14`)" with "56 files carry the fake header (42 runtime-side mirror + 14 codegen-side (8 providers+templates + 6 ancillary)) — **disjoint sets** per the lint glob brace-expansion `{runtime/src/grammars,codegen/src}/**/*.rs`; `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/runtime/src/grammars/ \| wc -l = 42` and `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/codegen/src/ \| wc -l = 14`, summing to the 56 total with zero overlap (the two path roots are siblings)." Re-executed both citations at HEAD `f8e279877`: `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/runtime/src/grammars/ \| wc -l` returns **42**; `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/codegen/src/ \| wc -l` returns **14**. The brace expansion `{runtime/src/grammars,codegen/src}` produces sibling roots whose intersection under the glob is empty by construction. Total: 56. | Set-theoretic inclusion-relation correction (CH2 N-V3-CH2-1 orphan). The V3-phrased "42 *including* 14" mis-stated a disjoint pair as nested; the V4 phrasing makes the disjointness explicit and republishes both citations. **The lint-glob mechanism is mechanically unchanged**: the brace `{runtime/src/grammars,codegen/src}` was already the glob in V3 prose at line 307; only the prose census aligns with the glob's actual union semantics. No NEW lint glob; no NEW path root; no NEW codegen-side or runtime-side file enumerated; no parallel pipeline; no NEW C-N candidate. The 56-file census broadens the V3 prose count from the codegen-only 14 to the glob-actual 42 + 14, but **every one of those 56 files was already inside the CH7-companion lint scope** (per V2 F-V2-SYNTHESIS-5 + V3 F-V3-SYNTHESIS-2). | **NO** |
| **F-V4-SYNTHESIS-2** | SYNTHESIS §3.1 C-3 prune-cluster row in-table cell at line 344 (per the V4 commit @@ -339,7 +344,7 @@ hunk header; line shifted 339→344 due to F-V4-SYNTHESIS-1 inserting +5 lines net into §2.4): "A4 findings 2 + 3 + 4 + 5 + 6 + 7 (no regen-css subcommand, **3** scanners are fixture lookups, 14/15 .bbnf orphan)" → "(no regen-css subcommand, **4** scanners are fixture lookups, 14/15 .bbnf orphan)". Re-executed at HEAD `f8e279877`: `grep -rln 'CANONICAL_FIXTURE\|CAPTURED_W2_INPUT' skinny/crates/codegen/src/` returns 4 files (`at_rules_and_media`, `nested_layout`, `vendor_and_custom_atrules`, `stylesheet_selectors` via `CAPTURED_W2_INPUT`). | Final-residual arithmetic-correction sweep landing (CH7 §1.5 inheritance-miss orphan from V3). The F-V2-SYNTHESIS-4 + F-V3-SYNTHESIS-3 sweep updated SYNTHESIS §0.1 + §0.2 + §1.1 line 81 + §1.2 line 122 + §5.1; the V3 inheritance-miss flag identified §3.1 line 342 (now 344) as the residual cell. V4 closes the sweep. **Same indictment**: 4 of 7 CSS scanners short-circuit via fixture-lookup constant tables, all targeted by PRUNE-2 wholesale deletion under C-3 (= R4 + R5). The "scanners" remain fixture-lookup constant tables (not Lock-1 renamed scanners); the C-3 wave envelope is unchanged; the C-3 row count (11 findings) is unchanged. No NEW sidecar producer; no NEW substrate; no NEW scanner-rename vector. | **NO** |

Both V4 micro-folds clear the CH5 charge: zero parallel substrate,
zero NEW sidecar producer, zero renamed-scanner Lock 1 violation,
zero Track 1 ≡ Track 2 collapse vector. The V4 edits are bounded to
the single file SYNTHESIS-AUDIT-OVERFIT.md and to the two specifically
enumerated regions (§2.4 lines 308-318 + §3.1 line 344); A1-A6 stand
byte-identical from V3.

### §1.2 Direct CH5 invariant re-execution

The four V1 CH5 §1 sub-vectors re-execute clean at HEAD `f8e279877`.

| Sub-vector | V1 method | V4 re-execution | Result |
| --- | --- | --- | --- |
| (1) Renamed-scanner / fallback combinator scan | `grep -rn 'fn parse_combinator\|combinator_fallback\|parse_with_fallback' crates/core/src/runtime/ skinny/crates/runtime/src/` | Re-executed at HEAD `f8e279877` | **Zero matches.** Lock-1-adjacent renamed-scanner audit CLEAN. |
| (2) LegacyPath bounded-leak scan | `grep -rln 'LegacyPath\|LegacySegment' skinny/crates` and `... crates/core/src/runtime/` | Re-executed at HEAD `f8e279877`. `skinny/crates` returns **zero matches**; `crates/core/src/runtime/` returns **4 files** (`json/parse_with.rs`, `css_l4/parse_with.rs`, `bbnf/parse_with.rs`, `google_sheets/parse_with.rs`) per V1 CH5 §1.2 expectation. | LegacyPath shim remains **bounded** to the 4 grammar-specific `parse_with.rs` files; zero leak into generic crates (`skinny/crates/{ir,codegen,passes,runtime}`). |
| (3) Substrate-union refusal token presence | `grep -rn 'UnionTape\|second tape\|public substrate API\|class/mask stream\|parser-owned cursor' restart/skinny/tranches/sk-v14/audit-overfit/` | Re-executed at HEAD `f8e279877`. Returns refusal-token mentions, all in REFUSAL / pre-block / guard context (zero authorisations). | Substrate-union refusal posture intact across SYNTHESIS + ledger artefacts. |
| (4) Track-collapse refusal scan | `grep -rcE 'Track 1 ≡ Track 2\|Track 1.*Track 2' restart/skinny/tranches/sk-v14/audit-overfit/` | Re-executed at HEAD `f8e279877`. Returns mentions in V1 CHALLENGE-CONTEXT, V1+V2+V3 CH5 disposition files, V1 CONSOLIDATED, V2 ADDENDUM — all in disjunction-preserving / refusal context. | Track 1 / Track 2 boundary semantically preserved across all artefacts. |

All four invariants hold. CH5 substrate-union closure is intact at
HEAD `f8e279877`. The V4 micro-fold packet (SYNTHESIS-only, +9/-4)
does not touch any source file under `crates/core/src/runtime/`,
`skinny/crates/`, or `crates/core/src/`; the four CH5 invariants
re-execute identically to V3 because the underlying source tree is
unchanged across the V3→V4 commit pair (`git diff 007624849..f8e279877
-- skinny/ crates/ | wc -l` returns 0).

### §1.3 F-V2-A6-1 LegacyPath disambiguation — V4 standing verification

The V2 CH5 §1.2 verification block (LegacyPath classification as
Lock-1-*adjacent* coupling subject to C-1 PRUNE-4 collapse) carries
forward verbatim at V4. A6 is a V4 STAND artefact (no V4 fold per
the V4 commit message stating "A1-A6 STAND verbatim (no V4 folds)");
`git show f8e279877 --stat` confirms only one file modified
(SYNTHESIS-AUDIT-OVERFIT.md), so A6's disambiguation paragraph at §0
line 12 + ledger row Status "NEW (scope-extension over V13 Pattern G;
not a reversal)" persists byte-identical from V3.

The V3 CH5 §1.3 disposition — (a) classification correctness
(scope-extension framing preserves both V13 carveout reading + V14
explicit survey); (b) substrate-union impact (Lock-1-adjacent, not
Lock-1 substrate-union violation; subject to C-1 PRUNE-4 collapse
target wired through SYNTHESIS §3.1 row C-1); (c) no Lock 1
substrate-union violation introduced (bounded to 4 `parse_with.rs`
files with zero leak into generic crates) — holds at V4 without
modification.

### §1.4 Cross-axis coherence — Track 1 / Track 2 boundary at V4

The V1 CH5 §3.1 + V2 CH5 §1.3 + V3 CH5 §1.4 observation (A4 NEW-1 /
R4 scope-extension as preserving Track 1 / Track 2 disjunction) carries
forward intact. Re-verified at HEAD `f8e279877`:

- A4 finding 16 LOW persists byte-identical from V3 (A4 is a V4 STAND
  artefact; `git diff 007624849..f8e279877 -- restart/skinny/tranches/
  sk-v14/audit-overfit/sk-v14-audit-overfit-generator-truth.md` returns
  empty). Track separation preserved verbatim under V4.
- F-V2-A4-1 "scope extension, not reversal" framing at A4 §0 + §3
  PRESERVES the Track 1 / Track 2 disjunction by indicting only the
  SKINNY-track providers (`json_provider.rs` + 7 CSS providers); the
  totality-track `xtask::regen::run` path remains untouched.

The V1 CH5 §4.1 binding note for S-P3 wave manifest (R4 specification
must encode Track 1 / Track 2 separation as a hard constraint,
attaching `regen-css-l4-<provider>` subcommands to `skinny/xtask/src/
main.rs:8` USAGE line only) carries forward unchanged into V4 → V5 →
S-P3.

### §1.5 F-V4-SYNTHESIS-1 set-theoretic broadening — special CH5 cross-check

The 42-including-14 → 42+14=56 (disjoint) arithmetic correction
(V4 commit message clarifies CH2 N-V3-CH2-1 inclusion-relation defect)
warrants a CH5 cross-check because the corrected census now explicitly
names 42 runtime-side mirror files as additional carriers of the
fake `@generated` header. CH5 must verify the 42 runtime-side files
are not introducing a NEW parallel-substrate vector beyond what V1/
V2/V3 already enumerated under Pattern H + Lock-14 indictment.

Re-executed: `git grep -l '@generated by skinny bbnf-codegen' skinny/
crates/runtime/src/grammars/` returns 42 files spanning the per-grammar
runtime mirror directories. Cross-referenced against A6 CRIT-1 + CRIT-2
(Pattern H 67 + 48 file census per SYNTHESIS §3.1 C-1 row) and A3 C1..
C11 / H1..H7 / M1..M5 / L1..L7 Lock-14 enumeration: the 42 runtime-
side mirror files are the **identical-content round-tripping vector**
that A4 finding 15 enumerates between codegen-side template and
runtime-side `generated.rs`. The runtime-side mirror is the
downstream of the per-grammar dispatch (A4 finding 9 indictment) and
collapses under C-1 PRUNE-4 (per-grammar runtime collapse onto
template / genuine codegen) per SYNTHESIS §3.1 row C-1.

| Census band | Files | CH5 substrate classification |
| --- | --- | --- |
| 42 runtime-side mirror files (`skinny/crates/runtime/src/grammars/**/*.rs` carrying the fake `@generated` header) | The downstream of the per-grammar dispatch; identical-content twin of the codegen-side template per A4 finding 15. | NOT a parallel substrate. They are the runtime-side mirror of the EXISTING codegen-side sidecar pipeline; PRUNE-4 collapses them wholesale per the SYNTHESIS §3.1 row C-1 (Lock-14 refactor cluster: per-grammar dispatch → generic registry; per-grammar runtime collapse onto template / genuine codegen). The 42 files were already inside the C-1 envelope; the V4 census-correction makes their inclusion in the lint-glob scope explicit but does not open a NEW C-N candidate. |
| 14 codegen-side files (`skinny/crates/codegen/src/**/*.rs` carrying the fake `@generated` header) | Already enumerated under V3 F-V3-SYNTHESIS-2; 7 CSS L4 providers + 1 JSON provider + 4 json_templates submodules + json_typed_direct + lib.rs. | NOT a parallel substrate (V3 CH5 §1.5 disposition verbatim). PRUNE-2 + PRUNE-3 + PRUNE-4 collapse targets per SYNTHESIS §3.1 C-1 + C-5. |
| Disjointness verification | The brace expansion `{runtime/src/grammars,codegen/src}` produces sibling roots; no path can match both branches because the first character after `skinny/crates/` is unique per branch (`runtime/` vs `codegen/`). | Verified by construction: empty intersection. The V4 prose now correctly reflects glob-actual union semantics (42 + 14 = 56, zero overlap). |

The 42 runtime-side mirror files map cleanly to the **existing** C-1
(PRUNE-4) collapse target per SYNTHESIS §3.1. The 56-file census
broadens the prose census surface (V3 had narrowed it to the 14
codegen-side only); the C-1..C-5 prune mapping is unchanged.
**No NEW C-N candidate, no NEW parallel substrate, no NEW pipeline,
no Lock 1 substrate-union violation surfaced by F-V4-SYNTHESIS-1.**
The CH7-companion lint glob — `{runtime/src/grammars,codegen/src}/
**/*.rs` — is mechanically unchanged from V3; the lint already
catches all 56 files; only the prose census aligns with the glob's
actual union semantics.

## §2 — Per-artefact V4 disposition table

| Artefact | V4 disposition | Δ from V3 |
| --- | --- | --- |
| `SYNTHESIS-AUDIT-OVERFIT.md` | ACCEPT | F-V4-SYNTHESIS-{1,2} are two prose touches: §2.4 lines 308-318 inclusion-relation arithmetic correction (CH2 N-V3-CH2-1 orphan), and §3.1 line 344 C-3 row in-table cell residual count (CH7 §1.5 inheritance-miss orphan from V3). Per §1.1 + §1.5 above: zero CH5 firing across both folds. The 74-finding aggregate + C-1..C-5 envelopes + 9-grammar census + Track 1/Track 2 disjunction + lint-glob brace-expansion mechanism all hold byte-identical to V3. |
| A1 `…css-measurement.md` | ACCEPT (STAND) | No V4 fold; V3 disposition carries unchanged. |
| A2 `…admit-mechanism.md` | ACCEPT (STAND) | No V4 fold; V3 disposition carries unchanged. |
| A3 `…lock14-scan.md` | ACCEPT (STAND) | No V4 fold; A3 STAND verbatim from V3. The 30-finding ledger (11 CRIT + 6 HIGH + 5 MED + 8 LOW per F-V2-A3-1 reclassification) holds; PRUNE-3 + R4 collapse path unchanged. |
| A4 `…generator-truth.md` | ACCEPT-with-COUPLING-NOTE | No V4 fold; A4 STAND verbatim from V3. V2 §3.1 Track 1 / Track 2 binding note carries forward verbatim. Coupling note unchanged in substance. |
| A5 `…decision-engine.md` | ACCEPT (STAND) | No V4 fold; A5 STAND verbatim from V3. F-V3-A5-1 + F-V3-SYNTHESIS-1 action-class propagation matrix completion preserved without modification. |
| A6 `…pre-restart-pattern.md` | ACCEPT-with-COUPLING-NOTE | No V4 fold; A6 STAND verbatim. F-V2-A6-1 LegacyPath disambiguation paragraph + §2 ledger row Status "NEW (scope-extension over V13 Pattern G; not a reversal)" persist byte-identical. CH5 §1.2 substrate-union classification (Lock-1-*adjacent*; bounded to 4 `parse_with.rs` files; subject to C-1 PRUNE-4 collapse) holds at V4. |

## §3 — Critical CH5 findings

### §3.1 No new critical findings under V4

V4 micro-fold inspection (`git diff f65d4dc78..f8e279877 -- restart/
skinny/tranches/sk-v14/audit-overfit/` shows one file modified +9/-4;
`git diff 007624849..f8e279877 -- skinny/ crates/ | wc -l` returns 0
confirming zero source-tree change across the V3→V4 commit pair)
confirms zero new CH5 firings introduced by the V4 packet. The two
V1+V2+V3 ACCEPT-with-COUPLING-NOTE observations (A4 §3.1 Track 1 /
Track 2; A6 §3.2 + §3.3 LegacyPath shim + substrate-doc opt-out
enshrinement) carry forward unchanged with the underlying A4 / A6
artefacts byte-identical at V4 (both STAND artefacts).

### §3.2 Fresh-finding scan — V4 micro-fold side-effects

Per the V4 CH5 dispatch task ("Verify no parallel substrate / Track 1
≡ Track 2 / renamed-scanner Lock 1. Fresh-finding scan."):
fresh-finding scan executed at HEAD `f8e279877`.

| Scan target | Method | V4 result |
| --- | --- | --- |
| New BIR / TypeDesc / TapeKind variant introduced by V4 packet | `git diff f65d4dc78..f8e279877 -- restart/skinny/tranches/sk-v14/audit-overfit/ \| grep -nE 'enum (BackendIr\|TypeDesc\|TapeKind)\|new variant'` | Zero hits. The V4 packet is prose-only; no enum, no variant, no type introduction. |
| New `<g>_provider.rs` introduced by V4 | `git diff f65d4dc78..f8e279877 -- … \| grep -nE 'provider.rs\|sidecar'` | F-V4-SYNTHESIS-1 mentions "providers+templates" in the corrected census parenthetical; this is the V3 8+6=14 codegen-side cite preserved verbatim. Zero new provider files. |
| New runtime-side mirror file enumerated by V4 census-correction | `git grep -l '@generated by skinny bbnf-codegen' skinny/crates/runtime/src/grammars/ \| wc -l` | Returns 42 — the identical-content twin of the codegen-side template per A4 finding 15. All 42 files were already inside C-1 PRUNE-4 collapse envelope per SYNTHESIS §3.1; the V4 census-correction makes their lint-glob inclusion explicit but does not enumerate a NEW substrate. |
| New `Lock-14`-leaking grammar identifier in nominally-generic crate | `git diff f65d4dc78..f8e279877 -- …` | Zero new symbols introduced. F-V4-SYNTHESIS-1 corrects census arithmetic (42 disjoint from 14, sum 56); F-V4-SYNTHESIS-2 refines existing count 3 → 4. Both are precision-only re-citations of existing surface. |
| New combinator-fallback scanner | `grep -rn 'fn parse_combinator\|combinator_fallback\|parse_with_fallback' crates/core/src/runtime/ skinny/crates/runtime/src/` | Zero matches (V4 re-execution; renamed-scanner Lock 1 audit CLEAN). |
| New regen pipeline conflation Track 1 ≡ Track 2 | A4 finding 16 + V1 §3.1 + V2 F-V2-A4-1 framing + V3 stand + V4 STAND | Track 1 / Track 2 disjunction preserved verbatim at HEAD `f8e279877`; the V4 SYNTHESIS-only edit does not touch A4 finding 16 (A4 is a V4 STAND artefact, byte-identical from V3). |
| New shadow CSP / parallel decision engine | No A5 V4 fold (A5 STAND); F-V3-A5-1 + F-V3-SYNTHESIS-1 action-class propagation matrix completion preserved | Zero NEW resolver path. The action-class widening "preserved through PRUNE-5 as gate-rejection invariant" carries forward verbatim across §5 closing + SYNTHESIS §3.1; the resolver clause (W5 → W6 → W7 → lowering via the five fail-closed checks at `codegen/src/lower/rust.rs:37-89`) is untouched. |
| New parallel-substrate vector from F-V4-SYNTHESIS-1 census correction | Per §1.5 above: the 42 additional runtime-side mirror files all map to existing C-1 (PRUNE-4) collapse target per SYNTHESIS §3.1; the 14 codegen-side files were already enumerated under V3 F-V3-SYNTHESIS-2. | Zero NEW C-N candidate, zero NEW pipeline. The census-correction is arithmetic precision (set-union vs set-inclusion); the C-1..C-5 prune mapping is unchanged. |
| New lint-glob mechanism | `git diff f65d4dc78..f8e279877 -- restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md \| grep -E '{|brace'` | The V4 prose explicitly cites the brace expansion `{runtime/src/grammars,codegen/src}/**/*.rs` (V3 §2.4 line 307 had the same brace expansion in the surrounding prose; the V4 edit makes the union semantics explicit). No NEW glob, no NEW path root, no NEW lint mechanism. |

The fresh-finding scan returns **zero new findings**. The V4 folds
are editorial-precision (arithmetic set-union correction, in-table
cell residual count); neither introduces a new substrate, sidecar,
scanner-rename, or track-collapse vector.

### §3.3 Forward V3-binding note (V2 CH5 §4 note 3) carries to V5 / S-P3 unchanged

V2 CH5 §4 note 3 specified that the CH7-companion lint glob must
extend to the totality-track `crates/core/src/runtime/**/*.rs` after
C-1 PRUNE-4 lands. The V4 packet did not pre-empt this extension —
F-V4-SYNTHESIS-1's census-correction touches only the skinny-track
brace expansion `{runtime/src/grammars,codegen/src}/**/*.rs` (sibling
roots inside `skinny/crates/`); the totality-track glob mechanism is
untouched. The V2 forward-binding obligation carries to V5 / S-P3
wave-close gate manifest unchanged. **Forward observation; not a V4
REVISE blocker.**

### §3.4 V4-specific observation — F-V4-SYNTHESIS-1 strengthens CH5 substrate-union closure

The arithmetic correction "42 *including* 14" → "42 + 14 = 56 disjoint"
is, beyond CH2's set-theoretic mandate, a **substrate-union refusal
strengthening** under CH5. The V3 phrasing tacitly suggested the
runtime-side mirror was a *subset* of the codegen-side enumeration —
which would have implied a single substrate generating both. The V4
correction makes explicit that the runtime-side mirror and the
codegen-side sidecar are **two disjoint file populations**, both of
which the C-1 Lock-14 refactor cluster collapses but via distinct
mechanisms (PRUNE-3 for codegen-side dispatch, PRUNE-4 for runtime-
side per-grammar collapse). The CH5 invariant that the audit refuses
a substrate-union (two parallel pipelines sharing one substrate) is
*reinforced* by the V4 correction: by making the disjointness
explicit, the V4 prose precludes the misreading that the runtime-side
mirror is silently part of the codegen-side substrate. **This is a
positive CH5 strengthening, not a regression.**

## §4 — V5 confirming-pass recommendations

The audit campaign at V4 closes with 100 % ACCEPT under the CH5 lens
(matching V1 + V2 + V3 = four consecutive 100 % cycles, exceeding the
`ORCHESTRATOR.md §3Z` two-consecutive-cycle convergence floor by two
full cycles). The V4 micro-fold packet's two edits clear the CH5
charge across all three sub-vectors (no parallel substrate, no
Track-collapse, no renamed-scanner Lock 1 violation) plus the
F-V2-A6-1 LegacyPath disambiguation continuity.

Four binding notes for V5 confirming-pass dispatch (per V4 commit
message: "V5 confirming pass closes §3Z chain at V max=5 ceiling"):

1. **V1 CH5 §4.1 + §4.2 + V2 CH5 §4 + V3 CH5 §4 forward bindings
   carry into V5 unchanged.** The R4 specification language must
   encode Track 1 / Track 2 separation as a hard constraint
   (`regen-css-l4-<provider>` subcommands attach to `skinny/xtask/
   src/main.rs:8` USAGE line only); PRUNE-4 must explicitly state
   the substrate-union closure target; the CH7-companion lint glob
   must extend to `crates/core/src/runtime/**/*.rs` after C-1 PRUNE-4
   lands.

2. **F-V4-SYNTHESIS-1 56-file census + disjointness establishes the
   correct CH5 census surface for V5.** Any V5 cross-flag against a
   "codegen-side N codegen files" count must use the disjoint-union
   semantics (42 runtime-side mirror + 14 codegen-side = 56 total,
   zero overlap) and not the V3-prose-narrowed 14 or the V3-prose-
   inclusive "42 including 14". The lint-glob mechanism scoping
   unchanged; only the prose census semantics align with the glob's
   set-union behaviour. **Forward CH5 cross-check obligation; not a
   V5 REVISE blocker.**

3. **F-V4-SYNTHESIS-2 arithmetic-correction sweep is closed.** The
   "3 of 7 → 4 of 7 CSS scanners short-circuit" residual cell sweep
   (initiated F-V2-SYNTHESIS-4, carried F-V3-SYNTHESIS-3, closed
   F-V4-SYNTHESIS-2) now spans all SYNTHESIS surfaces: §0.1, §0.2,
   §1.1, §1.2, §3.1, §5.1. V5 should re-verify by spot-grep
   (`grep -nE '3 scanners\|4 scanners' SYNTHESIS-AUDIT-OVERFIT.md`)
   that no further "3 scanners" residuals persist. **Forward
   observation; not a V5 REVISE blocker.**

4. **V4-introduced positive strengthening (per §3.4 above) locks the
   substrate-union refusal posture at V5.** The disjoint-set framing
   of the 42-vs-14 census precludes the misreading that the
   runtime-side mirror is a silent substrate of the codegen-side
   sidecar. V5 should confirm this framing persists in SYNTHESIS §2.4
   without regression to the V3 "including" phrasing. **Forward
   observation; not a V5 REVISE blocker.**

None of the four notes is a REVISE; all are forward-binding
clarifications. The CH5 lens advances without convergence blocker.

## §5 — Verdict

**CH5 — HIDDEN COUPLING V4: ACCEPT (7 / 7 = 100 %).**

The S-P0 audit campaign at SK-V14 V4 holds the CH5 lens without
firing. Both V4 micro-fold edits are verified editorial-precision
(set-theoretic union correction for F-V4-SYNTHESIS-1; in-table cell
residual count alignment for F-V4-SYNTHESIS-2); neither introduces a
parallel substrate, sidecar producer, renamed-scanner Lock 1
violation, or Track 1 ≡ Track 2 collapse vector.

The F-V2-A6-1 LegacyPath disambiguation (re-verified at HEAD
`f8e279877` against unchanged A6 STAND artefact) continues to
classify the shim as: (a) NEW finding by scope-extension over V13
Pattern G (not a reversal of V13's CLEAN disposition); (b) Lock-1-
*adjacent* coupling (not a Lock-1 substrate-union violation); (c)
subject to C-1 PRUNE-4 typed-path collapse with bounded scope (4
`parse_with.rs` files in `crates/core/src/runtime/`; zero leak into
`skinny/crates` verified via re-executed `grep -rln 'LegacyPath\|
LegacySegment' skinny/crates` returning zero matches).

The F-V4-SYNTHESIS-1 42+14=56 disjoint-set arithmetic correction
(the V4 fold with the broadest census-surface impact) introduces
zero NEW parallel-substrate vector — the 42 runtime-side mirror
files all map to the EXISTING C-1 (PRUNE-4) collapse target per
SYNTHESIS §3.1; the 14 codegen-side files were already enumerated
under V3 F-V3-SYNTHESIS-2; the disjointness is verified by
construction (brace expansion produces sibling roots `runtime/` vs
`codegen/` whose first-character disambiguation forces empty
intersection); the lint-glob mechanism is unchanged; the count
broadening aligns prose with mechanism without opening a NEW C-N
candidate. Beyond mere CH2 set-theoretic correctness, the correction
*reinforces* CH5's substrate-union refusal posture (per §3.4) by
making explicit that the runtime-side mirror and codegen-side sidecar
are two disjoint populations, both inside C-1 but via distinct
collapse mechanisms (PRUNE-3 vs PRUNE-4).

The F-V4-SYNTHESIS-2 in-table cell residual sweep closes the
F-V2-SYNTHESIS-4 + F-V3-SYNTHESIS-3 arithmetic-correction chain
across all SYNTHESIS surfaces (§0.1, §0.2, §1.1, §1.2, §3.1, §5.1);
the "4 of 7 CSS scanners short-circuit" indictment is now uniform.
The "scanners" remain fixture-lookup constant tables (not Lock-1
renamed scanners); PRUNE-2 wholesale-deletion target under C-3 is
unchanged.

The fresh-finding scan returns zero new coupling findings. The
V1+V2+V3 two ACCEPT-with-COUPLING-NOTE observations (A4 §3.1
Track 1 / Track 2 boundary; A6 §3.2 + §3.3 LegacyPath bounded +
substrate-doc opt-out enshrinement) carry forward unchanged at V4
(A4 and A6 are STAND artefacts byte-identical from V3).

Per `ORCHESTRATOR.md §3Z`, the CH5 lens convergence criterion is met
at V4 (100 % ACCEPT × 4 consecutive cycles V1 → V2 → V3 → V4; zero
open critical defects; zero orphan unresolved REVISE). The two-
consecutive-cycle requirement was already satisfied at V2 → V3 with
both at 100 %; V3 → V4 reinforces; the V5 confirming pass over
(expected) unchanged V4 artefacts will close the §3Z chain at the
V max=5 ceiling. **CH5 releases to G-S-P0-CONVERGED per the SK-V14
ORCHESTRATOR-PROMPT THE SK LOOP.**
