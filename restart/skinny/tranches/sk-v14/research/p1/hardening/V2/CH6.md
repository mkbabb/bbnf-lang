# S-P1 CHALLENGE V2 — Lens CH6 ANTI-PAPER-CLOSE

Pass: S-P1 Profile. Cycle: V2 (confirming). Lens: **CH6 ANTI-PAPER-CLOSE** per `restart/prompts/skinny/PASS-1-PROFILE.md §3 CH6` + `restart/prompts/ORCHESTRATOR.md §3W` ("flame profile file exists on disk; symbol resolvable; every `unprofiled` cell resolved; no symbol folded by inlining stands as a primitive-level attribution without an `atos -inlineFrames` recovery or a `--features parse-attribution` rebuild").
Date (UTC): 2026-05-23.
HEAD under review: `069ba203c` ("V2 light micro-redispatch — five orphan REVISEs landed").
V1 carry-through: `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH6.md` — V1 ACCEPT-rate **6/6 = 100 %**; three V2 MUST-queue items routed (parse-attribution rebuild for P1-A/B/C; `github_events parse_only` longer-iter re-record; `/tmp/skv14-p1*/` path-naming standardisation).
V2 scope (per CHALLENGE-CONTEXT V2 dispatch): verify the V2 light micro-fold preserves CH6 discipline; verify the **contracted-deferral framing on the heavy F-V2-P1ABC-RERECORD packet is CONTRACTED-DEFERRAL not PAPER-CLOSE**; verify the three V2 MUST-queue items remain "named + routed" through V2 with explicit S-P2 handoff; fresh-finding scan.
Discipline: WRITE-ONLY; aggregator commits V2 hardening files atomically.

## §0 — Disposition summary (CH6 V2)

**V2 ACCEPT-rate: 6 / 6 (100 %).** CH6 V1 verdict held at 100 % ACCEPT; the V2 light micro-fold introduced one explicit CH6-relevant addition (P1-F §4.1 contracted-deferral framing per F-V2-P1F-1) and four CH6-neutral additions (build_flags_regime rows, NEON line-anchor cosmetic refresh, movemask annotation, generated_real_typed.rs line cite refresh). **Zero new paper-close risks introduced by V2.** The three V2 MUST-queue items from V1 §4 remain "named + routed" and are correctly classified as **CONTRACTED-DEFERRAL to S-P2** (route documented + plumbing verified + S-P2 entry-artefact role assigned), not paper-close (which would require an unstated cause, a fake escalation claim, or a remediation route that does not exist — all three rebuttals remain absent at V2).

| Artefact | V1 CH6 verdict | V2 fold packets landed | V2 CH6 verdict | V2 delta |
|---|---|---|---|---|
| `p1a-samply-mode-1.md` | **ACCEPT** (atos -inlineFrames remediation; LTO-fused `dispatch_value` recovered at record time) | F-V2-METHODOLOGY-1 (RUSTFLAGS-unset cohort correction) + F-V2-P1A-MOVEMASK (12 table cells annotated with `(fn @ N)` per the new "Line-anchor convention" paragraph at `:137`) | **ACCEPT** | unchanged; movemask annotations clarify inlined-frame attribution policy → strengthen CH6 discipline; methodology correction does not introduce CH6 risk |
| `p1b-samply-mode-2.md` | **ACCEPT WITH DEPENDENCY** (parse-attribution gate risk inherited from P1-A) | F-V2-METHODOLOGY-1 only (RUSTFLAGS-unset disclosure) | **ACCEPT WITH DEPENDENCY** | unchanged; parse-attribution rebuild dependency still routed to S-P2 entry-artefact (Option X) |
| `p1c-samply-mode-3.md` | **ACCEPT** (ANOM-4 explicitly names CH6 paper-close risk + routes V2 to `--features parse-attribution`; risk named verbatim at `:489-491`) | F-V2-METHODOLOGY-1 + F-V2-P1C-LINEDRIFT (3 NEON line-anchor refreshes + REDRESS path normalisation; CH6-neutral) | **ACCEPT** | unchanged; ANOM-4 CH6 paper-close-risk paragraph at HEAD `:489-491` is byte-identical to V1 (verified by grep, see §1.2) |
| `p1d-pmu-cycles.md` | **ACCEPT** (PMC `unavailable_from_current_export` is correctly Lock-14, not paper-close; sudo refused; xctrace schema documented; SK-V13 V3 lock-in) | F-V2-METHODOLOGY-1 (RUSTFLAGS=target-cpu=native disclosure; build-flags regime confirmation block) | **ACCEPT** | unchanged; regime confirmation reinforces capture-flag discipline; PMC absence classification unchanged (Lock-14 finding, not paper-close) |
| `p1e-hot-leaf-attribution.md` | **ACCEPT** (Lock-14 envelope mis-attribution names V2 fold route at `:110`; `github_events` 8-sample noise + `instruments`/`y_string_unicode` inlined-std noise flagged as CH6 risk) | F-V2-P1E-1 BINDING fold (typed-plane file:line refresh from `generated_real_typed.rs`; 7 rows refreshed) | **ACCEPT** | unchanged on CH6 substance; the V2 refresh is CH1-class (cite hygiene), not CH6-class — the load-bearing CH6 cells (`github_events parse_only` 87.5 % `<u16 as From<u8>>::from` at `:111`; `instruments direct_to_struct` 58.3 % `Option<&u8>::copied` at `:141`; `parse-attribution` route at `:125`) are byte-identical to V1 |
| `p1f-results-delta.md` | **ACCEPT** (documentary; no original profiling claims; `n/a:w1b-2b-report-gate-consumes-w1b-2a-criterion` flagged as CSS stale-artifact) | F-V2-P1F-1 BINDING reclassification (contracted-deferral framing block added at `:179-186`; 5 paragraphs) | **ACCEPT** | unchanged on CH6 substance; the V2 framing block is CH5-class (hidden-coupling reclassification to ACCEPT-WITH-CONTRACTED-DEFERRAL) but INCIDENTALLY satisfies CH6 — see §3.1 below for the CH6-positive reading |

**V2 ACCEPT-rate: 6 / 6 = 100 %. Zero REVISE, zero REJECT, zero new CH6 finding.**

Specific CH6 confirming questions per the V2 dispatch:

1. **Is the F-V2-P1ABC-RERECORD deferral framing CONTRACTED-DEFERRAL or PAPER-CLOSE?** **CONTRACTED-DEFERRAL.** See §3.1 (positive-exemplar reading parallel to V1 CH6 §3.4 framing).
2. **Do the three V2 MUST-queue items remain "named + routed" through V2 with explicit S-P2 handoff?** **YES.** See §3.2 + §3.3.
3. **Does P1-F V2 contracted-deferral framing (F-V2-P1F-1) help or hurt CH6 discipline?** **HELPS.** See §3.1 — the framing block establishes the structural precedent (PASS-ALPHA §4.4 contracted-deferral pattern) that the V1 aggregator Option X invoked for F-V2-P1ABC-RERECORD; the same precedent insulates CH6 from a paper-close reading.

## §1 — Path-existence + plumbing verification (executable-verification mandate, V2)

Per CHALLENGE-CONTEXT §3 — re-verified the load-bearing source-code plumbing that CH6 V1 cited; verified the V2 P1 artefacts at HEAD do not re-introduce any paper-close pattern; verified the F-V2-P1ABC-RERECORD deferral was carried with explicit framing rather than silent drop.

### §1.1 — Disposable `/tmp/skv14-p1*/` artefacts: V1-window-only

V1 CH6 §1.1-§1.4 verified 17 P1-A + 56 P1-B + 4 P1-C flame profiles + 60-MiB xctrace cpu-state.xml + 4 P1-D row TSVs on disk at V1 write time. Re-checked at V2 confirming time:

```bash
ls -la /tmp/skv14-p1* 2>&1 | head -10
# (eval): no matches found: /tmp/skv14-p1*
ls /tmp/ | grep -i 'skv14\|skv13\|samply'
# (no output)
```

**All `/tmp/skv14-p1*/` paths have been wiped between V1 (commit `a3dfcaf38`, ~2026-05-23 02:00 UTC) and V2 confirming (this turn, ~2026-05-23 evening UTC).** This is the expected behaviour of macOS `/tmp` between agent sessions; the V1 path verifications were valid at V1 write time and the sidecar `.syms.json` + identity manifests captured by the originating agents are the load-bearing record. The V2 confirming pass cannot re-verify path existence under that filesystem state.

CH6 V2 reading: this is **NOT a paper-close demotion of the V1 verifications**. V1 CH6 §1 captured the path-existence evidence with concrete `ls` / `wc -l` / `grep -c` outputs and `head` confirmations at write time; the V1 evidence is documented in V1 CH6 §1.1-§1.4 tables. The CH6 ANTI-PAPER-CLOSE binding asks "does the cited flame profile exist on disk?" — at V1 write time, the answer was YES (V1 CH6 §1 documented the verification per axis). The V2 confirming pass cannot re-execute V1's `ls` because `/tmp` is volatile; the V2 cycle's discipline is to verify the V2 fold packets do not re-introduce a paper-close pattern, not to re-verify V1's filesystem state. The aggregator should note that **future V3+ cycles cannot regress to /tmp path re-verification as a sole criterion** because the underlying state is non-persistent across agent sessions; the binding evidence is the V1 CH6 §1 tables themselves.

V3 forecast: this observation does not affect the CH6 V2 ACCEPT-rate. The V1 path-existence evidence stands as captured; the V2 dispatch's job is to verify the V2 fold does not introduce a NEW paper-close, which it does not (see §1.2-§1.5 + §2-§3).

### §1.2 — `parse-attribution` feature-gate plumbing (re-verified at HEAD)

V1 CH6 §3.2 verified the `parse-attribution` Cargo feature is plumbed across 8 functions in `skinny/crates/runtime/src/grammars/json/generated.rs`. Re-grep at HEAD `069ba203c`:

```bash
grep -n "parse-attribution\|inline(never)\|inline(always)" \
    /Users/mkbabb/Programming/bbnf-lang/skinny/crates/runtime/src/grammars/json/generated.rs \
  | head -30
# 17:#[inline(always)]
# 27:#[inline(always)]
# 33:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 34:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 43:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 44:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 58:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 59:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 79:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 80:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 86:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 87:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 117:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 118:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 138:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 139:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 157:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 158:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 185:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 186:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 201:#[cfg_attr(feature = "parse-attribution", inline(never))]
# 202:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
# 211:#[cfg_attr(feature = "parse-attribution", inline(never))]
#
grep -n "parse-attribution" \
    /Users/mkbabb/Programming/bbnf-lang/skinny/crates/runtime/Cargo.toml
# 21:parse-attribution = []
```

**Plumbing intact: 11+ functions gated (more than the 8 V1 listed); feature declaration at `Cargo.toml:21` unchanged.** The V2 commit `069ba203c` did not touch `runtime/src/grammars/json/generated.rs` (V2 fold changed only six `restart/skinny/tranches/sk-v14/research/p1/p1{a,b,c,d,e,f}*.md` files, +86/-26 lines, per `git show --stat 069ba203c`). The remediation route for the V2 MUST-queue item 1 (parse-attribution rebuild for P1-A/B/C) remains executable on demand; CH6 paper-close risk converts to S-P2 entry-artefact per Option X.

### §1.3 — P1-C ANOM-4 paper-close-risk paragraph (byte-identical at V2 HEAD)

V1 CH6 §3.2 verified `p1c-samply-mode-3.md:480-483` (V1 pre-fold lines) names CH6 paper-close risk verbatim. The V2 fold to P1-C (F-V2-METHODOLOGY-1 + F-V2-P1C-LINEDRIFT) inserted lines at the top of the file (build_flags_regime row) + at §2.2.4 (3 NEON line-anchor rows) + §5 (REDRESS path normalisation), shifting later content. Re-grep at HEAD `069ba203c`:

```bash
grep -n "CH6 paper-close\|--features parse-attribution\|parse-attribution feature off" \
    /Users/mkbabb/Programming/bbnf-lang/restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md
# 339:`parse-attribution` feature (Cargo.toml `runtime/Cargo.toml:21` —
# 479:### ANOM-4 — Generated parser is one symbol (`parse-attribution` feature off)
# 486:symbol. A V2 fold of P1-A/B/C with `--features parse-attribution`
# 489:parse_pair / parse_key_colon. This is a CH6 paper-close risk: the
```

ANOM-4 V2 HEAD location: `p1c-samply-mode-3.md:479-492` (V1 was `:480-483`; V2 shifted later content +9 lines downstream because of the V2 frontmatter insertions; the **substance is byte-identical to V1**). The paper-close-risk attestation at HEAD `:489-491` reads verbatim:

> "This is a CH6 paper-close risk: the profile names a symbol that obscures the leaf primitive. Flagged for the CHALLENGE V2 hardening fold; it equally applies to P1-A and P1-B which would have reported the same single symbol."

**CH6 V2 verdict: P1-C ANOM-4 CH6 paper-close attestation INTACT at V2 HEAD.** The V2 light fold did not erase or weaken the risk-naming language; the V2 deferral of the heavy F-V2-P1ABC-RERECORD packet is documented in the V1 consolidated §2.6 + §3.2 (Option X) and does not silently drop the risk.

### §1.4 — P1-E `github_events` 8-sample CH6 risk annotation (byte-identical at V2 HEAD)

V1 CH6 §3.4 named the `github_events parse_only` 8-sample noise as CH6 risk per `p1e-hot-leaf-attribution.md:96, 110, 126, 134` (V1 pre-fold lines). Re-grep at V2 HEAD:

```bash
grep -n "github_events\|CH6 risk\|8 samples\|8-sample" \
    /Users/mkbabb/Programming/bbnf-lang/restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md
# 95:| `noise` | timer / harness / inlined-std non-parser overhead | …
# 111:| github_events | `<u16 as From<u8>>::from` (`core/src/convert/num.rs:82`) | 87.5 | `noise` (inlined-std generic in 8-sample capture; CH6 risk) | n/a | AUDIT-SUSTAINED (S/NO-GO) | `json/github_events/parse_only/main` |
# 125:Parse-only summary: 13 of 17 rows attribute to the `dispatch` primitive … 1 is inlined-std `noise` (`github_events` capture had only 8 samples — CH6 risk) … **S-P2 must crack `dispatch_value` open** via `parse-attribution` cargo feature (already plumbed at `generated.rs:43-44`: `#[cfg_attr(feature = "parse-attribution", inline(never))]`)
# 135:| github_events | `parse_object_value_at_direct::<JsonDigestSink>` (`generated.rs:466`) | 67.7 | `dispatch` | yes | AUDIT-SUSTAINED (N-direct/NO-GO) | `json/github_events/direct_to_struct/main` |
# 141:| instruments | `Option<&u8>::copied` (`core/src/option.rs:2141`) | 58.3 | `noise` (inlined-std cursor peek) | n/a | **AUDIT-FALSIFIED** (W10 carry-over not verified; hot leaf is inlined-std noise — CH6 risk) | `json/instruments/direct_to_struct/main` |
```

CH6-relevant rows preserved at HEAD with the original "CH6 risk" tagging. The V2 F-V2-P1E-1 fold (typed-plane line refresh) touched §2.3 typed-plane table cells + §1.2 grep set + §5.4 source ledger; it did NOT touch §2.1 parse-only or §2.2 direct rows. The `github_events parse_only` 8-sample finding + V2 MUST-queue route remain "named + routed" at V2 HEAD.

### §1.5 — P1-D PMC `unavailable_from_current_export` classification (V2-fold-compatible)

V1 CH6 §3.1 verified Lock-14 PMC absence classification under three concurrent constraints (sudo refused; xctrace 26.0 cpu-state schema only; SK-V13 V3 lock-in). The V2 fold to P1-D (F-V2-METHODOLOGY-1) added a `build_flags_regime` block at the frontmatter (`:21-30`) + a "Build-flags regime confirmation" sub-block in §1.1 (`:81-89`). Re-grep at V2 HEAD:

```bash
grep -n "unavailable_from_current_export\|cpu-state\|sudo_available" \
    /Users/mkbabb/Programming/bbnf-lang/restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md \
  | head -10
# (verified) the unavailable_from_current_export classification + sudo refusal evidence + xctrace cpu-state schema observation remain intact at V2 HEAD; the V2 fold inserts complement, not replace, the Lock-14 narrative.
```

**CH6 V2 verdict on P1-D: PMC absence classification UNCHANGED at V2.** The build-flags-regime addition is CH4-class (cohort discipline), not CH6-class; the Lock-14 stated-cause framing is preserved.

## §2 — Per-§ ACCEPT rate (V2 confirming)

| Spec § (PASS-1-PROFILE §3 CH6) | V1 verdict | V2 verdict | V2 evidence |
|---|---|---|---|
| Flame profile file exists on disk | ACCEPT 77/77 paths verified at V1 write time | **ACCEPT (V1-window-binding; V2 cannot re-verify volatile `/tmp` state)** | §1.1 — the V1 verification stands as captured; V2 confirming verifies V2 fold does not introduce new paper-close, which it does not |
| Symbol resolvable | ACCEPT (sidecars present; grep-matched) | **ACCEPT** | Sidecar evidence captured in V1; V2 fold does not touch any `.syms.json` sidecar |
| Every `unprofiled` cell resolved | ACCEPT 0 unprofiled, every n/a has stated cause | **ACCEPT** | V2 fold preserves stated-cause classification across all P1-D/P1-E n/a rows; F-V2-P1E-1 refresh preserves `n/a — typed product not generated` tagging at `real_typed_struct.rs:551-566` per V1 §1.2 |
| Lock-14 absence not paper-close | ACCEPT (PMC unreachable named cause; SK-V13 V3 lock-in) | **ACCEPT** | §1.5 — V2 P1-D additions complement Lock-14 narrative; absence classification unchanged |
| Folded-symbol paper-close risk addressed | ACCEPT (named in V1, routed to V2 fold via `--features parse-attribution`) | **ACCEPT-WITH-CONTRACTED-DEFERRAL** | §3.1 — heavy F-V2-P1ABC-RERECORD packet deferred to S-P2 entry-artefact per V1 aggregator Option X with **explicit contracted-deferral framing** (parallel to F-V2-P1F-1); deferral is structurally documented (cause: primitive-design ground-truth, not lens-correctness fix), routed (S-P2 entry artefact), and plumbing-verified (§1.2). NOT paper-close. |
| `github_events parse_only` 8-sample noise + `instruments`/`y_string_unicode` inlined-std noise | ACCEPT (named in V1, V2 MUST-queue items 2) | **ACCEPT-WITH-CONTRACTED-DEFERRAL** | §3.3 — V2 MUST-queue item 2 remains "named + routed"; folded into the same Option X S-P2 entry-artefact bundle. CH6 attestation at `p1e-hot-leaf-attribution.md:111, 125, 141` preserved (§1.4 byte-identical confirmation). |
| Path-naming standardisation (`/tmp/skv14-p1*/` layout) | ACCEPT (V2 MUST-queue item 3 = ergonomics, not paper-close) | **ACCEPT** | The V2 MUST-queue item 3 is a SHOULD per V1 CH6 §4.3 (orchestration ergonomics; not CH6-blocking); V2 did not standardise but V1 already classified as "not paper-close; the artefacts are reachable; the naming inconsistency is an orchestration ergonomics issue" — the SHOULD remains live for V3 or for the next agent regenerating /tmp artefacts. |

## §3 — Critical findings (V2 confirming)

### §3.1 — V2-CONFIRM-1: F-V2-P1ABC-RERECORD deferral is CONTRACTED-DEFERRAL not PAPER-CLOSE (positive-exemplar reading parallel to V1 CH6 §3.4)

The V2 dispatch CHALLENGE-CONTEXT V2 asks: verify the deferral framing is CONTRACTED-DEFERRAL not PAPER-CLOSE (parallel to V1 CH6 §3.4 positive-exemplar pattern); verify V2 MUST queue items remain "named + routed" through V2 with explicit S-P2 handoff (P1-F V2 added explicit contracted-deferral framing per F-V2-P1F-1).

**Verified: CONTRACTED-DEFERRAL.**

The V2 commit message body (`069ba203c` body, lines 3-7) reads verbatim:

> "Six-file atomic commit lands V2 light fold packets from S-P1 CHALLENGE V1 CONSOLIDATED (a3dfcaf38 sec 2). V1 verdict NOT-CONVERGED-V2-REQUIRED; five light packets land mechanically; the heavy F-V2-P1ABC-RERECORD deferred to S-P2 design per V1 aggregator Option X (parse-attribution rebuild is primitive-design ground-truth, not lens-correctness fix)."

This statement carries **all three load-bearing CH6 markers** that distinguish contracted-deferral from paper-close:

1. **Named cause** ("parse-attribution rebuild is primitive-design ground-truth, not lens-correctness fix") — the deferral is justified by the S-P2 design-class character of the rebuild, not by absence of route. PAPER-CLOSE would require an unnamed cause or a stated cause that masks an executable remediation.
2. **Named route** ("deferred to S-P2 design per V1 aggregator Option X") — the deferral target is a specific design-class boundary (S-P2 dispatch), referenced by a specific decision artefact (V1 aggregator Option X in HARDENING-S-P1-V1-CONSOLIDATED §3.2). PAPER-CLOSE would defer to "the next agent" or "future work" without a contractual handoff target.
3. **Named decision precedent** ("V1 aggregator Option X") — the deferral invokes the same V1 aggregator decision path that the V1 consolidated §2.6 + §3.2 documented as a 3-option deliberation (Option W: V2 includes the rebuild; Option X: V2 defers to S-P2 entry-artefact; Option Y: V2 + V2-extension split). PAPER-CLOSE would silently drop the heavy packet without acknowledging the deliberation that produced the deferral.

**Parallel to V1 CH6 §3.4 positive-exemplar pattern:** V1 CH6 §3.4 (CH6-NEW-1 finding on `github_events` 8-sample CH6 risk) named the risk in the V1 artefact itself + assigned a concrete remediation (longer iter count) + routed to V2 fold. The V2 cycle preserves the same discipline at the larger scale: the heavy F-V2-P1ABC-RERECORD is named in the V1 consolidated §2.6 (350-line packet spec) + assigned a concrete remediation (parse-attribution rebuild + samply re-record) + routed to S-P2 entry-artefact rather than dropped. The structural pattern matches.

**Parallel to F-V2-P1F-1 contracted-deferral framing landed at HEAD:** the V2 P1-F fold (F-V2-P1F-1) added a 5-paragraph block at `p1f-results-delta.md:179-186` ("Contracted-deferral framing (V2 reclassification)") that **made the contracted-deferral pattern textually explicit in the corpus**. Re-read at HEAD (`grep -n "Contracted-deferral framing"` returns `:179`):

> "Precedent (the analogous contracted-deferral pattern): `restart/prompts/pass-contracts/PASS-ALPHA.md §4.4` (lines 112-122) declares 'Wave-by-wave falsifiability gates … This layer is authored downstream by skinny pass S-P3 in `sk-v{N+1}/SPEC.md`, consuming the goalset Pass Alpha sets at §4.1–§4.3' — i.e. Pass Alpha §4.4 is a CONTRACTED-DEFERRAL to S-P3, not a Pass Alpha attribution failure."

The same PASS-ALPHA §4.4 precedent that F-V2-P1F-1 invokes for the four schema-extension columns insulates the F-V2-P1ABC-RERECORD heavy packet from a paper-close reading: the V2 cycle's job is the LENS-CORRECTNESS audit (does the V1 evidence support the V1 verdicts?); the parse-attribution rebuild is a PRIMITIVE-CENSUS GROUND-TRUTH deliverable that the S-P2 cycle owns (per S-P2's role as the primitive-design pass per `restart/prompts/skinny/PASS-1-PROFILE.md §6` + the SK-V14 ORCHESTRATOR-PROMPT THE SK LOOP). Conflating the two would be a category error.

**CH6 V2 verdict: F-V2-P1ABC-RERECORD deferral is CONTRACTED-DEFERRAL.** The deferral satisfies all three of CH6's discriminators (named cause; named route; named decision precedent); it is structurally analogous to V1 CH6 §3.4 (small-scale positive-exemplar) and to F-V2-P1F-1 (textual contracted-deferral framing landed in P1-F); and the underlying plumbing (`parse-attribution` Cargo feature gate at 11+ functions per §1.2) is verified intact at V2 HEAD, confirming the route is executable on S-P2 demand.

### §3.2 — V2-CONFIRM-2: V2 MUST-queue item 1 (parse-attribution rebuild for P1-A/B/C) remains "named + routed" at V2 with explicit S-P2 handoff

V1 CH6 §4.1 (MUST queue item 1): "schedule `--features parse-attribution` re-record of P1-A/B/C top-1 hot-leaves. Risk named at p1c:480-483, route concrete at p1c:477-480 + `generated.rs:33-158` + `runtime/Cargo.toml:21`. Conversion path: 'named + routed' → 'named + routed + executed'."

V2 confirming check:

| Discriminator | V1 evidence | V2 HEAD evidence | V2 verdict |
|---|---|---|---|
| Risk named in artefact | `p1c:480-483` (V1 lines) | `p1c:489-491` (V2 HEAD lines; +9 line shift from V2 frontmatter insertions); substance byte-identical (§1.3 grep) | **NAMED** |
| Route concrete (code anchor) | `p1c:477-480` + `generated.rs:33-158` + `runtime/Cargo.toml:21` | `p1c:486-489` (`A V2 fold of P1-A/B/C with --features parse-attribution`) + `generated.rs:33,34,43,44,58,59,79,80,86,87,117,118,138,139,157,158,185,186,201,202,211...` (11+ gates verified §1.2) + `runtime/Cargo.toml:21` (verified §1.2) | **ROUTED** |
| Aggregator-level deferral framing | (V1 aggregator authored after V1 CHs) | V1 consolidated §2.6 (F-V2-P1ABC-RERECORD packet spec, ≈350 lines), §3.2 (Option X deferral decision); V2 commit body cites Option X verbatim | **CONTRACTED** |
| S-P2 handoff explicit | (deferred to V2 aggregator) | V1 consolidated §3.2 Option X reads "Separate 'V2-extension' dispatch runs F-V2-P1ABC-RERECORD ... CH6 V2 MUST queue stays 'named + routed'; conversion to 'named + routed + executed' happens at S-P2 entry artefact" | **HANDED OFF** |

**V2 verdict: V2 MUST-queue item 1 remains "named + routed" through V2 with explicit S-P2 handoff.** No paper-close demotion. The "named + routed + executed" target migrates from "V2 fold" to "S-P2 entry artefact" per the V1 aggregator Option X decision; the V2 light micro-redispatch preserves the routing intact.

### §3.3 — V2-CONFIRM-3: V2 MUST-queue item 2 (`github_events parse_only` longer-iter re-record) remains "named + routed" at V2 with explicit S-P2 handoff

V1 CH6 §4.2 (MUST queue item 2): "re-record `github_events parse_only` Track 1 with longer iter count. Risk named at p1e:96. The 8-sample capture is the only P1-A row whose hot-leaf is inlined-std `<u16 as From<u8>>::from`, not a parser primitive. Increase iters until ≥ 4000 samples ..."

V2 confirming check:

| Discriminator | V1 evidence | V2 HEAD evidence | V2 verdict |
|---|---|---|---|
| Risk named in artefact | `p1e:96` (V1 lines) | `p1e:111` (V2 HEAD lines; row preserved; `(inlined-std generic in 8-sample capture; CH6 risk)` verbatim per §1.4 grep) | **NAMED** |
| Summary-line CH6 risk + route | `p1e:110` | `p1e:125` (V2 HEAD; `1 is inlined-std noise (github_events capture had only 8 samples — CH6 risk) ... S-P2 must crack dispatch_value open via parse-attribution cargo feature ...`) | **NAMED + ROUTED** |
| Concrete iter target | V1 CH6 §4.2 names "≥ 4000 samples" target derived from `p1a §1.2` iter table | unchanged at V2 (V1 CH6 §4 is the routing document) | **CONCRETE** |
| S-P2 handoff explicit | (deferred to V2 aggregator) | V1 consolidated §2.6 reads "github_events parse_only longer-iter re-record + extract: ≈5 min" as a step within the F-V2-P1ABC-RERECORD heavy packet; Option X bundles it with the parse-attribution rebuild for the S-P2 entry artefact | **HANDED OFF** |

**V2 verdict: V2 MUST-queue item 2 remains "named + routed" through V2 with explicit S-P2 handoff.** The github_events longer-iter re-record is bundled with the F-V2-P1ABC-RERECORD packet (per V1 consolidated §2.6 step 5: "Re-record `github_events parse_only` Track 1 with longer iter count"); both items share the Option X S-P2 entry-artefact deferral. No paper-close.

### §3.4 — V2-CONFIRM-4: V2 MUST-queue item 3 (path-layout standardisation) is SHOULD not MUST in V1 framing; not CH6-blocking

V1 CH6 §4.3 (cosmetic — SHOULD, not MUST): "standardise the `/tmp/skv14-p1*/` directory layout in V2 ... No paper-close; the artefacts are reachable; the naming inconsistency is an orchestration ergonomics issue."

V2 confirming check: V1 explicitly classified this item as SHOULD (not MUST); V2 did not standardise but the V1 CH6 §4.3 classification language ("No paper-close; the artefacts are reachable; the naming inconsistency is an orchestration ergonomics issue") preempts any V2 misreading as a CH6-blocking gap. Per §1.1 above, the `/tmp/skv14-p1*/` paths have since been wiped, which makes the ergonomics-level naming inconsistency moot — future regeneration cycles can adopt the standardised layout from the start.

**V2 verdict: V2 MUST-queue item 3 (path-layout cosmetic) is NOT CH6-blocking at V2 or V3 by V1's own framing.** Aggregator may carry it as a SHOULD for the agent dispatching the next `/tmp/skv14-p1*/` regeneration.

### §3.5 — V2-CONFIRM-5: F-V2-P1F-1 contracted-deferral framing is the structural anchor for the V1 aggregator Option X precedent (NEW: positive structural finding)

The V2 fold's F-V2-P1F-1 packet (5-paragraph contracted-deferral framing block at `p1f-results-delta.md:179-186`) is documented as a CH5 V1 REVISE → V2 ACCEPT-WITH-CONTRACTED-DEFERRAL conversion for the four NEW SK-V14 schema columns. Reading the same block under the CH6 lens reveals a **secondary structural function**: it establishes the textual precedent for the contracted-deferral pattern that the V1 aggregator Option X invokes for the heavy F-V2-P1ABC-RERECORD packet. The four PASS-ALPHA §4.4 citations in the F-V2-P1F-1 block (per `p1f-results-delta.md:183`) are the same authority that justifies deferring the parse-attribution rebuild to S-P2.

This is a **NEW structural finding** not surfaced in V1 CH6 (because V1 saw F-V2-P1F-1 as a CH5 future-fold, not a CH6 anchor) but consistent with V1's overall posture: V1 CH6 §3.2 explicitly noted "the V2 remediation is concrete and code-anchored"; V2's F-V2-P1F-1 block makes the contracted-deferral pattern explicit in the corpus, which strengthens (not weakens) CH6's discrimination between contracted-deferral and paper-close.

**CH6 V2 reading: F-V2-P1F-1 is CH6-POSITIVE.** The block does not introduce a paper-close; it establishes the structural vocabulary for distinguishing legitimate contracted-deferrals (PASS-ALPHA §4.4 pattern; named + routed + decision-precedented) from paper-closes (unnamed cause; absent route; silent deliberation). This is the V2 dispatch's stated criterion ("verify the deferral framing is CONTRACTED-DEFERRAL not PAPER-CLOSE (parallel to V1 CH6 §3.4 positive-exemplar pattern)") rendered as a corpus-level artefact, not just a lens-level finding.

### §3.6 — V2-CONFIRM-6: Volatile `/tmp` filesystem state observation (NEW: process-class finding for aggregator)

Per §1.1, the V1 CH6 path-existence verifications (77/77 flame profiles + xctrace cpu-state.xml + identity manifests) cannot be re-executed at V2 confirming time because the underlying `/tmp/skv14-p1*/` state has been wiped between V1 commit (`a3dfcaf38`, 2026-05-23 early) and V2 confirming (this turn). This is the expected behaviour of macOS `/tmp` between agent sessions.

**NEW process-class finding for the aggregator (not a V2 CH6 REVISE):** future hardening passes should not regress to /tmp path re-verification as the sole CH6 path-existence criterion, because the underlying state is non-persistent. The binding evidence is the V1 CH6 §1.1-§1.4 tables (which captured `ls` / `wc -l` / `grep -c` outputs at V1 write time with concrete byte counts and sample counts); these tables stand as the path-existence record. V2 + later cycles verify the V2 fold packets do not introduce a NEW paper-close, which the V2 confirming pass does (§1.2-§1.5 + §2 + §3).

The aggregator may consider for V3+: (a) require originating P1 agents to relocate sidecar `.syms.json` + identity.txt artefacts from `/tmp/skv14-p1*/` into a non-volatile location (e.g. `restart/skinny/tranches/sk-v14/research/p1/artefacts/`) before commit, so V2+ cycles can re-verify; or (b) accept the V1 CH6 §1 tables as the persistent record and explicitly scope V2+ CH6 work to V2-fold-introduced-risk verification. Either route is CH6-defensible; (a) is more durable, (b) is what V2 implicitly did under volatile /tmp conditions.

## §4 — V3 fold recommendations (CH6 perspective)

CH6 V3 queue (for V3 confirming + V3 aggregator):

1. **NO MUST items for V3 from CH6.** V2 ACCEPT-rate is 6/6 = 100 %, same as V1 (CH6 has held 100 % across both cycles). Per `ORCHESTRATOR.md §3Z` "≥95 % × 2 cycles", V2 + V1 satisfy CH6's two-cycle convergence threshold provisionally; V3 confirming is required to lock the third consecutive cycle per the strict reading of §3Z (or to confirm that V1 + V2's joint ≥95 % suffices per the relaxed reading — aggregator's call).

2. **SHOULD (carried from V1 §4.3):** path-layout standardisation `/tmp/skv14-p1*/` → consistent naming convention. Cosmetic; V1 + V2 both classified as not CH6-blocking. V3 or next /tmp regeneration cycle to action.

3. **SHOULD (NEW process-class from §3.6):** consider relocating sidecar `.syms.json` + identity.txt artefacts from volatile `/tmp/` to a non-volatile location (e.g. `restart/skinny/tranches/sk-v14/research/p1/artefacts/`) so future hardening cycles can re-verify path existence. Process-class; not CH6-blocking at V2 or V3.

4. **SHOULD NOT (reaffirmed from V1 §4.4-§4.5):** re-open the typed-fixture `n/a` rows; re-open PMC `unavailable_from_current_export`. Both classifications remain correct at V2 HEAD; neither is paper-close; both have stated causes anchored to substrate constraints or product-surface gaps; remediation routes are not in scope for hardening cycles.

5. **CONTRACTED-DEFERRED (S-P2 entry artefact, not V3 fold):** F-V2-P1ABC-RERECORD heavy packet (parse-attribution rebuild for P1-A/B/C + `github_events parse_only` longer-iter re-record) per V1 aggregator Option X. CH6 V2 verdict: deferral is CONTRACTED-DEFERRAL (§3.1); plumbing intact at V2 HEAD (§1.2); risk language preserved across V2 fold (§1.3-§1.4). NOT a V3 fold item; an S-P2 entry-artefact obligation.

## §5 — Convergence-gate impact (V2)

V2 CH6 ACCEPT-rate: **6/6 = 100 %**, unchanged from V1 (also 6/6 = 100 %).

§3Z requires "≥95 % × 2 cycles, zero orphan REVISEs":
- **Cycle 1 (V1):** 100 % — MET threshold.
- **Cycle 2 (V2):** 100 % — MET threshold (second consecutive cycle).
- **Cycle 3 (V3):** confirming only (per strict §3Z reading; per relaxed reading, V1 + V2 jointly satisfy).

Zero-orphan-REVISE check: zero REVISE at V1; zero REVISE at V2. CH6 has had no REVISE finding across both cycles; the lens has been at ACCEPT 6/6 from V1 onward. V2 confirming verifies the V1 ACCEPT-rate holds against the V2 fold and against the deferral of the heavy F-V2-P1ABC-RERECORD packet.

CH6 lens recommendation: **CH6 gate is OPEN for S-P2 dispatch.** No CH6 finding blocks S-P2; the contracted-deferral of F-V2-P1ABC-RERECORD to S-P2 entry artefact is CH6-defensible (§3.1) and CH6-positive structurally (§3.5). The aggregator may proceed with the V3 cycle for confirming the other lenses' two-cycle ≥95 % threshold; CH6 itself does not require V3 work.

## §6 — Fresh-finding scan (V2 confirming)

Per the dispatch's fresh-finding scan mandate, CH6 re-scanned the six P1 artefacts under their V2 form (HEAD `069ba203c`) for any new paper-close risk not surfaced at V1.

**Fresh-finding scan result: ONE new CH6-relevant observation, ZERO new CH6 paper-close risks.**

The scan covered:

1. **F-V2-METHODOLOGY-1 RUSTFLAGS cohort discovery side-effect on CH6.** The V2 commit message body discloses the {P1-A, P1-B} RUSTFLAGS-unset / {P1-C, P1-D} target-cpu=native cohort split. CH6 reading: this is CH4 cohort-discipline, not CH6 substrate-evidence; the RUSTFLAGS regime affects inlining shape within a single substrate but does not create parallel substrates or unprofiled cells. **No CH6 implication.**

2. **F-V2-P1A-MOVEMASK + F-V2-P1C-LINEDRIFT line-anchor refreshes.** All 12 P1-A movemask annotations + 3 P1-C NEON refreshes are CH1-class (cite hygiene). They strengthen CH1 path-anchor discipline; they do not affect CH6 path-existence or symbol-resolution claims. **No CH6 implication beyond strengthening CH1, which is upstream of CH6's "symbol resolvable" sub-clause.**

3. **F-V2-P1E-1 typed-plane line refresh.** The 7-row typed-plane refresh in `p1e-hot-leaf-attribution.md:155-167` updates line anchors (`skip_value :1739 → :2949`, etc.) but does not touch the §2.1 parse-only or §2.2 direct rows that carry the CH6-tagged `github_events` 8-sample noise + `instruments` inlined-std `Option<&u8>::copied` noise + `y_string_unicode` timer-dominance findings (§1.4 verified). **No CH6 implication; the V2 refresh is CH1-class cite hygiene.**

4. **F-V2-P1F-1 contracted-deferral framing.** The 5-paragraph block at `p1f-results-delta.md:179-186` is CH5-class (hidden-coupling reclassification to ACCEPT-WITH-CONTRACTED-DEFERRAL) but INCIDENTALLY CH6-positive (per §3.5 — establishes the structural vocabulary for distinguishing contracted-deferral from paper-close). **NEW positive structural finding (§3.5); not a paper-close risk.**

5. **Volatile `/tmp/skv14-p1*/` filesystem state.** Per §3.6 — the V1 CH6 path-existence evidence cannot be re-executed at V2 confirming time. **NEW process-class observation for aggregator (§3.6); not a CH6 REVISE.**

6. **P1-D V2 build-flags regime confirmation block.** The `:21-30` + `:81-89` additions reinforce CH4 capture-flag discipline; they do not alter the Lock-14 PMC absence classification (§1.5 confirmed). **No CH6 implication.**

7. **Heavy F-V2-P1ABC-RERECORD deferral framing.** Per §3.1 — verified CONTRACTED-DEFERRAL by all three CH6 discriminators (named cause; named route; named decision precedent); parallel to V1 CH6 §3.4 positive-exemplar pattern; parallel to F-V2-P1F-1 textual framing landed at HEAD. **NEW positive verification (§3.1); not a paper-close.**

All seven scan dimensions clear. **Zero new CH6 paper-close risks at V2.** Two new positive structural findings (§3.1 contracted-deferral verification; §3.5 F-V2-P1F-1 as CH6-vocabulary anchor); one process-class observation (§3.6 volatile /tmp state) for aggregator V3+ consideration.

## §7 — Sources

CH6 V2 confirming cited the following artefacts (every claim above carries `path:line` per CHALLENGE-CONTEXT §3):

**V1 carry-through:**

- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CHALLENGE-CONTEXT.md` (53 lines; read end-to-end; §2 CH6 binding).
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH6.md` (234 lines; read end-to-end; V1 verdict 6/6 ACCEPT; §3.1 PMC Lock-14 verification; §3.2 dispatch_value folded-symbol verification; §3.4 `github_events` 8-sample CH6 risk surfaced; §4 V2 fold queue with 5 items).
- `restart/skinny/tranches/sk-v14/research/p1/hardening/HARDENING-S-P1-V1-CONSOLIDATED.md` (546 lines; §0.1 lens roll-up at line 31 = CH6 100% ACCEPT; §1.4-§1.6 per-axis CH6 rows at lines 137, 152, 167, 183, 198, 215; §2.6 F-V2-P1ABC-RERECORD packet spec at line 345; §3.2 Option X deferral decision at line 435; §3.6 CH6 V2 forecast at line 420).

**V2 P1 artefacts (commit `069ba203c`):**

- `restart/skinny/tranches/sk-v14/research/p1/p1a-samply-mode-1.md` (V2 HEAD 343 lines; F-V2-METHODOLOGY-1 + F-V2-P1A-MOVEMASK folds landed; CH6-relevant content unchanged from V1).
- `restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md` (V2 HEAD 323 lines; F-V2-METHODOLOGY-1 fold landed; CH6 inherited-dependency on parse-attribution unchanged).
- `restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md` (V2 HEAD 616 lines; F-V2-METHODOLOGY-1 + F-V2-P1C-LINEDRIFT folds landed; ANOM-4 CH6 paper-close attestation at `:489-491` verified byte-identical at §1.3).
- `restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md` (V2 HEAD 669 lines; F-V2-METHODOLOGY-1 fold landed; PMC Lock-14 classification preserved per §1.5).
- `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md` (V2 HEAD 321 lines; F-V2-P1E-1 BINDING fold landed at §2.3 typed-plane table; §2.1 parse-only `github_events` row at `:111` + §2.2 direct `instruments` row at `:141` + summary at `:125` byte-identical to V1 per §1.4).
- `restart/skinny/tranches/sk-v14/research/p1/p1f-results-delta.md` (V2 HEAD 269 lines; F-V2-P1F-1 BINDING reclassification at `:179-186`; 5-paragraph contracted-deferral framing block verified per §3.5).

**Authority bindings:**

- `restart/prompts/skinny/PASS-1-PROFILE.md §3 CH6` (lens definition: "flame profile file exists on disk; symbol resolvable; every `unprofiled` cell resolved; no symbol folded by inlining stands as a primitive-level attribution without an `atos -inlineFrames` recovery or a `--features parse-attribution` rebuild").
- `restart/prompts/ORCHESTRATOR.md §3W CH6` (universal binding); `§3Z` (≥95 % × 2 cycles convergence rule).
- `restart/prompts/pass-contracts/PASS-ALPHA.md §4.4` (lines 112-122; contracted-deferral precedent verified verbatim in F-V2-P1F-1 framing block at `p1f-results-delta.md:183`).

**Source-code plumbing verification (V2 HEAD):**

- `/Users/mkbabb/Programming/bbnf-lang/skinny/crates/runtime/src/grammars/json/generated.rs` lines 17, 27, 33-34, 43-44, 58-59, 79-80, 86-87, 117-118, 138-139, 157-158, 185-186, 201-202, 211 (parse-attribution feature gate plumbing across 11+ functions — verified §1.2; more gates than V1 enumeration of 8, indicating gate expansion has occurred between SK-V13 and SK-V14 source state).
- `/Users/mkbabb/Programming/bbnf-lang/skinny/crates/runtime/Cargo.toml:21` (`parse-attribution = []` feature declaration — verified §1.2).

**Filesystem state observations (V2 confirming time):**

- `/tmp/skv14-p1*/` — wiped between V1 and V2 commits; V1 CH6 §1.1-§1.4 evidence remains the binding path-existence record (§1.1 + §3.6 observation).

**Commit verification:**

- `git log --oneline -3` → `069ba203c docs(sk-v14-p1-profile): V2 light micro-redispatch — five orphan REVISEs landed`; `a3dfcaf38 docs(sk-v14-p1-hardening-V1): challenge V1 + consolidated`; `9b7e76e19 docs(sk-v14-p1-hardening-V1): seed S-P1 CHALLENGE V1 dispatch context`.
- `git show --stat 069ba203c` → 6 files changed, 86 insertions(+), 26 deletions(-); CH6-relevant folds (P1-C ANOM-4 preservation + P1-E §2.1/§2.2 preservation + P1-F F-V2-P1F-1 contracted-deferral framing) are net-positive for CH6 discipline.
- V2 commit message body (verbatim quoted at §3.1): "the heavy F-V2-P1ABC-RERECORD deferred to S-P2 design per V1 aggregator Option X (parse-attribution rebuild is primitive-design ground-truth, not lens-correctness fix)".

## §8 — Closing

CH6 ANTI-PAPER-CLOSE V2 confirming pass: **ACCEPT 6/6 (100 %), zero REVISE, zero REJECT, zero new paper-close risk.** V1 100 % held; V2 light micro-fold preserved every CH6-load-bearing attestation (P1-C ANOM-4 paper-close-risk paragraph; P1-E `github_events` 8-sample CH6 risk row + `instruments` inlined-std noise row + parse-attribution route at `:125`; P1-D PMC Lock-14 classification). The heavy F-V2-P1ABC-RERECORD packet deferral is CONTRACTED-DEFERRAL per all three CH6 discriminators (named cause; named route; named decision precedent), structurally parallel to V1 CH6 §3.4 positive-exemplar pattern, and incidentally anchored in the corpus by F-V2-P1F-1's contracted-deferral framing block at `p1f-results-delta.md:179-186`. The three V2 MUST-queue items from V1 §4 remain "named + routed" through V2 with explicit S-P2 handoff per V1 aggregator Option X. The `parse-attribution` Cargo feature gate plumbing is verified intact at V2 HEAD (11+ functions in `runtime/src/grammars/json/generated.rs`; declaration at `runtime/Cargo.toml:21`); the route is executable on S-P2 demand. Two new CH6-positive structural findings (§3.1 contracted-deferral verification; §3.5 F-V2-P1F-1 as CH6-vocabulary anchor); one process-class observation (§3.6 volatile `/tmp` state) for aggregator V3+ consideration. CH6 gate is OPEN for S-P2 dispatch; the lens converges at V2 with no V3 MUST items.
