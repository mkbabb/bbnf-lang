# SK-V12 W1a CHALLENGE V3 - CH6 Anti-Paper-Close

Date: 2026-05-20.
Wave: W1a - GrammarConfig + Lock 14 Legality Gate.
Lens: CH6 anti-paper-close.
Disposition: ACCEPT.

## Finding

`PLAN-V3.md` preserves the CH6 boundary.

W1a remains a legality gate only. It explicitly does not emit CSS L4, add a CSS
benchmark row, compare against lightningcss, open Sheets/BBNF-self fallback,
add schema/outcome fields, or claim non-JSON admission. The expanded owner
roster is still bounded to JSON legality, Lock 14 enforcement, JSON guard
refresh, and REDRESS evidence; CSS/runtime/generated/benchmark files, broad
report plumbing, `xtask`, and `bin/gate` remain unowned.

Same-wave consumers are required. Generated JSON config/profile fields without
generated consumers fail W1a, and the Lock 14 generic-root scan must be
consumed by the existing `gate-json` path. `not_refreshed:no_behavior_drift` is
invalid after JSON-producing paths move.

Future-phase wording is acceptable because it is framed as a constraint, not
evidence: W1b remains the first CSS generation surface, and any future CSS
provider must supply its own grammar-owned scan/sink. That does not admit CSS,
move SOTA, open fallback, or close SK-V12.

## Reject If Redress Claims

- CSS row admission, SOTA movement, lightningcss comparison, or SK-V12 close.
- Sheets or BBNF-self fallback before measured CSS redress.
- Config/profile metadata without same-wave generated consumers.
- Lock 14 scan not consumed by `gate-json`.
- PASS after JSON-producing paths move without refreshed native JSON guard
  evidence and exact floor proof.
- "Ready for W1b", "wired", "integrated", or similar future-phase wording used
  as substitute for executable W1a evidence.

## CH6 Disposition

ACCEPT. No CH6 revision is required before W1a redress.
