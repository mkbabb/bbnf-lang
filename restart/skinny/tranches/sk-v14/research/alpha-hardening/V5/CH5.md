# CH5 HIDDEN COUPLING — Pass Alpha V5 Disposition

Lens scope per `restart/skinny/tranches/sk-v14/research/alpha-hardening/V1/CHALLENGE-CONTEXT.md:149-160`, carried verbatim through V2 / V3 / V4 and now V5 per `ORCHESTRATOR.md §3Z` (the V≤5 ceiling confirming-pass binding). The CH5 charge persists: no parallel substrate, no sidecar producer, no renamed-scanner Lock 1 violation, no Track 1 ≡ Track 2 dishonesty; substrate-union closure holds. V5 overlay: re-verify the V4 100 % baseline against the V5-state artefacts; confirm the V5 micro-fold packet (commit `87ee874f0` — `F-V5-α-E-1` cost/cap table roster-count generalisation at α-E:756 + wall-clock recomputation at α-E:770-774) introduces no new CH5 contamination. Authority unchanged: `restart/locks/LOCKS.md:73-82` (Lock 1 substrate-ceiling fold, 2026-05-21 amendment).

## §0 — Disposition summary

- **ACCEPT-rate: 100 %** (46 of 46 §-level dispositions across the eight artefacts; V2 / V3 / V4 baseline holds; V5 micro-fold packet contains zero CH5-surface edit).
- **REJECT count: 0.**
- **REVISE count: 0.**
- **Critical findings: 0.**
- **V2 / V3 / V4 baseline anchor scoreboard: 6 PRESERVED / 0 DRIFTED / 0 REGRESSED.**
- **V5 micro-fold contamination scan: 0 new parallel substrate / 0 new Track-collapse vector / 0 new renamed-scanner slip / 0 new sidecar producer.**

The substrate-union architecture stands as V2-hardened, V3-confirmed, V4-preserved, and now V5-preserved. Every V2 CH5 anchor (E-3 owner-paths triad, F-14 G-SIMD-GRAMMAR-POLICY triad, F-15 `track2_entry_point` column, F-16 UnionTape refusal, C-1 α-C §2 P-7 triple gate, E-4 module-path discipline) persists byte-for-byte at the same V4 line addresses in the V5 state. The V5 micro-fold packet's single fold — `F-V5-α-E-1` (CH2 V4 non-finding remediation; "8 grammars" → "per rostered grammar" at α-E:756 + wall-clock total recomputation `8 × 30 = 240 min` → `N × 30 min where N = live rostered-grammar enumeration via cargo metadata | jq` at α-E:770-774, with C-4 variable rename `N → M` to avoid collision) — targets the §10 cost/cap table surface (an axis CH2 + CH4 share). The fold's edit domain is alpha-E lines 753-774; no CH5 anchor sits within that range. The V5 fold induces +1 net line in §10 (4 lines → 5 lines for the wall-clock paragraph at 770-774); the +1 line propagates only to lines > 770. No CH5 anchor sits below line 770, so all CH5 anchor absolute line addresses persist from V4. The E-3 triad remains at `alpha-E:474-491`, the E-4 module-path discipline remains at `alpha-E:514-524 + 545-547`, F-16 remains at `HANDOFF:233-236`, F-14 remains at `SYNTHESIS:316-319`, F-15 remains at `SYNTHESIS:240`, and C-1 remains at `α-C:348-385`. All citations from V4 carry forward without translation.

CH5 converges at 100 % for V5. Combined with V2 (100 %), V3 (100 %), and V4 (100 %), the lens has now logged four consecutive cycles at the ceiling — double the `ORCHESTRATOR.md §3Z` two-consecutive-cycle convergence requirement. The CH5 lens has been at lock-state since V2; V3, V4, and V5 simply reconfirm. V5 is the bracket-close §3Z confirming pass at the V≤5 ceiling; CH5 closes its participation at the same lock state it has held since V2.

## §1 — V2 / V3 / V4 baseline anchor verification table (V5 state)

The V5 confirming pass re-verifies that every V2-landed, V3-preserved, V4-preserved CH5 anchor persists in the V5-state artefacts (post-`87ee874f0`). All six anchors verified PRESERVED below; line numbers reflect the V5 state.

| V2 Anchor | Origin | V5 location | V5 evidence | Status |
|---|---|---|---|---|
| **E-3** (BINDING REJ → V2 LANDED) | CH5 V1 §2 BINDING REJECT, V2 fold | `alpha-E-candidate-shortlist.md:474-491` | "Per-shape Lock-1 triad declaration (mandatory; CH5 §2 REJECT remediation, V1 hardening)" block intact verbatim at line 474. Triple `substrate_target=existing_tape \| retention_lifetime=generated_function \| policy_owner=generated_grammar` named at lines 476-477; allowed-value enumeration at lines 479-483 cites `LOCKS.md:73-82` directly; abrogation path bound to `[abrogate-before-patch]` + C-4 architectural-block escalation (§11); `xtask gate-json` rejection wires through the same gate path that rejects empty equality columns from C-2. The V5 fold edits §10 at lines 753-774, ≈ 280 lines downstream from E-3; the fold's +1 net line shift propagates only to lines > 770, leaving E-3's absolute address unchanged from V4. | **PRESERVED** |
| **F-14** | CH5 V1 §2 REVISE #1, V2 fold | `SYNTHESIS.md:316-319` | G-SIMD-GRAMMAR-POLICY clause appends: "Every SIMD consumer wired by C-4 declares `substrate_target`, `retention_lifetime`, and `policy_owner` per `LOCKS.md:73-82`; `xtask gate-json` rejects any row whose REDRESS lacks the triple." Intact verbatim. SYNTHESIS not touched by the V5 packet (V5 fold is α-E-only per commit `87ee874f0`). | **PRESERVED** |
| **F-15** | CH5 V1 §2 REVISE #2, V2 fold | `SYNTHESIS.md:240` | `track2_entry_point` telemetry column intact verbatim at line 240: "**NEW (CH5)** — symbol path of the Track 2 oracle entry point; `xtask gate-json` rejects any row where the Track 1 and Track 2 entry-point symbol paths share a common ancestor in `runtime::tape::` beyond the public `Tape` / `OffsetFlags` types." Column position preserved between `Track 1/2 Mbps` and `comparator_plane`. SYNTHESIS not touched by the V5 packet. | **PRESERVED** |
| **F-16** | CH5 V1 §2 REVISE #3, V2 fold | `HANDOFF.md:233-236` | UnionTape refusal bullet intact verbatim: "proposes `UnionTape`, a second tape, a public substrate API, a retained class/mask stream, or parser-owned cursor/list state; only G-Omega may amend Lock 1's substrate-union closure (`LOCKS.md:73-82`)." G-Omega amendment route named explicitly. HANDOFF not touched by the V5 packet. | **PRESERVED** |
| **C-1** | CH5 V1 §2 REVISE #4, V2 fold | `alpha-C-redress-digest.md:348-385` | P-7 triple gate intact verbatim. Clause (a) distinct symbol paths via `xtask gate-json` per-row column (lines 355-360); clause (b) distinct concrete `Sink` types via compile-time `TypeId::of::<Track1::Sink>() != TypeId::of::<Track2::Sink>()` (lines 362-368); clause (c) distinct buffer addresses via `Track1::tape() as *const _ as usize` vs `Track2::tape() as *const _ as usize` recorded into per-iter equality column with `xtask gate-json` rejecting equality (lines 370-379). Closing prose at lines 381-385 cites `LOCKS.md:80-81` and labels the triple as the mechanical correlate of the renamed-scanner clause. α-C STAND artefact since V2; not edited by V3, V4, or V5 packets. | **PRESERVED** |
| **E-4** | CH5 V1 §2 REVISE #5, V2 fold | `alpha-E-candidate-shortlist.md:514-524, 545-547` | "Post-wave hot-leaf module-path discipline (CH5 §2 REVISE #5, V1 hardening)" sub-clause intact verbatim at line 514. `runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` named as pre-emptive REJECT targets at lines 518-520 + restated at lines 545-547. Closing prose ties the discipline to the "renamed-scanner-with-shared-buffer slip the CH5 charge names". The V5 fold's edit domain (lines 753-774) sits ≈ 240 lines downstream of E-4; the +1 net line shift propagates only to lines > 770, leaving E-4's absolute address unchanged from V4. | **PRESERVED** |

All six V2 CH5 anchors persist byte-for-byte at the same V4 line addresses. The Lock-1 triad propagates coherently from `LOCKS.md:73-82` through SYNTHESIS §2 + §4, HANDOFF §7, α-E §6, and α-C §2. The V4 CH5 100 % verdict carries forward intact into V5.

## §2 — V5 micro-fold contamination scan

Per the dispatch context, the V5 micro-fold packet (commit `87ee874f0`) lands a single fold: `F-V5-α-E-1` (CH2 V4 non-finding remediation — cost/cap table roster-count generalisation). CH5's V5 charge is to verify the fold introduces no parallel substrate, no Track 1 ≡ Track 2 collapse, and no renamed-scanner Lock 1 violation.

### §2.1 — F-V5-α-E-1 (alpha-E:753-774) — cost/cap table phrasing, not substrate edit

The V4 hardening pass surfaced CH2's non-finding observation that α-E §10's cost/cap table read "C-1 sub-waves (8 grammars; per sub-wave)" with a hardcoded "8 × 30 = 240 min" wall-clock total in the accompanying prose. The V4 CH1 fold-list separately required the round-trip xtask gate to derive its grammar enumeration from live workspace metadata (`cargo metadata --no-deps | jq '.metadata.bbnf.grammars[].ident'`); the V5 fold completes the same generalisation in §10's cost/cap table by:

```
- | C-1 sub-waves (8 grammars; per sub-wave) | 20 min | 15 min | 30 min |
+ | C-1 sub-waves (per rostered grammar; per sub-wave) | 20 min | 15 min | 30 min |
```

and recomputing the wall-clock prose:

```
- cluster total is 8 × 30 = 240 min of redress windows, run serialised
- per §9; the C-4 cluster total is N × 45 min where N is the number of
- shapes the wiring exercises (≥ 2 per E-8's two-grammar-family
- requirement).
+ cluster total is N × 30 min of redress windows where N is the live
+ rostered-grammar enumeration (`cargo metadata | jq` over the grammar
+ roster at HEAD), run serialised per §9; the C-4 cluster total is
+ M × 45 min where M is the number of shapes the wiring exercises
+ (≥ 2 per E-8's two-grammar-family requirement).
```

The C-1 row gets the literal roster-count token `N` (live rostered-grammar enumeration); the C-4 cluster's existing `N` is renamed to `M` to avoid variable collision in the same paragraph.

CH5 verification of this V5 fold:

- **Substrate edit?** No. The fold edits cost/cap text in α-E §10 only. The `cargo metadata | jq` reference is a cross-citation to C-3's process-time gate (the same shell pipeline F-V4-α-E-1 corrected to schema-valid form at α-E:362-387); it does not introduce a new shell command or a new runtime artefact. No source file, no runtime, no tape, no sink, no codegen path, no scanner, no buffer is touched by §10's text. `cargo metadata` remains the canonical Cargo workspace metadata interface Lock 14 already sanctions (`LOCKS.md:220` workspace metadata declaring its strategy).
- **New sidecar producer?** No. The fold authors no new runtime artefact, no new producer, no new emitter. The §10 prose is a process-time scheduling-discipline derivation: the redress-window total for the C-1 cluster equals `N × 30 min`, where `N` is read at HEAD time from workspace metadata via the same shell pipeline C-3 lands. No runtime substrate is introduced; the derivation runs at planning-time, not within any parser, codegen, or runtime path.
- **Public substrate API?** No. The fold introduces zero API token. The variable renames (`8 grammars` → `per rostered grammar`; the literal `240 min` → `N × 30 min`; `N` → `M` for C-4) are pure prose / table-cell substitutions. No public interface, no trait surface, no module-path token is altered.
- **Track 1 ≡ Track 2 collapse?** No. The fold operates on per-grammar redress-wave scheduling totals (a wave-program cost-cap concern), not on bench Track binding. The Track surface — F-15 `track2_entry_point` column (SYNTHESIS:240), C-1 P-7 triple gate (α-C:348-385), F-16 UnionTape refusal (HANDOFF:233-236) — is untouched.
- **Renamed scanner / shared buffer slip?** No. The fold introduces no scanner, no buffer, no scheduling primitive. The variable rename `N → M` is local to the §10 paragraph and serves only to disambiguate the C-1 grammar-roster count from the C-4 shape-exercise count; both variables remain prose-bound to their respective wave-cluster cost calculations.
- **Forward-blindness regression?** The fold actively **strengthens** Lock 14 in the §10 surface. V4's §10 carried a stale "8 grammars" parenthetical that would have aged out of sync with the workspace as additional grammars admit (the V4 CH1 executable-verification mandate enumerated nine grammars against the live workspace — `bbnf, json, css_l4, css_pretty, google_sheets, ebnf, bnf, csv, math`). The V5 fold deletes the hardcoded count from §10's table-cell label and replaces the wall-clock arithmetic with the same `cargo metadata | jq` derivation C-3's round-trip gate consumes, making the cost-cap table forward-blind to roster size. This propagates the F-V4-α-E-1 roster-count generalisation into the §10 cost-cap surface — i.e., the V5 fold closes the residual CH2 / CH4 forward-blindness slip the V4 packet left exposed.

**F-V5-α-E-1 contamination verdict: CLEAN.** Confirms the dispatch context's expectation: "V5 micro-fold (commit `87ee874f0`) is CH2/CH4-axis (cost/cap table phrasing); CH5-orthogonal. No runtime substrate, Track-1≡Track-2, or Lock-1 surface touched." Confirmed.

### §2.2 — Cross-fold interaction (V5 micro-fold ↔ V2 / V3 / V4 anchors)

The single V5 micro-fold touches one artefact (α-E §10 cost/cap table at lines 753-774). No CH5 anchor sits within that range. CH5 anchor positions in the V5 state:

- SYNTHESIS §2 line 240 (F-15) — not touched by V5 packet.
- SYNTHESIS §4 lines 316-319 (F-14) — not touched by V5 packet.
- HANDOFF §7 lines 233-236 (F-16) — not touched by V5 packet.
- α-E §6 lines 474-491 (E-3) — not touched (V5 fold edits α-E §10 lines 753-774, ≈ 280 lines downstream).
- α-E §6 lines 514-524 + 545-547 (E-4) — not touched (≈ 240 lines downstream of E-4).
- α-C §2 lines 348-385 (C-1) — not touched (α-C remains a V2 STAND artefact through V3 + V4 + V5; no fold has ever edited it).

The V5 fold's +1 net line shift (4-line wall-clock paragraph → 5-line paragraph at α-E:770-774) propagates only to lines > 770. No CH5 anchor sits below line 770 in any artefact. Substrate / Track / sidecar tokens occurring below line 770 in α-E (the §11 pre-block list at line 717, the E-8 narrative at line 738) are above the fold region; the fold sits below both of these tokens. Consequently every CH5 anchor and every CH5-surface refusal / pre-block / guard occurs at its V4 absolute address.

No cross-fold interaction surface arises. The V5 micro-fold and the V2 / V3 / V4 CH5 anchor set are disjoint at the prose-block level.

## §3 — Per-artefact V5 disposition table

The V5 disposition replicates the V4 disposition row-for-row at the same V4 line addresses where the V5 fold induces no shift, and at the +1-shifted absolute address where the fold is downstream. Since no CH5 anchor sits downstream of line 770 in α-E, every CH5 V4 ACCEPT carries forward with unchanged absolute address; no V4 ACCEPT degrades to REVISE or REJECT.

| Artefact | § | Disposition | Reason |
|---|---|---|---|
| SYNTHESIS.md | §0.1 Close condition | ACCEPT | Same-plane / same-corpus / same-equality bind preserves Track separation; STAND from V4 (V5 packet does not touch SYNTHESIS). |
| SYNTHESIS.md | §0.2 Goalset row enumeration | ACCEPT | Per-plane gate still bars plane collapse; STAND from V4. |
| SYNTHESIS.md | §0.3 R-target goalset | ACCEPT | R8 "distinct parse_only path" remains inside `generated_json`; STAND from V4. |
| SYNTHESIS.md | §0.4 P-1..P-7 pre-blocks | ACCEPT | P-7 "Track 1 ≡ Track 2 dishonesty" carried verbatim; STAND from V4. |
| SYNTHESIS.md | §0.5 wave-by-wave deferral | ACCEPT | Deferral remains contracted to PASS-ALPHA §4.4; STAND from V4. |
| SYNTHESIS.md | §1 Corrected diagnosis | ACCEPT | OffsetFlags + Tape carried grammar-neutral; STAND from V4. |
| SYNTHESIS.md | §2 Telemetry binding | ACCEPT | F-15 `track2_entry_point` column at line 240 intact verbatim; rejection rule under `runtime::tape::` co-ancestry preserved. |
| SYNTHESIS.md | §3 Candidate shortlist | ACCEPT | C-4 row at line 274 names per-shape Lock-1 triad in falsifiability cell; risk reads "VERY HIGH (Lock-1 substrate-ceiling surface)" intact. |
| SYNTHESIS.md | §4 S-P3 constraints | ACCEPT | F-14 G-SIMD-GRAMMAR-POLICY triad appendix at lines 316-319 intact; `xtask gate-json` enforcement clause preserved. |
| SYNTHESIS.md | §5 Pre-blocked routes | ACCEPT | "parser-owned sidecars, hidden Track 1 ≡ Track 2 coupling, stale comparator sidecars" preserved; STAND from V4. |
| SYNTHESIS.md | §6 Close posture | ACCEPT | Plane-honest prose; STAND from V4. |
| HANDOFF.md | §1 Bracket verdict | ACCEPT | Substrate-pillar carry-forward language unchanged; STAND from V4. |
| HANDOFF.md | §3 Honest baseline | ACCEPT | OffsetFlags + Tape grammar-neutral pillar preserved; STAND from V4. |
| HANDOFF.md | §6 Next-move | ACCEPT | CH5 review bound; STAND from V4. |
| HANDOFF.md | §7 Refusal conditions | ACCEPT | F-16 UnionTape refusal at lines 233-236 byte-identical to V4; G-SIMD refusal at lines 211-215 untouched; P-7 refusal at lines 223-227 untouched. HANDOFF not touched by V5 packet. CH5 surface preserved. |
| HANDOFF.md | §8 V1 disposition | ACCEPT | PENDING flag honest; STAND from V4. |
| alpha-A | §1-§4 per-plane row tables | ACCEPT | STAND from V4 (no V5 α-A edit). |
| alpha-A | §5 telemetry | ACCEPT | STAND from V4. |
| alpha-A | §6 audit verdict summary | ACCEPT | STAND from V4. |
| alpha-A | §7 forward pointers | ACCEPT | STAND from V4. |
| alpha-B | §1.1 per-plane comparator binding | ACCEPT | STAND from V1+V2+V3+V4 (α-B not re-authored). |
| alpha-B | §2 parse_only rebound overlay | ACCEPT | STAND. |
| alpha-B | §3-4 direct / typed overlays | ACCEPT | STAND. |
| alpha-B | §5 CSS L4 overlay | ACCEPT | STAND. |
| alpha-B | §6 comparator-availability gap | ACCEPT | STAND. |
| alpha-B | §8 escalations | ACCEPT | STAND. |
| alpha-C | §1 per-entry dispositions | ACCEPT | STAND from V2+V3+V4 (no V5 α-C edit). |
| alpha-C | §2 P-1..P-6 pre-blocks | ACCEPT | STAND from V2+V3+V4. |
| alpha-C | §2 P-7 pre-block | ACCEPT | C-1 triple gate at lines 348-385 byte-identical to V2+V3+V4: distinct symbol paths + `TypeId` inequality + buffer-address inequality; `LOCKS.md:80-81` citation preserved. |
| alpha-C | §3 pattern-level summary | ACCEPT | Round-trip eligibility per P-7 captured at line 396 ("YES iff distinct entry points + buffers"); STAND from V2+V3+V4. |
| alpha-C | §4 reopen obligations | ACCEPT | R8 distinct parse_only path scoped inside `generated_json`; STAND from V2+V3+V4. |
| alpha-C | §5 closing posture | ACCEPT | STAND from V2+V3+V4. |
| alpha-D | §2 V-1..V-8 VALIDATED | ACCEPT | STAND from V1+V2+V3+V4. |
| alpha-D | §3 INVALIDATED | ACCEPT | STAND. |
| alpha-D | §4 DEMOTED | ACCEPT | STAND. |
| alpha-D | §5 STILL-OPEN | ACCEPT | STAND. |
| alpha-D | §6 net ledger | ACCEPT | STAND. |
| alpha-E | §3 C-1 Lock-14 refactor cluster | ACCEPT | Trait-based dispatch + ONE grammar-agnostic generator preserved; no V5 packet edit. |
| alpha-E | §4 C-2 comparator rebind | ACCEPT | Comparator binding inside `bbnf-bench` only; no V5 packet edit. |
| alpha-E | §5 C-3 regen-css pipeline | ACCEPT | Schema-correct derived enumeration at lines 362-387 preserved from V4; CH5 surface untouched. No V5 edit to §5. |
| alpha-E | §6 C-4 owner-paths + Lock-1 triad | ACCEPT | E-3 per-shape Lock-1 triad declaration at lines 474-491 byte-identical to V2+V3+V4; `LOCKS.md:73-82` citation + allowed-value enumeration + abrogation path preserved. Zero shift from V5 fold (fold is ≈ 280 lines downstream). |
| alpha-E | §6 C-4 same-wave consumer plan | ACCEPT | E-4 module-path discipline at lines 514-524 + 545-547 byte-identical to V2+V3+V4; `runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` REJECT pre-emptions preserved. |
| alpha-E | §6 C-4 pre-blocked routes | ACCEPT | "No new public substrate API; no new BIR variant" preserved at line 193 and following; STAND from V2+V3+V4. |
| alpha-E | §7 C-5 clean revert | ACCEPT | STAND from V2+V3+V4. |
| alpha-E | §8 consolidated pre-blocks | ACCEPT | P-7 carried verbatim; STAND from V2+V3+V4. |
| alpha-E | §9 concurrency matrix | ACCEPT | STAND from V2+V3+V4. |
| alpha-E | §10 cost / caps / telemetry | ACCEPT | F-V5-α-E-1 substitutes "8 grammars" → "per rostered grammar" at line 756 and `8 × 30 = 240 min` literal → `N × 30 min where N = live rostered-grammar enumeration via cargo metadata | jq` at lines 770-774 (with C-4 variable `N → M` to avoid collision). Edit is bound to cost/cap text in §10; no substrate, no Track-binding, no scanner, no buffer is touched. The `cargo metadata | jq` reference cross-cites C-3's existing process-time gate pipeline; no new shell command, no new runtime artefact. CH5 surface unchanged; fold strengthens forward-blindness symmetric to the F-V4-α-E-1 generalisation. |
| alpha-E | §11 convergence / escalation | ACCEPT | Architectural-block escalation honoured per C-4 fail-mode; STAND from V2+V3+V4. |
| DISPATCH-CONTEXT | all §α-X | ACCEPT | STAND from V1+V2+V3+V4 (not re-authored). |

46 of 46 §-level dispositions ACCEPT. V5 CH5 ACCEPT-rate is **100 %**.

## §4 — Fresh-finding scan (V5 cycle)

The V5 confirming pass repeats the V2 / V3 / V4 fresh-finding mandate over the V5-state artefacts.

### §4.1 — Parallel-substrate scan

Surveyed all V5-state mentions of `sidecar`, `parallel substrate`, `UnionTape`, `second tape`, `public substrate`, `class/mask`, `renamed scanner`, `shared buffer`, `shared sink`, `shared dispatcher`, `cursor list`, `cursor/list`, plus the V3-introduced `runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` module-path tokens. Every occurrence is a refusal / pre-block / guard. Aggregate counts unchanged from V4: SYNTHESIS 5, HANDOFF 3, α-C 2, α-E 7. No occurrence is an authorisation.

The V5 micro-fold packet adds zero new substrate-mention occurrences. F-V5-α-E-1 introduces only the table-cell substitution `per rostered grammar`, the variable rename `N → M`, and the inline citation `cargo metadata | jq` (a cross-reference to the C-3 round-trip pipeline F-V4-α-E-1 corrected at α-E:362-387). Each new token is workspace-metadata / wave-scheduling concern, not bbnf substrate.

No new parallel substrate. No new sidecar producer. No public-substrate-API surface slip.

### §4.2 — Track 1 ≡ Track 2 collapse scan

The V5 state preserves both the F-15 `track2_entry_point` column (SYNTHESIS:240) and the C-1 α-C §2 P-7 triple gate (α-C:348-385). The V5 micro-fold packet did not touch SYNTHESIS or α-C; both anchors are byte-identical to V4. The `xtask gate-json` rejection logic continues to wire through the same gate path that rejects empty equality columns from C-2 (α-E §6 same-wave consumer plan at lines 487-490, address unchanged from V4).

No Track 1 ≡ Track 2 collapse vector surfaced in V5.

### §4.3 — Renamed-scanner Lock 1 violation scan

The V5 state preserves E-4's module-path discipline (α-E:514-524, 545-547) naming `runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` as pre-emptive REJECT targets. The α-C P-7 clause (c) (distinct buffer addresses) is the runtime correlate; clause (b) (distinct concrete `Sink` types via `TypeId` inequality) is the compile-time correlate. The triad still works in tandem to catch a renamed scanner sharing either the buffer or the sink type even when the symbol path is fresh.

No renamed-scanner violation surfaced in V5.

### §4.4 — V5-specific micro-fold cross-check

The V5 micro-fold packet contains exactly one fold: F-V5-α-E-1 (α-E §10 cost/cap table roster-count generalisation + wall-clock recomputation + C-4 variable rename). Its edit domain:

- F-V5-α-E-1 touches α-E §10 lines 753-774 only. The fold's diff is 6 insertions, 5 deletions (net +1 line below the wall-clock paragraph). α-E §6's C-4 / Lock-1 triad / module-path discipline (lines 437-558) is untouched. α-E §11's pre-block list (line 717) and E-8 narrative (line 738) are above the fold region (lines 717, 738 are upstream of line 753). CH5 surface in α-E is preserved at identical V4 line addresses.

The V5 commit body's own attribution of F-V5-α-E-1 is `CH2 V4 non-finding` (cost/cap table roster-count generalisation). The fold-attribution sits squarely on the CH2 / CH4 axis (Lock-14 forward-blindness + cost-cap reasonableness); CH5 surface was never on the fold-list.

### §4.5 — Convergence

Per `ORCHESTRATOR.md §3Z`, convergence requires ≥ 95 % ACCEPT for two consecutive cycles. CH5 has logged V2 = 100 %, V3 = 100 %, V4 = 100 %, and now V5 = 100 % — four consecutive cycles at the ceiling, double the two-consecutive-cycle bar. The CH5 lens has been at lock-state since V2 close; V3, V4, and V5 simply reconfirm. V5 is the §3Z confirming pass at the V≤5 ceiling; CH5 closes its bracket participation at the same lock state.

## §5 — Recommended folds for V6 (if any)

**None.** V5 is the V≤5 ceiling confirming pass; no V6 cycle is sanctioned within the SK-V14 alpha-hardening bracket. Every V2 / V3 / V4 CH5 anchor persists byte-for-byte in the V5 state at unchanged absolute line addresses (the V5 fold sits ≈ 240-280 lines downstream of every CH5 anchor; the +1 net line shift propagates only to lines > 770). The V5 micro-fold targets the CH2 / CH4 cost/cap surface and binds verbatim to the V4 CH2 non-finding prescription; it introduces no CH5-surface defect. The fresh-finding scan surfaced zero new REJECT and zero new REVISE.

CH5 verdict for V5: the substrate-union architecture is locked. The Lock-1 triad propagates coherently. The Track 1 ≡ Track 2 boundary is mechanically enforced by the triple gate (symbol path + TypeId + buffer address). The renamed-scanner-with-shared-buffer slip is closed by the module-path discipline plus the buffer-address inequality check. No further CH5 fold is required for the SK-V14 alpha-hardening bracket.

The lens releases to G-Alpha auto-sign per the SK-V14 ORCHESTRATOR-PROMPT pin. The §3Z multi-cycle convergence requirement is satisfied on CH5's own terms (four consecutive ≥ 95 % cycles); the orchestrator proceeds directly to S-P0 once the V5 aggregator confirms the bracket-wide chain.
