# AX.W15 — FINAL + Bench Matrix + AY Handoff

**Opens after**: W14 close
**Agents**: 1 serial
**Hard gate**: Every entry ≥ post-AU; JSON ≥ sonic × 1.07×; parallel ≥ 2.5× on ≥ 1 MB; zero parity divergence; zero DTA symbols; AY handoff contract verified clean; `docs/tranches/AY/AY.md` exists.

## Scope

Tranche close + AY handoff verification. No new substrate; consolidation + FINAL.md authoring + AY first-draft.

1. **`docs/tranches/AX/FINAL.md`** — per-wave ledger + gate-status summary + post-tranche-review decisions (cf. invariant 18 + defensible-floor/post-tranche-review from parent AX.md).
2. **`docs/benchmarks/post-AX.json`** — 17-entry single-thread matrix + parallel matrix + parity results.
3. **AY handoff verification** — all 7 handoff-contract conditions (AX.md §AX → AY handoff contract).
4. **`docs/tranches/AY/AY.md` first draft** — if AY.md already exists (drafted in prior planning), verify alignment with AX's actual close state; if not, author it.
5. **Post-tranche review** for inconclusive-ledger levers: vpaddq_u8, vdotq_s32, PMULL verify, Bloom+GADT, ShapeRef consumer. Decision at close: retain or retire.

## File bounds

| File | Access |
|---|---|
| `docs/tranches/AX/FINAL.md` | create |
| `docs/benchmarks/post-AX.json` | create |
| `docs/benchmarks/post-AX-parallel.json` | create |
| `docs/tranches/AX/PROGRESS.md` | append W15 close entry |
| `docs/tranches/AY/AY.md` | create OR verify existing alignment |

## Phase sub-items

### W15.1 Per-wave ledger consolidation

For each of W0a–W14: cite commits, hard-gate-pass evidence (nm/cargo asm/wire-contract/samply/bench), investigation-queue resolutions, post-tranche-review dispositions.

### W15.2 Bench matrix

19-entry single-thread matrix:
- `json_monolithic` × {data_s, twitter, citm, canada, data_xl}
- `css_l4` × {normalize, bootstrap, tailwind}
- `google_sheets_monolithic` × {parse_simple, parse_nested, parse_stress}
- `bbnf_monolithic` × {json, ebnf, css_pretty, google_sheets, bbnf_self, css_l4_grammar}

Parallel matrix: same entries with `ParseOptions::parallel_threshold = 0` (force parallel); report speedup ratios vs single-thread.

Parity results:
- sonic-rs zero-divergence on 5 JSON entries.
- lightningcss zero-divergence on 3 CSS entries.
- simdjson OnDemand zero-divergence on 5 JSON entries (lazy-path).
- serde_json zero-divergence on 5 JSON entries.
- cssparser token-parity on 3 CSS entries.

### W15.3 AY handoff contract verification

All 7 conditions (per AX.md):

1. `grep -rE 'dispatch_one|try_branch|advance_or_pop_with|dta_run|DtaTable|DtaState|FrameStack' crates/` returns zero.
2. `nm target/release/deps/{json,css_l4,google_sheets,bbnf}_monolithic-*` shows zero DTA symbols.
3. Every grammar's `parse()` routes through shape dispatcher.
4. All 17 bench entries ≥ post-AU on single-thread.
5. `has_w4_classified` and analog gate predicates DELETED (not merely narrowed).
6. All 9 e-graph rewrites active; 12 detector files deleted.
7. Zero `#[ignore]` in workspace.

Any miss is an AX-close BLOCKER, not an AY-open negotiation.

### W15.4 Post-tranche review decisions

Per AX.md §"Post-tranche review candidates" — for each of:
- vpaddq_u8 (W4)
- vdotq_s32 (W4)
- PMULL verify (W4)
- Bloom+GADT (W5)
- ShapeRef consumer (W5)

Review wave-close ledger's bench-delta evidence. Decision: RETAIN (lever stays in master) OR RETIRE (lever deletes in a W15 follow-up commit). Rationale documented per item.

### W15.5 AY.md verification / authoring

AY.md was drafted in prior planning (replay + recovery + incremental + JIT at Y5). Verify alignment with AX's actual close state:
- If AX delivered cleanly, AY.md's invariants hold.
- If AX close has residual debt (e.g. a surviving gate predicate, a LOW lever retained awaiting measurement), AY.md amends its handoff-invariant section.

## Hard gate

1. Every single-thread parse entry ≥ post-AU (17/17 gate per AX thesis).
2. JSON entries ≥ sonic × 1.07× on single-thread NEON.
3. Parallel entries ≥ 2.5× single-thread on ≥ 1 MB inputs (canada / citm / tailwind / data_xl).
4. Zero parity divergence across all five comparators.
5. Zero `#[ignore]` additions in AX (beyond those closed at W3).
6. Zero DTA symbols (`nm` + `grep` verified).
7. AY → AX handoff contract (7 conditions) all verify clean.
8. Post-tranche review decisions documented per inconclusive lever.
9. `docs/tranches/AY/AY.md` exists with thesis + invariants + wave-schedule.

## Verification artefacts

- `docs/tranches/AX/FINAL.md` with per-wave ledger.
- `docs/benchmarks/post-AX.json` + `post-AX-parallel.json`.
- `nm` outputs archived.
- `grep` outputs archived.
- Parity harness CI log pointing at the last passing run.
- `docs/tranches/AX/PROGRESS.md` final entry.

## Dependencies

- Depends on: W14 (multi-visitor examples integrated).
- Blocks: AY open.

## Indefatigability

When W15 closes, AX is the architectural terminus:
- One codegen path reached by ALL four grammars.
- Interpreter deleted; workspace crates renamed; no `bbnf-` prefix.
- First-class Value API with structural parity vs sonic-rs + lightningcss.
- SoA primary tape with AoS sidecar.
- Every viable novel lever deployed or rejected with rationale.
- Five structural-parity CI gates.
- Document-parallel fork as amortisation multiplier.
- E-graph grammar rewriting subsumes shape classification; detectors deleted.
- Subsystem ledger zero.
- Mid-wave benches run. Frozen gate predicates. AY.md drafted.

The 400-commit work augmented. Not forked.
