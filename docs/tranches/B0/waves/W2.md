# B0.W2 — Routine/Heavy Split + AY Runway Close

**Opens after**: W1 close
**Agents**: 2 parallel
**Hard gate**: Routine correctness, profiling-prep, and heavy proof loops are intentionally separated, and the AY executor can start W5 on the repaired runway immediately.
**Status**: planned

## Scope

1. Separate routine correctness loops from heavy bench/proof loops.
2. Align CI and public workflow with the repaired runway.
3. Close B0 with the measured command surface AY actually needs.

## File bounds

| File | Access |
|---|---|
| `Makefile` | modify |
| `.github/workflows/ci.yml` | modify |
| `scripts/test-tier.sh` | modify |
| `docs/instructions/tranche/SPEC.md` | modify |
| `docs/instructions/PROFILING.md` | modify |

**Do NOT touch**: parser/runtime code, AY runtime wave specs, any
`.bbnf` grammar.

## Phase sub-items

### B0.W2.1 Routine/heavy split

Mechanism: separate routine correctness commands from heavy bench and
proof commands in the public workflow and CI surface.

Files touched: `Makefile`, `.github/workflows/ci.yml`,
`scripts/test-tier.sh`.

Sub-gate: routine and heavy surfaces are distinct command paths.

### B0.W2.2 Instruction sync

Mechanism: update tranche and profiling instructions so the repaired AY
runway is the documented truth.

Files touched: `docs/instructions/tranche/SPEC.md`,
`docs/instructions/PROFILING.md`.

Sub-gate: instructions match the separated public command surface.

### B0.W2.3 Close proof

Mechanism: capture the command and timing artefacts that prove AY can
open W5 on the repaired runway immediately.

Files touched: `docs/instructions/PROFILING.md`.

Sub-gate: close artefacts exist and point to the public AY command
paths.

## Hard gate

1. Routine correctness commands no longer invoke heavy proof loops by
   default. Artefact: `docs/benchmarks/post-B0-W2-commands.txt`.
2. Routine command timings improve versus post-W1. Artefact:
   `docs/benchmarks/post-B0-W2-mid.json`.
3. The close ledger names the exact public AY proof commands and their
   separated surfaces. Artefact: `docs/benchmarks/post-B0-W2-commands.txt`.

## Verification artefacts

- `docs/benchmarks/post-B0-W2-commands.txt`
- `docs/benchmarks/post-B0-W2-mid.json`
- commit hashes for each W2 sub-phase

## Dependencies

- **Depends on**: W1 close
- **Blocks**: AY.W5
