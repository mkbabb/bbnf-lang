# AZ-III.W1 - O5 Reclose

**Name**: W1 - O5 Reclose
**Opens after**: W0 clean-dispatch gate.
**Agents**: up to 10 parallel.
**Hard gate**: the AZ-II.O5 close packet is refreshed and green: regen, no-default build, metadata, deletion scans, and A1 residue disposition.
**Status**: planned

## Scope

1. Refresh `cargo xtask regen --check` and repair grammar-general drift.
2. Re-run `cargo build -p bbnf --no-default-features --profile ax-iter`.
3. Prove metadata has no `tape` or `json-prototype` package.
4. Refresh deletion scans for `crates/tape`, public tape runtime,
   `json-prototype`, Gorgeous JIT, and A1 provenance residue.
5. Commit or delete stale O5 evidence; no stale blocked artifact may
   masquerade as current truth.

## File Bounds

| File | Access |
|---|---|
| `Cargo.toml` | modify-carve |
| `crates/core/**` | modify-carve |
| `crates/ir/**` | modify-carve |
| `crates/gorgeous/**` | modify-carve/delete stale JIT only |
| `docs/tranches/AZ-III/**` | modify |
| `docs/tranches/AZ-II/**` | modify-carve |
| `docs/benchmarks/AZ-III/W1-*.txt` | create |
| `docs/benchmarks/AZ-II/cutover/O5-tape-delete-scan.txt` | modify/archive |

Do NOT touch: BA/BB source, path APIs, rewrite inference, optimization work.

## Agent Units

### AZ-III.W1.1 Regen Drift

- Mechanism: identify generated drift root causes and repair shared
  emitter/IR causes before regenerating.
- Files: generator source and generated grammar files only as required.
- Sub-gate: `cargo xtask regen --check` passes after committed regen.

### AZ-III.W1.2 No-Default Build

- Mechanism: verify no-default build and repair only source-owned
  blockers.
- Files: build-facing source or manifests.
- Sub-gate: `cargo build -p bbnf --no-default-features --profile ax-iter`.

### AZ-III.W1.3 Deletion and Metadata Audit

- Mechanism: prove package and source deletion surfaces are gone.
- Files: manifests, docs evidence.
- Sub-gate: `cargo metadata --locked --no-deps --format-version 1` has
  no `tape` or `json-prototype` package.

### AZ-III.W1.4 A1 Residue Redress

- Mechanism: delete or archive live-product, prototype, JIT, and
  provenance residue named by O3a-A1.
- Files: A1-owned docs/source surfaces only.
- Sub-gate: refreshed grep artifact has no live-product A1 residue.

## Hard Gate

1. Regen check green.
2. No-default build green.
3. Metadata clean.
4. Deletion scan clean or every hit is explicitly historical archive.
5. AZ-II O5 docs point to AZ-III W1 close evidence.

## Format And Lint Cadence

Run Rust formatting and lint/build gates after each accepted source
integration batch: at minimum `cargo fmt --check`, the wave-owned build
or test command, and `git diff --check`. Run `cargo xtask regen --check`
after generator changes and again at close.

## Verification Artefacts

- `docs/benchmarks/AZ-III/W1-regen-check.txt`
- `docs/benchmarks/AZ-III/W1-no-default-build.txt`
- `docs/benchmarks/AZ-III/W1-metadata.txt`
- `docs/benchmarks/AZ-III/W1-deletion-scan.txt`

## Commit Plan

- Source fixes use concrete scopes such as `grammar/generated`,
  `emitter/struct-direct`, `runtime/bbnf`, or `cutover.O5`.
- Generated refresh is separate unless inseparable from the generator
  fix.
- Close docs commit: `docs(az-iii.W1): close O5 continuation evidence`.

## Dependencies

- **Depends on**: W0.
- **Blocks**: W2 semantic truth and W4 benchmark truth.

## Archaeology

AZ-II.O5 had stale blocked evidence: no-default build failure became
obsolete, but regen drift stayed red. W1 refreshes truth before
claiming closure.
