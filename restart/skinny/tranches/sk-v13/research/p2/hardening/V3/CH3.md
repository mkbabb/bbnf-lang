# SK-V13 S-P2 V3 CH3 Regression / REDRESS

Verdict: ACCEPT.

## Evidence

- V3 preserves the S-P2 CH3 bar. The challenge prompt requires rejection when a
  candidate reopens REDRESS 28/33, 82-84, 88/89, historical blocked routes, or
  related pre-blocks without fresh P1 evidence plus new framing
  (`restart/prompts/skinny/PASS-2-RESEARCH.md:109`-`:117`). The V2
  consolidation left only one V3 fold action: prevent P2-F's CSS row/fact-stream
  scopes from reading as immediate primitive eligibility, while preserving the
  accepted REDRESS and Lock 1 folds
  (`restart/skinny/tranches/sk-v13/research/p2/hardening/HARDENING-S-P2-V2-CONSOLIDATED.md:22`-`:36`).

- V3 resolves the CSS row-scope risk without converting it into a route reopen.
  P2-F now defines `CSS-ROW-SCOPE-CONDITIONAL` as a generated CSS parity
  row/fact-stream scope, not a primitive admission, requiring either fresh narrow
  CSS parser profile or same-wave strict lightningcss/cssparser row movement
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:44`-`:47`).
  The six CSS rows are stamped with that verdict and each row says it is not
  primitive eligibility (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:69`-`:74`).
  P2-F repeats the fold in the grammar-neutrality and V3 disposition sections:
  CSS rows 1-6 are conditional row-production scopes, not CH1 primitive hot-leaf
  evidence or primitive eligibility
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:123`-`:132`,
  `:158`-`:161`, `:240`-`:245`). This is not a support-only closure because any
  SIMD primitive inside such a row inherits scalar/checkasm/consumer gates.

- REDRESS-126 remains a production split, not retroactive SIMD admission. The
  REDRESS record says W4 was `ROUTE-PRODUCTION-SPLIT`, not CSS ADMIT, not
  production SIMD/ASM admission, and not `RESULTS.md` movement
  (`skinny/REDRESS.md:3766`-`:3773`); it also says W4 did not wire generated CSS
  or claim a same-wave production consumer (`skinny/REDRESS.md:3800`-`:3805`).
  V3 preserves that distinction. P2-B allows `a64_ascii_set_run_skip` production
  wiring only as a future CSS scanner consumer and forbids counting SK-V12 W4 as
  row movement (`restart/skinny/tranches/sk-v13/research/p2/p2b-dav1d-process.md:64`-`:76`).
  P2-A marks C7 as a conditional route-production candidate requiring a fresh
  narrow CSS parser profile or same-wave CSS scan-block measurement; it cannot
  close as another microbench-only helper
  (`restart/skinny/tranches/sk-v13/research/p2/p2a-sota-teardown.md:89`-`:94`).
  P2-F maps C7 the same way (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:118`).

- REDRESS 28/33 and 82-84 are not reopened through string, unicode, or dispatch
  renames. The historical record rejects active 16-byte tiny-string dispatch and
  invalidates Class A `match_tiny_plain_string` NEON wiring as the parse-G fix
  (`skinny/REDRESS.md:324`-`:337`, `:394`-`:418`). It also rejects the
  single-quartet unicode classifier, generated-retained StringBlock16 tiny probe,
  and object-pair value-byte control compaction
  (`skinny/REDRESS.md:2285`-`:2316`, `:2318`-`:2356`, `:2358`-`:2395`). V3 keeps
  string/unicode candidates policy-driven and row-moving rather than replaying
  those exact shapes: P2-A rejects REDRESS 28/33 and 83 replays without
  grammar-neutral policy and row movement
  (`restart/skinny/tranches/sk-v13/research/p2/p2a-sota-teardown.md:75`-`:76`,
  `:136`-`:139`), P2-E blocks tiny-string NEON, single-quartet unicode, and
  object-pair replay (`restart/skinny/tranches/sk-v13/research/p2/p2e-parse-that-gaps.md:138`-`:143`),
  and P2-F requires generated string/escape policy before generality claims
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:78`,
  `:85`-`:86`).

- REDRESS 88/89/90 and orphan accounting stay bounded. REDRESS 88 rejected PMULL
  prefix-XOR as a default production hot body, REDRESS 89 rejected the CSSC
  CTZ/bulk consumer, and REDRESS 90 admitted only stack-canary hardening while
  keeping both bitmap body fills rejected (`skinny/REDRESS.md:2510`-`:2540`,
  `:2542`-`:2585`, `:2587`-`:2618`). V3 keeps PMULL/CSSC as a materially
  different union route with scalar reference, checkasm, same-wave consumer, and
  REDRESS 88/89/96/97/98 citations, not local default-body replacements
  (`restart/skinny/tranches/sk-v13/research/p2/p2c-arch-esoterica.md:16`,
  `:38`, `:59`-`:61`). Standalone prefix-XOR, next-bit, bulk emit,
  `byte_context`, cache hints, EOR3/BCAX, and D1 capacity remain inventory or
  `NOT-S-P3-ELIGIBLE` in P2-F unless a later accepted research cycle adds fresh
  evidence and a same-wave consumer
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:90`-`:95`,
  `:162`-`:166`, `:255`-`:259`).

- REDRESS 96/97/98 material differentials are preserved for union/structural
  work. The historical failures were correctness-green but measured failures:
  the class-column implementation regressed every must-improve row, the
  allocation-free streaming cursor also failed every required row, and REDRESS
  98 retired the old union-substrate gate
  (`skinny/REDRESS.md:2795`-`:2848`, `:2850`-`:2906`, `:2910`-`:2949`). V3 does
  not turn scanner micro-evidence into a retained sidecar or route reopen. P2-D
  says structural SIMD is scanner micro-signal only and does not reopen a retained
  SIMD-position vector, streaming cursor, class column, or parser-owned cursor
  (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:62`-`:85`).
  Its surviving D2/D3 shapes state the material differential: codegen-time
  same-tape event projection with no retained class side vector, or mask-to-tape
  writeback where the retained projection is the tape
  (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:187`-`:227`,
  `:229`-`:276`). P2-F repeats that union variants are legal only as
  codegen-private tape/fact routing over one substrate and must carry REDRESS
  96/97/98 differentials (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:147`-`:149`,
  `:174`-`:178`).

- REDRESS 119/120 direct fixpoints are not turned into profile-envelope
  admissions. REDRESS 119 closes W8 as a measured direct fixpoint with no source
  intervention or row movement, and REDRESS 120 closes SK-V11 as a measured
  fixpoint, not direct GO or grammar-generalization admission
  (`skinny/REDRESS.md:3497`-`:3527`, `:3531`-`:3553`). V3 preserves that
  boundary: P2-B and P2-D require prior-fixpoint citation plus fresh material
  differential for every direct reopen
  (`restart/skinny/tranches/sk-v13/research/p2/p2b-dav1d-process.md:137`-`:139`;
  `restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:400`-`:404`),
  and P2-F says P1 profile signals do not reopen direct rows by themselves
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:187`-`:191`).

## Blockers / Fold Requirements

No CH3 blocker for V3.

Carry these S-P3 preconditions forward:

1. CSS rows 1-6 are conditional row-production scopes only. S-P3 must require
   fresh narrow CSS parser profiling or same-wave strict lightningcss/cssparser
   row movement before planning them, and must not cite them as primitive
   hot-leaf admissions.
2. `a64_ascii_set_run_skip` / `ByteSetRunSkip64` may be shortlisted only as CSS
   production wiring with same-wave production consumer evidence. A second
   microbench-only close would replay REDRESS-126.
3. Union and structural SIMD candidates must cite REDRESS 96/97/98 and make the
   projection the existing tape or sink-only fact stream. Retained structural
   vectors, class columns, streaming cursors, parser-owned cursors, aux density
   tables, and class-lane-only variants remain rejected.
4. PMULL, CSSC CTZ, prefix-XOR, next-bit, bulk emit, `byte_context`, cache
   hints, EOR3/BCAX, LD4/TBX/SMIN/SMAX, and standalone D1 lazy capacity cannot
   enter S-P3 as support-only waves. They need fresh accepted evidence, scalar
   oracle, checkasm/parity, material differential, and same-wave measured
   consumer.
5. String/unicode/number candidates must remain grammar-policy driven and
   row-moving. JSON-shaped tiny-string, single-quartet unicode, StringBlock16
   tiny-probe, object-pair compaction, and one-row numeric patches remain
   rejected replays unless materially reframed with strict row evidence.

## Disposition

CH3 accepts V3. The current V3 packet preserves REDRESS material differentials
and does not accidentally turn CSS row scopes into route reopens or support-only
closures. Route to the V3 aggregator with the fold requirements above as S-P3
preconditions, not V3 revision blockers.
