---
lens: CH5 HIDDEN-COUPLING
pass: T-P2-research (SK-V17 totality)
cycle: V3
generated_at: 2026-05-29T00:00:00Z
reviewer_method: "PASS-2-RESEARCH §3 CH5 + ORCHESTRATOR §3W/§3Z. Re-read all six V3 2X dossiers (2a..2f) + their prior_cycle_dispositions_folded / v3_fold_log / first_cycle_additions blocks + the V2 CH5 disposition set (hardening/V2/CH5.md, the single open REVISE CH5-V2-001) + the V1 spec surfaces (restart/locks/LOCKS.md Lock 1 :75, v+1 ELEVATION :137-149; Lock 10 5-shape :107-108 + manifest vocab :118-127; LAC-1E-14 substrate-manifest category :100-116; restart/ARCHITECTURE.md plane (4) :1804, :1088, :1206). Every load-bearing substrate / sidecar / classifier-state / substrate_target / retention_lifetime / live-coupling-site claim re-anchored LIVE at master HEAD 91b6893b0 via grep -n/-c + sed -n against skinny/crates/runtime/src/tape/mod.rs, crates/core/src/runtime/{bbnf/arena.rs,tape/mod.rs}, crates/simd-scan/src/alphabet.rs. The single V2 REVISE traced from its disposition text to its V3 fold-site to confirm SUBSTANTIVE fold, not paper-fold. All V3 first_cycle_additions / precision_sharpenings scanned for NEW coupling."
master_head: 91b6893b0b61d1c3213d02afe4ec62f22c16ae38
t_p1_locked_sha: 91b6893b0b61d1c3213d02afe4ec62f22c16ae38
scope: "substrate-union (Lock 1) preserved — tape+projection ONE substrate; tape NOT a silent 6th BackendShape (Lock 10 5-shape canon); proposes substrate-manifest category OR G-Omega-gated 6th, never silent; mask stream = transient producer not retained sidecar; Layer 0/Layer 1 clean two-layer dependency not a coupling; no cross-call classifier state (Lock 1 v+1)"
first_hygiene_action: "CH1-V5-001 (enumerated-filename residual) — re-verified RESOLVED on disk at 91b6893b0: skinny/crates/codegen/src/lower/collapsed_stage.rs EXISTS; collapsed_tape.rs ABSENT; grep 'collapsed_stage}.rs' / ',collapsed}' / 'collapsed_tape' across 1a/1e = 0. No enumerated-filename residual remains in 1a or 1e. No fold required on first touch."
v2_revise_folded:
  CH5-V2-001: FOLDED-SUBSTANTIVE     # FOLD-L5 (2b:271-273) + FOLD-L6 (2b:299-303) prose labels 'within-block-only'/'within-call-only' replaced with canonical Lock 1 v+1 enum 'retention_lifetime = transient-single-call' (LOCKS.md:146-148), descriptive gloss retained; frontmatter revised: list 2b:21
disposition_counts:
  accept: 24
  revise: 0
  reject: 0
  total: 24
accept_rate_pct: 100.0
convergence_note: "Second consecutive >=95% CH5 cycle (V2 95.8%, V3 100.0%); zero orphan REVISE; V3 <= 5. CH5 axis meets the ORCHESTRATOR §3Z convergence gate."
---

# CH5 HIDDEN-COUPLING — SK-V17 T-P2 V3

## Verdict

The single V2 hidden-coupling residual — **CH5-V2-001**, the FOLD-L5/L6
manifest-vocabulary drift — is **substantively folded** at V3: both Lock-16
surfaces now carry the canonical Lock 1 v+1 enum token
`retention_lifetime = transient-single-call`, anchored to `LOCKS.md:146-148`,
with the descriptive "within block sequence" / "within `scan_components_to_index`
call" prose demoted to gloss. The V3 cycle introduced **no new substrate, no
sidecar producer, no cross-call classifier state, and no silent 6th
`BackendShape`** — the only V3 additions are CH1-lane precision sharpenings
(grep counts, intrinsic-name census, anchor re-targeting) that *narrow* claims
already coupling-clean at V2. The dossier set has crossed from
"coupling-clean by manifest" (V2) to **"coupling-clean by manifest, with every
manifest token canonical to its governing lock"** (V3).

All four central CH5 charges hold at V3, each re-anchored LIVE at `91b6893b0`:

- **No silent 6th `BackendShape`.** Every `6th`/`sixth`/`silent shape` mention
  across all six dossiers is in the negative — refuted (`2f:13`, `2f:79`,
  `2f:273`; `2d:74`,`:78`), fenced as the substrate-manifest CATEGORY
  (`2f:233-251`, `2d:87-110`, `2c` SK17-2C-D), or explicitly G-Omega-gated
  (`2f:543-544`, `2d:108-110`). The 5-shape canon
  `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` is held verbatim
  (`LOCKS.md:107-108`, re-verified live). The tape is the substrate-manifest
  category the five shapes project FROM, on the LAC-1E-14 FactStream precedent
  (`LOCKS.md:100-116`) PLUS the independent `admits_collapsed_stage` x86-binding
  mechanical anchor (`ARCHITECTURE.md:1206`, x86+avx512bw+Entry — mechanically
  refuses on aarch64) — the no-6th verdict rests on TWO grounds (CH6-V1-V03,
  re-confirmed at `2a:cross_cutting`/`2f:28`), not the precedent-echo alone.
- **No parallel substrate / retained sidecar.** The AoS `TapeRec`/
  `TapeStructBuilder` ↔ SoA `Tape` dual is named the *transient fold-state only*
  with the dual end-state a Lock-1 violation (`2a:75-76`/`:109-111`, `2f`,
  `2d:FOLD-2D-04`). The eager `OpenFrame` `Vec<OpenFrame>::clone` 86.07%
  pathology is the DELETION target (`LOCKS.md:75`, `2a:76`, `2f:509-511`). The
  mask stream is a transient producer (ARCH plane (4) `:1804`, re-verified). The
  fact-stream is fenced oracle-only (`admitted_fact_output`, "NOT re-admitted
  into the one tape", `2b:209-211`). `PayloadArena` + sparse-flag pair are
  declared MEMBERS of the one tape (`existing_tape`/`output_row`), not a second
  store.
- **No cross-call classifier state.** The Lock 1 v+1 ELEVATION
  (`LOCKS.md:137-149`, "no cross-call retained classifier state. Period.") is
  honoured; the L1 classifier + L5/L6 masks declare
  `retention_lifetime = transient-single-call`; `depth_carry` is init-0-per-parse,
  threaded WITHIN one `scan_components_to_index` call, NEVER retained across
  calls (`2b:268-272`/`:299-303`).
- **Layer 0 / Layer 1 clean two-layer dependency.** 2b keeps the dependency
  one-directional (PV17-002/003); `CollapsedStage` spine x86-pinned + aarch64
  mechanically refused; no hidden x86 close-path coupling.

§3Z expects convergence toward all-ACCEPT; this lens returns **100% ACCEPT on
the CH5 axis** (24/24, zero REVISE, zero REJECT) — the second consecutive
≥95% CH5 cycle (V2 95.8% → V3 100.0%) with zero orphan REVISE and V ≤ 5,
clearing the convergence gate on this lens.

---

## V2 REVISE fold-verification (CH5-V2-001 traced to source)

| V2 finding | V3 fold-site (verified live) | substantive? |
|---|---|---|
| **CH5-V2-001** FOLD-L5/L6 `retention:` prose labels (`within-block-only` / `within-call-only`) are not canonical Lock 1 v+1 `retention_lifetime` enum members, admitting a T-P3 mis-read as `retained-within-chunk` | **FOLD-L5** (`2b:271-273`): `retention_lifetime = transient-single-call` "(CH5-V2-001 fold — the canonical Lock 1 v+1 enum value, `LOCKS.md:146-148`; the carry is a 1-bit overflow within one block sequence, the descriptive gloss 'within block sequence' is single-call-bounded per its own body)". **FOLD-L6** (`2b:299-303`): `retention_lifetime = transient-single-call` "(CH5-V2-001 fold … the `depth_carry` is init-0-per-parse, threaded WITHIN a single `scan_components_to_index` call and NEVER retained across calls … NOT `retained-within-chunk`)". Frontmatter `revised:` list carries `CH5-V2-001-FOLD-L5-L6-retention-prose-replaced-with-canonical-retention_lifetime-transient-single-call-enum` (`2b:21`). | YES — both manifest tokens now match their bodies AND the Lock 1 v+1 enum; the descriptive prose is retained as gloss; the row explicitly disambiguates against the `retained-within-chunk` mis-read the V2 REVISE flagged. No paper-fold. |

The fold is the EXACT edit CH5-V2-001 specified: canonical token in the Lock-16
surface, gloss preserved. It additionally aligns FOLD-L5/L6 with the L1-classifier
row (`2f:296` / `2a:417`) and with FOLD-L9's `retained-across-call-boundary`
REJECT-class citation, completing the dossier's lifetime-vocabulary consistency.

---

## Live-anchored coupling re-verifications (V3, all at `91b6893b0`)

| Coupling-sensitive fact | Live check | Result |
|---|---|---|
| 5-shape canon held verbatim; 6th G-Omega-gated | `LOCKS.md:107-108`; grep `6th`/`sixth`/`silent…shape` across 2X = all-negative | CONFIRMED |
| Substrate-manifest CATEGORY precedent (LAC-1E-14) | `LOCKS.md:100-116` (FactStream 5th category; "NOT a 6th `BackendShape` variant") | CONFIRMED |
| **Dual `retention_lifetime` vocabulary mapped correctly per primitive class** | Lock 10 manifest set `{local_loop, generated_function, output_row}` (`LOCKS.md:120-124`) for RETAINED tape members; Lock 1 v+1 set `{transient-single-call, retained-within-chunk, retained-across-call-boundary}` (`:146-148`) for TRANSIENT classifier carries | CONFIRMED — L3/L8 PayloadArena+sparse use `output_row` (retained member); L5/L6 masks + L1 classifier use `transient-single-call` (transient carry); each primitive class uses its GOVERNING lock's enum, not a cross-contaminated token |
| Mask stream = transient producer | ARCH `:1804` plane (4) "Transient producer; never a retained sidecar … structural projection IS the tape" | CONFIRMED |
| LIVE `arena.rs:47` coupling-site (the FOLD-B fence target) | `crates/core/src/runtime/bbnf/arena.rs:47` `match StructRegistry::compound_kind_for_layout(layout)` inside `BbnfCompoundKind::from_layout` (eager arena path) | CONFIRMED — present-tense wire FOLD-B severs |
| Tape path fence-clean | `crates/core/src/runtime/tape/mod.rs:185` `begin_compound` reads `layout.rule_id & 0x1F`; `grep StructRegistry crates/core/src/runtime/tape/` = 0 | CONFIRMED |
| SoA `Tape` retains `payloads: PayloadArena` + sparse `flag_cursors`/`flag_values` | `skinny/crates/runtime/src/tape/mod.rs:97-99` (six members: `source`, `offsets`, `flag_cursors`, `flag_values`, `payloads`, `id`) | CONFIRMED — the members L3/L8/F2 classify as `existing_tape` |
| `ValueRef<G>` projects over `&'doc Tape`, no second tree | `skinny/crates/runtime/src/tape/mod.rs:175` `ValueRef<'doc,'input:'doc,K=AnyKind,G:EventGrammar>{ tape:&'doc Tape, cursor:u32, … }` | CONFIRMED — lazy view, not a parallel value substrate |
| `TapeStructBuilder` (AoS) UNWIRED in core production (V3 grep-precision) | `crates/core/src/runtime/tape/mod.rs:58` defines it; only `crates/core/tests/tape_substrate.rs` wires it; production runtime grep-zero external construction | CONFIRMED — the dead AoS shape is fold-input, not a live second substrate |
| Shared NEON classifier alphabet-as-data (the only grammar datum) | `crates/simd-scan/src/alphabet.rs:19-37` `StructuralAlphabet` struct (singletons/digraph_mask/digraph_pairs/quote_classes) | CONFIRMED — grammar-neutral primitive, no retained classifier state |
| Lock 1 v+1 no-cross-call-carry | `LOCKS.md:137-149` "Carry MUST stay within a single chunk-call boundary" | CONFIRMED |

---

## V3 first_cycle_additions / precision_sharpenings — scanned for NEW coupling

The V3 cycle added NO new fold candidates; every change is a CH1-lane precision
sharpening or a re-confirmation. Each scanned on the CH5 axis:

| V3 addition | CH5 verdict |
|---|---|
| 2a `TAPESTRUCTBUILDER-GREP-PRECISION` (prior "grep-zero outside runtime/tape/" → precise: 1 TEST site `tests/tape_substrate.rs`; production grep-zero) | ACCEPT — sharpens the divergence-A claim WITHOUT introducing a live second substrate; the AoS builder remains dead/test-only, the UNWIRED verdict unchanged. No coupling. |
| 2a `EQSET-INTRINSIC-COUNT-PRECISION` (12 → 8 distinct NEON intrinsics / 27 calls, 87-LOC body) | ACCEPT — primitive-body census; no substrate/state implication. |
| 2a `CH6-V1-V03-CONFIRMED` (independent `admits_collapsed_stage` ARCH:1206 anchor re-verified) | ACCEPT — strengthens the no-6th-shape verdict; defence-in-depth, not a new mechanism. |
| 2f `CH1-2F-01-RESIDUAL` (LAC-2F-FOLD-03 re-anchored `alphabet.rs:118` → `:19-37` for the `StructuralAlphabet` struct) | ACCEPT — anchor-precision; the classifier remains alphabet-as-data with no retained state; zero orphan. |
| 2b `PV17-001..007` (PrimitiveKernels struct IS Layer-1 vocab; Layer-1-in-Rust; x86inc Layer-0 vendored skinny-only; checkasm dav1d discipline; eq-set the one real NEON body; L5/L6 net-new masks grammar-neutral; Lock-16 manifest-row schema) | ACCEPT — all are vocabulary/manifest framings already coupling-reviewed at V1/V2; PV17-002/003 keep Layer 0/Layer 1 one-directional (no coupling); PV17-006 carries the L5/L6 masks whose retention token is now canonical (CH5-V2-001 folded). |
| 2c/2d `SK17-2C-*` / `T2D17-*` (re-stated D/E/F folds) | ACCEPT — restatements of V2-ACCEPTed folds; substrate_target manifest preserved; no new substrate. |

No V3 addition proposes a parallel substrate, a sidecar, a cross-call classifier
carry, or a silent 6th shape. The precision sharpenings only *tighten*
already-clean claims.

---

## §3W lens-axis dispositions (V3, per dossier section)

### 2a — SOTA-landscape
| § / candidate | disposition | rationale (CH5 axis) |
|---|---|---|
| FOLD-2A-A flat-tape one-encoding (AoS↔SoA) | ACCEPT | Dual = transient fold-state only; end-state Lock-1 violation; AoS `TapeStructBuilder` test-only (V3 precision), production grep-zero. |
| FOLD-2A-B eager OpenFrame retirement | ACCEPT | Deletes the 86.07% `Vec<OpenFrame>::clone` substrate; retirement REDUCES coupling. |
| FOLD-2A-C lazy `ValueRef<G>` plane | ACCEPT | View rides the one tape (`tape/mod.rs:175`); PayloadArena declared `existing_tape`. |
| FOLD-2A-D tape = substrate-manifest not 6th shape | ACCEPT | LAC-1E-14 precedent + independent `admits_collapsed_stage` ARCH:1206 corroboration; "propose, do NOT silently add" satisfied. |
| FOLD-2A-E NEON classifier | ACCEPT | `substrate_target=existing_tape` a PRE-condition resolved after OnceCell classify-before-wiring (LAC-2A-SKV17-02); alphabet-as-data, no retained state. |
| FOLD-2A-F StructRegistry fence | ACCEPT | Names live `arena.rs:47`; fence severs a present wire. |

### 2b — Primitive-vocabulary
| § / candidate | disposition | rationale (CH5 axis) |
|---|---|---|
| A1 two-layer vocabulary (Layer 0 vendored / Layer 1 Rust) | ACCEPT | One-directional dependency (PV17-002/003); realization-medium reconcile, not a coupling. |
| FOLD-L2 `push_plain_offset` + fact-stream oracle fence | ACCEPT | Fact-stream fenced `admitted_fact_output`, NOT re-admitted into the tape (`2b:209-211`). |
| FOLD-L3 PayloadArena | ACCEPT | Four-token manifest: `existing_tape`/`output_row`/`generated_grammar` + `write_count==0` bound (`2b:217-227`); member of one tape, not a sidecar. |
| FOLD-L4 tokenize-once shared-scan | ACCEPT | Index == tape offsets OR `local_temp_only`; REDRESS-53 pre-block. |
| **FOLD-L5 `comment_body_mask_64` / FOLD-L6 `bracket_depth_mask_64`** | **ACCEPT** (was CH5-V2-001) | FOLDED: both Lock-16 surfaces now carry `retention_lifetime = transient-single-call` (`2b:271-273`/`:299-303`), canonical Lock 1 v+1 enum (`LOCKS.md:146-148`), gloss retained, explicit disambiguation against `retained-within-chunk`. Bodies were already coupling-correct; the token now matches. |
| FOLD-L7 `OneShotSimd` capacity | ACCEPT | Sizes the EXISTING `offsets`; no second vector. |
| FOLD-L8 sparse-flag | ACCEPT | `flag_cursors`/`flag_values` PRE-EXIST as tape members; `existing_tape`/`output_row`; "not 'no substrate'; adds no SECOND substrate" (`2b:336-338`). |
| FOLD-L9 Alt-mode (DEFERRED appendix) | ACCEPT | Recorded-not-shortlisted; O(1) `offsets.len()`/`truncate` rollback; no `split_off`, no `Vec<Vec>`; cites `retained-across-call-boundary` as the REJECT class. |
| FSM/frame-stack + udot-CSS refuted | ACCEPT | Filed `architectural-block-with-REDRESS`; CollapsedStage x86-pinned + aarch64-refused. |

### 2c — Grammar-neutrality
| § / candidate | disposition | rationale (CH5 axis) |
|---|---|---|
| SK17-2C-A flat-tape grammar-neutral substrate | ACCEPT | Grammar-column-free; sparse flags only; bars the AV.04 dense class column. |
| SK17-2C-B OpenFrame retirement | ACCEPT | Reduces coupling; no-delete-before-replacement fence. |
| SK17-2C-C lazy `ValueRef<G>` | ACCEPT | Projection over the one tape; no second value tree. |
| SK17-2C-D tape = substrate-manifest not 6th shape | ACCEPT | "any proposal that reads as a 6th `BackendShape` is REJECT (no silent shape)". |
| SK17-2C-E NEON classifier | ACCEPT | No retained cross-call classifier state (Lock 1 v+1); alphabet per-call constructed. |
| SK17-2C-F FieldSource fence | ACCEPT | Shared 2f-F6 owner names runtime `StructRegistry` walk as the refuted indirection. |
| SK17-2C-ONBOARD future-grammar gate | ACCEPT | verify_action with live HEAD baseline; a generic-crate grammar branch = coupling = FAIL. |

### 2d — Cost-model + 5-shape
| § / candidate | disposition | rationale (CH5 axis) |
|---|---|---|
| FOLD-2D-01 tape = substrate-manifest not 6th shape | ACCEPT | Exact LAC-1E-14 reuse; 5-shape domain held verbatim (`2d:87-110`); the load-bearing D verdict. |
| FOLD-2D-02 cost selects per-rule INTO the one tape | ACCEPT | "a shape selecting a NEW substrate is the Lock-1 violation the cost model must reject in CSP"; e-graph rejects non-admitted `substrate_target`. Mask stream stays a transient producer — the CH5 §3W requirement on 2D. |
| FOLD-2D-03 lazy `ValueRef<G>` plane | ACCEPT | Read strategy over a retaining shape; not a shape. |
| FOLD-2D-04 AoS↔SoA single-encoding | ACCEPT | Dual end-state = Lock-1 violation. |
| FOLD-2D-05 NEON classifier scan-cost | ACCEPT | Never retains across calls; transient `Vec<u32>`. |
| FOLD-2D-06 FieldSource fence | ACCEPT | Names live `arena.rs:47` site; present-tense-anchored. |
| FOLD-2D-07 aarch64 CollapsedStage UNKNOWN-2D-05 | ACCEPT | NEON under four LLVM shapes' scan-leaf FFI; `admits_collapsed_stage` x86-bound; mechanically refused on aarch64. |

### 2e — Host-arch esoterica
| § / candidate | disposition | rationale (CH5 axis) |
|---|---|---|
| FOLD-2E-A..F (six folds) | ACCEPT | Each substrate-coupling claim re-anchored; "Fold Coherence Note (CH5 hidden-coupling pre-empt)" persists: folds co-dependent-not-orthogonal, "No fold implies a parallel substrate, a sidecar producer, or a Lock-1 violation." |
| FOLD-2E-B PayloadArena | ACCEPT | `payloads: PayloadArena` declared `existing_tape`/`output_row`/`generated_grammar` (LAC-2E-SKV17-04). |
| FOLD-2E-F fence | ACCEPT | Live `arena.rs:47` named; FOLD-B deletion severs it. |

### 2f — Fold-gaps
| § / candidate | disposition | rationale (CH5 axis) |
|---|---|---|
| F1 OpenFrame retirement / F3 AoS↔SoA / F4 tape-substrate-not-6th | ACCEPT | F4 primary D-refutation; "propose, do NOT silently add a 6th" discharged in the negative; independent `admits_collapsed_stage` anchor. |
| F2 lazy `ValueRef<G>` + PayloadArena | ACCEPT | Defended-#2 declares PayloadArena `existing_tape`/`output_row`/`generated_grammar` (`2f:516-521`). |
| F5 NEON classifier manifest row | ACCEPT | `retention_lifetime=transient-single-call`, `substrate_target=existing_tape`, same-wave consumer = the tape. CANONICAL token (matches L5/L6 post-fold). |
| F6 fence (OWNER of shared fence) | ACCEPT | Names `arena.rs:47` `StructRegistry::compound_kind_for_layout`, FOLD-B severs it, contrasts fence-clean `begin_compound` grep-zero `StructRegistry` (`2f:354-367`). Falsifiable. |
| F7 OnceCell classification (all 8 carriers) | ACCEPT | Names live `OnceCell<StructuralIndex>` retention; classify-before-wiring; same-wave consumer co-waved with F1/F3. The strongest CH5 row in the set. |
| F8 BackendShape selector wiring | ACCEPT | Selector outputs a projection mode, never a substrate; `substrate_target` binding on every `BackendExpr`. |
| F9 StructLayout rename | ACCEPT | Identifier reconcile; grammar-blind; no coupling. |
| LAC-2F-FOLD-01..05 | ACCEPT | No LAC proposes a parallel substrate or 6th shape; LAC-2F-FOLD-03 uses canonical `existing_tape`/`transient-single-call`. |

---

## What the lens did NOT find (the strong-coupling axes that PASS at V3)

- **No silent 6th `BackendShape`.** Every D-fold proposes the substrate-manifest
  CATEGORY explicitly (LAC-1E-14, `LOCKS.md:100-116`) + the independent
  `admits_collapsed_stage` x86-binding corroborating anchor
  (`ARCHITECTURE.md:1206`); the verdict rests on TWO grounds. 5-shape canon held
  verbatim (`LOCKS.md:107-108`); a 6th remains G-Omega gated.
- **No parallel substrate / sidecar.** AoS/SoA dual = transient fold-state only,
  end-state Lock-1 violation; AoS `TapeStructBuilder` test-only/production-dead
  (V3 precision); eager OpenFrame = deletion target; mask stream = transient
  producer; fact-stream = oracle-only `admitted_fact_output`, not re-admitted;
  `Vec<u32>` index == tape offsets.
- **No cross-call classifier state.** Lock 1 v+1 ELEVATION honoured; classifier +
  L5/L6 masks `transient-single-call` (now canonical at BOTH body and manifest
  token — CH5-V2-001 folded); `depth_carry` init-0-per-parse.
- **Retention vocabulary correctly partitioned by lock.** Retained tape members
  (L3 PayloadArena, L8 sparse flags) use Lock 10's manifest set (`output_row`);
  transient classifier carries (L5/L6, F5/L1 classifier) use Lock 1 v+1's set
  (`transient-single-call`). No primitive cross-contaminates the two governing
  enums — the precise defect CH5-V2-001 guarded against is now closed at every
  surface.
- **The StructRegistry fence is falsifiable.** All five carriers (2a/2c/2d/2e
  cross-ref, 2f owner) name the LIVE `arena.rs:47`
  `StructRegistry::compound_kind_for_layout` wire FOLD-B severs; the tape path is
  fence-clean (`begin_compound` grep-zero `StructRegistry`).
- **Layer 0 / Layer 1 clean two-layer dependency.** One-directional (PV17-002/003);
  CollapsedStage spine x86-pinned + aarch64-refused; no hidden x86 close-path
  coupling.
- **No re-opened REDRESS.** AZ-IV eager, StructRegistry indirection, fact-stream,
  broadcast, FNV, x86 — none re-grounded as viable.

---

## §3W disposition summary

- **ACCEPT: 24** (the six D-folds + tape-substrate category across all dossiers
  on doubled corroboration; the now-folded CH5-V2-001 L5/L6 canonical retention
  token; PayloadArena + sparse-flag tape-member declarations; the
  transient-producer / no-cross-call-state discipline; the OpenFrame retirement;
  F7 OnceCell classification; the Layer 0/Layer 1 cleanliness; 2e's coherence
  note; the deferred FOLD-L9 no-new-substrate property; the five 2f LACs; the
  V3 precision sharpenings that tighten without coupling).
- **REVISE: 0** (the single V2 REVISE is substantively folded; no new residual
  surfaced).
- **REJECT: 0** (no fold implies a parallel substrate, a silent 6th shape, a
  retained sidecar, a cross-call classifier carry, or a Lock-1 violation; the
  fold direction is correct throughout; every prior CH5 residual — four V1 + one
  V2 — is now closed).
- **Accept-rate (CH5 axis): 24/24 = 100.0%.**

V3 is the convergence cycle on the CH5 axis: the last vocabulary-precision
residual is canonicalised, no new coupling appears, and every load-bearing
substrate / sidecar / classifier-state / fence fact re-anchors clean at
`91b6893b0`. Combined with V2's 95.8%, the CH5 axis meets the ORCHESTRATOR §3Z
two-consecutive-≥95% gate with zero orphan REVISE and V ≤ 5. T-P3 inherits a
coupling-clean, fully-manifest-declared fold with the substrate-union (Lock 1)
preserved as ONE substrate, the tape classified as a substrate-manifest CATEGORY
(never a silent 6th `BackendShape`), and every retention token canonical to its
governing lock.

First hygiene action CH1-V5-001: re-verified RESOLVED on disk at `91b6893b0`
(`collapsed_stage.rs` present, `collapsed_tape.rs` absent, enumerated-filename
residual grep-zero in 1a/1e); no edit required on first touch.
