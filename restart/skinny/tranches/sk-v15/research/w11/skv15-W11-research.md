# SK-V15 Wave W11 Research: Close Reconciliation

Status: PROCEED-TO-W11-CLOSE-PLAN.

Date: 2026-05-28. Evidence HEAD before close packet: `8bada626a`.

## Scope

W11 reconciles `skinny/RESULTS.md`, `skinny/REDRESS.md`,
`restart/skinny/ROLLING-SOTA-DELTA.md`, `restart/skinny/tranches/sk-v15/HANDOFF.md`,
and the dependency table in `SPEC.md` / `DISPATCH-PROMPT.md`.

W11 does not admit CSS L4. W11 closes SK-V15 only if PASS-IMPL V2 accepts the
cycle as a prune/rebuild close with row-level routed rejection or intrinsic-block
proof for every unresolved implementation axis.

## Current Ledger

| Surface | W11 finding |
|---|---|
| JSON | 51 / 51 rows remain `A`, `GO`, `strict`, and `measured-row`; no SK-V15 wave changed JSON admission rows. |
| CSS L4 | 24 retained rows are diagnostic/non-admitted only; rolling delta keeps all CSS rows `OPEN`. |
| Lock 14 / Lock 16 | W2 restored self-reporting gates; W11 repaired W10 parent-diff accounting; default `gate-json --check-results` passes. |
| Pattern H | Root runtime census remains 67 and every root runtime file has line-1 generated/provenance text. |
| Decision Engine | W7 e-graph/CSP/generated-selection exact tests pass; grammar-named scaffold proof is retired. |
| BackendShape lowerers | W8/W9 lowerer exact tests pass across all five shapes; all-five report gate passes. |
| FNV quarantine | W10 bench-only quarantine and adversarial strict-product fixtures pass; production migration remains blocked. |

## Dirty-State Classification

The working tree contains pre-existing dirty files outside W11 ownership:

- `docs/precepts`
- SK-V12/SK-V13 CSS report JSON files
- `skinny/crates/bbnf-bench/src/generated_real_typed.rs`
- seven `skinny/crates/runtime/src/grammars/css_l4_*/generated.rs` files

These files are not staged by W11. Broad generated reproducibility checks that
consume those paths remain routed diagnostics, not W11 close blockers, because
the close evidence is taken from clean committed owner paths plus explicit
dirty-state classification.

## Fresh Evidence Commands

Required checks passed at HEAD:

- `grep -cE '^[0-9]+\. \*\*' restart/locks/LOCKS.md` returned `16`.
- `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l`
  returned `67`.
- `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' -print0 | xargs -0 awk 'FNR==1 && $0 !~ /^\/\/ Generated by / && $0 !~ /^\/\/ @generated by / {print FILENAME ":" $0}'`
  returned no rows.
- `cargo xtask check-json` passed from `skinny/`.
- `cargo xtask gate-json --check-results` passed from `skinny/`.
- `cargo test --manifest-path skinny/Cargo.toml -p passes decision_ -- --nocapture`
  passed 3 tests.
- `cargo test --manifest-path skinny/Cargo.toml -p codegen decision_spine_changes_generated_selection_fixture -- --exact`
  passed.
- `cargo test --manifest-path skinny/Cargo.toml -p codegen backend_lowerer_fixture_rejects_label_string_scaffold -- --exact`
  passed.
- `cargo test --manifest-path skinny/Cargo.toml -p codegen lower_ -- --nocapture`
  passed the five lowerer tests.
- `cargo xtask gate-json --check-results --skv15-backend-lowerers-report ../restart/skinny/tranches/sk-v15/research/w9/skv15-W9-backend-lowerers-report.json`
  passed from `skinny/`.
- `cargo test --manifest-path skinny/Cargo.toml -p bbnf-bench fnv_quarantine::tests:: -- --nocapture`
  passed 3 tests.
- `cargo xtask gate-json --check-results --skv15-fnv-quarantine-report ../restart/skinny/tranches/sk-v15/research/w10/skv15-W10-fnv-quarantine-report.json`
  passed from `skinny/`.
- `SKV15_W6_REPORT_OUT=/tmp/skv15-W6-css-typed-retime.json RUSTFLAGS='-C target-cpu=native' cargo test -p bbnf --test css_l4_w6_typed_retime --release -- --nocapture`
  passed 2 tests and re-proved CSS rejection with `admitted_rows=0`.

## CSS Retime Re-Attestation

The W11 retime did not reproduce a CSS admission. It reproduced the W6
rejection:

| field | W11 value |
|---|---:|
| corpus bytes | 979638 |
| Track 1 passes | 2 |
| Track 1 errors | 2 |
| cssparser passes | 4 |
| cssparser errors | 0 |
| Track 1 Mbps | 3.426 |
| cssparser Mbps | 1995.168 |
| margin Mbps | -1992.742 |
| typed summaries equal | false |
| admitted rows | 0 |

This consumes the CSS rows as evidence of non-admission and keeps CSS routed to
SK-V16 as implementation remainder after proof, not as a substitute for an
SK-V15 repair.
