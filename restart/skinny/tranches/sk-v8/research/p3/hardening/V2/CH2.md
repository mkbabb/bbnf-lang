# SK-V8 S-P3 Hardening V2 CH2 GENERALITY

Verdict: ACCEPT.

Confidence: 96%.

## Scope

Reviewed `restart/prompts/ORCHESTRATOR.md` Section 3W/3Z,
`restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md`,
`restart/skinny/tranches/sk-v8/SPEC.md`,
`restart/skinny/tranches/sk-v8/DISPATCH-PROMPT.md`,
`restart/skinny/tranches/sk-v8/HANDOFF.md`,
`restart/skinny/tranches/sk-v8/research/p3/p3-v2-hardening-fold.md`,
P3-A through P3-F, and
`restart/skinny/tranches/sk-v8/research/p3/hardening/HARDENING-S-P3-V1-CONSOLIDATED.md`.
The lens is generality: Lock 14, no generic JSON policy, no new
directive/BIR/substrate/API/`BackendShape`/`UnionTape`, non-JSON proof where
generic crates move, and no JSON-overfit plan constraint without a gate boundary.

## Blockers

None.

## Evidence

The governing CH2 standard is explicit: Lock 14 means no grammar-name leak and
every proposed intervention must be grammar-neutral across CSS L4, Sheets, and
BBNF-self, not only JSON (`restart/prompts/ORCHESTRATOR.md:74-88`). The same
prompt makes no new BBNF directives and no JSON code in generic crates
non-negotiable CH2 checks (`restart/prompts/ORCHESTRATOR.md:197-205`).
S-P3 specializes the same rule: SPEC Section 2.1 must exist, must require
non-JSON proof for generic-crate edits, and any wave that lets JSON policy enter
a generic crate fails CH2
(`restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md:116-120`).

V2 did not loosen the V1 CH2 boundary. The V2 fold records CH2 as preserved and
states that no new directive, BIR, `BackendShape`, substrate surface,
`UnionTape`, public substrate API, or grammar-specific generic policy was
introduced
(`restart/skinny/tranches/sk-v8/research/p3/p3-v2-hardening-fold.md:18-25`).
It also keeps the packet planning-only, with W1-W6 blocked behind W0 close,
exact owner paths, same-wave consumer, row gates, revert protocol, LOC/time fit,
required challenge acceptance, and orchestrator/user dispatch
(`restart/skinny/tranches/sk-v8/research/p3/p3-v2-hardening-fold.md:27-36`).

The live SPEC carries the required Lock 14 gate. Section 2.1 requires public API
scan, grammar-branch scan, primitive/table scan, role/fact boundary,
template/provider boundary, and CSS L4 / Sheets / BBNF-self proof for any
generic CostFacts, codegen, runtime, SIMD, or parser-template edit
(`restart/skinny/tranches/sk-v8/SPEC.md:300-325`). The same SPEC blocks new
directives, BIR variants, `BackendShape`, `UnionTape`, new substrate surface,
public substrate API, parser-owned facts/cursors, sidecar substrate, and JSON
policy in generic crates (`restart/skinny/tranches/sk-v8/SPEC.md:230-246`).
Those blocks are repeated in the inherited route ledger
(`restart/skinny/tranches/sk-v8/SPEC.md:767-784`).

The conditional dispatch path preserves the gate instead of relying on prose.
DISPATCH requires every generic-crate edit to include the SPEC Section 2.1 Lock
14 gate, including non-JSON proof where relevant
(`restart/skinny/tranches/sk-v8/DISPATCH-PROMPT.md:90-107`). It blocks W3 from
Tier B, `tape_vs_tape` as production consumer, sidecars, old offset append path,
new substrate surface, `UnionTape`, new `BackendShape`, directive, BIR, or
public substrate API
(`restart/skinny/tranches/sk-v8/DISPATCH-PROMPT.md:127-140`). HANDOFF likewise
requires public API, grammar-branch, primitive/table, template/provider, and
CSS L4 / Sheets / BBNF-self proof for any generic-crate edit
(`restart/skinny/tranches/sk-v8/HANDOFF.md:119-137`).

The W3 structural alphabet remains generic data plus opaque ordinals, not a JSON
role system. SC-3 defines `StructuralAlphabet` as the generic seam, with
per-grammar codegen-emitted class tables and opaque tape class/fact ids that
generic code never interprets
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:324-355`).
It gives non-JSON instances for CSS L4, Sheets, and BBNF-self and states that
only generated tables and generated walks vary by grammar while substrate,
SIMD compaction, and structural primitives remain identical
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:357-399`).
SC-6 confirms zero grammar-named code in generic substrate, no generic role API,
generated byte sets plus opaque ordinals only, and no new directive
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-6-lock1-amendment-generalisation.md:508-535`).

W3's JSON-specific implementation surfaces are contained at the gate boundary.
The lead hypothesis is one retained `Tape` with scan-written opaque ordinals and
generated retained JSON Track 1 parsing as the current production consumer; it
does not claim non-JSON production migration or direct/path closure
(`restart/skinny/tranches/sk-v8/SPEC.md:551-563`). The exit gate requires exactly
one retained tape, generated JSON retained parser as consumer, Lock 14 plus
non-JSON proof, and rejects `tape_vs_tape`/telemetry-only consumers
(`restart/skinny/tranches/sk-v8/SPEC.md:565-586`). The W3 pre-block list names
new directive, BIR, `BackendShape`, `UnionTape`, public substrate API, sidecar,
parser-owned class/fact slot, old offset append path, Tier B, and
CostFacts-template union as blocked
(`restart/skinny/tranches/sk-v8/SPEC.md:588-599`).

The apparent JSON-specific W2 and W3 constraints do not overfit generic policy.
W2 is bounded to generated typed product rows from explicit host/API schema
facts, with Lock 14 and non-JSON proof required if generic code changes; hidden
schema directives and generic JSON schema facts are pre-blocked
(`restart/skinny/tranches/sk-v8/SPEC.md:460-497`). W1 keeps CostFacts and
comparator fields grammar-neutral, makes generic CostFacts paths contain no JSON
policy, and requires non-JSON proof
(`restart/skinny/tranches/sk-v8/SPEC.md:407-430`). W5 closes the audit loop by
proving CSS L4, Sheets, and BBNF-self implications for any generic edit and
blocking generic JSON public APIs, grammar-name branches,
`StructuralAlphabet::json`, and renamed JSON helpers
(`restart/skinny/tranches/sk-v8/SPEC.md:663-708`).

## Candidate Disposition

| Wave | CH2 disposition |
|---|---|
| W0 | ACCEPT. Telemetry-only baseline; no behavior/source authority before G-Alpha and no generic JSON policy route (`restart/skinny/tranches/sk-v8/SPEC.md:29-37`, `restart/skinny/tranches/sk-v8/DISPATCH-PROMPT.md:56-88`). |
| W1 | ACCEPT. CostFacts are gate-consumed and grammar-neutral; generic CostFacts paths must contain no JSON policy and pass non-JSON proof (`restart/skinny/tranches/sk-v8/SPEC.md:407-430`). |
| W2 | ACCEPT. Typed rows are generated from explicit host/API schema facts; hidden schema directives and generic JSON schema facts are blocked (`restart/skinny/tranches/sk-v8/SPEC.md:460-497`). |
| W3 | ACCEPT. Tier A is one retained `Tape` with generated opaque ordinals and generated JSON retained consumer only inside the per-grammar surface; `UnionTape`, new substrate/API, sidecars, and parser-owned facts are blocked (`restart/skinny/tranches/sk-v8/SPEC.md:551-599`). |
| W4 | ACCEPT. Direct guard work remains generated Track 1 plus independent Track 2, with Lock 14 and non-JSON proof if generic code changes (`restart/skinny/tranches/sk-v8/SPEC.md:621-659`). |
| W5 | ACCEPT. This is the explicit grammar-neutral audit and Lock 14 preservation wave (`restart/skinny/tranches/sk-v8/SPEC.md:663-708`). |
| W6 | ACCEPT. Close rejects any accepted source change lacking Lock 14 proof or same-wave consumer proof (`restart/skinny/tranches/sk-v8/SPEC.md:730-765`). |

## Residual Non-Blocking Risks

- The W3 phrase "structural-projection union" is easy to misread as a
  `UnionTape` or sixth `BackendShape`, but the live gates explicitly define it
  as representation replacement inside one retained `Tape` and block `UnionTape`
  and new substrate/API surfaces (`restart/skinny/tranches/sk-v8/SPEC.md:551-563`,
  `restart/skinny/tranches/sk-v8/SPEC.md:588-599`).
- JSON is the measured SK-V8 consumer for W2/W3, so implementation agents must
  keep respecting the Section 2.1 boundary: JSON-specific templates, generated
  output, tests, and host/API schema facts are allowed surfaces; generic crates
  must remain policy-neutral (`restart/skinny/tranches/sk-v8/SPEC.md:323-325`).
- V2 CH2 ACCEPT does not itself converge S-P3 or dispatch implementation. The
  pass still needs the S-P3 V2 consolidation under ORCHESTRATOR Section 3Z, and
  no SK-V8 implementation wave dispatches before G-Alpha; G-Alpha can authorize
  W0 only (`restart/prompts/ORCHESTRATOR.md:118-128`,
  `restart/skinny/tranches/sk-v8/SPEC.md:814-825`).

## Required Fold If REVISE

None. CH2 is ACCEPT for V2; no generality fold is required.
