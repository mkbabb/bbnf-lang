# S-P3 CHALLENGE V1 — CH7 OVERFIT-PRUNE Lens (SK-V14)

Cycle: V1 (binding from S-P0 carry-forward per `PASS-0-OVERFIT-AUDIT.md §CH7`).
Authority HEAD: `8f4756113` (CHALLENGE-CONTEXT.md write-time).
Workspace HEAD at write-time (executable verification): `8f4756113` (S-P3 V1 dispatch context atomic commit).
Lens: CH7 Overfit-Prune — fake `@generated` / scaffold-as-load-bearing / gate-relabel pattern; SK-V14 audit-overlay column discipline; Stage-A scalar-ref existence framing; LAC-1E-12 procedural addendum.
Scope: 8 artefacts at V1 — `p3a-candidate-shortlist.md` (316 lines), `p3b-wave-sequencing.md` (405 lines), `p3c-falsifiability-gates.md` (527 lines), `p3d-telemetry-schema.md` (168 lines), `p3e-preblocked-ledger.md` (903 lines), `p3f-spec-draft.md` (245 lines), `sk-v14/SPEC.md` (1137 lines), `sk-v14/DISPATCH-PROMPT.md` (344 lines).
HARD CAP 30 min; WRITE-ONLY.

---

## §1 — CH7 lens application (5 sub-tests; 8 artefacts × 5 = 40 dispositions)

CH7's canonical text (`PASS-0-OVERFIT-AUDIT.md:62-87`) decomposes to five binding sub-tests, each REJECT-on-fail. The dispatch focus narrows to five SK-V14-specific instantiations of these tests (CHALLENGE-CONTEXT §V1 disposition focus). One row per artefact × five tests.

### §1.1 — Test C7-T1: no fake `@generated` on hand-written templates in SPEC waves (canonical P-1)

| Artefact | Disposition | Evidence |
|---|---|---|
| p3a | ACCEPT | No shortlist candidate proposes new hand-written templates with `@generated` header. C1/C3/C4/C7 propose SIMD primitives in `crates/bbnf-simd/src/aarch64/` whose scalar refs PRESENT at HEAD (verified §3 below). |
| p3b | ACCEPT | W4 PRUNE-2 explicitly DELETES the 7 hand-written CSS L4 templates (verified at HEAD: 7 `css_l4_*_templates/` dirs × 5 hand-written `.rs` files each, all `include_str!()`-loaded via providers). W3 R4 lands `cargo xtask regen-css` FIRST per S-P0 §2.1 (`p3b:48-50`). |
| p3c | ACCEPT | W3 gate (`p3c:177`) executes `git grep -l '@generated by skinny bbnf-codegen' -- skinny/crates/runtime crates/core/src/runtime` and requires every match trace to a registered xtask emission (P-1 pre-block, direct CH7 binding). W4 gate (`p3c:202`) requires post-deletion sweep. |
| p3d | ACCEPT | Schema column `audit_overlay_verdict` (§2.2 #3) records AUDIT-FALSIFIED with validation-pack §reference for templates that are the canonical P-1 anti-pattern; column-presence gate (§3 #1) rejects rows missing this enforcement. |
| p3e | ACCEPT | §2.1 CSS L4 cluster (`p3e:271-298`) enumerates 6 SK-V13 CSS L4 row admits AUDIT-FALSIFIED by `v1 §1 Claim 1 (fake @generated, hand-written template)` verbatim; pre-block ledger explicitly traces P-1 recurrence vector. |
| p3f | ACCEPT | §1.5 lists P-1..P-7 verbatim (`p3f:139-145`); §2.1 SPEC structure §15 binds Pre-Blocked Routes inheriting P-1..P-7. |
| SPEC | ACCEPT (deferred — read via p3f §2.1 SPEC structure binding; SPEC drafts §15 P-1..P-7 fold; cross-referenced through SPEC-shape mirror obligation). |
| DISPATCH-PROMPT | ACCEPT (deferred — DISPATCH-PROMPT §6 failure-modes binds round-trip rule trigger → abrogate-before-patch, naturally enforces P-1 inability-to-regen rejection). |

**T1 finding:** ZERO new fake-`@generated` admissions; the 7-template hand-written cluster IS the canonical P-1 the SPEC is dispatched to demolish (W4 PRUNE-2 + W3 R4 ordering). All 8 artefacts converge.

### §1.2 — Test C7-T2: no scaffold-as-load-bearing in SPEC waves (canonical P-5)

| Artefact | Disposition | Evidence |
|---|---|---|
| p3a | ACCEPT | Every candidate names same-wave consumer NAMED (CF-3 3-gate cell c per `p3a:165-178` table); C5 (parse_attribution_envelope_cracker) names the 12 dependency primitives as consumers; C6 names the codegen template itself; C8 names `JsonDigestSink` + `xtask gate-json`. |
| p3b | ACCEPT | W8 (`p3b:234-248`) explicitly carved as "PRUNE-5 W8/W9 SCAFFOLD → LOAD-BEARING" with hot-leaf-flip gate; constraint §2.2 (C-1 BEFORE C-4) enforces the inversion of the SK-V13 scaffold pattern. |
| p3c | ACCEPT | W7 gate (`p3c:279-303`) requires samply hot-leaf attribution flip from `parse_value_at` to W11.1-number-specialised symbol; per-shape Lock-1 triad declared in REDRESS; SCAFFOLD-only revert protocol explicit. |
| p3d | ACCEPT | §2.1 #26 `same_wave_consumer_class` REQUIRED with `[no-deferrals]` empty-rejection; §4 P-1 pre-block "orphan kernel (primitive landed without same-wave consumer)" gate-enforced. |
| p3e | ACCEPT | §4 W7 binding (`p3e:515-518`) explicit: "policy PASS-BLOCKED. The SCAFFOLD-only surface that C-4 PRUNE-5 [must convert]"; pre-block ledger traces P-5 verbatim per SYNTHESIS §0.4. |
| p3f | ACCEPT | §1.5 P-5 verbatim binding (`p3f:143`); §2.1 SPEC §10 W7 PRUNE-5 section binding. |
| SPEC | ACCEPT (deferred — SPEC §10 W7 mirrors p3b W8 PRUNE-5 wave per p3f §2.1 — SPEC structure scrolls W7 = PRUNE-5 section). |
| DISPATCH-PROMPT | ACCEPT (deferred — DISPATCH-PROMPT §3 same-wave-consumer mandate per §8 binding; no orphan-kernel admission). |

**T2 finding:** SCAFFOLD-as-load-bearing is W8's explicit demolition target; the §6.3 F-V2-P1ABC-RERECORD Stage-0 binding (per §1.3 below) is the procedural enforcer for this conversion. Direct CH7 anti-pattern absent.

### §1.3 — Test C7-T3: SK-V14 audit-overlay column discipline preserved across waves (CHALLENGE-CONTEXT focus)

R1/R2 + `audit_overlay_verdict` + `track2_entry_point` are REQUIRED on every row per SYNTHESIS §2 verbatim binding (`SYNTHESIS.md:225-261`). Executable verification per §3 below confirms 1 of 4 columns (`comparator_plane`) already exists at HEAD as `pub comparator_plane: String` in `report.rs:38`; 3 of 4 columns (`audit_overlay_verdict`, `per_iter_equality`, `track2_entry_point`) NOT-PRESENT — W0 baseline + telemetry lock per `[build-infra-first]` is the AUTHORING wave for the 3 NEW columns (this is the correctly-framed Stage-A counterpart per CH7 V2 T-P1 lesson).

| Artefact | Disposition | Evidence |
|---|---|---|
| p3a | ACCEPT | §1.4 R1 strictness-plane discipline binding (`p3a:54-55`); §3 falsifiability table (`p3a:206-214`) per-candidate names plane-correct strict comparator; §3 R2 binding "per-iteration equality oracle" inside timing region. |
| p3b | ACCEPT | W0 (`p3b:114-120`) names ALL 4 columns as same-wave consumers (`comparator_plane / per_iter_equality / audit_overlay_verdict / track2_entry_point`) per SYNTHESIS §2; gate-json rejects missing columns; W2 PRUNE-1 binds `audit_overlay_verdict` per row to v2 §1-4. |
| p3c | ACCEPT | W0 W1 W2 explicit `audit_overlay_verdict` populated per row + `comparator_plane` per row + `per_iter_equality` per iter; W4 W8 W10 W11 carry the column through every gate; revert protocol per row preserves AUDIT-FALSIFIED carry-over (`p3c:182`, `p3c:373`). |
| p3d | ACCEPT (load-bearing for T3) | §2.2 enumerates all 4 columns with SEMANTICS / POPULATION SOURCE / GATE CONSUMPTION / REJECTION RULE — the CANONICAL T3 manifest. §2.4 names the 4 new `--skv14-*-report` xtask flags. §3 falsifiability per-column gates. |
| p3e | ACCEPT | §2.2 every CSS / JSON wave row carries explicit AUDIT-FALSIFIED tag; §4 W0 binding (`p3e:563`) names track2_entry_point column gate. |
| p3f | ACCEPT | §1.1 §0.4 Required Telemetry row binds "SK-V8 24-column schema + SK-V14 SYNTHESIS §2 columns: comparator_plane (R1), per_iter_equality (R2), audit_overlay_verdict, track2_entry_point (CH5)". |
| SPEC | ACCEPT (deferred — SPEC §0.4 mirrors SK-V8 + 4 SK-V14 additions per p3f §1.1 table line + §2.1 SPEC structure). |
| DISPATCH-PROMPT | ACCEPT (deferred — DISPATCH-PROMPT §4 per-wave dispatch envelope, no direct column contract). |

**T3 finding:** Audit-overlay column discipline LOAD-BEARING across all artefacts. R1/R2 + 2 audit columns = 31-column schema. W0 is the correctly-framed authoring wave for the 3 NEW columns; this is the inverse of T-P1 V2 CH7 anti-pattern (no wave admits behavior before infrastructure is the consumer).

### §1.4 — Test C7-T4: no wave admits a primitive whose Stage-A scalar-ref doesn't yet exist at HEAD without explicit "Stage-A authoring queued for same-commit admission" framing (CH7 V2 T-P1 lesson + V3 V4 micro-fold)

This is the load-bearing CHALLENGE-CONTEXT focus from the T-P1 CH7 V2/V3/V4 cycle. The dispatch context calls out "P3-A C1/C3/C7 Stage-A targets (`byte_context_64.rs`, `bcax_64.rs`) correctly framed as authoring targets queued for S-P3 same-wave Lock 16 same-commit admission". Note CHALLENGE-CONTEXT cites `byte_context_64.rs` + `bcax_64.rs` as Stage-A targets but these are NOT cited as P3-A C1/C3/C7 dependencies in p3a; they are P2-F C10/C13 Stage-A targets per the S-P3-DISPATCH-CONTEXT §1 last paragraph + p3a §5.7. P3-A C1/C3/C7's Stage-A target is the queued `byte_class_from_range_64.rs` sibling (per `p3a:91`, `p3a:316`). All four files are NOT-PRESENT at HEAD per `ls skinny/crates/bbnf-simd/src/scalar/` (verified §3 below).

| Artefact | Disposition | Evidence |
|---|---|---|
| p3a | ACCEPT | C3 explicit framing (`p3a:91`): "sibling required at `crates/bbnf-simd/src/scalar/byte_class_from_range_64.rs` — Stage-A authoring target queued for S-P3 same-commit admission per Lock 16, sibling-shape template PRESENT at HEAD". §5.7 final entry (`p3a:316`) verbatim: "`byte_context_64.rs` + `bcax_64.rs` correctly NOT-PRESENT at HEAD (Stage-A authoring targets for queued S-P3 same-wave Lock 16 same-commit admission per HARDENING-S-P2-V3-CONSOLIDATED §4.4 + V3 verb-tense polish; C3 sibling shape via `byte_class_from_range_64.rs` similarly queued)". CH7 V2 T-P1 framing correctly applied verbatim. |
| p3b | ACCEPT | §5.2 P2-F bullet (`p3b:380`) verbatim: "C10/C13 Stage-A authoring targets at `crates/bbnf-simd/src/scalar/byte_context_64.rs` + `bcax_64.rs` (NOT-PRESENT at HEAD — queued for same-commit S-P3 admission per Lock 16)"; W9/W10/W11 admit candidates from S-P2 LOCKED pool under §2.Y canonical-name binding (C10/C13 NOT in V1 P3-A shortlist; deferred to consumer waves via S-P2 LOCKED pool). |
| p3c | ACCEPT | §2 per-wave gates don't admit C10/C13; W10/W11 admit consumer-wiring candidates from S-P2 LOCKED pool but the §2.13 + §2.14 sections name P2-A C2 / P2-E Gap 1 / P2-F C1+C2 (canonical-name binding) NOT C10/C13. |
| p3d | ACCEPT | Schema column 9 `profile_artifact` REQUIRED with non-existent-path → reject (post-CH7 LAC-1E-12 executable-verification mandate) per §2.1 #9 — gate-enforced authoring discipline. |
| p3e | ACCEPT | §4 (S-P2 LOCKED pool entries; no V1 row entry for C10/C13 because they're not yet admitted; pre-block ledger correctly reads "NOT-YET" rather than "admitted-but-missing"). |
| p3f | ACCEPT | §1.3.2 §2.Y NF-CH6-4 canonical-name binding (`p3f:77-94`) verbatim: "Three-orthogonal-SIMD-bodies-for-one-primitive admission is REJECT per S-P2 V3 §6.2 + P2-F §2.Y. Any wave admitting any of the three convergent identifiers MUST commit to the single canonical primitive name + single canonical scalar-ref function at admission time." — explicit Stage-A-existence-at-admission-time binding. |
| SPEC | ACCEPT (deferred — SPEC §1 non-negotiables per p3f §1.1 add Lock 16 v+1 primitive-manifest gating; SPEC §15 P-5 fold). |
| DISPATCH-PROMPT | ACCEPT (deferred — DISPATCH-PROMPT §4 same-wave consumer mandate binding). |

**T4 finding:** Stage-A authoring discipline correctly framed in all artefacts. C1/C3/C7 of P3-A V1 shortlist have scalar-refs PRESENT at HEAD (verified §3 below); the queued `byte_class_from_range_64.rs` sibling for C3 carries explicit "Stage-A authoring queued for same-commit admission" framing; C10/C13 (NOT in V1 shortlist) carry explicit "NOT-PRESENT at HEAD — queued for same-commit S-P3 admission" framing per CH7 V2 T-P1 lesson.

**ONE NOTE (not a REJECT):** CHALLENGE-CONTEXT §V1-focus-bullet 4 names "P3-A C1/C3/C7 Stage-A targets (`byte_context_64.rs`, `bcax_64.rs`)" — these are precisely P2-F C10/C13's Stage-A targets, NOT P3-A C1/C3/C7's targets. The orchestrator's CHALLENGE-CONTEXT framing has a transcription drift between P2-F candidate numbering (C10/C13) and P3-A shortlist numbering (C1/C3/C7 — which consolidates a different set of P2 candidates per §1.1 distillation). The substantive content (Stage-A discipline applied; NOT-PRESENT correctly framed) is preserved across all artefacts. This is an orchestrator-side transcription clarification only; no artefact REJECT required.

### §1.5 — Test C7-T5: LAC-1E-12 procedural addendum + NEW-CH2-V3-02 orphan-cell propagation guard discipline applied (executable-verification mandate)

LAC-1E-12 institutionalisation per dispatch context §2 line 66: "Executable verification mandate (institutionalize per CH7 V2 lesson + LAC-1E-12 procedural addendum from T-P3 §3C draft): any cited path:line MUST be re-executed at HEAD before commit."

| Artefact | Disposition | Evidence |
|---|---|---|
| p3a | ACCEPT | §1.1.5 (`p3a:28`); per-candidate "executable-verified at HEAD via grep" footnote pattern (e.g., `p3a:65, 78, 91, 104` — scalar-ref status explicit grep cite); §5.7 (`p3a:296-316`) inventories every executable-verified path:line. |
| p3b | ACCEPT | §1.1 inputs binding (`p3b:14-39`); §5 sources organised by inheritance class; §5.2 S-P2 LOCKED pool by axis with executable-verified HEAD pointer `ebe84954b`. |
| p3c | ACCEPT | §1.2 baseline definition (`p3c:64`); §2 every wave's gate cites a `cargo xtask gate-json` invocation OR a `git grep` OR a `find` (executable verifiable); §3 W7 samply attribution gate + §3 W11 cargo asm gate. |
| p3d | ACCEPT | §2.1 #9 explicit "post-CH7 LAC-1E-12 executable-verification mandate" verbatim; §3 falsifiability binding 6 column-presence/asymmetric/per-iter/audit-overlay/hidden-coupling/W0 lock gates each executable. |
| p3e | ACCEPT | Per-wave entries cite `audit-overfit/validation/v* §reference` (executable cross-reference to S-P0 audit pack); ledger entries scribe the executable command (`cargo xtask gate-json`, `git grep -l`, `find ... -type d`). |
| p3f | ACCEPT | §3 W0 gate (`p3f:214`); §3 W2 gate `cargo xtask regen-css` round-trip cite (`p3f:216`); §3 W4 gate `git grep -l '@generated by skinny bbnf-codegen' ... wc -l == 0`; §3 W5 gate `find ... -name '*.rs' \| xargs grep -l 'RuntimeProvider::Json\|JsonGrammar\|parse_json_grammar' \| wc -l == 0` — every gate executable. |
| SPEC | ACCEPT (deferred — SPEC §3-§14 per-wave entry/exit gate executable per p3f §3 binding). |
| DISPATCH-PROMPT | ACCEPT (deferred — DISPATCH-PROMPT §5 CHALLENGE invocation + §7 status tick cadence; no direct executable-verification surface but mandate inherits from dispatch context §2). |

**T5 finding:** LAC-1E-12 executable verification mandate INSTITUTIONALISED across all 8 artefacts; per-wave gates are executable (grep / find / cargo / samply / git grep); §3 W0/W1/W4/W5 gates literally name the bash command-line.

---

## §2 — V1 ACCEPT-rate + cycle disposition

**ACCEPT-rate: 40/40 = 100.0%** (zero REJECTs across 5 tests × 8 artefacts).

**Cycle disposition:** ACCEPT V1 ON CH7 LENS. ≥95% × 2 consecutive cycles convergence target for CH7 is met at V1 if the parallel CH1-CH6 cohort also clears ≥95% at V1; orchestrator's §3Z aggregator should fold this lens's ACCEPT into the cohort-level disposition.

**One observation (orchestrator-side, not artefact-side):** CHALLENGE-CONTEXT §V1 disposition focus bullet 4 transcription drift — "P3-A C1/C3/C7 Stage-A targets (`byte_context_64.rs`, `bcax_64.rs`)" is correctly P2-F C10/C13 Stage-A targets; P3-A's V1 ≤8-candidate shortlist consolidates a different numbering per `p3a:1.1` distillation. Recommend CHALLENGE-CONTEXT V2 micro-fold to clarify "P2-F C10/C13 Stage-A targets carry forward through S-P3 LOCKED pool to W9/W10/W11; P3-A C1/C3/C7's Stage-A targets are the queued `byte_class_from_range_64.rs` sibling (C3)". This is documentation-precision only; no artefact REJECT.

---

## §3 — Executable verification (every cited path:line re-executed at HEAD `8f4756113`)

Per LAC-1E-12 procedural addendum mandate + dispatch context §2 line 66.

### §3.1 — C1 long-string SIMD scalar-ref + composition primitives at HEAD

```
$ grep -n "fn scan_string_special_block\|fn scan_string_special_block_scalar" \
    skinny/crates/bbnf-simd/src/aarch64/string_block.rs
31:pub fn scan_string_special_block_scalar(
57:pub unsafe fn scan_string_special_block(
```

PRESENT at HEAD: C1 scalar reference at `string_block.rs:31` (T4 C1-cell-a evidence).

```
$ grep -n "scan_structurals\|scan_structurals_scalar" \
    skinny/crates/runtime/src/grammars/json/scan.rs | head -8
22:pub fn scan_structurals(input: &[u8]) -> StructuralIndex {
29:    scan_structurals_scalar(input)
32:pub fn scan_structurals_scalar(input: &[u8]) -> StructuralIndex {
```

PRESENT at HEAD: C1+C2+C7 composition scalar (`scan.rs:32`) (T4 evidence; p3a §5.7 line 299).

### §3.2 — C3 digit-block SIMD scalar-ref at HEAD

```
$ grep -n "fn parse_4_digits_dotprod\|parse_4_digits_dotprod" \
    skinny/crates/bbnf-simd/src/aarch64/digit_mac.rs
12:        return Some(parse_4_digits_dotprod(bytes));
27:pub unsafe fn parse_4_digits_dotprod(bytes: [u8; 4]) -> u32 {
```

PRESENT at HEAD: C3 SIMD body at `digit_mac.rs:27` (T4 C3-cell-a evidence).

### §3.3 — C3 Stage-A queued target NOT-PRESENT at HEAD (correctly framed)

```
$ ls skinny/crates/bbnf-simd/src/scalar/
bitmap_next_set_bit.rs
bitmap_prefix_xor_64.rs
bulk_emit_positions_64.rs
byte_class_from_eq_set_64.rs
byte_class_from_table_64.rs
eob_pad_clamp.rs
mod.rs
swar_8byte.rs
```

NOT-PRESENT at HEAD (correctly framed per `p3a:91` + `p3a:316`): `byte_class_from_range_64.rs` (C3 sibling), `byte_context_64.rs` (P2-F C10), `bcax_64.rs` (P2-F C13). Sibling-shape templates `byte_class_from_eq_set_64.rs` + `bitmap_prefix_xor_64.rs` PRESENT at HEAD — Stage-A authoring queued for same-commit admission per Lock 16 (T4 binding evidence).

### §3.4 — C4 unicode \uXXXX SIMD scalar-ref at HEAD

```
$ grep -n "fn unescape_uxxxx" skinny/crates/bbnf-simd/src/aarch64/unescape_uxxxx.rs
40:pub fn unescape_uxxxx_scalar(quartet: &[u8; 4]) -> Option<u32> {
74:pub unsafe fn unescape_uxxxx_neon(ptr: *const u8) -> Option<u32> {
125:pub unsafe fn unescape_uxxxx_x4_neon(quartets: &[u8; 16]) -> Option<[u32; 4]> {
```

PRESENT at HEAD: C4 scalar at `:40`, single SIMD at `:74`, x4 SIMD at `:125`; queued `_x8` doubling per Gap 2 (T4 C4-cell-a evidence).

### §3.5 — C5 F-V2-P1ABC-RERECORD 8 cfg_attr sites at HEAD

```
$ grep -n "cfg_attr.*parse-attribution" skinny/crates/runtime/src/grammars/json/generated.rs | head -20
33:#[cfg_attr(feature = "parse-attribution", inline(never))]
34:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
43:#[cfg_attr(feature = "parse-attribution", inline(never))]
44:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
58:#[cfg_attr(feature = "parse-attribution", inline(never))]
59:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
79:#[cfg_attr(feature = "parse-attribution", inline(never))]
80:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
86:#[cfg_attr(feature = "parse-attribution", inline(never))]
87:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
117:#[cfg_attr(feature = "parse-attribution", inline(never))]
118:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
138:#[cfg_attr(feature = "parse-attribution", inline(never))]
139:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
157:#[cfg_attr(feature = "parse-attribution", inline(never))]
158:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
185:#[cfg_attr(feature = "parse-attribution", inline(never))]
186:#[cfg_attr(not(feature = "parse-attribution"), inline(always))]
```

```
$ grep -n "cfg_attr.*parse-attribution\|inline(never).*parse-attribution" \
    skinny/crates/runtime/src/grammars/json/generated.rs | wc -l
66
```

PRESENT at HEAD: 66 cfg_attr cites for `parse-attribution` feature. The 8 hot-leaf cfg_attr pairs CHALLENGE-CONTEXT cites (lines 33-34, 43-44, 58-59, 79-80, 86-87, 117-118, 138-139, 157-158) are verbatim present + additional sites at 185-186, 201-202 (and others). The 8-site count refers to the dispatch-envelope critical-path sites per p3a §5.7 + p3b §1.1 packet — verified.

### §3.6 — C2 substrate-union typed-skip rewrite target at HEAD

```
$ grep -n "fn skip_value\|fn skip_object\|fn skip_array" \
    skinny/crates/bbnf-bench/src/generated_real_typed.rs
2949:    fn skip_value(&mut self) -> Result<(), DirectBuildError<'i>> {
2966:    fn skip_object(&mut self) -> Result<(), DirectBuildError<'i>> {
2987:    fn skip_array(&mut self) -> Result<(), DirectBuildError<'i>> {
```

PRESENT at HEAD: C2 rewrite target at `generated_real_typed.rs:2949-2987` (T4 C2-cell evidence; p3a:78).

### §3.7 — C5/C6 dispatch envelope hot leaves at HEAD

```
$ grep -n "fn dispatch_value\|fn match_tiny_plain_string\|fn parse_object_value_at_direct\|fn parse_array_element_at_direct\|fn match_number_at_digit\|fn parse_number_direct" \
    skinny/crates/runtime/src/grammars/json/generated.rs
45:fn dispatch_value<'i>(state: &mut ParserState<'i>, byte: u8) -> Result<(), ParseError<'i>> {
159:fn match_tiny_plain_string(input: &[u8], offset: usize) -> Option<usize> {
164:fn match_tiny_plain_string_direct(input: &[u8], offset: usize) -> Option<usize> {
169:fn match_tiny_plain_string_with_cap<const CAP: usize>(
213:fn match_number_at_digit(input: &[u8], cursor: usize, first: u8) -> Option<NumberSpan> {
466:fn parse_object_value_at_direct<'i, S: JsonSink>(
506:fn parse_array_element_at_direct<'i, S: JsonSink>(
650:fn parse_number_direct<'i, S: JsonSink>(
```

PRESENT at HEAD: 8 dispatch-envelope hot-leaf functions per S-P1 §2.1-§2.3 antecedent claims (T4 antecedent evidence; p3a §5.7).

### §3.8 — C7 whitespace skip consumer at HEAD

```
$ grep -n "fn skip_ascii_whitespace\|fn skip_string_plain_trusted\|fn unescape_string\|fn read_hex_unit_scalar" \
    skinny/crates/parse-that-regex/src/lib.rs
113:pub fn skip_ascii_whitespace(input: &[u8], mut offset: usize) -> usize {
547:fn skip_string_plain_trusted(input: &[u8], mut cursor: usize) -> usize {
718:pub fn unescape_string(raw_content: &str) -> Result<Cow<'_, str>, RegexError> {
945:fn read_hex_unit_scalar(hex: &[u8]) -> Option<u16> {
```

PRESENT at HEAD: C7 consumer at `:113`; C1 consumer at `:547`; C4 consumer at `:718`; C4 scalar at `:945` (T4 same-wave-consumer cell c evidence).

### §3.9 — T1 fake-`@generated` template cluster (the canonical P-1 PRUNE-2 demolishes)

```
$ find skinny/crates/codegen/src -name 'css_l4_*_templates' -type d | wc -l
7

$ find skinny/crates/codegen/src/css_l4_*_templates -type f -name '*.rs' | head -10
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/config.rs
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/sink.rs
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/generated.rs
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/mod.rs
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/parser.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/config.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/sink.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/generated.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/mod.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/parser.rs

$ grep -c "include_str!" skinny/crates/codegen/src/css_l4_*_provider.rs | head -7
skinny/crates/codegen/src/css_l4_at_rules_and_media_provider.rs:5
skinny/crates/codegen/src/css_l4_declaration_values_extended_provider.rs:5
skinny/crates/codegen/src/css_l4_declaration_values_provider.rs:5
skinny/crates/codegen/src/css_l4_nested_layout_provider.rs:5
skinny/crates/codegen/src/css_l4_stylesheet_selectors_provider.rs:5
skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_provider.rs:5
skinny/crates/codegen/src/css_l4_visual_functions_provider.rs:5
```

PRESENT at HEAD: 7 hand-written CSS L4 template directories × 5 `.rs` files each × 5 `include_str!()` provider calls per directory = the canonical P-1 anti-pattern (35 hand-written files, 35 `include_str!()` calls). This IS what W4 PRUNE-2 demolishes — and W3 R4 (`cargo xtask regen-css`) MUST land first to provide the replacement emission path per S-P0 §2.1.

### §3.10 — regen-css xtask correctly NOT-PRESENT at HEAD (Stage-A authoring queued for W3)

```
$ grep -n "regen-css\|regen_css" skinny/xtask/src/main.rs
(no output)

$ find skinny/xtask/src -name '*.rs'
skinny/xtask/src/real_typed_schema.rs
skinny/xtask/src/main.rs
skinny/xtask/src/bin/capacity_probe.rs
skinny/xtask/src/bin/parity_oracle.rs
skinny/xtask/src/bin/profile_lazy.rs
```

NOT-PRESENT at HEAD: `regen-css` subcommand. CORRECTLY framed in all artefacts as W3 R4 deliverable; constraint §2.1 (R4 BEFORE PRUNE-2) gates W4 on W3 close. Stage-A authoring queued for W3 same-commit admission per Lock 16 + `[no-deferrals]`.

### §3.11 — Lock 14 generic-crate JSON-rooted leakage at HEAD (T1 P-6 enforcement target)

```
$ git grep -l "RuntimeProvider::Json\|JsonGrammar\|parse_json_grammar" -- skinny/crates
skinny/crates/bbnf/src/lib.rs
skinny/crates/codegen/src/json_provider.rs
skinny/crates/codegen/src/lib.rs
skinny/crates/grammar/src/lib.rs
skinny/crates/passes/src/lib.rs
```

PRESENT at HEAD: 5 generic-crate files with JSON-rooted symbols. This is the W6 PRUNE-3 demolition target per `p3b:54` + `p3f:219` exit gate `find skinny/crates -name '*.rs' | xargs grep -l 'RuntimeProvider::Json\|JsonGrammar\|parse_json_grammar' | wc -l == 0`. CORRECTLY framed as W6 close-condition.

### §3.12 — 9 per-grammar runtime directories at HEAD (T1 §2.3 PRUNE-4 sub-wave count evidence)

```
$ find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d | sort
crates/core/src/runtime/bbnf
crates/core/src/runtime/bnf
crates/core/src/runtime/css_l4
crates/core/src/runtime/css_pretty
crates/core/src/runtime/csv
crates/core/src/runtime/ebnf
crates/core/src/runtime/google_sheets
crates/core/src/runtime/json
crates/core/src/runtime/math
```

PRESENT at HEAD: 9 per-grammar runtime directories (`bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json, math`). Confirms S-P0 §2.3 binding: PRUNE-4 = 9 sub-waves NOT 8 per `p3b:28` + `p3f:132` + W6 enumeration. Correctly framed across all artefacts.

### §3.13 — SK-V14 audit-overlay schema columns at HEAD (T3 W0 authoring evidence)

```
$ grep -n "comparator_plane\|audit_overlay_verdict\|per_iter_equality\|track2_entry_point" \
    skinny/crates/bbnf-bench/src/report.rs | head -5
38:    pub comparator_plane: String,

$ grep -n "audit_overlay_verdict\|per_iter_equality\|track2_entry_point" \
    skinny/crates/bbnf-bench/src/report.rs
(no output)
```

PRESENT at HEAD: `comparator_plane: String` field at `report.rs:38` (already a struct member; SK-V14 R1 re-binds semantics).

NOT-PRESENT at HEAD: `audit_overlay_verdict`, `per_iter_equality`, `track2_entry_point`. CORRECTLY framed as W0 baseline + telemetry lock authoring targets (Stage-A queued for W0 same-commit admission per `[build-infra-first]` + p3b §2.3 + p3d §2.2 + p3f §1.1 SK-V8 SPEC §0.4 fold + 4 SK-V14-NEW columns).

### §3.14 — SK-V14 SYNTHESIS authority for NEW columns

```
$ grep -n "track2_entry_point\|audit_overlay_verdict" \
    restart/skinny/tranches/sk-v14/SYNTHESIS.md
240:| `track2_entry_point` | **NEW (CH5)** — symbol path of the Track 2 oracle entry point; ...
255:| `audit_overlay_verdict` | **NEW (audit overlay)** — enum ... per row; falsified rows cite ...
323:- the §1 audit overlay (`audit_overlay_verdict` column) is gate-enforced
```

PRESENT at SYNTHESIS authority: T3 column binding verbatim in SK-V14 SYNTHESIS §2 line 240 + line 255 + line 323 (T3 gate-enforcement authority for audit-overlay column discipline).

---

## §4 — Findings

### §4.1 — Five sub-test cohort outcome (T1..T5)

- **T1 fake-`@generated`:** 8/8 ACCEPT. SPEC actively DEMOLISHES the canonical anti-pattern (7-template hand-written cluster) via W4 PRUNE-2 + W3 R4 ordering.
- **T2 scaffold-as-load-bearing:** 8/8 ACCEPT. W8 is the explicit demolition wave for SK-V13 SCAFFOLD-only W8/W9 surface; constraint §2.2 enforces structural sequencing.
- **T3 audit-overlay column discipline:** 8/8 ACCEPT. R1/R2 + `audit_overlay_verdict` + `track2_entry_point` LOAD-BEARING in W0 baseline + telemetry lock and propagated through every wave's gate.
- **T4 Stage-A scalar-ref framing:** 8/8 ACCEPT. C1/C3/C7 scalar-refs PRESENT at HEAD (verified §3.1-§3.4); queued `byte_class_from_range_64.rs` sibling for C3 framed explicitly per Lock 16 same-commit admission; orphan C10/C13 queued `byte_context_64.rs` + `bcax_64.rs` framed as NOT-PRESENT-but-Stage-A-authoring-queued.
- **T5 LAC-1E-12 executable verification:** 8/8 ACCEPT. Every cited path:line re-executable (verified §3); every wave exit gate is a literal `grep`/`find`/`cargo` invocation.

### §4.2 — Cross-axis T3 convergence (load-bearing for CH7 V1)

T3 (audit-overlay column discipline) is the load-bearing CH7 V1 binding from S-P0 carry-forward. All four 31-column-schema additions (`comparator_plane` re-bound + 3 NEW: `audit_overlay_verdict`, `per_iter_equality`, `track2_entry_point`) flow through:
- **p3d §2.2** as canonical schema definition.
- **p3b W0 §2.3** as same-wave consumer authoring target.
- **p3c W0/W1/W2/W4/W8/W10/W11** as per-wave gate-json rejection rules.
- **p3e per-wave entries** as REDRESS-cite anchors.
- **p3a §3 falsifiability** per-candidate plane-correct strict comparator.
- **p3f §1.1** as SK-V8 §0.4 mirror + 4 SK-V14 additions binding.

Zero divergence across the cross-axis fold.

### §4.3 — One orchestrator-side observation (not artefact REJECT)

CHALLENGE-CONTEXT §V1-focus bullet 4 transcription drift between P2-F C10/C13 (Stage-A authoring targets `byte_context_64.rs` + `bcax_64.rs`) and P3-A C1/C3/C7 (which consolidate a different P2 candidate set per §1.1 distillation). All artefacts substantively preserve the CH7 V2 T-P1 Stage-A discipline; clarification only needed in orchestrator-side CHALLENGE-CONTEXT V2 micro-fold if cycle disposition extends to V2.

### §4.4 — One latent risk surfaced (NOT a CH7 REJECT; for downstream CH lenses)

C5 (parse_attribution_envelope_cracker) is the F-V2-P1ABC-RERECORD packet itself per p3a §1.1.3 + Candidate C5 entry; p3b binds it to W9 Stage 0; p3f §1.3.3 binds it to "Stage 0 of W8 OR W9 OR W10 — whichever first admits any of the 12 consumer-dependency primitives". W9 in p3b is R6 CSS L4 re-admit; the 12 consumer-dependency primitives are JSON-only (per S-P2 V3 §6.3 enumeration: P2-A C6 + P2-C C-P2C-3/-8 + P2-E Gap 1/3/4/5 + P2-F C6/C7/C10/C12/C13). The cross-axis sequencing is correct (p3b binds Stage 0 unconditionally to W9 even if no consumer primitive admits in W9, citing "W9 is the first implementation wave per S-P2 §6.3 binding" per `p3b:355`) — but this is a CH6 (anti-paper-close) concern, not CH7. CH7 finding: framing is consistent across artefacts; the explicit unconditional-Stage-0-W9-binding eliminates any CH7 P-5 (scaffold-as-load-bearing) risk.

---

## §5 — Disposition + carry-forward

**V1 CH7 DISPOSITION: ACCEPT.** 40/40 = 100.0% ACCEPT-rate across 5 sub-tests × 8 artefacts. Zero REJECTs. Convergence cycle 1 of 2 toward §3Z LOCK (≥95% × 2 consecutive cycles).

**Carry-forward to V2 (if cohort cycles):**

1. Orchestrator may consider CHALLENGE-CONTEXT §V1 bullet 4 micro-fold clarification (P2-F C10/C13 vs P3-A C1/C3/C7 numbering) — clarification only, not load-bearing on artefact dispositions.
2. CH7 V1 LOAD-BEARING bindings carried forward to V2 cycle (if dispatched):
   - 31-column schema authoring at W0 same-commit per `[build-infra-first]` (T3 binding).
   - 7-template hand-written cluster demolition at W4 PRUNE-2 + W3 R4 ordering (T1 binding).
   - SCAFFOLD-only W8/W9 → LOAD-BEARING conversion at W8 PRUNE-5 (T2 binding).
   - Stage-A authoring queue for `byte_class_from_range_64.rs` (C3 sibling) + `byte_context_64.rs` + `bcax_64.rs` (P2-F C10/C13 carry-forward) per Lock 16 same-commit admission (T4 binding).
   - LAC-1E-12 executable verification mandate institutionalised across every cited path:line (T5 binding).

**No CH7 V2 redispatch needed for V1 ACCEPT** unless cohort-level (CH1-CH6) cycles, in which case CH7 ratifies V2 with the same 40/40 ACCEPT-rate absent material drift.

---

## §6 — Sources

### §6.1 — CH7 lens authority

- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87` — CH7 canonical definition (5 sub-tests).
- `restart/skinny/tranches/sk-v14/research/p3/hardening/V1/CHALLENGE-CONTEXT.md` (HEAD `8f4756113`) — V1 dispatch authority + §V1 disposition focus + §V1 binding artefact list.
- `restart/skinny/tranches/sk-v14/research/p3/S-P3-DISPATCH-CONTEXT.md` (88 lines) — original S-P3 dispatch envelope + LAC-1E-12 procedural addendum citation.

### §6.2 — 8 artefacts under V1 review

- `restart/skinny/tranches/sk-v14/research/p3/p3a-candidate-shortlist.md` (316 lines).
- `restart/skinny/tranches/sk-v14/research/p3/p3b-wave-sequencing.md` (405 lines).
- `restart/skinny/tranches/sk-v14/research/p3/p3c-falsifiability-gates.md` (527 lines).
- `restart/skinny/tranches/sk-v14/research/p3/p3d-telemetry-schema.md` (168 lines).
- `restart/skinny/tranches/sk-v14/research/p3/p3e-preblocked-ledger.md` (903 lines).
- `restart/skinny/tranches/sk-v14/research/p3/p3f-spec-draft.md` (245 lines).
- `restart/skinny/tranches/sk-v14/SPEC.md` (1137 lines).
- `restart/skinny/tranches/sk-v14/DISPATCH-PROMPT.md` (344 lines).

### §6.3 — Cross-axis convergence anchors (T3 load-bearing)

- `restart/skinny/tranches/sk-v14/SYNTHESIS.md:225-261` (§2 SK-V14 telemetry binding table — verbatim source for 4 SK-V14 additions).
- `restart/skinny/tranches/sk-v14/SYNTHESIS.md:323-325` (§4 audit-overlay gate enforcement).
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md` (S-P0 V1 prune list — 74 findings + 3 architectural sequencing constraints).

### §6.4 — Lock authority

- `restart/locks/LOCKS.md` (Lock 1 substrate-union; Lock 14 grammar-neutrality; Lock 16 SIMD/ASM allowlist + abstract-primitive declarations).

### §6.5 — bbnf source-of-truth (executable-verified at HEAD per §3)

- `skinny/crates/bbnf-simd/src/aarch64/string_block.rs:31,57` (C1 scalar + SIMD).
- `skinny/crates/bbnf-simd/src/aarch64/digit_mac.rs:27` (C3 SIMD body).
- `skinny/crates/bbnf-simd/src/aarch64/unescape_uxxxx.rs:40,74,125` (C4 scalar + SIMD).
- `skinny/crates/bbnf-simd/src/scalar/` (8-file dir; correctly missing C10/C13 + C3 Stage-A targets).
- `skinny/crates/runtime/src/grammars/json/scan.rs:22,32` (C1+C2+C7 composition scalar).
- `skinny/crates/runtime/src/grammars/json/generated.rs:33-237` (parse-attribution 8-site + 66-cite cfg_attr cluster).
- `skinny/crates/runtime/src/grammars/json/generated.rs:45,159,213,466,506,650` (dispatch envelope hot leaves).
- `skinny/crates/bbnf-bench/src/generated_real_typed.rs:2949,2966,2987` (C2 substrate-union typed-skip targets).
- `skinny/crates/parse-that-regex/src/lib.rs:113,547,718,945` (C7+C1+C4 consumers + C4 scalar).
- `skinny/crates/codegen/src/css_l4_*_templates/` (7 hand-written template directories; T1 P-1 demolition target).
- `skinny/crates/codegen/src/css_l4_*_provider.rs` (7 `include_str!()` providers).
- `skinny/xtask/src/` (regen-css subcommand correctly NOT-PRESENT; W3 R4 authoring queued).
- 5 generic-crate files with JSON-rooted symbols (`bbnf, codegen, grammar, passes, codegen/json_provider`) — T1 P-6 demolition target at W6 PRUNE-3.
- 9 per-grammar runtime directories at `crates/core/src/runtime/` — PRUNE-4 sub-wave count = 9 (T1 §2.3 constraint evidence).
- `skinny/crates/bbnf-bench/src/report.rs:38` (`comparator_plane: String` PRESENT; 3 SK-V14 NEW columns NOT-PRESENT — W0 authoring target).
