# HARDENING-S-P2-V5-CONSOLIDATED

Date: 2026-05-17.
Pass: S-P2 Research - substrate-ceiling cohort.
Cycle: V5.

Cycle verdict: BLOCKED.
ACCEPT-rate: 5/6 (83.3%).
ACCEPT list: CH1, CH2, CH3, CH5, CH6.
REVISE list: CH4.
REJECT list: none.

Convergence status: not converged. With six lenses, the `>=95% ACCEPT`
threshold requires all six lenses to ACCEPT, and S-P2 requires two consecutive
qualifying ACCEPT cycles or an explicit user pin. V4 was REVISE, so even a full
V5 ACCEPT would have been only the first qualifying cycle after REVISE. V5 also
reached the pass hard ceiling (`V <= 5`) with one unresolved REVISE disposition,
so ORCHESTRATOR §3Z and PASS-2 §4 require escalation to the user with this
`BLOCKED` verdict. No S-P3 dispatch is authorized. No SK-V8 implementation wave
dispatch is authorized.

## Lens Disposition Table

| Lens | Disposition | Score | Basis |
|---|---:|---:|---|
| CH1 CORRECTNESS | ACCEPT | 96 | SC-1 closes the V4 deferral phrase, preserves strict comparator columns, rejects generic JSON roles, and keeps S-P3 blocked (`hardening/V5/CH1.md`). |
| CH2 GENERALITY | ACCEPT | 97 | SC-2 keeps the two-stage SOTA claim grammar-neutral, generated-byte-set based, and challenge-gated before selection (`hardening/V5/CH2.md`). |
| CH3 REGRESSION | ACCEPT | 95 | SC-3 preserves one retained `Tape`, opaque ordinals/fact ids, no old offset append fallback, and no unowned production consumer (`hardening/V5/CH3.md`). |
| CH4 COST | REVISE | 91 | Cost surface is accepted, but packet governance still budgets a normal post-V5 qualifying cycle even though S-P2 hard-caps at V5 (`hardening/V5/CH4.md`). |
| CH5 HIDDEN COUPLING | ACCEPT | 96 | The retained substrate, no-new-API posture, `tape_vs_tape` residual routing, and V4 fold do not reopen hidden sidecar coupling (`hardening/V5/CH5.md`). |
| CH6 ANTI-PAPER-CLOSE | ACCEPT | 95 | V4's SC-1 deferral blocker is closed and Lock 1 remains a Pass Omega candidate, but CH6 flags that consolidation must escalate rather than imply V6 (`hardening/V5/CH6.md`). |

## Blocking Finding

CH4 identifies a hard-ceiling governance defect. Current packet text says V1,
V2, V3, and V4 did not converge and that a future V5 ACCEPT would be only the
first ACCEPT cycle after REVISE "unless followed by another qualifying ACCEPT
cycle" (`SYNTHESIS.md`, `SPEC.md`, and `HANDOFF.md`). That was correct before
V5 dispatched, but after V5 it implies a normal V6 route. ORCHESTRATOR §3Z and
PASS-2 §4 cap the pass at V5. Since V5 is not converged, the legal next action
is escalation with the unresolved REVISE named, not automatic S-P3 and not a
normal V6 challenge.

## Accepted Folds To Preserve

- The V4 CH6 blocker is closed. SC-1 no longer routes the grammar-generalisation
  caveat to a separate S-P3 proof; it rejects the current packet candidate when
  Tier A cannot satisfy the invariant and requires fresh W0 evidence plus a
  newly accepted S-P3/W3 plan for any later reconsideration.
- Strict-vs-strict comparator discipline remains accepted: sonic-strict is the
  only same-run strict admission plane; simdjson DOM and other sidecars remain
  planning signals unless refreshed under same-run output-plane rules.
- Tier A remains structural-class cursor migration only. Tier B owns
  string-boundary, quote/backslash/parity, CostFacts-template, and non-JSON
  production migration.
- Lock 14 remains accepted: generated byte sets, opaque ordinals/fact ids,
  grammar-module interpretation, no JSON role leakage into generic code, and
  non-JSON proof obligations.
- No new directive, BIR variant, `BackendShape`, `UnionTape`, public substrate
  API, parser-owned cursor/facts, or parallel substrate is admitted.
- `tape_vs_tape` remains routed residual telemetry, not a W3 production
  same-wave consumer.

## Required Escalation Disposition

Because this is V5, the CH4 governance defect cannot be folded into a normal
V6 dispatch without violating the hard ceiling. The packet must stop at the
user gate with these options:

1. User explicitly pins S-P2 final despite the single unresolved V5 REVISE,
   after which the packet governance text must be updated to state that V5 was
   the ceiling and S-P3 may consume only the pinned S-P2 result.
2. User revises the orchestration instruction to override the hard ceiling and
   authorize an exceptional V6 challenge.
3. User rejects S-P2 convergence, keeping S-P3 and all SK-V8 waves blocked.

Until one of those happens, no S-P3 synthesis-plan dispatch is authorized and
`G-Alpha closed` cannot be presented from this path.
