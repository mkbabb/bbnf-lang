# T-P2 V3 Challenge Context — SK-V15

Challenge target commit: `d11a9eec0`.

Target packet:

- `restart/audit/totality/p2/2A-sota-landscape.md`
- `restart/audit/totality/p2/2B-primitive-vocabulary.md`
- `restart/audit/totality/p2/2C-grammar-neutrality.md`
- `restart/audit/totality/p2/2D-cost-model.md`
- `restart/audit/totality/p2/2E-host-arch-esoterica.md`
- `restart/audit/totality/p2/2F-parse-that-gaps.md`

Authority:

1. `restart/prompts/totality/PASS-2-RESEARCH.md` §3.
2. `restart/prompts/ORCHESTRATOR.md` §3W and §3Z.
3. `restart/audit/totality/p2/T-P2-DISPATCH-CONTEXT.md`.
4. `restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md`.
5. `restart/audit/totality/p2/hardening/HARDENING-T-P2-V1-CONSOLIDATED.md`.
6. `restart/audit/totality/p2/hardening/HARDENING-T-P2-V2-CONSOLIDATED.md`.
7. `restart/audit/skinny-impl-overfit/V1/CONSOLIDATED-AUDIT.md`.
8. `restart/skinny/tranches/sk-v15/SPEC.md`.

Entry facts:

- T-P2 V1 returned `REVISE`; T-P2 V2 returned 7/7 `ACCEPT`.
- V3 is the confirmation challenge cycle over the unchanged V2 research packet.
- If V3 returns 7/7 `ACCEPT`, with zero orphan `REVISE` items and no target
  packet edits, T-P2 satisfies the two-consecutive-clean §3Z convergence rule
  within the V≤5 ceiling.
- Do not rewrite T-P1's entry state: it remains
  `CLEAN-FINAL-G1-AUTO-PINNED-NOT-NORMAL-3Z`.
- The V2 fold addendum is authority and history. The target under hardening is
  the six-dossier packet above; do not treat the addendum's old broken URL
  mention as an active dossier defect unless the live dossier retained it as a
  cited source.

V3 confirmation checks:

1. CH1 correctness must confirm the live dossier packet still carries the
   repaired simdjson tape source and no live dead-source citation.
2. CH2 generality must confirm no new directive, no new BIR, no sixth
   BackendShape, and no single-grammar route is disguised as totality.
3. CH3 regression must confirm retained cursor/list/class-column/sidecar
   routes remain pre-blocked and provider-before-consumer sequencing remains
   intact for retirement/deletion paths.
4. CH4 cost must confirm the V2 row-shape/cost manifest survives unchanged
   and the W7/W8/W9 split remains bounded.
5. CH5 hidden coupling must confirm no hidden substrate, grammar switch,
   runtime regex substrate, self-excluding gate, CSS fact-stream/CSSOM
   substitution, or wrong-host close evidence is admitted.
6. CH6 anti-paper-close must confirm grounded/partial rows still carry inline
   transfer reason, admission gate, verification action, close status, and
   primitive scalar/parity/hardware/consumer/movement fields where applicable.
7. CH7 overfit-prune must confirm broadcast admission, fake CSS parity,
   self-excluding gates, source-only primitive inventory, wrong-host SIMD,
   FNV bench contrivance leakage, and delete-before-provider routes remain
   blocked.

Disposition vocabulary: `ACCEPT`, `REVISE`, or `REJECT`.

Every CH file must include:

- lens name;
- disposition;
- critical findings table;
- evidence inspected;
- fold requirements if disposition is REVISE or REJECT;
- explicit note on whether the issue blocks T-P2 V3 convergence.
