# T-P3 V1 Hardening Consolidated

Pass: T-P3 Synthesis. Cycle: V1.
Date: 2026-05-21.
Scope: six-lens challenge verdict for the V1 totality synthesis packet.
Output: this file.

## Verdict

`G-T-P3-V1-CHALLENGE`: REVISE.

V1 is directionally sound: source citations and target surfaces resolve, the
Lock 14/generalisation story is concrete, REDRESS fences hold, and hidden
coupling is blocked. It does not pass the T-P3 convergence gate because cost
discipline and anti-paper-close routing are incomplete. CH4 requires per-delta
LOC budget, propagation surface count, risk class, wave alignment, and receiver
or same-wave consumer. CH6 requires every open question or cross-cycle route to
name receiver, blocker, and receiving gate.

Acceptance rate: 4/6 = 66.7%.
Consecutive accepted cycles: 0.

| Lens | Disposition | Load-bearing finding |
|---|---|---|
| CH1 correctness | ACCEPT | T-P3 V1 cites real T-P1/T-P2 evidence, 3C covers all 41 LOCKS amendment candidates, and proposed LOCKS hunk targets resolve against the current `LOCKS.md`. |
| CH2 generality / Lock 14 | ACCEPT | The V1 packet keeps CSS L4 / Sheets / BBNF-self pressure concrete, bounds generated/provider exceptions, and does not narrow accepted amendments to JSON. |
| CH3 regression / REDRESS | ACCEPT | V1 does not revive REDRESS-rejected routes as admissions; union, PMULL/CSSC, direct, parse-that, and source-present primitive routes remain material-differential gated. |
| CH4 cost | REVISE | 3A, 3C, 3D, and 3F lack per-delta LOC/risk/wave/propagation fields; 3B lacks numeric propagation on its proposed deltas; 3E leaves D06 and some later generated-fixture tails too vague. |
| CH5 hidden coupling | ACCEPT | V1 preserves Lock 1 substrate ownership and blocks parallel substrates, sidecars, renamed scanner drift, Track 1/Track 2 dishonesty, and hidden retained parser state. |
| CH6 anti-paper-close | REVISE | Several open-question tables and route notes cite evidence but do not name receiver, blocker, and receiving gate, leaving future-cycle work as ambient process debt. |

## Required V2 Fold

1. Normalize every T-P3 delta or consequence row to carry:
   `loc_budget`, `propagation_surfaces`, `risk_class`, `wave_alignment`,
   `same_wave_consumer_or_receiver`, and `hard_cap_or_abrogate_gate` where an
   implementation or gate path is implied.
2. Backfill 3A, 3D, and 3F from aggregate prose into per-delta cost rows.
   Qualitative terms such as "small", "later", and "future wave" must be paired
   with numeric budgets and named receivers.
3. Preserve 3B's NEW-wave same-wave-consumer structure, but add numeric
   `propagation_surfaces` to MP-3B-D1 through MP-3B-D9.
4. Add a 3C cost/disposition ledger per hunk or candidate group with LOC
   budget, propagation count, risk class, wave alignment, and
   `same_wave_consumer_or_gate`. Keep ACCEPT/MODIFY as lock-text dispositions,
   not implementation admissions.
5. Tighten 3E-D06 and any "later" implementation tails with a capped receiver
   wave or an explicit non-budgeted handoff gate.
6. Normalize every open-question table to `receiver / blocker / gate`. 3E's
   format is the model. Replace informal `future`, `later`, `belongs to`, or
   lowercase `defer` routing with named handoffs and receiving gates.
7. Preserve 3F's measurable dispatch checklist, but add receiver/blocker/gate
   routing for the Omega CRUD / G-Omega ordering question.

## Disposition

T-P3 does not converge on V1. Fold the revise set into V2, commit the revised
T-P3 synthesis packet, then rerun the full six-lens challenge.
