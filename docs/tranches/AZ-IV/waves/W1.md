# AZ-IV.W1 - Runtime Surface And Semantic Parity

**Name**: W1 - Runtime Surface And Semantic Parity
**Opens after**: W0 - Truth And Canonical Regen
**Agents**: 5 parallel writer slots, 8 sub-units (W1.6/W1.7/W1.8 sequenced in shared worktrees with W1.1/W1.5)
**Hard gate**: generated parsers, typed path/query surfaces, and runtime projections match grammar-derived type-inference behavior; no grammar overfit; no silent fallback
**Status**: planned

## Scope

1. Harden BA's typed path/query semantics in the existing runtime surface: `Path`, `PathSegment`, `path!`, and every active `*PathQuery`.
2. Close Sheets parity on the current full suite (regenerated-tempdir output; no synthetic tag/payload fallback; no rule-name projection table).
3. Close CSS lightningcss parity, including named-color payload materialization and Tailwind non-timeout behavior (depends on W0 `Map { fn_id }` preservation).
4. Keep JSON and BBNF parity green against regenerated outputs.
5. Make TS backend executable: syntax/typecheck, Node execution, EOF enforcement, discriminated unions, repeat loops.
6. Prove shape generality beyond JSON with real-grammar or synthetic-IR execution tests.
7. Retire old color compatibility exports or move them behind explicit test-support/compat boundaries.
8. Make semantic parity type-inference driven: generated projection tables, `TypeDesc`, `StructRegistry`, obligations, and grammar facts own payload/field/discriminant decisions.
9. Preserve BA host-binding semantics by landing executable TS status and a Python/host-binding decision ledger with exact signatures/errors or explicit no-surface evidence.
10. Eliminate every `from_rule_name(&str) -> Kind` discriminator impl in production runtime (`crates/core/src/runtime/{bbnf,bnf,csv,css_pretty,ebnf,google_sheets,math}/arena.rs`) per `audit/HARDENING-2026-05-01-fermat.md` F1. Replacement: `StructRegistry`-projected discriminator field populated at codegen.
11. Eliminate the `(layout.kind, layout.rule_name.as_str())` builder dispatches at `crates/core/src/runtime/json/builder.rs:262`, `crates/core/src/runtime/css_l4/builder.rs:325, :521` per Fermat F2/F3. Replacement: typed `OpenFrame` selection from registry/typedesc.
12. Delete `crates/core/src/runtime/css_l4/builder.rs:790-833 leak_static_str` rule-name allowlist; rule names that need static lifetimes go through one canonical interner.
13. Replace `crates/core/src/pipeline/compile.rs:219-224` rule-name → `GrammarAuditTag` aliasing with `GrammarAuditTag::Custom(entry_name)` for every grammar; the audit artefact keys directly on the entry rule string.
14. Architectural patch for `recover_modifier` / `recover_binary_op` / `wrap.rs:89-99`: alt_dispatch emitter pushes modifier and binary-operator tokens as typed Span leaves; lower path deletes its three byte-recovery functions. Substrate at `crates/core/src/backend/rust/emitter/shapes/alt_dispatch/branches.rs:227-298` is ready; the activation is the W1 task (per Fermat F8).
15. Per-grammar runtime module deduplication (Fermat F-recommendation): emit `arena.rs` + `builder.rs` from a shared template parameterised by `StructRegistry` + `TypeDesc` rather than maintaining seven hand-coded twins. Document/value/view surfaces are scope-bounded; the deduplication target is the structural skeleton, not the typed-leaf API.

## Triumvirate Dispatch

Dispatch a triumvirate if any of these occurs:

- runtime path hardening requires a second parser, untyped fallback resolver, parent-pointer sidecar, or public parallel API;
- CSS or Sheets parity requires a normalizer escape hatch, rule-name projection table, or synthetic payload default instead of grammar-derived structure;
- TS parity needs a separate parser implementation from the emitted backend;
- shape generality cannot be proven without literal parser-name branches;
- three diagnostic loops fail to isolate a parity class;
- a parity surface needs a new `from_rule_name` arm, a new literal rule-name projection table, or a new `EmitStrategy::for_grammar` parser-name arm to land (per `audit/HARDENING-2026-05-01-fermat.md`);
- a regression test demonstrates that deleting a `from_rule_name` arm cannot be replaced by a `StructRegistry` lookup without a type-inference pass extension.

Empty/no-evidence returns get one verbatim redispatch with the same worktree pointer. A second empty/no-evidence return triggers mandatory triumvirate. Three diagnostic-loop iterations without isolating root cause auto-trigger triumvirate. JSONL transcript quiet >15 minutes is an orchestrator-side auto-trigger; record the condition and dispatch time in `PROGRESS.md` alongside the wave's evidence ledger.

## File Bounds

| File | Access |
|---|---|
| `crates/core/src/runtime/path.rs` | modify |
| `crates/core/src/runtime/view.rs` | modify-carve |
| `crates/core/src/runtime/*/document.rs` | modify |
| `crates/core/src/runtime/css_l4/**` | modify |
| `crates/core/src/backend/rust/view/**` | modify/delete |
| `crates/core/src/css_types.rs` | modify-carve |
| `crates/ir/src/types/**` | modify-carve |
| `crates/ir/src/passes/types/**` | modify-carve |
| `crates/core/benches/json/value.rs` | modify |
| `grammar/json/**` | modify-carve |
| `grammar/css/**` | modify |
| `grammar/google-sheets/**` | modify |
| `grammar/bbnf/**` | modify-carve |
| `crates/core/src/backend/ts/**` | modify |
| `crates/core/src/backend/rust/emitter/shapes/**` | modify-carve |
| `crates/core/tests/**parity*.rs` | modify |
| `crates/core/tests/backend_ts.rs` | modify |
| `crates/core/tests/pipeline_compile_request.rs` | modify |
| `crates/core/tests/shape_dispatch_emission.rs` | modify |
| `crates/core/tests/projection_totality.rs` | modify |
| `crates/core/tests/*path*.rs` | create/modify |

Do NOT touch: rewrite/ruler production activation, benchmark thresholds, new path crates, parent-pointer sidecars.

## Disjointness

Runtime path/projection/color, Sheets grammar/tests, CSS grammar/tests, TS backend, and shape generality have disjoint primary write bounds. If a shared generated output or fixture must change, sequence the owning integration commit after all source changes.

## Worktree Plan

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| AZ-IV.W1.1 Runtime Path Surface | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-runtime` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-runtime/target/w1-runtime` |
| AZ-IV.W1.2 Sheets Parity | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-sheets` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-sheets/target/w1-sheets` |
| AZ-IV.W1.3 CSS Parity | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-css` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-css/target/w1-css` |
| AZ-IV.W1.4 TS Backend | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-ts` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-ts/target/w1-ts` |
| AZ-IV.W1.5 Shape Generality | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-shape` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-shape/target/w1-shape` |

## Agent Units

### AZ-IV.W1.1 Runtime Path And Projection Surface

- Mechanism: harden `runtime::path`, `path!`, per-document `*PathQuery`, direct struct projection, and runtime view/color boundaries in place. Delete the `backend/rust/view/color` shim and fold `peel.rs` into `named_types.rs`; the live CSS color flow remains `runtime::css_l4::CssColor`.
- Files: `crates/core/src/runtime/path.rs`, `crates/core/src/runtime/*/document.rs`, `crates/core/src/runtime/view.rs`, `crates/core/src/runtime/css_l4/**`, `crates/core/src/backend/rust/view/**`, `crates/core/benches/json/value.rs`.
- Sub-gate: path traversal is zero-allocation, type-inference checked, and same-harness 3-field JSON access beats sonic-rs `pointer!` by at least 20%; 30-field access is parity-or-better. Per `audit/HARDENING-2026-05-01-heisenberg.md` Amendment 2: `crates/core/src/backend/rust/view/color.rs` is deleted; the `Color`/`ColorSpace`/`COLOR_PAYLOAD_BYTES` decoder migrates to `crates/core/tests/common/legacy_color_payload.rs` (test support only) or is folded into `crates/core/tests/lightningcss_parity.rs` directly. `crates/core/src/runtime/view.rs:35`'s `pub use crate::backend::rust::view::color::*` re-export is deleted. `crates/core/src/backend/rust/view/peel.rs` is folded into `crates/core/src/backend/rust/view/named_types.rs` as a private helper and the standalone file is deleted. `view/named_types.rs` is promoted to `crates/core/src/backend/rust/named_types.rs` (no remaining sibling files justify the directory). Production CSS continues to flow through `crate::runtime::css_l4::CssColor`.

### AZ-IV.W1.2 Sheets Parity

- Mechanism: fix grammar/type-inference-derived Sheets structure for strings, arrays, range refs, prefixes, errors, discriminants, lexeme tables, and serialization.
- Files: `grammar/google-sheets/**`, `crates/core/tests/*sheets*.rs`, generated Sheets output.
- Sub-gate: full Sheets parity suite green from regenerated output, with no synthetic tag/payload fallbacks and no hand-coded serializer lexeme maps that duplicate grammar facts.

### AZ-IV.W1.3 CSS Parity

- Mechanism: make lightningcss parity field-level where claimed and eliminate early-return payload gaps, rule-name projection tables, and defensive default payload synthesis.
- Files: `grammar/css/**`, `crates/core/src/runtime/css_l4/**`, CSS parity tests.
- Sub-gate: CSS normalize/bootstrap/named-color payload tests green from generated typed structures; normalizers are bounded diagnostics, not the parity proof; Tailwind parses without timeout-class failure.

### AZ-IV.W1.4 TS Backend

- Mechanism: repair discriminated union, repeat loop, EOF, host function wiring, and executable output gates.
- Files: `crates/core/src/backend/ts/**`, `crates/core/tests/backend_ts.rs`, `crates/core/tests/pipeline_compile_request.rs`.
- Sub-gate: `backend_ts`, `pipeline_compile_request ts_backend`, tempdir TS syntax/typecheck, Node execution, EOF rejection, and host-binding behavior pass; Python/other host path binding status is captured in a signature/error ledger.

### AZ-IV.W1.5 Shape Generality

- Mechanism: prove shape detector/emitter equivalence on non-JSON grammar facts; replace `pipeline/compile.rs:219-224` rule-name aliasing with `GrammarAuditTag::Custom(entry_name)` (per `audit/HARDENING-2026-05-01-fermat.md` F6); migrate host directive decoders to typed `BbnfCompoundKind`-driven flow (Fermat F10).
- Files: `crates/core/src/backend/rust/emitter/shapes/**`, shape tests, synthetic fixtures, `crates/core/src/pipeline/compile.rs:219-224`, `crates/core/src/grammar/host.rs`.
- Sub-gate: every active `ShapeTag`, including transparent cases, executes on two real grammars or one real grammar plus non-JSON synthetic IR, or is removed from the active denominator. Audit-tag aliasing carries no rule-name dispatch table.

### AZ-IV.W1.6 BBNF Alt-Dispatch Typed-Leaf Push

- Mechanism: activate the alt_dispatch typed-leaf substrate at `crates/core/src/backend/rust/emitter/shapes/alt_dispatch/branches.rs:227-298` so the emitter pushes modifier and binary-operator tokens as typed Span leaves; delete `recover_modifier`, `recover_binary_op`, and the `wrap.rs:89-99` source-byte scanner (per `audit/HARDENING-2026-05-01-fermat.md` F8). Substrate exists; the W1 task is activation.
- Files: `crates/core/src/backend/rust/emitter/shapes/alt_dispatch/branches.rs`, `crates/core/src/lower/expression/repeat.rs` (delete `recover_modifier`), `crates/core/src/lower/expression/pratt.rs` (delete `recover_binary_op`), `crates/core/src/lower/expression/wrap.rs` (delete byte-recovery branch), generated BBNF output.
- Sub-gate: `cargo xtask regen --check` passes for `bbnf` and `bnf` grammars without source-byte recovery in the lower path; BBNF self-host parity remains 95/95 GREEN; `rg "recover_modifier|recover_binary_op|byte_span\\(\\)" crates/core/src/lower/` returns zero hits.

### AZ-IV.W1.7 Per-Grammar Runtime Module Deduplication

- Mechanism: eliminate every `from_rule_name(&str) -> Kind` discriminator impl in production runtime (`bbnf/arena.rs:133`, `bnf/arena.rs:24`, `csv/arena.rs:48`, `css_pretty/arena.rs:34`, `ebnf/arena.rs:30`, `google_sheets/arena.rs:147-187`, `math/arena.rs:43`); replace `(layout.kind, layout.rule_name.as_str())` builder dispatches at `runtime/json/builder.rs:262`, `runtime/css_l4/builder.rs:325, :521`; delete `runtime/css_l4/builder.rs:790-833 leak_static_str`. Replacement: `StructRegistry`-projected discriminator field populated at codegen via the layout's already-tracked rule-id; the runtime reads `layout.compound_kind` directly. Per `audit/HARDENING-2026-05-01-fermat.md` F1/F2/F3 OVERFIT-HARD.
- Files: `crates/core/src/runtime/{bbnf,bnf,csv,css_pretty,ebnf,google_sheets,json,math}/arena.rs`, `crates/core/src/runtime/{json,css_l4}/builder.rs`, `crates/ir/src/registry/struct.rs` (extend with `compound_kind_for_layout`), generated outputs.
- Sub-gate: `crates/core/tests/no_grammar_name_branch.rs` (the new CI static AST scan) returns zero match arms keyed on a literal rule-name string under `crates/core/src/runtime/**` or `crates/core/src/backend/rust/emitter/shapes/**` (excluding `generated/` and `#[cfg(test)]`); JSON, CSS L4, and Sheets parity remain GREEN through the discriminator-projection migration.

### AZ-IV.W1.8 Substrate-Path Hard-Fail And Strategy Manifest

- Mechanism: replace the `crates/core/src/backend/rust/emitter/shapes/substrate.rs:70-78` fallback to `::bbnf::runtime::JsonStructBuilder` with a `panic!("substrate_path: invalid binding {path}: {err}")` (per `audit/HARDENING-2026-05-01-fermat.md` F4 + `heisenberg.md` F7). Replace `EmitStrategy::for_grammar` 9-arm allowlist (`crates/ir/src/registry/strategy.rs:143-262`) with manifest-driven binding registry: each grammar contributes one `[package.metadata.bbnf-grammars.<ident>]` row with builder/document paths; resolver reads parsed manifest, no Rust source arm-list. A synthetic grammar `__test_strategy_synth__` registered only via manifest must round-trip codegen without adding a Rust arm (per `AZ-IV.md` §Hard Gates 14).
- Files: `crates/core/src/backend/rust/emitter/shapes/substrate.rs`, `crates/ir/src/registry/strategy.rs`, `Cargo.toml` (`[package.metadata.bbnf-grammars]`), `crates/core/tests/synthetic_grammar_strategy.rs` (new).
- Sub-gate: substrate.rs panics with a structured error message naming the offending binding string and the strategy authoring path; W0 manifest gate already enforces well-formed binding paths so the fallback is dead substrate. Synthetic-grammar test passes.

## Hard Gate

1. Regenerated tempdir outputs are used for parity. Evidence: `docs/tranches/AZ-IV/audit/W1-parity-matrix.txt` shows each parity suite ran against `cargo xtask regen --tempdir` output, not checked-in `crates/core/src/grammar/generated/**`.
2. `Path`, `PathSegment`, `path!`, and every active `*PathQuery` close BA's Rust path requirements without a new path crate.
3. JSON direct struct projection benchmarks include `bbnf_get_twitter` and `bbnf_value_*` rows against sonic-rs peers.
4. JSON sonic-rs and BBNF self-host suites remain green; BBNF parity covers generated document structure, not only rule/directive fingerprints.
5. Sheets full suite is green and projection/serialization derive from grammar/type facts.
6. CSS field/payload parity is green without early-return escapes, rule-name projection tables, or normalizer-only proof.
7. TS is syntax/type/execution checked, enforces EOF, and proves host function wiring.
8. Shape generality tests execute generated parser behavior, not only `syn::parse2`.
9. Duplicate CSS host shims and old color compatibility are deleted or fenced as test support with no production dependency.
10. **No grammar-name branch in production runtime path** (per `audit/HARDENING-2026-05-01-fermat.md` Amendment): `crates/core/tests/no_grammar_name_branch.rs` (a CI-enforced static AST scan over `crates/core/src/runtime/**` and `crates/core/src/backend/rust/emitter/shapes/**`, excluding `generated/` and `#[cfg(test)]`) fails closed if any `match <expr> { "<literal-rule-name>" => ... }` arm or `match (<expr>, "<literal-rule-name>") => ...` arm appears in production code. Replacement is type-inference-derived discriminator (`StructRegistry::compound_kind(layout)`, `TypeDesc::*` projection), not hand allowlists.
11. **Manifest-driven `EmitStrategy::for_grammar`** (Fermat F5 + AZ-IV.md §Hard Gates 14): each grammar contributes one `[package.metadata.bbnf-grammars.<ident>]` row with builder/document paths; the resolver reads the parsed manifest, never a Rust source arm-list. Regression test `crates/core/tests/synthetic_grammar_strategy.rs` proves a synthetic grammar `__test_strategy_synth__` registered only via manifest round-trips codegen without adding a Rust arm.
12. **Substrate-path hard-fail** (Fermat F4 + Heisenberg F7 + AZ-IV.md §Hard Gates 15): `crates/core/src/backend/rust/emitter/shapes/substrate.rs:70-78` no longer falls back to `::bbnf::runtime::JsonStructBuilder` on `syn::parse_str::<syn::Path>` failure; it `panic!`s with the offending binding string. The W0 manifest gate already enforces well-formed binding paths; the runtime fallback is dead substrate that must be deleted.
13. **`from_rule_name` and rule-name builder dispatch eliminated** (Fermat F1/F2/F3): all 7 `from_rule_name(&str) -> Kind` impls in `crates/core/src/runtime/{bbnf,bnf,csv,css_pretty,ebnf,google_sheets,math}/arena.rs` are replaced with `StructRegistry::compound_kind_for_layout(layout) -> CompoundKindId`; `(layout.kind, layout.rule_name.as_str())` matches at `runtime/json/builder.rs:262`, `runtime/css_l4/builder.rs:325, :521` are replaced with `OpenFrame::from_layout(layout, &registry)`; `runtime/css_l4/builder.rs:790-833 leak_static_str` rule-name allowlist is deleted.
14. **`view/color` shim deleted** (Heisenberg F4): `crates/core/src/backend/rust/view/color.rs`, `crates/core/src/backend/rust/view/peel.rs`, and the `runtime/view.rs:35` re-export are gone; production CSS uses `runtime::css_l4::CssColor`; legacy decoder lives at `crates/core/tests/common/legacy_color_payload.rs` if any tests still reference it.
15. **BBNF source-byte recovery functions deleted** (Fermat F8): `recover_modifier`, `recover_binary_op`, and the `wrap.rs:89-99` byte-recovery branch are gone after the alt_dispatch typed-leaf push lands; `rg "recover_modifier|recover_binary_op" crates/core/src/lower/` returns zero hits and BBNF self-host parity stays 95/95.

## Format And Lint Cadence

Run `cargo fmt --all -- --check`, focused `cargo clippy --profile ax-iter`, focused nextest for touched parity suites, and `git diff --check` after integration batches and before close.

## Verification Artefacts

- `docs/tranches/AZ-IV/audit/W1-parity-matrix.txt` (regenerated tempdir parity per gate 1)
- `docs/tranches/AZ-IV/audit/W1-path-query.json`
- `docs/tranches/AZ-IV/audit/W1-host-bindings.md`
- `docs/tranches/AZ-IV/audit/W1-ts.txt`
- `docs/tranches/AZ-IV/audit/W1-shape-generality.txt`
- `docs/tranches/AZ-IV/audit/W1-no-overfit-scan.txt` (output of `crates/core/tests/no_grammar_name_branch.rs`)
- `docs/tranches/AZ-IV/audit/W1-synthetic-strategy.txt` (output of `crates/core/tests/synthetic_grammar_strategy.rs`)
- `docs/tranches/AZ-IV/audit/W1-view-color-deletion.txt` (rg evidence for shim removal)
- `docs/tranches/AZ-IV/audit/W1-recover-deletion.txt` (rg evidence for source-byte recovery removal)

## Commit Plan

Every dispatch carries `HARD CAP: N min. At 0.9N commit, at N halt.` Defaults: research 20, plan 15, redress 30, audit 25 (per `docs/precepts/instructions/ORCHESTRATION.md` §Triumvirate). Read-only audit/research agents do not commit at the cap; write-authorized agents commit at 0.9N only when the staged slice is clean and owned.

Use `commit-discipline` before each commit. Expected scopes: `runtime/path`, `runtime/projection`, `runtime/css-l4`, `grammar/sheets`, `grammar/css`, `backend/ts`, `emitter/shape`, `grammar/generated`, `runtime/view-color-delete`, `runtime/discriminator-from-registry`, `lower/recover-delete`, `emitter/substrate-panic`, `registry/manifest-strategy`. Commit bodies are required for generated output, benchmark, deletion, and gate-changing parity commits.

## Dependencies

- **Depends on**: W0 - Truth And Canonical Regen.
- **Blocks**: W2 - Optimization Substrate Activation.
