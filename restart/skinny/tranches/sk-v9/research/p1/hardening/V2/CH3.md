# SK-V9 S-P1 V2 CH3 Regression / REDRESS

Disposition: ACCEPT.

The rerun does not reopen blocked routes. Apache/CITM/Canada measured
`real_typed_struct` rows remain absent, Canada typed shortcuts remain blocked,
direct rows clamped by W0 remain `N-direct / NO-GO`, structural scan remains a
diagnostic non-producer, and no sidecar substrate or new public fact slot is
introduced.

P1-F explicitly records the guard rows that might otherwise be overclaimed:
Apache, instruments, and numbers direct rows keep `N-direct / NO-GO` despite
pass-shaped fresh numbers.
