# HARDENING-S-P2-V4-CONSOLIDATED

Date: 2026-05-17.
Pass: S-P2 Research - substrate-ceiling cohort.
Cycle: V4.

Cycle verdict: REVISE.
ACCEPT-rate: 5/6 (83.3%).
ACCEPT list: CH1, CH2, CH3, CH4, CH5.
REVISE list: CH6.
REJECT list: none.

Convergence status: not converged. With six lenses, the `>=95% ACCEPT`
threshold requires all six lenses to ACCEPT, and S-P2 also requires two
consecutive qualifying ACCEPT cycles or an explicit user pin. V1, V2, V3, and
V4 are therefore non-converged cycles. No S-P3 dispatch is authorized. No SK-V8
implementation wave dispatch is authorized.

## Lens Disposition Table

| Lens | Disposition | Score | Basis |
|---|---:|---:|---|
| CH1 CORRECTNESS | ACCEPT | 95 | V4 corrects the remaining RESULTS-column drift and keeps sidecar values out of sonic-strict evidence (`hardening/V4/CH1.md`). |
| CH2 GENERALITY | ACCEPT | 96 | Generated byte sets, opaque ordinals/fact ids, no generic policy, and no new directive/BIR/substrate remain accepted (`hardening/V4/CH2.md`). |
| CH3 REGRESSION | ACCEPT | 94 | Strict comparator, `parse_only` guard status, enforcing gate wording, and stale-sidecar refusal are folded (`hardening/V4/CH3.md`). |
| CH4 COST | ACCEPT | 95 | Tier A/B scope, owner/cost proof, and `tape_vs_tape` residual routing are costed or bounded (`hardening/V4/CH4.md`). |
| CH5 HIDDEN COUPLING | ACCEPT | 96 | One retained `Tape`, no old offset append fallback, no `UnionTape`, and no telemetry-as-consumer aperture are accepted (`hardening/V4/CH5.md`). |
| CH6 ANTI-PAPER-CLOSE | REVISE | 92 | SC-1 still contains one "rejected or routed to a separate S-P3 proof" phrase, leaving a deferral-shaped aperture (`hardening/V4/CH6.md`). |

## Blocking Finding

CH6 finds one remaining paper-close defect. SC-1's R2 fold now says a
number-heavy maintain miss rejects the current Tier A candidate for this cycle,
and that later reconsideration requires fresh W0 evidence plus a newly accepted
S-P3/W3 plan. However, SC-1's grammar-generalisation caveat still says that if
Tier A cannot satisfy the invariant for a grammar class, "the union candidate is
rejected or routed to a separate S-P3 proof" (`SC-1-offset-tape-teardown.md`).

That phrase conflicts with `restart/prompts/skinny/PASS-2-RESEARCH.md`, which
rejects "a future wave will detail" as paper-close. The accepted V4 packet
already states that SC-1..SC-6 authorize no W3 plan by themselves; the residual
SC-1 phrase must be made equally strict.

## Accepted Folds To Preserve

- Strict-vs-strict comparator discipline is accepted. SC-1, SC-2, SC-3, and
  SYNTHESIS now use the authoritative `Delta vs sonic-strict` column and label
  simdjson DOM values as sidecar planning signals.
- `parse_only` remains substrate-guard / non-SOTA telemetry while validation is
  deferred or view-boundary; `K` and future `S` rows do not admit.
- `gate-json` strict-admission refusal is executable: plane, strictness,
  freshness, and measured-row validation must hold.
- Tier A is structural-class cursor migration only; Tier B owns string-boundary,
  quote/backslash/parity, CostFacts-template, and non-JSON production migration.
- Lock 14 is accepted: generated byte sets, opaque structural-class ordinals,
  opaque fact ids, grammar-module interpretation, no generic recovery/layout
  policy.
- No new directive, BIR variant, `BackendShape`, `UnionTape`, public substrate
  API, parser-owned cursor/facts, or parallel substrate is admitted.
- `tape_vs_tape` is a routed residual and never a W3 production same-wave
  consumer.
- S-P2 convergence guard is packet-wide enough for CH6 except the HANDOFF minor
  tense note: V1, V2, and V3 did not converge; V4 also did not converge because
  CH6 is REVISE.

## Required V5 Fold Actions

1. Replace SC-1's grammar-generalisation phrase "rejected or routed to a
   separate S-P3 proof" with the strict posture used elsewhere: if Tier A cannot
   satisfy the invariant for a grammar class, the current Tier A candidate is
   rejected for this S-P2 cycle. Later reconsideration requires fresh W0
   evidence plus a newly accepted S-P3/W3 plan with exact owner paths, tests,
   numeric thresholds, same-wave production consumer, and challenge acceptance;
   it is not a carry-forward promise from S-P2.
2. Update HANDOFF's governance sentence to name V1, V2, V3, and V4 as
   non-converged cycles after this challenge, preserving that a future V5 ACCEPT
   would be only the first qualifying ACCEPT cycle after REVISE unless followed
   by another qualifying ACCEPT cycle or explicitly pinned by the user.
3. Re-challenge S-P2 V5 after the fold. If V5 ACCEPTs, it is still only the
   first qualifying ACCEPT cycle after V4 REVISE unless the user explicitly pins
   S-P2 final.

No S-P3 dispatch is authorized. No SK-V8 implementation wave dispatch is
authorized.
