# BB.W1 — Cache-Key + Workspace Partition Discipline

**Opens after**: W0 close
**Agents**: 2 parallel
**Hard gate**: Cache invalidation and workspace partition drag are reduced with measured warm-iteration improvements versus post-W0.
**Status**: planned

## Scope

1. Reduce over-broad codegen cache invalidation and related rebuild
   drag.
2. Partition or route workspace-heavy operations more intentionally.
3. Prove lower warm-iteration cost against post-W0.

## File bounds

| File | Access |
|---|---|
| `crates/derive/src/lib.rs` | modify |
| `.cargo/config.toml` | modify |
| `Cargo.toml` | modify |
| `scripts/prepare-profile-wave.sh` | modify |
| `docs/instructions/PROFILING.md` | modify |

**Do NOT touch**: parser runtime code, replay/recovery/debug tooling,
any `.bbnf` grammar.

## Phase sub-items

### BB.W1.1 Cache-key discipline

Mechanism: tighten codegen cache invalidation and related rebuild scope
so schema/version or workspace changes stop forcing unnecessary broad
re-expansion.

Files touched: `crates/derive/src/lib.rs`, `Cargo.toml`.

Sub-gate: targeted cache-key and rebuild scope shrink measurably.

### BB.W1.2 Workspace partition discipline

Mechanism: route workspace-heavy profiling or proof loops through more
intentional partitions and prepared-entry workflows.

Files touched: `.cargo/config.toml`, `scripts/prepare-profile-wave.sh`,
`docs/instructions/PROFILING.md`.

Sub-gate: warm iteration after the first build is measurably lower.

## Hard gate

1. Warm iteration cost improves versus post-W0. Artefact:
   `docs/benchmarks/post-BB-W1-mid.json`.
2. Cache-key and rebuild discipline are documented accurately. Artefact:
   `docs/benchmarks/post-BB-W1-profiles.txt`.
3. The targeted cache/workspace drag sources are named and reduced.
   Artefact: `docs/benchmarks/post-BB-W1-profiles.txt`.

## Verification artefacts

- `docs/benchmarks/post-BB-W1-profiles.txt`
- `docs/benchmarks/post-BB-W1-mid.json`
- commit hashes for each W1 sub-phase

## Dependencies

- **Depends on**: W0 close
- **Blocks**: W2, W3, W4
