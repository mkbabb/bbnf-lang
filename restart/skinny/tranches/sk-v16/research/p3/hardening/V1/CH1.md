# S-P3 V1 CH1 Correctness

Disposition: ACCEPT.

P3-A candidates trace to S-P2 survivors or Alpha/S-P0 gate obligations, and the
SPEC labels those two classes distinctly. P3-C supplies measurable row gates:
JSON maintain formulas, CSS typed equality, CSS typed SOTA, Pattern H count,
and selected primitive row floors. W0 is baseline/report-consumer first.

No CSS speed gate can run before typed equality. No behavior wave can close
without same-wave consumer and measurement.
