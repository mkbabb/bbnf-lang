# S-P3 V1 CH7 Overfit-Prune

Disposition: ACCEPT.

The plan does not use CSS W8R metrics as admission floors, dirty generated
files as proof, checksum/FNV as semantics, PMULL/CSSC as production promotion,
or x86/AVX evidence. Native SIMD is conditional and must be profile-first,
scalar-referenced, strict-checkasm/parity verified, same-wave consumed, and
aarch64-only.
