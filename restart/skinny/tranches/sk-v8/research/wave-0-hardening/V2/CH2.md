# SK-V8 W0 Hardening V2 - CH2

Verdict: REJECT.

Confidence: 76%.

Scope: current HEAD `cb0fdba0` (`fix(sk-v8-wave0): fold hardening V1 gate blockers`) reviewed against W0 V1 blockers, ORCHESTRATOR hardening discipline, and the CH2 lens: strict-vs-strict comparator provenance, workload-specific native benchmark paths, same-run/source freshness, and sidecar/native plane separation. I did not edit source.

## Findings

1. **Native workload source paths are fixed, but native same-run evidence class is still under-validated.** The V1 CH2 source-path blocker is materially folded: the gate reads workload-specific native estimates from `sonic_rs_direct_to_struct`, `serde_json_direct_to_struct`, `sonic_rs_real_typed_struct`, and `serde_json_real_typed_struct` at `skinny/crates/bbnf-bench/src/bin/gate.rs:50` through `skinny/crates/bbnf-bench/src/bin/gate.rs:65`; rendered comparator evidence now selects those paths by workload at `skinny/crates/bbnf-bench/src/bin/gate.rs:476` through `skinny/crates/bbnf-bench/src/bin/gate.rs:519`; and W0 validation rejects native source-path mismatch at `skinny/crates/bbnf-bench/src/report.rs:1046` through `skinny/crates/bbnf-bench/src/report.rs:1077`. The rendered manifest demonstrates the fix for `json/twitter/direct_to_struct/main` and `json/twitter/real_typed_struct/main` at `skinny/RESULTS.md:49` through `skinny/RESULTS.md:50`.

   The remaining blocker is that the validator only checks native comparator source artifact shape. The generic comparator loop requires nonempty `comparator_plane`, `comparator_strictness`, `comparator_freshness`, `sidecar_freshness`, and `source_artifact` at `skinny/crates/bbnf-bench/src/report.rs:977` through `skinny/crates/bbnf-bench/src/report.rs:989`; it validates finite Mbps only if `value_mbps` is present at `skinny/crates/bbnf-bench/src/report.rs:997` through `skinny/crates/bbnf-bench/src/report.rs:1004`; and then `validate_native_comparator_source` checks only the source path at `skinny/crates/bbnf-bench/src/report.rs:1046` through `skinny/crates/bbnf-bench/src/report.rs:1077`. For W0 rows that remain `deferred` / `view-boundary`, a native comparator can be marked `historical:*`, can carry a non-`n/a` sidecar freshness, can have a mismatched plane or strictness, or can omit its manifest-side Mbps while preserving the expected source path and still bypass strict-admission refusal because `validate_w0_admission_boundary` returns early for non-strict rows at `skinny/crates/bbnf-bench/src/report.rs:917` through `skinny/crates/bbnf-bench/src/report.rs:927`. That leaves `comparator_freshness=same-run-native` as a generated assertion, not a fully gate-consumed invariant, despite the W0 note that native Rust comparators are same-run at `skinny/crates/bbnf-bench/src/bin/gate.rs:290` through `skinny/crates/bbnf-bench/src/bin/gate.rs:291`.

2. **Sidecar/source freshness is not structurally bound to the sidecar evidence plane.** The generator correctly marks C++ sidecars as `historical:sk-v7-sidecar-profile` when populated and `absent:not-collected-for-{workload}` when missing, with `sidecar-profile:*` or `absence:*` sources at `skinny/crates/bbnf-bench/src/bin/gate.rs:532` through `skinny/crates/bbnf-bench/src/bin/gate.rs:553`. The report validator, however, only checks that populated sidecars are not `absent:*` and are either `historical:*` or `sidecar-same-run`, and that missing sidecars have `absent:<reason>` at `skinny/crates/bbnf-bench/src/report.rs:1005` through `skinny/crates/bbnf-bench/src/report.rs:1034`. It does not bind sidecar `source_artifact` to `sidecar-profile:sk-v7-cpp:{corpus}:{id}` or `absence:w0:{corpus}:{workload}:{id}`, does not require `comparator_freshness` and `sidecar_freshness` to agree for sidecar slots, and does not require a structured same-run sidecar manifest before accepting `sidecar-same-run`. SPEC requires populated sidecar values to have manifest/freshness coverage and `gate-json` to reject malformed sidecar manifests at `restart/skinny/tranches/sk-v8/SPEC.md:357` through `restart/skinny/tranches/sk-v8/SPEC.md:370`; the current validator still accepts too much string-shaped sidecar evidence.

3. **Profile placeholder acceptance is folded for the CH2 path.** W0 now labels Criterion slope evidence explicitly as `criterion-slope-profile`, generates matching hot-leaf strings at `skinny/crates/bbnf-bench/src/bin/gate.rs:591` through `skinny/crates/bbnf-bench/src/bin/gate.rs:600`, and requires the expected row-specific artifact and hot-leaf shape at `skinny/crates/bbnf-bench/src/report.rs:878` through `skinny/crates/bbnf-bench/src/report.rs:900`. The regression test for old placeholder shape is present at `skinny/crates/bbnf-bench/src/report.rs:1489` through `skinny/crates/bbnf-bench/src/report.rs:1507`.

## Missed Tests Or Evidence

- Existing native provenance coverage mutates only the source path (`skinny/crates/bbnf-bench/src/report.rs:1463` through `skinny/crates/bbnf-bench/src/report.rs:1486`). Add report-path tests that keep the expected direct or typed native source path but mutate `comparator_freshness` to `historical:*`, `sidecar_freshness` away from `n/a`, `comparator_plane` away from the row output plane, `comparator_strictness` away from `strict`, and `value_mbps` to `None`; all should fail W0 validation.
- Existing malformed sidecar coverage mutates a populated sidecar to `sidecar_freshness=absent:*` (`skinny/crates/bbnf-bench/src/report.rs:1412` through `skinny/crates/bbnf-bench/src/report.rs:1434`). Add tests that keep sidecar freshness plausible but corrupt `source_artifact`, mismatch `comparator_freshness` versus `sidecar_freshness`, and attempt `sidecar-same-run` without a structured same-run manifest.
- I ran `cargo test -p bbnf-bench`; it passed 45 library tests and 2 `gate` binary tests. I also ran `CARGO_TARGET_DIR=/tmp/skv8-w0-target RUSTFLAGS="-C target-cpu=native" cargo xtask gate-json --advisory --check-results`; it exited successfully and left the repository status clean. These commands prove the current happy path is stable, not that the missed negative cases above are gated.

## Mandatory Fold Items

1. Extend `validate_native_comparator_source` or add an adjacent native evidence validator so `sonic_rs_strict` and `serde_json` must have workload-matched source path, workload-matched output plane, `comparator_strictness=strict`, `comparator_freshness=same-run-native`, `sidecar_freshness=n/a`, and present finite `value_mbps`.
2. Extend sidecar validation so populated historical sidecars require a source shape tied to the row corpus and comparator id, absent sidecars require `absence:w0:{corpus}:{workload}:{id}`, and any `sidecar-same-run` claim is rejected unless a structured same-run sidecar manifest supplies corpus identity, comparator id, binary/build identity, plane, strictness, freshness, and run id.
3. Add negative tests for native same-run freshness mutation and sidecar source/freshness mismatch, then rerun `cargo test -p bbnf-bench` and the check-only W0 gate.
