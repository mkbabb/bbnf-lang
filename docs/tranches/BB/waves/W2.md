# BB.W2 — Command, CI, and Automation Consolidation

**Opens after**: W1 close
**Agents**: 2 parallel
**Hard gate**: The post-AY command surface, CI, and automation paths express the intended workflow directly, without reintroducing heavy-loop ambiguity.
**Status**: planned

## Scope

1. Consolidate the public command surface after the B0/AY split.
2. Align CI and automation with the intended post-AY workflow.
3. Prove lower routine cost without losing heavyweight surfaces.

## File bounds

| File | Access |
|---|---|
| `Makefile` | modify |
| `.github/workflows/ci.yml` | modify |
| `scripts/test-tier.sh` | modify |
| `docs/instructions/tranche/SPEC.md` | modify |
| `docs/instructions/PROFILING.md` | modify |

**Do NOT touch**: parser runtime code, replay/recovery/debug tooling,
any `.bbnf` grammar.

## Phase sub-items

### BB.W2.1 Public workflow consolidation

Mechanism: consolidate the public command surface so the post-AY
workflow is explicit, small, and does not reopen the pre-B0 sprawl.

Files touched: `Makefile`, `scripts/test-tier.sh`.

Sub-gate: the public command surface is explicit and non-redundant.

### BB.W2.2 CI and automation sync

Mechanism: align CI and automation with the intended workflow so routine
paths and heavy proof paths stay distinct.

Files touched: `.github/workflows/ci.yml`,
`docs/instructions/tranche/SPEC.md`,
`docs/instructions/PROFILING.md`.

Sub-gate: instructions, CI, and automation agree.

## Hard gate

1. The post-AY public command surface is explicit and non-redundant.
   Artefact:
   `docs/benchmarks/post-BB-W2-commands.txt`.
2. Routine command timings improve versus post-W1. Artefact:
   `docs/benchmarks/post-BB-W2-mid.json`.
3. CI and documentation preserve the heavy proof surfaces intentionally.
   Artefact:
   `docs/benchmarks/post-BB-W2-commands.txt`.

## Verification artefacts

- `docs/benchmarks/post-BB-W2-commands.txt`
- `docs/benchmarks/post-BB-W2-mid.json`
- commit hashes for each W2 sub-phase

## Dependencies

- **Depends on**: W1 close
- **Blocks**: W3, W4
