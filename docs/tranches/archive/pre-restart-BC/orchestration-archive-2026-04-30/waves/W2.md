# BC.W2 - Consumer Reference Rollout

**Opens after**: W1
**Agents**: repo-sliced implementation, up to 10 when harness permits
**Hard gate**: every consumer references `docs/precepts` and has local tail
**Status**: complete

## Scope

1. Add `docs/precepts` submodule to every top-level consumer.
2. Add or replace `docs/instructions/README.md` with a local-only tail.
3. Remove duplicated `docs/instructions/tranche/*.md` where those paths
   already exist.
4. Add `crates/csp-solver/docs/instructions/README.md` pointing at bbnf-lang
   `docs/precepts` and listing local crate concerns.

## File Bounds

| File | Access |
|---|---|
| `<consumer>/.gitmodules` | create/modify |
| `<consumer>/docs/precepts` | create |
| `<consumer>/docs/instructions/README.md` | create/modify |
| `<consumer>/docs/instructions/tranche/*.md` | delete where already present |
| `/Users/mkbabb/Programming/glass-ui/docs/tranches/D/{D.md,HANDOFF.md,dispatch/AGENT.md}` | modify |
| `crates/csp-solver/docs/instructions/README.md` | create |

Do NOT touch: application source, package lockfiles, dirty tranche docs outside
BC, root screenshots, generated dist files, runtime databases.

## Agent Units

### BC.W2.1 Existing Instruction Consumers

- Mechanism: bbnf-lang, config, speedtest.
- Files: `docs/instructions/**`, `.gitmodules`, `docs/precepts`.
- Sub-gate: duplicate shared tranche files are absent.

### BC.W2.2 Frontend Library Consumers

- Mechanism: glass-ui, keyframes.js, value.js.
- Files: `.gitmodules`, `docs/precepts`, `docs/instructions/README.md`.
- Sub-gate: local tails cite package-specific commands and browser proof.

### BC.W2.3 Product / Library Consumers

- Mechanism: words, fourier-analysis, parse-that, gaggle, ffuzzy.
- Files: `.gitmodules`, `docs/precepts`, `docs/instructions/README.md`.
- Sub-gate: local tails cite project-specific commands and proof surfaces.

### BC.W2.4 Nested Crate

- Mechanism: csp-solver local note only.
- Files: `crates/csp-solver/docs/instructions/README.md`.
- Sub-gate: no nested `crates/csp-solver/docs/precepts` submodule.

## Hard Gate

1. Every top-level consumer has `docs/precepts`.
2. Every top-level consumer has `docs/instructions/README.md`.
3. `bbnf-lang`, config, and speedtest duplicate tranche instruction files are
   absent.
4. `crates/csp-solver` has a crate-local instruction note and no nested
   precepts submodule.

## Verification Artefacts

- `git status --short` per repo.
- `test -e <repo>/docs/precepts` per repo.
- `find <repo>/docs/instructions -maxdepth 2 -type f` per repo.

## Dependencies

- **Depends on**: W1
- **Blocks**: W3
