# AZ-I.W4 — FINAL (three-grammar slice at AU parity + AZ-II handoff)

**Opens after**: AZ-I.W3 close
**Agents**: 1 (close-verification)
**Hard gate**: three-grammar slice of the 17-entry AU-baseline
matrix at or above AU floor on the struct-only path; all parity
harnesses green; tape-scoped-to-BBNF verified; IR audit pass
reports 100% `->` coverage on the three; AZ-II handoff contract
verified.
**Status**: absorbed (2026-04-28). The FINAL close ceremony folds into the W2-act close per `docs/tranches/AZ-I/audit/W2-CLOSE-AUDIT.md` §2 + Audit-5 Proposal B — the matrix re-run + samply capture + AZ-II handoff verification + AZ-I FINAL.md authoring all sit at the W2-act close commit, not in a separate wave. W4's own bench gate, parity harness, and audit-pass coverage gates carry forward into W2-act.C.

## Scope

1. Re-run the 17-entry matrix on the final struct-only path for
   JSON, CSS L4, and Sheets; BBNF entries run on the tape path
   (unchanged from AU baseline).
2. Capture samply fleet under `docs/benchmarks/profiles/AZ-I/W4/`.
3. Run all parity harnesses: sonic-rs, simdjson OnDemand,
   serde_json (JSON); lightningcss, cssparser (CSS L4);
   Sheets-native.
4. Run the IR audit pass; confirm 100% `->` coverage on JSON,
   CSS L4, Sheets.
5. Run the tape-scoped-to-BBNF CI check: grep confirms zero live
   tape symbols (`tape::`, `TapeBuilder`, `TapeCursor`,
   `TapeRec`, `push_rec`, `output.tape()`, `payload_bytes`) on the
   JSON, CSS L4, Sheets parse paths; `cargo build -p bbnf` green.
6. Verify the AZ-II handoff contract (AZ-I.md §Handoff contract to
   AZ-II): three data grammars direct-to-struct, registry closed,
   tape crate compiles, BBNF unchanged, matrix at AU parity.
7. Author `docs/tranches/AZ-I/FINAL.md` with deltas, reversals
   taken (if any), and the handoff contract verification report.

## File bounds

| File | Access |
|---|---|
| `docs/benchmarks/AZ-I/W4/final-matrix.json` | create |
| `docs/benchmarks/profiles/AZ-I/W4/` | create |
| `docs/tranches/AZ-I/FINAL.md` | create |

**Do NOT touch**: any runtime or emitter code (all frozen at W3
close); `crates/tape/` (stays compilable for BBNF).

## Phase sub-items

### AZ-I.W4.1 — Matrix + samply capture

Run the three-data-grammar slice + BBNF-slice of the 17-entry
matrix three times (cold-parse only, no warm benches per
`feedback_no-warm-benches`); capture samply; emit
`final-matrix.json`.

Sub-gate: all throughput gates met on the struct path (JSON, CSS,
Sheets) and on the tape path (BBNF, at AU baseline).

### AZ-I.W4.2 — Parity harness + audit pass

All parity harnesses green; IR audit pass reports 100% `->`
coverage on the three data grammars.

Sub-gate: both conditions true on the final close commit.

### AZ-I.W4.3 — Tape-scoped-to-BBNF verification

Run the grep check: `rg 'tape::|Tape(Rec|Builder|Cursor|Offset|Kind)|push_rec|output\.tape\(|payload_bytes'
crates/core/src/runtime/{json,css_l4,sheets}/` → zero matches.
The broader live-symbol scan may hit exclusively in the BBNF-scoped
consumer set and historical docs; no hit is permitted in JSON, CSS L4,
or Sheets runtime paths.
`cargo build -p bbnf` green.

Sub-gate: all three greps produce the expected result set.

### AZ-I.W4.4 — Handoff contract + FINAL.md

Verify the seven-point AZ-II handoff contract from AZ-I.md. Author
`FINAL.md` with the deltas, reversal log, and handoff report.

Sub-gate: handoff contract verified; `FINAL.md` lands.

## Hard gate

1. Three-data-grammar slice at AU floor or better on the
   struct-only path. BBNF-slice at AU baseline on the tape path.
2. All parity harnesses green across the full fixture corpus.
3. IR audit pass: 100% `->` coverage on JSON, CSS L4, Sheets.
4. Tape-scoped-to-BBNF grep checks pass; `cargo build -p bbnf`
   green.
5. AZ-II handoff contract verified (all seven points).
6. `FINAL.md` lands with deltas and reversal log.

## Dependencies

- **Depends on**: AZ-I.W3 close.
- **Blocks**: AZ-II open.
