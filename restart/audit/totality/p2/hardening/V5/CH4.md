# CH4 COST — SK-V18 T-P2 CHALLENGE (cycle V5)

Lens name: CH4 COST. Every grounded primitive must carry an admission cost —
scalar reference + checkasm parity per Lock 16, a same-wave consumer named,
realistic LOC/risk, no orphan-kernel research. Confabulated/unverifiable
citations and refuted-route groundings are inadmissible.

Disposition: `REVISE`.

Target packet (SK-V18, regenerated 2026-06-01): `2A-sota-landscape.md`
(cycle SK-V18-T-P2), `2B-primitive-vocabulary.md` (V3-SKV18), `2C-grammar-
neutrality.md` (V3), `2D-cost-model.md` (V3-SKV18), `2E-host-arch-esoterica.md`
(V6-SKV18), `2F-parse-that-gaps.md` (V3). All six carry `sk_cycle: SK-V18`.

The cost-field schema is `T-P2-V2-FOLD-ADDENDUM.md:27-64` (transfer_reason /
admission_gate / verification_action / close_status / loc_estimate / risk_class /
wave_owner / hard_cap_fit; plus SIMD-row scalar_reference / parity_or_checkasm /
hardware_gate / same_wave_consumer / row_movement_target; plus the v+1 columns
`rollback path` / `abrogate threshold`). The live wave-band authority is SPEC §8
`restart/skinny/tranches/sk-v18/SPEC.md:436-444` (G1/G2/G3/G5-G6/PROVE each
`≤450 hand source/test/gate LOC`, `≤90 min wave wall`, `30/45 min/redress`).

## V4 fold verification (all three CH4-V4 obligations LANDED)

All three prior CH4-V4 REVISE folds are confirmed present and correct:

- **CH4-V4-01 (2C cost-double-count)** — FOLDED. The `ONE-GENERATOR-
  GENERALISATION-THESIS` row (`2C:351`) now reads `SUM of two §8 bands: G2 ≤450
  + G3 ≤450`, and explicitly self-describes as `CROSS-REFERENCE row only` with
  `no body is charged AT this row` / `to avoid double-budgeting` against the two
  owning rows (the G2 body = CSS-typed-provider rows + SPEC §5; the G3 body =
  2D LAC-2D-V3-01). Verified no triple-count: 2D carries exactly one G3 `≤450`
  (`LAC-2D-V3-01`). The thesis is no longer under-budgeted by half.
- **CH4-V4-04 (2E unqualified Kutenin SPEC-CPU figure at the Source Registry)**
  — FOLDED. `2E:75` now carries `(Kutenin-reported / lineage-only, NOT a
  promotable bbnf row figure)` and quotes the post verbatim ("10-15 percent
  improvements on a `strlen` distribution extracted from the SPEC CPU 2017
  benchmark"), with `verified verbatim against the post this pass`, `memchr`
  named without a separate figure and `memcmp` NOT tied to it. The SECTION-A
  grounding row (`2E:113`) carries its own tail LEDGER FENCE. Both fold sites
  are now correctly qualified.
- **CH4-V4-05 (2A asmjson row missing v+1 cost cells)** — FOLDED. `2A:180`
  T2A-V18-ASMJSON-001 now carries `rollback path = re-author the JSON SIMD
  classifier ONLY if a future profile shows json/scan.rs hot...` and `abrogate
  threshold = if the S-P1 scan-free finding falsifies under PROVE/Sheets...
  RE-OPEN the JSON-kernel question through the profile-first gate rather than
  ship a neutralized json/scan.rs that silently regresses`. Both schema-required
  cells are present and well-formed for a deletion route.

## Citation spot-verification (load-bearing, this lens)

Every load-bearing cost grounding was confirmed against a primary source or the
on-disk tree. No confabulation found.

External (WebSearch):

- **Mison** (the 2D SinkOnly G1 R-C grounding — the only currently-substantive
  lowerer): "Mison: A Fast JSON Parser for Data Analytics", Li, Katsipoulakis,
  Chandramouli, Goldstein, Kossmann, PVLDB Vol 10(10) 2017, DOI
  10.14778/3115404.3115416 — VERIFIED EXACT this pass (all five authors, venue,
  volume, DOI). The "consumer-known speculative projection skips general
  materialization" framing matches the paper's projection-pushdown abstract.
  The SinkOnly direct-projection grounding is honest.
- **Lemire-2026 / Kutenin / iburg** — carried verified from CH4-V4 (Lemire-2026
  ARM match post live, eq-fan correctly attributed to the comment thread;
  Kutenin SHRN-by-4 post real, figure now fenced; iburg LOPLAS 1(3) 1992 DOI
  10.1145/151640.151642 real). Re-confirmed standing this pass.

Local (on-disk, this lens):

- **`find_css_significant` salvage kernel** (drives the `≤150 LOC` retarget in
  2B/2E/2F + PTG-2F-13, the SKV18-A2 "salvage not new kernel" assertion):
  `runtime_simd.rs:169-216` — VERIFIED EXACT. Two-fan split `set_a=fixed[..8]` /
  `set_b=fixed[8]⧺delimiters[..≤4]`, `byte_class_from_eq_set_64(block,set_a) |
  byte_class_from_eq_set_64(block,set_b)`, `mask.trailing_zeros()`, scalar tail.
  Only caller is `#[cfg(test)]` at `lib.rs:574` (the dead-kernel R7 proof). The
  salvage is real → the ≤150 retarget estimate is credible.
- **`RuntimeEmitterKind{CompiledLowering,RequestFacts}` fork** (the G3 `≤450`
  DELETE target grounding the un-fork cost in 2C/2D): `grammar_provider.rs:39-43`
  — VERIFIED EXACT. The iburg-faithful `select_lowering(cost.chosen)` 5-shape
  dispatch is live at `lower/mod.rs:18-24` (all five `BackendShape` arms). The
  fork is the redundancy; the DELETE is real and the `≤450` is well-targeted.
- **2D DERIVED-not-new engine** (the `≈0 LOC` LAC-2D-V3-03 regression-guard):
  `backend_egraph.rs` — `NormalizeDirectSinkCost` struct `:191`, impl `:193`,
  instantiated `:75` inside the `enable_rewrites` branch (`:74`);
  `BackoffScheduler` `:73`; `DecisionCostModel` `:83,:259`. VERIFIED the engine
  EXISTS → the "DERIVED-not-new / ≈0 LOC guard" is honest, not a build budget.
- **parse-that `scan_balanced` shell** (drives the PTG-2F-09 `≤200` re-home, the
  "substrate exists, NOT a from-scratch build" assertion): `/Users/mkbabb/
  Programming/parse-that/rust/parse_that/src/parsers/scan/balanced.rs:26` —
  VERIFIED EXACT. `scan_balanced(bytes, &BalancedScanConfig{open,close,quotes,
  ...,terminators})`, the `n <= 8` structural-byte cap, `build_nibble_luts` /
  `find_first_of_nibble_lut` SIMD skip, `memchr2` quote skip. The
  `BalancedScanConfig` takes its alphabet as caller data (the (a)-neutrality
  falsifier 2F-09 correctly gates the vendored shell on). The substrate-presence
  refutation of the V2 "gap" is honest; the `≤200` re-home (not build) is
  credible. CITATION DRIFT (nit, not a REVISE): 2F-09 cites the cap at
  `balanced.rs:44`; the `n <= 8` assert is actually at `:45` — a one-line drift;
  the cap and reasoning are real.
- **REDRESS ledger fences** (the cost/risk grounding the G6 retarget, the
  `Movemask64Shrn` / `Interleave4Classify` / CollapsedStage fences, and every
  "net-win deferred to H1" rest on): REDRESS 144 `:4418-4438`
  `G-W12-SIMD-ASM-PRODUCTION` PASS-ADMIT — Track 1 `444.208` vs `434.1316` Mbps,
  `+109.87%`, `Scanner::scan_block` — VERIFIED EXACT. REDRESS 96/97/98
  `:2928-2933` `G-W3-UNION-SUBSTRATE` retired (M5 Max wide-issue scalar cheaper
  than a streamed/materialized SIMD cursor) — VERIFIED EXACT. REDRESS 126
  `:3766-3805` `ROUTE-PRODUCTION-SPLIT` — `find_ascii_set_member64(..., b"{};")`
  microbench-only, "a passing microbench requires W4 to halt before production
  CSS wiring" — VERIFIED EXACT. The ledger framing across 2B/2E/2F/2D is honest.
- **Orphan-kernel discipline** (the load-bearing test of THIS lens): a full grep
  across all six dossiers confirms EVERY `same_wave_consumer = NONE` / SECTION-B
  primitive carries `0`, `n/a`, or an explicitly CONDITIONAL `≤N if ever built /
  if reopened` LOC with a `DO NOT author / DEFER permanently / no orphan kernel`
  abrogate clause (2E SECTION-B `:142-148` TBL/LD4/PMULL/I8MM; 2D CollapsedStage
  committed-`≈0`-inert vs conditional-`≤450` SPLIT `:98`; 2A sonic-rs SK-V19-
  receiver `:58` conditional `150-350/leaf`; 2F PTG float/quote-parity `0`/`≤100`
  profile-gated). NO no-consumer primitive carries a committed SK-V18 budget.
  This is exactly what the lens demands.

## Enumeration under CH4 COST

| # | dossier / row | disposition | basis |
|---|---|---|---|
| 1 | 2B eq-set member scan (`byte_class_from_eq_set_64`, SKV18-A1) — `≈0 LOC`, LOW, body live | ACCEPT | NEON body verified; scalar oracle + checkasm + live CSS `count_top_level_commas` consumer all present. Full Lock-16 cell. |
| 2 | 2B/2E/2F find_component_delim NEON retarget (salvage two-fan OR-reduce) — `≤150 LOC`, MED-HIGH | ACCEPT | Salvage kernel verified EXACT at `runtime_simd.rs:169-216`; REDRESS 144/96-98/126 ledger fences verified EXACT; rollback (revert to scalar) + abrogate (REDRESS-98 risk → keep scalar, G6 outcome C) present and identical across 2B/2E/2F. |
| 3 | 2E `Movemask64Shrn` SHRN-by-4 swap — `≈10 LOC`, LOW, bit-identity checkasm-gated | ACCEPT (row + registry); REVISE only the Assertion-4 prose | Kernel-internal single-site swap; SECTION-A row `:113` carries its own tail ledger fence + `(10-15% lineage-only)`; the Source Registry `:75` is now fully qualified (CH4-V4-04 fold). The Assertion-4 restatement is the one residual miss (see #11 / CH4-V5-01). |
| 4 | 2E SECTION-B host-present-no-consumer rows (TBL/LD4/PMULL/I8MM/CSSC/DotProd/SHA3) | ACCEPT | All carry `0` / `n/a` / conditional `≤N if reopened` with `DO NOT author / DEFER permanently / no orphan kernel`. Exemplary orphan-kernel discipline. |
| 5 | 2D R-A G3 un-fork — `≤450 LOC`, HIGH, + LAC-2D-V3-03 `≈0 LOC` regression-guard | ACCEPT | Engine verified to EXIST (`NormalizeDirectSinkCost` :191/:193/:75); fork verified live (`grammar_provider.rs:39-43`); G3 relocates the consumer; rollback (PATH-local revert) + abrogate (DELETE-rebuild per [abrogate-before-patch]) present. |
| 6 | 2D CollapsedStage cost SPLIT (committed `≈0` inert / conditional `≤450` G5/G6-gated) | ACCEPT | The CH4-V2-02 split is intact; the ≤450 is not budgeted against the inert slot; promotion gated on profile + transient-mask + scalar oracle + checkasm + consumer + clearing REDRESS 96/98. |
| 7 | 2A T2A-V1-SOTA-JSON-003 sonic-rs — SK-V19-receiver, blocked, conditional `150-350/leaf` | ACCEPT | Correctly NOT charged an SK-V18 budget; full v+1 cells (rollback/abrogate/hard_cap_fit) present; no-orphan abrogate explicit. |
| 8 | 2A T2A-V18-ASMJSON-001 — deletion route, `primitive_cost=none` + v+1 cells | ACCEPT | The CH4-V4-05 fold LANDED: `rollback path` (re-author JSON SIMD classifier if json/scan.rs profiles hot) + `abrogate threshold` (re-open through profile-first gate if scan-free falsifies under PROVE/Sheets) both present and well-formed for a deletion route. |
| 9 | 2F per-gap cost manifest (PTG-2F-09..13) + provenance-reconcile row | ACCEPT | Every gap keyed to §8 band with rollback/abrogate; `scan_balanced` substrate verified EXACT in the upstream tree; the substrate-presence refutation of the V2 "gap" is honest; provenance reconcile is a `≈0 LOC` decision. (Minor `:44`/`:45` cite drift noted, not load-bearing.) |
| 10 | 2C `ONE-GENERATOR-GENERALISATION-THESIS` umbrella (`2C:351`) — `SUM of two §8 bands` cross-reference | ACCEPT | The CH4-V4-01 fold LANDED and is correct: the row self-describes as `CROSS-REFERENCE row only` / `no body charged AT this row`; verified no triple-count (2D carries exactly one G3 `≤450` at LAC-2D-V3-01). |
| 11 | **2E Assertion 4 (`2E:199`) — "movemask Kutenin grounds (2 instrs, 10–15% SPEC CPU 2017)" restated UNQUALIFIED, no ledger fence, no lineage-only qualifier** | **REVISE** | (see CH4-V5-01) CH4-V4-04 qualified the SAME "10–15% SPEC CPU 2017" figure at the Source Registry (`:75`) and the SECTION-A row (`:113`, which carries a tail fence), but the Assertion-4 prose at `:199` was NOT folded. An Assertion is a defended-claim surface; an unqualified SPEC-CPU percentage there reads as a promotable warrant for the SHRN swap — exactly the load-bearing-figure problem CH4-V4-04 named. Carry the `(Kutenin-reported / lineage-only, not a promotable bbnf figure)` qualifier into Assertion 4, OR drop the percentage and keep "2-instr canonical movemask". |

## Issues opened by this lens (V5)

- **CH4-V5-01 (2E Assertion-4 unqualified Kutenin SPEC-CPU figure, REVISE).**
  `2E:199` Assertion 4 ("The movemask pack is upgradeable...") restates
  "movemask Kutenin grounds (2 instrs, 10–15% SPEC CPU 2017)" as a bare
  parenthetical with no ledger fence and no lineage-only qualifier. The
  CH4-V4-04 fold correctly qualified the identical figure at the Source Registry
  (`:75`) and the SECTION-A row carries its own tail fence (`:113`), but the
  Assertion — a defended-claim surface where the figure reads as a promotable
  warrant for the SHRN swap — was missed. Correction: append the same
  `(Kutenin-reported / lineage-only, NOT a promotable bbnf figure — see the
  SECTION-A ledger fence + REDRESS 96/97/98/126)` qualifier to Assertion 4, or
  drop the percentage and retain only the qualitative "2-instr canonical
  movemask the project already ships". Single-cell prose qualifier; the swap's
  admission (kernel-internal, single-site, checkasm-gated, scalar-ref bit-
  identical) is otherwise intact.

## Items confirmed NOT defects (orphan-kernel + over-budget axes, scanned explicit)

- **No orphan kernel admitted.** Every primitive with `same_wave_consumer = NONE`
  carries `0`/`n/a`/conditional LOC with a DO-NOT-AUTHOR/DEFER abrogate; no
  no-consumer kernel carries a committed SK-V18 budget. The G6 retarget — the
  only committed SIMD build — is profile-anchored (94.1% hot leaf) WITH its
  same-wave P3-collapsed generated call site.
- **No confabulated citation.** Mison (re-verified EXACT this pass), iburg,
  egg/POPL-2021, Langdale/Lemire-2019, Lemire-2026, Kutenin, OR-Tools CP-SAT,
  FFmpeg checkasm, parse-that `scan_balanced` — all real and faithfully
  attributed. The Kutenin SPEC-CPU figure is the technique's published lineage
  (verified verbatim in the V4 fold), correctly fenced at `:75`/`:113`; the lone
  remaining gap is the Assertion-4 restatement (a REVISE, not a confabulation).
- **No refuted-route grounding charged a cost.** Float no-fallback (2F), SVE2
  svmatch (2E, host-absent FEAT_SVE2), x86 AVX-512 close (2A/2B/2C/2E,
  P1-deleted), neutrally-named CSS-only primitive (2C `:360`), retained
  frame-stack rebuild (2B SKV18-A5), wire-`find_css_significant`-as-is (2E
  refuted, R7) — all carry `0 LOC` / `n/a` / REFUTED, none budgeted.
- **No double-count.** The CH4-V4-01 umbrella fold is verified clean (cross-
  reference row, no body charged, single owning G3 `≤450` in 2D); the 2C
  gate-only rows (`:352-360`) carry small ADDITIVE gate LOC explicitly distinct
  from the kernel bodies they gate; the eq-set `≈0 LOC` does not re-charge the
  find_component_delim `≤150` retarget.

## §3Z note

V5 is the V≤5 hard ceiling. This `REVISE` opens exactly ONE fold (CH4-V5-01), a
single-cell prose qualifier carrying the already-landed CH4-V4-04 Kutenin
lineage-only fence from the Source Registry (`:75`) into the parallel Assertion-4
restatement (`:199`) the V4 pass did not reach. It does not re-litigate any
primitive's admission, citation, wave owner, or orphan-kernel discipline — all of
which are clean. All three CH4-V4 folds landed correctly; the engine-exists
re-grounding, the orphan-kernel discipline, the REDRESS ledger fences, and every
load-bearing citation (Mison, the salvage kernel, the un-fork target, the live
engine, the parse-that shell) verify. Because this is the hard ceiling, the single
qualifier fold should be folded into the committed packet (or surfaced as a
known-residual on close); it is not a confabulation, an orphan kernel, a refuted-
route budget, or a broadcast cost.

TALLY accept=10 revise=1 reject=0
