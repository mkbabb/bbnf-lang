---
agent: 1C
pass: T-P1-excavation
cycle: V5
generated_at: 2026-05-29T19:05:00Z
spec_surfaces_audited:
  - restart/ARCHITECTURE.md
  - restart/MASTER-PLAN.md
  - restart/locks/LOCKS.md
  - restart/skinny/tranches/sk-v17/SPEC.md
  - restart/skinny/tranches/sk-v17/research/p2/p2d-substrate-tape.md
  - restart/skinny/tranches/sk-v17/research/p2/p2f-grammar-neutral.md
files_audited_count: 33
live_truth_method: "wc -l + grep -n/-c/-rln + sed -n path:line over crates/core/src/runtime/{tape/*,json/*,css_l4/*,mod.rs,builder.rs,view.rs,<g>/view.rs}, crates/core/src/grammar/generated/*.rs (9-dir / 8-scan-wired scan_structural+OnceCell census: scan_structural call-count per file via grep -c, structural_index field presence via grep -n, OnceCell mention-count via grep -c distinguishing real fields from doc-comments — math.rs:288-289 ScanState body read confirms field-absence), crates/core/src/backend/rust/emitter/shapes/substrate.rs, crates/ir/src/registry/struct.rs, crates/simd-scan/src/{lib.rs,alphabet.rs}, skinny/crates/runtime/src/grammars/json/value.rs, skinny/crates/runtime/src/tape/mod.rs; Read over tape/{record,mod}.rs, css_l4/{builder,value}.rs, json/{value,builder,view}.rs, struct.rs, LOCKS.md:75/86/784; verified @generated headers on every per-grammar value/builder/view.rs; no cargo/build mutation. Master HEAD 445925167."
prior_cycle_dispositions_folded:
  accepted:
    - CH5-S2-sidecar-OnceCell-retained-scan-cache-not-tape-projection-held
    - CH5-S3-no-cross-call-classifier-state-scan_structural-per-call
    - U-RT17-001-substrate_target-UNKNOWN-2D-05-carried
  rejected: []
  revised:
    - CH1-V4-001-math.rs-OnceCell-is-doc-comment-not-field-scan_structural=0-no-structural_index-field-ScanState-only-nospace_bits-nospace_start-math.rs:281,285-doc-comments-math.rs:288-289-ScanState
    - CH1-V3-001-RT17-004-childreniter-anchor-corrected-to-per-grammar-view.rs
    - CH1-V3-001-substrate.rs-CssStructBuilder-doc-line-62-to-60
    - CH1-V3-002-per-grammar-value-builder-modules-are-GENERATED-not-HAND-WRITTEN
    - 1c-REVISE-3A-LOCKS75-offset-tape-admissibility-vs-AV04-class-column
    - scan-census-numbers-are-call-site-lines-not-file-LOC
  first_cycle_additions: []
divergence_count:
  spec_claims_implemented: 2
  spec_claims_unimplemented: 4
  impl_exceeds_spec: 1
  unknown: 1
locks_amendment_candidates: 0
---

## Executive Summary

1C excavates the crates/core RUNTIME layer (the SK-V18 fold target) against
`ARCHITECTURE.md` §9/§4.3, `LOCKS.md` (1/2/10/14), and the SKINNY-proven
runtime/tape/value model (SK-V17 SPEC §0–§2, master `445925167`). Census:
`crates/core/src/runtime/` carries ONE generic tape construct (`TapeStructBuilder`
+ AoS 16-byte `TapeRec` + `PayloadArena` + `TapeCursor`, `tape/`), nine per-grammar
runtime dirs (json, css_l4, bbnf, bnf, csv, ebnf, css_pretty, google_sheets, math),
and three shared top-level surfaces (`builder.rs`, `view.rs`, `tape/`) plus two
`*_template.rs` codegen templates. Every per-grammar `value.rs`/`builder.rs`/`view.rs`
carries a `// @generated by xtask regen-{json,css}; do not edit by hand.` header
(json/value.rs:1, css_l4/value.rs:1, json/builder.rs:1, css_l4/builder.rs:1,
json/view.rs:1) — they are GENERATED per-grammar providers, which Lock 14 ALLOWS;
they are NOT hand-written. The divergence is that they are EAGER (per-grammar
typed-enum value trees fed by `OpenFrame` builders), NOT the lazy `ValueRef<G>`
projection the SKINNY path proves. The generic tape is UNWIRED: live parse paths
call `JsonStructBuilder::new()` (`json/parse_with.rs:34`) and `CssStructBuilder::new()`
(`css_l4/parse_with.rs:34`). By contrast the SIMD structural scan IS wired into ALL
EIGHT generated grammars (`scan_structural(input, &alphabet)` call-site lines
json.rs:732, css_l4.rs:15982, bbnf.rs:4843, bnf.rs:848, csv.rs:566, ebnf.rs:1381,
css_pretty.rs:1905, google_sheets.rs:3559 — these are scan-call line anchors, not
file LOC; the files are far larger, e.g. json.rs=3505, css_l4.rs=108406), each
holding an identical `OnceCell<StructuralIndex>` field (json.rs:701). `math.rs` is the
lone generated grammar with ZERO scan_structural calls (scan_structural=0) AND
NO `structural_index` field: its `ScanState` carries only `nospace_bits`/`nospace_start`
(math.rs:288-289), and the only two `OnceCell` mentions (`grep -c OnceCell math.rs = 2`)
are doc-comment text at math.rs:281,285 — not a field. 8 wired, not 9. The runtime carries no
`SinkOnly`/`OffsetTape`/`EventTape`/`EagerTape`/`CollapsedStage` symbols
(BackendShape is codegen-side). Lock 14 holds: grammar-named modules live ONLY
under per-grammar `runtime/<g>/` dirs (ALLOWED); the emitter data-binds
builder/document paths (`substrate.rs:43,55`), never branches on a grammar name.
No parallel substrate, no cross-call classifier state. Divergences catalogued for
T-P2/T-P3; no proposals.

## Spec-Claim ↔ Implementation Table

| ID | Spec / corpus claim (path:line) | Live runtime evidence (path:line) | Verdict | Divergence class | Note |
|---|---|---|---|---|---|
| RT17-001 | ARCH §9 / Lock 1: the runtime substrate is `&'i Tape<'i>` + cursor; "if structural offsets are retained, the structural projection IS the tape" (`restart/ARCHITECTURE.md:1088`; `restart/locks/LOCKS.md:75`). Lock 1 admits the projection forms explicitly: "**the projection may be an offset tape, event tape, or collapsed-stage event sink**" (`LOCKS.md:75`) and "**Lazy-offset tape with sparse flags is admitted**" (`LOCKS.md:86`). SK-V17 names the proven runtime tape as an offset tape: SoA `Tape<'input>` (`offsets:Vec<u32>` + sparse `flag_cursors`/`flag_values` + `PayloadArena`) with `ValueRef<'doc,'input,K,G>` (`restart/skinny/tranches/sk-v17/SPEC.md:51-53`; p2d `:34-49`; `skinny/crates/runtime/src/tape/mod.rs:94-100,175`). | crates/core runtime tape is `TapeStructBuilder` over a 16-byte AoS `TapeRec` (`crates/core/src/runtime/tape/record.rs:103`; `:1-10` "16-byte post-order node … AoS, `#[repr(C)]`, align 4"), with `TapeCursor`. The mod doc states "kept AoS first … the same `TapeCursor` API rides a later SoA split" (`crates/core/src/runtime/tape/mod.rs:5-9`). | cross-tree runtime tape encoding drift (AoS vs offset-SoA) | unimplemented | Two runtime tape ENCODINGS, both admitted offset-tape shapes under Lock 1: core AoS `TapeRec` vs skinny offset-SoA `Tape`. Neither is the AV.04 dense kind-partitioned class-column SoA that Lock 1's "**columnar SoA is dead**" / "Columnar SoA stays buried" clause bars (`LOCKS.md:75`; AV.04 spec `docs/tranches/AV/research/04-columnar-soa.md`, registered `LOCKS.md:784`). The fold must converge to EXACTLY ONE encoding post-SK-V18 ("parallel substrates are dead", `LOCKS.md:75`); the dual encoding is a transient fold-state, not a permissible end-state (Open Q U-RT17-002). Re-emit is generator-side / regen-gated. |
| RT17-002 | SK-V17 W2 generalizes the runtime value-API to ONE `BackendRule`-walking projection generator emitting `document/value/view/visitor`, isomorphic to JSON's `value_from_ref` over the EXISTING `Tape`/`ValueRef`, grammar-parametric `ValueRef<…,G>` (`restart/skinny/tranches/sk-v17/SPEC.md:58-62`; p2f `:30-40`; skinny `value_from_ref` at `skinny/crates/runtime/src/grammars/json/value.rs:143`, over `ValueRef` `:144`). | crates/core has per-grammar GENERATED EAGER value/view modules (`// @generated by xtask regen-{json,css}`, json/value.rs:1, css_l4/value.rs:1): `json/value.rs` exports `JsonValue`/`JsonNumber`/`JsonPair`/`JsonArray`/`JsonObject` enums+structs (`:4,:13,:29,:34,:38`) with NO `value_from_ref`/`ValueRef` (grep over `crates/core/src/runtime/json/` returns empty); `css_l4/value.rs` exports eager `CssTypedValue` (`:414`) + many eager unit enums (`:7 CssLengthUnit`, …). | per-grammar EAGER value-API vs lazy `ValueRef<G>` | unimplemented | The §0.1.11 fold target: core's value layer is GENERATED-but-EAGER per-grammar typed enums fed by `OpenFrame` builders, not the lazy `ValueRef<G>` projection. Per-grammar generated providers are Lock 14 ALLOWED — the divergence is the EAGER materialization shape, not the per-grammar locus, not hand-authorship (the files are `@generated`). Skinny path at `…/grammars/json/value.rs:143` (the `grammars/` segment is load-bearing; the short `json/value.rs:143` form resolves to skinny but the segment disambiguates from core). |
| RT17-003 | SK-V17 §0.1.11 item 11: the unified tape/layout/projection is structured so the TOTALITY tree (`crates/core/src/runtime/tape/`) can adopt it in SK-V18; NEON "is gated behind tape activation" (`restart/skinny/tranches/sk-v17/SPEC.md:106,110-114`). | Confirmed UNWIRED-but-scan-wired split: `TapeStructBuilder` is referenced NOWHERE outside `crates/core/src/runtime/tape/` for live parsing; live JSON uses `JsonStructBuilder::new()` (`json/parse_with.rs:11,34`), live CSS uses `CssStructBuilder::new()` (`css_l4/parse_with.rs:10,34`). Yet `scan_structural` is wired into ALL 8 generated grammars (call-site lines json.rs:732 … google_sheets.rs:3559; each grep `scan_structural(`=1), each holding `OnceCell<StructuralIndex>` (json.rs:701 field). `math.rs` has scan_structural=0 and NO `structural_index` field — its only `OnceCell` mentions are doc-comments at math.rs:281,285 (`grep -c OnceCell math.rs = 2`, both doc-comment text). | tape-unwired + scan-wired (all 8) split | spec-claims-implemented (UNWIRED confirmed as stated) | The runtime tape is dormant fold-target code while the structural scan is live across all 8 grammars; the index exists but no tape consumes it (the eager builders consume neither). This is the SK-V18 subject by spec design (`SPEC:110-114`), not a defect. |
| RT17-004 | Lock 14: generic crates carry ZERO grammar-named modules; per-grammar runtime providers/templates are ALLOWED (`restart/locks/LOCKS.md:75` "per-grammar runtime modules (template-emitted at `runtime/src/grammars/<name>/`) emit accessors"; SK-V17 SPEC `:334` "allowed CSS-specific surfaces: per-grammar providers/templates"). The generic tape "dispatches on the `StructLayout` … never on per-grammar route strings" (`crates/core/src/runtime/tape/mod.rs:55`). | crates/core runtime: grammar-named symbols (`JsonStructBuilder`, `CssStructBuilder`, `CssTypedValue`, `JsonChildrenIter` at `json/view.rs:39`, `CssChildrenIter` at `css_l4/view.rs:44`) live ONLY under per-grammar dirs `runtime/<g>/`, all in `@generated` files. The generic tape (`runtime/tape/`) + shared `runtime/builder.rs`,`runtime/view.rs` (the top-level `view.rs` is 76 lines, carries NO grammar names — `:39`/`:44` there are doc-comment lines). Emitter data-binds `rust.builder_path`/`rust.document_path` from `EmitStrategy::StructDirect` (`substrate.rs:43,55`), not a grammar-name branch. | Lock 14 honoured | implemented | The literal `::bbnf::runtime::css_l4::CssStructBuilder` appears only in a `substrate.rs:60` doc-comment example; the live resolver parses a binding string (manifest data) and panics on malformed input (`substrate.rs` AZ-IV §Hard Gates 19 hard-fail at `:82`), never falling back to `JsonStructBuilder`. Per-grammar generated providers are the Lock-14-ALLOWED shape — they are template-emitted, not hand-written, so they are not a Lock-14 violation. |
| RT17-005 | Lock 1 v+1: a retained structural projection IS the tape if a tape consumes it, else `local_temp_only` (`restart/locks/LOCKS.md:75`; substrate_target manifest, ARCH `:1083`). REDRESS-53 bars "L1/L4 index retained as a parallel vector" (SPEC `:825`,`:839`; second-substrate block `:807-811`). | ALL 8 generated grammars carry an identical `OnceCell<StructuralIndex>` sidecar fed by `scan_structural` (json field+init at `:701`,`:711`; ensure-fn at `:719`; scan-call sites bbnf:4843, bnf:848, css_l4:15982, css_pretty:1905, csv:566, ebnf:1381, google_sheets:3559, json:732). It is a per-parse retained scan cache that today feeds the eager builders, NOT a tape. `math.rs` is the lone NO-scan grammar: scan_structural=0 and NO `structural_index` field — its only `OnceCell` references are doc-comment text at math.rs:281,285. | retained scan-cache (8 carriers), pre-fold | unimplemented (substrate_target unclassified) | Under SK-V18 the `OnceCell<StructuralIndex>` must become the tape's `offsets` (index IS the tape) OR be re-classified `local_temp_only`; a retained index parallel to a wired tape is REDRESS-53 re-entry (SPEC `:825`,`:839`). The largest grammar (css_l4) carries the identical retained index — census enumerates all 8, not a 4-grammar sample. |
| RT17-006 | SK-V17 W1 global pre-block: "StructRegistry / Arena<G> / Builder<G> hot-path indirection (28-65× bbnf/sheets, 983× css bootstrap, 10583× WATCHDOG tailwind). No registry lookup in the per-leaf hot path" (`restart/skinny/tranches/sk-v17/SPEC.md:794-795`; W1 row `:824`). | `StructRegistry` is `layouts: BTreeMap<RuleId, StructLayout>` (`crates/ir/src/registry/struct.rs:313-314`); `FieldSource` (`:84`) lives INSIDE `StructLayout` (`:202`) inside the registry. The runtime tape `begin_compound(&StructLayout)` (`crates/core/src/runtime/tape/mod.rs:185`) takes a RESOLVED `&StructLayout` by reference (not a per-leaf registry lookup). | hot-path-indirection pre-block (do-not-redrive) | unimplemented (fold must fence) | DO-NOT-REDRIVE: the FieldSource walk that produces the projection (RT17-002 Value-API + Layout-shape rows) MUST be compile-time projection-emission, resolved ONCE at codegen, NOT a per-leaf `StructRegistry`/`FieldSource` walk in the runtime hot path — a naive per-leaf walk re-opens the 28-65×/983×/10583× regression (SPEC `:794-795`). |
| RT17-007 | ARCH §7.3 BackendShape 5-shape canon `{EagerTape,OffsetTape,EventTape,SinkOnly,CollapsedStage}` (`restart/ARCHITECTURE.md:31,1114`); SK-V17 NEON sits under the 4 LLVM shapes, NO aarch64 CollapsedStage; CollapsedStage aarch64 is the spec-named UNKNOWN-2D-05 (`restart/ARCHITECTURE.md:1206`). | The crates/core RUNTIME tree carries ZERO `SinkOnly`/`OffsetTape`/`EventTape`/`EagerTape`/`CollapsedStage` symbols (grep over `crates/core/src/runtime/` + `crates/core/src/grammar/generated/` returns empty). BackendShape is a codegen/IR-side selection (1B scope), not a runtime-emitted symbol. | shape-canon-codegen-side (runtime-clean) | impl-exceeds-spec (runtime carries no shape leak) | The runtime correctly carries no shape enum; the shape choice is upstream (cost model → emitter). CollapsedStage's aarch64 candidate is the spec-flagged UNKNOWN-2D-05 (`ARCH:1206`), NOT a fresh runtime gap. |

## Cross-Tree Runtime Map (SK-V17 fold subject)

| Concern | SKINNY-proven (skinny/crates) | TOTALITY fold target (crates/core) | Fold reconciliation (one-substrate, Lock 1) |
|---|---|---|---|
| Runtime tape encoding | offset SoA `Tape<'input>`: `offsets:Vec<u32>` + sparse `flag_cursors`/`flag_values` + `PayloadArena` (`skinny/crates/runtime/src/tape/mod.rs:94-100`; p2d `:34-49`) — an admitted offset-tape (`LOCKS.md:75` "the projection may be an offset tape"; `:86` "Lazy-offset tape with sparse flags is admitted") | AoS 16-byte `TapeRec` in `TapeStructBuilder` (`crates/core/src/runtime/tape/record.rs:103`; `tape/mod.rs:58`) — also an offset-tape shape (16-byte post-order node) | ONE encoding post-SK-V18 ("parallel substrates are dead", `LOCKS.md:75`); adopt proven offset-SoA or prove AoS parity. Neither is the AV.04 dense class-column SoA barred by "columnar SoA is dead" / "Columnar SoA stays buried" (`LOCKS.md:75`; AV.04 spec registered `LOCKS.md:784`); the dual encoding is a transient fold-state, not a permissible end-state |
| Builder | `TapeBuilder`/`push_plain_offset` (SPEC `:53,:444`) | generic `TapeStructBuilder` impl `StructBuilder` (UNWIRED, `tape/mod.rs:58,133`) + live eager `JsonStructBuilder` (`json/builder.rs:1` `@generated`, 231 LOC) / `CssStructBuilder` (`css_l4/builder.rs:1` `@generated`, 817 LOC) `OpenFrame` | retire eager `OpenFrame` builders; wire `TapeStructBuilder`; re-emit touches 22+ files (8 generated parsers + ~10 emitter shape files + runtime/{mod,builder}.rs + both parse_with.rs + tests), all generator-side / regen-gated |
| Value API | grammar-parametric `ValueRef<'doc,'input,K,G>` + per-grammar `value_from_ref` (`skinny/crates/runtime/src/tape/mod.rs:175`; `skinny/crates/runtime/src/grammars/json/value.rs:143-144`) | per-grammar `@generated` EAGER typed enums; NO `ValueRef`/`value_from_ref` in core (grep empty over `crates/core/src/runtime/json/`, `css_l4/`); `CssTypedValue` (`css_l4/value.rs:414`) | generate ONE `BackendRule`/`FieldSource`-walking lazy projection. The FieldSource walk is **compile-time projection-emission, resolved once** (RT17-006; SPEC `:794-795`) — NOT a per-leaf runtime `StructRegistry` walk. The per-grammar generated locus stays (Lock-14 ALLOWED); the EAGER shape is what changes |
| Layout shape | `BackendRule` + `LayoutFacts.backend_shape` (alphaC `:29`) | `StructLayout` + `FieldSource{TypedLeaf,BranchTag,SeqPosition,RepeatElement,RuleReference}` (`crates/ir/src/registry/struct.rs:84,202`) | Lock-2 name reconcile (`StructLayout`→`Layout`, `LOCKS.md:160`); FieldSource IS the BackendRule-walk recipe, walked **once at compile time** (not per-leaf), citing SPEC `:794-795` |
| Structural scan | aarch64 `select_classifier(&[u8;64])`→`Vec<u32>` (`skinny/crates/bbnf-simd/src/dispatch.rs:42`); CSS scan is the W3 wire, **gated behind tape activation** (SPEC `:106`) — wiring-state, not design property | multi-arch `scan_structural(input, &alphabet)`→`StructuralIndex`, WIRED into ALL 8 generated grammars (`scan_structural(`=1 each; call-site lines json:732, css_l4:15982, bbnf:4843, bnf:848, csv:566, ebnf:1381, css_pretty:1905, google_sheets:3559) (`crates/simd-scan/src/lib.rs:80`). `math.rs` is the lone field-only-absent grammar: scan_structural=0 and NO `structural_index` field (its `OnceCell` mentions at math.rs:281,285 are doc-comments, not a field) — 8 wired, not 9 | scan is ALREADY grammar-general in core (8 grammars wired); the residual gap is the missing TAPE CONSUMER, NOT the scan |
| Per-grammar runtime modules | `runtime/src/grammars/<g>/{value,view,document}.rs` (Lock 14 allowed, template-emitted) | `runtime/<g>/` dirs (json,css_l4,bbnf,bnf,csv,ebnf,css_pretty,google_sheets,math), all `@generated` (Lock 14 allowed) | generic `runtime/{tape,builder.rs,view.rs}` stay grammar-neutral; per-grammar providers data-bound via `substrate.rs:43,55` |

## Divergences Catalogued

| ID | Divergence | Evidence | Files-touched / LOC / risk |
|---|---|---|---|
| RT17-001 | Runtime tape encoding differs: core AoS `TapeRec` vs skinny-proven offset-SoA `Tape` — BOTH admitted offset-tape shapes (`LOCKS.md:75,:86`), NEITHER the barred AV.04 dense class-column SoA. The Lock-1 post-fold closure obligation is EXACTLY ONE encoding ("parallel substrates are dead", `LOCKS.md:75`); a dual encoding is not a permissible end-state. | `crates/core/src/runtime/tape/record.rs:103,120`; `skinny/crates/runtime/src/tape/mod.rs:94-100`; p2d `:34-49`; `LOCKS.md:75,:86,:784` | 200-600 LOC SK-V18 fold; medium. Files-touched: re-emit is generator-side, regen-gated; eager-builder retirement touches 22+ files (see RT17-003). |
| RT17-002 | Value API is per-grammar `@generated` EAGER typed enums in core, not lazy `ValueRef<G>`. The files are generated (Lock-14 ALLOWED), so the divergence is the EAGER materialization shape, not hand-authorship or per-grammar locus. | `crates/core/src/runtime/css_l4/value.rs:1,:414`, `json/value.rs:1,:4-38`, `css_l4/view.rs:44`, `json/view.rs:39`; no `value_from_ref` in `crates/core/src/runtime/json/`; skinny `…/grammars/json/value.rs:143-144` | 300-700 LOC; high. Distinguish HAND-WRITTEN generator-LOC (the emitter projection generator) from REGENERATED emitted-LOC (per-grammar value.rs/view.rs re-emit set). |
| RT17-003 | Core runtime tape UNWIRED while structural scan WIRED in ALL 8 grammars — asymmetric. | `crates/core/src/runtime/json/parse_with.rs:34`; `css_l4/parse_with.rs:34`; `scan_structural(`=1 in all 8 generated (call sites json.rs:732 … google_sheets.rs:3559); `TapeStructBuilder` grep-zero outside `tape/` | 0 LOC to catalogue; high to wire (SK-V18 subject). Eager-builder retirement re-emit surface = 22+ files (8 generated parsers carrying `*StructBuilder` + ~10 emitter shape files + runtime/{mod,builder}.rs + both parse_with.rs + tests). |
| RT17-004 | Per-grammar runtime dirs carry grammar-named symbols in `@generated` files (Lock 14 ALLOWED); generic `runtime/{tape,builder.rs,view.rs}` are grammar-neutral. | `crates/core/src/runtime/{json,css_l4}/*` (`@generated` headers `:1`); `json/view.rs:39` (`JsonChildrenIter`), `css_l4/view.rs:44` (`CssChildrenIter`); top-level `view.rs` 76 lines grammar-clean; `tape/mod.rs:55`; `substrate.rs:43,55,60,82` | 0 LOC; Lock 14 honoured, no leak. Confirm at fold: tape wiring replaces `*StructBuilder` route with `TapeStructBuilder` dispatching on `StructLayout`; emitter `substrate_path` survives as DATA, not a grammar-name branch. |
| RT17-005 | `OnceCell<StructuralIndex>` retained scan-cache in ALL 8 SCAN-WIRED generated grammars (math.rs has scan_structural=0 and NO `structural_index` field — its `OnceCell` mentions at math.rs:281,285 are doc-comments) — substrate_target unclassified pre-fold. | json field+init `:701,:711`; ensure-fn `:719`; scan-call sites bbnf:4843, bnf:848, css_l4:15982, css_pretty:1905, csv:566, ebnf:1381, google_sheets:3559, json:732 | 0 LOC to catalogue; high to classify. Must become tape `offsets` or `local_temp_only`; parallel-to-wired-tape = REDRESS-53 (SPEC `:825,:839`). |
| RT17-006 | StructRegistry/Arena<G>/Builder<G> hot-path indirection pre-block: FieldSource walk must be compile-time, resolved once. | `crates/ir/src/registry/struct.rs:84,202,313`; `crates/core/src/runtime/tape/mod.rs:185` (`begin_compound(&StructLayout)` takes resolved layout by ref); SPEC `:794-795`, W1 row `:824` | 0 LOC (do-not-redrive fence); high regression if violated (28-65×/983×/10583×). No per-leaf registry lookup. |
| RT17-007 | Runtime tree carries no BackendShape symbols (codegen-side); aarch64 CollapsedStage is spec-named UNKNOWN-2D-05. | grep empty over `crates/core/src/runtime/` + `…/generated/` for the 5 shape names; `restart/ARCHITECTURE.md:1206` UNKNOWN-2D-05 | 0 LOC; runtime-clean. |

## Gaps / Missing Primitives

| Gap | Evidence | Receiver |
|---|---|---|
| No live tape consumer in crates/core runtime: `TapeStructBuilder` exists but no parse path calls it. | `crates/core/src/runtime/json/parse_with.rs:34` uses `JsonStructBuilder`; `css_l4/parse_with.rs:34` uses `CssStructBuilder`; `TapeStructBuilder` grep-zero outside `tape/` | SK-V18 fold (T-P2/T-P3 map; SK-V17 proves in skinny first) |
| No `ValueRef<G>` lazy-projection generator in crates/core; value APIs are `@generated` but EAGER per grammar. | `crates/core/src/runtime/{json,css_l4}/value.rs:1` (`@generated`; no `value_from_ref`/`ValueRef`) | SK-V17 W2 proves in skinny (SPEC §5); SK-V18 folds into core |
| Core CSS structural scan **IS wired** (`css_l4.rs:15976-15982` builds `StructuralAlphabet` + calls `scan_structural`); the residual gap is the missing TAPE CONSUMER, NOT the scan. The skinny-side CSS scan (W3) + the absent tape consumer are the true Lock-1 gaps. | `crates/core/src/grammar/generated/css_l4.rs:15936,15951,15976-15982`; SPEC §9.9 item 9 `:99-106` (skinny NEON "gated behind tape activation") | SK-V17 W3 wires skinny CSS scan (gated behind tape); SK-V18 wires the core tape consumer |
| No aarch64 CollapsedStage admission path; the §7.3 canon's 5th shape is x86-only and the aarch64 candidate is the spec-named UNKNOWN-2D-05. | `restart/ARCHITECTURE.md:1206`; SPEC §9 `:854` | T-P2 research per UNKNOWN-2D-05 (spec-flagged open unknown, not fresh decision) |

## Open Questions

| UNKNOWN | Blocking question | verify_action |
|---|---|---|
| U-RT17-001 (substrate_target) | Is the `OnceCell<StructuralIndex>` in the 8 scan-wired generated parsers a `local_temp_only` scratch or an `existing_tape` projection under the Lock 1 v+1 substrate_target manifest (`ARCH:1083`; `LOCKS.md` Lock 1 v+1)? | T-P2 read all 8 generated `structural_index` fields (json.rs:701 + 7 siblings) and classify against the four admitted substrate_target values; the fold must declare it before wiring the tape, else REDRESS-53 re-entry (SPEC `:825`). Scope to ALL 8 scan-wired, not json alone (math.rs neither scans nor carries the field — its `OnceCell` mentions at math.rs:281,285 are doc-comments). |
| U-RT17-002 (one-substrate closure) | Do the two runtime tape encodings (core AoS / skinny offset-SoA — both admitted offset-tape shapes, neither the barred AV.04 class-column) converge to EXACTLY ONE in SK-V18 per Lock 1 (`LOCKS.md:75` "parallel substrates are dead"), or does the totality tree keep AoS and prove parity? A dual encoding MUST NOT read as a permissible end-state. | Re-read `crates/core/src/runtime/tape/mod.rs:5-9` ("AoS first … later SoA split") against SPEC `:110-114` and `LOCKS.md:75,:86`; T-P2 names the single convergence-target encoding; T-P3 asserts the exactly-one-encoding post-SK-V18 obligation. |
| U-RT17-003 (FieldSource walk timing) | Is the SK-V18 value-projection FieldSource walk guaranteed compile-time-resolved-once (no per-leaf registry indirection in the runtime hot path)? | T-P2 confirm the projection generator emits a static per-rule projection (no runtime `StructRegistry::layout(rule)` call per leaf); cite SPEC `:794-795` as the fence; any per-leaf walk re-opens the 28-65×/983×/10583× regression. |
