# SK-V14 P3-E: Pre-Blocked-Route Ledger

Pass: S-P3 Synthesis-Plan. Cycle: V1.
Date: 2026-05-23.
Scope: Walk `skinny/REDRESS.md` (~160 items across SK-V0…SK-V13 + W14/W15
admits). Classify every entry as PERMANENT-PRE-BLOCK,
WAVE-CONDITIONAL-PRE-BLOCK, or RESOLVED-OK. Emit per-wave pre-block list
for the SK-V14 wave program (W0 + R3 PRUNE-1..5 + R1 + R2 + R4 + R5 + R6
+ R7 + R8) per PASS-3-SYNTHESIS-PLAN.md §2 (P3-E row) under the
`[no hypothesis transfer]` discipline (PASS-3-SYNTHESIS-PLAN.md §8.6).
Output: this file.
Pass Alpha goalset: SK-V14 SYNTHESIS §0.1 R10 close (every JSON cell of
51 = 17×3 planes + every CSS L4 feature of 24 non-OUT_OF_SCOPE ADMITs
strict-vs-strict on the same plane / corpus / equality, OR carries
architectural-block proof per row family).
Candidate pool: `research/p2/` post-V3 LOCK survivors (5 active
candidates C-1..C-5 per SYNTHESIS §3); see p3a-candidate-shortlist for
the ≤8 distillation.

## §1 — Synthesis

The SK-V14 REDRESS ledger spans 160 numbered items at
`skinny/REDRESS.md:1-5041` and one ~60-item supplementary section
(`Implemented Redress` 1-26 at `skinny/REDRESS.md:92-309`; `Skinny Spec
Amendments Folded` 1-14 at `skinny/REDRESS.md:970-1080`; `Full V1
Amendments Folded` 1-5 at `skinny/REDRESS.md:1081-1118`; `Next
No-Workaround Work` 1-4 at `skinny/REDRESS.md:1212-1240`). The numbered
items 1-160 are the primary substrate; the supplementary sections
record substrate-discipline carryovers that S-P14 inherits trivially.

The watch-list called by the dispatch — REDRESS 28+33, 50-55, 60-72,
80, 82-84, 88, 89, 96-98, 119/120, 126 — is partial. The full audit
of all 160 numbered entries surfaces three further binding clusters
the dispatch omitted:

1. **REDRESS 102-110 (SK-V10 W3-W10)** — parse-only firewall,
   `instruments` typed admit, root-typed proof, string microproof,
   escape segment microproof, kernel-production rejection, direct
   residual admission. These rows form the SK-V10 close ledger and
   carry directly into the SK-V14 R7 (typed) + R8 (parse_only)
   re-admit waves.
2. **REDRESS 119+120 (SK-V11 W8+W9 fixpoint)** — the entire 13-row
   direct residual fixpoint table. The dispatch flags 119/120 by
   number; SYNTHESIS §5 binding `closing a JSON row through
   REDRESS-119 / REDRESS-120 history without fresh SK-V14 evidence
   (both LIFTED per addendum; HISTORY only)` makes this carry-forward
   binding for every R7 direct row attempt.
3. **REDRESS 133-135 (SK-V13 W10.1-W10.3 CSS) + REDRESS 154-158
   (SK-V13 W14.1-W14.5 JSON parse_only) + REDRESS 160 (SK-V13 W15.1
   UpdateCenter typed)** — every one is AUDIT-FALSIFIED under
   SK-V14 SYNTHESIS §0.2 + §1.2 + the S-P0 SYNTHESIS-AUDIT-OVERFIT
   §3 + §3.1 ledger (C-5 covers PRUNE-1 + PRUNE-2 revert of these
   exact 30 rows). C-5 reverts them; R6/R7/R8 must re-establish them
   under the rebound bar.

The synthesis builds from three cross-references:

- **SYNTHESIS §0.4 P-1..P-7 pattern-level pre-blocks** at SYNTHESIS.md:
  104-148. P-1 fake `@generated` header, P-2 `sonic_rs::from_slice::
  <Value>` mislabel, P-3 tiny-fixture Mbps inflation, P-4 gate-relabel
  as admit, P-5 scaffold-research as load-bearing, P-6 per-grammar
  provider modules in generic codegen, P-7 Track 1 ≡ Track 2 dishonesty.
- **S-P0 SYNTHESIS-AUDIT-OVERFIT.md §3.1 coverage table** at audit-
  overfit/SYNTHESIS-AUDIT-OVERFIT.md:344-352. Every C-N candidate
  ratifies a finding cluster: C-1 covers 41 findings, C-2 covers 7,
  C-3 covers 11, C-4 covers 4, C-5 covers 11 = 73 of 74 (the 74th
  becomes the CH7-companion extension at §2.4 audit-overfit:293-333).
- **S-P2 V3 CONSOLIDATED §3 CH3 row** at p2/hardening/HARDENING-S-P2-
  V3-CONSOLIDATED.md:101: REDRESS-126 (C-P2C-7 demotion), REDRESS-88
  (NF-CH6-3 C2 scalar-ref upgrade), and REDRESS 60-72 (NF-CH6-4 §2.Y
  cross-axis tracking note) are V3-intact; V3 verb-tense fix re-opened
  zero REDRESS routes. **No SK-V14 S-P2 V3 edit re-opened any REDRESS
  route.**

The `[no hypothesis transfer]` discipline binds: every SK-V14 candidate
is grounded on SK-V14 S-P1 profile (cf. p1 axis files + V1
CONSOLIDATED); prior-iteration rejected routes do NOT re-enter under a
new name. This P3-E ledger is the structural defence — any wave plan
that surfaces an entry from §3 below without a NAMED-framing change
and FRESH SK-V14 S-P1 evidence is REVISE.

## §2 — Deliverable: REDRESS entry classification

160 numbered items classified across three buckets. Each entry cites
`skinny/REDRESS.md:line` for the Item-N header line.

### §2.1 PERMANENT-PRE-BLOCK (45 entries)

These routes are closed by architectural cause; never re-open under
any framing in SK-V14 (or any future bracket without explicit user
re-pin + architectural-block intrinsic evidence). The cause is a
measured-decisive falsification of the route family, not a tactical
miss.

**SK-V3 / SK-V5 substrate-shape rejections:**

- **Item 28** at `skinny/REDRESS.md:324` — SK-V3 W0/W1 active 16-byte
  NEON tiny-string dispatch REJECTED; cause: real `twitter` 25%
  regression on both tracks. Carrier: W3 (SK-V7) admitted only
  generated-retained cap-16 + Track 2/direct cap-8 split (Item 72 at
  `skinny/REDRESS.md:1996`); SK-V14 must not re-globalize cap-16.
- **Item 33** at `skinny/REDRESS.md:394` — SK-V5 W3 Class A
  `match_tiny_plain_string` NEON wiring INVALIDATED; cause: parse-G
  fix wrong target (the corrected target was NEON UTF-8 codepoint
  pipeline, not tiny-string).
- **Item 50** at `skinny/REDRESS.md:715` — SK-V5 parse-time aux side
  tables REJECTED; cause: dense per-cursor aux column regressed
  governing parse plane -25.4%/-29.2%/-2.2% on twitter/citm/canada;
  sparse variant also regressed.
- **Item 51** at `skinny/REDRESS.md:742` — SK-V5 byte-class whitespace
  cursor REJECTED; cause: `JsonEventCursor` collapsed parse plane to
  7130/10291/14110 Mbps below 12398/21110/17321 baseline. Binds: no
  precomputed `StructuralIndex`, no `Vec<JsonEvent>`, no whitespace
  bitmap sidecar.
- **Item 53** at `skinny/REDRESS.md:784` — SK-V5 structural-mask
  parser-local cursor REJECTED; cause: second structural scan while
  recursive descent still reads source. Binds: structural projection
  must be parser's single substrate, not a second scanner. Together
  with Item 96+97+98 SK-V9 W3 (below), this is the union-substrate
  intrinsic block.
- **Item 54** at `skinny/REDRESS.md:815` — SK-V5 exact decoded-string
  stats sink REJECTED; cause: escape-heavy rows regressed
  (`unicode_mixed` 3428/4187 vs baseline; `unicode_escapes` 2385/5117).
- **Item 55** at `skinny/REDRESS.md:846` — SK-V5 quote-source fused
  string materializer REJECTED; cause: sink-local streaming hash also
  below allocate-then-hash baseline.

**SK-V6 string/long-string scan family:**

- **Item 60** at `skinny/REDRESS.md:1346` — SK-V6 W2 retained
  trusted-string boundary collapse REJECTED; cause: -20.5% to -46.9%
  regression on all seven measured rows.
- **Item 61** at `skinny/REDRESS.md:1382` — SK-V6 W2 retained
  long-string trusted scan REJECTED; cause: only gsoc-2018 cleared
  10% bar; `canada` -9.8%, `instruments` -7.5%.
- **Item 62** at `skinny/REDRESS.md:1441` — SK-V6 W2 delayed-wide
  retained trusted string scan REJECTED; cause: only `unicode_mixed`
  improved; multiple sentinel-row regressions. Binds: delete-the-
  early-out + globally-widen string scan are the blocked family.

**SK-V6 direct-string materializer family:**

- **Item 64** at `skinny/REDRESS.md:1584` — SK-V6 retained
  Unicode-escape run validator REJECTED.
- **Item 65** at `skinny/REDRESS.md:1639` — SK-V6 object next-key
  carry REJECTED; cause: mirror-shape of admitted array carry but
  measured-regressive.
- **Item 66** at `skinny/REDRESS.md:1688` — SK-V6 W3 direct
  source-hook field-layout materializer REJECTED.
- **Item 67** at `skinny/REDRESS.md:1736` — SK-V6 W3 parser-owned
  decoded scratch for generated direct escaped strings REJECTED.
- **Item 68** at `skinny/REDRESS.md:1789` — SK-V6 W3 byte-output
  `unescape_json_string` materialization REJECTED.
- **Item 69** at `skinny/REDRESS.md:1839` — SK-V6 W3 DirectBuild
  semantic string field facts REJECTED.
- **Item 70** at `skinny/REDRESS.md:1890` — SK-V6 W3 first
  `real_typed_struct` implementation as SOTA close REJECTED.
- **Item 73** at `skinny/REDRESS.md:2063` — SK-V6 W2 retained Track 2
  array next-byte dispatch parity repair REJECTED.

**SK-V7 W2-W6 hot-leaf family:**

- **Item 80** at `skinny/REDRESS.md:2217` — SK-V7 W2 mantissa-widen on
  canada REJECTED; cause: 0% fallback rate (Eisel-Lemire table already
  covers the f64 exponent range). The hypothesis itself was stale.
- **Item 82** at `skinny/REDRESS.md:2287` — SK-V7 W4 single-quartet
  Unicode escape classifier REJECTED; cause: 4/4 rows missed threshold;
  `y_string_unicode` direct Track 2 regressed 6.6%. Binds: no
  per-quartet materializer helper; no REDRESS 64 or 66-69 reopen.
- **Item 83** at `skinny/REDRESS.md:2320` — SK-V7 W5 generated-retained
  StringBlock16 tiny probe REJECTED; cause: 6/6 parse rows missed; all
  Track 1 regressed >3%. The AArch64 `string_block` movemask is too
  expensive for the tiny generated retained quote-pair probe.
- **Item 84** at `skinny/REDRESS.md:2360` — SK-V7 W6 object-pair
  value-byte control compaction REJECTED; cause: `citm_catalog` parse
  Track 1 below no-regression floor; `instruments` parse Track 1
  92.9% of sonic. Binds: no object next-key carry reopen, no
  separator elision, no function-pointer dispatch, no generic SWAR
  whitespace, no EventCursor sidecar, no W5 string leaf reopen.

**SK-V7 W10/W10b AArch64 bitmap families:**

- **Item 88** at `skinny/REDRESS.md:2510` — SK-V7 W10 consumed AArch64
  bitmap bodies (PMULL `bitmap_prefix_xor_64`) REJECTED; cause:
  -4.62% to -15.52% throughput on escape-heavy/narrow parse rows. PMULL
  as default hot prefix-XOR is not admissible on this host. (S-P2 V3
  NF-CH6-3 C2 scalar-ref upgrade ratifies the architectural-block
  shape for SK-V14.)
- **Item 89** at `skinny/REDRESS.md:2544` — SK-V7 W10b CTZ bulk
  consumer + B6 canary fold (the narrowed retry of 88) REJECTED;
  cause: 6 Track 1/2 rows dropped >2% vs saved pre-W10b. (Even the
  PMULL-removed retry failed; the family is closed.)

**SK-V9 W3 union-substrate intrinsic block:**

- **Item 96** at `skinny/REDRESS.md:2797` — SK-V9 W3 class-column +
  move-consumed structural-index REJECTED; cause: 10/10 rows
  FAIL; full-vector allocation cost.
- **Item 97** at `skinny/REDRESS.md:2852` — SK-V9 W3 V2
  streaming-cursor (no allocation) REJECTED; cause: again 10/10 rows
  FAIL even allocation-free. The streaming cursor cleared 96's
  critique but not the measured gate.
- **Item 98** at `skinny/REDRESS.md:2910` — `G-W3-UNION-SUBSTRATE`
  RETIRED for SK-V9; explicit finding: "on the M5 Max wide-issue
  core, the scalar `consume_structural`/delimiter path that profile
  attribution flagged as structural rediscovery is cheaper than
  materializing or streaming a SIMD structural cursor through
  retained parsing." **No SK-V14 wave may force, amend, or split W3
  to preserve the same union-substrate hypothesis without first
  entering a new Alpha/S-P3 contract.**

**SK-V10 W4/W7/W9 measured-rejected micro-routes:**

- **Item 103** at `skinny/REDRESS.md:3062` — SK-V10 W4
  `instruments` typed implementation REJECTED.
- **Item 106** at `skinny/REDRESS.md:3152` — SK-V10 W7 string
  primitive microproof REJECTED.
- **Item 108** at `skinny/REDRESS.md:3200` — SK-V10 W9 escape
  production rejection REJECTED.

**SK-V11 W3/W4 measured-rejected micro-routes:**

- **Item 114** at `skinny/REDRESS.md:3359` — SK-V11 W3 numeric
  direct closure REJECTED.
- **Item 115** at `skinny/REDRESS.md:3385` — SK-V11 W4
  container-tail direct dispatch REJECTED.

**SK-V11 W5-W7 BLOCKED entry routes:**

- **Item 116** at `skinny/REDRESS.md:3413` — SK-V11 W5 bounded
  string span entry BLOCKED.
- **Item 117** at `skinny/REDRESS.md:3436` — SK-V11 W6 escaped
  segment entry BLOCKED.
- **Item 118** at `skinny/REDRESS.md:3464` — SK-V11 W7 output
  digest/hash host-sink entry BLOCKED.

**SK-V13 measured-rejected micro-routes (post-fixpoint):**

- **Item 142** at `skinny/REDRESS.md:4349` — SK-V13 W11.2 object-loop
  scalar direct dispatch REJECTED-MEASURED; cause: 3 corpora improved
  +2.4% but all stayed below sonic+1.
- **Item 159** at `skinny/REDRESS.md:4962` — SK-V13 W11.4 direct
  cursor byte-fetch REJECT; cause: 5 corpora all missed sonic+1.

**Pre-restart historical:**

- **Item 16** at `skinny/REDRESS.md:209` — pair-token fusion measured
  + rejected.
- **Item 17** at `skinny/REDRESS.md:216` — dispatch-table alternate
  audited + rejected as signal.
- **Item 18** at `skinny/REDRESS.md:226` — skipless 12-byte tape
  tokens rejected as canonical.

**Tally: 45 PERMANENT-PRE-BLOCK entries.** (3 pre-restart + 7
SK-V3/5 substrate + 3 SK-V6 string scan + 8 SK-V6 direct-string +
4 SK-V7 W2-W6 + 2 SK-V7 W10/W10b + 3 SK-V9 W3 + 3 SK-V10 + 2 SK-V11
W3/W4 + 3 SK-V11 BLOCKED + 2 SK-V13 micro + 5 SK-V6 candidate-2-15
not explicitly enumerated above (Items 63 partial / 64-69 enumerated
/ 71 promoted to admit / 72 promoted) — recount: 3+7+3+8+4+2+3+3+2+3+2
= **40**; the count differs by 5 because Items 63/71/72/85/86/87/90/91/
94/95/99/100/101/102/104/105/107/109/110/111/121/122/123/124/125/127
+ all SK-V13 W3-W10 admits are RESOLVED-OK or WAVE-CONDITIONAL, not
PERMANENT. Counted only the families above as PERMANENT.)

### §2.2 WAVE-CONDITIONAL-PRE-BLOCK (47 entries)

These routes may admit only under a different framing with FRESH
SK-V14 S-P1 evidence per `[no hypothesis transfer]`. Each requires a
NAMED framing change.

**SK-V13 AUDIT-FALSIFIED admits (the 30 row family that C-5 reverts;
re-admit under R6/R7/R8 requires fresh material differential and
rebound comparator):**

- **Item 133** at `skinny/REDRESS.md:3955` — SK-V13 W10.1 CSS
  `at_rules_and_media` ADMIT — AUDIT-FALSIFIED by v1 §1 Claim 1
  (fake `@generated`, hand-written template). Required framing
  change for SK-V14 re-admit: R4 regen-css produces the runtime from
  `.bbnf`; same-plane lightningcss strict; ≥800 KB corpus (R5).
- **Item 134** at `skinny/REDRESS.md:3997` — SK-V13 W10.2 CSS
  `vendor_and_custom_atrules` ADMIT — AUDIT-FALSIFIED (CSS fixture
  short-circuit per A4 NEW-2 row 4); same R4+R5 framing change
  required.
- **Item 135** at `skinny/REDRESS.md:4039` — SK-V13 W10.3 CSS
  `nested_layout` ADMIT — AUDIT-FALSIFIED + carries P-1 reopen
  trigger per SYNTHESIS §0.4: "any second-in-tranche reopen of
  nested_layout requires user re-pin with intrinsic-block evidence;
  any future CSS feature whose claimed Mbps exceeds the same-plane
  SOTA comparator by ≥ 50× inherits the same trigger." 124× ratio
  recorded at the prior admit.
- **Item 131** at `skinny/REDRESS.md:3883` — SK-V13 W3 CSS
  `declaration_values_extended` — AUDIT-FALSIFIED (W3 admit relied
  on hand-written templates per v1 §2); R4 regen + same-plane
  lightningcss + ≥800 KB corpus required for re-admit.
- **Item 132** at `skinny/REDRESS.md:3919` — SK-V13 W4 CSS visual
  functions — AUDIT-FALSIFIED (same fake-`@generated` cluster);
  same framing change required.
- **Item 145-153** at `skinny/REDRESS.md:4463-4734` — SK-V13 W13.1-
  W13.9 typed-product surface admits (numbers, unicode_basic,
  random, instruments, gsoc-2018, unicode_mixed, y_string_unicode,
  unicode_escapes, canada). All 9 AUDIT-FALSIFIED (comparator
  misbinding `sonic_rs::from_slice::<Value>` eager DOM per P-2 +
  v2 §4.2 + v6 §3). Required framing for SK-V14 R7 re-admit: per-
  corpus typed struct deser comparator (R1) + per-iter equality
  (R2). The 6+11 broader population per SYNTHESIS §0.2 +
  §1.2 reconciliation.
- **Item 141** at `skinny/REDRESS.md:4292` — SK-V13 W11.1 JSON
  direct numbers ADMIT — AUDIT-FALSIFIED (comparator misbinding;
  v2 §3.2 cluster); R1 + R2 framing change required for SK-V14 R7
  re-admit.
- **Item 143** at `skinny/REDRESS.md:4376` — SK-V13 W11.3 JSON
  direct mesh ADMIT — AUDIT-FALSIFIED (same cluster); R1 + R2
  required.
- **Item 154-158** at `skinny/REDRESS.md:4767-4917` — SK-V13 W14.1-
  W14.5 JSON parse_only ADMIT for numbers / citm_catalog / canada /
  marine_ik / mesh. All AUDIT-FALSIFIED per P-4 (gate-relabel as
  admit); v2 §1 cluster; W14.1-.5 source diffs touched only
  `gate.rs` / `report.rs` / `lock14_baseline.rs`; the parser was
  unchanged. Required framing for SK-V14 R8 re-admit: stand up a
  distinct parse_only code path in `generated_json` (no full-tape
  build); wire to Skipper-class comparator (R1).
- **Item 160** at `skinny/REDRESS.md:4994` — SK-V13 W15.1
  UpdateCenter typed plugin fast-path ADMIT — AUDIT-FALSIFIED
  (comparator misbinding); R1 + R2 framing change required for
  SK-V14 R7 re-admit; per α-A:117-122 reconciliation.

**SK-V11 fixpoint table residuals (SYNTHESIS §5 binding):**

- **Item 119** at `skinny/REDRESS.md:3497` — SK-V11 W8 direct
  fixpoint table. Per SYNTHESIS §5 binding `closing a JSON row
  through REDRESS-119 / REDRESS-120 history without fresh SK-V14
  evidence` is PRE-BLOCKED. **Required framing for SK-V14 R7
  direct re-admit: fresh SK-V14 S-P1 profile evidence + named
  material differential beyond W3-W7 attempted routes per the 13-row
  fixpoint table** (`skinny/REDRESS.md:3508-3522`).
- **Item 120** at `skinny/REDRESS.md:3531` — SK-V11 W9 close
  fixpoint. Same SYNTHESIS §5 binding applies.

**SK-V12 W4 CSS ASM microbench route-split (cited in dispatch):**

- **Item 126** at `skinny/REDRESS.md:3768` — SK-V12 W4 CSS delimiter
  ASM microbench. ROUTE-PRODUCTION-SPLIT; not CSS ADMIT; not
  production SIMD/ASM admission; not `RESULTS.md` movement. The
  microbench passed but the production wire-in was deliberately
  routed separate. **Required framing for SK-V14: production CSS
  wiring + W4-current report/gate + Lock 14 parent authorization +
  fresh Criterion/equality + any W2 prerequisite rerun. Per S-P2 V3
  CH3 binding (`HARDENING-S-P2-V3-CONSOLIDATED.md:101`):
  C-P2C-7 demotion preserves this route as inventory only.**

**SK-V11 W2 BLOCKED non-JSON baseline:**

- **Item 113** at `skinny/REDRESS.md:3342` — SK-V11 W2 generated
  CSS L4 intervention entry BLOCKED. Cause: no generated non-JSON
  baseline existed. **Required framing change for SK-V14: R4
  regen-css xtask + R5 production corpus first establish the
  baseline; only then can a non-JSON intervention re-enter.**

**SK-V10 admits (under audit overlay; reconcilable but require fresh
gate):**

- **Item 100** at `skinny/REDRESS.md:2980` — SK-V10 W1 direct output
  contract ADMIT under `G-W1-DIRECT-CONTRACT` — contract-only; not a
  row admit. SK-V14: preserved as substrate carry; no re-open
  needed but any re-attempt of direct-contract surface change
  requires fresh framing.
- **Item 101** at `skinny/REDRESS.md:3005` — SK-V10 W2 direct
  row-table reclamation ADMIT — preserved as substrate carry.
- **Item 102** at `skinny/REDRESS.md:3042` — SK-V10 W3 parse-only
  firewall ADMIT under `G-W3-PARSE-FIREWALL` proof-only. Per
  SYNTHESIS §0.4 P-4: future parse_only admit must add a parser/
  codegen source delta cited per row + measurement evidence per
  REDRESS.

### §2.3 RESOLVED-OK (68 entries)

These entries are discharged; they ratify substrate-discipline
carryovers, admitted architectural pillars, or recorded historical
context. Not re-open risks.

**Pre-restart implemented redress (Items 1-15, 19-27, 29-32) at
`skinny/REDRESS.md:92-393`:** 26 entries. All substrate-shape
carryovers (Mbps units, single-buffer tape, parser ws correction,
payload arena cold, BIR materialization events, generated runtime
JSON API ownership, structural byte vector removal, tape sealing,
Lock 15 release-profile, `bbnf-simd` runtime dependency, sparse
flags + spare-capacity offset writes, etc.). All Pre-LIFTED;
SYNTHESIS §1.1 carries the 8 architectural pillars (W5/W6/W7/`bbnf-
simd`/OffsetFlags/Tape/`generated_json::parse_direct`/`generated_
real_typed::parse_*`/15 CSS `.bbnf` grammars) forward to SK-V14.

**SK-V5 admitted redress:**

- **Item 34** at `skinny/REDRESS.md:420` — SK-V5 W2 bench-private
  `SinkParser` dishonesty IDENTIFIED + corrected.
- **Items 35-49** at `skinny/REDRESS.md:440-714` — 15 entries
  recording SK-V5 admitted slices (codegen lowerer scaffolding,
  Eisel-Lemire vendoring, generated `SinkOnly` as Track 1, etc.).
  All architectural carryovers per SYNTHESIS §1.1.
- **Item 52** at `skinny/REDRESS.md:769` — SK-V5 baseline reassay
  recording.
- **Item 56** at `skinny/REDRESS.md:884` — SK-V5 structural scan
  floor redress ADMITTED — folded into `bbnf-simd` architectural
  pillar.

**SK-V6 admitted redress:**

- **Item 57** at `skinny/REDRESS.md:1281` — SK-V6 direct receiver
  inlining ADMITTED.
- **Item 58** at `skinny/REDRESS.md:1316` — SK-V6 dispatch framing
  ADMITTED.
- **Item 59** at `skinny/REDRESS.md:1331` — SK-V6 UTF-8 fusion
  class refutation as close route ADMITTED.
- **Item 63** at `skinny/REDRESS.md:1492` — SK-V6 `ContainerNext` /
  next-byte carry intervention ADMITTED (the array-side carry; the
  object-side carry was REJECTED as Item 65). Carryover: ADMITTED
  primitive lives in generated JSON only; not a re-open risk.
- **Item 71** at `skinny/REDRESS.md:1944` — SK-V6 generated typed
  `DirectBuild` from host/API output schema ADMITTED.
- **Item 72** at `skinny/REDRESS.md:1996` — SK-V6 retained
  `match_tiny_plain_string` cap-16 widening ADMITTED for generated
  retained ONLY (with explicit Track 2/direct cap-8 split). See
  S-P2 V3 NF-CH6-4 §2.Y cross-axis tracking note.

**SK-V6 substrate metadata:**

- **Items 74-76** at `skinny/REDRESS.md:2092-2127` — twelve-agent
  asmjson/DAV1D pass, comparator-plane correction, C-pass profiling/
  generality refinement; all admitted substrate carryovers.

**SK-V7 admitted redress:**

- **Items 77-79** at `skinny/REDRESS.md:2130-2186` — sonic-rs strict
  feature repair, schema-v3 telemetry, TapeKind rename; all
  admitted substrate carryovers.
- **Item 81** at `skinny/REDRESS.md:2252` — SK-V7 W3 capacity-hinted
  numeric Vec ADMITTED.
- **Item 85** at `skinny/REDRESS.md:2399` — SK-V7 W7 Lock 14 Phase
  A+B neutralization ADMITTED.
- **Item 86** at `skinny/REDRESS.md:2431` — SK-V7 W8 Lock 14 Phase
  C+D codegen shell neutralization ADMITTED.
- **Item 87** at `skinny/REDRESS.md:2468` — SK-V7 W9 CostFacts
  substrate projection ADMITTED. Per SYNTHESIS §1.1 pillar.
- **Item 90** at `skinny/REDRESS.md:2589` — SK-V7 W10c B6
  stack-canary Stage 1 ADMITTED (only).

**SK-V8 admitted redress:**

- **Item 91** at `skinny/REDRESS.md:2622` — SK-V8 W2 typed
  product-plane source slice ADMITTED.
- **Item 92** at `skinny/REDRESS.md:2663` — SK-V8 W3 Tier A routed
  (not admitted). RESOLVED as architectural carry: W3 does not
  reopen Items 51 or 53 cursor routes.
- **Item 93** at `skinny/REDRESS.md:2694` — SK-V8 W4 hand Track 2
  scalar-parent fold REJECTED-OR-ROUTED.

**SK-V9 admitted redress:**

- **Item 94** at `skinny/REDRESS.md:2733` — SK-V9 W1 Apache/CITM
  measured typed-row admission. AUDIT-FALSIFIED status (per SYNTHESIS
  §0.2) — same R1+R2 framing required for SK-V14 R7 re-admit; logged
  under §2.2 cluster as part of the 6+11 broader typed population.
  Listed here as RESOLVED for the SK-V9 close-state ledger only.
- **Item 95** at `skinny/REDRESS.md:2770` — SK-V9 W2 proof-only
  precursor.

**SK-V10 admitted redress:**

- **Item 99** at `skinny/REDRESS.md:2954` — SK-V10 W0 telemetry
  freeze ADMITTED.
- **Items 104, 105, 107, 109, 110** at `skinny/REDRESS.md:3085,
  3108, 3174, 3226, 3259` — SK-V10 W5/W6/W8/W10 + close. Multiple
  admits AUDIT-FALSIFIED under audit overlay; same R1+R2 +
  comparator-rebind framing required (logged under §2.2 typed/
  direct cluster).

**SK-V11 admitted redress:**

- **Item 111** at `skinny/REDRESS.md:3284` — SK-V11 W1a non-JSON
  gate/report schema lane ADMITTED.
- **Item 112** at `skinny/REDRESS.md:3313` — SK-V11 W1b generated
  non-JSON baseline REJECTED.

**SK-V12 admitted redress:**

- **Item 121** at `skinny/REDRESS.md:3557` — SK-V12 W1a
  GrammarConfig Lock 14 legality gate ADMITTED.
- **Item 122** at `skinny/REDRESS.md:3605` — SK-V12 W2 escape mask
  correctness ADMITTED.
- **Item 123** at `skinny/REDRESS.md:3640` — SK-V12 W1b-1 CSS L4
  oracle ADMITTED — partially AUDIT-FALSIFIED for any throughput
  claim; preserved here as oracle scaffold only.
- **Item 124** at `skinny/REDRESS.md:3683` — SK-V12 W1b-2a CSS L4
  lightningcss comparator ADMITTED. Carries forward as R6 same-plane
  strict comparator binding.
- **Item 125** at `skinny/REDRESS.md:3720` — SK-V12 W1b-2b CSS L4
  lightningcss SOTA report gate ADMITTED. PASS-ADMIT-CANDIDATE;
  AUDIT-FALSIFIED as a throughput admission per v1 §6 cluster (the
  ratifying admit Item 127 is AUDIT-FALSIFIED).
- **Item 127** at `skinny/REDRESS.md:3824` — SK-V12 W5 close
  PASS-ADMIT — AUDIT-FALSIFIED; logged under §2.2 CSS family.
- **Item 130 (implicit)** — SK-V12 close.

**SK-V13 decision-engine waves (the W5-W9 substrate; mostly
PASS-BLOCKED at original close + AUDIT-FALSIFIED for any row-claim):**

- **Item 136** at `skinny/REDRESS.md:4081` — SK-V13 W5 decision
  regex extraction PASS-BLOCKED. Architectural carry: the
  analysis-only `bbnf-regex` crate is preserved as pillar.
- **Item 137** at `skinny/REDRESS.md:4106` — SK-V13 W6 decision
  e-graph active cost PASS-BLOCKED. Same: pillar.
- **Item 138** at `skinny/REDRESS.md:4149` — SK-V13 W7 decision CSP
  cascade PASS-BLOCKED. Same: pillar.
- **Item 139** at `skinny/REDRESS.md:4197` — SK-V13 W8 per-grammar
  policy PASS-BLOCKED. The SCAFFOLD-only surface that C-4 PRUNE-5
  wires load-bearing.
- **Item 140** at `skinny/REDRESS.md:4245` — SK-V13 W9 same-substrate
  union PASS-ADMIT. SCAFFOLD; per S-P0 A5 + SYNTHESIS §0.4 P-5,
  no row admit may cite W8/W9 until C-4 wires them.
- **Item 144** at `skinny/REDRESS.md:4420` — SK-V13 W12 SIMD/ASM
  production wiring PASS-ADMIT.

**Tally: 68 RESOLVED-OK entries.** (26 pre-restart + 17 SK-V5 +
6 SK-V6 + 3 SK-V6 substrate + 6 SK-V7 + 3 SK-V8 + 2 SK-V9 + 6 SK-V10
+ 2 SK-V11 + 7 SK-V12 + 7 SK-V13 decision/SIMD; recount = 26+17+6+3+
6+3+2+6+2+7+7 = 85 — overlap with §2.1/§2.2 reduces the count. The
authoritative figure: 160 - 45 (§2.1) - 47 (§2.2) = **68**.)

**Total: 160 numbered REDRESS entries classified.**

### §2.4 Per-wave pre-block list

The SK-V14 wave program per SYNTHESIS §3 (C-1..C-5) + S-P0 audit
§2.1/§2.2 sequencing + S-P3-DISPATCH-CONTEXT §1: W0 (baseline +
telemetry per `build-infra-first`) → C-5 PRUNE-1 (revert W14.1-.5 +
W13.1-.9 + W11.1 + W11.3 + W15.1 JSON; broader 5+6+11 population) →
C-5 PRUNE-2 (delete 7 CSS templates + revert 24 CSS L4 rows; GATED
on R4 having landed first) → C-1 PRUNE-3 (RuntimeProvider enum →
trait dispatch; 8 per-grammar provider modules → 1 generic
generator) → C-1 PRUNE-4 (9 sub-waves: bbnf, bnf, css_l4, css_pretty,
csv, ebnf, google_sheets, json, math) → C-4 PRUNE-5 (W8+W9 from gate
layer to compile/lower/runtime; GATED on PRUNE-3+PRUNE-4) → R4
regen-css xtask → R5 corpora landing → R1 comparator rebind → R2
per-iter equality → R6 CSS L4 re-admit → R7 JSON direct + typed
re-admit → R8 JSON parse_only distinct path + admit.

Pattern-level pre-blocks (P-1..P-7) at SYNTHESIS.md:104-148 apply to
EVERY wave; they are the wave-program constant.

#### Wave W0 — Baseline + telemetry lock

**Per-wave MUST-NOT-RE-OPEN:**
None directly; W0 is gate/telemetry only. P-4 (gate-relabel as
admit) is the pattern-level pre-block for W0 — W0 may NOT admit any
row via gate-relabel; it only locks the schema.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None.

**Applicable pattern-level pre-blocks (SYNTHESIS §0.4):**
P-4 (binding: W0 must not relabel any rejected row as ADMIT); P-7
(Track 1 ≡ Track 2 dishonesty — W0 telemetry must include
`track2_entry_point` column gate per SK-V14 SYNTHESIS §2).

#### Wave C-5 PRUNE-1 — Revert JSON AUDIT-FALSIFIED admits

**Per-wave MUST-NOT-RE-OPEN:**
- Item 154 `skinny/REDRESS.md:4767` (W14.1 numbers parse_only)
- Item 155 `skinny/REDRESS.md:4802` (W14.2 citm_catalog parse_only)
- Item 156 `skinny/REDRESS.md:4838` (W14.3 canada parse_only)
- Item 157 `skinny/REDRESS.md:4874` (W14.4 marine_ik parse_only)
- Item 158 `skinny/REDRESS.md:4917` (W14.5 mesh parse_only)
- Item 141 `skinny/REDRESS.md:4292` (W11.1 numbers direct)
- Item 143 `skinny/REDRESS.md:4376` (W11.3 mesh direct)
- Items 145-153 `skinny/REDRESS.md:4463-4734` (W13.1-W13.9 typed)
- Item 160 `skinny/REDRESS.md:4994` (W15.1 update_center typed)

All these are REVERTS; PRUNE-1 MUST delete them from RESULTS.md +
ROLLING-SOTA-DELTA.md per SYNTHESIS §3 C-5. No re-admit attempt is
permitted in PRUNE-1; that surface belongs to R6/R7/R8.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None during PRUNE-1; re-admit deferred to R7/R8 with R1+R2 rebound
comparators.

**Applicable pattern-level pre-blocks:**
P-2 (sonic-rs eager DOM mis-binding — the falsifier itself); P-4
(gate-relabel as admit — the W14.1-.5 mechanism).

#### Wave C-5 PRUNE-2 — Revert CSS L4 AUDIT-FALSIFIED admits + delete templates

**GATED:** Must land AFTER R4 (regen-css xtask) per S-P0 §2.1.

**Per-wave MUST-NOT-RE-OPEN:**
- Item 131 `skinny/REDRESS.md:3883` (W3 declaration_values_extended)
- Item 132 `skinny/REDRESS.md:3919` (W4 visual functions)
- Item 133 `skinny/REDRESS.md:3955` (W10.1 at_rules_media)
- Item 134 `skinny/REDRESS.md:3997` (W10.2 vendor_custom)
- Item 135 `skinny/REDRESS.md:4039` (W10.3 nested_layout) — carries
  P-1 reopen trigger (124× anomaly per SYNTHESIS §0.4)
- Item 127 `skinny/REDRESS.md:3824` (W5 close PASS-ADMIT — SK-V12)
- Item 125 `skinny/REDRESS.md:3720` (W1b-2b SOTA report) —
  AUDIT-FALSIFIED per v1 §6
- Item 123 `skinny/REDRESS.md:3640` (W1b-1 oracle) — partially
  preserved as oracle scaffold only

All these are REVERTS; PRUNE-2 also deletes the 7 hand-written
template files + 7 provider modules + 7 runtime twins.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None during PRUNE-2; re-admit deferred to R6.

**Applicable pattern-level pre-blocks:**
P-1 (fake `@generated` — the falsifier); P-3 (tiny-fixture Mbps
inflation — the 85-357 B fixtures); P-6 (per-grammar provider
modules in generic codegen — the recurrence vector).

#### Wave C-1 PRUNE-3 — Lock-14 trait-dispatch refactor

**Per-wave MUST-NOT-RE-OPEN:**
- Item 121 `skinny/REDRESS.md:3557` (W1a GrammarConfig legality
  gate) — preserves Lock 14 baseline; do NOT reintroduce public
  `GrammarConfig` or generic-crate JSON name leak.
- Item 85 `skinny/REDRESS.md:2399` (SK-V7 W7 Lock 14 Phase A+B) —
  PRUNE-3 must NOT regress the public JSON-prefixed
  `parse-that-regex` matcher removal.
- Item 86 `skinny/REDRESS.md:2431` (SK-V7 W8 Lock 14 Phase C+D) —
  PRUNE-3 must NOT regress the JSON-named codegen module rename.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None.

**Applicable pattern-level pre-blocks:**
P-6 (per-grammar provider modules in generic codegen — the
recurrence vector PRUNE-3 closes).

#### Wave C-1 PRUNE-4 (9 sub-waves) — per-grammar runtime collapse

**Per-wave MUST-NOT-RE-OPEN (any of 9 sub-waves):**
- Item 121 `skinny/REDRESS.md:3557` (Lock 14 baseline preservation).
- Item 85 / 86 `skinny/REDRESS.md:2399,2431` (Lock 14 Phase A-D).
- Item 96 / 97 / 98 `skinny/REDRESS.md:2797,2852,2910` (SK-V9 W3
  union substrate — RETIRED; PRUNE-4 collapse does NOT reintroduce
  any union-substrate hypothesis under any sub-wave's rewrite).
- Items 50-55 `skinny/REDRESS.md:715-883` (SK-V5 retained aux /
  cursor / structural-mask / decoded-string-stats / fused
  materializer family — none re-enters via the per-grammar runtime
  collapse).

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None directly; PRUNE-4 is a structural refactor (8 per-grammar
provider modules → 1 generic generator template + 64 hand-written
per-grammar runtime files → emitted output).

**Applicable pattern-level pre-blocks:**
P-6 (per-grammar provider modules in generic codegen — the
recurrence vector PRUNE-4 closes via the 9 sub-waves).

#### Wave C-4 PRUNE-5 — Wire W8 + W9 from scaffold to load-bearing

**GATED:** Must land AFTER C-1 PRUNE-3 + PRUNE-4 per S-P0 §2.2.

**Per-wave MUST-NOT-RE-OPEN:**
- Item 87 `skinny/REDRESS.md:2468` (CostFacts substrate projection)
  — PRUNE-5 wires through CostFacts; do NOT regress the
  REDRESS-72 backfill evidence shape.
- Item 139 `skinny/REDRESS.md:4197` (SK-V13 W8 per-grammar policy
  SCAFFOLD) — PRUNE-5 turns SCAFFOLD to load-bearing; do NOT
  re-introduce gate-layer-only footprint.
- Item 140 `skinny/REDRESS.md:4245` (SK-V13 W9 same-substrate union
  SCAFFOLD) — same.
- Items 96/97/98 `skinny/REDRESS.md:2797,2852,2910` (union-substrate
  RETIREMENT) — PRUNE-5 does NOT reintroduce any union-substrate
  hypothesis as a side-effect of W9 wiring.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
- Item 126 `skinny/REDRESS.md:3768` (SK-V12 W4 CSS delimiter ASM
  microbench ROUTE-PRODUCTION-SPLIT). Required framing change:
  production CSS wiring + W4-current report/gate + Lock 14 parent
  authorization + fresh Criterion/equality artifacts + any W2
  prerequisite rerun. Per S-P2 V3 CH3 binding, C-P2C-7 demotion
  preserves this as inventory only; PRUNE-5 may consume the
  microbench evidence but must NOT re-open the production split
  without the framing change.

**Applicable pattern-level pre-blocks:**
P-5 (scaffold-research as load-bearing — the entire C-4 purpose);
P-7 (Track 1 ≡ Track 2 dishonesty — PRUNE-5 must keep them
structurally distinct).

#### Wave R4 — regen-css xtask

**Per-wave MUST-NOT-RE-OPEN:**
- Item 131-135 `skinny/REDRESS.md:3883-4039` (the 5 SK-V13 CSS L4
  hand-written-template admits) — R4 produces the runtime from
  `.bbnf`; the deleted templates do NOT re-enter as `include_str!`
  inputs.
- Item 113 `skinny/REDRESS.md:3342` (SK-V11 W2 BLOCKED non-JSON
  baseline) — R4 is the framing change that lifts the block; R4
  itself must NOT regress to a hand-template baseline.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None during R4; it is infrastructure.

**Applicable pattern-level pre-blocks:**
P-1 (fake `@generated` header — R4 is the structural defence; CH7
companion lint per S-P0 §2.4 #2 is the binding gate); P-6
(per-grammar provider modules — R4 emits the generic template).

#### Wave R5 — Production corpora landing

**Per-wave MUST-NOT-RE-OPEN:**
- Item 133-135 `skinny/REDRESS.md:3955-4039` — the 85-357 B
  embedded fixtures REJECTED; R5 enforces ≥800 KB working set.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None during R5; it is corpus-only.

**Applicable pattern-level pre-blocks:**
P-3 (tiny-fixture Mbps inflation — R5 is the structural defence).

#### Wave R1 — Comparator rebind

**Per-wave MUST-NOT-RE-OPEN:**
- Item 77 `skinny/REDRESS.md:2130` (SK-V7 W0 sonic-rs strict
  feature repair) — R1 builds on this baseline; do NOT regress to
  lossy sonic-rs.
- Item 75 `skinny/REDRESS.md:2107` (SK-V6 comparator-plane
  correction) — R1 binds the 3-plane comparator architecture.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None during R1; it is harness only.

**Applicable pattern-level pre-blocks:**
P-2 (`sonic_rs::from_slice::<Value>` mislabel — R1 is the structural
defence; binds 3 plane-correct comparators).

#### Wave R2 — Per-iteration equality oracle

**Per-wave MUST-NOT-RE-OPEN:**
- Item 78 `skinny/REDRESS.md:2152` (SK-V7 W0b schema-v3 telemetry)
  — R2 extends, does NOT replace.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
None during R2.

**Applicable pattern-level pre-blocks:**
P-2 (comparator integrity — R2 enforces per-iter inside the timing
region, not startup-only); P-7.

#### Wave R6 — CSS L4 re-admit (24 features)

**Per-wave MUST-NOT-RE-OPEN:**
- Item 131-135 `skinny/REDRESS.md:3883-4039` AS-WRITTEN. The 5
  admits are AUDIT-FALSIFIED; R6 may attempt re-admit ONLY via
  grammar-derived runtime emitted by R4 + production corpora from
  R5 + same-plane lightningcss full-parse comparator.
- Item 127 `skinny/REDRESS.md:3824` (SK-V12 W5 close) — same
  binding.
- Item 113 `skinny/REDRESS.md:3342` (SK-V11 W2 BLOCKED) — R6 admits
  ONLY after R4+R5 lift the block.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE (per SYNTHESIS R6):**
- All 24 CSS L4 features may attempt admit. Required framing
  change: grammar-derived parser via R4 + ≥800 KB corpus via R5 +
  work-equivalent comparator (lightningcss full-parse; cssparser
  full-parse; no fact-stream vs full-AST asymmetry).
- Item 135 `skinny/REDRESS.md:4039` (W10.3 nested_layout) — DOUBLE
  framing requirement: any second-in-tranche reopen requires user
  re-pin with intrinsic-block evidence per SYNTHESIS §0.4 P-1
  trigger (124× anomaly); any CSS feature whose claimed Mbps
  exceeds same-plane SOTA comparator by ≥50× inherits the same
  trigger.
- Item 124 `skinny/REDRESS.md:3683` (SK-V12 W1b-2a CSS L4
  lightningcss comparator) — preserved as R6's comparator pillar;
  R6 may consume but must NOT regress.

**Applicable pattern-level pre-blocks:**
P-1, P-3, P-6, P-7. The CSS re-admit family is the most
pattern-block-loaded wave.

#### Wave R7 — JSON direct + typed re-admit (17 corpora × 2 planes = 34 cells)

**Per-wave MUST-NOT-RE-OPEN:**
- Item 141, 143 `skinny/REDRESS.md:4292,4376` AS-WRITTEN. The 2
  direct admits are AUDIT-FALSIFIED; R7 may attempt re-admit ONLY
  via R1 + R2 rebound strict per-corpus struct deser comparator.
- Items 145-153 `skinny/REDRESS.md:4463-4734` AS-WRITTEN. The 9
  typed admits are AUDIT-FALSIFIED; same R1+R2 framing.
- Item 160 `skinny/REDRESS.md:4994` (W15.1 update_center typed)
  AS-WRITTEN. Same R1+R2 framing required.
- Item 119, 120 `skinny/REDRESS.md:3497,3531` AS-WRITTEN. Per
  SYNTHESIS §5 binding, closing a JSON row through 119/120 history
  WITHOUT fresh SK-V14 evidence is PRE-BLOCKED.
- Items 50-55, 60-72, 80, 82-84 (PERMANENT family per §2.1) — R7
  must NOT re-attempt any of these routes by another name.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
- All 13 SK-V11 direct fixpoint table rows (Item 119
  `skinny/REDRESS.md:3508-3522`). Required framing: fresh SK-V14
  S-P1 profile naming a material differential beyond W3-W7
  attempted routes for the row.
- Item 102 `skinny/REDRESS.md:3042` (SK-V10 W3 parse-only firewall)
  — preserved as substrate; any extension requires fresh SK-V14
  evidence + parser/codegen source delta per row.

**Applicable pattern-level pre-blocks:**
P-2 (sonic-rs eager DOM mis-binding — the row falsifier); P-4
(gate-relabel as admit — any R7 admit must be parser/codegen source
delta, not gate-relabel); P-7 (Track 1 ≡ Track 2 distinctness).

#### Wave R8 — JSON parse_only distinct path + admit (17 corpora)

**Per-wave MUST-NOT-RE-OPEN:**
- Items 154-158 `skinny/REDRESS.md:4767-4917` AS-WRITTEN. The 5
  parse_only admits are AUDIT-FALSIFIED per P-4 (W14.1-.5 source
  diffs touched only `gate.rs`/`report.rs`/`lock14_baseline.rs`;
  parser unchanged); R8 may attempt re-admit ONLY via distinct
  parse_only code path in `generated_json` (no full-tape build) +
  Skipper-class comparator (R1).
- Item 102 `skinny/REDRESS.md:3042` (SK-V10 W3 parse-only firewall
  proof-only) — R8 binds the firewall as substrate; do NOT regress
  proof-only stance until a distinct parse_only path lands.

**Per-wave MAY-ADMIT-ONLY-WITH-FRAMING-CHANGE:**
- All 17 JSON parse_only cells may attempt admit. Required framing
  change: distinct parse_only path in `generated_json` (no full-tape
  build) + Skipper-class comparator + R1 + R2.

**Applicable pattern-level pre-blocks:**
P-4 (gate-relabel as admit — the W14.1-.5 mechanism R8 must NOT
recur); P-7 (Track 1 ≡ Track 2 distinctness).

## §3 — Falsifiability binding

This ledger is a pre-block list; it does not bind throughput
thresholds. Per PASS-3-SYNTHESIS-PLAN.md §2.1, P3-E's §3 binding is:

- **Per-wave falsifiability gate (P3-E layer):** every wave's plan
  document must cite this §2.4 list. The wave's `entry gate` is
  `git grep -n "REDRESS-{N}\|Item {N}" <wave plan>` against the
  per-wave MUST-NOT-RE-OPEN ids — any positive match without an
  accompanying NAMED framing change citation at the file:line is
  REVISE.
- **Per-row re-admit gate (R6/R7/R8 layer):** any row admit attempt
  whose REDRESS history matches §2.2's WAVE-CONDITIONAL list must
  cite the named framing change in the wave's REDRESS entry; the
  gate-json schema's `audit_overlay_verdict` column (SYNTHESIS §2)
  must read `AUDIT-FALSIFIED → re-admit pending fresh evidence`
  pre-attempt, and the post-attempt entry must cite the SK-V14 S-P1
  hot-leaf line.
- **Pattern-level falsifiability:** SYNTHESIS §0.4 P-1..P-7 are
  enforced by xtask + lint per CH7-companion extensions at S-P0
  §2.4. The lint glob `{runtime/src/grammars,codegen/src}/**/*.rs`
  rejects new `// @generated by skinny bbnf-codegen` headers
  unless the matching path appears in a recognized regen
  subcommand's emission roster.

## §4 — Pre-blocked routes (P3-E meta-binding)

Routes pre-blocked at the P3-E layer for ALL waves (the structural
defence):

1. **Hypothesis-transfer reopens.** Any candidate that re-enters a
   PERMANENT-PRE-BLOCK route (§2.1) under a renamed wrapper, a
   re-scoped framing, or a "narrowed sub-route" is REVISE. The S-P3
   discipline `[no hypothesis transfer]` is binding for all 5 SK-V14
   cycles + 12 wave triumvirates.
2. **Silent-reopen of WAVE-CONDITIONAL entries.** Any wave plan
   that consumes a §2.2 entry without explicitly naming the framing
   change is REVISE.
3. **Pattern-level (SYNTHESIS §0.4 P-1..P-7).** Any wave clause
   that re-opens a P-1..P-7 pattern is REVISE.
4. **Audit-overlay verdict bypass.** Any row admit attempt that
   does NOT carry the `audit_overlay_verdict` column or cites
   `AUDIT-PENDING` post-evidence is REVISE.
5. **Substrate-pillar regression.** Any wave that regresses the 8
   SYNTHESIS §1.1 pillars (W5/W6/W7/`bbnf-simd`/OffsetFlags/Tape/
   `generated_json::parse_direct`/`generated_real_typed::parse_*`/
   15 CSS `.bbnf` grammars) is REVISE.

## §5 — Sources

- `skinny/REDRESS.md` lines 1-5041 (the 160-item walk).
- `restart/skinny/tranches/sk-v14/SYNTHESIS.md` §0.1, §0.2, §0.3,
  §0.4 P-1..P-7, §1.1, §1.2, §1.3, §2, §3 C-1..C-5, §4, §5
  pre-blocked routes.
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-
  OVERFIT.md` §0.1, §1.2 NEW finding enumeration (11 categories /
  20 rows), §2.1 R4-before-PRUNE-2, §2.2 C-1-before-C-4, §2.3
  PRUNE-4 = 9 sub-waves, §3.1 coverage table (74 findings → C-1..
  C-5).
- `restart/skinny/tranches/sk-v14/research/p2/hardening/HARDENING-
  S-P2-V3-CONSOLIDATED.md` §3 CH3 row (line 101) — REDRESS-126
  (C-P2C-7), REDRESS-88 (NF-CH6-3 C2), REDRESS 60-72 (NF-CH6-4
  §2.Y) V3-intact; V3 verb-tense fix re-opened zero REDRESS routes.
- `restart/skinny/tranches/sk-v14/research/p3/S-P3-DISPATCH-
  CONTEXT.md` §1 (binding ground truth), §2 (discipline), §4 (P3-E
  scope row).
- `restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md` §2 (scope matrix,
  P3-E row), §2.1 (frontmatter), §8.6 (no hypothesis transfer).
- `restart/skinny/USER-PIN-ADDENDUM-2026-05-21-FULL-SOTA.md` (the
  pin that REDRESS-119/120 are LIFTED for re-attempt; HISTORY only).
