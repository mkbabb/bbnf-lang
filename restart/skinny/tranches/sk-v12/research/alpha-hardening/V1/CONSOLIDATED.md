# SK-V12 Pass Alpha Hardening V1 - Consolidated

Date: 2026-05-20.

Disposition: REVISE.

## Lens Results

| Lens | Disposition | Blocking finding |
|---|---|---|
| CH1 correctness | REVISE | Threshold provenance and local citations are insufficient. |
| CH2 generality / Lock 14 | REVISE | Sheets and BBNF-self fallbacks lack matching intervention paths; selected-grammar baseline needs an executable pre-gate. |
| CH3 regression / REDRESS | ACCEPT | No REDRESS regression; W3, parse-only, and JSON direct fixpoint pre-blocks are preserved. |
| CH4 cost / scope | REVISE | Alpha-level hard caps, split triggers, baseline preflight proof, and E3 risk class need correction. |
| CH5 hidden coupling | ACCEPT | No hidden substrate, Track 1 / Track 2 coupling, plane relabel, or JSON-policy leak found. |
| CH6 next-tranche impact | REVISE | Candidate-local revert protocols, minute caps, and a G-Alpha summary are missing. |

## Required V2 Folds

1. Replace unsupported E1-E3 `>= 1 Mbps` and `sample count >= 100`
   thresholds with finite positive throughput plus recorded sample count, or
   cite an owning gate policy.
2. Add local citations and formula provenance for result-surface summaries,
   direct residual tables, and guard-floor tables.
3. Add a selected-grammar executable baseline pre-gate: generated emission or
   generated per-grammar runtime path, runtime module build, same-plane oracle,
   strict equality smoke, and non-JSON gate consumption.
4. Generalize the intervention candidate so CSS, Sheets, or BBNF-self baseline
   selection all have a concrete second-stage intervention path.
5. Add an Alpha cost/cap matrix: LOC budget, risk, wave slot, plan cap,
   redress cap, same-wave consumer, split-before-dispatch rule, and expected
   row effect.
6. Raise E3 risk to medium-high unless S-P1 later proves a materially smaller
   runnable path.
7. Add candidate-local revert protocols for E1-E5, including rejected-patch
   locations and dependent-wave blocking rules.
8. Add a G-Alpha-ready summary with target rows, interventions, caps,
   pre-blocked routes, predicted close state, and failure dispositions.

V1 does not converge. V2 must patch the Alpha packet and rerun six-lens
hardening before G-Alpha presentation.
