# BA.W6 — BA Close

**Thesis** (the seven-wave foundation closes; the lock cross-reference table is verified; the per-grammar generated-LOC table is emitted; the carry ledger to BB.W0 is named explicitly; PROGRESS.md and FINAL.md land). **Closer-gate** (`cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis` is 100% pass for BA-owned surfaces; `crates/ir/src/passes/tests/substrate_audit.rs` green; per `audit/HARDENING-SYNTHESIS-2026-05-03.md:204` the BA-amendment punch-list closes).

## §1 — Deliverable

Hereupon BA close — the foundation tranche having delivered, in seven waves, the layered re-org of `crates/core/src/`, the grammar-leak excision from `bbnf-ir`, the 23 god-module splits and layout-lowering rename, the path-crate triplet consolidation, the cursor + byte-skip unification, and the JSON direct-to-struct codegen first cut. W6 is the close ceremony: PROGRESS.md and FINAL.md document landings; the lock cross-reference table at BA.md §13-Lock honoured (lines 76-90) is verified cell-by-cell; the per-grammar generated-LOC table (BA.md §Generated-LOC budget table at lines 110-121) is emitted with post-BA actuals; the carry ledger to BB.W0 (BA.md §Carry-tags TO BB at lines 57-64) is named explicitly per BA→BB.C1 through BA→BB.C5.

The substrate audit green-gate verifies. Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43` ("Lane 05 confirms substrate-audit still fails with 32 zero-caller substrates") and the routed-carry table at line 226 ("32 zero-caller substrates | BA.W0 | `substrate_audit` green; each row deleted or whitelisted with same-wave/later-wave consumer and rationale"), the BA close requires `crates/ir/src/passes/tests/substrate_audit.rs` to be green. The path to green: BA.W0 deleted `generate/serialize/` (~156 LOC zero-caller), BA.W3 deleted the legacy `runtime/path.rs` (163 LOC zero-caller after migration) and the four `runtime/<g>/parse_with.rs` files (~480 LOC), BA.W5 deleted `OpenFrame` from JSON. The 32-substrate count at BA.W6 close should be zero (or each row whitelisted with rationale).

The performance gates verify. BA-G1 (twitter ≤ 400 µs) closed at BA.W5.M4; BA-G2 (≤ 2 heap allocs) closed at BA.W5.M5; BA-G3 (xtask regen ≤ 30 s, CSS L4 compile_paths_request ≤ 25 s — Lane 04 baseline 59.98 s + 52.53 s; halving) verified by post-BA timing at W6.M2; BA-G9 (Document::get<T> ≤ 5×) closed at BA.W4.M2. The BA close re-runs each gate's measurement and confirms the close-condition. Per `feedback_bench_single_run`, the bench is single-invocation; per `feedback_no_warm_benches`, cold per-parse only.

The lock cross-reference table cell-by-cell verification. Per BA.md §13-Lock honoured table (lines 76-90), each lock has a wave that addresses it: L1 (W0+W5), L2 (W2), L3 (W4), L4 (deferred to BB.W3 — ratifiable), L5 (W5 demonstrates), L6 (W0), L7 (W3), L8 (G1+G9 closed), L9 (W5 slice-borrow), L10 (deferred to BB.W3 — ratifiable), L11 (deferred to BB.W0 — ratifiable), L12 (Pre-BA ceremony), L13 (W0+W2+W3). Each cell's "Notes" column flags weak adherence or carry-deferral; W6 verifies no cell carries a fault. Per `feedback_no_deferrals`, deferred locks must point to a real receiving wave with a gate; the BB.W3 / BB.W0 deferrals are ratifiable per the BA→BB carry-tag table.

The carry ledger to BB is named explicitly per surgery #24. Per BA.md §Carry-tags TO BB, the BB receivers are sub-waved per-grammar (option (b) per `docs/tranches/BA/audit/W5-substrate-identity-decision.md`):

- BA→BB.C1a (owner BA.W5; receiver BB.W1a): Direct-to-struct extends to CSS L4; closer-gate `rg -n 'enum OpenFrame' crates/core/src/runtime/css_l4/` returns 0.
- BA→BB.C1b (owner BA.W5; receiver BB.W1b): Direct-to-struct extends to BBNF; same gate.
- BA→BB.C1c (owner BA.W5; receiver BB.W1c): Direct-to-struct extends to Sheets; same gate.
- BA→BB.C1d (owner BA.W5; receiver BB.W2): Direct-to-struct extends to five-grammar cohort.
- BA→BB.C2 (owner BA.W2): Layout-lowering rename canonised; BB references `Layout`/`LayoutSink` only.
- BA→BB.C3 (owner BA.W4b): Cursor-unified `parse_with` + `__EAGER_EMPTY_PATH` substrate; BB.W2 extends to all grammars.
- BA→BB.C4 (owner BA.W3b): `path-core` crate exists; BB.W5's `pointer!` macro reuses without proc-macro/cdylib mirror.
- BA→BB.C5 (owner BA.W1): Grammar-agnostic `bbnf-ir`; BB.W3's CSP/e-graph/miner extensions reference grammars only via `&str` ident through workspace metadata.
- BA→BB.C6/C7/C8 (deferred locks 4/10/11; receivers BB.W3/BB.W3/BB.W0).

Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228` (the routed-carry table), W6 also closes routed carries: the F4 Tailwind regex_scan timeout (W2.M6 disposition: profile under direct-projection — if not closed, route to BB.W3 per `audit/HARDENING-SYNTHESIS-2026-05-03.md:84-87`); the 32 zero-caller substrates (BA.W0+W3+W5 deletions); generated-size budgets (BA.W2 + BA.W5 artefacts).

The Era V failure mode is closed. Per `docs/tranches/meta-audit/archaeology/era-V-dta-psi-rut.md:7-10`, "Era V's signature failure mode is **substrate-first-consumer-later**: every tranche ships the compile-time emission of constants, tables, and shape dictionaries; no tranche fully activates the runtime consumer that reads them." BA does not repeat: every wave (W0 layered re-org → W1 grammar-leak → W2 splits → W3 path triplet → W4 cursor unification → W5 JSON direct-to-struct) lands a substrate WITH its consumer in the SAME wave. W6 is the verification — the BA close confirms each wave's substrate has been consumed.

The PROGRESS.md and FINAL.md voice is archaic-permissive per `feedback_archaic_diction`. Hereupon BA closes; the foundation tranche having delivered, in seven waves, the layered re-org, the grammar-leak excision, the god-module splits, the path-crate consolidation, the cursor unification, and the JSON direct-to-struct codegen first cut. The substrate is the typed-enum + slice-borrow per Lock 9; the demonstration grammar is JSON; the carry to BB is the renamed canonical surface (Layout/LayoutSink), the cursor-unified parse, the consolidated path-crate triplet, the grammar-agnostic IR, and the direct-to-struct emitter scaffolding. BA verifies the 13 locks in seven waves and hands BB a ratified foundation per the BA.md §Closing posture (line 137-139).

The post-BA performance trajectory is favourable. Per BA-G1 (twitter ≤ 400 µs, beating sonic-rs's 436 µs by ≥ 8%), BA closes one row of the SOTA matrix; BB.W3-W6 closes more (BB-G1 bootstrap ≤ 3.5 ms beating lightningcss 4.16 ms by ≥ 15%; BB-G3 citm ≤ 800 µs beating sonic-rs 854 µs by ≥ 6%; BB-G4 canada ≤ 3.0 ms beating sonic-rs 3.144 ms by ≥ 4.5%); BC.W6 tightens further (BC-G1 twitter ≤ 380 µs beating sonic-rs by ≥ 13%; BC-G2 bootstrap ≤ 3.0 ms beating lightningcss by ≥ 27%; BC-G3 canada ≤ 2.8 ms beating sonic-rs by ≥ 11%). The BA close hands BB a working JSON parser at sonic-rs-parity-or-better; BB extends across the remaining grammars; BC tightens.

## §2 — Milestones

> **M0 — PROGRESS.md emission**
>
> *Surface*: `docs/tranches/BA/PROGRESS.md` (new file).
> *Action*: Emit a per-wave summary: each wave's deliverable, the gate it closed, the milestone-by-milestone landings, the artefact paths (`docs/tranches/BA/audit/W0-toolchain-baseline.md`, `W2-generated-size-budget.md`, `W2-arena-builder-disposition.md`, etc.). Per `feedback_no_metalanguage_docs`, no commit references; cite path:line only. Per `feedback_archaic_diction`, archaic-permissive voice ("hereupon", "begotten", "thereof").
> *Gate*: `docs/tranches/BA/PROGRESS.md` exists; the seven-wave landings are documented.
> *Exit-criteria*: `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/PROGRESS.md && wc -l /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/PROGRESS.md | awk '{print ($1 > 100)}' | tr -d '\n'` returns `1`.

> **M1 — Lock cross-reference table verification**
>
> *Surface*: BA.md §13-Lock honoured (lines 76-90); per-lock wave-owner cells.
> *Action*: For each of the 13 locks, verify the wave-owner cell honours the lock per the closer-gate from that wave. Lock 1 (W0 residue scrub + W5 JSON OpenFrame retiral) — `rg -n 'TapeRec\|TapeCursor\|TapeBuilder\|TapeOffset' crates/*/src/` returns zero outside `tests/`; `rg -n 'enum OpenFrame' crates/core/src/runtime/json/` returns zero. Lock 2 (W2 rename) — `rg -nE 'type_projection|type_collapsing|StructLayout|schema_synthesis' crates/*/src/ | rg -v 'pub use'` returns zero. Lock 3 (W4 unification) — `rg -n 'pub fn parse_with' crates/core/src/grammar/generated/` returns ≥ 9. Lock 7 (W3 path triplet) — `find crates -maxdepth 1 -type d -name 'path*' | wc -l` returns 3. Lock 12 (Pre-BA ceremony) — `archive/{ser,gorgeous}/` exist; `Cargo.toml` workspace members reduced by 2. Lock 13 (W0+W2+W3) — `find crates -name '*.rs' ! -path '*/generated/*' | xargs wc -l | awk '$1>500'` returns empty.
> *Gate*: every cell in the §13-Lock honoured table verified; no cell carries a fault.
> *Exit-criteria*: A verification artefact at `docs/tranches/BA/audit/W6-lock-verification.md` records each lock's verification command + output; `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/audit/W6-lock-verification.md`.

> **M2 — BA-G3 close: `cargo xtask regen --check` ≤ 30 s; CSS L4 `compile_paths_request` ≤ 25 s**
>
> *Surface*: `cargo xtask regen --check` runtime; `crates/core/src/pipeline/compile/` (post-W2 split per `audit/MODULES-2026-05-03.md:696-702`).
> *Action*: Run `time cargo xtask regen --check` post-BA; verify wall ≤ 30 s. Run the CSS L4 `compile_paths_request` benchmark; verify wall ≤ 25 s. Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`, Lane 04 measured 59.98 s + 52.53 s as pre-BA baseline; halving is the BA close requirement. The W2 god-module splits and W4 cursor unification reduce per-pass IR walking cost; the W5 direct-to-struct emit reduces emit-stage cost for JSON. Per BA.md §Build/iter time gate (line 102-106), the halving is the BA close gate.
> *Gate*: regen ≤ 30 s; compile_paths_request ≤ 25 s.
> *Exit-criteria*: `time cargo xtask regen --check 2>&1 | grep real | awk '{print $2}'` returns ≤ 30 s; CSS L4 compile_paths_request artefact records ≤ 25 s.

> **M3 — Per-grammar generated-LOC table emission**
>
> *Surface*: BA.md §Generated-LOC budget table (lines 110-121); per-grammar `crates/core/src/grammar/generated/<g>.rs` LOC.
> *Action*: For each of nine grammars, measure post-BA `wc -l` and verify against the BA.md §Generated-LOC budget table target column: `json.rs` ≤ 2,100 (−40%), `bbnf.rs` ≤ 20,860 (−3%), `css_l4.rs` ≤ 104,800 (−2%), `google_sheets.rs` ≤ 13,800 (−2%), `css_pretty.rs` ≤ 8,930 (−1%), `ebnf.rs` ≤ 7,570 (−1%), `bnf.rs` ≤ 3,257 (−1%), `csv.rs` ≤ 1,676 (−1%), `math.rs` ≤ 862 (−1%). Total target ~163,855 (−2.9%). Emit `docs/tranches/BA/audit/W6-generated-loc-budget.md` recording the actuals.
> *Gate*: total post-BA LOC ≤ 163,855 (or with documented overflow rationale per BA-G10).
> *Exit-criteria*: `for g in json bbnf css_l4 google_sheets css_pretty ebnf bnf csv math; do wc -l /Users/mkbabb/Programming/bbnf-lang/crates/core/src/grammar/generated/$g.rs; done | awk '{sum+=$1} END {print (sum <= 163855)}' | tr -d '\n'` returns `1`.

> **M4 — Carry ledger to BB.W0 named explicitly**
>
> *Surface*: BA.md §Carry-tags TO BB (lines 57-64); BA.md §Carry-tags TO BC (lines 65-70); routed-carries from `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228`.
> *Action*: Emit a carry-ledger artefact at `docs/tranches/BA/audit/W6-carry-ledger.md` enumerating each carry-tag, its owner wave (BA.W?), the description, the receiving wave (BB.W? or BC.W?), the close-criterion. Per the BA.md §Carry-tags TO BB table:
> - BA→BB.C1 (BA.W5; receiving BB.W0/W1): direct-to-struct extension to all grammars.
> - BA→BB.C2 (BA.W2; receiving BB references Layout/LayoutSink only): layout-lowering canon.
> - BA→BB.C3 (BA.W4; receiving BB.W2): cursor-unified parse_with extends to all grammars.
> - BA→BB.C4 (BA.W3; receiving BB.W5): pointer! macro at crates/path/.
> - BA→BB.C5 (BA.W1; receiving BB.W3): grammar-agnostic bbnf-ir for CSP/e-graph/miner.
> Per the BA.md §Carry-tags TO BC table:
> - BA→BC.C1 (BA.W2; receiving BC.W0): layout-lowering canon supports IR contract spec.
> - BA→BC.C2 (BA.W5; receiving BC IR contract formalisation): direct-to-struct emitter pattern.
> *Gate*: artefact exists; every carry-tag named with owner + receiver + close-criterion.
> *Exit-criteria*: `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/audit/W6-carry-ledger.md && rg -n 'BA→BB.C[1-5]' /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/audit/W6-carry-ledger.md | wc -l | tr -d '\n'` returns `5`.

> **M5 — `substrate_audit` test green**
>
> *Surface*: `crates/ir/src/passes/tests/substrate_audit.rs` (per BA-G4 cite at BA.md line 18).
> *Action*: Run `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)'`; verify green. The test's pre-BA failure (32 zero-caller substrates per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`) closes via BA.W0/W3/W5 deletions and BA.W1's grammar-leak excision. Each row in the substrate-audit's input either deletes (W0/W3/W5) or whitelists with consumer rationale (BA.W6 close artefact).
> *Gate*: substrate_audit test passes; zero-caller count is zero (or each row whitelisted).
> *Exit-criteria*: `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)' --profile ax-iter 2>&1 | rg 'test result: ok' | wc -l | tr -d '\n'` returns ≥ 1.

> **M6 — FINAL.md emission**
>
> *Surface*: `docs/tranches/BA/FINAL.md` (new file).
> *Action*: Emit the BA close document. Per `feedback_archaic_diction`, archaic-permissive voice. Per `feedback_no_metalanguage_docs`, no commit references. Records: (a) BA's seven waves landed; (b) the 13 locks honoured cell-by-cell; (c) the carry ledger to BB.W0 explicit; (d) the closing posture (per BA.md §Closing posture line 137-139, "Hereupon BA opens. The substrate is the typed-enum + slice-borrow; the demonstration grammar is JSON; the carry to BB is the renamed canonical surface, the cursor-unified parse, the consolidated path-crate triplet, the grammar-agnostic IR, and the direct-to-struct emitter scaffolding"). The FINAL document's voice is the same archaic-permissive register as BA.md.
> *Gate*: `docs/tranches/BA/FINAL.md` exists; the BA close documents.
> *Exit-criteria*: `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/FINAL.md && wc -l /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/FINAL.md | awk '{print ($1 > 80)}' | tr -d '\n'` returns `1`.

## §3 — Closer gate

Per BA.md §Wave summary row BA.W6:

```
# BA-owned test surfaces 100% pass
cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis  ; expect: 100% pass

# substrate_audit green
cargo nextest run -p bbnf-ir -E 'test(substrate_audit)'
                                                       ; expect: 100% pass

# BA close artefacts
test -f docs/tranches/BA/PROGRESS.md                  ; expect: pass
test -f docs/tranches/BA/FINAL.md                     ; expect: pass
test -f docs/tranches/BA/audit/W6-lock-verification.md
                                                      ; expect: pass
test -f docs/tranches/BA/audit/W6-generated-loc-budget.md
                                                      ; expect: pass
test -f docs/tranches/BA/audit/W6-carry-ledger.md     ; expect: pass

# BA-G3: regen ≤ 30 s on M1 Pro
time cargo xtask regen --check                         ; expect: ≤ 30 s real

# Per-grammar generated-LOC table verified
for g in json bbnf css_l4 google_sheets css_pretty ebnf bnf csv math; do
  wc -l crates/core/src/grammar/generated/$g.rs
done                                                   ; expect: total ≤ 163,855
```

Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:204` ("BA.W0 is **not ready**; another amendment pass is required before execution"), W6 close confirms the BA-amendment punch list closes — the eight HARDENING-SYNTHESIS amendments at lines 49-201 (BA.md, BA/W0/W2/W3.md, BB/BB.md, BB/W0.md, BB/W3.md, BB/W4.md, BC/BC.md, BC/W0.md, BC/W5.md, GESTALT.md, codegen-paths.md, standard W0-W5 commit-plan line) are addressed in the BA / BB / BC plan tranches.

## §4 — Invariants

§I1. **Lock cross-reference verification**. Every cell in BA.md §13-Lock honoured (lines 76-90) verified per M1; the closer-gate's `cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis` is the cumulative gate.

§I2. **No metalanguage** (per `feedback_no_metalanguage_docs`). PROGRESS.md and FINAL.md cite path:line only; no commit references; no draft-history references.

§I3. **Archaic diction** (per `feedback_archaic_diction`). The voice of PROGRESS.md and FINAL.md is archaic-permissive ("hereupon", "begotten", "thereof", "appurtenant", "extant"); not corporate.

§I4. **Generated-size budget verified** (per BA-G10). The W6.M3 generated-LOC table records actuals against BA.md §Generated-LOC budget table targets; overflow without justification blocks the wave.

§I5. **Substrate audit green** (per BA-G4). The substrate-audit test passes; the 32 zero-caller substrates (per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`) close via BA.W0/W3/W5 deletions or whitelist rationale.

§I6. **Carry-tags explicit** (per `feedback_no_deferrals`). Every carry-tag at M4 names a real receiving wave with a close-criterion; no fictional successor letters; no "future tranche" placeholders.

§I7. **No deferrals** (per `feedback_no_deferrals`). The deferred locks (Lock 4, 10, 11) at BA close point to real receiving waves with gates: Lock 4 → BB.W3, Lock 10 → BB.W3, Lock 11 → BB.W0. Each is ratifiable per the BA→BB carry-tag table.

§I8. **Era V failure mode closed** (per `docs/tranches/meta-audit/archaeology/era-V-dta-psi-rut.md:7-10`). BA does not ship substrate-without-consumer; W6 verifies cell-by-cell.

## §5 — Risks specific to this wave

| Risk | Likelihood | Detection | Mitigation |
|---|---|---|---|
| BA-G3 (regen ≤ 30 s) is missed because the W2 god-module splits regress IR-walker cache locality | Medium | M2 `time cargo xtask regen --check` measurement | The split discipline preserves IR-walker shape (per W2.M0 layout-lowering rename, the IR walker traverses the same trait surface); if regen exceeds 30 s, profile via `samply record -- cargo xtask regen --check` and route the residual cost to BB.W0 generated-size-budget consideration |
| The substrate-audit test still fails at W6 because a hidden zero-caller substrate was missed across BA.W0/W3/W5 deletions | Medium | M5 `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)'` | Whitelist the row in the test's input with same-wave consumer rationale; per `audit/HARDENING-SYNTHESIS-2026-05-03.md:226`, "each row deleted or whitelisted with same-wave/later-wave consumer and rationale" — BA.W6 close documents the rationale |
| The post-BA generated-LOC total exceeds 163,855 because the W2 layout-lowering rename produces verbose generated names | Low | M3 LOC measurement | Per `feedback_generated_size_budget`, "Generated code has per-tranche line-count budget; overflow blocks wave until O(N) generator regression traced"; if overflow, root-cause the generator regression and surgically address |
| The W4 + W5 carries to BB.W2 / BB.W1 are insufficiently specified at BA.W6 close (BB.W0 cannot dispatch without surface contract from BA) | Low | M4 carry-ledger + BB.W0 authorship review | The carry-ledger at M4 specifies each carry's surface contract (which symbols, which paths); BB.W0 dispatches with the carry-ledger as input |
| FINAL.md voice drifts from archaic-permissive to corporate due to status-tick fatigue | Low | Read-through audit at W6 close | Re-emit FINAL.md if voice drifts; per `feedback_archaic_diction`, the voice is deliberate, not artefactual |

## §6 — Cross-references

- **Closes BA-G3** (per BA.md §Hard gates): `cargo xtask regen --check` ≤ 30 s on M1 Pro; CSS L4 `compile_paths_request` ≤ 25 s. Lane 04 baseline halved.
- **Closes BA-G4** (per BA.md): All 13 locks honoured at BA close per the §13-Lock cross-reference table; substrate_audit green.
- **Closes BA-G10** (per BA.md): Generated-file LOC accounting; per-grammar pre-/post-BA LOC table emitted; no overflow without justified cause.
- **Carry-tags TO BB**: BA→BB.C1 through C5 explicit per M4 (per BA.md §Carry-tags TO BB lines 57-64).
- **Carry-tags TO BC**: BA→BC.C1, BA→BC.C2 explicit per M4 (per BA.md §Carry-tags TO BC lines 65-70).
- **Routed carries** (per `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228`):
  - F4 Tailwind regex_scan timeout: BA.W2 disposition or route to BB.W3 per HARDENING-SYNTHESIS amendment at line 84-87.
  - 32 zero-caller substrates: closed at BA.W6.M5.
  - Generated-size budgets: closed at BA.W2.M6 + BA.W6.M3 artefacts.
  - F5 TS Node-execute: routed to BD per HARDENING-SYNTHESIS amendment at line 56-58.
  - AscentStrategy: routed to BC.W0 disposition per HARDENING-SYNTHESIS routed-carry table at line 224.
- **Preceding wave**: BA.W5 (JSON direct-to-struct codegen first cut; BA-G1 + BA-G2 closed).
- **Following wave**: BB.W0 (sister crate emigration). BB.W0 inherits the BA→BB.C1 through C5 carry-tags as inputs.
- **Routed-carries closed at W6**: per `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228` (the routed-carry table), W6 closes:
  - "32 zero-caller substrates → BA.W0 → substrate_audit green" — verified at M5.
  - "Generated-size budgets → BA.W2 and BB.W4 → Per-generated-file LOC windows and warning deltas recorded before emitter/regen close" — BA.W2.M6 + BA.W6.M3 artefacts.
  - "Sibling dirty-state ambiguity → BC.W0" — DEFERRED; BA.W6 names BC.W0 as receiving wave with `W0-sibling-baseline.txt` artefact requirement.
  - "AscentStrategy → BA.W5 or BC.W0, pending amendment" — DEFERRED to BC.W0 per `audit/HARDENING-SYNTHESIS-2026-05-03.md:224`; BA.W6 names BC.W0 as receiving wave; the `path/ascent.rs:277` (per `audit/MODULES-2026-05-03.md:678`) AscentStrategy disposition is BC-owned.
  - "F5 TS Node-execute → BD" — DEFERRED to BD per HARDENING-SYNTHESIS amendment line 56-58; BA.W6.FINAL records BD owner/reason/deadline; BA does not edit TS or WASM emitters.
  - "F4 Tailwind regex_scan → BA.W2 disposition or BB.W3 routed" — disposition recorded at W2.M6 + W5.M5 close; if direct-projection closed F4 by mechanism, no carry; if not, BB.W3 is the receiving wave with path-shape rewrite hypothesis named.

## §7 — Iter-time check

| Cargo Command | Expected Duration | Pass-Rate Target | Notes |
|---|---|---|---|
| `cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis --profile ax-iter` | ≤ 90 s | 100% | BA-owned surface gate per BA.md §Wave summary BA.W6 row |
| `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)' --profile ax-iter` | ≤ 12 s | 100% | substrate_audit green gate (BA-G4) |
| `time cargo xtask regen --check` | ≤ 30 s real | exit 0 | BA-G3 closer-gate; halving of Lane 04 59.98 s baseline |
| `cargo bench --bench bench_json -- twitter` | ≤ 60 s | mean ≤ 400 µs | BA-G1 close re-verification |
| `for g in json bbnf css_l4 google_sheets css_pretty ebnf bnf csv math; do wc -l crates/core/src/grammar/generated/$g.rs; done` | < 1 s | total ≤ 163,855 | M3 generated-LOC table |
| `find crates -name '*.rs' ! -path '*/generated/*' \| xargs wc -l \| awk '$1>500'` | < 2 s | empty | Lock 13 god-module discipline gate |
