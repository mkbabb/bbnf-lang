---
agent: 1A
pass: T-P1-excavation
cycle: V5
generated_at: 2026-05-29T00:00:00Z
spec_surfaces_audited:
  - restart/ARCHITECTURE.md
  - restart/MASTER-PLAN.md
  - restart/locks/LOCKS.md
  - restart/skinny/tranches/sk-v17/SPEC.md
files_audited_count: 21
live_truth_method: "nl -ba + grep path:line over restart/{ARCHITECTURE.md,MASTER-PLAN.md,locks/LOCKS.md,skinny/tranches/sk-v17/SPEC.md}; Read over crates/core/src/runtime/tape/{mod,record,arena,cursor}.rs, crates/core/src/runtime/{json,css_l4}/builder.rs, crates/core/src/runtime/{json,css_l4}/value.rs, crates/ir/src/registry/struct.rs, crates/simd-scan/src/{lib,alphabet}.rs, skinny/crates/runtime/src/tape/mod.rs, skinny/crates/runtime/src/grammars/json/value.rs, skinny/crates/bbnf-simd/src/dispatch.rs; per-grammar scan_structural census via grep -c over crates/core/src/grammar/generated/*.rs (8 wired + math.rs=0); no cargo/build mutation. V4 re-verified all spec/impl anchors live: SPEC :258/:806/:807-811/:825/:839/:110-114/:852-854, LOCKS :75/:107-108/:119-126/:137-149/:160, ARCH :1088/:1186/:1206, record.rs:103/120/121, tape/mod.rs:7/9/55/58, ir/struct.rs:84/202/314/331, skinny tape/mod.rs:94-100/104-111/175, StructLayout=960. V5 re-verified live: StructLayout=960, TapeStructBuilder grep-zero outside runtime/tape/, value_from_ref absent in crates/core/src/runtime/json/value.rs (=0) + present skinny json/value.rs:143, math.rs scan_structural=0, scan_structural signature simd-scan/lib.rs:80, scan call sites css_l4.rs:15982 + json.rs:732, alphabet.rs:33-37 quote_classes doc, record.rs:103/120, and the @generated headers at crates/core/src/runtime/{json,css_l4}/value.rs:1. Master HEAD 445925167."
prior_cycle_dispositions_folded:
  accepted:
    - CH5-S3-V3-renamed-scanner-cross-call-classifier-state  # re-verified scan_structural per-call simd-scan/lib.rs:80
  rejected: []
  revised:
    - V4-CH-1A-Gaps-row-1a126-lock14-mischaracterisation  # FOLD V5 REVISE: 1a:126 "value APIs are hand-written per grammar" was the SAME Lock-14 mischaracterisation V3-CH1-V3-003 charged against 1D (and 1D/1C/1F folded). Files are @generated by xtask regen-{json,css} (Lock-14 ALLOWED), NOT hand-written (which would be the Lock-14 VIOLATION). Restated to "@generated per grammar but EAGER, not hand-written, divergence is the eager materialization shape" mirroring 1f-coherence-scan.md:114 + 1d-skinny-lessons.md:146. @generated headers at value.rs:1 live-verified this cycle. Localized to this Gaps row; 1A's own Table (SUB17-004) + Cross-Tree map already correct.
  v4_carried_verified:  # V4 dispositions held this cycle, anchors live-re-verified
    - CH3-R4-SUB17-005-x86-bar-at-806-not-807  # :79 — x86 bar :806; second-substrate block :807-811
    - CH5-S8-V4-REDRESS-53-anchor-at-825-839    # firewall row :104 REDRESS-53 anchored SPEC :825/:839; :807-811 second-substrate cite only
  first_cycle_additions: []
  v3_carried_verified:  # V3 dispositions already folded + live re-verified this cycle
    - CH5-S1-parallel-substrate-firewall
    - CH5-S2-onceCell-retained-scan-cache
    - CH5-S6-5shape-canon-aarch64-absorption
    - CH5-S7-second-substrate-D6-monotonic-direction
    - CH1-V2-002-value_from_ref-path
    - CH5-S4-nine-pending-vecs-count
    - CH5-S8-lock1-one-encoding-closure-obligation
    - S1.9-css_l4-scan-wired-all-8
    - COH17-006-StructLayout-960-site-surface
    - SUB17-009-StructRegistry-per-leaf-lookup-hot-path-fence
divergence_count:
  spec_claims_implemented: 2
  spec_claims_unimplemented: 4
  impl_exceeds_spec: 2
  unknown: 2
locks_amendment_candidates: 0
---

## Executive Summary

1A is the Lock-1 substrate-union spine and CH5's firewall counterpart; the
substrate facts previously scope-bled into the 1F triad migrate here per CH5-S0.
The excavation maps the SKINNY-proven unified-tape model against the `crates/core`
fold target at master `445925167`. The central substrate fact is a NAMING + SHAPE
bifurcation the SK-V18 fold must reconcile under exactly-one-encoding (Lock 1):
the SKINNY-proven substrate is SoA `Tape<'input>` (`offsets: Vec<u32>` + sparse
`flag_cursors`/`flag_values` + `PayloadArena`, `skinny/crates/runtime/src/tape/mod.rs:94`)
with grammar-parametric `ValueRef<'doc,'input>` (`:175`) and `value_from_ref`
(`skinny/crates/runtime/src/grammars/json/value.rs:143`); the `crates/core` fold
target is `TapeStructBuilder` over a 16-byte AoS `TapeRec` (`crates/core/src/runtime/tape/record.rs:103`,
const-asserted 16-byte at `:120`), AoS-first by its own doc (`mod.rs:6-9`). The
core tape is empirically UNWIRED — `TapeStructBuilder` is grep-zero outside
`runtime/tape/`; the live substrate is the eager `JsonStructBuilder`/`CssStructBuilder`
`OpenFrame` machine (`json/builder.rs:9`, `css_l4/builder.rs:16`). By contrast the
structural SIMD scan IS wired and grammar-general across ALL 8 generated parsers
INCLUDING `css_l4.rs:15982` and `css_pretty.rs` — the prior 1F "css_l4 scan absent"
gap is FALSE (CH1-V2-003). The Lock-1 union is honoured within `crates/core` (one
tape, dead; the retained `OnceCell<StructuralIndex>` is a scan cache, not a second
substrate). The load-bearing fold-fence: `begin_compound(&StructLayout)` consults
the `StructRegistry` BTreeMap (`crates/ir/src/registry/struct.rs:331`); a naive
per-leaf registry lookup re-opens the 28-65×/983×/10583× regression — the
`FieldSource` walk must be compile-time projection-emission resolved once (SPEC
:794-795). No new substrate, BIR variant, or shape is proposed.

**V4 fold** (CH3-R4, CH5-S8): two off-by-one secondary §9 anchors corrected — SUB17-005
now cites the x86 bar at SPEC `:806` (the second-substrate block is the separate
`:807-811`); the Sidecar-producer firewall row re-anchors REDRESS-53 to its naming lines
SPEC `:825` (W2 row) + `:839` (shortlist condition 1), retaining `:807-811` only as the
second-substrate-block cite. No verdict change; primary cites (`:258`, `:852-854`,
ARCH `:1206`) and the monotonic-direction guard hold. All V3 anchors live-re-verified
this cycle (CH5-S3 scan_structural per-call at `simd-scan/lib.rs:80` re-confirmed).

**V5 fold** (CH against 1A Gaps-row): the lone 1A-owned REVISE corrects the Gaps-row
at `1a:126` which had said "value APIs are hand-written per grammar" — the SAME Lock-14
mischaracterisation V3-CH1-V3-003 charged against 1D and that 1D/1C/1F folded. The core
value modules are `@generated by xtask regen-{json,css}` (header live-verified at
`crates/core/src/runtime/{json,css_l4}/value.rs:1`), which Lock 14 ALLOWS; hand-authoring
them would be the Lock-14 VIOLATION. The divergence is the EAGER materialization shape,
not the generation provenance — restated to mirror `1f-coherence-scan.md:114` and
`1d-skinny-lessons.md:146`. 1A's own Spec-Claim Table (SUB17-004) and Cross-Tree map
(value-API row) already carried the correct "per-grammar eager … not lazy `ValueRef`"
framing, so the defect was localized to the one prose Gaps row. No verdict change; all
V4/V3 anchors live-re-verified this cycle.

## Spec-Claim ↔ Implementation Table

| ID | Spec / corpus claim (path:line) | Live / counter-surface evidence (path:line) | Verdict | Divergence class | Note |
|---|---|---|---|---|---|
| SUB17-001 | Lock 1: "Tape is the substrate, properly unioned with direct-to-struct … if structural offsets are retained, the structural projection IS the tape" (`restart/locks/LOCKS.md:75`); ARCH restates the union + "the structural projection IS the tape (no second sidecar)" (`restart/ARCHITECTURE.md:1088`). SK-V17 names the proven substrate the SoA `Tape` (`restart/skinny/tranches/sk-v17/SPEC.md:230-241`). | SKINNY-proven SoA `Tape<'input>` carries six position-keyed members: `source`, `offsets: Vec<u32>`, `flag_cursors: Vec<u32>`, `flag_values: Vec<u8>`, `payloads: PayloadArena`, `id` (`skinny/crates/runtime/src/tape/mod.rs:94-100`). `from_offsets` constructs it with `debug_assert_eq!(flag_cursors.len(), flag_values.len())` (`:104-117`). | spec-claims-implemented (in skinny) | union honoured | The proven substrate carries NO class column — sparse flags keyed to positions, one offsets vector. The Lock-1 union holds in the proven tree. |
| SUB17-002 | SK-V17 §0.1.11: the unified tape model is "structured so the TOTALITY tree (`crates/core/src/runtime/tape/`) can adopt them in SK-V18" (`restart/skinny/tranches/sk-v17/SPEC.md:110-114`). | `crates/core` fold target is `TapeStructBuilder` (`crates/core/src/runtime/tape/mod.rs:58`) over a 16-byte AoS `TapeRec` (`record.rs:103`; const-asserted `size_of==16` + `align_of==4` at `:120-121`), NOT the proven SoA `Tape`. The core mod-doc states the AoS shape is provisional: "kept AoS first … the same `TapeCursor` API rides a later SoA split" (`mod.rs:6-9`). | spec-claims-unimplemented | cross-tree shape drift (transient) | Two encodings (AoS core / SoA skinny-proven). NOT a parallel substrate within either tree. **Lock-1 closure obligation:** post-SK-V18 exactly ONE encoding survives across both trees (`LOCKS.md:75` "parallel substrates are dead"); AoS/SoA coexistence is admissible ONLY as a transient fold-state — a post-fold dual encoding is a Lock-1 violation, not a tree-local choice (CH5-S8). |
| SUB17-003 | SK-V17 §0.1.11 frames the core tape as the SK-V18 adoption target; the wiring state is "tape UNWIRED" (`restart/skinny/tranches/sk-v17/SPEC.md:110`, gate `:174` `tape_activated` is "NOT a crates/core grep"). | Confirmed UNWIRED: `TapeStructBuilder`/`TapeRec`/`PayloadArena`/`TapeCursor` are grep-zero outside `crates/core/src/runtime/tape/` (sole non-`tape/` hit is a doc-comment mention of `TapeRec::PAYLOAD_F64_DIRECT_BIT` in `backend/rust/emitter/shapes/number.rs:17`, not a usage). Live JSON parse uses `JsonStructBuilder::new()` (`json/parse_with.rs:34`); CSS uses `CssStructBuilder` `OpenFrame`+`pending_*` (`css_l4/builder.rs:16,66-79`). | spec-claims-implemented (UNWIRED confirmed) | tape-unwired + scan-wired asymmetry | The fold target's tape is dead code while its structural scan is live across all 8 grammars — the index exists, no tape consumes it, the eager builders consume neither. |
| SUB17-004 | SK-V17 W2: ONE `BackendRule`-walking accessor generator emits `document/value/view/visitor`, isomorphic to JSON's `value_from_ref` over the EXISTING `Tape`/`ValueRef` (`restart/skinny/tranches/sk-v17/SPEC.md:54-61`); grammar-parametric `ValueRef` (`skinny/crates/runtime/src/tape/mod.rs:175`). | `crates/core` has NO `ValueRef`/`value_from_ref` in `json/value.rs` (grep empty). The CSS value layer is an eager typed enum `CssTypedValue<'p>` (`crates/core/src/runtime/css_l4/value.rs:414`) fed by the `OpenFrame` builder. The core value API is per-grammar eager enums, not a single lazy `ValueRef<G>` projection. | spec-claims-unimplemented | per-grammar value-API vs lazy-ValueRef generalization | The SK-V18 fold target: core's value layer is eager-per-grammar, not the lazy `ValueRef` projection the SKINNY path proves. **Files-touched:** the projection generator is generator-side (one accessor generator); the regenerated set is per-grammar `value.rs`/`view.rs`/`document.rs` (8 grammars), regen-gated. |
| SUB17-005 | §7.3 BackendShape canon: `CollapsedStage` "fuses mask-state and emission for AVX-512-class hardware" (`restart/ARCHITECTURE.md:1088`), lowers to `skinny/crates/bbnf-simd/src/x86_64/{grammar}_collapsed.asm` (`:1186`); admission gate `target.arch == x86 + avx512bw`, "aarch64 mechanically refused", and the aarch64 candidate is named **UNKNOWN-2D-05** (`:1206`). | SK-V17 is aarch64-only and EXPLICITLY bars x86/AVX-512/SVE — the x86 bar is `restart/skinny/tranches/sk-v17/SPEC.md:806` ("x86 / AVX-512 / SVE: aarch64 only"; the second-substrate block is the separate `:807-811`), and `:258`; plus the "asmjson collapsed-stage FSM (x86, host-blocked)" + "D6 second substrate" (REJECTed `:852-854`). The NEON path produces only a `Vec<u32>` structural index via `select_classifier` (`skinny/crates/bbnf-simd/src/dispatch.rs:42`). | spec-claims-unimplemented (no aarch64 CollapsedStage) | spec-x86-CollapsedStage vs skinny-aarch64-NEON | The 5-shape canon holds at Lock 10 (`LOCKS.md:107-108`); CollapsedStage admission is x86-pinned, mechanically refused on aarch64, and the aarch64 candidate is the **spec-named open unknown UNKNOWN-2D-05** (`ARCHITECTURE.md:1206`) — NOT a fresh undiscovered gap. SK-V17's NEON union sits under the four LLVM shapes' scan-leaf FFI. |
| SUB17-006 | Lock 2 RETIRED the term `StructLayout`; canonical name is `Layout`/`LayoutFacts` (`restart/locks/LOCKS.md:160`). | `crates/core`/`crates/ir` (the fold target) carry `StructLayout` LIVE: `pub struct StructLayout` (`crates/ir/src/registry/struct.rs:202`), consumed by `begin_compound(&StructLayout)` (`crates/core/src/runtime/tape/mod.rs:185`). `grep -rn StructLayout crates/` = **960** references spanning `ir/registry/struct.rs:202` + the codegen emitter + ~16 test files. | spec-claims-unimplemented (name not migrated in core) | spec-retired-name vs core-live-name | The totality tree has NOT migrated `StructLayout` → `Layout` per Lock 2. **Re-priced:** the rename is generator-side, regenerating all 8 parsers + ~16 tests; the live surface is 960 sites, not 40-120 LOC. Risk medium. Lock-2 reconciliation surface for T-P3 (no candidate raised here per 1E ownership). |
| SUB17-007 | SK-V17 NEON leaf datum is `alphabet: &[u8;64]` via `select_classifier` (`restart/skinny/tranches/sk-v17/SPEC.md:99-106`, :315); the eq-set fan is the CSS-admissible backend. | `crates/core`'s `StructuralAlphabet` is richer config: `singletons: &'static [u8]`, `digraph_mask: [u64;4]`, `digraph_pairs: &'static [(u8,u8)]`, `quote_classes: &'static [u8]` (`crates/simd-scan/src/alphabet.rs:19-37`). | impl-richer-alphabet-config | impl-exceeds-spec | The totality alphabet config is richer (digraphs, quote classes) than the proven `[u8;64]`. **Generality is config-breadth, not proof-breadth:** the `quote_classes` doc comment (`alphabet.rs:33-37`) shows the field is JSON/CSS-motivated (`b'"'` for JSON, `b'\''`+`b'"'` for CSS) — JSON/CSS-exercised-only like SPEC :112, not breadth-of-proof. Both grammar-as-data (Lock 14 honoured). |
| SUB17-008 | MASTER §13 SIMD vocabulary is x86-heavy (AVX-512 `CollapsedStage`); ARCH lists bbnf-simd as AVX2/AVX512/NEON/WASM/scalar (`restart/ARCHITECTURE.md:89`). SK-V17 bars x86/AVX/SVE (`restart/skinny/tranches/sk-v17/SPEC.md:258`). | The wired core scan crate `simd-scan` carries neon + avx2 + avx512 + wasm + scalar kernels generically (`crates/simd-scan/src/lib.rs:53-65`, dispatch `:80-95`). SK-V17 uses ONLY the narrower aarch64 `skinny/crates/bbnf-simd` `select_classifier(&[u8;64])` (`dispatch.rs:42`). | impl-multiarch-exceeds-spec-scope | impl-exceeds-spec | Core's scan layer is BROADER (multi-arch) than the proven aarch64-only path. Two distinct simd crates: `crates/simd-scan` (totality, multi-arch, WIRED) and `skinny/crates/bbnf-simd` (skinny, aarch64, proven). Scope question for the fold, not a defect. |

## Cross-Tree Substrate Map (SK-V18 fold subject)

| Concern | SKINNY-proven (skinny/crates) | TOTALITY fold target (crates/core) | Fold reconciliation |
|---|---|---|---|
| Tape encoding | SoA `Tape<'input>`: `source` + `offsets: Vec<u32>` + sparse `flag_cursors: Vec<u32>`/`flag_values: Vec<u8>` + `PayloadArena` + `id` (`skinny/crates/runtime/src/tape/mod.rs:94-100`) | AoS 16-byte `TapeRec` in `TapeStructBuilder` (`crates/core/src/runtime/tape/mod.rs:58`, `record.rs:103,120`) | ONE substrate (Lock 1, `LOCKS.md:75`); SK-V18 adopts the proven SoA or proves AoS parity. **Exactly-one-encoding post-fold; AoS/SoA coexistence is transient-only.** |
| Borrow shape | `&'input` source + `ValueRef<'doc,'input>` borrows the doc + input (`skinny/.../tape/mod.rs:94,175`) — the `&'i Tape<'i>` cursor shape Lock 1 names (`LOCKS.md:75`) | `TapeStructBuilder<'p>` + `TapeCursor` (cursor.rs); no `ValueRef` | the `&'i Tape<'i>` + cursor borrow shape is proven in skinny; core adopts it with the value-API fold (SUB17-004) |
| Builder | `TapeBuilder`/`push_plain_offset` (SPEC :241-244) | `TapeStructBuilder` impl `StructBuilder` (UNWIRED) + live eager `JsonStructBuilder`/`CssStructBuilder` `OpenFrame` (`json/builder.rs:9`, `css_l4/builder.rs:16`) | retire eager `OpenFrame` builders; wire tape builder (Lock 1: no Vec<OpenFrame>::clone) |
| Value API | grammar-parametric `ValueRef<'doc,'input>` + `value_from_ref` per grammar (`skinny/crates/runtime/src/tape/mod.rs:175`; `skinny/crates/runtime/src/grammars/json/value.rs:143`) | per-grammar eager typed enums; NO `ValueRef`/`value_from_ref` in core (grep empty over `crates/core/src/runtime/json/value.rs`) | generate one `BackendRule`/`FieldSource`-walking lazy projection. **The `FieldSource` walk is COMPILE-TIME projection-emission, resolved once at codegen — NOT a per-leaf runtime walk through the live `StructRegistry`** (`FieldSource` lives in `StructRegistry`, `crates/ir/src/registry/struct.rs:84,313`; SPEC :794-795). |
| Layout shape | `BackendRule` + `LayoutFacts.backend_shape` (skinny ir) | `StructLayout` + `FieldSource{TypedLeaf, BranchTag, SeqPosition, RepeatElement, RuleReference}` (`crates/ir/src/registry/struct.rs:84,202`) | Lock-2 name reconcile (SUB17-006); FieldSource IS the BackendRule-walk recipe. **Resolved at compile time, not per-leaf indirection.** |
| Structural scan | aarch64 `select_classifier(&[u8;64])` → `Vec<u32>` (`skinny/crates/bbnf-simd/src/dispatch.rs:42`); skinny CSS scan UNWIRED today (W3 wiring-state, SPEC :99), JSON-lane wired | multi-arch `scan_structural(&StructuralAlphabet)` → `StructuralIndex`, WIRED into ALL 8 generated grammars incl `json.rs:732`, `css_l4.rs:15982`, `css_pretty.rs`, `ebnf.rs`, `bnf.rs`, `csv.rs`, `google_sheets.rs`, `bbnf.rs` (`crates/simd-scan/src/lib.rs:80`) | scan already grammar-general AND CSS-wired in core; the missing primitive is the TAPE CONSUMER, not the scan |
| BackendShape | 5-shape canon, NEON under 4 LLVM shapes, NO aarch64 CollapsedStage (SPEC :852-854) | 5-shape enum, CollapsedStage x86/AVX-512-pinned, aarch64 = UNKNOWN-2D-05 (`ARCHITECTURE.md:1206`) | canon holds at Lock 10 (`LOCKS.md:107-108`); aarch64 absorbs into the 4 LLVM shapes; CollapsedStage-aarch64 is a spec-named open unknown |

## Substrate-Union Firewall (Lock 1, CH5 spine)

| Firewall check | Live evidence | Verdict |
|---|---|---|
| Parallel substrate / second tape within crates/core | ONE tape construct: `TapeStructBuilder` (`tape/mod.rs:58`), `TapeRec` AoS (`record.rs:103`), `PayloadArena` (`arena.rs`), `TapeCursor` (`cursor.rs`) — UNWIRED (grep-zero outside `tape/`). Live substrate is the eager `OpenFrame` builders (`json/builder.rs:9`, `css_l4/builder.rs:16`). | NO same-tree parallel substrate. The unwired tape is the SK-V18 fold target sitting dormant; the AoS-vs-SoA cross-tree gap (SUB17-002) is a fold-convergence question, not a same-tree second substrate. |
| Sidecar producer / retained structural projection | Generated parsers retain `OnceCell<StructuralIndex>` initialized via `scan_structural`, in ALL 8 grammars: `json.rs:686`/init `:732`, `css_l4.rs:15936`/init `:15982`, `css_pretty.rs`, `ebnf.rs`, `bnf.rs`, `csv.rs`, `google_sheets.rs`, `bbnf.rs`. | Per Lock 1 "if structural offsets are retained, the structural projection IS the tape" (`ARCHITECTURE.md:1088`) — but the retained index feeds the eager `OpenFrame` builders, NOT a tape; it is a retained scan cache, not the tape projection. Under SK-V18 it must become the tape's `offsets` (index IS the tape) OR `local_temp_only` (`LOCKS.md:119-126`); a retained index parallel to a wired tape is REDRESS-53, named at SPEC `:825` (W2 row: "L1/L4 index as parallel retained vector (REDRESS-53)") + `:839` ("A retained parallel index collapses into REDRESS-53"); the general second-substrate block is the separate `:807-811`. **Verify at fold: scope to ALL 8 carriers, not json.rs alone.** |
| Renamed-scanner / cross-call classifier state (Lock 1 v+1) | core scan is `scan_structural(input, &alphabet)` (`crates/simd-scan/src/lib.rs:80`) producing a `StructuralIndex` per call; no cross-call classifier-state retention visible in dispatch (`lib.rs:80-95`). | Lock 1 v+1 ELEVATION ("no cross-call retained classifier state … carry MUST stay within a single chunk-call boundary", `LOCKS.md:137-149`) NOT tripped. The `OnceCell<StructuralIndex>` retains the OUTPUT index per-parse, not classifier carry state. verify_action: grep `prev_state\|carry\|prefix_xor\|retained` over `crates/simd-scan/src/` before any SIMD fold. |
| StructRegistry per-leaf hot-path indirection (CH3-R1 pre-block fence) | `begin_compound(&StructLayout)` (`crates/core/src/runtime/tape/mod.rs:185`) reads `layout.rule_id & 0x1F`; the `StructLayout` originates from `StructRegistry` (`crates/ir/src/registry/struct.rs:313`), a `BTreeMap<RuleId, StructLayout>` (`:314`) queried via `layout(rule_id)` (`:331`). `FieldSource` lives inside the same registry (`:84`). | **DO-NOT-REDRIVE FENCE.** A naive per-leaf `StructRegistry` BTreeMap lookup on the hot path re-opens the 28-65×/983×/10583× regression (SPEC :794-795 + W1 row :824). The `FieldSource`/`StructLayout` walk MUST be compile-time projection-emission (codegen emits the field routing once), NOT a per-leaf runtime registry lookup. The `BackendRule`/`FieldSource`-walk projection generator (SUB17-004) resolves the layout ONCE at codegen, never per-leaf. |

## Divergences Catalogued

| ID | Divergence | Evidence | Files-touched / LOC / risk |
|---|---|---|---|
| SUB17-002 | Tape encoding: core AoS `TapeRec` vs skinny-proven SoA `Tape`. Post-fold exactly-ONE encoding (Lock-1 closure). | `crates/core/src/runtime/tape/record.rs:103,120`; `skinny/crates/runtime/src/tape/mod.rs:94` | 200-600 LOC SK-V18 fold; eager-builder retirement touches 22+ files (8 generated parsers + emitter shape hierarchy + `runtime/mod.rs` + both `parse_with.rs`, via `grep -rln JsonStructBuilder\|CssStructBuilder`); re-emit is generator-side/regen-gated; medium |
| SUB17-003 | Core tape UNWIRED while structural scan WIRED across all 8 grammars — asymmetric. | `crates/core/src/runtime/json/parse_with.rs:34`; `css_l4.rs:15982`; TapeStructBuilder grep-zero outside tape/ | 0 LOC to catalogue; high to wire (the SK-V18 subject) |
| SUB17-004 | Value API is per-grammar eager enums in core, not lazy `ValueRef`. | `crates/core/src/runtime/css_l4/value.rs:414`; no `value_from_ref` in `json/value.rs` | generator-side: one accessor generator; regenerated: per-grammar `value.rs`/`view.rs`/`document.rs` × 8 (regen-gated); 300-700 LOC; high |
| SUB17-005 | §7.3 CollapsedStage is x86/AVX-512; aarch64 candidate is spec-named UNKNOWN-2D-05; SK-V17 has no aarch64 CollapsedStage. | `restart/ARCHITECTURE.md:1206`; SPEC :258,:852-854 | 0 LOC (canon holds); medium (fold must absorb aarch64-NEON without a 6th shape per Lock 10 `:107-108`) |
| SUB17-006 | `StructLayout` is Lock-2-retired but live in crates/core/crates/ir at 960 sites. | `restart/locks/LOCKS.md:160`; `crates/ir/src/registry/struct.rs:202`; `grep -rn StructLayout crates/`=960 | generator-side rename regenerating 8 parsers + ~16 tests; 960-site surface; medium |
| SUB17-007 | core `StructuralAlphabet` (digraph/quote-class) richer than proven `[u8;64]`; generality is config-breadth, JSON/CSS-exercised-only. | `crates/simd-scan/src/alphabet.rs:19-37` (quote_classes doc `:33-37`); `skinny/crates/bbnf-simd/src/dispatch.rs:42` | 50-200 LOC alphabet reconcile; low (both grammar-as-data) |
| SUB17-008 | Two SIMD crates: `crates/simd-scan` (multi-arch, wired) vs `skinny/crates/bbnf-simd` (aarch64, proven). | `crates/simd-scan/src/lib.rs:53-65`; `skinny/crates/bbnf-simd/src/dispatch.rs:42`; SPEC :258 | 100-400 LOC scope reconcile; medium |
| SUB17-009 | StructRegistry/StructLayout/FieldSource per-leaf-lookup is hot-path indirection that re-opens the 28-65×/983×/10583× regression if walked per-leaf. | `crates/ir/src/registry/struct.rs:84,313,331`; `crates/core/src/runtime/tape/mod.rs:185`; SPEC :794-795 | 0 LOC to catalogue; the fence is no-per-leaf-registry-lookup; HIGH if violated |

## Gaps / Missing Primitives

| Gap | Evidence | Receiver |
|---|---|---|
| No live tape consumer in crates/core: `TapeStructBuilder` exists but no parse path calls it. | `crates/core/src/runtime/json/parse_with.rs:34` uses `JsonStructBuilder`; `TapeStructBuilder` grep-zero outside `tape/` | SK-V18 fold (T-P2/T-P3 map; SK-V17 proves in skinny first) |
| No `ValueRef` lazy-projection generator in crates/core; the per-grammar value APIs are `@generated by xtask regen-{json,css}` (`value.rs:1`) but EAGER, NOT hand-written — Lock-14 ALLOWED (hand-written would be the Lock-14 VIOLATION); the divergence is the EAGER materialization shape, not the generation provenance (mirrors 1f-coherence-scan.md:114, 1d-skinny-lessons.md:146). The fold acts on the GENERATOR to emit a lazy `ValueRef<G>` projection. | `crates/core/src/runtime/{css_l4,json}/value.rs:1` (`@generated by xtask regen-*` headers, live-verified; no `value_from_ref`) | SK-V17 W2 proves the lazy projection in skinny (SPEC §5); SK-V18 retargets the existing regen path |
| No aarch64 CollapsedStage admission path; the §7.3 canon's 5th shape is x86-only and the aarch64 candidate is the spec-named open unknown UNKNOWN-2D-05. | `restart/ARCHITECTURE.md:1206`; SPEC :852-854 | T-P2 research: how aarch64-NEON union maps into the canon without a 6th shape |
| Core CSS structural scan IS wired (`css_l4.rs:15982`); the residual TRUE gap is the absent tape CONSUMER of the grammar-general index, not the scan. The skinny-side CSS scan (`bbnf-simd`) is unwired (W3 wiring-state, SPEC :99) — a skinny-tree fact, not a core gap. | `crates/core/src/grammar/generated/css_l4.rs:15976-15982` builds the full `StructuralAlphabet` and calls `scan_structural`; identical pattern to `json.rs:732` | SK-V17 W3 wires CSS scan in skinny; core's scan is already wired — the tape consumer is the SK-V18 fold |

## Open Questions

| UNKNOWN | Blocking question | verify_action |
|---|---|---|
| U-SUB17-001 | Are the two tape encodings (core AoS / skinny SoA) intended to converge to ONE in SK-V18 (Lock-1 closure obligation), or does the totality tree keep AoS and prove parity — and which encoding is the convergence target? | T-P2 re-read `crates/core/src/runtime/tape/mod.rs:6-9` ("AoS first … later SoA split") against SPEC :110-114 and `LOCKS.md:75` ("parallel substrates are dead"); name the convergence-target tape shape. Exactly ONE encoding survives post-fold; the dual encoding is a transient fold-state only. |
| U-SUB17-002 | Is the `OnceCell<StructuralIndex>` in the 8 core generated parsers a `local_temp_only` scratch or an `existing_tape` projection under the Lock 1 v+1 `substrate_target` manifest (`LOCKS.md:119-126`)? | T-P2 read `crates/core/src/grammar/generated/json.rs:686-732` + `css_l4.rs:15936-15982` and classify against the four admitted `substrate_target` values; scope the classification to ALL 8 carriers; the fold must declare it before wiring the tape, else REDRESS-53 re-entry. |
