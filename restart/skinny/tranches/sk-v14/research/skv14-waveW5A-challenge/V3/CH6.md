# SK-V14 W5A CHALLENGE V3 CH6: Consumers and Revert
Disposition: ACCEPT
Acceptance score: 94/100
Findings:
- ACCEPT: Same-wave consumers are mandatory and executable. The plan requires `regen-css` plus every `check-css-l4-*` companion to call the new request path and forbids the old `codegen::emit_runtime_profile(target.profile)` call boundary, then names exact commands for `regen-css` and all seven CSS checks.
- ACCEPT: JSON/CSS/Sheets/BBNF proof consumers are bound to one request path instead of parallel proof paths. CSS uses `regen-css` and companions, JSON uses `check-json` plus equality against `emit_from_source("json", source)`, and Sheets/BBNF-self must fail closed or emit generated-role witnesses through the same parser/contract.
- ACCEPT: Evidence cannot paper-close on advisory or freshness-only proof. The plan pairs exact tests with nonzero-pass log assertions, source-consuming grep gates, JSON whole-directory no-diff proof, provider/template count and staged/unstaged rename checks, and exact no-diff maintain gates for `skinny/RESULTS.md` and `restart/skinny/ROLLING-SOTA-DELTA.md`.
- ACCEPT: The revert protocol is complete for W5A's risk slice. It requires rejected-patch escrow, one-slice revert of `grammar_provider.rs`, parser support, `lib.rs` request entrypoint edits, `regen.rs`/`regen_css.rs` routing, and the W5A Lock 14 guard, while retaining the provider/template mesh and adding REDRESS attribution for the failed construct/proof/metadata/JSON/Sheets/BBNF/guard surface.
- ACCEPT: Current code confirms why these folds are necessary: CSS has seven companion commands and fifteen source inputs, but `regen::write_targets` and `regen::check_target` still call `emit_runtime_profile(target.profile)` and only hash source/metadata for freshness; JSON currently uses `emit_from_source("json", source)` through a separate `check-json` path.
Required folds:
- NONE
Evidence:
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:39` requires `regen-css` and every `check-css-l4-*` companion to use the new request path and removes `emit_runtime_profile(target.profile)` at the `regen.rs` boundary.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:41` binds JSON unchanged-output to `cargo xtask check-json`, whole-directory no-diff/hash proof, and an in-code equality test against `emit_from_source("json", source)`.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:42` requires Sheets and BBNF-self to use the same request path with named source-located fail-closed constructs or generated-role witnesses.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:61` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:81` name exact grammar/codegen tests, `check-json`, `regen-css`, all seven CSS companion commands, `gate-json`, and exact maintain no-diff.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:87` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:106` add fail-closed grep/count gates for the old profile call, request-path presence, provider/template counts, staged and unstaged provider/template rename/delete checks, LOC cap, and forbidden downstream path diffs.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:111` defines rejected-patch escrow and the one-slice revert, while `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:113` binds CSS, JSON, Sheets, and BBNF-self to same-wave source-request consumers.
- `restart/skinny/tranches/sk-v14/SPEC.md:660` through `restart/skinny/tranches/sk-v14/SPEC.md:668` require source+metadata codegen, CSS migration, JSON unchanged proof, Sheets/BBNF proof, Lock 14 guard, and all seven CSS companions as W5A tasks.
- `restart/skinny/tranches/sk-v14/SPEC.md:670` through `restart/skinny/tranches/sk-v14/SPEC.md:680` make those tasks W5A exit gates, including same-path CSS companions, JSON unchanged-output, Sheets/BBNF named proof, no new provider/template surface, and the W5A LOC cap.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-C-css-companion-emission.md:213` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-C-css-companion-emission.md:229` recommend one source-consuming request, replacing `emit_runtime_profile`, parsing CSS L4 constructs without `grammar_id == css_l4`, and proving every CSS companion traverses the new contract.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:72` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:100` require `check-json` to migrate through the same source+metadata request and remain byte-equivalent to the old JSON source path.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:131` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:141` identify the paper-close risks: JSON-only proof, freshness-only hashing, incomplete JSON roster, grammar-name CSS exceptions, and generic "unsupported" Sheets/BBNF claims.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-E-sheets-bbnf-witness.md:74` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-E-sheets-bbnf-witness.md:125` recommend same-contract Sheets/BBNF tests with named unsupported codes, source locations, source hash/offset assertions, and no reuse of handwritten witness artifacts.
- `skinny/xtask/src/regen_css.rs:25` through `skinny/xtask/src/regen_css.rs:75` enumerate all seven CSS L4 runtime targets and companion commands; `skinny/xtask/src/regen_css.rs:77` through `skinny/xtask/src/regen_css.rs:118` route regen and checks through `regen::write_targets` / `regen::check_target`.
- `skinny/xtask/src/regen.rs:14` through `skinny/xtask/src/regen.rs:33` show the current CSS regen/check call boundary still uses `codegen::emit_runtime_profile(target.profile)`, and `skinny/xtask/src/regen.rs:61` through `skinny/xtask/src/regen.rs:74` show source/metadata are currently hashed for freshness only.
- `skinny/xtask/src/main.rs:20` through `skinny/xtask/src/main.rs:33` expose `check-json`, `regen-css`, and the seven CSS companion commands; `skinny/xtask/src/main.rs:159` through `skinny/xtask/src/main.rs:165` show current `check-json` uses `emit_from_source("json", &source)`.
- `skinny/crates/codegen/src/lib.rs:98` through `skinny/crates/codegen/src/lib.rs:107` define the current JSON source-consuming path, while `skinny/crates/codegen/src/lib.rs:117` through `skinny/crates/codegen/src/lib.rs:120` define the profile-only runtime path W5A must remove from the xtask boundary.
