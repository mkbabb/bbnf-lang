---
lens: CH7 OVERFIT-PRUNE (V3)
pass: T-P2-research
cycle: V3
reviewed: [2a-sota-landscape, 2b-primitive-vocabulary, 2c-grammar-neutrality, 2d-cost-model, 2e-host-arch, 2f-fold-gaps]
master_head: 91b6893b0
t_p1_locked_sha: 445925167154de73540e3ea3283d0170371de790
contract: restart/prompts/totality/PASS-2-RESEARCH.md §3 + ORCHESTRATOR.md §3W/§3Z
focus: no contrivance; the fold is the genuinely-general tape model (not CSS/JSON-special-cased); lightningcss the fair bar; no fixture/FNV/broadcast re-entry
prior_cycle: V2 (41 ACCEPT, 0 REVISE, 0 REJECT — CONVERGED; CH7-001/002/003 DISCHARGED)
accept: 42
revise: 0
reject: 0
verdict: CONVERGED (2nd consecutive clean cycle — V2 + V3 both ≥95% ACCEPT; zero orphan REVISE; V≤5)
---

# CH7 OVERFIT-PRUNE — T-P2 SK-V17 V3

CH7 re-scans the V3 fold dossiers for its four overfit pathologies:
(1) **contrivance** — a fold dressed as general but structurally CSS/JSON-special-cased;
(2) **unfair-bar distortion** — lightningcss (the CSS >SOTA anchor) inflated, conflated,
or substituted by a non-comparable measurement; (3) **fixture/FNV/broadcast re-entry** — a
fold smuggling a per-corpus literal, a 24-row broadcast tuple, or an FNV/fixture runtime back
across the LOCKED skinny pre-blocks (L-SK17-04/05, SK17L-010); (4) **orphan-kernel /
by-exercise-overclaim** — a primitive or generality claim with no benched antecedent or no
non-JSON witness, asserted "proven" on construction alone.

**Headline. V3 converges CH7 clean — 42 ACCEPT, 0 REVISE, 0 REJECT — the SECOND consecutive
clean cycle (V2 41/0/0, V3 42/0/0).** The three V1 contrivance-folds (CH7-001/002/003,
discharged at V2) hold verbatim in V3 with zero regression: the fabricated "recognizer beats
lightningcss 2-3×" remains DELETED at source, replaced by the affirmative anti-claim at 2e:59
(``no "2-3×" CSS figure exists in SPEC/RESULTS/T-P1 and none is asserted here``), which is a
stronger guard than mere removal; the lightningcss bar is held at its true UNMEASURED-PENDING
posture across every touch-site (2a:56, 2e:57-58/:199-200/:413); and full typed-AST parity is
framed everywhere as the SK-V18 strict-equality GATE obligation, not a property held at this
pass (2e:63-64). The single new V3 fold — CH5-V2-001-COHORT-ALIGN, a retention-token
declaration-precision alignment across 2a/2e/2f — is NOT an overfit; it correctly
*disambiguates two distinct Lock-1 lifetime enums* rather than conflating them, which is the
opposite of a contrivance. No V3 fold introduces a new contrivance, re-opens a
fixture/FNV/broadcast pre-block, admits an orphan kernel, or silently adds a 6th BackendShape.
The by-construction-not-by-exercise honesty (2c refutation-#3) — the strongest overfit guard
in the set — is preserved verbatim and remains the genuinely-general proof.

**Hygiene (CH1-V5-001 first-action).** The dispatch directs folding the enumerated-filename
residual on first touch of 1a/1e. Re-verified live on-disk at 91b6893b0:
`grep ',collapsed}' {1a,1b,1e}*.md` = empty (exit 1, zero matches); `collapsed_stage.rs`
exists (the enumerated executing form, e.g. `skinny/crates/codegen/src/lower/collapsed_stage.rs`),
`collapsed_tape.rs` does NOT exist anywhere outside worktree shadows. The residual was
discharged at the T-P1 convergence commit and remains discharged; 2e:33 carries the identical
on-disk verification in its `hygiene_action_folded` line. No 1a/1e fold required. Hygiene
satisfied.

---

## V2-DISCHARGED contrivance-folds — re-verified load-bearing-correct at V3 (no regression)

### CH7-001 (fabricated "2-3×" CSS beat) → HELD-DISCHARGED-ACCEPT

Repo-wide `grep '2-3×|2-3x|beats lightningcss|recognizer beats'` across all six V3 dossiers
returns exactly three lines, all correct: (a) the CH7-001 disposition-log line (2e:26); (b)
the affirmative anti-claim (2e:59); (c) the correct sonic-rs re-citation (2e:410, "the proven
JSON recognizer beats sonic-rs on the same plane, RESULTS.md:5-55"). The exec summary
(2e:50-65) cites the recognizer fact to its TRUE measured antecedent — Track 1 cold > sonic-rs
strict on the same plane, with the actual corpus numbers (twitter 8349 > 4913, citm 33366 >
21250, canada 16709 > 12970, RESULTS.md:5-55) — and explicitly marks the CSS surface
UNMEASURED-PENDING. The fabricated CSS magnitude is gone; the anti-claim survives verbatim.
ACCEPT.

### CH7-002 ("full parity with lightningcss" as held property) → HELD-DISCHARGED-ACCEPT

Parity is the SK-V18 strict-equality GATE obligation everywhere it appears in 2E:
exec summary (2e:63-64, `css_typed_summary_equal=true` before speed, SPEC:129, "not a
property held at this pass"), FOLD-2E-C surface, and the LAC. preserve-rich-ast is the
non-negotiable TARGET, never an asserted result. 2a:384 carries the same obligation form
("a non-negotiable TARGET on the UNMEASURED-PENDING CSS surface, SPEC:207"). ACCEPT.

### CH7-003 (three-fact conflation in 2A) → HELD-DISCHARGED-ACCEPT

2A keeps the three facts cleanly separated (2a:50-56): (a) the JSON recognizer measured fact
(the only >SOTA-witnessed plane); (b) the materialization gap as a CODE-SHAPE divergence
(eager `OpenFrame`, SPEC:791 — NOT a benchmarked CSS deficit, lightningcss UNMEASURED-PENDING
SPEC:207); (c) full typed-AST parity as the SK-V18 strict-equality GATE. The "2-3×" number is
absent from 2A. ACCEPT.

---

## V3 NEW fold — the retention-token COHORT-ALIGN is NOT an overfit (ACCEPT)

The one new V3 fold is CH5-V2-001-COHORT-ALIGN (2e:30), a cross-dossier declaration-precision
alignment of the V2 retention-token discipline onto 2E's classifier surfaces (FOLD-2E-E +
LAC-2E-SKV17-02), aligning with sibling 2a LAC-2A-SKV17-02 (:417) and 2f-F5 (:295).

CH7's scan: this fold does NOT smuggle a corpus literal, a fact-profile, or a special-cased
runtime. It is the OPPOSITE of conflation — it correctly distinguishes **two distinct Lock-1
lifetime enums** that the V1 spec defines separately, verified live at LOCKS.md:
- the **substrate-manifest** enum `retention_lifetime ∈ {local_loop, generated_function,
  output_row}` (LOCKS.md:122-123) — applied to tape members (`substrate_target=existing_tape`,
  `retention_lifetime=output_row`, 2e:167);
- the **primitive** enum `retention_lifetime ∈ {transient-single-call, retained-within-chunk,
  …}` (LOCKS.md:146-148) — applied to the classifier primitive
  (`retention_lifetime=transient-single-call`, 2e:76/:96, "no cross-call retained classifier").

2e:173 carries the explicit non-conflation guard: ``retention_lifetime is the OTHER enum —
see FOLD-2E-E; never conflated``. The fold tightens a declaration toward a grammar-neutral
transient-single-call lifetime (no retained per-grammar classifier state, which is precisely
the anti-overfit posture); it carries no per-grammar or per-corpus datum. ACCEPT — a hardening
fold that strengthens the generality guard, not a contrivance.

---

## ACCEPT census — the fold remains genuinely general (no V3 regression)

### Grammar-generality vehicle (the anti-contrivance core) — ACCEPT (unchanged)
- **Type-parameterised value plane** (`ValueRef<…,G:EventGrammar>`, 2c; 2a; 2e:182; 2f:75).
  Grammar enters as a TYPE param monomorphised at codegen, zero runtime `match grammar`. JSON
  and CSS instantiate the SAME cursor type. The structural proof the fold is not special-cased.
  ACCEPT.
- **Alphabet-as-data classifier** (`select_classifier(alphabet:&[u8;64])` /
  `scan_structural(input,&StructuralAlphabet)`, 2c:85; 2b:92; 2e:124; 2f-F5). simdjson/sonic
  classify a FIXED JSON alphabet; bbnf takes the alphabet as `[u8;64]`/`StructuralAlphabet`
  DATA mined per grammar (Lock 14, LOCKS.md:418-423). Wired scan-leaf across 8 core grammars
  (1B BSHAPE17-009). CSS exercises a REAL NEON body (the `;{` slot-59 `& 0x3f` collision routes
  to the eq-set fan, the one proven aarch64 Layer-1 body, byte_class_from_eq_set_64.rs:33), not
  a scalar delegate. ACCEPT.
- **2c-ONBOARD future-grammar onboarding** (2c:262-295) — an executable verify_action with a
  live HEAD baseline at 91b6893b0: Predicate 1 = 7 string-ident/doc leak sites in strategy.rs
  (the catalogued ARCH-3A-D09 surface, monotonic-decrease); Predicate 2 = 9 @generated grammar
  dirs + tape/ (CH6-V2-COUNT-NIT corrected 8→9 to ground-truth `find ... -type d` = 9). The
  Lock-14 falsifier operationalising "not special-cased." ACCEPT.

### lightningcss-bar honesty — ACCEPT (load-bearing-correct, held from V2)
- **2c refutation-#3 — fleet-wide claim REFUTED on JSON+CSS exercise alone** (2c:70, :92).
  `sheets_witness` is a 24-LOC `EventGrammar` stub with no `.bbnf`/`BackendRule` (1D
  SK17L-009); the fold's grammar-neutrality is breadth-of-CONFIG, proven by-exercise on
  JSON+CSS only; Lock 14 v+1 scopes the wording (LOCKS.md:382-387). The
  by-construction-not-by-exercise honesty — the strongest overfit guard. ACCEPT.
- **2e lightningcss bar UNMEASURED-PENDING** (2e:57-58, :199-200, :413). No inflation, no
  conflation with the JSON-vs-sonic margin; the bar is held, not asserted. ACCEPT.

### fixture / FNV / broadcast pre-block integrity — ACCEPT (no re-entry)
- **2b FOLD-L7 one-shot capacity** (2b:313-314): sizes the EXISTING `offsets` vector from the
  L1 scan count, "no per-corpus capacity literal," binds Lock 8 + cites the L-SK17-05
  FNV/fixture-overfit fence (2b:314). ACCEPT.
- **2b FOLD-L8 sparse-flag side-table** (2b:332-333): kind-disambiguation rides `ValueRef<G>`,
  EXERCISED on JSON+CSS only, the 24-LOC `sheets_witness` stub named; the row may NOT and does
  NOT claim fleet-wide generality; the `W5C_REQUEST_FACT_PROFILES`-into-flag-form regression
  stays pre-blocked. ACCEPT — broadcast/fact re-entry fenced.
- **2d cost-model fail-closed** (2d:287, UNKNOWN-2D-S17-02): the model "fails closed on
  grammar-named (`json_*`/`css_*`) or broadcast/stale evidence" (LOCKS.md:599). ACCEPT —
  broadcast pre-block holds.

### orphan-kernel / by-exercise discipline — ACCEPT (no orphan admitted)
- **2b udot/i8mm refusal** (2b:166): grammar-neutral + admissible for number-heavy grammars
  BUT requires a same-wave consumer with a profiled antecedent — CSS has none, so "No orphan
  kernel" (SPEC §6 pre-block :655 honoured). ACCEPT.
- **2b FOLD-L9 DEFERRED demotion** (2b:344-351): the wired enumeration ends at L8; L9 is
  DEFERRED-PENDING-REPROFILE, demoted out of the wired set so T-P3 does not read it as
  shortlisted. ACCEPT — exemplary anti-orphan posture, held from V2.

### no silent 6th BackendShape — ACCEPT (consistently REFUTED)
- **D tape-as-substrate-manifest** (2a; 2c refutation-#1; 2d:47-98 FOLD-2D-01; 2e FOLD-2E-D;
  2f-F4 grounded / F4d refuted). The LAC-1E-14 FactStream precedent applied (LOCKS.md:100-109);
  the no-6th-shape verdict carries the INDEPENDENT corroborating anchor `admits_collapsed_stage`
  x86-bound (ARCH:1206 — mechanically refuses on aarch64). "Propose, do NOT silently add a 6th"
  satisfied by the substrate-manifest category form (2d:74, "A 6th shape remains G-Omega gated,
  LOCKS:107-109"). ACCEPT.

### StructRegistry/FieldSource fence — ACCEPT (regression firewall inviolate)
- **F per-leaf walk REFUTED, compile-time projection-emission GROUNDED** (2c refutation-#2;
  2d:77/:213-228 FOLD-2D-06; 2f-F6:331-359 grounded / F6b refuted). The
  28-65×/983×/10583× regression (SPEC:793-795) pre-blocked; `begin_compound` reads
  `layout.rule_id & 0x1F` only (tape/mod.rs:186, grep-zero StructRegistry — re-verified live);
  the LIVE coupling-site `arena.rs:47 StructRegistry::compound_kind_for_layout(layout)` named
  as the present wire FOLD-B severs (CH5-V1-003 shared fold across 2a/2d/2e/2f). ACCEPT.

---

## Provenance spot-checks (CH7-owned citations, live at 91b6893b0)
- `crates/core/src/runtime/bbnf/arena.rs:47` → `StructRegistry::compound_kind_for_layout(layout)`
  in the eager arena path — exists, the live coupling-site FOLD-B severs. EXACT.
- `crates/core/src/runtime/tape/mod.rs:186` → `begin_compound` reads `layout.rule_id & 0x1F`
  only; grep-zero StructRegistry — fence intact. EXACT.
- `restart/locks/LOCKS.md:122-123` (substrate enum) vs `:146-148` (primitive enum) — two
  distinct lifetime enums; the V3 COHORT-ALIGN applies the correct one to each surface, no
  conflation. EXACT.
- `SPEC:122` → `lightningcss full-CSSOM … THE fair >SOTA bar … the only strict admission
  anchor` — verbatim.
- `SPEC:207` → `ALL per-corpus lightningcss endpoints are UNMEASURED-PENDING` — verbatim.
- Repo-wide `grep` for the fabricated "2-3×" beat → only the disposition-log + anti-claim +
  correct sonic re-citation; no live false claim survives.
- Hygiene: `grep ',collapsed}' {1a,1b,1e}` = empty; `collapsed_stage.rs` exists,
  `collapsed_tape.rs` does not. CH1-V5-001 discharged.

---

## CH7 V3 disposition summary

| ID | Dossier:line | V2 disposition | V3 status | Note |
|---|---|---|---|---|
| CH7-001 | 2e:26,:59,:410 | DISCHARGED-ACCEPT | HELD-ACCEPT | Fabricated "2-3×" still deleted; anti-claim + sonic re-cite verbatim |
| CH7-002 | 2e:63-64; 2a:384 | DISCHARGED-ACCEPT | HELD-ACCEPT | Parity = SK-V18 strict-equality GATE obligation everywhere |
| CH7-003 | 2a:50-56 | DISCHARGED-ACCEPT | HELD-ACCEPT | JSON-fact / code-shape-divergence / parity-obligation cleanly split |
| CH5-V2-001-COHORT-ALIGN | 2e:30,:76,:96,:167,:173 | (V2 REVISE on 2B) | ACCEPT (new fold) | Retention-token disambiguation across two distinct Lock-1 enums; anti-overfit, not contrivance |
| Generality vehicle | 2c:85; 2b:92; 2e:124 | ACCEPT | CARRIED-ACCEPT | Type-param plane + alphabet-as-data, no regression |
| 2c refutation-#3 | 2c:70,:92 | ACCEPT | CARRIED-ACCEPT | by-construction-not-by-exercise honesty preserved verbatim |
| FNV/broadcast/fixture | 2b:313-333; 2d:287 | ACCEPT | CARRIED-ACCEPT | All pre-blocks intact, no re-entry |
| udot/L9 anti-orphan | 2b:166,:344-351 | ACCEPT | CARRIED-ACCEPT | No orphan kernel; L9 DEFERRED |
| no-6th-shape | 2d:74; 2f-F4 | ACCEPT | CARRIED-ACCEPT | Substrate-manifest category; admits_collapsed_stage x86-bound |
| StructRegistry fence | 2f-F6; 2d:213-228 | ACCEPT | CARRIED-ACCEPT | begin_compound 0x1F; arena.rs:47 live wire named |
| 2c-ONBOARD | 2c:262-295 | ACCEPT | CARRIED-ACCEPT | verify_action with live HEAD baseline; CH6 count-nit corrected 8→9 |

**Counts: 42 ACCEPT, 0 REVISE, 0 REJECT.** Zero orphan REVISE. CH7 verdict: **CONVERGED**
— the SECOND consecutive clean CH7 cycle (V2 + V3). The fold is the genuinely-general tape
model — type-parameterised, alphabet-as-data, by-construction-honest, lightningcss-bar-
faithful, with every fixture/FNV/broadcast/orphan/6th-shape pre-block intact and the
StructRegistry/FieldSource regression fence inviolate. The three V1 contrivances stay
eliminated at source; the one new V3 fold strengthens (not weakens) the generality guard.
CH7 raises no new defect.
