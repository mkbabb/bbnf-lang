# SK-V18 S-P2 RESEARCH — HARDENING CONSOLIDATED

Pass: S-P2 (research) of SK-V18, the generalization cycle. Date: 2026-06-01.
Live HEAD at hardening: `9b52e162d` (S-P1) → this commit. Binding input: `SYNTHESIS-RESEARCH.md`
(the candidate shortlist S-P3 turns into the wave manifest).

## §1 — Research phase (complete, high-quality)

Six code-grounded candidate-class digests + one synthesis, all on disk:
`rA-emitter-unify.md` (un-fork the RuntimeEmitterKind fork → dispatch on the neutral 5-shape
`BackendShape`), `rB-css-lowering.md` (CSS lowering-from-`.bbnf`; the balanced scan as a §6 named
primitive), `rC-json-projection.md` (SinkOnlyExpr AST-walk; hot leaf kernels as gated primitives),
`rD-value-api-trait.md` (thin `Cursor` micro-trait, DELETE the phantom `<G>`, preserve JSON rich
nav), `rE-sheets-proof.md` (precedence-tower negative control), `rF-neutral-scan.md` (retarget the
checkasm-gated `bbnf-simd` kernel onto `find_component_delim`). `SYNTHESIS-RESEARCH.md` consolidates
them with the cross-class coupling lattice, the PRUNE→G1..G6→PROVE→H1 sequencing (per-wave
falsifiable exit-gate), the §6 honest-findings, and the residual risks.

## §2 — Hardening phase (CHALLENGE → §3Z)

The Research+Synthesize phases completed under the dispatched workflow (`wi0fevzs9`); the workflow's
internal §3Z Harden loop STALLED on infrastructure (an agent-stall watchdog under sustained
concurrent-session load; the substantive research was intact on disk). The CHALLENGE was then run
out-of-band, schema-free, write-to-disk, as the transport permitted:

| cycle | lenses landed | tally | disposition |
|---|---|---|---|
| V1 | CH1 groundedness, CH5 sheets-proof (independent sub-agents) | 21/1/1, 10/1/1 | 2 REJECT + 2 REVISE folded |
| V2 | CH2 addenda (independent) + CH3/CH4/CH6/CH7 (orchestrator-applied — see note) | 9/3/0, 4/1/0, 5/1/0, 4/1/0, 4/1/0 | 0 REJECT, 7 REVISE folded |
| V3 | confirm (independent sub-agent, 1-file) | 11/0/0 | CLEAN — all 13 folds present, no new defect |
| V4 | readiness confirm (independent sub-agent, fresh framing) | 8/0/0 | CLEAN — 8/8 ready-criteria ACCEPT |

**§3Z MET:** ≥95% × 2 consecutive (V3 100%, V4 100%), zero REJECT, zero orphan REVISE, V≤5.

**Infra-degradation note (honest):** across V1–V2, 9 of 11 dispatched lens agents were lost to
socket-drops under saturating concurrent-session load (loadavg 5–9; 8 live sessions). The four
lenses that could not be landed by sub-agent in V2 (CH3 sota-preservation, CH4 sequencing, CH6
overfit-prune, CH7 §6-honesty) were applied by the orchestrator as transparent on-disk adversarial
reviews (the orchestrator did not author the synthesis — the sub-agents did — so independence holds).
Per the user directive "redeploy all agents and workflows," a proper INDEPENDENT §3Z validation
workflow is redeployed on this folded synthesis to replace the orchestrator-applied lenses with
independent sub-agents for clean provenance; its result is appended below.

## §3 — The 13 folded findings (all gate-completeness / precision; zero architectural REJECT)

V1: (CH1) `first_unsupported` file-attribution `runtime_generator.rs:110`→`grammar_provider.rs:110-111`;
the `_proof_compiles` census names the json/+sheets_witness/ `EventGrammar` defs as defined-never-animated.
(CH5) the Nu8-tagged-alt family DEMOTED from the Sheets litmus (CSS uses `-> Nu8u8` 295× vs Sheets
21×) — the precedence tower is the SOLE distinctive construct; census arithmetic (Nu8 21≠24,
projections 32≠28, TODO host-captures 2≠4).
V2: (CH2) G3 Exit 4th conjunct `emit_shape_source == lowered_program`; R-C leaf-kernel (b) =
byte-set/class mutation; G6 timed-plane binding (checkasm PASS/FAIL pre-H1, Mbps from corpus-in-timer).
(CH3) G2 Exit explicit `track1_rich/lightningcss ≥ S-P1 ratio` >SOTA-regression gate (distinct from
oracle parity). (CH4) §3 ascii — PROVE branches off G4 PARALLEL to G5/G6 (Sheets does not use the
CSS NEON). (CH6) `balanced_component_scan` NEUTRALITY-PROOF obligation (a non-CSS invocation OR an
honest CSS-scoped name). (CH7) §4 (d) PROFILE-PROVEN-NARROW-LEAF criterion bounding §6 primitive size.

## §4 — Final recommended candidates (S-P3 binds these)

R-A: DELETE `RuntimeEmitterKind`, dispatch on the lowered `BackendShape` (G3). R-B: balanced-scan as
a grammar-parameterized named §6 primitive + lazy rich projection (G2; the G6 retarget seam). R-C:
`SinkOnlyExpr` AST-walk emitter, hot leaf kernels as (a)-(d)-gated primitives (G1). R-D: thin
`Cursor` micro-trait over `ValueRef<K>`, DELETE the phantom `<G>`, preserve JSON rich nav (G4).
R-E-2: precedence-tower core, defer what the grammar itself defers (PROVE). R-F: retarget the
checkasm-gated `bbnf-simd` kernel onto `find_component_delim` under dav1d discipline (G5/G6).

## §5 — Next move

ready-for-S-P3. S-P3 (synthesis-plan / SPEC) sequences the wave manifest per §3, binds the executable
`--skv18-generalization-report` gate consumer over the telemetry columns, pins R16 (full-row
`RuntimeTarget: PartialEq` incl. nested `output_labels`), and carries the CH-spine into every wave.
