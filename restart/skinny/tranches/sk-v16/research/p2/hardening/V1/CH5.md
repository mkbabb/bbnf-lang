# S-P2 V1 CH5 Hidden Coupling

Disposition: ACCEPT.

Lock 1 coupling is explicitly preserved:

- retained structural projection must be the tape;
- transient masks are local to a chunk call or direct tape/sink emission;
- retained sidecars, parser-owned structural streams, retained cursor/list,
  public `UnionTape`, second tape, and class-column producers are rejected.

P2-D records that no committed lazy-materialisation ratio table exists and
routes that gap to an evidence-only ratio report using existing stats helpers.
It does not invent metrics and does not propose a second substrate.

Track 1/Track 2 sidecar dishonesty remains excluded. CSS legacy fact-stream
proof and FNV/checksum loops are rejected as primitive sources.
