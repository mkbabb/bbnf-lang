# CH2 GENERALITY — Pass Alpha SK-V17 (cycle V3)

Lens: CH2 Generality (PASS-ALPHA §3). Reviewer focus: does the goalset + every
candidate respect **Lock 14 grammar-neutrality** — works for JSON / Sheets /
BBNF-self, not just CSS, OR is honestly re-framed as a per-grammar template
surface; is the unified tape / layout / projection grammar-general?

Host: aarch64 Apple M5 Max only. HEAD of record: `1c5bd7a25` (`git rev-parse`
re-confirmed). Every disposition cites `path:line` / measured fact.

Subjects reviewed: `research/alpha/alphaA..alphaE.md`, `SYNTHESIS.md`,
`HANDOFF.md`. (No `alphaF-contract-draft.md` exists at `research/alpha/`; α-F's
output IS `SYNTHESIS.md` + `HANDOFF.md` per PASS-ALPHA §2 row α-F.)

Method: V3 folds the V2 CHALLENGE dispositions. CH2 V3 does NOT re-litigate by
trust the single load-bearing V2 finding (CH2-V2-F1, the non-dischargeable
`sheets_witness` projection EXIT gate, surfacing in six sites). It
**re-verifies the (b′) fold against the codebase at each of the six sites**, then
adversarially scans the newly-introduced V3 surfaces — the 6→24 broadcast-count
reconciliation, the `css_l4.toml`-is-totality SK-V18 demotion, and the split of
the telemetry generality column into `projection_generality_exercise` vs
`simd_non_json_exercise` — for fresh generality defects.

---

## §0 — V2 fold re-verification (CH2-V2-F1, all six sites + the affirmed-clean surfaces)

CH2-V2 found a single-root, orphan-free REVISE set: CH2-V2-F1 surfacing at
`SYNTHESIS:101`, `SYNTHESIS:215-224`, `SYNTHESIS:331/335`, `alphaE:189-197`,
`alphaE:475`, `alphaD:145`. The V2 directive was the (a′/b′) disjunction-honesty
fix. V3 adopted **option (b′)** uniformly (alphaE V3 changelog `:12-30`). Each
site is re-greped against `skinny/crates/` at `1c5bd7a25` and read in the V3
artefacts.

| V2 REVISE site | (b′) fold in V3 | Codebase re-verification | Verdict |
|---|---|---|---|
| `SYNTHESIS:101` layout-projection gate | `SYNTHESIS:105` now closes: "`sheets_witness` is a 24-line `EventGrammar` byte-classification witness with NO `.bbnf` source / parser / `BackendRule` shape to walk, so it CANNOT serve as a projection-generator exercise; non-CSS-non-JSON projection generality is asserted-by-construction with proof deferred to SK-V18" | `find runtime/src/grammars/sheets_witness` = 2 files (`event_grammar_witness.rs` 24 LOC + `mod.rs` 1 LOC) = **25 LOC**; `find skinny -name '*.bbnf'` = **only `skinny/grammars/json.bbnf`**; codegen fail-closes `google_sheets`/`bbnf` via `w5a_sheets_bbnf_fail_closed_through_runtime_contract` (`lib.rs:1075-1090`, verified) | **FOLDED — verified** |
| `SYNTHESIS:215-224` §0.4 generality clause | `SYNTHESIS:234-257` rewritten: exercised projection riders are "**JSON + CSS only**", `sheets_witness` is "NOT a viable third exercise", and explicitly "the contract does NOT claim the Lock 14 CSS+Sheets minimum is *met* in SK-V17 — that minimum is the SK-V18 close-out" | Lock 14 phrase #2 (`LOCKS.md:386-387`) reads verbatim: "With only one of Sheets or BBNF-self, the claim is scoped to the witnessed grammars and may not use fleet-wide grammar-neutral wording." V3 witness is JSON+CSS (NOT Sheets) ⇒ correctly does not invoke the CSS+Sheets minimum as met | **FOLDED — verified** |
| `SYNTHESIS:331/335` telemetry generality column | Split into TWO columns: `projection_generality_exercise` (`:365`, value `json`\|`css_l4`, "`sheets_witness` is NOT a valid value here, it has no `BackendRule` to project from") + `simd_non_json_exercise` (`:369`, value `css_l4`, "the dischargeable non-JSON SIMD exercise, distinct from the projection-generality column above") | This is EXACTLY the CH2-V2 §2-Section-2 directive: keep `simd_non_json_exercise = css_l4` (sound — CSS+JSON share the `select_classifier(alphabet)` kernel), and forbid the projection column from citing `sheets_witness`. `select_classifier(alphabet)` `dispatch.rs:42`, `lo6_table_admissible` `:101` verified | **FOLDED — verified, and the split is sharper than the V2 ask** |
| `alphaE:189-197` C1 EXIT gate | `alphaE:216-235` rewritten: "**The exercised projection riders are JSON + CSS ONLY**… `sheets_witness` is NOT a dischargeable projection target in SK-V17… a real Sheets value-grammar rider with its own `.bbnf` + `BackendRule` is an SK-V18 fold target, not an SK-V17 exit gate" | `value_from_ref` is `// @generated by skinny bbnf-codegen` (`json/value.rs:1,143`), emitted from grammar shape (json.bbnf has **0** `->` arms — `grep -c '\->'` = 0 — so the generator walks the BackendRule shape, not `->` arms; CH2-V2-F2 affirmed this is genuine) | **FOLDED — verified** |
| `alphaE:475` §3 falsifiability discipline | `alphaE:506-524` rewritten: "The exercised projection riders are JSON + CSS ONLY… [`sheets_witness`] is NOT a dischargeable projection target… **No candidate may claim four-grammar or fleet-wide generality; each binds its witness to JSON+CSS explicitly, with non-CSS-non-JSON projection deferred to SK-V18**" | The V3 changelog `:45-48` explicitly marks the V2 "emit for `sheets_witness`" clause as **[SUPERSEDED by V3]** — a clean, honest supersession note, not a silent rewrite | **FOLDED — verified** |
| `alphaD:145` (O2) | `alphaD:151` (O2 row) rewritten: "**Generality is exercised on CSS+JSON only — sheets_witness is NOT a projection target**… A `ValueRef`-cursor lazy view over `sheets_witness` is therefore structurally non-dischargeable, NOT a live disjunction… non-CSS-non-JSON projection… is an explicit **SK-V18 fold target**" | `alphaD:54-65` (validated-witness discussion) and `alphaD:210` (closing generality bullet, "by-construction, witnessed only when the generator emits a non-CSS-non-JSON rider — Lock 14") are consistent; no residual live-disjunction language | **FOLDED — verified** |

**All six V2 REVISE sites are correctly discharged via option (b′) and verified
against the codebase at `1c5bd7a25`.** The V3 fold is real, not paper: the
sheets_witness non-dischargeability is grep-proven (25 LOC, no `.bbnf`, no
`BackendRule`, fail-closed at `lib.rs:1075-1090`), and the contract now honestly
scopes the witness to JSON+CSS, explicitly declining to claim the Lock 14
CSS+Sheets minimum is met (deferring it to SK-V18 per Lock 14 phrase #2).

### The affirmed-clean Lock-14 surfaces (carried from V1/V2, re-verified)

- **C2 NEON neutrality vehicle** (`select_classifier(alphabet)` + `lo6_table_admissible`,
  `dispatch.rs:42,101`): re-verified. `lo6_table_admissible` (`dispatch.rs:101-115`)
  walks the alphabet, checks each byte's `& 0x3f` slot for collision, returns
  `false` (→ `SelectedBackend::Scalar`) on collision — the honest grammar-general
  scalar-fallback gate. The alphabet is the only grammar datum; no CSS branch in the
  kernel. JSON exercises it (`json/scan.rs:22` `scan_structurals`, `:32`
  `scan_structurals_scalar`). This is Lock 14 phrase #3 honored exactly.
- **C4a/C4b number-leaf primitive**: udot `parse_4_digits_dotprod` orphan
  (`digit_mac.rs:27,40`) is a grammar-general digit-block decode; i8mm is
  **grep-clean-absent from skinny** (re-verified: `grep -rn i8mm skinny/crates/` = 0)
  and correctly gated behind a re-profile (no orphan kernel).
- **C0/C1 derive-from-`.bbnf`-rule no-relocated-overfit pre-block** (`alphaE:259-263`,
  `SYNTHESIS:200-201`): `W5C_REQUEST_FACT_PROFILES` is live at `codegen/src/lib.rs:336`
  (const), iterated `:567,:611`, selected `:299`; the seven `RequestFactsProfile`
  literals carrying `emitter: RuntimeEmitterKind::RequestFacts` are at
  `regen_css.rs:45,63,81,99,117,135,153` (verified, all seven), regenerated via
  `regen_css` `:164`. The retire-and-derive-from-grammar-shape framing is the sharpest
  Lock-14 anti-overfit gate in the cohort.

---

## §1 — Fresh V3-surface scan (defects introduced by the V3 edits)

V3 made three edits beyond the (b′) fold: the 6→24 broadcast reconciliation, the
`css_l4.toml`-is-totality demotion, and the telemetry column split. Each is scanned
for a NEW generality defect.

### CH2-V3-S1 — the `css_l4.toml` SK-V18 demotion is generality-honest (ACCEPT)

V3 added (`SYNTHESIS:105`): "The 594-vs-34-line `css_l4.toml`-vs-`json.toml`
asymmetry is a TOTALITY artefact — `css_l4.toml` is grep-clean-absent from
`skinny/`, it lives only at the repo-root totality tree — so its LOC convergence is
an SK-V18 totality-fold metric, INFORMATIONAL only, NOT an SK-V17 close gate." Also
`alphaD:39` and `alphaD:154` (O5 retire-list). Re-verified: `find skinny -name
'css_l4.toml'` = **0**; the file lives only at `xtask/runtime-projections/css_l4.toml`
(repo-root totality, + worktree copies); `json.toml` likewise only at the totality
root. **This is the correct generality call:** gating an SK-V17 close on the
LOC-convergence of a non-benched totality file would be the wrong-tree dishonesty
the contract elsewhere REJECTs. Demoting it to an INFORMATIONAL SK-V18 totality-fold
metric (while keeping the *substantive* anti-overfit gate — "every residual CSS
routing entry names its `.bbnf` rule, the regen profile array trends toward the JSON
shape", which IS skinny-greppable) is generality-clean. **ACCEPT.**

### CH2-V3-S2 — the telemetry column split is the correct generality taxonomy (ACCEPT, and commend)

V3 split the single V2 generality column into two
(`projection_generality_exercise` `:365` + `simd_non_json_exercise` `:369`). This
is a *strictly better* generality artefact than the V2 ask: it disentangles the two
distinct generality axes that the V2 finding conflated. The projection-generator
axis (which walks a `BackendRule` shape) is honestly bound to `{json, css_l4}` and
explicitly forbids `sheets_witness` as a value; the SIMD-leaf axis (which routes a
byte alphabet through `select_classifier`) is honestly bound to `css_l4` as a real
dischargeable non-JSON exercise. The two columns cite each other ("distinct from the
projection-generality column above" / "distinct from the SIMD-leaf generality"),
foreclosing the conflation. This is the cleanest expression of the Lock-14
two-axis-generality distinction in the cohort. **ACCEPT.**

### CH2-V3-S3 — the 6→24 broadcast reconciliation introduces no generality claim (ACCEPT)

The 6→24 reconciliation (`SYNTHESIS:139-157`, `HANDOFF:48-53`) is a CH1-correctness
edit, not a generality edit. Re-verified: `grep -c '^| css_l4/.*/direct_to_struct/main '
skinny/RESULTS.md` = **24**. The substantive generality-relevant conclusion (zero
ADMITTED typed CSS rows; the broadcast is pre-blocked) is unchanged by the count
correction, and the pre-block (`SYNTHESIS:202-204`, `HANDOFF:158-160`) does not
introduce any per-grammar special-casing. No generality surface touched. **ACCEPT.**

---

## §2 — Per-section dispositions

### alphaA — results extraction

- **§0 tree-correction + core-tree-is-fold-target (`alphaA:48-70`)** — ACCEPT.
  Finding-A discharge stable; totality symbols grep-clean (`StructLayout`/`OpenFrame`/
  `CssArena`/`TapeStructBuilder`/`begin_compound` = 0 in `skinny/crates/`, re-verified).
- **§4 O(1)-checkpoint genericity (`alphaA:203,217-218`)** — ACCEPT. "generic across
  all grammars, no CSS special-case" (container-keyed scratch hoist) is the Lock-14
  template.
- **§7 lever neutrality vehicle (`alphaA:349-356`)** — ACCEPT. `:354` "JSON/CSS/Sheets/
  BBNF share this one scanner vocabulary" is the *alphabet-keyed scanner abstraction*
  (the `select_classifier(alphabet)` vehicle, `dispatch.rs:42`/`:101` verified), where
  the alphabet is the only grammar datum — NOT a claim all four are exercised. Lock 14
  phrase #3 respected; CH2-V2 ACCEPTed this exact line and V3 did not weaken it.

### alphaB — competitor deltas

- **§0/§2/§3/§4 plane taxonomy + deltas** — ACCEPT. lightningcss-full-CSSOM-fair-bar /
  cssparser-token-scan-plane-mismatch is grammar-domain-honest. No generality defect.
- **§5 JSON comparator guard (`alphaB:254-263`)** — ACCEPT. The shared-substrate-touch
  → re-run-JSON-strict tripwire is the cross-grammar regression guard CH2 wants.

### alphaC — REDRESS digest

- **§intro core-tree-vs-skinny map (`alphaC:29-34`)** — ACCEPT, and commend. Correctly
  cites Lock 2 (`LOCKS.md:160`) retiring `StructLayout` → `Layout`/`LayoutFacts`.
- **§2b/§5b/§3 fact-stream + W5C + fixture pre-blocks (`alphaC:348,352`)** — ACCEPT.
  The "emitter derives tape ops from `BackendRule`/`LayoutFacts` shape, NOT a hand-coded
  per-grammar profile table" gate is grammar-neutral and load-bearing.
- **§4-§8 broadcast / FNV / x86 / ledger** — ACCEPT. aarch64-NEON-only is alphabet-keyed
  not grammar-keyed.

### alphaD — validated/invalidated ledger

- **§intro + §validated witness discussion (`alphaD:32-34,54-65`)** — ACCEPT. The
  core-tree→skinny substrate map and the "only `json` and `sheets_witness` carry an
  `EventGrammar` witness; sheets_witness has no shape to lower; the only exercised
  projection riders are JSON + CSS" framing is Finding-B/CH2-V2-F1-clean.
- **§3 O1 tape wiring (`alphaD:150`)** — ACCEPT. "Generality is JSON-WITNESSED only…
  Sheets/BBNF are by-construction-not-by-exercise" + the anti-relabel pruning gate is
  Lock-14 clean.
- **§3 O2 lazy view (`alphaD:151`)** — **ACCEPT (V2 REVISE discharged).** The
  CH2-V2-F1 (b′) fix landed verbatim: "Generality is exercised on CSS+JSON only —
  sheets_witness is NOT a projection target… structurally non-dischargeable, NOT a
  live disjunction… non-CSS-non-JSON projection… is an explicit SK-V18 fold target."
- **§3 O3 NEON / O4 spine / O5 codegen unification (`alphaD:152-154`)** — ACCEPT.
  `select_classifier`/`lo6_table_admissible` neutrality vehicle verified; O5 retire-list
  carries `W5C_REQUEST_FACT_PROFILES` (`lib.rs:336`) + the css_l4.toml-is-totality
  demotion (CH2-V3-S1).
- **§closing generality bullet (`alphaD:205-214`)** — ACCEPT. "Every candidate must
  generalize beyond CSS to JSON (witnessed today) / Sheets / BBNF-self (by-construction,
  witnessed only when the generator emits a non-CSS-non-JSON rider — Lock 14)" is the
  honest witnessed posture.

### alphaE — candidate shortlist

- **§0 ground-truth + architecture-doc translation (`alphaE:37-118`)** — ACCEPT, and
  commend. The 148-fn (not 187) and lightningcss-833 (not 974) corrections are honest;
  the sheets-witness ground-truth row (`:82`) states the non-dischargeability inline.
- **C0 de-fact-stream + W5C retirement (`alphaE:130-181`)** — ACCEPT. Finding-C folded;
  derive-from-BackendRule pre-block is Lock-14 phrase #1 respected.
- **C1 tape wiring + generality EXIT gate (`alphaE:183-263`)** — **ACCEPT (V2 REVISE
  discharged).** The EXIT gate (`:216-235`) now reads "The exercised projection riders
  are JSON + CSS ONLY… `sheets_witness` is NOT a dischargeable projection target…
  non-CSS-non-JSON projection… is an SK-V18 fold target." The no-relocated-overfit /
  derive-from-`.bbnf`-rule pre-blocks (`:259-263`) are the Lock-14 anti-overfit
  discipline.
- **C2 NEON structural pre-scan (`alphaE:265-329`)** — ACCEPT, and commend. The
  strongest grammar-neutral candidate: reuses `select_classifier`/`PrimitiveKernels`/
  `lo6_table_admissible` (`dispatch.rs:42,50,101` verified), falls back to scalar
  honestly on lo6 collision (NOT a CSS special-case), forbids "CSS-specific scanner
  vocabulary" (`:326-327`). JSON is the genuine non-CSS witness (`json/scan.rs:22,32`).
- **C3 commit-by-construction spine (`alphaE:331-377`)** — ACCEPT. Non-depositing-Alt
  detection is a generic codegen property exercised on JSON+CSS, grammar-shape-driven.
- **C4a udot orphan wiring (`alphaE:379-410`)** — ACCEPT. Grammar-general number-leaf
  primitive; density tuning grammar-derived not a corpus literal.
- **C4b NET-NEW i8mm kernel (`alphaE:412-459`)** — ACCEPT. i8mm grep-clean-absent
  (re-verified); GATED behind re-profile; ONE fn-pointer via `PrimitiveKernels`
  OnceLock, not a subsystem. Grammar-neutral.
- **§3 cross-cutting falsifiability discipline (`alphaE:491-531`)** — **ACCEPT (V2
  REVISE discharged).** The Lock-14-witnessed-not-asserted bullet (`:508-524`) now
  scopes the witness to JSON+CSS, marks `sheets_witness` non-dischargeable, defers the
  Sheets value-grammar rider to SK-V18, and states "No candidate may claim four-grammar
  or fleet-wide generality." The V2 "emit for `sheets_witness`" clause is explicitly
  superseded in the V3 changelog (`:45-48`).

### SYNTHESIS.md (α-F)

- **Benched-surface note (`SYNTHESIS:25-62`)** — ACCEPT, and commend. Full core-tree→
  skinny translation; wrong-tree dishonesty explicitly REJECTed (`:33-34`); grep-clean
  re-verified.
- **§0.1 JSON guard / tape activation / CSS equality / preserve-rich-ast
  (`SYNTHESIS:103-114`)** — ACCEPT. Keyed on the benched skinny tape, grep-verifiable;
  `tape_activated` "NOT satisfiable by a grep in `crates/core/`" (`:363`).
- **§0.1 Layout-driven projection gate (`SYNTHESIS:105`)** — **ACCEPT (V2 REVISE
  discharged).** Closing sentence now reads "`sheets_witness`… CANNOT serve as a
  projection-generator exercise; non-CSS-non-JSON projection generality is
  asserted-by-construction with proof deferred to SK-V18." The css_l4.toml-LOC
  demotion (CH2-V3-S1) is generality-honest.
- **§0.1 Foldable-into-TOTALITY gate (`SYNTHESIS:113`)** — ACCEPT. "non-CSS-non-JSON
  projection rider (Sheets/BBNF-self) is the SK-V18 generality proof (`sheets_witness`
  cannot serve as an SK-V17 projection exercise — it has no `BackendRule` shape)."
- **§0.3 receiver goalset (`SYNTHESIS:171`)** — ACCEPT. Owner paths are the benched
  skinny tree; totality paths flagged SK-V18 fold target.
- **§0.4 pre-blocks + generality clause (`SYNTHESIS:178-257`)** — **ACCEPT (V2 REVISE
  discharged).** The generality clause (`:234-257`) is the full (b′) fix: "exercised
  projection riders are JSON + CSS only", `sheets_witness` "NOT a viable third
  exercise" with the 25-LOC/no-`.bbnf`/no-`BackendRule`/fail-closed rationale, and the
  explicit Lock-14-phrase-#2 honesty ("the contract does NOT claim the Lock 14
  CSS+Sheets minimum is *met* in SK-V17 — that minimum is the SK-V18 close-out"). The
  parenthetical correctly carves out the SIMD `simd_non_json_exercise = css_l4` as a
  genuinely dischargeable distinct exercise. This is the load-bearing repair, landed.
- **§0.5 per-corpus close conditions (`SYNTHESIS:259-294`)** — ACCEPT. Benched set
  fixed to `{bootstrap, tailwindcss, material-components-web, animate}`
  (`css_l4_corpus.rs:22-54`); `normalize` "NOT in this set"; per-corpus endpoints
  honestly UNMEASURED-PENDING.
- **§0.6 strict comparator gate (`SYNTHESIS:296-313`)** — ACCEPT. Grammar-domain honest.
- **Section 2 telemetry binding (`SYNTHESIS:347-369`)** — **ACCEPT (V2 REVISE
  discharged).** The column split (CH2-V3-S2): `projection_generality_exercise` (`:365`,
  `json`\|`css_l4`, sheets_witness explicitly forbidden) + `simd_non_json_exercise`
  (`:369`, `css_l4`, the dischargeable non-JSON SIMD exercise). The `css_corpus` enum
  (`:349`) is the benched set. This is the exact CH2-V2-directed fix, sharpened.
- **Section 3 trajectory (`SYNTHESIS:389-412`)** — ACCEPT. "the unified
  tape/layout/projection model is proven generalizable (JSON+CSS witnessed) and SK-V18
  becomes the Sheets/BBNF-self… tranche" is the honest witnessed posture; gates on
  `animate OR bootstrap`.

### HANDOFF.md (α-F)

- **Benched-substrate disclosure + current state + four-lever route
  (`HANDOFF:9-84`)** — ACCEPT. Names the benched skinny tape; flags core-tree as SK-V18
  fold target; 6→24 broadcast reconciliation (`:48-53`, CH2-V3-S3) introduces no
  generality claim.
- **Generality scope (`HANDOFF:86-96`)** — ACCEPT (V2 REVISE discharged). "the
  projection generator's exercised riders are JSON + CSS only… `sheets_witness` CANNOT
  serve as a projection-generator exercise… SK-V17 does NOT claim the Lock 14 CSS+Sheets
  minimum is met (witness is JSON+CSS, not Sheets — Lock 14 phrase #2, `LOCKS.md:386-387`).
  The NEON SIMD leaf's non-JSON exercise IS `css_l4`… sound and distinct from the
  projection-generality scope." This is the (b′) fix propagated into the HANDOFF
  directly (not merely pointed at SYNTHESIS — stronger than the V2 fix asked).
- **CH7 overfit-prune scope (`HANDOFF:131-142`)** — ACCEPT. The css_l4.toml-is-totality
  noted-not-gated framing matches CH2-V3-S1.
- **Pre-blocked routes + hidden-coupling (`HANDOFF:144-181`)** — ACCEPT. Comprehensive,
  grammar-neutral.
- **Next move + close criterion (`HANDOFF:183-232`)** — ACCEPT. Close criterion is
  `animate OR bootstrap`; `tape_activated` PayloadArena-counter-proven not core-tree-grep;
  the generality posture is self-contained and (b′)-clean.

---

## §3 — Disposition summary

Total sections dispositioned: **31** (same census as V2 for comparability) +
3 fresh-V3-surface findings (CH2-V3-S1/S2/S3, all ACCEPT) = **34**.

- **ACCEPT: 34**
- **REVISE: 0**
- **REJECT: 0**

ACCEPT rate: 34/34 = **100%** (31/31 = **100%** on the comparable V2 census).

The single load-bearing V2 REVISE root (CH2-V2-F1) and all six of its inherited
sites are **discharged via option (b′), verified against the codebase at
`1c5bd7a25`**: `sheets_witness` is grep-proven a 25-LOC `EventGrammar` witness with
no `.bbnf` / no `BackendRule` / fail-closed at `lib.rs:1075-1090`; the contract now
honestly scopes the exercised projection riders to JSON+CSS and declines to claim
the Lock 14 CSS+Sheets minimum is met (deferring it to SK-V18 per Lock 14
phrase #2, `LOCKS.md:386-387`). The three fresh V3 surfaces (css_l4.toml SK-V18
demotion, telemetry column split, 6→24 broadcast reconciliation) introduce **no
new generality defect**; the telemetry split (CH2-V3-S2) is in fact a sharper
two-axis-generality taxonomy than the V2 fix requested.

**Zero orphan REVISE.** No disposition is left dangling; there is no open generality
defect.

### What CH2-V3 affirms as Lock-14-clean and load-bearing

The unified tape/projection model **is** grammar-general where exercised: JSON is
the real witness (`json/value.rs:1,143` generated `value_from_ref` walked from the
`BackendRule` shape, not `->` arms — json.bbnf has 0 `->` arms), CSS is the new
first-mover rider, both share one view-emitter walking one `BackendRule` shape. The
NEON neutrality vehicle (`select_classifier(alphabet)` + `lo6_table_admissible`,
`dispatch.rs:42,101`) is one kernel keyed on a per-grammar byte alphabet with an
honest scalar-fallback on lo6 collision — Lock 14 phrase #3 exactly. The
derive-from-`.bbnf`-rule no-relocated-overfit pre-block, the `W5C_REQUEST_FACT_PROFILES`
retire-list (`lib.rs:336`), and the css_l4.toml-is-totality demotion are all
grammar-neutral. The contract no longer claims any generality it cannot test: the
projection generator's non-CSS-non-JSON generality is honestly asserted-by-construction
with proof deferred to SK-V18, and the SIMD-leaf non-JSON generality is honestly
bound to the dischargeable `css_l4` exercise.

**Convergence: 100% ACCEPT clears the §3Z 95% bar on the CH2 lens, with zero orphan
REVISE.** This is the second consecutive measured improvement on CH2 (V1 → V2 80.6%
→ V3 100%). CH2 converges at V3.
