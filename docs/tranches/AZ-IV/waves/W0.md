# AZ-IV.W0 - Truth And Canonical Regen

**Name**: W0 - Truth And Canonical Regen
**Opens after**: AZ-IV plan creation
**Agents**: 5 parallel
**Hard gate**: strict regen and active truth surfaces are green, current, and enforced
**Status**: planned

## Scope

1. Reconcile active forward docs so AZ-IV is the execution canon and BA/BB are historical inputs until reopened.
2. Fix active workspace topology claims: wasm derive residue, parse-that derive residue, `csp-solver` source of truth, npm lock drift, docs sync drift.
3. Make `cargo xtask regen --check` green live for all manifest grammars.
4. Replace literal parser-name strategy binding with manifest/registry-driven binding or prove totality with a synthetic grammar test.
5. Preserve egraph `Map { fn_id }` semantics through extraction.
6. Harden manifest metadata and staged regen triggers so metadata changes cannot bypass regen checks.
7. Produce a BA/BB lossless coverage ledger before implementation waves open: every BA/BB requirement is marked absorbed, retired with evidence, or routed with an exact successor.
8. Stop `scripts/bootstrap-bbnf.sh` from nuking `target/.bbnf-cache/`; the content-keyed cache at `crates/derive/src/lib.rs:300-358` is the truth (per `audit/HARDENING-2026-05-01-boole.md` §Part-a). Cycle-2 wall must be `<= 10 %` of cycle-1 wall; remove `bbnf-bootstrap` from `iter-check-full` if its cost is not amortized by the cache fix.
9. Eradicate `bbnf_derive` source/manifest/lock residue across this repo, `wasm/`, and `parse-that/rust/`; rephrase the 4 stale `crates/derive/src/lib.rs:NNN` doc-link references in `xtask/src/regen.rs` (lines 14, 97, 308, 319) so they cite commit `6142387f` (per `audit/HARDENING-2026-05-01-diophantus.md` §C). Lands as new sub-unit `AZ-IV.W0.2.c bbnf_derive Eradication`.
10. Enforce csp-solver canonical-source policy: bbnf-lang in-tree is bench authoritative, csc411 sibling is algorithm-evolution authoritative. Lands as new sub-unit `AZ-IV.W0.2.b csp-solver Canonicalisation Enforcement`.

## Triumvirate Dispatch

Dispatch a triumvirate if any of these occurs:

- regen drift root cause requires changing more than `xtask`, strategy registry, and one lowering/emitter surface;
- parser binding totality cannot be proven without a second runtime path;
- egraph `Map` preservation requires changing the rewrite language rather than cost/extraction/writeback semantics;
- sibling topology repair requires deleting or moving a sibling repo path outside declared bounds;
- BA/BB coverage reveals a requirement that cannot fit AZ-IV's grammar-derived StructDirect thesis;
- an agent hits three diagnostic loops without isolating root cause.

Empty/no-evidence returns get one verbatim redispatch with the same worktree pointer. A second empty/no-evidence return triggers mandatory triumvirate.

## File Bounds

| File | Access |
|---|---|
| `docs/GESTALT.md` | modify |
| `docs/codegen-paths.md` | modify |
| `docs/tranches/REMAINING-TRAJECTORY.md` | modify |
| `docs/tranches/BA/**` | modify |
| `docs/tranches/BB/**` | modify |
| `docs/tranches/AZ-IV/**` | modify |
| `Cargo.toml` | modify |
| `.cargo/config.toml` | modify |
| `xtask/src/regen.rs` | modify |
| `xtask/src/main.rs` | modify-carve |
| `crates/ir/src/registry/strategy.rs` | modify |
| `crates/core/src/lower/expression/wrap.rs` | modify-carve |
| `crates/core/src/lower/expression/repeat.rs` | modify-carve |
| `crates/core/src/lower/expression/alt.rs` | modify-carve |
| `crates/ir/src/egraph/**` | modify-carve |
| `crates/egraph/src/**` | modify-carve |
| `crates/core/src/grammar/generated/**` | modify generated |
| `wasm/**` | modify |
| `/Users/mkbabb/Programming/parse-that/**` | modify-carve |
| `/Users/mkbabb/Programming/pprint/**` | modify-carve |

Do NOT touch: runtime path API expansion, rewrite/ruler rule activation beyond `Map` preservation, benchmark threshold changes.

## Disjointness

No two W0 units share a `modify` path. If doc reconciliation and source repair both need one file, sequence those commits inside the owning unit.

W0.3 owns the lowering triad `crates/core/src/lower/expression/{wrap,repeat,alt}.rs` exclusively; no other W0 sub-unit may write these files. The triad is one unit of repair (canonical-parser-tree divergence in `lower_mapped_factor`, `lower_factor` modifier recovery, and `lower_concatenation`/`lower_alternation` iteration markers) and lands in one commit so the regen output remains coherent across the wave.

## Worktree Plan

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| AZ-IV.W0.1 Truth Ledger | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-truth` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-truth/target/w0-truth` |
| AZ-IV.W0.2 Topology Repair | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-topology` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-topology/target/w0-topology` |
| AZ-IV.W0.3 Regen Totality | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-regen` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-regen/target/w0-regen` |
| AZ-IV.W0.4 Map Preservation | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-map` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-map/target/w0-map` |
| AZ-IV.W0.5 Metadata Gate | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-metadata` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w0-metadata/target/w0-metadata` |

## Agent Units

### AZ-IV.W0.1 Truth Ledger

- Mechanism: update active planning docs to make AZ-IV the execution route, freeze BA/BB as historical, and record a lossless BA/BB requirement map.
- Files: `docs/GESTALT.md`, `docs/codegen-paths.md`, `docs/tranches/REMAINING-TRAJECTORY.md`, `docs/tranches/BA/**`, `docs/tranches/BB/**`, `docs/tranches/AZ-IV/**`.
- Sub-gate: `docs/tranches/AZ-IV/audit/W0-ba-bb-coverage.md` maps every BA/BB wave hard gate to absorbed/retired/routed status, and `rg -n "AZ-IV|BA|BB|TapeDirect|bbnf_derive|future tranche" docs/GESTALT.md docs/codegen-paths.md docs/tranches/REMAINING-TRAJECTORY.md docs/tranches/BA docs/tranches/BB` shows only current or historical wording.

### AZ-IV.W0.2 Topology Repair

- Mechanism: remove active derive residue, pick one `csp-solver` source, repair lock/docs sync gates.
- Files: `wasm/**`, `.cargo/config.toml`, root and `extension/` package locks, `crates/bootstrap/Cargo.toml` (metadata-only), `crates/csp-solver/**` and `/Users/mkbabb/Programming/csc411/CSC411_HW2_ProgrammingQuestion/csp-solver/**` (canonical-source declaration enforcement per `docs/GESTALT.md:928-933`), `/Users/mkbabb/Programming/parse-that/rust/bootstrap/**`, `/Users/mkbabb/Programming/parse-that/rust/Cargo.lock`, `/Users/mkbabb/Programming/parse-that/typescript/package-lock.json`, sibling parse-that/pprint docs as needed.
- Sub-gate: root and `wasm/` `cargo metadata --locked` pass; production `rg "bbnf_derive|crates/derive"` hits are zero or explicitly archived.
- Sub-gate addendum (per `audit/HARDENING-2026-05-01-diophantus.md`): production `rg "bbnf_derive|crates/derive"` over the active source tree (this repo's `crates/`, `xtask/`, `wasm/`, plus `parse-that/rust/`) returns at most archived-doc-comment hits with explicit retirement language (`pre-B2`, `retired`, `purged`); zero `use bbnf_derive::*`, zero `bbnf_derive = ` manifest entries, zero `name = "bbnf_derive"` `Cargo.lock` blocks. csp-solver `diff -rq` between bbnf-lang `crates/csp-solver/src` and csc411 `CSC411_HW2_ProgrammingQuestion/csp-solver/src` returns "Only in <canonical>" entries plus identical content for the shared file set, never "Files differ" rows.

### AZ-IV.W0.2.b csp-solver Canonicalisation Enforcement

- Mechanism: enforce the canonicalisation policy already declared in `docs/GESTALT.md:928-933` — bbnf-lang `crates/csp-solver` is bench authoritative; csc411 sibling is algorithm-evolution authoritative. Either rebase the in-tree copy onto a named csc411 commit hash and update `Cargo.toml` metadata to record that hash, or pin `[patch.crates-io]` in `.cargo/config.toml` to a specific csc411 commit so consumers can verify. Document the 5 csc411-only files (`builder/`, `constraint/all_different_except.rs`, `domain/cost_finite.rs`, `solver/gac_alldiff_except.rs`, `py.rs`) as declared-out-of-scope for the bbnf-lang copy.
- Files: `crates/csp-solver/**`, `/Users/mkbabb/Programming/csc411/CSC411_HW2_ProgrammingQuestion/csp-solver/**` (read-only reference), `.cargo/config.toml`, `docs/GESTALT.md` (clarify what "authoritative for benches" means concretely).
- Sub-gate: `diff -rq` between the two trees lists only the 5 declared csc411-only files plus an identical content set for the 22 shared files, never "Files differ"; `cargo metadata --locked` passes in both repos.

### AZ-IV.W0.2.c bbnf_derive Eradication

- Mechanism: delete `bbnf_derive = "0.2"` from `parse-that/rust/bootstrap/Cargo.toml:13`, rewrite `parse-that/rust/bootstrap/src/lib.rs:8` to consume the post-B2 contract (path-included grammar via `cargo xtask regen`, mirroring bbnf-lang's `crates/bootstrap` thin re-export), regenerate `parse-that/rust/Cargo.lock` and `wasm/Cargo.lock` to drop the `bbnf_derive` package + transitive edge, and refresh `parse-that/typescript/package-lock.json` against `parse-that/typescript/package.json`. Also rephrase the 4 stale `crates/derive/src/lib.rs:NNN` doc-link references in `xtask/src/regen.rs` (lines 14, 97, 308, 319) so they cite commit `6142387f` instead of unverifiable line numbers in a deleted file.
- Files: `/Users/mkbabb/Programming/parse-that/rust/bootstrap/**`, `/Users/mkbabb/Programming/parse-that/rust/Cargo.lock`, `/Users/mkbabb/Programming/parse-that/typescript/package-lock.json`, `wasm/Cargo.lock`, `xtask/src/regen.rs` (4-line doc-comment fix only).
- Sub-gate: `rg "bbnf_derive"` over `parse-that/rust/`, `parse-that/typescript/`, `wasm/`, and bbnf-lang `crates/`,`xtask/` returns no live source/manifest/lock matches — only archived doc-comments with commit-hash citations. `cargo metadata --locked` passes in `parse-that/rust/` and `wasm/`.

### AZ-IV.W0.3 Regen Totality

- Mechanism: repair strict regen drift, parser strategy binding, and the canonical-parser-tree divergence in lowering. The canonical generated `BbnfBootstrap` parser inlines mapped_factor's `->` punctuator, factor modifiers (`?`, `*`, `+`, `?w`), and inner concat/alt iteration markers without wrapping them in compounds. The current predicate-based detectors in `lower/expression/{wrap,repeat,alt}.rs` (`span_text().starts_with("->")`, `kind() == BbnfKind::Unit`, span-trim modifier match) silently skip when the predicate misses, dropping IR `Map { fn_id }` wrappers and `Repeat` nodes. Replace predicate detection with structural detection (positional / compound_kind enumeration plus source-gap fallback) across all three files in one commit; panic loudly on unmatched annotations rather than skipping silently. Regen 9/9 grammars as a follow-on mechanical commit once the lowering chain is fixed.
- Files: `xtask/src/regen.rs`, `xtask/src/main.rs`, `crates/ir/src/registry/strategy.rs`, `crates/core/src/lower/expression/wrap.rs`, `crates/core/src/lower/expression/repeat.rs`, `crates/core/src/lower/expression/alt.rs`, `crates/core/src/grammar/generated/**` (mechanical regen output only).
- Sub-gate: `cargo xtask regen --check` passes live for every manifest grammar (9/9), the synthetic grammar binding test proves no new literal parser-name arm, the regen template scrubs stale `Walker-tape compound emission` doc comments from generated `*.rs` files (post-regen `rg -n 'Walker-tape|__dta_walker_inline' crates/core/src/grammar/generated/` returns zero hits), `BbnfBootstrap::parse(grammar/bbnf/bbnf.bbnf)` round-trips cleanly (no `Syntax { offset: 36 }` error), and the IR for `int_lit`/`float_lit` emits the `Map { fn_id, Regex }` wrapper so the hregex emitter dispatches `push_leaf_with_i64`/`push_leaf_with_f64` (not the `push_leaf_with_str` fallback). Hand-coded source-side `__dta_walker_inline::run` references in `crates/core/src/backend/rust/emitter/shapes/**` and `crates/ir/src/passes/recognizers/shape_dispatch/mod.rs` are scrubbed under W2.4 (`AZ-IV.W2.4 DTA DFA Cleanup`), not W0.3 — W0.3 owns generated-template scrubs only.

### AZ-IV.W0.4 Map Preservation

- Mechanism: pin semantic wrappers through egraph cost/extraction/writeback.
- Files: `crates/ir/src/egraph/**`, `crates/egraph/src/**`, focused tests.
- Sub-gate: named test fails before/fixes after if `Map { fn_id }` is stripped from an equivalent inner node.

### AZ-IV.W0.5 Metadata Gate

- Mechanism: make manifest metadata and staged regen triggers fail closed.
- Files: `xtask/src/regen.rs`, `scripts/hooks/pre-commit`, `Cargo.toml`, focused tests.
- Sub-gate: unknown metadata features fail, `Cargo.toml` metadata edits trigger staged regen relevance, and full regen remains CI blocker.

## Hard Gate

1. `cargo xtask regen --check` passes live and artefact is saved under `docs/tranches/AZ-IV/audit/W0-regen.txt`.
2. `cargo metadata --locked` passes at root and `wasm/`.
3. `Map` preservation test passes and cites cost/extraction/writeback behavior.
4. Active docs no longer route execution to stale BA/BB plans.
5. BA/BB coverage ledger is complete before W1 opens and has no `unknown`, `later`, or ownerless rows.
6. `git diff --check` passes.
7. **Dev-iteration baseline gate** (per `audit/HARDENING-2026-05-01-boole.md` §Part-a): `docs/tranches/AZ-IV/audit/W0-dev-baseline.txt` records cold and warm walls for `cargo iter-check`, `cargo iter-test-leaf`, `cargo iter-check-full`, `cargo bench-iter-json --no-run`, `cargo nextest run --workspace --cargo-profile ax-iter`, and `cargo xtask regen --check`. Each row carries (a) the wall, (b) the AZ-III baseline from `docs/benchmarks/archive/AZ-III/W0p-bench-iter-walls.txt` and `docs/tranches/meta-audit/04-toolchain-pain.md`, (c) delta vs baseline, (d) status `MET` / `REGRESSED` / `IMPROVED`. Regressions block W0 close until root-caused.
8. **bbnf-bootstrap cache honesty**: cycle-2 wall of `cargo iter-check-full` is `<= 10 %` of cycle-1 wall; archived in `docs/tranches/AZ-IV/audit/W0-bootstrap-cache-honesty.txt`. The fix lands in scope item 8 below.
9. **Generated-size budget** (per `audit/HARDENING-2026-05-01-cantor.md` Amendment 5): per-grammar generated LOC stays within `+/- 5 %` of the pre-W0 LOC recorded by the orchestrator at base commit; archived in `docs/tranches/AZ-IV/audit/W0-generated-size.txt`. Overflow blocks W0 close until the regression is traced, deliberately accepted with a recorded ceiling raise, or rolled back.
10. **`bbnf_derive` zero-hit gate** (per `audit/HARDENING-2026-05-01-diophantus.md` §B): `rg "bbnf_derive"` across this repo's `crates/`, `xtask/`, `wasm/`, plus `parse-that/rust/`, `parse-that/typescript/` returns zero live source/manifest/lock matches; only archived doc-comments with commit-hash citations remain.
11. **csp-solver canonical diff gate**: `diff -rq` between bbnf-lang `crates/csp-solver/src` and csc411 `CSC411_HW2_ProgrammingQuestion/csp-solver/src` lists only the 5 declared csc411-only files plus identical content for the 22 shared files; no "Files differ" rows.

## Format And Lint Cadence

Run `cargo fmt --all -- --check`, focused `cargo clippy --profile ax-iter` for touched Rust crates, and `git diff --check` after each integration batch and before close.

## Verification Artefacts

- `docs/tranches/AZ-IV/audit/W0-regen.txt`
- `docs/tranches/AZ-IV/audit/W0-metadata.txt`
- `docs/tranches/AZ-IV/audit/W0-dev-baseline.txt` (cold + warm walls per command, deltas vs AZ-III baseline)
- `docs/tranches/AZ-IV/audit/W0-bootstrap-cache-honesty.txt` (cycle-1 vs cycle-2 walls)
- `docs/tranches/AZ-IV/audit/W0-generated-size.txt` (per-grammar pre/post LOC table; budget table below)
- `docs/tranches/AZ-IV/audit/W0-derive-eradication.txt` (rg evidence, lock-diff, metadata pass)
- `docs/tranches/AZ-IV/audit/W0-csp-solver-canonical.txt` (`diff -rq` output)
- `docs/tranches/AZ-IV/audit/W0-ba-bb-coverage.md` (refreshed with landed evidence)
- `docs/tranches/AZ-IV/audit/W0-close-ledger.md`

### Generated-size budget

| Generated artefact | Pre-W0 LOC | Post-W0 LOC ceiling | Source |
|---|---:|---:|---|
| `crates/core/src/grammar/generated/bbnf.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/json.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/css_l4.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/css_pretty.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/google_sheets.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/ebnf.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/bnf.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/csv.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |
| `crates/core/src/grammar/generated/math.rs` | TBD at dispatch | +/- 5 % of pre-W0 | regen output |

The orchestrator records pre-W0 LOC at base commit. Overflow blocks W0 close until the regression is traced, deliberately accepted with a recorded ceiling raise, or rolled back.

## Commit Plan

Every dispatch carries `HARD CAP: N min. At 0.9N commit, at N halt.` Defaults: research 20, plan 15, redress 30, audit 25 (per `docs/precepts/instructions/ORCHESTRATION.md` §Triumvirate). Read-only audit/research agents do not commit at the cap; write-authorized agents commit at 0.9N only when the staged slice is clean and owned.

Use `commit-discipline` before each commit. Expected scopes: `docs(az-iv)`, `chore(workspace-topology)`, `fix(regen)`, `fix(egraph-cost)`, `fix(xtask-metadata)`, `chore(grammar/generated)`, `fix(scripts/bootstrap-bbnf-cache)`, `chore(parse-that/derive-eradication)`, `chore(csp-solver/canonical-diff)`. Broad, generated, deletion, gate, and cross-repo commits require bodies.

## Dependencies

- **Depends on**: AZ-IV plan creation.
- **Blocks**: W1 - Runtime Surface And Semantic Parity.
