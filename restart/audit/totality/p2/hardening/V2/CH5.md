# CH5 HIDDEN COUPLING

Lens name: CH5 HIDDEN COUPLING - no retained sidecar, public substrate
expansion, grammar switch, hidden directive, new BIR, sixth `BackendShape`,
self-excluding gate, or CSS fact-stream/CSSOM substitution admitted.

Disposition: ACCEPT

Target: SK-V15 T-P2 V2 packet at challenge target `7cda9b82e`.

## Critical Findings

| id | severity | finding | evidence | convergence impact |
|---|---:|---|---|---|
| CH5-V2-00 | none | No CH5-critical hidden-coupling defect found in the V2 packet. The six dossiers preserve the V1 CH5 accept surfaces while folding CH1/CH3/CH4/CH6 obligations. | V2 challenge requires CH5 to re-check no new directive, BIR, sixth `BackendShape`, retained sidecar, CSS broadcast admission, self-excluding gate, CSS fact-stream/CSSOM substitution, or x86/AVX-512 M5 close evidence (`restart/audit/totality/p2/hardening/V2/CHALLENGE-CONTEXT.md:54`-`57`). The live dossiers reject or gate those routes: 2A blocks retained sidecar/class-column routes (`restart/audit/totality/p2/2A-sota-landscape.md:51`, `:87`, `:110`); 2B keeps Layer 0 diagnostic and source inventory non-admitting (`restart/audit/totality/p2/2B-primitive-vocabulary.md:59`-`:61`, `:152`-`:155`, `:180`-`:185`); 2C refutes grammar switches, CSS generator sidecars, self-excluding gates, CSS fake parity, and future new directive/BIR/sixth-shape onboarding (`restart/audit/totality/p2/2C-grammar-neutrality.md:52`-`:54`, `:69`-`:70`, `:123`-`:127`, `:148`-`:149`); 2D preserves exactly five `BackendShape` variants and blocks sidecar expansion in lowerers (`restart/audit/totality/p2/2D-cost-model.md:47`-`:53`, `:62`, `:67`-`:68`, `:75`-`:76`, `:85`, `:117`); 2E keeps aarch64 as the only close route and rejects `svmatch_u8` / AVX-512 close (`restart/audit/totality/p2/2E-host-arch-esoterica.md:67`-`:82`, `:121`-`:124`); 2F blocks runtime regex substrate import and CSS value parsing through JSON-shaped semantics (`restart/audit/totality/p2/2F-parse-that-gaps.md:73`-`:80`, `:98`-`:103`, `:120`-`:122`). | CH5 does not block T-P2 V2. V2 can count as the first clean hardening cycle for this lens; it cannot by itself satisfy §3Z because the challenge context requires a second consecutive clean cycle before T-P2 convergence (`restart/audit/totality/p2/hardening/V2/CHALLENGE-CONTEXT.md:37`-`:38`). |

## Guard Matrix

| guard | disposition | inspected evidence |
|---|---|---|
| Retained sidecar / public substrate expansion | PASS | 2A says SOTA parser sources do not justify retained sidecars and admits only transient same-loop masks consumed into the existing substrate (`restart/audit/totality/p2/2A-sota-landscape.md:35`-`:36`, `:51`-`:52`, `:87`, `:110`). 2D lowerer gates require no sidecar expansion and EventTape output without retained sidecars (`restart/audit/totality/p2/2D-cost-model.md:67`-`:68`, `:75`-`:76`). |
| Layer 0 / Layer 1 coupling | PASS | 2B keeps Layer 0 as vendored x86 macro infrastructure, diagnostic only for SK-V15, and Layer 1 as one-way contract vocabulary with per-primitive gates (`restart/audit/totality/p2/2B-primitive-vocabulary.md:40`-`:49`, `:59`-`:61`, `:90`-`:97`). |
| Hidden directive / new BIR / sixth `BackendShape` | PASS | 2C explicitly preserves no new directive, no new BIR, and no sixth `BackendShape` (`restart/audit/totality/p2/2C-grammar-neutrality.md:52`-`:54`) and requires future onboarding without those additions (`restart/audit/totality/p2/2C-grammar-neutrality.md:149`). 2D preserves exactly `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` (`restart/audit/totality/p2/2D-cost-model.md:47`-`:49`, `:62`, `:85`, `:117`). |
| Generic grammar switch | PASS | 2C names `RuntimeGenerationMode` and `runtime_profiles()` as the generic-crate grammar switch and marks it refuted/blocked, with generated manifest replacement only (`restart/audit/totality/p2/2C-grammar-neutrality.md:70`, `:126`, `:144`). |
| Self-excluding gate | PASS | 2C refutes current Lock 14 scan output because it omits leak roots, and the LAC requires inclusion/exclusion reporting that fails on same-change omissions (`restart/audit/totality/p2/2C-grammar-neutrality.md:74`, `:127`, `:148`). |
| CSS fact-stream / CSSOM substitution | PASS | 2A refutes `CssFullParseSummary`, fact-stream `parse()`, and lightningcss-as-current-close (`restart/audit/totality/p2/2A-sota-landscape.md:60`-`:62`, `:81`-`:85`). 2C and 2F keep fact streams diagnostic and require generated typed CSS provider plus same-workload cssparser equality before close (`restart/audit/totality/p2/2C-grammar-neutrality.md:64`-`:69`, `:110`-`:115`; `restart/audit/totality/p2/2F-parse-that-gaps.md:78`, `:90`-`:92`, `:102`). |
| CSS broadcast admission | PASS | 2A, 2C, 2D, 2E, and 2F each prevent the 24-row CSS broadcast from admitting rows or laundering primitive/shape/PMU evidence (`restart/audit/totality/p2/2A-sota-landscape.md:59`, `:79`; `restart/audit/totality/p2/2C-grammar-neutrality.md:68`, `:125`-`:126`; `restart/audit/totality/p2/2D-cost-model.md:98`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:80`, `:124`; `restart/audit/totality/p2/2F-parse-that-gaps.md:80`, `:103`). |
| Runtime regex / new hidden substrate | PASS | 2F keeps regex facts local to `bbnf-regex`, blocks runtime DFA/regex import absent a named generated-runtime consumer and CH3/CH5 review, and requires a consumer scan before any reopen (`restart/audit/totality/p2/2F-parse-that-gaps.md:73`-`:74`, `:84`, `:98`-`:100`, `:109`, `:120`). |
| x86 / AVX-512 M5 close evidence | PASS | 2B and 2E demote x86/AVX-512 to diagnostic/future-backend evidence only, and 2D keeps CollapsedStage AVX-512 material diagnostic until an aarch64 route exists (`restart/audit/totality/p2/2B-primitive-vocabulary.md:64`, `:157`-`:164`, `:182`; `restart/audit/totality/p2/2D-cost-model.md:65`, `:87`, `:97`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:67`-`:82`, `:86`-`:89`, `:123`). |

## Evidence Inspected

- `restart/audit/totality/p2/hardening/V2/CHALLENGE-CONTEXT.md`
- `restart/prompts/totality/PASS-2-RESEARCH.md`
- `restart/prompts/ORCHESTRATOR.md`
- `restart/audit/totality/p2/T-P2-DISPATCH-CONTEXT.md`
- `restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md`
- `restart/audit/totality/p2/hardening/HARDENING-T-P2-V1-CONSOLIDATED.md`
- `restart/audit/totality/p2/2A-sota-landscape.md`
- `restart/audit/totality/p2/2B-primitive-vocabulary.md`
- `restart/audit/totality/p2/2C-grammar-neutrality.md`
- `restart/audit/totality/p2/2D-cost-model.md`
- `restart/audit/totality/p2/2E-host-arch-esoterica.md`
- `restart/audit/totality/p2/2F-parse-that-gaps.md`
- Targeted searches over the six dossiers for `sidecar`, `retained`,
  `substrate`, `directive`, `BIR`, `BackendShape`, `sixth`,
  `RuntimeGenerationMode`, `CSS_GENERATED_RS`, `CssFullParseSummary`,
  `fact-stream`, `CSSOM`, `broadcast`, `self-excluding`, `svmatch`,
  `AVX-512`, `x86`, `RegexProgram`, and `regex-automata`.

## Fold Requirements

None for CH5. Preserve the V2 fences in any subsequent fold: sidecar-like
routes must stay transient/same-loop or require a new Alpha/P1/SPEC contract;
generated provider work must not become a generic grammar switch; the five
`BackendShape` canon must remain closed; CSS fact streams and brace counters
must remain diagnostic; runtime regex import must remain blocked absent named
consumer plus CH3/CH5 review.

## Convergence Impact

CH5 does not block T-P2 V2 convergence. If the other V2 lenses also return
clean, this cycle may be counted as the first clean T-P2 hardening cycle only.
Per the V2 challenge context, a second consecutive clean challenge cycle is
still required before normal §3Z T-P2 convergence.
