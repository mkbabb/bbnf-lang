# T-P2 V4 CH6 Anti-Paper-Close

Pass: T-P2 Research.
Cycle: V4.
Lens: CH6 anti-paper-close.
Date: 2026-05-21.
Scope: anti-paper-close only: whether V4 treats non-shortlist blockers, exact
command shapes, or REDRESS-slice mappings as admissions.

## Verdict

ACCEPT.

V4 preserves the anti-paper-close boundary. It folds the V3 CH1/CH4 repair set
by correcting 2E counted-source IDs, replacing deferred executable cells with
exact commands or explicit non-shortlist blockers, and adding REDRESS-slice
ownership. Those repairs are not treated as admissions. Across 2A-2F, source
evidence, exact command shapes, local owner summaries, microbench results, and
REDRESS mappings remain below admission until a strict equality/oracle gate,
same-wave consumer, row movement or measured rejection, rollback/abrogate
discipline, and Lock/G-Omega discipline are consumed.

## Evidence

| disposition | target | finding |
|---|---|---|
| ACCEPT | `T-P2-V4-FOLD-ADDENDUM.md:11-22`, `:41-58` | V4 states it does not weaken the prior anti-paper-close, state-machine, REDRESS, material-differential, or numeric-abrogate contracts. The executable delta replaces deferred cells with exact commands / first consumers, or marks rows non-shortlist until missing cells exist. A later S-P3 wave can select such a row only by adding the missing command and first consumer, passing CHALLENGE, and keeping the same-wave redress gate. |
| ACCEPT | `T-P2-V4-FOLD-ADDENDUM.md:49-54` | Exact command shapes are not admissions. `escape_mask_64` remains prerequisite-only until a same-wave consumer is wired; `tbl_tbx_escape_decode_batch`, `digit_run_accumulate_udot`, `string_context_64`, and `cache_hint_prefetch_store` are non-shortlist until strict parity/equality or exact caller evidence exists; `pmull_cssc_structural_union_emit64` still lacks a V4 first consumer and combined strict matrix. |
| ACCEPT | `T-P2-V4-FOLD-ADDENDUM.md:62-79` | REDRESS-slice ownership is a routing bridge, not dispatch authority. The table says a slice may reach S-P3 only after its blocker is cleared; otherwise it remains research evidence. The mapped rows preserve strict sonic/lightningcss/cssparser gates, row movement, rollback paths, abrogate thresholds, and blockers for byte-hash substitution, DOTPROD parity, PMULL/CSSC replay, source-present cache hints, and parse-that import. |
| ACCEPT | `T-P2-V4-FOLD-ADDENDUM.md:81-86`; `2B-primitive-vocabulary.md:126-149`; `2E-host-arch-esoterica.md:142-147`; `2F-parse-that-gaps.md:188-196` | Local tables cannot paper-close by vocabulary drift. V4 reserves `admissibility_state` for the normalized enum and says 2B/2E/2F prose tables are owner summaries only. 2B, 2E, and 2F all point back to the V3/V4 addenda as authoritative for blockers, non-shortlist status, REDRESS ownership, LOC/risk/rollback, and normalized state. |
| ACCEPT | `2B-primitive-vocabulary.md:38-46`, `:53-60`, `:136-161`; `2E-host-arch-esoterica.md:115-138`, `:149-158`; `2F-parse-that-gaps.md:198-231` | Future gates remain strict and measured. The dossiers require scalar reference, strict checkasm/parity or equality/oracle, same-wave production consumer, expected row/feature gate, rollback, abrogate threshold, and row movement or measured rejection before admission. `source_backed`, `scalar_backed`, `checkasm_backed`, and `micro_proven` remain non-admitting. |
| ACCEPT | `2C-grammar-neutrality.md:82-87`, `:113-126`, `:157-168`; `2D-cost-model.md:23-38`, `:69-90`, `:139-149` | V4 does not convert research density into closure in the generality or cost lanes. 2C says standards citations and one admitted CSS row ground candidates only; future witness rows remain `NOT-VALIDATED` without same-wave consumers and gate telemetry. 2D says citations ground candidates only, decision-engine rows remain `source_backed`, and parity/equality failure, stale costs, row regression, or substrate-kind violations fail closed. |
| ACCEPT | `2D-cost-model.md:92-108`, `:151-160`; `2F-parse-that-gaps.md:162-186`, `:237-243`; `PASS-2-RESEARCH.md:200-207` | G-Omega discipline holds. V4 still blocks retained sidecars, second tapes, public substrate APIs, new BIR/API expansion, and new `BackendShape` variants unless the explicit lock/user amendment path authorizes them. T-P2's own pass rules forbid silently absorbing techniques that require new directives, BIR variants, or substrates. |
| ACCEPT | `HARDENING-T-P2-V3-CONSOLIDATED.md:12-31`, `:33-54`; `T-P2-V4-FOLD-ADDENDUM.md:24-39`, `:41-86` | V4 addresses the V3 revise set without inflating CH6. The V3 consolidated audit required 2E source-ID repair, exact commands / first consumer paths or non-admit blockers, REDRESS-slice ownership, and normalized state wording. V4 implements those as provenance, blocker, and ownership machinery; it does not claim convergence of any candidate route from those repairs alone. |

## Required Repairs

None for CH6.

Carry-forward requirement: future V4 consumers must keep non-shortlist rows
non-admitting until their missing command, first consumer, strict equality or
oracle, row movement or measured rejection, rollback, and abrogate conditions
are present in the same wave and pass CHALLENGE.
