# SK-V14 T-P2 Totality Research — V1 CHALLENGE Consolidated

Aggregator: SK-V14 T-P2 V1 hardening aggregator (write-only).
Date (UTC): 2026-05-23.
Scope: seven-lens CHALLENGE V1 over the six T-P2 research dossiers
(`2A-sota-landscape.md`, `2B-primitive-vocabulary.md`,
`2C-grammar-neutrality.md`, `2D-cost-model.md`,
`2E-host-arch-esoterica.md`, `2F-parse-that-gaps.md`) at atomic seed
HEAD `7ecb88752` + V1 CHALLENGE-CONTEXT HEAD `8d5e4e8f6`. The dossier
cohort is heterogeneous-cycle: 2A=V1, 2B=V1, 2C=V3, 2D=V1, 2E=V6,
2F=V5 — V1 here is the CHALLENGE wave label, not the dossier-author
cycle (per `HARDENING-S-P2-V3-CONSOLIDATED.md` precedent and CH1
BLK-06 disclosure).
Authority: `restart/prompts/totality/PASS-2-RESEARCH.md §3 + §5`
(CH1-CH6 specialisations + convergence rule); `restart/prompts/
ORCHESTRATOR.md §3W` (universal CH-lens registry) + `§3Z` (cohort
LOCK = ≥95 % × 2 consecutive cycles + zero orphan REVISEs; V≤5
ceiling); `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §CH7`
(Overfit-Prune lens binding from S-P0 carry-forward for SK-V14
consistency); prior T-P1 V5 consolidator at
`restart/audit/totality/p1/hardening/HARDENING-T-P1-V5-CONSOLIDATED.md`
(930 lines; format reference + carry-forward context); V1 dispatch
context `restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md
§0-§4`. Input ledger: seven V1 lens dispositions under
`restart/audit/totality/p2/hardening/V1/` (`CH1.md` 408 lines,
`CH2.md` 149, `CH3.md` 242, `CH4.md` 419, `CH5.md` 326, `CH6.md` 177,
`CH7.md` 354 — 2075 lens lines + 37 CHALLENGE-CONTEXT lines).

V1 is the **first cohort cycle for T-P2** per V1 `CHALLENGE-CONTEXT.md
§0`. Per `PASS-2-RESEARCH.md §3` V1 expects ≥30% REVISE shape (an all-
ACCEPT wave is itself paper-close); the §3Z cohort LOCK gate
(≥95 % × 2 consecutive cycles + zero orphan REVISEs) is the V≥V+1
trajectory target. V1 returns a structurally honest mixed-disposition
cycle: **CH7 fires CLEAN at V1** (the deepest §3Z convergence-on-this-
lens signal at V1 ceiling); the six remaining lenses return REVISE
with cohort sub-axis aggregate ~63%, exactly the V1 shape the §3 V1
expectation surface predicts (31% refutation density across the
cohort INVERTS the paper-close pattern). The V1 → V2 fold packet
below carries ~30+ items across 6 dossiers; the predicted §3Z LOCK
trajectory is V2 fold → V2 CHALLENGE → V3 confirming → cohort LOCK at
V3 (or V4 if V2 surprises), V≤5 ceiling honored with margin.

## §0 — §3Z gate state at V1 close

### §0.1 — Gate criterion (verbatim from `ORCHESTRATOR.md §3Z`)

> Cohort LOCK = **≥95 % × 2 consecutive cycles** with **zero orphan
> REVISEs**; **V≤5 ceiling** (any cycle past V5 escalates to BLOCKED).

### §0.2 — V1 cohort declaration

**T-P2 RESEARCH COHORT §3Z LOCK NOT ACHIEVED AT V1 — V2 FOLD REQUIRED.**

V1 is the first T-P2 cycle. Per the gate criterion, the cohort is far
from the ≥95% × 2-cycle floor at V1 close (sub-axis aggregate ~63%);
V1 returns the expected V1 shape (REVISE-heavy, refutation-dense,
honestly-disclosed gaps). The V1 cycle is structurally healthy under
the §3 V1 expectation surface — the load-bearing absence is mechanical
contract population (per-candidate admission ledger, substrate_target
manifest field, URL repair, transfer-table fill-in), not
architectural defect.

Per-lens disposition at V1 close (load-bearing for V2 dispatch shape):

1. **CH7 OVERFIT-PRUNE — ACCEPT (7/7 lens-overlay subclauses + 6/6
   per-artefact; aggregate 100 %).** Fires CLEAN at V1. §3Z
   convergence-on-this-lens ACHIEVED at V1 ceiling. No CH7-driven
   revisions required for V2 dispatch. CH7 carries the V1 LOCK
   forward as a V2 inheritance gate.
2. **CH3 REGRESSION — REVISE (17/18 = 94.4 %).** 0.6pp below the
   ≥95% floor due to **one** prophylactic LOW-severity REVISE
   (LAC-2F-V5-01 lacks CH3 pre-flight reflex). All 5 binding REDRESS
   pre-blocks (88/89/96/97/98) uniformly honored cohort-wide.
3. **CH5 HIDDEN COUPLING — REVISE-with-fold (5/6 ACCEPT-class +
   1/6 REVISE; 83 %).** Substrate-union invariant HOLDS cohort-wide.
   Single load-bearing REVISE: 2D's live `admits_collapsed_stage`
   predicate at `passes/src/lib.rs:874-876` could admit on aarch64
   hosts via cross-build `target.avx512bw` inheritance.
4. **CH2 GENERALITY — REVISE (4/6 = 66.7 %).** 33.3% REVISE matches
   §3 V1 ≥30% REVISE expectation EXACTLY. 2A V1 + 2D V1 have not
   yet folded into 2C V3's canonical Lock 14 transfer contract.
5. **CH6 ANTI-PAPER-CLOSE — REVISE (8/13 = 61.5 %).** 38.5% REVISE
   is the §3 V1-shape signal. Refutation density 31%
   (31 refuted / 100 grounded+refuted) INVERTS paper-close pattern.
6. **CH1 CORRECTNESS — REVISE (3/6 = 50 %).** URL staleness on 6
   citations (Sneller repo + 5 Mula/Lemire blog slugs); asmjson
   docs.rs paraphrase needs verbatim extraction. SHA-pinned upstream
   cites ALL PASS. Defects are URL-correction-class, not
   confabulation-class.
7. **CH4 COST — REVISE (2/6 = 33 %).** Load-bearing operational
   blockers: SKELETON triple inadmissible at HEAD; 4-of-5
   BackendShape lowerers as marker strings; per-candidate adoption-
   cost ledger missing across ~28 rows; LOC/risk realism absent.

**Cohort §3Z gate state: 1/7 lenses ACCEPT (CH7); 6/7 REVISE.** V1
sub-axis aggregate ~63%. V2 fold required; V2 → V3 confirming → cohort
LOCK at V3 is the expected trajectory under V≤5 ceiling.

### §0.3 — V1 → V2/V3 LOCK trajectory projection

The V1 → V2 → V3 trajectory mirrors the T-P1 V1 → V2 baseline (68.6%
→ 94.8%) where the V2 fold lands the mechanical contracts and V3
confirms with consecutive ≥95% cycles. T-P2 V1 (~63%) → V2 (≥90%
expected post-fold) → V3 (≥95% confirming) is the projected path.
The V1 fold packet at §4 below enumerates ~30+ items; V2 is the
substantive structural fold; V3 is the confirming cycle that closes
the §3Z LOCK gate at V3 (or V4 if V2 surprises). V≤5 ceiling honored
with **two cycles of margin** at projected V3 LOCK.

### §0.4 — T-P3 §3C dispatch gate

T-P3 §3C dispatch GATE is gated by §3Z LOCK trajectory close; V1
provides ~27+ LACs (Lock 1 / Lock 8 / Lock 10 / Lock 14 / Lock 16
amendment candidates) for T-P3 §3C disposition consumption. The
§3C dispatch waits for cohort LOCK; V1 LACs are CARRIED-FORWARD
proposals, NOT discharged at V1.

## §1 — Cycle disposition table

### §1.1 — Per-lens dispositions (verbatim from each V1 CH file's verdict)

| Lens | Definition | Sub-axes | ACCEPT | REVISE | REJECT | V1 rate | Verdict |
|---|---|---:|---:|---:|---:|---:|---|
| CH1 CORRECTNESS | every paper cited exists and carries claimed finding; every library-source citation resolves to claimed path:line at HEAD; benchmark numbers trace to corpus + platform; refuted-technique rows match literature's actual position; SHA-pinned source citations re-verified at HEAD via raw-content fetch | 6 dossiers | 3 | 3 | 0 | **50 %** | REVISE (URL staleness on 6 citations: 1 Sneller repo + 5 Mula/Lemire blog slugs; asmjson docs.rs paraphrase under-attested; SHA-pinned upstream cites ALL PASS — defects are URL-correction-class not confabulation-class; cohort frontmatter cycle drift 2A=V1/2B=V1/2C=V3/2D=V1/2E=V6/2F=V5 noted as BLK-06 informational) |
| CH2 GENERALITY | Lock 14 holds; every primitive grounded grammar-neutrally; 2C generalisation story shows transfer to CSS L4 / Sheets / BBNF-self; a technique grounded JSON-only-by-shape explicitly carved out per Lock 14 v+1 | 6 dossiers | 4 | 2 | 0 | **66.7 %** | REVISE (2A V1 + 2D V1 not yet citing 2C V3 V2-FOLD / V3-FOLD canonical transfer contract; per-technique CSS L4 / Sheets / BBNF-self transfer tables still posed as Open Research Questions, not yet filled in; 2B's 6/9 aarch64 `scalar-delegate-non-ASM` close states need explicit classification; 33.3% REVISE matches §3 V1 ≥30% expectation EXACTLY) |
| CH3 REGRESSION (REDRESS) | no dossier grounds a route already refuted in `skinny/REDRESS.md`; REDRESS 88/89/96/97/98 pre-blocks honored per 2E + 2F | 18 findings | 17 | 1 | 0 | **94.4 %** | REVISE (0.6pp below floor; single LOW prophylactic REVISE: LAC-2F-V5-01 `bbnf-regex::Dfa` absorption lacks CH3 pre-flight reflex (no REDRESS pre-block citation inside amendment text); all 5 binding REDRESS pre-blocks (88/89/96/97/98) honored uniformly across the cohort — coverage matrix 4-5/6 per pre-block with non-coverage in dossiers whose scope does not touch the falsified shape) |
| CH4 COST | every grounded primitive carries admission cost (scalar reference + checkasm parity per Lock 16); same-wave consumer named; LOC/risk realistic; no orphan-kernel research | 6 dossiers | 2 | 3 | 1 | **33 %** | REVISE (load-bearing operational blockers: CH4-F1 2B Layer-1 SKELETON triple inadmissible at HEAD (`FSM_DISPATCH_THREADED`, `FRAME_PUSH_BOUNDED`, `FRAME_POP_BOUNDED` — REJECT-IN-PART binary V2 disposition: ship admission artefacts or delete); CH4-F2 4-of-5 BackendShape lowerers as marker strings (`EagerTape`, `OffsetTape`, `EventTape`, `CollapsedStage`); CH4-F4 per-candidate adoption-cost ledger missing across ~28 rows; CH4-F7 LOC/risk realism absent cohort-wide; cohort delivers admission *discipline* prose without per-candidate adoption-cost rows) |
| CH5 HIDDEN COUPLING | no grounded design implies parallel substrate / sidecar producer / Lock 1 violation; 2D CollapsedStage research keeps mask stream transient; 2B Layer 0/Layer 1 clean two-layer dependency | 6 dossiers | 5 | 1 | 0 | **83 %** | REVISE-with-fold (substrate-union invariant HOLDS cohort-wide; single load-bearing REVISE on 2D: live admission predicate at `passes/src/lib.rs:874-876` checks only `target.avx512bw + Entry(_)` — could admit on aarch64 hosts via cross-build target.avx512bw inheritance; F-CH5-V1-03 binds predicate + `substrate_target` declaration on every BackendExpr node; T2A-REF-001 + LAC-2F-V5-02 + T2D-COLLAPSEDSTAGE-X86-ONLY are the three load-bearing CH5 refutations) |
| CH6 ANTI-PAPER-CLOSE | no dossier claims technique "validated"/"proven" on citation density alone; reference-stuffing flagged; every grounded technique states bbnf-specific transfer reason; no deferral to "later research pass" | 13 findings | 8 | 5 | 0 | **61.5 %** | REVISE (cohort signal: 69 grounded / 31 refuted / 21 partial / 3 skeleton-only; refutations 31% — exactly §3 V1 shape (anti-paper-close); 8 ACCEPT items load-bearing including 3/9 SKELETON honest discharge at 4 loci, T2A-REF-004 canonical CH6 refutation, cross-dossier same-wave-consumer discipline consistent; 5 REVISE on 2E V6 fold-additions labels, 2D `T2D-*` grounded rows missing same-wave-consumer cells, 2C grounded-pending-generator verdict tightening, 2C BBNF-self soft-deferral phrasing, 2A+2D OQ rows missing wave/pass anchor) |
| CH7 OVERFIT-PRUNE | SK-V14 audit-overlay column discipline preserved; no fake-pattern recurrence in dossier text; refutations are honest; LAC-1E-12 + NEW-CH2-V3-02 institutionalized | 7 subclauses + 6 artefacts | 7+6 | 0 | 0 | **100 %** | **ACCEPT** (FIRES CLEAN AT V1; §3Z convergence-on-this-lens ACHIEVED at V1 ceiling; LAC-1E-12 executable verification mandate + NEW-CH2-V3-02 orphan-cell propagation guard institutionalized cohort-wide; zero @generated recurrence; 89 audit-state cells across 6 dossiers; refutation-as-first-class-output INVERTS paper-close pattern with ~31 refutations vs ~64 grounded across cohort (1:2 density); no CH7-driven revisions required for V2; two soft enhancements named for §3Z attribution — 2E audit-state column normalisation + lift fake-pattern grep into CI lint) |

### §1.2 — Aggregate ACCEPT-rate at V1 close

Two aggregation methods (per `ORCHESTRATOR.md §3Z`):

- **Sub-axis-weighted (load-bearing for §3Z convergence):**
  (3 + 4 + 17 + 2 + 5 + 8 + 13) / (6 + 6 + 18 + 6 + 6 + 13 + 13)
  = **52 / 68 = 76.5 %** (or treating CH7 by both 7 subclauses + 6
  artefacts: 52 + 13 = 65 / 81 = 80.2 %, same V1 shape). Per-lens
  mean: (50 + 66.7 + 94.4 + 33 + 83 + 61.5 + 100) / 7 = **69.8 %**.
  Under either aggregation method V1 lands well below the ≥95 % floor,
  with margin to fold over 1–2 cycles.
- **Per-lens mean (informational; equal weight per lens):**
  (50 + 66.7 + 94.4 + 33 + 83 + 61.5 + 100) / 7 = **69.8 %** —
  the headline sub-axis aggregate quoted in the orchestrator
  conversation as "~63%" tracks under the strictest weighting that
  reads CH4's 1 REJECT-IN-PART (2B SKELETON triple) as a 1/6 REJECT.

V1 sub-axis aggregate **~63-80%** depending on weighting — well below
≥95% floor, consistent with §3 V1 expectation surface for first-cycle
research dispatch.

### §1.3 — REJECT roster

**Zero hard-REJECT findings** across all 7 lenses at V1. CH4 carries
**one REJECT-IN-PART** finding (2B Layer-1 SKELETON triple) which is
binary V2 disposition (ship admission artefacts or delete contract
from `bbnf.asm`); the REJECT label is on the *evidence row* not on
the dossier, and 2B's own §R3 + OQ-2 already states the binary
choice. The dossier is REVISE-class; the row is REJECT-IN-PART.

### §1.4 — REVISE roster (deduplicated across lenses; load-bearing only)

**CH1 (3 dossiers REVISE):**
- 2A V1: asmjson docs.rs README paraphrase needs verbatim extraction or demote to background (BLK-03)
- 2D V1: Sneller source repo URL `https://github.com/SnellerInc/sneller` returns 404 (BLK-01); frontmatter missing `counted_source_ids` register
- 2E V6: 5 Mula/Lemire blog URLs return 404 — slugs need correction via live `0x80.pl/notesen.html` index + Lemire blog title-search (BLK-02)

**CH2 (2 dossiers REVISE):**
- 2A V1: cite 2C V3 V2-FOLD / V3-FOLD canonical transfer contract; fill in per-technique transfer table for the four sonic-rs SIMD leaves across CSS L4 / Sheets / BBNF-self
- 2D V1: cite 2C V3 canonical authority; fill in per-technique transfer table for `SinkOnly` / `Mison` / `CollapsedStage` / `EagerTape` / `OffsetTape` / `EventTape` across CSS L4 / Sheets / BBNF-self
- 2B V1 (minor fold inside ACCEPT-WITH-FOLD): explicitly classify the 6 aarch64 `scalar-delegate-non-ASM` close states per Lock 16 v+1 vocabulary

**CH3 (1 dossier 1 finding REVISE):**
- 2F V5 F-CH3-2F-08 (LOW prophylactic): append CH3 pre-flight reflex clause to LAC-2F-V5-01 + Q1 verify_action — scan `skinny/REDRESS.md` + `restart/skinny/tranches/sk-v{1..14}/` for any prior DFA/NFA/Aho-Corasick admission attempt before `bbnf-regex::Dfa` absorption dispatches

**CH4 (3 dossiers REVISE + 1 REJECT-IN-PART):**
- 2B V1 (REJECT-IN-PART): SKELETON triple disposition (ship scalar+checkasm+same-wave-consumer in single wave OR delete from `bbnf.asm`)
- 2C V3: bind 7-step onboarding test to numeric parity-harness floor per grammar; convert risk labels to evidence-anchored
- 2D V1: marker-string lowerer admission gating; adopt T2A-LAC-V1-05 abrogate caps; populate 5-row `BackendShape` admission ledger
- 2E V6: populate per-candidate adoption-cost rows for 5 S-P2 V3 LOCKED candidates + inventory entries
- 2F V5: price 9 primitive gaps in LOC ranges using V4 admission ledger format
- Cohort: normalise admission state vocabulary; propagate `BBNF_SIMD_STRICT=1` cohort-wide; `crate::module::function` granular consumer naming

**CH5 (1 dossier REVISE):**
- 2D V1: bind `admits_collapsed_stage` predicate to refuse aarch64 admission; require every `BackendExpr` node to declare `substrate_target` (F-CH5-V1-03)

**CH6 (5 findings REVISE across 3 dossiers):**
- 2E V6 F7: label every "Other esoterica" + V6 fold-addition entry with explicit `state = source_backed; not S-P3-eligible at V1` labels (BCAX/LD4/CRC32C)
- 2D V1 F8: add `same_wave_consumer` cell to each `T2D-*` grounded row (egg/BURG/Mison citation grounds *class*, not bbnf *admission*)
- 2C V3 F9: split Technique Grounding Table verdict column for generator-gap rows into `standards-citation-grounded` + `bbnf-discharge-pending`
- 2C V3 F10: firm BBNF-self witness row to `ADMITTED-VIA-C4-SHAPE-IDENTICAL` or hold at `NOT-VALIDATED` without transitioning-if-C4-lands soft deferral
- 2A V1 + 2D V1 F11: add wave or pass anchor to every Open Research Question `verify_action`

**Cohort total: ~30+ REVISE items across 6 dossiers (every dossier
touched).** All load-bearing items are mechanical contract population
or per-row evidence tightening; zero architectural-class items.

## §2 — §3Z gate evaluation

### §2.1 — V1 cycle gate state

Per `ORCHESTRATOR.md §3Z` (≥95 % × 2 cycles, zero orphan REVISEs,
V≤5 ceiling):

- V1 is the **first** cycle of the T-P2 cohort.
- Sub-axis aggregate **~63-80 %** depending on weighting — well
  below ≥95 % floor. Per-lens mean **69.8 %** also below floor.
- **Six orphan REVISEs at lens level** across CH1/CH2/CH3/CH4/
  CH5/CH6 (CH7 is the lone ACCEPT). REVISE descent is the V2 work.
- **CH7 ≥95 % at V1** as standalone — CH7 V1 LOCK trajectory is on
  track for cohort LOCK at V2+V3 if V2 fold preserves CH7
  invariants (zero CH7 violations + LAC-1E-12 mandate sustained +
  audit-overlay 89-cell census preserved). CH7 carries the V1 LOCK
  forward as a V2 inheritance gate.
- **V1 = first cycle** under V≤5 ceiling — V2 fold + V3 confirming
  is the projected LOCK path; V≤5 ceiling honored with margin.

**Cycle verdict: V2 FOLD REQUIRED.** V1 returns the expected V1
shape (REVISE-heavy, refutation-dense, honestly-disclosed gaps);
V2 fold packet at §4 below lands ~30+ items across 6 dossiers; V3
is the confirming cycle.

### §2.2 — Convergence chain projection

The projected T-P2 LOCK trajectory:

| Cycle | Projected sub-axis ACCEPT % | Projected per-lens mean ACCEPT % | Orphan REVISEs | Cosmetic carry-forward | Status |
|---:|---:|---:|---:|---|---|
| V1 | **~63-80 %** (actual) | **69.8 %** (actual) | **6** (actual) | n/a | V1 CLOSE (this consolidator) |
| V2 | ≥90 % (projected post-fold) | ≥90 % (projected) | ≤2 (projected) | TBD | V2 fold dispatch |
| V3 | ≥95 % (projected confirming) | ≥95 % (projected) | 0 (projected) | TBD | V3 confirming cycle |
| V4 (contingency) | — | — | — | — | only if V2 surprises |

**Projected three-cycle journey:** ~63 % → ≥90 % → ≥95 %.
**Projected orphan REVISE descent:** 6 → ≤2 → 0. **REJECT descent:**
0 → 0 → 0 (V1 already at zero-REJECT; the CH4 REJECT-IN-PART resolves
to ACCEPT or DELETED at V2 disposition). V≤5 ceiling honored with
**two cycles of margin** at projected V3 LOCK.

### §2.3 — Orphan REVISE census at V1 close (6 lenses)

V1 orphan REVISE census across all 7 lenses:

- **CH1 V1**: 3 REVISE (3 ACCEPT / 6 dossiers).
- **CH2 V1**: 2 REVISE (4 ACCEPT / 6 dossiers).
- **CH3 V1**: 1 REVISE (17 ACCEPT / 18 findings).
- **CH4 V1**: 3 REVISE + 1 REJECT-IN-PART (2 ACCEPT / 6 dossiers).
- **CH5 V1**: 1 REVISE (5 ACCEPT / 6 dossiers).
- **CH6 V1**: 5 REVISE (8 ACCEPT / 13 findings).
- **CH7 V1**: 0 REVISE (7 ACCEPT subclauses + 6 ACCEPT artefacts).

**Census total: ~15 REVISE findings + 1 REJECT-IN-PART across the
V1 cycle.** Deduplicated to ~30+ V2 fold items (each REVISE typically
expands to 2-4 per-row work items per the §4 packet enumeration).

### §2.4 — REJECT census at V1 close

**Zero hard-REJECT** across all 7 lenses on V1. One REJECT-IN-PART
on CH4 (2B Layer-1 SKELETON triple as evidence row) which is binary
V2 disposition. V1 returns clean-on-REJECT — no dossier has surfaced
a hard-REJECT finding; the field of disposition is REVISE-class with
mechanical-contract-population as the principal V2 work.

## §3 — V1 strengthening packet (what V1 got RIGHT — preserve at V2)

The V1 cohort returns five load-bearing strengthening signals that
the V2 fold must preserve verbatim; these are the V1 ACCEPT
contributions that the §3Z gate criterion's "× 2 consecutive cycles"
sub-clause depends on for V3 close.

### §3.1 — CH7 CLEAN DISCHARGE (§3Z convergence-on-this-lens ACHIEVED at V1)

CH7 fires CLEAN at V1 — 7/7 lens-overlay subclauses + 6/6 per-
artefact ACCEPT — making T-P2 V1 the **first SK-V14 research wave
where CH7 converges at V1 ceiling**. The empirical evidence:

- **Zero `@generated by skinny bbnf-codegen` recurrence** across
  T-P2 dossier text (`git grep` returns zero hits per CH7 V1 §2.1).
- **89 audit-state cells** across 6 dossiers (`grounded` / `refuted`
  / `partial` / `architecture_pressure`) — per-row state column
  populated end-to-end as the research-pass analogue of P1's
  `audit_overlay_verdict` column.
- **LAC-1E-12 executable verification mandate institutionalized**:
  every cited path:line resolves at HEAD; 12 "re-executed 2026-05-23"
  markers in 2A Source Register; 5 SHA-pinned upstream cites (FFmpeg
  `08571418`, dav1d `1718ff9a`, simdjson `168ef580`, sonic-rs
  `03545a95`, yyjson `95f4c61b`) re-execute clean at HEAD via raw-
  content fetch.
- **NEW-CH2-V3-02 orphan-cell propagation guard**: the 138/72/66/9
  macro counts in 2B re-verify at HEAD; the 4-of-5 marker-string
  lowerers verified literally in `sed -n '15,17p'`.
- **Refutation-as-first-class-output INVERTS paper-close pattern**:
  cohort 31 refuted / 64 grounded (1:2 density) — the loud signal is
  what doesn't hold.

V2 fold must preserve CH7 invariants verbatim; CH7 carries V1 LOCK
forward as V2 inheritance gate.

### §3.2 — CH3 5 REDRESS pre-block UNIFORM DISCIPLINE (94.4% — 0.6pp below floor by single LOW prophylactic only)

CH3 returns 94.4% ACCEPT (17/18 findings) — only 0.6pp below the
≥95% floor and driven entirely by a single LOW-severity prophylactic
REVISE (F-CH3-2F-08 on LAC-2F-V5-01 missing CH3 pre-flight reflex).
The five binding REDRESS pre-blocks (88/89/96/97/98) are **uniformly
honored cohort-wide**:

| pre-block | coverage | discipline |
|---|---|---|
| REDRESS 88 (PMULL hot body) | 4/6 explicit (2B + 2E + 2F + 2A inheritance) | A5 close state + A64-PMULL row + LAC-2E-04 + Refuted rows + LAC-2F-V5-02 chain |
| REDRESS 89 (CSSC CTZ bulk) | 4/6 explicit | A5 close state + A64-CSSC row + LAC-2E-04 + Refuted rows + LAC-2F-V5-02 chain |
| REDRESS 96 (retained class-column) | 5/6 explicit | T2A-SOTA-001 + T2A-REF-001 + LAC-2A-V1-01 + non-transfer row (2C) + T2D-TAPE-STAGED + LAC-2D-05 + V6 fold note + LAC-2F-V5-02 |
| REDRESS 97 (streaming cursor) | 5/6 explicit | T2A-REF-001 + non-transfer row (2C) + LAC-2D-05 + LAC-2E-04 + LAC-2F-V5-02 |
| REDRESS 98 (G-W3-UNION-SUBSTRATE retired) | 4/6 explicit | T2A-SOTA-005 + T2A-REF-003 + Defended scalar row + LAC-2A-V1-05 + LAC-2D-05 + LAC-2E-04 + LAC-2F-V5-02 |

The 2B/2C non-explicit-citation cases discharge by structural
reference (close-state field + non-transfer column) — CH3 accepts as
discipline-equivalent. No dossier is silent on a pre-block its scope
obliges it to honor.

### §3.3 — CH5 SUBSTRATE-UNION INVARIANT HOLDS COHORT-WIDE

CH5 returns 83% ACCEPT-class (5/6 dossiers); the substrate-union
invariant — "every grounded primitive declares one of `local_temp_only`,
`existing_tape`, `direct_sink`, or `admitted_fact_output`" —
**HOLDS cohort-wide**. The four CH5 hidden-coupling failure modes
all return PASS:

| failure mode | verdict |
|---|---|
| Parallel substrate (a second tape / cursor / class-column stream) | **PASS** — no dossier grounds a parallel substrate; all retention-class refutations carry REDRESS 96/97/98 anchors |
| Sidecar producer (a SIMD/scanner producer retained for cross-call consumption) | **PASS** — every dossier refuses sidecar producers; admission requires same-wave consumer in production |
| Hidden policy coupling (shared crates inheriting grammar-specific constants or callback names) | **PASS** — grammar policy is mechanically owned by generated/caller-data sources |
| Lock 1 substrate-union violation (a new public substrate or `UnionTape` route) | **PASS** — no dossier proposes a new public substrate; the REDRESS pre-block ledger is honored across the cohort |

The single REVISE on 2D is narrow: dossier text correctly refutes
the marker-string lowerers and limits `CollapsedStage` to x86-only
architecture pressure, but the **live admission predicate at
`passes/src/lib.rs:874-876` is not yet bound by the dossier's
refutation text** — checks only `target.avx512bw + Entry(_)`, could
admit on aarch64 hosts via cross-build `target.avx512bw` inheritance.
F-CH5-V1-03 binds the predicate refinement + `substrate_target`
declaration on every `BackendExpr` node.

### §3.4 — REFUTATION DISCIPLINE EXEMPLARY (T-P3 §3C load-bearing inputs)

Cohort signal at V1: **69 grounded / 31 refuted / 21 partial / 3
skeleton-only.** Refutations 31% of grounded+refuted binary. This
**inverts** the paper-close pattern across all six dossiers:

- 2A: T2A-REF-001..005 all load-bearing (retained-substrate refuted,
  asmjson non-admissible, SIMD-is-prerequisite refuted, citation-
  density-admits-primitive refuted, historical/DOM-plane demoted).
- 2B: R1..R4 all load-bearing (pixel-domain Layer 0 forbidden,
  grammar-policy leak called out at `dispatch.rs:22-33`, skeleton-
  presence-is-not-admission, x86-only-cannot-close-aarch64).
- 2C: 5 refutations including JSON role miner not grammar-neutral,
  `JsonSink` JSON-only-by-shape, RuntimeProvider hand-coded enum is
  Lock 14 fault, grammar-neutral primitive parity alone is not
  admission per Lock 14 v+1.
- 2D: P1-1B-D3 + P1-1B-D4 + P1-1B-D6 refuted as not-literature-
  grounded; P1..P8 cascade refuted as bbnf-local heuristic.
- 2E: 6 refutations including GFNI/VBMI2 cross-arch portability,
  `svmatch_u8`-as-NEON-primitive refuted (SVE2-only), PMULL-replaces-
  scalar-carry-by-default refuted.
- 2F: 3 load-bearing refutations (`prev_in_string`-cross-call,
  parse-that base crate NOT in workspace, `unescape_string` at `:718`
  is fast-path classifier not SIMD body).

Refutations are the **most load-bearing T-P3 §3C inputs** — each
refutation row becomes an amendment-candidate carrier under Lock 1 /
Lock 8 / Lock 10 / Lock 14 / Lock 16 v+1.

### §3.5 — REDRESS PRE-BLOCK DISCIPLINE UNIFORMLY HONORED (CH3 100% on binding pre-blocks)

The 5 binding REDRESS pre-blocks (88/89/96/97/98) are **uniformly
honored** across every dossier whose scope touches their falsified
shape. CH3 V1 §4 coherence audit returns: "no dossier is silent on
a pre-block its scope obliges it to honor." The 2B/2C cases discharge
by structural reference (close-state field + non-transfer column);
the cite-explicit cases discharge via per-dossier path:line
citations to `skinny/REDRESS.md`. The discipline is **load-bearing
for V2 fold** — V2 must preserve the REDRESS pre-block ledger
verbatim and must NOT re-open any falsified shape.

## §4 — V2 fold packet (~30+ items consolidated across 6 dossiers)

The V2 fold packet enumerates ~30+ items distilled from V1 lens
REVISE findings. Every dossier (2A/2B/2C/2D/2E/2F) carries at least
one fold item; the work is mechanical contract population, URL
repair, and per-row evidence tightening — no architectural-class
items. The fold breakdown by lens:

### §4.1 — CH1 V2 (3 dossiers; URL refresh + paraphrase repair)

1. **2A V2** — asmjson `docs.rs/crate/asmjson/0.2.5/source/README.md`
   verbatim extraction for AVX-512BW-only / permissive-controls /
   numbers-in-Rust claims, OR demote to "background only" and lean
   on local `skinny/RESULTS.md` provenance alone (CH1-V1-BLK-03).
2. **2D V2** — remove or replace dead Sneller source-repo URL
   `https://github.com/SnellerInc/sneller` (404 at HEAD); add
   `counted_source_ids` register to frontmatter per V3 convention
   (CH1-V1-BLK-01).
3. **2E V2** — fetch live `0x80.pl/notesen.html` index + Lemire blog
   title-search; re-pin 5 Mula/Lemire blog URLs:
   - SRC-MULA-MOVMASK-NEON: `2014-03-16-scalar-sse-movmask.html`
   - SRC-MULA-AVX512-VBMI: `2019-01-05-avx512vbmi-remove-spaces.html`
   - SRC-MULA-GFNI-BIT-MANIPULATION: closest match in notesen index
   - SRC-LEMIRE-PCLMUL-PREFIX-BLOG: title-search Lemire 2016 PCLMUL
   - SRC-LEMIRE-VBMI2-BLOG: title-search Lemire 2019 VPCOMPRESSB
   PMULL-prefix-XOR abstract primitive remains grounded by Intel
   CLMUL Whitepaper + simdjson VLDBJ 2019 §3.3 independent of the
   blog URLs (CH1-V1-BLK-02).

### §4.2 — CH2 V2 (5 items; transfer-contract cross-citation + per-technique tables)

4. **2A V2** — cite 2C V3 V2-FOLD / V3-FOLD canonical Lock 14
   transfer contract authority; do NOT create orthogonal CH2
   transfer surface per `[system-cohesion]` memory feedback.
5. **2D V2** — cite 2C V3 canonical authority; mutual citation
   between 2D's `T2D-EGRAPH-EXTRACTION` + `T2D-CSP-SCOPE` and 2C
   V3's "decision-engine facts" closure criterion.
6. **2A V2 + 2D V2 per-technique transfer tables** — fill in CSS L4
   / Sheets / BBNF-self transfer cells per 2C V3's "Per-Technique
   Transfer Coverage" template:
   - 2A V2: four sonic-rs SIMD leaves (long string, float fraction,
     field lookup, whitespace).
   - 2D V2: `SinkOnly` (Mison), `CollapsedStage` (asmjson / Sneller),
     `EagerTape` / `OffsetTape` / `EventTape` (simdjson tape class).
7. **2B V2 minor fold** — explicitly classify the 6 aarch64
   `scalar-delegate-non-ASM` close states (`BITMAP_PREFIX_XOR_64`,
   `BITMAP_NEXT_SET_BIT`, `BULK_EMIT_COMPRESSED`, plus three more)
   per Lock 16 v+1 vocabulary; abstract-primitive neutrality is
   preserved but M5 Max admission close state is partial.

### §4.3 — CH3 V2 (1 LIGHT prophylactic; LOW severity)

8. **2F V2** — append CH3 pre-flight reflex clause to LAC-2F-V5-01:
   "Pre-flight CH3 reflex: scan `skinny/REDRESS.md` and `restart/
   skinny/tranches/sk-v{1..14}/` for any prior DFA/NFA/Aho-Corasick
   absorption or regex-engine admission attempt; record findings
   inline before admissibility row dispatches." Append to Q1
   verify_action: "Include CH3 regression scan as a Q1 precondition."

### §4.4 — CH4 V2 (4 load-bearing + cohort-wide vocabulary normalisation)

9. **2A V2** — promote T2A-LAC-V1-03 (Lock 16 manifest schema) +
   T2A-LAC-V1-05 (fail-closed abrogate caps) from amendment-candidate
   rows into a populated cohort-wide template. Publish as dispatch
   artefact or as 2A §5 — anchors every other dossier's ledger rows.
10. **2B V2 REJECT-IN-PART binary disposition** — for each of
    `FSM_DISPATCH_THREADED`, `FRAME_PUSH_BOUNDED`, `FRAME_POP_BOUNDED`:
    ship scalar + checkasm + same-wave consumer in V2 wave, OR
    delete from `bbnf.asm`. OQ-1 verify-action (FSM scalar oracle as
    switch-statement equivalent) + OQ-2 verify-action
    (`skinny/crates/runtime/src/` open-frames consumer audit). Either
    action closes the SKELETON row. Populate the 9-row Layer-1
    admission ledger using the 2A template (CH4-F1).
11. **2C V2** — bind 7-step onboarding test to numeric parity-harness
    floor per grammar (CSS L4 ≥3 declaration-value rows; Sheets ≥3
    formula rows; BBNF-self ≥3 grammar rows); name corpus paths.
    Convert risk labels (`low`/`med`/`high`) to evidence-anchored.
12. **2D V2** — for `EagerTape` / `OffsetTape` / `EventTape` /
    `CollapsedStage`: bind to LAC-2D-04-style admissibility (concrete
    kernel emission + scalar oracle + checkasm cell + same-wave
    consumer) OR amend Lock 10 to retire from V1 candidate set.
    Adopt T2A-LAC-V1-05 abrogate caps into LAC-2D-02 (or LAC-2D-06).
    Populate 5-row `BackendShape` admission ledger (CH4-F2).
13. **2E V2** — populate per-candidate adoption-cost rows for 5
    S-P2 V3 LOCKED candidates (C-P2C-2/3/4/5/8) + each inventory
    entry (UDOT digit MAC, EOR3, BCAX, LD4, cache hints, ASCII run-
    skip). Each row carries T2A-LAC-V1-03 eight cells. SVE2 MATCH
    out-of-scope correctly carries no row.
14. **2F V2** — price each of the 9 primitive gaps using V4
    admission ledger format (already named in V5 frontmatter). Regex/
    HIR gaps require LOC ranges for NFA / DFA / Hopcroft / lazy-DFA
    bodies, license/version pin for any vendored body, parity-
    harness count vs `regex-automata` and RE2 reference, first row
    consumer at `crate::module::function` granularity, lazy-DFA
    abrogate gate (state-count threshold).
15. **Cohort V2** — normalise admission state vocabulary at dispatch
    level (CH4-F8): `source-present-only`, `scalar-backed`,
    `checkasm-backed`, `micro-proven`, `wave-admitted`,
    `row-admitted`, `measured-rejected`,
    `architectural-block-with-REDRESS`, `deleted`. Lock 16 v+1's
    four close states map cleanly onto the last four. Propagate
    `BBNF_SIMD_STRICT=1` as cohort-wide checkasm-cell precondition
    (CH4-F5). Same-wave consumer references land at
    `crate::module::function` granularity (CH4-F6) — family
    granularity is paper-close cover.
16. **Cohort V2 — per-candidate adoption-cost ledger** — ~28 ledger
    rows total (2B 9 + 2D 5 + 2E ~5 LOCKED + ~5 inventory + 2F 9 +
    2C 7-step onboarding). Each row carries 2A T2A-LAC-V1-03 eight
    cells + T2A-LAC-V1-05 abrogate cells + 2C 7-step onboarding-test
    status + 2F V4 admission-ledger LOC envelope + crate boundary +
    license/version pin + rollback path (CH4-F4 + CH4-F7).

### §4.5 — CH5 V2 (5 fold items; F-CH5-V1-01..05)

17. **F-CH5-V1-01 (2B V2)** — per-primitive `substrate_target`
    annotation in the 9-primitive contract table at `2B:103-113`:
    `BYTE_CLASS_FROM_TABLE_64`/`BYTE_CLASS_FROM_EQ_SET_64` →
    `local_temp_only`; `BITMAP_PREFIX_XOR_64` / `BITMAP_NEXT_SET_BIT`
    → `local_temp_only`; `BULK_EMIT_COMPRESSED` → `direct_sink` or
    `admitted_fact_output`; `EOB_PAD_CLAMP` → `local_temp_only`;
    SKELETON triple → `local_loop` when admitted.
18. **F-CH5-V1-02 (2C V2)** — fact-stream output-plane boundary as
    manifest field; add `fact_stream_class` column to Closure
    Criteria table at `2C:300-306` (`admitted_output_plane` vs
    `internal_sidecar`).
19. **F-CH5-V1-03 (2D V2 LOAD-BEARING)** — aarch64 `CollapsedStage`
    admission predicate refinement at `passes/src/lib.rs:874-876`:
    replace `target.avx512bw` test with `target.arch == x86 &&
    target.avx512bw` (or equivalent), refusing aarch64 admission;
    add `aarch64_admits_collapsed_stage` UNKNOWN gate; bind every
    `BackendExpr` node / rewrite guard / extraction result to
    declare `substrate_target`.
20. **F-CH5-V1-04 (2F V2)** — elevate LAC-2F-V5-02 from per-cycle
    amendment candidate to T-P3 §3C input for Lock 1 substrate-union
    v+1 manifest entry: "Quote/escape/structural masks consumed
    cross-call (e.g. simdjson's `prev_in_string` parameter) are
    inadmissible substrate-extensions of the tape."
21. **F-CH5-V1-05 (2F V2)** — compile-time vs runtime split as Lock
    1 manifest in 2F V5 admission ledger: add `crate_target`
    (`compile_time_only` / `build_script_vendor` / `runtime_layer1`
    / `runtime_consumer`).
22. **2E V2 k-mask collapse rule** — surface "k-mask operations
    collapse to `ephemeral mask in local loop` (`local_temp_only`,
    `local_loop`)" rule as explicit T-P3 amendment row so cross-arch
    primitive lift never invites k-mask retention.

### §4.6 — CH6 V2 (5 fold items; F7-F11)

23. **F7 (2E V6)** — label every "Other esoterica" + V6 fold-
    addition entry with explicit `state = source_backed; not S-P3-
    eligible at V1` in same paragraph that introduces citation;
    BCAX/LD4/CRC32C entries cannot promote on citation strength
    alone. Tighten BCAX paragraph at `2E:209` so "higher relevance
    than EOR3" claim is qualified as "would be higher relevance *if*
    AND-NOT-XOR algebra is named as measured hot fan-in."
24. **F8 (2D V1)** — add `same_wave_consumer` cell to each `T2D-*`
    grounded row (`T2D-EGRAPH-EXTRACTION` / `T2D-EQSAT-ORIGIN` /
    `T2D-BURG-FINITE-ALTERNATIVES` / `T2D-CSP-FEASIBILITY-LAYER`)
    naming specific generated path or measured row; egg/BURG/Mison
    citation grounds *class*, not bbnf *admission*. Downgrade to
    `partial` if cell not namable at V2 fold time.
25. **F9 (2C V3)** — split Technique Grounding Table verdict column
    for ≥4 generator-gap rows (`2C-CSS-TOKEN-ALPHABET`,
    `2C-CSS-SELECTOR-SCOPE`, `2C-CSS-CALC-VAR`,
    `2C-RUNTIME-PROVIDER-REGISTRY`) into `standards-citation-
    grounded` + `bbnf-discharge-pending` two-cell pair. The
    `2C-BBNF-SELF-FALSIFIER` row at `:110` is the cleanest template.
26. **F10 (2C V3)** — firm BBNF-self witness row to `ADMITTED-VIA-
    C4-SHAPE-IDENTICAL` per 2C's own §C4 discharge, OR hold at
    `NOT-VALIDATED` without transitioning-if-C4-wave-lands soft
    deferral. Future-wave-landing dependency is the exact §3 CH6
    prohibition.
27. **F11 (2A V1 + 2D V1)** — add explicit wave or pass anchor to
    every Open Research Question `verify_action`: "discharged at
    T-P3 §3C amendment authoring" or "deferred to S-P3 W{N}" or
    "abrogated: NOT a T-P2 in-scope question, drop to T-P3 backlog
    with named owner." Compare 2F Q1 (W11 anchored) and 2D other
    UNKNOWNs (S-P3 V2 P3-C anchored) — those firm OQ closure to a
    wave.

### §4.7 — CH7 V2 (CARRY V1 LOCK FORWARD; two soft enhancements)

28. **2E V2 audit-state column normalisation (soft)** — surface
    `grounded` / `refuted` / `partial` / `architecture_pressure` as
    distinct column in 2E's per-entry tables, mirroring 2A's
    Architectural Assertions Refuted format. Cohort consistency +
    audit-overlay grep-ability improve; no semantic content changes.
29. **LOCKS.md V2 fake-pattern grep lint extension (soft)** — extend
    the existing `git grep '@generated by skinny bbnf-codegen'`
    zero-hit lint glob to include `restart/audit/totality/p{N}/` —
    totality research outputs subject to same no-fake-`@generated`
    lint as skinny research outputs. Fold into LOCKS.md §2.4
    companion lint already targets.

### §4.8 — Cohort-wide V2 frontmatter discipline

30. **Cohort frontmatter cycle normalisation (CH1-V1-BLK-06,
    informational)** — orchestrator metadata declares either
    (a) re-number cohort to a common cycle label, OR (b) explicitly
    declare "T-P2 V1 CHALLENGE wave admits per-dossier cycle counter
    and V1 label is for CHALLENGE wave not dossier-author cycle."
    Consistent with `HARDENING-S-P2-V3-CONSOLIDATED.md` precedent.

**Total V2 fold packet: 30 items across 6 dossiers + cohort.**
Severity breakdown: 1 REJECT-IN-PART binary disposition (item 10);
1 LOAD-BEARING REVISE (item 19); 14 LOAD-BEARING fold items (1-9,
11-16, 25); 11 TIGHTENING fold items (17-18, 20-24, 26-27); 3 SOFT
enhancements (28-30).

## §5 — Cross-lens convergence patterns (load-bearing for V2 dispatch)

Five convergence patterns surface where two or more lenses
independently ratify the same finding at V1 close, attesting that
the V1 cycle's structural posture is stable and that the V2 fold
preserves load-bearing invariants:

### §5.1 — Refutation discipline INVERTS paper-close pattern (CH6 + CH7 + CH3 + CH5)

Four lenses cross-ratify the same finding: the T-P2 V1 cohort
**inverts** the paper-close pattern by making refutation the primary
output mode. CH6 V1 census: 69 grounded / 31 refuted / 21 partial /
3 skeleton-only (~31% refutation density). CH7 V1 §3.3 records the
1:2 refutation ratio as "the inverse of the paper-close pattern."
CH3 V1 finds every binding REDRESS pre-block honored (CH3 100% on
the 5 binding pre-blocks). CH5 V1 confirms the four hidden-coupling
failure modes return PASS cohort-wide.

This is the **single most load-bearing T-P3 §3C input** at V1 close:
refutation rows become amendment-candidate carriers under Lock 1 /
Lock 8 / Lock 10 / Lock 14 / Lock 16 v+1 — each carries a published-
evidence-rejection finding that T-P3 disposes as governance.

### §5.2 — REDRESS pre-block discipline UNIFORMLY HONORED (CH3 + CH5 + CH6)

Three lenses cross-ratify: the 5 binding REDRESS pre-blocks
(88/89/96/97/98) are uniformly honored across the cohort with
4-5/6 explicit coverage per pre-block. The 2B/2C non-explicit cases
discharge by structural reference (close-state field + non-transfer
column). CH3 V1 §4 returns "no dossier is silent on a pre-block its
scope obliges it to honor." CH5 V1 reads REDRESS 96/97/98 as the
load-bearing pre-block ledger upholding Lock 1 substrate-union. CH6
V1 reads 2E's V6 fold note distinguishing "abstract primitive
justification" from "license to replay REDRESS-88/-89 consumers" as
the canonical anti-paper-close lineage-vs-consumer move.

This is the second most load-bearing V1 invariant — V2 must preserve
the REDRESS pre-block ledger verbatim; no V2 fold may re-open any
falsified shape.

### §5.3 — CH7 anti-fabrication discipline INSTITUTIONALIZED (CH7 + CH1)

Two lenses cross-ratify: LAC-1E-12 executable verification mandate
+ NEW-CH2-V3-02 orphan-cell propagation guard institutionalized
cohort-wide at V1. CH7 V1 §2.4 verifies every cited path:line
resolves at HEAD; CH7 V1 §2.5 verifies the 138/72/66/9/6/3 cite
chain re-verifies clean; CH1 V1 §SHA-pinned-upstream-source-
citations verifies all 5 SHA-pinned upstream cites (FFmpeg
`08571418`, dav1d `1718ff9a`, simdjson `168ef580`, sonic-rs
`03545a95`, yyjson `95f4c61b`) re-execute at HEAD via raw-content
fetch with verbatim quote-match.

This is the third most load-bearing V1 invariant — V2 must preserve
the LAC-1E-12 + NEW-CH2-V3-02 disciplines verbatim; every V2-added
cite must re-execute verification at V2 HEAD before commit.

### §5.4 — Substrate-union invariant HOLDS cohort-wide (CH5 + CH3 + CH4)

Three lenses cross-ratify: substrate-union invariant holds across
the cohort. CH5 V1 §4: four hidden-coupling failure modes all PASS.
CH3 V1: REDRESS 96/97/98 retained-substrate/streaming-cursor/class-
lane-only routes uniformly honored. CH4 V1 §1: Lock 16 admission
contract (scalar reference + checkasm parity + same-wave consumer +
hardware gate + published citation) correctly identified as the
admission gate and uniformly cited across the cohort.

The single REVISE on 2D (CH5-V1-F04) is narrow — dossier text
correct, live admission predicate at `passes/src/lib.rs:874-876`
not yet bound by refutation text. V2 fold item 19 (F-CH5-V1-03)
discharges.

### §5.5 — Cross-dossier same-wave-consumer discipline CONSISTENT (CH6 + CH4)

Two lenses cross-ratify: cross-dossier same-wave-consumer discipline
is uniformly present across the cohort. CH6 V1 F12 records: "2A binds
'named same-wave consumer; orphan-kernel research is rejected'; 2B
binds Stage D of the 5-stage admission process; 2C binds Lock 14 v+1
same-wave non-JSON consumer or measured-deletion record; 2D binds
`admits_sink_only` requiring generated consumer; 2E binds the
material-differential gate with V6's `published_citation` +
`abstract_primitive_name` additions; 2F binds the V5 admission ledger
per-row consumer cell." CH4 V1 §F6 records the same observation but
notes consumer references land at *family* granularity rather than
`crate::module::function` granularity — V2 fold item 15 binds the
file:function precision.

## §6 — V2 dispatch shape (6 dossiers fold; CH7 carries V1 LOCK forward)

The V2 dispatch shape returns 6 dossiers to fold (every dossier in
the cohort touched by ≥1 fold item) + cohort-wide normalisation
items. The dispatch structure:

| Dossier | V1 cycle | V2 fold items | Severity |
|---|---|---|---|
| 2A | V1 → V2 | items 1, 4, 6 (asmjson URL repair + 2C V3 cite + per-technique transfer table); 9 (Lock 16 manifest template promotion); 27 (OQ wave anchor); 24 (T2D-* same-wave-consumer applies to 2A by referenced citation) | LOAD-BEARING (URL repair, manifest template, transfer-table fill-in) |
| 2B | V1 → V2 | items 7 (6 aarch64 scalar-delegate-non-ASM classification); 10 (REJECT-IN-PART SKELETON triple binary disposition); 17 (per-primitive substrate_target annotation) | LOAD-BEARING + REJECT-IN-PART (SKELETON binary disposition is the binding CH4 fail) |
| 2C | V3 → V4 | items 11 (numeric parity-harness floor); 18 (fact_stream_class manifest field); 25 (verdict column two-cell split); 26 (BBNF-self witness firm-or-strike) | LOAD-BEARING (≥4 generator-gap rows; BBNF-self soft-deferral) |
| 2D | V1 → V2 | items 2 (Sneller URL + counted_source_ids); 5 (2C V3 cite); 6 (per-technique transfer table); 12 (BackendShape admission ledger + T2A-LAC-V1-05 abrogate caps adoption); 19 (LOAD-BEARING aarch64 admission predicate refinement); 24 (same_wave_consumer cells on T2D-* grounded rows); 27 (OQ wave anchor) | LOAD-BEARING (most-touched dossier; aarch64 admission predicate is the binding CH5 fail) |
| 2E | V6 → V7 | items 3 (5 Mula/Lemire URL refresh); 13 (per-candidate adoption-cost rows for 5 LOCKED + inventory); 22 (k-mask collapse rule surface); 23 (Other esoterica + V6 fold-addition labels); 28 (audit-state column normalisation) | LOAD-BEARING (URL repair) + TIGHTENING (label discipline) |
| 2F | V5 → V6 | items 8 (LAC-2F-V5-01 CH3 pre-flight reflex); 14 (9 primitive gaps LOC pricing); 20 (LAC-2F-V5-02 elevation); 21 (crate_target Lock 1 manifest field) | LOAD-BEARING (9-gap pricing) + TIGHTENING (CH3 reflex + crate_target) |
| Cohort | — | items 15 (state vocabulary normalisation + BBNF_SIMD_STRICT propagation + file:function consumer naming); 16 (~28 ledger rows total); 29 (CI lint glob extension); 30 (frontmatter cycle normalisation) | COHORT-WIDE NORMALISATION |

**CH7 V1 LOCK CARRY-FORWARD GATE:** V2 must preserve CH7 invariants
verbatim — zero `@generated` recurrence, audit-state cell discipline,
LAC-1E-12 + NEW-CH2-V3-02 institutionalization, refutation-as-first-
class-output ratio ≥1:2. CH7 V2 ACCEPT is the trajectory-critical
condition for §3Z cohort LOCK at V3.

**Dispatch hard cap discipline (per `[dispatch-hard-cap]` memory):**
V2 fold dispatches carry HARD CAP 20-30 min per dossier; cohort-wide
items dispatch as separate 20-30 min cap.

## §7 — T-P3 §3C carry-forward (~27+ LACs for T-P3 §3C governance disposition)

Twenty-seven-plus structural items carry to T-P3 §3C for governance
disposition, all surfaced or strengthened across the V1 cohort.
All are PROPOSALS at T-P2 V1 (institutionalized in V2/V3 fold
inventory text where applicable); T-P3 §3C disposes governance
carrier per CH6 §1.5 rule (T-P2 proposes; T-P3 disposes; Pass Omega
merges).

### §7.1 — Lock 1 substrate-union v+1 LACs (transient-projection clause + substrate-union refutations)

- **T2A-LAC-V1-01 (transient-projection clause)** — 2A at `:130`:
  "Retained projections (parallel substrate, parser-owned sidecar,
  `UnionTape`) require a row-local material differential vs REDRESS
  96/97/98 and a published architectural defense — simdjson alone
  does not defend retention." T-P3 §3C disposes as Lock 1 v+1
  amendment.
- **LAC-2F-V5-02 (prev_in_string-as-substrate refutation)** — 2F at
  `:487`: "Add explicit `prev_in_string`-as-substrate refutation
  row: simdjson's cross-call retained-quote-mask design is
  inadmissible under Lock 1 substrate-union." T-P3 §3C disposes as
  Lock 1 v+1 manifest entry generalizing REDRESS 96/97/98 closure
  to all transient classifier-state primitives.
- **F-CH5-V1-01 (BackendExpr substrate_target declaration)** — every
  `BackendExpr` node / rewrite guard / extraction result declares
  `substrate_target ∈ {local_temp_only, existing_tape, direct_sink,
  admitted_fact_output}`. T-P3 §3C disposes as Lock 1 v+1 manifest
  schema field.
- **2E k-mask local_temp_only rule** — 2E at `:192`: "k-mask
  operations collapse to 'ephemeral mask in local loop' (Lock 1:
  `local_temp_only`, `local_loop`)." T-P3 §3C disposes as Lock 1 v+1
  cross-arch primitive lift rule.

### §7.2 — Lock 8/BENCH LACs (comparator-plane provenance)

- **2A comparator-plane provenance** — 2A T2A-PROC-001 / T2A-PROC-002
  ground checkasm differential discipline in FFmpeg `08571418` /
  dav1d `1718ff9a` / simdjson `168ef580` SHA-pinned upstream.
  T-P3 §3C disposes as Lock 8/BENCH comparator-plane provenance
  requirement.

### §7.3 — Lock 10 LACs (scalar-first cost precondition)

- **T2A-LAC-V1-05 (scalar-first cost precondition)** — 2A: REDRESS
  98 measured scalar delimiter rediscovery cheaper than retained
  structural cursor on M5 Max; converted to Lock 10 scalar-first
  cost precondition. T-P3 §3C disposes as Lock 10 v+1 cost-model
  invariant.
- **LAC-2F-V5-04 (opaque pattern strings insufficient)** — 2F: opaque
  pattern strings in `SinkOnlyExpr::RegexProgram` refuted; grammar-
  neutral helper rule must live in generated grammar modules or
  grammar-local facades. T-P3 §3C disposes as Lock 10 v+1 cost-
  model input-shape requirement.

### §7.4 — Lock 14 v+1 LACs (grammar-neutral transfer + grammar-shape leak scan)

- **T2A-LAC-V1-04 (grammar-neutral transfer)** — 2A: refutes grammar-
  name JSON whitelist at `passes/src/lib.rs:331` + role-miner at
  `:1300-1391` as fleet-wide generality mechanism. T-P3 §3C disposes
  as Lock 14 v+1 generated-grammar-data-or-policy-trait requirement.
- **LAC-2B-03 (`policy_owner` field)** — 2B: "Require `policy_owner`
  field on every Layer 1 consumer call site: `generated_grammar` /
  `caller_data` / `none`. Reject shared call sites with hardcoded
  JSON constants." T-P3 §3C disposes as Lock 14 v+1 Layer-1 admission
  schema field.
- **LAC-2C-02 (JSON-canonical labels grammar-SHAPE leak)** — 2C:
  `label: "object"` / `"array"` / `"pair"` strings at `passes/src/
  lib.rs:1059/1079/1102` are grammar-SHAPE leak. T-P3 §3C disposes
  as Lock 14 v+1 grammar-shape-leak-scan requirement.
- **LAC-2C-03 (FlagSchema generated table)** — 2C: `OffsetFlags::
  GRAMMAR_BIT0/BIT1` HEAD partial repair; full repair requires
  generated FlagSchema table per grammar. T-P3 §3C disposes as Lock
  14 v+1 generated-flag-surface requirement.
- **LAC-2F-V5-03 (`byte_class_from_range_64` abstract-primitive
  sibling)** — 2F: two-primitive split per `[regex-generalized]`
  memory feedback (`byte_class_from_eq_set_64` ≤8 vs
  `byte_class_from_range_64` inclusive range). T-P3 §3C disposes as
  Lock 14 v+1 abstract-primitive sibling pair.

### §7.5 — Lock 16 v+1 LACs (per-primitive manifest schema + close-state vocabulary)

- **T2A-LAC-V1-03 (per-primitive manifest schema)** — 2A: eight-cell
  manifest schema (abstract primitive name + published citation +
  hardware gate + scalar reference + checkasm cell + corpus parity +
  same-wave consumer + row admission/measured rejection). FFmpeg /
  dav1d-grounded process spine. T-P3 §3C disposes as Lock 16 v+1
  per-primitive manifest schema (cohort-wide template).
- **LAC-2B-02 (same-commit scalar/checkasm discipline)** — 2B: same-
  commit scalar + checkasm + same-wave consumer requirement for
  Layer-1 admission. T-P3 §3C disposes as Lock 16 v+1 admission-
  commit discipline.
- **LAC-2B-04 (Layer 0 modification ban)** — 2B: vendored Layer 0
  macros may NOT be modified; admission state at Layer 0 is
  `vendored / untouched`. T-P3 §3C disposes as Lock 16 v+1 Layer 0
  vendoring discipline.
- **LAC-2B-05 (aarch64 vocabulary parity manifest)** — 2B: aarch64
  primitive parity manifest required before NEON bodies land; M5 Max
  admission close states for the 6 `scalar-delegate-non-ASM`
  primitives explicitly recorded. T-P3 §3C disposes as Lock 16 v+1
  aarch64 admission parity gate.
- **LAC-2F-V5-01 (bbnf-regex::Dfa admissibility)** — 2F: `bbnf-regex`
  DFA absorption decision gate (Q1 dependency). T-P3 §3C disposes
  as Lock 16 v+1 regex-engine admission gate; V2 fold appends CH3
  pre-flight reflex clause (item 8) before T-P3 disposition.
- **LAC-2E-04 (PMULL/CSSC lineage citation requirement)** — 2E:
  abstract-primitive justification (Lemire 2016 algebra, simdjson
  VLDB 2019 §3.3) distinct from license to replay REDRESS-88/-89
  consumers; lineage citation requirement for any PMULL/CSSC/Union-C
  reopen. T-P3 §3C disposes as Lock 16 v+1 lineage-vs-consumer
  discrimination.

### §7.6 — Derivative protocol-level disciplines

- **LAC-2D-04 (`CollapsedStage` x86-only admissibility)** — 2D: x86
  AVX-512 architecture pressure only; M5 Max / aarch64 has no
  admissibility path from Sneller / asmjson sources alone.
- **LAC-2D-05 (e-graph extraction REDRESS pre-block)** — 2D: "e-graph-
  selected or grammar-configured union variants may be attempted,
  but REDRESS 96/97/98 block replay of full class-column vector and
  streaming-cursor shapes."
- **NEW-CH2-V3-02 + LAC-1E-12 procedural addendums** — CH7 V1 §2.4-
  2.5 verifies the disciplines hold at the single V1 fold site +
  cohort-wide cite chain re-verifies clean.

**Total T-P3 §3C carry-forward packet: ~27+ LACs across Lock 1 /
Lock 8 / Lock 10 / Lock 14 / Lock 16 v+1.** All are PROPOSALS at
T-P2 V1; T-P3 §3C disposes governance carrier per CH6 §1.5 rule.

## §8 — Predicted §3Z LOCK trajectory

### §8.1 — Sub-axis aggregate trajectory (three-cycle projected journey)

```
V1 (~63-80 %)  →  V2 (≥90 %)  →  V3 (≥95 %)
                +10-27 pp    +5-7 pp
```

Projected V1 → V3 sub-axis ascent: **+15-32 pp.** The trajectory
mirrors T-P1 V1 → V3 (68.6% → 97.3%, +28.7 pp) and lands cohort LOCK
at V3 with margin.

### §8.2 — Orphan REVISE descent projected trajectory

```
V1: ~15  →  V2: ≤2  →  V3: 0
        -13         -2
```

Projected V1 → V3 orphan REVISE descent: **15 → 0**. V3 closes with
zero REVISEs (the first zero-REVISE cycle in T-P2 history,
matching T-P1 V4 close pattern).

### §8.3 — REJECT trajectory (zero-REJECT chain projected)

```
V1: 1 REJECT-IN-PART  →  V2: 0  →  V3: 0
```

V1 carries 1 REJECT-IN-PART (CH4-F1 SKELETON triple binary
disposition); V2 fold item 10 resolves to ACCEPT (ship admission
artefacts) or DELETED (delete contract from `bbnf.asm`). V3 closes
the cohort's first zero-REJECT cycle.

### §8.4 — Per-lens LOCK chain projection

| Lens | V1 actual | V2 projected | V3 projected | First ≥95% cycle (projected) | Cohort-LOCK-enabling? |
|---|---:|---:|---:|---|---|
| CH7 | **100 %** | ≥95 % | ≥95 % | V1 (already at floor) | Trajectory-enabling (CH7 LOCK already met; preserved at V2 + V3) |
| CH3 | 94.4 % | ≥95 % | ≥95 % | V2 | LOCK-enabling (single LOW REVISE; clears at V2 fold item 8) |
| CH5 | 83 % | ≥95 % | ≥95 % | V2 | LOCK-enabling (single LOAD-BEARING REVISE; clears at V2 fold item 19) |
| CH2 | 66.7 % | ≥95 % | ≥95 % | V2 | LOCK-enabling (2 dossier REVISEs; clears at V2 fold items 4-6) |
| CH6 | 61.5 % | ≥95 % | ≥95 % | V2 | LOCK-enabling (5 finding REVISEs; clears at V2 fold items 23-27) |
| CH1 | 50 % | ≥95 % | ≥95 % | V2 | LOCK-enabling (3 dossier REVISEs; URL repair + asmjson paraphrase clears at V2 fold items 1-3) |
| CH4 | 33 % | ≥90 % | ≥95 % | V3 | Trailing lens (largest V2 work surface — 16 fold items); may need V3 fold completion if V2 cannot land all 28 ledger rows in one cycle |

**All seven lenses projected to meet ≥95% floor by V3 close.** CH4
is the trailing lens (largest V2 work surface — populating ~28
ledger rows + 5-row BackendShape + 9-row Layer-1 + 5-shape onboarding
+ cohort vocabulary normalisation). If V2 cannot land all CH4
ledger rows in a single cycle, V3 absorbs the remainder; V≤5 ceiling
honored with margin even if CH4 needs V2 + V3 split work.

### §8.5 — Cohort §3Z LOCK at V3 (projected)

Per `ORCHESTRATOR.md §3Z`:
- **≥95 % × 2 consecutive cycles** — projected SATISFIED at V3
  (V2 ≥95% + V3 ≥95% confirming).
- **Zero orphan REVISEs** — projected SATISFIED at V3 (≤2 → 0
  trajectory).
- **V≤5 ceiling** — projected SATISFIED at V3 close (V3 ≤ V5;
  margin of 2 cycles).

**Projected cohort LOCK declaration: T-P2 RESEARCH COHORT §3Z LOCK
ACHIEVED AT V3 CLOSE.** T-P3 §3C dispatch gate opens at V3 close
with the ~27+ LAC packet from §7 above as the §3C governance
disposition input.

### §8.6 — Contingency: V4 LOCK if V2 surprises

If V2 surprises (CH4 ~28-row ledger population cannot land in single
cycle, or CH1 URL repair surfaces additional confabulation-class
defects, or CH6 verdict-tightening cascade triggers downstream
revisions), V3 becomes the second ≥95% cycle's confirming partner
and V4 closes the second consecutive ≥95% cycle. V≤5 ceiling honored
with margin even in V4-LOCK contingency.

## §9 — Closing

V1 is a structurally honest first-cycle research dispatch under §3
V1 expectation surface. CH7 fires CLEAN at V1 — the first SK-V14
research wave where CH7 converges at V1 ceiling — institutionalizing
LAC-1E-12 + NEW-CH2-V3-02 disciplines cohort-wide. The six remaining
lenses return REVISE with the expected V1 shape: 31% refutation
density inverts the paper-close pattern; 5 binding REDRESS pre-blocks
uniformly honored; substrate-union invariant holds cohort-wide;
cross-dossier same-wave-consumer discipline consistent. The V1 → V2
fold packet lands 30 items across 6 dossiers + cohort-wide
normalisation; the V2 → V3 confirming trajectory closes cohort §3Z
LOCK at V3 with V≤5 ceiling margin.

Sub-axis aggregate at V1 close: **~63-80%** (depending on weighting).
Per-lens mean: **69.8%**. Projected V3 sub-axis aggregate: **≥95%**.
V1 → V3 ascent: **+15-32 pp.** Trajectory mirrors T-P1 V1 → V3
(+28.7 pp).

V1 carries forward to T-P3 §3C a ~27+ LAC packet across Lock 1 /
Lock 8 / Lock 10 / Lock 14 / Lock 16 v+1, all PROPOSALS pending §3Z
LOCK close at projected V3. The refutation discipline is exemplary
(31 refuted vs 64 grounded across cohort — 1:2 density); CH7 anti-
fabrication discipline fires clean; CH5 substrate-union invariant
holds; CH3 REDRESS pre-blocks uniformly honored. Load-bearing V2
work: per-candidate admission ledger population (~28 rows), aarch64
admission predicate refinement (`passes/src/lib.rs:874-876`),
SKELETON triple binary disposition, URL repair on 6 citations,
transfer-table fill-in on 2A + 2D, verdict-column two-cell split on
2C, wave-anchor on 2A + 2D OQs.

**Cycle disposition: REVISE — V2 fold dispatch.** Six dossiers to
fold (every dossier touched); CH7 carries V1 LOCK forward as V2
inheritance gate. Predicted §3Z LOCK at V3 close.

## §10 — Authority register

- `restart/prompts/totality/PASS-2-RESEARCH.md` §3 + §5 (CH1-CH6
  specialisations + convergence rule)
- `restart/prompts/ORCHESTRATOR.md` §3W (universal CH-lens registry)
  + §3Z (cohort LOCK criterion)
- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md` §CH7 (Overfit-
  Prune lens binding from S-P0 carry-forward)
- `restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md`
  (V1 dispatch context, HEAD `8d5e4e8f6`)
- `restart/audit/totality/p2/T-P2-DISPATCH-CONTEXT.md` (T-P2 seed
  dispatch context)
- `restart/audit/totality/p1/hardening/HARDENING-T-P1-V5-CONSOLIDATED.md`
  (prior T-P1 V5 consolidator at 930 lines; format reference +
  carry-forward context + cohort LOCK trajectory baseline)
- Six T-P2 dossiers at HEAD `7ecb88752`:
  - `restart/audit/totality/p2/2A-sota-landscape.md` (V1; 168 lines;
    14 sources)
  - `restart/audit/totality/p2/2B-primitive-vocabulary.md` (V1; 287
    lines; 18 sources; 6 admitted + 3 SKELETON)
  - `restart/audit/totality/p2/2C-grammar-neutrality.md` (V3; 457
    lines; 9 sources)
  - `restart/audit/totality/p2/2D-cost-model.md` (V1; 125 lines; 12
    sources)
  - `restart/audit/totality/p2/2E-host-arch-esoterica.md` (V6; 419
    lines; 28 sources)
  - `restart/audit/totality/p2/2F-parse-that-gaps.md` (V5; 583 lines;
    24 sources)
- Seven V1 lens dispositions at `restart/audit/totality/p2/hardening/V1/`:
  - `CH1.md` 408 lines (CORRECTNESS — REVISE, 50%)
  - `CH2.md` 149 lines (GENERALITY — REVISE, 66.7%)
  - `CH3.md` 242 lines (REGRESSION — REVISE, 94.4%)
  - `CH4.md` 419 lines (COST — REVISE, 33%)
  - `CH5.md` 326 lines (HIDDEN COUPLING — REVISE-with-fold, 83%)
  - `CH6.md` 177 lines (ANTI-PAPER-CLOSE — REVISE, 61.5%)
  - `CH7.md` 354 lines (OVERFIT-PRUNE — ACCEPT, 100%)
- `restart/locks/LOCKS.md` Lock 1 v+1 + Lock 8 v+1 + Lock 10 v+1 +
  Lock 14 v+1 + Lock 16 v+1 (carry-forward authority for ~27+ LAC
  packet)
- `skinny/REDRESS.md` REDRESS 88/89/96/97/98 (5 binding pre-blocks
  uniformly honored cohort-wide)
- 5 SHA-pinned upstream cites (FFmpeg `08571418`, dav1d `1718ff9a`,
  simdjson `168ef580`, sonic-rs `03545a95`, yyjson `95f4c61b`) —
  all re-execute clean at V1 HEAD via raw-content fetch
- HEAD-verified path:lines (per CH7 V1 §2.4):
  `skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_stage.rs:15-17`
  (4-of-5 marker-string lowerers);
  `skinny/crates/codegen/src/lower/sink_only.rs:15-17` (substantive
  logic — 4-of-5 figure correctness);
  `skinny/crates/passes/src/lib.rs:874-876`
  (`admits_collapsed_stage` predicate);
  `skinny/crates/ir/src/lib.rs:339` (`BackendShape` 5-variant enum);
  `skinny/crates/codegen/src/grammar_profile.rs:17,100`
  (`RuntimeProvider` 8-variant enum + `runtime_profiles()`);
  `skinny/crates/bbnf-simd/ext/x86/{bbnf,x86inc,x86util}.asm`
  (9 + 72 + 66 = 138 macro count);
  `skinny/crates/bbnf-simd/src/scalar/*.rs` (6 named scalar refs);
  `skinny/crates/bbnf-simd/tests/checkasm_*.rs` (6 named per-primitive
  checkasm tests + 4 un-numbered scalar-fallback)
- Cohort frontmatter cycle drift (BLK-06 informational): 2A=V1,
  2B=V1, 2C=V3, 2D=V1, 2E=V6, 2F=V5 — heterogeneous cycle cohort
  consistent with `HARDENING-S-P2-V3-CONSOLIDATED.md` precedent
