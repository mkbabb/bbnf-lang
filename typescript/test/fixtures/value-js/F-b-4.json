{
 "note": "F-b-4 (COHESION §0ck 1, accepted as spec-correct) at the raw-grammar reading: rule → indices into verdicts-0.1.4.json sources where the one analysis answers differently from 0.1.4. Reconciled by source text 2026-09-24 with value.js bench/paired/oracle/F-b-4.json (valueTop ↔ entry:parseCssValue, functionParam ↔ reader:functionParam, commaItems/semiItems/spaceItems ↔ reader:splitTopLevel(,/;/space)): every row is there, except valueTop \"🎨ff0099cc\", accepted raw only through badTerm, which value.js’s action answers as a refusal (X.P.W7 .t T-5).",
 "rows": {
  "valueTop": [
   26,
   121,
   127,
   131,
   133
  ],
  "functionParam": [
   1,
   7,
   10,
   22,
   26,
   33,
   44,
   59,
   61,
   83,
   133
  ],
  "commaItems": [
   1,
   7,
   10,
   22,
   26,
   33,
   44,
   59,
   61,
   83,
   133
  ],
  "semiItems": [
   1,
   7,
   10,
   22,
   26,
   33,
   44,
   59,
   61,
   83,
   133
  ],
  "spaceItems": [
   1,
   7,
   10,
   22,
   26,
   33,
   44,
   59,
   61,
   74,
   75,
   82,
   83,
   85,
   88,
   92,
   115,
   117,
   119,
   120,
   121,
   122,
   125,
   127,
   128,
   129,
   130,
   131,
   133
  ]
 },
 "sources": {
  "valueTop": [
   "🎨ff0099cc",
   "R-b-2 — css-variables-1 §3: a var() in the animation family is valid at parse time",
   "component values — comma, then slash, then space lists; calls; scalars",
   "keyframe selectors — css-animations-1 §3.2, scroll-animations-1 §4.2",
   "… 74.9%, … 50%, … 100%"
  ],
  "functionParam": [
   "oklch(🎨0% 0.15 50deg)",
   "🎨gb(255 0 153 / 0.5)",
   "hsl(é20, 50%, 50%)",
   "éab(50% -100% 100%)",
   "🎨ff0099cc",
   "hsl(120, 50%, 50%)é",
   "rgb(255 0 153 / 0.5)é",
   "rgb(255 0 153 / 0.5)🎨",
   "rgb(é55 0 153 / 0.5)",
   " is §7 (§6.1 is Named Colors) — the INTENT, cited at the true bytes.\n// This discharges X-W12 ",
   "… 74.9%, … 50%, … 100%"
  ],
  "commaItems": [
   "oklch(🎨0% 0.15 50deg)",
   "🎨gb(255 0 153 / 0.5)",
   "hsl(é20, 50%, 50%)",
   "éab(50% -100% 100%)",
   "🎨ff0099cc",
   "hsl(120, 50%, 50%)é",
   "rgb(255 0 153 / 0.5)é",
   "rgb(255 0 153 / 0.5)🎨",
   "rgb(é55 0 153 / 0.5)",
   " is §7 (§6.1 is Named Colors) — the INTENT, cited at the true bytes.\n// This discharges X-W12 ",
   "… 74.9%, … 50%, … 100%"
  ],
  "semiItems": [
   "oklch(🎨0% 0.15 50deg)",
   "🎨gb(255 0 153 / 0.5)",
   "hsl(é20, 50%, 50%)",
   "éab(50% -100% 100%)",
   "🎨ff0099cc",
   "hsl(120, 50%, 50%)é",
   "rgb(255 0 153 / 0.5)é",
   "rgb(255 0 153 / 0.5)🎨",
   "rgb(é55 0 153 / 0.5)",
   " is §7 (§6.1 is Named Colors) — the INTENT, cited at the true bytes.\n// This discharges X-W12 ",
   "… 74.9%, … 50%, … 100%"
  ],
  "spaceItems": [
   "oklch(🎨0% 0.15 50deg)",
   "🎨gb(255 0 153 / 0.5)",
   "hsl(é20, 50%, 50%)",
   "éab(50% -100% 100%)",
   "🎨ff0099cc",
   "hsl(120, 50%, 50%)é",
   "rgb(255 0 153 / 0.5)é",
   "rgb(255 0 153 / 0.5)🎨",
   "rgb(é55 0 153 / 0.5)",
   " : \"\";\n\n// ── §1 · the 56 empty-/whitespace-body vectors ───────────────────────────────\n\n/** The ten CSS colour heads plus four non-colour heads that take the same\n *  ",
   " against the live referents —\n    //   page@0.5103          → oklch(0.9700 0.0148 32.0)   ← the CREAM COLLAPSE\n    //   resting(light) 0.8117 → oklch(0.3817 0.1300 32.0)\n    //   resting(dark)  0.3552 → oklch(0.8090 0.1092 32.0)\n    // Post-cure (referent = the resting rung + the cusp walk):\n    //   resting(light) 0.8117 → oklch(0.3817 0.1300 32.0)  (full pick chroma)\n    //   resting(dark)  0.3552 → oklch(0.8252 0.0984 32.0)  (cusp-capped)\n    const OWNER_BRICK = \"oklch(0.51 0.13 32)\";\n    const OWNER_AMBIENT = 0.5103; // §5.1 live ",
   " is\n// §5.1 and ",
   " is §7 (§6.1 is Named Colors) — the INTENT, cited at the true bytes.\n// This discharges X-W12 ",
   " members. */\nconst PROTO_KEYS = [\"constructor\", \"__proto__\"] as const;\n\nconst PROTO_ENTRIES = [\n    [\"parseCssColor\", (key: string) => parseCssColor(key)],\n    [\"parseCssScalar\", (key: string) => parseCssScalar(key)],\n    [\"parseCssValue\", (key: string) => parseCssValue(key)],\n    [\"parseCssValues\", (key: string) => parseCssValues(key)],\n    [\"coerceToSyntax\", (key: string) => coerceToSyntax(key, \"*\")],\n] as const;\n\n/** The embedded forms — ",
   " re-exports. This unwraps it once so the round-trip assertions\n * below read the string they are actually about — no forwarding shim survives\n * in ",
   " where\n    // it once threw. The consumer branches on that shape — there is no\n    // ",
   ". */\nconst HEADS = [\n    \"rgb\", \"rgba\", \"hsl\", \"hsla\", \"hwb\", \"lab\", \"lch\", \"oklab\", \"oklch\", \"color\",\n    \"calc\", \"linear-gradient\", \"color-mix\", \"steps\",\n] as const;\n\n/** An empty body and a whitespace body — ",
   "<easing-function> — css-easing-2",
   "D6 — the certification headroom (the interim-model tolerance)",
   "O-14 · sampleInterpolationRamp ≡ the library (strict)",
   "R-b-2 — css-variables-1 §3: a var() in the animation family is valid at parse time",
   "W7-4 — the seal ink (the SEEDS.md w7 rider; R1 survivor)",
   "color(display-p3-linear …) (SC-2) — admitted, written as the exact xyz it names",
   "component values — comma, then slash, then space lists; calls; scalars",
   "css-color-4 §4 — math functions in every component (SC-1), resolved at parse where absolute",
   "every stop serializes paintable — oklch(L C H[ / A]), never a raw hsv()/xyz() string",
   "honest absence: <2 operands → null; unparseable operand → null",
   "keyframe selectors — css-animations-1 §3.2, scroll-animations-1 §4.2",
   "… 74.9%, … 50%, … 100%"
  ]
 }
}
