# SK-V12 W4 CHALLENGE V1 - CH6 Anti-Paper-Close

Verdict: REVISE.

W4 has a legal ADMIT-candidate path without W4 moving `RESULTS.md`, but only as
a W4 disposition feeding W5 close. It cannot be final campaign ADMIT.

## Findings

1. REDRESS-125 is a `PASS-ADMIT-CANDIDATE`, not final close and not
   `RESULTS.md` movement. W4 must consume current post-W4 CSS/JSON/orphan
   state before `BEHAVIOR-PASS-CSS-ADMIT`, not just cite REDRESS-125.

2. Micro-prove-first is stated but not enforceable enough. PLAN-V2 must name a
   W4-specific gate/report consumer for microbench and same-wave-consumer
   evidence, and align the checkasm/parity requirement with the selected
   caller API rather than relying only on the primitive mask checkasm.

3. Orphan zero must be evidence, not assertion. PLAN-V2 must make
   `orphan-disposition.md` a hard W4 output with A4's fields: status,
   production grep, test/checkasm status, REDRESS adjacency, material
   differential, and final `orphan_count=0` calculation.

4. W3 skip is correct only for ADMIT, not FIXPOINT. SPEC Section 10 and A6
   allow W3 to remain skipped on the REDRESS-125 ADMIT path, but W3 becomes
   mandatory if W5 closes as FIXPOINT.
