# HARDENING-PASS-2-V3 — Independent rerun against amended codegen synthesis

## §1 Target identification

| Field | Value |
|---|---|
| Target | `PASS-2` |
| Path | `restart/audit/pass-2-codegen/PASS-2.md` |
| Lines audited | 1-559 |
| Sub-agent reports audited | `agent-{1..6}*.md` (`restart/audit/pass-2-codegen/`) — 582 lines combined |
| Wave-2 classification | `restart/audit/pass-2-codegen/wave-2-classification-amendment.md:1-29` |
| Amendment commits | `2778f34d` (Wave-1.2: BIR confirmation + import-deny + payload refinement), `d206b895` (Wave-2: emission contract + lookbehind + emission table + budgets + SOTA + carries + OpenFrame) |
| Audit time | 65 minutes |
| Sister-audit comparator | `restart/audit/hardening/HARDENING-PASS-2-V2.md` (§6, post-audit comparison only) |
| Authority anchors | `restart/README.md:13-25,108-117,285-340,381-396`; `restart/locks/LOCKS.md:34-60,118-125`; `docs/precepts/instructions/STYLE.md`; `docs/precepts/instructions/LESSONS-LEARNED.md` |

PASS-2 is a single architectural pass, not a multi-wave execution plan. Lane 2 collapses to the "counted-inheritance" row only; Lane 8 carries the deferral audit.

## §2 Cohort verdict

| Lane | Verdict | KEEP | REINVENT | DISCARD | Recommendation |
|---|---|---:|---:|---:|---|
| 1 — Lock-Adherence | AMENDMENT-REQUIRED | 9 | 4 | 0 | Add canonical `passes::layout` / `LayoutFacts` / `LayoutSink` wording (Lock 2); declare PASS-2 obligation under unified-cursor-plus-byte-skip with eager-empty elision (Lock 3); spell `parse / parse_in / parse_owned` lowering surface or explicit PASS-3 cession (Lock 9). |
| 2 — Sequencing Discipline | N/A | 1 | 0 | 0 | Wave-by-wave inheritance row at lines 506-516 is inheritance accounting, not executable sequencing. Lane 8 covers the carries. |
| 3 — Cohesion | AMENDMENT-REQUIRED | 5 | 2 | 0 | Bind the consumer-acceptance gate text at lines 338-345 to artefact paths under `runtime/src/grammars/<name>/` rather than prose-only. Tighten yaml proof at §5 to verify "two onboarding surfaces only". |
| 4 — SOTA Anchoring | KEEP | 4 | 0 | 0 | Throughput trajectory at lines 429-437 is row-complete (competitor / dataset / platform / target / mechanism / evidence). Mechanism rows at lines 441-446 are correctly demoted. |
| 5 — Grammar-Authoritative | AMENDMENT-REQUIRED | 4 | 2 | 0 | yaml proof at lines 368-374 needs verbatim two-surface invariant: a command that proves only `grammars/yaml.bbnf` plus the metadata block were added. The Lock 14 grep verifications at `restart/locks/LOCKS.md:60` are not quoted in PASS-2. |
| 6 — Generated-Code Budget | KEEP | 5 | 0 | 0 | Per-grammar generated_loc + xtask wall ceiling + baseline + SYNTHESIS carry pointer all present (lines 380-394). Non-generated child-count proof + per-area enforcing command present (lines 400-410). |
| 7 — Friction-Forecast | AMENDMENT-REQUIRED | 2 | 4 | 0 | PASS-2-owned codes carry no verbatim user-facing strings; Pratt/SIMD misfire diagnostic absent; lifetime-error verbatim string absent. |
| 8 — Carry & Deferral | KEEP | 4 | 1 | 0 | 8-row Receiver/Blocker/Receiving-gate ledger at lines 531-540. One row (BIR snapshot baseline at line 419) names provisional-owner but no concrete commit gate. |
| 9 — Greenfield Discipline | KEEP | 5 | 1 | 0 | Replacement posture sound; ParseStream/rewrite-mode/Unicode-set/declaration-crate stale text retired with conflict-ledger evidence at lines 11-17. |
| **Total** | **AMENDMENT-REQUIRED** | **39** | **14** | **0** | Eight surgeries before PASS-2 advances. |

Final decision: **AMENDMENT-REQUIRED**. The architecture survives every adversarial probe; eight surgical edits close residual lock-vocabulary, friction-diagnostic, and yaml-proof gaps.

## §3 Lane 1 — Lock-Adherence

Lane standard: every one of the 14 locks is settled authority. PASS-2 must honour those that fall in its layer (codegen + runtime template) and either honour or non-own the rest without contradiction.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:13`,`:36`,`:443` | Lock 1 — Tape + direct-to-struct union | Tape is named the runtime substrate; typed views borrow `&'i Tape<'i>` plus node id; OpenFrame deletion is mechanism-gate. | Honours `restart/locks/LOCKS.md:34` and `restart/README.md:285-314`; the conflict-ledger row at line 13 explicitly retires the prompt's stale `ParseStream` surface. | Tape record byte-layout is not specified here; that lives in PASS-3 per `restart/README.md:312`. | A challenger demands PASS-2 own byte layout. Defeated: PASS-2 owns codegen/runtime template; PASS-3 owns API + ABI. The handoff at lines 322-345 records the boundary. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:67` (BIR `Layout`), `:459` (perf row), `:475` (`@layout` analysis output) | Lock 2 — Layout-lowering canon | The BIR carries a `Layout` variant with `@layout` analysis as generation site; runtime emission table cites `@layout` analysis output per grammar. | The intent honours Lock 2: layout is a first-class pass output, not a TypeDesc/StructLayout/TypeMap residue. | The canonical wording — `passes::layout`, `LayoutFacts`, `LayoutSink` — is absent. PASS-2 references neither the canonical pass module name nor the public side-table name. | Steelman: PASS-2 may not need the canonical strings if the BIR variant captures the obligation. Defeated: `restart/locks/LOCKS.md:36` makes the wording lock authority; downstream consumers (PASS-1 / SYNTHESIS) read PASS-2 looking for this surface. Add a one-line clause naming the upstream pass module + side-table. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:38` (Rust V1 emits parser functions), `:340` (`parse_signature_compile` gate) | Lock 3 — Cursor + byte-skip unified, eager-empty elision | Rust V1 lowerer emits parser functions and the runtime template owns parse signatures. | The PASS-3 handoff names parse signatures; the consumer acceptance gate at line 340 binds compile parity. | PASS-2 is silent on the unified cursor/byte-skip implementation surface and on the `__EAGER_EMPTY_PATH` elision. The lowering of one parse implementation that consults cursor only when consult returns `Skip` is not declared anywhere in PASS-2. | A challenger argues PASS-1 owns the parse mechanism. Partly defeated: PASS-1 owns the BIR producer; PASS-2 owns lowerer code that emits the unified parse loop. PASS-2 must declare at least which BIR variants drive the cursor branch and which elide it (likely `Ref`, `Lit`, `RegexDfa`, `Scanner`). | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:32` (Backend IR is the boundary), `:220-241` (import-deny gate) | Lock 5 — IR + per-backend lower | BIR types live at `ir/src/backend_ir/` per line 188; `codegen/src/backend_ir/README.md` is documentation only; lowerers consume BIR; verbatim deny command at line 239 closes the boundary. | The Wave-1.2 amendment correctly relocated ownership; the producer-side exemption for `passes/extract/` at line 242 names the only Grammar IR consumer. | The deny command at line 239 names two paths (`crates/codegen/src/lower`, `crates/codegen/src/runtime_template`) but elides `crates/codegen/src/backend_ir/` itself. A future contributor might hand-author BIR adapters there. | Steelman: README-only directory is documentation, not source. Defeated: the deny gate should still scan the documentation directory's source listing. Tighten the gate to `crates/codegen/src/`. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:46`,`:302-317`,`:476` | Lock 6 — xtask emits committed source | Content-equality writing kept (line 317); regen split into 9 sub-modules under `xtask/src/regen/`; hand-written prohibition stated at line 476. | Honours `restart/locks/LOCKS.md:44` and tracks current regen at `xtask/src/regen.rs:400-461`. | Per-sub-module LOC budgets are stated at line 408 (4-10 children, no row > 500 LOC), and content-equality remains unchanged. | A challenger points at the new sub-modules being silent on a per-file LOC ceiling. Defeated: line 408 explicitly states the per-file row check. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:332` (`path-core` consumer), `:343` (`grammar_schema_load` gate) | Lock 7 — Path crate consolidation | PASS-2 hands path metadata to `path-core` via emitted runtime; consumer acceptance gate binds the load. | Avoids resurrecting `runtime/path.rs` (CENSUS deletion target); aligns with PASS-3 path ownership. | The `path-ts` deferral is at line 539; the in-grammar test surface for `path-core` consuming generated metadata is named but does not specify which grammar's emitted descriptor is consumed first. | Steelman: PASS-2 cannot specify a per-grammar gate; that is per-X. Defeated: line 343 names a load test; per-X is owned by the runtime emission table at lines 463-474. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:425-446` | Lock 8 — Surpass SOTA, not AU | Throughput trajectory at lines 429-437 names competitor + dataset + platform + bbnf target + mechanism + evidence per row; mechanism rows at 441-446 are correctly demoted. | Every parse-throughput row carries the Lock 8 tuple; AU is unmentioned anywhere in the document. | None — the row-complete shape is the corrected post-Wave-2 outcome. | Defeated: this lane is closed by the Wave-2 amendment. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:340-345` (consumer acceptance gates) | Lock 9 — Slice-borrow primary, bumpalo + owned escapes | The `parse_signature_compile` gate at line 340 mentions `parse_<grammar>` and `parse_owned_<grammar>`. | The escape hatches reach naming. | `parse_in(input, &bump)` is unmentioned in PASS-2; the bumpalo arena lifetime + `'arena: 'i` constraint named at `restart/README.md:310` is silent. The slice-borrow / Cow / String trio is not enumerated in the runtime template parameter schema (lines 130-143). | Steelman: PASS-3 owns API surface. Defeated only partially: PASS-2 owns the lowerer that must emit the three fn signatures; the schema must declare bump-arena lifetime parameter. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:73` (`PrattSpine` variant), `:74` (`SimdScan` variant), `:160-164` (detection thresholds) | Lock 10 — Pratt + SIMD auto-detected | PASS-2 names auto-detection via cost model and detection-threshold table; no `@pratt` / `@simd` directives appear. | Honours `restart/locks/LOCKS.md:52` and `restart/README.md:180-182`. | None at architecture level. Friction-side misfire diagnostic absent (Lane 7 surgery). | Defeated: directive-free detection is the closed surface. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:65` (`RegexDfa` carries Unicode), `:163-165` | Lock 11 — Path-deps for incubating sister crates | Unicode lives at `parse-that/regex`; grammar-level Unicode set surface is rejected at line 15. | Avoids grammar-level Unicode algebra; aligns with `restart/README.md:131-143`. | PASS-2 does not state whether `parse-that` remains path-dep until stable; that nonownership is implicit. | Steelman: this is a workspace-shape concern owned by SYNTHESIS. Defeated. | KEEP |
| (no PASS-2 site) | Lock 12 — ser/gorgeous archive precondition | PASS-2 does not touch archive ceremony. | Silence is the correct posture for a codegen pass. | Silence. | Steelman: a hardener might demand PASS-2 own the precondition. Defeated: Tranche A.W0 owns the ceremony per `restart/README.md:394`. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:174-223`,`:400-410` | Lock 13 — No god directories | Per-crate trees at lines 174-223 list 4-10 children per directory; non-generated LOC + child-count proof at lines 400-410 binds enforcement commands. | Responds to CENSUS god-module evidence at `restart/corpora/CENSUS.md:321-354`; sibling-API uniformity is implied by the runtime emission table. | The 9-grammar runtime sub-tree at line 263-269 (`grammars/<name>/`) is explicitly generated; the cap exemption at line 410 is correctly stated. | A challenger demands a child-count proof for the generated runtime tree itself. Defeated: line 410 covers it. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:48` (no per-grammar declaration crates), `:368-374` (yaml proof), `:476` (hand-written prohibition) | Lock 14 — Full grammar generalisation | yaml smoke proof present; per-X tables at runtime emission (463-474) and generated LOC (380-392); hand-written-runtime prohibition stated. | Honours the two onboarding surfaces (`restart/README.md:13-25`); rejects per-grammar declaration crates as default. | The Lock 14 verification commands at `restart/locks/LOCKS.md:60` (`rg JsonParser\|CssL4Parser\|...`, `find crates/core/src/runtime`) are not quoted in PASS-2. yaml proof at line 368-374 does not specify a "two-surface diff" command verifying that only `grammars/yaml.bbnf` + the metadata block were added. | Steelman: the verification commands are owned by the lock document. Defeated: PASS-2 must assert and quote them, or the proof is prose-only. | REINVENT |

Lane verdict: **AMENDMENT-REQUIRED** — KEEP 9 / REINVENT 4 / DISCARD 0.

Surgery list: layout-canon wording (Lock 2); cursor + byte-skip unified obligation (Lock 3); BIR-tree deny path tightening (Lock 5); slice-borrow / `parse_in` / `parse_owned` lowering surface (Lock 9); Lock 14 verification commands (Lock 14).

## §4 Lane 2 — Sequencing Discipline (counted inheritance row)

Lane standard: PASS-2 is a single architecture pass. Inheritance carries are accounted in Lane 8; this lane verifies the wave-by-wave inheritance row does not impersonate executable sequencing.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:506-516` | Wave-by-wave inheritance ledger | Maps BC.W0..W3 + BD.W0..W4 inheritance into the greenfield codegen layer; explicitly marks BD.W5 parity as downstream. | Avoids pretending PASS-2 itself runs multi-wave work; keeps TS production and BD parity outside PASS-2's gate. | The header reads "Wave-by-wave carries", which can be misread as a sequencing claim. | A hardener could force Lane 2 to apply. Defeated: these are inheritance carries; Lane 8 owns the deferral discipline. | KEEP |

Lane verdict: **N/A** — KEEP 1 / REINVENT 0 / DISCARD 0.

## §5 Lane 3 — Cohesion

Lane standard: every claim verifiable from artefacts the target produces or cites; no orphan claim, no orphan deliverable.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:50-77` | 23-variant BIR table | Variants enumerated with payload, generation site, Rust lowering, WASM lowering, TS status. | Cardinality defence at line 78 cites MLIR `arith` (60), Cranelift `InstructionData` (40), rustc HIR `ExprKind` (35), chalk `TyKind` (23) — all live cite paths. | The cardinality defence cites a swc rustdoc URL rather than path:line at line 78; that breaks the path:line discipline. | A challenger argues external-doc citation is acceptable since the local repo lacks swc. Partly defeated: replace with a corpus citation (`restart/corpora/SOTA.md:N` is the local proxy). | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:111-123` | `BackendLowerer` 8-method API | Public trait collapses the current `Emitter` 566-line surface. | Verifiable against `crates/core/src/backend/emitter.rs:31-566`. | The trait surface lacks an explicit `emit_pratt_table` / `emit_simd_alphabet` method; both are folded into `emit_node`/`emit_scanner_tables`. The runtime emission table at lines 463-474 implies them. | Steelman: keeping the trait at 8 methods is the Lock 13 cohesion point. Defeated: line 124 explicitly cites the 8-method PASS-B forecast. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:127-145` | Runtime template parameter schema | 13-row table maps parameter to required source and generated consumer. | Extends the BB cohort template precedent at `docs/tranches/BB/audit/W2-cohort-template-spec.md:8-22`. | The schema lacks rows for visitor bitflag (`VisitTypes`), bumpalo arena lifetime, and incremental-parse marker; PASS-3 carries them but the parameter set should at least name visitor bitflag for runtime emission. | A challenger points to the runtime emission table at lines 463-474 carrying visitor reference. Defeated only partially: visitor bitflag is named in `restart/README.md:316-318` and feeds runtime emission directly; the schema row should exist. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:147-156` | SIMD coverage matrix | aarch64 NEON / x86_64 AVX2 / AVX512 / wasm32 / scalar with current-inheritance + PASS-2 obligation. | Backed by `crates/simd-scan/src/lib.rs:19-29` and `:70-114`. | Per-platform throughput target is silent; mechanism-row demotion at lines 441-446 covers it. | Defeated: the parity-target shape is the SOTA gate at lines 436-437. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:323-345` | PASS-3 handoff + consumer acceptance gates | 8-row contract → 6 named verification commands at lines 340-345. | Replaces prose-only handoff with `cargo test`/`cargo bench`/`cargo xtask` commands. | The acceptance commands target `crates/runtime/...`/`crates/path/...` paths but do not bind a per-grammar fixture path. | Steelman: per-grammar fixtures live at `restart/corpora/MODULES.md` and runtime emission table. Defeated: the gate is consumer-shape, not per-grammar; per-grammar fixtures live downstream at BD.W4. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:351-364` | PASS-1 handoff | 8-row product/use mapping; SYNTHESIS named as reconciler at line 364. | Honest about parallel-Phase-1 absence of PASS-1 artefacts. | The receiving gate for SYNTHESIS Wave-2 reconciliation is named at line 533 but not at the §5 boundary; cohesion suffers from the cross-section pointer. | Defeated: the carry ledger at lines 531-540 owns the gate. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:367-374` | yaml future-grammar smoke | 5-row Surface/Proof table with the `cargo xtask bbnf build yaml --check && cargo test -p runtime future_grammar_yaml_runtime` gate. | Reaches Lock 14 onboarding surface. | The proof does not verify the "two surfaces only" invariant: a `git diff` / `find` command that proves only `grammars/yaml.bbnf` and the metadata block were added is absent. The Lock 14 verification commands at `restart/locks/LOCKS.md:60` are not quoted. | Steelman: the proof captures the build outcome, which is a stronger end-state. Defeated: the input invariant is the lock; without it, a contributor can pass the build by adding hidden Rust code. | REINVENT |

Lane verdict: **AMENDMENT-REQUIRED** — KEEP 5 / REINVENT 2 / DISCARD 0.

## §6 Lane 4 — SOTA Anchoring

Lane standard: every parse-throughput gate cites competitor + dataset + platform + bbnf target. Non-throughput rows must not claim Lock 8 honour.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:425` | Performance gate prelude | Prose names twitter / canada / citm / bootstrap / animate / 7 GB/s with corpus path:lines. | Anchored at `restart/corpora/SOTA.md:50-58`/`:73-89`/`:130-136`. | None — prelude is prose, table follows. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:429-437` | Throughput trajectory table | 7 rows: twitter / citm / canada / bootstrap / animate + 2 simdjson on-demand platform splits. | Every row names competitor, dataset, platform, target, mechanism, evidence artefact (cargo bench command or kernel parity report). | The kernel-parity rows (lines 436-437) cite "kernel parity + index throughput report" instead of a `cargo bench` command. | Steelman: kernel parity is not a parser benchmark; `cargo bench` does not apply. Defeated: the artefact name should still resolve to a path; the report path is unspecified. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:441-446` | Mechanism gate table | 3-row table: OpenFrame deletion, Pratt auto-detection, WASM parity. | Correctly demoted from throughput gate; OpenFrame row reads as deletion (line 443), not preservation. | None — the Wave-2 amendment cleaned the language. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:451-460` | Per-construct contribution plan | Maps `AltDispatch` / `AltSpeculative` / `SimdScan` / `RegexDfa` / `PrattSpine` / `HostCall` / `Layout` to expected contribution + anchor citation. | Anchored at `restart/corpora/RESTART-SKETCH.md:201-217`/`:154-184`/`SOTA.md:73-89`/etc. | None — contribution narrative is mechanism-scope, not throughput claim. | Defeated. | KEEP |

Lane verdict: **KEEP** — KEEP 4 / REINVENT 0 / DISCARD 0. Lane 4 is closed by Wave-2 amendment.

## §7 Lane 5 — Grammar-Authoritative Discipline

Lane standard: zero `match grammar { Json => ..., CssL4 => ..., ... }` arms; per-X tables for every "all grammars" claim; future-grammar onboarding test for `yaml.bbnf` via two surfaces only.

Verification:

```
rg -nP "match\s+\w+\s*\{[^}]*Json\s*=>|CssL4\s*=>|Bbnf\w*\s*=>|GoogleSheets\w*\s*=>" restart/audit/pass-2-codegen/PASS-2.md
```

Result: zero matches.

```
rg -ni 'json|css_l4|bbnf|google_sheets|sheets|css_pretty|bnf|csv|ebnf|math' restart/audit/pass-2-codegen/PASS-2.md | wc -l
```

Result: ~95 matches; classified below.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:380-392` | Per-grammar generated_loc table | 11-row table (9 grammars + yaml smoke + total) with current LOC, PASS-2 max, disposition, xtask wall ceiling, baseline. | Per-X ratification of every grammar carries Lock 14's positive shape. | None — per-X table is the lock-honoured shape. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:463-474` | Per-grammar runtime emission table | 10 rows (9 grammars + yaml smoke), 7 columns (`generated.rs`, `parser.rs`, `host.rs`, host source, layout source, error source, Pratt/SIMD source). | Most thorough per-X table in the document; hand-written prohibition at line 476. | The Pratt/SIMD column for `bbnf` row says "`PrattSpine` LUT for grammar operator family" — this is a generated emission, but the per-row choice is grammar-derived from grammar shape, which is the correct posture. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:480-490` | Per-grammar smoke gate table | 10-row table mapping grammar to smoke gate (twitter/citm/canada for json; bootstrap/animate for css_l4; etc.). | Per-X gate enumeration; matches the SOTA evidence rows at lines 431-435. | The smoke gate for `bbnf` says "parse grammar corpus and emit metadata"; the underlying corpus path is unspecified. | Steelman: corpus path lives at `restart/corpora/MODULES.md`. Defeated: cite `crates/core/grammars/bbnf.bbnf` or `restart/corpora/MODULES.md:N`. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:368-374` | yaml two-surface onboarding proof | 5-row table specifies grammar source / metadata / runtime emission / registry / gate. | Reaches the future-grammar onboarding posture. | The proof is end-state only: it validates yaml builds + tests pass. It does not verify the "two surfaces only" input invariant — i.e., that the diff against the prior commit consists exclusively of `grammars/yaml.bbnf` and one `[workspace.metadata.bbnf.grammars.yaml]` block. The Lock 14 verification commands at `restart/locks/LOCKS.md:60` (`rg JsonParser\|CssL4Parser\|...`, `find crates/core/src/runtime`, `match grammar` grep) are not quoted in PASS-2. | A challenger argues the onboarding test owns build + test as the only fail-fast surface. Defeated: a contributor could still add hidden Rust code in a generic crate and pass the build. The two-surface invariant must be a verification command. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:474` | yaml in runtime emission table | `yaml (smoke)` row with `auto-detected from grammar shape` for the Pratt/SIMD column. | Per-X table entry for the future grammar present without hardcoding. | The Pratt/SIMD column cell cites "auto-detected from grammar shape" — correct posture per Lock 10. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:391` | yaml in generated_loc | `yaml (smoke) | 0 | ≤ 4,000 | future-grammar | ≤ 4s | provisional (owner: SYNTHESIS Wave-2)`. | Carries an explicit provisional baseline with named owner. | None — provisional ownership is correctly bound to SYNTHESIS Wave-2. | Defeated. | KEEP |

Lane verdict: **AMENDMENT-REQUIRED** — KEEP 4 / REINVENT 2 / DISCARD 0.

Per-X table presence verified for: generated_loc (lines 380-392), runtime emission (463-474), smoke gate (480-490), xtask wall ceiling (380-392 join), throughput trajectory (429-437 — JSON datasets per SOTA).

Per-X tables absent for: visitor bitflag per grammar (PASS-3 receives); path schema descriptor per grammar (PASS-3 receives); diagnostic vocabulary per grammar (PASS-3 receives). These cessions are explicit at the §4 PASS-3 handoff; not a fault.

## §8 Lane 6 — Generated-Code Budget

Lane standard: every proposed crate / module / wave carries a generated-LOC budget, an xtask regen-cycle wall budget, and a per-grammar LOC delta projection. Faults: silent budgets, tranche-level budgets without wave-level decomposition, missing baselines.

PASS-2 carries the strongest budget seed of the four hardening targets.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:380-392` | Per-grammar generated_loc | 11 rows: current_loc / PASS-2 max (initial +2%) / disposition / xtask wall ceiling / baseline. | Per-X budget; explicit baseline category (observed vs provisional with owner); hotspot disposition for css_l4 + google_sheets. | css_l4 max at 109,281 (+2%) is conservative; once REINVENT lands the ratchet should reduce. The disposition row says "REINVENT hotspot" but no follow-on budget for post-REINVENT max. | Steelman: the post-REINVENT ratchet is downstream tranche work. Defeated: the +2% is initial; a follow-on row would tighten the budget but is correctly deferred to the receiving tranche per the carry ledger. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:394` | SYNTHESIS carry pointer | "SYNTHESIS Wave-2 carries this table into MASTER-PLAN.md and ARCHITECTURE.md per HARDENING-CONSOLIDATED §4.24". | Explicit cross-target binding; row-for-row identity required. | None — the binding is precise. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:400-410` | Non-generated LOC + child-count + enforcing command | 7-row table: ir / codegen-rust / codegen-wasm / runtime_template / runtime / host / xtask-regen. Each row: 500 LOC cap + 4-10 child target + `find` command. | Honours Lock 13 at file + directory level; enforcing command per area. | The runtime row at line 406 says "(handwritten only)" — cited because `runtime/src/grammars/<name>/` is generated; but the `find` command at line 406 excludes only `runtime/src/grammars/*`, not nested `<name>/host.rs` artefacts. | Steelman: the exclusion is correct because the entire `grammars/` subtree is generated. Defeated: the exclusion uses `-not -path 'crates/runtime/src/grammars/*'` which excludes any depth — correct. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:414-421` | Regen-cycle wall budget | 6-row table: regen check after metadata-only change ≤ 22s / cohort grammar regen ≤ 4s / css_l4 regen ≤ 12s / BIR snapshot all 9 ≤ 5s / write phase / yaml smoke regen ≤ 4s. Each carries baseline category. | Baseline category column is explicit (observed vs provisional with owner). | The BIR snapshot row at line 419 names the owner ("PASS-2 amendment agent") and receiver ("SYNTHESIS Wave-2 measurement gate") but no concrete commit gate or fixture path. | Steelman: provisional means the gate is open until measurement lands. Defeated: a concrete gate path (`target/codegen/bir-snapshot.bench.json`?) would tighten. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:171-223` | Per-crate tree | 6 trees: ir-backend_ir / codegen / runtime / host / simd-scan / xtask-regen. Each tree obeys 4-10 children per Lock 13. | Each tree validates the directory budget; sibling-API uniformity implicit. | None at file level. | Defeated. | KEEP |

Lane verdict: **KEEP** — KEEP 5 / REINVENT 0 / DISCARD 0. Lane 6 is closed; PASS-2 carries the budget seed for SYNTHESIS Wave-2.

## §9 Lane 7 — Friction Forecast

Lane standard: forecast where users and grammar authors hit the new API and do not understand it. Particular foci: layout lowering errors, Pratt + SIMD auto-detection misfire diagnostics, lifetime escape hatches, lookbehind width violation, runtime-template emission failures.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:520-527` | Diagnostic ledger | 6-code table: `BBNF-GEN001` (lowerer imports Grammar IR) / `BBNF-GEN014` (LOC overrun) / `BBNF-CODEGEN021` (BIR snapshot drift) / `BBNF-CODEGEN033` (template-metadata gap) / `BBNF-LIFE009` (lifetime surface violation) / `BBNF-SEM040` (unbounded lookbehind). | Codes are PASS-2-owned and tied to producers. | None of the codes carries a verbatim user-facing string. The lookbehind diagnostic at line 168 quotes `BBNF1004` but cites it as PASS-1-owned; PASS-2's `BBNF-SEM040` is named without a string. | Steelman: PASS-2 owns routing; PASS-1 owns user strings. Defeated: friction-forecast lane requires verbatim error strings; routing diagnostics still need a user-facing surface (the developer reading codegen output is still a user). Add a verbatim string per code. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:160-164` | Detection threshold table | 4-row table: Pratt / SIMD / PHF / Lookbehind with select-when, reject-when, evidence. | Auto-detection thresholds explicit; matches `restart/locks/LOCKS.md:52`. | The misfire diagnostic for Pratt and SIMD is absent. If the optimizer classifies a non-Pratt grammar shape as Pratt, the user sees no error, only a wrong codegen. | Steelman: misfire is a cost-model bug, not a user error. Defeated: when the optimizer rejects an apparent Pratt candidate, the grammar author should see why; without this, friction is infinite. Add a `BBNF-OPT-NNN` code with verbatim diagnostic string ("rule {rule} resembles operator chain but {reason}; promote to Pratt with {hint}"). | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:166-168` | Lookbehind diagnostic routing | `|<` finite-width contract; `BBNF1004` (PASS-1 user-facing) and `BBNF-SEM040` (PASS-2 routing) emitted together. | Codegen-side ratification of grammar surface; explicit two-diagnostic split. | None at architecture level. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:474` | yaml friction surface | `yaml (smoke)` row says `auto-detected from grammar shape`; no friction surface for what happens when auto-detection fails. | Auto-detection posture is correct. | A grammar author whose yaml-bbnf has a Pratt-like construct that auto-detection fails to recognize has no error surface; the build silently emits a slow recursive-descent path. | Steelman: cost-model emits decision logs (`agent-5-pratt-simd-auto-detection.md` mentions decision logs). Defeated: the decision log is producer-side; the user surface is missing. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:526` | `BBNF-LIFE009` | Emitted owned/borrowed constructor violates lifetime surface; runtime compile tests trigger. | The code exists. | Verbatim error string absent; the Lock 9 lifetime triplet (`'i` / `'arena` / owned) needs a per-violation message. The friction is highest here: a grammar author who annotates `-> Color` and the codegen emits a `&'i str` field where they expected `String` will see a compile-time lifetime error from a generated file. | Steelman: rust compiler error suffices. Defeated: a generated-source error is harder to read than a hand-written; PASS-2 should add a comment-emission contract that the generated file carries a `// BBNF-LIFE009: <msg>` line. | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:344` | `diagnostic_vocabulary` consumer gate | PASS-1 codes round-trip through PASS-3 user-facing error type. | Cross-pass binding. | None — gate is precise. | Defeated. | KEEP |

Lane verdict: **AMENDMENT-REQUIRED** — KEEP 2 / REINVENT 4 / DISCARD 0.

Surgery list: verbatim strings for `BBNF-GEN001` / `BBNF-GEN014` / `BBNF-CODEGEN021` / `BBNF-CODEGEN033` / `BBNF-LIFE009` / `BBNF-SEM040`; new `BBNF-OPT-NNN` for Pratt/SIMD misfire; comment-emission contract for generated lifetime errors.

## §10 Lane 8 — Carry & Deferral

Lane standard: every "deferred to" / "carries to" / "future" / "TBD" names (a) receiver, (b) blocker, (c) receiving gate. Faults: any without all three.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:531-540` | 8-row carry ledger | Receiver / Blocker / Receiving gate triple per row: PASS-1 reconciliation / PASS-3 metadata / TS production / BD.W5+J parity / publication / fixtures / `path-ts` / WASM ABI. | Each row carries the three-column lock; receivers are concrete tranches (SYNTHESIS Wave-2, Tranche E/G, BD.W1, BD.W4, BD.W5, A.W1, J.W1, J.W3). | The TS production row at line 535 says receiving gate is "BD.W1 with the same BIR snapshot consumed by Rust V1" — gate name precise; blocker is "TS production deferred by PASS-2 prompt" — accurate. | A challenger argues "fictional successors" if the tranches are not drafted yet. Defeated: the tranches are letter-anchored at `restart/README.md:446` and BD waves exist at `docs/tranches/BD/waves/W{1,2,3,4,5}.md`. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:419` | BIR snapshot baseline carry | Owner: PASS-2 amendment agent; receiver: SYNTHESIS Wave-2 measurement gate. | Explicit owner + receiver. | The receiving gate is not a concrete commit / file path; "measurement gate" is a phase, not a verifiable artefact. | Steelman: provisional ownership permits abstract gates. Defeated: even provisional gates should name a target file (e.g., `target/codegen/bir-snapshot.bench.json`). | REINVENT |
| `restart/audit/pass-2-codegen/PASS-2.md:391`, `:421` | yaml smoke regen carry | Owner: SYNTHESIS Wave-2; receiver: Tranche A/G/F yaml onboarding gate. | Bound to a tranche. | A/G/F is a multi-tranche disjunction; one of A, G, or F should be the canonical receiver. | Steelman: the yaml onboarding can land at any of the three. Defeated: the carry should pin one tranche (Tranche G runtime publication is the natural receiver per `restart/README.md:446`). | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:533` | PASS-1 reconciliation | Receiver SYNTHESIS Wave-2 + Tranche E (typed-IR consolidation); blocker is variant-schema drift; gate is `cargo xtask bbnf bir --all --check` byte-identical against committed snapshot. | Concrete gate. | None — the gate is byte-precise. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:539` | `path-ts` proc-macro carry | Receiver Tranche BD.W1 / Tranche A.W1; blocker is Rust toolchain forbidding proc-macro path-dep sharing; gate is `path-ts` building against the same `path-core` AST. | Cites the lock at `restart/locks/LOCKS.md:46`. | None. | Defeated. | KEEP |

Lane verdict: **KEEP** — KEEP 4 / REINVENT 1 / DISCARD 0. The carry ledger is the strongest of the four hardening targets.

## §11 Lane 9 — Greenfield Discipline

Lane standard: no quick solutions, no workarounds, no legacy code uncontested, no contrivance, idiomatic gestalt. Architectural transpositions for elegance / simplicity / performance are mandatory.

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/audit/pass-2-codegen/PASS-2.md:5-7` | Replacement posture | "REINVENT codegen around Backend IR, Tape-backed runtime template, and BIR-only lowerers." Discards stale `ParseStream` naming, grammar-level Unicode sets, rewrite-mode walker, per-grammar declaration crates as default, OpenFrame checkpointing, and direct Grammar IR consumers. | Unambiguous greenfield posture; cites Lock 5 violation in current source at `crates/core/src/backend/driver/mod.rs:1-6` and `crates/core/src/backend/emitter.rs:1-13`. | None. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:11-17` | Conflict ledger | 5-row table: Tape name / Rewrite mode / Unicode set / Per-grammar declaration crates / TS production. Each conflict carries stale authority + settled resolution with path:line citations. | Resolves the inherited prompt/inheritance contradictions explicitly. | None — the table is the lock-resolution surface. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:174-188` | BIR ownership tightening | Wave-1.2 amendment relocated BIR types to `ir/src/backend_ir/`; `codegen/src/backend_ir/README.md` is documentation only. | Honours `restart/README.md:108-113`; closes the V1 hardening lane-1 DISCARD. | The README.md inside `codegen/src/backend_ir/` is a documentation file that may grow; the deny gate at line 239 does not scan it. | Already noted in Lane 1; surgery moves with that lane. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:497-503` | Inheritance ledger | 6-row KEEP / REINVENT / DISCARD per inheritance source: PASS-B audit / Amendment 01 / BC typed IR / BB cohort template / `simd-scan` / current source. | Every inheritance source is contested or kept with explicit reason. | None — ledger is complete. | Defeated. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:506-516` | Wave-by-wave carry table | 9-row table for BC.W0..W3 + BD.W0..W4. Each row carries KEEP / REINVENT / DISCARD with path:line citations. | Inheritance accountability honoured. | The BD.W4 row at line 516 says "Use as downstream parity fixtures. | PASS-2 fixture fleet implementation." — the DISCARD column reads as "PASS-2 does not own fixture fleet". | Steelman: PASS-2 cedes fixture fleet to BD.W4. Defeated: cession is correct. | KEEP |
| `restart/audit/pass-2-codegen/PASS-2.md:543-553` | Punch list | 10 items: BIR + 23-variant + snapshot / PASS-1-to-PASS-2 handoff tests / `Emitter` collapse + import-deny / runtime tape + TapeBuilder + OpenFrame deletion / runtime grammar template / xtask split + budgets / `simd-scan` BIR alphabet / Rust V1 first then WASM / conflict guards / cession to PASS-3/BD. | Captures architectural-transition surgery. | The punch list does not name the Lock 14 verification commands. | Steelman: the list is an implementation-task list, not a verification list. Defeated only partially: the conflict-guard item at line 552 names the four conflicts but no verification commands. Add `rg -nE 'JsonParser\|CssL4Parser\|...'` and `find crates/core/src/runtime` invocations. | REINVENT |

Lane verdict: **KEEP** — KEEP 5 / REINVENT 1 / DISCARD 0.

## §12 Tightened gate-rerun results

| Gate | Command | Expected | Actual | Pass/Fail |
|---|---|---|---|---|
| 2. Path crate naming | `rg -n "bbnf-path\|bbnf-test-fixtures\|path!"` on PASS-2.md | zero matches | zero matches | PASS |
| 3. Backend IR ownership | `rg -n "codegen/src/backend_ir"` | zero ownership claims (doc stub OK if tagged) | 2 matches at lines 188 + 225 — both explicitly tag the path as documentation-only (`README.md is documentation only` at line 225; "PASS-2 names no `codegen/src/backend_ir/` ownership path" at line 188) | PASS |
| 6. OpenFrame | `rg -n "OpenFrame"` | deletion archaeology only | 5 matches at lines 7, 36, 88, 443, 547 — every match reads as deletion (DISCARD verdict, OpenFrame checkpoint clone pathology, no-clone-stack invariant, deletion mechanism gate, "delete OpenFrame-style runtime builders") | PASS |
| 7. GrammarIR import-deny | `rg -n "GrammarIR"` | lowerer import-deny gate present with verbatim command | 2 matches at lines 5, 239. Line 239 carries the verbatim deny: `rg -n "GrammarIR" crates/codegen/src/lower crates/codegen/src/runtime_template`. Line 5 cites the current-source violation. | PASS |
| 9. SOTA rows | `rg -n "twitter\|canada\|citm\|bootstrap\|animate\|On-Demand"` | numeric competitor rows | 9 matches; throughput trajectory at lines 431-435 carries 5 numeric rows + 2 simdjson on-demand kernel parity at lines 436-437 + 2 prose context lines | PASS |
| 10. Carry-ledger | `rg -n "receiver\|blocker\|receiving gate"` | carry ledger rows | matches at 419, 421 (provisional baselines); the 8-row carry ledger at lines 531-540 has its own column headers — the ledger explicitly names Receiver / Blocker / Receiving gate per row | PASS |
| 11. yaml proof | `rg -n "yaml.bbnf\|workspace.metadata.bbnf.grammars.yaml"` | future-grammar proof | 1 match at line 370 (`grammars/yaml.bbnf` lowers to BIR); the metadata-block path string is paraphrased as "workspace metadata registers yaml" at line 371 — verbatim metadata key absent | PARTIAL |
| 12. Budgets | `rg -n "generated_loc\|regen_wall\|xtask"` | per-grammar budget + xtask wall | 17 matches — generated_loc column at line 380; xtask wall ceilings at lines 380-392; regen-wall budget table at lines 414-421 | PASS |
| 13. Diagnostic strings | `rg -n "BBNF-LIFE\|BBNF-LAYOUT\|BBNF-OPT\|BBNF-GRAMMAR\|BBNF-POINTER\|lookbehind\|HostSignature"` | expected codes + verbatim strings | 7 matches: `BBNF-LIFE009` at line 526; `lookbehind` at lines 74, 164, 168, 182, 499, 527. `BBNF-LAYOUT*`, `BBNF-OPT*`, `BBNF-GRAMMAR*`, `BBNF-POINTER*` codes absent — these are PASS-3-side codes per the consolidated catalog. `HostSignature` not used; `host signature` (lower-case) not used either | PARTIAL |

Gate verdict: 7 PASS, 2 PARTIAL. Both PARTIALs (yaml metadata-block verbatim; missing diagnostic codes) match the Lane 1 + Lane 7 surgeries.

## §13 V2 comparison (read after V3 audit was formed)

V2 read at `restart/audit/hardening/HARDENING-PASS-2-V2.md:1-90+` after the V3 body above was committed to draft.

| Aspect | V2 result | V3 result | Convergence |
|---|---|---|---|
| Final decision | **READY** (V2 §2 line 37: "BIR-ownership ratification, the lowerer import-deny gate, the runtime emission table … resolve every V1 punch item") | **AMENDMENT-REQUIRED** | **Divergent.** V2 grants READY. V3 finds eight surgical edits surviving Wave-2: layout canon vocabulary (Lock 2), cursor + byte-skip obligation (Lock 3), deny-gate path widening (Lock 5), bumpalo lifetime row (Lock 9), Lock 14 two-surface invariant (Lock 14), verbatim diagnostic strings (Lane 7), Pratt/SIMD misfire codes (Lane 7), swc URL replacement (Lane 3). |
| Lane 1 totals | KEEP 13 / REINVENT 1 / DISCARD 0 (V2 line 21) | KEEP 9 / REINVENT 4 / DISCARD 0 | V3 surfaces 3 new REINVENTs not in V2's lane 1: layout canon, cursor + byte-skip, slice-borrow lowering. |
| Lane 3 totals | KEEP 8 / REINVENT 0 / DISCARD 0 (V2 line 23) | KEEP 5 / REINVENT 2 / DISCARD 0 | V3 finds visitor-bitflag-row missing in template schema; finds yaml two-surface invariant gap; finds swc rustdoc URL discipline. V2 marks all eight rows KEEP. |
| Lane 4 totals | KEEP 7 / REINVENT 0 / DISCARD 0 (V2 line 24) | KEEP 4 / REINVENT 0 / DISCARD 0 | Convergent. Both grant the Wave-2 SOTA amendment. V3 audits 4 rows (table + mechanism + per-construct + prelude); V2 audits 7. |
| Lane 5 totals | KEEP 6 / REINVENT 1 / DISCARD 0 (V2 line 25) | KEEP 4 / REINVENT 2 / DISCARD 0 | Convergent on direction; V3 amplifies. Both surface yaml proof gap; V3 also surfaces smoke gate corpus path absence. |
| Lane 6 totals | KEEP 7 / REINVENT 0 / DISCARD 0 (V2 line 26) | KEEP 5 / REINVENT 0 / DISCARD 0 | Convergent. Both grant the Wave-2 budget amendment. |
| Lane 7 totals | KEEP 6 / REINVENT 0 / DISCARD 0 (V2 line 27) | KEEP 2 / REINVENT 4 / DISCARD 0 | **Strongly divergent.** V2 grants the 6-code diagnostic ledger as sufficient; V3 finds verbatim user-facing strings still absent, Pratt/SIMD misfire diagnostic absent, lifetime-error verbatim string absent. The friction-forecast lane test (per `restart/prompts/audit-specs/HARDENING-LENS-SET.md:131-141`) requires verbatim error messages — codes alone are not gate-passing. |
| Lane 8 totals | KEEP 8 / REINVENT 0 / DISCARD 0 (V2 line 28) | KEEP 4 / REINVENT 1 / DISCARD 0 | Convergent. Both grant the Wave-2 carry-ledger amendment. V3 surfaces the BIR-snapshot baseline as still provisional. |
| Lane 9 totals | KEEP 7 / REINVENT 0 / DISCARD 1 (V2 line 29) | KEEP 5 / REINVENT 1 / DISCARD 0 | Convergent. V2 carries the OpenFrame DISCARD as confirmed; V3 reads OpenFrame as already-deleted-language and marks lane KEEP, with a single REINVENT for the punch-list verification commands. |
| Lock 5 BIR ownership | V2 confirms ratification (KEEP at V2:48-49) | V3 confirms ratification + adds deny-gate path widening | V3 sharper at deny-gate scope. |
| Lock 2 layout canon | V2 silent at lane 1 (every row KEEP) | V3 REINVENT — `passes::layout` / `LayoutFacts` / `LayoutSink` wording missing | V3 surfaces new fault. |
| Lock 3 cursor + byte-skip | V2 silent | V3 REINVENT — unified parse + `__EAGER_EMPTY_PATH` elision obligation absent | V3 surfaces new fault. |
| Lock 9 slice-borrow | V2 silent | V3 REINVENT — `parse_in(input, &bump)` + bumpalo lifetime row missing in template schema | V3 surfaces new fault. |
| Verbatim diagnostic strings | V2 grants 6-code ledger (KEEP at V2 lane 7) | V3 REINVENT — codes lack verbatim user-facing strings | V3 sharper. |
| Pratt/SIMD misfire diagnostic | V2 silent | V3 REINVENT — friction surface absent | V3 surfaces new fault. |
| Cardinality citation | V2 silent | V3 REINVENT — swc rustdoc URL breaks path:line discipline | V3 surfaces new fault. |
| yaml two-surface input invariant | V2 grants yaml proof at lane 5 KEEP | V3 REINVENT — proof is end-state-only; no input-invariant verification command | V3 sharper. |

V3 totals: KEEP 39 / REINVENT 14 / DISCARD 0 vs V2 totals: KEEP 62 / REINVENT 2 / DISCARD 1.

**Disposition of divergence:** V2 read the post-Wave-2 surface as resolving the V1 ledger. V3 reads the same surface as resolving the V1 ledger plus surfacing residual gaps in the lock-vocabulary surface, the friction-diagnostic surface, the slice-borrow lowering surface, and the yaml two-surface invariant. Both auditors agree on every Wave-1.2 + Wave-2 amendment outcome (BIR ownership, import-deny gate, SOTA trajectory, budget tables, carry ledger, runtime emission table). The divergence is structural: V2 treats the Hardening prompt's lane-7 standard as met by routing-code presence; V3 treats it as met only by verbatim user-facing strings. The HARDENING.md §3 lane 7 standard at `restart/prompts/audit-specs/HARDENING-LENS-SET.md:131-141` reads "specify the verbatim error message" — V3's reading aligns with the prompt language.

The V3 verdict is therefore not a relitigation of V2 but a sharper read of two specific lanes (1 vocabulary + 7 verbatim strings) plus three new minor findings (cardinality citation, visitor-bitflag schema row, yaml input invariant). The 8-item punch list is surgical and amendable in 25-35 minutes.

## §14 Punch list

Ordered surgical edits for the residual V3 verdict.

| # | Target file:line | Surgery | Verdict source | Owner | Scope | Lane(s) |
|---|---|---|---|---|---|---|
| 1 | `restart/audit/pass-2-codegen/PASS-2.md:67`, `:459`, `:475` | Add a one-line clause naming the canonical Lock 2 surface: "PASS-1's `passes::layout` produces the `LayoutFacts` side-table; PASS-2's `Layout` BIR variant consumes it via `LayoutSink`." | REINVENT (Lock 2) | PASS-2 amendment | one-line addition adjacent to BIR `Layout` row | 1 |
| 2 | `restart/audit/pass-2-codegen/PASS-2.md` (new paragraph at §3 close, after line 168) | Declare the Rust V1 / WASM V1 obligation for unified cursor + byte-skip: "Rust V1 lowerer emits one parse implementation; cursor consultation generates a byte-skip when consult returns `Skip`; the empty-path case (`__EAGER_EMPTY_PATH`) elides cursor calls. The unified path is realized by `Ref` / `Lit` / `RegexDfa` / `Scanner` BIR variants; `PrattSpine` and `SimdScan` carry their own dispatch and elide cursor consultation in the inner loop." | REINVENT (Lock 3) | PASS-2 amendment | new paragraph | 1 |
| 3 | `restart/audit/pass-2-codegen/PASS-2.md:239` | Tighten the deny command to scan `crates/codegen/src/` (whole tree) rather than `crates/codegen/src/lower crates/codegen/src/runtime_template`; add an exception comment naming `crates/codegen/src/backend_ir/README.md` as documentation. | REINVENT (Lock 5) | PASS-2 amendment | one-line edit | 1 |
| 4 | `restart/audit/pass-2-codegen/PASS-2.md:127-145` (runtime template parameter schema) | Add three rows: `visitor_bitflags` (source: BIR view shapes; consumer: generated `visitor.rs` impl), `bump_arena` (source: PASS-3 API contract; consumer: `parse_in` signature lowering), `incremental_marker` (source: cost model; consumer: optional source-map sidecar). | REINVENT (Lock 9 + Cohesion) | PASS-2 amendment | 3-row table extension | 1, 3 |
| 5 | `restart/audit/pass-2-codegen/PASS-2.md:368-374` (yaml proof) | Add a "two-surface invariant" row to the proof table: `Two-surface invariant | git diff HEAD~1 reveals exactly two added paths: grammars/yaml.bbnf and one [workspace.metadata.bbnf.grammars.yaml] block in Cargo.toml. Verify with rg JsonParser\|CssL4Parser\|... crates/{ir,codegen,runtime,host,passes}/src/ returns zero and find crates/runtime/src/grammars yaml -mindepth 1 -maxdepth 1 returns the generated subdir only.` | REINVENT (Lock 14) | PASS-2 amendment | row addition | 1, 5 |
| 6 | `restart/audit/pass-2-codegen/PASS-2.md:520-527` (diagnostic ledger) | Add a fourth column `Verbatim string`. Fill: `BBNF-GEN001 → "lowerer at {file} imports Grammar IR; codegen consumes Backend IR only"`; `BBNF-GEN014 → "grammar {name} generated_loc {actual} exceeds budget {max}; ratchet upstream"`; `BBNF-CODEGEN021 → "BIR snapshot for {grammar} drifted; rerun cargo xtask regen --check and commit the diff"`; `BBNF-CODEGEN033 → "runtime template for {grammar} omits {metadata}; PASS-3 consumer cannot bind"`; `BBNF-LIFE009 → "emitted constructor for {rule} returns {actual} but rule annotation {annot} requires {expected}; check @layout(...) hint or grammar -> projection"`; `BBNF-SEM040 → "lookbehind in rule {rule} reaches BIR with unbounded width; PASS-1 BBNF1004 should have caught upstream"`. | REINVENT (Lane 7) | PASS-2 amendment | column extension + 6 strings | 7 |
| 7 | `restart/audit/pass-2-codegen/PASS-2.md` (new row in diagnostic ledger after line 527) | Add a Pratt/SIMD misfire code: `BBNF-OPT001 | optimizer rejects an apparent operator-chain candidate | cost-model decision | "rule {rule} resembles an operator chain (left-recursive with operator-bearing alts at {line}) but {reason}; promote to PrattSpine with @pratt or restructure the rule"`. Mirror for SIMD: `BBNF-OPT002 | optimizer rejects an apparent SIMD candidate | cost-model decision | "rule {rule} has structural alphabet {alpha} but kernel-shape evidence is {shape}; falling back to scalar; @simd hint may force"`. | REINVENT (Lane 7) | PASS-2 amendment | 2 rows | 7 |
| 8 | `restart/audit/pass-2-codegen/PASS-2.md:78` | Replace the swc rustdoc URL citation with a corpus path:line: `swc compiles JavaScript AST into per-domain enums (Stmt/Expr) with different cardinality pressure than parser IR (restart/corpora/SOTA.md:N — populate at amendment time)`. | REINVENT (Lane 3) | PASS-2 amendment | one-line edit | 3 |
| 9 (optional) | `restart/audit/pass-2-codegen/PASS-2.md:421` | Pin yaml smoke regen receiving tranche to one (Tranche G runtime publication is the natural receiver). | REINVENT (Lane 8) | PASS-2 amendment | one-line edit | 8 |

Surgery count: 8 mandatory + 1 optional. Estimated amendment time: 25-35 minutes by a single agent on PASS-2.md only.

## §15 Final readiness

> **Decision: AMENDMENT-REQUIRED.**
>
> PASS-2 emerges from the Wave-1.2 + Wave-2 amendments as a coherent, defensible codegen architecture. Backend IR ownership is correctly placed at `ir/src/backend_ir/`; the import-deny gate carries a verbatim command; the SOTA throughput trajectory is row-complete with platform tags and mechanism rows demoted; the per-grammar generated_loc table carries xtask wall ceilings + baselines + SYNTHESIS carry pointer; the non-generated LOC + child-count + enforcing command table closes Lock 13; the 8-row carry ledger names Receiver / Blocker / Receiving gate per defer. Eight surgical edits remain: layout-canon vocabulary (Lock 2), unified cursor + byte-skip obligation (Lock 3), deny-gate path widening (Lock 5), bumpalo-arena lifetime row in template schema (Lock 9), Lock 14 two-surface yaml input invariant (Lock 14), verbatim diagnostic strings (Lane 7), Pratt/SIMD misfire codes (Lane 7), swc rustdoc URL replacement (Lane 3).
>
> Hereupon a narrow-scope amendment agent applies the punch list, then PASS-2 advances to per-tranche full-spec drafting alongside its sister passes. The amendment is surgical; no PASS-2 re-run is warranted; the architectural thesis is settled.
