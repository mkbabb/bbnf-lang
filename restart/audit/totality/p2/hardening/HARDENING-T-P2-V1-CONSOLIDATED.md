# T-P2 V1 Hardening Consolidated — SK-V15

Pass: T-P2 Research.
Cycle: V1.
Target commit: `0fb621116`.
Challenge context: `restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md`.

## Verdict

`REVISE`.

Acceptance rate: 3 / 7 = 42.9%.

V1 is an expected research-hardening cycle. The packet is directionally
correct: it rejects the CSS broadcast, brace-counter CSS proof, fact-stream
Value/API substitution, self-excluding Lock 14 gates, x86/AVX-512 close
evidence, and source-only SIMD inventory. It does not converge because four
lenses found bounded fold requirements.

## Lens Dispositions

| lens | disposition | summary | convergence impact |
|---|---|---|---|
| CH1 correctness | REVISE | One bounded 2D provenance defect: the simdjson tape documentation URL returns 404. | Blocks V1 until folded. |
| CH2 generality | ACCEPT | No CH2-critical grammar-name leak, fleet-wide overclaim, hidden directive, BIR expansion, or BackendShape expansion found. | No block. |
| CH3 regression | REVISE | 2A LAC-04 leaves retained sidecar/class-column routes sounding conditionally viable; SK-V15 must fence them as REDRESS-refuted. | Blocks V1 until folded. |
| CH4 cost | REVISE | Grounded primitives and rebuild routes need uniform LOC/risk/wave-owner/hard-cap cost fields, especially 2C/2D/2F and transferred primitive rows. | Blocks V1 until folded. |
| CH5 hidden coupling | ACCEPT | No retained sidecar, parallel substrate, grammar switch, new BIR, or BackendShape expansion was admitted. | No block. |
| CH6 anti-paper-close | REVISE | Several grounded rows carry source density and bbnf notes without inline admission gate, verification action, and close status. | Blocks V1 until folded. |
| CH7 overfit-prune | ACCEPT | The packet rejects broadcast admission, self-excluding gates, CSS fake parity, x86 close evidence, and deletion-before-provider routes. | No block. |

## Fold Packet For V2

1. **CH1 fold — 2D provenance.** Replace
   `https://simdjson.github.io/simdjson/md_doc_tape.html` in
   `restart/audit/totality/p2/2D-cost-model.md` with a resolvable primary
   source for tape materialization, or remove that URL and ground the staged
   tape assertion in the existing simdjson paper/source citations. Record the
   URL check result in V2.
2. **CH3 fold — 2A REDRESS fence.** Rewrite 2A LAC-04 so retained
   cursor/list/class-column/sidecar routes are pre-blocked for SK-V15. The
   allowed route is transient same-loop masks or generated single-substrate
   consumption compatible with the existing substrate. Any future sidecar-like
   route requires a new Alpha/P1/SPEC contract, not row-local measurement alone.
3. **CH4 fold — cost/admission manifest.** Add uniform cost fields for every
   grounded primitive and rebuild route: scalar reference, parity/checkasm
   command or equivalent, hardware gate, same-wave consumer, row movement
   target, LOC estimate, risk class, wave owner, and hard-cap fit. Split
   Decision Engine and five lowerer work into W7/W8/W9 units. Split generated
   provider, Pattern H, CSS typed provider, regex/HIR, SIMD, string/UTF-8,
   float, and CSS value routes separately.
4. **CH6 fold — anti-paper-close row shape.** Every row whose state includes
   `grounded` must carry `transfer_reason`, `admission_gate`,
   `verification_action`, and `close_status`. Source count or source register
   length cannot validate a technique. Verification requirements that are
   prerequisites for a row must move into that row, not remain only in OQ/LAC.
5. **Preserve accepts.** Do not weaken the CH2/CH5/CH7 fences while folding:
   no new directive, no new BIR, no sixth BackendShape, no retained sidecar, no
   broadcast admission, no self-excluding gate, no CSS fact-stream/CSSOM
   substitution, and no x86/AVX-512 M5 close evidence.

## V2 Dispatch Guidance

V2 should rewrite the six dossiers in place and keep `cycle: V2` in
frontmatter. The fold is bounded and does not require new V1 spec-surface
edits. The highest-risk files are:

| dossier | fold focus |
|---|---|
| `2A-sota-landscape.md` | CH3 LAC-04 fence; CH6 inline gate fields for SOTA rows; CH4 cost fields for transferred parser leaves. |
| `2B-primitive-vocabulary.md` | CH4 LOC/risk/wave-owner/hard-cap fields on the primitive manifest; preserve CH6-compliant structure. |
| `2C-grammar-neutrality.md` | CH4/CH6 cost and gate fields for generated provider, Pattern H, CSS value, future grammar onboarding, and full-surface Lock 14 scan routes. |
| `2D-cost-model.md` | CH1 simdjson source repair; CH4 split Decision Engine/lowerers into W7/W8/W9 costed units; CH6 inline gate fields. |
| `2E-host-arch-esoterica.md` | CH4 LOC/risk/wave-owner/hard-cap fields on each host primitive; preserve aarch64-only close route. |
| `2F-parse-that-gaps.md` | CH4/CH6 per-gap cost, owner, scalar/checkasm, hardware gate, consumer, row movement, and close status. |

## Close Status

T-P2 V1 does not converge. No REJECT landed, so the pass continues by folding
the four REVISE lenses into T-P2 V2.
