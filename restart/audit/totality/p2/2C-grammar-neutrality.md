---
agent: 2C
pass: T-P2-research
cycle: V3
generated_at: 2026-06-01T19:10:00Z
sk_cycle: SK-V18
scope: 2C-grammar-neutrality / generalisation
t_p1_inventories_consumed: [1A, 1B, 1C, 1D, 1E, 1F, 1F-coherence, 1F-anti-pattern, 1F-past-corpora]
primary_sources_cited: 31
techniques_grounded: 18
techniques_refuted: 9
locks_amendment_candidates: 3
host_close_route: Apple-M5-Max-aarch64-ONLY
x86_standing: SECONDARY/DELETED-in-skinny (hardware-gated literature pressure, never the close route)
prior_cycle_carry_forward: V2 (SK-V15) content retained verbatim below; SK-V18 extension appended as the V3 section (does not re-derive converged SK-V15 rows)
sk_v18_extension_additions:
    - SK-V18-2C-ONE-GENERATOR-GENERALISATION-THESIS
    - SK-V18-2C-NAMED-PRIMITIVE-ABCD-AS-NEUTRALITY-DISCIPLINE
    - SK-V18-2C-CSS-BALANCED-SCAN-NEUTRALITY-PROOF-FORCED-DEMOTION
    - SK-V18-2C-SHEETS-PRECEDENCE-TOWER-NEGATIVE-CONTROL
    - SK-V18-2C-5-SHAPE-BACKENDSHAPE-GENERALISES-BEYOND-JSON
    - SK-V18-2C-9-GRAMMAR-FLEET-ONBOARDING-TEST
    - SK-V18-2C-RELOCATED-SEAM-FIREWALL-NEUTRALITY-GATE
    - SK-V18-2C-TOTALITY-TREE-9-IDENT-LEAK-REFUTATION
    - SK-V18-2C-NEUTRALLY-NAMED-CSS-ONLY-PRIMITIVE-REFUTATION
sk_v18_locks_amendment_candidates: 3
v3_folds_applied:
    - "CH4-V3-01: added the V3 SK-V18 Cost Manifest — per-row loc_estimate + risk_class + rollback + abrogate keyed to the live §8 band, the manifest 2B/2D/2E/2F all carry and 2C previously lacked."
    - "CH4-V3-02: the css_provider_source==generated CSS-typed second seam is now costed as a G2-owned gate-only row (≈ +5 gate LOC, LOW, additive) in the V3 cost manifest — not covered by 2D LAC-2D-V3-02's RuntimeTarget-field firewall."
sk_v18_entry_state: T-P1-CONVERGED-1A-1F-V5-HARDENED
implementation_floor: PASS-IMPL-V1-CSS-CONTRIVANCE-JSON-HONEST (SK-V15 floor; SK-V18 raises CSS to grammar-DERIVED via G2)
stale_sk_v14_material_reused: none
---

## Executive Summary

Lock 14 transfer is defensible only if grammar-specific behavior enters the
fleet through grammar source, workspace metadata, and generated per-grammar
runtime surfaces. SK-V15 starts from a split floor: JSON has honest same-plane
rows, while CSS L4 admission is contrived by a 24-row measurement broadcast,
`CSS_GENERATED_RS` string-literal generation, and `CssFullParseSummary`
brace-counter output. Current root runtime code does contain a typed CSS
document/value surface, but that is only partial evidence because Pattern H
still has root runtime files (71 at HEAD; 67 in the SK-V15-era census) with no
line-1 generated provenance, CSS legacy path shims remain, and the skinny
admission path still returns fact-stream or full-parse strings. Therefore JSON-only, CSS-only, generator-sidecar, and
generic-crate grammar-switch routes are refuted as fleet-wide generalisation.

The admissible transfer route is narrower: generate typed values, sinks,
metadata facts, grammar policy, and provider manifests from source plus
metadata; prove CSS L4 and at least one Sheets or BBNF-self receiver without
editing generic code; demote broadcast CSS rows before retiming; and require
future grammar onboarding tests that fail on any generic crate grammar branch.
V2 keeps the V1 non-regression fences intact: no new directive, no new BIR, no
sixth `BackendShape`, no CSS broadcast admission, no self-excluding Lock 14
gate, no CSS fact-stream/CSSOM substitution, and no deletion before a same-wave
replacement provider is proven.

## Technique Grounding Table — SK-V15 HISTORICAL (non-SK-V18-cost)

*Every `wave_owner=W#` value in this table is the RETIRED SK-V15 ledger and is NOT
a live SK-V18 cost cell. The SK-V15→SK-V18 wave map is in §"V3 Wave-Owner Re-key";
the live SK-V18-cost owners (`G#`/`PROVE`/`SK-V19`) are in the V3 Technique
Grounding Table. Do NOT copy a `W#` owner from a row below as a live SK-V18 cost.*

| spec-claim or T-P1 divergence id | published source cited | grounded / refuted / partial | bbnf-specific note | V2 row-shape |
|---|---|---|---|---|
| SK-V15-2C-GENERATOR-INPUT-SURFACE | Lock 14 defines the only legal fleet inputs as grammar source, workspace metadata, and optional per-grammar declaration crate at `restart/locks/LOCKS.md:349`; the grammar parser records source hashes and frontend closure facts at `skinny/crates/grammar/src/lib.rs:29`-`35`, `skinny/crates/grammar/src/lib.rs:60`-`64`, and `skinny/crates/grammar/src/lib.rs:87`-`112`. | grounded | The generator may consume `RuntimeSourceFacts`, `RuntimeFrontendSource`, imports, layout, host captures, and projection facts. It may not invent a CSS tokeniser in a Rust string literal and call the result grammar-derived. | transfer_reason=legal Lock 14 input surface for generated grammar facts; admission_gate=generated output must derive from grammar source/metadata only and reject hand-written parser body insertion; verification_action=regen/check diff plus Lock 14 leak-root scan; close_status=admissible-after-gate; loc_estimate=120-180; risk_class=medium; wave_owner=W2/W7 generator-owner split; hard_cap_fit=yes, fact-reader/manifest only. |
| SK-V15-2C-METADATA-GRAMMAR-OWNERSHIP | CSS regen declares fifteen CSS source inputs and workspace metadata at `skinny/xtask/src/regen_css.rs:5`-`24`; every CSS target repeats `source_inputs` and `metadata_inputs` at `skinny/xtask/src/regen_css.rs:26`-`97`; tests only prove seven CSS companions and fifteen sources at `skinny/xtask/src/regen_css.rs:147`-`169`. | partial | This is a useful ownership sketch, but it is CSS-only. The SK-V15 non-JSON proof matrix requires CSS plus Sheets or BBNF-self for generic surfaces at `restart/skinny/tranches/sk-v15/SPEC.md:206`-`217`. | transfer_reason=CSS metadata shows the ownership pattern but not fleet generality; admission_gate=add a non-CSS receiver without generic owner-path edits; verification_action=compare generated source/metadata manifests for CSS plus Sheets or BBNF-self; close_status=partial-blocked; loc_estimate=80-140; risk_class=medium; wave_owner=W7; hard_cap_fit=yes if limited to manifest extension. |
| SK-V15-2C-CSS-SYNTAX-TOKEN-SURFACE | W3C CSS Syntax Module Level 3, "Tokenization" and "Consume a token", `https://www.w3.org/TR/css-syntax-3/#tokenization`; local CSS token/value sources at `grammar/css/l4/stylesheet.bbnf:12`-`14`, `grammar/css/l4/selectors.bbnf:9`-`39`, and `grammar/css/l4/values.bbnf:23`-`63`. | grounded | CSS tokenization is not JSON punctuation. Byte-class and scanner primitives transfer only when the alphabet, escape policy, comments, identifiers, number starts, and block structure are generated facts. | transfer_reason=CSS token alphabet and scanner policy are grammar facts, not JSON punctuation reuse; admission_gate=generated token facts cover escapes/comments/identifiers/numbers/blocks and no generic CSS branch appears; verification_action=generated-facts diff plus cssparser token equality sample; close_status=admissible-after-gate; loc_estimate=160-240; risk_class=medium; wave_owner=W5; hard_cap_fit=yes if scoped to CSS token facts. |
| SK-V15-2C-CSS-VALUE-API-SURFACE | CSS Typed OM Level 1 defines `CSSStyleValue.parse()` and value objects at `https://www.w3.org/TR/css-typed-om-1/#cssstylevalue-objects`; CSS Values and Units Level 4 defines value syntax and `calc()` at `https://www.w3.org/TR/css-values-4/#value-defs` and `https://www.w3.org/TR/css-values-4/#calc-notation`; current root runtime has `CssDocument`, walk/view methods, and typed values at `crates/core/src/runtime/css_l4/document.rs:10`-`79`, `crates/core/src/runtime/css_l4/document.rs:138`-`207`, and `crates/core/src/runtime/css_l4/value.rs:326`-`424`. | partial | The root runtime proves the desired value shape exists as code. It does not close CSS admission: the skinny live CSS path still emits strings from `CSS_GENERATED_RS`, and Pattern H provenance is absent. Closure requires the typed CSS value/document/view/visitor surface to be generator-derived and measured against same-workload `cssparser`/CSSOM comparators per `restart/skinny/tranches/sk-v15/SPEC.md:54`-`63`. | transfer_reason=typed CSS provider is the required non-JSON receiver surface; admission_gate=generator-derived document/value/view/visitor provider, cssparser same-workload equality, and no fact-stream or brace-counter admission; verification_action=non-writing generator proof, row-local CSS value equality, and retime after W1 demotion; close_status=partial-blocked; loc_estimate=450-700; risk_class=high; wave_owner=W5; hard_cap_fit=yes only as scoped CSS typed provider, not full CSSOM rewrite. |
| SK-V15-2C-CSS-CUSTOM-PROPERTIES-AND-CALC | CSS Custom Properties Level 1 defines `--*` custom properties and `var()` at `https://www.w3.org/TR/css-variables-1/#defining-variables`; local CSS grammar encodes `calc`, `min`, `max`, `clamp`, `var`, and `env` at `grammar/css/l4/values.bbnf:31`-`63`. | grounded | CSS Value API generation must preserve property/value semantics, not reduce values to generic strings or JSON number/string rules. | transfer_reason=custom property and calc semantics force CSS-specific typed value generation; admission_gate=generated value nodes preserve var/calc/min/max/clamp/env semantics and reject generic string/number fallback; verification_action=cssparser comparator corpus plus typed value round-trip checks; close_status=admissible-after-gate; loc_estimate=180-280; risk_class=medium-high; wave_owner=W5; hard_cap_fit=yes within CSS typed provider cap. |
| SK-V15-2C-SELECTOR-GENERALISATION | Selectors Level 4 defines selector families at `https://www.w3.org/TR/selectors-4/`; local selector grammar covers namespaces, attributes, pseudo-classes, pseudo-elements, combinators, and selector lists at `grammar/css/l4/selectors.bbnf:12`-`106`. | grounded | Selector rows need generated selector AST/value facts. Reusing a declaration-value token row or JSON object/member projection is a workload mismatch. | transfer_reason=selectors exercise AST/value facts outside declaration values and JSON shapes; admission_gate=generated selector facts and AST output with no declaration-value row reuse; verification_action=selector-list equality corpus and generated source provenance check; close_status=admissible-after-gate; loc_estimate=180-260; risk_class=medium; wave_owner=W5; hard_cap_fit=yes if scoped to selector facts. |
| SK-V15-2C-CSSOM-COMPARATOR-BOUNDARY | CSSOM specifies the object model at `https://www.w3.org/TR/cssom-1/`; SK-V15 requires cssparser as near-term same-workload comparator and lightningcss only after comparable CSSOM/value output at `restart/skinny/tranches/sk-v15/SPEC.md:59`-`63`. | grounded | A brace-counter `CssFullParseSummary` is not CSSOM. CSS retime must compare typed document/value output, not rule-count summaries. | transfer_reason=defines the comparator plane that prevents CSS fake parity; admission_gate=cssparser first, lightningcss only after comparable CSSOM/value output exists; verification_action=row-local typed document/value equality and timing command per row; close_status=admissible-after-gate; loc_estimate=80-140; risk_class=medium; wave_owner=W8; hard_cap_fit=yes as comparator harness, not provider rewrite. |
| SK-V15-2C-CSS-BROADCAST-ADMISSION | The overfit audit records one W8R CSS measurement broadcast across 24 rows at `restart/audit/skinny-impl-overfit/V1/CONSOLIDATED-AUDIT.md:21`-`31`; current `RESULTS.md` repeats the same tuple beginning at `skinny/RESULTS.md:112`; W8 measurement loops one aggregate profile at `skinny/crates/bbnf-bench/src/css_l4_w8.rs:206`-`228`. | refuted | The 24-row CSS route is not row-local evidence. W1 must demote/collapse it per `DEP-W1-CSS-BROADCAST` at `restart/skinny/tranches/sk-v15/SPEC.md:194` and Section 4 at `restart/skinny/tranches/sk-v15/SPEC.md:264`-`281`. | transfer_reason=none; admission_gate=blocked because repeated tuple signatures are diagnostic only without independent row command/input/equality/timing; verification_action=W1 duplicate-signature gate and non-admit broadcast group marking; close_status=refuted; loc_estimate=40-80 for gate/demotion only; risk_class=low; wave_owner=W1; hard_cap_fit=yes. |
| SK-V15-2C-CSS-GENERATOR-SIDECAR | `emit_frontend_facts` writes `generated.rs` from `CSS_GENERATED_RS` at `skinny/crates/codegen/src/runtime_generator.rs:81`-`104`; the string literal starts at `skinny/crates/codegen/src/runtime_generator.rs:713`, emits fact streams at `skinny/crates/codegen/src/runtime_generator.rs:717`-`760`, defines `CssFullParseSummary` at `skinny/crates/codegen/src/runtime_generator.rs:762`-`771`, and produces a full-parse string at `skinny/crates/codegen/src/runtime_generator.rs:773`-`813`. | refuted | This is a generator-sidecar: CSS source facts become metadata in a hand-written runtime body, not grammar-derived parser/value generation. SK-V15 already routes it to W6 deletion only after W5 typed provider proof at `restart/skinny/tranches/sk-v15/SPEC.md:195`-`196`. | transfer_reason=none; admission_gate=blocked until a same-wave generated typed provider replaces the sidecar; verification_action=prove `CSS_GENERATED_RS`, fact-stream parse, and `CssFullParseSummary` absent from live admission after W5; close_status=refuted; loc_estimate=0 before provider, 80-140 deletion cleanup after proof; risk_class=high; wave_owner=W6 after W5; hard_cap_fit=yes only with delete-before-provider protection. |
| SK-V15-2C-GENERIC-GRAMMAR-SWITCH | `RuntimeGenerationMode::{PassCompiled, FrontendFacts}` at `skinny/crates/codegen/src/grammar_profile.rs:11`-`15`; `runtime_profiles() -> [&GrammarProfile; 8]` hand-lists JSON plus seven CSS profiles at `skinny/crates/codegen/src/grammar_profile.rs:89`-`99`; runtime emission dispatches on the mode at `skinny/crates/codegen/src/runtime_generator.rs:19`-`29`; CSS profile IDs are matched at `skinny/crates/codegen/src/runtime_generator.rs:114`-`153`. | refuted | This is the generic-crate grammar switch named in the task. It is JSON/CSS routing under different names and is not a future-grammar onboarding mechanism. | transfer_reason=none; admission_gate=blocked because generic-crate grammar switches violate Lock 14 onboarding; verification_action=generated provider manifest diff rejects hand-edited mode/profile/match arms; close_status=refuted; loc_estimate=0 for switch preservation, 180-260 for replacement manifest route under LAC-2C-SK15-01; risk_class=medium; wave_owner=W7; hard_cap_fit=yes if manifest-only. |
| SK-V15-2C-PATTERN-H-COLLAPSE | Pattern H census was 67 files in the SK-V15-era count (the LIVE count at HEAD is 71 by `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l` — the absolute count is NOT the binding claim, the per-file line-1 provenance is; see V3 OQ on the 67→71 drift); overfit audit records 5/9 true template, 4/9 bespoke, and 0/67 line-1 generated headers at `restart/audit/skinny-impl-overfit/V1/CONSOLIDATED-AUDIT.md:37`-`43`; simple-template scope is explicit at `crates/core/src/runtime/builder_template.rs:17`-`25` and `crates/core/src/runtime/builder_template.rs:43`-`63`; CSS remains bespoke with `OpenFrame` at `crates/core/src/runtime/css_l4/builder.rs:15` and legacy path shim at `crates/core/src/runtime/css_l4/parse_with.rs:1`-`10`. | partial | The simple cohort is the success pattern; root CSS/JSON/Sheets/BBNF bespoke files are not collapsed. W4 may add provenance and checks, but destructive deletion is blocked until generated replacement exists per `DEP-W4-PATTERN-H-PROVENANCE` and `DEP-W4-W6-CSS-LEGACY-RUNTIME-SHIM` at `restart/skinny/tranches/sk-v15/SPEC.md:198`-`199`. | transfer_reason=Pattern H provenance distinguishes generated candidates from hand-owned generic helpers; admission_gate=full line-1 provenance over the live census (71 at HEAD, not the SK-V15-era 67) plus non-writing regen/check, with deletion blocked until replacement provider exists; verification_action=file census, header scan, owner grouping, and regen/check report; close_status=partial-blocked; loc_estimate=160-240; risk_class=medium; wave_owner=W4; hard_cap_fit=yes as provenance-only route. |
| SK-V15-2C-SHEETS-FUTURE-GRAMMAR | Sheets grammar defines numbers, doubled-quote strings, error literals, sheet prefixes, cell/range references, operators, function calls, LET, LAMBDA, and array rows at `grammar/google-sheets/google-sheets.bbnf:6`-`18`, `grammar/google-sheets/google-sheets.bbnf:34`-`84`, and `grammar/google-sheets/google-sheets.bbnf:97`-`160`; current typed value shape is `SheetsValue` at `crates/core/src/runtime/google_sheets/value.rs:3`-`13`. | grounded | Sheets is the anti-JSON falsifier. It needs function/reference/operator facts and typed values from source, not object/array/pair mining or JSON sink callbacks. | transfer_reason=Sheets proves non-CSS and anti-JSON receiver materialization; admission_gate=grammar source/metadata addition only, generated function/reference/operator/value provider, no generic code change; verification_action=regen/check diff, typed value equality corpus, and Lock 14 scan of generic owner paths; close_status=admissible-after-gate; loc_estimate=250-400; risk_class=high; wave_owner=W7; hard_cap_fit=yes as one negative-control receiver. |
| SK-V15-2C-BBNF-SELF-FUTURE-GRAMMAR | BBNF-self grammar imports, literals, regexes, closures, alternation, directives, and grammar items at `grammar/bbnf/bbnf.bbnf:4`-`15`, `grammar/bbnf/bbnf.bbnf:41`-`52`, and `grammar/bbnf/bbnf.bbnf:54`-`85`. | grounded | BBNF-self exercises grammar-source ownership recursively. It must onboard with the same generator contract, without special generic code for `Bbnf`. | transfer_reason=BBNF-self tests recursive grammar-source ownership without a new directive or BIR variant; admission_gate=generated import/directive/closure/alternation provider and no `Bbnf` branch in generic crates; verification_action=regen/check diff, self grammar parse corpus, and generic-code no-change gate; close_status=admissible-after-gate; loc_estimate=220-360; risk_class=high; wave_owner=W7; hard_cap_fit=yes as alternate negative-control receiver. |
| SK-V15-2C-GATE-EXCLUSION | The SK-V15-era allowed/scan-root list lived at `skinny/crates/bbnf-bench/src/lock14_baseline.rs:2370`-`2379` (a frozen SK-V15 pointer — at HEAD that file is 5095 LOC and `:2367`-`2379` is the generic `is_allowed_path`/`validate_backend_shape_surface` helpers, NOT a scan-root list; the LIVE allowed-root list is `lock14_baseline.rs:345`-`435`). At the SK-V15 baseline the list EXCLUDED `runtime_generator.rs`, `grammar_provider.rs`, `json_sink_direct.rs`, `json_typed_direct.rs`, and `json_templates`; overfit audit named this omission at `restart/audit/skinny-impl-overfit/V1/CONSOLIDATED-AUDIT.md:45`-`47`. **SUPERSEDED AT HEAD:** the live root list `lock14_baseline.rs:345`-`435` now PRESENTS `grammar_profile.rs` (`:345`), `json_sink_direct.rs` (`:351`), `json_typed_direct.rs` (`:357`), and `json_templates/*` (`:363`-`393`), and `runtime_generator.rs` (e.g. `:1144`) + `grammar_provider.rs` (e.g. `:1123`) now appear in-file — i.e. the SK-V15 "leak roots excluded" exclusion has been corrected; this historical row records the SK-V15-era defect, not a HEAD defect. | refuted | A gate that excludes the leak roots cannot evidence Lock 14. SK-V15 requires inclusion and exclusion reporting at `restart/skinny/tranches/sk-v15/SPEC.md:64`-`68` and `restart/skinny/tranches/sk-v15/SPEC.md:219`-`236`. | transfer_reason=none for current scan; admission_gate=full-surface scan must include leak roots or fail with explicit exclusions; verification_action=report included roots, excluded roots, self-scan status, primitive status, gate consumer, affected rows, and disposition; close_status=refuted; loc_estimate=120-180 for full-surface scan route; risk_class=medium; wave_owner=W2; hard_cap_fit=yes. |
| SK-V15-2C-FUTURE-GRAMMAR-ONBOARDING-TEST | Lock 14 says adding a grammar is a config plus grammar-source change with no generic-crate code change at `restart/locks/LOCKS.md:349`; SK-V15 proof receiver matrix requires CSS plus Sheets or BBNF-self for generic surfaces at `restart/skinny/tranches/sk-v15/SPEC.md:206`-`217`. | grounded | The onboarding test is not "add enum arm and pass." It is: add grammar source/metadata, run regen/check, observe generated files or gate-consumed rejected alternative, and fail if any generic owner path changes except generated manifests. | transfer_reason=future grammar onboarding is the genericity proof for Lock 14; admission_gate=source/metadata-only receiver addition with no generic owner edit except generated manifest output; verification_action=add Sheets or BBNF-self fixture, run regen/check, assert generic diff is empty or generated-only; close_status=admissible-after-gate; loc_estimate=120-180; risk_class=medium; wave_owner=W7; hard_cap_fit=yes as test/gate route. |

## Architectural Assertions Defended

1. Grammar-neutrality is an input-surface property, not a prose label. The
   admissible inputs are grammar source, workspace metadata, and optional
   per-grammar host/declaration crates. The local grammar parser already
   exposes source hashes, imported source closure, layout directives, host
   captures, and projections (`skinny/crates/grammar/src/lib.rs:60`-`112`);
   those are the facts a generator can legally consume.

2. CSS L4 requires a grammar-derived typed value/document surface. W3C CSS
   Syntax, Values, Variables, Selectors, Typed OM, and CSSOM together define a
   token stream, value grammar, selector model, typed value API, and object
   model. The current root runtime has `CssDocument`, `CssView`, `CssFocus`,
   `CssDocumentKind`, and `CssTypedValue` (`crates/core/src/runtime/css_l4/document.rs:10`-`79`,
   `crates/core/src/runtime/css_l4/document.rs:138`-`207`; `crates/core/src/runtime/css_l4/value.rs:413`-`424`).
   That is the correct target shape for SK-V15 W5/W6, provided it becomes
   generated and row-measured rather than hand-owned Pattern H code.

3. Pattern H collapse is structural, not file-count deletion. The binding claim
   is STRUCTURAL — line-1 provenance and non-writing regen/check evidence per
   root runtime file — NOT an absolute file count. (The SK-V15-era contract cited
   "exactly 67"; the LIVE census at HEAD is 71 —
   `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l` = 71,
   the +4 being `tape/{mod,cursor,arena,record}.rs`. The structural-provenance
   claim is unchanged by the drift; the absolute "67" is superseded and is NOT
   the live count — see V3 OQ on the 67→71 attribution.) The simple
   runtime template documents the right abstraction boundary: it covers only
   grammars whose values are span/unit/compound and says richer grammars emit
   their own projection shape from metadata (`crates/core/src/runtime/builder_template.rs:17`-`25`).

4. Future grammar onboarding must use negative controls. CSS L4 alone is not
   enough because CSS-specific generator tables can still overfit. Sheets
   (`grammar/google-sheets/google-sheets.bbnf:141`-`160`) and BBNF-self
   (`grammar/bbnf/bbnf.bbnf:54`-`85`) force function calls, references,
   arrays, directives, alternations, closures, and import ownership that JSON
   and CSS cannot fake.

5. Fact streams may remain diagnostic or narrow product rows, but not as a
   hidden substrate. CSS fact streams are valid if classified as output rows
   with gate-consumed telemetry; they cannot serve as a retained EventTape,
   CSSOM, or typed Value API proof. SK-V15 requires `CSS_GENERATED_RS`,
   `CssFullParseSummary`, fact-stream-only `parse()`, and brace-counter output
   to retire from live CSS admission (`restart/skinny/tranches/sk-v15/SPEC.md:56`-`58`).

## Architectural Assertions Refuted

| assertion | disposition | reason |
|---|---|---|
| JSON-only routes can be promoted to fleet-wide grammar-neutrality. | refuted | JSON honest rows do not prove CSS, Sheets, or BBNF-self. JSON `JsonValue`/object-array-pair shape at `crates/core/src/runtime/json/value.rs:3`-`39` is a grammar-specific value model, not a generic grammar model. |
| CSS-only provider tables are enough to prove generalisation. | refuted | `regen_css.rs` hard-lists seven CSS targets and fifteen CSS files (`skinny/xtask/src/regen_css.rs:5`-`97`). It proves a CSS cohort table, not arbitrary grammar onboarding. |
| `CSS_GENERATED_RS` is grammar-derived generation. | refuted | The parser body is a string literal in `skinny/crates/codegen/src/runtime_generator.rs:713`-`1359`; source facts are emitted as metadata lines, while parser/tokeniser logic lives in the sidecar body. |
| `CssFullParseSummary` is same-workload CSSOM or Value API evidence. | refuted | It records counts for rules, at-rules, qualified rules, and declarations at `skinny/crates/codegen/src/runtime_generator.rs:765`-`771`; CSSOM and Typed OM require object/value representations, not brace-counter summaries. |
| The 24 CSS L4 rows are 24 independent admits. | refuted | One W8 aggregate measurement is multiplied across feature rows (`skinny/crates/bbnf-bench/src/css_l4_w8.rs:206`-`228`; `skinny/RESULTS.md:112`-`135`). SK-V15 W1 must demote or collapse it. |
| `RuntimeGenerationMode` plus `runtime_profiles()` is a future grammar switch. | refuted | The mode split and eight-profile array are generic-crate grammar switches (`skinny/crates/codegen/src/grammar_profile.rs:11`-`15`, `skinny/crates/codegen/src/grammar_profile.rs:89`-`99`) and violate the no-code-change onboarding rule. |
| Current Lock 14 scan output proves grammar-neutrality. | refuted (SK-V15-era; superseded at HEAD) | At the SK-V15 baseline the scan root list omitted `runtime_generator.rs`, `grammar_provider.rs`, JSON direct generators, and JSON templates; excluding the leak roots was a contrivance. **The `lock14_baseline.rs:2370`-`2379` pointer is a frozen SK-V15 line — at HEAD it is the generic `is_allowed_path`/`validate_backend_shape_surface` helpers, and the live root list `lock14_baseline.rs:345`-`435` now INCLUDES `grammar_profile.rs`/`json_sink_direct.rs`/`json_typed_direct.rs`/`json_templates/*`, with `runtime_generator.rs`+`grammar_provider.rs` present in-file.** The exclusion is corrected at HEAD; the live SK-V18 self-gate falsification rests instead on the 13-site grep RED (`crates/ir/src/`+`crates/analysis/src/`) and the 9-ident `strategy.rs:137`-`185` table. |
| Current CSS root runtime Value API closes SK-V15 CSS. | partial only | Typed CSS code exists in `crates/core/src/runtime/css_l4/`, but lacks line-1 generator provenance and is not the skinny live admission path. It becomes close evidence only after generator-derived provenance, semantic equality, and same-workload retiming. |

## Open Research Questions

| UNKNOWN | verify_action |
|---|---|
| What is the smallest generated manifest that can replace `runtime_profiles()` without changing public codegen callers? | Prototype a generated provider manifest for JSON plus seven CSS profiles, then add Sheets or BBNF-self without editing `skinny/crates/codegen/src/grammar_profile.rs`; gate on diff and Lock 14 scan. |
| Can the root CSS `CssTypedValue` surface be emitted from existing grammar facts without hand-coded CSS branches? | Run a non-writing generator proof over `grammar/css/l4/*.bbnf` plus metadata and compare emitted `value.rs`, `document.rs`, `view.rs`, and builder projection against the current root runtime. |
| Which root runtime files in the 67-file Pattern H census are true generated-output candidates versus intentional hand-owned generic templates? | Produce a line-1 provenance scan, group by generator owner, and require non-writing regen/check for every generated candidate; record intrinsic blocks for any hand-owned generic helper. |
| Which CSS comparator plane is near-term closeable: typed declaration/value tree versus fuller CSSOM stylesheet tree? | Build row-local equality against `cssparser` first, then only admit lightningcss when Track 1 emits comparable CSSOM/value output, per `restart/skinny/tranches/sk-v15/SPEC.md:61`-`63`. |
| What future grammar should be the first non-CSS negative-control receiver? | Prefer Sheets if validating function/reference/value materialization; prefer BBNF-self if validating import/directive/grammar-source recursion. Either must fail if a generic crate changes. |

## LOCKS-AMENDMENTS-CANDIDATE — SK-V15 HISTORICAL (non-SK-V18-cost)

*Every `wave_owner=W#` value in the cost/admission column below is the RETIRED
SK-V15 ledger, NOT a live SK-V18 cost cell. The live SK-V18 amendment candidates
(G#/SK-V19 owners) are in §"V3 LOCKS-AMENDMENTS-CANDIDATE"; the wave map is in
§"V3 Wave-Owner Re-key". Do NOT copy a `W#` owner from a row below as a live SK-V18 cost.*

| id | lock(s) | amendment candidate | evidence | close test | V2 cost/admission row |
|---|---|---|---|---|---|
| LAC-2C-SK15-01 generated provider manifest | Lock 14, Lock 6 | Generic codegen may consume a generated provider manifest, but may not hand-code `RuntimeGenerationMode`, profile arrays, CSS profile matches, or JSON/CSS runtime families in non-generated source. | `skinny/crates/codegen/src/grammar_profile.rs:11`-`15`, `skinny/crates/codegen/src/grammar_profile.rs:89`-`99`; `skinny/crates/codegen/src/runtime_generator.rs:19`-`29`, `skinny/crates/codegen/src/runtime_generator.rs:114`-`153`. | Add a Sheets or BBNF-self provider with grammar source/metadata only; no generic owner-path edit except generated manifest output. | transfer_reason=replace generic grammar switch with generated source-owned provider inventory; admission_gate=manifest-only generated output, no hand-edited `RuntimeGenerationMode`, profile array, or CSS match arm; verification_action=add Sheets or BBNF-self provider and assert generic owner diff is generated-only; close_status=admissible-after-gate; loc_estimate=180-260; risk_class=medium; wave_owner=W7; hard_cap_fit=yes, not a bulk generated-provider rewrite. |
| LAC-2C-SK15-02 CSS value API close boundary | Lock 1, Lock 8, Lock 14 | CSS typed value/document/view/visitor surfaces count as close evidence only when generated from grammar source and same-workload measured; fact-stream and brace-counter outputs are diagnostic. | Root typed surface at `crates/core/src/runtime/css_l4/document.rs:10`-`79` and `crates/core/src/runtime/css_l4/value.rs:413`-`424`; contrived live path at `skinny/crates/codegen/src/runtime_generator.rs:713`-`813`; SK-V15 close condition at `restart/skinny/tranches/sk-v15/SPEC.md:54`-`63`. | CSS rows emit typed values, row-local equality, and cssparser comparator evidence; `CSS_GENERATED_RS` and `CssFullParseSummary` absent from live admission paths. | transfer_reason=CSS typed provider is the closeable CSS receiver surface; admission_gate=generated typed provider plus same-workload cssparser equality, with fact-stream and brace-counter paths diagnostic only; verification_action=non-writing generator proof, row-local equality corpus, and retiming after W1 broadcast demotion; close_status=partial-blocked; loc_estimate=450-700; risk_class=high; wave_owner=W5; hard_cap_fit=yes only if scoped to typed provider, not full CSSOM. |
| LAC-2C-SK15-03 broadcast duplicate gate | Lock 8, Lock 14 | Any repeated throughput tuple across distinct row IDs is diagnostic unless each row has independent command, input, equality, timing, and `measurement_row_id`. | Overfit audit at `restart/audit/skinny-impl-overfit/V1/CONSOLIDATED-AUDIT.md:21`-`31`; repeated `RESULTS.md` rows at `skinny/RESULTS.md:112`-`135`. | Gate rejects duplicate CSS tuple signatures unless a broadcast group is explicitly non-admit. | transfer_reason=none; admission_gate=duplicate throughput tuple cannot admit without independent row command/input/equality/timing; verification_action=W1 broadcast collapse/demotion and duplicate-signature rejection; close_status=refuted; loc_estimate=40-80; risk_class=low; wave_owner=W1; hard_cap_fit=yes. |
| LAC-2C-SK15-04 Pattern H provenance before deletion | Lock 6, Lock 14 | Pattern H collapse means FULL line-1 generated provenance over the LIVE census (every root runtime file, count not pinned to an absolute — 71 at HEAD vs the SK-V15-era 67) plus regen/check proof; deletion is blocked unless replacement provider lands no later than the deletion consumer. | SK-V15 dependency row at `restart/skinny/tranches/sk-v15/SPEC.md:198`; simple-template boundary at `crates/core/src/runtime/builder_template.rs:17`-`25`; CSS bespoke builder at `crates/core/src/runtime/css_l4/builder.rs:15`. | `find` census equals the live count (71 at HEAD), line-1 header scan covers every file in that census, and non-writing regen/check covers every root runtime file claimed generated. The absolute count is informational; the binding gate is per-file provenance coverage, not a fixed N. | transfer_reason=Pattern H provenance prevents source-present paper close and premature deletion; admission_gate=67/67 header scan plus non-writing regen/check before any deletion; verification_action=file census, generated-owner map, and deletion blocked until replacement provider lands; close_status=partial-blocked; loc_estimate=160-240; risk_class=medium; wave_owner=W4; hard_cap_fit=yes, provenance-only. |
| LAC-2C-SK15-05 full-surface Lock 14 scan | Lock 14, Lock 16 | Lock 14 gates must report included roots, excluded roots, owner, reason, self-scan status, primitive status, gate consumer, affected rows, and disposition. Excluding a same-change leak path is a failing finding. | Live allowed-root list at `skinny/crates/bbnf-bench/src/lock14_baseline.rs:345`-`435` (the SK-V15-era `:2370`-`2379` pointer is frozen and at HEAD resolves to the generic `is_allowed_path`/`validate_backend_shape_surface` helpers, not a root list); SK-V15 gate table at `restart/skinny/tranches/sk-v15/SPEC.md:219`-`236`. At HEAD the leak roots are now PRESENT in the scan file (`grammar_profile.rs:345`, `json_sink_direct.rs:351`, `json_typed_direct.rs:357`, `json_templates/*:363`-`393`, `runtime_generator.rs` `:1144`, `grammar_provider.rs` `:1123`) — the SK-V15 exclusion is superseded; this LAC records the SK-V15-era obligation. | Gate output lists exclusions and fails while `runtime_generator.rs`, `grammar_provider.rs`, JSON direct generators, or templates are omitted from relevant generic scans. | transfer_reason=full-surface Lock 14 scan is the gate that catches generic leak roots; admission_gate=inclusion/exclusion report fails on same-change leak-path omission and cannot self-exclude; verification_action=scan reports roots, owners, reasons, self-scan status, primitive status, consumers, affected rows, and disposition; close_status=admissible-after-gate; loc_estimate=120-180; risk_class=medium; wave_owner=W2; hard_cap_fit=yes as separate scan route. |
| LAC-2C-SK15-06 future grammar onboarding | Lock 14, Lock 16 | Future grammar proof requires a source/metadata-only receiver addition; a generic code branch, new directive, new BIR, or sixth `BackendShape` is not an admissible onboarding mechanism. | Lock 14 source/metadata rule at `restart/locks/LOCKS.md:349`; SK-V15 receiver matrix at `restart/skinny/tranches/sk-v15/SPEC.md:206`-`217`; Sheets grammar at `grammar/google-sheets/google-sheets.bbnf:6`-`160`; BBNF-self grammar at `grammar/bbnf/bbnf.bbnf:4`-`85`. | Add Sheets or BBNF-self through grammar source/metadata, run regen/check, and fail if any generic owner path changes except generated manifests. | transfer_reason=future grammar onboarding proves grammar neutrality beyond CSS; admission_gate=source/metadata-only receiver with no new directive, BIR variant, sixth `BackendShape`, or generic grammar branch; verification_action=receiver fixture diff, generated manifest diff, and full-surface Lock 14 scan; close_status=admissible-after-gate; loc_estimate=120-180 for gate plus 220-400 for chosen receiver; risk_class=high; wave_owner=W7; hard_cap_fit=yes with one receiver, not both in same hard cap. |

---
---

# SK-V18 EXTENSION (V3 — the one-generator generalisation lens)

*The section above is the converged SK-V15 (V2) dossier, retained verbatim. This
V3 section EXTENDS — it does not re-derive — with the SK-V18-specific grounding:
how the named-primitive (a)-(d) gate is the discipline that keeps a hand-shaped
hot kernel grammar-PARAMETERISED rather than grammar-specific; the
`css_balanced_component_scan` neutrality-proof obligation (honestly CSS-scoped,
not falsely neutral); the Sheets precedence-tower negative control; and the
9-grammar-fleet onboarding test against the totality roster.*

## V3 Executive Summary

The SK-V18 generalisation thesis — ONE grammar-driven generator emitting
JSON + CSS + Sheets (ultimately the nine totality grammars) from `.bbnf`,
>SOTA preserved, aarch64-ONLY — is DEFENSIBLE against the published architecture,
but ONLY under the §6 named-primitive (a)-(d) gate. The honest finding the
profile forces is that a fully grammar-derived CSS parser CANNOT preserve the
94.1% scalar-scan win (`find_component_delim` + `consume_balanced_at`,
`SYNTHESIS-PROFILE.md:96`-`98`) via a generic IR tree-walk — a tree-walk descent
IS lightningcss's own architecture and categorically regresses. The escape is a
NAMED, `.bbnf`-invoked, grammar-DERIVED-arg, profile-narrow primitive. The
neutrality-proof obligation is the discipline that distinguishes a
grammar-PARAMETERISED kernel from a grammar-specific one: a primitive named
neutrally but exercised by one grammar is an overfit-in-waiting. The GROUND
s6/C4 finding is that the two offered non-CSS dischargers (JSON `{}`/`[]`
nesting, Sheets `paren_expr`) are PARSE-with-emit descents structurally
incompatible with the CSS byte-SKIP shell — so the FORCED-honest outcome is the
CSS-scoped rename `css_balanced_component_scan`, NOT a fabricated cross-grammar
caller. Sheets is the negative control: its 7-level precedence tower (Pratt /
precedence-climbing, POPL 1973) is the SOLE construct JSON+CSS structurally lack,
so it cannot be a relabeled courier. The relocated-seam firewall
(`emit_shape_source == lowered_program`) and the totality-tree 9-ident leak
(`crates/ir/src/registry/strategy.rs:137`-`185`) are the two genuine-neutrality
falsifiers the arm-census grep cannot see.

REFUTED: a neutrally-named CSS-only primitive; the 9-ident grammar-named table in
the generic `ir` crate as evidence of neutrality; the narrow 4-name Lock-14 leak
regex as a sufficient gate; and any "add an enum arm and pass" onboarding.

## V3 Technique Grounding Table

| spec-claim / T-P1 divergence id | published source cited | grounded / refuted / partial | bbnf-specific note |
|---|---|---|---|
| SK-V18-2C-ONE-GENERATOR-GENERALISATION-THESIS — ONE grammar-driven generator emits JSON+CSS+Sheets from `.bbnf`, each grammar-DERIVED | Lock 14 three-surface rule (`<name>.bbnf` + workspace metadata + optional `crates/<grammar>/`) at `restart/locks/LOCKS.md:349`; SK-V18 SPEC tranche success criterion at `restart/skinny/tranches/sk-v18/SPEC.md:63`-`64`,`:313`; the grammar-NEUTRAL 5-shape `select_lowering` at `skinny/crates/codegen/src/lower/mod.rs:18`-`24` | grounded | The thesis is admissible: the generator's only legal inputs are grammar source + metadata, and the dispatch axis (`BackendShape`) is already grammar-neutral. It is NOT yet realised — both live emit paths (`json_sink_direct::render` at `grammar_provider.rs:37`, the CSS `CSS_GENERATED_RS` const courier at `:91`) are grammar-specialised; SK-V18 is the build. bbnf-note: "one generator" is an INPUT-SURFACE property, not a prose label — proven by md5-distinct generated.rs per grammar derived from one neutral renderer, not by a shared string template. transfer_reason=Lock-14-legal single generator over neutral inputs; admission_gate=both emit paths grammar-DERIVED (verbatim_blob_present==false) + 5-shape dispatch + per-grammar md5-distinct output; verification_action=regen --check byte-equivalence + grep CSS_GENERATED_RS==0; close_status=admissible-after-G1∧G2∧G3; wave_owner=G1/G2/G3. |
| SK-V18-2C-NAMED-PRIMITIVE-ABCD-AS-NEUTRALITY-DISCIPLINE — the (a)-(d) gate keeps a hot kernel grammar-PARAMETERISED, not grammar-specific | SK-V18 SPEC §1 (a)-(d) contract at `restart/skinny/tranches/sk-v18/SPEC.md:993`-`1004`; the §6 honest-finding framing at `:378`-`390`; FFmpeg checkasm differential discipline at `https://ffmpeg.org/doxygen/8.0/checkasm_8c_source.html` (cited in 2B at `2B-primitive-vocabulary.md:63`); dav1d scalar-reference-first discipline (carried in `SYNTHESIS-PROFILE.md:101` "dav1d discipline: scalar reference FIRST, checkasm-style differential parity") | grounded | The (a)-(d) gate is the load-bearing neutrality discipline: (a) grammar-INVOKED-by-name from the `.bbnf` rule shape; (b) emitted ARGS VARY under invoking-rule mutation (the byte-set / numeric-class mutation falsifier — distinguishes a DERIVED leaf from a relabeled courier routing the same literal through a new walk); (c) `verbatim_blob_present == false`; (d) PROFILE-PROVEN-NARROW-LEAF (primitive LOC ≤ profiled hot-leaf extent; surrounding skeleton walk-derived). bbnf-note: (a)-(c) prove grammar-COUPLING; (d) bounds SIZE so the escape cannot admit an arbitrarily large relabeled blob. Lock 16 already requires the scalar-ref + checkasm-parity + same-wave-consumer manifest; (a)-(d) ADDS the grammar-coupling proof. transfer_reason=transfer the checkasm/dav1d process discipline (not media semantics) as the admissibility envelope for a hand-shaped kernel; admission_gate=ALL FOUR of (a)-(d) green or REJECT; verification_action=per-primitive mutate-falsifier + checkasm differential + primitive-LOC ≤ profiled-extent machine check; close_status=admissible-after-gate; wave_owner=G2 (CSS) ∧ G1 (JSON leaves). |
| SK-V18-2C-CSS-BALANCED-SCAN-NEUTRALITY-PROOF-FORCED-DEMOTION — `css_balanced_component_scan` proven by honest CSS-scoping, not a fabricated cross-grammar caller | SK-V18 SPEC G2.3 + the GROUND s6/C4 finding at `restart/skinny/tranches/sk-v18/SPEC.md:972`-`983`,`:985`-`1004`; the CSS byte-SKIP shell `consume_balanced_at` at the generated source `consume_balanced_at`/`find_component_delim` (`SYNTHESIS-PROFILE.md:96`-`98`); the two offered dischargers on disk — JSON `parse_object_direct`/`parse_array_direct` with `sink.*` emit, Sheets `paren_expr` at `grammar/google-sheets/google-sheets.bbnf:137` descending into `expression` (`grammar/google-sheets/google-sheets.bbnf:163`); W3C CSS Syntax Level 3 "Consume a component value" / balanced-block consumption at `https://www.w3.org/TR/css-syntax-3/#consume-a-component-value` | grounded | The neutrality-proof obligation FORCES the CSS-scoped name: the balanced-recognizer SHELL emits NOTHING (pure byte-skip over nested `()[]{}`, skipping strings/comments), whereas JSON's `{}`/`[]` nesting and Sheets' `paren_expr` are PARSE-with-emit descents — structurally incompatible callers. "Invoke the SAME primitive" is UNREACHABLE by the named non-CSS candidates, so `css_balanced_component_scan` is the FORCED-honest name, NOT a defect. bbnf-note: the neutrality of the inner kernel must be split into TWO distinct claims. (i) The BASE ≤8-byte eq-set member scan (`byte_class_from_eq_set_64`, one fan over a caller-supplied byte set) IS genuinely neutral — STRUCTURALLY (the set is caller DATA, the kernel names no grammar), NOT by a JSON consumer census: `find_ascii_set_member64` has NO live non-test/non-bench runtime caller, so it is NOT a JSON consumer proof, and the JSON `scan_dispatch` path rides a DIFFERENT primitive (`byte_class_from_table_64`); the base kernel's only live production consumer is CSS `count_top_level_commas`. (ii) The TWO-FAN ≤13-byte OR-reduce COMPOSITION (`set_a`/`set_b` split + `byte_class_from_eq_set_64 | byte_class_from_eq_set_64`, the `find_css_significant` shape the G6 retarget instantiates) is EXERCISED BY CSS ONLY — it has only `#[cfg(test)]` callers (`runtime/src/lib.rs:574`), no JSON/Sheets caller, so the COMPOSITION inherits the SAME single-grammar-exercise obligation as the shell and is subject to the neutrality-proof (forced-demote or prove a non-CSS two-fan caller), NOT presumed neutral. Only the balanced-recognizer shell AND the two-fan composition are CSS-scoped; the base one-fan kernel is structurally neutral. The CSS-scoped name + the honest disclosure IS the discharge — `g2_balanced_scan_neutrality_discharged` is GREEN via the rename. transfer_reason=CSS balanced-block consumption is a CSS Syntax algorithm, scoped honestly; admission_gate=neutral name REJECTED with zero structurally-compatible non-CSS caller, CSS-scoped name + disclosure REQUIRED; verification_action=grep `rg balanced_component_scan skinny/crates/codegen` after G2 (expect css-prefixed only); close_status=admissible-via-forced-demotion; wave_owner=G2∧G6 (one seam). |
| SK-V18-2C-SHEETS-PRECEDENCE-TOWER-NEGATIVE-CONTROL — the precedence tower is the SOLE Sheets-distinctive construct JSON+CSS lack | Vaughan Pratt, "Top Down Operator Precedence", POPL 1973, ACM DOI 10.1145/512927.512931 (`https://dl.acm.org/doi/10.1145/512927.512931`); precedence-climbing as a special case of Pratt (Norvell, "From Precedence Climbing to Pratt Parsing", `https://www.engr.mun.ca/~theo/Misc/pratt_parsing.htm`; Clang uses precedence climbing in its hand-written recursive descent parser); local 7-level left-assoc tower at `grammar/google-sheets/google-sheets.bbnf:97`-`163` (`compare_op`/`add_op`/`mul_op`/`unary_prefix` op tables, cyclic `paren_expr → expression`, `expression = comparison_expr`) | grounded | Sheets is the anti-courier falsifier. The precedence tower is right-iterated EBNF (`A = B (op B)*`) lowering to the EXISTING `SinkOnlyExpr` vocabulary (`Seq`+`RepeatLoop`+`Alt{Dispatch}`+`CallRule`) — so it needs NO new IR primitive; the stress is purely on G3's GENERALITY (does the body come from the grammar, or is it a relabeled JSON/CSS courier?). bbnf-note: the `Nu8`-tagged-alt family (`compare_op`/`add_op` etc.) is NOT the litmus — CSS L4 uses `-> Nu8u8` 295× vs Sheets' 21×, so it is a SHARED construct the generator must already handle to emit CSS at all; the precedence-tower cascade `comparison → concat → additive → multiplicative → unary → power → postfix → primary` IS the distinctive shape. If G3 cannot render recursive `CallRule`/`RepeatLoop` chains from grammar structure, the tower breaks FIRST = generalisation is NOT real. transfer_reason=Pratt/precedence-climbing is the canonical operator-precedence construct; it is the structural shape JSON (flat object/array) and CSS (flat declaration/balanced-block) cannot fake; admission_gate=Sheets emits via the generator ONLY (no `_GENERATED_RS` Sheets blob), md5-distinct, `sheets_grammar_shape == pratt-operator`; verification_action=PROVE-wave generated.rs md5 + no-blob grep + tower parse corpus; close_status=admissible-after-G3∧G4 (PROVE make-or-break); wave_owner=PROVE. |
| SK-V18-2C-5-SHAPE-BACKENDSHAPE-GENERALISES-BEYOND-JSON — the 5-shape `{EagerTape,OffsetTape,EventTape,SinkOnly,CollapsedStage}` is the neutral dispatch axis | live `select_lowering` 5-shape match at `skinny/crates/codegen/src/lower/mod.rs:18`-`24`; SK-V18 SPEC re-affirms the 5-shape domain at `restart/skinny/tranches/sk-v18/SPEC.md:75`-`78`; 1E verified-invariant row "5-shape `BackendShape` canon" at `restart/audit/totality/p1/1E-locks-evidence.md` (verified) | grounded | The `BackendShape` discriminator is grammar-DERIVED (a cost-model decision over rule shapes), NOT a grammar tag — this is exactly why R-A's un-fork dispatches on it. JSON is `SinkOnly` (the scan-free direct projection); CSS is the lowered scan IR shape; Sheets' tower lowers to the same vocabulary. bbnf-note: the SK-V15 V2 dossier (above) treated `BackendShape` as a non-regression fence ("no sixth `BackendShape`"); SK-V18 makes it the POSITIVE dispatch mechanism the un-forked emitter reads from `program.policy_summary.backend_shape`. The 5-shape domain is the falsifiable neutrality property: a sixth shape added to admit a grammar = overfit. transfer_reason=a fixed grammar-neutral shape domain over which all grammars dispatch is the generalisation backbone; admission_gate=exactly 5 shapes; emitter reads shape from lowered program NOT from any `RuntimeTarget`/grammar field; verification_action=`emit_shape_source == lowered_program` grep (render body reads zero `target.*` fields) + 5-shape count; close_status=admissible-after-G3; wave_owner=G3. |
| SK-V18-2C-9-GRAMMAR-FLEET-ONBOARDING-TEST — the totality roster is JSON, GoogleSheets, CSS L4, BBNF-self, CSV, Math, BNF, EBNF, CSS-Pretty | the 9-row `PRODUCTION_MANIFEST_TABLE` at `crates/ir/src/registry/strategy.rs:137`-`185` (9 `idents` rows enumerated); 9 generated grammars at `crates/core/src/grammar/generated/{bbnf,bnf,css_l4,css_pretty,csv,ebnf,google_sheets,json,math}.rs`; 8 grammar source roots under `grammar/` (`bbnf,bnf,css,ebnf,google-sheets,json,misc,tests` — CSV/Math under `misc/`, CSS-Pretty derived from CSS); 1F-coherence COH18-014 9-grammar census at `restart/audit/totality/p1/1F-coherence-scan.md:88`; Lock 14 per-wave gate matrix at `restart/locks/LOCKS.md` (the "one strict CSS L4 positive row, both Sheets and BBNF-self … negative-control witnesses" clause) | partial (SK-V18-witnessed-3 / SK-V19-receiver-9) | The onboarding test for ANY of the 9 (and arbitrary user grammars) is: add grammar source + metadata, run regen --check, observe generated files OR a gate-consumed rejected alternative, and FAIL if any generic owner path changes except generated manifests. SK-V18 proves the un-fork on 3 (JSON+CSS+Sheets); SK-V19 adopts to 9 (the COH18-014 / COH18-008 boundary). bbnf-note: with only one of Sheets-or-BBNF-self witnessed, the claim is SCOPED to the witnessed grammars and may not use fleet-wide grammar-neutral wording (the Lock 14 gate clause). The 9-grammar fleet is the generalisation target; the 3-grammar skinny tree is the witness. transfer_reason=the fleet roster is the onboarding-test surface; admission_gate=source/metadata-only addition, no generic owner edit except generated manifest; verification_action=add a receiver fixture, regen --check, assert generic diff is generated-only; close_status=admissible-after-gate (SK-V18 proves 3, SK-V19 scales to 9); wave_owner=PROVE (SK-V18) / SK-V19 adoption; **tranche_scope=SK-V18-witnessed (3 grammars: JSON+CSS+Sheets) ∧ SK-V19-receiver (the remaining 6 / the fleet-wide onboarding test) — the `grounded` status is SK-V18-closeable ONLY for the 3 witnessed grammars; the fleet-wide claim is a SK-V19 receiver, not SK-V18-closeable**. |
| SK-V18-2C-RELOCATED-SEAM-FIREWALL-NEUTRALITY-GATE — `emit_shape_source == lowered_program`; md5-distinct is necessary-NOT-sufficient | SK-V18 SPEC §5-risk-1 + the relocated-seam firewall at `restart/skinny/tranches/sk-v18/SPEC.md:179`-`185`,`:1097`,`:1115`; `SYNTHESIS-RESEARCH.md:272`-`279`; the R16 `RuntimeTarget: PartialEq` full-row co-gate at `SYNTHESIS-RESEARCH.md:110`-`121`; planned (not-yet-live) gate `runtime_target_rows_collapsed` (`rg runtime_target_rows_collapsed skinny/crates skinny/xtask` == 0, per 1E LAC-1E-V5-02) | grounded | The sharpest neutrality risk is un-forking the VISIBLE `RuntimeEmitterKind` enum while leaving a per-grammar branch in a neutral-identifier DATA table (the `RuntimeTarget` strategy field / per-profile columns). The arm-census grep is syntactically INCAPABLE of seeing it; only the structural `runtime_target_rows_collapsed` full-row `PartialEq` co-gate catches it. bbnf-note: the binding rule is that the un-forked `render(program)` reads output-shape ONLY from `program.policy_summary.backend_shape`, NEVER from `target.profile`/`target.emitter`/`target.output_labels`/`target.profile_contract` — falsifier: grep the render body for any `target.*` read == 0. md5-distinctness proves the OUTPUT differs per grammar but NOT that the SELECTION is grammar-derived; both gates are mandatory. SECOND SEAM (CSS side channel): the firewall must extend beyond `RuntimeTarget` fields to the CSS-typed-surface side channel — when G2 builds the grammar-derived CSS typed provider, the un-forked `render(program)` must NOT reach into the hand-owned generic `crates/core/src/runtime/css_l4/` surface (`document.rs`/`value.rs`/`view.rs`) as a side channel (the CSS analog of reading `target.profile`). Add `css_provider_source == generated` with a close test that the G2 emitter reads ZERO symbols from the generic `crates/core/src/runtime/css_l4/` hand-owned surface; as written the `RuntimeTarget`-field firewall does not fence this CSS-typed-surface seam. transfer_reason=the relocated seam (RuntimeTarget data column AND CSS-typed side channel) is the only neutrality-defeating mechanism a green arm-census passes; admission_gate=`emit_shape_source==lowered_program` ∧ `runtime_target_rows_collapsed==true` ∧ `css_provider_source==generated` (G2 emitter reads zero hand-owned `crates/core/src/runtime/css_l4/` symbols) ∧ md5-distinct; verification_action=render-body `target.*`-read grep + full-row PartialEq derive + `rg` G2-emitter-body for `crates/core/src/runtime/css_l4/` symbol reads == 0 + md5; close_status=admissible-after-G2∧G3∧P3; wave_owner=G2∧G3∧P3. |
| SK-V18-2C-WITNESS-EMISSION-NEUTRALITY-COUPLING — Sheets is the FIRST grammar to exercise the `EventGrammar` witness emission | SK-V18 SPEC P4 witness-emission scan-root coupling at `restart/skinny/tranches/sk-v18/SPEC.md:515`-`525`,`:717`; `SYNTHESIS-RESEARCH.md:104`-`108` (P4 `FORBIDDEN_GENERIC_TOKENS ⊇ {CSS_,_RS,EventGrammar,*EventGrammar}`); the `_witness.rs` `EventGrammar` impls in `grammars/json/` + `grammars/sheets_witness/` defined-but-only-test-consumed at `SYNTHESIS-RESEARCH.md:27`-`30` | partial | The Lock-14 gate's `FORBIDDEN_GENERIC_TOKENS` extension must catch any `SheetsEventGrammar` literal the generator would emit — Sheets is the first grammar to exercise the witness-emission coupling. bbnf-note: BEYOND the lexical token leak, the `<G: EventGrammar = AnyGrammar>` phantom axis on the PUBLIC `ValueRef`/`tape/mod.rs:175` substrate type is a STRUCTURAL hidden-coupling axis the P4 token-grep CANNOT see — a phantom `<G>` parameter woven through a public substrate type is exactly the relocated-seam class (per-grammar identity riding a surface a name-grep is blind to), with ZERO non-test production animator. The binding disposition is therefore NOT only the P4 token scan: R-D DELETE-default (delete the phantom `<G>` axis from the public `ValueRef` before G2/G3, G4 `Cursor` micro-trait) is the structural fix, co-gated by `event_grammar_phantom_axis_animated == false` OR `value_ref_grammar_param_deleted` (analogous to `runtime_target_rows_collapsed`). A token grep is necessary-not-sufficient against a type-level phantom coupling on a public substrate. P4 MUST land BEFORE G2/G3 so the un-forked emitter is neutrality-scanned AS authored. transfer_reason=witness-emission is a Sheets-first neutrality surface AND the `<G>` phantom axis is a public-substrate structural coupling; admission_gate=`FORBIDDEN_GENERIC_TOKENS ⊇ {CSS_,_RS,EventGrammar,*EventGrammar}` (lexical) ∧ (`value_ref_grammar_param_deleted` OR `event_grammar_phantom_axis_animated == false`) (structural, R-D DELETE-default at G4); verification_action=P4 gate falsifier (inject `SheetsEventGrammar` literal → gate RED, revert) PLUS the structural co-gate that the public `ValueRef` carries no live phantom `<G>` axis; close_status=admissible-after-P4 (lexical) ∧ G4 (structural `<G>` delete); wave_owner=P4 (pre-G2/G3) ∧ G4 (R-D phantom-axis delete). |
| SK-V18-2C-IMPORT-CLOSURE-RELAXATION-DATA-NOT-ARM — single-file Sheets onboards via a frontend-requirements DATA change | SK-V18 SPEC import-closure relaxation at `restart/skinny/tranches/sk-v18/SPEC.md:204`-`205`,`:511`-`513`; `SYNTHESIS-RESEARCH.md:200`-`205` (`w5a_sheets` flips via the import-closure-derived-from-facts relaxation, NOT a `match grammar` arm); `RuntimeFrontendRequirements{import_closure,…}` 10-bool struct at `grammar_provider.rs:46`-`57` | grounded | The neutrality test for onboarding a single-file grammar is that `import_closure` becomes present-iff-grammar-has-imports (a derived-from-facts DATA flag), NOT a `match grammar { GoogleSheets => no_imports }` arm. bbnf-note: this is the precise discipline that distinguishes admissible onboarding (a frontend-requirements data relaxation) from inadmissible (a generic-crate grammar branch). transfer_reason=import-closure presence is a grammar FACT, not a grammar identity; admission_gate=relaxation is a data flag derived from grammar source, no `match grammar` arm; verification_action=grep generic crates for a Sheets/import-closure arm == 0; close_status=admissible-after-PROVE; wave_owner=PROVE. |
| SK-V18-2C-NEUTRALLY-NAMED-CSS-ONLY-PRIMITIVE — a primitive named neutrally but exercised by ONE grammar | SK-V18 SPEC `:390` ("CSS-only primitive is an overfit-in-waiting"), the GROUND s6/C4 finding `:972`-`983`; Lock 14 clause "A primitive claimed grammar-neutral must exercise at least one non-JSON consumer or record a measured deletion/rejection" at `restart/locks/LOCKS.md` (the shared `bbnf-simd` paragraph); 1E LAC-1E-V5-03 at `restart/audit/totality/p1/1E-locks-evidence.md:149` | refuted | A neutrally-named single-grammar primitive is REJECTED as an overfit-in-waiting. The honest path is the FORCED CSS-scoped rename, not a neutral name with a contrived/absent second caller. This is the inverse-grounding of the forced-demotion row above. transfer_reason=none; admission_gate=BLOCKED — neutral name with zero structurally-compatible non-that-grammar caller is inadmissible; verification_action=any `*_component_scan` neutral name must show a real non-CSS caller OR be renamed CSS-scoped; close_status=refuted; wave_owner=G2. |
| SK-V18-2C-TOTALITY-TREE-9-IDENT-LEAK — the 9-row grammar-named `idents` table in the generic `ir` crate as neutrality | the live leak at `crates/ir/src/registry/strategy.rs:137`-`185` (9 `idents` rows: Json/GoogleSheets/CssL4/Bbnf/Csv/Math/Bnf/Ebnf/CssPretty); consumed via `for_grammar_with_manifest(grammar_ident, registry, PRODUCTION_MANIFEST_TABLE)` at `:216`; 1F-coherence COH18-005 + COH18-012 at `restart/audit/totality/p1/1F-coherence-scan.md:79`,`:86`; Lock 14 self-gate at `restart/locks/LOCKS.md:349` | refuted | The 9-grammar-named ident table in the generic `ir` crate is the totality-tree relocated-seam analog and FALSIFIES the Lock-14 self-gate: LOCKS:349 ASSERTS its verification command "returns ZERO" but the live `rg 'JsonParser|CssL4Parser|BbnfBootstrap|GoogleSheetsParser' crates/ir/src/ crates/analysis/src/` returns 13 sites — the gate is RED, not "benignly catching a leak". (SCOPE NOTE: the literal LOCKS:349 command spans 13 crates `crates/{ir,parse,codegen,runtime,path,path-core,egraph,csp-solver,parse-that-regex,parse-that,bbnf-simd,analysis,lsp}/src/`; the cited `crates/ir/src/ crates/analysis/src/` form is a 2-crate SUBSET, equivalent-at-HEAD because the other 11 crates are absent at repo-root by those exact names or empty of hits — all 13 sites fall in `ir`(11)+`analysis`(2). The gate-RED / 13-site falsification holds under both forms at HEAD; the 2-crate form would diverge only under a future repo-root promotion of the skinny crates.) bbnf-note: the narrow 4-name regex catches only 4 of 9 idents rows (Json/GoogleSheets/CssL4/Bbnf); the other 5 (Csv/Math/Bnf/Ebnf/CssPretty) escape it entirely. R16 structural row-collapse must collapse ALL 9 (SK-V19 totality fold; D-1E-V5-14). transfer_reason=none — grammar names in a neutral data table the arm-grep cannot see; admission_gate=BLOCKED — structural full-row collapse required, narrow regex insufficient; verification_action=R16 `PartialEq` row-collapse over all 9 idents rows + widen the leak regex to all 9 names; close_status=refuted (SK-V19 receiver); wave_owner=SK-V19; **tranche_scope=SK-V19-receiver — the EMPIRICAL leak (13-site self-gate falsification) is verified at HEAD, but its CLOSE (R16 structural row-collapse over all 9 idents) is a SK-V19 wave, NOT SK-V18-closeable; the `refuted` status is a SK-V18 finding routed to a SK-V19 receiver**. |
| SK-V18-2C-CSS-TYPES-HOST-SHIM-LEAK — `crates/core/src/css_types.rs` in the generic core crate as a Lock-14-(c) declaration crate | the still-live file at `crates/core/src/css_types.rs:1` (66 LOC, "Host shims for the CSS L4 grammar's `-> parse_hex_color(...)` map"); Lock 14 names it verbatim as the mess at `restart/locks/LOCKS.md:349`; 1F-coherence COH18-006 + U-COH18-002 at `restart/audit/totality/p1/1F-coherence-scan.md:80`,`:121` | refuted | Lock 14 (c) admits ONLY a per-grammar declaration crate `crates/<grammar>/`; `css_types.rs` lives in the GENERIC core crate, so Lock 14 (c) does NOT apply — it is the lock-NAMED mess, not a relocated-admissible shim. bbnf-note: admissible ONLY if SK-V19 relocates it to a `crates/css/` declaration crate (the receiver), or deletes it. transfer_reason=none in-place; admission_gate=BLOCKED unless relocated to `crates/css/` declaration crate; verification_action=confirm path is `crates/core/src/` (generic) not `crates/css/`; close_status=refuted-in-place (SK-V19 relocate-or-delete); wave_owner=SK-V19; **tranche_scope=SK-V19-receiver — `css_types.rs` in generic core is verified at HEAD, but the relocate-or-delete CLOSE is a SK-V19 wave, NOT SK-V18-closeable; the `refuted` status is a SK-V18 finding routed to a SK-V19 receiver**. |

## V3 Architectural Assertions Defended

1. **Grammar-neutrality is the (a)-(d) named-primitive discipline, not a prose
   label.** The single largest paper-close surface in the SK-V18 contract is the
   §6 escape — admitting a hand-shaped hot kernel. It is grammar-NEUTRAL (works
   for any grammar with that construct) iff (a) grammar-INVOKED-by-name, (b)
   emitted-output-VARIES-under-invoking-rule-mutation, (c)
   `verbatim_blob_present == false`, (d) PROFILE-PROVEN-NARROW-LEAF. (a)-(c)
   prove grammar-COUPLING; (d) bounds SIZE. A primitive failing any of the four
   is a relabeled blob (`restart/skinny/tranches/sk-v18/SPEC.md:257`-`266`,
   `:993`-`1004`). This is the discipline that keeps the 94.1% CSS scan
   grammar-PARAMETERISED rather than grammar-specific.

2. **The `css_balanced_component_scan` CSS-scoped name is the FORCED-honest
   outcome, not a defect.** The balanced-recognizer SHELL emits nothing (pure
   byte-skip over nested `()[]{}`); the two offered non-CSS dischargers (JSON
   `parse_object_direct`/`parse_array_direct`, Sheets `paren_expr`) are
   PARSE-with-emit descents (`grammar/google-sheets/google-sheets.bbnf:137`,
   `:163`), structurally incompatible. "Invoke the SAME primitive" is
   UNREACHABLE; the CSS-scoped rename + honest disclosure IS the discharge. The
   eq-set neutrality SPLITS: the BASE ≤8-byte one-fan kernel
   (`byte_class_from_eq_set_64`) is STRUCTURALLY neutral (caller-supplied byte set,
   names no grammar) — NOT proven by a JSON consumer (`find_ascii_set_member64`
   has zero live runtime callers; the JSON `scan_dispatch` path rides
   `byte_class_from_table_64`, a different primitive; the base kernel's only live
   production consumer is CSS). But the TWO-FAN ≤13-byte OR-reduce COMPOSITION the
   G6 retarget instantiates (`find_css_significant` shape) is CSS-exercised-ONLY
   (only `#[cfg(test)]` callers), so the composition is subject to the SAME
   neutrality-proof obligation as the shell — forced-demote or prove a non-CSS
   two-fan caller. This is W3C CSS Syntax "Consume a component value" scoped
   honestly, not a false neutral. NOTE — the cited neutrality-evidence file
   `skinny/crates/runtime/src/runtime_simd.rs:6`-`7` carries an INACCURATE source
   comment ("the same kernel JSON's `scan_structurals` rides"): this is empirically
   FALSE — distinguish the JSON PRODUCT path (scan-free / `SinkOnly` direct projection,
   S-P1) from the JSON structural-scan FACILITY: on the aarch64 close host,
   `scan_structurals` (`json/scan.rs:22`) returns `neon::scan` (`:25`) →
   `bbnf_simd::aarch64::classify_tbl4` (`:214`,`:219`,`:228`) — the TBL
   `byte_class_from_table_64` family, a DIFFERENT primitive than the eq-set kernel;
   `scan_structurals_scalar` (`:29`) is the `#[allow(unreachable_code)]` non-aarch64
   fallback, not the close-host route. Either way the eq-set kernel is never on the
   JSON structural-scan path, and the JSON `scan_dispatch` path rides the TBL
   `byte_class_from_table_64` family. The structural
   neutrality (caller-supplied byte set, kernel names no grammar) stands, but the
   source comment's JSON-rides claim is a same-wave G6 source-fix obligation so a
   future consumer reading the file does not re-adopt the refuted dual-consumer claim.

3. **Sheets is the negative control because the precedence tower is the SOLE
   construct JSON+CSS structurally lack.** The 7-level left-assoc tower
   (`grammar/google-sheets/google-sheets.bbnf:97`-`163`) is Pratt /
   precedence-climbing (POPL 1973, DOI 10.1145/512927.512931) — right-iterated
   EBNF lowering to the EXISTING `SinkOnlyExpr` vocabulary, needing no new IR
   primitive, stressing only G3's generality. The `Nu8`-tagged-alt family is a
   SHARED construct (CSS uses `-> Nu8u8` 295× vs Sheets 21×), correctly DEMOTED
   from the litmus. If G3 cannot render recursive `CallRule`/`RepeatLoop` chains
   from grammar structure, the tower breaks FIRST = generalisation is not real.
   The binding fallback is honest surfacing, never a `_GENERATED_RS` Sheets blob.

4. **The 5-shape `BackendShape` is the neutral dispatch backbone, and the
   relocated-seam firewall is its falsifiable neutrality property.** The
   un-forked emitter reads output-shape ONLY from
   `program.policy_summary.backend_shape` (`skinny/crates/codegen/src/lower/mod.rs:18`-`24`),
   never from a `RuntimeTarget`/grammar field (`emit_shape_source ==
   lowered_program`). md5-distinct output is necessary-NOT-sufficient; the
   structural `runtime_target_rows_collapsed` full-row `PartialEq` co-gate is
   mandatory because the arm-census grep cannot see a per-grammar branch
   relocated into a neutral data table.

5. **The onboarding test against the 9-grammar fleet is the genericity proof.**
   The totality roster is JSON, GoogleSheets, CSS L4, BBNF-self, CSV, Math, BNF,
   EBNF, CSS-Pretty (`crates/ir/src/registry/strategy.rs:137`-`185`; 9 generated
   at `crates/core/src/grammar/generated/`). Onboarding any one (or an arbitrary
   user grammar) is a source/metadata-only change with regen --check; a generic
   code branch, new directive, new BIR, or sixth `BackendShape` is inadmissible.
   SK-V18 proves the un-fork on 3 (JSON+CSS+Sheets); SK-V19 scales to 9. With
   only one of Sheets-or-BBNF-self witnessed, the claim is scoped to the
   witnessed grammars (Lock 14 gate clause).

## V3 Architectural Assertions Refuted

| assertion | disposition | reason |
|---|---|---|
| A neutrally-named primitive exercised by ONE grammar proves neutrality. | refuted | Overfit-in-waiting. The forced-honest outcome is the CSS-scoped rename or a measured deletion/rejection (`restart/skinny/tranches/sk-v18/SPEC.md:390`; Lock 14 `bbnf-simd` clause; 1E LAC-1E-V5-03). |
| The `Nu8`-tagged-alt family is a Sheets-distinctive litmus construct. | refuted | CSS L4 uses `-> Nu8u8` 295× across its import closure vs Sheets' 21×; it is a SHARED construct the generator must already handle to emit CSS at all (`SYNTHESIS-RESEARCH.md:42`). The precedence-tower cascade is the real litmus. |
| The 9-row grammar-named `idents` table in `crates/ir/` is grammar-neutral. | refuted | It is the totality relocated-seam analog; the names are live-consumed via `for_grammar_with_manifest` at `strategy.rs:216`. The Lock-14 self-gate ASSERTS ZERO but returns 13 live sites — RED (`1F-coherence-scan.md:79`,`:86`; D-1E-V5-14). |
| The narrow 4-name leak regex (`JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser`) is a sufficient Lock-14 gate. | refuted | It catches only 4 of 9 idents rows; Csv/Math/Bnf/Ebnf/CssPretty escape it entirely (`1F-coherence-scan.md:79`). Genericity needs structural full-row collapse, not a name-list grep. |
| `crates/core/src/css_types.rs` is an admissible Lock-14-(c) declaration crate. | refuted | Lock 14 (c) admits ONLY `crates/<grammar>/`; this file is in the GENERIC core crate and is named verbatim by the lock as the mess (`1F-coherence-scan.md:80`; U-COH18-002). |
| md5-distinct generated.rs per grammar proves grammar-derived selection. | refuted (necessary-not-sufficient) | md5-distinctness proves the OUTPUT differs but not that the SELECTION is grammar-derived; a relocated seam riding neutral per-profile columns produces distinct output under a green arm-census. The `runtime_target_rows_collapsed` co-gate is mandatory (`SYNTHESIS-RESEARCH.md:276`-`279`; LAC-1E-V5-02). |
| A generic IR tree-walk lowering can preserve the 94.1% CSS scan. | refuted | A tree-walk descent IS lightningcss's own architecture and categorically regresses >SOTA; the flat balanced scan was built to AVOID that descent (the genuine §4 tension, `SYNTHESIS-RESEARCH.md:50`-`52`,`:222`-`230`). The escape is the (a)-(d) gated named primitive. |
| Adding a grammar can be "add an enum arm and pass". | refuted | Onboarding is source/metadata-only with regen --check; a generic-crate grammar branch, `RuntimeGenerationMode` mode-split, profile array, or `match grammar` arm violates Lock 14 (carried from V2 SK-V15-2C-GENERIC-GRAMMAR-SWITCH; reaffirmed for the 9-grammar fleet). |
| x86/AVX-512 esoterica can close an M5 Max generalisation row. | refuted | aarch64 / Apple M5 Max is the ONLY close route; the entire x86 surface is a DELETION target in skinny. x86 literature is hardware-gated architecture pressure that closes nothing (`restart/skinny/tranches/sk-v18/SPEC.md:51`-`52`; 1E LAC-1E-V5-04). |

## V3 Open Research Questions

| UNKNOWN | verify_action |
|---|---|
| Does the totality 9-grammar tree ALREADY un-fork, or only the skinny 3-grammar witness? | Inspect `crates/core/src/grammar/generated/*.rs` provenance + whether `for_grammar_with_manifest` (`strategy.rs:216`) is the generator path or only `regen --check` (routed to U-COH18-001); the SK-V18→SK-V19 boundary (COH18-014/COH18-008). |
| Can the import-closure relaxation for single-file Sheets be a pure data flag with NO `match grammar` arm anywhere in generic crates? | After PROVE, `rg` generic crates (`ir`, `codegen`, `analysis`) for any Sheets/import-closure conditional == 0; confirm `w5a_sheets` flips via a `RuntimeFrontendRequirements` data change (`grammar_provider.rs:46`-`57`). |
| Will a future non-CSS caller ever RE-PROMOTE `css_balanced_component_scan` to a neutral name? | Re-grep `rg balanced_component_scan skinny/crates/codegen` after a future grammar with a byte-SKIP (non-emitting) balanced construct lands; until then the CSS-scoped name is permanent (1E U2). |
| Does the §6 (a)-(d) gate belong in LOCKS (Lock 14/16 addendum) or stay SK-V18-SPEC-scoped governance? | T-P3 disposition of LAC-1E-V5-01 (1E U1); Pass Omega ratifies. |
| Is the +4 Pattern-H census drift (67→71 in `crates/core/src/runtime`) a grammar-roster change, a sub-wave update, or an O(N) generator regression? | Re-run `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l`; trace the +4 (`tape/{mod,cursor,arena,record}.rs`) to LAC-1E-15 attribution or open the generator-regression scan (1E U3 / D-1E-V5-06). |
| The R-F grounding source `SYNTHESIS-RESEARCH.md:43` justifies eq-set neutrality with "JSON neutrality is honest (same eq-set kernel JSON's `scan_structurals` already rides)" — that is the SAME falsified dual-consumer claim this dossier refutes (the JSON PRODUCT path is SCAN-FREE per S-P1; and the JSON structural-scan FACILITY rides the TBL family, not the eq-set kernel: on aarch64 `scan_structurals` at `json/scan.rs:22` returns `neon::scan` (`:25`) → `classify_tbl4` (`:214`,`:219`,`:228`), with `scan_structurals_scalar` (`:29`) the `#[allow(unreachable_code)]` non-aarch64 fallback; JSON `scan_dispatch` rides `byte_class_from_table_64`). The R-F retarget route itself (salvage the two-fan set-split onto the recursive scalar shell) is grammar-neutral and correctly grounded; only its JSON-neutrality JUSTIFICATION line is stale. | The HONEST R-F grounding is "the JSON product path is scan-free, so the eq-set neutrality is STRUCTURAL (caller-supplied byte set, kernel names no grammar), NOT an empirical-dual-consumer claim"; record `SYNTHESIS-RESEARCH.md:43`'s "scan_structurals already rides" as a same-wave source-fix obligation so the synthesis source is not cited at face value, and confirm the retarget grounding rests on the structural-neutrality argument, not the refuted dual-consumer one. |

## V3 Wave-Owner Re-key (CH4 cost-field discipline)

The V2 (SK-V15) grounding/LAC rows above carry SK-V15 `W#` wave owners
(W1/W4/W5/W6/W7/W8) verbatim — those describe the retired SK-V15 ledger and stand
only as the historical record. For the SK-V18 close, every SK-V18-scope row is keyed
to the LIVE SK-V18 §8 wave ledger `P4/P5/G1/G2/G3/G4/G5-G6/PROVE/H1`
(`restart/skinny/tranches/sk-v18/SPEC.md:436`-`444`; G2/G5-G6 `≤450 hand LOC`,
`≤90 min wall`, `30-45 min/redress`). The V3 grounding/assertion rows already carry
G-letter owners; the SK-V15→SK-V18 wave map for the carried-forward concerns:
SK-V15 W5 (typed CSS provider) → **G2**; W6 (CSS sidecar deletion) → **G2** (delete
`CSS_GENERATED_RS` in-wave, no separate deletion wave); W7 (generated provider
manifest / future-grammar onboarding) → **G3** (un-fork) ∧ **PROVE** (Sheets witness)
∧ **SK-V19** (fleet adoption); W4 (Pattern-H provenance) → **P3/G3** structural
row-collapse; W1/W8 (CSS broadcast demote / comparator) → **H1** (honesty close).
No SK-V18 close cost is budgeted against a `W#` wave. The 3 SK-V19-receiver rows
(9-IDENT-LEAK, CSS-TYPES-HOST-SHIM, fleet-onboarding) carry `tranche_scope` inline.

## V3 SK-V18 Cost Manifest (CH4 per-row loc_estimate / risk_class)

Per-row admission cost for the SK-V18-scope V3 grounding rows, keyed to the live
§8 wave band (`restart/skinny/tranches/sk-v18/SPEC.md:436`-`444`; G1/G2/G3/PROVE each
`≤450 hand source/test/gate LOC`, `≤90 min wall`, `30-45 min/redress`). Mirrors the
2B (`:401`-`417`) / 2D (`:95`-`98`) / 2E (`:132`-`144`) / 2F (`:92`-`100`) manifest
shape. The 3 SK-V19-receiver rows carry their cost in their inline `tranche_scope`
(cost = SK-V19, NOT charged to an SK-V18 band); the firewall / named-primitive /
5-shape rows are GATE-ONLY (small additive gate LOC, no body build).

| row (V3 grounding id) | wave_owner | loc_estimate | risk_class | rollback | abrogate |
|---|---|---|---|---|---|
| ONE-GENERATOR-GENERALISATION-THESIS | G1∧G2∧G3 | **SUM of two §8 bands: G2 ≤450 (the grammar-DERIVED CSS provider — `lower/css_scan.rs` + `css_scan_direct.rs` + primitive shell + arg-derivation, `CSS_GENERATED_RS` DELETED; SPEC `:439`) + G3 ≤450 (the un-fork — DELETE `RuntimeEmitterKind`, dispatch on `BackendShape`, byte-equivalent output; SPEC `:440`).** The thesis is the COMBINED outcome of those two waves; its bodies ARE G2 and G3 (each separately budgeted ≤450 across two ≤90-min walls), NOT a single ≤450 here. This umbrella is a CROSS-REFERENCE row only — the G2 body is owned by the CSS-typed-provider rows below + SPEC §5; the G3 body is owned by 2D LAC-2D-V3-01 (G3, ≤450) — no body is charged AT this row (to avoid double-budgeting against the two owning rows). | HIGH (relocated-seam risk; co-gated) | PATH-local revert to the two specialised emit paths (`json_sink_direct::render` + `CSS_GENERATED_RS` courier) | if the un-fork cannot dispatch all 3 witnessed grammars on `BackendShape` without a `match grammar` arm, DELETE the un-fork and carry the SPEC-declared two-path unworkability — do NOT ship a shim |
| NAMED-PRIMITIVE-ABCD-AS-NEUTRALITY-DISCIPLINE | G2 (CSS) ∧ G1 (JSON leaves) | ≈ +20 gate LOC (the (a)-(d) machine check is a gate, not a kernel; the kernel cost is the 2E/2F SECTION-A manifest) | LOW (gate-only) | remove the (a)-(d) gate, fall back to the Lock-16 scalar-ref+checkasm cell alone | escalate to a structural primitive-LOC-vs-profiled-extent assertion if the gate is bypassed |
| CSS-BALANCED-SCAN-NEUTRALITY-PROOF-FORCED-DEMOTION | G2∧G6 (one seam) | ≈ +5 rename LOC + the 2E/2F G6 retarget body (≤150 LOC, owned there) | LOW (forced rename) | revert the `css_`-prefix rename | n/a (the rename IS the discharge; no body to abrogate here) |
| SHEETS-PRECEDENCE-TOWER-NEGATIVE-CONTROL | PROVE | ≤450 hand LOC (the Sheets generator path; the tower lowers to the EXISTING `SinkOnlyExpr` vocabulary — no NEW IR primitive, so the cost is generator generality, not a new construct) | MED (PROVE make-or-break generality) | revert to JSON+CSS-only generator scope (defer Sheets to SK-V19) | if G3 cannot render the recursive `CallRule`/`RepeatLoop` tower from grammar structure, the generalisation is NOT real — carry that as the SPEC §6 overfit verdict, do NOT relabel a JSON/CSS courier as the Sheets body |
| 5-SHAPE-BACKENDSHAPE-GENERALISES-BEYOND-JSON | G3 | ≈ +10 gate LOC (`emit_shape_source==lowered_program` grep + 5-shape count — gate-only; the dispatch already exists at `lower/mod.rs:18`-`24`) | LOW (gate-only) | remove the additive grep gate | escalate to a structural assertion that the render body reads zero `target.*` fields |
| RELOCATED-SEAM-FIREWALL-NEUTRALITY-GATE (RuntimeTarget seam) | G2∧G3∧P3 | ≈ +10 gate LOC (`runtime_target_rows_collapsed` full-row `PartialEq` + render-body `target.*`-read grep == 0; the same cost as 2D LAC-2D-V3-02) | MED (necessary-not-sufficient; co-gate) | remove the additive grep gate; keep md5-distinct only (insufficient — disclosed) | escalate to the R16 structural full-row collapse |
| RELOCATED-SEAM-FIREWALL — CSS-typed second seam (`css_provider_source==generated`) [CH4-V3-02] | G2 | ≈ +5 gate LOC (a SEPARATE additive grep: the G2 emitter body reads ZERO symbols from the hand-owned generic `crates/core/src/runtime/css_l4/` surface — NOT covered by 2D LAC-2D-V3-02's `RuntimeTarget`-field firewall) | LOW (gate-only, additive) | remove the additive `crates/core/src/runtime/css_l4/`-symbol-read grep | escalate to a structural css-provider-origin assertion (the typed provider type is generator-emitted, not a hand-owned-surface re-export) |
| WITNESS-EMISSION-NEUTRALITY-COUPLING | P4 (lexical) ∧ G4 (structural `<G>` delete) | ≈ +10 gate LOC (the `FORBIDDEN_GENERIC_TOKENS` extension) + the R-D phantom-axis DELETE (≈ -1 type param on `ValueRef`, a deletion not a build) | MED (public-substrate structural coupling) | revert the `FORBIDDEN_GENERIC_TOKENS` extension; the `<G>` delete is a deletion (rollback = re-add the phantom param, but it has zero animator) | DELETE the phantom `<G>` axis from the public `ValueRef` (R-D DELETE-default) — never animate it to satisfy the gate |
| IMPORT-CLOSURE-RELAXATION-DATA-NOT-ARM | PROVE | ≈ +5 LOC (flip `import_closure` to a derived-from-facts DATA flag; NOT a `match grammar` arm) | LOW (data flag) | revert to the prior import-closure default | if onboarding requires a `match grammar { GoogleSheets => no_imports }` arm, that is the inadmissible-branch verdict — do NOT add the arm |
| NEUTRALLY-NAMED-CSS-ONLY-PRIMITIVE (refuted) | G2 | 0 LOC (REFUTED — no admission; the discharge is the forced rename above) | n/a | n/a | n/a (refuted route, no body) |
| TOTALITY-TREE-9-IDENT-LEAK (refuted) | SK-V19 | cost = SK-V19 (per inline `tranche_scope`); the EMPIRICAL 13-site self-gate falsification is verified at HEAD, its R16 structural-row-collapse CLOSE is SK-V19 | n/a in SK-V18 | n/a | DEFER to SK-V19 R16 — do NOT bolt a 9-name regex widen into an SK-V18 gate as a patch |
| CSS-TYPES-HOST-SHIM-LEAK (refuted) | SK-V19 | cost = SK-V19 (per inline `tranche_scope`); the generic-core file location is verified at HEAD, the relocate-or-delete CLOSE is SK-V19 | n/a in SK-V18 | n/a | DEFER to SK-V19 relocate-to-`crates/css/`-or-delete |
| 9-GRAMMAR-FLEET-ONBOARDING-TEST (partial) | PROVE (SK-V18 witnesses 3) / SK-V19 (fleet 9) | SK-V18 portion ≤450 hand LOC (PROVE witnesses 3 grammars); the fleet-9 onboarding cost = SK-V19 (per inline `tranche_scope`) | MED (SK-V18 witness) / SK-V19 (fleet) | revert to JSON+CSS-only witness (defer Sheets) | if a generic owner path must change to onboard a receiver grammar (beyond generated manifests), that is the inadmissible-branch verdict — DEFER fleet to SK-V19 |

## V3 LOCKS-AMENDMENTS-CANDIDATE

*These three are the 2C-scope SK-V18 generalisation candidates. They are
DOWNSTREAM PARTNERS of the 1E primary candidates (LAC-1E-V5-01/02/03), framed
here from the grammar-neutrality lens — CANDIDATES ONLY; never amend, never
re-number. T-P3 dispositions; Pass Omega ratifies.*

| id | lock(s) | amendment candidate | evidence | close test |
|---|---|---|---|---|
| LAC-2C-SK18-01 neutrality-proof obligation (the FORCED-demotion clause) | Lock 14, Lock 16 | Bind the neutrality-proof obligation as a grammar-neutrality discipline: a primitive named neutrally but exercised by ONE grammar in a campaign MUST be PROVEN neutral by ≥1 structurally-compatible non-that-grammar invocation OR demoted to an honestly grammar-scoped name. The gate REJECTS a neutral name with zero structurally-compatible non-that-grammar caller; the CSS-scoped rename + honest disclosure IS the discharge (NO fabricated cross-grammar caller). The INNER caller-data sub-kernel may remain neutral independently of the SHELL. (Partners 1E LAC-1E-V5-03; framed from the 2C generalisation lens.) | `restart/skinny/tranches/sk-v18/SPEC.md:972`-`983`,`:390`,`:1034`; the GROUND s6/C4 dischargers at `grammar/google-sheets/google-sheets.bbnf:137`,`:163`; W3C CSS Syntax "consume a component value" `https://www.w3.org/TR/css-syntax-3/#consume-a-component-value`; Lock 14 `bbnf-simd` non-JSON-consumer clause at `restart/locks/LOCKS.md:349`. | `rg balanced_component_scan skinny/crates/codegen` after G2 returns CSS-prefixed names only; `g2_balanced_scan_neutrality_discharged == true` via the rename, NOT a fabricated caller. |
| LAC-2C-SK18-02 fleet-scoped neutrality wording | Lock 14 | Bind the fleet-scoping discipline: a generalisation claim witnessed by fewer than the full roster is SCOPED to the witnessed grammars and may NOT use fleet-wide grammar-neutral wording. SK-V18 proves the un-fork on 3 (JSON+CSS+Sheets); fleet-wide (9-grammar) wording requires the SK-V19 adoption OR both Sheets AND BBNF-self negative-control witnesses in the same wave. (Partners the Lock 14 per-wave gate clause; framed from the 2C onboarding-test lens.) | the 9-row roster at `crates/ir/src/registry/strategy.rs:137`-`185`; Lock 14 per-wave gate "with only one of Sheets or BBNF-self, the claim is scoped to the witnessed grammars" at `restart/locks/LOCKS.md:349`; 1F-coherence COH18-014/COH18-008 at `restart/audit/totality/p1/1F-coherence-scan.md:88`. | A wave claiming fleet-wide neutrality with only one negative-control witness fails the scoping gate; SK-V19 onboarding of the remaining 6 grammars is source/metadata-only with regen --check. |
| LAC-2C-SK18-03 totality-tree row-collapse precondition | Lock 14 | Re-key the totality relocated-seam: the 9-row grammar-named `idents` table in the generic `ir` crate (and the `css_types.rs` host shim in generic core) are Lock-14 leaks the narrow 4-name regex cannot fully see (catches 4 of 9). Genericity requires STRUCTURAL full-row collapse over all 9 idents rows + widening the leak regex to all 9 names; the Lock-14 self-gate is currently FALSIFIED/RED (asserts ZERO, returns 13). (Partners D-1E-V5-14; the SK-V19 R16 totality receiver.) | live leak at `crates/ir/src/registry/strategy.rs:137`-`185`,`:216`; `crates/core/src/css_types.rs:1`; the falsified self-gate `rg 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/ir/src/ crates/analysis/src/` = 13 vs asserted ZERO (the cited 2-crate grep is a SUBSET of the 13-crate LOCKS:349 command `crates/{ir,parse,codegen,runtime,path,path-core,egraph,csp-solver,parse-that-regex,parse-that,bbnf-simd,analysis,lsp}/src/`, equivalent-at-HEAD because the other 11 crates are absent at repo-root or empty of hits — verified `wc -l`=13 under the literal 13-crate form); 1F-coherence COH18-005/006/012 at `restart/audit/totality/p1/1F-coherence-scan.md:79`-`86`. | R16 `PartialEq` full-row collapse subsumes all 9 idents rows; widened leak regex covers Csv/Math/Bnf/Ebnf/CssPretty; `css_types.rs` relocated to a `crates/css/` declaration crate or deleted (SK-V19). |
