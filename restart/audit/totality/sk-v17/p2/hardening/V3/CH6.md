---
lens: CH6 ANTI-PAPER-CLOSE
pass: T-P2-research (SK-V17 totality fold)
cycle: V3
reviewer: CH6 (V3)
generated_at: 2026-05-29T00:00:00Z
subject_dossiers: [2a-sota-landscape, 2b-primitive-vocabulary, 2c-grammar-neutrality, 2d-cost-model, 2e-host-arch, 2f-fold-gaps]
master_head: 91b6893b0
t_p1_source_sha: 445925167
contract: restart/prompts/totality/PASS-2-RESEARCH.md §3 (CH6), §8, ORCHESTRATOR §3W
focus: no fold deferred to "a future wave will detail"; each carries a concrete scalar-ref/manual/measurement; no engineered-defer
prior_cycles: V1 (90.2% ACCEPT — 1 REJECT R01, 3 REVISE V01/V02/V03), V2 (100% ACCEPT — fold-verification cycle, count-nit CH6-V2-COUNT-NIT surfaced for CH1-lane)
sections_reviewed: 41
accept: 41
revise: 0
reject: 0
accept_pct: 100.0
verdict: PASS (V3 = second consecutive ≥95% ACCEPT; all V1+V2 dispositions folded + re-verified at HEAD; zero new engineered-defer; convergence-cycle all-ACCEPT, distinct from V1-paper-close all-ACCEPT)
---

# CH6 ANTI-PAPER-CLOSE — T-P2 SK-V17 V3

## Lens posture

CH6 scans for paper-close: a technique claimed "validated/proven" on citation
density alone; reference-stuffing (N sources cited, none integrated); a fold that
fails to state the bbnf-specific reason it transfers; and — the load-bearing axis
for THIS dispatch — **engineered-defer**: a fold-proposal or research question
shoved to "a future wave / a later research pass will detail this" WITHOUT a
concrete scalar-ref, manual/published-precedent, or measurement attached at THIS
pass.

**The legitimate-defer boundary (binding, carried verbatim from V1/V2).** PASS-2
§1 + the dispatch establish SK-V18 as the *implementation* wave and T-P3 as the
*synthesis* pass; T-P2 is forward-architecture *grounding*, not implementation
(PASS-2 §intro lines 19–25). Therefore "the impl lands in SK-V18" / "200–700 LOC
SK-V18 fold" / "T-P3 names the convergence-target" is NOT a CH6 defer — it is the
research→implementation and research→synthesis boundary the pass is built on. CH6
flags only deferral of the *grounding itself*: where a dossier owes a citation, a
scalar-ref, or a measurement at THIS pass and instead points at a later research
cycle. A fold that names its concrete admission artefact NOW (scalar-ref path,
checkasm command, manual precedent, measured regression) and schedules only the
*coding* for SK-V18 or the *disposition* for T-P3 3C is ACCEPT.

**V3 posture — second-consecutive convergence verification.** V1 returned 90.2%
ACCEPT (the load-bearing REJECT CH6-V1-R01 + 3 REVISE). V2 returned 100% ACCEPT as
a fold-verification cycle, surfacing one CH1-lane count-nit (CH6-V2-COUNT-NIT, the
ONBOARD "8 vs 9 grammar dirs" recap). V3's mandate is to confirm — against the
live tree at HEAD `91b6893b0`, not against the dossiers' claim-of-fold — that (a)
every V1 disposition stayed folded through the V3 regeneration, (b) the V2
count-nit folded, and (c) the V3 regeneration introduced NO new engineered-defer.
Every disposition below is re-executed at HEAD this cycle; a "folded" assertion is
worthless to CH6 without a ground-truth re-run.

## Census

| dossier | sections | ACCEPT | REVISE | REJECT |
|---|---|---|---|---|
| 2a-sota-landscape | 8 (6 folds + 2 refutations) | 8 | 0 | 0 |
| 2b-primitive-vocabulary | 9 (L1–L9 folds) | 9 | 0 | 0 |
| 2c-grammar-neutrality | 7 (6 folds + onboarding) | 7 | 0 | 0 |
| 2d-cost-model | 7 (FOLD-2D-01..07) | 7 | 0 | 0 |
| 2e-host-arch | 6 (FOLD-2E-A..F) | 6 | 0 | 0 |
| 2f-fold-gaps | 9 (F1–F9) | 9 | 0 | 0 |
| cross-cutting (V03 echo + count-nit) | — | (folded into above) | 0 | 0 |
| **total** | **41** | **41** | **0** | **0** |

Accept rate **100.0%** — at the §3W ≥95% bar, second consecutive cycle ≥95%
(V2=100%, V3=100%). The §3 "Cycle V1 expects ≥30% REVISE; an all-ACCEPT wave is
paper-close" rule governs **V1** (which correctly carried the load-bearing REJECT +
3 REVISE), not the convergence cycles. V3's all-ACCEPT is the convergence-cycle
all-ACCEPT the §4 criterion ADMITS — every ACCEPT grounded on a ground-truth
re-execution at HEAD (greps run, byte-counts taken, line refs resolved, NEON
intrinsics enumerated) — not on citation density. This is the criterion's
"≥95% ACCEPT for two consecutive cycles" advance condition, not the V1-paper-close
the rule bars.

---

## V1 + V2 DISPOSITIONS — FOLD RE-VERIFICATION (the load-bearing V3 audit)

### CH6-V1-R01 (REJECT, V2-discharged) — engineered-defer of aarch64 esoterica grounding → **STILL FOLDED + RE-VERIFIED at HEAD**

**The V1 defect.** 2E U-2E-04 routed the aarch64-esoterica grounding to "a future
2E-esoterica cycle" (a defer-loop with 2D UNKNOWN-2D-05); the frontmatter
overcounted uncited aarch64 ISA citations.

**V3 re-verification at `2e-host-arch.md:479`.** U-2E-04 reads "**CONCRETE sources
NOW (CH6-V1-R01 fold; no defer-loop):** the Arm A64 ISA manual FSM-dispatch lineage
+ Lemire 2026 `svmatch_u8`-on-NEON port + Validark 2024 … **Bounded refutation
recorded:** NEON has no AVX-512-mask branchless-FSM analogue … mechanically refused
on aarch64 at `admits_collapsed_stage` (LAC-2D-06). No admission this pass … This
resolves 2d:264 UNKNOWN-2D-05 … no future-cycle defer." The defer-loop phrase is
absent; the grounding action is performed at this pass. The frontmatter
(`2e:10`) reads `primary_sources_cited: 9 … frontmatter count = exercised-in-body,
not inventory; CH6-V1-R01`. Both defer-loop ends reconciled: 2D UNKNOWN-2D-05
(`2d:285`) reads "This is NOT a defer-loop (CH6-V1-R01 reconciled): the
verify_action names the concrete refuting sources, it does not punt to 'a future
cycle'."

**Ground-truth at HEAD `91b6893b0`.** The mechanical anchor the bounded refutation
rests on is live: `admits_collapsed_stage` at `ARCHITECTURE.md:1206` co-requires
`target.arch == x86 + target.avx512bw + Entry(_)` with the verbatim parenthetical
"aarch64 mechanically refused" — re-read this cycle. The one real NEON body
`byte_class_from_eq_set_64.rs` = 3705 bytes; the named-as-scalar-delegate siblings
ARE genuine line-3 passthroughs — `byte_class_from_table_64.rs:3` =
`crate::scalar::byte_class_from_table_64::byte_class_from_table_64_scalar(...)`,
`bitmap_prefix_xor_64.rs:3` = `crate::scalar::bitmap_prefix_xor_64::...`. The
grounding is real, not a citation gesture.

**Disposition: ACCEPT (R01 stays discharged).** The V2 fold survived the V3
regeneration intact, re-verified mechanically at ARCH:1206 + the three NEON source
files. No residual.

### CH6-V1-V01 (REVISE, V2-discharged) + CH6-V2-COUNT-NIT — onboarding predicates executed; count corrected → **STILL FOLDED + RE-VERIFIED**

**V1 concern.** The ONBOARD "test" was asserted but not executed. **V2 nit.** The
Predicate-2 prose recapped "8 grammar dirs" while the inline enumeration listed 9.

**V3 re-verification.** ONBOARD is reclassified as "a verify_action with a measured
HEAD baseline" (`2c:271`); both predicates re-run at HEAD with live results inline.
The V2 count-nit fold is recorded in the 2C frontmatter: "Leading numeral corrected
to 9 at the ONBOARD Predicate-2 body, the frontmatter recap, and the CH6
self-anchor. Ground-truth-exact."

**Ground-truth re-execution at HEAD `91b6893b0` (this cycle):**
- Predicate 1: `grep -rn 'JsonParser|CssL4Parser' crates/ir/src crates/simd-scan/src`
  = **7 hits** — matches the dossier's catalogued ARCH-3A-D09 string-ident-registry
  leak surface (`strategy.rs`), reported NON-ZERO + monotonic-decrease, not
  whitewashed clean. Exact.
- Predicate 2: `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d` =
  **9 grammar dirs** (bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json,
  math) **+ tape/**. The count is now 9 everywhere — the V2 off-by-one is corrected;
  all 9 carry `@generated`, zero hand-written, plus the neutral tape substrate.

**Disposition: ACCEPT (V01 + COUNT-NIT both stay discharged).** The predicate is a
measured HEAD fact; the recap count matches the live count (9). The honesty is
exemplary — Predicate 1 reported NON-ZERO and catalogued, not paper-closed clean.

### CH6-V1-V02 (REVISE, V2-discharged) — F9 path-(b) UNKNOWN sizing route → **STILL FOLDED + RE-VERIFIED**

**V1 concern.** F9's NON-ZERO/UNKNOWN path-(b) carried no concrete next measurement.

**V3 re-verification at `2f-fold-gaps.md:475-489` + LAC-2F-FOLD-05 (`2f:584`).** The
"path-(b) sizing route (CH6-V1-V02 fold — UNKNOWN → bounded measurable step)"
section is present: "size as the 0→N introduce-site delta — baseline
`grep -rcn StructLayout crates/`=960 (rename surface) vs `grep -rcn
'backend_shape\|LayoutFacts' crates/`=0 (side-table surface)."

**Ground-truth re-execution at HEAD `91b6893b0` (this cycle):**
- `grep -rn 'StructLayout' crates/` = **960** — exact match to the rename-surface
  baseline.
- `grep -rn 'backend_shape\|LayoutFacts' crates/` = **0** — exact match to the
  side-table-surface baseline (the NON-ZERO/UNKNOWN claim re-verified: the
  side-table carrier is genuinely absent in crates/, present only in the cited
  skinny template `skinny/crates/passes/src/lib.rs:90,:96,:385`).

**Disposition: ACCEPT (V02 stays discharged).** The floating UNKNOWN remains a
bounded 0→N introduce-site delta with both grep baselines re-verified live — no
future-pass defer.

### CH6-V1-V03 (REVISE, V2-discharged) — five D-folds echo LAC-1E-14 → **STILL FOLDED + RE-VERIFIED**

**V1 concern.** Five dossiers grounded the tape-as-substrate-not-6th-shape verdict
on the SAME LAC-1E-14 precedent (apparent 5-source agreement = 1×5).

**V3 re-verification.** Each D-fold carries the independent `admits_collapsed_stage`
x86-co-requirement mechanical anchor beyond the shared precedent — confirmed in the
2A frontmatter ("the independent admits_collapsed_stage ARCH:1206 mechanical anchor
is carried at FOLD-2A-D :205-213 … re-verified live at ARCH:1206"), 2C frontmatter
(CH6-V1-V03: "D-fold carries an independent corroborating anchor (ARCH:1206
CollapsedStage x86-only/aarch64-refused) beyond the shared LAC-1E-14 citation"), 2D
(FOLD-2D-07 `2d:240-249`), 2E (LAC-2E-SKV17-01 `2e:494`: "Corroborated by the
`admits_collapsed_stage` x86 co-requirement"), and 2F (`2f:84`: "`admits_collapsed_stage`
is x86-bound (ARCH:1151) so mechanically refuses on aarch64 — independent
corroboration of no-6th-shape beyond LAC-1E-14 (CH6-V1-V03)").

**Ground-truth at HEAD `91b6893b0`.** The mechanical anchor is a different KIND of
evidence than the precedent: ARCH:1206 (re-read this cycle) is a mechanical fact —
the only shape that would consume hand-written NASM is x86-pinned in the predicate,
so aarch64 cannot acquire a 6th shape. If LAC-1E-14 were mis-read, ARCH:1206
independently proves the aarch64 no-6th-shape verdict. The 5-fold agreement rests
on two orthogonal grounds per dossier. (The minor cross-dossier line-ref drift — 2f
cites ARCH:1151/:1282, others :1206; both resolve to the `admits_collapsed_stage`
x86-co-requirement region — remains a CH1-lane reconcile, not a CH6 concern.)

**Disposition: ACCEPT (V03 stays discharged).** Defence-in-depth preserved through
V3 regeneration; the verdict no longer rests on a single echoed precedent.

---

## NEW-DEFER RE-SCAN (V3 regeneration introduced no engineered-defer)

A cohort-wide scan for engineered-defer phrasing the V3 regeneration might have
introduced — two passes:

**Pass 1 (broad new-V3 patterns):** `we (will|leave|defer) | to be (handled|
addressed|resolved) (in|by) (a |the )?(future|later|next) | sized/priced/determined
in T-P3 | left for | punted | out of scope | not (yet )?(grounded|sized|measured)
(here|this pass)` across all six dossiers returned **zero hits** (RC=1).

**Pass 2 (residual defer-phrasing, filtering negatives/reconciled forms):** returned
**three hits, all legitimate** — identical to the V2 finding, no new ones:

1. `2b:381` — OQ-2b-04: "**A future wave** with scalar-ref (**present**) + a real
   NEON body + strict checkasm + same-wave consumer …; the lo6 table path is
   JSON-only (CSS uses eq-set), so the table NEON body has no CSS antecedent —
   likely stays scalar-delegate through SK-V18." This is a legitimate
   *implementation-defer*: the scalar-ref EXISTS today (verified live:
   `bitmap_prefix_xor_64.rs:3` + `byte_class_from_table_64.rs:3` are real scalar
   bodies under `crate::scalar::*`), the deferred item is the NEON *body*, and the
   bbnf-specific transfer reason is stated (JSON-only lo6 table, no CSS antecedent).
   The grounding (scalar-ref + close-state taxonomy) is present NOW; only the NEON
   coding is future. ACCEPT.

2. `2c:413` — "alphabet-as-data, compile-time `FieldSource` walk); no deferral to
   'a later research pass'." The *negative* form — an explicit non-defer affirmation.
   ACCEPT.

3. `2f:612` — "resolved on disk, not deferred to a later pass." The CH1-V5-001
   hygiene non-defer affirmation (enumerated-filename residual folded on disk).
   ACCEPT.

**T-P3 references (9/8/3/4/10/10 across the six dossiers)** were each spot-checked:
every one routes a *synthesis disposition* (3C) or *scope/convergence-target
naming* to T-P3 — the research→synthesis boundary PASS-2 §intro establishes — while
the *grounding* (the cited source, the measured baseline, the bounded refutation) is
discharged at THIS pass. None is a grounding-defer. The Open Research Questions in
all six dossiers (U-2A/2B/2C/2D/2E/2F-*) carry verify_actions that name a concrete
present artefact (a grep baseline, a named manual source, a skinny template
path:line, a mechanical predicate), not "a future research cycle."

No new engineered-defer. The V3 regeneration held the anti-paper-close discipline.

---

## ACCEPT (41) — anti-paper-close strengths re-confirmed at HEAD

Spot-confirmed at HEAD `91b6893b0` this cycle:

- **The eq-set fan is a real NEON body, table/prefix are honest scalar delegates.**
  Re-measured live: `byte_class_from_eq_set_64.rs` = 87 LOC, **8 distinct NEON
  intrinsics** (vaddv_u8, vandq_u8, vceqq_u8, vdupq_n_u8, vget_high_u8,
  vget_low_u8, vld1q_u8, vorrq_u8) across 23 intrinsic calls — the dossier's "8
  distinct NEON intrinsics" claim is EXACT (the "/27 calls" figure differs from my
  23-call count by a regex-pattern nuance — CH1-lane, not a CH6 concern; the
  load-bearing claim "this is a real NEON body, not a scalar delegate" holds
  unambiguously). The two named-as-scalar-delegate siblings ARE genuine line-3
  passthroughs to `crate::scalar::*` — no NEON-body claim is made where only a
  scalar delegate exists. The close-state taxonomy matches ground truth exactly.

- **L5/L6 net-new masks remain the anti-paper-close exemplar** (`2b`). Net-new,
  scalar-ref ABSENT, but naming the exact REQUIRED-NEW artefact paths NOW, the ISA
  idiom (`overflowing_add` carry NOT PMULL per REDRESS-88; scalar running-balance
  NOT CTZ per REDRESS-89), and the Lock-16 "scalar ref + checkasm BEFORE wiring"
  gate. Verified live: `ls src/scalar/comment* src/scalar/bracket*` = no matches
  (genuinely absent, honestly named). The admission cost is concrete and present;
  only the coding is SK-V18.

- **Every fold states a bbnf-specific transfer reason; no fold rests on "SOTA does
  it this way."** Each D-fold carries the grammar-neutral verdict + the divergence
  antecedent + the measured anchor. The materialization-plane fold carries the
  lightningcss-bar-is-UNMEASURED-PENDING honesty (`2a:382-386`, CH7-003 fold) — "a
  non-negotiable TARGET on the UNMEASURED-PENDING CSS surface (SPEC `:207`), NOT a
  property held at this pass" — rather than a fabricated CSS speedup. The
  obligation/target framing is itself an anti-paper-close correction held through V3.

- **Refutations are first-class and bounded, not deferrals.** The 6th-shape
  refutation (G-Omega gate + SPEC §9 + the ARCH:1206 mechanical anchor); the
  aarch64 CollapsedStage bounded-refutation (concrete sources named NOW,
  NEON-has-no-AVX-512-mask analogue); the FSM/frame macro
  `source-present-unwired`/`architectural-block-with-REDRESS` close-state (`2b:97`).
  Each is a bounded refutation with a named fence, not a punt. The asmjson/Sneller
  x86 rows are explicitly "diagnostic architecture-pressure ONLY," never a latent
  aarch64 candidate (`2d:78`, `2d:279`).

- **Open UNKNOWNs carry present verify_actions, not future-cycle punts.** The
  carried UNKNOWNs (tape SoA-vs-AoS closure, OnceCell classify-before-wiring,
  simd-scan narrowing, StructDirect cost-fold) each name a concrete present artefact
  to resolve against — a measured layout-cost decision under Lock 15, the all-8
  `scan_structural` carrier read, the `simd-scan/lib.rs` vs aarch64-mandate compare,
  the skinny `LayoutFacts` template. The "pending" markers (`2a:284`,`2a:384`) are
  the OnceCell-wiring-pending real-open and the UNMEASURED-PENDING CSS honesty — not
  grounding-defers.

- **The hygiene action CH1-V5-001 stays resolved on disk** (`2c`, `2e`, `2f:612` all
  carry the enumerated form + the non-defer affirmation; the residual brace-glob
  occurrences survive only inside hardening artefacts as quoted-defect records,
  correctly unaltered).

## Verdict

CH6 returns **100.0% ACCEPT, 0 REVISE, 0 REJECT** for V3 — the **second
consecutive ≥95%-ACCEPT cycle** (V2=100%, V3=100%). Every V1 disposition (the
load-bearing REJECT CH6-V1-R01 aarch64-esoterica grounding-defer-loop, and the
three REVISE CH6-V1-V01/V02/V03) and the V2 CH1-lane count-nit (CH6-V2-COUNT-NIT)
is **re-verified folded against the live tree at HEAD `91b6893b0` this cycle**, not
merely asserted folded: the onboarding greps re-run live (P1=7 in strategy.rs,
P2=9 grammars +tape, the recap count now 9 everywhere); the path-(b) sizing
baselines re-run live (StructLayout=960, backend_shape|LayoutFacts=0); the
`admits_collapsed_stage` x86 co-requirement re-read at ARCH:1206; the eq-set fan
re-measured (87 LOC, 8 distinct NEON intrinsics) with the table/prefix scalar
delegates confirmed as line-3 passthroughs; the L5/L6 scalar files confirmed
absent. Both defer-loop ends (2E U-2E-04 + 2D UNKNOWN-2D-05) remain reconciled; the
2E frontmatter count remains exercised-in-body (9).

The cohort-wide new-defer re-scan found **zero engineered-defer** the V3
regeneration introduced — the three residual defer-phrasing hits are an
implementation-defer with a *present* scalar-ref (2b OQ-2b-04), and two explicit
non-defer affirmations (2c, 2f). The V3 all-ACCEPT is the convergence-cycle
all-ACCEPT the §4 criterion ADMITS (every ACCEPT grounded on a ground-truth
re-execution), distinct from the V1-paper-close all-ACCEPT the §3 rule bars (V1
correctly carried the ≥30%-REVISE + the load-bearing REJECT).

CH6 advances on V3 with zero open critical defects and zero orphan REVISE. The two
standing CH1-lane line-ref nits (the 2f ARCH:1151/:1206 predicate line-ref drift;
the eq-set "/27 calls" vs my-23-call regex nuance) are surfaced for CH1 — neither
is a CH6 paper-close concern and neither gates the V3 verdict.
