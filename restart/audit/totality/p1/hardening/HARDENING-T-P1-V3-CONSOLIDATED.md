# T-P1 V3 Hardening Consolidation

Pass: T-P1 Excavation. Cycle: V3.
Date: 2026-05-21.
Scope: consolidation of `restart/audit/totality/p1/hardening/V3/CH1.md`
through `CH6.md` against the folded V3 excavation artifacts.

## Verdict

Disposition: REVISE.

V3 resolves the substantive V2 hardening findings. CH2, CH3, CH4, CH5,
and CH6 accept. CH1 accepts the targeted V2 correctness folds, but marks
the cycle REVISE because `1A-substrate-evidence.md` and
`1B-codegen-evidence.md` still identify themselves as `cycle: V2` inside
the V3 artifact set. No V3 lens issued REJECT.

## Lens Dispositions

| Lens | Disposition | Load-bearing result |
|---|---|---|
| CH1 correctness | REVISE | The command-output, Lock 14 citation, Lock 13 count, and hardening-as-primary-evidence fixes are accepted. The only blocking issue is cycle/provenance metadata in 1A and 1B. |
| CH2 generality / Lock 14 | ACCEPT | V3 preserves grammar-name versus grammar-shape distinctions, CSS/Sheets/BBNF-self implications, GrammarConfig partial status, and generated per-grammar allowance boundaries. |
| CH3 regression / REDRESS | ACCEPT | V3 preserves admitted SinkOnly and CSS evidence, keeps REDRESS-119/120 as history only, and retains the hard-pre-block versus fresh-differential-unblocked split without row demotion. |
| CH4 cost / hard-cap realism | ACCEPT | 1A-1E carry the planning schema inline, and the 1F files now explicitly declare their ID-keyed metadata tables as the authoritative CH4 carrier. |
| CH5 hidden coupling | ACCEPT | `StructuralIndex`, CSS source-sidecar comparator evidence, proof-witness root coupling, and Track 2 shared runtime substrate helpers are explicitly classified without creating retained parallel-substrate closure. |
| CH6 anti-paper-close | ACCEPT | UNKNOWN rows carry verify actions, JSON substrate claims are scoped, Lock 1 is narrowed to scoped JSON evidence, and future routing is not treated as closure. |

## Required V4 Fold

1. Relabel `1A-substrate-evidence.md` from `cycle: V2` to the active
   folded cycle and add fold provenance stating that V3 CH1 required no
   substantive 1A evidence changes beyond cycle identity.
2. Relabel `1B-codegen-evidence.md` from `cycle: V2` to the active folded
   cycle and add fold provenance stating that V3 CH1 required no
   substantive 1B evidence changes beyond cycle identity.
3. Keep the V3 accepted corrections unchanged. V4 is a metadata/provenance
   fold, not a route to alter accepted CH2-CH6 evidence.

## V4 Entry Rule

The V4 inventory set should identify a single active cycle consistently
across 1A through 1F. If a file receives only metadata/provenance changes,
that must be explicit so the next challenge can distinguish identity repair
from new substantive evidence.
