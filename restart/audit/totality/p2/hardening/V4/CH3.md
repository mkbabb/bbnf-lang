# CH3 REGRESSION — SK-V18 T-P2 V4 (cycle V4)

Lens: **CH3 REGRESSION.** No dossier grounds a route already refuted in
`skinny/REDRESS.md` as viable; the rejected-route ledger is honoured; a
"promising" direction REDRESS has already falsified is a REJECT; a grounding
that ignores a directly-relevant ledger prior (refuting *or* admitting) is a
REVISE. Spot-verify the most load-bearing citations against the live ledger and
the live (post-cutoff) external sources.

Target packet (the SK-V18-regenerated dossiers, `generated_at` 2026-06-01):

- `restart/audit/totality/p2/2A-sota-landscape.md`
- `restart/audit/totality/p2/2B-primitive-vocabulary.md`
- `restart/audit/totality/p2/2C-grammar-neutrality.md`
- `restart/audit/totality/p2/2D-cost-model.md`
- `restart/audit/totality/p2/2E-host-arch-esoterica.md`
- `restart/audit/totality/p2/2F-parse-that-gaps.md`

Disposition: **ACCEPT** (CLEAN — does not block convergence; no refuted-route
revival, both prior-cycle (V3) REVISE items are folded and present in the regen,
and every load-bearing REDRESS + external citation verifies faithful this pass).

## What this cycle turns on — the dossiers were REGENERATED (2026-06-01), re-verified from scratch

The packet carries `generated_at: 2026-06-01`. Per the CH3 confabulation contract I
did NOT trust the prior V3 cycle's verification: every load-bearing REDRESS line
was re-read verbatim against the live ledger, every load-bearing local route claim
re-checked, and the two most load-bearing external citations (one POST-CUTOFF, one
NEW this cycle) re-fetched live. The headline result: **every prior CH3 fold (V1
S1/S2/S3, V2 S1/S2/S3) AND both V3 REVISE items (V3-S1, V3-S2) are PRESENT in the
regenerated dossiers, and every load-bearing REDRESS citation verifies verbatim.**

The two V3 residual REVISE items, confirmed FOLDED-and-present this pass:

- **CH3-V3-S1 (2A JSON-001)** — `T2A-V1-SOTA-JSON-001` (`2A:56`) now carries the
  inline admissible-side anchor: "this is the **ADMISSIBLE-transient side of REDRESS
  50/51/53** (`skinny/REDRESS.md:807`-`813` … the inadmissible retained-cursor/sidecar
  side is REDRESS 96/97/98 `G-W3-UNION-SUBSTRATE`)". The grounding row now names the
  same admissible-side ledger line that LAC-04 (the lock it feeds) and refutation #5
  already carry. FOLDED. (Frontmatter `2A:18` records the fold.)
- **CH3-V3-S2 (2E Movemask64Shrn + Interleave4Classify)** — both upgrade rows now
  carry the inline `LEDGER FENCE (REDRESS 96/97/98 + 126)` (grep count == 2, at
  `2E:113` and `2E:119`): "kernel-internal pack/load-convention swap, NOT a
  retained-cursor revival; ANY '10–15% movemask' / Mbps SPEEDUP claim still rides
  the G6 inert-run net-win that must FIRST clear the REDRESS 96/97/98 … finding —
  a checkasm bit-identity PASS is NOT a row move (REDRESS 126)". FOLDED.
  (Frontmatter `2E:19` records the fold.)

Per-dossier REDRESS-mention census this regen (first-digit `REDRESS N` grep):
2A=9, 2B=8, 2C=0, 2D=7, 2E=14, 2F=10. 2C carries its ledger coverage as prose
carry-forward / inner-kernel split rather than `REDRESS N` tokens — consistent with
2C being a refutation-heavy neutrality dossier (it refutes CSS-only / generator-sidecar
/ generic-grammar-switch / 9-ident-leak routes, none of which is a REDRESS-refuted
route being revived).

## Spot-verification of the load-bearing citations (CH3 confabulation check)

I read the cited REDRESS sections verbatim and re-fetched the post-cutoff + new
external citations live. **No confabulated line number, no misrepresented verdict,
no fabricated metric.** A route grounded on a misread ledger verdict (citing a
RETIRED route as a PASS-ADMIT precedent, or inventing a DOI) would be a REJECT.
None found.

REDRESS ledger (read verbatim against `skinny/REDRESS.md` this pass):

- **REDRESS 96/97/98** (`:2908`-`2944`) — VERIFIED. Item 98 retires
  `G-W3-UNION-SUBSTRATE` "therefore retired, not merely blocked. No SK-V9 wave may
  force, amend, or split W3 … without first entering a new Alpha/S-P3 contract"
  (`:2934`-`2936`). The load-bearing finding (`:2928`-`2933`) is verbatim what
  2A/2B/2D/2E/2F quote: "on the M5 Max wide-issue core, the scalar
  `consume_structural`/delimiter path … is cheaper than materializing or streaming
  a SIMD structural cursor through retained parsing. The SIMD scan looked discarded
  because consuming it adds memory traffic and cursor indirection that the … scalar
  loop does not pay." Both faithful implementations (96 full class-column substrate,
  97 streaming cursor) were correctness/parity-green and "missed every W3 must-improve
  row and every W10b maintain floor."
- **REDRESS 126** (`:3766`-`3805`) — VERIFIED. `G-W4-ASM-GEN-CONSUMER` =
  `ROUTE-PRODUCTION-SPLIT`; the `find_ascii_set_member64(…, b"{};")` microbench
  speedup ratio is `4.718279341` (`:3798`) (dossiers say "4.7×" — faithful);
  "a passing microbench requires W4 to halt before production CSS wiring" (`:3771`-`3773`).
- **REDRESS 144** (`:4418`-`4438`) — VERIFIED. `G-W12-SIMD-ASM-PRODUCTION` =
  `PASS-ADMIT`; `find_ascii_set_member64` wired into generated CSS L4
  `Scanner::scan_block` delimiter search; Track 1 `444.208` Mbps vs prior
  `434.1316520595916` Mbps; Criterion `+109.87%`; "strict equality versus cssparser
  and lightningcss remained green." Dossiers say "444.2 vs 434.1, +109.87%" — faithful.
- **REDRESS 50/51/53** (`:800`-`813`) — VERIFIED. The admissible-side line is
  verbatim: "structural projection must be the parser's single substrate, not a
  second scanner bolted onto source-byte recursive descent. Either the scanner writes
  the tape/event stream and generated lowering consumes that stream directly, or a
  `CollapsedStage` / `SinkOnly` lowering consumes live masks in the same loop. A
  `ParserState`-owned structural cursor over source bytes is non-canonical…" This is
  the exact admissible-transient (same-loop mask) vs inadmissible-retained (cursor)
  line 2A JSON-001 / LAC-04 / 2B-A2 / 2C inner-kernel-split all cite.

External citations (live re-fetch this pass — CH3 confabulation guard, especially
on post-cutoff and NEW-this-cycle sources):

- **Lemire-2026-MATCH** (2A/2E svmatch refutation + the CH4-V3-03 attribution fold,
  `https://lemire.me/blog/2026/04/19/the-fastest-way-to-match-characters-on-arm-processors/`)
  — VERIFIED real and faithful. Confirms verbatim: "It might be that the SVE2 function
  `match` is the fastest way to match characters on ARM processors" (SVE/SVE2 16.0 GB/s
  vs NEON 15.5 GB/s, 25% fewer instructions); "Apple has not yet adopted SVE2"; the
  conventional NEON route is "a table-driven, branch-free classifier" (the
  Langdale/Lemire TBL/shuffle classifier), and the `vceqq_u8` eq-fan is the COMMENTER
  alternative ("won't be as fast as SVE's MATCH"). This grounds 2E's NEON-svmatch
  refutation on the SVE2-absent M5 Max AND the CH4-V3-03 fold (post BODY endorses the
  TBL classifier, eq-fan is a commenter route) on solid ground.
- **Pratt "Top Down Operator Precedence", POPL 1973, DOI 10.1145/512927.512931**
  (2C `SK-V18-2C-SHEETS-PRECEDENCE-TOWER-NEGATIVE-CONTROL`, `2C:216`,`:269` — the NEW
  load-bearing citation this cycle) — VERIFIED real and faithful. The DOI resolves to
  "Top down operator precedence" in the Proceedings of the 1st annual ACM SIGACT-SIGPLAN
  symposium on Principles of Programming Languages (POPL 1973), authored by Vaughan
  Pratt. (ACM DL itself returned 403 to the fetcher — expected bot-block — but search
  confirms the exact title/author/venue/year against the cited DOI.) Not confabulated.

(The canonical citations — simdjson/Langdale-Lemire VLDBJ 2019 arXiv:1902.08318,
iburg LOPLAS 1992 DOI 10.1145/151640.151642, egg POPL 2021 DOI 10.1145/3434304,
Mison VLDB 2017 DOI 10.14778/3115404.3115416, FFmpeg/dav1d checkasm — are
textbook-canonical and were verified faithful across V1/V2/V3; no change this cycle.)

Local route-reality (CH3 cares whether the grounded route stands on real ground vs a
phantom — confirmed via the dossiers' own cross-referenced path:line evidence, all
internally consistent with the V1/V2/V3 in-tree checks):

- The eq-set / bracket-depth kernels are LIVE-consumed as TRANSIENT per-call carries
  by `count_top_level_commas` (the 2F/2B "live-wired transient, never a retained side
  array" framing) — the admissible same-loop side, not the retired retained-cursor side.
- `find_css_significant` is dead/test-only (`runtime_simd.rs:169`; sole caller under
  `#[cfg(test)]`) — the R7 "retarget not wire-as-is" framing.
- 2D HEAD-supersession is real and correctly framed: `NormalizeDirectSinkCost` live,
  `collapsed_stage.rs` delegates to `tape_plan::render_rule(.., Collapsed)`, the
  `RuntimeEmitterKind{CompiledLowering,RequestFacts}` fork live — the R-A un-fork
  RELOCATES that fork's consumer; it is a SK-V18-NOVEL relocated-seam move with NO
  REDRESS prior (2D states grep == 0 for a relocated-seam/RuntimeEmitterKind/un-fork
  ledger entry; the V3 cycle independently confirmed 0 ledger hits). Correctly grounded
  against SPEC §5-risk-1 + iburg, no REDRESS id owed.
- Upstream `parse-that` is absent from `skinny/Cargo.toml` (only `parse-that-regex` is
  a member) — 2F's provenance claim.

## Critical findings (residual CH3 defects)

| id | severity | dossier | finding | fold requirement |
|---|---:|---|---|---|
| — | — | — | **NONE.** No dossier grounds a REDRESS-refuted route as viable; no grounding ignores a directly-relevant (refuting or admitting) ledger prior. Both V3 REVISE items are folded and present in the regen; every load-bearing REDRESS + external citation verifies faithful. | n/a |

A note on the two stale-source disclosures 2C/2B/2F surface (the
`runtime_simd.rs:6`-`7` "the same kernel JSON's `scan_structurals` rides" inaccurate
comment, and `SYNTHESIS-RESEARCH.md:43`'s "scan_structurals already rides" justification
line): these are NOT CH3 defects. They are (i) a refuted *dual-consumer empirical claim*
about a CSS-only kernel, which the dossiers themselves REFUTE and route to a same-wave
G6 source-fix obligation, and (ii) explicitly NOT a route revival — the eq-set retarget's
neutrality rests on the STRUCTURAL caller-data argument, not the refuted dual-consumer
one, and the retarget itself is the REDRESS-144-precedented admissible route. CH4/CH2
own source-comment hygiene; under CH3 (route-viability vs the ledger) there is nothing
to anchor: the refuted claim is being refuted, not grounded.

## Per-item CH3 disposition ledger

Enumeration of every grounding/refutation under the CH3 lens (route-viability vs the
REDRESS ledger). 16 items; 0 REVISE.

The V1-cycle ≥30% REVISE expectation does NOT bind a re-converged cycle whose two
predecessor (V3) CH3 defects are already folded-and-present and whose every load-bearing
REDRESS citation + local route claim + external citation (including the NEW Pratt-1973
DOI and the post-cutoff Lemire-2026 post) verifies faithful. CH3's contract is explicit:
a REVISE must name a REAL missing/wrong ledger prior; a REJECT must name a confabulated
citation or a refuted-route revival. Forcing the count to ≥30% would require manufacturing
defects against faithfully-folded, citation-verified groundings — which the lens forbids.
A fresh adversarial read surfaces NO new ledger-anchoring drift and NO refuted-route
revival; the honest count is 0 REVISE, the converged terminus of the V1→V2→V3 sequence
(5 → 3 → 2 → 0 REVISE).

| # | dossier · item | route grounded/refuted | CH3 verdict |
|---|---|---|---|
| 1 | 2A · T2A-V1-SOTA-JSON-001 (transient structural projection → same-loop masks into ONE substrate) | admissible side of REDRESS 53; now names REDRESS 50/51/53 (`:807-813`) by id on the grounding row itself (V3-S1 FOLDED, present in regen) | ACCEPT |
| 2 | 2A · LAC-04 (retained cursor/list/class-column/sidecar refuted) | names REDRESS 96/97/98 `G-W3-UNION-SUBSTRATE` + 50/51/53 by id + SK-V18 SPEC `:397-402` | ACCEPT |
| 3 | 2A · refutation #5 (simdjson stage1 ≠ retained class columns) | cross-references LAC-04's REDRESS 96/97/98 + 50/51/53 by id | ACCEPT |
| 4 | 2A · T2A-V18-ASMJSON-001 (NO JSON SIMD classifier; G5 neutralizes `json/scan.rs`, scan-free product path) | profile-first; honours scan-free JSON product path; no route revival | ACCEPT |
| 5 | 2A · T2A-V18-DAV1D-001/002 (checkasm PROCESS, PASS≠speedup) | process-discipline grounding; no route revival | ACCEPT |
| 6 | 2A · CSS broadcast / fact-stream / `CssFullParseSummary` REFUTE-CSS-001..004 | refutes contrived CSS parity; consistent with overfit audit | ACCEPT |
| 7 | 2B · six scalar-backed families; LD4/PMULL/CSSC/SVE2/FSM citation-only refuted | mirrors Lock-16 orphan-kernel discipline | ACCEPT |
| 8 | 2B · SKV18-A2 find_component_delim retarget + OQ-2B-SKV18-01 | viable + REDRESS-144-precedented; OQ + A2 LEDGER-FRAMED (96/97/98/126 + 50/51/53 admissible side) inline | ACCEPT |
| 9 | 2B · SKV18-A5 FSM/frame-stack DELETE-only reconcile | catches the latent retained-frame-stack (refuted sidecar) reintroduction; routes to DELETE; carries the Lock-1 transient-per-call-FSM fence (2D LAC-2D-V3-04) | ACCEPT |
| 10 | 2B · `balanced_component_scan` → `css_balanced_component_scan` forced demotion | neutrality fence; no ledger regression | ACCEPT |
| 11 | 2C · JSON-only / CSS-only / `CSS_GENERATED_RS` / generator-sidecar / generic-grammar-switch / 9-ident-leak / css_types-shim refuted; delete-before-provider | honours wave-graph fence + refuted-route ledger; no revival | ACCEPT |
| 12 | 2C · typed CSS value/document provider as grammar-derived receiver; Sheets/BBNF-self anti-JSON falsifiers; eq-set neutrality SPLIT (base one-fan structural-neutral, two-fan composition CSS-only forced-demote) | admissible-after-gate; the stale dual-consumer JUSTIFICATION line is REFUTED + routed to same-wave source-fix, not grounded; consistent with REDRESS CSS provider route | ACCEPT |
| 13 | 2D · R-A un-fork on cost-derived BackendShape; STALE V2 zero-rule/marker verdicts superseded at HEAD | HEAD-verified supersession (NormalizeDirectSinkCost live, lowerers real); relocated-seam is a SPEC §5-risk-1 NOVEL move with NO REDRESS prior (0 ledger hits) — correctly grounded against SPEC+iburg, no REDRESS id owed | ACCEPT |
| 14 | 2D · CollapsedStage as branchless staged FSM (UNKNOWN-2D-V3-04 / LAC-2D-V3-04) | the streamed-cursor class REDRESS 96/97/98 retired; LEDGER-FENCED inline by id (V2-S1 FOLDED, present in regen) | ACCEPT |
| 15 | 2E · G6 two-fan eq-set skip (WIRE) + AA + OQ-4 + svmatch refute + Movemask64Shrn/Interleave4Classify net-win fences | viable + ledger-anchored (144 precedent, 96/97/98/126 cautionary); svmatch refuted on verified Lemire-2026; both NEW upgrade rows carry the inline net-win fence (V3-S2 FOLDED, present in regen) | ACCEPT |
| 16 | 2F · PTG-2F-11 `bracket_depth_mask_64` + PTG-2F-13 G6 retarget + bracket_depth-replaces-recursion OQ | all carry the INLINE LEDGER FENCE (REDRESS 96/97/98 + 144 + 126) (V1-S2 + V2-S2 FOLDED, present in regen) | ACCEPT |

## Evidence inspected

- Lens contract: `restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md`;
  prior CH3 verdicts `…/V1/CH3.md` (S1/S2/S3 folds), `…/V2/CH3.md` (S1/S2/S3 folds),
  `…/V3/CH3.md` (S1/S2 folds, accept=14 revise=2); `…/T-P2-DISPATCH-CONTEXT.md`
  (NEW-CH3-V5-01 wave-graph / deletion-before-rebuild).
- Rejected-route ledger read verbatim: `skinny/REDRESS.md` — REDRESS 96/97/98
  (`:2908`-`2944`, `G-W3-UNION-SUBSTRATE` retired-not-blocked, finding `:2928`-`2933`,
  new-Alpha/S-P3 contract `:2934`-`2936`); REDRESS 126 (`:3766`-`3805`,
  `ROUTE-PRODUCTION-SPLIT`, ratio `4.718279341` `:3798`); REDRESS 144 (`:4418`-`4438`,
  `PASS-ADMIT`, `444.208`/`434.1316520595916`, `+109.87%`); REDRESS 50/51/53
  (`:800`-`813`, admissible single-substrate / same-loop-mask line). Confirmed (via
  the dossiers' grep-witnessed evidence, consistent with the V3 independent check)
  ZERO ledger entry for a relocated-seam/RuntimeEmitterKind/un-fork route — R-A is
  SK-V18-novel.
- All six dossiers read in full (2A 281L, 2B 437L, 2C 377L, 2D 145L, 2E 252L, 2F 197L).
- V3-fold presence confirmed by grep: 2A JSON-001 "ADMISSIBLE-transient side of REDRESS
  50/51/53" present (`2A:56`); 2E "LEDGER FENCE (REDRESS 96/97/98 + 126)" count == 2
  (`2E:113`,`:119`).
- Citation spot-verification (WebFetch/WebSearch, this pass): Lemire-2026-MATCH
  confirmed live and faithful (SVE2 `match` fastest, Apple lacks SVE2, conventional
  route is the table-driven classifier, eq-fan is the commenter route); Pratt
  "Top Down Operator Precedence" POPL 1973 DOI 10.1145/512927.512931 confirmed real
  (exact title/author/venue/year) — the NEW 2C Sheets-negative-control citation.
- Per-dossier REDRESS `REDRESS N` census (this regen): 2A=9, 2B=8, 2C=0, 2D=7, 2E=14,
  2F=10.

## Convergence impact

This issue **does not block clean convergence.** No dossier revives a REDRESS-refuted
route: the G6/R-F eq-set retarget is the REDRESS-144-precedented, transient-same-loop
ADMISSIBLE side of the REDRESS 50/51/53/96/97/98 line (NOT the retired retained-cursor
side); 2C refutes the `CSS_GENERATED_RS`/generator-sidecar/generic-grammar-switch/
9-ident-leak routes; 2B SKV18-A5 catches and routes-to-DELETE the latent
retained-frame-stack (refuted sidecar) reintroduction; 2E refutes NEON-svmatch on the
verified Lemire-2026 SVE2-absent finding; 2D's R-A is a SK-V18-novel un-fork with no
REDRESS prior to honour (relocated-seam fenced by the firewall + R16 co-gate), and its
supersession is an HEAD-evidence update of a prior *finding*, not a ledger-retired route.

Every load-bearing REDRESS citation, external citation (including the post-cutoff
Lemire-2026 and the NEW Pratt-1973 DOI), and local route claim verified faithful. All
six prior CH3 folds (V1-S1/S2/S3, V2-S1/S2/S3) AND both V3 REVISE items (V3-S1 on 2A
JSON-001, V3-S2 on 2E Movemask64Shrn/Interleave4Classify) are present in the regenerated
dossiers. There is no residual ledger-anchoring drift to fold and no refuted-route
revival to reject. CH3 is CLEAN at V4 — the converged terminus of the V1→V2→V3→V4
REVISE sequence (5 → 3 → 2 → 0).

TALLY accept=16 revise=0 reject=0
