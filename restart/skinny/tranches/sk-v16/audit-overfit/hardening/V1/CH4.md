# SK-V16 S-P0 V1 CH4 - Cost

Disposition: REVISE-FOLDED -> ACCEPT.

Initial CH4 rejected because the prune list did not carry enough S-P3 budget and
split shape. The fold added:

- a CH4 cost contract requiring owner paths, manual source/test LOC budget,
  generated-output status, docs/ledger LOC budget, phase hard cap, split trigger,
  same-commit consumer or executable gate, split-prone flag, and over-cap
  disposition;
- P0-1 split rows for CSS provider, JSON templates, Lock 14 scan expansion, and
  `xtask` ownership;
- P0-3 split rows for CSS generated drift and `generated_real_typed.rs`;
- P0-4 split rows for template design, `css_l4` source binding, and 67-file
  census/round-trip gate;
- A3/A4/A6 split-prone notes.

After fold, CH4 returned ACCEPT.
