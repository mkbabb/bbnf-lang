# T-P3 V3 CH3 Regression / REDRESS Review

Pass: T-P3 Totality Synthesis.
Cycle: V3.
Lens: CH3 regression / REDRESS.
Agent: CH3.
Date: 2026-05-21.
Ownership: `restart/audit/totality/p3/hardening/V3/CH3.md` only.

## Verdict

ACCEPT.

V3 preserves the V2 CH3 acceptance surface. The V2 consolidated verdict says the
only required V3 fold was CH1 source-map hygiene, while CH3 had already accepted
the REDRESS, JSON row reopen, union, SIMD, and CSS parity routing
(`restart/audit/totality/p3/hardening/HARDENING-T-P3-V2-CONSOLIDATED.md:23`-`30`,
`restart/audit/totality/p3/hardening/HARDENING-T-P3-V2-CONSOLIDATED.md:32`-`44`).
The V3 artifacts keep that boundary: they repair stale-cycle/source citations but
do not make `skinny/RESULTS.md`, `skinny/REDRESS.md`, source, generated runtime,
or SK-V13 W0 dispatch authoritative before G3/G-Omega.

## Findings

| ID | Disposition | Finding | Evidence |
|---|---|---|---|
| CH3-P3-V3-001 | ACCEPT | V3 remains proposal-only and does not grant write authority to source, `RESULTS`, or `REDRESS`. | The proposed locks diff states T-P3 must not edit `restart/locks/LOCKS.md` and that Pass Omega CRUD applies ratified edits only after G3/G-Omega (`restart/audit/totality/p3/3C-locks-v+1-diff.md:12`-`16`). Its governance footer blocks implementation waves from using proposed v+1 text to edit source, write `RESULTS/REDRESS`, add surfaces, or dispatch SK-V13 W0 before G-Omega (`restart/audit/totality/p3/3C-locks-v+1-diff.md:403`-`414`). The handoff delta keeps `skinny/RESULTS.md` and `skinny/REDRESS.md` unchanged except by authorized pass owners (`restart/audit/totality/p3/3F-migration-handoff.md:116`-`126`). |
| CH3-P3-V3-002 | ACCEPT | Row-plane truth is preserved: parse-only, direct, typed, and CSS fact-stream rows remain distinct gates, with no silent demotion or one-row CSS close. | 3A proposes explicit row-plane admission rules and says parse-only concessions are revoked while the single CSS row is evidence, not full grammar closure (`restart/audit/totality/p3/3A-architecture-synthesis.md:40`, `restart/audit/totality/p3/3A-architecture-synthesis.md:55`). 3C Lock 8 requires separate JSON parse/direct/typed gates, same-plane comparators, non-JSON BENCH/companion feed, and no prior A/GO silent demotion (`restart/audit/totality/p3/3C-locks-v+1-diff.md:142`-`175`). 3D keeps parse/direct NO-GO rows and the SK-V12 CSS row as separate evidence surfaces (`restart/audit/totality/p3/3D-skinny-fold.md:70`-`76`). |
| CH3-P3-V3-003 | ACCEPT | V3 does not revive REDRESS-119/120 or stale direct fixpoints as close authority. | MASTER H.W4 remains pending because SinkOnly/direct rows are partial and the full strict matrix is open (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:90`). H.W6 and J.W1 remain pending on full CSS parity and all 51 JSON rows (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:93`, `restart/audit/totality/p3/3B-master-plan-reconciliation.md:101`). MP.NW5 requires a same-run strict sonic matrix and per-row admission or architectural block, not report-only movement (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:116`, `restart/audit/totality/p3/3B-master-plan-reconciliation.md:157`). |
| CH3-P3-V3-004 | ACCEPT | Union-substrate REDRESS history is retained as a material-differential gate, not replayed as a route. | Lock 1 names REDRESS 96/97/98 as binding substrate-ceiling history and blocks full class-column, streaming-cursor, class-lane, sidecar, and `UnionTape` replays without fresh material differential, proof, same-wave consumer, strict row gate, rollback, and abrogate threshold (`restart/audit/totality/p3/3C-locks-v+1-diff.md:95`-`101`). 3D routes rejected union history through a replay filter rather than a ban on fresh evidence (`restart/audit/totality/p3/3D-skinny-fold.md:74`, `restart/audit/totality/p3/3D-skinny-fold.md:98`). 3F keeps future union attempts gated by material differential, changed data movement, consumer, and row gate (`restart/audit/totality/p3/3F-migration-handoff.md:197`). |
| CH3-P3-V3-005 | ACCEPT | SIMD/ASM and source-present primitive routes remain non-admitting unless they wire, delete, scalar-delegate, or architecturally block with REDRESS evidence. | Lock 16 keeps `escape_mask_64` prerequisite-only until a JSON/CSS consumer wires it and moves or rejects a named row (`restart/audit/totality/p3/3C-locks-v+1-diff.md:355`-`362`). It requires every source-present primitive to end as `wired`, `deleted`, `scalar-delegate-non-ASM`, or `architectural-block-with-REDRESS`, with `inventory_demoted_with_evidence` historical only (`restart/audit/totality/p3/3C-locks-v+1-diff.md:364`-`371`). PMULL/CSSC, union, ASM-gen, cache-hint, parse-that, and hardware facade routes still require material differential and same-wave production consumer or measured deletion/rejection (`restart/audit/totality/p3/3C-locks-v+1-diff.md:387`-`393`). |
| CH3-P3-V3-006 | ACCEPT | Non-JSON CSS evidence remains strict and fenced: admitted fact streams are output planes, not hidden substrates or full parity. | 3E states the SK-V12 CSS declaration-values row is positive proof but not full CSS parity (`restart/audit/totality/p3/3E-grammar-generalisation.md:23`-`28`). It classifies fact streams as output planes, not retained sidecars or a sixth `BackendShape` (`restart/audit/totality/p3/3E-grammar-generalisation.md:127`-`129`, `restart/audit/totality/p3/3E-grammar-generalisation.md:140`). 3C requires non-JSON telemetry to feed BENCH Section 8 or a dedicated companion gate and forbids the legacy JSON renderer alone from admitting appended non-JSON rows (`restart/audit/totality/p3/3C-locks-v+1-diff.md:160`-`168`). |
| CH3-P3-V3-007 | ACCEPT | Regression-prone routing is gated before implementation: G3, Pass Omega, CRUD, G-Omega, and SK-V13 W0 conditions are measurable. | 3F requires Pass Omega to consume latest totality packets, skinny `RESULTS.md`, skinny `REDRESS.md`, current V1 surfaces, and SK-V13 evidence before CRUD (`restart/audit/totality/p3/3F-migration-handoff.md:135`-`145`). It then requires user G-Omega before authoritative V1 amendment merge, and allows SK-V13 W0 only after G-Omega plus S-P3 convergence (`restart/audit/totality/p3/3F-migration-handoff.md:147`-`151`). The measurable dispatch checklist keeps SK-V13 W0 blocked unless G-Omega is closed, S-P3 converged, and W0 SPEC/DISPATCH exists (`restart/audit/totality/p3/3F-migration-handoff.md:153`-`161`). |

## Required Revisions

None for CH3.

## Carry-Forward Notes

- The section titles that still say "V2 Cost..." identify the V2 repair
  ledgers carried into V3; they are not a CH3 regression because the V3
  frontmatter, summaries, and gates preserve authority and row-plane boundaries.
  CH1 may still choose to normalize those labels as source-map hygiene.
- Preserve the V2 CH3 carry-forward requirement that REDRESS-119/120
  row-specific differential language remain adjacent to any future 51-row JSON
  matrix (`restart/audit/totality/p3/hardening/V2/CH3.md:37`-`44`).

## Evidence Checked

- V3 T-P3 artifacts: `3A-architecture-synthesis.md`,
  `3B-master-plan-reconciliation.md`, `3C-locks-crystallisation.md`,
  `3C-locks-v+1-diff.md`, `3D-skinny-fold.md`,
  `3E-grammar-generalisation.md`, and `3F-migration-handoff.md`.
- V2 consolidated hardening and V2 CH3:
  `HARDENING-T-P3-V2-CONSOLIDATED.md` and `V2/CH3.md`.
- Current authority references inside the packet: `skinny/RESULTS.md`,
  `skinny/REDRESS.md`, REDRESS 96/97/98, REDRESS 119/120, REDRESS 122,
  REDRESS 126, REDRESS 127, and SK-V13 G-Omega/S-P3 gates.
