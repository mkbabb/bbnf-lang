# SK-V15 S-P3 V1 Hardening Consolidated

Pass: S-P3 Synthesis-Plan hardening.
Cycle: V1.
Date: 2026-05-28.
Input packet: `4fe37c042` plus current V1 hardening worker outputs.
Output: this consolidated disposition.

## Verdict

Overall disposition: REVISE.

All seven lenses returned REVISE. The V1 packet has the right SK-V15
direction: prune CSS contrivances, restore Lock 14/16 gates, abrogate
codegen leaks, rebuild CSS typed output, activate the Decision Engine,
quarantine FNV, and preserve JSON 51/51. It is not dispatchable because
the executable SPEC/DISPATCH surfaces do not yet carry the complete
hardening folds. The next S-P3 version must be a real V2 fold, not a
consolidated waiver.

## Lens Dispositions

| Lens | Disposition | Source | Blocking fold |
|---|---|---|---|
| CH1 CORRECTNESS | REVISE | `V1/CH1.md` | Rebuild P3-C and final SPEC/DISPATCH gates around one wave graph; bind every wave to `SK-V15-open` rows and remove W8R CSS broadcast numbers from live typed-admission floors. |
| CH2 GENERALITY | REVISE | `V1/CH2.md` | Add executable SPEC/DISPATCH generality gate: every generic-crate edit needs non-JSON proof through CSS L4, Sheets, or BBNF-self. |
| CH3 REGRESSION | REVISE | `V1/CH3.md` | Promote the dependency table and a normalized REDRESS pre-block list into final dispatch surfaces. |
| CH4 COST | REVISE | `V1/CH4.md` | Add per-wave risk/LOC budgets and split old W5 and W7 to fit the 30 minute redress cap. |
| CH5 HIDDEN COUPLING | REVISE | `V1/CH5.md` | Carry the full forbidden sidecar/substrate set into executable SPEC/DISPATCH language and counter-bind EventTape. |
| CH6 ANTI-PAPER-CLOSE | REVISE | `V1/CH6.md` | Fold P3-C to the final graph, bind W6/W7 to exact executable consumers, and make W11/SK-V16 routing non-evidentiary. |
| CH7 OVERFIT-PRUNE | REVISE | `V1/CH7.md` | Normalize telemetry field names, promote gate-exclusion reporting, remove CSS broadcast floors, and counter-bind EventTape. |

## Canonical V2 Fold Shape

V2 must reindex from W0-W9 to W0-W11. This uses the two spare waves under
the 12-wave ceiling and absorbs the CH4 hard-cap findings without adding a
new candidate family.

| Final wave | Receiver | Required V2 fold |
|---|---|---|
| W0 | Baseline and telemetry lock | Use exactly the P3-D/SPEC SK-V15 telemetry fields; alias-only fields reject unless a schema bump is gate-consumed. |
| W1 | CSS admission honesty | Demote/collapse CSS W8R broadcast evidence; W8R values are diagnostic negative fixtures only. |
| W2 | Lock 14 / Lock 16 gate restoration | Gate reports included roots, excluded roots, exclusion reasons, and scan of its own exclusion list. |
| W3 | Codegen leak abrogation | One coherent generic leak family per wave; non-JSON receiver proof required for generic edits. |
| W4 | Pattern H generated discipline | True line-1 generator provenance plus non-writing regen/check proof before destructive root runtime deletion. |
| W5 | CSS typed Value provider | Build typed CSS value/document/view/visitor provider and gate-consumed diagnostic proof; no old-proof retirement unless W6-grade proof also lands. |
| W6 | CSS same-workload retime and old-proof retirement | Fresh same-run cssparser typed-output comparison, old CSS proof retirement, RESULTS/REDRESS/gate updates, and JSON guard maintain. |
| W7 | Decision Engine spine | At least one e-graph rewrite, non-tautological CSP, grammar-neutral facts, and exact same-wave generated-selection consumer. |
| W8 | Lowerers A | Fixture harness plus EagerTape and OffsetTape lowerers or gate-consumed rejected alternatives. |
| W9 | Lowerers B | EventTape, SinkOnly, CollapsedStage, and all-five gate; EventTape is an existing BackendShape lowering only, never a sidecar event stream or sixth shape. |
| W10 | FNV quarantine | Bench-only quarantine plus adversarial strict-product fixtures; no production selector or correctness arbiter. |
| W11 | Close reconciliation and PASS-IMPL V2 handoff | Close only on accepted axes or row-level intrinsic-block proof. SK-V16 routing is routed remainder after proof, not close evidence. |

## Required V2 Fold Roster

1. Rebuild P3-B, P3-C, P3-F, SPEC, and DISPATCH around the final W0-W11
   integer wave graph. Remove stale "P3-B does not exist" language and all
   W0-W9 references that now point at the wrong receiver.
2. Add per-wave cost columns to P3-B and SPEC Section 2: risk class, manual
   source/test LOC budget, generated LOC status, docs/ledger LOC budget,
   phase caps, split trigger, and same-wave consumer. Mirror the budget in
   DISPATCH envelopes.
3. Add SPEC Section 2.1 generality / Lock 14 gate and DISPATCH mirror
   language. Every generic-crate edit must name forbidden grammar tokens,
   non-JSON receiver proof, proof command, generated-output/no-diff
   expectation, and fail action.
4. Promote the NEW-CH3-V5-01 dependency table into SPEC and DISPATCH with
   the required columns: retired/deleted artifact, delete/retire wave,
   rebuild provider wave, proof command, and provider-no-later evidence.
5. Normalize REDRESS pre-block coverage across all P3/SPEC/DISPATCH
   surfaces. The shared list must include 28+33, 50-55, 60-72, 80,
   82-84, 88, 89, 96-98, 183/184/209-213, 215, 242-247, and FNV
   closed-enum production migration.
6. Normalize telemetry names to the exact P3-D/SPEC fields:
   `measurement_row_id`, `measurement_origin`, `value_plane`,
   `css_comparator_workload`, `generator_source`, `lock14_scan_scope`,
   `lock16_status`, `checkasm_or_parity_status`,
   `gate_exclusion_report`, and `broadcast_group_id`.
7. Remove W8R broadcast metrics from every live CSS typed-admission
   threshold. They may remain only as diagnostic negative fixtures. W6 must
   capture fresh typed-output cssparser comparison after Track 1 emits typed
   CSS output.
8. Fold the full CH5 forbidden set into SPEC Section 1, global gates, the
   pre-block section, and W7/W8/W9 lowerer language: no parser-owned
   structural projection, retained cursor/list, aux density/projection
   table, sidecar event vector, parallel source pass, second tape, public
   UnionTape, retained sidecar table, class column, whitespace bitmap, new
   substrate/API, or alternate document projection.
9. Strengthen W7/W8/W9 executable consumers. W7 names the decision
   gate/test/generated-selection fixture. W8/W9 name lowerer fixtures for
   each BackendShape or a gate-consumed rejected alternative.
10. Amend W11 close language so PASS-IMPL V2 evidence is the close proof.
    SK-V16 routing is permitted only after accepted or intrinsically blocked
    SK-V15 axes are evidenced at HEAD.

## V2 Acceptance Bar

S-P3 V2 may re-enter hardening only after the V2 packet carries the fold
roster above in executable surfaces. Directional prose in P3 artifacts is
insufficient; the SPEC and DISPATCH prompt must make the routes
unexecutable.

The V2 CHALLENGE can ACCEPT only if:

- one canonical W0-W11 graph exists across P3-B, P3-C, P3-F, SPEC, and
  DISPATCH;
- every wave has row/gate proof against `SK-V15-open` or exact
  no-behavior proof;
- every generic-crate edit has a non-JSON proof receiver;
- old CSS proof cannot retire before typed provider and same-workload proof;
- lowerers and EventTape cannot create sidecars or a sixth BackendShape;
- all gate exclusions are printed and consumed by the gate;
- no V2 close relies on SK-V16 future work.
