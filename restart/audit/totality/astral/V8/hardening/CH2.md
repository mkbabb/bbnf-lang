# Pass Omega V8 CH2 Generality

Date: 2026-05-26.
Lens: CH2 generality.
Scope: Omega-A through Omega-F, `locks-diff.md`, `master-plan-diff.md`,
REDRESS-212, and the SK-V14 W5B-FRONTEND V2 challenge archive.
Disposition: ACCEPT.

## Verdict

ACCEPT. The W5B.0 through W5B.4 split generalizes beyond CSS if, and only if,
the V8 CRUD/SPEC fold carries the exact gates already named by the V8 packet:
W5B.0 is a Lock 14 authority gate, W5B.1 through W5B.3 lower frontend facts
through request-local generic structures, and W5B.4 consumes that closure with
CSS as the strict positive witness plus JSON, Sheets, and BBNF-self proof carry.

This is not a CSS-only acceptance. CSS L4 is the hard compatibility witness;
the admitted mechanism is generic BBNF grammar-source frontend/import/IR
closure. No grammar-specific generic-crate branch, public directive revival,
new BIR variant, new BackendShape, substrate extension, lock amendment, or
provider/template deletion is authorized in W5B.

## Basis

- Omega-A classifies REDRESS-212 as a wave-graph and cap-accounting correction,
  not an architecture or lock change, and says formal W5B.0 through W5B.4
  sub-waves are required before W5C-GEN can unblock.
- Omega-B preserves the corrected chain: W5B.0 Lock 14 routing, W5B.1 import
  closure, W5B.2 layout/discard lowering, W5B.3 pretty/span/projection
  lowering, and W5B.4 request-consumer proof carry before W5C-GEN.
- Omega-C keeps LOCKS read/no-op because Lock 14 already binds generated-output
  allowance, grammar-name/shape leak census, and non-JSON witnesses; the V2
  challenge found missing execution detail, not missing lock text.
- Omega-D and `master-plan-diff.md` preserve W5B as frontend/import/IR only,
  keep W5C-GEN blocked until aggregate W5B close, and explicitly preserve
  "no grammar-name branches in generic crates."
- Omega-E limits skinny-corpus alignment to active authority text and keeps
  BENCH/SUBSTRATE read/no-op; W5B.0 is authority-only and does not touch
  grammar/codegen/xtask frontend implementation paths.
- Omega-F carries the CH2 forward lens: Lock 14 leak census over all W5B
  generic owner paths and public-retirement tests for the full compatibility
  set.
- `locks-diff.md` is zero delta: no public syntax, BIR, BackendShape, substrate,
  or lock is added.
- REDRESS-212 correctly rejects the old one-wave W5B-FRONTEND shape and routes
  the correction through Pass Omega V8 without retained W5B source redress.
- The V2 challenge CH2 blockers are answered by the V8 fold: the generic owner
  leak census moves to W5B.0, and full public-retirement tests are no longer
  limited to `@ws`.

## Generality Matrix

| Requirement | CH2 disposition |
|---|---|
| Generalizes beyond CSS | PASS. W5B.1 import closure is request-source-map based; W5B.2/W5B.3 lower constructs into request-local frontend facts; W5B.4 carries JSON unchanged-output plus Sheets/BBNF-self proof. CSS L4 remains the strict positive witness, not a generic-crate policy branch. |
| Lock 14 neutrality | PASS. W5B.0 owns owner-path roster, parent-diff routing, W5C/W5D subject rejection, provider/template modification rejection, all-template guard, and generic owner-path leak census before any frontend source edits. |
| Public-retirement coverage | PASS with binding fold. V8 must carry exact negative public-parse tests for `@ws`, `@pretty`, `?w`, `>>`, `<<`, `@{...}` span capture, `->` projection metadata, typed projections, and import-only request closure. These constructs may exist only as request-local compatibility/frontend facts. |
| All-template guards | PASS. W5B.0 guards every `_templates` path, not just CSS template directories, and rejects modified provider/template files while allowing only the W5B-owned `grammar_provider.rs` adjacency. |
| No grammar-name branches | PASS. W5B does not authorize generic `match grammar` arms, grammar-named public APIs, grammar-named generic modules, per-grammar feature flags, or hand-written per-grammar runtime files. |
| W5C/W5D separation | PASS. W5B does not replace the provider-free generator body and does not delete providers/templates. W5C-GEN remains blocked until W5B.4 closes aggregate W5B-FRONTEND. |

## Exact Folds

The following folds are CH2-binding in the V8 CRUD/SPEC patch:

1. W5B.0 LOCK14-GATE must run before W5B.1+ source work and must contain:
   owner-path roster, parent-diff routing, W5C/W5D subject rejection,
   modified-provider/template rejection tests, all `_templates` path guard, and
   Lock 14 leak census over all W5B generic owner paths.
2. W5B.1 IMPORT-CLOSURE must prove request-local import DAG resolution, stable
   source hashing, missing-import fail-closed behavior, import-cycle
   fail-closed behavior, and no public syntax addition.
3. W5B.2 LAYOUT-DISCARD must prove `@ws`, `?w`, `>>`, and `<<` lower only into
   request-local frontend facts, with exact negative public-parse tests.
4. W5B.3 PRETTY-SPAN-PROJECTION must prove `@pretty`, `@{...}` span capture,
   `->` projection metadata, and typed projections lower only into request-local
   facts, with exact negative public-parse and malformed-input tests; no public
   directive, BIR variant, BackendShape, or substrate may be added.
5. W5B.4 REQUEST-CONSUMER must prove `emit_runtime_from_request` consumes the
   frontend closure before provider rendering; JSON unchanged-output,
   Sheets/BBNF-self proof carry, `regen-css`, all seven CSS companions,
   provider/template no-change proof, and W5B maintain evidence all pass in the
   same close.
6. The V8 SPEC/master/dispatch fold must state that W5B.0 through W5B.3 are not
   W5B close points. W5B-FRONTEND closes only after W5B.4; W5C-GEN remains
   blocked until that aggregate close.
7. Any omission of the all-template guard, full public-retirement test set,
   generic owner-path leak census, non-JSON proof carry, or no-grammar-name
   branch prohibition reopens CH2 as REVISE.

## Non-Folds

No `restart/locks/LOCKS.md` amendment is required. No architecture amendment is
required. No BENCH or SUBSTRATE change is required. No source, generated output,
RESULTS, or rolling-delta movement is authorized by CH2 V8.
