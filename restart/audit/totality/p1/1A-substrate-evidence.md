---
agent: 1A
pass: T-P1-excavation
cycle: V4
generated_at: 2026-05-21T12:00:00Z
spec_surfaces_audited: [ARCHITECTURE.md, MASTER-PLAN.md, LOCKS.md, HANDOFF.md, skinny/REDRESS.md, skinny/RESULTS.md, restart/skinny/INDEX.md, restart/skinny/SUBSTRATE.md, restart/skinny/COMPILER.md, restart/skinny/BENCH.md, restart/skinny/WORKSPACE.md, restart/skinny/HARDENING.md]
files_audited_count: 37
live_truth_method: "nl -ba for path:line citations; V4 metadata-only fold after V3 CH1 accepted substantive corrections but required active-cycle provenance; uncaptured negative-search claims are downgraded to UNKNOWN/verify_action unless backed by cited live code; no build or cargo tests run"
v4_metadata_fold: "V3 CH1 required cycle/provenance repair only; no substantive 1A evidence claims changed in V4."
prior_cycle_dispositions_folded:
  accepted: [CH3-001, CH3-005, CH3-006, CH5-001, CH5-003, CH5-006, CH6-1A-UNKNOWN-verify-actions]
  rejected: []
  revised: [CH1-LayoutFacts-backend_shape, CH1-negative-search-hygiene, CH3-SinkOnly-admission, CH3-CSS-L4-admission, CH4-cost-metadata, CH5-StructuralIndex-transient-plane, CH5-CSS-source-sidecar-plane, CH5-Track2-shared-substrate-caveat, CH6-closure-wording]
  first_cycle_additions: []
  carried_forward_rows: [1A-SUB-001, 1A-SUB-002, 1A-SUB-003, 1A-SUB-004, 1A-SUB-005, 1A-SUB-006, 1A-SUB-007, 1A-SUB-008, 1A-SUB-009, 1A-SUB-010, 1A-SUB-011, 1A-SUB-012, 1A-SUB-013, 1A-SUB-014, 1A-SUB-015]
divergence_count:
  spec_claims_implemented: 6
  spec_claims_unimplemented: 5
  impl_exceeds_spec: 0
  unknown: 4
locks_amendment_candidates: 1
---

## Executive Summary

The skinny substrate implements the center of Lock 1 for JSON: `Tape<'input>` owns source-backed offset storage, generated `JsonRoot` owns a sealed tape, and `ValueRef<'doc, 'input, K>` is the tape-plus-cursor borrow shape. `BackendShape` has the five specified variants, and V2 corrects the V1 misclassification of `LayoutFacts.backend_shape`: the live side table exists in `passes` and is populated into `PipelineOutput`; the remaining divergence is measured cost-selection quality, priority drift, and incomplete runtime materialization. Generated JSON `SinkOnly` direct parsing is admitted direct evidence, not a regression because it lacks a `TapeBuilder`; what remains unaudited is whether retained and direct outputs are one scheduled event/materialization plan. CSS L4 declaration-values is also preserved as an admitted same-plane fact-stream product row, with the V1 gap restated as a missing substrate/telemetry category rather than a replacement mandate. `StructuralIndex` is classified as a live transient scanner/capacity plane, not a retained authoritative substrate. Uncaptured negative-search claims are not used as closure evidence in V2. Estimated closure is medium-to-high risk: roughly 1.5K-3K LOC for BIR/runtime completeness, plus larger generated-template and benchmark risk outside this agent's scope.

## Spec-Claim ↔ Implementation Table

| id | claim path:line | impl path:line | verdict | note |
|---|---|---|---|---|
| 1A-SUB-001 | `restart/ARCHITECTURE.md:31` and `restart/locks/LOCKS.md:52` claim tape is the substrate unioned with direct-to-struct. | `skinny/crates/runtime/src/tape/mod.rs:94-101`; `skinny/crates/runtime/src/grammars/json/view.rs:12-23`; `skinny/crates/runtime/src/grammars/json/generated.rs:393-407`; `skinny/REDRESS.md:535-557`; `skinny/RESULTS.md:145-147` | partial / admitted direct evidence + unaudited scheduling | Retained JSON owns a `Tape`; generated JSON direct exists as admitted `SinkOnly` direct evidence. The remaining V1 gap is not "direct lacks `TapeBuilder`"; it is that one shared `TapeEmit` / `DirectBuild` event scheduling plan is not audited from the runtime/codegen evidence. LOC/risk: 400-900 LOC medium in codegen/runtime to prove one scheduled materialization plan. |
| 1A-SUB-002 | `restart/locks/LOCKS.md:52` says typed-value records borrow `&'i Tape<'i>` plus cursor. | `skinny/crates/runtime/src/tape/mod.rs:175-180`; `skinny/crates/runtime/src/tape/mod.rs:191-222`; `skinny/crates/runtime/src/grammars/json/view.rs:68-73` | implemented | Live shape is stricter: `ValueRef<'doc, 'input, K, G>` borrows `&'doc Tape<'input>` and carries a cursor. LOC/risk: 0-100 LOC low if docs should mention the separate document lifetime. |
| 1A-SUB-003 | `restart/ARCHITECTURE.md:1525-1526` requires direct views to point into tape and one public node identity `(TapeId, node id, payload class)`. | `skinny/crates/runtime/src/tape/mod.rs:170-177`; `skinny/crates/runtime/src/grammars/json/value.rs:104-111`; `skinny/crates/runtime/src/grammars/json/value.rs:143-171` | partial / diverged | `TapeId` and cursor exist; `JsonToken` has payload fields, but `ValueRef` identity is tape+cursor only and `JsonNodeKind` is recomputed from source byte, not stored payload class. LOC/risk: 200-500 LOC medium if identity must be made explicit. |
| 1A-SUB-004 | `restart/ARCHITECTURE.md:911-949` claims a 20-variant BIR alphabet including `SimdScan`, `PrattSpine`, `CallHost`, `LayoutScope`, `ErrorRecover`, `PathEval`, `DebugMark`, and `Return`. | `skinny/crates/ir/src/lib.rs:416-452`; `skinny/crates/ir/src/lib.rs:454-461` | unimplemented / diverged | Live `BackendExpr` has 13 variants; `SimdScan` is a separate `Recognizer`. The cited enum range does not contain `PrattSpine`, `CallHost`, `LayoutScope`, `ErrorRecover`, `PathEval`, or `DebugMark`. LOC/risk: 600-1,200 LOC high, because adding variants touches validation, lowerers, snapshots, and VM expectations. |
| 1A-SUB-005 | `restart/ARCHITECTURE.md:951-961` says access pattern varies by `BackendShape` without changing BIR payloads. | `skinny/crates/ir/src/lib.rs:401-408`; `skinny/crates/ir/src/cost.rs:127-135` | implemented at type level | The five variants exist and `all_backend_shapes()` returns all five. Runtime materialization is not complete; see 1A-SUB-007. LOC/risk: type-level 0; runtime completion 800-1,500 LOC high. |
| 1A-SUB-006 | `restart/ARCHITECTURE.md:1060-1098` claims `LayoutFacts.backend_shape` is cost-model-derived by an 8-step algorithm. | `skinny/crates/passes/src/lib.rs:44-55`; `skinny/crates/passes/src/lib.rs:84-92`; `skinny/crates/passes/src/lib.rs:387-440`; `skinny/crates/passes/src/lib.rs:446-505`; `skinny/crates/ir/src/cost.rs:4-13`; `skinny/crates/ir/src/cost.rs:56-79` | implemented side table / partial cost model | `LayoutFacts.backend_shape: HashMap<RuleId, BackendShape>` is live in `passes` and populated from `derive_backend_shape_with_diagnostics`. The remaining divergence is cost-model and priority-algorithm strength: current priority names/order and evidence diagnostics are not yet the full measured 8-clause spec, and runtime materialization by shape remains incomplete. LOC/risk: 250-600 LOC medium for measured priority/evidence repair, plus runtime/codegen work captured in related rows. |
| 1A-SUB-007 | `restart/ARCHITECTURE.md:1068-1087` and `restart/ARCHITECTURE.md:1104-1108` claim `EagerTape`, `OffsetTape`, `EventTape`, `SinkOnly`, and `CollapsedStage` lowering outputs. | `skinny/crates/runtime/src/tape/mod.rs:94-101`; `skinny/crates/runtime/src/grammars/json/parser.rs:47-51`; `skinny/crates/runtime/src/grammars/json/generated.rs:407-421` | partial / UNKNOWN event/collapsed runtime | Offset-style retained JSON and SinkOnly-style direct JSON exist. The cited runtime rows do not prove event cells or collapsed-stage kernel/shim; V2 routes those negative claims to `1A-UNK-003` and same-wave consumer proof rather than closing by uncaptured search. LOC/risk: 800-1,800 LOC high in runtime/codegen plus ISA test risk. |
| 1A-SUB-008 | `restart/ARCHITECTURE.md:1507-1517` claims `runtime/src/tape` owns `token`, `builder`, `span`, `payload`, `view`, and `trace` modules. | `skinny/crates/runtime/src/tape/mod.rs:1-5`; `skinny/crates/runtime/src/tape/assembler.rs:42-48`; `skinny/crates/runtime/src/tape/mod.rs:38-89` | partial / diverged | Live `mod.rs` declares `assembler`, `event_grammar`, tests, and `offsets`; builder/payload/view concepts exist but not as the named module contract in the cited module declarations. LOC/risk: 250-600 LOC medium if this is structural API, low if docs can accept skinny names. |
| 1A-SUB-009 | `restart/ARCHITECTURE.md:1522-1524` requires append-only checkpoints, bounded rollback, and slice-borrow payload policy. | `skinny/crates/runtime/src/tape/assembler.rs:61-85`; `skinny/crates/runtime/src/tape/assembler.rs:93-123`; `skinny/crates/runtime/src/tape/mod.rs:65-78` | partial / UNKNOWN checkpoint surface | Append-only offset writes and sparse flag patching exist in the cited builder API. The cited rows do not prove a checkpoint/rollback API, and payload policy is only evidenced as a byte arena without declared per-token policy. LOC/risk: 300-700 LOC medium. |
| 1A-SUB-010 | `restart/ARCHITECTURE.md:1530-1536` says direct builders do not bypass the substrate event stream and direct scalars are not a second authoritative tree. | `skinny/crates/runtime/src/grammars/json/generated.rs:393-407`; `skinny/crates/runtime/src/grammars/json/generated.rs:435-459`; `skinny/crates/runtime/src/grammars/json/sink.rs:4-15`; `skinny/REDRESS.md:535-557`; `skinny/RESULTS.md:145-147` | partial / admitted SinkOnly, scheduling UNKNOWN | JSON direct parsing writes to generated `JsonSink` methods and returns no queryable document identity, which matches admitted `SinkOnly` direct evidence. It should not be downgraded solely because no `TapeBuilder` is instantiated. The remaining gap is whether direct and retained paths share an auditable accepted event/materialization schedule. LOC/risk: 400-900 LOC medium/high to make event scheduling auditable. |
| 1A-SUB-011 | `restart/ARCHITECTURE.md:1555-1559` says retained APIs are document-first and `SinkOnly` retains no identity. | `skinny/crates/runtime/src/grammars/json/view.rs:12-23`; `skinny/crates/runtime/src/grammars/json/view.rs:63-66`; `skinny/crates/runtime/src/grammars/json/generated.rs:407-421` | implemented | Retained JSON root owns `JsonDocument { tape, source_str }`; direct parse accepts a sink and returns only `Result<(), ParseError>`. LOC/risk: none for JSON. |
| 1A-SUB-012 | `restart/ARCHITECTURE.md:1561-1576` claims byte input -> mask stream -> typed event cursor -> substrate shape; mask stream is transient, not retained substrate. | `skinny/crates/runtime/src/grammars/json/scan.rs:1-7`; `skinny/crates/runtime/src/grammars/json/scan.rs:22-35`; `skinny/crates/runtime/src/grammars/json/scan.rs:47-53`; `skinny/crates/runtime/src/grammars/json/generated.rs:12-15` | partial / transient scanner plane | `StructuralIndex` is a live JSON-owned scanner/capacity plane and must be classified explicitly. Current cited evidence supports transient capacity/proof use (`OneShotSimd` capacity), while generated retained parse's `attach_structural_index` is a no-op. No retained document identity is proven from `StructuralIndex`; typed event cursor remains UNKNOWN pending targeted call-site audit. LOC/risk: 200-500 LOC medium. |
| 1A-SUB-013 | `restart/ARCHITECTURE.md:1788-1794` says runtime files under `runtime/src/grammars/<name>/` are template-emitted and hand-written runtime files are forbidden. | `skinny/crates/runtime/src/grammars/json/generated.rs:1`; `skinny/crates/runtime/src/grammars/json/view.rs:1`; `skinny/crates/runtime/src/grammars/json/sink.rs:1`; `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1` | unknown / diverged | Some files carry generated headers; `json/sink.rs` explicitly says it is JSON-owned and not part of the generated roster. Generator provenance for all grammar files cannot be verified from comments alone. LOC/risk: 200-500 LOC medium to template or quarantine hand-owned grammar files. |
| 1A-SUB-014 | `restart/ARCHITECTURE.md:22-23`, `restart/ARCHITECTURE.md:1900-1902`, and `restart/locks/LOCKS.md:52` reject new substrates, sidecars, OpenFrame ladders, and columnar SoA. | `skinny/crates/runtime/src/tape/mod.rs:94-101`; `skinny/crates/runtime/src/grammars/json/scan.rs:1-7`; `skinny/crates/runtime/src/grammars/json/scan.rs:47-53`; `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:222`; `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:299`; `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:504` | partial / UNKNOWN old-name census | The retained JSON substrate is tape, but V1's old-name negative search was uncaptured and cannot close the row. Live renamed side planes exist: JSON `StructuralIndex` as transient scanner/capacity input and CSS `same-plane-source-sidecar` comparator evidence. Neither is proven as a retained authoritative substrate, but both must stay fenced and non-authoritative until targeted scans are captured. LOC/risk: 100-300 LOC low/medium for audit artifacts and fencing docs, higher only if retained identity is found. |
| 1A-SUB-015 | `restart/ARCHITECTURE.md:1530-1534` requires shared validation, spans, diagnostics, node kind, and payload policy across direct/tape. | `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1-6`; `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:25-35`; `skinny/crates/runtime/src/grammars/css_l4_declaration_values/sink.rs:18-33`; `skinny/REDRESS.md:3824-3840`; `skinny/RESULTS.md:94`; `skinny/RESULTS.md:145-148` | admitted CSS fact-stream evidence / substrate-category gap | CSS declaration-values emits a TSV-like fact stream through `FactSink` with no `Tape`, `ValueRef`, `DocumentView`, or direct typed root. V2 preserves this as an admitted same-plane product row; the substrate finding is that V1 lacks a category for admitted fact-stream telemetry/product rows, not that the row should be replaced or demoted. LOC/risk: 100-300 LOC low/medium for lock/audit taxonomy; 500-1,000 LOC medium/high only if a future wave chooses to produce a tape/direct substrate for CSS. |

## Divergences Catalogued

| id | divergence | loc_budget | risk | wave | hard_cap | same_wave_consumer | evidence_basis |
|---|---|---|---|---|---|---|---|
| 1A-DIV-001 | BIR alphabet is underspecified in live `ir/src` relative to the architecture. The spec claims 20 variants; live `BackendExpr` covers 13 variants and `SimdScan` is a separate `Recognizer`. | 600-1,200 LOC | high | T-P2 IR/lowering wave | cap at 1,500 LOC before re-scoping to amendment | Codegen/runtime lowerers and VM tests in the same wave | Spec `restart/ARCHITECTURE.md:922-943`; live `skinny/crates/ir/src/lib.rs:416-461`. |
| 1A-DIV-002 | Direct-to-struct union is not auditable as one scheduled materialization path. Generated JSON `SinkOnly` direct parsing is admitted evidence; the open gap is shared `TapeEmit` / `DirectBuild` scheduling, not absence of `TapeBuilder` inside `SinkOnly`. | 400-900 LOC | medium/high | T-P2 codegen/runtime scheduling | cap at 1,100 LOC or split into codegen/runtime subrows | Generated retained parse and direct parse row tests in the same wave | Spec `restart/ARCHITECTURE.md:1019-1020`, `restart/ARCHITECTURE.md:1530-1536`; live retained `skinny/crates/runtime/src/grammars/json/parser.rs:7-12`; live direct `skinny/crates/runtime/src/grammars/json/generated.rs:393-421`; admission `skinny/REDRESS.md:535-557`, `skinny/RESULTS.md:145-147`. |
| 1A-DIV-003 | Runtime event cursor and EventTape are not proven as runtime substrate. Offset-backed `ValueRef` exists; typed `EventCursor` remains UNKNOWN until targeted symbol/call-site output is captured. | 300-800 LOC | medium | T-P2 runtime substrate | cap at 900 LOC before deferring EventTape to amendment | Runtime tape API tests plus retained parser consumer | Spec `restart/ARCHITECTURE.md:1561-1576`; live offset view `skinny/crates/runtime/src/tape/mod.rs:175-222`; `skinny/crates/runtime/src/tape/offsets.rs:1-5`; negative symbol status routed to `1A-UNK-003`. |
| 1A-DIV-004 | `CollapsedStage` exists in `BackendShape` but no audited runtime shape exists. | 500-1,000 LOC plus assembly/ISA parity harness | high | SIMD/ASM wave only after same-wave consumer is named | cap at 1,200 LOC without parity harness, otherwise split | A production grammar × ISA row must consume it in the same wave | Spec `restart/ARCHITECTURE.md:1107-1108`; live enum `skinny/crates/ir/src/lib.rs:401-408`; shape list `skinny/crates/ir/src/cost.rs:127-135`; viability diagnostics in `skinny/crates/passes/src/lib.rs:465-482`. |
| 1A-DIV-005 | Tape module layout does not match §9.1 names. This may be a structural API gap or a stale naming contract. | 250-600 LOC | medium | T-P2 runtime API/doc alignment | cap at 700 LOC; if higher, route to lock amendment | Runtime tape public API docs and parser compile users | Spec `restart/ARCHITECTURE.md:1507-1517`; live modules `skinny/crates/runtime/src/tape/mod.rs:1-5`. |
| 1A-DIV-006 | CSS declaration-values runtime is admitted same-plane fact-stream evidence, but V1 has no substrate/telemetry category for that product row. | 100-300 LOC for taxonomy; 500-1,000 LOC only if future tape/direct substrate is required | medium | T-P1/T-P2 audit taxonomy; implementation only in later runtime wave | cap taxonomy at 350 LOC; implementation requires separate wave charter | 1E lock wording and CSS row-plane accounting in the same wave | Spec `restart/ARCHITECTURE.md:1561-1569`; live `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1-35`; `skinny/crates/runtime/src/grammars/css_l4_declaration_values/sink.rs:18-33`; admission `skinny/REDRESS.md:3824-3840`, `skinny/RESULTS.md:94`, `skinny/RESULTS.md:145-148`. |
| 1A-DIV-007 | Renamed scanner/sidecar planes are live and must stay fenced: JSON `StructuralIndex` is a transient scanner/capacity plane, and CSS `same-plane-source-sidecar` is comparator evidence, not runtime substrate. | 100-300 LOC | low/medium | T-P1 audit hardening / T-P2 fence enforcement | cap at 400 LOC unless retained identity is found | 1F hidden-coupling rows and runtime/codegen audit consumers | JSON scan `skinny/crates/runtime/src/grammars/json/scan.rs:1-7`, `skinny/crates/runtime/src/grammars/json/scan.rs:47-53`; CSS comparator `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:222`, `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:299`, `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs:504`. |

## Gaps / Missing Primitives

| gap | evidence | impact | LOC/risk estimate |
|---|---|---|---|
| Full 20-variant BIR in audited `ir/src` | Spec rows `restart/ARCHITECTURE.md:922-943`; live rows `skinny/crates/ir/src/lib.rs:416-461`. | Lowerer/VM contract cannot cover the V1 alphabet from current `ir/src`. | 600-1,200 LOC, high. |
| Measured `LayoutFacts.backend_shape` cost/priority completion | Spec `restart/ARCHITECTURE.md:1047` and `restart/ARCHITECTURE.md:1090-1098`; live side table and population at `skinny/crates/passes/src/lib.rs:44-55`, `skinny/crates/passes/src/lib.rs:84-92`, and `skinny/crates/passes/src/lib.rs:387-440`; live `CostFacts` at `skinny/crates/ir/src/cost.rs:4-13`. | The named side table is implemented in `passes`; the gap is measured evidence and full priority semantics, not symbol absence. | 250-600 LOC, medium. |
| Typed event cursor abstraction | Spec `restart/ARCHITECTURE.md:1571-1575`; cited live evidence only proves offset-backed `ValueRef` at `skinny/crates/runtime/src/tape/mod.rs:175-222`; symbol absence is routed to `1A-UNK-003` pending captured command output. | EventTape cannot be accepted as implemented from the current cited evidence. | 300-800 LOC, medium. |
| Checkpoint/rollback API | Spec `restart/ARCHITECTURE.md:1522-1523`; cited live builder evidence exposes push/patch/finish at `skinny/crates/runtime/src/tape/assembler.rs:61-123`. | Speculative `Alt` cannot prove bounded rollback from the currently cited builder surface. | 300-700 LOC, medium. |
| Payload policy and payload-class identity | Spec `restart/ARCHITECTURE.md:1524-1526`; live `PayloadArena` is byte storage at `skinny/crates/runtime/src/tape/mod.rs:38-89`; `ValueRef` stores no payload class at `skinny/crates/runtime/src/tape/mod.rs:175-180`. | Public node identity is weaker than spec. | 200-500 LOC, medium. |
| Generated provenance for grammar runtime files | Spec `restart/ARCHITECTURE.md:1793`; `json/sink.rs` says not generated at `skinny/crates/runtime/src/grammars/json/sink.rs:1`. | Lock 14/Lock 1 substrate rows cannot distinguish emitted runtime surface from hand-owned exception. | 200-500 LOC, medium. |

## Open Questions

| id | status | question | verify_action |
|---|---|---|---|
| 1A-UNK-001 | UNKNOWN | Is `skinny/crates/runtime/src/grammars/json/scan.rs` a transient scan/capacity helper only, or can `StructuralIndex` become a retained sidecar in other build modes? | Run `rg -n "scan_structurals\\(|StructuralIndex|positions\\(" skinny/crates` and inspect all call sites, then run JSON retained/direct tests with `BBNF_CAPACITY_PLAN=C` to confirm no retained document identity depends on `StructuralIndex`. |
| 1A-UNK-002 | UNKNOWN | Which `runtime/src/grammars/<name>/` files are actually generator-owned versus hand-written probe code? | Run the codegen regeneration command and compare `git diff -- skinny/crates/runtime/src/grammars`; require generated-file manifest or header parity for each file. |
| 1A-UNK-003 | UNKNOWN | Is `EventCursor` absent from all live runtime/codegen paths, or did V1 miss a renamed typed-event cursor? | Capture `rg -n "EventCursor|event cursor|TypedEvent|EventTape" skinny/crates restart` output into an audit artifact or replace all absence claims with exact cited rows. |
| 1A-UNK-004 | UNKNOWN | Are old parallel-substrate names (`OpenFrame`, `ParseStream`, `Columnar`, `SoA`, `Vec<OpenFrame>`) absent from the intended live scope? | Capture `rg -n "OpenFrame|ParseStream|Columnar|SoA|Vec<OpenFrame>" skinny/crates restart/ARCHITECTURE.md restart/locks/LOCKS.md` output with scope notes; do not use V1's uncaptured negative search as closure evidence. |

## Lock Amendment Candidate Surfaced To 1E

| candidate | current lock/spec text | evidence | proposed amendment for 1E disposition | loc_budget | risk | wave | hard_cap | same_wave_consumer | evidence_basis |
|---|---|---|---|---|---|---|---|---|---|
| 1A-LOCK1-AMEND-001 | Lock 1 says the greenfield tape lives at `runtime/src/tape/`, typed records borrow into it, and per-grammar runtime modules emit accessors with one materialisation surface at `restart/locks/LOCKS.md:52`. | Live skinny has a measured JSON offset-tape/direct prototype; generated JSON SinkOnly is admitted direct evidence; CSS declaration-values emits an admitted fact stream with no tape at `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1-35`; `json/sink.rs` is explicitly JSON-owned and not generated at `skinny/crates/runtime/src/grammars/json/sink.rs:1`. | Add a Lock 1 clarification: non-substrate empirical fact streams may exist only as fenced skinny evidence/probes and must not be described as V1 runtime substrate closure until routed through `TapeEmit` / `DirectBuild` or explicitly excluded from the substrate claim. Preserve admitted CSS L4 row evidence while naming the substrate/telemetry category gap. | 100-300 LOC docs/audit taxonomy; implementation excluded unless separately chartered | medium | T-P1/T-P2 lock audit; route implementation questions to T-P3 3C after 1E | hard cap 350 LOC for amendment/taxonomy | 1E Lock 1/Lock 8 row-plane accounting and CSS audit rows in same wave | `restart/locks/LOCKS.md:52`; `skinny/REDRESS.md:3824-3840`; `skinny/RESULTS.md:94`; `skinny/RESULTS.md:145-148`; `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1-35`; `skinny/crates/runtime/src/grammars/json/sink.rs:1`. |
