# SK-V12 S-P2 CHALLENGE V3 - CH2 Generality

Pass: S-P2 Research. Cycle: V3.
Date: 2026-05-20.
Lens: CH2 GENERALITY.
Scope: convergence check against commit `6b8be238`, verifying that the folded S-P2 artifacts preserve Lock 14 and that no candidate, support family, diagnostic, oracle, accounting item, or ISA inventory entry has become JSON-overfit.
Disposition: ACCEPT.

## §1 - Basis

CH2 applies the PASS-2 rule that every candidate must carry a P2-F grammar-neutral verdict; a primitive proposed only because JSON needs it is JSON-overfit unless it has a neutral byte-set, classifier, or tape expression and works for CSS L4, Sheets, and BBNF-self, or is reframed as a per-grammar template surface (`restart/prompts/skinny/PASS-2-RESEARCH.md:102`-`:107`). The universal CH2 lens states the same Lock 14 boundary: no grammar-name leak and no intervention that works only for JSON (`restart/prompts/ORCHESTRATOR.md:83`-`:84`). Lock 14 itself forbids grammar-specific code, grammar-named modules, grammar-specific public APIs, per-grammar feature flags, and hand-written per-grammar runtime files in generic crates (`restart/locks/LOCKS.md:78`).

The V2 consolidation already returned all six ACCEPT and records CH2 ACCEPT across current candidates, support families, oracle-only surfaces, accounting-only surfaces, and rejected/inventory entries (`restart/skinny/tranches/sk-v12/research/p2/hardening/HARDENING-S-P2-V2-CONSOLIDATED.md:12`-`:17`). V3 is therefore a convergence check over the same folded cohort; V2 says S-P2 converges only if V3 also returns ACCEPT with no open REVISE/REJECT (`restart/skinny/tranches/sk-v12/research/p2/hardening/HARDENING-S-P2-V2-CONSOLIDATED.md:19`-`:21`, `:42`-`:44`).

## §2 - Findings

1. P2-F remains the governing grammar-neutral map. It names eight families, with six conditional parser/support families, F7 oracle-only, and F8 accounting-only; none is selected for a wave (`restart/skinny/tranches/sk-v12/research/p2/p2f-grammar-neutral.md:23`-`:40`). Its grammar matrix explicitly tests CSS L4, Sheets, and BBNF-self and preserves the Lock 14 rule that byte sets, delimiters, escapes, number policy, layout/trivia, FIRST tables, and output products are generated metadata or host declarations, not generic-crate grammar branches (`restart/skinny/tranches/sk-v12/research/p2/p2f-grammar-neutral.md:42`-`:53`). F7/F8 are still parser-candidate-ineligible (`restart/skinny/tranches/sk-v12/research/p2/p2f-grammar-neutral.md:52`-`:53`).

2. P2-A C1-C7 remain grammar-neutral candidate shapes, not JSON residual patches. The triad table keeps scalar/checkasm/same-wave consumer obligations on every candidate (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:29`-`:37`), and the grammar-neutrality table keeps generated byte classes, caller-owned string/escape/number policy, grammar FIRST/follow facts, row-owned output products, and generated branch tables as the admission boundary (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:139`-`:149`). The artifact explicitly says any candidate that cannot be expressed through generated CSS L4 declaration values, Sheets, or BBNF-self remains JSON-only research (`restart/skinny/tranches/sk-v12/research/p2/p2a-sota-teardown.md:163`).

3. P2-B's twelve process gates remain support/admission gates with grammar-owned callers. The table rejects retained class columns, JSON role enums, parser-owned sidecars, second scans, JSON surrogate policy inside `bbnf-simd`, numeric materialization changes, retained cursors, and digest-as-parser semantics (`restart/skinny/tranches/sk-v12/research/p2/p2b-dav1d-process.md:36`-`:49`). Its generality table states that byte tables, byte sets, terminator classes, string special scans, hex quartet decode, digit spans, bitmap helpers, tail clamping, movemask ordering, and digest equality are grammar-neutral only as mechanics or oracle support (`restart/skinny/tranches/sk-v12/research/p2/p2b-dav1d-process.md:53`-`:68`). The risk section keeps generated non-JSON priority ahead of JSON direct retry work (`restart/skinny/tranches/sk-v12/research/p2/p2b-dav1d-process.md:72`-`:79`).

4. P2-C does not leak new AArch64 inventory into JSON-only parser candidates. The current candidate count is six, while LD4 interleaved classification and SHA3 ternary boolean fold remain non-selectable ISA inventory because current S-P1 evidence does not name a hot interleaved stream or three-input boolean fold (`restart/skinny/tranches/sk-v12/research/p2/p2c-arch-esoterica.md:36`-`:42`, `:55`-`:65`, `:122`-`:133`). The six current candidates are byte-set masks, digit-span helpers, string-special scans, hex decode, layout/trivia run skip, and transient-mask support with generated consumers and caller-owned policy (`restart/skinny/tranches/sk-v12/research/p2/p2c-arch-esoterica.md:44`-`:53`, `:67`-`:76`, `:78`-`:87`, `:89`-`:98`, `:100`-`:109`, `:111`-`:120`).

5. P2-D remains non-selectable under current S-P1 and therefore cannot be JSON-overfit implementation authority. It records zero selectable tape-substrate candidates, three same-tape diagnostics, and one rejected parallel-substrate route (`restart/skinny/tranches/sk-v12/research/p2/p2d-substrate-tape.md:67`-`:83`). Its grammar-neutrality section forbids JSON corpus names, JSON punctuation, generic knowledge of JSON `HAS_ESC`, grammar-name matches, second retained representations, and any revived structural class lane under current evidence (`restart/skinny/tranches/sk-v12/research/p2/p2d-substrate-tape.md:85`-`:111`).

6. P2-E's parse-that candidates remain mechanics below grammar policy. `pt_byte_set_run_skip`, `pt_bounded_plain_string_end`, `pt_digit_run_span_accumulate`, `pt_hex_quad_decode`, and `pt_escaped_string_segments` all require generated caller data/policy and same-wave consumers (`restart/skinny/tranches/sk-v12/research/p2/p2e-parse-that-gaps.md:50`-`:91`, `:95`-`:149`, `:153`-`:218`, `:222`-`:280`, `:284`-`:356`). Its common rule keeps syntax, policy, and output projection in generated grammar code and rejects any helper whose behavior bakes JSON, CSS, Sheets, or BBNF-self semantics into `parse-that-regex`, `parse-that`, or `bbnf-simd` (`restart/skinny/tranches/sk-v12/research/p2/p2e-parse-that-gaps.md:360`-`:372`).

7. The non-JSON grammar evidence is concrete and still active. CSS L4 supplies identifiers, escaped selector identifiers, quoted strings, dimensions, math/function dispatch, var/env, and URL forms (`grammar/css/l4/tokens.bbnf:7`-`:9`; `grammar/css/l4/values.bbnf:29`-`:69`, `:84`-`:90`). Sheets supplies numbers, doubled-quote strings, error literals, cell/range spans, operator towers, function calls, LET/LAMBDA calls, arrays, and formula roots (`grammar/google-sheets/google-sheets.bbnf:6`-`:12`, `:34`-`:63`, `:97`-`:167`). BBNF-self supplies identifiers, multi-delimiter literals, regex spans, comments, terms, rules, directives, and value-expression numeric/string/operator surfaces (`grammar/bbnf/bbnf.bbnf:9`-`:18`, `:29`-`:56`, `:75`-`:85`; `grammar/bbnf/expressions.bbnf:6`-`:30`).

8. The product/result boundary still prevents paper generality. RESULTS remains `N-direct / NoGo`, with Track 1 as generated JSON parse and Track 2 as independent hand-coded tape parsing (`skinny/RESULTS.md:143`-`:146`). REDRESS 111 admitted only a non-JSON report lane, REDRESS 112 rejected generated non-JSON baseline authority because codegen still routes through JSON provider policy, REDRESS 113 blocked the CSS intervention without that baseline, and REDRESS 120 routes SK-V12 to generated non-JSON baseline first (`skinny/REDRESS.md:3282`-`:3309`, `:3311`-`:3338`, `:3340`-`:3350`, `:3531`-`:3553`).

## §3 - Revise List

None for CH2.

## §4 - Carry-Forward Guards

- P2-F F7 remains oracle-only and F8 remains accounting-only; neither is a parser primitive.
- P2-C I1/I2 remain non-selectable ISA inventory until fresh profile evidence names a legal stream/expression and the same wave supplies scalar parity plus a generated/runtime consumer.
- P2-D same-tape items remain diagnostics/ineligible under current S-P1, and `structural_class_lane_union` remains rejected.
- S-P3 may shortlist only generated-metadata, grammar-neutral primitives with same-wave consumers; JSON direct residual retry work and parse-only work remain outside this CH2 ACCEPT.
