# CH2 — GENERALITY (V5)

S-P0 CHALLENGE V5 confirming-pass lens disposition (SK-V14 Overfit
Audit Pass; Lock 14 generality posture; V max=5 ceiling cycle).

Authority: `restart/prompts/ORCHESTRATOR.md §3W` row CH2 ("Lock 14
holds: no grammar-name leak; every proposed intervention is grammar-
neutral and works for CSS L4 / Sheets / BBNF-self, not only JSON.")
+ `restart/skinny/tranches/sk-v14/audit-overfit/hardening/V4/CH2.md`
§4.2 + §5 V5 optional fold recommendation (close §2.4:319-321
residual closing sentence) + V5 micro-redispatch commit `037eec6b6`
(1 file modified, +4 / -3; F-V5-SYNTHESIS-1{a,b}).

V5 artefacts under review (HEAD == `037eec6b6`):

- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`
  (V5; F-V5-SYNTHESIS-1a §2.4:318 "siblings" framing precision +
  F-V5-SYNTHESIS-1b §2.4:319-321 residual closing sentence deletion)

A1, A2, A3, A4, A5, A6 STAND verbatim (per V5 commit message + commit
stat: only SYNTHESIS-AUDIT-OVERFIT.md modified).

## §0 — Disposition summary

| Total dispositions | ACCEPT | ACCEPT-with-NOTE | REVISE | REJECT |
| ---: | ---: | ---: | ---: | ---: |
| **8** | **8** | **0** | **0** | **0** |

ACCEPT-rate **8/8 = 100 %**. F-V5-SYNTHESIS-1b closes the V4 §4.2
cohesion polish note cleanly: the residual sentence "The lint glob
`codegen/src/**/*.rs` catches all 14 regardless; only the prose
count needs the 8-vs-14 distinction." has been deleted from
SYNTHESIS §2.4. The V4 fold's explicit 8+6 decomposition at line
312 ("14 codegen-side (8 providers+templates + 6 ancillary)")
already states the breakdown, making the V2 → V3 precision-repair
back-reference redundant; with that back-reference removed the
prose flows from the disjoint-set arithmetic directly into the
runtime-only-lint-insufficiency clause (lines 321-325). The lint-
mechanism's grammar-neutral binding to both runtime + codegen
halves remains explicit (lines 309-321) — load-bearing GENERALITY
content preserved.

F-V5-SYNTHESIS-1a is CH4 territory (the "siblings" framing
precision); from the CH2 vantage the replacement preserves the
load-bearing disjoint-set arithmetic + the grammar-neutral lint-
glob binding, so it carries no CH2-specific disposition impact.
CH2 confirms only that the rephrase does not perturb the
GENERALITY reading.

V4 baseline (per V4 CH2.md §6 — 8 ACCEPT + 0 ACCEPT-with-NOTE) holds
under V5: F-V3-A4-1 + F-V3-A5-1 + F-V3-SYNTHESIS-{1,3} + F-V4-
SYNTHESIS-{1,2} all carry forward unchanged (V5 commit stat
confirms zero touches to A4 / A5 / axis files). The V4 §4.2 sub-
CH2-fatal editorial wrinkle retires under V5 by F-V5-SYNTHESIS-1b
deletion.

The CH2 GENERALITY posture remains sound end-to-end after the V5
micro-fold. Lock 14 baseline (A3 30 violations) holds verbatim
under V5 — A3 carried no V5 fold; V5 commit stat confirms A3 file
unchanged. The 9-grammar census re-attests byte-identically at V5
HEAD. Primitive-crate generic posture (zero grammar-name leaks
across `bbnf-regex`, `bbnf-simd`, `simd-scan`, `parse-that-regex`,
`ir`) preserved through V5. Fake-`@generated` 56-file surface (42 +
14 disjoint) preserved with the §2.4 prose now reading cohesively
without the V2 → V3 precision-repair residue.

## §1 — Per-artefact V5 disposition

| Artefact | V4 → V5 fold(s) | V5 Disposition | CH2 verification |
| --- | --- | --- | --- |
| `SYNTHESIS-AUDIT-OVERFIT.md` (V5) | F-V5-SYNTHESIS-1a (§2.4:318 "the two path roots are siblings" → "the two path roots sit under sibling workspace crates `skinny/crates/runtime/` and `skinny/crates/codegen/`; the lint glob brace-expansion is path-pattern-based, not structural-relation-based") + F-V5-SYNTHESIS-1b (§2.4:319-321 residual closing sentence "The lint glob `codegen/src/**/*.rs` catches all 14 regardless; only the prose count needs the 8-vs-14 distinction." DELETED) | **ACCEPT** | §3.1 below; F-V5-SYNTHESIS-1b closes V4 §4.2 cohesion polish note cleanly (residual back-reference removed; prose now flows from disjoint-set arithmetic directly to runtime-only-lint-insufficiency); F-V5-SYNTHESIS-1a is CH4 territory but CH2 confirms the rephrase preserves the load-bearing disjoint-set arithmetic + grammar-neutral lint-glob binding. |
| `sk-v14-audit-overfit-generator-truth.md` (A4) | STAND (no V5 folds) | **ACCEPT** | V4 CH2 §1 A4 row carries forward; V5 commit stat confirms A4 file unchanged |
| `sk-v14-audit-overfit-decision-engine.md` (A5) | STAND (no V5 folds) | **ACCEPT** | V4 CH2 §1 A5 row carries forward; V5 commit stat confirms A5 file unchanged |
| `sk-v14-audit-overfit-css-measurement.md` (A1) | STAND (no V5 folds; V1+V2+V3+V4 STAND) | **ACCEPT** | V4 CH2 §1 A1 row carries forward |
| `sk-v14-audit-overfit-admit-mechanism.md` (A2) | STAND (no V5 folds; V1+V2+V3+V4 STAND) | **ACCEPT** | V4 CH2 §1 A2 row carries forward |
| `sk-v14-audit-overfit-lock14-scan.md` (A3) | STAND (no V5 folds; V1+V2+V3+V4 STAND) | **ACCEPT** | V4 CH2 §1 A3 row carries forward; 30 violations re-verified at V5 HEAD (§3.2 below) |
| `sk-v14-audit-overfit-pre-restart-pattern.md` (A6) | STAND (no V5 folds; V1+V2+V3+V4 STAND) | **ACCEPT** | V4 CH2 §1 A6 row carries forward; Pattern H 9-grammar census preserved at V5 HEAD |
| **Aggregate** | | **ACCEPT** | — |

## §2 — Per-§ ACCEPT-rate (V5 cycle)

Per V4 CH2 §5 V5 optional-fold recommendation, the V5 lens has one
specific fold-verification clause (V4 §4.2 cohesion polish closure
via F-V5-SYNTHESIS-1b) plus the standard fresh-finding scan + carry-
forward bookkeeping. F-V5-SYNTHESIS-1a is enumerated under CH4
primary; CH2 carries it as a coincident verification only.

| CH2 V5 sub-clause | ACCEPT | ACCEPT-with-NOTE | REVISE | REJECT |
| --- | ---: | ---: | ---: | ---: |
| (i) **F-V5-SYNTHESIS-1b closes V4 §4.2 residual closing sentence at §2.4:319-321 (back-reference deletion; prose flow restored)** | 1 | 0 | 0 | 0 |
| (ii) **F-V5-SYNTHESIS-1a "siblings" framing rephrase preserves disjoint-set arithmetic + grammar-neutral lint-glob binding (CH4 primary; CH2 coincident only)** | 1 | 0 | 0 | 0 |
| (iii) **Fresh-finding scan over V5 deltas + Lock 14 baseline re-verification + primitive-crate posture re-check** | 1 | 0 | 0 | 0 |
| (iv) **V5 carry-forward of V4 ACCEPTs (A1, A2, A3, A4, A5, A6 STAND)** | 5 | 0 | 0 | 0 |
| **All** | **8** | **0** | **0** | **0** |

## §3 — Executable verification (CH2 V5 evidence)

Per the V1 CHALLENGE-CONTEXT §4 executable-verification mandate
(carried through to V5 per §3Z confirming-pass discipline), the
following were re-run against the working tree at HEAD == `037eec6b6`
(V5 micro-redispatch landing commit).

### §3.1 F-V5-SYNTHESIS-1b — residual closing sentence deletion verification

V4 CH2 §4.2 flagged the §2.4:319-321 closing sentence as a sub-CH2-
fatal cohesion-only editorial wrinkle — the residual "8-vs-14
distinction" clause carried forward from F-V3-SYNTHESIS-2 (which
addressed the V2 §4.1 NOTE on 8 → 14 prose precision) read
disjointly after F-V4-SYNTHESIS-1 replaced the surrounding prose
with the explicit "56 (42 + 14)" disjoint-set reading. V5
verification of the closing fold:

```
$ grep -n "8-vs-14\|catches all 14 regardless" \
    restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md
[no output]
```

Both phrases ("8-vs-14" and "catches all 14 regardless") absent from
the V5 SYNTHESIS surface — the F-V5-SYNTHESIS-1b deletion is
complete. The V5 prose at §2.4:316-325 now reads:

> "… `git grep -l '@generated by skinny bbnf-codegen' skinny/
> crates/codegen/src/ | wc -l = 14`, summing to the 56 total with
> zero overlap (the two path roots sit under sibling workspace
> crates `skinny/crates/runtime/` and `skinny/crates/codegen/`;
> the lint glob brace-expansion is path-pattern-based, not
> structural-relation-based). A runtime-only lint would let the
> codegen-side twin re-introduce the fake header silently (the
> identical-content round-tripping vector A4 finding 15 enumerates
> between codegen-side template and runtime-side `generated.rs`).
> …"

The prose flows from the disjoint-set arithmetic directly into the
runtime-only-lint-insufficiency clause without the V2 → V3
precision-repair residue. The 8+6 decomposition at line 312 ("14
codegen-side (8 providers+templates + 6 ancillary)") already
documents the breakdown explicitly, so the V4 closing back-reference
to "the 8-vs-14 distinction" was carrying an already-stated
distinction; the deletion removes the redundancy without losing any
GENERALITY content. The lint mechanism's grammar-neutral binding to
both runtime + codegen halves remains load-bearing through the V5
prose. V4 §4.2 note CLOSED.

V5 SYNTHESIS commit diff for §2.4 confirms the two-edit packet:

```
$ git show 037eec6b6 -- restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md
…
-   with zero overlap (the two path roots are siblings).
-   The lint glob `codegen/src/**/*.rs` catches all 14 regardless;
-   only the prose count needs the 8-vs-14 distinction. A runtime-only
+   with zero overlap (the two path roots sit under sibling workspace
+   crates `skinny/crates/runtime/` and `skinny/crates/codegen/`; the
+   lint glob brace-expansion is path-pattern-based, not
+   structural-relation-based). A runtime-only
…
```

+4 / -3 LOC inside the single §2.4 paragraph; F-V5-SYNTHESIS-1a and
F-V5-SYNTHESIS-1b apply to adjacent text and land as a single diff
hunk. The deletion is clean (no dangling tokens or orphan punctuation).

### §3.2 Lock 14 baseline re-verification — A3 unchanged at V5 HEAD

V5 commit `037eec6b6` stat confirms A3 (`sk-v14-audit-overfit-lock14-
scan.md`) carries no V5 fold:

```
$ git show 037eec6b6 --stat
 .../audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)
```

Only one file touched at V5. A3 ledger holds at 30 at V5 HEAD:

```
$ grep -cE "^\| (C[0-9]+|H[0-9]+|M[0-9]+|L[0-9]+) \|" \
    restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-lock14-scan.md
30
```

Per-severity distribution preserved from V1+V2+V3+V4: 11 CRIT + 6
HIGH + 5 MED + 8 LOW = 30. H3 → L8 reclassification carries
through. The production call site `passes/src/lib.rs:478`
(`finalize_rule(&grammar.name, …)`) remains the grammar-generic
threading anchor; the test-fixture `decision_csp.rs:230-240` (under
`#[cfg(test)] mod tests`) remains the L8-reclassified violation
surface.

### §3.3 Primitive-crate generic posture re-check at V5 HEAD

Re-verification at V5 HEAD == `037eec6b6`:

```
$ grep -rn -E '\b(Json|Css|Sheets|JsonValue|CssValue|SheetsValue|CssL4|CssPretty)\b' \
    skinny/crates/bbnf-regex/src/ \
    skinny/crates/bbnf-simd/src/ \
    skinny/crates/simd-scan/src/ \
    skinny/crates/parse-that-regex/src/ \
    skinny/crates/ir/src/
[no output]
```

Still clean across all 5 primitive crates. The V5 micro-redispatch
was a single-file SYNTHESIS prose edit (commit stat confirms 1 file
modified; zero source touched). No SK-V14 commit through V5 has
introduced grammar-name leaks into the primitive layer.

### §3.4 Fake-`@generated` 56-file surface re-verification at V5 HEAD

```
$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ skinny/crates/codegen/src/ | wc -l
56

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ | wc -l
42

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/ | wc -l
14
```

Aggregate: 42 runtime-side + 14 codegen-side = 56 files (zero
overlap, sibling-workspace-crate path roots per the V5 rephrase).
The lint glob `{runtime/src/grammars,codegen/src}/**/*.rs` brace-
expansion binds both halves of the round-tripping vector. V5
SYNTHESIS §2.4:311-321 now describes this disjoint binding with
the V5 sibling-workspace-crate framing precision; the 8+6
decomposition is explicit; the residual back-reference is excised.
GENERALITY scope preserved end-to-end.

### §3.5 F-V5-SYNTHESIS-1a — "siblings" framing precision (CH4 primary; CH2 coincident)

V5 commit message identifies F-V5-SYNTHESIS-1a as the CH4 §4 note
closure (path-roots `runtime/src/grammars/` is 3-deep,
`codegen/src/` is 2-deep — not direct siblings; their PARENT
workspace crates `runtime/` + `codegen/` are). The rephrase
preserves the load-bearing disjoint-set claim; from the CH2 vantage
the rephrase preserves:

- the grammar-neutral lint-glob binding (`{runtime/src/grammars,
  codegen/src}/**/*.rs` brace-expansion);
- the load-bearing 42 + 14 = 56 disjoint-set arithmetic
  (with zero overlap);
- the explicit 8+6 codegen-side decomposition at line 312;
- the runtime-only-lint-insufficiency clause's grammar-neutral
  framing (the codegen-side twin would re-introduce the fake
  header silently in any grammar, not just JSON).

The rephrase from "the two path roots are siblings" to "the two
path roots sit under sibling workspace crates `skinny/crates/
runtime/` and `skinny/crates/codegen/`; the lint glob brace-
expansion is path-pattern-based, not structural-relation-based"
tightens the framing precision (the lint glob's binding mechanism
is now characterised by its path-pattern nature, not by an
inaccurate structural-sibling claim) without perturbing the
GENERALITY reading. CH2 confirms: no perturbation; no new CH2
finding.

### §3.6 F-V4-SYNTHESIS-2 fixture-scanner count cross-surface coherence at V5 HEAD

Re-verification at V5 HEAD == `037eec6b6`:

```
$ grep -rln 'CANONICAL_FIXTURE\|CAPTURED_W2_INPUT' \
    skinny/crates/codegen/src/
skinny/crates/codegen/src/css_l4_nested_layout_templates/generated.rs
skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_templates/generated.rs
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/generated.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/generated.rs
```

4 templates confirmed (identical to V4 §3.4 evidence; no change at
V5 HEAD). Cross-surface count consistency check at V5 HEAD: all
three SYNTHESIS surfaces (§1.1:81 in-table NEW-2 cell, §1.2:122 in-
prose decomposition, §3.1:347 C-3 row prose cell) continue to agree
on the "4" count. The fixture-lookup violation class is grammar-
neutral; cross-surface count coherence preserves the GENERALITY
scope reading.

### §3.7 Per-grammar 9-grammar census re-verification at V5 HEAD

```
$ cargo metadata --format-version 1 --no-deps 2>/dev/null | \
    jq -r '.metadata.bbnf.grammars[].ident' 2>/dev/null
bbnf
json
css_l4
css_pretty
google_sheets
ebnf
bnf
csv
math
```

9 grammars confirmed at V5 HEAD (identical to V1+V2+V3+V4
enumeration). PRUNE-4 sub-wave count = 9 across A3 + A6 + SYNTHESIS
§2.3 + §3.3. No V5 fold touched the census or the co-derivation
note; 9-grammar census preserved.

## §4 — Fresh-finding scan (V5 cycle)

V5 axis-redispatch surface is minimal: 1 file modified, +4 / -3
LOC, 2 atomic prose edits inside `SYNTHESIS-AUDIT-OVERFIT.md` that
fold into a single contiguous diff hunk at §2.4:318-321. The
fresh-finding scan focuses on (a) the V5-edited prose surface itself
and (b) the broader Lock-14 / primitive-crate / per-grammar
generality posture for any drift since V4.

### §4.1 V5-edited prose surface — no new CH2 findings

The V5 fold at SYNTHESIS §2.4:318-321 (F-V5-SYNTHESIS-1a +
F-V5-SYNTHESIS-1b) lands the V4 §4.2 cohesion polish recommendation
plus the F-V5-SYNTHESIS-1a "siblings" framing precision. The
replacement carries:

- the load-bearing disjoint-set arithmetic preserved verbatim (42
  + 14 = 56 with zero overlap);
- the grammar-neutral lint-glob binding preserved
  (`{runtime/src/grammars,codegen/src}/**/*.rs` brace-expansion);
- the 8+6 codegen-side decomposition at line 312 retained;
- the "siblings" framing tightened from inaccurate path-root direct-
  sibling claim to accurate workspace-crate sibling framing with
  the lint-glob mechanism characterised as path-pattern-based;
- the V2 → V3 precision-repair residual closing sentence excised
  (the V4 fold's explicit 8+6 decomposition already states the
  breakdown; the back-reference was redundant);
- the runtime-only-lint-insufficiency clause flowing directly from
  the disjoint-set arithmetic (cohesion restored).

CH2 GENERALITY scope: the lint glob's grammar-neutral binding to
both runtime + codegen sides is preserved as load-bearing; the
brace-expansion mechanism is named directly; the disjoint-set
arithmetic is explicit. No new CH2 finding surfaces on the V5 prose
edit.

A markdown-source wrap artefact appears at the diff boundary
(lines 321-322 read "A runtime-only / lint would let the" with a
soft wrap inside the noun phrase); this is a visual artefact of
the V5 patch landing inside an existing reflowed paragraph and
renders as continuous prose. Not a defect. The deletion is clean
(no dangling tokens, no orphan punctuation, no semantic gaps).

### §4.2 Lock 14 broader scan re-verification at V5 HEAD

Beyond the A3 ledger re-check at §3.2, the V5 fresh-finding scan
re-confirms the broader Lock-14 posture invariants:

- **Per-grammar dispatch generality:** the production call site
  `skinny/crates/passes/src/lib.rs:478` (`finalize_rule(&grammar.name,
  …)`) remains the grammar-generic threading anchor at V5 HEAD; A3
  C1..C11 cluster carries forward as PRUNE-3 + PRUNE-4 inputs.
- **Test-fixture posture:** `decision_csp.rs:230-240` under
  `#[cfg(test)] mod tests` remains the L8-reclassified violation
  surface (per V2 F-V2-A3-1 reclassification).
- **9-grammar census:** preserved at V5 HEAD per §3.7 above.
- **Pattern H 67-file census (A6 CRIT-1):** preserved at V5 HEAD
  per V5 commit stat (A6 file unchanged).
- **48-file Pattern H sub-census (A6 CRIT-2):** preserved at V5
  HEAD per V5 commit stat (A6 file unchanged).
- **Fake-`@generated` 56-file surface:** preserved at V5 HEAD per
  §3.4 above (42 + 14 disjoint).
- **Primitive-crate generic posture:** preserved at V5 HEAD per
  §3.3 above (zero leaks across 5 crates).
- **Fixture-scanner 4-count cross-surface coherence:** preserved
  at V5 HEAD per §3.6 above.

No new CH2 GENERALITY findings surface under the V5 fresh-finding
scan.

### §4.3 Two-consecutive-cycle convergence chain — CH2 self-attestation

V3 CH2 disposition: 8 ACCEPT-equivalent (7 ACCEPT + 1 ACCEPT-with-
NOTE upgraded under V4) / 8 = 100 % effective (with one V3-deferred
editorial sub-defect later retired by V4).
V4 CH2 disposition: 8 ACCEPT / 8 = 100 % (V3 N-V3-CH2-1 closed by
F-V4-SYNTHESIS-1; F-V4-SYNTHESIS-2 closed V3 CH7 §1.5 cross-flag).
V5 CH2 disposition: **8 ACCEPT / 8 = 100 %** (V4 §4.2 cohesion
polish note closed by F-V5-SYNTHESIS-1b; F-V5-SYNTHESIS-1a verified
as CH4 primary with CH2 coincident-only confirmation).

§3Z two-consecutive-cycle ≥ 95 % criterion satisfied for CH2: V4
100 % + V5 100 %. No NEW orphan REVISEs at V5 for CH2; no carry-
forward defects; no fresh findings.

## §5 — V5 convergence forecast

V5 closes the V4 §4.2 cohesion polish note cleanly via F-V5-
SYNTHESIS-1b; V5 CH2 disposition is 100 % ACCEPT (8 ACCEPT + 0
ACCEPT-with-NOTE / 8 = 100 %); the §3Z two-consecutive-cycle ≥ 95
% convergence chain for CH2 specifically is satisfied (V4 100 %
+ V5 100 %). G-S-P0-CONVERGED aggregate gating depends on the
other six lenses' V5 disposition; CH2 carries no outstanding
findings into the convergence aggregate.

V5 is the V max=5 ceiling cycle per `restart/prompts/skinny/PASS-0-
OVERFIT-AUDIT.md §Procedure step 4`. No further CH2-territory
optional folds remain; the V5 SYNTHESIS surface reads cohesively
end-to-end on the §2.4 fake-`@generated`-lint paragraph. Per the
SK-V14 ORCHESTRATOR-PROMPT THE SK LOOP, G-S-P0-CONVERGED gates
S-P1 dispatch (Task #3).

## §6 — Disposition

**CH2 V5 VERDICT: ACCEPT.** F-V5-SYNTHESIS-1b closes V4 §4.2
cohesion polish note at SYNTHESIS §2.4:319-321 cleanly: the
residual "8-vs-14 distinction" closing sentence has been excised
(both target phrases "8-vs-14" and "catches all 14 regardless"
absent from the V5 SYNTHESIS surface per §3.1 grep); the V5 prose
now flows from the disjoint-set arithmetic directly into the
runtime-only-lint-insufficiency clause; the 8+6 codegen-side
decomposition at line 312 retains the breakdown the deleted
back-reference was pointing at, so no GENERALITY content is lost.

F-V5-SYNTHESIS-1a (the CH4 §4 "siblings" framing precision) is
CH4 primary; CH2 confirms only that the rephrase preserves the
load-bearing disjoint-set arithmetic, the grammar-neutral lint-glob
binding, and the path-pattern-based brace-expansion mechanism.
No CH2-specific disposition impact.

Lock 14 baseline holds — A3 ledger preserved at 30 violations
verbatim (V5 commit stat confirms A3 file unchanged); per-grammar
census (9 grammars) preserved at V5 HEAD; primitive-crate generic
posture preserved (zero name leaks across 5 crates); Pattern H 67-
and 48-file censuses preserved (V5 commit stat confirms A6 file
unchanged); fake-`@generated` 56-file surface preserved with the
disjoint-set arithmetic explicit and the §2.4 prose now cohesive
end-to-end (no V2 → V3 precision-repair residue); fixture-scanner
4-count cross-surface coherence preserved (per §3.6).

No new CH2 GENERALITY findings surface under the V5 fresh-finding
scan. The V5 §2.4 prose is the cleanest the paragraph has read
across the V1 → V5 chain — load-bearing arithmetic + mechanism +
decomposition all explicit, residual back-references retired,
framing precision tightened.

V5 CH2 ACCEPT-rate: **8/8 = 100 %** (8 ACCEPT + 0 ACCEPT-with-NOTE
+ 0 REVISE + 0 REJECT). §3Z V5 ≥ 95 % satisfied for CH2; V4 + V5
both at 100 % satisfies the two-consecutive-cycle ≥ 95 % criterion
for CH2 specifically (G-S-P0-CONVERGED aggregate gating depends on
the other six lenses' V5 disposition).

---

**Authored:** 2026-05-23 (SK-V14 S-P0 CHALLENGE V5 lens CH2 —
GENERALITY; V max=5 ceiling cycle).
**Status:** WRITE-ONLY; aggregator commits all 8 V5 hardening files
atomically with `docs(sk-v14-audit-overfit-hardening-V5): challenge
V5 + consolidated`.
**Authority:** `restart/prompts/ORCHESTRATOR.md §3W` CH2 row;
V4 CH2 base at `restart/skinny/tranches/sk-v14/audit-overfit/
hardening/V4/CH2.md` §4.2 + §5 V5 optional-fold recommendation; V1
CHALLENGE-CONTEXT `restart/skinny/tranches/sk-v14/audit-overfit/
hardening/V1/CHALLENGE-CONTEXT.md §3` per-lens focus row; V4
CONSOLIDATED §2.2 + §2.3 Option B recommendation
(F-V5-SYNTHESIS-1{a,b} prescriptions); V5 micro-redispatch commit
`037eec6b6`.
