# SK-V18 S-P3 V5-independent CHALLENGE — SECTION-6-HONESTY (clean re-validation)

Lens: are the §6 named primitives (CSS balanced scan, JSON leaf kernels) gated
(a) grammar-invoked + (b) varies-under-mutation + (c) `verbatim_blob_present==false` +
(d) profile-proven-narrow-leaf-with-walk-derived-skeleton — so the escape cannot admit a
relabeled blob? Is the `balanced_component_scan` neutrality-proof obligation present and
genuine? Independent re-validation (these were orchestrator-applied in S-P2 V2 under infra
load; this is the clean independent pass — NOT a fold-confirm).

Target: `restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md` (§1 R-B/R-C/R-E
cells, §3 G1/G2/G5/G6/PROVE exit-gates, §4 the four §6 surfaces + the four-part escape gate,
§5 risks 2/3/6). Disk spot-checks taken THIS pass (defect suspected on the neutrality
obligation — confirmed it is a real, possibly-undischargeable obligation, see C4):
`css_l4_declaration_values/generated.rs:657-680` (`find_component_delim(&self, pos, delimiters: &[u8])`),
`:693-713` (`consume_balanced_at` — recursive `()[]{}` skip + string/comment skip),
`:493/:512/:550/:585/:687` (call sites passing `b";{}"`/`b"{};"`/`b";}"`/`b":{};"` literal byte-set ARGS),
`json/generated.rs:50/510/801/954` (`b'-' | b'0'..=b'9'` digit-class dispatch),
`:164` (`match_tiny_plain_string_direct`), `:823-859`/`:863-892`
(`parse_object_value_at_direct`/`parse_array_element_at_direct` — `b'{'`/`b'['` dispatch to
`parse_object_direct`/`parse_array_direct`, a PARSE-with-`sink.*`-emit, NOT a byte-skip),
`grammar/google-sheets/google-sheets.bbnf:137` (`paren_expr = "(" , expression ?w , ")"` —
full recursive parse-descent into `expression`).

---

## Claims under the SECTION-6-HONESTY lens

### C1 [ACCEPT] — the four-part escape gate (a)-(b)-(c)-(d) is normatively present and binds BOTH named §6 findings
§4 lines 259-266 define the escape as admissible ONLY under (a) grammar-INVOKED-by-name +
(b) emitted-output-VARIES-under-invoking-rule-mutation + (c) `verbatim_blob_present == false`
+ (d) PROFILE-PROVEN-NARROW-LEAF "the primitive covers a SINGLE hot leaf attributable to a
named S-P1-profile hot leaf … and the surrounding structural SKELETON MUST be walk-derived",
and states explicitly "A primitive failing any of the four is a relabeled hand-written blob =
REJECT." (d) carries its own machine-check ("machine-checkable as primitive LOC vs the
profiled hot-leaf extent") and is correctly distinguished from (a)-(c): "(a)-(b)-(c) prove the
primitive is grammar-coupled; (d) bounds its SIZE so the escape cannot admit an arbitrarily
large relabeled blob that merely varies under mutation." This is the exact closure that stops
the size-laundering attack (a blob that varies under mutation but spans a whole rule body).
The four parts are all present, all machine-grounded, none is prose-only. ACCEPT.

### C2 [ACCEPT] — the CSS balanced-scan PRIMARY finding is a genuinely narrow profile-proven leaf with a walk-derived skeleton
§4 line 222 anchors it to the 94.1% hot leaf (`find_component_delim` + `consume_balanced_at`),
matching S-P1 §3 (79.5% + 14.6% = 94.1%). Disk confirms the on-the-ground shape the synthesis
claims: `find_component_delim` (`:657-680`) takes `delimiters: &[u8]` as a parameter, and the
five call sites (`:493/:512/:550/:585/:687`) each pass a DISTINCT delimiter byte-set literal
(`b";{}"`, `b"{};"`, `b";}"`, `b":{};"`) — so the byte-set IS already a caller-supplied
argument, not a hardcoded constant inside the recognizer. The recognizer+balanced-skip pair is
~57 LOC (`:657-713`), a narrow leaf, NOT a rule-spanning body. The SKELETON the synthesis
commits to walk-deriving is exactly the per-rule call sites (which delimiter set each rule
scans for) — those are emergent from the rule shapes and are correctly assigned to the walk.
(d) is satisfied: narrow leaf + walk-derived skeleton. (a) is structurally evidenced (the call
is by name with a grammar-derived ARG). ACCEPT.

### C3 [ACCEPT] — the (b) varies-under-mutation falsifier is concretely specified for BOTH findings, not asserted
For CSS, §3 G2 exit (line 169) binds "the named `balanced_component_scan` primitive passes the
per-primitive (a)-(c) mutate-falsifier (mutate the invoking `.bbnf` rule → emitted ARG byte
sets change)" — a concrete, runnable falsifier keyed to the ARG byte-set (which disk confirms
IS the variable surface). For JSON, §4 lines 244-248 give the SHARPEST form of (b): "widen the
`number` rule's digit class in the `.bbnf` → the `b'0'..=b'9'` literal in the emitted kernel
widens; a kernel that does NOT vary under its own rule's class mutation is a relabeled fixed
courier even though the surrounding skeleton varies (the byte-equivalence gate alone is
satisfiable by routing the SAME literal through the new walk, so (b) is what distinguishes a
derived leaf from a relabeled one)." Disk confirms the literal under test is real and
mutation-bearing (`b'-' | b'0'..=b'9'` at `json/generated.rs:50/510/801/954`). This is the
load-bearing insight: byte-equivalence (G1's diff-control gate) is NECESSARY-NOT-SUFFICIENT,
and (b) is the orthogonal check that the literal is DERIVED not ROUTED. Correctly captured.
ACCEPT.

### C4 [REVISE] — the neutrality-proof obligation is PRESENT and honest, but its two offered non-CSS candidates are both PARSE-with-emit, not balanced-SKIP, so the obligation is structurally UNDISCHARGEABLE by them and the synthesis must say the demotion is the EXPECTED outcome (not a coin-flip fallback)
§4 lines 231-237 carry the CH6-folded NEUTRALITY-PROOF obligation: `balanced_component_scan`
is "named neutrally but is exercised ONLY by CSS in this campaign"; it correctly DECOMPOSES the
primitive — "Its inner alphabet-scan sub-kernel (the `bbnf-simd` eq-set member scan) is
genuinely neutral (caller-supplied byte set), but the balanced-recognizer SHELL must be PROVEN
neutral by at least one NON-CSS invocation … the JSON object/array balanced `{}`/`[]` nesting
OR the Sheets `paren_expr` balancing must invoke the SAME primitive — ELSE it is demoted to an
honestly CSS-scoped name (`css_balanced_component_scan`)". The obligation is genuine and the
decomposition (neutral inner kernel vs CSS-only shell) is exactly right.

The DEFECT: the two offered non-CSS dischargers cannot, on the disk evidence, invoke the SAME
shell. `consume_balanced_at` (`generated.rs:693-713`) is a balanced byte-SKIP: it advances
`pos` past nested `()[]{}` WITHOUT parsing the interior and emits NOTHING. But both offered
candidates are full recursive PARSE-with-emit, structurally a different operation:
- JSON `{}`/`[]` nesting (`json/generated.rs:833-834`) dispatches `b'{' => parse_object_direct`
  / `b'[' => parse_array_direct`, each of which descends and emits `sink.object_*`/`sink.array_*`
  — it PARSES the interior, it does not skip it.
- Sheets `paren_expr = "(" , expression ?w , ")"` (`google-sheets.bbnf:137`) descends into
  `expression` — also a parse, not a skip.
A skip-recognizer and a parse-descent share a depth-tracking control SHAPE but not a callable
body: the parser must visit/emit every interior token; the skipper must NOT. They cannot be the
SAME primitive without one of them paying the other's cost (a skipper forced to emit, or a
parser forced to discard — the latter is exactly the >SOTA-regressing tree-walk §4 line 226
forbids). So on present evidence the obligation's "OR invoke the SAME primitive" branch is
UNREACHABLE, and the demotion to `css_balanced_component_scan` is not a 50/50 fallback — it is
the FORCED outcome. EDIT (`SYNTHESIS-RESEARCH.md` §4 R-B bullet, the sentence at lines 234-236):
after "the JSON object/array balanced `{}`/`[]` nesting OR the Sheets `paren_expr` balancing
must invoke the SAME primitive", insert: "— noting that both are PARSE-with-emit descents while
`consume_balanced_at` is a byte-SKIP recognizer (`generated.rs:693-713` emits nothing), so a
shared body requires the skip-shell to be the genuinely-shared substrate (NOT the parse
descent); if no non-CSS grammar exercises the SKIP semantics, the demotion to
`css_balanced_component_scan` is the EXPECTED outcome, not a contingency —". This converts a
latent neutral-name overfit-in-waiting (a primitive that passes the obligation only because the
obligation's dischargers were never structurally checked) into an honest forced demotion, and
removes the risk that G2 ships a neutrally-named CSS-only shell by routing JSON's parse-nesting
through a degenerate "balanced" wrapper that does no real work. REVISE (the obligation is
present and correctly shaped; its discharge analysis is incomplete and would, unrevised, admit
a false-neutral name under a green obligation gate).

### C5 [ACCEPT] — the Sheets precedence-tower §6 CANDIDATE is correctly held to (a)-(d) and is NOT pre-admitted as a primitive
§4 lines 249-255 mark the 7-level precedence tower as "a §6 CANDIDATE, not yet realized": it is
right-iterated EBNF (`A = B (op B)*`) lowering to the EXISTING `SinkOnlyExpr` vocabulary
(`Seq`+`RepeatLoop`+`Alt{Dispatch}`+`CallRule`), "so it needs NO new IR primitive." The
synthesis does NOT relabel the tower as a hand-shaped primitive — it stresses G3's GENERALITY
("does the body come from the grammar?") and admits a §6 primitive ONLY conditionally ("IF G3
cannot render recursive `CallRule`/`RepeatLoop` chains from grammar structure, the tower breaks
first = a §6 finding surfaces"). This is the honest posture: it does not pre-purchase an escape
it has not earned, and it ties the PROVE make-or-break to the generality test rather than to a
hand-written tower. Consistent with §5-risk-5 (the un-forked G3 emitter does not yet exist; if
Sheets cannot emit via the generator ONLY, "surface honestly, do NOT stub-prove, do NOT
hand-write a `_GENERATED_RS` Sheets block"). ACCEPT.

### C6 [ACCEPT] — `verbatim_blob_present == false` (c) is bound as a per-wave EXIT gate at every emitter wave, so the no-verbatim-blob conjunct is enforced, not aspirational
(c) is not left to the §4 definition alone — it is a hard exit-gate: G1 (line 166)
"`verbatim_blob_present == false` (both the SinkOnly literals AND the parse-only `_RS` courier
folded)"; G2 (line 168) "`verbatim_blob_present == false`, `CSS_GENERATED_RS` grep == 0"; PROVE
(line 201) "no `const.*_RS.*r#` Sheets blob". Disk confirms the live verbatim surfaces the gate
must clear still exist (the S-P0 census: `CSS_GENERATED_RS` const courier, and the JSON
`parse_w11_1_number_*` metalang-leaked names at `json/generated.rs:801/841/881` that P5 retires)
— so the gate fires on real surfaces, not a cleared field. The (c) conjunct of the §6 escape is
therefore wired to a falsifiable per-wave check, and the §6 primitives inherit it directly (a
primitive that is a verbatim `&str` fails (c) at its own wave's exit). ACCEPT.

### C7 [ACCEPT] — the §6 escape cannot be used to launder the >SOTA-regressing tree-walk, because (d)'s walk-derived-skeleton requirement forces the COMBINATOR descent OUT of the primitive
The single most dangerous §6 abuse is the inverse of size-laundering: declaring the WHOLE flat
scan a "primitive" to avoid the grammar-walk having to derive it — which would let the campaign
keep the hand-written scan verbatim under a neutral name. (d)'s "the surrounding structural
SKELETON MUST be walk-derived" + the "primitive spanning a rule's whole body or an unprofiled
region is a REJECT regardless of (a)-(b)-(c)" (lines 262-264) is exactly the clause that forbids
this: the primitive is bounded to the profiled hot LEAF (the inner inert-run skip + the
delimiter test), while the per-rule call structure (which delimiters, in which rule context)
MUST come from the walk. §4 line 226 independently names the failure mode the gate guards
against ("A naive grammar-walk lowering produces the combinator-shaped recursive descent
(lightningcss's own architecture) that categorically regresses >SOTA") — and the resolution is
NOT "make the whole thing a primitive" but "name the narrow scan, parameterize by
grammar-derived byte sets, INVOKE it from the emitted scan." The escape is correctly bounded on
BOTH sides — too-large (d-REJECT) and too-routed (b-REJECT). ACCEPT.

---

## Net

The §6-honesty surface is, independently, SOUND on six of seven claims. The four-part escape
gate (a)-(b)-(c)-(d) is normatively present, machine-grounded, bound to per-wave EXIT gates
(c at G1/G2/PROVE), and bounded on both the size axis (d) and the routing axis (b) so it can
admit neither an oversized relabeled blob nor a same-literal-routed-through-a-new-walk courier.
Both named §6 findings (CSS balanced scan PRIMARY, JSON leaf kernels SECONDARY) are anchored to
real S-P1 profile hot leaves verified on disk, with narrow leaf extents and walk-derived
skeletons. The Sheets tower is correctly held as an unrealized CANDIDATE, not a pre-purchased
primitive.

The ONE defect (C4): the `balanced_component_scan` neutrality-proof obligation is PRESENT and
correctly decomposed (neutral inner eq-set kernel vs CSS-only balanced shell), but its two
offered non-CSS dischargers — JSON `{}`/`[]` nesting and Sheets `paren_expr` — are both
PARSE-with-emit descents, whereas the CSS shell is a byte-SKIP recognizer that emits nothing
(`consume_balanced_at`, `generated.rs:693-713`). They are structurally incompatible operations,
so the obligation's "invoke the SAME primitive" branch is unreachable by the candidates the
synthesis names, and the demotion to `css_balanced_component_scan` is the FORCED — not
contingent — outcome. Unrevised, this risks G2 shipping a neutrally-named CSS-only shell behind
a green obligation gate (the exact overfit-in-waiting CH6 raised). The REVISE is a single
inserted clause in the §4 R-B bullet making the parse-vs-skip distinction explicit and naming
demotion as expected.

TALLY accept=6 revise=1 reject=0
