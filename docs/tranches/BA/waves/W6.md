# BA.W6 — BA Close

**Thesis** (the 13-wave foundation closes; the lock cross-reference table is verified — Lock 1 honoured at BA close per option (a), not deferred; the per-grammar generated-LOC table is emitted with all-grammar OpenFrame retiral evidence; the carry ledger to BB is named explicitly with per-grammar OpenFrame migration receivers RETIRED; PROGRESS.md and FINAL.md land). **Closer-gate** (`cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis` is 100% pass for BA-owned surfaces; `crates/ir/src/passes/tests/substrate_audit.rs` green; `rg -n 'enum OpenFrame' crates/core/src/runtime/` returns 0 across all nine grammars; per `audit/HARDENING-SYNTHESIS-2026-05-03.md:204` the BA-amendment punch-list closes).

## §1 — Deliverable

Hereupon BA close — the foundation tranche having delivered, in 13 waves (W0, W1, W2, W3a, W3b, W3c, W4a, W4b, W4c, W5a, W5b, W5c, W5d, W5e, W6), the layered re-org of `crates/core/src/`, the grammar-leak excision from `bbnf-ir`, the 23 god-module splits and layout-lowering rename, the path-crate triplet consolidation, the cursor + byte-skip unification, and the all-grammar direct-to-struct codegen migration (W5a JSON → W5b CSS L4 → W5c BBNF → W5d Sheets → W5e cohort). W6 is the close ceremony: PROGRESS.md and FINAL.md document landings; the lock cross-reference table at BA.md §13-Lock honoured is verified cell-by-cell — Lock 1 is **honoured at BA close** per option (a); the per-grammar generated-LOC table is emitted with post-BA actuals (~150,900 LOC; −10.7% from 168,750 pre-BA); the carry ledger to BB is named explicitly per the renumbered BA→BB.C1' (cohort hand-written → BB.W2 template-consolidation only) + BA→BB.C2..C5 + deferred-with-receiver Locks 4/10/11.

The substrate audit green-gate verifies. Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43` ("Lane 05 confirms substrate-audit still fails with 32 zero-caller substrates") and the routed-carry table at line 226, the BA close requires `crates/ir/src/passes/tests/substrate_audit.rs` to be green. The path to green: BA.W0 deleted `generate/serialize/` (~156 LOC zero-caller), BA.W3 deleted the legacy `runtime/path.rs` (163 LOC zero-caller after migration) and the four `runtime/<g>/parse_with.rs` files (~480 LOC), BA.W5a deleted JSON `OpenFrame`, BA.W5b deleted CSS L4's 14-variant `OpenFrame`, BA.W5c deleted BBNF's `OpenFrame`, BA.W5d deleted Sheets' `OpenFrame`, BA.W5e deleted the cohort's `OpenFrame` plus the `SimpleStructBuilder` template plus `EmitStrategy::OpenFrame` resolver variant. The 32-substrate count at BA.W6 close should be zero (or each row whitelisted with rationale).

The performance gates verify. BA-G1a (twitter ≤ 400 µs) closed at BA.W5a.M4; BA-G1b (CSS L4 bootstrap regression-bound) at W5b.M4; BA-G1c (BBNF self-host roundtrip) at W5c.M5; BA-G1d (Sheets parse_simple) at W5d.M5; BA-G1e (cohort regression-bound) at W5e.M5; BA-G2 (≤ 2 heap allocs) closed at BA.W5a.M5; BA-G3 (xtask regen ≤ 30 s, CSS L4 compile_paths_request ≤ 25 s — Lane 04 baseline 59.98 s + 52.53 s; halving) verified by post-BA timing at W6.M2; BA-G9 (Document::get<T> ≤ 5×) closed at BA.W4.M2. The BA close re-runs each gate's measurement and confirms the close-condition. Per `feedback_bench_single_run`, the bench is single-invocation; per `feedback_no_warm_benches`, cold per-parse only.

The lock cross-reference table cell-by-cell verification. Per BA.md §13-Lock honoured table, each lock has a wave that addresses it: L1 (W0 + W5a + W5b + W5c + W5d + W5e — **honoured at BA close** per option (a)), L2 (W2), L3 (W4a/b/c), L4 (deferred to BB.W3 — ratifiable), L5 (W5a..W5e demonstrate the pattern across all 9 grammars), L6 (W0), L7 (W3a/b/c), L8 (G1a closed; G1b/c/d/e are internal-progress non-SOTA per surgery #11), L9 (W5a..W5e slice-borrow per grammar), L10 (deferred to BB.W3 — ratifiable; BBNF Pratt at W5c + Math Pratt at W5e are per-grammar precursors), L11 (deferred to BB.W0 — ratifiable), L12 (Pre-BA ceremony), L13 (W0+W2+W3+W5b). Each cell's "Notes" column flags weak adherence or carry-deferral; W6 verifies no cell carries a fault. Per `feedback_no_deferrals`, deferred locks must point to a real receiving wave with a gate; the BB.W3 / BB.W0 deferrals are ratifiable per the BA→BB carry-tag table.

The carry ledger to BB is named explicitly per surgery #24. Per BA.md §Carry-tags TO BB and option (a) per `docs/tranches/BA/audit/W5-substrate-identity-decision.md`, the per-grammar OpenFrame migration receivers retire (BA owns it):

- BA→BB.C1' (owner BA.W5e; receiver BB.W2): cohort hand-written direct-to-struct → BB.W2 cohort template-consolidation. The five hand-written W5e modules (BNF, CSV, EBNF, CSS Pretty, Math) are absorbed into one parameterised cohort template per gap D (`docs/tranches/BB/audit/W2-cohort-template-spec.md`); receiving gate is BB.W2 cohort template byte-equal to W5e hand-written modules at first commit.
- BA→BB.C2 (owner BA.W2): Layout-lowering rename canonised; BB references `Layout`/`LayoutSink` only.
- BA→BB.C3 (owner BA.W4b): Cursor-unified `parse_with` + `__EAGER_EMPTY_PATH` substrate; already met at BA close — every grammar carries `pub fn parse_with` (verified at W4b).
- BA→BB.C4 (owner BA.W3b): `path-core` crate exists; BB.W5's `pointer!` macro reuses without proc-macro/cdylib mirror.
- BA→BB.C5 (owner BA.W1): Grammar-agnostic `bbnf-ir`; BB.W3's CSP/e-graph/miner extensions reference grammars only via `&str` ident through workspace metadata.
- BA→BB.C6/C7/C8 (deferred locks 4/10/11; receivers BB.W3/BB.W3/BB.W0).

Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228` (the routed-carry table), W6 also closes routed carries: the F4 Tailwind regex_scan timeout (W5b CSS L4 disposition: profile under direct-projection — if not closed, route to BB.W3 per `audit/HARDENING-SYNTHESIS-2026-05-03.md:84-87`); the 32 zero-caller substrates (BA.W0+W3+W5a..e deletions); generated-size budgets (BA.W2 + BA.W5a/b/c/d/e artefacts).

The Era V failure mode is closed. Per `docs/tranches/meta-audit/archaeology/era-V-dta-psi-rut.md:7-10`, "Era V's signature failure mode is **substrate-first-consumer-later**: every tranche ships the compile-time emission of constants, tables, and shape dictionaries; no tranche fully activates the runtime consumer that reads them." BA does not repeat: every wave (W0 layered re-org → W1 grammar-leak → W2 splits → W3 path triplet → W4 cursor unification → W5 JSON direct-to-struct) lands a substrate WITH its consumer in the SAME wave. W6 is the verification — the BA close confirms each wave's substrate has been consumed.

The PROGRESS.md and FINAL.md voice is archaic-permissive per `feedback_archaic_diction`. Hereupon BA closes; the foundation tranche having delivered, in 13 waves, the layered re-org, the grammar-leak excision, the god-module splits, the path-crate consolidation, the cursor unification, and the all-grammar direct-to-struct codegen migration (W5a JSON → W5b CSS L4 → W5c BBNF → W5d Sheets → W5e cohort). The substrate is the typed-enum + slice-borrow across all nine grammars per Lock 9; the demonstration progresses through five sub-waves; the carry to BB is the renamed canonical surface (Layout/LayoutSink), the cursor-unified parse, the consolidated path-crate triplet, the grammar-agnostic IR, the all-grammar direct-to-struct emitter foundation, and the cohort hand-written modules awaiting consolidation. BA verifies the 13 locks in 13 waves and hands BB a ratified foundation per the BA.md §Closing posture.

The post-BA performance trajectory is favourable. Per BA-G1 (twitter ≤ 400 µs, beating sonic-rs's 436 µs by ≥ 8%), BA closes one row of the SOTA matrix; BB.W3-W6 closes more (BB-G1 bootstrap ≤ 3.5 ms beating lightningcss 4.16 ms by ≥ 15%; BB-G3 citm ≤ 800 µs beating sonic-rs 854 µs by ≥ 6%; BB-G4 canada ≤ 3.0 ms beating sonic-rs 3.144 ms by ≥ 4.5%); BC.W6 tightens further (BC-G1 twitter ≤ 380 µs beating sonic-rs by ≥ 13%; BC-G2 bootstrap ≤ 3.0 ms beating lightningcss by ≥ 27%; BC-G3 canada ≤ 2.8 ms beating sonic-rs by ≥ 11%). The BA close hands BB a working JSON parser at sonic-rs-parity-or-better; BB extends across the remaining grammars; BC tightens.

## §2 — Milestones

> **M0 — PROGRESS.md emission**
>
> *Surface*: `docs/tranches/BA/PROGRESS.md` (new file).
> *Action*: Emit a per-wave summary: each wave's deliverable, the gate it closed, the milestone-by-milestone landings, the artefact paths (`docs/tranches/BA/audit/W0-toolchain-baseline.md`, `W2-generated-size-budget.md`, `W2-arena-builder-disposition.md`, `W5a-json-direct-to-struct.md`, `W5b-css-l4-direct-to-struct.md`, `W5c-bbnf-direct-to-struct.md`, `W5d-google-sheets-direct-to-struct.md`, `W5e-cohort-direct-to-struct.md`, etc.). Per `feedback_no_metalanguage_docs`, no commit references; cite path:line only. Per `feedback_archaic_diction`, archaic-permissive voice ("hereupon", "begotten", "thereof").
> *Gate*: `docs/tranches/BA/PROGRESS.md` exists; the 13-wave landings are documented (W0 + W1 + W2 + W3a/b/c + W4a/b/c + W5a/b/c/d/e + W6).
> *Exit-criteria*: `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/PROGRESS.md && wc -l /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/PROGRESS.md | awk '{print ($1 > 100)}' | tr -d '\n'` returns `1`.

> **M1 — Lock cross-reference table verification**
>
> *Surface*: BA.md §13-Lock honoured (lines 76-90); per-lock wave-owner cells.
> *Action*: For each of the 13 locks, verify the wave-owner cell honours the lock per the closer-gate from that wave. Lock 1 (W0 residue scrub + W5a JSON + W5b CSS L4 + W5c BBNF + W5d Sheets + W5e cohort OpenFrame retiral) — `rg -n 'TapeRec\|TapeCursor\|TapeBuilder\|TapeOffset' crates/*/src/` returns zero outside `tests/`; **`rg -n 'enum OpenFrame' crates/core/src/runtime/` returns zero across all nine grammars (Lock 1 honoured at BA close per option (a))**; `rg -n 'EmitStrategy::OpenFrame' crates/ir/src/registry/strategy.rs` returns zero. Lock 2 (W2 rename) — `rg -nE 'type_projection|type_collapsing|StructLayout|schema_synthesis' crates/*/src/ | rg -v 'pub use'` returns zero. Lock 3 (W4 unification) — `rg -n 'pub fn parse_with' crates/core/src/grammar/generated/` returns ≥ 9. Lock 7 (W3 path triplet) — `find crates -maxdepth 1 -type d -name 'path*' | wc -l` returns 3. Lock 12 (Pre-BA ceremony) — `archive/{ser,gorgeous}/` exist; `Cargo.toml` workspace members reduced by 2. Lock 13 (W0+W2+W3+W5b CSS L4 14-variant split consumed) — `find crates -name '*.rs' ! -path '*/generated/*' | xargs wc -l | awk '$1>500'` returns empty.
> *Gate*: every cell in the §13-Lock honoured table verified; no cell carries a fault.
> *Exit-criteria*: A verification artefact at `docs/tranches/BA/audit/W6-lock-verification.md` records each lock's verification command + output; `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/audit/W6-lock-verification.md`.

> **M2 — BA-G3 close: `cargo xtask regen --check` ≤ 30 s; CSS L4 `compile_paths_request` ≤ 25 s**
>
> *Surface*: `cargo xtask regen --check` runtime; `crates/core/src/pipeline/compile/` (post-W2 split per `audit/MODULES-2026-05-03.md:696-702`).
> *Action*: Run `time cargo xtask regen --check` post-BA; verify wall ≤ 30 s. Run the CSS L4 `compile_paths_request` benchmark; verify wall ≤ 25 s. Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`, Lane 04 measured 59.98 s + 52.53 s as pre-BA baseline; halving is the BA close requirement. The W2 god-module splits and W4 cursor unification reduce per-pass IR walking cost; the W5 direct-to-struct emit reduces emit-stage cost for JSON. Per BA.md §Build/iter time gate (line 102-106), the halving is the BA close gate.
> *Gate*: regen ≤ 30 s; compile_paths_request ≤ 25 s.
> *Exit-criteria*: `time cargo xtask regen --check 2>&1 | grep real | awk '{print $2}'` returns ≤ 30 s; CSS L4 compile_paths_request artefact records ≤ 25 s.

> **M3 — Per-grammar generated-LOC table emission**
>
> *Surface*: BA.md §Generated-LOC budget table (lines 110-121); per-grammar `crates/core/src/grammar/generated/<g>.rs` LOC.
> *Action*: For each of nine grammars, measure post-BA `wc -l` and verify against the BA.md §Generated-LOC budget table target column under option (a): `json.rs` ≤ 2,200 (−40%; W5a), `bbnf.rs` ≤ 19,000 (−12%; W5c), `css_l4.rs` ≤ 100,000 (−7%; W5b), `google_sheets.rs` ≤ 12,000 (−15%; W5d), `css_pretty.rs` ≤ 8,500 (−6%; W5e), `ebnf.rs` ≤ 7,000 (−8%; W5e), `bnf.rs` ≤ 3,000 (−9%; W5e), `csv.rs` ≤ 1,500 (−11%; W5e), `math.rs` ≤ 800 (−8%; W5e). Total target ~150,900 (−10.7%). Emit `docs/tranches/BA/audit/W6-generated-loc-budget.md` recording the actuals.
> *Gate*: total post-BA LOC ≤ 150,900 (or with documented overflow rationale per BA-G10).
> *Exit-criteria*: `for g in json bbnf css_l4 google_sheets css_pretty ebnf bnf csv math; do wc -l /Users/mkbabb/Programming/bbnf-lang/crates/core/src/grammar/generated/$g.rs; done | awk '{sum+=$1} END {print (sum <= 150900)}' | tr -d '\n'` returns `1`.

> **M4 — Carry ledger to BB.W0 named explicitly**
>
> *Surface*: BA.md §Carry-tags TO BB (lines 57-64); BA.md §Carry-tags TO BC (lines 65-70); routed-carries from `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228`.
> *Action*: Emit a carry-ledger artefact at `docs/tranches/BA/audit/W6-carry-ledger.md` enumerating each carry-tag, its owner wave (BA.W?), the description, the receiving wave (BB.W? or BC.W?), the close-criterion. Per the BA.md §Carry-tags TO BB table under option (a):
> - BA→BB.C1' (BA.W5e; receiving BB.W2): cohort hand-written direct-to-struct → BB.W2 cohort template-consolidation; receiving gate is BB.W2 cohort template byte-equal to W5e hand-written modules.
> - BA→BB.C2 (BA.W2; receiving BB references Layout/LayoutSink only): layout-lowering canon.
> - BA→BB.C3 (BA.W4b; already met at BA close): cursor-unified parse_with for all grammars; receiving gate `rg -n 'pub fn parse_with' crates/core/src/grammar/generated/` returns ≥ 9.
> - BA→BB.C4 (BA.W3b; receiving BB.W5): pointer! macro at crates/path/.
> - BA→BB.C5 (BA.W1; receiving BB.W3): grammar-agnostic bbnf-ir for CSP/e-graph/miner.
> Per the BA.md §Carry-tags TO BC table:
> - BA→BC.C1 (BA.W2; receiving BC.W0): layout-lowering canon supports IR contract spec.
> - BA→BC.C2 (BA.W5a..W5e; receiving BC IR contract formalisation): direct-to-struct emitter pattern across all 9 grammars.
> *Gate*: artefact exists; every carry-tag named with owner + receiver + close-criterion.
> *Exit-criteria*: `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/audit/W6-carry-ledger.md && rg -n 'BA→BB\.(C1.*|C[2-5])' /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/audit/W6-carry-ledger.md | wc -l | tr -d '\n'` returns ≥ 5.

> **M5 — `substrate_audit` test green**
>
> *Surface*: `crates/ir/src/passes/tests/substrate_audit.rs` (per BA-G4 cite at BA.md line 18).
> *Action*: Run `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)'`; verify green. The test's pre-BA failure (32 zero-caller substrates per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`) closes via BA.W0/W3 deletions, BA.W1's grammar-leak excision, and BA.W5a..W5e's all-grammar OpenFrame retiral (including the `EmitStrategy::OpenFrame` resolver variant, the `SimpleStructBuilder` template, the BBNF aggregator, the per-grammar checkpoint surfaces). Each row in the substrate-audit's input either deletes (W0/W3/W5a..e) or whitelists with consumer rationale (BA.W6 close artefact).
> *Gate*: substrate_audit test passes; zero-caller count is zero (or each row whitelisted).
> *Exit-criteria*: `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)' --profile ax-iter 2>&1 | rg 'test result: ok' | wc -l | tr -d '\n'` returns ≥ 1.

> **M6 — FINAL.md emission**
>
> *Surface*: `docs/tranches/BA/FINAL.md` (new file).
> *Action*: Emit the BA close document. Per `feedback_archaic_diction`, archaic-permissive voice. Per `feedback_no_metalanguage_docs`, no commit references. Records: (a) BA's 13 waves landed (W0, W1, W2, W3a, W3b, W3c, W4a, W4b, W4c, W5a, W5b, W5c, W5d, W5e, W6); (b) the 13 locks honoured cell-by-cell — Lock 1 honoured at BA close per option (a) (no deferral); (c) the carry ledger to BB explicit (per-grammar OpenFrame migration receivers retire; renumbered C1' covers cohort template-consolidation only); (d) the closing posture per BA.md §Closing posture: "the substrate is the typed-enum + slice-borrow across all nine grammars; the demonstration grammars are JSON (W5a) → CSS L4 (W5b) → BBNF (W5c) → Sheets (W5d) → cohort (W5e); the carry to BB is the renamed canonical surface, the cursor-unified parse, the consolidated path-crate triplet, the grammar-agnostic IR, the all-grammar direct-to-struct emitter foundation, the cohort hand-written modules awaiting consolidation, and the three deferred-with-receiver locks (Locks 4, 10, 11)". The FINAL document's voice is the same archaic-permissive register as BA.md.
> *Gate*: `docs/tranches/BA/FINAL.md` exists; the BA close documents.
> *Exit-criteria*: `test -f /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/FINAL.md && wc -l /Users/mkbabb/Programming/bbnf-lang/docs/tranches/BA/FINAL.md | awk '{print ($1 > 80)}' | tr -d '\n'` returns `1`.

## §3 — Closer gate

Per BA.md §Wave summary row BA.W6:

```
# BA-owned test surfaces 100% pass
cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis  ; expect: 100% pass

# substrate_audit green
cargo nextest run -p bbnf-ir -E 'test(substrate_audit)'
                                                       ; expect: 100% pass

# Lock 1 honoured at BA close (no OpenFrame across any grammar)
rg -n 'enum OpenFrame' crates/core/src/runtime/        ; expect: 0 matches
rg -n 'EmitStrategy::OpenFrame' crates/ir/src/registry/strategy.rs
                                                       ; expect: 0 matches

# BA close artefacts
test -f docs/tranches/BA/PROGRESS.md                  ; expect: pass
test -f docs/tranches/BA/FINAL.md                     ; expect: pass
test -f docs/tranches/BA/audit/W6-lock-verification.md
                                                      ; expect: pass
test -f docs/tranches/BA/audit/W6-generated-loc-budget.md
                                                      ; expect: pass
test -f docs/tranches/BA/audit/W6-carry-ledger.md     ; expect: pass

# BA-G3: regen ≤ 30 s on M1 Pro
time cargo xtask regen --check                         ; expect: ≤ 30 s real

# Per-grammar generated-LOC table verified
for g in json bbnf css_l4 google_sheets css_pretty ebnf bnf csv math; do
  wc -l crates/core/src/grammar/generated/$g.rs
done                                                   ; expect: total ≤ 150,900
```

Per `audit/HARDENING-SYNTHESIS-2026-05-03.md:204` ("BA.W0 is **not ready**; another amendment pass is required before execution"), W6 close confirms the BA-amendment punch list closes — the eight HARDENING-SYNTHESIS amendments at lines 49-201 (BA.md, BA/W0/W2/W3.md, BB/BB.md, BB/W0.md, BB/W3.md, BB/W4.md, BC/BC.md, BC/W0.md, BC/W5.md, GESTALT.md, codegen-paths.md, standard W0-W5 commit-plan line) are addressed in the BA / BB / BC plan tranches.

## §4 — Invariants

§I1. **Lock cross-reference verification**. Every cell in BA.md §13-Lock honoured (lines 76-90) verified per M1; the closer-gate's `cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis` is the cumulative gate.

§I2. **No metalanguage** (per `feedback_no_metalanguage_docs`). PROGRESS.md and FINAL.md cite path:line only; no commit references; no draft-history references.

§I3. **Archaic diction** (per `feedback_archaic_diction`). The voice of PROGRESS.md and FINAL.md is archaic-permissive ("hereupon", "begotten", "thereof", "appurtenant", "extant"); not corporate.

§I4. **Generated-size budget verified** (per BA-G10). The W6.M3 generated-LOC table records actuals against BA.md §Generated-LOC budget table targets; overflow without justification blocks the wave.

§I5. **Substrate audit green** (per BA-G4). The substrate-audit test passes; the 32 zero-caller substrates (per `audit/HARDENING-SYNTHESIS-2026-05-03.md:43`) close via BA.W0/W3 deletions and BA.W5a..W5e all-grammar OpenFrame retiral (including `EmitStrategy::OpenFrame` resolver variant, `SimpleStructBuilder` template, BBNF aggregator, per-grammar checkpoint surfaces). Lock 1 honoured at BA close per option (a) — no per-grammar deferral.

§I6. **Carry-tags explicit** (per `feedback_no_deferrals`). Every carry-tag at M4 names a real receiving wave with a close-criterion; no fictional successor letters; no "future tranche" placeholders.

§I7. **No deferrals** (per `feedback_no_deferrals`). The deferred locks (Lock 4, 10, 11) at BA close point to real receiving waves with gates: Lock 4 → BB.W3, Lock 10 → BB.W3, Lock 11 → BB.W0. Each is ratifiable per the BA→BB carry-tag table.

§I8. **Era V failure mode closed** (per `docs/tranches/meta-audit/archaeology/era-V-dta-psi-rut.md:7-10`). BA does not ship substrate-without-consumer; W6 verifies cell-by-cell.

## §5 — Risks specific to this wave

| Risk | Likelihood | Detection | Mitigation |
|---|---|---|---|
| BA-G3 (regen ≤ 30 s) is missed because the W2 god-module splits regress IR-walker cache locality | Medium | M2 `time cargo xtask regen --check` measurement | The split discipline preserves IR-walker shape (per W2.M0 layout-lowering rename, the IR walker traverses the same trait surface); if regen exceeds 30 s, profile via `samply record -- cargo xtask regen --check` and route the residual cost to BB.W0 generated-size-budget consideration |
| The substrate-audit test still fails at W6 because a hidden zero-caller substrate was missed across BA.W0/W3/W5a..W5e deletions | Medium | M5 `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)'` | Whitelist the row in the test's input with same-wave consumer rationale; per `audit/HARDENING-SYNTHESIS-2026-05-03.md:226`, "each row deleted or whitelisted with same-wave/later-wave consumer and rationale" — BA.W6 close documents the rationale |
| The post-BA generated-LOC total exceeds 150,900 because per-grammar W5a..W5e shrinkage is below target | Low | M3 LOC measurement | Per `feedback_generated_size_budget`, "Generated code has per-tranche line-count budget; overflow blocks wave until O(N) generator regression traced"; if overflow, root-cause the generator regression and surgically address |
| The cohort hand-written → BB.W2 template carry (BA→BB.C1') is insufficiently specified at BA.W6 close (BB.W2 cannot dispatch without parameter-table extracts from W5e) | Low | M4 carry-ledger + BB.W2 authorship review | The carry-ledger at M4 names W5e's M8 artefact as the parameter-table source; BB.W2 dispatches with it as input |
| FINAL.md voice drifts from archaic-permissive to corporate due to status-tick fatigue | Low | Read-through audit at W6 close | Re-emit FINAL.md if voice drifts; per `feedback_archaic_diction`, the voice is deliberate, not artefactual |

## §6 — Cross-references

- **Closes BA-G3** (per BA.md §Hard gates): `cargo xtask regen --check` ≤ 30 s on M1 Pro; CSS L4 `compile_paths_request` ≤ 25 s. Lane 04 baseline halved.
- **Closes BA-G4** (per BA.md): All 13 locks honoured at BA close per the §13-Lock cross-reference table; substrate_audit green.
- **Closes BA-G10** (per BA.md): Generated-file LOC accounting; per-grammar pre-/post-BA LOC table emitted; no overflow without justified cause.
- **Carry-tags TO BB**: BA→BB.C1' (cohort template-consolidation) + C2 + C3 + C4 + C5 + C6/C7/C8 deferrals explicit per M4 (per BA.md §Carry-tags TO BB).
- **Carry-tags TO BC**: BA→BC.C1, BA→BC.C2 explicit per M4 (per BA.md §Carry-tags TO BC lines 65-70).
- **Routed carries** (per `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228`):
  - F4 Tailwind regex_scan timeout: BA.W2 disposition or route to BB.W3 per HARDENING-SYNTHESIS amendment at line 84-87.
  - 32 zero-caller substrates: closed at BA.W6.M5.
  - Generated-size budgets: closed at BA.W2.M6 + BA.W6.M3 artefacts.
  - F5 TS Node-execute: routed to BD per HARDENING-SYNTHESIS amendment at line 56-58.
  - AscentStrategy: routed to BC.W0 disposition per HARDENING-SYNTHESIS routed-carry table at line 224.
- **Preceding wave**: BA.W5e (cohort direct-to-struct migration; Lock 1 honoured at BA close completes).
- **Following wave**: BB.W0 (sister crate emigration). BB.W0 inherits the BA→BB.C1' + C2..C5 carry-tags as inputs; BB.W1 (per the BB tranche reorientation under option (a)) consumes the post-BA all-grammar direct-to-struct foundation; BB.W2 absorbs the cohort hand-written modules into one parameterised template per BA→BB.C1'.
- **Routed-carries closed at W6**: per `audit/HARDENING-SYNTHESIS-2026-05-03.md:218-228` (the routed-carry table), W6 closes:
  - "32 zero-caller substrates → BA.W0 → substrate_audit green" — verified at M5.
  - "Generated-size budgets → BA.W2 and BB.W4 → Per-generated-file LOC windows and warning deltas recorded before emitter/regen close" — BA.W2.M6 + BA.W6.M3 artefacts.
  - "Sibling dirty-state ambiguity → BC.W0" — DEFERRED; BA.W6 names BC.W0 as receiving wave with `W0-sibling-baseline.txt` artefact requirement.
  - "AscentStrategy → BA.W5 or BC.W0, pending amendment" — DEFERRED to BC.W0 per `audit/HARDENING-SYNTHESIS-2026-05-03.md:224`; BA.W6 names BC.W0 as receiving wave; the `path/ascent.rs:277` (per `audit/MODULES-2026-05-03.md:678`) AscentStrategy disposition is BC-owned.
  - "F5 TS Node-execute → BD" — DEFERRED to BD per HARDENING-SYNTHESIS amendment line 56-58; BA.W6.FINAL records BD owner/reason/deadline; BA does not edit TS or WASM emitters.
  - "F4 Tailwind regex_scan → BA.W2 disposition or BB.W3 routed" — disposition recorded at W2.M6 + W5.M5 close; if direct-projection closed F4 by mechanism, no carry; if not, BB.W3 is the receiving wave with path-shape rewrite hypothesis named.

## §7 — Iter-time check

| Cargo Command | Expected Duration | Pass-Rate Target | Notes |
|---|---|---|---|
| `cargo nextest run -p bbnf -p bbnf-ir -p bbnf-analysis --profile ax-iter` | ≤ 90 s | 100% | BA-owned surface gate per BA.md §Wave summary BA.W6 row |
| `cargo nextest run -p bbnf-ir -E 'test(substrate_audit)' --profile ax-iter` | ≤ 12 s | 100% | substrate_audit green gate (BA-G4) |
| `time cargo xtask regen --check` | ≤ 30 s real | exit 0 | BA-G3 closer-gate; halving of Lane 04 59.98 s baseline |
| `cargo bench --bench bench_json -- twitter` | ≤ 60 s | mean ≤ 400 µs | BA-G1a close re-verification |
| `rg -n 'enum OpenFrame' crates/core/src/runtime/` | < 1 s | 0 | Lock 1 honoured at BA close (option (a)) |
| `for g in json bbnf css_l4 google_sheets css_pretty ebnf bnf csv math; do wc -l crates/core/src/grammar/generated/$g.rs; done` | < 1 s | total ≤ 150,900 | M3 generated-LOC table (post-W5a..W5e all-grammar shrinkage) |
| `find crates -name '*.rs' ! -path '*/generated/*' \| xargs wc -l \| awk '$1>500'` | < 2 s | empty | Lock 13 god-module discipline gate |

## §8 — Verification artefacts

- `docs/tranches/BA/PROGRESS.md` — per-wave summary; 13-wave landings (M0).
- `docs/tranches/BA/FINAL.md` — BA close document; 13 locks honoured cell-by-cell + carry ledger to BB explicit (M6).
- `docs/tranches/BA/audit/W6-lock-verification.md` — each lock's verification command + output (M1).
- `docs/tranches/BA/audit/W6-generated-loc-budget.md` — per-grammar post-BA actuals (M3).
- `docs/tranches/BA/audit/W6-carry-ledger.md` — BA→BB.C1' + C2..C5 + C6/C7/C8 carry-tag enumeration (M4).
- `docs/tranches/BA/audit/W5a-json-direct-to-struct.md` (consumed) — JSON disposition.
- `docs/tranches/BA/audit/W5b-css-l4-direct-to-struct.md` (consumed) — CSS L4 disposition.
- `docs/tranches/BA/audit/W5c-bbnf-direct-to-struct.md` (consumed) — BBNF disposition.
- `docs/tranches/BA/audit/W5d-google-sheets-direct-to-struct.md` (consumed) — Sheets disposition.
- `docs/tranches/BA/audit/W5e-cohort-direct-to-struct.md` (consumed) — cohort disposition + cohort template hand-off parameter table for BB.W2.

## §9 — Audit lane forecast

The W6 audit lane forecast: post-W6, the following lanes close fully:

- Lane 04 (sota anchoring) — BA-G1a (twitter ≤ 400 µs vs sonic-rs 436 µs) closed; BA-G1b/c/d/e are internal-progress (BB tranche pursues lightning-css 4.16 ms parity at BB-G1).
- Lane 05 (substrate audit) — substrate_audit green at M5.
- Lane 06 (generated code budget) — total ≤ 150,900 verified at M3.
- Lane 09 (closer-gate ratification) — every BA gate's closer-condition verified.

Lanes still open beyond BA: Lock 4 (e-graph) at BB.W3; Lock 10 (Pratt + SIMD auto-detected) at BB.W3; Lock 11 (path-deps for sister crates) at BB.W0; Lock 5 (IR contract spec) at BC.W0.

## §10 — Phase-4 surgery ledger

| Surgery # | Description | Landed at | Verification |
|---|---|---|---|
| 1 | Delete CSS L4/BBNF/Sheets/cohort `OpenFrame` preservation; replace W5.M6 with `rg -n 'enum OpenFrame' crates/core/src/runtime/` returning 0 | BA.W5a..W5e (option (a) — full all-grammar migration) | W6.M1 closer-gate verifies aggregate |
| 2 | Lock 1 substrate identity flipped from option (b) to option (a) per user override; W5 split into W5a..W5e per-grammar | BA.W5a..W5e | W6.M1 lock cross-reference |
| 24 | BA.md C1 receiver retires per-grammar OpenFrame migration scope under option (a); renumbered C1' is cohort hand-written → BB.W2 template-consolidation only | BA.md §Carry-tags TO BB rewrite | W6.M4 carry-ledger artefact |
| 11 | Per-grammar engineering gates BA-G1b/c/d/e marked internal-progress (non-SOTA); only BA-G1a is SOTA-anchored | BA.md §Hard gates rewrite | W6.M1 lock cross-reference (Lock 8) |
