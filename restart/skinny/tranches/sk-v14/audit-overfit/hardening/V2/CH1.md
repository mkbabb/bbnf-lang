# CH1 — CORRECTNESS lens disposition (SK-V14 S-P0 Overfit Audit V2)

Authority: `restart/prompts/ORCHESTRATOR.md §3W CH1` + dispatch
`restart/skinny/tranches/sk-v14/audit-overfit/hardening/V1/CHALLENGE-
CONTEXT.md` §3 + V2 overlay `…/V2/CHALLENGE-V2-ADDENDUM.md` §1 (CH1
focus: verify F-V2-A4-2 line-cite refresh + F-V2-SYNTHESIS-1 census +
F-V2-SYNTHESIS-4 Three→Four + SYNTHESIS §1.2 NEW-2).

V1 disposition (`…/V1/CH1.md`, 85.7 % ACCEPT, two REVISEs):
1. SYNTHESIS census arithmetic: §0.1 / §1.1 / §1.2 disagreed 63/11
   vs 54/20.
2. A4 §0 NEW-count + line-cite drift (json_provider :62/66/70/74/50
   off by 2; "3 of 7 scanners" actually 4 of 7).

V2 axis-redispatch commit `1735882a5` lands five fold packets across
SYNTHESIS, A3, A4, A5, A6 (A1 + A2 STAND, zero V2 deltas verified by
`git diff d4cbc8204..1735882a5 -- …a1 …a2` returning empty).

## §0 — V2 Verdict + disposition summary

**Verdict: ACCEPT (≈97 %).** Both V1 REVISEs are resolved at the
fold-target sites; the V2 axis-redispatch lands every line-cite + census
+ verdict-line alignment cleanly. One residual citation-arithmetic
inconsistency in SYNTHESIS §1.1 NEW-cluster prose at line 81 (still
reads "3 of 7 CSS scanners" where §1.2 was correctly updated to "Four
of the seven") is the sole CH1 V2 defect, all four other sites at the
SYNTHESIS-internal Three→Four scope are aligned.

| Disposition | Count | Notes |
| --- | ---: | --- |
| ACCEPT | 7 of 7 artefacts | A1, A2 STAND verbatim; A3 V2 H3-reclassification verified at source; A4 V2 all three folds land at the correct line coordinates; A5 V2 verdict-line aligned at all four sites; A6 V2 LegacyPath both-readings paragraph clears the disambiguation; SYNTHESIS V2 has 5/5 folds substantively landed. |
| REVISE | 0 artefacts at the substantive level | Below-threshold cosmetic line-edit at SYNTHESIS §1.1 line 81 (residual "3 of 7" in NEW-cluster prose; the corresponding §1.2 enumeration is correctly "Four"). |
| REJECT | 0 | No artefact contains a falsifiable factual error in source-side claims. |

ACCEPT-rate: 7/7 artefacts = **100 %** substantively; with one
documented cosmetic carry-over flagged below for V3 hygiene (does not
gate CH1 V2 ACCEPT-rate above §3Z 95 % threshold).

## §1 — V1 REVISE confirming pass

### §1.1 V1 REVISE-1 (SYNTHESIS census 63/11 vs 54/20) — RESOLVED

Executable verification:

`grep -n "54\|73 %\|27 %\|85 %\|15 %\|63 \|11 NEW\|11 of 74\|11 conceptual" SYNTHESIS-AUDIT-OVERFIT.md`

Returns the V2 census wording at lines 64-67:

```
Across the six per-axis files, 54 of 74 findings (73 %) CONFIRM the
SK-V13 audit pack byte-for-byte; 20 of 74 are NEW (27 %) on per-row
count (per-axis table column-sum 8+7+29+4+3+3 = 54 CONFIRMS;
0+2+1+12+1+4 = 20 NEW). The per-category count is 11 NEW conceptual
clusters …
```

Verified by re-arithmetic:

```
CONFIRMS column-sum: 8+7+29+4+3+3 = 54 ✓
NEW      column-sum: 0+2+1+12+1+4 = 20 ✓
54 + 20 = 74 ✓
```

Aggregate row at SYNTHESIS §0.1 line 25 reads **"20 NEW"** (V2 fold:
was "11 NEW"). Distinction between per-row (20) and per-category (11)
is now explicit in both §0.1 aggregate row and §1.1 prose. §5.1
closing summary line 491-495 also aligned: "11 NEW conceptual
categories (= 20 NEW per-row findings) … 54 findings CONFIRM the V13
audit pack". §3.2 orphan-check at lines 351-354 also re-aligned.

**Disposition**: V1 REVISE-1 RESOLVED.

### §1.2 V1 REVISE-2 (A4 NEW-count + line-cite drift) — RESOLVED

Executable verification of the F-V2-A4-2 fold per V2 dispatch §1
mandate:

`sed -n '60p;64p;68p;72p;48p;80,100p' skinny/crates/codegen/src/json_provider.rs` returns:

```
pub(crate) fn config_rs() -> String {       <- line 48
pub(crate) fn generated_rs() -> String {    <- line 60
pub(crate) fn parser_rs() -> String {       <- line 64
pub(crate) fn view_rs() -> String {         <- line 68
pub(crate) fn value_rs() -> String {        <- line 72
fn normalize(source: &str) -> String {      <- line 80
    let mut out = String::new();            <- line 81
    out.push_str("// @generated by skinny bbnf-codegen; do not edit by hand.\n");
    let trimmed = source.trim_matches('\n');
    …
}                                            <- line 100
```

(sed prints in source-line order; the V2 dispatch's argument list
ordering `60p;64p;68p;72p;48p` is preserved by sed as a source-order
selection.)

Cross-checked against A4 V2 finding 11 row: "`json_provider::
generated_rs` (line **60**), `parser_rs` (**64**), `view_rs` (**68**),
`value_rs` (**72**) and `config_rs` (**48**)". Match exact. Finding 8
range updated from `:85-99` → `:80-100` (correctly widens to the full
`normalize` function body, which spans lines 80-100). Finding 10
updated `:62-84` → `:60-78`. Finding 11 declarative scope updated
`:50-84` → `:48-78`. All line-cite drift from V1 REVISE-2 cleared.

V2 fold F-V2-A4-3 "Three → Four" in A4 §0 verified by `git diff
d4cbc8204..1735882a5 …`:

```diff
-2. Three of the seven CSS "scanners" short-circuit on a `const
+2. Four of the seven CSS "scanners" short-circuit on a `const
   CANONICAL_FIXTURE` / `CAPTURED_W2_INPUT` byte-equality check and
-  return a precomputed `CANONICAL_FACTS` blob. `css_l4_nested_layout
+  return a precomputed `CANONICAL_FACTS` blob (`nested_layout`,
+  `at_rules_and_media`, `stylesheet_selectors`,
+  `vendor_and_custom_atrules`). `css_l4_nested_layout
```

V2 fold F-V2-A4-1 scope-extension framing at A4 §0 verified — the new
paragraph at lines 33-46 ("This delta is a **scope extension, not a
reversal**, of the V13 §7.1 row 1 'HONEST' verdict for
`json_provider`. …") preserves the V13 HONEST verdict on the
~70-line `json_sink_direct::render` sink chunk at `codegen/src/lib.
rs:215-217` while clearly framing V14's discovery as roster extension
over the other 5 emitted files; the sink chunk is correctly cited as
"~15 % minority share".

**Disposition**: V1 REVISE-2 RESOLVED.

### §1.3 V1 §3.3 minor off-by-one cite drift (A3 C1/C2; lib.rs:338-349)

The V2 commit message at `1735882a5` records:

> Bonus observation from A4 V2: V1 CH1 also claimed lib.rs:338-349 →
> :337-347 for finding 13, but grep confirms `emission_is_deterministic`
> starts at :338 — existing cite correct; spurious claim, untouched.

Re-verified:

`grep -n "emission_is_deterministic" skinny/crates/codegen/src/lib.rs`
returns line 338. A4 V2 finding 13 cite `:338-349` stands. V1 CH1
§3.3's "off by 1-2; cite is substantively correct" claim was itself
the off-by-one; the V2 commit correctly absorbed this. CH1 V2 confirms
the absorption.

A3 C1/C2 range edges (`:3-26 → :3-25`, `:35-44 → :34-44`) were not
folded — V2 dispatch did not call for that hygiene tidy, and the
substance carries; left for future cosmetic.

## §2 — V2 fresh-finding scan

### §2.1 Residual carry-over at SYNTHESIS §1.1 line 81

`sed -n '78,84p' SYNTHESIS-AUDIT-OVERFIT.md` returns:

```
| A1 | 8 | 0 | (none — measurement audit reduces to byte-identical confirmation at SK-V14 no-implementation-yet posture) |
| A2 | 7 | 2 | F8 (…); F9 (…) |
| A3 | 29 | 1 | D1 (…) |
| A4 | 4 | 12 | NEW-1 (…); NEW-2 (3 of 7 CSS scanners short-circuit on `CANONICAL_FIXTURE`/`CAPTURED_W2_INPUT` byte-equality → fixture-lookup tables dressed as parsers); NEW-3 (…); plus 9 quantification extensions of V13 findings |
| A5 | 3 | 1 | NEW-MED (…) |
| A6 | 3 | 4 | NEW-HIGH-1 (…); NEW-HIGH-2 (…); NEW-MED (…); NEW-LOW (…) |
```

The §1.1 NEW finding cluster prose at A4 row line 81 still reads
**"NEW-2 (3 of 7 CSS scanners…)"**. The F-V2-SYNTHESIS-4 fold updated
§1.2 NEW-2 numeral block at line 122 ("Four of the seven CSS L4
template generators … fixture-lookup tables") + §0.1 A4 NEW-count
column (3 → 12 = 4 categories × 1 + extension rows) but the cosmetic
in-table NEW-cluster cell at §1.1 line 81 was not touched. The
substantive arithmetic (the count of `12` in the NEW column at row A4
already reflects the 4-fixture / 3-CANONICAL + 1-CAPTURED breakdown +
all 8 additional NEW rows in A4 §2 ledger; per F-V2-A4-3 commit
narrative "Three of the seven CSS L4 template generators (`nested_
layout`, `at_rules_and_media`, `stylesheet_selectors`, `vendor_and_
custom_atrules`)" had four names listed under "Three" — same kind of
defect that V1 flagged, persisting here at §1.1 only) is now
consistent with the §1.2 enumeration; the prose summary at §1.1 is the
last cosmetic carry-over.

**Disposition**: BELOW-THRESHOLD COSMETIC. Substantive count is
correctly 12 in the NEW column (A4 row); §1.2 enumeration is correctly
"Four of the seven". V1's central finding (which the V2 fold absorbed)
remains internally consistent everywhere except this one in-table
prose cell. Recommend V3 hygiene fold to replace "3 of 7" → "4 of 7"
at SYNTHESIS line 81 NEW-2 parenthetical. Does not gate CH1 V2 ACCEPT.

### §2.2 Methodology line-count: file is 100 lines, A4 V2 claims 101

`wc -l skinny/crates/codegen/src/json_provider.rs` returns **100**.
`awk 'END{print NR}' …json_provider.rs` returns **100**. `tail -c 1
…json_provider.rs | xxd` returns `0a` (file is newline-terminated; no
trailing fragment line).

A4 V2 methodology line at `…generator-truth.md:153` reads:

> `skinny/crates/codegen/src/json_provider.rs` (full, **101** lines).

Off by one. V2 fold F-V2-A4-2 narrative recorded "methodology line
count 99 → 101"; correct refresh would have been "99 → 100" (the V2
axis re-pull surfaced the actual line count as 100, not 101). The V2
A4 ledger row 8 + 10 + 11 line cites are all correct on the actual
file's coordinates; only the methodology summary line carries the +1
drift.

**Disposition**: BELOW-THRESHOLD COSMETIC, same class as §2.1.
Recommend V3 hygiene fold to "99 → 100". Does not gate CH1 V2 ACCEPT.

### §2.3 V2 verdict-line alignment (CH6 cross-check; CH1 confirmation)

`grep -n "PASS at SK-V14 starting baseline\|FAIL at HEAD\|PARTIAL PASS"
…audit-overfit/sk-v14-audit-overfit-decision-engine.md` returns:

```
11: - **Verdict: FAIL at HEAD, PASS conditioned on C-5 (PRUNE-1 + PRUNE-2) + C-4 (PRUNE-5) landing.** …
102: **Verdict: FAIL at HEAD, PASS conditioned on C-5 (PRUNE-1 + PRUNE-2) + C-4 (PRUNE-5) landing.**
107: - **No-scaffold-only-admit clause: FAIL at HEAD, PASS conditioned on C-5 (PRUNE-1 + PRUNE-2) + C-4 (PRUNE-5) landing.** …
```

Across A5 §0:11, §3:102, §3:107. SYNTHESIS §0.1 A5 row at line 23
reads "**PARTIAL PASS** (resolver clause PASS; scaffold-clause FAIL
at v13 close + at v14 HEAD; PASS conditioned on C-5 (PRUNE-1 + PRUNE-
2) + C-4 (PRUNE-5) landing)". Both alignments verified verbatim. A5
§4 row 4 LOW row also folded to add the gate-rejection-invariant
clause. §5 closing also folded for coherence. Verdict-line alignment
is complete at all four targeted sites.

### §2.4 A3 V2 H3→LOW reclassification verified at source

`sed -n '230,240p' skinny/crates/passes/src/decision_csp.rs` returns:

```
…
let resolved = finalize_rule("json", RuleId(0), candidates, active);
…
```

`grep -n "#\[cfg(test)\]\|mod tests" decision_csp.rs` returns lines
200-201:

```
200: #[cfg(test)]
201: mod tests {
```

Line 235 (the `finalize_rule("json", …)` call) is inside the
`#[cfg(test)] mod tests { … }` block. `sed -n '475,480p' skinny/crates/
passes/src/lib.rs` returns line 478 production call:

```
crate::decision_csp::finalize_rule(&grammar.name, rule.id, candidates, active)
```

Production call site is generic over grammar identity via
`&grammar.name`. A3 V2 L8 reclassification narrative is exactly
correct: the H3 violation is test-fixture name leak (severity L8), not
production-path overfit. H6 takes HIGH freestanding on its own
structural merits at decision_csp file-wide grep absence.

Severity distribution: 11 CRIT + 7 HIGH + 5 MED + 7 LOW = 30 (V1) →
11 CRIT + 6 HIGH + 5 MED + 8 LOW = 30 (V2). Total finding count
unchanged at 30; A3 §0 + §2.B + §2.D + §3 + §4 Tier-2 + §4 Tier-4 all
re-aligned in the V2 diff. CH1 verifies no orphan severity arithmetic.

### §2.5 A6 V2 LegacyPath both-readings disambiguation

A6 V2 §0 line 12-22 adds:

> V13-disposition disambiguation for finding (a): the V13 `SYNTHESIS-
> AUDIT-OVERFIT.md` line 74 "Honest patterns left clean" disposition
> ("No backwards-compat shims that aren't legitimate refactors (Agent
> F Pattern G)") did not enumerate the `LegacyPath` alias surface. V14
> records the `LegacyPath` alias as a NEW finding rather than a
> reversal of the V13 Pattern G CLEAN reading: the alias was
> introduced at `0e8dbc104 feat(runtime/parse-with-…)` (2026-05-02),
> before V13 close, …

A6 §2 ledger row Status updated from `NEW` to `NEW (scope-extension
over V13 Pattern G; not a reversal)`. The §2 ledger row body also
embeds the disambiguation paragraph in the finding column. CH1 V2:
both readings (NEW finding + scope-extension framing not reversal of
Pattern G CLEAN) are explicitly preserved; reader can reconcile A6 V14
finding (a) with V13 SYNTHESIS line 74 unambiguously.

### §2.6 CH7 cross-check: 9 grammars in `bbnf` metadata

Re-executed (per V2 dispatch §1 CH7 mandate):

`cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.
grammars[].ident'` returns 9 grammar idents:

```
bbnf, json, css_l4, css_pretty, google_sheets, ebnf, bnf, csv, math
```

`find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d | wc -l`
returns **9**. The PRUNE-4 sub-wave count = 9 binding from V1 holds
verbatim under V2. SYNTHESIS §1.3 "Pattern H file count: 64 → 67" +
§2.3 "PRUNE-4 sub-wave count is 9, not 8" both stand.

## §3 — Cross-artefact arithmetic re-verification

Per-axis severity column sums (SYNTHESIS §0.1 table) under V2:

```
A1: 4+2+2+0 = 8     A2: 4+3+1+1 = 9     A3: 11+7+5+7 = 30
A4: 9+4+2+1 = 16    A5: 0+2+1+1 = 4     A6: 3+2+1+1 = 7
                                          ─────────────
                                          Total: 74 ✓
```

Aggregate row: 31 + 20 + 12 + 11 = **74** ✓. Per-severity column
sums: 31 CRIT ✓, 20 HIGH ✓, 12 MED ✓, 11 LOW ✓ (unchanged from V1).

CONFIRMS / NEW column sums (V2 §1.1 table):
- CONFIRMS: 8+7+29+4+3+3 = **54** ✓
- NEW: 0+2+1+12+1+4 = **20** ✓
- 54+20=74 ✓ matches the column-sum SYNTHESIS prose at line 64-67
  verbatim.

Coverage table (SYNTHESIS §3.1): C-1 (41) + C-2 (7) + C-3 (11) + C-4
(4) + C-5 (11) = **74** ✓.

A3 V2 severity redistribution: 11 + 6 + 5 + 8 = **30** ✓ (V1 was
11+7+5+7=30, redistributed by F-V2-A3-1 H3 HIGH→LOW; aggregate
preserved). A3 §0 + §2.B + §2.D + §3 + §4 Tier-2 + §4 Tier-4 all
self-consistent.

All arithmetic reconciles under V2.

## §4 — Spot-verified cites (V2 sample of 12)

The following file:line cites were re-executed and reproduce
verbatim against the V2-committed artefacts:

1. `git diff d4cbc8204..1735882a5 -- …a1 …a2` returns empty — A1 + A2 STAND verbatim.
2. `sed -n '60p;64p;68p;72p;48p;80,100p' …json_provider.rs` returns config/generated/parser/view/value at correct lines (per §1.2).
3. `wc -l …json_provider.rs` returns 100 (A4 V2 methodology says 101; §2.2 carries the off-by-one).
4. `grep -n "Three\|Four" …SYNTHESIS-AUDIT-OVERFIT.md | head -5` confirms "Four of the seven CSS L4 template generators" at §1.2 line 122 (Three→Four fold landed); §1.1 line 81 still reads "3 of 7" (§2.1 residual).
5. `grep -n "#\[cfg(test)\]\|mod tests\|fn finalize_rule" …decision_csp.rs` returns 16 (production fn), 200 (cfg test), 201 (mod tests) — confirms line 235 is inside cfg-test mod.
6. `sed -n '475,480p' …passes/src/lib.rs` returns `finalize_rule(&grammar.name, …)` — production-side generic over grammar identity.
7. `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d | wc -l` returns 9.
8. `cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident' | wc -l` returns 9 (bbnf + 8 grammar idents; line count = 9 idents enumerated).
9. SYNTHESIS §1.3 Pattern H census line 211 reads "Total = 67 hand-written per-grammar runtime files" — re-arithmetic 8+7+7+7+7+7+10+7+7 = 67 ✓.
10. A5 V2 verdict at §0:11 + §3:102 + §3:107 all read "FAIL at HEAD, PASS conditioned on C-5 (PRUNE-1 + PRUNE-2) + C-4 (PRUNE-5) landing" — verbatim cross-site alignment.
11. A6 V2 §0 disambiguation paragraph at line 12-22 cites commit `0e8dbc104` (W3.2, 2026-05-02) as LegacyPath alias introduction — pre-V13 close; both-readings preservation explicit.
12. SYNTHESIS §1.3 co-derivation note at lines 220-231 explicitly tags A3/A5/A6 as "co-derived, not orthogonal" for the css_pretty +3 / +1 delta; CH3 regression-narrative inputs explicit.

## §5 — V3 hygiene recommendations

The following two cosmetic items are below CH1 V2 ACCEPT threshold but
recommended for V3 fold:

1. **SYNTHESIS §1.1 line 81** — replace "NEW-2 (3 of 7 CSS scanners
   short-circuit…)" with "NEW-2 (4 of 7 CSS scanners short-circuit…)".
   The F-V2-SYNTHESIS-4 fold updated the §1.2 enumeration body
   ("Four of the seven CSS L4 template generators") + the A4 §0 NEW-
   count narrative ("Four of the seven CSS scanners") + the §0.1 A4
   NEW column count (3 → 12 absorbing the 4-fixture roster). The §1.1
   in-table NEW cluster prose cell is the last residual using the
   old "Three / 3 of 7" framing.

2. **A4 §1 (Methodology) line 153** — replace "(full, 101 lines)" with
   "(full, 100 lines)". Source-of-truth via `wc -l` is 100; the V2
   F-V2-A4-2 narrative recorded "99 → 101" but the correct refresh is
   "99 → 100".

Both are sub-grammatical hygiene; neither falsifies any substantive
A4 / SYNTHESIS finding (A4 ledger rows 8 / 10 / 11 already cite the
correct file-line coordinates; SYNTHESIS §1.2 enumeration is correct).

---

**V2 ACCEPT-rate: 7/7 artefacts = 100 %** (substantively).

Both V1 REVISEs are RESOLVED at the fold-target sites. The V2 axis-
redispatch lands cleanly across all five folded artefacts (SYNTHESIS,
A3, A4, A5, A6); A1 + A2 STAND verbatim per the V2 dispatch contract.
The two residual cosmetic items at §5 above are below the CH1 V2
ACCEPT threshold; flagging for V3 hygiene only.

**No CH1-grade source-side correctness defects identified in V2.**
Every file:line cite re-executes to the quoted output; the 74-finding
aggregate + severity distribution are arithmetically self-consistent;
the 54 CONFIRMS / 20 NEW per-row + 11 NEW conceptual category census
reconciles across §0.1 + §1.1 + §1.2 + §3.2 + §4.3 + §5.1; A5
verdict-line is aligned across all four targeted sites; A3 severity
redistribution (HIGH 7→6 + LOW 7→8) preserves the 30-total and is
source-verified inside the `cfg(test)` block; A6 V2 LegacyPath
disambiguation explicitly preserves both readings; CH7 cross-check
returns 9 grammar idents.

V2 forecast (CHALLENGE-V2-ADDENDUM §4) was 98-100 % aggregate; CH1
contributes 100 %.
