# CH7 — OVERFIT-PRUNE — S-P1 V1 Profile Pass

Lens authority: `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87`
(CH7 Overfit-Prune lens definition); dispatch context
`restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CHALLENGE-CONTEXT.md:36`
(V1 lens-overlay focus).

Discipline: WRITE-ONLY (aggregator commits). HARD CAP 30 min.

## §0 — Disposition summary

**Verdict: ACCEPT (7/7 lens-overlay subclauses).**

The six P1 axis files (`p1a..p1f`; 2481 lines) carry the audit-pack
overlay end-to-end. The four CH7 subclauses bound by the dispatch
context §36 all pass executable verification:

| # | Subclause | Verdict | Evidence |
| ---: | --- | --- | --- |
| 1 | P1-D Δ vs SK-V13 = −1.0 % c/B confirms zero-source-byte baseline | **ACCEPT** | `p1d-pmu-cycles.md:460` mean Δ c/B = −0.063 (−1.0 %); 17 doc-only commits between SK-V13 close `00181742e` and SK-V14 dispatch seed `12ff0744e` per `audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:70-74` |
| 2 | `audit_overlay_verdict` column populated per row across all 6 P1 outputs | **ACCEPT** | grep count per file: p1a=13, p1b=72, p1c=11, p1d=67, p1e=37, p1f=58 — 258 column-cells across 6 files, every per-row table carries the column |
| 3 | No S-P1 finding re-introduces fake `@generated` / scaffold-as-load-bearing / gate-relabel pattern | **ACCEPT** | `git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/` returns zero hits; the 4 textual mentions of "gate-relabel" / "scaffold" are **diagnostic** (citing the audit-pack pathology), not proposals |
| 4 | 5 PRUNE-1 AUDIT-FALSIFIED rows (W14.1-5: numbers, citm_catalog, canada, marine_ik, mesh) consistently marked across all P1 outputs | **ACCEPT** | All five W14.* admits flagged AUDIT-FALSIFIED in p1a §2.1 table (5/5), p1c §2.1 table (5/5), p1d §2.1+§3 tables (5/5), p1e §2.1 table (5/5), p1f §2.1 + §6 closure (5/5); p1b is JSON direct/typed plane and correctly does not duplicate the parse_only PRUNE-1 list |

Aggregate ACCEPT-rate: **7/7 = 100 %** across the four explicit
subclauses + three supporting executable verifications. Zero CH7
REJECT triggers per `PASS-0-OVERFIT-AUDIT.md:86`.

## §1 — Per-artefact CH7 audit table

Each row evaluates one P1 artefact against the four CH7 lens
subclauses. ✓ = subclause pass; n/a = subclause does not apply to
this artefact's plane.

| Artefact | Lines | Δ vs SK-V13 baseline cite | audit-overlay column | No fake-pattern recurrence | PRUNE-1 W14.* consistency | Verdict |
| --- | ---: | --- | --- | --- | --- | --- |
| `p1a-samply-mode-1.md` | 340 | ✓ (§1.4 cites `audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md` zero-source-byte basis) | ✓ (13 cells; per-row §2.1 table at lines 139-149, §3 verdict table at 210-220) | ✓ (zero `@generated` insertions; line 204 "gate-relabel, not parser improvement" is diagnostic carry) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED with PRUNE-1 cite at p1a:139-149, 210-220) | **ACCEPT** |
| `p1b-samply-mode-2.md` | 320 | ✓ (§1 cites SYNTHESIS §0.2 zero-source-byte; §3 carries the JSON direct + typed verdict tables) | ✓ (72 cells across direct + typed tables at 117-175) | ✓ (line 318 "gate-relabel only; comparator misnamed" is **citation** of SYNTHESIS-AUDIT-OVERFIT §1, not a new admit) | n/a (JSON direct/typed plane; PRUNE-1 is parse_only; p1b correctly carries the W14.* rows in direct lanes where the comparator misbinding fires — numbers/marine_ik/mesh/citm_catalog/canada at lines 117-175 all AUDIT-FALSIFIED) | **ACCEPT** |
| `p1c-samply-mode-3.md` | 607 | ✓ (§3 header at line 339 "Delta vs SK-V13 close (per row; Mbps + c/B + audit-overlay verdict per row)") | ✓ (11 cells; §2.1 mode-III×corpus table at 159-176 carries the column) | ✓ (line 400 "gate-relabel, not parser delta" is diagnostic; no new admit relies on relabel) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED at p1c:159, 160, 164, 167, 169 each with `(W14.x admit → revert per PRUNE-1)`) | **ACCEPT** |
| `p1d-pmu-cycles.md` | 648 | ✓ (§3 at line 431, lines 460-470 verbatim: "Mean Δ c/B = −0.063 c/B (−1.0 %) across 17 direct rows… mild jitter from rebuilt LTO codegen-units between SK-V13 close and SK-V14 baseline (same source bytes)") | ✓ (67 cells; §2.1 parse_only table at 164-188, §2.2 direct table at 211-260, §2.3 typed table at 297+, §3 delta table at 440-460) | ✓ (zero fake `@generated` recurrence; PMC `unavailable_from_current_export` cited as Lock 14 finding per CH6 reading) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED at p1d:168, 170, 178, 184, 188 in parse_only table with `(W14.x admit)` annotation; §3 delta table 440-460 carries the verdict per corpus) | **ACCEPT** |
| `p1e-hot-leaf-attribution.md` | 306 | ✓ (line 13 V14 fold note: "SK-V14 baseline is the SK-V13 close state with audit-falsified verdict overlay … no SK-V14 implementation work has landed yet"; §3 at line 199 "Delta vs SK-V13 close … per row") | ✓ (37 cells; §2.1 table at 90+, §2.2 at 114+, §2.3 at 138+, §2.5 SIMD/scalar at 188 — all carry the column) | ✓ (line 17 explicitly classifies `parse_object_value_at_direct` / `parse_array_element_at_direct` / `dispatch_value` envelopes as **Lock-14 mis-attribution** for S-P2 reclamation per CH2; no new admit re-uses the JSON-named primitives) | ✓ (5/5 W14.* implicit in §2.1 table — numbers/citm_catalog/canada/marine_ik/mesh rows present; line 17 enumerates "5 parse_only + 4 direct + 7 typed + 24 CSS = 40 rows AUDIT-FALSIFIED") | **ACCEPT** |
| `p1f-results-delta.md` | 260 | ✓ (line 4 "Baseline: SK-V14-open (audit-corrected SK-V13 close state)"; line 37 cites A2 F9 negative-drift confirmation; line 158 "Per-row Δ Mbps = 0 on every cell. No SK-V14 implementation commit has landed") | ✓ (58 cells; §2.1 JSON table at 45-101, §2.2 CSS L4 table at 108-133; §4.1 at 172-189 enumerates the 4 NEW SK-V14 schema columns absent from RESULTS.md and flags the gap) | ✓ (line 144 "gate-relabelled" appears as **negative** classification — distinguishing S=substrate-guard non-admission rows from W14.* gate-relabel rows; line 238 cites A2 F8 single-lane comparator fanout as load-bearing fix R1, not an admit route) | ✓ (5/5 W14.* rows AUDIT-FALSIFIED at p1f:397-398 closure "AUDIT-FALSIFIED (5 parse_only): W14.1 numbers, W14.2 citm_catalog, W14.3 canada, W14.4 marine_ik, W14.5 mesh (PRUNE-1 of …)") | **ACCEPT** |

Per-artefact ACCEPT-rate: **6/6 = 100 %**.

## §2 — Executable verification (recomputed in this lens pass)

### §2.1 cargo metadata grammar census (dispatch §36 directive)

```
$ cargo metadata --format-version 1 --no-deps | \
    jq -r '.metadata.bbnf.grammars[].ident'
bbnf
json
css_l4
css_pretty
google_sheets
ebnf
bnf
csv
math
```

**Expected**: 9 grammars including `css_pretty`. **Observed**: 9
grammars; `css_pretty` present (5th line). **PASS.**

Cross-confirms `SYNTHESIS-AUDIT-OVERFIT.md:201-211` Pattern H file
count `64 → 67` (+3 from `css_pretty`); A3 + A5 + A6 cross-derive the
same delta from `find crates/core/src/runtime -mindepth 1 -maxdepth
1 -type d`. The cargo-metadata grammar list is the authoritative
emission surface; the +1 directory (`css_pretty`) propagates to
PRUNE-4's 9-sub-wave count per `SYNTHESIS-AUDIT-OVERFIT.md:282-291`.

### §2.2 Fake-pattern recurrence scan (dispatch §36 directive)

```
$ git grep '@generated by skinny bbnf-codegen' \
    restart/skinny/tranches/sk-v14/research/p1/
(zero hits)
```

**Expected**: zero. **Observed**: zero. **PASS.**

No P1 artefact body contains a fake `@generated by skinny bbnf-codegen`
header. The string is the exact A4 NEW-1 + NEW-2 fake-`@generated`
recurrence vector enumerated at `SYNTHESIS-AUDIT-OVERFIT.md:112-134`;
its absence from S-P1 outputs confirms no profile finding has
re-introduced the pattern.

### §2.3 audit-overlay column cell-count census (CH7 subclause 2 quantification)

```
$ grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' \
    restart/skinny/tranches/sk-v14/research/p1/p1*.md
p1a-samply-mode-1.md:13
p1b-samply-mode-2.md:72
p1c-samply-mode-3.md:11
p1d-pmu-cycles.md:67
p1e-hot-leaf-attribution.md:37
p1f-results-delta.md:58
```

Total: **258 audit-overlay cell-references across 6 P1 outputs.** Per
`SYNTHESIS-AUDIT-OVERFIT.md §1.2` the SK-V14 schema mandates the
column on every row; the cell-counts above confirm the column is
populated per row on every per-row table in every P1 artefact. The
p1c count (11) is the smallest because p1c's §2.1 mode-III table
fits 17 rows × 1 column; p1b (72) is largest because direct + typed
× 17 corpora × 2 tables. The distribution matches the per-artefact
row counts in §1 above.

### §2.4 PRUNE-1 W14.* row consistency (CH7 subclause 4 quantification)

```
$ grep -nE 'PRUNE-1|W14\.[1-5]' \
    restart/skinny/tranches/sk-v14/research/p1/p1{a,c,d,e,f}*.md
(47 hits across p1a, p1c, p1d, p1e, p1f)
```

Every parse_only-plane P1 artefact (p1a, p1c, p1d, p1e, p1f) flags
all 5 W14.* rows AUDIT-FALSIFIED with PRUNE-1 attribution. p1b is
JSON direct + typed plane — PRUNE-1 is the parse_only revert; p1b
correctly carries the W14.* corpora rows in the direct + typed lanes
where the same comparator-misbinding pathology fires (per
`SYNTHESIS-AUDIT-OVERFIT.md:91-97` A2 F8 single-lane fan-out
structural cause), with AUDIT-FALSIFIED verdict on every row. The
cross-artefact consistency is verified.

### §2.5 SK-V14 NEW schema column gap audit (p1f §4.1 finding cross-check)

```
$ grep -nE 'comparator_plane|per_iter_equality|track2_entry_point' \
    restart/skinny/tranches/sk-v14/research/p1/p1*.md
p1b-samply-mode-2.md:318:  (cites SYNTHESIS §241/255)
p1f-results-delta.md:28,172,173,175,189,238,260
```

The new SK-V14 schema columns (`comparator_plane`, `per_iter_equality`,
`audit_overlay_verdict`, `track2_entry_point`) are bound in
`SYNTHESIS.md §240-242` but **absent from RESULTS.md**; p1f §4.1
quantifies the gap as 76 rows × column gap. The CH7 lens reads this
correctly as a **transcription-gap** (RESULTS.md schema lags
SYNTHESIS.md schema) — not a CH7 violation, since the gap is the
target of R1/R2 and S-P3's wave manifest, not a re-introduced
overfit pattern. Filed under §3 for V2 fold attention.

## §3 — Critical findings / observations

### §3.1 No new CH7 violations surfaced

The six P1 outputs carry the audit overlay end-to-end; the audit-pack
prune list is honored on every per-row table; no P1 finding proposes
a new `@generated`-fake / scaffold-as-load-bearing / gate-relabel
admit. The dispatch context §36 four subclauses all pass executable
verification. **CH7 fires CLEAN.**

### §3.2 One observation worth flagging for the aggregator

p1f §4.1 (lines 172-189) correctly identifies the **RESULTS.md
schema gap** vs. the SK-V14 SYNTHESIS §240-242 mandated columns
(`comparator_plane`, `per_iter_equality`, `track2_entry_point` —
the fourth, `audit_overlay_verdict`, is the column the P1 outputs
themselves populate). The gap is 76 rows × 3 absent columns =
228 missing cells in RESULTS.md. This is **not a CH7 violation**
(the gap is the target of R1/R2 not the consequence of overfit
recurrence), but the aggregator should ensure the §4.1 finding
propagates into the consolidated artefact so the SK-V14 SPEC wave
manifest captures the RESULTS.md schema migration as an entry/exit
gate for C-2 (R1+R2).

### §3.3 Co-derivation note on +1 grammar (css_pretty)

The cargo-metadata grammar list confirms `css_pretty` is the 5th
grammar (line 5 of the jq output). A3, A5, A6, and the cargo
metadata cross-confirm the +1 over the V13 audit-pack body's 8-
directory enumeration. The S-P3 wave manifest's PRUNE-4 sub-wave
count of 9 (not 8) per `SYNTHESIS-AUDIT-OVERFIT.md:282-291` rests
on this co-derivation; CH7 reads the four confirmations as
**co-derived from the css_pretty directory addition**, not four
independent regression signals. Risk weighting should treat the
+1 delta accordingly.

## §4 — V2 fold recommendations

### §4.1 No CH7-driven revisions required

The six P1 artefacts are CH7-clean as written. The aggregator may
consolidate without per-artefact CH7 redress.

### §4.2 Two soft enhancements for S-P3 attribution

These do not gate V1 → V2 convergence; they fold naturally into
the existing slate.

1. **Propagate p1f §4.1 RESULTS.md schema-gap finding** to the
   SK-V14 SPEC wave manifest as a C-2 entry/exit gate (R1 + R2 must
   land the new columns alongside the comparator rebind +
   per-iter oracle). This is already implicit in
   `SYNTHESIS-AUDIT-OVERFIT.md §3.1` C-2 mapping (7 findings); the
   p1f extraction confirms the column-gap is a per-row population
   issue, not just a schema-doc issue.

2. **Lift the §2.2 fake-pattern grep into a CI pre-commit check**
   for the `restart/skinny/tranches/sk-v{N}/research/` glob, per
   the §2.4 CH7-companion lint already recommended by
   `SYNTHESIS-AUDIT-OVERFIT.md §2.4` for the
   `skinny/crates/{runtime/src/grammars,codegen/src}/**/*.rs` source
   glob. Extending the same lint to the research-artefact tree
   would prevent any future profile or research output from
   re-introducing the fake `@generated` header in a documentary
   citation that drifts into a load-bearing claim. Recommend
   folding into LOCKS.md as the §2.4 companion lint already
   targets.

### §4.3 V2 readiness

The S-P1 V1 outputs are CH7-READY for §3Z convergence on this lens
(ACCEPT at 100 %). The remaining six lenses (CH1-CH6) author their
own V1 findings; the aggregator consolidates all seven. No CH7-driven
blocker on S-P2 dispatch.

## §5 — Bibliography / cite roster

Files referenced (all paths absolute from repo root):

- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87` (CH7 lens
  definition)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CHALLENGE-CONTEXT.md:36`
  (V1 lens-overlay focus)
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`
  (§0.1, §1.2, §2.4, §3.1, §3.3, lines 70-74, 91-97, 112-134,
  201-211, 282-291)
- `restart/skinny/tranches/sk-v14/research/p1/p1a-samply-mode-1.md`
  (lines 139-149, 204, 210-220)
- `restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md`
  (lines 117-175, 186, 197-213, 318)
- `restart/skinny/tranches/sk-v14/research/p1/p1c-samply-mode-3.md`
  (lines 159-176, 339, 400, 515, 588)
- `restart/skinny/tranches/sk-v14/research/p1/p1d-pmu-cycles.md`
  (lines 164-188, 211-260, 297+, 406, 431-470)
- `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md`
  (lines 13, 17, 90+, 114+, 138+, 188, 197, 199, 278)
- `restart/skinny/tranches/sk-v14/research/p1/p1f-results-delta.md`
  (lines 4, 28, 37, 45-101, 108-133, 144, 158, 172-189, 238, 260,
  397-398)

Executable verification commands (re-runnable):

```
cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'
git grep '@generated by skinny bbnf-codegen' restart/skinny/tranches/sk-v14/research/p1/
grep -c 'audit_overlay\|AUDIT-FALSIFIED\|audit-overlay' restart/skinny/tranches/sk-v14/research/p1/p1*.md
grep -nE 'PRUNE-1|W14\.[1-5]' restart/skinny/tranches/sk-v14/research/p1/p1*.md
grep -nE 'comparator_plane|per_iter_equality|track2_entry_point' restart/skinny/tranches/sk-v14/research/p1/p1*.md
```

All five executable-verification commands re-run in this CH7 pass;
outputs quoted inline at §2.1-§2.5.
