# CH5 HIDDEN-COUPLING — T-P3 SK-V18 cycle (hardening V2)

Verdict: REVISE (two carried/introduced citation-precision defects on the
second-pack and second-classifier EVIDENCE ANCHORS — the two surfaces my lens is
most responsible for; both mechanical, neither a substantive coupling
violation). The substantive hidden-coupling posture is now SOUND and the three
V1 findings are all FOLDED: the v+1 diff applies clean (`git apply --check` =
exit 0, `@@ -622,6 +622,33 @@`, 27 insertions, `LOCKS.md` only — DEFECT-CH5-V1-01
RESOLVED), the relocated-seam second seam is re-pathed to the SKINNY CSS surface
with the totality `crates/core/src/runtime/css_l4/` surface explicitly marked the
SK-V19-adoption seam under the cursor/scanner DEFER bundle (DEFECT-CH5-V1-02
RESOLVED), and the single-substrate falsifier now carries the SKINNY-scope note
folding the totality `simd-scan` `NibbleLut`/`WideLut` into the SK-V19
scanner-unification disposition (DEFECT-CH5-V1-03 RESOLVED in substance). No
parallel substrate / sidecar producer / FactStream-as-shape / runtime-regex
substrate / x86-as-aarch64-close is admitted; the one ACTIVE parallel-scanner
risk (totality `OnceCell<StructuralIndex>` 8/9 + `simd-scan`
`next_structural_at_or_after` probe API) is carried as ONE priced SK-V19 DEFER at
every surface, never a silent drop.

Target packet: working-tree SK-V18 V2-fold extensions over committed `530eb2755`
(`restart/audit/totality/p3/{3A,3B,3C-crystallisation,3C-v+1-diff,3D,3E,3F}` all
`M`, mtimes 20:16–20:27 — re-folded AFTER V1 CH5 at 20:10). This review judges the
working-tree content, the live cycle artefact.

## Lens scope reminder

CH5 = no delta implies a parallel substrate / sidecar producer / renamed-scanner
Lock-1 violation / Track1≡Track2 dishonesty; the substrate union must hold across
every 3A delta; 3C's accepted amendments must introduce no coupling.

## Spot-verification of the most load-bearing deltas

| check | required by lens | result |
|---|---|---|
| Cited finding-id resolves (relocated-seam) | yes | `LAC-1E-V5-02` at `1E:148`; D04 second-seam re-path at `3A:207`,`:29`; resolves. |
| Cited finding-id resolves (parallel scanner) | yes | `COH18-015` at `1F-coherence:104`; the totality `OnceCell` row at `1F-anti-pattern:44`; `1E LAC-1E-V5-01..07` at `1E:147-153` — resolve, richly developed, crate attribution fixed (`OnceCell` in `crates/core` consumer, not `simd-scan`). |
| Cited LOCKS section exists | yes | 16 numbered locks (`grep -cE '^[0-9]+\. \*\*' = 16`); five `BackendShape` `{Eager/Offset/Event/SinkOnly/Collapsed}` at `lower/mod.rs:18-24` (verified verbatim); Lock-1 v+1 ELEVATION at `LOCKS.md:137-149`; SK-V17 addendum `:610-622`; governance boundary `:625`. |
| **The v+1 diff applies** | **yes — PASSES** | `git apply --check /tmp/tp3-locks-v2.diff` = exit 0; `--recount` = exit 0; `--stat` = `LOCKS.md | 27 +++`. Hunk header `@@ -622,6 +622,33 @@` now arithmetically correct. DEFECT-CH5-V1-01 RESOLVED. |
| `RuntimeEmitterKind` fork real (D03/L05-L10) | spot | `grammar_provider.rs:40-42` `enum RuntimeEmitterKind {CompiledLowering, RequestFacts}`; dispatch `:110` — real; the un-fork DELETEs it. |
| `CSS_GENERATED_RS` verbatim courier real (D05/L06) | spot | `runtime_generator.rs:701` `const CSS_GENERATED_RS: &str = r#"` — real. |
| D04 second-seam re-pathed off the totality tree | spot | `3A:207` SECOND SEAM now reads ZERO from SKINNY `css_l4_*`+`runtime_simd.rs`; totality `crates/core/src/runtime/css_l4/` (exists, 7 files) marked SK-V19-adoption seam under D-SKV18-L01/MP.SK19 DEFER. DEFECT-CH5-V1-02 RESOLVED. |
| OnceCell is per-parse, not a retained sidecar | spot | `1F-anti-pattern:44` Lock-1-classifies it `retention_lifetime = generated_function` (per-parse `&mut ScanState`, NOT `retained-across-call-boundary`); `ensure_structural_index` is `get_or_init`, intra-parse; emitted 8/9 (math inert). ADMISSIBLE class, fenced, NOT a violation. |
| Single-substrate skinny falsifier honest (L16) | spot | `rg build_nibble_luts\|find_first_of_nibble_lut skinny/crates/bbnf-simd = 0` (true); canonical SHRN movemask `vshrn_n_u16::<4>` at `aarch64/movemask.rs:5`. |
| `tape/` +4 is the ONE substrate (D12/L13) | spot | re-keyed to per-file provenance over the LIVE 71-census; +4 = `tape/{mod,cursor,arena,record}.rs` tape-fold trace; `find … = 71` confirmed. Canonical substrate, not parallel. |
| REDRESS pre-block 51/53/246/247 second-scanner fence (3D-D08) | spot | `3D:128` enumerates all four; G6 retarget-onto-the-live-shell vs parser-local second scanner discrimination intact — exactly the CH5 distinction. |
| Five `BackendShape`, no FactStream-as-shape (3E) | spot | `lower/mod.rs:18-24` exactly five; `3E-D05`/`D16` keep fact streams DIAGNOSTIC, `BackendShape` grammar-DERIVED not a tag; "a sixth shape added to admit a grammar = overfit." |

## Per-delta disposition under the CH5 lens

Enumerating the deltas that touch hidden coupling. Non-coupling deltas (D02
named-primitive gate, D07 aarch64-delete/x86-prune, D11 leak-census, D13 Sheets,
D14 authority) are CH5-neutral ACCEPT pass-through. CH5-V1 findings now folded
flip three V1 REVISEs to ACCEPT; two NEW/CARRIED evidence-anchor defects raise
two REVISEs.

| delta (artefact) | CH5 disposition |
|---|---|
| `ARCH-3A-V4-SK18-D01` phantom `<G>` strike (3A) | ACCEPT. `<G>` axis DELETE re-anchored on the `Cursor` micro-trait + config-breadth classifier, both VIEWs over the existing `Tape`/`ValueRef`. No new substrate. |
| `ARCH-3A-V4-SK18-D03` un-fork DELETE `RuntimeEmitterKind` (3A) | ACCEPT. Removes the SECOND (source-family) discriminator; routes dispatch through cost-selected `BackendShape`; `generator_grammar_type_count==0` stated as falsifier. Collapses coupling. Fork real at `grammar_provider.rs:40-42`. |
| `ARCH-3A-V4-SK18-D04` relocated-seam firewall (3A) | ACCEPT — DEFECT-CH5-V1-02 RESOLVED. `emit_shape_source==lowered_program` core firewall intact; the SECOND SEAM now scans the SKINNY CSS surface only, with the totality `crates/core/src/runtime/css_l4/` surface explicitly the SK-V19-adoption seam under the same DEFER bundle as D-SKV18-L01/MP.SK19. Scope conflation removed (`3A:207`). |
| `ARCH-3A-V4-SK18-D06` 5-shape positive dispatch axis (3A) | ACCEPT. "A sixth shape added to admit a grammar is overfit." No FactStream-as-shape; five preserved. |
| `ARCH-3A-V4-SK18-D08` CollapsedStage inert slot (3A) | ACCEPT. Bars a `Collapsed` lowerer threading a per-call FSM mask through RETAINED parsing = the streamed-cursor REDRESS 96/97/98 RETIRED prior. Anti-sidecar. |
| `ARCH-3A-V4-SK18-D09` G6 retarget + single-movemask (3A) | ACCEPT (substance). Retarget-the-live-shell, caller-data byte set keeps inner kernel neutral; canonical SHRN movemask. The second-pack evidence-anchor wording is the DEFECT-CH5-V2-01 carrier (REVISE on the 3C clause, not on this 3A delta). |
| `ARCH-3A-V4-SK18-D10` `css_balanced_component_scan` forced demotion (3A) | ACCEPT. Honestly grammar-scoped SHELL name; inner caller-data kernel stays neutral. Defeats neutral-name coupling. |
| `ARCH-3A-V4-SK18-D12` Pattern-H 71-baseline + `tape/` +4 trace (3A) | ACCEPT. The +4 is the ONE shared substrate (`tape/`), recorded as a roster trace; "+N MUST trace or open O(N) scan." 71 confirmed. |
| `D-SKV18-L05-L10-unfork` (3C v+1) | ACCEPT — DEFECT-CH5-V1-02 RESOLVED. The diff clause now states the firewall "scans the skinny CSS surface (`skinny/crates/runtime/src/grammars/css_l4_*` + `runtime_simd.rs`); the totality `crates/core/src/runtime/css_l4/` surface is the SK-V19-adoption seam … NOT a skinny-cycle scan target (CH5-DEFECT-V1-02; SK-V18 = skinny generalization per D14)." Scope conflation gone. |
| `D-SKV18-L16-single-substrate-movemask` (3C v+1) | REVISE — DEFECT-CH5-V2-01 (NEW) + DEFECT-CH5-V2-02 (CARRIED). The SKINNY-scope note that DEFECT-CH5-V1-03 demanded IS now present (substance fixed), but the clause's two EVIDENCE ANCHORS mis-cite: (a) `scalar/byte_class_from_eq_set_64.rs:79-87` — the `scalar/` file is 38 LOC, `:79-87` is out-of-bounds; the `vaddv_u8` shift-add pack at `:79-87` lives in `aarch64/`; (b) `crates/simd-scan/src/lib.rs:68` for `NibbleLut`/`WideLut` — that export is `:67`; `:68` is `{StructuralIndex, next_structural_at_or_after}`. |
| `D-SKV18-L16-retarget-not-author` (3C v+1) | ACCEPT. Retargets the checkasm-gated `byte_class_from_eq_set_64` onto the live shell; byte-set as caller data; `runtime_simd.rs:169`,`:180-204` cites resolve. Single substrate preserved. |
| `D-SKV18-L10-collapsed-slot` (3C v+1) | ACCEPT. Shape-slot only; transient-mask (per-call, no retained side stream) + REDRESS 96/97/98 clearance; `collapsed_stage.rs:16` resolves. Anti-sidecar. |
| `D-SKV18-L01-cursor-generality` (3C v+1) | ACCEPT — the ACTIVE parallel-scanner risk is correctly DEFERRED. The totality `OnceCell<StructuralIndex>` (8/9, per-parse verified) + `simd-scan` `next_structural_at_or_after` probe API are named FUNCTIONALLY PARALLEL with divergent APIs + a skinny-only `parity_hash`, routed to ONE priced SK-V19 disposition (≈+20..+217 LOC). Honest handling CH5 demands. Re-anchor keeps ≥1 e-graph rewrite + feasibility CSP (regression guard) — no zero-rule scaffold. |
| `D-SKV18-L13-pattern-h-recensus` (3C v+1) | ACCEPT. 71-vs-67 honest; +4 traced to the one substrate; the 9-ident `strategy.rs` leak + `css_types.rs` RED self-gate are carried as SK-V19 close, not silently green. |
| `MP.SK19.SCANNER-UNIFY` / `MP-3B-SKV18-D07` DEFER (3B) | ACCEPT. `3B:177`,`:197` "renamed/parallel-scanner risk is ACTIVE … Decide UNIFY vs renamed-parallel-scanner"; `B.W0..B.W4`: "Lock 1 one-substrate forbids the parallel substrate." Explicit, priced, deferred. |
| `3D-D08-substrate-sidecar-lock` (3D) | ACCEPT. Retained structural sidecars, parser-local cursors, public `UnionTape`, sixth shape/substrate all blocked; G6 retarget never parser-local; REDRESS 51/53/246/247 fenced; substrate union Lock-1-authoritative. |
| `3E-D02/D05/D09/D16` CSS-typed / 5-shape fleet matrix (3E) | ACCEPT. Fact streams stay DIAGNOSTIC (no Track1≡Track2 CSS-fact-as-Value-API), five shapes preserved, `BackendShape` grammar-DERIVED not a tag, future-grammar onboarding source/metadata-only with empty generic-owner diff. |
| `3F` §13.6 SK-V19 tee-up + CH3 pre-block carry (3F) | ACCEPT. `3F-MH-003` + `3F:255`,`:265` carry the OnceCell re-route (COH18-015), REDRESS 51/53/246/247, and the SK-V16/V17 reconcile as a Pass-Omega-V6 / pre-W-PRUNE blocker into the SK-V19 GENERALIZE plan. |

## Defects

### DEFECT-CH5-V2-01 (REVISE) — second-pack evidence anchor mis-pathed (`scalar/` → `aarch64/`); cited range out-of-bounds

- Target: `restart/audit/totality/p3/3C-locks-v+1-diff.md:73` (`D-SKV18-L16-single-substrate-movemask`, Evidence line) and `:54` (the carried NEON-classifier-manifest clause), both citing `skinny/crates/bbnf-simd/src/scalar/byte_class_from_eq_set_64.rs:79`-`87`.
- Conflicting evidence: `wc -l skinny/crates/bbnf-simd/src/scalar/byte_class_from_eq_set_64.rs` = **38** (lines 79-87 do not exist). The `vaddv_u8` shift-add `movemask_u8x16` the clause characterizes as the second-pack convention lives at `skinny/crates/bbnf-simd/src/aarch64/byte_class_from_eq_set_64.rs:79`-`87` (87 LOC; `:79` `unsafe fn movemask_u8x16`, `:83`-`:84` `vaddv_u8(vget_low_u8(bits))`/`vaddv_u8(vget_high_u8(bits))`). The crystallisation `3C-locks-crystallisation.md:139` cites it correctly with the bare path `byte_class_from_eq_set_64.rs:79-87` (which resolves to aarch64); the v+1 diff ALONE adds the wrong `scalar/` prefix.
- Why CH5, not merely CH1: this is the EVIDENCE ANCHOR for the live second-`vaddv_u8`-pack convention — the exact second-mask-substrate the L16 single-movemask rule exists to forbid. Spot-verifying it surfaces a genuine coupling fact the clause half-states: there are FIVE distinct private `movemask_u8x16` definitions in skinny `bbnf-simd/aarch64/` (`movemask.rs:4` canonical SHRN; `bracket_depth_mask_64.rs:74`, `comment_body_mask_64.rs:68`, `byte_class_from_eq_set_64.rs:79` all `vaddv_u8` shift-add; `match_tiny_plain_string.rs:100` SHRN). The clause's present-tense "the project's ONE canonical SHRN movemask pack" is aspirational, not current — which is WHY the `bbnf_simd_single_mask_convention` distinct-pack census is correctly declared PLANNED. The mis-path makes the one cite that would let a reader SEE the live second pack un-resolvable.
- Repair: in `3C-locks-v+1-diff.md`, change both `scalar/byte_class_from_eq_set_64.rs:79`-`87` cites (`:73` Evidence line and `:54` manifest clause) to `aarch64/byte_class_from_eq_set_64.rs:79`-`87`. Optionally add a one-clause note that the canonical-SHRN-pack assertion is the PLANNED-co-gate END STATE (live count = 5 distinct `movemask_u8x16` defs, 3 of them `vaddv_u8`), not a current invariant — so the clause does not read as already-converged.
- Owner: 3C author. Severity: medium (the single un-resolvable cite is the second-pack-convention anchor; the substantive single-substrate posture is otherwise honest via the PLANNED census).

### DEFECT-CH5-V2-02 (REVISE) — NibbleLut/WideLut second-classifier anchor off-by-one (`:68` → `:67`); carried verbatim from V1's own conflation

- Target: `restart/audit/totality/p3/3C-locks-v+1-diff.md:73` (the DEFECT-V1-03 SCOPE NOTE: "the totality `crates/simd-scan` exports a second `NibbleLut`/`WideLut` classifier convention (`crates/simd-scan/src/lib.rs:68`)") and the matching cost-matrix cell `3C-locks-crystallisation.md:88`.
- Conflicting evidence: `crates/simd-scan/src/lib.rs:67` = `pub use alphabet::{KernelShape, NibbleLut, StructuralAlphabet, WideLut};` — the second-classifier export. `:68` = `pub use index::{StructuralIndex, next_structural_at_or_after};` — the probe API, a DIFFERENT export. The V2 fold faithfully followed V1's DEFECT-CH5-V1-03 repair directive, which itself cited `:68` for `{NibbleLut, WideLut}` (`V1/CH5.md:89`) while `COH18-015` (`1F-coherence:104`) and `1F-anti-pattern:44` correctly cite `:68` only for `{StructuralIndex, next_structural_at_or_after}`. The off-by-one is inherited from V1's conflation of the two adjacent `pub use` lines.
- Why CH5, not merely CH1: `NibbleLut`/`WideLut` is the totality second-classifier CONVENTION whose existence the scope note exists to disclose (so the skinny `rg = 0` green is never read as a totality single-substrate proof). Citing the probe-API line instead of the classifier-export line points a SK-V19 scanner-unification owner at the wrong symbol when reconciling the two conventions, and risks the two `:68` cites (one correct for the probe API, one wrong for the classifier) being read as the same export.
- Repair: change `crates/simd-scan/src/lib.rs:68` → `:67` in both the `3C-locks-v+1-diff.md:73` scope note and the `3C-locks-crystallisation.md:88` cost-matrix cell (and, for hygiene, in any downstream carry of V1's DEFECT-V1-03 text). Keep `:68` only where the cited symbol is the probe API.
- Owner: 3C author (carried from V1 CH5). Severity: low (scope-honesty sharpening; the totality `NibbleLut`/`WideLut` is correctly deferred to SK-V19 — this binds the cite to the right line so the classifier and the probe API are not read as one export).

## Residual risk (out-of-packet notes, no in-packet REVISE)

- The canonical-movemask cite `aarch64/movemask.rs:5` names the `vshrn_n_u16::<4>` BODY line; the `pub unsafe fn movemask_u8x16` DECLARATION is `:4`. This is a body-vs-signature line choice, consistent across the diff and crystallisation, and the cited line IS the characterizing intrinsic — NOT a defect.
- The ACTIVE parallel-scanner risk (COH18-015) remains correctly deferred but UN-CLOSED until SK-V19 adoption decides UNIFY vs renamed-parallel-scanner. CH5 is satisfied for SK-V18 because the SK-V18 SKINNY tree holds one substrate (one `Tape`/`ValueRef`/`PayloadArena`, one `bbnf-simd` scanner with its own `StructuralIndex`/`scan_dispatch`, no `OnceCell` retention, no `next_structural_at_or_after`); the risk is a classification carry, not a violation, named at every surface (3A Open-Q CH5 `:270`, 3C D-SKV18-L01 DEFER, 3B MP.SK19 `:177`, 3D-D08, 3E, 3F §13.6 + `1F-anti-pattern:44`).
- The stale V1 CHALLENGE-CONTEXT expects "67 Pattern H runtime files"; live is 71, recorded honestly (D-SKV18-D12 / L13). Pass Omega should refresh the invariant to 71 to avoid a future-cycle false REJECT.

TALLY accept=18 revise=2 reject=0
