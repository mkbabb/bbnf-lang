# SK-V13 W12 CHALLENGE - CSS Delimiter SIMD Production Split

Date: 2026-05-21.
Plan: `restart/skinny/tranches/sk-v13/research/w12/plan.md`.
Disposition: ACCEPT with constraints.

## CH1 Correctness

PASS. The selected consumer is the plain CSS declaration-values `scan_block`
loop whose delimiter set is exactly `b"{};"`. The promoted API must return the
same cursor as the scalar byte walk for every cursor/end/tail case. Redress must
run strict caller checkasm and CSS fact-stream equality after wiring.

## CH2 Generality / Lock 14

PASS with boundary. The wave is grammar-neutral only through the `bbnf-simd`
byte-set primitive plus a CSS consumer. It does not legalize JSON policy,
generic-crate behavior, x86 work, or quote-aware scanner rewrites. Runtime and
codegen template edits must match.

## CH3 Regression / REDRESS

PASS with hard REDRESS constraints. REDRESS-126 is production-split history, not
admission. W12 must either complete the split with a measured production
consumer or record measured rejection; a second deferral is REJECT. REDRESS
88/89/96/97/98 keep JSON PMULL/CSSC structural local body swaps out of this
wave.

## CH4 Cost

PASS. The redress cap is the W12 45 + 15 allowance. The intended code delta is
small, but gate/report/orphan-audit work is non-trivial and must stay scoped to
the listed owner paths.

## CH5 Hidden Coupling

PASS with zero-orphan expansion. The redress audit must classify every
`skinny/crates/bbnf-simd/src/aarch64/*.rs` primitive, not only the five
REDRESS-126 demoted rows. Inventory-only/test-only modules such as
`byte_class_from_eq_set_64`, `digit_mac`, `match_tiny_plain_string`,
`quad_load`, and `unescape_uxxxx` x1 must be named as wired, demoted,
test-only, scalar-delegate, or non-admission inventory. `orphan_count_after`
must be zero.

## CH6 Anti-Paper-Close

PASS with measured-close rule. Checkasm, scalar reference, and microbench parity
are necessary but insufficient. `PASS-ADMIT` requires a production CSS consumer
and same-row strict equality/measurement. If the production route does not move
the row, the source patch is saved to `/tmp/skv13-waveW12-rejected.patch`, the
consumer/API source is reverted, and REDRESS records `REJECTED-MEASURED`.

## Accepted Contract

- Gate id: `G-W12-SIMD-ASM-PRODUCTION`.
- Selected primitive/API: `bbnf_simd::find_ascii_set_member64`.
- Selected consumer: CSS L4 declaration-values `scan_block`.
- Required report facts: scalar reference `pass`, strict checkasm `pass`,
  production consumer `wired` or measured-reverted, CSS strict equality, final
  orphan count zero, REDRESS-126 citation, and JSON/CSS guard state.
- Forbidden: JSON parser/tape/codegen edits, PMULL/CSSC body swaps, x86 source,
  quote-aware CSS scanner expansion, support-only primitive admission, retained
  orphan, and second production split.
