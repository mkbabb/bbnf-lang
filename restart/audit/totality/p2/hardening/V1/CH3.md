# CH3 REGRESSION

Lens: CH3 REGRESSION - no REDRESS-refuted route reopens as viable, no admitted
row regresses, no wave-graph dependency cycle is introduced, and deletion /
retirement routes prove replacement provider before deletion consumer per
NEW-CH3-V5-01.

Disposition: REVISE

## Critical Findings

| id | severity | finding | evidence | fold requirement |
|---|---:|---|---|---|
| CH3-V1-01 | medium | 2A mostly treats structural sidecars as refuted, but its Lock 1 amendment wording still says retained cursor/list/class-column sidecars "require fresh row-local material differential." That leaves a REDRESS-refuted route sounding conditionally viable instead of fenced. SK-V15's pre-block list forbids retained cursor/list, aux projection tables, sidecar event vectors, structural streams, public `UnionTape`, and parallel source passes; REDRESS 50/51/53 and 96/97/98 are fences, not inspiration. | 2A refutes retained sidecars at `restart/audit/totality/p2/2A-sota-landscape.md:48` and `restart/audit/totality/p2/2A-sota-landscape.md:84`, but loosens the amendment at `restart/audit/totality/p2/2A-sota-landscape.md:107`; SK-V15 forbids the vocabulary at `restart/skinny/tranches/sk-v15/SPEC.md:147`-`153` and pre-blocks REDRESS 50-55/60-72/96-98 at `restart/skinny/tranches/sk-v15/SPEC.md:475`-`477`; REDRESS rejects parser-owned side tables/cursors at `skinny/REDRESS.md:729`-`740`, `skinny/REDRESS.md:756`-`767`, `skinny/REDRESS.md:807`-`813`, and retires the union-substrate thesis at `skinny/REDRESS.md:2910`-`2950`. | Rewrite 2A LAC-04 so retained cursor/list/class-column/sidecar routes are explicitly pre-blocked for SK-V15. The allowed route should be only transient same-loop masks or generated single-substrate consumption already compatible with the existing tape/direct substrate; any future sidecar-like route must require a new Alpha/P1/SPEC contract, not merely row-local measurement. |

## Evidence Inspected

- Authority and lens contract: `restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md:49`-`56`, `restart/prompts/totality/PASS-2-RESEARCH.md:112`-`115`, `restart/prompts/ORCHESTRATOR.md:83`-`88`, and `restart/audit/totality/p2/T-P2-DISPATCH-CONTEXT.md:138`-`146`.
- SK-V15 regression floor: admitted JSON rows must be maintained while CSS rows are audit-demoted (`restart/skinny/tranches/sk-v15/SPEC.md:126`-`132`); no delete/retirement before rebuild proof (`restart/skinny/tranches/sk-v15/SPEC.md:135`-`145`); dependency rows require provider-before-consumer ordering (`restart/skinny/tranches/sk-v15/SPEC.md:187`-`204`).
- 2A: CSS broadcast, fact-stream, brace-counter, lightningcss wrong-plane, and retained sidecar claims are refuted (`restart/audit/totality/p2/2A-sota-landscape.md:35`-`42`, `:56`-`:59`, `:76`-`:84`); old CSS proof retirement is correctly blocked before typed provider (`restart/audit/totality/p2/2A-sota-landscape.md:106`).
- 2B: source-present SIMD/ASM inventory is not admitted without scalar oracle, strict differential, aarch64 gate, same-wave consumer, and row movement (`restart/audit/totality/p2/2B-primitive-vocabulary.md:39`-`45`, `:58`-`:67`, `:150`-`:155`).
- 2C: CSS broadcast, `CSS_GENERATED_RS`, `CssFullParseSummary`, generic grammar switches, Pattern H deletion, and Lock 14 gate exclusions are refuted or blocked until replacement proof (`restart/audit/totality/p2/2C-grammar-neutrality.md:61`-`:64`, `:116`-`:120`, `:137`-`:141`).
- 2D: zero-rule e-graph, tautological CSP, marker lowerers, AVX-512 close, and CSS broadcast shape-consumer evidence are refuted; five BackendShapes are preserved without a new shape (`restart/audit/totality/p2/2D-cost-model.md:56`-`:64`, `:81`-`:86`, `:102`-`:106`).
- 2E: x86/AVX-512, PMULL/CSSC from ISA/checkasm alone, NEON `svmatch_u8`, and PMU laundering of CSS broadcast are refuted (`restart/audit/totality/p2/2E-host-arch-esoterica.md:71`-`:78`, `:117`-`:120`).
- 2F: runtime regex import, CSS-through-JSON parse-that semantics, CSS broadcast movement, and pre-provider CSS retirement are blocked (`restart/audit/totality/p2/2F-parse-that-gaps.md:71`-`:78`, `:96`-`:101`, `:111`-`:120`).

## Fold Requirements

1. Revise `restart/audit/totality/p2/2A-sota-landscape.md:107` to fence retained cursor/list/class-column/sidecar routes as REDRESS-refuted for SK-V15, not conditionally viable with fresh row-local material differential.
2. Carry the replacement wording into any consolidated T-P2 V1 fold note: transient masks may be producers only when consumed in the single substrate; retained sidecars require a new upstream contract, not this pass.

## Convergence Impact

This issue blocks clean T-P2 V1 convergence under CH3 until folded. It is not a
REJECT because the other five dossiers and most of 2A honor the REDRESS ledger,
preserve admitted JSON rows as guards, and keep CSS/provider retirement ordered
behind replacement proof.
