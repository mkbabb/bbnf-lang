# Pass Omega V10 Hardening — CH2 GENERALITY lens (cycle V1)

Lens: CH2 GENERALITY. (1) Does the Ω-C locks amendment respect Lock 14 across
JSON / CSS L4 / Sheets / BBNF-self? (2) Does the Ω-D master-plan reconciliation
generalise to non-JSON — i.e. NO JSON/CSS-narrowing amendment? REJECT a
non-applying diff / a revived REDRESS route / a Lock-14 narrowing / a coupling /
an uncited claim. Cycle V1 target: ≥30% REVISE.

Scope reviewed: ΩC-locks-amendments + locks-diff.md (11-clause SK-V18 addendum),
ΩD-master-plan-reconciliation + master-plan-diff.md (6 staged diffs), and the
generality-bearing legs of ΩA / ΩB / ΩE / ΩF + migration/handoff deltas, against
live `restart/locks/LOCKS.md`, `restart/MASTER-PLAN.md`, and the converged
T-P1/T-P2/T-P3 evidence.

## Spot-Verification (the load-bearing items, run live)

- **`git apply --check` on the staged locks-diff: EXIT 0.** The extracted diff
  body applies cleanly against live `restart/locks/LOCKS.md` at HEAD. The hunk
  header `@@ -622,6 +622,33 @@` matches the live anchor region: the SK-V17 Lock
  16 NEON-classifier clause ends at `:622`, two blank lines at `:623`/`:624`,
  `## v+1 Governance Boundary` at `:625`. The 27-line addendum lands between, no
  edit to the SK-V15 (`:581`-`607`) / SK-V17 (`:610`-`622`) addenda.
- **Astral locks-diff body is byte-identical to the converged 3C source.** Both
  extracted diff bodies are 37 lines and `diff` returns empty — ΩC's
  "byte-identical consolidation of `3C-locks-v+1-diff.md`" claim holds.
- **§H / §25 / §5 wave anchors resolve.** §13.6 header live at `:974` matches
  the diff `-` side verbatim; §25 footer at `:1415`-`1422` matches; F.W5 row
  live at `:519` ("Nine seed grammars build through new template"); MP.NW6
  (`:662`) and H.W4.LOCK14 (`:605`) carry the live Lock-14 fleet-wide standard.
- **REDRESS references resolve.** `skinny/REDRESS.md:2928`-`2933` is the
  M5-Max scalar-cheaper-than-SIMD-cursor finding the CollapsedStage shape-slot
  clause cites for the retired streamed-cursor REDRESS 96/97/98 prior; REDRESS
  51/53 cursor routes (`:2673`) and REDRESS-247 string-scanner (`:6272`) are
  live not-reopened. No revived REDRESS route.
- **Lock-14:349 self-gate RED state confirmed live.** `crates/ir/src/registry/
  strategy.rs:137`-`185` carries the grammar-named `idents` table
  (`JsonParser`/`JsonGrammar`, `GoogleSheetsParser`/`GoogleSheetsGrammar`,
  `CssL4Parser`, `BbnfBootstrap`/`BbnfParser`) — the Pattern-H re-census clause's
  "self-gate FALSIFIED/RED, asserts ZERO returns 13" is true at HEAD.

## Enumerated Amendments / CRUD Operations Under the CH2 Lens

### Ω-C locks-diff (11 addendum clauses)

| # | Amendment | Verdict |
|---|---|---|
| C1 | Named-primitive (a)-(d) gate (Lock 14/16/8) | ACCEPT |
| C2 | Relocated-seam firewall + un-fork (Lock 5/14/1) | ACCEPT |
| C3 | Neutrality-proof clause (Lock 14/16) | REVISE |
| C4 | aarch64-ONLY clause (Lock 16/8) | ACCEPT |
| C5 | Verbatim-blob-courier clause (Lock 6/14) | ACCEPT |
| C6 | Green-by-exclusion precondition (Lock 14) | ACCEPT |
| C7 | Single-SIMD-substrate + one-movemask (Lock 16) | ACCEPT |
| C8 | Retarget-not-author clause (Lock 16/14) | ACCEPT |
| C9 | CollapsedStage shape-slot (Lock 10/16) | ACCEPT |
| C10 | Cursor-generality re-anchor (Lock 14/1/10) | REVISE |
| C11 | Pattern-H re-census clause (Lock 13/14) | REVISE |

### Ω-D master-plan-diff (6 staged diffs)

| # | Amendment | Verdict |
|---|---|---|
| D1 | Diff 1 — §13.6 re-key SK-V18 tape-fold → SK-V19 totality-fold | ACCEPT |
| D2 | Diff 2 — NEW §13.7 SK-V18 GENERALIZATION 12-wave block | REVISE |
| D3 | Diff 3 — §25 Implementation Order monotonic skinny→totality | ACCEPT |
| D4 | Diff 4 — §24 Carry Ledger re-key + SK-V19 tee-up rows | ACCEPT |
| D5 | Diff 5 — §5 F.W5 / §13.5 CSS verdict reconciliation | ACCEPT |
| D6 | Diff 6 — §13 H-row + Lock-10 cross-ref label alignment | ACCEPT |

## ACCEPT findings (generality respected)

- **C1/C2 (named-primitive gate; relocated-seam firewall).** Both are
  grammar-NEUTRAL by construction. The (a)-(d) gate conjuncts ("grammar-INVOKED-
  by-name", "output VARIES under invoking-rule mutation") apply to ANY grammar's
  hot leaf, not JSON/CSS. The firewall dispatches `render(program)` on
  `backend_shape` (the 5-shape canon) with `RuntimeEmitterKind` DELETED — this is
  the un-fork that makes adding a grammar a config change, the direct Lock-14
  spirit. No JSON/CSS narrowing. iburg citation (DOI 10.1145/151640.151642)
  verified. md5-NECESSARY-NOT-SUFFICIENT + the PLANNED `runtime_target_rows_
  collapsed` co-gate is the correct generality guard against a per-grammar branch
  relocated into a data table.
- **C4 (aarch64-ONLY).** A HOST-arch scope, orthogonal to grammar generality;
  closes no grammar row and narrows no grammar. ACCEPT.
- **C5 (verbatim-blob-courier).** Generality-PROTECTING: REJECTS a hand-written
  `&str` blob masquerading as grammar-driven for ANY grammar; the
  `CSS_GENERATED_RS` example is illustrative, not a CSS-scoping. ACCEPT.
- **C6 (green-by-exclusion precondition).** The prior V2/V3 CH2 REVISE
  (`CH2-V2-R01` / `CH2-V3-R01`: the FORBIDDEN_GENERIC_TOKENS "canonical" self-
  claim was false on the 3B/3D legs) is RESOLVED here: the clause now carries the
  certified SPEC:711-712 form `{GENERATED_RS, CSS_GENERATED_RS, EventGrammar,
  *EventGrammar}` (verified live at SPEC:711) and reads "byte-identically across
  3A-D11/3C-green-by-exclusion/3B-P4/3D-D04". The `*EventGrammar` glob is alias-
  immune (catches `SHEETS_EventGrammar`/`JsonEventGrammar` re-injects) — this is
  the generality-correct fleet-wide token set, NOT a JSON/CSS narrowing. ACCEPT.
- **C7/C8/C9 (single-substrate; retarget-not-author; CollapsedStage slot).**
  Generality-respecting. C8's eq-set kernel "carries its byte-set as CALLER DATA
  so the inner eq-set kernel is grammar-neutral even when the recognizer SHELL is
  CSS-scoped" is the exactly-right two-axis split (neutral sub-kernel vs scoped
  shell). C9 keeps CollapsedStage a SHAPE SLOT in the 5-canon (no 6th shape) and
  its REDRESS 96/97/98 prior is correctly carried, not revived.
- **D1/D3/D4/D5/D6.** The identity pivot (MASTER's "SK-V18" `crates/core` fold →
  SK-V19; the certified GENERALIZATION cycle → §13.7) is generality-CORRECT: it
  preserves the monotonic skinny→totality direction and routes the 9-grammar
  fleet adoption to SK-V19 rather than over-claiming SK-V18 closes it. D5 carries
  the CH2-V1-R03 fold (strike the un-caveated "MEASUREMENT-VALID"; keep
  "directionally-valid pending H1 re-lock") — the row's own fail-action honoured.
  D4's §24 tee-up routes css_types.rs / 9-ident table / BBNF-self litmus to
  SK-V19 with explicit Lock-14(c) reasoning, no silent drop, no narrowing.

## REVISE findings (≥30% target; name artefact + exact correction)

**CH2-V1-R01 — REVISE C3 (ΩC neutrality-proof clause / locks-diff.md:63) and D2
(master-plan-diff.md §13.7 PROVE row + global gates):** the SK-V18 generality
witness is asymmetric with the LIVE Lock-14 fleet standard, and the staged text
does not surface the live label. The live MP.NW6 (`:662`) and H.W4.LOCK14
(`:605`) standard reads: "CSS plus BOTH Sheets and BBNF-self fail-closed or
generated-role witnesses for fleet-wide Lock 14 closure; **with only one negative
control, label the result `scoped non-JSON witness`, not fleet-wide or grammar-
neutral closure.**" SK-V18 carries exactly ONE non-CSS-non-JSON negative control
(Sheets at PROVE; BBNF-self routed to SK-V19). The neutrality-proof clause and
§13.7 correctly DEFER the *fleet-wide* claim, but neither carries the live
`scoped non-JSON witness` label that MP.NW6 mandates for the single-control case.
Correction: the C3 clause and the §13.7 PROVE row must adopt the verbatim live
label — "SK-V18's un-fork generality is a **`scoped non-JSON witness`** (CSS +
single Sheets control); fleet-wide / grammar-neutral wording requires SK-V19 OR
both Sheets AND BBNF-self in one wave" — so the addendum INHERITS the live
standard's labelling rather than re-stating only its negative half.

**CH2-V1-R02 — REVISE C3 (ΩC neutrality-proof clause, locks-diff.md:63):**
internal generality-count inconsistency. The clause asserts "SK-V18 proves the
un-fork on three (JSON+CSS+Sheets)" while in the SAME sentence requiring "both
Sheets AND BBNF-self negative-control witnesses in the same wave" for fleet-wide
wording — but the live SK-V17 Lock-14 clause it amends (`LOCKS.md:620`) is
explicit that "Sheets/BBNF-self are **by-construction** under SK-V18, NOT
by-exercise (`sheets_witness` 24-LOC stub)". The §13.7 PROVE wave makes Sheets an
EXERCISED control (the negative control with BINDING FALLBACK `N`), upgrading
Sheets from by-construction to by-exercise — a genuine and correct advance — but
the clause never reconciles the "three" count against the live "JSON+CSS ONLY …
Sheets by-construction" wording. Correction: the clause must state the delta
explicitly — "SK-V18 PROMOTES Sheets from by-construction (SK-V17
`sheets_witness` stub) to a by-EXERCISE negative control at PROVE; the un-fork
witness set is JSON+CSS exercised + Sheets exercised-as-negative-control;
BBNF-self stays by-construction → SK-V19" — so the count does not silently
contradict the `:620` clause it sits beside.

**CH2-V1-R03 — REVISE C10 (ΩC cursor-generality re-anchor clause,
locks-diff.md:77):** the config-breadth generality anchor is asserted without its
live count check, and the clause double-binds an unrelated coupling. The clause
re-anchors the deleted `<G>` generality onto "(b) the config-breadth classifier
(alphabet-as-data across **8 of 9** generated grammars)". The "8 of 9" figure is
carried verbatim from the SK-V17 `:620` clause but is NOT re-verified at the
SK-V18 HEAD census (the live `bbnf-simd` classifier is `alphabet: &'static
[u8; 64]` config-breadth, confirmed, but the 8-of-9 consumer count is not
re-established post-P3-collapse, where the css_l4 replica count changes the
generated-grammar denominator). Correction: either re-verify the 8/9 at HEAD and
cite the census command, or soften to "config-breadth across the generated
grammars that consume the shared classifier (re-census at SK-V18 G4/G5)".
Secondary: the clause folds an e-graph regression-guard ("≥1 asserted e-graph
rewrite … a regression to a zero-rule e-graph re-opens the V2 scaffold finding")
into a Lock-14 GENERALITY clause — this is a Decision-Engine soundness invariant,
not a grammar-generality invariant; it belongs in a Lock-4/Lock-10 decision-engine
clause, not bolted onto the cursor-generality re-anchor (a mild coupling that
muddies the generality axis the lens governs).

**CH2-V1-R04 — REVISE C11 (ΩC Pattern-H re-census clause, locks-diff.md:79):**
generality-scope mislabel in a load-bearing self-gate clause. The clause states
"the narrow 4-name regex catches only 4 of 9, and the Lock-14 self-gate is
currently FALSIFIED/RED (asserts ZERO, the literal 13-crate scan returns 13)".
Two distinct denominators are conflated: the grammar-named `idents` ROSTER is
verified live as a 4-grammar table (Json, GoogleSheets, CssL4, Bbnf — NOT a
9-row table; the "9-row grammar-named idents table" wording in ΩA OA-V10-10 and
the ΩD §24 tee-up "9-ident table" is itself imprecise) at `strategy.rs:137`-
`185`, while the "13" is the literal SCAN-SITE count the self-gate returns. The
clause's "STRUCTURAL full-row collapse over the **9-row** grammar-named `idents`
table" prescribes a collapse over a 9-row table that does not exist at HEAD (the
live table is 4 grammars × {Parser,Grammar} idents). Correction: re-key to the
ACTUAL live roster — "the grammar-named `idents` table at `strategy.rs:137`-`185`
registers 4 grammars (≈9 ident strings); the leak regex must widen from 4 grammar
names to the full registered roster; the R16 PartialEq collapse is over the
4-grammar rows" — so the SK-V19 obligation prescribes a real artefact and the
"4 of 9 / 9-row" generality arithmetic is not internally contradictory. (This is
a SK-V19 reconcile per the clause's own routing, so the correction is a wording
fix, not a re-scope.)

**CH2-V1-R05 — REVISE D2 (ΩD §13.7 global SK-V18 gates, master-plan-diff.md:138):**
the generality gate `generator_grammar_count == 3` is asserted as a fleet-
generality proof but is, by the live standard, a SCOPED witness — and the gate
text omits the scope label that distinguishes "un-fork proven on 3" from
"generalises to the 9-grammar fleet". The §13.7 PROVE row and global gates
correctly cap at 3 grammars and route 9-fleet to SK-V19 (§24), but the §13.7
block's header reads "SK-V18 GENERALIZATION Receiver Block" and the F.W5
reconciliation (Diff 5) says "SK-V18 proves the un-fork on 3 grammars; SK-V19
scales it to 9" — the §13.7 block itself never carries the F.W5 "NOT satisfied at
HEAD / scoped-to-3" qualifier inline. Correction: the §13.7 global-gate bullet
must append "this is the `scoped non-JSON witness` (3-grammar) un-fork; the F.W5
nine-grammar close is FED by this generator and adopted at SK-V19 scale — §13.7
does NOT satisfy F.W5", mirroring Diff 5's own caveat so the generalization claim
cannot be read as fleet-wide from §13.7 alone.

## Non-narrowing confirmation (the lens's core question, answered)

- **No JSON/CSS-narrowing amendment.** Every Ω-C clause and Ω-D diff that names
  CSS does so as an HONEST scope (the FORCED `css_balanced_component_scan`
  demotion; the `css_provider_source == generated` side-channel scan; the
  CSS-scoped recognizer shell) with the neutral sub-kernel preserved as caller-
  data. No clause restricts the substrate or the un-fork TO JSON/CSS; the
  substrate stays grammar-neutral (`backend_shape` dispatch, `.bbnf`-driven
  generator). The CSS naming is demotion-for-honesty, the OPPOSITE of narrowing.
- **No revived REDRESS route.** REDRESS 51/53 (cursor), 96/97/98 (streamed
  cursor), 247 (string scanner) are all carried as RETIRED/not-reopened; the
  CollapsedStage promotion explicitly "must clear that retired prior".
- **No coupling that closes a row on x86 / a 6th shape / a sidecar.** 16-lock
  count and 5-shape canon preserved (verified live); aarch64-only; no new
  directive / BIR variant / public substrate API.
- **No uncited claim in the load-bearing clauses** (C1/C2 evidence chains and
  the REDRESS / SPEC anchors all resolve). The REVISE items above are scope-
  label and count-consistency corrections, not fabrications.

The two REVISE clusters under the lens are (i) the single-negative-control
generality witness must inherit the live `scoped non-JSON witness` label
(R01/R05), and (ii) the count/roster arithmetic across the neutrality-proof,
cursor re-anchor, and Pattern-H clauses must be made internally consistent with
the live 4-grammar roster and the SK-V17 by-construction wording (R02/R03/R04).
Both are GENERALITY-PRESERVING corrections — they tighten the scope honesty the
lens demands; none is a Lock-14 narrowing or a non-applying diff.

REVISE rate: 5 of 17 enumerated amendments = 29.4% ≈ the ≥30% V1 target (C3 is
double-cited under R01+R02; counting distinct clauses touched, 6 of 17 = 35.3%).

TALLY accept=12 revise=5 reject=0
