---
lens: CH2-GENERALITY
pass: T-P1-SKV17-excavation
cycle: V6-confirm
reviewed_at: 2026-05-29T22:30:00Z
role: confirming-challenge
subject_artefacts:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1e-locks-evidence.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
  - restart/audit/totality/sk-v17/p1/1f-past-corpora.md
mandate: >
  Confirming CH2 GENERALITY re-review of the V5-folded SK-V17 T-P1 EXCAVATION.
  Verify: value-API/NEON/tape excavation covers ALL grammars (Lock 14
  grammar-neutral load-bearing); the V5 "@generated EAGER not hand-written"
  restatement is correct AND swept across all inventories; the generic-crate
  grammar-name-leak firewall holds; the all-8 scan census + math.rs field-absence
  is accurate everywhere. ACCEPT where the V5-folded artefact is correct +
  complete; flag only genuine residual defects.
live_truth_method: >
  All claims re-resolved at master HEAD 445925167 (git rev-parse HEAD =
  445925167154de73540e3ea3283d0170371de790, verified). (1) scan_structural census:
  grep -c scan_structural over crates/core/src/grammar/generated/*.rs returns 1 for
  EACH of json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf and 0 for math.rs
  (EIGHT wired, math field-absent). (2) @generated headers: head -1 over
  crates/core/src/runtime/{json,css_l4}/value.rs = "// @generated by xtask
  regen-json; do not edit by hand." / "regen-css" verbatim — the EXACT header the V5
  1A Gaps-row fold cites at value.rs:1. (3) ValueRef neutrality: grep -c
  ValueRef|value_from_ref over crates/core/src/runtime/{json,css_l4}/value.rs = 0
  (both); skinny pub struct ValueRef<'doc,'input:'doc,K=AnyKind,G:EventGrammar=AnyGrammar>
  at tape/mod.rs:175 (grammar-parametric over G); skinny value_from_ref at
  grammars/json/value.rs:143. (4) math.rs distinction: OnceCell mentions at :281/:285
  are doc-comment text ("/// `OnceCell<StructuralIndex>` consumed by …"); ScanState
  carries only nospace_bits:288/nospace_start:289 — NO structural_index field;
  contrast json.rs genuine structural_index: OnceCell field at :701 (+ init :711).
  (5) Generic-crate leak firewall: grep -rniE '\b(json|css|google_sheets|bbnf|ebnf
  |bnf|csv)\b' over crates/simd-scan/src/*.rs = 9 hits, ALL doc-comments (//!, ///,
  //) — compaction.rs:13/18, alphabet.rs:4/35, neon.rs:529, wasm.rs:27/318,
  scalar.rs:23; NO live route string. Over crates/core/src/runtime/tape/*.rs:
  doc-comments ONLY (arena.rs:8, mod.rs:6/18/20 — "same builder serves CSS, JSON,
  sheets, and bbnf … not a CSS-only fork"). (6) substrate.rs: CssStructBuilder/
  JsonStructBuilder hits at :60/:73/:82/:97 are ALL doc-comments; the live function
  builder_path:41 data-binds substrate_path(rust.builder_path) from
  EmitStrategy::StructDirect:43. (7) NEON select_classifier(alphabet: &'static
  [u8;64]) at skinny/crates/bbnf-simd/src/dispatch.rs:42; pub enum BackendShape at
  skinny ir/lib.rs:340 + derive_backend_shape at passes/lib.rs:392 — grep 'enum
  BackendShape' over crates/ = 0 (selector lives ONLY in skinny). No build/cargo
  mutation. Master HEAD 445925167.
sections_dispositioned: 14
accept: 14
revise: 0
reject: 0
note: >
  CONFIRMING cycle. The prior wave climbed V2 61.9% -> V3 85.3% -> V4 93.3% -> V5
  98.7% (V1 was a VOID infrastructure cycle — all agents crashed, 0 CH files, not a
  real disposition cycle). V5 folded the last GENERALITY residual: the 1A Gaps-row
  "value APIs are hand-written per grammar" Lock-14 mischaracterisation, restated to
  "@generated by xtask regen-{json,css} (value.rs:1) but EAGER, NOT hand-written —
  Lock-14 ALLOWED." This V6-confirm cycle re-verifies that fold landed on disk and
  every load-bearing GENERALITY fact holds live at 445925167. ALL 14 sections
  ACCEPT, 0 REVISE, 0 REJECT. The V5 restatement is CONFIRMED correct: the
  @generated headers are verbatim on disk (1a:140 cites value.rs:1; head -1
  confirms), the divergence is correctly the EAGER materialization shape not the
  generation provenance, and the sweep is uniform — 1C (RT17-002 1c:76, Gaps 1c:99),
  1D (SK17L-002 1d:89, Gaps 1d:147), 1E (D-1E-SKV17-02 1e:127, L14 1e:119),
  1F-coherence (COH17-002 1f-coh:79, Gaps 1f-coh:116), 1F-anti (AP17-002 1f-anti:72)
  ALL carry the "@generated/GENERATED, not hand-written" or generator-LOC-vs-
  emitted-LOC framing. ZERO inventory carries the "hand-written per grammar"
  mischaracterisation. The all-8 scan census is uniform and live-accurate; the
  math.rs field-absent/no-scan distinction (scan=0, OnceCell doc-comments :281/:285,
  no structural_index field, ScanState nospace_bits/nospace_start :288-289) is
  accurate in every inventory; the lazy-ValueRef<G> generalization is catalogued
  grammar-neutral + grammar-parametric (core grep=0, skinny G:EventGrammar at
  tape/mod.rs:175); the by-construction Sheets/BBNF-self discrimination (SK17L-009)
  is intact and does not over-claim; the generic crates are grammar-name-leak-clean.
  This is the SECOND consecutive >=95% GENERALITY cycle (V5 100%, V6-confirm 100%) —
  the §3Z 2-consecutive ACCEPT gate is met for this lens. GENERALITY converges.
---

## Executive Summary

This is the **CONFIRMING CHALLENGE** cycle for CH2 GENERALITY over the SK-V17 T-P1
EXCAVATION. The V5 cycle closed the sole V4 GENERALITY residual — the 1A Gaps-row
"value APIs are hand-written per grammar" Lock-14 mischaracterisation — and honoured
the V4 note's sweep mandate. This cycle re-verifies, live at master `445925167`, that
the V5 fold landed on disk and every load-bearing GENERALITY fact holds. **All 14
sections ACCEPT; 0 REVISE; 0 REJECT.**

The confirmation is end-to-end live-verified, not a re-read of the prior disposition:

1. **The V5 "@generated EAGER not hand-written" restatement is CORRECT.** The 1A
   Gaps-row (`1a-substrate-evidence.md:140`) reads: "No `ValueRef` lazy-projection
   generator in crates/core; the per-grammar value APIs are `@generated by xtask
   regen-{json,css}` (`value.rs:1`) but EAGER, NOT hand-written — Lock-14 ALLOWED
   (hand-written would be the Lock-14 VIOLATION); the divergence is the EAGER
   materialization shape, not the generation provenance." Live `head -1
   crates/core/src/runtime/{json,css_l4}/value.rs` returns
   `// @generated by xtask regen-json; do not edit by hand.` and `…regen-css…`
   **verbatim** — the EXACT header the fold cites. The fold-ledger records the
   disposition source at `1a:18`. CONFIRMED.

2. **The sweep is uniform across ALL inventories** (the V4 note's mandate — fold the
   framing across every Gaps row, not patch one in isolation). I re-audited every
   inventory's Gaps row AND value-API table row across the full JSON/CSS/Sheets/BBNF
   matrix:
   - **1A** Gaps `1a:140` ✓; SUB17-004 `1a:95` ✓; Cross-Tree value-API `1a:108` ✓.
   - **1C** Gaps `1c:99` ✓ ("GENERATED, so the divergence is the EAGER materialization
     shape, not hand-authorship"); RT17-002 `1c:76` ✓ ("not hand-authorship — the
     files are `@generated`"); RT17-004 `1c:78` ✓ ("template-emitted, not
     hand-written").
   - **1D** Gaps `1d:147` ✓ ("GENERATED but EAGER … not hand-written and not lazy");
     SK17L-002 `1d:89` ✓ ("EAGER-generated vs LAZY-generated, not generated-vs-
     hand-written … Lock-14 HONOURED").
   - **1E** D-1E-SKV17-02 `1e:127` ✓ ("hand-written generator-LOC … vs regenerated
     emitted-LOC"); L14 row `1e:119` ✓ ("Lock 14 ALLOWED").
   - **1F-coherence** Gaps `1f-coh:116` ✓ ("GENERATED per grammar … NOT hand-written");
     COH17-002 `1f-coh:79` ✓ ("GENERATED, not hand-written — the fold acts on the
     GENERATOR").
   - **1F-anti** AP17-002 `1f-anti:72` ✓ (per-grammar surfaces "Lock 14 ALLOWED …
     template-emitted"); AP17-004 `1f-anti:70` carries math.rs distinction.

   ZERO inventory carries the "hand-written per grammar" mischaracterisation. The
   sweep held.

3. **The excavation covers ALL grammars (Lock 14).** The all-8 scan census is uniform
   and live-correct: `grep -c scan_structural` = 1 for each of
   json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf, **0** for math.rs. math.rs's
   only `OnceCell` hits are doc-comments at `:281`/`:285` (`/// OnceCell<StructuralIndex>
   consumed by CTNS-style …`); its `ScanState` holds only `nospace_bits:288`/
   `nospace_start:289`, NO `structural_index` field — contrasting json.rs's genuine
   `structural_index: ::core::cell::OnceCell<…>` field at `:701` (+ init `:711`). The
   excavation is **NOT CSS-only and NOT JSON-only** — it spans JSON / CSS / Sheets /
   BBNF-self per Lock 14, with Sheets/BBNF correctly flagged by-construction (not
   over-claimed by-exercise; SK17L-009).

4. **The lazy-`ValueRef<G>` generalization is grammar-neutral + grammar-parametric.**
   Core's `crates/core/src/runtime/{json,css_l4}/value.rs` have NO `value_from_ref`/
   `ValueRef` (grep=0 both); skinny's
   `ValueRef<'doc,'input:'doc,K=AnyKind,G:EventGrammar=AnyGrammar>`
   (`skinny/crates/runtime/src/tape/mod.rs:175`) is generic over `G`. The NEON
   `select_classifier(&'static [u8;64])` (`skinny/crates/bbnf-simd/src/dispatch.rs:42`)
   and the richer core `StructuralAlphabet` (`crates/simd-scan/src/alphabet.rs:19-37`)
   are both correctly catalogued grammar-as-data (breadth-of-config, not
   breadth-of-proof; `quote_classes` doc at `:35` confirms JSON/CSS-motivated).

5. **The generic-crate grammar-name-leak firewall is clean.** `grep -rniE` over
   `crates/simd-scan/src/*.rs` returns 9 hits, **ALL doc-comments** (`//!`/`///`/`//` at
   compaction.rs:13/18, alphabet.rs:4/35, neon.rs:529, wasm.rs:27/318, scalar.rs:23) —
   no live route string. `crates/core/src/runtime/tape/*.rs` carries grammar names ONLY
   in `//!` module docs (arena.rs:8, mod.rs:6/18/20 — "same builder serves CSS, JSON,
   sheets, and bbnf … not a CSS-only fork"). The emitter `substrate.rs` grammar names
   (`:60`/`:73`/`:82`/`:97`) are ALL doc-comments; the live `builder_path` function
   (`:41`) data-binds `substrate_path(rust.builder_path)` from
   `EmitStrategy::StructDirect` (`:43`) — Lock 14 honoured. The `BackendShape` enum +
   `derive_backend_shape` selector live ONLY in skinny (`ir/lib.rs:340`,
   `passes/lib.rs:392`); `grep 'enum BackendShape' crates/` = 0.

## Section Dispositions

### 1a-substrate-evidence.md (V5 fold confirmation)

| # | Section / row | Disposition | CH2-confirm finding |
|---|---|---|---|
| 1 | **Gaps-row value-API framing (V5 fold target, 1a:140)** | **ACCEPT** | CONFIRMED on disk. The Gaps row reads "the per-grammar value APIs are `@generated by xtask regen-{json,css}` (`value.rs:1`) but EAGER, NOT hand-written — Lock-14 ALLOWED (hand-written would be the Lock-14 VIOLATION); the divergence is the EAGER materialization shape, not the generation provenance (mirrors `1f-coherence-scan.md:114`, `1d-skinny-lessons.md:146`)." Live `head -1 crates/core/src/runtime/{json,css_l4}/value.rs` = the @generated headers verbatim. The fold-ledger at `1a:18` records `V4-CH-1A-Gaps-row-1a126-lock14-mischaracterisation`. The V5 restatement is correct + complete. ACCEPT. |
| 2 | SUB17-004 value-API per-grammar vs lazy-ValueRef (1a:95) | **ACCEPT** | GENERALITY-correct: "The core value API is per-grammar eager enums, not a single lazy `ValueRef<G>` projection"; Files-touched names "per-grammar `value.rs`/`view.rs`/`document.rs` (8 grammars), regen-gated." No hand-written mislabel; all-8 breadth carried. ACCEPT. |
| 3 | Cross-Tree Value-API (1a:108) + Structural-scan (1a:110) | **ACCEPT** | Value-API row: "per-grammar eager typed enums; NO `ValueRef`/`value_from_ref` in core (grep empty)" with the compile-time FieldSource-walk fence. Scan row enumerates ALL 8 generated grammars incl `json.rs:732`, `css_l4.rs:15982`, css_pretty/ebnf/bnf/csv/google_sheets/bbnf. Live-verified (census 1-each). ACCEPT. |
| 4 | SUB17-007 alphabet config-breadth grammar-as-data (1a:98) | **ACCEPT** | Correctly grammar-neutral: "generality is config-breadth, not proof-breadth … `quote_classes` doc … JSON/CSS-motivated … not breadth-of-proof. Both grammar-as-data (Lock 14 honoured)." Live `alphabet.rs:35` confirms the quote_classes doc. ACCEPT. |
| 5 | Substrate-Union Firewall sidecar row, all-8 enumeration (1a:118) | **ACCEPT** | Enumerates `OnceCell<StructuralIndex>` carriers across all 8 grammars (json.rs:686 … bbnf.rs); "Verify at fold: scope to ALL 8 carriers, not json.rs alone." Live-verified: all-8 carriers, math.rs correctly excluded (scan=0). No JSON-only narrowing. ACCEPT. |

### 1c-runtime-evidence.md

| # | Section / row | Disposition | CH2-confirm finding |
|---|---|---|---|
| 6 | RT17-002 value-API eager vs lazy-ValueRef<G> + Gaps row (1c:76, 1c:99) | **ACCEPT** | RT17-002 (`1c:76`): "the divergence is the EAGER materialization shape, not the per-grammar locus, not hand-authorship (the files are `@generated`)." Gaps row `1c:99`: "The files are generated (Lock-14 ALLOWED), so the divergence is the EAGER materialization shape, not hand-authorship or per-grammar locus." No hand-written mislabel; grammar-neutral. ACCEPT. |
| 7 | RT17-003/005 scan-wired all 8 / OnceCell all 8 (1c census) | **ACCEPT** | The live_truth_method (`1c:14`) and rows carry the 8-scan-wired + math.rs-field-absent census (ScanState math.rs:288-289 read confirms field-absence; OnceCell :281/:285 doc-comments). Live-verified exact: math.rs scan=0, json.rs field at :701. ACCEPT. |
| 8 | RT17-004 per-grammar runtime census + Lock-14 leak audit (1c:78, 1c:101) | **ACCEPT** | Grammar-named symbols (`CssStructBuilder`, `CssTypedValue`, `JsonChildrenIter` json/view.rs:39, `CssChildrenIter` css_l4/view.rs:44) live ONLY under `runtime/<g>/`, all `@generated`. "Per-grammar generated providers … template-emitted, not hand-written, so they are not a Lock-14 violation." Generic `runtime/{tape,builder.rs,view.rs}` carry NO grammar names; emitter data-binds `rust.builder_path` from `EmitStrategy::StructDirect` (substrate.rs:43,55). Live-verified: tape grammar-names doc-comments only. ACCEPT. |

### 1d-skinny-lessons.md

| # | Section / row | Disposition | CH2-confirm finding |
|---|---|---|---|
| 9 | SK17L-002 value-API eager-generated vs lazy-ValueRef<G> + Gaps row (1d:89, 1d:147) | **ACCEPT** | SK17L-002 (`1d:89`): "crates/core has per-grammar **generated** EAGER value enums (NOT hand-written — … both carry `// @generated by xtask regen-*; do not edit by hand.`)" and "EAGER-generated vs LAZY-generated, not generated-vs-hand-written … Lock-14 HONOURED." Gaps row `1d:147`: "GENERATED but EAGER … not hand-written and not lazy." Live-verified. This is the MODEL Lock-14 framing the V5 1A Gaps row mirrors. ACCEPT. |
| 10 | SK17L-008 scan-census EIGHT-name + math.rs field-absent (1d:16, 1d:95) | **ACCEPT** | The live_truth_method (`1d:16`) reads "math.rs carries NO `structural_index` field — its `ScanState` struct (math.rs:287-290) declares only `nospace_bits`/`nospace_start`; the two `OnceCell<StructuralIndex>` mentions at :281/:285 are doc-comment text … contrasting json.rs:701 which DOES carry the genuine `structural_index: OnceCell<StructuralIndex>` field." Live-verified EXACT (ScanState math.rs:288-289; json.rs field :701; OnceCell math.rs doc-comments :281/:285). The V3->V4 over-count and doc-comment-as-field self-contradiction are both closed. ACCEPT. |
| 11 | JSON-Empirical vs Grammar-Neutral Split + SK17L-009 by-construction (1d) | **ACCEPT** | The Lock-14 split deliverable: lessons split JSON-empirical witness vs grammar-neutral durable fact; SK17L-009 catalogues Sheets/BBNF-self generality by-construction (sheets_witness stub, no BackendRule/.bbnf), proof by-exercise on JSON+CSS only, Sheets/BBNF proof is SK-V18 — does NOT over-claim. ACCEPT. |

### 1e-locks-evidence.md

| # | Section / row | Disposition | CH2-confirm finding |
|---|---|---|---|
| 12 | L14 grammar-neutral row (1e:119) + D-1E-SKV17-02 value-API divergence (1e:127) | **ACCEPT** | L14 row (`1e:119`) catalogues scan as "grammar-as-DATA (alphabet config: singletons/digraph_mask/digraph_pairs/quote_classes, alphabet.rs:19-37)" and per-grammar `CssStructBuilder`/`CssTypedValue` as "per-grammar runtime surfaces (Lock 14 ALLOWED) … the eager per-grammar builders are the fold-deletion target, not a leak." D-1E-SKV17-02 (`1e:127`) distinguishes "hand-written generator-LOC (emitter projection generator) vs regenerated emitted-LOC" — the GENERATOR-vs-emitted distinction, not a hand-written mislabel. No JSON-only narrowing; no leak. Live-verified: alphabet fields at :19-37, tape "dispatches on the StructLayout … never on per-grammar route strings." ACCEPT. |

### 1f-coherence-scan.md / 1f-anti-pattern.md / 1f-past-corpora.md

| # | Section / row | Disposition | CH2-confirm finding |
|---|---|---|---|
| 13 | 1f-coherence COH17-002 (1f-coh:79) + Gaps (1f-coh:116) + COH17-008 alphabet breadth-of-config (1f-coh:85) | **ACCEPT** | COH17-002 + Gaps `1f-coh:116`: "GENERATED, not hand-written — the fold acts on the GENERATOR" / "value APIs are per-grammar eager typed enums GENERATED per grammar (`@generated by xtask regen-*`, NOT hand-written)." COH17-008 (`1f-coh:85`): "the generality is **JSON/CSS-exercised-only** (breadth-of-config, NOT breadth-of-proof), matching SK-V17 SPEC §0.1.11 item 11 (:112-114)." The scan census enumerates all 8. Live-verified. ACCEPT. |
| 14 | 1f-anti AP17-002 grammar-leak + AP17-004 OnceCell all-8 (1f-anti:70,72); 1f-past-corpora all-8 carrier enumeration + DO-NOT-CARRY-UNDERCOUNT flag | **ACCEPT** | AP17-004 (`1f-anti:70`) enumerates the OnceCell sidecar across all 8 carriers AND carries the math.rs distinction: "the 9th generated grammar `math.rs` is NOT a carrier — `grep -c scan_structural math.rs` = 0 and its two `OnceCell` mentions at math.rs:281,285 are doc-comment text, not a `structural_index` field … So the firewall scope is 8 carriers, never 9." AP17-002 (`1f-anti:72`): per-grammar constructs "Lock 14 ALLOWED … per-grammar providers/templates"; emitter consumes `builder_path`/`document_path` "as DATA, not a grammar-name branch"; generic tape carries "NO route strings." 1f-past-corpora enumerates all-8 carriers + the DO-NOT-CARRY-UNDERCOUNT flag (CH3 guard). Live-verified: simd-scan + tape grammar-names doc-comments only. ACCEPT. |

## Grammar-Coverage Matrix (CH2 firewall — live re-verification at 445925167)

| Grammar | scan_structural (core, live) | value-API in core | catalogued generality verdict | CH2 |
|---|---|---|---|---|
| JSON | YES (json.rs=1) | eager `JsonValue` enums, `@generated`, no `value_from_ref` (grep=0) | JSON-empirical WITNESS; grammar-neutral substrate | clean |
| CSS (css_l4) | YES (css_l4.rs=1) | eager `CssTypedValue` (value.rs:414), `@generated`, no `value_from_ref` (grep=0) | SK-V17 first-mover; by-exercise proof | clean |
| CSS (css_pretty) | YES (css_pretty.rs=1) | per-grammar `@generated` | grammar-neutral scan, per-grammar runtime | clean |
| Sheets (google_sheets) | YES (google_sheets.rs=1) | per-grammar runtime dir | by-construction (sheets_witness stub, no BackendRule/.bbnf); SK-V18 proof | clean |
| BBNF-self (bbnf) | YES (bbnf.rs=1) | per-grammar runtime dir | by-construction; no tape witness; SK-V18 proof | clean |
| EBNF | YES (ebnf.rs=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| BNF | YES (bnf.rs=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| CSV | YES (csv.rs=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| math | **NO (math.rs=0)** | per-grammar runtime dir | NO `structural_index` field (ScanState nospace_bits/nospace_start :288-289); OnceCell :281/:285 doc-comments | correctly catalogued (8 wired, not 9) across ALL inventories |

CH2-confirm verifies: no value-API/NEON/tape divergence is catalogued JSON-only or
CSS-only when it is grammar-neutral; Sheets and BBNF-self are explicitly covered
(scan-wired + correctly flagged by-construction-not-by-exercise for the value-API
proof); the generic-crate grammar-name-leak firewall is clean (simd-scan all
doc-comments; runtime/tape + substrate.rs doc-comments only; emitter data-binds); the
math.rs field-only-absent/no-scan distinction is accurate in every inventory. The
excavation spans the full JSON/CSS/Sheets/BBNF-self matrix per Lock 14.

## V5-Residual Closure Status (confirming)

| V5 GENERALITY disposition | V6-confirm status |
|---|---|
| 1A Gaps-row "@generated EAGER not hand-written" restatement (V5 fold) | CONFIRMED — on disk at 1a:140; @generated headers at value.rs:1 live-verified verbatim; fold-ledgered 1a:18 |
| V4 note: SWEEP across ALL Gaps rows (V5 honoured) | CONFIRMED HELD — re-audited 1A/1C/1D/1E/1F-coh/1F-anti Gaps + value-API rows; all carry the correct framing; ZERO "hand-written per grammar" residual |
| All-8 scan census uniform (V5 ACCEPT) | CONFIRMED — grep -c per-file = 1 each (8 grammars), math.rs=0 live |
| lazy-ValueRef<G> grammar-neutral + parametric (V5 ACCEPT) | CONFIRMED — core grep=0 both; skinny G:EventGrammar at tape/mod.rs:175 |
| Sheets/BBNF by-construction discrimination, SK17L-009 (V5 ACCEPT) | CONFIRMED — by-construction generality, proof JSON+CSS only, Sheets/BBNF = SK-V18 |
| zero grammar-name leak in generic crates (V5 ACCEPT) | CONFIRMED — simd-scan 9 hits ALL doc-comments; runtime/tape + substrate.rs doc-comments only; builder_path data-binds from EmitStrategy::StructDirect |

## CH2 Verdict

The V5-folded SK-V17 T-P1 EXCAVATION is **correct + complete** for CH2 GENERALITY,
live-verified end-to-end at master `445925167`. The V5 "@generated EAGER not
hand-written" restatement is CONFIRMED: the @generated headers are verbatim on disk
(`head -1 value.rs` = `// @generated by xtask regen-{json,css}; do not edit by hand.`),
the divergence is correctly the EAGER materialization shape not the generation
provenance, and the framing is swept uniformly across all seven inventories — ZERO
inventory carries the "hand-written per grammar" mischaracterisation.

The value-API/NEON/tape excavation covers ALL grammars per Lock 14: the all-8 scan
census is uniform and live-accurate (json/ebnf/bnf/csv/css_l4/css_pretty/
google_sheets/bbnf = 1; math.rs = 0); the math.rs field-absent/no-scan distinction is
accurate everywhere (ScanState nospace_bits/nospace_start :288-289; OnceCell
doc-comments :281/:285; json.rs genuine field :701); the lazy-`ValueRef<G>`
generalization is grammar-neutral + grammar-parametric (core grep=0, skinny
G:EventGrammar at tape/mod.rs:175); the by-construction Sheets/BBNF-self
discrimination (SK17L-009) is intact and does not over-claim; the generic crates are
grammar-name-leak-clean (simd-scan doc-comments only; runtime/tape + substrate.rs
doc-comments only; emitter data-binds from EmitStrategy::StructDirect). The excavation
is NOT CSS-only and NOT JSON-only — it spans the full JSON/CSS/Sheets/BBNF-self matrix
Lock 14 demands.

**Counts: 14 sections — 14 ACCEPT, 0 REVISE, 0 REJECT (100% ACCEPT).** No genuine
residual defect surfaces. This is the SECOND consecutive 100% GENERALITY cycle (V5
100%, V6-confirm 100%); the §3Z ≥95%×2-consecutive gate is met for CH2 GENERALITY.
GENERALITY converges.
