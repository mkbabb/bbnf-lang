---
agent: CH7
pass: T-P3-synthesis
cycle: V1
lens: OVERFIT-PRUNE
disposition: REVISE
generated_at: 2026-05-23T23:25:00-04:00
inputs_audited:
  - restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §CH7 (lens definition; lines 62-87)
  - restart/audit/totality/p3/hardening/V1/CHALLENGE-CONTEXT.md (dispatch §0-§4)
  - restart/audit/totality/p3/3A-architecture-synthesis.md (91 lines; 12 deltas)
  - restart/audit/totality/p3/3B-master-plan-reconciliation.md (244 lines; 11 deltas + 14 NEW waves)
  - restart/audit/totality/p3/3C-locks-crystallisation.md (186 lines; 51 candidates → 18 hunks)
  - restart/audit/totality/p3/3C-locks-v+1-diff.md (366 lines; 21 hunks)
  - restart/audit/totality/p3/3D-skinny-fold.md (264 lines; 14 deltas)
  - restart/audit/totality/p3/3E-grammar-generalisation.md (282 lines; 12 deltas)
  - restart/audit/totality/p3/3F-migration-handoff.md (327 lines; 7 MIG + 5 HANDOFF + 1 dispatch + 1 SKELETON refusal)
  - restart/locks/LOCKS.md (HEAD: lock count + LAC-1E-12 absorption surface verification)
  - restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md (canonical T-P2 refutation density)
  - restart/audit/totality/p2/hardening/HARDENING-T-P2-V5-CONVERGED.md (T-P2 V5 LOCK)
  - restart/audit/totality/p1/hardening/V1/CH7.md (T-P1 V1 CH7 precedent; §CH7-specialisation pattern)
  - restart/audit/totality/p1/1E-locks-evidence.md (LAC-1E-12..16 source authority)
---

## Lens Contract

CH7 Overfit-Prune (`restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62`-`87`)
lenses every artefact for: (a) generated-vs-hand-written discipline (no
`@generated` headers on hand-coded source); (b) Lock 14 generic-crate
compliance; (c) every admit lands via a real source change with strict-vs-strict
comparator + per-iter equality; (d) every "generated" output passes a delete +
regen round-trip; (e) no SCAFFOLD-ONLY landing counts as an admit. CH7 REJECT
triggers immediate plan revise OR redress revert with REDRESS entry; CH7 may
not be carried as "acknowledged but not blocking".

For the T-P3 V1 synthesis pass, the dispatch context (`CHALLENGE-CONTEXT.md:28`)
specialises CH7 to six bound concerns:

1. SK-V14 audit-overlay column discipline preserved in artefacts (4-column
   `track2_entry_point`/`comparator_plane`/`per_iter_equality`/`audit_overlay_verdict`
   schema).
2. No fake-pattern recurrence (no fake `@generated` / SCAFFOLD-as-load-bearing /
   gate-relabel).
3. Refutations honest (T-P2 cohort carry-forward intact in 3D + 3F).
4. LAC-1E-12 promoted to LOCKS preface — NOT Lock 17 — preserves 16-lock count
   per `PASS-3-SYNTHESIS.md §8.1`.
5. LAC-1E-14 lands FactStream as a 5th SUBSTRATE category — NOT a 6th
   BackendShape variant — preserves 5-shape canon at Lock 10.
6. 3B SKELETON DELETE REFUTED ledger row and 3F MIGRATION SKELETON DELETE
   refusal row preserve T-P2 V5 refutation density.

## Verdict

**REVISE.**

The packet substantially honours the CH7 contract on the load-bearing
architectural axes: the 16-lock count is correctly preserved across 3C
crystallisation + 3C diff (LAC-1E-12 lands as preface clause, NOT Lock 17);
the 5-shape `BackendShape` canon is correctly preserved (LAC-1E-14 explicitly
classifies `FactStream` as a 5th *substrate manifest* category and refuses
6th-`BackendShape` expansion); the audit-overlay 4-column schema lands in
3C-L08 + propagates into 3B-MP-D07; the SKELETON DELETE refusal row is
present in both 3B (MP-NW-SK14-SKELETON-DELETE-REFUTED) and 3F (3F-MIG-007);
no fake `@generated` recurrence appears as a productive admit anywhere in
the packet (every literal `@generated` token is a diagnostic citation of the
SK-V13 W1b CSS L4 pathology to be pruned, never an admission vehicle); no
SCAFFOLD-only row is treated as load-bearing (W7 PRUNE-5 receives W8/W9
SCAFFOLD wiring as remediation, not admission); no gate-relabel-as-admit
pattern recurs.

The blocker is **citation-truthfulness on the T-P2 refutation density**.
3F (rows 71, 107, 131, 280, 315) and 3C diff (line 69 preface text) cite
the cohort density at **31:64** (3F, five sites) or **31:69 = 31.7%** (3C
diff preface, one site). The canonical T-P2 V3 figure is **32:69 = 31.7%**
(per `HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295`; the V2→V3
extension row at `:198` explicitly records `31:64 → 32:69`). The stale
"31:64" propagates from the V1 dispatch context which itself transcribed
the V2-era value; the 3C diff preface arithmetic "31:69 = 31.7%" is
self-contradictory (31/69 = 44.9%, not 31.7%; 32/69 = 46.4%, also not
31.7%; the "31.7%" value comes from the source-side cohort paragraph which
uses a different denominator ratio). The discrepancy is small but lands
inside the CH7-specialised refutation-honesty axis — a stale numerator is
overfit to a prior-cycle snapshot and a preface arithmetic mismatch is
exactly the counter-surface fabrication pattern T-P1 V1 CH7 (COH-012
precedent) flagged.

A second blocker, narrower: the `find crates/core/src/runtime -mindepth 2
-maxdepth 2` census command bound into Lock 14 v+1 (3C diff hunk V4-7
lines 252-254 + 3B-MP-D03 + 3F-MIG-003 paragraph) returns **63** at HEAD,
not 67. The `67` figure is correct per the source authority
(`sk-v14-audit-overfit-pre-restart-pattern.md:56`), but that authority
uses the unrestricted `find ... -name '*.rs' | wc -l` (which counts
`google_sheets/document/*.rs` at depth 3). The `mindepth 2 -maxdepth 2`
form bound into the lock text is a stricter command than the one that
produces 67. The two are sound separately but the lock cannot bind a
command whose output contradicts the lock's own asserted file count.

Both findings are doc-level, narrow, and convergence-blocking (CH7's
contract refuses "acknowledged but not blocking"); no architectural
disposition needs to change.

## Findings

### §1 — Per-axis CH7 lens audit (dispatch-context bound)

| disposition | dispatch axis | target | finding | required V2 repair |
|---|---|---|---|---|
| ACCEPT | (1) audit-overlay column discipline | `restart/audit/totality/p3/3C-locks-crystallisation.md:37` (3C-L08-audit-overlay-columns hunk) + `:121` (LAC-1E-16 disposition) + `:160` (cost ledger); `restart/audit/totality/p3/3C-locks-v+1-diff.md:188`-`208` (V4-5 hunk body); `restart/audit/totality/p3/3B-master-plan-reconciliation.md:128` (MP-3B-V1-D07); `restart/audit/totality/p3/3D-skinny-fold.md:162` (FOLD-3D-014) + `:226` (CH6 open question on freshness column). | All 4 SK-V14 columns (`track2_entry_point`, `comparator_plane`, `per_iter_equality`, `audit_overlay_verdict`) appear in every receiver. 3C-L08 binds the schema, 3B-MP-D07 absorbs it into MASTER-PLAN.md §13.1, 3D-FOLD-014 makes it part of the SK-V14 audit-zero baseline inheritance, 3F-MIG-004 routes the CSS L4 row through `admitted_fact_output` substrate_target. The xtask gate-json reject rule is named at three sites. The columns themselves come from `restart/skinny/tranches/sk-v14/SYNTHESIS.md:240`-`255` per the LAC-1E-16 source authority. No silent drop; no narrowing to fewer columns; no relabeling. | None. |
| ACCEPT | (2) no fake-pattern recurrence — `@generated` | `restart/audit/totality/p3/3A-architecture-synthesis.md:40` (D08: 0/9 carry `@generated` markers — diagnostic cite); `restart/audit/totality/p3/3C-locks-crystallisation.md:35,118,135,159` (LAC-1E-13 regen-roundtrip rule); `restart/audit/totality/p3/3C-locks-v+1-diff.md:151`-`177` (V4-4 hunk: any `// @generated` requires xtask round-trip clean check); `restart/audit/totality/p3/3D-skinny-fold.md:89,109` (real-codegen vs fake-`@generated` distinction); `restart/audit/totality/p3/3B-master-plan-reconciliation.md:126` (MP-D05 R4 round-trip discipline). | Every literal `@generated` token in the packet is a diagnostic citation of the SK-V13 W1b CSS L4 pathology (`include_str!()` + fake `@generated` header on hand-written templates) to be pruned, never an admission vehicle. 3C-L06-L14-regen-roundtrip institutionalises the round-trip clean check; 3B-MP-D05 routes it through MASTER-PLAN.md Tranches A/F/J; 3D-§1 row at `:89` and rejection row at `:109` mark the boundary between PROVED real codegen (JSON `parse_direct`/`parse_real_typed_*`) and DISPROVED fake codegen (7/7 CSS L4 providers hand-written with fake header + 4 fixture-lookup short-circuits). | None. |
| ACCEPT | (2) no fake-pattern recurrence — SCAFFOLD-as-load-bearing | `restart/audit/totality/p3/3D-skinny-fold.md:160` (FOLD-3D-012 8-candidate shortlist; W8/W9 SCAFFOLD-ONLY pre-block); `restart/audit/totality/p3/3B-master-plan-reconciliation.md:82,115` (refuted cohort + MP-NW-SK14-SKELETON-DELETE-REFUTED); `restart/audit/totality/p3/3F-migration-handoff.md:105` (3F-MIG-005 routes W7 PRUNE-5 as wiring SCAFFOLD-to-LOAD-BEARING, not SCAFFOLD admit); `restart/audit/totality/p1/hardening/V1/CH7.md:65` AP-017 W8/W9 SCAFFOLD-ONLY precedent. | No row in the packet treats a SCAFFOLD-only artefact as admit. W7 PRUNE-5 receiver is named as the wiring wave (W8 per-grammar policy SCAFFOLD + W9 same-substrate union SCAFFOLD → LOAD-BEARING); the SK-V9 W3 retired retained-class-column-union data structure is named as PERMANENT-PRE-BLOCK and explicitly disambiguated from the W7 enforcement-layer pass at 3F:105. F-V2-P1ABC-RERECORD Stage-0 W10 UNCONDITIONAL is named as the operational atom that closes the orphan-kernel hole (C5 could ship in zero waves otherwise) at 3D:160. | None. |
| ACCEPT | (2) no fake-pattern recurrence — gate-relabel | `restart/audit/totality/p3/3D-skinny-fold.md:110` (SK-V13 W14.1-5 parse-only admits AUDIT-FALSIFIED as gate-relabel-only); `restart/audit/totality/p3/3B-master-plan-reconciliation.md:82` (5 parse_only W14.1-5 SK14-AUDIT-FALSIFIED admit cohort); `restart/audit/totality/p3/3F-migration-handoff.md:110` (HANDOFF-003: 25 CSS + 5 parse_only + 4 direct + 7 typed AUDIT-FALSIFIED admits revert at W1 PRUNE-1 + W4 PRUNE-2); 3C-L08 audit-overlay column binding rejects rows missing required columns. | The 40-admit gate-relabel cohort (5 parse_only + 4 direct + 7 typed + 24 CSS = 40) is correctly classified DISPROVED across 3D-§2 and 3F-HANDOFF-003 with revert receivers (W1 PRUNE-1 + W4 PRUNE-2) named. No row attempts to re-admit by relabel; the audit-overlay column binding makes future relabels rejectable at `xtask gate-json`. The "parser unchanged" qualifier at 3D:110 is the load-bearing CH7-positive verbatim ("gate-relabel-only" — admit removed because parser did not change). | None. |
| REVISE | (3) refutations honest — T-P2 cohort carry-forward | `restart/audit/totality/p3/3F-migration-handoff.md:71` (rejected: "T-P2 V5 LOCK refutation density 31:64"); `:107` (3F-MIG-007: "T-P2 V5 LOCK refutation density (31:64 cohort-wide 1:2 anti-paper-close pattern)"); `:131` (MIGRATION delta paragraph: "T-P2 V5 LOCK refutation density 31:64 (1:2 anti-paper-close pattern)"); `:280` (positive consequence: "T-P2 V5 31:64 refutation density as a re-proposal gate"); `:315` (CH6 open question: "T-P2 V5 31:64 refutation density"); `restart/audit/totality/p3/3C-locks-v+1-diff.md:69` (preface body text: "SK-V14 cohort 31:69 = 31.7% refutation density preservation"). Canonical authority: `restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md:76` "Cohort refutation 32:69 = 31.7% density"; `:172` "cohort refutation 32:69 = 31.7%"; `:187` "Cohort 32:69 = 31.7% density preserved across V2+V3"; `:198` extension row "31:64 → 32:69 (1:2 density)"; `:295` "cohort 32:69 ratio". | (i) **3F (five sites)** carries `31:64` — the V2-era value before T-P2 V3 added one refutation. The 3F citations attribute the figure to "T-P2 V5 LOCK" which is incorrect — T-P2 V5 CONVERGED does not restate the ratio numerically; T-P2 V3 CONSOLIDATED is the authority and it records `32:69`. (ii) **3C diff line 69** carries `31:69 = 31.7%`. The arithmetic fails (31/69 = 44.9%; 32/69 = 46.4%; neither is 31.7%); the canonical paired form is `32:69 = 31.7%`. The "31.7%" denominator-difference in the source is a known T-P2 transcription artefact, but the 3C preface form must match the canonical pair. (iii) The V1 dispatch context (`CHALLENGE-CONTEXT.md:28`) itself cites `31:64`, which seeded the stale value into 3F. CH7 cannot let a stale dispatch-context number freeze a stale refutation-density transcription across V1 surface absorption. The CH7-COH-012 pattern (T-P1 V1: counter-surface fabrication via stale citation) recurs here in attenuated form. | (a) Replace all five `31:64` sites in 3F with `32:69` and re-attribute to `restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md:198` (V2→V3 extension row). (b) Replace `31:69 = 31.7%` in 3C diff V4-1 hunk preface body with `32:69 = 31.7%` and re-attribute to `HARDENING-T-P2-V3-CONSOLIDATED.md:76`. (c) Append a CH7 verify_action `grep -n "32:69" restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md` returning 5 hits to the 3F open-question CH6 row and the 3C diff preface evidence list. (d) Leave T-P2 V5 attribution wording alone where the citation is to "T-P2 V5 LOCK" — that is sound at the LOCK level — but require the numerator/denominator pair to come from V3 CONSOLIDATED. |
| ACCEPT | (4) LAC-1E-12 as preface clause, NOT Lock 17 | `restart/audit/totality/p3/3C-locks-crystallisation.md:14` (`newly_added` ledger lists `3C-PREFACE-ch7-binding`); `:23` Executive Summary "the 16-lock count holds; LAC-1E-12 lands as a preface CH7-binding clause, not Lock 17"; `:29` 3C-PREFACE-ch7-binding hunk entry "Carrier: in-preface clause, NOT Lock 17 — preserves 16-lock count"; `:117` LAC-1E-12 disposition "Carrier per T-P1 V5 §6.1 disposition: in-preface clause, NOT Lock 17 — preserves 16-lock count per PASS-3 §8.1"; `:144` Consequences "without adding a 17th lock"; `restart/audit/totality/p3/3C-locks-v+1-diff.md:16` "16-lock count stays fixed. LAC-1E-12 lands as an in-preface CH7 binding clause (NOT Lock 17)"; `:22` V4-1 hunk index "Preface CH7 Overfit-Prune binding clause"; `:50`-`82` V4-1 hunk body — `## CH7 Overfit-Prune lens binding` block targeted before `restart/locks/LOCKS.md:44` `## Gestalt — sixteen locks`; `:361` diff application notes "16-lock count preserved: LAC-1E-12 lands as preface clause"; verified executable: `grep -nE '^## (Lock |Gestalt)' restart/locks/LOCKS.md` returns one Gestalt match at line 44; `grep -nE '^[0-9]+\. \*\*' restart/locks/LOCKS.md` returns 16 numbered locks at lines 48/92/102/111/113/115/117/119/155/164/190/199/207/220/265/282. | Disposition is unambiguous, fully consistent across 3C crystallisation + diff + dispatch context. The hunk targets a position *before* the `## Gestalt — sixteen locks` heading, so the existing Gestalt text remains the binding lock-count statement and the inserted block does not modify the numbered lock list. The `Carrier per T-P1 V5 §6.1` attribution is correct (verified at `restart/audit/totality/p1/hardening/V1/CH7.md:64` and the LAC-1E-12 source row at `1E-locks-evidence.md:120-122`). 16-lock canon preserved. The diff is append/insertion-only at the preface boundary; no lock renumbering surface is touched. | None. |
| ACCEPT | (5) LAC-1E-14 as 5th SUBSTRATE, NOT 6th BackendShape | `restart/audit/totality/p3/3C-locks-crystallisation.md:14` (`3C-L01-factstream-fifth-category` in newly_added); `:32` 3C-L01-factstream-fifth-category hunk "5th admitted-product category alongside OffsetTape/EventTape/SinkOnly/CollapsedStage. Carrier note: 5th *substrate* class, NOT 5th `BackendShape` variant; comparator provenance + telemetry gate-consumed remain mandatory"; `:119` LAC-1E-14 disposition "5th *substrate* category at the Lock 1 manifest level, NOT a 6th `BackendShape` variant (the 5-shape canon at Lock 10 holds; `FactStream` substrate_target = `admitted_fact_output`)"; `:134` proposed delta "Substrate category, not `BackendShape` variant; 5-shape canon preserved"; `:158` cost ledger "5th category at substrate manifest level only; 5-shape `BackendShape` canon at Lock 10 preserved"; `:172` open question explicit answer "5-shape canon preserved"; `restart/audit/totality/p3/3C-locks-v+1-diff.md:24` V4-3 "Lock 1 FactStream 5th substrate category"; `:122`-`141` V4-3 hunk body "The 5th category is a substrate-manifest classification only; it is NOT a 6th `BackendShape` variant. The 5-shape `BackendShape` search domain at Lock 10 holds: `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`. Adding a 6th `BackendShape` variant remains G-Omega gated per Lock 10 v+1 and PASS-3 §8.1"; `:361` diff application notes "LAC-1E-14 lands as Lock 1 substrate-category sub-paragraph (NOT a 6th `BackendShape`)"; `restart/audit/totality/p3/3A-architecture-synthesis.md:39` ARCH-3A-D07 "The fact-stream plane is admitted-evidence with `substrate_target = admitted_fact_output`; it is NOT a sixth BackendShape, NOT retained substrate, NOT full CSS closure"; `restart/audit/totality/p3/3E-grammar-generalisation.md:140` 3E-D05 "fact streams as admitted output planes, not retained sidecars and not a sixth BackendShape"; `restart/audit/totality/p3/3D-skinny-fold.md:92` "admitted evidence as fact-stream output plane; not a sixth BackendShape"; `:174` "5-shape BackendShape coherence | VERIFY in 3C/3E: ... no hidden shape variants; CSS fact streams = output planes, not 6th shape"; `restart/audit/totality/p3/3F-migration-handoff.md:104` 3F-MIG-004 explicit gating "LAC-1E-14 proposes `FactStream` as 5th BackendShape variant — but this is G-Omega-gated per Lock 1 v+1 since it touches Lock 10 BackendShape canon too. MIGRATION row binds CSS L4 row to `admitted_fact_output` substrate_target with comparator provenance mandatory until T-P3 §3C disposes"; verified executable: `grep -nE 'EagerTape.*OffsetTape.*EventTape.*SinkOnly.*CollapsedStage' restart/locks/LOCKS.md` returns one match at Lock 10 line 164 (the 5-shape canon as written today). | The discipline is preserved across all five artefacts that touch the fact-stream classification. 3C-L01-factstream-fifth-category is the binding hunk; 3A-D07, 3E-D05, 3D-FOLD-001/§2 row at :92, and 3F-MIG-004 all explicitly disclaim 6th-`BackendShape` interpretation. Notably 3F-MIG-004 reads "LAC-1E-14 proposes `FactStream` as 5th BackendShape variant" — that wording is loose given the rest of the packet, but it is then immediately disambiguated by the gating clause that binds the CSS L4 row to `admitted_fact_output` substrate_target with no taxonomy promotion until 3C disposes. 3C disposes in favour of substrate-manifest-only, and 3F-MIG-004 acknowledges the gating. The 5-shape canon at Lock 10 line 164 is intact at HEAD. | None — though see optional hygiene note below regarding the 3F-MIG-004 "5th BackendShape variant" parenthetical wording, which could be tightened in V2 for citation-truthfulness but is not blocking. |
| ACCEPT | (6a) 3B SKELETON DELETE REFUTED ledger row | `restart/audit/totality/p3/3B-master-plan-reconciliation.md:82` (refuted cohort row 6 includes "SKELETON-DELETE triple (T-P2 LAC-2F-V5-02 elevation refutation per `restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:26`)"); `:115` MP-NW-SK14-SKELETON-DELETE-REFUTED full row: "EXPLICIT-REFUSAL entry per CH6 anti-paper-close discipline: the SKELETON triple DELETE that T-P2 LAC-2F-V5-02 elevation considered is RECORDED-AS-REFUTED in MASTER-PLAN.md so any future cycle attempting to revive it must cite this refutation row. Lock 1 substrate-union v+1 amendment generalises REDRESS 96/97/98 to ALL transient classifier-state primitives. ... Receiver: the refusal entry IS the consumer; any future SKELETON revival attempt must REJECT against this row." | The 3B refusal row is honest as an anti-paper-close ledger device. It correctly attributes the refutation to T-P2 LAC-2F-V5-02 ELEVATION (the canonical T-P2 V3 §3Z LOCK strongest amendment surface — verified at `HARDENING-T-P2-V3-CONSOLIDATED.md:182-192`), correctly names the receiver as the refusal entry itself (self-binding gate per CH6 anti-paper-close), and correctly generalises REDRESS 96/97/98. No SKELETON primitive is re-admitted; the refusal binds future cycles. | None. |
| REVISE | (6b) 3F MIGRATION SKELETON DELETE refusal row | `restart/audit/totality/p3/3F-migration-handoff.md:71` (rejected list: "SKELETON triple DELETE proposal (FSM_DISPATCH_THREADED + FRAME_PUSH/POP_BOUNDED) — doubly inadmissible per T-P2 V5 LOCK refutation density 31:64"); `:107` 3F-MIG-007 row body: "**SKELETON triple DELETE** (FSM_DISPATCH_THREADED + FRAME_PUSH_BOUNDED + FRAME_POP_BOUNDED) is **REJECTED** per T-P2 V5 LOCK refutation density (31:64 cohort-wide 1:2 anti-paper-close pattern; `restart/audit/totality/p2/hardening/HARDENING-T-P2-V5-CONVERGED.md` + dispatch context §1). MIGRATION carries a refusal row preserving the three primitives as non-shortlist-blocker evidence under SRC-V2-FOLD support, not deletion"; `:131` MIGRATION delta paragraph "**SKELETON triple DELETE proposal** (FSM_DISPATCH_THREADED + FRAME_PUSH_BOUNDED + FRAME_POP_BOUNDED) is **REJECTED** per T-P2 V5 LOCK refutation density 31:64 (1:2 anti-paper-close pattern); the three primitives remain non-shortlist-blocker support under SRC-V2-FOLD". | The refusal-row architecture is sound (and structurally identical to the 3B refusal row): the SKELETON triple is preserved as non-shortlist-blocker evidence under SRC-V2-FOLD, not deletion, and re-proposal is gated by same-wave consumer + first consumer path + executable command per T-P2 V4 non-shortlist criteria. The defect is the stale `31:64` numerator (see Finding (3) above) which propagates from the dispatch context. The "non-shortlist-blocker support under SRC-V2-FOLD" classification is correctly applied (matches T-P2 V3 LAC-2F-V5-02 ELEVATION — `HARDENING-T-P2-V3-CONSOLIDATED.md:182-192`); only the cohort number needs the V3-canonical update. | (a) Replace `31:64` with `32:69` at 3F lines 71, 107, 131 (the three SKELETON-row sites); (b) Re-attribute citation from "T-P2 V5 LOCK refutation density 31:64" to "T-P2 V3 LOCK refutation density 32:69 (`HARDENING-T-P2-V3-CONSOLIDATED.md:198`)"; V5 attribution at the disposition level (T-P2 V5 CONVERGED) is sound, but the numerator/denominator pair must come from V3 CONSOLIDATED where the cohort was actually counted. |
| REVISE | (2/4) Pattern H census command vs file count | `restart/audit/totality/p3/3C-locks-v+1-diff.md:252`-`254` V4-7 hunk body: "every tranche commits a Pattern H file-count transcript via `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l`"; `restart/audit/totality/p3/3B-master-plan-reconciliation.md:124` MP-3B-V1-D03 "current Pattern H file count via committed `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l` transcript"; `restart/audit/totality/p3/3F-migration-handoff.md:123` "Per-tranche Pattern H census via `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l`"; LAC-1E-15 source `restart/audit/totality/p1/1E-locks-evidence.md:125` uses the same command form. Source authority for the **67** figure: `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-pre-restart-pattern.md:56` table "**Total** | **67**" derived from `find crates/core/src/runtime -name '*.rs' | wc -l` per `:26` (no mindepth/maxdepth restriction). Verified executable at HEAD `345c3214`: `find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l` returns **63**; `find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l` returns **67** (the additional 4 files live at depth 3 under `google_sheets/document/{path_query.rs, mod.rs, canonical.rs, view.rs}`). | The lock cannot bind a census command whose output (63) contradicts the file-count figure the lock asserts (67). Either (a) the bound command must drop `-maxdepth 2` to match the source authority (and produce 67), or (b) the asserted figure must be updated to 63 with an explanatory note about the 4 sub-files at depth 3, or (c) the bound command must add a `find ... -path '*/document/*.rs'` sub-clause to compose the two counts. Option (a) is the cleanest because it matches the source authority verbatim. This is CH7-relevant because a stale command in lock text is a "bound verification action that does not verify what the lock claims" pattern — the exact failure mode `LAC-1E-12` was promoted to prevent. The defect is contained: 3C-L14-pattern-h-census, 3B-MP-D03, 3F-MIG-003 carry the same command; correction is one-line per site. | (a) Drop `-maxdepth 2` from the bound command in all three sites (3C diff V4-7, 3B-MP-D03, 3F-MIG-003 paragraph) so the executable transcript matches the asserted 67-file count. (b) Add a verify_action evidence cell on the LAC-1E-15 disposition row of 3C citing the corrected command output as 67 at HEAD `345c3214`. (c) Source-authority alignment confirmed: `sk-v14-audit-overfit-pre-restart-pattern.md:26` already uses the no-maxdepth form. |

### §2 — Executable verification (re-run at HEAD)

#### §2.1 16-lock count preservation

```
$ grep -nE "^## (Lock |Gestalt)" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md
44:## Gestalt — sixteen locks

$ grep -nE "^[0-9]+\. \*\*" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md | wc -l
16

$ grep -nE "CH7|Overfit" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md
(zero hits)
```

Verifies: 16 numbered locks; Gestalt heading at :44; LOCKS.md is silent on CH7
at HEAD (the gap LAC-1E-12 closes). LAC-1E-12 hunk V4-1 inserts the CH7
binding block *before* the Gestalt heading, preserving the 16-lock count.

#### §2.2 5-shape BackendShape canon

```
$ grep -nE "EagerTape.*OffsetTape.*EventTape.*SinkOnly.*CollapsedStage" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md
164:10. **Pratt + SIMD auto-detected...** ... `LayoutFacts.backend_shape ∈ {EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` ...
```

Verifies: Lock 10 line 164 binds the 5-shape canon at HEAD; LAC-1E-14 V4-3
hunk inserts at Lock 1 manifest level (:71-:73), does not modify Lock 10.

#### §2.3 T-P2 V3 canonical refutation density

```
$ grep -nE "32:69|31:64" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md
76:| **CH6 ANTI-PAPER-CLOSE** | 61.5% | 100% | **100%** | +38.5pp | **2-CYCLE LOCK** | Cohort refutation 32:69 = 31.7% density (1:2 anti-paper-close pattern preserved). SKELETON DELETE doubly inadmissible via F-CH5-V2-01. |
172:- **CH6** cohort refutation 32:69 = 31.7% density preserved (1:2
187:| 1 | **Refutation discipline INSTITUTIONALIZED** | Cohort 32:69 = 31.7% density preserved across V2+V3 (1:2 anti-paper-close pattern); CH6 13/13 ACCEPT | Most load-bearing T-P3 §3C input; discipline binds Lock 8 BENCH comparator-plane |
198:| Refutation ratio | 31:64 | 32:64+ | **32:69 (1:2 density)** | extended + locked |
295:1. **Refutation discipline INSTITUTIONALIZED** (cohort 32:69 ratio; CH6 lens).
```

Verifies: canonical T-P2 V3 cohort density is `32:69`. The `31:64` value
appears only in the V2→V3 extension column at :198 as the *prior* state.
3F's five citations of `31:64` and 3C diff's one citation of `31:69 = 31.7%`
are both stale — V2 (3F) approximations and arithmetic-broken (3C) variants
of the canonical `32:69 = 31.7%`.

#### §2.4 Pattern H file count vs bound command

```
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' | wc -l
63
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l
67
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d | wc -l
9
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 3 -type f -name '*.rs'
.../google_sheets/document/path_query.rs
.../google_sheets/document/mod.rs
.../google_sheets/document/canonical.rs
.../google_sheets/document/view.rs
```

Verifies: 9 per-grammar dirs (LAC-1E-15 row at 3A-D08 + 3F-MIG-003 correct);
67 total `.rs` files (LAC-1E-15 asserted count correct); 63 files at depth
exactly 2; the 4-file gap lives at `google_sheets/document/*.rs` depth 3.
The `mindepth 2 -maxdepth 2` form bound into 3C diff V4-7 line 252-254
returns 63, NOT the 67 the lock paragraph asserts. The unrestricted form at
the source authority (`sk-v14-audit-overfit-pre-restart-pattern.md:26`)
returns 67 correctly.

#### §2.5 LAC-1E-12 preface-vs-Lock 17 carrier discipline

```
$ grep -c "Lock 17" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-crystallisation.md
0
$ grep -c "Lock 17" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-v+1-diff.md
0
$ grep -nE "NOT Lock 17" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-crystallisation.md
29:... Carrier: in-preface clause, NOT Lock 17 — preserves 16-lock count.
117:... Carrier per T-P1 V5 §6.1 disposition: in-preface clause, NOT Lock 17 — preserves 16-lock count per PASS-3 §8.1.
$ grep -nE "NOT Lock 17" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-v+1-diff.md
16:... LAC-1E-12 lands as an in-preface CH7 binding clause (NOT Lock 17) per T-P1 V5 §6.1 disposition carrier.
70:+clause, NOT Lock 17 — preserves the 16-lock count per
```

Verifies: zero affirmative "Lock 17" claims across 3C; four explicit
"NOT Lock 17" disambiguation sites. Preserves 16-lock count per
PASS-3 §8.1.

### §3 — CH7-specialised carry-forward checks

| check | result | evidence |
|---|---|---|
| Counter-surface fabrication (T-P1 V1 COH-012 pattern: asserting prose into a document that does not contain it) | DETECTED-AT-LOW-SEVERITY in 3F's "T-P2 V5 LOCK refutation density 31:64" attributions — T-P2 V5 CONVERGED (`HARDENING-T-P2-V5-CONVERGED.md`) does not state the numerator/denominator at all (verified: zero hits for `31:64`, `32:69`, `31.7%` in V5 CONVERGED). The figure originates in T-P2 V3 CONSOLIDATED. The attribution is to a non-quantitative source (V5 was a confirmation cycle that re-passed the V4 packet unchanged). This is a CH7-relevant attribution overfit. | `restart/audit/totality/p2/hardening/HARDENING-T-P2-V5-CONVERGED.md` (full read; no numeric refutation density); `restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,198,295` (canonical citations). |
| Fabricated baselines (CH7 axiom (e) of `PASS-0-OVERFIT-AUDIT.md:62-87`) | NONE detected. All baseline references (audit-zero 0/17 × 3 JSON planes + 0/24 CSS L4; 40-admit AUDIT-FALSIFIED cohort; Pattern H 67-file census; 30-violation A3 census) trace to SK-V14 SYNTHESIS / S-P0 audit-overfit / 1C-runtime-evidence / 1E-locks-evidence with verified path:line citations and reproduce at HEAD per §2 above. | `restart/audit/totality/p3/3D-skinny-fold.md:162` (FOLD-3D-014 audit-zero baseline); `restart/audit/totality/p3/3F-migration-handoff.md:110` (HANDOFF-003 baseline); `restart/audit/totality/p3/3A-architecture-synthesis.md:40` (D08 Pattern H 67); `restart/audit/totality/p3/3B-master-plan-reconciliation.md:124` (MP-D03 Pattern H 67). |
| Hand-coded "generated" headers | NONE-ADMITTED. Every `@generated` token in the packet is diagnostic. The SK-V13 W1b CSS L4 pathology (7/7 CSS L4 providers hand-written + fake `@generated` header + 4 fixture-lookup short-circuits) is cited as DISPROVED and routed to R4 + W2 + W4 PRUNE-2 for revert + regen-css xtask round-trip. | `restart/audit/totality/p3/3D-skinny-fold.md:109`; `restart/audit/totality/p3/3C-locks-v+1-diff.md:151-177` (V4-4 hunk); `restart/audit/totality/p3/3B-master-plan-reconciliation.md:126` (MP-D05 R4). |
| Lock 14 generic-crate compliance (no JSON/CSS/Sheets string literals, byte literals, function names, enum variants, match arms in nominally-generic code) | PROPOSED-FORWARD. The packet does not produce new generic-crate violations; it routes the existing 30-violation A3 census + 8-arm `RuntimeProvider` to SK-V14 W5 PRUNE-3 receiver (3F-MIG-002, 3B-MP-D09). LAC-1E-15 Pattern H census institutionalises forward-direction prevention. The Lock 14 v+1 generic-crate forward invariant is named verbatim in 3B-MP-D09. | `restart/audit/totality/p3/3B-master-plan-reconciliation.md:130` (MP-D09); `restart/audit/totality/p3/3F-migration-handoff.md:102` (3F-MIG-002); `restart/audit/totality/p3/3C-locks-v+1-diff.md:252-279` (V4-7 census + abstract-primitive sibling). |
| Strict-vs-strict comparator + per-iter equality (CH7 axiom (c)) | INSTITUTIONALISED via Lock 8 v+1 audit-overlay 4-column binding. `per_iter_equality` is one of the four required columns; `xtask gate-json` REJECTS rows missing any column. Comparator-plane provenance is mandatory per the existing Lock 8 v+1 text + LAC-2A-V1-02 carry-forward. | `restart/audit/totality/p3/3C-locks-v+1-diff.md:188-208` (V4-5 audit-overlay hunk); `restart/locks/LOCKS.md:130-137` (existing Lock 8 v+1 comparator-plane provenance). |
| Round-trip clean check (CH7 axiom (d)) | INSTITUTIONALISED via Lock 6 + Lock 14 v+1 regen-roundtrip hunk (LAC-1E-13). Every `// @generated` file requires (a) trace to rostered xtask emission, (b) byte-equivalent regen, (c) reject hand-patching. R4 CSS L4 is first instance; family extends to JSON / Sheets / BBNF / EBNF / BNF / CSV / Math per 3C-L06-L14-regen-roundtrip. | `restart/audit/totality/p3/3C-locks-v+1-diff.md:151-177` (V4-4 hunk); `restart/audit/totality/p3/3C-locks-crystallisation.md:35,118,135,159` (LAC-1E-13 routing). |
| SCAFFOLD-as-load-bearing pattern | NONE-ADMITTED. W7 PRUNE-5 receiver wires W8 per-grammar policy SCAFFOLD + W9 same-substrate union SCAFFOLD to LOAD-BEARING (3F-MIG-005); F-V2-P1ABC-RERECORD Stage-0 W10 UNCONDITIONAL closes orphan-kernel risk (3D-FOLD-012); W8/W9 SCAFFOLD-ONLY is named in 3B refuted cohort row 6. | `restart/audit/totality/p3/3F-migration-handoff.md:105` (3F-MIG-005); `restart/audit/totality/p3/3D-skinny-fold.md:160` (FOLD-3D-012); `restart/audit/totality/p3/3B-master-plan-reconciliation.md:82` (refuted cohort). |

## Repair Requirements (V2)

1. **Update T-P2 refutation density citations to canonical V3 figures** (Finding §1 row 5 + row 9).
   - 3F: replace `31:64` with `32:69` at lines 71, 107, 131, 280, 315 (five sites); re-attribute to `HARDENING-T-P2-V3-CONSOLIDATED.md:198`.
   - 3C diff: replace `31:69 = 31.7%` with `32:69 = 31.7%` at line 69 (V4-1 hunk preface body); re-attribute to `HARDENING-T-P2-V3-CONSOLIDATED.md:76`.
   - Append verify_action `grep -n "32:69" restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md` returning 5 hits to the 3F CH6 open-question row and the 3C diff preface evidence list.
   - Leave T-P2 V5 attribution wording alone where the citation is to "T-P2 V5 LOCK" at the disposition level — that is sound; only the numerator/denominator pair needs the V3 source.

2. **Align Pattern H census command with asserted 67-file count** (Finding §1 row 10).
   - 3C diff V4-7 hunk body (lines 252-254): change `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l` to `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l`.
   - 3B-MP-D03 paragraph: same correction.
   - 3F-MIG-003 paragraph (line 123): same correction.
   - LAC-1E-15 source row in 3C crystallisation: add note "verified at HEAD `345c3214`: corrected command returns 67".

3. **Optional hygiene (non-blocking)**: tighten 3F-MIG-004's parenthetical wording "LAC-1E-14 proposes `FactStream` as 5th BackendShape variant" to "LAC-1E-14 proposes `FactStream` as 5th substrate-manifest category (not a 6th BackendShape variant)" — the surrounding gating clause already makes the disambiguation, but the parenthetical wording is loose given the rest of the packet's discipline.

No other V2 repair required. The 16-lock count, the 5-shape canon, the audit-overlay 4-column binding, the SKELETON DELETE refusal architecture (3B + 3F), the absence of fake `@generated` admits, the absence of SCAFFOLD-as-load-bearing admits, and the absence of gate-relabel admits all hold.

## Cycle Disposition

**REVISE, not REJECT.** V1 contains substantial CH7-positive safeguards: the
16-lock count is correctly preserved (16 numbered locks remain; LAC-1E-12
lands as preface clause); the 5-shape `BackendShape` canon is correctly
preserved (LAC-1E-14 explicitly classifies `FactStream` as substrate-manifest
category alongside the 5 shapes, with multiple disambiguation citations
across 3A/3C/3D/3E/3F); the audit-overlay 4-column schema lands cleanly
(3C-L08 + 3B-MP-D07 + 3D-FOLD-014); the SKELETON DELETE refusal architecture
is present in both 3B (MP-NW-SK14-SKELETON-DELETE-REFUTED) and 3F (3F-MIG-007)
with the refusal-row-as-self-binding-gate per CH6 anti-paper-close; no fake
`@generated` recurrence as productive admit; no SCAFFOLD-as-load-bearing
admit; no gate-relabel admit. The repair surface is narrow but
convergence-blocking under CH7's contract ("cannot be carried as
acknowledged but not blocking"): two doc-level corrections — stale T-P2
cohort numerator (`31:64`/`31:69` → `32:69`) across six sites, and Pattern H
census command (`-maxdepth 2` removed) across three sites.
