# SK-V15 W4-C Authority And Dependencies

W4 authority is narrow: repair Pattern H provenance and root runtime
regen/check proof. It does not authorize CSS typed provider work, old CSS proof
retirement, Decision Engine work, or broad runtime deletion.

Binding SPEC lines:

- W4 manifest row: risk High, manual LOC 120-280, generated runtime checks,
  entry gate W2 and W3 admitted/routed, exit gate true provenance plus
  non-writing regen/check proof.
- W4 tasks: restore Pattern H provenance over the 67 root runtime files, add
  non-writing check and delete-plus-regen proof before destructive runtime
  deletion, reject header-only generated status.
- W4 exit: the `find -mindepth 2` count returns 67, all 67 intended files carry
  true line-1 generated provenance, and generator/check proof can reproduce
  them or emits intrinsic block.

Dependency rows consumed:

- `DEP-W4-PATTERN-H-PROVENANCE`: truth repair is allowed; fake/header-only
  close is blocked. Proof requires the 67-file count, line-1 provenance scan,
  and non-writing regen check.
- `DEP-W4-W6-CSS-LEGACY-RUNTIME-SHIM`: CSS `LegacyPath` / `LegacySegment`
  removal is allowed in W4 only with replacement proof; otherwise it remains
  blocked until W5/W6 typed CSS proof.

No destructive root runtime deletion is allowed unless W4 first proves
delete-plus-regen through the new non-writing check route. Header presence is a
gate input, not proof by itself.

## CHALLENGE Risks

Reject a W4 plan if it:

- adds `@generated` text without generator ownership;
- treats line-1 marker presence as sufficient proof;
- uses the stale `-maxdepth 2` Pattern H count;
- deletes or replaces root runtime files before same-wave regen/check proof;
- removes `LegacyPath` / `LegacySegment` without same-wave replacement proof;
- closes on documentation-only evidence.
