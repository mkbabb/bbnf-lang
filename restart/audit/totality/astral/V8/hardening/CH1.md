# Pass Omega V8 CH1 Correctness

Date: 2026-05-26.
Lens: CH1 correctness.
Disposition: REVISE.

## Findings

1. The load-bearing citations resolve. REDRESS-212 records no retained
   frontend/codegen/xtask source redress and routes the correction through Pass
   Omega V8 (`restart/skinny/tranches/sk-v14/research/skv14-W5B-FRONTEND-redress.md:9`-`18`).
   The V2 challenge is correctly cited as 2/7 ACCEPT with CH1, CH2, CH4, CH5,
   and CH6 still REVISE (`restart/skinny/tranches/sk-v14/research/skv14-waveW5B-FRONTEND-challenge/V2/HARDENING-SKV14-W5B-FRONTEND-V2-CONSOLIDATED.md:1`-`19`).
2. The proposed V8 amendment correctly captures the structural cause: one-shot
   W5B-FRONTEND cannot close the full frontend/import/IR surface under the
   current cap, so the honest route is SPEC/Omega sub-waves or a narrowed
   non-closing slice (`restart/skinny/tranches/sk-v14/research/skv14-waveW5B-FRONTEND-challenge/V2/HARDENING-SKV14-W5B-FRONTEND-V2-CONSOLIDATED.md:28`-`41`).
   The W5B.0..W5B.4 split in the corrective packet and master-plan diff matches
   that evidence (`restart/skinny/tranches/sk-v14/research/skv14-W5B-FRONTENDR-corrective-packet.md:79`-`97`,
   `restart/audit/totality/astral/V8/master-plan-diff.md:7`-`26`).
3. CH1 does not converge because V8 underfolds the V2 CH1 exactness blockers.
   The consolidated V2 result requires lowering-table owner file/type, exact
   fail-closed tests, exact Lock 14 tests, per-log nonzero proof, and
   maintain/SPEC resolution (`restart/skinny/tranches/sk-v14/research/skv14-waveW5B-FRONTEND-challenge/V2/HARDENING-SKV14-W5B-FRONTEND-V2-CONSOLIDATED.md:13`).
   CH1 repeats the same required fold at
   `restart/skinny/tranches/sk-v14/research/skv14-waveW5B-FRONTEND-challenge/V2/CH1.md:40`-`46`.
   V8 carries the wave split and broad "exact grammar tests" wording, but it
   does not require owner file/type per construct, exact fail-closed test names
   for every construct, or per-test/per-log nonzero assertions in the proposed
   SPEC/master/dispatch folds.

## Required Folds

1. Add a CH1-binding W5B lowering-table requirement to the V8 SPEC/master-plan
   fold: every construct row must carry `construct`, `owner file/type`,
   `target representation`, `exact positive test`, and `exact fail-closed
   test`. Prose fail-closed cells are not acceptable.
2. Add exact W5B.0 Lock 14 test requirements, not only generic "tests prove"
   language: roster admits only W5B owner paths, W5C/W5D subjects reject,
   modified provider/template files reject, all `_templates` paths are guarded,
   and `grammar_provider.rs` remains the only provider-adjacent W5B exception.
3. Add per-test/per-log nonzero proof to the W5B.1..W5B.4 gates. A wildcard
   `/tmp/skv14-w5b-*.log` aggregate assertion remains rejected.
4. Preserve the maintain fold as already routed: either SPEC-authorized exact
   no-diff for W5B's non-admit capability sequence, or fresh SK-V14-open
   full-table maintain evidence inside W5B.4.
5. Carry the consolidated LOC fold explicitly: redress report edits and
   reject-only `skinny/REDRESS.md` edits count when touched.

## Verdict

REVISE. The wave-graph amendment is directionally correct and the cited
evidence resolves, but CH1 cannot accept until the exact test/table/nonzero and
LOC-accounting folds above are carried into the proposed amendment.
