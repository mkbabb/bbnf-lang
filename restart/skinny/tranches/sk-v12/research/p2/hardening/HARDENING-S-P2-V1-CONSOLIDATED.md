# SK-V12 S-P2 CHALLENGE V1 — Consolidated

Pass: S-P2 Research. Cycle: V1.
Date: 2026-05-20.
Scope: consolidate the six-lens CHALLENGE review over the committed S-P2 research cohort.
Disposition: REVISE.

## §1 — Dispositions

| Lens | Disposition | File | Load-bearing result |
| --- | --- | --- | --- |
| CH1 Correctness | REVISE | `hardening/V1/CH1.md` | Comparator strictness and ISA sourcing are acceptable, but P2-A has broken local paths, P2-C has two speculative ISA candidates, and P2-D overcounts diagnostics as current candidates. |
| CH2 Generality | ACCEPT | `hardening/V1/CH2.md` | Lock 14 framing is sufficient: candidates are grammar-neutral, conditional, oracle-only, accounting-only, or rejected. |
| CH3 Regression | ACCEPT | `hardening/V1/CH3.md` | No REDRESS-blocked route is reopened as implementation authority; the packet is research inventory only. |
| CH4 Cost | REVISE | `hardening/V1/CH4.md` | P2-A, P2-C, P2-D, and P2-F do not consistently carry scalar-reference status, checkasm/parity expectation, and same-wave-consumer/proof notes. |
| CH5 Hidden Coupling | ACCEPT | `hardening/V1/CH5.md` | Lock 1 holds: no parallel substrate, sidecar producer, retained cursor, or Track 1 equals Track 2 collapse is admitted. |
| CH6 Anti-Paper-Close | REVISE | `hardening/V1/CH6.md` | P2-A has uncitable local references and non-grounded candidate entries; P2-D counts a future retained-view diagnostic as a current candidate. |

Accept rate: 3 / 6 = 50.0%.

Cycle verdict: REVISE. S-P2 does not converge on V1.

## §2 — Required Fold

The V2 research fold must resolve these items before the next CHALLENGE cycle:

1. `p2a-sota-teardown.md`
   - Correct stale local sources: `skv12-p1-capture-manifest.md` and `skv12-p1-replay.tsv` must point under `research/p1/`; `restart/prompts/ORCHESTRATOR.md:374` and `:474` must be replaced with live line references; the SK-V11 P2 convergence path must include `research/p2/hardening/`.
   - Add explicit candidate accounting for C1-C7: scalar-reference status or scalar sketch/current executable reference, checkasm/parity expectation or `N/A`, and same-wave-consumer note. If an entry cannot carry that triad, demote it to support-only comparator evidence.

2. `p2c-arch-esoterica.md`
   - Demote `a64_ld4_interleaved_classifier64x4` to ISA inventory unless it supplies a current S-P1 hot-leaf/source-line antecedent for an already-interleaved stream and a scalar deinterleave oracle.
   - Demote `a64_sha3_ternary_bool_fold` to ISA inventory unless it supplies a current S-P1 hot-leaf/source-line antecedent for a real three-input boolean fold and an explicit scalar formula.
   - Add an explicit same-wave-consumer note for each remaining C1-C8 entry or mark the entry non-selectable support inventory.

3. `p2d-substrate-tape.md`
   - Reframe `offset_tape_capacity_policy`, `sparse_flag_lookup_policy`, and `retained_cursor_skip_projection` as diagnostics/ineligible under current SK-V12 S-P1 unless fresh profile evidence names those exact loci.
   - Keep `structural_class_lane_union` rejected.
   - Add checkasm/parity status and same-wave proof/consumer notes where any same-tape diagnostic remains listed.

4. `p2f-grammar-neutral.md`
   - Add per-family triad accounting directly for F1-F8, or split oracle-only/accounting-only families out of the parser-candidate table.
   - Preserve CH2's accepted Lock 14 framing: grammar metadata owns byte sets and policy; SIMD masks stay transient; output digest remains oracle-only unless a real row-owned product consumes it.

## §3 — Accepted Guards To Carry Forward

- S-P2 research remains read-only against `skinny/` source and does not select waves.
- Generated non-JSON baseline work remains first priority; JSON direct residual rows remain fixpoint/pre-blocked under REDRESS 119/120.
- No candidate may import simdjson-style retained structural indexes, parser-owned class columns, event sidecars, or second scan streams.
- SIMD/ASM entries require scalar reference, strict parity/checkasm, and same-wave generated/runtime consumer before S-P3 may shortlist them as behavior work.
- Output digest/hash remains an oracle or row-owned output-plane contract, not parser proof.

## §4 — Next Move

Fold the V1 REVISE list into the six S-P2 artifacts, commit the fold, then dispatch S-P2 CHALLENGE V2. Convergence still requires at least two consecutive all-ACCEPT cycles or ≥95% ACCEPT cycles with zero open critical defects and no orphan REVISE.
