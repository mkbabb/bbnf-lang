# SK-V14 W5A CHALLENGE V3 CH5: Hidden Coupling
Disposition: ACCEPT
Acceptance score: 96/100
Findings:
- ACCEPT: The V3 plan does not introduce a sidecar provider substrate. It names one grammar-neutral `RuntimeGenerationRequest` path carrying grammar source plus workspace metadata into codegen, with `grammar_provider.rs` as the single source-consuming contract module, while CSS provider modules, CSS template directories, root CSS runtime, and grammar CSS sources remain explicit non-owner paths (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:18`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:20`-`35`). This matches SPEC's W5A source-consuming contract task and temporary no-deletion guard, not a second provider mesh (`restart/skinny/tranches/sk-v14/SPEC.md:662`-`668`).
- ACCEPT: The live hidden coupling is correctly identified and made fail-closed for redress. Current HEAD still has `regen.rs` validating source/metadata inputs, then discarding them by calling `codegen::emit_runtime_profile(target.profile)` in both write and check paths (`skinny/xtask/src/regen.rs:14`-`18`, `skinny/xtask/src/regen.rs:30`-`32`). That call enters `emit_runtime_profile`, selects by grammar profile, and renders via static provider/template branches (`skinny/crates/codegen/src/lib.rs:117`-`120`, `skinny/crates/codegen/src/lib.rs:162`-`210`; `skinny/crates/codegen/src/grammar_profile.rs:16`-`25`, `skinny/crates/codegen/src/grammar_profile.rs:100`-`110`). The plan requires `regen-css` and every `check-css-l4-*` companion to use the new request path and forbids `emit_runtime_profile(target.profile)` at the `regen.rs` call boundary (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:37`-`43`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:86`-`91`).
- ACCEPT: NEW-CH5-V4-01 deletion/consumer coupling is preserved. W5A proves the consumer path first; it does not delete the targets that current code still imports/includes. The current CSS provider modules still embed template directories with `include_str!` and `RuntimeProvider::*` profiles (`skinny/crates/codegen/src/css_l4_declaration_values_provider.rs:4`-`14`, `skinny/crates/codegen/src/css_l4_declaration_values_provider.rs:20`-`48`), so deleting them before the same-wave consumer no longer compiles through them would repeat the REDRESS-184/209 failure shape. The plan blocks that by keeping provider/template counts at 8 and 7, failing provider/template `A`/`D`/`R` diffs, and running `regen-css` plus all seven CSS companions through the migrated source-consuming path (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:43`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:72`-`79`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:92`-`95`).
- ACCEPT: Provider/template deletion remains W5B only. SPEC Section 8 allows the old provider count to remain non-zero in W5A but forbids new provider modules or template directories (`restart/skinny/tranches/sk-v14/SPEC.md:678`-`680`). SPEC Section 8B then assigns deletion of the seven CSS provider modules and seven CSS template directories to W5B after W5A admission, with `regen-css` and all seven companions as same-wave W5B consumers (`restart/skinny/tranches/sk-v14/SPEC.md:715`-`724`, `restart/skinny/tranches/sk-v14/SPEC.md:727`-`750`). The plan mirrors that split by pre-blocking deletion/rename before W5B and by making W5A ADMIT unlock W5B only (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:117`-`125`).
Required folds:
- NONE
Evidence:
- `rg -n "emit_runtime_profile\\(target\\.profile\\)|RuntimeGenerationRequest|emit_runtime_from_request" skinny/xtask/src skinny/crates/codegen/src restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md` shows the current hidden coupling in `skinny/xtask/src/regen.rs:18` and `skinny/xtask/src/regen.rs:32`, and the plan gate that removes it at `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:87`-`91`.
- `find skinny/crates/codegen/src -name '*_provider.rs' ! -name 'grammar_provider.rs' -print | sort` returns the current 8 old provider modules, including the 7 CSS providers plus `json_provider.rs`; `find skinny/crates/codegen/src -type d -name 'css_l4_*_templates' -print | sort` returns the current 7 CSS template directories. These are preserved in W5A and deleted only in W5B per `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:43`, `restart/skinny/tranches/sk-v14/SPEC.md:721`-`730`.
- `git diff --name-status HEAD -- skinny/crates/codegen/src` and `git diff --cached --name-status -- skinny/crates/codegen/src` produced no output during this read-only review, so no provider/template add/delete/rename was present in the current working or staged codegen slice.
- Gate references: `cargo xtask regen-css`; `cargo xtask check-css-l4-at-rules-and-media`; `cargo xtask check-css-l4-declaration-values`; `cargo xtask check-css-l4-declaration-values-extended`; `cargo xtask check-css-l4-nested-layout`; `cargo xtask check-css-l4-stylesheet-selectors`; `cargo xtask check-css-l4-vendor-and-custom-atrules`; `cargo xtask check-css-l4-visual-functions`; and the plan's fail-closed greps/counts at `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:86`-`107`.
