# SK-V13 Alpha Hardening V1 Consolidated

Date: 2026-05-21.

Pass: Pass Alpha SK-V12 -> SK-V13.
Cycle: V1.
Verdict: REVISE.

## Lens Dispositions

| Lens | Disposition | Fold requirement |
|---|---|---|
| CH1 Correctness | REVISE | Fix `> comparator + 1` margin wording, CSS architectural-block close language, Alpha-A row extraction granularity, and line anchors. |
| CH2 Generality / Lock 14 | REVISE | Remove the Alpha-E local override loophole for directives/BIR/BackendShape/public substrate surfaces; make decision-engine fallback fail closed. |
| CH3 Regression / REDRESS | REVISE | Recast profile-truth reopen survey as priority/risk, not eligibility; expand Alpha-C mandatory reopen accounting to the full 51 JSON rows. |
| CH4 Cost | REVISE | Recompute CSS LOC under the full 23-row scope, assign hard caps per fanout, and add dependency/conflict constraints for parallelism. |
| CH5 Hidden Coupling | ACCEPT-WITH-REVISE | Carry single-tape/no-sidecar/no-dynamic-substrate gates into S-P3 for every union/SIMD wave. |
| CH6 Anti-Paper-Close | REVISE | Add S-P3 precedence guard so weaker scoping language cannot authorize scaffold-only, optional, deferred, or support-only behavior waves. |

## Consolidated Findings

The Alpha V1 contract is directionally correct and keeps the 2026-05-21
full-SOTA addendum as the controlling close bar. The defects are foldable:

- The SOTA margin must be expressed exactly as `Track 1 > strict comparator + 1`
  for JSON and CSS, not `>= 1 Mbps` or "at least 1 Mbps".
- CSS rows below parity close only with architectural-level intrinsic-block
  proof. Measured implementation rejection remains REDRESS evidence for
  continued work, not a close state.
- Alpha-A must expose row-level accounting for the current 42 rendered rows plus
  the 10 absent typed rows, including explicit c/B schema debt where the current
  RESULTS surface does not emit it.
- Source maps must carry line-level anchors for material numeric and authority
  claims.
- S-P3 cannot locally override Lock 10 / Lock 14 / Lock 16 by adding new
  directives, BIR variants, `BackendShape`s, public substrate APIs, or
  grammar-specific generic behavior.
- The decision-engine fold must make the hardcoded JSON P1-P8 cascade
  non-admissible for new rows after the resolver wave; any fallback must be
  gate-visible and row-rejecting.
- The profile-truth scoping survey's old three-row direct shortlist is priority
  language only. All 13 direct residual rows remain W11 obligations unless
  admitted or architecturally blocked.
- The mandatory JSON reopen/accounting set is all 51 rows: 17 `parse_only`, 17
  `direct_to_struct`, and 17 `real_typed_struct`, including 10 absent typed
  rows.
- The CSS cost surface must be recomputed for full parity scope and tied to
  concrete fanout caps, dependencies, and conflict rules before S-P3 wave
  authority.
- Scoping prose marked optional, scaffold-only, diagnostic, deferred, or
  post-admission is subordinate to `SYNTHESIS.md`, `HANDOFF.md`, and the user
  addendum.

## V2 Required Output

Alpha V2 must edit the committed Alpha packet and affected scoping text, then
rerun CH1-CH6. The expected V2 fold set is:

1. `alpha-A-results-extraction.md` row-level current-state table.
2. `alpha-B-competitor-deltas.md` strict `> comparator + 1` wording and line
   anchor repair.
3. `alpha-C-redress-digest.md` full 51-row mandatory reopen accounting.
4. `alpha-E-candidate-shortlist.md` no-local-override wording, decision-engine
   fail-closed fallback, cost/cap/concurrency tables.
5. `SYNTHESIS.md` / `HANDOFF.md` precedence and margin wording clarifications.
6. `sk-v13-scoping-profile-truth.md` reopen survey language corrected from
   eligibility to priority/risk.

No implementation wave or G-Omega presentation may proceed from Alpha V1.
