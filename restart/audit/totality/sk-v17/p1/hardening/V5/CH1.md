---
lens: CH1-CORRECTNESS
pass: T-P1-excavation
cycle: V5
subject: SK-V17 T-P1 EXCAVATION
generated_at: 2026-05-29T19:40:00Z
master_head: 445925167154de73540e3ea3283d0170371de790
reviewer_method: "Read PASS-1-EXCAVATION.md §3 CH1 + ORCHESTRATOR §3W (carried via prompt); Read prior V4/CH1.md to confirm the two V4 REVISE dispositions (CH1-V4-001 1C, CH1-V4-002 1D) were folded; Read all six V5 1X inventories end-to-end (1a-substrate, 1b-codegen, 1c-runtime, 1d-skinny-lessons, 1e-locks, 1f-coherence + 1f-anti-pattern); re-anchored every load-bearing file:line/SHA via grep -n/-c + sed -n + wc -l over crates/core/src/{runtime,backend,grammar/generated}, crates/ir/src/registry/{struct,strategy}.rs, crates/simd-scan/src/{lib,alphabet}.rs, skinny/crates/{runtime,ir,passes,codegen,bbnf-simd}, restart/{ARCHITECTURE,MASTER-PLAN}.md, restart/locks/LOCKS.md, restart/skinny/tranches/sk-v17/SPEC.md; no recalled LOC, every load-bearing claim re-anchored at source at HEAD 445925167"
inventories_reviewed:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1b-codegen-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1e-locks-evidence.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
prior_cycle_dispositions_carried:
  - CH1-V4-001 (1C: math.rs OnceCell<StructuralIndex> is doc-comment text :281/285, NOT a struct field; grep count 2 not 1) — RESOLVED + live-verified: 1C Exec + RT17-005 now read "math.rs scan_structural=0 ... NO structural_index field ... ScanState carries only nospace_bits/nospace_start (math.rs:288-289) ... OnceCell mentions at math.rs:281,285 are doc-comments"
  - CH1-V4-002 (1D: SK17L-008 + frontmatter called the math.rs doc-comment an "index FIELD") — RESOLVED + live-verified: 1D SK17L-008 + frontmatter now read "scan_structural=0; no structural_index field; ScanState struct (math.rs:287-290) holds only nospace_bits/nospace_start; :281/:285 are doc-comments"
disposition_counts:
  accept: 7
  revise: 1
  reject: 0
sections_dispositioned: 8
---

## CH1 CORRECTNESS — Scope + Method

CH1 verifies, for V5: every spec-claim ↔ implementation row resolves at the cited
file:line/SHA; the tape-substrate / eager-OpenFrame divergence is accurately mapped;
SKINNY-proven facts are cited correctly. No recalled LOC, no recalled symbol path.
V5's primary obligation: confirm the two V4 REVISE dispositions (CH1-V4-001 on 1C,
CH1-V4-002 on 1D, shared math.rs root) were folded, plus the localized V5 REVISEs each
author carried (1A Gaps-row Lock-14 mischaracterisation; 1B symbol-anchor `layout`-vs-
struct-decl; 1E CH4 LAC-04 path-b + CH5-S8 REDRESS-53 anchor), then re-scan all six 1X
inventories for residual or newly-introduced citation defect.

Master HEAD confirmed `445925167154de73540e3ea3283d0170371de790` (`git rev-parse HEAD`).

**Verdict in brief.** The two V4 math.rs REVISEs are folded **and live-verified**: at
`crates/core/src/grammar/generated/math.rs` the `ScanState` struct (`:287-289`) carries
only `nospace_bits: u64` + `nospace_start: isize`; lines 281 and 285 are doc-comment
text inside the `ScanState` doc-block; `grep -c scan_structural math.rs` = **0**. The
scan census is **8 wired** (json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf each
`grep -c scan_structural` = 1), math the lone non-scanning generated grammar — confirmed
across 1C, 1D, and 1F-anti-pattern, all three now stating the corrected "8 not 9, math
carries no field" form. The V5-localized author REVISEs all fold correctly and resolve
at source: 1A's Gaps-row "@generated not hand-written" relabel (`json/value.rs:1` =
`// @generated by xtask regen-json`, `css_l4/value.rs:1` = `regen-css`, both live; core
`value_from_ref`/`ValueRef` grep-zero in `json/value.rs`); 1B's symbol-anchor fix
(`StructRegistry::layout` at `struct.rs:331`, `layout_by_name` at `:337`, NOT the
struct-decl line `:313`); 1E's CH4 LAC-04 path-b re-pricing (`grep -rn LayoutFacts
crates/` = **0**, confirming `LayoutFacts` is skinny/prior-totality-only and any
crates/core side-table realisation is non-zero) and CH5-S8 REDRESS-53 re-anchor
(`grep REDRESS-53` SPEC resolves at `:577`/`:657`/`:825`/`:839`). Of ~90 distinct
file:line/SHA citations re-anchored across the six inventories, the overwhelming
majority resolve verbatim. **One new defect requiring REVISE** surfaced, in 1B's
frontmatter only: the `live_truth_method` brace-glob shorthand
`{eager,offset,event,collapsed}_tape.rs=17 each` mislabels the lowerer file
`collapsed_stage.rs` as the non-existent `collapsed_tape.rs` (the body BSHAPE17-004 /
BSHAPE17-005 cite the correct `collapsed_stage.rs:15-17`). The 17-LOC fact is true; only
the frontmatter filename is wrong. No REJECT: no inverted impl verdict, no unresolved
load-bearing recall, no SKINNY-proof mis-citation.

---

## §1 — 1A substrate-evidence.md spec↔impl table + V5 Gaps-row fold + counts — ACCEPT

All eight SUB17 rows + the Cross-Tree map + the Substrate-Union Firewall + Open-Question
rows re-anchored. Load-bearing citations resolve verbatim:

- **SUB17-001** SoA `Tape<'input>` six members — `skinny/crates/runtime/src/tape/mod.rs:94-100`
  (`source: &'input [u8]`, `offsets: Vec<u32>`, `flag_cursors: Vec<u32>`, `flag_values:
  Vec<u8>`, `payloads: PayloadArena`, `id: TapeId`) — **verbatim-confirmed**. Lock 1
  `LOCKS.md:75` (substrate union, "parallel substrates are dead", offset-tape admitted),
  ARCH `:1088` — confirmed.
- **SUB17-002/003** core AoS `TapeRec` `record.rs:103` (`pub struct TapeRec`), 16-byte +
  align-4 const-asserts `:120-121` — **verbatim**; `TapeStructBuilder` `tape/mod.rs:58`;
  UNWIRED (`grep -rln TapeStructBuilder crates/core/src/ | grep -v /tape/` = **empty**,
  re-ran) — confirmed.
- **SUB17-004** core has no `value_from_ref`/`ValueRef` in `json/value.rs`
  (`grep -c value_from_ref\|ValueRef` = **0**); `CssTypedValue<'p>` at `css_l4/value.rs:414`
  (`pub enum CssTypedValue<'p> {`) — confirmed.
- **SUB17-005** x86 bar at SPEC `:806` ("**x86 / AVX-512 / SVE:** aarch64 only (Apple
  cores have no SVE)"); second-substrate block `:807-811`; D6 REJECT `:854`; ARCH `:1206`
  UNKNOWN-2D-05 ("aarch64 candidate is UNKNOWN-2D-05 ... aarch64 mechanically refused") —
  **all verbatim**. The CH3-R4 off-by-one (:806-not-807) holds folded.
- **SUB17-006** `StructLayout` Lock-2-retired (`LOCKS.md:160`) yet live at `struct.rs:202`;
  `grep -rn StructLayout crates/` = **960** (re-ran, exact) — confirmed.
- **SUB17-007** richer `StructuralAlphabet` (`singletons`/`digraph_mask:[u64;4]`/
  `digraph_pairs`/`quote_classes`) at `alphabet.rs:19-37`; quote_classes JSON/CSS doc
  `:33-37` ("`b'"'` for JSON, `b'\''` and `b'"'` for CSS") — **verbatim**.
- **SUB17-008** signature `scan_structural(input: &[u8], alphabet: &StructuralAlphabet)`
  `simd-scan/lib.rs:80` — confirmed; **SUB17-009 fence** `struct.rs:84` (FieldSource),
  `:202`/`:313`/`:331`; `begin_compound(&StructLayout)` `tape/mod.rs:185` reading
  `layout.rule_id & 0x1F` `:186` — confirmed.

**V5 Gaps-row fold (the lone 1A-owned REVISE).** The Gaps-row at `1a:140` now reads "the
per-grammar value APIs are `@generated by xtask regen-{json,css}` ... but EAGER, NOT
hand-written — Lock-14 ALLOWED" — the SAME Lock-14 mischaracterisation V3-CH1-V3-003
charged against 1D, correctly localized and corrected here. The `@generated` headers are
live-verified (`value.rs:1`); the prior "value APIs are hand-written per grammar" framing
is gone. 1A's own Table (SUB17-004) + Cross-Tree map already carried the correct framing,
so the defect was a single prose row — folded. Counts (implemented=2, unimplemented=4,
impl-exceeds-spec=2, unknown=2) tally against the verdict column.

DISPOSITION (§1): **ACCEPT** — every row resolves; the V5 Gaps-row Lock-14 relabel is
correct and source-anchored; the V4/V3 off-by-one + REDRESS anchors hold.

---

## §2 — 1A SKINNY-proven citations + tape↔OpenFrame map — ACCEPT

CH1's named focus against 1A. SKINNY-proven inputs resolve at source:

- `Tape<'input>` SoA + `offsets: Vec<u32>` — `skinny/.../tape/mod.rs:94-100`. ✓
- `ValueRef<'doc, 'input: 'doc, K = AnyKind, G: EventGrammar = AnyGrammar>` —
  `skinny/.../tape/mod.rs:175` (1A's `ValueRef<'doc,'input>` shorthand is a faithful
  elision of the full generic list). ✓
- `value_from_ref` — `skinny/crates/runtime/src/grammars/json/value.rs:143`
  (`pub(crate) fn value_from_ref<'doc, 'input: 'doc>(`); the `grammars/` segment carried
  throughout. ✓
- `select_classifier` aarch64 — `skinny/crates/bbnf-simd/src/dispatch.rs:42` (carried; the
  body anchor is consistent with the V4-confirmed `&'static [u8;64]` signature). ✓

The tape-substrate ↔ eager-OpenFrame map (Cross-Tree table + Firewall) is accurate
end-to-end: dead AoS `TapeStructBuilder` vs live eager `OpenFrame` builders
(`json/builder.rs:9`, `css_l4/builder.rs:16`), the `OnceCell<StructuralIndex>` scan-cache,
the no-cross-call-classifier-state Lock-1-v+1 check (`LOCKS.md:137-149`), the
per-leaf-registry fence (`SPEC:794-795`). No Track-1≡Track-2 dishonesty; the tape is
catalogued dormant, not a sidecar.

DISPOSITION (§2): **ACCEPT**.

---

## §3 — 1B codegen-evidence.md spec↔impl table + V5 symbol-anchor fold — REVISE

The nine BSHAPE17 rows + Cross-Tree Codegen Map + Do-Not-Redrive Ledger re-anchored.
Load-bearing facts resolve:

- **BSHAPE17-001/002** `enum BackendShape` + `derive_backend_shape` exist ONLY in skinny
  (`skinny/crates/ir/src/lib.rs:340` = `pub enum BackendShape {`;
  `skinny/crates/passes/src/lib.rs:392` = `pub fn derive_backend_shape(`);
  `grep -rn 'enum BackendShape' crates/` = **0**, `grep -rn 'derive_backend_shape' crates/`
  = **0** — confirmed exit-1/zero. egraph + csp-solver present in core (`Cargo.toml:2`,
  carried). ✓
- **BSHAPE17-003** `pub enum EmitStrategy {` `strategy.rs:104`; `is_struct_direct`
  `matches!(self, EmitStrategy::StructDirect { .. })` `:224`; `PRODUCTION_MANIFEST_TABLE`
  `:134` with **exactly 9** `ManifestStrategyEntry {` row literals (`:136,142,148,154,160,
  166,172,178,184`) — the "9 rows" claim is **verbatim-correct** (the `grep -c
  ManifestStrategyEntry`=13 includes the struct-decl `:314`, impl `:327`, test param `:252`,
  not rows). ✓
- **BSHAPE17-004** lowerer LOC: `eager_tape.rs`/`offset_tape.rs`/`event_tape.rs` = 17 each
  (`wc -l`), `collapsed_stage.rs` = 17, `sink_only.rs` = 270 — **the 17/270 LOC facts
  confirmed**; the body cites `collapsed_stage.rs:15-17` (`sed -n '15,17p'` =
  `pub fn lower_rule(rule: &BackendRule) -> String { super::tape_plan::render_rule(rule,
  TapeFlavor::Collapsed) }`) — **correct**. ✓ (frontmatter filename defect, see below).
- **BSHAPE17-006** V5 symbol-anchor fold: `StructRegistry::layout` at `struct.rs:331`
  (`pub fn layout(&self, rule_id: RuleId) -> Option<&StructLayout>`), `layout_by_name`
  `:337`; the struct-decl is `pub struct StructRegistry {` at `:313` — so the V5 correction
  (lookup symbol is `layout`/`layout_by_name` at `:331`/`:337`, NOT `lookup` at the
  struct-decl `:313`) is **verbatim-correct**, the same symbol-vs-decl class CH1 corrected
  elsewhere. `begin_compound(&StructLayout)` `tape/mod.rs:185`, reads `layout.rule_id & 0x1F`
  `:186` — confirmed. ✓
- **BSHAPE17-009 (NEW V5)** shared classifier already general: `scan_structural(input,
  &StructuralAlphabet)` `lib.rs:80`; `KernelShape::select(alphabet: &StructuralAlphabet)`
  `alphabet.rs:118`; `grep -rln scan_structural crates/core/src/grammar/generated` = 8
  grammars — confirmed. impl-exceeds-spec verdict sound. ✓

**One defect requiring REVISE:**

**CH1-V5-001 — 1B frontmatter `live_truth_method` mis-names the lowerer file.** The
frontmatter brace-glob reads "wc -l skinny/crates/codegen/src/lower/{eager,offset,event,
collapsed}_tape.rs=17 each" — but the fourth file does **not** exist as `collapsed_tape.rs`;
the actual file is `collapsed_stage.rs` (`ls skinny/crates/codegen/src/lower/` =
`collapsed_stage.rs eager_tape.rs event_tape.rs mod.rs offset_tape.rs rust.rs
schema_direct.rs sink_only.rs tape_plan.rs`). The brace-glob `{...}_tape.rs` expands
`collapsed` to `collapsed_tape.rs`, a non-existent path; `wc -l collapsed_tape.rs` errors.
The 17-LOC fact is true of `collapsed_stage.rs`, and the body (BSHAPE17-004 row + Cross-Tree
"CollapsedStage" row + BSHAPE17-005 + Do-Not-Redrive ledger) cites the **correct**
`collapsed_stage.rs:15-17`. This is a frontmatter-shorthand citation imprecision, not a
body-claim error; the same recalled-vs-live-path class CH1 has flagged before.
- **file:line**: `1b-codegen-evidence.md` frontmatter `live_truth_method`
  ("wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs=17 each"),
  contradicted by `ls skinny/crates/codegen/src/lower/` (the file is `collapsed_stage.rs`,
  not `collapsed_tape.rs`).
- **Fix**: change the brace-glob to enumerate the real filenames —
  "wc -l skinny/crates/codegen/src/lower/{eager_tape,offset_tape,event_tape,collapsed_stage}.rs
  = 17 each, sink_only=270". The body BSHAPE17-004/005 citations of `collapsed_stage.rs:15-17`
  are already correct and need no change. No verdict change; the four-scaffold + 270-LOC-
  sink_only census stands.

DISPOSITION (§3): **REVISE** — every body row resolves verbatim (incl. the V5 symbol-anchor
fix at `:331`/`:337`, the 9-row manifest, the grep-zero BackendShape/derive selectors, the
NEON impl-exceeds-spec); one new frontmatter defect (CH1-V5-001) mis-names `collapsed_stage.rs`
as `collapsed_tape.rs` in the `wc -l` glob, while the body cites it correctly.

---

## §4 — 1C runtime-evidence.md + V4 math.rs fold (CH1-V4-001) — ACCEPT

**V4 disposition folded + live-verified:** CH1-V4-001 RESOLVED. 1C's Exec Summary +
RT17-005 + Cross-Tree scan row now state "`math.rs` ... scan_structural=0 AND NO
`structural_index` field: its `ScanState` carries only `nospace_bits`/`nospace_start`
(math.rs:288-289), and the only two `OnceCell` mentions (`grep -c OnceCell math.rs = 2`)
are doc-comment text at math.rs:281,285". Live ground truth re-anchored:
`grep -n "OnceCell\|nospace" math.rs` = doc-comments at `:281`/`:285`, `ScanState` fields
`nospace_bits: u64` (`:288`) + `nospace_start: isize` (`:289`); `grep -c scan_structural
math.rs` = **0**. The "(math.rs:OnceCell=1)" mis-count V4 flagged is gone, replaced by the
correct "=2, both doc-comments". The defect is fully folded.

Other base citations re-anchored and confirmed:
- 8 scan call-sites: `json.rs:732`, `css_l4.rs:15982` (both `::simd_scan::scan_structural(
  input, &alphabet)`, `sed -n` confirmed), plus bbnf/bnf/csv/ebnf/css_pretty/google_sheets
  (`grep -c scan_structural` = 1 each, carried). ✓
- Real `structural_index: ::core::cell::OnceCell<…>` FIELD at `json.rs:701` and
  `css_l4.rs:15951` (both `pub(crate) structural_index: ::core::cell::OnceCell<`, `sed -n`
  confirmed — genuine struct fields, contrast math.rs doc-comments). ✓
- `@generated` headers: `json/value.rs:1` = `// @generated by xtask regen-json`,
  `css_l4/value.rs:1` = `// @generated by xtask regen-css` (live). RT17-002/RT17-004
  "GENERATED ... NOT hand-written, Lock-14 ALLOWED" reads consistently. ✓
- `begin_compound(&StructLayout)` `tape/mod.rs:185`; `JsonChildrenIter`/`CssChildrenIter`
  at the per-grammar `view.rs:39`/`:44` (V3 fold, carried); RT17-007 grep-empty shape-symbol
  finding. ✓

Counts (implemented=2, unimplemented=4, impl-exceeds-spec=1, unknown=1) tally.

DISPOSITION (§4): **ACCEPT** — the V4 math.rs REVISE is folded + live-verified; the
8-grammar census, the json.rs:701 / css_l4.rs:15951 real fields, the @generated relabel,
and every line citation are sound.

---

## §5 — 1D skinny-lessons.md RESULTS/REDRESS/corpus + V4 math.rs fold (CH1-V4-002) — ACCEPT

CH1's named focus per §3 ("RESULTS-row and REDRESS-entry citations in 1D resolve to real
entries"). **V4 disposition folded + live-verified:** CH1-V4-002 RESOLVED. SK17L-008's impl
column + the frontmatter `live_truth_method` now read "`grep -c scan_structural` per
generated file = {json,...,bbnf}:1, math.rs:0 (EIGHT wired; math.rs carries NO
`structural_index` field — its `ScanState` struct (math.rs:287-290) declares only
`nospace_bits`/`nospace_start`; the two `OnceCell<StructuralIndex>` mentions at :281/:285
are doc-comment text)". The self-contradictory "index FIELD ... field-only ... doc-comments"
form V4 flagged is gone. Re-anchored live: `ScanState` at `math.rs:287-289`, doc-comments
`:281`/`:285`, scan_structural=0 — matches verbatim. The shared-root math.rs defect is
folded in lockstep with 1C (§4).

Re-anchored and confirmed (no recalled corpus entry):
- skinny `BackendShape` enum `skinny/crates/ir/src/lib.rs:340` (`pub enum BackendShape {`);
  `derive_backend_shape` `skinny/crates/passes/src/lib.rs:392` — confirmed (the cost.rs
  `for_backend_shape` carried from V4). ✓
- `value_from_ref` `skinny/.../grammars/json/value.rs:143`; the `grammars/` segment carried. ✓
- `StructLayout` Lock-2-retired `LOCKS.md:160`, live `struct.rs:202`, ~960 references —
  confirmed (960 exact). ✓
- alphaC/alphaD/SPEC corpus refs (the 118x AZ-IV, the 28-65x/983x/10583x StructRegistry
  pre-block, the x86/AVX bar, SPEC `:793-795` "No registry lookup in the per-leaf hot path")
  — the SPEC anchors re-verified verbatim (`:793-795`, `:825`, `:839`, `:854`); the alphaC/
  alphaD line refs carried from V4 (V4 CH1 confirmed them verbatim and they are unchanged
  this cycle).

The JSON-empirical vs grammar-neutral split (CH2 firewall) is accurate; the do-not-redrive
ledger maps each pre-block to a real corpus anchor. Counts (implemented=1, unimplemented=4,
impl-exceeds-spec=2, unknown=2) tally.

DISPOSITION (§5): **ACCEPT** — the V4 math.rs REVISE is folded + live-verified; RESULTS/
REDRESS/corpus citations resolve; the eager-vs-lazy (not generated-vs-hand-written)
divergence framing is intact.

---

## §6 — 1E locks-evidence.md + V5 CH4 LAC-04 / CH5-S8 folds — ACCEPT

**V4 dispositions folded + live-verified.** The two V4 REVISEs that touched 1E both fold
correctly at source:

- **CH4 LAC-04 path-b re-pricing.** 1E now states `LayoutFacts` is skinny/prior-totality-
  only: `grep -rn LayoutFacts crates/` = **0** (re-ran, confirmed) — so path-(b) "lock
  re-scope to side-table" is NOT ~0 LOC against the fold target; any crates/core side-table
  realisation is non-zero/UNKNOWN, present only at `skinny/crates/passes/src/lib.rs:85,:91`
  (carried). The LAC-04 candidate text now carries the qualified pricing. **Correct.**
- **CH5-S8 REDRESS-53 re-anchor.** 1E moved the REDRESS-53 re-entry citation off the
  adjacent sidecar clause onto the verbatim naming lines: `grep -n REDRESS-53 SPEC` resolves
  at `:577` (parallel-vector naming), `:657`, `:825` (W2 row), `:839` (shortlist condition) —
  all **verbatim-confirmed** (`sed -n '577p;825p;839p'`). The D-1E-SKV17-03 / ledger / U2 rows
  cite `:577`/`:825`/`:839`. **Correct.**

Verified Invariants + Spec-Claim rows re-anchored:
- 16-lock count — the 16 numbered headings carried (V3 ACCEPT, re-listed in 1e frontmatter). ✓
- 5-shape canon `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` `LOCKS.md:107-108`
  (`sed -n` = "The 5-shape `BackendShape` search domain at Lock 10 holds: {EagerTape,
  OffsetTape, EventTape, SinkOnly, CollapsedStage}") — verbatim. ✓
- Scan wired all 8 — `grep -c scan_structural` = 1 each; signature `lib.rs:80` — confirmed. ✓
- ONE tape construct UNWIRED — `TapeStructBuilder` `tape/mod.rs:58`, `TapeRec` 16-byte `:120` —
  confirmed. ✓
- L02 drift — `StructLayout` `struct.rs:202` consumed by `begin_compound(&StructLayout)`
  `tape/mod.rs:185`; 960 sites — confirmed. ✓
- L10 CollapsedStage UNKNOWN-2D-05 `ARCH:1206`; SK-V17 aarch64-only `SPEC:258`/`:806`/`:808` —
  confirmed. ✓

The six LOCKS-AMENDMENTS-CANDIDATE rows each carry supporting path:line evidence; the
"scanned axes" preamble is present (not a silent omission). The COH-014 prior-totality
contradiction row resolves (the prior-totality `1F-coherence-scan.md:87` JSON+Sheets carrier
enumeration vs the all-8 live census). Counts (implemented=3, unimplemented=5,
impl-exceeds-spec=2, unknown=2; candidates=6) tally.

DISPOSITION (§6): **ACCEPT** — both V4 REVISEs (CH4 LAC-04 path-b, CH5-S8 REDRESS-53 anchor)
are folded + live-verified at source; every lock verdict + candidate resolves; LayoutFacts=0
and REDRESS-53 at :577/:825/:839 confirmed.

---

## §7 — 1F triad (coherence + anti-pattern) + V4 math.rs fold — ACCEPT

**V4 disposition folded + live-verified.** 1F-anti-pattern's Sidecar row now carries the
corrected math.rs scope: "The 8-carrier OnceCell census is exactly the 8 scan-wired grammars
(`grep -c scan_structural` = 1 each); the 9th generated grammar `math.rs` is NOT a carrier —
`grep -c scan_structural math.rs` = 0 and its two `OnceCell` mentions at `math.rs:281,285`
are doc-comment text, not a `structural_index` field (the same symbol-vs-doc-comment class
CH1-V4-001 corrected in 1C/1D)." 1F-coherence's frontmatter `prior_cycle_dispositions_folded`
carries `V4-CH1-V4-001-math.rs-not-a-COH17-002-value-api-carrier-no-leak-in-1f-coherence`.
Both fold the V4 root correctly.

1F-coherence rows re-anchored:
- COH17-001 SoA `Tape` offset-tape-admissible vs AV.04 dense class-column-buried split:
  `LOCKS.md:75` carries BOTH clauses ("The projection may be an offset tape ..." AND
  "columnar SoA ... never activated ... Columnar SoA stays buried") — **verbatim-confirmed**;
  `flag_cursors`/`flag_values` at `skinny/.../tape/mod.rs:97-98` (sparse, not dense column). ✓
- COH17-002 `@generated` value-API, no `value_from_ref` in core; `CssTypedValue` `value.rs:414`,
  `CssChildrenIter` `view.rs:44` — confirmed. ✓
- COH17-004 CollapsedStage x86-pinned: 1F cites `ARCH:1109` (the enum doc-comment
  "AVX-512-class collapsed-stage backend", `sed -n '1109p'` confirmed) + `:1186` + `:1206`;
  the prose "fuses mask-state" line is `:1088`. Both `:1109` and `:1088` resolve to real
  CollapsedStage content — no defect (different valid anchors for the same fact). ✓
- COH17-006 StructLayout 960; COH17-008 alphabet quote_classes JSON/CSS-exercised `:35-37` —
  confirmed. ✓

1F-anti-pattern rows: parallel-substrate firewall (one UNWIRED tape), the 817-LOC CSS
builder god-module with six `pending_*` Vecs (`:74-79`) + one `pending_value: Option` (`:71`)
= seven fields (`grep -n pending_ css_l4/builder.rs` confirmed `:71` Option + `:74-79` six Vecs,
none `Vec<Vec>`), the StructRegistry hot-path do-not-redrive fence, the no-cross-call-
classifier-state Lock-1-v+1 check — all resolve. CH5 firewall verdict (no Track-1≡Track-2
dishonesty) is sound. Counts tally (coherence implemented=1/unimplemented=4/exceeds=2/unknown=1;
anti-pattern all-0 divergence + 1 unknown).

DISPOSITION (§7): **ACCEPT** — the V4 math.rs fold lands in both 1F outputs; every coherence
+ anti-pattern row resolves verbatim; the offset-tape-admissible vs class-column-buried
distinction is correctly anchored to the dual `LOCKS.md:75` clauses.

---

## §8 — Prior-cycle (V4) disposition fold integrity — ACCEPT

Both V4 REVISE dispositions are folded **and live-re-verified** at source, plus the
V5-localized author REVISEs:

- **CH1-V4-001 (1C math.rs field)** RESOLVED + live-verified (§4): `ScanState` fields
  `nospace_bits`/`nospace_start` `:288-289`, doc-comments `:281`/`:285`, scan_structural=0.
- **CH1-V4-002 (1D math.rs field, shared root)** RESOLVED + live-verified (§5): SK17L-008 +
  frontmatter restated; no self-contradiction.
- **1A V5 Gaps-row** (Lock-14 @generated relabel) folded + verified (§1).
- **1B V5 symbol-anchor** (`layout`/`layout_by_name` at `:331`/`:337`) folded + verified (§3).
- **1E V5 CH4 LAC-04 path-b + CH5-S8** folded + verified (§6).

The frontmatter `prior_cycle_dispositions_folded` lists across all six inventories map to
real V4/V3 disposition IDs with accurate fix descriptions. No prior disposition is mis-folded
or left orphan; the math.rs root is corrected in lockstep across all three carriers (1C, 1D,
1F-anti-pattern), so CH1-V4-001/002 is not orphan. The fold is source-anchored, not
paper-folded.

DISPOSITION (§8): **ACCEPT**.

---

## Disposition Summary

| § | Subject | Disposition | Finding |
|---|---|---|---|
| §1 | 1A spec↔impl table + V5 Gaps-row Lock-14 fold + counts | **ACCEPT** | — |
| §2 | 1A SKINNY citations + tape↔OpenFrame map | **ACCEPT** | — |
| §3 | 1B codegen spec↔impl table + V5 symbol-anchor fold | **REVISE** | CH1-V5-001 (1B frontmatter `wc -l` brace-glob names `collapsed_tape.rs`; the file is `collapsed_stage.rs`; body cites it correctly) |
| §4 | 1C runtime census + V4 math.rs fold (CH1-V4-001) | **ACCEPT** | — |
| §5 | 1D skinny-lessons RESULTS/REDRESS/corpus + V4 fold (CH1-V4-002) | **ACCEPT** | — |
| §6 | 1E locks + V5 CH4 LAC-04 / CH5-S8 folds | **ACCEPT** | — |
| §7 | 1F triad (coherence + anti-pattern) + V4 math.rs fold | **ACCEPT** | — |
| §8 | Prior-cycle (V4) disposition fold integrity | **ACCEPT** | — |

**Counts**: ACCEPT 7 · REVISE 1 · REJECT 0. (ACCEPT-rate 7/8 = 87.5%.)

**Verdict**: V5 folds both V4 math.rs REVISE dispositions (CH1-V4-001 1C, CH1-V4-002 1D)
correctly and live-re-verifies them at source — `math.rs`'s `ScanState` carries only
`nospace_bits`/`nospace_start` (`:288-289`), lines 281/285 are doc-comments,
`grep -c scan_structural math.rs` = 0; the 8-wired census (math the lone non-scanning
generated grammar) is correct across 1C, 1D, and 1F-anti-pattern in lockstep. The five
V5-localized author REVISEs all fold at source: 1A's Gaps-row @generated relabel
(`value.rs:1` headers live, `value_from_ref`=0 in core), 1B's symbol-anchor fix
(`StructRegistry::layout`/`layout_by_name` at `:331`/`:337`, not the struct-decl `:313`),
1E's CH4 LAC-04 path-b re-pricing (`LayoutFacts`=0 in crates/) + CH5-S8 REDRESS-53 re-anchor
(`:577`/`:825`/`:839`). Every load-bearing substrate fact re-anchors verbatim: SoA `Tape`
six-member, AoS `TapeRec` 16-byte const-asserts, `ValueRef` generic, `value_from_ref:143`,
`TapeStructBuilder` grep-zero outside `tape/`, `BackendShape`/`derive_backend_shape`
skinny-only (grep-zero in crates/), `EmitStrategy::StructDirect` + 9 manifest rows,
`StructLayout`=960, `begin_compound(&StructLayout)`, the SPEC x86-bar/:806, REDRESS-53/
:577/:825/:839, D6/:854, ARCH/:1088/:1109/:1206 UNKNOWN-2D-05, LOCKS/:75/:107-108/:160.
One new defect (CH1-V5-001): 1B's frontmatter `wc -l` brace-glob mis-names the lowerer file
`collapsed_stage.rs` as the non-existent `collapsed_tape.rs` — a frontmatter-shorthand
imprecision; the 17-LOC fact is true and the body BSHAPE17-004/005 cite `collapsed_stage.rs:15-17`
correctly. No REJECT: no inverted impl verdict, no unresolved load-bearing recall, no
SKINNY-proof mis-citation. With CH1-V5-001 folded, CH1's correctness floor is met for SK-V17
T-P1 V5 — the lens is one frontmatter-glob defect short of a clean ACCEPT-all.
