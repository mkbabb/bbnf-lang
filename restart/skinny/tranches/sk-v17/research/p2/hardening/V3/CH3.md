# SK-V17 S-P2 V3 — CH3 REGRESSION

Lens: CH3 REGRESSION. Cycle: V3. Date: 2026-05-29.
Pass: S-P2 Research, PASS-2-RESEARCH §3 CH3 + ORCHESTRATOR §3W.
Master HEAD: `0ae1caa52`. S-P1 LOCKED at `0ae1caa52`.
Subject: `restart/skinny/tranches/sk-v17/research/p2/{p2a..p2f}.md` (p2b–p2f Cycle V3; p2a body current-cycle, frontmatter stale-labelled V2 — §7 hygiene note).
Prior: `p2/hardening/V2/CH3.md` (36 ACCEPT / 0 REVISE / 0 REJECT — clean; R1–R3 folded, four §3 coupling notes self-bound).

## §0 — Mandate + method

CH3 disposition is narrow: **does any candidate/section re-open a `skinny/REDRESS.md`
pre-block route — AZ-IV eager, StructRegistry indirection, fact-stream-as-output, the
24-row evidence broadcast, FNV/fixture, or the historical blocked instruction routes
(28+33 Class-A NEON tiny-string, 50-55 UTF-8 fusion, 60-72 retained-parse + sidecar
producers + digest cap-16, 80 mantissa-widen, 82-84 single-quartet/StringBlock16/
object-pair, 88 PMULL prefix-XOR hot body, 89 CSSC CTZ next-bit bulk consumer, plus the
historical NEON/UTF8 blocked routes) — WITHOUT fresh P1 evidence + a new framing?** A
re-open without BOTH is REJECT; antecedent-only / cost-only gaps belong to CH1 / CH4 and
are not double-counted here.

V3 method has two arms (per V2):
1. **Fold-verification.** Confirm every V2-inherited CH3 disposition (the V1 REVISE items
   R1/R2/R3, all carried CLEAN through V2) and the four V2 §3 coupling notes actually land
   in the V3 artefact text. A fold that did not land is an orphan REVISE (ORCHESTRATOR §3Z
   zero-orphan-REVISE bar) and re-opens the finding. V2 was clean of REVISE, so the binding
   carry-forward is the four §3 coupling conditions (G3 index==tape identity, D5 `BackendRule`
   branch-tag, CF-1 routing-derived-from-grammar, G2 scalar-balance default).
2. **Fresh scan.** Re-disposition every active candidate + non-candidate/anchor at the new
   V3 line numbers, and grep the pre-block re-open tests (PMULL/`vmull_p64` on a hot body;
   retained cursor / sidecar / `StructLayout`/`TapeStructBuilder`/`TapeCursor`/`UnionTape`;
   FNV/hex kernel; x86/SVE/AVX; eager `Box::new` per leaf; CTZ bulk-default body) for any
   NEW unflagged re-open introduced in V3.

Authoritative pre-block surface: `SYNTHESIS.md` §0.4 inherited-family id list
(`28+33, 50-55, 60-72, 80, 82-84, 88, 89, 96-98, 183/184/209-213, 215, 242-247, FNV
closed-enum`), cross-checked against `skinny/REDRESS.md` Item 88 (:2510 PMULL prefix-XOR
hot body, measured -10/-12/-15%), Item 89 (:2544 CSSC CTZ bulk consumer + B6 fold), Items
82-84 (:2318), Items 50-55, Item 90 (:2589 PMULL/CSSC remain rejected :2595), the W6/W11
AZ-IV typed-CSS reject (118× canada regression) + W10 FNV quarantine + W1/W6 broadcast-diagnostic.

## §1 — Census (V3)

| Artefact | Active candidates | Non-candidate / gated / anchor entries |
|---|---|---|
| p2a | CP-A1, CP-A2, CP-A3, CP-A4 | Non-candidate block (FNV/digit/asmjson dropped, :378-396) |
| p2b | C-B1, C-B2 | C-B3 (process-rejected udot), C-B0 (admission process / S-P3 gate) |
| p2c | C1→C2, C2, C3, C4 | C5 (udot orphan), C6 (i8mm gated contingency), 7 REDRESS-block flags (§3) |
| p2d | D1, D2, D3, D4, D5 | D6 (second-substrate reject-on-sight anchor) |
| p2e | G1, G2, G3 | G4 (udot gated), G5 (FNV non-candidate) |
| p2f | CF-1, CF-2, CF-3 | CF-4a (udot orphan), CF-4b (i8mm reject GATED), CF-0 (negative space) |

Active candidates scored by CH3: **21** (CP-A1..A4, C-B1/B2, C1/C2/C3/C4, D1..D5,
G1/G2/G3, CF-1/CF-2/CF-3). Non-candidate / gated / anchor entries scored for completeness:
**15**. Total dispositioned sections: **36**. Census is unchanged from V2 (same 21+15); no
candidate was added or removed in V3. CH3 confirms no NEW candidate entered the V3 pool — so
the only fresh-introduction risk is a re-framing slip at the new line numbers, scanned in §4.

## §2 — Fold-verification (V2 carry-forward, the load-bearing V3 check)

The three V1 REVISE items (R1/R2/R3, carried clean through V2) and the four V2 §3 coupling
conditions are confirmed landed in the V3 text. None is an orphan REVISE.

- **R1 — p2c broadcast citation.** The per-member-`vdupq` vs 24-row-evidence-broadcast
  disambiguation is intact. `p2c-arch-esoterica.md:190-194`: `vdupq_n_u8` "a SIMD lane-splat
  of one alphabet [member]" is named explicitly distinct from the "*evidence-measurement*
  practice of projecting one CSS timing tuple across 24 conceptual rows, SYNTHESIS §0.2/§0.4".
  p2a §4 reciprocally (`p2a:446-449`) names the pre-block as the evidence-measurement
  broadcast, "not a SIMD broadcast op". **FOLDED.**

- **R2 — p2e G2 §2 CTZ bound placement.** The §4 REDRESS-89 bound is inline at the G2 §2
  shape. `p2e-parse-that-gaps.md:166-177`: "**The shipped/default body is the scalar running
  balance over the two precomputed masks**", and the CTZ-ranges refinement "is NOT co-equal:
  it is a checkasm-gated, parity-proven, CONSUMER-ONLY optional path … and if S-P3 ever
  shortlists it as the UNCONDITIONAL body, CH3 must REVISE it back to the consumer framing
  (the §4 REDRESS-89 bound, promoted inline here per the V1 CHALLENGE)." The within-chunk /
  no-cross-call-retention depth-carry invariant is at the §2 shape (`p2e:150-154`). **FOLDED.**

- **R3 — p2e/p2c CTZ cross-artefact consistency.** `p2e:177` names `p2c-arch-esoterica.md §3.2`
  ("the two artefacts are reconciled — bulk-default CTZ is REDRESS-89-blocked, and a
  parity-gated consumer-of-a-precomputed-mask is the only admissible CTZ use"). p2c states the
  reciprocal: §3.2 `p2c:343-348` ("CSSC CTZ as a dedicated bulk next-bit consumer — REDRESS-89
  BLOCKED … The admissible form is C4 — a single `trailing_zeros` first-match extract"), and
  C4 `p2c:220-232`. The two artefacts agree in text. **FOLDED.**

V2 §3 coupling conditions (binding on the S-P3 shortlist) — confirmed self-bound in V3:

- **G3 index==tape-offsets identity (§3.1).** Present `p2e:192,299-303`: "G3 emits ONLY the
  structural index; if the offsets are retained, the structural projection IS the tape
  (`LOCKS.md:75`). No `UnionTape`, no second substrate … carry/depth threads WITHIN a single
  `scan_components_to_index` call, reset per parse." Backed by p2d's substrate-union conclusion
  (`p2d:128-136`, Lock-1 verbatim). REDRESS-53 not re-opened.
- **D5 flag semantics = `BackendRule` branch-tag (§3.2).** Present `p2d:396` ("ONLY as a
  `BackendRule` branch-tag projection"), `p2d:407-411` (relocated `W5C_REQUEST_FACT_PROFILES`
  in flag form ⇒ JSON-overfit / CH2 REVISE), summary-table row `p2d:433`
  ("GENERALISABLE-WITH-GUARD; CH2 REVISE if semantics become a per-rule catalogue"). The
  fact-stream-routing-in-flag-form re-entry is fenced.
- **CF-1 routing derived-from-grammar (§3.3).** Present `p2f:106-115,166-173` and §4 item 1
  (`p2f:380-383`): "relocating `W5C_REQUEST_FACT_PROFILES`' per-rule branching … into a
  projection-data table is the overfit re-entry seam and is FORBIDDEN. Every residual CSS
  routing entry must name its `.bbnf` rule." (Re-opens the Lock-14-phrase-#1 family if violated.)
- **G2 scalar-balance default (§3.4 / R2).** Folded into §2-G2 as above (`p2e:166-177`); S-P3
  cannot read G2 as proposing CTZ-as-default.

## §3 — Fresh dispositions (V3, at the new line numbers)

### ACCEPT (no pre-block re-open; fresh P1 antecedent + new framing both present)

- **CP-A1 / C-B1 / C2 / CF-2 (the eq-set byte-class scan).** Same `byte_class_from_eq_set_64`
  route; the lo6-`& 0x3f`-collision finding is independently re-derived in all four artefacts
  (p2a:240-253 "Why NOT the lo6 table route on CSS"; p2c:155-163,167-200; p2f:46-88,176-218;
  p2b:115-165). Each carries the §4 disambiguation from the blocked routes it is NOT: p2a §4
  separates the 64-byte Class-B classifier from REDRESS-28/33 tiny-string (`p2a:433-437`) and
  82-84 (`p2a:438-441`); p2c §3 flags 3+4 name the same. Fresh framing: the eq-set fan
  (`vceqq`×`vorrq`) over a ≤8-byte alphabet, never the tiny-string/quartet kernel. P1
  antecedent: `find_component_delim` 56.52–59.24% + `consume_balanced_at` 10.31–11.05%. ACCEPT.

- **C1 (lo6 TBL) — verdict INADMISSIBLE for CSS, route falls to C2.** p2c measured the
  `;`/`{` `& 0x3f` slot-59 collision (`p2c:130-166`), honestly retiring C1 in favour of C2 via
  the `lo6_table_admissible` guard (`dispatch.rs:101`) — a route-elimination, not a re-open.
  CP-A1 §4 promotes this to a hard re-open test: routing CSS through the lo6 path "would let
  S-P3 shortlist a SIMD win that silently runs scalar — REJECT. CSS = eq-set fan only"
  (`p2a:429-437`). Re-derived in p2f §1.2 (`p2f:46-88`). ACCEPT (as route-elimination + a
  sharpened defensive bound).

- **CP-A2 / C-B2 / D1 / CF-1 (tape-append `push_plain_offset`).** Counter-designs three
  pre-blocks at once and names all three: AZ-IV eager (lazy-by-default, `PayloadArena`
  `write_count==0` — `p2d:448-451`, `p2a:413-417`), StructRegistry indirection (single
  non-generic `TapeBuilder`, codegen-monomorphised — `p2d:452-456`, `p2a:418-422`, `p2f:401`),
  fact-stream-as-output (RETIRES `emit_fact_stream` as the live plane — `p2a:423-425`,
  `p2d:457-459`, `p2b:252-256`). Fresh P1 antecedent: `emit_fact_stream` 24.59–25.01% self +
  ~91% of the ~58–64% alloc floor. ACCEPT.

- **CP-A3 / D2 / CF-1 lazy `ValueRef` rider.** Direct AZ-IV counter-design; the re-open test
  is named and guarded in every artefact: no eager per-leaf `Box::new`, no f64-alloc-per-number,
  `PayloadArena write_count==0` on the source-re-readable path (`p2d:446-451`, `p2a:413-417`,
  `p2f`). Fresh framing: kind recovered from the source byte at the offset (no stored tag),
  isomorphic to JSON's proven `value_from_ref` (`json/value.rs:143`). ACCEPT.

- **CP-A4 / CF-3 (tokenize-once / commit-by-construction Alt-mode).** Bounded to the
  single-substrate REDRESS-53 shape: the index IS the tape, consumption is a per-grammar
  template, no parser-local second cursor (`p2a:350-377`, `p2f:220-262`). CF-3 self-flags its
  P1 antecedent as a hard blocking post-CF-1 re-confirm obligation ("NO measured
  speculative-rollback antecedent — LOCKED profile measured only recognition control 28.87% +
  block dispatch 2.45%, classed `structural` not rollback; admits ONLY on a blocking post-CF-1
  typed-tape re-profile", `p2f:348`) — a CH1/CH4 antecedent concern, not a CH3 re-open. ACCEPT
  for CH3 (REDRESS-53 not re-opened).

- **C3 (shrn movemask) / C4 (host CTZ first-match, fold-only).** C3 is a sub-task, not a
  standalone primitive (`p2c:201-218`), with the duplicated-movemask consolidation risk flagged
  in §4 (`p2c` movemask-divergence). C4 is the single `trailing_zeros` extract already in
  `byte_class_from_eq_set_64_neon`, explicitly bounded AWAY from the REDRESS-89 bulk consumer
  (`p2c:220-232` "promoting CTZ into a *dedicated bulk next-bit consumer* … is the
  REDRESS-89-rejected route … fold-only"; restated §3.2 `p2c:343-350`, summary `p2c:286`).
  Fresh framing present; no re-open. ACCEPT.

- **D3 (O(1) checkpoint/truncate) / D4 (one-shot SIMD reserve) / D5 (sparse-flag side-table).**
  D3 is the already-banked O(1) `offsets.len()` marker (`p2d:281-330`), lever-status held
  CONDITIONAL on a post-CF-1 typed-tape re-profile — a CH1 antecedent honesty, not a re-open.
  D4 reuses the shared scan count, gated behind D1/D2 + the NEON scan (`p2d:331-374`). D5 stores
  opaque flag bits in the EXISTING sparse side-vectors (`assembler.rs:93-113`), does NOT widen
  the per-position record, and carries the self-imposed Lock-14 `BackendRule`-branch-tag guard
  (`p2d:375-413`) — the §3.2 coupling now self-bound. None re-opens 60-72 sidecar/class-column
  (the side-table is the existing sparse flags, not a new substrate). ACCEPT.

- **D6 (second-substrate reject-on-sight anchor).** Records `StructLayout`/`TapeStructBuilder`/
  `TapeCursor`/sidecar/retained-cursor/aux-density-table/`UnionTape`/cross-call-carry as REJECT
  under Lock 1 (`p2d:414-423`). Explicit CH5/CH3 anchor; proposes nothing. ACCEPT.

- **G1 `comment_body_mask_64` (NET-NEW).** No prior REDRESS entry for a comment-region mask.
  Region fill reuses the `escape_mask_64` `overflowing_add` carry idiom (`lib.rs:188`), NOT
  PMULL — p2e §4 (`p2e:286-289`) names this to stay clear of REDRESS-88. Digraph-parameterised
  at the §2 signature (`p2e:102-138`), the Lock-14 fix carried since V1. Fresh P1 antecedent:
  the comment-skip arm of `find_component_delim`/`consume_balanced_at`. ACCEPT.

- **G2 `bracket_depth_mask_64`.** R2/R3 folded (§2 above): scalar running balance is the
  default body, CTZ-ranges is consumer-only + parity-gated + REVISE-back-conditioned,
  cross-linked to p2c §3.2. The within-chunk depth-carry / no-cross-call-retention invariant is
  at the §2 shape (`p2e:150-154`). REDRESS-89 not re-opened. ACCEPT.

- **G3 `scan_components_to_index` (composition).** REDRESS-53 (:766) blocks a "precomputed
  `StructuralIndex`" for a PARSER-LOCAL second scanner producing a sidecar parallel to a
  retained parse. G3's fresh framing (Lock-1 v+1): the produced `Vec<u32>` IS the tape's
  `offsets`, carry/depth threads WITHIN a single call and resets per parse (`p2e:191-192,
  299-303`). Backed by p2d's substrate-union conclusion. ACCEPT — with the §5 coupling note
  (carried from V2 §3.1) that S-P3 MUST carry the index==tape-offsets identity verbatim; a G3
  that retains the index as a vector parallel to a parse collapses into REDRESS-53 and
  CH3-REJECTs at the wave.

- **C-B0 admission process / the 7 REDRESS-block flags (p2c §3) / the §4 ledgers (p2a/b/d/e/f).**
  C-B0 is the load-bearing deliverable: the six-gate admission checklist (G1 scalar oracle, G2
  checkasm differential, G3 ABI/OOB, G4 same-wave consumer, G5 strict comparator, G6
  grammar-neutral) every shortlisted candidate must clear (`p2b:204-222`), with per-candidate
  gate-status (C-B1 clears G1–G3+G6, owes G4+G5; C-B3 fails G1/G2/G4). p2c §3 numbers all 7
  instruction routes with measured refutations + re-open tests (`p2c:330-386`): PMULL-88 (re-open
  test = `pmull`/`vmull_p64` text in the hot prefix-XOR body, `p2c:340-341`), CTZ-89,
  tiny-string-28/33, 82-84, x86/GFNI/VBMI2/VPCLMUL, SVE/SME, runtime-feature-detect-in-hot-loop.
  This is the CH3-defensive posture the pass asks for. ACCEPT.

- **All non-candidate / orphan-blocked / anchor entries (p2a non-candidate block, CP-BLOCKED
  asmjson, C-B3, C5, C6, G4, G5, CF-0, CF-4a, CF-4b for the CH3 axis).** The FNV leaf
  (p2a-non-candidate/G5/CF-0) is a non-candidate that RETIRES with the fact-stream; all artefacts
  pre-emptively REJECT any FNV/hex kernel proposal (`p2a:380-396`, `p2e:220-229`,
  `p2f:328-341,373-383`, `p2d:469-473`, `p2b:262-265`). The udot/i8mm orphans
  (C5/C6/G4/CF-4a/CF-4b/C-B3) are orphan-blocked on the no-antecedent axis (CH1/CH4), gated
  behind a future post-W1/W2 typed-path re-profile (`p2c:236-277`, `p2f:299-327`,
  `p2b:188-203`); they do not re-open a named REDRESS *route* — `udot` and `usmmla` are host
  instructions bound to a re-admission gate, not blocked REDRESS routes. asmjson collapsed-stage
  is x86-AVX-512-host-blocked, correctly excluded (`p2a:150-166`). ACCEPT for CH3 (these are
  CH1/CH4 surfaces; flagged §6 as hand-offs).

### REVISE (touches a pre-block; framing imprecise or under-bounded — fix, do not drop)

- **None.** Every inherited REVISE (R1/R2/R3) is confirmed folded into the V3 text (§2); no NEW
  framing imprecision was introduced. The fresh scan (§4) found no V3 candidate that touches a
  pre-block with an imprecise or under-bounded framing.

### REJECT (re-opens a pre-block without fresh P1 evidence + new framing)

- **None.** No V3 candidate re-opens AZ-IV eager, StructRegistry indirection,
  fact-stream-as-output, the 24-row evidence broadcast, FNV/fixture, or any historical blocked
  instruction route (28+33, 50-55, 60-72, 80, 82-84, 88, 89, the historical NEON/UTF8 blocked
  routes) without both fresh P1 evidence and a new framing. The eager/registry/fact-stream/
  broadcast/FNV pre-blocks are each affirmatively counter-designed and named; the
  instruction-route pre-blocks (88/89/82-84/28+33) are flagged with refutations and bounded away
  from in every §4.

## §4 — Fresh-scan re-open tests (no NEW V3 re-open)

Grep-verified across all six V3 artefacts:

- **PMULL/`vmull_p64` on a hot body (REDRESS-88).** All occurrences are negative/reject framing:
  `p2e:286-287` ("NOT PMULL. No PMULL text is introduced"), `p2c:48-52,332-341` ("Promoting PMULL
  into the hot prefix-XOR body is REDRESS-88 blocked"; re-open test = `pmull`/`vmull_p64` text in
  the hot body), `p2c:106` (inventory row: "PMULL promotion REDRESS-88 blocked"), `p2a:442-444`,
  `p2b:251`. No candidate proposes PMULL as a prefix-XOR/string-region hot body.
- **Retained cursor / sidecar / `StructLayout`/`TapeStructBuilder`/`TapeCursor`/`UnionTape`
  (REDRESS 60-72 / Lock 1).** All occurrences are reject-test framing: `p2a:418-422`, `p2d:165-167`
  ("NO SK-V17 substrate candidate may introduce …"), `p2d:414-423` (D6 reject-on-sight),
  `p2e:299-303`, `p2f:401-404`. No candidate proposes one.
- **Eager `Box::new` per leaf (AZ-IV).** Guarded by the `write_count==0`/`allocation_count`
  invariant in p2a/p2d/p2f (`p2d:448-451`); all `Box::new`/`eager` occurrences are reject framing
  (`p2d:285,448`, `p2a:413,330-331`). No candidate materialises eagerly.
- **CTZ bulk-default body (REDRESS-89).** Bounded in C4 (fold-only, `p2c:220-232`), G2
  (scalar-balance default, CTZ consumer-only + parity-gated + REVISE-back, `p2e:166-177`),
  cross-linked p2c §3.2 ↔ p2e §4 (`p2c:343-348` ↔ `p2e:275-285`). No candidate ships CTZ as the
  unconditional body.
- **FNV/hex kernel (FNV quarantine).** p2a-non-candidate / G5 / CF-0 record it as a retired
  non-candidate; all artefacts pre-REJECT any FNV/hex kernel proposal (`p2d:469`, `p2f:337-341`,
  `p2e:220-229`). No candidate.
- **x86/AVX/GFNI/VBMI2/VPCLMUL/SVE/SME.** Inventoried as host-blocked in p2c §1.1 (`p2c:57-58`,
  "exists … but is diagnostic-only / dead on the aarch64 host", `p2c:366-367`) and p2c §3 flags
  5+6, p2f §4.7 (`p2f:405-407`), p2a §4 (`p2a:450-451`); no candidate proposes an x86/SVE path.
  asmjson is the explicit host-blocked non-candidate (p2a §1.4).
- **C6 i8mm `usmmla` (GATED CONTINGENCY header).** "GATED CONTINGENCY, NOT an active candidate
  (twins P2-F CF-4b)" (`p2c:257-277`). `usmmla` is grep-clean-absent + net-new; it is not a NAMED
  blocked REDRESS *route*, so it does not re-open one. It is a CH1 antecedent surface (no P1 leaf)
  — hand-off §6.

No NEW pre-block re-open was introduced in V3. The census is identical to V2 (21+15); no
candidate changed family, and every re-framing at the new line numbers preserves the V2-bounded
disposition.

## §5 — Binding S-P3 shortlist conditions (carry-forward, not artefact edits)

These four coupling notes are self-bound in the V3 text and carry forward as binding S-P3
shortlist conditions — a shortlisted candidate that violates one CH3-REJECTs at the wave:

1. **G3 index==tape-offsets identity verbatim.** A G3 that retains the structural index as a
   vector parallel to a retained parse collapses into REDRESS-53.
2. **D5 flag bit = `BackendRule` branch-tag projection.** A D5 whose flag semantics become a
   hand-curated per-rule catalogue is the relocated-`W5C_REQUEST_FACT_PROFILES` overfit (CH2 REVISE).
3. **CF-1 / D1 routing derived-from-grammar.** Every residual CSS routing entry must name its
   `.bbnf` rule; relocating per-rule branching into projection DATA is the Lock-14-phrase-#1
   re-entry seam.
4. **G2 scalar-balance default.** S-P3 may not shortlist G2 with CTZ as the unconditional body;
   the CTZ-ranges path is consumer-only + parity-gated; promotion to the default body is a
   REDRESS-89 re-open and CH3 REVISES it back.

## §6 — Cross-lens hand-offs (not CH3 surfaces)

- **CH1 (antecedent):** C5/C6/G4/CF-4a/CF-4b/C-B3 (udot + i8mm) have NO benched CSS P1
  antecedent — orphan-blocked, gated behind a post-W1/W2 typed-path re-profile. CH3 does not
  REJECT these (they re-open no named REDRESS route); CH1 owns the speculative-kernel
  disposition. D3 (`p2d:281-330`) and CF-3 (`p2f:220-262,348`) both carry post-CF-1 re-profile
  obligations as their antecedent — CH1 must hold them to it (the LOCKED 28.87%+2.45%
  recognition-control figures are not a measured speculative-rollback antecedent). CP-A1's §4
  re-confirm obligation (the ~69% recognition-scan share must be RE-PROFILED post-tape;
  `p2a:457-463`) is likewise a CH1 surface.
- **CH4 (cost):** G1/G2 are NET-NEW kernels requiring net-new scalar twins + new checkasm
  differentials (ABSENT today, `p2e:155` "ABSENT — must be authored"); G4 needs a
  `checkasm_digit_mac` (ABSENT). C-B0's six-gate process (`p2b:204-222`) is the cost/parity
  surface CH4 owns. CH3 notes the kernels are NEW (not re-opens) and defers the cost gate to CH4.
- **CH5 (hidden coupling):** D6 is the explicit substrate-union anchor; G3's index==tape identity
  (§5) is the Lock-1 surface CH5 owns. CH3's REJECT-test for a retained sidecar overlaps CH5's;
  both agree the substrate union holds.
- **CH6 (anti-paper-close):** CF-3 and D3 defer their P1 antecedent to a "post-CF-1 typed-tape
  re-profile" — CH3 confirms this is NOT a deferred *candidate* (the primitive shape is fully
  specified now); the deferral is the antecedent measurement, which CH6 owns.

## §7 — Artefact-hygiene note (sub-CH3, not a re-open)

`p2a-sota-teardown.md:3` reads `Pass: S-P2 Research. Cycle: V2.` while every other V3 artefact
(p2b–p2f) reads `Cycle: V3` and p2a's own body is current-cycle (`p2a:495` "verified this cycle at
master HEAD `0ae1caa52`"; the CP-A1 demotion §4 carries "this cycle"). This is a stale frontmatter
label, NOT a pre-block re-open — p2a's content re-opens nothing — so it does not change the CH3
ACCEPT verdict. Flagged for the aggregator: the V3+1 dispatch should correct `p2a` frontmatter to
`Cycle: V3` (PASS-2 §2.1 schema-mandated field consistency). This is the only non-clean observation
CH3 surfaces this cycle.

## §8 — Counts

- Sections dispositioned: **36** (21 active candidates + 15 non-candidate/gated/anchor).
- **ACCEPT: 36.** **REVISE: 0.** **REJECT: 0.**
- **ACCEPT rate (all 36 sections): 36/36 = 100%.**
- **ACCEPT rate (21 active candidates): 21/21 = 100%.**

All inherited REVISE dispositions (R1 p2c broadcast citation; R2 p2e G2 §2 CTZ bound placement;
R3 p2e/p2c CTZ cross-artefact consistency) are confirmed folded into the V3 text; all four V2 §3
coupling conditions (G3 index==tape identity, D5 `BackendRule` branch-tag, CF-1
routing-derived-from-grammar, G2 scalar-balance default) are self-bound in the V3 artefacts and
carry forward as binding S-P3 shortlist conditions (§5). No orphan REVISE. No NEW pre-block re-open
introduced in V3.

CH3 verdict: the S-P2 V3 pool re-opens no `skinny/REDRESS.md` pre-block route. CH3 is CLEAN on its
own axis. The one observation — the stale `p2a` `Cycle: V2` frontmatter label (§7) — is an
artefact-hygiene nit, not a regression re-open, and does not block convergence; CH3 recommends a
one-line frontmatter correction if a V3+1 cycle dispatches for other reasons. This is the second
consecutive clean CH3 cycle (V2: 36/36; V3: 36/36).
