---
lens: CH2-GENERALITY
pass: T-P1-SKV17-excavation
cycle: V5
reviewed_at: 2026-05-29T20:30:00Z
subject_artefacts:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1e-locks-evidence.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
  - restart/audit/totality/sk-v17/p1/1f-past-corpora.md
mandate: >
  Lock 14 grammar-neutrality firewall. No divergence catalogued as JSON-only (or
  CSS-only) when it is a grammar-neutral substrate fact. Excavation covers ALL
  grammars (JSON / CSS / Sheets / BBNF-self) for the value-API + NEON + tape
  surfaces (Lock 14). Every grammar-name leak in a generic crate flagged.
  JSON-empirical vs grammar-neutral separated. The V4 note mandated the
  "@generated not hand-written" framing be SWEPT across ALL Gaps rows in V5, not
  patched one inventory at a time — this cycle verifies the sweep landed.
live_truth_method: >
  grep -c scan_structural over crates/core/src/grammar/generated/*.rs for all 9
  generated grammars (json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf = 1
  each; math.rs = 0 — EIGHT wired, math field-absent); grep -n OnceCell
  crates/core/src/grammar/generated/math.rs returns ONLY doc-comments at :281,:285
  (no structural_index field; math ScanState carries only nospace_bits:288 /
  nospace_start:289); contrast json.rs:701 genuine structural_index: OnceCell field
  (+ :711 init). head -1 crates/core/src/runtime/{json,css_l4}/value.rs = "//
  @generated by xtask regen-{json,css}; do not edit by hand." (live-verified, the
  exact header the V5 1A fold cites). grep ValueRef/value_from_ref over
  crates/core/src/runtime/{json,css_l4}/value.rs = 0 (both); skinny ValueRef def at
  tape/mod.rs:175 "pub struct ValueRef<'doc,'input:'doc,K=AnyKind,G:EventGrammar=AnyGrammar>"
  (grammar-parametric over G); skinny value_from_ref at
  grammars/json/value.rs:143. Grammar-name grep over crates/simd-scan/src/*.rs =
  EMPTY; over crates/core/src/runtime/tape/*.rs = doc-comments ONLY (//! module docs
  arena.rs:8, record.rs:4, cursor.rs:5, mod.rs:6/18/20), no live route string.
  substrate.rs CssStructBuilder/JsonStructBuilder hits at :60/:73/:82/:97 are ALL
  doc-comments; live emitter data-binds builder_path from EmitStrategy::StructDirect.
  alphabet.rs:35 quote_classes doc confirms JSON/CSS-motivated (breadth-of-config).
  No build/cargo mutation. Master HEAD 445925167.
sections_dispositioned: 14
accept: 14
revise: 0
reject: 0
note: >
  V4 GENERALITY closed at ≈93% with ONE residual REVISE — the 1A Gaps-row (1a:126)
  "value APIs are hand-written per grammar" Lock-14 mischaracterisation, the
  SECOND consecutive cycle of an identical-class REVISE (V3: 1D hand-written/math;
  V4: 1A hand-written). V5 FOLDS it cleanly AND honours the V4 note's sweep
  mandate. 1A's Gaps-row (now 1a:140) is restated to "the per-grammar value APIs
  are `@generated by xtask regen-{json,css}` (`value.rs:1`) but EAGER, NOT
  hand-written — Lock-14 ALLOWED (hand-written would be the Lock-14 VIOLATION); the
  divergence is the EAGER materialization shape, not the generation provenance
  (mirrors 1f-coherence-scan.md:114, 1d-skinny-lessons.md:146)." The fold ledger
  records the disposition source (1a:18). Live truth confirms the @generated
  headers at value.rs:1 verbatim. The V4 note's SWEEP is satisfied: I audited EVERY
  inventory's Gaps row + value-API row for the "hand-written" framing — 1C Gaps
  (1c:111), 1D Gaps (1d:147), 1F-coherence Gaps (1f-coh:116), and their respective
  value-API table rows (1C RT17-002 1c:76, 1D SK17L-002 1d:89, 1E D-1E-SKV17-02
  1e:127, 1F COH17-002 1f-coh:79) ALL carry the "@generated/GENERATED, not
  hand-written" framing or the GENERATOR-LOC-vs-emitted-LOC distinction. ZERO new
  Lock-14 framing residual surfaces — the predicted "third instance" did NOT
  materialise because V5 swept rather than patched. The all-8 scan census, the
  lazy-ValueRef<G> grammar-neutral catalogue, the by-construction Sheets/BBNF
  discrimination, and the zero-leak generic-crate firewall all hold live-verified.
  GENERALITY converges this cycle: 14/14 ACCEPT, 100%.
---

## Executive Summary

CH2 GENERALITY firewall over the SK-V17 T-P1 cycle-V5 inventories. The single V4
GENERALITY residual REVISE — the 1A Gaps-row "value APIs are hand-written per
grammar" Lock-14 mischaracterisation — is **FOLDED**, and the V4 note's explicit
sweep mandate ("ensure the cross-inventory '@generated not hand-written' framing is
swept across ALL Gaps rows in the V5 fold, not patched one inventory at a time, lest
a third instance surface") is **honoured**.

1A's Gaps-row (now `1a-substrate-evidence.md:140`) reads: "No `ValueRef`
lazy-projection generator in crates/core; the per-grammar value APIs are
`@generated by xtask regen-{json,css}` (`value.rs:1`) but EAGER, NOT hand-written —
Lock-14 ALLOWED (hand-written would be the Lock-14 VIOLATION); the divergence is the
EAGER materialization shape, not the generation provenance (mirrors
`1f-coherence-scan.md:114`, `1d-skinny-lessons.md:146`)." The fold ledger records the
disposition source `V4-CH-1A-Gaps-row-1a126-lock14-mischaracterisation` at `1a:18`,
and the V5-fold narrative at `1a:76-86` re-states the correction. Live truth confirms
the @generated headers at `crates/core/src/runtime/{json,css_l4}/value.rs:1` verbatim
(`// @generated by xtask regen-{json,css}; do not edit by hand.`). The V4 REVISE is
closed.

The V4 note's predicted failure mode — a third structurally-identical Lock-14 framing
instance surfacing in yet another inventory — did NOT materialise, precisely because
V5 swept rather than patched. I audited EVERY inventory's Gaps row AND value-API
table row for the "hand-written per grammar" framing across the full
JSON/CSS/Sheets/BBNF matrix:

- **1A** Gaps `1a:140` ✓ ("@generated … but EAGER, NOT hand-written"); table SUB17-004
  `1a:95` ✓; Cross-Tree Value-API `1a:108` ✓.
- **1C** Gaps `1c:111` ✓ ("@generated but EAGER per grammar"); table RT17-002 `1c:76` ✓
  ("not hand-authorship — the files are `@generated`"); RT17-004 `1c:78` ✓
  ("template-emitted, not hand-written").
- **1D** Gaps `1d:147` ✓ ("GENERATED but EAGER … not hand-written and not lazy");
  table SK17L-002 `1d:89` ✓ ("EAGER-generated vs LAZY-generated, not
  generated-vs-hand-written … Lock-14 HONOURED").
- **1E** divergence D-1E-SKV17-02 `1e:127` ✓ ("hand-written generator-LOC … vs
  regenerated emitted-LOC"); L14 row `1e:119` ✓ ("Lock 14 ALLOWED").
- **1F-coherence** Gaps `1f-coh:116` ✓ ("GENERATED per grammar … NOT hand-written");
  table COH17-002 `1f-coh:79` ✓ ("GENERATED, not hand-written — the fold acts on
  the GENERATOR").
- **1F-anti-pattern** AP17-002 grammar-leak `1f-anti:72` ✓ (per-grammar surfaces
  "Lock 14 ALLOWED … template-emitted").
- **1F-past-corpora** PC17-003/005 — no hand-written framing; carries the eager-tree
  fold-deletion target correctly.

ZERO inventories now carry the "hand-written per grammar" mischaracterisation. The
sweep landed.

The all-eight-grammar scan census is uniform and correct across every inventory and
live-verified: `grep -c scan_structural` returns 1 for each of
json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf, and **0** for math.rs;
math.rs's only `OnceCell` hits are doc-comments at `:281`,`:285` (no `structural_index`
field — its `ScanState` holds only `nospace_bits:288`/`nospace_start:289`), contrasting
json.rs's genuine `structural_index: OnceCell<…>` field at `:701`. The excavation is
**NOT CSS-only and NOT JSON-only** — it spans the full JSON / CSS / Sheets / BBNF-self
matrix Lock 14 demands.

The lazy-`ValueRef<G>` generalization is catalogued grammar-NEUTRAL and
grammar-PARAMETRIC across all inventories (1A SUB17-004, 1C RT17-002, 1D SK17L-002,
1E D-1E-SKV17-02, 1F COH17-002): live-verified, core's
`crates/core/src/runtime/{json,css_l4}/value.rs` have NO `value_from_ref`/`ValueRef`
(grep=0 both), while skinny's `ValueRef<'doc,'input,K=AnyKind,G:EventGrammar=AnyGrammar>`
(`skinny/crates/runtime/src/tape/mod.rs:175`) is generic over G. The NEON
`select_classifier(&[u8;64])` and the richer core `StructuralAlphabet`
(`crates/simd-scan/src/alphabet.rs:19-37`) are both correctly catalogued as
grammar-as-data (breadth-of-config, not breadth-of-proof; `quote_classes` doc at
`:35` confirms JSON/CSS-motivated) — Lock 14 honoured.

The JSON-empirical-vs-grammar-neutral split (1D §lines 99-111) and the
by-construction Sheets/BBNF-self discrimination (SK17L-009, 1d:96) survive intact:
JSON is the only tape WITNESS today; CSS is the SK-V17 first-mover; Sheets/BBNF-self
generality is by-construction (alphabet-as-data), an SK-V18 fold target — NOT
over-claimed as proven-by-exercise. ZERO grammar-name leaks in generic crates:
`crates/simd-scan/src/*.rs` returns EMPTY; `crates/core/src/runtime/tape/*.rs`
returns doc-comments ONLY (`//!` module docs); the emitter `substrate.rs` grammar
names (`:60`,`:73`,`:82`,`:97`) are ALL doc-comments, the live functions data-bind
`builder_path` from `EmitStrategy::StructDirect` — Lock 14 honoured.

## Section Dispositions

### 1a-substrate-evidence.md (V4 residual REVISE folded)

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 1 | **Gaps-row value-API framing (V4 REVISE target, now 1a:140)** | **ACCEPT** | FOLDED. The Gaps row now reads "No `ValueRef` lazy-projection generator in crates/core; the per-grammar value APIs are `@generated by xtask regen-{json,css}` (`value.rs:1`) but EAGER, NOT hand-written — Lock-14 ALLOWED (hand-written would be the Lock-14 VIOLATION); the divergence is the EAGER materialization shape, not the generation provenance (mirrors `1f-coherence-scan.md:114`, `1d-skinny-lessons.md:146`)." This is the EXACT restatement the V4 disposition prescribed. Live-verified: `head -1 crates/core/src/runtime/{json,css_l4}/value.rs` = "`// @generated by xtask regen-{json,css}; do not edit by hand.`" The fold-ledger (`1a:18`) records `V4-CH-1A-Gaps-row-1a126-lock14-mischaracterisation` with the disposition source verbatim; the V5-fold narrative `1a:76-86` re-states it. The "hand-written per grammar" mischaracterisation is GONE. ACCEPT. |
| 2 | SUB17-004 value-API per-grammar vs lazy-ValueRef (table row 1a:95) | **ACCEPT** | GENERALITY-correct and unchanged from V4: "The core value API is per-grammar eager enums, not a single lazy `ValueRef<G>` projection"; Files-touched names "the regenerated set is per-grammar `value.rs`/`view.rs`/`document.rs` (8 grammars), regen-gated." No hand-written mislabel; all-8 breadth carried. ACCEPT. |
| 3 | Cross-Tree Value-API row (1a:108) + Structural-scan row (1a:110) | **ACCEPT** | Value-API row: "per-grammar eager typed enums; NO `ValueRef`/`value_from_ref` in core (grep empty)" — no hand-written mislabel; FieldSource walk compile-time-resolved fence carried. Scan row enumerates "ALL 8 generated grammars incl `json.rs:732`, `css_l4.rs:15982`, `css_pretty.rs`, `ebnf.rs`, `bnf.rs`, `csv.rs`, `google_sheets.rs`, `bbnf.rs`." Live-verified. ACCEPT. |
| 4 | SUB17-007 alphabet config-breadth grammar-as-data (1a:98) | **ACCEPT** | Correctly grammar-neutral: "generality is config-breadth, not proof-breadth: the `quote_classes` doc comment shows the field is JSON/CSS-motivated … JSON/CSS-exercised-only … not breadth-of-proof. Both grammar-as-data (Lock 14 honoured)." Live-verified at `alphabet.rs:35`. ACCEPT. |
| 5 | Substrate-Union Firewall sidecar row, all-8 enumeration (1a:118) | **ACCEPT** | Enumerates `OnceCell<StructuralIndex>` carriers across all 8 grammars (json.rs:686 … bbnf.rs); the "Verify at fold: scope to ALL 8 carriers, not json.rs alone" note is the correct generality scope. Live-verified: all-8 carriers confirmed, math.rs correctly excluded. No JSON-only narrowing. ACCEPT. |

### 1c-runtime-evidence.md

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 6 | RT17-002 value-API eager vs lazy-ValueRef<G> + Gaps row (1c:76, 1c:111) | **ACCEPT** | RT17-002 carries the corrected framing: "the divergence is the EAGER materialization shape, not the per-grammar locus, not hand-authorship (the files are `@generated`)." Gaps row `1c:111`: "value APIs are `@generated` but EAGER per grammar." No hand-written mislabel. Grammar-neutral, no leak. ACCEPT. |
| 7 | RT17-003 + RT17-005 scan-wired all 8 / OnceCell all 8 (1c:77,79) | **ACCEPT** | Lists exactly the 8 scan-wired grammars (call-site lines json.rs:732 … google_sheets.rs:3559) and states "`math.rs` has scan_structural=0 and NO `structural_index` field — its only `OnceCell` mentions are doc-comments at math.rs:281,285" and "8 wired, not 9." Live-verified exact: math.rs scan=0, doc-comment-only at :281/:285, no field (ScanState only nospace_bits/nospace_start :288-289). ACCEPT. |
| 8 | RT17-004 per-grammar runtime census + Lock-14 leak audit (1c:78) | **ACCEPT** | Grammar-named symbols (`CssStructBuilder`, `CssTypedValue`, `JsonChildrenIter` json/view.rs:39, `CssChildrenIter` css_l4/view.rs:44) live ONLY under `runtime/<g>/`, all `@generated`. Generic `runtime/{tape,builder.rs,view.rs}` carry NO grammar names. The cell states providers "are template-emitted, **not hand-written**, so they are not a Lock-14 violation." Live-verified: tape/*.rs grammar-name grep = doc-comments only; substrate.rs data-binds. ACCEPT. |

### 1d-skinny-lessons.md

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 9 | SK17L-002 value-API eager-generated vs lazy-ValueRef<G> + Gaps row (1d:89, 1d:147) | **ACCEPT** | SK17L-002: "crates/core has per-grammar **generated** EAGER value enums (NOT hand-written — … both carry `// @generated by xtask regen-*; do not edit by hand.`)" and "EAGER-generated vs LAZY-generated, not generated-vs-hand-written … Lock-14 HONOURED." Gaps row `1d:147`: "GENERATED but EAGER … not hand-written and not lazy." Live-verified. This is the MODEL Lock-14 framing the V5 1A Gaps row now mirrors. ACCEPT. |
| 10 | SK17L-008 scan-census EIGHT-name + math.rs field-absent (1d:95, 1d:33) | **ACCEPT** | The row reads "WIRED into EXACTLY 8 generated grammars … `math.rs` is the lone generated grammar with `scan_structural`=0 and no `structural_index` field: its `ScanState` struct (`math.rs:287-290`) declares only `nospace_bits`/`nospace_start`, and its only `OnceCell<StructuralIndex>` mentions (`math.rs:281`,`:285`) are doc-comment text … (Contrast json.rs:701, which DOES carry a genuine `structural_index` field.)" Live-verified exact. The V3→V4 over-count and the V4 doc-comment-as-field self-contradiction (CH1-V4-002, fold-ledgered at 1d:34) are BOTH closed. ACCEPT. |
| 11 | JSON-Empirical vs Grammar-Neutral Split table (1d:99-111) + SK17L-009 by-construction (1d:96) | **ACCEPT** | The Lock-14 split deliverable. Five lessons split JSON-empirical witness vs grammar-neutral durable fact; the scan row (1d:109) reads "core: WIRED across all 8 grammars (SK17L-008); skinny: JSON-wired, CSS is W3"; the "a CSS-only generator that never re-emits JSON FAILS CH2" fence (1d:108) is CH2-aware. SK17L-009: Sheets/BBNF-self generality by-construction (sheets_witness stub, no BackendRule/.bbnf), proven by-exercise on JSON+CSS only, Sheets/BBNF proof is SK-V18 — does NOT over-claim. ACCEPT. |

### 1e-locks-evidence.md

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 12 | L14 grammar-neutral row (1e:119) + D-1E-SKV17-02 value-API divergence (1e:127) | **ACCEPT** | L14 row catalogues scan as "grammar-as-DATA (alphabet config)" and per-grammar `CssStructBuilder`/`CssTypedValue` as "per-grammar runtime surfaces (Lock 14 ALLOWED) … the eager per-grammar builders are the fold-deletion target, not a leak." D-1E-SKV17-02 distinguishes "hand-written generator-LOC (emitter projection generator) vs regenerated emitted-LOC (per-grammar value.rs/view.rs re-emit set)" — the GENERATOR-vs-emitted distinction, not a hand-written mislabel. The Verified-Invariant "Structural scan wired across ALL 8 grammars" (1e:109) carries the all-8 census with per-grammar line anchors. LAC-1E-SKV17-03 (1e:180) scopes the substrate_target classification to ALL 8, not a 4-grammar sample. No JSON-only narrowing; no leak; no hand-written framing. ACCEPT. |

### 1f-coherence-scan.md / 1f-anti-pattern.md / 1f-past-corpora.md

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 13 | 1f-coherence COH17-002 + Gaps row 1f-coh:79/116; COH17-008 alphabet breadth-of-config 1f-coh:85 | **ACCEPT** | COH17-002 + Gaps row 1f-coh:116 correctly say "GENERATED, not hand-written (the fold acts on the GENERATOR)" — the model framing 1A's Gaps row now mirrors. COH17-008 carries "the generality is **JSON/CSS-exercised-only** (breadth-of-config, NOT breadth-of-proof)" matching SPEC §0.1.11. The scan census enumerates all 8 (1f-coh:67, :95, :118). Live-verified. ACCEPT. |
| 14 | 1f-anti-pattern AP17-002 grammar-leak + AP17-004 OnceCell all-8 (1f-anti:70,72); 1f-past-corpora all-8 carrier enumeration (99-107) + DO-NOT-CARRY-UNDERCOUNT flag (87-97) | **ACCEPT** | 1f-anti AP17-004 sidecar row (1f-anti:70) enumerates the OnceCell sidecar across all 8 carriers AND carries the math.rs distinction: "the 9th generated grammar `math.rs` is NOT a carrier — `grep -c scan_structural math.rs` = 0 and its two `OnceCell` mentions … are doc-comment text, not a `structural_index` field … So the firewall scope is 8 carriers, never 9." AP17-002 grammar-leak row (1f-anti:72) dispositions per-grammar constructs as Lock-14-ALLOWED and confirms the generic tape carries no route strings. 1f-past-corpora enumerates all-8 carriers (99-107) and the DO-NOT-CARRY-UNDERCOUNT flag (87-97) is an exemplary CH2/CH3 cross-guard. No JSON-only mis-catalogue; no uncited grammar-name leak. ACCEPT. |

## Grammar-Coverage Matrix (CH2 firewall verification)

| Grammar | scan_structural wired (core, live) | value-API in core | catalogued generality verdict | CH2 |
|---|---|---|---|---|
| JSON | YES (json.rs scan=1) | eager `JsonValue` enums, `@generated`, no `value_from_ref` | JSON-empirical WITNESS (tape proven); grammar-neutral substrate | clean |
| CSS (css_l4) | YES (css_l4.rs scan=1) | eager `CssTypedValue` (value.rs:414), `@generated` | SK-V17 first-mover; by-exercise proof | clean |
| CSS (css_pretty) | YES (css_pretty.rs scan=1) | per-grammar `@generated` | grammar-neutral scan, per-grammar runtime | clean |
| Sheets (google_sheets) | YES (google_sheets.rs scan=1) | per-grammar runtime dir | by-construction (sheets_witness stub, no BackendRule/.bbnf); SK-V18 proof | clean |
| BBNF-self (bbnf) | YES (bbnf.rs scan=1) | per-grammar runtime dir | by-construction; no tape witness; SK-V18 proof | clean |
| EBNF | YES (ebnf.rs scan=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| BNF | YES (bnf.rs scan=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| CSV | YES (csv.rs scan=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| math | **NO (math.rs scan=0)** | per-grammar runtime dir | NO `structural_index` field (ScanState nospace_bits/nospace_start :288-289); OnceCell at :281/:285 are doc-comments | correctly catalogued (8 wired, not 9) across ALL inventories |

CH2 verifies: no value-API/NEON/tape divergence is catalogued JSON-only or CSS-only
when it is grammar-neutral; Sheets and BBNF-self are explicitly covered (scan-wired +
correctly flagged by-construction-not-by-exercise for the value-API proof); the
generic-crate grammar-name-leak firewall is clean (simd-scan empty; runtime/tape +
substrate.rs are doc-comments only); the math.rs field-only-absent/no-scan distinction
is accurate in every inventory. The excavation spans the full JSON/CSS/Sheets/BBNF-self
matrix per Lock 14.

## V4-Residual Closure Status

| V4 GENERALITY disposition | V5 status |
|---|---|
| 1A Gaps-row "value APIs are hand-written per grammar" (V4 sole REVISE) | CLOSED — restated to "@generated per grammar but EAGER, not hand-written" at 1a:140; fold-ledgered 1a:18; @generated headers at value.rs:1 live-verified |
| V4 note: SWEEP "@generated not hand-written" across ALL Gaps rows | HONOURED — audited all 7 inventories' Gaps + value-API rows; 1C/1D/1E/1F-coh/1F-anti all carry the correct framing; ZERO new instance surfaced |
| All-8 scan census uniform across inventories (V4 ACCEPT) | HELD — 1A/1C/1D/1E/1F all carry all-8 with per-grammar line anchors; math.rs=0 live-confirmed |
| lazy-ValueRef<G> grammar-neutral + parametric catalogue (V4 ACCEPT) | HELD — grep-verified core has no ValueRef (=0 both); skinny parametric over G:EventGrammar (tape/mod.rs:175) |
| Sheets/BBNF by-construction discrimination, SK17L-009 (V4 ACCEPT) | HELD — by-construction generality, proof on JSON+CSS only, Sheets/BBNF = SK-V18 |
| zero grammar-name leak in generic crates (V4 ACCEPT) | HELD — simd-scan grep EMPTY; runtime/tape + substrate.rs grammar names are doc-comments only; emitter data-binds |

## CH2 Verdict

The V5 cycle closes the sole V4 GENERALITY residual cleanly AND honours the V4
note's sweep mandate. The 1A Gaps-row "value APIs are hand-written per grammar"
Lock-14 mischaracterisation is struck and replaced with "the per-grammar value APIs
are `@generated by xtask regen-{json,css}` (`value.rs:1`) but EAGER, NOT
hand-written — Lock-14 ALLOWED" — live-confirmed (`head -1 value.rs` = the @generated
header verbatim). Critically, V5 did NOT patch one inventory in isolation: I audited
EVERY inventory's Gaps row and value-API table row across the full
JSON/CSS/Sheets/BBNF matrix, and ALL seven inventories carry the "@generated, not
hand-written" framing or the GENERATOR-LOC-vs-emitted-LOC distinction. The V4 note's
predicted "third instance" did NOT materialise — the sweep prevented it.

The all-8 grammar scan census is uniform and correct across every inventory; the
math.rs field-absent/no-scan distinction (scan=0, OnceCell doc-comments at :281/:285,
no `structural_index` field, ScanState only nospace_bits/nospace_start :288-289) is
accurate everywhere; the lazy-`ValueRef<G>` generalization is catalogued
grammar-neutral and grammar-parametric (core grep=0, skinny G:EventGrammar at
tape/mod.rs:175); the by-construction Sheets/BBNF-self discrimination (SK17L-009) is
intact and does not over-claim; the generic crates are grammar-name-leak-clean
(simd-scan empty; runtime/tape + substrate.rs doc-comments only; emitter data-binds).
The excavation spans the full JSON / CSS / Sheets / BBNF-self matrix Lock 14 demands
— not CSS-only, not JSON-only.

Counts: 14 sections — 14 ACCEPT, 0 REVISE, 0 REJECT (100% ACCEPT). The V4 residual is
resolved and NO new Lock-14 framing residual surfaces. This is the FIRST cycle with
zero GENERALITY REVISE — the third-instance risk the V4 note flagged is closed by the
cross-inventory sweep. Combined with V4's ≈93%, GENERALITY meets the ≥95%×2 gate IF
V4 is counted as the first ≥95%-adjacent cycle; on its own merits V5 is a clean 100%
GENERALITY convergence. The substantive firewall — all-8 census, value-API
neutrality, by-construction Sheets/BBNF, leak-clean generic crates — holds
live-verified end-to-end.
