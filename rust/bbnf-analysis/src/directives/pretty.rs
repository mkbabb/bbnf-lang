//! `@pretty` directive extraction, validation, and semantic token generation.
//!
//! Re-exports the original `state::pretty` logic for backward compatibility.
//! The canonical implementations live in `state/pretty.rs`.

pub use crate::state::pretty::*;
