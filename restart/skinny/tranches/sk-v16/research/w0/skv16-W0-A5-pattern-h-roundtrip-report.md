# SK-V16 W0-A5 Pattern H Roundtrip/Census Report

Status: read-only research. No files edited, staged, or committed.

## Finding

Pattern H is currently `67` only with the lock-bound census command:

```sh
find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l
```

The forbidden bounded-depth variant returns `63` because it omits the four depth-3 Google Sheets document files. W0 must bind the correct command into the report consumer and negative fixtures; it must not rely on header-only generated provenance and must not edit generated runtime sources.

## Required W0 Report Fields

`--skv16-pattern-h-roundtrip-report <path>` should validate at least:

```json
{
  "schema_id": "sk-v16-pattern-h-roundtrip-v1",
  "wave_id": "SK-V16-W0",
  "census_command": "find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l",
  "pattern_h_count": 67,
  "forbidden_maxdepth_command": "find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' | wc -l",
  "forbidden_maxdepth_count": 63,
  "depth3_required_paths": [
    "crates/core/src/runtime/google_sheets/document/canonical.rs",
    "crates/core/src/runtime/google_sheets/document/mod.rs",
    "crates/core/src/runtime/google_sheets/document/path_query.rs",
    "crates/core/src/runtime/google_sheets/document/view.rs"
  ],
  "line1_provenance_expected_regex": "^// @generated by xtask regen-[a-z0-9-]+; do not edit by hand\\.$",
  "line1_provenance_count": 67,
  "roundtrip_command": "cargo xtask check-runtime",
  "roundtrip_status": "pass",
  "generator_owner_status": "roundtrip_proven",
  "header_only_proof": false,
  "generated_source_edits_in_w0": false
}
```

For W0 research, `roundtrip_status` may be `not_run_in_readonly_research`, but the W0 gate fixture must reject missing, failed, or producer-only roundtrip fields. W7/W8 own the later generator-collapse implementation work.

## Current Census Shape

Observed current grammar file counts under `crates/core/src/runtime`:

```text
bbnf           8
bnf            7
css_l4         7
css_pretty     7
csv            7
ebnf           7
google_sheets 10
json           7
math           7
total          67
```

All 67 files currently have line-1 provenance of the form:

```text
// @generated by xtask regen-<grammar>; do not edit by hand.
```

That is necessary but not sufficient. Header-only provenance must remain a rejecting state unless paired with the non-writing generator roundtrip check and expected path-set proof.

## Proof Commands

Run from `/Users/mkbabb/Programming/bbnf-lang`:

```sh
find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l
find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' | wc -l
git status --short -- crates/core/src/runtime
cargo xtask check-runtime
```

Header scan:

```sh
while IFS= read -r file; do
  first=$(sed -n '1p' "$file")
  case "$first" in
    "// @generated by xtask regen-"*"; do not edit by hand.") ;;
    *) printf '%s:1:%s\n' "$file" "$first" ;;
  esac
done < <(find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | sort)
```

Expected W0 result: no bad header rows, count `67`, forbidden maxdepth count `63`, and no dirty `crates/core/src/runtime` files.

## Negative Fixtures

W0 should include rejecting fixtures for:

- `pattern_h_count != 67`
- forbidden maxdepth command accepted as proof
- missing any Google Sheets document depth-3 path
- `line1_provenance_count < 67`
- header-only proof with no roundtrip command or roundtrip status
- roundtrip status missing, failed, or producer-only
- `generated_source_edits_in_w0 = true`
- unexpected producer-only field

## W0 Boundary

W0 may add report consumers and negative fixtures. It must not edit `crates/core/src/runtime/**`, run writing `regen-*` commands as a close condition, or accept generated-source edits as part of the W0 slice. Generated-owner collapse remains routed to later Pattern H waves with byte-equivalent non-writing proof.
