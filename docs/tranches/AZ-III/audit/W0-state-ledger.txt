# AZ-III W0 State Ledger - 2026-04-30

HEAD after history repair:

```text
b20ea61b docs(az-iii.waves): harden continuation wave gates and triumvirate dispatch
```

Backup and preservation:

```text
backup branch: codex/az-history-before-reword-20260430-114057
stash: stash@{0} pre-az-history-reword 2026-04-30T11:40:57-04:00
stale sequencer backup: /tmp/bbnf-reword/stale-sequencer-20260430-115015
```

The stash was applied back to the main worktree after the message-only
history rewrite. The stash remains as a safety copy.

Root worktree:

- staged: none;
- dirty: restored AZ-II implementation/source slice, now formatted by
  `cargo fmt --all`;
- untracked: `docs/benchmarks/archive/AZ-II/cutover/O5-tape-delete-scan.txt`,
  `docs/precepts-plan.md`;
- stale `.git/sequencer` state was saved under `/tmp/bbnf-reword` and
  cleared with `git cherry-pick --quit`; no tree changes were made by
  that cleanup.

Sibling repo state:

- `/Users/mkbabb/Programming/parse-that`: pre-existing staged
  `.gitmodules`, `docs/instructions/README.md`, and `docs/precepts`
  remain staged; rust formatting and lock refresh were committed; untracked
  `rust/.cargo/` and rustc ICE file remain.
- `/Users/mkbabb/Programming/pprint`: rust formatting committed; untracked
  `pprint` and `rust/.cargo/` remain.

Format and lint baseline:

```text
bbnf-lang: cargo fmt --all -- --check PASS
bbnf-lang: cargo iter-clippy FAIL
bbnf-lang: cargo iter-check PASS, with generated-code warnings
bbnf-lang: cargo iter-test FAIL
parse-that/rust: cargo fmt --all -- --check PASS
parse-that/rust: cargo clippy --workspace -- -D warnings FAIL
parse-that/rust: cargo test --workspace FAIL
pprint/rust: cargo fmt --all -- --check PASS
pprint/rust: cargo clippy --all-targets -- -D warnings FAIL
pprint/rust: cargo test PASS, with one warning and two ignored doctests
```

Root clippy failure surface:

- `crates/ser/src/writer.rs`: collapsible nested error checks.
- `crates/egraph-derive/src/lib.rs`: collapsible path/type check.
- `crates/simd-scan/src/compaction.rs`, `parity.rs`, `neon.rs`:
  doc continuation, `is_multiple_of`, needless borrows, and iterator
  lints.
- `crates/csp-solver/**`: collapsible checks, type complexity,
  `Result<(), ()>`, argument-count, div-ceil, range-loop, and closure lints.

parse-that clippy failure surface:

- registry dependency conflict with published `parse_that 0.3.3` expecting
  old `pprint::Doc` / `pprint::Join`;
- `rust/regex/**`: range-loop, mutable range-bound, collapsible-if,
  duplicate branch, manual range contains, redundant closure, unwrap
  default, counter-loop, div-ceil, and UTF-8 loop lints.

parse-that test failure surface:

- `cargo test --workspace` fails while compiling published
  `parse_that 0.3.3`, whose `parsers/csv.rs` imports old `pprint::Doc` and
  `pprint::Join` APIs that are absent from the current sibling `pprint`
  workspace.

pprint clippy failure surface:

- `benches/digit_count.rs`: generic benchmark samples need `Sync`;
- tests: approximate constants, unused skipped enum field, and useless
  `.into_iter()` calls;
- benches: redundant `RcDoc::as_string` closures.

pprint test surface:

- `cargo test` passes: 31 builder tests, 14 derive tests, 12 digit-count
  tests, 13 pretty tests, and 2 ignored doctests. The run emits one
  dead-code warning for `TestEnum::Skipped(i32)`.

Root compile/test surface:

- `cargo iter-check` passes under `ax-iter`, with generated-code warnings
  for labeled `break` expressions, useless unsigned comparisons, and a small
  set of unused helpers/imports.
- `cargo iter-test` compiles and starts nextest, then fails fast:
  202/1509 tests run, 201 passed, 1 failed, 25 skipped, 1307 not run.
  The failing test is
  `bbnf::ax_w0a2s_real_css_probe bootstrap_full_parse`; `bootstrap.css`
  fails at offset 9317 because shape-dispatcher `skip_space` does not
  consume a leading or inter-rule block comment.
  Earlier first-attempt failures before fail-fast cancellation also exposed
  CSS hex/named-color typed-payload and `:dir()` branch payload misses.

W0 close remains blocked until the dirty root implementation slice is either
committed in owned batches or routed into W1/W2/W3 dispatch worktrees, and
until the clippy surfaces are assigned to the relevant waves.

## 2026-04-30 - Dirty Slice Disposition

Updated after orchestrator inspection of the restored slice:

- The "AZ-II implementation/source slice" is now confirmed to be 437
  source files with +4813/-4081 net +732 LOC, all of which is rustfmt
  2024-edition normalisation: collapsed `if/else` to single-line,
  reflow of struct-field, `use`-import sort, and single-line function
  signatures. `git diff -w` over the constraint solver, shape_dict,
  emitter, and registry surfaces returns empty. No semantic or
  architectural change rides on the slice. Generated grammar files
  under `crates/core/src/grammar/generated/**` are not part of this
  slice; rustfmt vs prettyplease drift on those files is a W1
  responsibility.
- The two untracked docs (`docs/benchmarks/archive/AZ-II/cutover/O5-tape-delete-scan.txt`
  and `docs/precepts-plan.md`) were authored alongside the AZ-II.O5
  scan and the BC precepts close; they are evidence artefacts that
  belong with W1 - O5 Reclose and the BC submodule pin respectively.

Disposition: PRESERVED, routed to W1 - O5 Reclose. The slice and the
two untracked evidence files are stashed as `stash@{0}` with the title
`AZ-III.W0 preserved: post-AZ-II cargo fmt --all + W0/W1 evidence;
routed to W1`. W1 - O5 Reclose reapplies the stash inside its
sibling-worktree commit plan (one of: `style(workspace): apply cargo
fmt --all` + `docs(az-ii.cutover.O5): record tape deletion scan` +
`docs(precepts): land precepts extraction plan`) once `cargo xtask
regen` clears the global drift gate; the pre-commit `regen --check`
hook fails as long as the 9/9 grammar drift remains, so the slice
cannot land in W0 without bypassing hooks (forbidden).

Hard gate 2 verdict: every dirty slice is now classified
(committed = none, preserved = the stash, blocked = none).
Implementation dispatch can proceed against clean main.
