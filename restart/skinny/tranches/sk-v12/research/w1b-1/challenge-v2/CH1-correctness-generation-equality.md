# SK-V12 W1b-1 CHALLENGE V2 CH1 - Correctness / Generation / Equality

Verdict: ACCEPT FOR REDRESS.

## Evidence

- Plan V2 preserves the V1 CH1-correct row and boundary:
  `css_l4/declaration_values/direct_to_struct/main`,
  `css_l4_declaration_value_fact_stream`, scalar-only W1b-1, no `bbnf-simd`,
  no aarch64, and no `lightningcss` throughput/admission claim. The
  lightningcss bar remains explicitly owned by W1b-2.
- SPEC Section 6 now matches the plan-time owner surface needed for this route:
  CSS grammar inputs, `skinny/Cargo.toml`, codegen profile/provider/templates,
  runtime export, generated CSS runtime directory, independent oracle/bench,
  report/gate files, `lock14_baseline.rs`, the CSS fixture, companion report,
  artifacts, and REDRESS/RESULTS authority. That repairs the V1 owner gap
  without changing the CH1 correctness target.
- Plan V2 strengthens, rather than weakens, the generated proof route. Track 1
  must be emitted by a CSS-owned codegen profile/provider/templates surface,
  produce generated `mod.rs`, `config.rs`, `parser.rs`, and `generated.rs`,
  and pass a reproducibility test that byte-compares freshly rendered output
  against committed runtime output.
- The fixture is fixed and falsifiable: the LF-terminated 187-byte CSS corpus
  with SHA-256
  `cbb639460a72ef82e7c1b7c53ccc69495a35f6860b29ad72370b042b470d7374`.
  This is sufficient for W1b-1 scaffold equality because SPEC Section 6 asks
  for generated execution plus independent oracle equality, not a full
  lightningcss SOTA admission.
- The equality route is explicit: retained Track 1 facts and retained
  `cssparser` oracle facts must be byte-equal before timing, with first-diff
  artifacts on failure. The companion gate consumes strictness, checksums,
  input bytes, generated size, validation/profile artifacts, Lock 14/16 status,
  scalar-reference status, and parity status in the same wave.
- The cap-fit V2 narrowing does not create a CH1 correctness blocker. It removes
  nonessential scope while keeping the minimum falsifiable scaffold: generated
  runtime proof, CSS-local fact sink, independent oracle/equality, retained
  artifacts, and gate-consumed report fields.

## Required Redress Preconditions

- Track 1 parsing/token traversal must be generated by the CSS provider. The
  CSS-local `sink.rs` may normalize and emit facts, but it must not become the
  tokenizer, declaration parser, generated substitute, or oracle.
- The generated-runtime reproducibility test must fail on any divergence
  between freshly rendered CSS runtime files and committed generated output.
- The canonical fact stream must be semantically meaningful for the selected
  fixture: schema version, input checksum/bytes, declaration ordinal, enclosing
  context such as `@media`, property name, `!important` state, value/token facts,
  stable source positions or equivalent spans, and final stream hash. Digest
  equality, count equality, or pretty-print equality is insufficient.
- The oracle must be independent: no calls to generated Track 1,
  `runtime::generated_json`, root CSS runtime, `lightningcss`,
  `parse_that_regex`, `bbnf-simd`, generated parser internals, or shared CSS
  parser/classification helpers.
- `gate-json --skv12-non-json-report ... --check-results` must reject missing
  or producer-only values for the CH1 load-bearing fields: strict output
  equality, row/output-plane identity, generated LOC/module bytes, input and
  grammar checksums, retained artifact paths, scalar-reference status, and
  parity status.
- W1b-1 may record only a scaffold/equality `C/GO` non-JSON row with finite
  Track 1 and oracle Mbps. It must not add `lightningcss_mbps`, CSS ADMIT
  language, a new outcome variant, or main JSON telemetry columns in
  `skinny/RESULTS.md`.
- If generated proof, fact-stream equality, oracle independence, or companion
  gate consumption fails, redress must save
  `/tmp/skv12-waveW1b-1-rejected.patch`, revert only the W1b-1 owner slice, and
  record `BLOCKED/FAIL` for `G-W1b-1-CSS-L4-ORACLE`.

## Blockers

None for CH1.

Plan V2 repairs the V1 owner/cap issues without invalidating the
correctness/generation/equality route. Redress may proceed from the CH1 lens if
the other V2 lenses accept their surfaces.
