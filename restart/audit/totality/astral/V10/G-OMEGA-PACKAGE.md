# G-OMEGA PACKAGE — Pass Omega V10 (SK-V18 Generalization Cycle)

Date: 2026-06-01. HEAD at audit: `25297a7fc`.
Gate: **G-Omega V10 is the MANDATORY user gate AHEAD.** This package is the
proposal-only presentation the user reads before authorizing the CRUD merge into the
V1 governance surfaces. Every staged diff is STAGED ONLY; the CRUD merge executes
POST-G-Omega, after user sign-off. No live governance surface is touched.

Cycles: 5 (V1..V5). converged=false (consec=0, voids=0). REVISE-rate trajectory:
29.4% → 5.9% → 11.8% → 11.8% → 12.5% (monotone-low after V1; residual near-exhausted,
one single-class fresh defect per cycle — never a premature 3Z declaration).

## 1. Cycle Summary

**Consumed:** T-P1 (1A-1F evidence inventories; near-converged NON-normal-§3Z),
T-P2 (R-A..R-F teardown + §6 (a)-(d) gate validation + aarch64-only; near-converged
NON-normal-§3Z), T-P3 (CONVERGED into 3A..3F + the 3C-locks-v+1-diff, 21 candidates
disposed). **SK-V18 lessons (ΩB):** the INFLECTION cycle — the inversion of SK-V13's
fake admit. >SOTA is MEASUREMENT-VALID (JSON 51/51 strict cold > sonic-rs; CSS cold >
lightningcss 1.66-3.38× DIRECTIONAL) but the ONE generator does NOT exist (forked
couriers + `RuntimeEmitterKind` family-fork + 7 css_l4 replicas + phantom `<G>` +
x86 + green-by-exclusion gate); a naive grammar-walk regresses to lightningcss's own
94.1% scan; >SOTA survives ONLY via §6-(a)-(d)-gated named primitives.
**Corpus-coherence delta (ΩA):** the live corpus is INCONSISTENT on COH18-001 —
HANDOFF/MASTER §13.6/ARCH §0/§7.4 all call SK-V18 the `crates/core/` tape-fold; the
CERTIFIED SK-V18 is the SKINNY generalization (12-wave PRUNE→G1..G6→PROVE→H1,
aarch64-only, ≈ −10800 LOC). The `crates/core/` adoption is SK-V19. The 5-shape
canon is byte-identical across all surfaces; the T-P3 deltas CORRECT (not introduce)
the drift.

## 2. CHALLENGE CONSOLIDATED Verdict (per-cycle r)

| Cycle | r | Fresh defect |
|---|---|---|
| V1 | ≈29-36% | firewall-tense, dangling SCANNER-UNIFY owner, "Pass-Omega-V6" labels, net-LOC dual-figure, four-item fence dropping 246 |
| V2 | ≈5.9% | byte-identity-to-3C claim FALSE (hardened-FROM-3C, not ==3C) |
| V3 | ≈11.8% | single-movemask census mis-singularized (≥4 non-delegating packs live) |
| V4 | ≈11.8-12.5% | P2 falsifier `:633`→`:627`; retarget clause elides test-only-reachable shell |
| V5 | ≈12.5% (2/16) | fabricated-VERBATIM `LOCKS.md:349` "overfitting mess" quote (rides 2 merge-bound surfaces, from 3F) |

converged=false is HONEST: each cycle absorbs the entire prior REVISE set and finds
one shrinking, single-class, citation-precision residual rather than a premature 3Z.
At V5, 14 of 16 staged operations are citation-clean; the lone surviving class is a
one-line strip-the-quote-marks fix (the SK-V19 `css_types.rs` disposition is sound).

## 3. Staged Locks-Diff Status (the G-Omega gate object)

- **`git apply --check` exit 0** (re-verified at HEAD) against live
  `restart/locks/LOCKS.md`.
- **9A / 11M / 0R / 1D** over 21 candidates → 11 addendum clauses, zero silent drops.
  - 9 ACCEPT: (a)-(d) gate, relocated-seam firewall, neutrality-proof, verbatim-blob,
    green-by-exclusion, un-fork def, relocated-seam lock, CollapsedStage slot,
    single-substrate+mask.
  - 11 MODIFY: aarch64-ONLY, Pattern-H re-census, cursor-generality re-anchor +
    e-graph guard, neutrality-proof scoping, retarget-not-author, single-substrate
    movemask.
  - 0 REJECT.
  - 1 DEFER: LAC-2F-V3-03 (audit-scope "balanced-scan gap" — folded to a one-line
    note, re-entry trigger: `ls` both scan trees before any gap claim).
- Diff body is the V10-hardened consolidation OF 3C (md5 differs; 6 of 11 clauses
  changed, V1-V3 corrections folded); NO clause dropped/added/REVERSED.
- **Load-bearing accepted clauses:** (1) named-primitive (a)-(d) gate — every kernel
  under the §6 escape passes (a) grammar-INVOKED, (b) output-VARIES, (c)
  `verbatim_blob_present==false`, (d) PROFILE-NARROW-LEAF, else REJECT; the single
  largest paper-close surface lifted to lock discipline. (2) relocated-seam firewall
  + un-fork — ONE `render(program)` on `backend_shape` only, `RuntimeEmitterKind`
  DELETED, `generator_grammar_branch_count==0` (PLANNED-at-G3, UNBUILT at HEAD);
  md5-distinctness NECESSARY-NOT-SUFFICIENT; the `runtime_target_rows_collapsed`
  full-row `PartialEq` co-gate is mandatory (arm-grep cannot see a relocated branch).
- Invariants: 16 locks (no Lock 17), 5 shapes (no sixth; CollapsedStage = SHAPE SLOT,
  diagnostic-only body), addition-only. Anchor: after `LOCKS.md:622`.

## 4. Staged Master-Plan-Diff Summary

`master-plan-diff.md` (Ω-D) applies 10 3B deltas + 4 carried: §13.6 RE-KEY (SK-V18
Tape-Fold → SK-V19 Totality-Fold; F1-F9 verbatim), NEW §13.7 SK-V18 GENERALIZATION
12-wave receiver (each row a same-wave consumer + RED exit-gate falsifier), §25
monotonic order (SK-V15→V17→V18-skinny→V19-core), §24 carry-ledger + SK-V19 tee-up,
§5/§13.5 CSS-verdict (directional pending H1; "MEASUREMENT-VALID" forbidden
pre-H1), §13-H label alignment. Net LOC "≈ −10800 campaign (per-wave sum ≈ −10685)" —
a REDUCTION ledger; refuted §H census = 0 (pivot is identity, not refutation); 16-lock
+ 5-shape preserved.

## 5. Planned CRUD Operations (merge on G-Omega-close)

- **CRUD-1 ARCHITECTURE:** `architecture-delta.staged.md` (git-apply exit 0) — §0
  SK-V15→SK-V18 authority, §7.4 re-title + CSS courier-prohibition, §7.3 x86
  CollapsedStage→diagnostic (slot retained), §9.2 phantom `<G>` strike + Cursor/
  config-breadth re-anchor, §10/§7.3 un-fork + firewall + co-gate; record 14 deltas.
- **CRUD-2 MASTER-PLAN:** `master-plan-diff.md` — §13.6→SK-V19, +§13.7, §25/§24/§5/
  §13.5/§13-H reconciliations.
- **CRUD-3 LOCKS:** `locks-diff.md` (the gate object, exit 0) — add the SK-V18 T-P3
  v+1 Crystallisation Addendum (11 clauses) after `:622`.
- **CRUD-4 HANDOFF+MIGRATION:** `handoff-delta.staged.md` + `migration-delta.staged.md`
  — strike HANDOFF `:16-19`, re-root dispatch `:103-105` to skinny W-PRUNE, insert
  Pass-Omega-V10 override + MIGRATION §0.0 receiver, 5 migration decisions,
  PRUNE-before-GENERALIZE gate; strip the V5 false "overfitting mess" quote-marks.
- **CRUD-5 SKINNY CORPUS:** `ΩE-skinny-corpus-staged-diff.md` — sync 6 surfaces off
  the V9/SK-V15 + V5/SK-V17 stale stack to SK-V18 anchors; FIX the BENCH.md CSS
  comparator inversion (lightningcss IS the >SOTA speed bar; cssparser is the EXACT
  oracle); adopt `track1_rich` (`784ceb418`); COMPILER.md 5-shape canon verbatim.
- **CRUD-6 AUDIT+CLEANUP:** citation scrub — SHAs resolve, locks-diff applies, 5-shape
  byte-identical post-merge, no stale "V6" label, net-LOC dual figure, V5 fabricated
  quote cleared.

## 6. SK-V18 Close — One Paragraph

SK-V18 is the INFLECTION tranche: the >SOTA is real and same-plane (JSON 51/51 strict
cold > sonic-rs; CSS canonical cold > lightningcss 1.66-3.38× DIRECTIONAL pending the
H1 quiet re-lock), but the "grammar-driven" emission story is DISPROVED — the ONE
generator does not yet exist (forked couriers + `RuntimeEmitterKind` family-fork + 7
css_l4 replicas + phantom `<G>` + crate-wide x86 + green-by-exclusion Lock-14 gate),
the round-trip passing only over hand-written content. S-P0..S-P3 are CERTIFIED (a
12-wave PRUNE-before-rebuild manifest, net ≈ −10800 campaign LOC, aarch64-only);
T-P1/T-P2 near-converged NON-normal-§3Z, T-P3 CONVERGED into 3A..3F + the 9A/11M/0R/1D
locks-diff (git apply --check exit 0). The durable lesson: >SOTA rides hand-written
bodies and survives ONLY via §6-(a)-(d)-gated named primitives; a naive grammar-walk
regresses to lightningcss's own 94.1% scan; the generalization is the wave-program
burden, gated at G-Omega.

## 7. Post-G-Omega Next-Move

CRUD merge (CRUD-1..6) → **W-PRUNE P1-P5 dispatch-eligible FIRST** (the only
dispatch-now cluster on S-P3 close; entry-gate-free, parallel on disjoint paths, P4
MUST land before G2/G3; every G1..H1 wave blocked on predecessor exit-gate + GREEN
entry predicate + triumvirate dispatch; G2/G4/G6 additionally blocked until the
SK-V16/V17 REDRESS four-item pre-block 51/53/246/247 lands on the committed ledger)
→ **SK-V19 totality-fold tee-up** (the REMAINDER after SK-V18 proof: the `strategy.rs`
9-grammar R16 row-collapse, `css_types.rs` relocate, Pattern-H 67/71 reconcile,
`simd-scan` asymmetry, LOCKS:620 generality-vehicle 1-line reconcile).
