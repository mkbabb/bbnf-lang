# SK-V13 S-P2 V4 CH4: Cost / Micro-Proof

Verdict: ACCEPT.

## Evidence

- V4 is a confirmation pass over the unchanged V3 packet. The V3 consolidated
  verdict records CH4 as ACCEPT and states that CSS row scopes remain conditional
  on narrow parser evidence or same-wave strict row movement, with SIMD inside a
  CSS row inheriting scalar reference, parity/checkasm, same-wave consumer, and
  row-movement gates
  (`restart/skinny/tranches/sk-v13/research/p2/hardening/HARDENING-S-P2-V3-CONSOLIDATED.md:19`-`26`).

- The V3 P2-F fold closes the V2 cost blocker without promoting CSS rows into
  primitive eligibility. `CSS-ROW-SCOPE-CONDITIONAL` is explicitly a generated
  CSS parity row/fact-stream scope, not a primitive admission, and S-P3 may plan
  it only with a fresh narrow CSS parser profile or same-wave strict
  lightningcss/cssparser row movement
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:44`-`47`).

- The six CSS row scopes individually carry scalar-oracle and consumer
  requirements while retaining the same conditional row-movement gate. This is
  enough for S-P3 to plan rows without treating feature coverage as a support-only
  primitive landing
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:69`-`74`).

- SIMD and ASM candidates remain cost-gated. P2-F keeps byte-set run-skip,
  byte classifiers, escape masks, string-special scan, escape decode, UDOT,
  PMULL/CSSC structural scan, digest folds, resolver surfaces, and union variants
  tied to scalar references, checkasm/parity where applicable, and same-wave row
  consumers
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:82`-`106`).

- The carry-forward classification preserves the no-support-only rule. CSS rows
  1-6 are conditional row-production scopes; SIMD inside them inherits its own
  scalar/checkasm/consumer gate; standalone EOR3/BCAX, cache hints,
  prefix/next/bulk bitmap helpers, byte-context, LD4/TBX/SMIN/SMAX refinements,
  and D1 lazy capacity remain ineligible without fresh P2 evidence and a
  same-wave consumer
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:151`-`166`).

- Generated-size and resolver-cost risk remain visible. P2-F carries regex facts
  -> e-graph candidates -> cost extraction -> CSP assignment -> codegen as the
  legal output-piping shape and rejects a fused CSP/e-graph solver, preserving the
  cost/model boundary S-P3 must enforce
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:199`-`209`).

- The V3 disposition explicitly freezes the accepted V2 boundaries: no new source
  claims, no promotion of support-only SIMD inventory, and no S-P3 shortlist
  entry for standalone EOR3/BCAX, cache hints, bitmap helpers, byte-context,
  LD4/TBX/SMIN/SMAX, or D1 lazy capacity unless later accepted research supplies
  fresh evidence
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:238`-`260`).

## Blockers

None for CH4.

## Fold Requirements

- S-P3 must preserve `CSS-ROW-SCOPE-CONDITIONAL` exactly: CSS rows 1-6 are
  row-production scopes, not primitive eligibility.
- Any CSS row plan must name fresh narrow CSS parser profiling evidence or
  same-wave strict lightningcss/cssparser row movement.
- Any SIMD/ASM primitive selected inside a CSS or JSON row must carry an
  executable scalar reference, strict checkasm/parity where applicable,
  same-wave consumer wiring, material-differential citation, and measured row
  movement.
- C7/B2/P2E-6 `ByteSetRunSkip64` remains conditional route-production: SK-V12 W4
  microbench evidence is not row admission without production CSS scan-block
  consumption.
- Support-only inventory remains out of S-P3 scope unless a later accepted
  research cycle adds a fresh hot-leaf/material-differential case plus same-wave
  consumer.

## Disposition

CH4 confirms V3 acceptance. The unchanged V3 packet preserves the
micro-prove-first discipline, scalar/checkasm/same-wave consumer gates,
generated-size and resolver-cost risk, and the prohibition on support-only
landings. No CH4 revise is required for S-P2 convergence.
