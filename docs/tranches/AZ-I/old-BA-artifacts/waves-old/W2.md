# BA.W2 — Structural-Side Leverage at Scale

**Opens after**: W1 close
**Agents**: 3 parallel
**Hard gate**: Structural-side accelerants improve the canonical path at scale without opening an alternate parser architecture.
**Status**: planned

## Scope

1. Add stronger object/key and structural dispatch leverage on the
   canonical path.
2. Add bounded regex or structural-index accelerants where the shared
   facts admit them.
3. Prove these accelerants pay for themselves on target fixtures.

## File bounds

| File | Access |
|---|---|
| `crates/core/src/backend/rust/emitter/shapes/compound.rs` | modify |
| `crates/core/src/generate/regex/emit.rs` | modify |
| `crates/core/src/generate/regex/dfa_codegen.rs` | modify |
| `crates/core/tests/value_api_apples_to_apples.rs` | modify |

**Do NOT touch**: replay/recovery/debug tooling, build/profile scripts, any alternate parser entrypoint.

## Phase sub-items

### BA.W2.1 Object and key dispatch leverage

Mechanism: add stronger multi-key and object-run accelerants on the
canonical path using shared structural facts.

Files touched: `crates/core/src/backend/rust/emitter/shapes/compound.rs`,
`crates/core/tests/value_api_apples_to_apples.rs`.

Sub-gate: value/path access tests continue to pass on the strengthened
dispatch path.

### BA.W2.2 Regex and structural index leverage

Mechanism: use bounded regex and structural-index facts where the
shared-cost plan admits them and where consumer benefit is measurable.

Files touched: `crates/core/src/generate/regex/emit.rs`,
`crates/core/src/generate/regex/dfa_codegen.rs`.

Sub-gate: emitted regex/structural accelerants are present on the
production path.

### BA.W2.3 Benefit proof

Mechanism: capture the designated benchmark set and prove the new
accelerants improve real entries rather than only isolated probes.

Files touched: `crates/core/tests/value_api_apples_to_apples.rs`.

Sub-gate: the designated benchmark set improves.

## Hard gate

1. `cargo expand -p bbnf --bench json_monolithic` or the targeted
   grammar bench shows emitted structural-side accelerants on the
   production path. Artefact: `docs/benchmarks/post-BA-W2-expand.txt`.
2. The designated benchmark set improves versus post-W1. Artefact:
   `docs/benchmarks/post-BA-W2-mid.json`.
3. Access/parity tests continue to pass. Artefact:
   `docs/benchmarks/post-BA-W2-tests.txt`.

## Verification artefacts

- `docs/benchmarks/post-BA-W2-expand.txt`
- `docs/benchmarks/post-BA-W2-mid.json`
- `docs/benchmarks/post-BA-W2-tests.txt`
- commit hashes for each W2 sub-phase

## Dependencies

- **Depends on**: W1 close
- **Blocks**: W3, W4
