# BC.W1 - AUDIT-B Routed Splits

**Name**: W1 - AUDIT-B Routed Splits
**Opens after**: W0 - Truth + Cleanup-Substrate Inventory
**Agents**: 5 parallel
**Hard gate**: `runtime/css_l4/builder.rs`, `passes/types/mod.rs`, `csp_strategy/mod.rs` split into directory-modules; max-file-LOC budget enforced (≤ 500 LOC per sub-module); cross-crate isomorphism per `feedback_directory_modules`
**Status**: planned

## Scope

1. Split `crates/core/src/runtime/css_l4/builder.rs` (1014 LOC at AUDIT-B time) into `crates/core/src/runtime/css_l4/builder/` directory-module with sub-modules organized by concern (declarations, values, selectors, etc.).
2. Split `crates/ir/src/passes/types/mod.rs` (786 LOC) into `crates/ir/src/passes/types/` directory-module sub-modules.
3. Split `crates/ir/src/passes/csp_strategy/mod.rs` into directory-module sub-modules.
4. Each sub-module ≤ 500 LOC. Cross-crate isomorphism: if `crates/core` and `crates/ir` both have a `types/` directory-module, the sub-module names (mod.rs / leaf / etc.) match the analogous structure where applicable.
5. Verify generated outputs still compile after splits; per-grammar parity tests pass; substrate-audit GREEN.
6. Update imports across the workspace; the new sub-module paths are the canonical paths going forward.

## Triumvirate Dispatch

Dispatch a triumvirate if any of these occurs:

- a target file's split surfaces an architectural decision the wave cannot make alone (e.g., a sub-module that needs a new public API exposed for cross-crate consumption);
- the cross-crate isomorphism cannot hold (e.g., `crates/core/src/runtime/types/` and `crates/ir/src/passes/types/` have intrinsically different structures);
- a split breaks a per-grammar parity test that the W0 inventory did not anticipate;
- the split-induced import churn introduces > 50 file modifications outside the named target files (scope reveal);
- three diagnostic-loop iterations fail to isolate why a split breaks a test;
- JSONL transcript quiet >15 minutes;
- first-pass return with no commit and no evidence (after one verbatim redispatch).

## File Bounds

| File | Access |
|---|---|
| `crates/core/src/runtime/css_l4/builder.rs` | delete (after split) |
| `crates/core/src/runtime/css_l4/builder/**` | create (directory-module) |
| `crates/ir/src/passes/types/mod.rs` | modify-carve (slim down) |
| `crates/ir/src/passes/types/**` | create new sub-modules |
| `crates/ir/src/passes/csp_strategy/mod.rs` | modify-carve (slim down) |
| `crates/ir/src/passes/csp_strategy/**` | create new sub-modules |
| Workspace files importing the targets | modify-carve (path updates) |
| `docs/tranches/BC/audit/W1-*` | create |

Do NOT touch: fixture contract (W2 owns); samply CI (W3 owns); Audit-A residue (W4 owns); cross-repo (W5 owns).

## Disjointness

W1.1 (`runtime/css_l4/builder/` split) writes `crates/core/src/runtime/css_l4/builder/**`. W1.2 (`passes/types/` split) writes `crates/ir/src/passes/types/**`. W1.3 (`passes/csp_strategy/` split) writes `crates/ir/src/passes/csp_strategy/**`. W1.4 (workspace import updates — coordinated, sequenced after W1.1+W1.2+W1.3) writes import statements across the workspace. W1.5 (audit doc + LOC verification) writes `docs/tranches/BC/audit/W1-*`. The five sub-units have disjoint primary write paths (W1.4 sequenced after the splits land).

## Worktree Plan

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| BC.W1.1 css_l4/builder split | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-css` | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-css/target/w1-css` |
| BC.W1.2 passes/types split | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-types` | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-types/target/w1-types` |
| BC.W1.3 csp_strategy split | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-csp` | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-csp/target/w1-csp` |
| BC.W1.4 Import Updates | (sequenced; main worktree integration step) | (orchestrator target) |
| BC.W1.5 Audit + LOC Verification | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-doc` | `/Users/mkbabb/Programming/bbnf-wt-bc-w1-doc/target/w1-doc` |

## Agent Units

### BC.W1.1 css_l4/builder split

- Mechanism: split `builder.rs` 1014 LOC into directory-module `builder/` with sub-modules: `declarations.rs`, `values.rs`, `selectors.rs`, `mod.rs` (root). Each sub-module ≤ 500 LOC.
- Files: `crates/core/src/runtime/css_l4/builder.rs` (delete), `crates/core/src/runtime/css_l4/builder/**` (create).
- Sub-gate: each sub-module ≤ 500 LOC; `cargo build` compiles cleanly; CSS L4 parity tests pass.

### BC.W1.2 passes/types split

- Mechanism: split `passes/types/mod.rs` 786 LOC into directory-module sub-modules per phase (Phase 4 emit, Phase 6 Repeat correction, Phase 7 scratch collection, etc.). Each sub-module ≤ 500 LOC.
- Files: `crates/ir/src/passes/types/mod.rs` (modify-carve), `crates/ir/src/passes/types/**` (create sub-modules).
- Sub-gate: each sub-module ≤ 500 LOC; `cargo build` compiles cleanly; type-inference tests pass.

### BC.W1.3 csp_strategy split

- Mechanism: split `csp_strategy/mod.rs` into directory-module sub-modules per strategy class. Each sub-module ≤ 500 LOC.
- Files: `crates/ir/src/passes/csp_strategy/mod.rs` (modify-carve), `crates/ir/src/passes/csp_strategy/**` (create sub-modules).
- Sub-gate: each sub-module ≤ 500 LOC; `cargo build` compiles cleanly; CSP strategy tests pass.

### BC.W1.4 Import Updates

- Mechanism: update workspace imports after the three splits land. The orchestrator integrates the three sub-units' commits, runs `cargo build`, fixes any import errors. The fix is mechanical (path-name change).
- Files: workspace imports (cross-cutting; orchestrator integration step).
- Sub-gate: `cargo build` GREEN; `cargo nextest run --workspace --cargo-profile ax-iter` 100% pass.

### BC.W1.5 Audit + LOC Verification

- Mechanism: document the post-W1 LOC distribution per directory-module; verify max-file-LOC budget. Document cross-crate isomorphism (or its absence with rationale).
- Files: `docs/tranches/BC/audit/W1-loc-distribution.txt`, `docs/tranches/BC/audit/W1-isomorphism.md`.
- Sub-gate: every sub-module ≤ 500 LOC; isomorphism documented.

## Hard Gate

1. `crates/core/src/runtime/css_l4/builder.rs` deleted; `builder/` directory-module exists with sub-modules ≤ 500 LOC each.
2. `crates/ir/src/passes/types/` directory-module exists with sub-modules ≤ 500 LOC each; `mod.rs` is now a slim root.
3. `crates/ir/src/passes/csp_strategy/` directory-module exists with sub-modules ≤ 500 LOC each.
4. Cross-crate isomorphism documented (or absence rationalised).
5. `cargo build` compiles cleanly; `cargo nextest run --workspace --cargo-profile ax-iter` 100% pass.
6. `cargo xtask regen --check` 9/9 green.
7. `crates/ir/tests/substrate_audit.rs` GREEN.
8. `cargo fmt --all -- --check`, focused `cargo clippy --profile ax-iter`, and `git diff --check` pass.

## Format And Lint Cadence

`cargo fmt --all -- --check`, focused `cargo clippy --profile ax-iter` for the three target directory-modules, full nextest at integration, and `git diff --check` after each integration batch and before close.

## Verification Artefacts

- `docs/tranches/BC/audit/W1-loc-distribution.txt`
- `docs/tranches/BC/audit/W1-isomorphism.md`
- `docs/tranches/BC/audit/W1-test-runs.txt`

## Commit Plan

Expected scopes: `runtime/css-l4/builder-split`, `ir/passes/types-split`, `ir/passes/csp-strategy-split`, `workspace/imports-update`, `docs/tranches/BC/W1-audit`. Commit bodies required for each split commit (sub-module structure rationale) and the W1 close status commit.

Every dispatch carries `HARD CAP: N min. At 0.9N commit, at N halt.`

## Dependencies

- **Depends on**: W0 (split scope confirmed by LOC verification).
- **Blocks**: W6 (close cites the post-W1 LOC distribution as evidence of `feedback_no-god-modules` compliance).
