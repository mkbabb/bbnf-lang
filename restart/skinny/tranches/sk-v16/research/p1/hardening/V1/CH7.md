# S-P1 V1 CH7 Overfit-Prune

Disposition: ACCEPT.

The profile set includes all 17 corpora and does not privilege float-heavy or
Unicode-only rows. P1-E quarantines harness costs and generated JSON-specific
leaves instead of converting them into broad primitives. P1-D records the PMU
export gap; P1-C records mode-III as diagnostic. No CSS or SIMD admission is
claimed from profile evidence alone.
