# CH2 — GENERALITY (V3)

S-P0 CHALLENGE V3 confirming-pass lens disposition (SK-V14 Overfit
Audit Pass).

Authority: `restart/prompts/ORCHESTRATOR.md §3W` row CH2 ("Lock 14
holds: no grammar-name leak; every proposed intervention is grammar-
neutral and works for CSS L4 / Sheets / BBNF-self, not only JSON.")
+ `restart/skinny/tranches/sk-v14/audit-overfit/hardening/V2/CH2.md`
§5 V3 fold recommendation (close §4.1 SYNTHESIS §2.4 codegen-side
count precision) + V3 micro-redispatch commit `007624849` (3 files
modified +10/-6; F-V3-A4-1 + F-V3-A5-1 + F-V3-SYNTHESIS-{1,2,3}).

V3 artefacts under review (commit `007624849`):

- `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-generator-truth.md` (A4 V3; F-V3-A4-1 line-count refresh 101→100)
- `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-decision-engine.md` (A5 V3; F-V3-A5-1 §5 action-class propagation)
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md` (V3; F-V3-SYNTHESIS-{1,2,3} — C-4 row A5 LOW cell + §2.4 codegen-side count + §1.1 NEW-2 Three→Four in-table cell)

A1, A2, A3, A6 STAND verbatim (per V3 commit message).

## §0 — Disposition summary

| Total dispositions | ACCEPT | ACCEPT-with-NOTE | REVISE | REJECT |
| ---: | ---: | ---: | ---: | ---: |
| **8** | **7** | **1** | **0** | **0** |

ACCEPT-rate **7/8 + 1 = 8/8 = 100 %** (ACCEPT-with-NOTE counts toward
ACCEPT-rate per §3Z convergence arithmetic). The single NOTE is a
fresh-finding scan discovery (N-V3-CH2-1: §2.4 prose arithmetic
inclusion-relation defect inherited through V3 fold) — editorial,
non-blocking, sub-CH2-fatal threshold.

F-V3-SYNTHESIS-2 closes the V2 §4.1 precision NOTE on the "8 codegen-
side template+provider files" count cleanly. The aggregate 14 count
is now executable-verified and inline-cited at SYNTHESIS §2.4:312-313
(`git grep -l '@generated by skinny bbnf-codegen' skinny/crates/
codegen/src/ | wc -l = 14`). The lint glob mechanism `codegen/src/**/
*.rs` is correctly described as catching all 14 regardless of the
prose-count interpretation. Lock 14 baseline (A3 30 violations) holds
verbatim — A3 carried no V3 fold; V3 commit `007624849` stat confirms
A3 file unchanged.

The CH2 GENERALITY posture remains sound end-to-end after the V3
micro-fold. The §2.4 fold strengthens the audit's arithmetic honesty
by replacing the V1-sampled 8 with the V3-executable-verified 14;
the lint glob's grammar-neutral binding (both runtime + codegen sides)
is preserved. The Three→Four propagation (F-V3-SYNTHESIS-3 at §1.1:81
in-table NEW-2 cell) completes the V2 fold coverage at the only V2-
missed cell, aligning the in-table NEW-2 cell with §1.2:122, §0.1,
and §5.1 (all V2-folded).

## §1 — Per-artefact V3 disposition

| Artefact | V2 → V3 fold(s) | V3 Disposition | CH2 verification |
| --- | --- | --- | --- |
| `sk-v14-audit-overfit-generator-truth.md` (A4 V3) | F-V3-A4-1 (line 153 cite refresh: full 101 lines → full 100 lines per `wc -l = 100`) | **ACCEPT** | CH2-neutral; line-count arithmetic refresh carries no GENERALITY scope drift; sample-target file is `skinny/crates/codegen/src/json_provider.rs` which is grammar-named but cited as the JSON-specific instance, not a scope-creep |
| `sk-v14-audit-overfit-decision-engine.md` (A5 V3) | F-V3-A5-1 (§5:133 action-class "no-op pre-C-4" → "preserved through PRUNE-5 as a gate-rejection invariant inside C-4 entry-gates so any admit attempting to cite W8/W9 pre-runtime-consumer is denied at admit time") | **ACCEPT** | CH2-neutral; action-class propagation aligns §5 with §4 row 4 V2-folded language; the W8/W9 entry-gate framing is grammar-neutral (admit-time denial applies to any future grammar attempting the same overfit) |
| `SYNTHESIS-AUDIT-OVERFIT.md` (V3) | F-V3-SYNTHESIS-1 (§3.1:339 C-4 row A5 LOW cell propagation) + F-V3-SYNTHESIS-2 (§2.4 codegen-side count 8→14 with executable-verified inline cite) + F-V3-SYNTHESIS-3 (§1.1:81 in-table NEW-2 cell Three→Four) | **ACCEPT-with-NOTE (N-V3-CH2-1)** | §3.1 below; F-V3-SYNTHESIS-2 closes V2 §4.1 NOTE; one fresh-finding NOTE on inherited arithmetic inclusion-relation defect at §2.4:311 |
| `sk-v14-audit-overfit-css-measurement.md` (A1) | STAND (no V3 folds) | **ACCEPT** | V2 CH2 §1 A1 row carries forward |
| `sk-v14-audit-overfit-admit-mechanism.md` (A2) | STAND (no V3 folds) | **ACCEPT** | V2 CH2 §1 A2 row carries forward |
| `sk-v14-audit-overfit-lock14-scan.md` (A3) | STAND (no V3 folds) | **ACCEPT** | V2 CH2 §1 A3 row carries forward; 30 violations re-verified at HEAD (§3.2 below) |
| `sk-v14-audit-overfit-pre-restart-pattern.md` (A6) | STAND (no V3 folds) | **ACCEPT** | V2 CH2 §1 A6 row carries forward; Pattern H 9-grammar census preserved |
| **Aggregate** | | **ACCEPT-with-NOTE** | — |

## §2 — Per-§ ACCEPT-rate (V3 cycle)

Per V2 CH2 §5 V3 fold recommendation, the V3 lens has one specific
fold-verification clause plus the standard fresh-finding scan +
carry-forward bookkeeping:

| CH2 V3 sub-clause | ACCEPT | ACCEPT-with-NOTE | REVISE | REJECT |
| --- | ---: | ---: | ---: | ---: |
| (i) **F-V3-SYNTHESIS-2 closes V2 §4.1 precision NOTE; "8 codegen-side template+provider files" → "14 codegen-side files (8 providers+templates + 6 ancillary)"** | 0 | 1 | 0 | 0 |
| (ii) **F-V3-A4-1 + F-V3-A5-1 + F-V3-SYNTHESIS-1 + F-V3-SYNTHESIS-3 (line-cite, action-class, in-table propagations)** | 4 | 0 | 0 | 0 |
| (iii) **Fresh-finding scan over V3 deltas + Lock 14 baseline re-verification** | 1 | 0 | 0 | 0 |
| (iv) **V3 carry-forward of V2 ACCEPTs (A1, A2, A3, A6)** | 2 | 0 | 0 | 0 |
| **All** | **7** | **1** | **0** | **0** |

Note: (i) admits as ACCEPT-with-NOTE because the fold closes the V2
NOTE successfully but introduces an editorial sub-defect in the prose
arithmetic (see §3.1 + §4.1 below); the GENERALITY scope is preserved.

## §3 — Executable verification (CH2 V3 evidence)

Per ADDENDUM §3 executable-verification mandate (carried through to
V3 per §3Z confirming-pass discipline), the following were re-run
against the working tree at HEAD == `007624849` (V3 micro-redispatch
landing commit).

### §3.1 F-V3-SYNTHESIS-2 — codegen-side count refresh

V2 CH2 §4.1 NOTE flagged SYNTHESIS §2.4 (line 311 pre-V3) prose "8
codegen-side template+provider files" against an executable-verified
codegen-side count of 14. V3 verification:

```
$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/ | wc -l
14

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/
skinny/crates/codegen/src/css_l4_at_rules_and_media_provider.rs
skinny/crates/codegen/src/css_l4_declaration_values_extended_provider.rs
skinny/crates/codegen/src/css_l4_declaration_values_provider.rs
skinny/crates/codegen/src/css_l4_nested_layout_provider.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_provider.rs
skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_provider.rs
skinny/crates/codegen/src/css_l4_visual_functions_provider.rs
skinny/crates/codegen/src/json_provider.rs
skinny/crates/codegen/src/json_templates/generated.rs
skinny/crates/codegen/src/json_templates/parser.rs
skinny/crates/codegen/src/json_templates/value.rs
skinny/crates/codegen/src/json_templates/view.rs
skinny/crates/codegen/src/json_typed_direct.rs
skinny/crates/codegen/src/lib.rs
```

14 confirmed. The V3 prose at SYNTHESIS §2.4:311-313 reads "42 files
carry the fake header including 14 codegen-side files (8 providers+
templates + 6 ancillary; `git grep -l '@generated by skinny bbnf-
codegen' skinny/crates/codegen/src/ | wc -l = 14`)." The 14 count is
inline-cited with the executable command — closes V2 §4.1 precision
NOTE.

Decomposition of 14:
- 8 "providers+templates": 7 CSS L4 providers (`css_l4_*_provider.rs`)
  + 1 JSON provider (`json_provider.rs`).
- 6 ancillary: 4 `json_templates/*.rs` submodules (generated, parser,
  value, view) + `json_typed_direct.rs` + `lib.rs`.

Arithmetic checks: 7 + 1 = 8; 4 + 1 + 1 = 6; 8 + 6 = 14. The
"providers+templates" label is mildly loose — `json_provider.rs` is
named "provider" not "template", and `json_templates/*` are templates;
the V2 CH2 §4.1 recommended framing "8 codegen-side template+provider
files (7 CSS L4 providers + 1 JSON provider) plus 6 ancillary
codegen-side files (4 json_templates submodules + json_typed_direct
+ lib.rs)" segregates more cleanly. The V3 fold landed a compressed
form; the math is correct and the lint glob mechanism is unaffected.
GENERALITY scope intact — `codegen/src/**/*.rs` brace expansion
catches all 14 regardless.

### §3.2 Lock 14 baseline re-verification — A3 unchanged

V3 commit `007624849` stat confirms A3 (`sk-v14-audit-overfit-lock14-
scan.md`) carries no V3 fold:

```
$ git show 007624849 --stat
 .../SYNTHESIS-AUDIT-OVERFIT.md            | 12 ++++++++----
 .../sk-v14-audit-overfit-decision-engine.md |  2 +-
 .../sk-v14-audit-overfit-generator-truth.md |  2 +-
 3 files changed, 10 insertions(+), 6 deletions(-)
```

A3 ledger holds at 30:

```
$ grep -cE "^\| (C[0-9]+|H[0-9]+|M[0-9]+|L[0-9]+) \|" \
    restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-lock14-scan.md
30
```

Per-severity distribution preserved from V2: 11 CRIT + 6 HIGH + 5 MED
+ 8 LOW = 30. H3→L8 reclassification carries through. The production
call site `passes/src/lib.rs:478` (`finalize_rule(&grammar.name, …)`)
remains the grammar-generic threading anchor; the test-fixture
`decision_csp.rs:230-240` (under `#[cfg(test)] mod tests`) remains
the L8-reclassified violation surface.

### §3.3 Primitive-crate generic posture re-check

Re-verification at V3 HEAD:

```
$ grep -rn -E '\b(Json|Css|Sheets|JsonValue|CssValue|SheetsValue|CssL4|CssPretty)\b' \
    skinny/crates/bbnf-regex/src/ \
    skinny/crates/bbnf-simd/src/ \
    skinny/crates/simd-scan/src/ \
    skinny/crates/parse-that-regex/src/ \
    skinny/crates/ir/src/
[no output]
```

Still clean across all 5 primitive crates. The V3 micro-redispatch
was docs-only (3 audit files modified; zero source touched per commit
`007624849` stat). No SK-V14 commit has introduced grammar-name leaks
into the primitive layer.

### §3.4 F-V3-SYNTHESIS-3 fixture-scanner count re-verification

V3 fold corrects "3 of 7 CSS scanners" → "4 of 7" at SYNTHESIS §1.1:81
in-table NEW-2 cell. Executable verification:

```
$ grep -rln 'CANONICAL_FIXTURE\|CAPTURED_W2_INPUT' \
    skinny/crates/codegen/src/
skinny/crates/codegen/src/css_l4_nested_layout_templates/generated.rs
skinny/crates/codegen/src/css_l4_vendor_and_custom_atrules_templates/generated.rs
skinny/crates/codegen/src/css_l4_at_rules_and_media_templates/generated.rs
skinny/crates/codegen/src/css_l4_stylesheet_selectors_templates/generated.rs
```

4 templates confirmed (3 use CANONICAL_FIXTURE + 1 uses CAPTURED_W2_
INPUT per V2 CH2 §3.2 decomposition). F-V3-SYNTHESIS-3 closes the V2-
missed in-table cell at §1.1:81. The fixture-lookup-table-dressed-as-
parser violation class is grammar-neutral (would catch any future
grammar attempting the same overfit).

### §3.5 Total fake-`@generated` surface re-verification

```
$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ skinny/crates/codegen/src/ | wc -l
56

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/runtime/src/grammars/ | wc -l
42

$ git grep -l '@generated by skinny bbnf-codegen' \
    skinny/crates/codegen/src/ | wc -l
14
```

Aggregate: 42 runtime-side + 14 codegen-side = 56 files. The lint
glob `skinny/crates/{runtime/src/grammars,codegen/src}/**/*.rs`
binds both halves of the round-tripping vector. GENERALITY scope
preserved.

## §4 — Fresh-finding scan (V3 cycle)

### §4.1 N-V3-CH2-1 LOW — SYNTHESIS §2.4:311 prose arithmetic
        inclusion-relation defect (V3-inherited from V2)

V3 SYNTHESIS §2.4:311 reads:

> "… CH2 §3.5 verified 42 files carry the fake header including 14
> codegen-side files (8 providers+templates + 6 ancillary; …)."

Per §3.5 above, the executable-verified surface is:
- 42 runtime-side files
- 14 codegen-side files
- 56 total

The phrase "42 files … including 14 codegen-side" mis-models the
inclusion relation: the 42 (runtime-side) and 14 (codegen-side) sets
are DISJOINT — neither is a subset of the other. Correct phrasings
include:
- "56 files carry the fake header (42 runtime-side mirror + 14
  codegen-side files (8 providers+templates + 6 ancillary))", OR
- "42 runtime-side files carry the fake header alongside 14 codegen-
  side files (8 providers+templates + 6 ancillary) for a total of 56".

The V3 fold F-V3-SYNTHESIS-2 inherited this arithmetic defect from
the V2 prose (V2 read "42 files carry the fake header including 8
codegen-side template+provider files" — same inclusion error, just
with the 8 count); V3 corrected the count but not the inclusion
relation. The lint glob mechanism is unaffected — `{runtime/src/
grammars,codegen/src}/**/*.rs` brace expansion catches both disjoint
sets correctly. This is editorial mechanism arithmetic, not a
GENERALITY scope drift; the lint's binding to both surfaces is the
load-bearing claim and remains correct.

**Severity:** LOW (editorial; non-blocking; sub-CH2-fatal threshold).

**Recommended V4 confirming-pass fold:** Tighten SYNTHESIS §2.4:311
phrasing to either of the two correct forms above. The 8-providers+
templates / 6-ancillary breakdown is already correctly stated; only
the inclusion-relation sentence needs the rewrite.

### §4.2 Per-grammar census re-verification

```
$ cargo metadata --format-version 1 --no-deps 2>/dev/null | \
    jq -r '.metadata.bbnf.grammars[].ident' 2>/dev/null
```

Per V2 CH2 §3.5: 9 grammars confirmed (bbnf, json, css_l4, css_pretty,
google_sheets, ebnf, bnf, csv, math). PRUNE-4 sub-wave count = 9
across A3 + A6 + SYNTHESIS §3.3. No V3 fold touched the census or
the co-derivation note; 9-grammar census preserved.

## §5 — V4 fold recommendations

If S-P0 V4 dispatches as the confirming-pass closer:

1. **N-V3-CH2-1 inclusion-relation refresh** (§4.1 above) — fold
   SYNTHESIS §2.4:311 "42 files carry the fake header including 14
   codegen-side files" → "56 files carry the fake header (42 runtime-
   side mirror + 14 codegen-side files (8 providers+templates + 6
   ancillary))" OR "42 runtime-side files carry the fake header
   alongside 14 codegen-side files (8 providers+templates + 6
   ancillary) for a total of 56". Editorial; no scope drift.

V4 is structurally the §3Z LOCK confirming pass over V3 artefacts
(per V3 commit message: "V3 + V4 confirming pass closes sec 3Z two-
consecutive-cycle chain"). CH2's V3 disposition is 100 % ACCEPT
(7 ACCEPT + 1 ACCEPT-with-NOTE / 8 = 100 %); V4 expects 100 %
ACCEPT again if N-V3-CH2-1 carries the §5.1 editorial fold —
satisfying the two-consecutive-cycle §3Z criterion (V3 100 % + V4
100 % → ≥ 95 % twice → G-S-P0-CONVERGED → S-P1 dispatch).

## §6 — Disposition

**CH2 V3 VERDICT: ACCEPT-with-NOTE.** F-V3-SYNTHESIS-2 closes the V2
§4.1 precision NOTE on the SYNTHESIS §2.4 codegen-side count cleanly:
the 8→14 refresh is executable-verified with inline cite (`git grep
... | wc -l = 14`); the decomposition (8 providers+templates + 6
ancillary) sums correctly; the lint glob mechanism is correctly
described as catching all 14 regardless. Lock 14 baseline holds —
A3 ledger preserved at 30 violations verbatim (V3 commit stat
confirms A3 file unchanged); per-grammar census (9 grammars)
preserved; primitive-crate generic posture preserved (zero name
leaks across 5 crates).

The Three→Four propagation (F-V3-SYNTHESIS-3 at §1.1:81 in-table
NEW-2 cell) completes the V2 fold coverage at the only V2-missed
cell — fixture-lookup violation count is grammar-neutral and now
arithmetically consistent across §0.1, §1.1, §1.2, §5.1, and the
underlying executable verification (4 templates use CANONICAL_
FIXTURE/CAPTURED_W2_INPUT short-circuits).

The single fresh-finding NOTE (N-V3-CH2-1: SYNTHESIS §2.4:311 prose
inclusion-relation defect "42 ... including 14" where 42-runtime and
14-codegen are disjoint sets) is editorial mechanism arithmetic
inherited from the V2 prose, not a GENERALITY scope drift. The
lint glob's grammar-neutral binding to both runtime + codegen
surfaces is the load-bearing claim and remains correct.

V3 CH2 ACCEPT-rate: **8/8 = 100 %** (7 ACCEPT + 1 ACCEPT-with-NOTE).
§3Z V3 ≥ 95 % satisfied for CH2; convergence depends on the other
six lenses' aggregate V3 disposition. V3 + V4 expected to close §3Z
LOCK two-consecutive-cycle chain.

---

**Authored:** 2026-05-23 (SK-V14 S-P0 CHALLENGE V3 lens CH2).
**Status:** WRITE-ONLY; aggregator commits all 8 V3 hardening files
atomically with `docs(sk-v14-audit-overfit-hardening-V3): challenge
V3 + consolidated`.
**Authority:** `restart/prompts/ORCHESTRATOR.md §3W` CH2 row;
V2 CH2 base at `restart/skinny/tranches/sk-v14/audit-overfit/
hardening/V2/CH2.md` §5 V3 fold recommendation; V1 CHALLENGE-CONTEXT
`restart/skinny/tranches/sk-v14/audit-overfit/hardening/V1/CHALLENGE-
CONTEXT.md §3` per-lens focus row; V3 micro-redispatch commit
`007624849`.
