---
agent: 3E
pass: T-P3-synthesis
cycle: V3
generated_at: 2026-05-29T22:10:00Z
subject: tape-fold grammar-generalisation (the tape / lazy ValueRef<G> / shared NEON select_classifier generality across JSON / CSS L4 / Sheets / BBNF-self, Lock 14)
t_p1_inventories_consumed: [1A, 1B, 1C, 1D, 1E, 1F]
t_p2_dossiers_consumed: [2A, 2B, 2C, 2D, 2E, 2F]
v1_surface_targeted: ARCHITECTURE.md (§7.3 Grammar-Generality BackendShape Matrix, §7.4 leak census, §12 Future Grammar Onboarding Test) + LOCKS.md (Lock 14, Lock 16 manifest, Lock 10 5-shape canon) — READ-ONLY; 3E proposes diffs, Pass Omega applies
master_head: 2a76916ac
t_p1_source_sha: 445925167
proposed_deltas_count: 9
delta_summary:
  carried_from_prior_cycle:
    - 3E17-D01-valueref-g-type-param-generality-vehicle
    - 3E17-D02-tape-substrate-grammar-column-free-fence
    - 3E17-D03-openframe-retirement-lock14-obligation
    - 3E17-D04-select-classifier-alphabet-as-data-manifest
    - 3E17-D05-per-grammar-backendshape-matrix-substrate-category
    - 3E17-D06-css-nonjson-consumer-eq-set-fan-binding
    - 3E17-D07-by-construction-not-by-exercise-scoping
    - 3E17-D08-future-grammar-onboarding-tape-predicates
    - 3E17-D09-fieldsource-compile-time-projection-grammar-neutral
  removed: []
  answered: []
  newly_added: []                       # cycles V2/V3 fold CH REVISEs only; no new deltas
prior_cycle_dispositions_folded:
  accepted:
    - CH2-V1-S1-3A-D02-valueref-value-plane            # ACCEPT (JSON byte-equal firewall) — reinforced, no change
    - CH2-V1-S8-3E17-D04-D06-alphabet-as-data          # ACCEPT (strongest generality claim) — no change
    - CH2-V2-S9-3E-matrix-cell-provenance              # V2 ACCEPT: predicted-cell tags + cell-level boundary crisp — carried unchanged
    - CH2-V2-S10-3E-P5a-P5b-split                       # V2 ACCEPT: classifier-scan/tape-consumer conflation severed — carried unchanged
    - CH2-V2-S11-3E17-D08-P6-value-axis-firewall        # V2 ACCEPT: two-axis onboarding test — carried unchanged
    - CH6-V2-07-3e-math-ebnf-defer-triple               # V2 ACCEPT: math/EBNF cell carries receiver/blocker/gate, zero unqualified DEFER
  rejected: []
  revised:
    - CH2-V1-S9-3E-matrix-sheets-bbnf-self-predicted-cells   # V1: matrix Sheets/BBNF-self/EBNF/BNF/CSV/math shape cells tagged 'predicted (cost-model-pending)' (folded V2)
    - CH2-V1-S10-3E-P5-classifier-scan-vs-tape-consumer      # V1: P5 split into P5a classifier-scan-measured / P5b tape-consumer-SK-V18-pending (folded V2)
    - CH2-V1-S11-3E17-D08-value-axis-firewall-P6             # V1: value-plane firewall added as onboarding predicate P6 (folded V2)
    - CH4-V1-03-3e-per-delta-cost-matrix                     # V1: 9-row CH4 cost matrix added mirroring 3A/3D (folded V2)
    - CH6-V1-08-3e-ebnf-bnf-csv-math-defer-triple            # V1: EBNF/BNF/CSV/math matrix cell qualified with receiver/blocker/gate triple under D07 (folded V2)
    - CH5-V1-R04-3E17-D08-leak-census-receiver-unnamed       # V3: orphan from V1 (CH5-V2-R02) — P1 leak-census 7 strategy.rs sites now route to MP.SK18.W3 (ValueRef<G> generator) AND carry an admitted catalogued non-zero Lock-14 baseline with a re-entry trigger
---

## Executive Summary

The SK-V17 tape-fold is grammar-neutral **by construction, not by assertion**.
The vehicle is proven and load-bearing: the grammar enters the value plane as a
TYPE parameter — `ValueRef<'doc,'input,K,G:EventGrammar>`
(`skinny/crates/runtime/src/tape/mod.rs:175`) — monomorphised at codegen, with
ZERO runtime `match grammar {…}` arm; and the NEON leaf's only grammar datum is
`alphabet: &[u8;64]` passed to `select_classifier`
(`restart/skinny/tranches/sk-v17/SPEC.md:314-315`). Both are Lock-14 vehicles:
the substrate carries zero grammar-specific code, per-grammar surfaces are
`@generated` (`LOCKS.md:349`-`358`).

This packet proposes nine proposed-deltas crystallising the five LOCKED T-P2 fold
designs into the V1 grammar-generality surface. It carries **no JSON narrowing,
no CSS-narrowing, no new directive, no new BIR variant, no new substrate, no
sixth `BackendShape`**; the five-shape canon holds verbatim
(`LOCKS.md:107-108`). Four refutations bind hardest and are carried as proposed
fences, not folds: (1) the tape is a substrate-manifest CATEGORY, not a 6th
shape (LAC-1E-14 + `admits_collapsed_stage` x86-binding,
`ARCHITECTURE.md:1206`,`:1796`); (2) grammar-neutrality is proven **by-exercise
on JSON+CSS only** — Sheets/BBNF-self are SK-V18 by-construction
(`SK17L-009`); (3) the `FieldSource` walk is grammar-neutral ONLY as
compile-time projection-emission (`SK17L-004` fence); (4) the eager `OpenFrame`
is the Lock-14-NAMED overfit whose retirement is a Lock 14 obligation
(`LOCKS.md:349`). The non-CSS generality story is concrete for Sheets formulas
and BBNF-self directives — their `BackendShape` matrix cells carried as
*predicted (cost-model-pending)* until `derive_backend_shape` wires to them — and
the future-grammar onboarding test is re-stated as a tape-aware verify_action
with a live HEAD baseline, carrying TWO orthogonal generality axes: the
classifier/leak axis (P1/P3, with CSS classifier-scan split from the SK-V18
tape-consumer at P5a/P5b) and the value-plane axis (P6: one `ValueRef<G>`
generator re-emits JSON byte-equal AND CSS lazy, a generator value-branch FAILS).
The P1 leak-census's 7 `strategy.rs` string-ident sites are NOT routed under an
unowned "HEAD → 0": their decrease is OWNED by **MP.SK18.W3** (the `ValueRef<G>`
projection generator wave, `3b:147`, whose codegen subsumes the BackendShape
strategy registry keys), with the monotonic-decrease tied to W3's close gate —
and, fail-closed, an admitted catalogued non-zero Lock-14 baseline (doc-comment
+ string-ident, not a `match grammar {}` arm) with a re-entry trigger.

## V3 Delta Summary

Cycle V3 folds the single remaining 3E-directed REVISE — **CH5-V2-R02**, the
orphan re-raise of CH5-V1-R04 (the P1 grammar-name leak-census routed the 7
`strategy.rs` sites under an unowned "monotonic-decrease-to-zero (HEAD → 0)"
with no named SK-V18 receiving wave). V3 binds the leak-census receiver to
**MP.SK18.W3** (the Lazy `ValueRef<G>` projection generator wave,
`3b-master-plan-reconciliation.md:147`) AND, fail-closed, declares the 7 sites
an admitted catalogued non-zero Lock-14 baseline with a re-entry trigger — the
string-ident registry + doc-comment leak is NOT a runtime `match grammar {}`
arm (`2c:279`-`281`). The V2 ACCEPTs (predicted-cell tags, P5a/P5b split, P6
value-axis firewall, math/EBNF defer triple) carry forward unchanged. All nine
deltas carry forward in substance; no delta is overturned; no new delta is
added.

| class | delta ids | status |
|---|---|---|
| carried | 3E17-D01..3E17-D09 | all nine carried from V2, substance unchanged; the prior SK-V14/V15 3E (`restart/audit/totality/p3/3E-grammar-generalisation.md`) remains the structure precedent only, not carried (different fold subject). |
| removed | none | — |
| answered | none | — |
| newly added | none | V3 is a fold cycle; the orphan CH5-V1-R04 (re-raised as CH5-V2-R02) is addressed in-place under 3E17-D08 and the P1 predicate row — receiver named (MP.SK18.W3) + admitted Lock-14 baseline + re-entry trigger; no fresh delta. |

## Proposed Delta Table

Every delta proposes a diff against a V1 surface section (READ-ONLY; Pass Omega
CRUD applies post-G-Omega). Each cites a T-P1 divergence-id or T-P2 grounding.
Lock surface noted; the Lock 14 / Lock 16 / Lock 10 dispositions feed 3C.

| proposed delta | source T-P1/T-P2 finding-id cited | affected V1-surface section | rationale |
|---|---|---|---|
| **3E17-D01-valueref-g-type-param-generality-vehicle**: name the grammar-parametric `ValueRef<'doc,'input,K,G:EventGrammar>` as the V1 spec's declared grammar-generality vehicle for the value plane — the grammar enters as a TYPE parameter monomorphised at codegen, never a runtime branch. | 2C Candidate-C + Defended-#1 (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:147`,`:306`); LAC-2F-FOLD-03 (`HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:110`); T-P1 divergence C (`SK17L-002`, `restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md:89`). | `ARCHITECTURE.md:1208`-`1234` Grammar-Generality BackendShape Matrix preamble + `LOCKS.md:349` Lock 14 generalisation vehicle. | The current matrix frames generality via per-grammar shape rows but does not name the type-param carriage as the mechanism. The fold makes the `G:EventGrammar` carriage the stated vehicle: one projection, per-grammar resolved at the type boundary, `@generated` per grammar (Lock-14 ALLOWED, `LOCKS.md:352-358`). preserve-rich-ast: the lazy view IS the rich-AST plane (`SPEC.md:172`,`:558`), not a span flatten. Lock 14. |
| **3E17-D02-tape-substrate-grammar-column-free-fence**: state the surviving one-encoding tape carries NO per-grammar dense class column — flags are sparse position-keyed side-vectors (`offsets:Vec<u32>` + `flag_cursors`/`flag_values` + `PayloadArena`); `begin_compound` reads `layout.rule_id & 0x1F` only. | 2C Candidate-A (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:103`-`123`); LAC-2F-FOLD-01 (`HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:85`); T-P1 divergence A (`SK17L-001`). | `LOCKS.md:75` Lock 1 substrate-union + `LOCKS.md:349` Lock 14 (zero grammar-specific substrate code). | The grammar-neutrality of the substrate is a FENCE, not just a property: a kind-partitioned dense class column would be the AV.04/REDRESS-96 barred shape (`LOCKS.md:129-135`,`:784`). The fold keeps the surviving encoding grammar-column-free; the choice AoS↔SoA is a Lock-1 one-encoding question (data/perf), not a grammar-neutrality one. Lock 1 + Lock 14. |
| **3E17-D03-openframe-retirement-lock14-obligation**: record the eager `OpenFrame` retirement as a Lock 14 OBLIGATION (not merely a perf win) — Lock 14 NAMES "CSS L4 14-variant `OpenFrame`" as the overfit it prevents; the 7-field `pending_*` builder is grammar-specific substrate state. | 2C Candidate-B + Defended-#2 (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:125`-`145`,`:312`); LAC-2F-FOLD-01/F1 (`HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:90`); T-P1 divergence B (`SK17L-003`). | `LOCKS.md:349` Lock 14 (the named overfit) + `LOCKS.md:75` Lock 1 (no `Vec<OpenFrame>::clone`). | `css_l4/builder.rs:16` (817 LOC god-module, 6 `pending_*` Vecs + `pending_value:Option` `:71-79`) is BOTH the AZ-IV 118× eager-tree pre-block AND the Lock-14-named overfit. Retiring it for the generic tape builder (`push_plain_offset`, "carries no grammar policy", `SK17L-001`) discharges the lock's purpose. **Fence**: no-delete-before-same-wave-replacement (the tape consumer proven first). Lock 14 + Lock 1 + AZ-IV pre-block. |
| **3E17-D04-select-classifier-alphabet-as-data-manifest**: fold the V1 NEON narrative from JSON-first to the alphabet-parametrised `select_classifier(alphabet:&[u8;64])` / `scan_structural(&StructuralAlphabet)` shared classifier, and register the eq-set fan `byte_class_from_eq_set_64` as a Lock-16 primitive-manifest row with the alphabet as grammar-policy-source DATA. | 2C Candidate-E + LAC-SK17-2C-02 (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:201`-`237`,`:384`); LAC-2F-FOLD-03/F5 (`HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:132`); T-P1 divergence E (`SK17L-008`). | `ARCHITECTURE.md:1255`-`1273` Primitive Vocabulary Transfer + `LOCKS.md:453`-`489` Lock 16 manifest + `LOCKS.md:392`-`400` Lock 14 alphabet-as-data. | The classifier scan-leaf is already wired across 8 core grammars (`SK17L-008`, impl-exceeds-spec) — config-breadth. The alphabet is the ONLY grammar datum (`SPEC.md:314-315`). The fold is a 0-LOC narrative fold + a manifest row. **CSS L4 is the non-JSON same-wave consumer that binds to a REAL NEON body** — the `;{` pair collides on the `& 0x3f` slot-59 table slot, routing to the eq-set fan (`SPEC.md:316-317`; `dispatch.rs:101-113`), the one proven aarch64 Layer-1 body (`aarch64/byte_class_from_eq_set_64.rs:33`), with scalar ref + checkasm. The `byte_class_from_table_64` aarch64 entry is a scalar-delegate-non-ASM close-state. Lock 16 + Lock 14. |
| **3E17-D05-per-grammar-backendshape-matrix-substrate-category**: rebase the §7.3 Grammar-Generality BackendShape Matrix so the tape is the substrate the 5 shapes PROJECT (a substrate-manifest category, NOT a 6th shape); the five-shape canon `{EagerTape,OffsetTape,EventTape,SinkOnly,CollapsedStage}` holds verbatim and CollapsedStage stays aarch64-refused. | 2C Candidate-D + LAC-SK17-2C-01 (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:172`-`199`,`:383`); LAC-2F-FOLD-02/F4 (`HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:162`); T-P1 divergence D (`SK17L-006`/`SK17L-007`). | `ARCHITECTURE.md:1088` five-shape canon + `ARCHITECTURE.md:1208`-`1234` matrix + `ARCHITECTURE.md:1799`-`1803` output-plane table + `LOCKS.md:107-108` Lock 10. | The matrix rows already assign per-grammar shapes; the fold restates the SUBSTRATE relationship — the tape is recorded at the Lock 1 substrate manifest (`substrate_target = existing_tape`, `LOCKS.md:119-127`), the same place FactStream sits as the 5th category. Stands on TWO grounds: LAC-1E-14 precedent (`LOCKS.md:100-108`) AND `admits_collapsed_stage` mechanically refusing aarch64 (`ARCHITECTURE.md:1206`,`:1282`). **Fence**: any proposal reading as a 6th `BackendShape` is REJECT (G-Omega gated, `LOCKS.md:109`). Lock 10 + Lock 1 + Lock 14. |
| **3E17-D06-css-nonjson-consumer-eq-set-fan-binding**: make CSS L4 the named non-JSON same-wave primitive consumer in the §12 onboarding protocol step 6, binding specifically to the eq-set fan NEON body via the slot-59 collision, not the scalar-delegate table entry. | 2C Candidate-E grounding-table CSS row (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:85`,`:218`-`230`); LAC-SK17-2C-02 (`:384`); SK-V17 SPEC §0.1 row 9 (`SPEC.md:103-106`). | `ARCHITECTURE.md:2111`-`2114` onboarding step 6 (primitive same-wave non-JSON consumer) + `LOCKS.md:396`-`397` Lock 16 "≥1 non-JSON consumer". | The existing step 6 names "C3 + C4 worked examples"; the tape-fold supplies a REAL exercised non-JSON consumer. Lock 16's "≥1 non-JSON consumer" binds to the eq-set fan's NEON body (the `;{`→slot-59 route, `SPEC.md:316-317`), not the 2-LOC table passthrough (`aarch64/byte_class_from_table_64.rs:2-3`). This closes the onboarding step's primitive predicate with a measured CSS binding. Lock 16 + Lock 14. |
| **3E17-D07-by-construction-not-by-exercise-scoping**: add the explicit by-construction-vs-by-exercise scoping clause — the tape/`ValueRef<G>`/classifier generality is breadth-of-CONFIG proven by-exercise on JSON+CSS only; with only one of Sheets/BBNF-self witnessed, the claim is SCOPED to the witnessed grammars and may not use fleet-wide wording. | 2C refuted-#3 + ONBOARD fence (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:353`,`:300`-`302`); `SK17L-009` (`restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md:96`); SK-V17 SPEC §0.1.11 (`SPEC.md:112-113`). | `LOCKS.md:382`-`387` Lock 14 per-wave gate (fleet-wide scoping rule) + `ARCHITECTURE.md:2124` onboarding closing clause. | `sheets_witness` is a 24-LOC `EventGrammar` stub with NO `.bbnf`/`BackendRule` (`SK17L-009`) — it CANNOT serve as a projection exercise. The fold's generality is breadth-of-config (alphabet/layout-as-data), value-plane-exercised JSON+CSS only. **The clause prevents the fold's grammar-neutrality from being mis-stated as fleet-proven.** The existing `LOCKS.md:382-387` already carries the scoping rule; the fold binds it to the tape value-plane specifically. Lock 14. |
| **3E17-D08-future-grammar-onboarding-tape-predicates**: extend the §12 7-step onboarding protocol with the tape-aware predicates — adding a grammar must wire `Tape`/`ValueRef<G>`/`TapeBuilder` through generated `value_from_ref` with NO generic-crate runtime dispatch branch; the falsifier is the per-wave grammar-name + grammar-shape leak census whose **decrease is OWNED by MP.SK18.W3** (the Lazy `ValueRef<G>` projection generator wave, `3b-master-plan-reconciliation.md:147`), the monotonic-decrease target tied to that wave's close gate — and, fail-closed, the 7 `strategy.rs` sites are an admitted catalogued non-zero Lock-14 baseline (string-ident registry + doc-comment, NOT a runtime `match grammar {}` arm, `2c:279`-`281`) with a named re-entry trigger if W3 does not zero them in-cycle. | 2C Candidate-ONBOARD + live HEAD baseline (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:262`-`302`); `SK17L-008`/`SK17L-009`; LAC-2F-FOLD designs §3Z (`HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:286`-`301`); CH5-V2-R02 receiver-binding (`hardening/V2/CH5.md:157`-`188`). | `ARCHITECTURE.md:2076`-`2124` Future Grammar Onboarding Test + `ARCHITECTURE.md:1288`-`1301` leak census + `LOCKS.md:374`-`387` Lock 14 per-wave gate. | The §12 protocol is provider-manifest-framed (SK-V15); the fold adds the tape predicates: Predicate 1 (grammar-name leak in generic crates) = 7 string-ident/doc sites in `strategy.rs` at HEAD, the catalogued ARCH-3A-D09 leak surface; its decrease is OWNED by MP.SK18.W3 (the value-plane generator codegen subsumes the `strategy.rs` BackendShape registry string-idents), monotonic-decrease tied to W3's close gate, NOT an unowned "HEAD → 0" and NOT a clean pass; if W3 cannot zero them in-cycle they are an admitted catalogued non-zero Lock-14 baseline with the re-entry trigger = next SK-V18 onboarding-wave leak-census gate. Predicate 2 (per-grammar runtime dirs) = 9 `@generated` dirs + `tape/` substrate, clean. A fold that requires a generic-crate runtime dispatch branch to onboard a grammar FAILS Lock 14 regardless of perf. Lock 14. |
| **3E17-D09-fieldsource-compile-time-projection-grammar-neutral**: state the `FieldSource{TypedLeaf,BranchTag,SeqPosition,RepeatElement,RuleReference}` walk that produces the `ValueRef<G>` projection is grammar-neutral ONLY as compile-time projection-emission resolved once at codegen — a per-leaf runtime `StructRegistry::layout` walk is BOTH the refuted 28-65×/983×/10583× indirection AND a grammar-shaped runtime dispatch. | 2C Candidate-F + refuted-#2 (`restart/audit/totality/sk-v17/p2/2c-grammar-neutrality.md:239`-`260`,`:347`); LAC-2F-FOLD-04/F6 (`HARDENING-T-P2-SKV17-V3-CONSOLIDATED.md:193`); T-P1 divergence F (`SK17L-004`). | `LOCKS.md:349` Lock 14 (grammar-as-data layout walk) + `LOCKS.md:75` Lock 1 (no second substrate) + `ARCHITECTURE.md:1271` BackendShape resolver row. | The `FieldSource` walk (`crates/ir/src/registry/struct.rs:84`) IS the `BackendRule`-walk recipe (grammar-neutral, layout-as-data) — but a per-leaf RUNTIME walk through the live `StructRegistry` BTreeMap re-opens the worst measured regression (`SPEC.md:793-795`) AND re-introduces per-grammar policy in the hot path. Compile-time emission keeps BOTH the perf AND the grammar-neutrality. `begin_compound` already takes a pre-resolved `&StructLayout` (grep-zero `StructRegistry`, `tape/mod.rs:185-186`). **Fence**: any per-leaf registry lookup in the projection hot path is REJECT. Lock 14 + Lock 1. |

## Per-Grammar BackendShape Matrix (the tape-fold rebase)

The five-shape canon `{EagerTape, OffsetTape, EventTape, SinkOnly,
CollapsedStage}` holds verbatim (`LOCKS.md:107-108`). The tape is the
grammar-neutral **substrate** the shapes project; per-rule shape selection is
cost-model-derived from Grammar IR (data, not a grammar branch). The matrix
restates the SK-V15 §7.3 rows under the SK-V17 tape model — shape assignments
unchanged; the substrate relationship made explicit. JSON+CSS are
by-exercise-proven; Sheets/BBNF-self are by-construction (SK-V18).

**Cell-level provenance discipline.** The dominant/secondary-shape cells for the
JSON and CSS rows are EXERCISED assignments (the `derive_backend_shape` selector
+ tape are wired and benched for those grammars). The Sheets/BBNF-self/EBNF/BNF/
CSV/math shape cells are tagged *predicted (cost-model-pending)* — the
`derive_backend_shape` selector is skinny-resident and unwired to those grammars
(`SK17L-008`; selector + `scan_structural` both skinny-only), so their concrete
shapes are the cost-model's PREDICTED output, not a proven assignment. The
by-construction boundary is therefore crisp at the CELL level, not only in the
status column: a reader scanning the dominant-shape column reads `predicted` on
every non-witnessed row and proven shapes only on JSON+CSS.

| grammar family / rule class | dominant shape | secondary / blocked | tape substrate relation | generality status | evidence |
|---|---|---|---|---|---|
| JSON `value`/`object`/`array`/`string`/`number` | `OffsetTape` | — | lazy-offset tape, `value_from_ref` lazy `ValueRef<G>` projection; the only tape WITNESS today | **by-exercise (proven)** — JSON rides it >SOTA | `skinny/.../json/value.rs:143`; `RESULTS.md:5-55`; `SK17L-001` |
| CSS `tokens`/`stylesheet`/`keyframes`/`media` | `OffsetTape` | `EventTape` (at-rule/recovery/layout payloads) | tape activated (SK-V17 first-mover); `select_classifier` structural scan → tape; typed CSSOM via lazy `ValueRef` projection | **by-exercise (SK-V17 first-mover)** | `SPEC.md:46-53`,`:73-83`; `ARCHITECTURE.md:1219`,`:1232-1233`; `2c:85` |
| CSS `selectors` (pseudo/attribute/combinator) | `EagerTape` | `EventTape` (pseudo payloads/recovery) | tape projects selector FIRST/follow + combinator facts; eq-set fan classifier (slot-59 route) | **by-exercise (SK-V17)** | `ARCHITECTURE.md:1221`; `grammar/css/l4/selectors.bbnf:12`,`:106` |
| CSS `values`/`color`/`gradients`/`transforms`/`filters`/`func-body` | `EagerTape` | `EventTape` (payload/recovery retention) | lazy `ValueRef` view preserves rich CSSOM (`CssColor`/`CssDimension`/`CssFunction`), NOT eager-tree (`OpenFrame` retired) | **by-exercise (SK-V17)** | `ARCHITECTURE.md:1223`,`:1226`-`1231`; `SPEC.md:78-83`; `2c:359` |
| Sheets `formula`/`cell_ref`/`range_ref` | `OffsetTape` *(predicted, cost-model-pending)* | `EventTape` (function/array payloads) *(predicted)* | tape projects reference/range/operator facts via `ValueRef<G>`; alphabet (separators, operators, doubled-`""` quote) is the classifier datum | **by-construction (SK-V18 proof)** | `ARCHITECTURE.md:1243-1245`; `grammar/google-sheets/google-sheets.bbnf:54`,`:84`,`:97`,`:160`; `SK17L-009` |
| Sheets `LET`/`LAMBDA`/array-literal, function calls | `EventTape` *(predicted, cost-model-pending)* | — | function-name DFA/payload facts projected through tape; no JSON object/array role reuse | **by-construction (SK-V18 proof)** | `ARCHITECTURE.md:1244`; `2c:72` (anti-JSON falsifier) |
| Sheets infix expression | `EagerTape` (Pratt, auto-detected) *(predicted, cost-model-pending)* | — | operator precedence facts; Pratt eligibility mined from grammar shape (Lock 10, no `@pratt`) | **by-construction (SK-V18 proof)** | `ARCHITECTURE.md:1245`; `LOCKS.md:269` |
| BBNF-self `grammar`/`declaration`/`term` dispatch | `OffsetTape` *(predicted, cost-model-pending)* | `EventTape` *(predicted)* | tape projects directive starts + identifier/literal/alternation facts; recursive grammar-source ownership through the same generator | **by-construction (SK-V18 proof)** | `ARCHITECTURE.md:1246`; `grammar/bbnf/bbnf.bbnf:4`,`:15`,`:41`,`:85`; `2c:73` |
| BBNF-self directives + generated grammar facts | `EventTape` + `SinkOnly` *(predicted, cost-model-pending)* | — | directive-kind enum + argument-schema facts reach `LayoutFacts` consumers; no generic `Bbnf` branch | **by-construction (SK-V18 proof)** | `ARCHITECTURE.md:1248`; `2c:73` |
| BBNF-self expression / operator chain | `EagerTape` (Pratt) *(predicted, cost-model-pending)* | — | precedence/associativity facts; `@pratt` forbidden (auto-detected) | **by-construction (SK-V18 proof)** | `ARCHITECTURE.md:1247`; `LOCKS.md:269` |
| EBNF / BNF / CSV / math | `OffsetTape` (EBNF/BNF/CSV), `EagerTape` Pratt (math) *(predicted, cost-model-pending)* | — | classifier scan-leaf wired (8-of-9 generated grammars; math has no `structural_index` field) | **by-construction (DEFERRED per 2C V4 grammar-selection; receiver = SK-V18 onboarding wave; blocker = no `structural_index`/scan witness for math; gate = Lock-14 future-grammar onboarding test, 3E17-D07)** | `ARCHITECTURE.md:1250-1252`; `SK17L-008` (math.rs `scan_structural`=0) |
| Any current SK-V17 grammar on Apple M5 Max / aarch64 | no `CollapsedStage` admission | `CollapsedStage` x86-only (mechanically refused) | `admits_collapsed_stage` binds x86+avx512bw; aarch64-NEON union sits under the 4 LLVM shapes' scan-leaf FFI; no 6th-shape pressure | UNKNOWN-2D-05 (no aarch64 strategy) | `ARCHITECTURE.md:1206`,`:1282`; `2c:191`-`193`; `SPEC.md:826` |

## Primitive Vocabulary Transfer (alphabet-as-data, the tape-fold view)

The shared classifier transfers across grammars because the alphabet is DATA, not
a JSON constant. Each primitive row needs scalar-ref + checkasm + hardware gate +
same-wave consumer + row movement (Lock 16, `LOCKS.md:495-505`). The CSS column
is the proven non-JSON consumer; Sheets/BBNF-self are by-construction.

| primitive family | CSS L4 transfer (proven non-JSON) | Sheets transfer (by-construction) | BBNF-self transfer (by-construction) | hard gate |
|---|---|---|---|---|
| Byte-set classify (`byte_class_from_eq_set_64`, the real NEON body) | delimiters, comments, identifiers, at-rule/hash/function starts; CSS `;{` → slot-59 eq-set fan route (real NEON, `SPEC.md:316-317`) | separators, operators, references, doubled-`""` quote policy | punctuation, directive starts, identifiers | Alphabet + quote/comment policy from generated grammar data, not JSON structural bytes (`LOCKS.md:392-397`); CSS binds the eq-set fan NEON body, not the table delegate. |
| Byte-range classify (`byte_class_from_range_64`, LAC-2F-V5-03 sibling) | CSS hex `[0-9a-fA-F]`, identifier ranges | numeric-literal ranges | identifier ranges | Sibling of the admitted eq-set primitive; the set≤8-vs-range split is the grammar-neutral generalisation vehicle (`LOCKS.md:426-434`). |
| String / escape scan | CSS strings, URLs, escaped identifiers (variable-width `\HEXHEX`) | doubled-`""` policy (no backslash) | shape-identical `\\.` + `\u`+4-nibble | Generated quote/escape/control/terminator policy; CSS variable-width is shape-orthogonal per 2C V4 §C4. |
| Direct/fact sink | CSS declaration/selector/stylesheet/value facts | formula/function/reference facts | grammar/directive facts | Sink callbacks + fact schema generated per grammar; `JsonSink` is NOT a generic contract (`ARCHITECTURE.md:1269`). |
| BackendShape resolver (`FieldSource` walk, compile-time) | selector/value/declaration shape facts | formula/reference shape facts | rule/expression/directive shape facts | Resolver consumes generated FIRST/follow + layout + host + recovery facts; the `FieldSource` walk is compile-time projection-emission, never per-leaf runtime (3E17-D09 fence). |
| Cross-chunk byte-context (`vextq_u8`) | CSS escaped-identifier boundary | Sheets quoted-sheet-name boundary | BBNF-self literal boundary | Lock 16 abstract-primitive; same-wave consumer + measured row movement; no cross-call retained state (`LOCKS.md:137-149`). |
| SIMD/ASM (NEON, aarch64-only) | CSS scan-block / value-row consumers after tape activation | numeric/reference consumers after Sheets receiver exists | token/literal consumers after BBNF-self receiver exists | scalar-ref + strict checkasm/parity + Apple-M5-Max/aarch64 gate + same-wave consumer + row movement; x86/AVX/SVE barred (`SPEC.md:806`). |

## Future-Grammar Onboarding Test (tape-fold predicates, with live HEAD baseline)

The §12 7-step onboarding protocol (`ARCHITECTURE.md:2081-2117`) proves Lock 14.
The tape-fold adds tape-aware predicates and re-states the falsifier as a
verify_action with a published HEAD baseline + monotonic-decrease rule — NOT a
present-tense clean gate.

| predicate | tape-fold requirement | live HEAD baseline (re-executed at `2a76916ac`) | disposition |
|---|---|---|---|
| P1 — grammar-name leak in generic crates | `rg 'JsonParser\|CssL4Parser' crates/ir/src crates/simd-scan/src` returns ZERO | **7 hits, all `crates/ir/src/registry/strategy.rs` (`:132,:137,:149,:197-198,:292,:315`)** — string-ident registry + doc-comment examples, the catalogued ARCH-3A-D09 leak surface (`ARCHITECTURE.md:1289-1296`), NOT runtime `match grammar {}` arms | **monotonic-decrease OWNED by MP.SK18.W3** (`3b-master-plan-reconciliation.md:147`, the `ValueRef<G>` generator wave — its codegen subsumes the `strategy.rs` BackendShape registry string-idents), the decrease target tied to W3's close gate; fail-closed, the 7 sites are an admitted catalogued non-zero Lock-14 baseline (doc-comment + string-ident, not a `match grammar` arm, `2c:279`-`281`) with re-entry trigger = next SK-V18 onboarding-wave leak-census gate; NOT a clean pass, NOT an unowned "HEAD → 0" |
| P2 — per-grammar dirs in generic runtime root | `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d` returns ZERO hand-written; all `@generated` | **9 grammar dirs** (bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json, math) — all carry `// @generated by xtask regen-<grammar>` — plus `tape/` (the shared substrate, grammar-neutral) | clean (Lock-14 ALLOWED; generated output) (`2c:282`-`288`) |
| P3 — tape substrate wiring (NEW) | adding a grammar wires `Tape`/`ValueRef<G>`/`TapeBuilder` through generated `value_from_ref`; NO new cursor/builder type; NO generic-crate runtime dispatch branch | `tape/` is the lone shared substrate dir; per-grammar `value.rs` is `@generated` (`SK17L-002`); JSON is the witness, CSS the first-mover | by-exercise JSON+CSS; by-construction Sheets/BBNF-self (`SK17L-009`) |
| P4 — five-shape eligibility | ≥1 rule per reachable `BackendShape` OR generated reason a shape is unreachable; NO 6th shape | five-shape canon holds; tape is the substrate category, not a shape (`ARCHITECTURE.md:1796`) | fail-closed if onboarding needs a 6th shape, new directive, BIR variant, or public substrate API |
| P5a — CSS classifier scan (wired/measured) | exercise ≥1 Layer-1 primitive via same-wave consumer | CSS L4 **classifier scan** is wired + measurable: the `;{` slot-59 collision routes to the eq-set fan NEON body (3E17-D06); 8-grammar `scan_structural` census (`SK17L-008`) | Lock 16 "≥1 non-JSON consumer" SATISFIED by the classifier scan (`LOCKS.md:396-397`) — this is the wired/measured axis |
| P5b — CSS tape consumer (SK-V18 fold-target) | the full tape-consumer chain (classifier → tape → `ValueRef<G>` projection) is exercised for CSS | CSS L4 **tape consumer** is the SK-V18 fold-target, NOT yet measured — `SK17L-008` states "the residual gap is the missing TAPE CONSUMER, not the scan" | tape-consumer-SK-V18-pending; receiver = SK-V18 fold wave; the classifier-scan measurement (P5a) does NOT imply the tape-consumer chain is measured |
| P6 — value-plane generality firewall (NEW) | the single `ValueRef<G>` generator re-emits JSON byte-equal AND CSS lazy from ONE `BackendRule` walk; a generator with a `match grammar` arm or a CSS-specific value branch FAILS | one `value_from_ref` accessor generator, monomorphised per grammar at codegen, zero runtime `match grammar` (`tape/mod.rs:175`); JSON byte-equal re-emit is the firewall (`SPEC.md:62`,`:557`) | a new grammar that requires a generator branch (`match grammar` arm or CSS-specific value path) to ride `ValueRef<G>` FAILS Lock 14 — this is the VALUE-AXIS falsifier, distinct from P1's classifier/leak axis |

**Fail-closed condition (preserved + extended)**: onboarding fails if it requires
a new directive, BIR variant, `BackendShape` (6th), public substrate API, retained
sidecar, hand-coded generic behavior, a generic-crate runtime dispatch grammar
branch to wire the tape, OR (P6, the value-axis firewall) a `ValueRef<G>`
generator branch — a `match grammar` arm or a CSS-specific value path — to ride
the value plane. The onboarding test thus carries TWO orthogonal generality axes:
the classifier/leak axis (P1/P3) and the value-plane axis (P6); a grammar that
passes the classifier axis but needs a generator value-branch still FAILS Lock 14.
With only one of Sheets or BBNF-self witnessed, the claim is SCOPED to the
witnessed grammars and may not use fleet-wide wording (`LOCKS.md:382-387`;
3E17-D07).

**P1 leak-census receiver (CH5-V2-R02 fold).** The P1 grammar-name-leak
predicate's monotonic-decrease is NOT an unowned "HEAD → 0". The decrease of
the 7 `crates/ir/src/registry/strategy.rs` string-ident sites
(`:132,:137,:149,:197-198,:292,:315`) is OWNED by **MP.SK18.W3**, the Lazy
`ValueRef<G>` projection generator wave (`3b-master-plan-reconciliation.md:147`):
that wave's codegen subsumes the `strategy.rs` BackendShape strategy registry —
the string-idents are the registry keys the generator resolves at codegen — so
the decrease target is tied to W3's close gate. **Fail-closed (admitted
baseline):** if MP.SK18.W3 cannot zero the 7 sites in-cycle, they are an
admitted catalogued non-zero Lock-14 baseline — they are doc-comment + string-
ident registry entries, NOT a runtime `match grammar {}` arm (`2c:279`-`281`),
so they are coupling-honest rather than coupling-active — with a named re-entry
trigger: the next SK-V18 onboarding-wave leak-census gate re-evaluates the
baseline and either continues the decrease or records the residual as
intrinsic. Either branch binds the coupling to an owner; the honest catalogue
is preserved, the unowned target is eliminated.

## Lock 14 Hardening Clauses For 3C

These feed 3C's Lock 14 / Lock 16 / Lock 10 disposition matrix. 3E provides the
Lock-14 receiver inputs only; 3C owns the line-level `LOCKS.md` diff and the
final ACCEPT/MODIFY/REJECT/DEFER disposition. 3C accepts NO CSS-narrowing.

| clause | proposed 3C disposition input | source LAC | evidence |
|---|---|---|---|
| L14-17-HC-01 type-param generality vehicle | ACCEPT: `ValueRef<…,G:EventGrammar>` is the named grammar-generality vehicle; grammar enters as a TYPE param monomorphised at codegen, zero runtime branch. | LAC-2F-FOLD-03 | `2c:147`,`:306`; `LOCKS.md:349`; `SPEC.md:175` (via `tape/mod.rs:175`) |
| L14-17-HC-02 grammar-column-free substrate fence | ACCEPT: the surviving one-encoding tape carries no per-grammar dense class column; sparse position-keyed flags only; a kind-partitioned column is the barred AV.04 shape. | LAC-2F-FOLD-01 | `2c:114`-`123`; `LOCKS.md:75`,`:129-135`,`:784` |
| L14-17-HC-03 OpenFrame retirement obligation | ACCEPT: eager `OpenFrame` retirement is a Lock 14 obligation (the named overfit), not merely a perf win; no-delete-before-same-wave-replacement. | LAC-2F-FOLD-01/F1 | `2c:125`-`145`,`:312`; `LOCKS.md:349`; `css_l4/builder.rs:71-79` |
| L14-17-HC-04 alphabet-as-data classifier manifest | ACCEPT: register the eq-set fan `byte_class_from_eq_set_64` as a Lock-16 manifest row; alphabet is grammar-policy-source DATA; CSS L4 is the non-JSON consumer binding the real NEON body, not the table delegate. | LAC-SK17-2C-02 / LAC-2F-FOLD-03 | `2c:201`-`237`,`:384`; `LOCKS.md:332-339`,`:423-425`,`:495-505`; `SPEC.md:316-317` |
| L14-17-HC-05 substrate-manifest category (no 6th shape) | ACCEPT: catalogue the tape as a substrate-manifest category under the fixed 5-shape canon; a 6th `BackendShape` is REJECT (G-Omega gated); two grounds (LAC-1E-14 + `admits_collapsed_stage` x86-binding). | LAC-SK17-2C-01 / LAC-2F-FOLD-02 | `2c:172`-`199`,`:383`; `LOCKS.md:100-109`,`:107-108`; `ARCHITECTURE.md:1206`,`:1796` |
| L14-17-HC-06 compile-time FieldSource fence | ACCEPT: the `FieldSource` projection walk is compile-time emission resolved once; a per-leaf runtime `StructRegistry::layout` walk is REJECT (re-opens 28-65×/983×/10583× AND a grammar-shaped runtime dispatch). | LAC-2F-FOLD-04/F6 | `2c:239`-`260`,`:347`; `SPEC.md:793-795`; `tape/mod.rs:185-186` |
| L14-17-HC-07 by-construction scoping | ACCEPT/MODIFY: bind the existing fleet-wide scoping rule to the tape value-plane — generality is breadth-of-config, value-plane-exercised JSON+CSS only; Sheets/BBNF-self SK-V18; no fleet-wide wording without ≥1 non-JSON-non-CSS witness. | LAC-2F-FOLD designs §3Z | `2c:353`,`:300-302`; `LOCKS.md:382-387`; `SK17L-009` |

## Consequences

**Positive.** The fold gives the V1 grammar-generality surface a *proven* vehicle
(type-param `ValueRef<G>` + alphabet-as-data classifier) replacing the SK-V15
provider-manifest framing with the SK-V17 tape model — same Lock 14 discipline,
now anchored to a benched substrate. The `OpenFrame` retirement is reframed as a
lock obligation (the overfit Lock 14 was authored to prevent). Grammar-neutrality
becomes falsifiable by the onboarding predicates with a published HEAD baseline,
not asserted.

**Cost.** 3E owns 0-LOC of implementation (proposed-deltas only; docs/gate scope).
The implementation costs inherit the LOCKED T-P2 carriers (tape-as-substrate
200–700 LOC; `ValueRef<G>` projection generator 300–700 LOC; classifier 0-LOC
narrative + manifest row + 100–400 LOC scope reconcile; `OpenFrame` retirement
within the tape-as-substrate band) split by 3B/G-Omega. No 3E delta carries
implementation dispatch.

**Propagation.** 3E17-D05 (the substrate-manifest category) is the highest-
propagation delta — it touches `ARCHITECTURE.md` §7.3 + the output-plane table +
Lock 10, and MUST stay coherent with 3A's surface deltas and 3B's wave matrix
(§8.2 the 5-shape canon coherence). 3E17-D01/D02/D03/D09 touch Lock 14 + Lock 1
(2 surfaces each). 3E17-D04/D06 touch Lock 16 + Lock 14 (the manifest row). All
nine feed 3C's disposition matrix.

**Per-delta CH4 cost matrix.** 3E carries 0 implementation LOC (proposed-deltas +
the Lock-14 onboarding/matrix surface only); each row's impl band is the inherited
LOCKED T-P2 carrier already named in the prose, split by 3B/G-Omega. The matrix
mirrors 3A/3D's shape (doc LOC | propagation | risk | wave | consumer/gate | fail
action); it is a formatting completion, not new analysis.

| delta | doc LOC | propagation | risk | wave alignment | consumer / gate | fail action |
|---|---:|---:|---|---|---|---|
| `3E17-D01` ValueRef<G> type-param vehicle | 0 impl / 2-6 doc | 2 (ARCH §7.3 preamble + Lock 14) + LAC | Low (narrative) | T-P3 §3C / MP.SK18.W3 (the `ValueRef<G>` projection generator wave, `3b:147`; inherits ARCH-3A-S17-D02 300-700) | accessor generator; JSON `value_from_ref` byte-equal re-emit gate | A generator with a `match grammar` arm or CSS-specific value branch FAILS (P6 firewall); REJECT. |
| `3E17-D02` grammar-column-free substrate fence | 0 impl / 2-4 doc | 2 (Lock 1 + Lock 14) + LAC | Med (fence; AV.04 re-open if violated) | T-P3 §3C / SK-V18 W1 (inherits 200-700 tape band) | substrate-manifest fence; flags-stay-sparse property | A kind-partitioned dense class column re-opens AV.04/REDRESS-96; REJECT. |
| `3E17-D03` OpenFrame retirement obligation | 0 impl / 2-4 doc | 2 (Lock 14 + Lock 1) | High (eager retirement; AZ-IV pre-block) | SK-V18 W1 (within tape-as-substrate band) | no-delete-before-same-wave-replacement; tape consumer proven first | Deleting `css_l4/builder.rs` before the tape consumer lands re-opens AZ-IV 118× eager-tree; REJECT. |
| `3E17-D04` alphabet-as-data classifier manifest | 0 impl / 0 narrative + manifest row | 3 (ARCH §13/§7.3 + Lock 16 + Lock 14) + LAC | Med (scope reconcile, no x86 path) | SK-V18 W3 (inherits 100-400 reconcile) | Lock-16 manifest row; checkasm parity; tape same-wave consumer | Any x86/AVX/SVE close path is REJECT; binding the table delegate instead of the eq-set fan body FAILS the non-JSON-consumer predicate. |
| `3E17-D05` substrate-manifest category (no 6th shape) | 0 impl / 2-6 doc | 3 (ARCH §7.3 + output-plane table + Lock 10) + LAC — highest-propagation | Med (canon coherence; §8.2) | T-P3 §3C / Pass Omega CRUD | Lock 1 substrate-manifest + Lock 10 5-shape review; 5-shape coherence with 3A/3B | Any reading as a 6th `BackendShape` is G-Omega-gated; G-Omega blocks CRUD until canon coherent across 3A/3B/3E. |
| `3E17-D06` CSS non-JSON consumer (eq-set fan) | 0 impl / 2-4 doc | 2 (ARCH §12 step 6 + Lock 16) | Med (classifier-scan measured; tape-consumer SK-V18) | SK-V18 W3 (classifier) / W1-W2 (tape consumer) | eq-set fan NEON body via `;{` slot-59; Lock 16 ≥1 non-JSON consumer | Binding the scalar-delegate table entry (not the eq-set fan body) FAILS; conflating P5a classifier-scan with P5b tape-consumer is a CH2 over-claim. |
| `3E17-D07` by-construction scoping clause | 0 impl / 2-4 doc | 2 (Lock 14 per-wave gate + ARCH §12 closing) | Low (scoping rule) | T-P3 §3C / per SK-V18 wave | witnessed-grammars scoping rule; ≥1 non-JSON-non-CSS witness before fleet-wide | Fleet-wide Lock-14 wording without a Sheets/BBNF-self value-plane witness is REJECT; scope to JSON+CSS. |
| `3E17-D08` onboarding tape predicates (P1-P6) | 0 impl / 4-8 doc | 3 (ARCH §12 + leak census + Lock 14 per-wave gate) | Low (predicate set; HEAD baseline catalogued) | T-P3 §3C / **MP.SK18.W3 owns the P1 leak-census decrease** (`3b:147`), each SK-V18 onboarding wave owns its predicate gate | leak-census P1 (HEAD=7, decrease OWNED by MP.SK18.W3; fail-closed admitted Lock-14 baseline + re-entry trigger); P6 value-axis firewall | A generic-crate runtime dispatch branch (classifier axis) OR a `ValueRef<G>` generator branch (value axis) to onboard a grammar FAILS Lock 14. The 7 `strategy.rs` sites are NOT an unowned "HEAD → 0": MP.SK18.W3 owns the decrease OR they are an admitted catalogued non-zero baseline with re-entry trigger = next SK-V18 onboarding-wave leak-census gate. |
| `3E17-D09` compile-time FieldSource fence | 0 impl / 2-4 doc | 2 (Lock 14 + Lock 1) + LAC | High (regression; 28-65×/983×/10583×) | SK-V18 fold gate | compile-time projection-emission; `begin_compound` registry-free property | Any per-leaf runtime `StructRegistry::layout` walk in the projection hot path re-opens the measured regression; REJECT. |

## Open Questions

| lens | open question | receiver | blocker | gate |
|---|---|---|---|---|
| CH1 | Which exact line-level `LOCKS.md` Lock 14/16/10 amendments will 3C accept for LAC-SK17-2C-01/02 and the overlapping LAC-2F-FOLD-01..05? | 3C LOCKS-diff owner; 3E provides Lock 14/16 receiver inputs only. | 3E cannot edit live V1 spec surfaces or own the final hunk placement; 16-lock count must be preserved (no silent renumber). | 3C disposition matrix + `git apply --check` on `3c-locks-v+1-diff.md` + lock-count invariant. |
| CH2 | Does the `ValueRef<G>` generator re-emit ALL per-grammar value surfaces from the single `BackendRule`-walk, or do CSS-specific value semantics (colour functions, calc) require a generator branch? | OQ-SK17-2C-02 receiver (T-P3/SK-V18 generator design); 3E carries the grammar-neutral requirement only. | CSS per-grammar deviations must be encoded in grammar metadata + source, NOT a generator branch (`LOCKS.md:349`). | onboarding predicate **P6** (value-axis firewall) is the falsifier: re-emit JSON byte-equal AND CSS lazy from one walk; any `match grammar` arm or CSS-specific value branch FAILS (`SPEC.md:62`,`:557`). |
| CH2 | Should the SK-V18 W3 onboarding gate require a NON-CSS-non-JSON value-plane witness (Sheets OR BBNF-self riding `ValueRef<G>` without a generator branch) before any fleet-wide Lock 14 wording, or is the witnessed-grammars scoping rule (3E17-D07) sufficient indefinitely? | SK-V18 W3 owner (3B) + 3E; from CH2-V1 consolidated open question. | the scoping rule (3E17-D07) holds the claim to JSON+CSS until a non-CSS-non-JSON value-plane witness lands; P6 is the gate that witness must pass. | W3 byte-equal re-emit gate (P6) + the witnessed-grammars scoping rule; no fleet-wide wording without ≥1 non-JSON-non-CSS value-plane witness. |
| CH2 | If only one of Sheets or BBNF-self fits the SK-V18 cap, which is the first non-CSS witness — Sheets (function/reference/operator materialisation) or BBNF-self (import/directive recursion)? | SK-V18 wave owner (3B/G-Omega); 3E carries both as mandatory falsifiers. | SK-V18 cap may fit one non-CSS receiver; the other remains by-construction until its wave. | SK-V18 onboarding plan gate with source/metadata-only diff + Lock 14 scan; the witnessed-grammars scoping rule (3E17-D07). |
| CH3 | Does the surviving one-encoding tape remain grammar-column-free under the cost model's per-rule shape selection, or does a high-cardinality grammar pressure a kind-partitioned column (the barred AV.04 shape)? | OQ-SK17-2C-01 receiver (T-P3/SK-V18); 3E17-D02 carries the fence. | a dense per-grammar class column re-opens REDRESS-96 (`LOCKS.md:129-135`,`:784`). | confirm flags stay sparse position-keyed side-vectors (`tape/mod.rs:96-98`); the cost model selects the shape, not the substrate layout. |
| CH5 | Is the tape substrate-manifest category expressible without a new manifest field, or does it require a `substrate_target` value the V1 manifest lacks? | OQ-SK17-2C-03 receiver (T-P3); 3E17-D05 carries the categorisation. | a new manifest field would be a silent new directive (G-Omega gated). | map the tape against the four admitted `substrate_target` values (`LOCKS.md:118-127`); OffsetTape/EventTape already cover `existing_tape`; no new field. |
| CH5 | Does MP.SK18.W3's `ValueRef<G>` generator codegen fully zero the 7 `strategy.rs` grammar-name string-ident sites, or does a residual subset persist as the admitted catalogued non-zero Lock-14 baseline past W3's close gate? | MP.SK18.W3 owner (3B) + Lock-14 leak-census gate; 3E17-D08 carries the receiver-binding (CH5-V2-R02 fold). | the decrease is OWNED by W3 (its codegen subsumes the BackendShape strategy registry keys); a residual that survives W3 is admitted only as doc-comment + string-ident, NOT a runtime `match grammar {}` arm (`2c:279`-`281`). | W3 close gate re-runs `rg 'JsonParser\|CssL4Parser' crates/ir/src crates/simd-scan/src`; any residual is recorded as the admitted baseline with re-entry trigger = next SK-V18 onboarding-wave leak-census gate — never an unowned "HEAD → 0". |
| CH6 | If the SK-V18 Sheets/BBNF-self witness cannot land in its wave, what intrinsic-block evidence is sufficient without weakening the by-construction claim into fleet-wide wording? | SK-V18 receiver/onboarding owner. | a missing witness cannot be deferred by prose; it needs owner path, exact blocker, and the witnessed-grammars scoping rule applied (3E17-D07). | SK-V18 plan gate records the intrinsic-block evidence OR scopes the claim to JSON+CSS; never fleet-wide. |
