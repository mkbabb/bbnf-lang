# AY-II.W2 — CSS L4 Lightningcss Total Typed-Semantic Parity

**Opens after**: W1 close
**Agents**: 5 parallel (W2.a / W2.b / W2.c / W2.d / W2.e on disjoint file bounds)
**Hard gate**: Every lightningcss 1.0.0-alpha.71 AST surface admitted by the declared CSS L4 grammar projects through a grammar-derived `CssL4Parser<TypeCamel>Projection` + `materialize_projection_*` emitted from CSP-inferred facts with zero hand-authored `RustNamedTypes` entries; `lightningcss_parity.rs` + `css_l4_canonical_parity.rs` + `typed_accessor_surface.rs` green release-profile with zero `#[ignore]`; bbnf CSS parse ≤ cssparser and within 2× lightningcss; samply hot-path owner is the fused pipeline; fat-LTO CSS bench clean (no `columns.rs:409` panic).
**Status**: planned

## Scope

W0 landed the fused substrate + W0.d's grammar-derived typed CSS projection skeleton. W1 closed JSON peer gates. W2 closes CSS against lightningcss + cssparser on four axes: admission parity, byte-identical canonical emission, typed-accessor totality, competitor throughput.

The wave enumerates every lightningcss 1.0.0-alpha.71 AST surface, maps each to a grammar rule in `grammar/css/l4/**`, and drives it through the W0.d projection pipeline. Where the grammar routes a typed surface through the `genericAtRule` / `genericDecl` / `genericFunction` catch-alls, this wave (a) extends the `.bbnf` with a `-> TypeName` annotation, or (b) classifies the feature OUT-OF-SCOPE with `grep`-evidenced rationale. Grammar edits are the bounded exception to the tranche's `.bbnf`-frozen rule; the parent AY-II.md's W0.d/W2 admission is the sole carve-out.

### Lightningcss AST → grammar mapping

Every row is a wave-gate item. State column: **exists** = annotation only; **partial** = extension + annotation; **MISSING** = new rule + annotation; **OUT-OF-SCOPE** = stays under `genericAtRule`, `grep`-evidenced zero occurrences in declared fixtures.

| lightningcss surface | grammar rule (file:line) | state |
|---|---|---|
| `MediaRule`, `KeyframesRule`, `MediaQuery`, `MediaCondition`, `MediaFeature`, `MediaType`, `KeyframeSelector` | `stylesheet.bbnf:22/30`, `media.bbnf:36/32/21/12`, `stylesheet.bbnf:27-28` | **exists** |
| `StyleRule` (qualified), `DeclarationBlock` | `stylesheet.bbnf:19/15` | **exists** |
| `FontFaceRule`, `SupportsRule`, `ContainerRule`, `LayerStatementRule`, `LayerBlockRule`, `PageRule`, `ImportRule`, `NamespaceRule`, `PropertyRule` | all under `genericAtRule` @ `stylesheet.bbnf:34` | **MISSING** → new `grammar/css/l4/at-rules.bbnf` module |
| `CounterStyleRule`, `ScopeRule`, `StartingStyleRule`, `ViewTransitionRule`, `NestingRule`, `CustomMediaRule`, `FontFeatureValuesRule`, `FontPaletteValuesRule`, `MozDocumentRule`, `ViewportRule` | `genericAtRule` fallback | OUT-OF-SCOPE (zero matches in normalize + bootstrap + tailwind; token-level admission holds via fallback) |
| `Property` enum (~200 variants) with per-group `PropertyId` dispatch | 27 typed decls + `genericDecl` @ `properties.bbnf:161-223` | **partial** — each typed-decl group annotates `-> <Group>Property` with per-property u8 variant idx |
| `CustomProperty`, `Important` | `customPropertyDecl` @ `properties.bbnf:206`, `importantSuffix` @ `properties.bbnf:23` | **exists** |
| `CssColor::RGBA` / `LAB` / `Predefined` / `Float` | `colorFunction` @ `color.bbnf:220`, `colorFn` @ `color.bbnf:253`, `colorMix` @ `color.bbnf:299`, `hex` @ `color.bbnf:189`, `namedColor` @ `color.bbnf:36-185` | **exists** — `-> input : Color` on all three functional forms; LargeAggregate 40 B layout documented at `color.bbnf:205-215` |
| `CssColor::CurrentColor` | `namedColor "currentcolor"` @ `color.bbnf:56` | **exists** |
| `CssColor::LightDark`, `CssColor::System` | not in grammar | OUT-OF-SCOPE (zero matches in declared fixtures) |
| `CssColor::Float` with `none` components | `colorValue` @ `color.bbnf:32` | **partial** — W2.a adds `"none" -> NaN` alternative |
| `Length` (48-unit `LengthValue` + `Calc`) | `length` @ `value-unit.bbnf:62`, `calcFunction` @ `func-body.bbnf:43` | **exists** — 48 unit tags at `value-unit.bbnf:20-37` already match lightningcss ordering |
| `Angle` / `Time` / `Resolution` / `Frequency` / `Percentage` / `FlexValue` / `NumberOrPercentage` | `angle`, `time`, `resolution`, `frequency`, `percentage`, `flex` @ `value-unit.bbnf:63-68`; `colorValue` @ `color.bbnf:32` | **exists** |
| `Image`, `Gradient` family, `Url` | `urlFunction` @ `func-body.bbnf:55`, `gradients.bbnf` | **exists** |
| `CSSString`, `CustomIdent`, `DashedIdent` | `string` @ `func-body.bbnf:10`, `ident` @ `properties.bbnf:19`, `dashIdent` @ `properties.bbnf:38`, `dashed` @ `func-body.bbnf:49` | **exists** |
| `Selector`, `SelectorList`, `RelativeSelectorList`, `Combinator`, `AttrOperator`, `AttrSelector` | `complexSelector` / `selectorList` / `relativeSelectorList` / `combinator` / `attrMatcher` / `attrSelector` @ `selectors.bbnf:99/106/103/93/35/39` | **exists** — u8 tags match lightningcss |
| `PseudoClass` hot subset (~20 keywords) | `simplePseudoClass` long-tail fallback @ `selectors.bbnf:69` | **partial** — W2.a adds `identifiedPseudoClass` u8-tagged alt before fallback |
| `PseudoClass::Nth/Lang/Dir/Is/Where/Not/Has` | `nthPseudo` / `langPseudo` / `dirPseudo` / `isPseudo` / `wherePseudo` / `notPseudo` / `hasPseudo` @ `selectors.bbnf:54-68` | **exists** |
| `PseudoElement` hot subset (`::before`, `::after`, `::first-letter`, `::first-line`, `::placeholder`, `::marker`, `::backdrop`, `::selection`) | `simplePseudoElement` long-tail @ `selectors.bbnf:79` | **partial** — W2.a adds `identifiedPseudoElement` u8-tagged alt |
| `FontFamily`, `FontWeight`, `BackgroundPosition`, `TransformList`, `Filter`, `EasingFunction` | `fontDecl`, `bgDecl`, `transforms.bbnf`, `filters.bbnf`, `easing.bbnf` | **partial** — typed annotations per function |
| `MediaFeature` range form (`(width >= 768px)`) | `mediaFeature` @ `media.bbnf:21` | **partial** — range-form alternative added |

### Wave scope bullets

1. Audit enumeration: walk `~/.cargo/registry/src/index.crates.io-*/lightningcss-1.0.0-alpha.71/src/{rules,values,properties,selector,media_query}*.rs`; produce `docs/tranches/AY-II/waves/W2-lightningcss-audit.csv` with columns `lightningcss_type,lightningcss_path,grammar_rule,grammar_file_line,state`. Table above is authoring seed; CSV is committed ground truth.
2. Emit `CssL4Parser<TypeCamel>Projection` + `materialize_projection_*` for every EXISTS + PARTIAL row via the W0.d pipeline (IR `payload::layout` + Rust backend `named_types`). No hand-coded mapping table. No `RustNamedTypes` hardcoded entries for `Length` / `Angle` / `Time` / `Color` / `Selector`.
3. Close MISSING rows via new grammar rules under `grammar/css/l4/at-rules.bbnf` (new module per the @import-module discipline; no god module). Plumb into `atRule` alt before `genericAtRule`. Each new rule carries `-> TypeName` + an adjacent comment citing its lightningcss target + the fixture demonstrating it.
4. Close OUT-OF-SCOPE rows with `grep -c` evidence in the audit CSV; `genericAtRule` fallback admits token-for-token so admission parity holds; typed parity deferred to BA.
5. Retire `__named_type_shim_color`. AUDIT-B §4 measured 49 `PROJECTION_DIRECT_TO_STRUCT` entries, 48 materializers, 1 resolver shim. W2.b emits `CssL4ParserColorProjection` directly from `colorFunction`/`colorFn`/`colorMix`'s declared Seq kernel, closing the shim. Wave-close `grep` on CSS expand returns zero `__named_type_shim_*`.
6. Per-group `PropertyProjection` emission: each of the 27 typed-declaration groups emits a `CssL4Parser<Group>PropertyProjection { variant_idx: u8, value: arena_handle }`. Group dispatch lives in `declaration` Alt; per-property u8 tags already declared drive `variant_idx`. CSP facts flow via `ir.payload_layouts`.
7. Extend `css_l4_canonical_parity.rs` corpus. Add `data/css/lightningcss-corpus/*.css` (≥ 15 hand-authored 5–30-line fixtures per targeted feature); add `canonical_parity_<feature>` per fixture. Graduate bootstrap from `scale_interop_` to `canonical_parity_` as `token_normalize` rules land; tailwind stays on `scale_interop_` per the calc-evaluator gap.
8. Tighten `lightningcss_parity.rs`: extend `color_channel_parity_rgb_family` to `color_channel_parity_all_families` (RGB + LAB/LCH/OKLab/OKLCH + Predefined + Float + CurrentColor; drop pre-wiring empty-list guard); add `selector_parity`, `media_query_parity`, `property_parity` tests with inline fixtures. Zero `#[ignore]` admitted.
9. Tighten `typed_accessor_surface.rs`: `css_l4_accessor_surface` floor counts become exact post-W2 numbers (e.g. `r.aggregate >= 12`, `r.leaf_scalar >= 60`, `r.alt >= 25`; exact floors enshrined from post-W2 expand).
10. CSS competitor bench publishing. `crates/core/benches/css/competitors.rs` gains a `bench_bbnf_css` group (normalize + bootstrap + tailwind); one `cargo bench -p bbnf --bench competitors` captures all three parsers; ratios publish to `docs/benchmarks/post-AY-II-W2-competitors-css.txt`.
11. Samply on tailwind + bootstrap + normalize under `profiling-prep` interactive `samply record` (not `--save-only`, per symbol-resolution edict). Captures at `.profiles/samply/AY-II-W2/css_{fixture}/`. Hot-path owner is the fused pipeline + `ValueBuilder::push_aggregate`; zero self-time on `navigate_tape`, generic child-walk, or tape-reconstruction.
12. fat-LTO CSS bench clean. AUDIT-D §5 `columns.rs:409` panic on `css_l4/bootstrap` (and Sheets) is transitively repaired by W0.a's `rollback_to` + W0.b's retry-IIFE migration; W2.e re-runs the full CSS fat-LTO suite to verify. Panic recurrence triggers diagnostic-loop relinquish per AY-II.md §13; W2 does not close.

## File bounds

| File | Access | Owner |
|---|---|---|
| `grammar/css/l4/stylesheet.bbnf` | modify | W2.a |
| `grammar/css/l4/properties.bbnf` | modify | W2.a |
| `grammar/css/l4/color.bbnf` | modify | W2.a |
| `grammar/css/l4/selectors.bbnf` | modify | W2.a |
| `grammar/css/l4/media.bbnf` | modify | W2.a |
| `grammar/css/l4/value-unit.bbnf` | modify | W2.a |
| `grammar/css/l4/func-body.bbnf` | modify | W2.a |
| `grammar/css/l4/values.bbnf` | modify | W2.a |
| `grammar/css/l4/gradients.bbnf` | modify | W2.a |
| `grammar/css/l4/transforms.bbnf` | modify | W2.a |
| `grammar/css/l4/filters.bbnf` | modify | W2.a |
| `grammar/css/l4/easing.bbnf` | modify | W2.a |
| `grammar/css/l4/keywords.bbnf` | modify | W2.a |
| `grammar/css/l4/at-rules.bbnf` | create | W2.a |
| `crates/ir/src/passes/payload/layout.rs` | modify | W2.b |
| `crates/core/src/backend/rust/view/named_types.rs` | modify-carve | W2.b |
| `crates/core/src/backend/rust/emitter/shapes/value_materialize.rs` | modify | W2.b |
| `crates/core/tests/lightningcss_parity.rs` | modify | W2.d |
| `crates/core/tests/css_l4_canonical_parity.rs` | modify | W2.c |
| `crates/core/tests/typed_accessor_surface.rs` | modify | W2.d |
| `crates/core/tests/css_l4_parity.rs` | modify | W2.d |
| `crates/core/tests/css_l4_named_color_parity.rs` | modify | W2.d |
| `crates/core/tests/common/css_normalize.rs` | modify | W2.c |
| `data/css/lightningcss-corpus/` | create | W2.c |
| `crates/core/benches/css/competitors.rs` | modify | W2.e |
| `crates/core/benches/css/l4.rs` | modify | W2.e |
| `docs/tranches/AY-II/waves/W2-lightningcss-audit.csv` | create | W2.a |
| `docs/benchmarks/post-AY-II-W2-competitors-css.txt` | create | W2.e |
| `docs/benchmarks/post-AY-II-W2-nm.txt` | create | orchestrator |
| `docs/benchmarks/post-AY-II-W2-samply-summary.txt` | create | W2.e |
| `.profiles/samply/AY-II-W2/css_{tailwind,bootstrap,normalize}/` | populate | W2.e |
| `target/expand/ay-css-l4.rs` | regen | orchestrator |
| `crates/core/src/grammar/generated.rs` | regen | orchestrator |

**Do NOT touch**: `grammar/json/**`, `grammar/google-sheets/**`, `grammar/bbnf/**`, `grammar/ebnf/**`, `grammar/bnf/**`, `grammar/css/l2/**`, `grammar/css/pretty.bbnf`; `crates/tape/**` (W0.a); `crates/core/src/runtime/{value_builder,parsed,path}.rs` (W0.c); `crates/core/src/backend/rust/emitter/shapes/{object,array,wrap,keyword,inline,alt_dispatch,flat,pratt}.rs` (W0.b); JSON / Sheets / BBNF benches + tests; `docs/tranches/AY-I/**`; any crate outside `crates/{core,ir}/` unless a grammar IR extension requires (record in return report).

## Phase sub-items

### AY-II.W2.a — Grammar annotation audit + extension

Owner: CSS L4 grammar files.

Mechanism:

1. Produce `W2-lightningcss-audit.csv` by walking `~/.cargo/registry/src/index.crates.io-*/lightningcss-1.0.0-alpha.71/src/`. Inventory table in §Scope is authoring seed.
2. EXISTS rows: add `-> TypeName` annotation. Specific edits (non-exhaustive — CSV is ground truth):
   - `stylesheet.bbnf`: `qualifiedRule -> StyleRule`, `mediaRule -> MediaRule`, `keyframesRule -> KeyframesRule`, `keyframeSel -> KeyframeSelector`, `blockContent -> DeclarationBlock`.
   - `properties.bbnf`: 27 typed decls annotate with `-> <Group>Property`; `customPropertyDecl -> CustomProperty`; `importantSuffix -> Important`.
   - `color.bbnf`: `colorType -> ColorFunctionKind`, `colorSpace -> PredefinedColorSpace`, `mixSpace -> ColorSpace`, `hueMethodKeyword -> HueInterpolationMethod`. The `-> input : Color` on `colorFunction`/`colorFn`/`colorMix` remains.
   - `value-unit.bbnf`: `length -> Length`, `angle -> Angle`, `time -> Time`, `frequency -> Frequency`, `resolution -> Resolution`, `flex -> FlexValue`, `percentage -> Percentage`.
   - `selectors.bbnf`: `combinator -> Combinator`, `attrMatcher -> AttrOperator`, `attrSelector -> AttrSelector`, `complexSelector -> Selector`, `selectorList -> SelectorList`, `relativeSelectorList -> RelativeSelectorList`, `nthPseudo -> NthSelector`, `langPseudo -> LangSelector`, `dirPseudo -> DirSelector`, `isPseudo -> IsPseudoSelector`, etc.
   - `media.bbnf`: `mediaQuery -> MediaQuery`, `mediaCondition -> MediaCondition`, `mediaFeature -> MediaFeature`, `mediaQueryList -> MediaQueryList`.
3. MISSING rows: new `grammar/css/l4/at-rules.bbnf` module `@import`'s `value-unit.bbnf` + `selectors.bbnf` + `properties.bbnf`, declares `fontFaceRule`, `supportsRule` + `supportsCondition`, `containerRule` + `containerCondition` (mirrors `mediaCondition`), `layerStatementRule`, `layerBlockRule`, `pageRule`, `importRule`, `namespaceRule`, `propertyRule`; each with `-> TypeName`. `stylesheet.bbnf`'s `atRule` alt prepends the new rules before `genericAtRule`. `media.bbnf` `mediaFeature` gains a range-form alternative.
4. PARTIAL rows: `selectors.bbnf` adds `identifiedPseudoClass` u8-tagged alt over `:hover -> 0u8 | :focus -> 1u8 | :active -> 2u8 | :visited -> 3u8 | :checked -> 4u8 | :disabled -> 5u8 | :enabled -> 6u8 | :empty -> 7u8 | :first-child -> 8u8 | :last-child -> 9u8 | :only-child -> 10u8 | :first-of-type -> 11u8 | :last-of-type -> 12u8 | :only-of-type -> 13u8 | :root -> 14u8 | :target -> 15u8 | :link -> 16u8 | :default -> 17u8 | :required -> 18u8 | :optional -> 19u8`, prepended to `pseudoClass` ahead of `simplePseudoClass`. `selectors.bbnf` adds `identifiedPseudoElement` for the 8 hot keywords. `color.bbnf` `colorValue` admits `"none" -> NaN`. `properties.bbnf` factors `fontFamilyList` for the font-family path. Gradient / transform / filter / easing rules gain per-function `-> TypeName`.
5. After each module edit, `cargo expand -p bbnf --bench css_l4 > target/expand/ay-css-l4.rs`; confirm the new annotation surfaces as a projection struct. Declined admission routes to W2.b per §13 diagnostic-loop.

Sub-gate:
- `W2-lightningcss-audit.csv` committed; every row has state + (OUT-OF-SCOPE) `grep` evidence.
- `cargo iter-check` exit 0.
- `grep -c 'pub struct CssL4Parser\w*Projection' target/expand/ay-css-l4.rs` ≥ 80 (pre-W2: 48 per AUDIT-B; each EXISTS annotation adds 30+, each MISSING adds 8+).
- `cargo test -p bbnf --test css_l4_parity --release` green (annotations do not perturb admission).
- No `.bbnf` edit touches a rule outside `grammar/css/l4/**`; new rules use `@import` (no god module).

### AY-II.W2.b — Projection emission completion

Owner: IR payload-layout pass + Rust backend view synthesis.

Mechanism:

1. Run `crates/core/tests/projection_totality.rs` (W0.d-landed) against the post-W2.a grammar. Every `-> Type`-annotated rule failing to emit a projection feeds an extension to `crates/ir/src/passes/payload/layout.rs`. CSP facts drive admission; zero grammar-name dispatch added.
2. Layout-pass extensions (grammar-structure-driven, not name-driven):
   - `Selector` / `SelectorList` / `RelativeSelectorList`: repeat-of-complex → `CssL4ParserSelectorProjection { components: (arena_offset, arena_len) }`; materializer walks the arena slab.
   - `MediaCondition` / `ContainerCondition` / `SupportsCondition` recursive Alt: variant-tag u8 + ColorRef-style arena-handle (mirrors `colorMix`'s ColorRef pattern @ `color.bbnf:266-268`). The ColorRef pattern generalizes to every recursive Alt.
   - `Length::Calc` / `mathExpr` nesting: same arena-handle pattern applied to `calcFunction` / `mathExpr` / `mathProduct`.
   - `Property<'i>` per-group: each of the 27 typed-decl groups emits `CssL4Parser<Group>PropertyProjection { variant_idx: u8, value: arena_handle }`. Group dispatch via `declaration` Alt; `ir.payload_layouts` already carries the u8 width.
3. Retire `__named_type_shim_color` @ `view/named_types.rs`. The W0.d `infer_named_shape` at line 125 already synthesizes the 5-field `(U8, F64, F64, F64, F64)` tuple for `colorFunction`'s Seq kernel; W2.b routes the emitter to consume that shape as the projection, not the shim. `RustNamedTypes::bindings` keeps `Color` as a CSP-inferred fact populated from `ir.types`; no hand-authored slice survives.
4. Emit `CssL4Parser<Group>PropertyProjection` via extension to `shapes/value_materialize.rs`: when a rule's declared type is `Named(PropertyGroup)` and body is `Seq([propsAlt, value_list, …])`, route the `propsAlt`'s u8 tag into `variant_idx` rather than a generic `field_0`.

Sub-gate:
- `cargo iter-check` exit 0.
- `cargo test -p bbnf --test projection_totality --profile ax-iter` green.
- `cargo test -p bbnf --test named_type_preservation --profile ax-iter` green (totality invariant holds post-W2).
- `grep -c '__named_type_shim' target/expand/ay-css-l4.rs` → 0.
- `grep -c 'pub struct CssL4Parser\w*Projection' target/expand/ay-css-l4.rs` ≥ 80; `grep -c 'fn materialize_projection_\w*_CssL4Parser'` equals the projection count.
- `grep 'static.*NamedTypeBinding\|const.*NamedTypeBinding\|&\[NamedTypeBinding\b' crates/core/src/backend/rust/view/named_types.rs` → empty.

### AY-II.W2.c — Canonical output byte-identical parity

Owner: `css_l4_canonical_parity.rs` corpus + `common/css_normalize.rs`.

Mechanism:

1. Author per-feature fixtures under `data/css/lightningcss-corpus/` (one per targeted surface; 5–30 lines each): `fontface.css`, `supports.css`, `container.css`, `layer-statement.css`, `layer-block.css`, `page.css`, `import.css`, `namespace.css`, `property.css`, `media-range.css`, `color-lab.css`, `color-oklch.css`, `color-predefined.css`, `color-mix.css`, `pseudoclass-hot.css`, `pseudoelement-hot.css`, `calc-nested.css`, `gradient-linear.css`, `gradient-radial.css`, `transform-list.css`, `filter-list.css`, `easing-cubic.css`.
2. Add `canonical_parity_<feature>` per fixture; each calls `assert_canonical_parity("lightningcss-corpus/<feature>.css")` through the existing harness (`shared_printer` + `token_normalize` + `first_diff`).
3. bootstrap.css graduation: run `canonical_parity_bootstrap`, inspect first-diff-byte context, extend `common/css_normalize.rs` with symmetric rewrites per divergence class (candidate classes: named-color→hex, calc folding, longhand→shorthand collapse, background-position normalization). Every added rule is a symmetric normalizer — no lightningcss-specific bridge. Classes exceeding symmetric normalization (calc evaluator territory) stay on `scale_interop_` with in-harness rationale.
4. tailwind remains `scale_interop_tailwind` per the calc-evaluator gap rationale (W1r.3a-diag); AY-II does not open a calc evaluator workstream (BA-scope).

Sub-gate:
- `cargo iter-check` exit 0.
- `cargo test -p bbnf --test css_l4_canonical_parity --release` green: `canonical_parity_normalize` + every `canonical_parity_<feature>` pass.
- `data/css/lightningcss-corpus/` committed with ≥ 15 fixtures.
- `scale_interop_bootstrap` + `scale_interop_tailwind` remain green (no regression).
- `common/css_normalize.rs` additions audit as symmetric rewrites only.

### AY-II.W2.d — Typed accessor + lightningcss-parity tightening

Owner: `lightningcss_parity.rs`, `typed_accessor_surface.rs`, `css_l4_parity.rs`, `css_l4_named_color_parity.rs`.

Mechanism:

1. `lightningcss_parity.rs`:
   - Extend `color_channel_parity_rgb_family` → `color_channel_parity_all_families`: RGBA (existing), LAB (lab / lch / oklab / oklch), Predefined (color() with space), Float (with `none` components), CurrentColor. Drop the pre-wiring empty-guard (`if bbnf_colors.is_empty() { return; }`); W2.b's projection landing makes it live.
   - New `selector_parity` test: 10 inline fixtures exercising each `Component` variant (type, class, id, attr-eq, attr-match, pseudo-class, pseudo-element, combinator, nested `selectorList`, `:has`). Parse both parsers; project bbnf's `SelectorProjection` vs lightningcss's `Selector<'i>`; compare component-count + ordered-kind-list.
   - New `media_query_parity` test: 5 fixtures (`@media print`, `@media screen and (min-width: 768px)`, `@media not print`, `@media (prefers-color-scheme: dark)`, `@media (width >= 600px)`). Compare `MediaQueryList::query_count` + per-query qualifier/type/condition-tree structure.
   - New `property_parity` test: 20-declaration inline CSS; assert lightningcss's `Property` variant ↔ bbnf's `CssL4Parser<Group>PropertyProjection.variant_idx` 1:1. Mapping table is test-local constant.
2. `typed_accessor_surface.rs`: `css_l4_accessor_surface` tightens from `>= 1` floors to exact post-W2 numbers (authoring seed: `r.aggregate >= 12`, `r.leaf_scalar >= 60`, `r.alt >= 25`; W2.d enshrines exact floors from the post-W2 expand).
3. `lightningcss_parity_{normalize,bootstrap,tailwind}`: tighten `bbnf > 1000` / `lc >= 500` to specific counts from the W2 close.
4. `css_l4_parity.rs` + `css_l4_named_color_parity.rs`: assertions track all 148 named colors; grep-assert emitter lands a projection per branch.
5. Zero `#[ignore]` admitted in the CSS parity test family. Pre-existing `#[ignore]` tags either retire (their blocker is closed) or attribute to a W2-scope MISSING row.

Sub-gate:
- `cargo iter-check` exit 0.
- `cargo test -p bbnf --test lightningcss_parity --release` green; new tests green.
- `cargo test -p bbnf --test typed_accessor_surface --profile ax-iter` green; counts match post-W2 enshrined floors.
- `cargo test -p bbnf --test css_l4_parity --release` + `--test css_l4_named_color_parity --release` green.
- `grep -rn '#\[ignore\]' crates/core/tests/{lightningcss_parity,css_l4_canonical_parity,typed_accessor_surface,css_l4_parity,css_l4_named_color_parity}.rs` → empty.

### AY-II.W2.e — CSS competitor bench + samply + fat-LTO clean

Owner: CSS bench harness + samply + close evidence.

Mechanism:

1. `crates/core/benches/css/competitors.rs` gains `bench_bbnf_css` group (`bbnf_css_normalize`, `bbnf_css_bootstrap`, `bbnf_css_tailwind`), each driving `CssL4Parser::parse`; `b.bytes = input.len() as u64` so per-byte throughput publishes on the shared axis; `benchmark_main!` lists all three parser groups.
2. `crates/core/benches/css/l4.rs` audit: no `parse_with_visitor` visitor-lane probe survives (the emission path retired at W0'.a); the fused pipeline + `Parsed::to_value()` is the benched consumer.
3. `cargo bench --profile bench -p bbnf --bench competitors` + `--bench css_l4` run fat-LTO; publish throughput + ratios to `docs/benchmarks/post-AY-II-W2-competitors-css.txt`.
4. samply per fixture via interactive `samply record cargo bench --profile profiling-prep -p bbnf --bench css_l4 bbnf_css_<fixture>` (NOT `--save-only`, per symbol-resolution edict); captures at `.profiles/samply/AY-II-W2/css_{fixture}/profile.json.gz`; top-5 self-time to `post-AY-II-W2-samply-summary.txt`.
5. Hot-path assertion: fused-pipeline parse fn + `FusedBuilder::push_leaf_*` / `begin_compound` / `end_compound` (W0'.a symbols) appear in top-5 self-time per fixture; zero self-time on `Parsed::to_value` / `navigate_tape` / `ValueBuilder::*` / tape-reconstruction paths. `core::str::from_utf8` or `parse_that::scan::decode::*` owning > 5% self-time on any fixture is flagged + routed to BA; AY-II closes CSS on peer-referenced ratios, not hot-path floor exceedance.
6. fat-LTO clean: `cargo bench --profile bench -p bbnf --bench css_l4` completes on every fixture without `columns.rs:409` panic; `cargo bench --profile bench -p bbnf --bench competitors` completes. Panic recurrence triggers diagnostic-loop relinquish per AY-II.md §13.

Sub-gate:
- `cargo bench --profile bench -p bbnf --bench css_l4` + `--bench competitors` complete; all fixtures publish throughput.
- `post-AY-II-W2-competitors-css.txt` committed; per-fixture ratios extracted.
- `post-AY-II-W2-samply-summary.txt` committed; top-5 per fixture named.
- `.profiles/samply/AY-II-W2/css_{tailwind,bootstrap,normalize}/profile.json.gz` committed.
- `nm target/release/deps/css_l4-*` shows `note_push` absent (W0.a transitive), `Columns::rollback_to` present (W0.a), `ValueBuilder::push` or W0.c-equivalent present.

## Hard gate — peer-referenced (wave close)

### Admission parity

1. `cargo test -p bbnf --test lightningcss_parity --release` green with new `color_channel_parity_all_families`, `selector_parity`, `media_query_parity`, `property_parity`. Zero `#[ignore]`. Artefact: test pass log.
2. `cargo test -p bbnf --test css_l4_parity --release` green. Artefact: test pass log.
3. `cargo test -p bbnf --test css_l4_named_color_parity --release` green on 148 named colors.

### Byte-identical canonical parity

4. `cargo test -p bbnf --test css_l4_canonical_parity --release` green on the full post-W2 corpus (`canonical_parity_normalize` + every `canonical_parity_<feature>`). Artefact: `data/css/lightningcss-corpus/` ≥ 15 fixtures + test pass log.

### Typed-accessor + projection invariants

5. `cargo test -p bbnf --test typed_accessor_surface --profile ax-iter` green; CSS L4 floors match post-W2 exact counts.
6. `cargo test -p bbnf --test projection_totality --profile ax-iter` green on CSS L4 (W0.d invariant re-verified post-W2 grammar extensions).
7. `grep -c 'pub struct CssL4Parser\w*Projection' target/expand/ay-css-l4.rs` ≥ 80 (AUDIT-B pre-W2: 48).
8. `grep -c 'fn materialize_projection_\w*_CssL4Parser' target/expand/ay-css-l4.rs` equals projection count (totality).
9. `grep -c '__named_type_shim' target/expand/ay-css-l4.rs` → 0.
10. `grep 'static.*NamedTypeBinding\|const.*NamedTypeBinding\|&\[NamedTypeBinding\b' crates/core/src/backend/rust/view/named_types.rs` → empty.
11. `grep -rn 'CssL4Parser' crates/core/src/backend/rust/ crates/core/src/runtime/ crates/tape/src/` as a dispatch predicate (`match` / `if` / `cfg`) → 0 (AY-II invariant §8 preservation).
12. `grep -E '^fn (Color|Length|Angle|Time|Resolution|Frequency|Percentage|Selector|MediaQuery) \b' crates/core/src/backend/rust/**/*.rs` → 0 hand-written accessors; only occurrences are GRAMMAR-emitted `materialize_projection_*` in generated code.

### Competitor + performance

13. `bbnf_css_<fixture> / cssparser_<fixture> ≤ 1.00` for normalize + bootstrap + tailwind. Artefact: `post-AY-II-W2-competitors-css.txt`.
14. `bbnf_css_<fixture> / lightningcss_<fixture> ≤ 2.00` for normalize + bootstrap + tailwind (lightningcss's broader typed AST is the peer target; AY-II's target is parity-within-2× budget; exceedance is BA-scope).
15. Samply hot-path owner per fixture: fused-pipeline parse fn + `ValueBuilder::push_aggregate` in top-5 self-time; zero self-time on `navigate_tape` / generic child-walk / tape-reconstruction. Artefact: `post-AY-II-W2-samply-summary.txt` + `.profiles/samply/AY-II-W2/`.

### fat-LTO + substrate stability

16. `cargo bench --profile bench -p bbnf --bench css_l4` + `--bench competitors` complete cleanly on every fixture. Artefact: bench logs in `post-AY-II-W2-competitors-css.txt`.
17. `nm target/release/deps/css_l4-*`: `Columns::rollback_to` + `FusedBuilder::*` present; `note_push` + `navigate_tape` + `ValueBuilder::*` + `parse_with_visitor_CssL4Parser` absent. Artefact: `post-AY-II-W2-nm.txt`.

## Verification artefacts

- `target/expand/ay-css-l4.rs` — post-W2 regen.
- `docs/tranches/AY-II/waves/W2-lightningcss-audit.csv` — lightningcss AST → grammar-rule ground truth.
- `docs/benchmarks/post-AY-II-W2-competitors-css.txt` — bbnf vs cssparser vs lightningcss ratios.
- `docs/benchmarks/post-AY-II-W2-samply-summary.txt` — top-5 self-time per fixture.
- `docs/benchmarks/post-AY-II-W2-nm.txt` — `nm` of `css_l4` + `competitors` bench binaries.
- `.profiles/samply/AY-II-W2/css_{tailwind,bootstrap,normalize}/profile.json.gz`.
- `data/css/lightningcss-corpus/*.css` — per-feature parity fixtures (≥ 15 files).
- Per-sub-agent commit SHAs (W2.a / W2.b / W2.c / W2.d / W2.e).
- `grammar/css/l4/at-rules.bbnf` + post-W2 `-> Type` annotation adds across the grammar family.
- Test pass logs: `lightningcss_parity`, `css_l4_canonical_parity`, `typed_accessor_surface`, `projection_totality`, `css_l4_parity`, `css_l4_named_color_parity`, `named_type_preservation`.

## Dependencies

- **Depends on**: W1 close.
- **Blocks**: W3.

## Archaeology

- AY-I.W2 (`docs/tranches/AY-I/audit/AYW2-named-collapse-probe.md`) attempted CSS named-type preservation via a hand-authored `RustNamedTypes` static slice carrying `Color` + `ColorMix`. AX.W1r.1 (cited at `crates/core/src/backend/rust/view/named_types.rs:11-18`) retired the static slice in favor of an `ir.types`-derived builder, landing the `Color` family via CSP inference. AY-I's typed-CSS work did not complete the full lightningcss AST surface — only `Color` landed, leaving `Length` / `Angle` / `Time` / `Selector` / `MediaQuery` / the 27-group `Property` dispatch / all at-rules (except `@media` / `@keyframes`) unannotated. W2 closes that surface, guided by AUDIT-B §4's 49-vs-48-vs-1 split (49 const entries, 48 materializers, 1 `__named_type_shim_color`). AY-I.W8's projected CSS close gate was a single `lightningcss_parity.rs` corpus-admission pass; W2's peer-referenced gate set is strictly broader (admission + byte-identical canonical + typed-accessor totality + competitor ratio + samply attribution).
- AUDIT-D §5 (`docs/tranches/AY-II/audit/AUDIT-D-perf-truth.md:154-188`) documented the `columns.rs:409` panic on `css_l4/bootstrap` + `google_sheets_monolithic/parse_nested` at AY mid-tranche HEAD. Root cause: W6.c Pratt outer-compound retarget reopened the rollback-under-open-frame hazard that W5.b's Shape-2 rationale was supposed to prevent. AY-II.W0.a's `Columns::rollback_to` + W0.b's retry-IIFE migration repair this transitively. W2.e verifies repair held across the post-W2 CSS annotations; panic recurrence traces to either a W2.a construct the emitter hasn't seen or a W0 regression, and the diagnostic-loop relinquish fires.
- AX.W1r.3 (`docs/tranches/AX/audit/W1r3a-diag.md`, cited at `css_l4_canonical_parity.rs:183`) documented the `scale_interop_<fixture>` vs `canonical_parity_<fixture>` split: byte-identical canonical parity for bootstrap/tailwind exceeds the harness because lightningcss performs semantic simplifications (calc folding, longhand-to-shorthand collapse) that no symmetric `token_normalize` can invert. W2.c preserves the split: bootstrap graduates from `scale_interop_` to `canonical_parity_` only if the post-W2 `token_normalize` additions close every divergence class; tailwind remains on `scale_interop_` per the calc-evaluator gap. The BA-scope calc-evaluator workstream would close tailwind's byte-parity; W2 does not open that.
