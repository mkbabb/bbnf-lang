# SK-V13 S-P2 V3 Hardening Consolidated

Pass: S-P2 Research. Cycle: V3.
Date: 2026-05-21.
Scope: consolidated CH1-CH6 challenge verdict for the SK-V13 S-P2 V3 research fold.
Output: this file.

## Verdict

`G-S-P2-V3-CONVERGED`: ACCEPT-PENDING-CONFIRMATION.

V3 resolves the sole V2 blocker. P2-F now stamps CSS rows 1-6 as
`CSS-ROW-SCOPE-CONDITIONAL`: generated row/fact-stream work, not primitive
eligibility. S-P3 may plan those rows only with fresh narrow CSS parser
profiling or same-wave strict lightningcss/cssparser row movement. All six
lenses accept the fold, making V3 the first accepted S-P2 cycle; V4 remains
required as the consecutive confirmation cycle before S-P2 can converge.

| Lens | Disposition | Load-bearing reason |
|---|---|---|
| CH1 correctness | ACCEPT | CSS rows 1-6 no longer read as primitive admissions; the C7/B2/P2E-6 route-production gate, D1 exclusion, orphan inventory fence, comparator strictness, Arm ISA anchors, and dav1d lineage bounds remain intact. |
| CH2 generality / Lock 14 | ACCEPT | `CSS-ROW-SCOPE-CONDITIONAL` preserves Lock 14 by keeping CSS-specific behavior in grammar metadata and generated row/fact streams rather than generic-crate control flow. |
| CH3 regression / REDRESS | ACCEPT | The V3 fold does not reopen historical REDRESS rejects under renamed CSS scopes and preserves the material-differential requirements for later S-P3 wave selection. |
| CH4 cost / micro-proof | ACCEPT | CSS row scopes remain conditional on narrow parser evidence or same-wave strict row movement, and any SIMD inside a CSS row still inherits scalar reference, parity/checkasm, same-wave consumer, and row-movement gates. |
| CH5 hidden coupling / Lock 1 | ACCEPT | CSS comparator/oracle lanes are not treated as runtime substrate; the single-substrate boundary, no-sidecar rule, and generated-fact-stream boundary remain unchanged. |
| CH6 anti-paper-close | ACCEPT | V3 removes the remaining paper-close path by preventing CSS feature rows from becoming support-only primitive work without a measured strict row result. |

Acceptance rate: 6/6 = 100%.
Consecutive accepted cycles: 1 (V3).

## Carry-Forward Requirements

1. S-P3 must preserve CSS rows 1-6 as conditional row-production scopes, not
   primitive eligibility.
2. Any CSS row plan must name either fresh narrow CSS parser profiling evidence
   or same-wave strict lightningcss/cssparser row movement.
3. Any SIMD primitive inside a CSS row must carry scalar reference,
   parity/checkasm where applicable, same-wave consumer wiring, REDRESS
   material-differential citation, and zero-orphan disposition.
4. The accepted V2 boundaries remain binding: no D1 standalone wave, no orphan
   inventory promotion, no dav1d gate authority without exact source anchors, no
   Arm ISA overclaim, no comparator-plane weakening, and no sidecar substrate.

## Cycle Disposition

S-P2 advances to V4 confirmation. If V4 accepts the unchanged V3 packet across
all six lenses, S-P2 converges and may authorize S-P3.
