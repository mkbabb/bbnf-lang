---
agent: 1A
pass: T-P1-excavation
cycle: V6
generated_at: 2026-05-23T21:08:29Z
spec_surfaces_audited: [ARCHITECTURE.md, MASTER-PLAN.md, LOCKS.md, HANDOFF.md, skinny/REDRESS.md, skinny/RESULTS.md, restart/skinny/tranches/sk-v14/SYNTHESIS.md, restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md, restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md, restart/skinny/INDEX.md, restart/skinny/SUBSTRATE.md, restart/skinny/COMPILER.md, restart/skinny/BENCH.md, restart/skinny/WORKSPACE.md, restart/skinny/HARDENING.md]
files_audited_count: 41
live_truth_method: "grep + line-anchored Read; SK-V14 audit-corrected baseline (S-P1 CH5 V3 substrate-union finding bound by orchestrator dispatch); structural cross-checks for DirectParser/ParserState cursor independence at HEAD; no build or cargo run inside the 45-min cap"
v6_fold_note: "SK-V14 T-P1 fold over V5-converged V4 inventory. Three first-cycle additions land: (a) SK-V14 binding row 1A-SUB-016 attributes substrate-union (Lock 1) to two structurally independent cursor types (ParserState.cursor over offset-tape vs DirectParser.cursor over raw bytes) confirmed at HEAD via codegen/json_typed_direct.rs and runtime/json/parser.rs. (b) 1A-SUB-017 records the JSON `dispatch_value` LTO-fused envelope (`runtime/src/grammars/json/generated.rs:43-56` gated 14 functions under `parse-attribution`) as the substrate-union dispatch primitive Lock-14-mis-attributed by name. (c) 1A-SUB-018 records the S-P1 CH2 13/17 parse_only + 14/17 direct rank-1 envelope mis-attribution census. V5-converged content carries forward; SK-V14 framing supersedes V4 in dispatch context §1 binding clause."
prior_cycle_dispositions_folded:
  accepted: [V4-CH1-cycle-consistent, V4-CH2-JSON-scope-honest, V4-CH3-REDRESS-history-only, V4-CH4-cost-metadata-inline, V4-CH5-StructuralIndex-transient, V4-CH6-UNKNOWN-verify-actions, V5-CH1-ACCEPT, V5-CH2-ACCEPT, V5-CH3-ACCEPT, V5-CH4-ACCEPT, V5-CH5-ACCEPT, V5-CH6-ACCEPT]
  rejected: []
  revised: []
  first_cycle_additions: [1A-SUB-016, 1A-SUB-017, 1A-SUB-018, 1A-DIV-008, 1A-UNK-005]
  carried_forward_rows: [1A-SUB-001, 1A-SUB-002, 1A-SUB-003, 1A-SUB-004, 1A-SUB-005, 1A-SUB-006, 1A-SUB-007, 1A-SUB-008, 1A-SUB-009, 1A-SUB-010, 1A-SUB-011, 1A-SUB-012, 1A-SUB-013, 1A-SUB-014, 1A-SUB-015]
divergence_count:
  spec_claims_implemented: 7
  spec_claims_unimplemented: 5
  impl_exceeds_spec: 0
  unknown: 6
locks_amendment_candidates: 1
---

## Executive Summary

The skinny substrate implements the centre of Lock 1 for JSON: `Tape<'input>` owns
source-backed offset storage at `skinny/crates/runtime/src/tape/mod.rs:94-101`,
generated `JsonRoot` owns a sealed tape at `runtime/src/grammars/json/view.rs:12-23`,
and `ValueRef<'doc, 'input, K, G>` is the tape-plus-cursor borrow shape at
`runtime/src/tape/mod.rs:175-180`. `BackendShape` carries the spec's five
variants at `crates/ir/src/lib.rs:339-345`, and `LayoutFacts.backend_shape` is
populated from `derive_backend_shape_with_diagnostics` at `passes/src/lib.rs:88-92`,
`passes/src/lib.rs:390-440`. SK-V14 S-P1 CH5 V3 binding now lands: `ParserState.cursor`
(`runtime/src/grammars/json/parser.rs:7-12`, offset-tape) and `DirectParser.cursor`
(`codegen/src/json_typed_direct.rs:518-522`, raw bytes) are structurally
independent cursor types at HEAD — the substrate-union of Lock 1 is implemented
as two parallel cursor implementations sharing no `Tape` abstraction, not as one
typed event cursor over a single offset substrate. Generated JSON direct
parsing remains admitted `SinkOnly` direct evidence (`runtime/src/grammars/json/generated.rs:466-502`,
`runtime/src/grammars/json/sink.rs:4-15`). The S-P1 dispatch-envelope dominance
finding (13/17 parse_only + 14/17 direct rank-1 leaves resolve to
`dispatch_value`/`parse_object_value_at_direct`/`parse_array_element_at_direct`
JSON-named envelopes) is a Lock-14 mis-attribution: the envelope hides the
grammar-neutral `dispatch` primitive. CSS L4 declaration-values remains admitted
fact-stream evidence with no substrate, not a regression. Estimated closure
remains medium-to-high risk; the new SK-V14 row adds 400-900 LOC to unify the
two cursor types into one substrate-union typed event cursor.

## Spec-Claim ↔ Implementation Table

| id | claim path:line | impl path:line | verdict | note |
|---|---|---|---|---|
| 1A-SUB-001 | `restart/ARCHITECTURE.md:1549-1552` and `restart/locks/LOCKS.md:48` claim tape is the substrate unioned with direct-to-struct. | `skinny/crates/runtime/src/tape/mod.rs:94-101`; `skinny/crates/runtime/src/grammars/json/view.rs:12-23`; `skinny/crates/runtime/src/grammars/json/generated.rs:466-502`; `skinny/REDRESS.md` SinkOnly admit; `skinny/RESULTS.md` SinkOnly row | partial / admitted direct evidence + unaudited scheduling | Retained JSON owns a `Tape`; generated JSON direct exists as admitted `SinkOnly` direct evidence. The remaining V1 gap is not "direct lacks `TapeBuilder`"; it is that one shared `TapeEmit` / `DirectBuild` event scheduling plan is not audited from the runtime/codegen evidence. LOC/risk: 400-900 LOC medium in codegen/runtime to prove one scheduled materialization plan. |
| 1A-SUB-002 | `restart/locks/LOCKS.md:48` says typed-value records borrow `&'i Tape<'i>` plus cursor. | `skinny/crates/runtime/src/tape/mod.rs:175-180`; `skinny/crates/runtime/src/tape/mod.rs:191-222`; `skinny/crates/runtime/src/grammars/json/view.rs:68-82` | implemented | Live shape is stricter: `ValueRef<'doc, 'input, K, G>` borrows `&'doc Tape<'input>` and carries a cursor. LOC/risk: 0-100 LOC low if docs should mention the separate document lifetime. |
| 1A-SUB-003 | `restart/ARCHITECTURE.md:1574-1575` requires direct views to point into tape and one public node identity `(TapeId, node id, payload class)`. | `skinny/crates/runtime/src/tape/mod.rs:91-100`; `skinny/crates/runtime/src/tape/mod.rs:218-223`; `skinny/crates/runtime/src/grammars/json/value.rs` (JsonNodeKind recomputed from source byte) | partial / diverged | `TapeId` and cursor exist; `ValueRef` identity is tape+cursor only and `JsonNodeKind` is recomputed from source byte, not stored payload class. LOC/risk: 200-500 LOC medium if identity must be made explicit. |
| 1A-SUB-004 | `restart/ARCHITECTURE.md:922-944` claims a 20-variant BIR alphabet including `SimdScan`, `PrattSpine`, `CallHost`, `LayoutScope`, `ErrorRecover`, `PathEval`, `DebugMark`, and `Return`. | `skinny/crates/ir/src/lib.rs:354-389`; `skinny/crates/ir/src/lib.rs:391-398` | unimplemented / diverged | Live `BackendExpr` has 13 variants at `ir/src/lib.rs:354-389` (Entry, Seq, Alt, RepeatLoop, OptionalBranch, ByteLiteral, RegexProgram, CallRule, SpanMark, TapeEmit, DirectBuild, ValueProject, Return); `SimdScan` is a separate `Recognizer` at `ir/src/lib.rs:391-398`. Missing from live enum: `PrattSpine`, `CallHost`, `LayoutScope`, `ErrorRecover`, `PathEval`, `DebugMark`. LOC/risk: 600-1,200 LOC high, because adding variants touches validation, lowerers, snapshots, and VM expectations. |
| 1A-SUB-005 | `restart/ARCHITECTURE.md:951-961` says access pattern varies by `BackendShape` without changing BIR payloads. | `skinny/crates/ir/src/lib.rs:338-345`; `skinny/crates/ir/src/cost.rs:124-135` (ShapeRationale); `skinny/crates/ir/src/cost.rs:149-159` (PriorityStep::ALL [P1..P8]) | implemented at type level | The five variants `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` exist; `PriorityStep::ALL` carries the 8-step P1-P8 priority for `derive_backend_shape`. Runtime materialization for `EventTape`/`CollapsedStage` is not complete; see 1A-SUB-007. LOC/risk: type-level 0; runtime completion 800-1,500 LOC high. |
| 1A-SUB-006 | `restart/ARCHITECTURE.md:1060-1098` claims `LayoutFacts.backend_shape` is cost-model-derived by an 8-step algorithm. | `skinny/crates/passes/src/lib.rs:82-92`; `skinny/crates/passes/src/lib.rs:104-110`; `skinny/crates/passes/src/lib.rs:381-440`; `skinny/crates/ir/src/cost.rs:4-17`; `skinny/crates/ir/src/cost.rs:137-159` | implemented side table / partial cost model | `LayoutFacts.backend_shape: HashMap<RuleId, BackendShape>` is live in `passes` and populated from `derive_backend_shape_with_diagnostics`. The remaining divergence is cost-model and priority-algorithm strength: current priority names/order and evidence diagnostics are not yet the full measured 8-clause spec, and runtime materialization by shape remains incomplete. LOC/risk: 250-600 LOC medium for measured priority/evidence repair, plus runtime/codegen work captured in related rows. |
| 1A-SUB-007 | `restart/ARCHITECTURE.md:1068-1098` and `restart/ARCHITECTURE.md:1107-1108` claim `EagerTape`, `OffsetTape`, `EventTape`, `SinkOnly`, and `CollapsedStage` lowering outputs. | `skinny/crates/runtime/src/tape/mod.rs:94-101` (offset substrate); `skinny/crates/runtime/src/grammars/json/parser.rs:7-31` (offset-tape via ParserState); `skinny/crates/runtime/src/grammars/json/generated.rs:466-502` (SinkOnly via DirectSink hooks) | partial / UNKNOWN event/collapsed runtime | Offset-style retained JSON and SinkOnly-style direct JSON exist. The cited runtime rows do not prove event cells or collapsed-stage kernel/shim; routes to `1A-UNK-003` and same-wave consumer proof rather than closing by uncaptured search. LOC/risk: 800-1,800 LOC high in runtime/codegen plus ISA test risk. |
| 1A-SUB-008 | `restart/ARCHITECTURE.md:1556-1565` claims `runtime/src/tape` owns `token`, `builder`, `span`, `payload`, `view`, and `trace` modules. | `skinny/crates/runtime/src/tape/mod.rs:1-12`; `skinny/crates/runtime/src/tape/assembler.rs:42-48`; `skinny/crates/runtime/src/tape/mod.rs:38-89` (PayloadArena); `skinny/crates/runtime/src/tape/offsets.rs:1-7` | partial / diverged | Live `mod.rs:1-5` declares `assembler`, `event_grammar`, tests, and `offsets`; builder/payload/view concepts exist but not as the spec-named modules. No `token`, `span`, `view`, or `trace` modules in the live `runtime/src/tape/` directory. LOC/risk: 250-600 LOC medium if this is structural API, low if docs can accept skinny names. |
| 1A-SUB-009 | `restart/ARCHITECTURE.md:1571-1573` requires append-only checkpoints, bounded rollback, and slice-borrow payload policy. | `skinny/crates/runtime/src/tape/assembler.rs:61-85` (push_offset); `skinny/crates/runtime/src/tape/assembler.rs:93-113` (patch_flags); `skinny/crates/runtime/src/tape/mod.rs:65-78` (write_bytes) | partial / UNKNOWN checkpoint surface | Append-only offset writes and sparse flag patching exist in the cited builder API. The cited rows do not prove a checkpoint/rollback API, and payload policy is only evidenced as a byte arena without declared per-token policy. LOC/risk: 300-700 LOC medium. |
| 1A-SUB-010 | `restart/ARCHITECTURE.md:1577-1585` says direct builders do not bypass the substrate event stream and direct scalars are not a second authoritative tree. | `skinny/crates/runtime/src/grammars/json/generated.rs:466-502` (parse_object_value_at_direct); `skinny/crates/runtime/src/grammars/json/generated.rs:506-542` (parse_array_element_at_direct); `skinny/crates/runtime/src/grammars/json/sink.rs:1-15` (JsonSink trait); `skinny/REDRESS.md` SinkOnly admit; `skinny/RESULTS.md` SinkOnly row | partial / admitted SinkOnly, scheduling UNKNOWN | JSON direct parsing writes to generated `JsonSink` methods and returns no queryable document identity, which matches admitted `SinkOnly` direct evidence. The remaining gap is whether direct and retained paths share an auditable accepted event/materialization schedule. SK-V14 1A-SUB-016 sharpens the gap. LOC/risk: 400-900 LOC medium/high to make event scheduling auditable. |
| 1A-SUB-011 | `restart/ARCHITECTURE.md:1604-1608` says retained APIs are document-first and `SinkOnly` retains no identity. | `skinny/crates/runtime/src/grammars/json/view.rs:12-23` (JsonRoot owns JsonDocument); `skinny/crates/runtime/src/grammars/json/view.rs:63-66` (JsonDocument { tape, source_str }); `skinny/crates/runtime/src/grammars/json/generated.rs:466-502` (direct accepts &mut S sink, returns ()) | implemented | Retained JSON root owns `JsonDocument { tape, source_str }`; direct parse accepts a sink and returns only `Result<(), ParseError>`. LOC/risk: none for JSON. |
| 1A-SUB-012 | `restart/ARCHITECTURE.md:1610-1629` claims byte input -> mask stream -> typed event cursor -> substrate shape; mask stream is transient, not retained substrate. | `skinny/crates/runtime/src/grammars/json/scan.rs:1-7` (JSON-owned scan note); `skinny/crates/runtime/src/grammars/json/scan.rs:22-35` (scan_structurals returns StructuralIndex transient); `skinny/crates/runtime/src/grammars/json/scan.rs:47-54` (structural_capacity_for); `skinny/crates/runtime/src/grammars/json/generated.rs:12-15` (attach_structural_index is no-op) | partial / transient scanner plane | `StructuralIndex` is a live JSON-owned scanner/capacity plane and must be classified explicitly. Current cited evidence supports transient capacity/proof use (`OneShotSimd` capacity), while generated retained parse's `attach_structural_index` is a no-op. No retained document identity is proven from `StructuralIndex`; typed event cursor remains UNKNOWN pending targeted call-site audit. LOC/risk: 200-500 LOC medium. |
| 1A-SUB-013 | `restart/ARCHITECTURE.md:1646-1660` says runtime files under `runtime/src/grammars/<name>/` are template-emitted and hand-written runtime files are forbidden. | `skinny/crates/runtime/src/grammars/json/generated.rs:1` (@generated header); `skinny/crates/runtime/src/grammars/json/view.rs:1` (@generated header); `skinny/crates/runtime/src/grammars/json/parser.rs:1` (@generated header); `skinny/crates/runtime/src/grammars/json/scan.rs:1` ("JSON-owned ... not part of the generated JSON roster"); `skinny/crates/runtime/src/grammars/json/sink.rs:1` (no @generated header); `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1` | unknown / diverged | Some files carry generated headers; `json/scan.rs:1` and `json/sink.rs:1` are explicitly hand-owned and not part of the generated roster. Generator provenance for all grammar files cannot be verified from comments alone. LOC/risk: 200-500 LOC medium to template or quarantine hand-owned grammar files. |
| 1A-SUB-014 | `restart/ARCHITECTURE.md:1547-1552`, `restart/ARCHITECTURE.md:1644-1645`, and `restart/locks/LOCKS.md:48` reject new substrates, sidecars, OpenFrame ladders, and columnar SoA. | `skinny/crates/runtime/src/tape/mod.rs:94-101` (single Tape); `skinny/crates/runtime/src/grammars/json/scan.rs:1-7` (JSON-owned transient scan); `skinny/crates/runtime/src/grammars/json/scan.rs:47-54` (structural_capacity_for uses StructuralIndex only as capacity input) | partial / UNKNOWN old-name census | The retained JSON substrate is tape; live renamed side planes exist: JSON `StructuralIndex` as transient scanner/capacity input and CSS comparator evidence. Neither is proven as a retained authoritative substrate, but both must stay fenced and non-authoritative until targeted scans are captured. LOC/risk: 100-300 LOC low/medium for audit artifacts and fencing docs, higher only if retained identity is found. |
| 1A-SUB-015 | `restart/ARCHITECTURE.md:1577-1585` requires shared validation, spans, diagnostics, node kind, and payload policy across direct/tape. | `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1`; `skinny/crates/runtime/src/grammars/css_l4_declaration_values/sink.rs`; `skinny/REDRESS.md` CSS fact-stream admit; `skinny/RESULTS.md` CSS fact-stream row | admitted CSS fact-stream evidence / substrate-category gap | CSS declaration-values emits a TSV-like fact stream through `FactSink` with no `Tape`, `ValueRef`, `DocumentView`, or direct typed root. Preserved as an admitted same-plane product row; the substrate finding is that V1 lacks a category for admitted fact-stream telemetry/product rows. LOC/risk: 100-300 LOC low/medium for lock/audit taxonomy; 500-1,000 LOC medium/high only if a future wave chooses to produce a tape/direct substrate for CSS. |
| 1A-SUB-016 | `restart/locks/LOCKS.md:48` says typed records borrow `&'i Tape<'i>` + cursor — one materialisation surface — no parallel substrate. | Offset-tape path: `skinny/crates/runtime/src/grammars/json/parser.rs:7-12` (`ParserState { input, bytes, cursor: usize, tape: TapeBuilder<'i> }`); `skinny/crates/runtime/src/grammars/json/generated.rs:33-56` (state-based dispatch_value over offsets). Direct path: `skinny/crates/codegen/src/json_typed_direct.rs:518-522` (`struct DirectParser<'i> { input, bytes, cursor: usize }` — NO `tape` field); `skinny/crates/codegen/src/json_typed_direct.rs:524-572` (DirectParser impl uses raw `self.cursor` only); `skinny/crates/runtime/src/grammars/json/generated.rs:466-502` (parse_object_value_at_direct takes `cursor: &mut usize` — no `Tape`). SK-V14 S-P1 CH5 V3 binding: substrate-union finding verified structurally independent at HEAD. | partial / diverged — substrate-union is two parallel cursor types | The two cursor types share no substrate abstraction. `ParserState.cursor` indexes bytes alongside `TapeBuilder<'i>` (offset substrate); `DirectParser.cursor` indexes bytes with no tape at all. Lock 1's "one materialisation surface" reads against two disjoint surfaces today; the SinkOnly admit fenced this with "no queryable document" but does not satisfy the spec's single-cursor-over-shared-substrate invariant. The Lock 14 mis-attribution is structural, not just nominal: the generated direct path is a copy-and-paste of the dispatch shape, not a shape lowered through a shared event-cursor abstraction. LOC/risk: 400-900 LOC medium/high to unify into one substrate-union typed event cursor; 0 LOC if Pass Omega ratifies the two-cursor implementation as the V1 substrate-union shape (1E candidate 1A-LOCK1-AMEND-001). |
| 1A-SUB-017 | `restart/ARCHITECTURE.md:967-980` distinguishes BIR `Alt { mode: Dispatch }` and `Alt { mode: Speculative }` as the dispatch primitives. | `skinny/crates/runtime/src/grammars/json/generated.rs:43-56` (dispatch_value: byte-keyed match returning `parse_object`/`parse_array`/`parse_string`/`parse_number`/`parse_literal`); `skinny/crates/runtime/src/grammars/json/generated.rs:466-502` (parse_object_value_at_direct: same byte-keyed dispatch over direct sink); `skinny/crates/runtime/src/grammars/json/generated.rs:506-542` (parse_array_element_at_direct: same dispatch shape) | implemented as JSON-named envelope | Three JSON-named dispatch envelopes implement the `Alt { Dispatch }` shape — `dispatch_value`, `parse_object_value_at_direct`, `parse_array_element_at_direct`. All three are gated by `#[cfg_attr(feature = "parse-attribution", inline(never))]` so production builds LTO-fuse them into the caller. The substrate-union question is whether these three are one dispatch primitive instantiated three times, or three separate primitives. Per SK-V14 S-P1 CH2: "the hot loop is a branch-on-first-byte dispatch step under the substrate-union scheme — the primitive *behind* the envelope is the `dispatch` primitive" (`restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:234`). LOC/risk: 200-500 LOC medium to lower one shared dispatch primitive emitted three times. |
| 1A-SUB-018 | `restart/ARCHITECTURE.md:1574-1575` requires one public node identity tied to substrate event/payload class. | SK-V14 S-P1 census: 13/17 parse_only rank-1 leaves → `dispatch_value` envelope; 14/17 direct rank-1 leaves → `parse_object_value_at_direct` or `parse_array_element_at_direct` envelope (`restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:234`, `restart/skinny/tranches/sk-v14/research/p1/p1b-samply-mode-2.md:116-146`); typed plane: 5/7 typed-product rows rank-1 to `DirectParser::skip_value` at `skinny/crates/bbnf-bench/src/generated_real_typed.rs:2949` (`restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:246`). | implemented as Lock-14 mis-attributed JSON envelope | The envelope name dominance is a Lock-14 mis-attribution per SK-V14 S-P1 CH2: the JSON-grammar names hide the grammar-neutral `dispatch` primitive. The substrate-union observation is that `DirectParser::skip_value` is "substrate + dispatch in equal parts" — substrate-walk-with-shape-validation. Per the SK-V14 CH5 V3 closure, the dispatch envelope dominance does not invalidate Lock 1 (`runtime/src/tape/` is still single); it does invalidate any reading of Lock 14 that allows the JSON envelope name to stand as the primitive name in T-P2 dispatch. LOC/risk: 100-300 LOC for grammar-neutral renaming + dispatch primitive lowering proof. |

## Divergences Catalogued

| id | divergence | loc_budget | risk | wave | hard_cap | same_wave_consumer | evidence_basis |
|---|---|---|---|---|---|---|---|
| 1A-DIV-001 | BIR alphabet is underspecified in live `ir/src` relative to the architecture. The spec claims 20 variants; live `BackendExpr` covers 13 variants and `SimdScan` is a separate `Recognizer`. | 600-1,200 LOC | high | T-P2 IR/lowering wave | cap at 1,500 LOC before re-scoping to amendment | Codegen/runtime lowerers and VM tests in the same wave | Spec `restart/ARCHITECTURE.md:922-944`; live `skinny/crates/ir/src/lib.rs:354-398`. |
| 1A-DIV-002 | Direct-to-struct union is not auditable as one scheduled materialization path. Generated JSON `SinkOnly` direct parsing is admitted evidence; the open gap is shared `TapeEmit` / `DirectBuild` scheduling. | 400-900 LOC | medium/high | T-P2 codegen/runtime scheduling | cap at 1,100 LOC or split into codegen/runtime subrows | Generated retained parse and direct parse row tests in the same wave | Spec `restart/ARCHITECTURE.md:1019-1020`, `restart/ARCHITECTURE.md:1577-1585`; live retained `skinny/crates/runtime/src/grammars/json/parser.rs:7-12`; live direct `skinny/crates/runtime/src/grammars/json/generated.rs:466-502`; admission `skinny/REDRESS.md` SinkOnly admit. |
| 1A-DIV-003 | Runtime event cursor and EventTape are not proven as runtime substrate. Offset-backed `ValueRef` exists; typed `EventCursor` remains UNKNOWN until targeted symbol/call-site output is captured. | 300-800 LOC | medium | T-P2 runtime substrate | cap at 900 LOC before deferring EventTape to amendment | Runtime tape API tests plus retained parser consumer | Spec `restart/ARCHITECTURE.md:1610-1629`; live offset view `skinny/crates/runtime/src/tape/mod.rs:175-222`; `skinny/crates/runtime/src/tape/offsets.rs:1-7`; negative symbol status routed to `1A-UNK-003`. |
| 1A-DIV-004 | `CollapsedStage` exists in `BackendShape` but no audited runtime shape exists. | 500-1,000 LOC plus assembly/ISA parity harness | high | SIMD/ASM wave only after same-wave consumer is named | cap at 1,200 LOC without parity harness, otherwise split | A production grammar × ISA row must consume it in the same wave | Spec `restart/ARCHITECTURE.md:1107-1108`; live enum `skinny/crates/ir/src/lib.rs:338-345`; shape list `skinny/crates/ir/src/cost.rs:124-135`; viability diagnostics in `skinny/crates/passes/src/lib.rs:381-440`. |
| 1A-DIV-005 | Tape module layout does not match §9.1 names. This may be a structural API gap or a stale naming contract. | 250-600 LOC | medium | T-P2 runtime API/doc alignment | cap at 700 LOC; if higher, route to lock amendment | Runtime tape public API docs and parser compile users | Spec `restart/ARCHITECTURE.md:1556-1565`; live modules `skinny/crates/runtime/src/tape/mod.rs:1-12`. |
| 1A-DIV-006 | CSS declaration-values runtime is admitted same-plane fact-stream evidence, but V1 has no substrate/telemetry category for that product row. | 100-300 LOC for taxonomy; 500-1,000 LOC only if future tape/direct substrate is required | medium | T-P1/T-P2 audit taxonomy; implementation only in later runtime wave | cap taxonomy at 350 LOC; implementation requires separate wave charter | 1E lock wording and CSS row-plane accounting in the same wave | Spec `restart/ARCHITECTURE.md:1610-1618`; live `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1`; admission `skinny/REDRESS.md` CSS fact-stream admit. |
| 1A-DIV-007 | Renamed scanner/sidecar planes are live and must stay fenced: JSON `StructuralIndex` is a transient scanner/capacity plane, and CSS source-sidecar is comparator evidence, not runtime substrate. | 100-300 LOC | low/medium | T-P1 audit hardening / T-P2 fence enforcement | cap at 400 LOC unless retained identity is found | 1F hidden-coupling rows and runtime/codegen audit consumers | JSON scan `skinny/crates/runtime/src/grammars/json/scan.rs:1-7`, `skinny/crates/runtime/src/grammars/json/scan.rs:22-54`. |
| 1A-DIV-008 | **SK-V14 binding (new in V6).** Substrate-union (Lock 1) is implemented at HEAD as two structurally independent cursor types: `ParserState.cursor` carries `TapeBuilder<'i>` for the offset substrate at `runtime/src/grammars/json/parser.rs:7-12`; `DirectParser.cursor` carries raw `cursor: usize` only at `codegen/src/json_typed_direct.rs:518-522`. The two paths share no `Tape` abstraction, no shared event cursor, and no shared dispatch lowering: the dispatch envelope is copy-and-pasted across the offset path (`dispatch_value` at `runtime/src/grammars/json/generated.rs:43-56`) and the direct path (`parse_object_value_at_direct` at `runtime/src/grammars/json/generated.rs:466-502`). Lock 1's "one materialisation surface" reads against two disjoint surfaces today. | 400-900 LOC for cursor unification; 100-300 LOC if Pass Omega ratifies two-cursor shape | high (substrate-union semantics) | T-P2 substrate-union research; T-P3 disposition; Pass Omega ratification | cap at 1,100 LOC for unification before lock amendment | Generated retained parse, direct parse, typed direct parse row tests in same wave | SK-V14 S-P1 CH5 V3 binding (`restart/skinny/tranches/sk-v14/research/p1/hardening/HARDENING-S-P1-V3-CONSOLIDATED.md:46`); `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:246`; live structural cross-check at HEAD. |

## Gaps / Missing Primitives

| gap | evidence | impact | LOC/risk estimate |
|---|---|---|---|
| Full 20-variant BIR in audited `ir/src` | Spec rows `restart/ARCHITECTURE.md:922-944`; live rows `skinny/crates/ir/src/lib.rs:354-398`. Missing: `PrattSpine`, `CallHost`, `LayoutScope`, `ErrorRecover`, `PathEval`, `DebugMark`. | Lowerer/VM contract cannot cover the V1 alphabet from current `ir/src`. | 600-1,200 LOC, high. |
| Single substrate-union event cursor (SK-V14 binding) | Two disjoint cursor structs at `runtime/src/grammars/json/parser.rs:7-12` and `codegen/src/json_typed_direct.rs:518-522`. | Lock 1's "one materialisation surface" reads against two disjoint surfaces; SinkOnly admit fenced the gap as "no queryable document" but does not satisfy single-cursor-over-shared-substrate. | 400-900 LOC, medium/high. |
| Measured `LayoutFacts.backend_shape` cost/priority completion | Spec `restart/ARCHITECTURE.md:1047` and `restart/ARCHITECTURE.md:1090-1098`; live side table and population at `skinny/crates/passes/src/lib.rs:82-92` and `skinny/crates/passes/src/lib.rs:381-440`; live `CostFacts` at `skinny/crates/ir/src/cost.rs:4-17`; live `PriorityStep::ALL` at `skinny/crates/ir/src/cost.rs:149-159`. | The named side table is implemented in `passes`; the gap is measured evidence and full priority semantics, not symbol absence. | 250-600 LOC, medium. |
| Typed event cursor abstraction | Spec `restart/ARCHITECTURE.md:1620-1623`; cited live evidence only proves offset-backed `ValueRef` at `skinny/crates/runtime/src/tape/mod.rs:175-222`; symbol absence is routed to `1A-UNK-003` pending captured command output. | EventTape cannot be accepted as implemented from the current cited evidence. | 300-800 LOC, medium. |
| Checkpoint/rollback API | Spec `restart/ARCHITECTURE.md:1571-1572`; cited live builder evidence exposes push/patch/finish at `skinny/crates/runtime/src/tape/assembler.rs:61-123`. | Speculative `Alt` cannot prove bounded rollback from the currently cited builder surface. | 300-700 LOC, medium. |
| Payload policy and payload-class identity | Spec `restart/ARCHITECTURE.md:1574-1575`; live `PayloadArena` is byte storage at `skinny/crates/runtime/src/tape/mod.rs:38-89`; `ValueRef` stores no payload class at `skinny/crates/runtime/src/tape/mod.rs:175-180`. | Public node identity is weaker than spec. | 200-500 LOC, medium. |
| Generated provenance for grammar runtime files | Spec `restart/ARCHITECTURE.md:1658-1660`; `json/sink.rs:1` carries no @generated header; `json/scan.rs:1` says explicitly not generated. | Lock 14/Lock 1 substrate rows cannot distinguish emitted runtime surface from hand-owned exception. | 200-500 LOC, medium. |

## Open Questions

| id | status | question | verify_action |
|---|---|---|---|
| 1A-UNK-001 | UNKNOWN | Is `skinny/crates/runtime/src/grammars/json/scan.rs` a transient scan/capacity helper only, or can `StructuralIndex` become a retained sidecar in other build modes? | Run `rg -n "scan_structurals\\(|StructuralIndex|positions\\(" skinny/crates` and inspect all call sites, then run JSON retained/direct tests with `BBNF_CAPACITY_PLAN=C` to confirm no retained document identity depends on `StructuralIndex`. |
| 1A-UNK-002 | UNKNOWN | Which `runtime/src/grammars/<name>/` files are actually generator-owned versus hand-written probe code? | Run the codegen regeneration command and compare `git diff -- skinny/crates/runtime/src/grammars`; require generated-file manifest or header parity for each file. |
| 1A-UNK-003 | UNKNOWN | Is `EventCursor` absent from all live runtime/codegen paths, or did the inventory miss a renamed typed-event cursor? | Capture `rg -n "EventCursor|event cursor|TypedEvent|EventTape" skinny/crates restart` output into an audit artifact or replace all absence claims with exact cited rows. |
| 1A-UNK-004 | UNKNOWN | Are old parallel-substrate names (`OpenFrame`, `ParseStream`, `Columnar`, `SoA`, `Vec<OpenFrame>`) absent from the intended live scope? | Capture `rg -n "OpenFrame|ParseStream|Columnar|SoA|Vec<OpenFrame>" skinny/crates restart/ARCHITECTURE.md restart/locks/LOCKS.md` output with scope notes; do not use uncaptured negative search as closure evidence. |
| 1A-UNK-005 | UNKNOWN | **(SK-V14 first-cycle addition).** Does Pass Omega ratify the two-cursor (ParserState vs DirectParser) implementation as the V1 substrate-union shape, or does Lock 1 require unification into one shared typed event cursor over a single substrate before V1 freeze? | T-P3 §3C must disposition the substrate-union ratification question against SK-V14 S-P1 CH5 V3 binding evidence + 1A-SUB-016; if ratified, fold the two-cursor shape into Lock 1 and the spec §9.2 text; if not, schedule unification wave in T-P2 plan. |
| 1A-UNK-006 | UNKNOWN | **(SK-V14 first-cycle addition).** Is `DirectParser::skip_value` (`bbnf-bench/src/generated_real_typed.rs:2949`) measurable as a single grammar-neutral "substrate-walk-with-shape-validation" primitive, or are the 5/7 typed-product rank-1 attributions an envelope artefact of `parse-attribution` being OFF? | Capture one full profile pass with `parse-attribution` ON (the SK-V14 S-P2 envelope-crack), confirm whether the inner primitives surface as separate rank-1 leaves, and revise the substrate-union dispatch primitive count accordingly. |

## Lock Amendment Candidate Surfaced To 1E

| candidate | current lock/spec text | evidence | proposed amendment for 1E disposition | loc_budget | risk | wave | hard_cap | same_wave_consumer | evidence_basis |
|---|---|---|---|---|---|---|---|---|---|
| 1A-LOCK1-AMEND-001 | Lock 1 says the greenfield tape lives at `runtime/src/tape/`, typed records borrow into it, and per-grammar runtime modules emit accessors with one materialisation surface at `restart/locks/LOCKS.md:48`. | Live skinny has a measured JSON offset-tape/direct prototype; generated JSON SinkOnly is admitted direct evidence; CSS declaration-values emits an admitted fact stream with no tape at `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1`; `json/sink.rs` and `json/scan.rs` are explicitly hand-owned and not part of the generated roster (`skinny/crates/runtime/src/grammars/json/sink.rs:1`, `skinny/crates/runtime/src/grammars/json/scan.rs:1`). **SK-V14 binding (new in V6):** the substrate-union is two structurally independent cursor types at HEAD — `ParserState { cursor, tape }` at `skinny/crates/runtime/src/grammars/json/parser.rs:7-12` and `DirectParser { cursor }` (no tape) at `skinny/crates/codegen/src/json_typed_direct.rs:518-522`. | Extend Lock 1 with two clarifications: (a) non-substrate empirical fact streams may exist only as fenced skinny evidence/probes and must not be described as V1 runtime substrate closure until routed through `TapeEmit` / `DirectBuild` or explicitly excluded; (b) ratify two-cursor implementation (offset-tape cursor in retained path + bytes-only cursor in SinkOnly direct path) as the V1 substrate-union shape, OR mandate cursor unification in T-P2 wave plan. T-P3 §3C disposes. | 100-300 LOC docs/audit taxonomy; 400-900 LOC if unification is mandated | medium/high | T-P1/T-P2 lock audit; route implementation to T-P3 3C after 1E | hard cap 350 LOC for amendment/taxonomy; cap at 1,100 LOC for unification | 1E Lock 1/Lock 8 row-plane accounting and CSS audit rows in same wave; SK-V14 binding rows for 1A-SUB-016 / 1A-SUB-017 / 1A-SUB-018 | `restart/locks/LOCKS.md:48`; `skinny/REDRESS.md` SinkOnly admit; `skinny/RESULTS.md` SinkOnly + CSS fact-stream rows; `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:1`; `skinny/crates/runtime/src/grammars/json/sink.rs:1`; `skinny/crates/runtime/src/grammars/json/scan.rs:1`; `skinny/crates/runtime/src/grammars/json/parser.rs:7-12`; `skinny/crates/codegen/src/json_typed_direct.rs:518-572`; `restart/skinny/tranches/sk-v14/research/p1/hardening/HARDENING-S-P1-V3-CONSOLIDATED.md:46`; `restart/skinny/tranches/sk-v14/research/p1/p1e-hot-leaf-attribution.md:246`. |
