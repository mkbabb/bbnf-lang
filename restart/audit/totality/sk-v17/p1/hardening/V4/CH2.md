---
lens: CH2-GENERALITY
pass: T-P1-SKV17-excavation
cycle: V4
reviewed_at: 2026-05-29T19:10:00Z
subject_artefacts:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
  - restart/audit/totality/sk-v17/p1/1f-past-corpora.md
mandate: >
  Lock 14 grammar-neutrality firewall. No divergence catalogued as JSON-only
  (or CSS-only) when it is a grammar-neutral substrate fact. Excavation covers
  ALL grammars (JSON / CSS / Sheets / BBNF-self) for the value-API + NEON +
  tape surfaces (Lock 14). Every grammar-name leak in a generic crate flagged.
  JSON-empirical vs grammar-neutral separated.
live_truth_method: >
  grep -c scan_structural over crates/core/src/grammar/generated/*.rs
  (json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf=1 each, math.rs=0 — EIGHT
  wired, math field-only); math.rs OnceCell/StructuralIndex hits are doc-comments
  only (:281,:285); grammar-name leak grep over crates/simd-scan/src/*.rs +
  crates/core/src/runtime/tape/*.rs (BOTH empty of grammar names ex doc); head -2
  crates/core/src/runtime/{json,css_l4}/value.rs confirms '@generated by xtask
  regen-{json,css}; do not edit by hand.'; value_from_ref/ValueRef grep over
  crates/core/src/runtime/{json,css_l4}/ = EMPTY; skinny ValueRef def at
  tape/mod.rs:175 'ValueRef<'doc,'input,K=AnyKind,G:EventGrammar=AnyGrammar>'
  (grammar-parametric); sheets_witness dir = {event_grammar_witness.rs, mod.rs},
  BackendRule/.bbnf grep = NONE; substrate.rs grammar names at :60/:73 are
  doc-comments, live data-bind builder_path/document_path at :43/:55; all-8
  OnceCell doc-line anchors (json:686 … bbnf:4797) confirmed verbatim. No build
  mutation. Master HEAD 445925167.
sections_dispositioned: 14
accept: 13
revise: 1
reject: 0
note: >
  V3 GENERALITY closed at ≈93% with ONE residual REVISE — the 1D SK17L-008
  "+math.rs" scan-census over-count. V4 FOLDS it cleanly: 1D now strikes
  "+math.rs", restates the verified EIGHT-name wired census, and names math.rs as
  the lone field-carrying NO-scan-call generated grammar (1d:33 fold-ledger,
  1d:94 SK17L-008 row, 1d:108 split table). 1C (RT17-003/005) and the 1F triad
  already carried the accurate 8-not-9 census; they remain clean. The all-8
  census, the lazy-ValueRef<G> grammar-neutral catalogue, the by-construction
  Sheets/BBNF discrimination, and the zero-leak generic-crate firewall all hold
  live-verified. ONE NEW residual REVISE surfaces: 1A's Gaps-row (1a:126) carries
  the "value APIs are hand-written per grammar" framing — the EXACT Lock-14
  mischaracterisation V3-CH1-V3-003 charged against 1D and that 1D/1C/1F folded
  to "@generated, not hand-written"; 1A's own SUB17-004 row (1a:81) and Cross-Tree
  map (1a:94) are correct, so this is a localized Gaps-row residual, not a
  table-wide failure. The prior cycle's REVISE is resolved; one structurally
  identical Lock-14-framing residual remains in a different inventory.
---

## Executive Summary

CH2 GENERALITY firewall over the SK-V17 T-P1 cycle-V4 inventories. The single V3
GENERALITY residual REVISE — the 1D SK17L-008 "+math.rs" scan-census over-count —
is **FOLDED**. 1D V4 strikes the "+math.rs" parenthetical, restates the verified
**EIGHT-name** wired census, and names `math.rs` as the lone generated grammar
carrying the `OnceCell<StructuralIndex>` field but ZERO `scan_structural` calls
(1d:33 fold-ledger entry `V3-SK17L-008-plus-math.rs-false-ninth-carrier`; 1d:94
SK17L-008 row; 1d:16 live_truth_method `math.rs:0`; 1d:108 split-table scan row).
Live truth confirms: `grep -c scan_structural` over the eight generated grammars
returns 1 each (json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf), `math.rs`
returns **0**, and math.rs's only `OnceCell`/`StructuralIndex` hits are doc-comments
at `:281`,`:285`. The V3 REVISE is closed.

The all-eight-grammar scan census is now uniform and correct across **every**
inventory: 1A (1a:96 Cross-Tree, 1a:104 firewall, all-8 enumeration), 1C (RT17-003
spec table, RT17-005 OnceCell list, Cross-Tree scan row 1c:88), 1D (SK17L-008 1d:94
+ split table 1d:108), 1F-coherence (1f:64-65, 1f:93, 1f:116 Gaps), 1f-anti-pattern
(AP17-002 all-8 enumeration line 62), 1f-past-corpora (all-8 carrier enumeration
lines 97-105). The excavation is **NOT CSS-only and NOT JSON-only** — it spans the
full JSON / CSS / Sheets / BBNF-self matrix Lock 14 demands.

The lazy-`ValueRef<G>` generalization is correctly catalogued as grammar-NEUTRAL and
grammar-PARAMETRIC across all inventories (1A SUB17-004, 1C RT17-002, 1D SK17L-002,
1F COH17-002): live-verified, core's `crates/core/src/runtime/{json,css_l4}/value.rs`
have NO `value_from_ref`/`ValueRef` (grep EMPTY) and are eager per-grammar typed
enums, while skinny's `ValueRef<'doc,'input,K=AnyKind,G:EventGrammar=AnyGrammar>`
(`skinny/crates/runtime/src/tape/mod.rs:175`) is generic over G. The NEON
`select_classifier(&[u8;64])` and the richer core `StructuralAlphabet`
(`crates/simd-scan/src/alphabet.rs:19-37`) are both correctly catalogued as
grammar-as-data (breadth-of-config, not breadth-of-proof) — Lock 14 honoured.

The JSON-empirical-vs-grammar-neutral split (1D §lines 98-110) and the
by-construction Sheets/BBNF-self discrimination (SK17L-009, 1d:95) survive intact:
JSON is the only tape WITNESS today; CSS is the SK-V17 first-mover; `sheets_witness`
is a verified stub (dir holds only `event_grammar_witness.rs`+`mod.rs`, NO
`BackendRule`/`.bbnf`); BBNF-self has no tape witness; Sheets/BBNF generality is
by-construction, an SK-V18 fold target. This is the exact JSON-vs-neutral separation
Lock 14 protects, and it does NOT over-claim Sheets/BBNF as proven-by-exercise.

ZERO grammar-name leaks in generic crates: `crates/simd-scan/src/*.rs` and
`crates/core/src/runtime/tape/*.rs` BOTH return EMPTY on a grammar-name grep
(non-doc). The emitter `substrate.rs` grammar names are doc-comments (`:60`,`:73`);
the live functions data-bind `builder_path`/`document_path` from
`EmitStrategy::StructDirect` (`:43`,`:55`), never a grammar-name branch — Lock 14
honoured.

ONE NEW residual REVISE: 1A's Gaps-row (`1a-substrate-evidence.md:126`) reads "value
APIs are hand-written per grammar." This is the SAME Lock-14 framing defect that
V3-CH1-V3-003 charged against 1D and that 1D/1C/1F all folded to "**@generated**, not
hand-written." The "hand-written per grammar" framing implies a hand-authored
per-grammar runtime — which would be a Lock-14 violation — whereas the files are
template-emitted (`// @generated by xtask regen-{json,css}`, Lock-14 ALLOWED). 1A's
own SUB17-004 row (1a:81) and Cross-Tree Value-API row (1a:94) avoid "hand-written"
and are correct; the defect is localized to the single Gaps row. Minor framing
defect, not a firewall breach — but it is precisely the GENERALITY/Lock-14
discrimination CH2 guards, so it disposition REVISE.

## Section Dispositions

### 1d-skinny-lessons.md (V3 residual REVISE folded)

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 1 | SK17L-008 scan-census "+math.rs" (V3 REVISE target, 1d:94) | **ACCEPT** | FOLDED. The row now reads "WIRED into EXACTLY 8 generated grammars: json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf each carry exactly one `scan_structural` call … `math.rs` is the lone generated grammar carrying the `OnceCell<StructuralIndex>` index FIELD but NO `scan_structural` call (`grep -c scan_structural math.rs` = 0; the only `OnceCell`/`StructuralIndex` hits are doc-comments at `math.rs:281`,`:285`) — so the wired census is EIGHT, not nine." Live-verified exact: 8 grammars at 1 call each, math.rs=0, doc-comment-only at :281/:285. The fold-ledger (1d:33) records the V3 disposition source verbatim. The inverse-over-count from V3 is gone. ACCEPT. |
| 2 | JSON-Empirical vs Grammar-Neutral Split table (1d:98-110) | **ACCEPT** | The Lock-14 split deliverable. Five lessons split into JSON-empirical witness vs grammar-neutral durable fact, each with a generality-status column ("tape: JSON-WITNESSED; CSS is SK-V17 first-mover; Sheets/BBNF by-construction (SK-V18)"). The scan row (1d:108) now reads "core: WIRED across all 8 grammars (SK17L-008); skinny: JSON-wired, CSS is W3" — the all-8 census carried into the split, no math over-count. The "a CSS-only generator that never re-emits JSON FAILS CH2" note (1d:107) is itself a CH2-aware fence. ACCEPT. |
| 3 | SK17L-009 Sheets/BBNF-self by-construction-not-by-exercise (1d:95) | **ACCEPT** | The load-bearing generality discrimination. Live-verified: `sheets_witness` dir holds only `event_grammar_witness.rs`+`mod.rs`; `BackendRule`/`.bbnf` grep returns NONE; BBNF-self has no tape witness. Correctly catalogued: Lock 14 generality is by-construction (alphabet-as-data), proven by-exercise on JSON (tape) + CSS (SK-V17 first-mover); Sheets/BBNF proof is SK-V18. Does NOT over-claim Sheets/BBNF as proven. ACCEPT. |
| 4 | SK17L-002 value-API eager-generated vs lazy-ValueRef<G> (1d:88) | **ACCEPT** | Grammar-neutral and grammar-parametric. The cell carries the corrected V3 framing: "crates/core has per-grammar **generated** EAGER value enums (NOT hand-written — … both carry `// @generated by xtask regen-*; do not edit by hand.`)" and "EAGER-generated vs LAZY-generated, not generated-vs-hand-written … Lock-14 HONOURED." Live-verified: regen headers present, no value_from_ref in core, skinny ValueRef parametric over G. This is the model Lock-14 framing — the contrast 1A's Gaps row fails to carry. ACCEPT. |

### 1c-runtime-evidence.md

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 5 | RT17-003 + RT17-005 scan-wired all 8 / OnceCell all 8 (1c:74,76) | **ACCEPT** | Lists exactly the 8 scan-wired grammars (call-site lines json.rs:732 … google_sheets.rs:3559) and does NOT add math to the scan-wired set; RT17-005 states "`math.rs` holds the field but no scan call" and "8 wired, not 9." 1C's census is the accurate one — it never carried the V3 1D over-count. Live-verified. ACCEPT. |
| 6 | RT17-004 per-grammar runtime census + Lock-14 leak audit (1c:75) | **ACCEPT** | Census lists nine per-grammar runtime dirs (json, css_l4, bbnf, bnf, csv, ebnf, css_pretty, google_sheets, math); grammar-named symbols (`CssStructBuilder`, `CssTypedValue`, `JsonChildrenIter`, `CssChildrenIter`) live ONLY under `runtime/<g>/`, all in `@generated` files (Lock-14 ALLOWED). Generic `runtime/{tape,builder.rs,view.rs}` carry NO grammar names. The cell explicitly states the providers "are template-emitted, **not hand-written**, so they are not a Lock-14 violation." Live-verified: grammar-name grep over `crates/core/src/runtime/tape/*.rs` = EMPTY; substrate.rs grammar names are doc-comments with live data-binding. Sheets+BBNF runtime surfaces catalogued generality-clean. ACCEPT. |
| 7 | RT17-002 value-API eager vs lazy-ValueRef<G> (1c:73) | **ACCEPT** | Carries the corrected framing: "per-grammar generated providers are Lock 14 ALLOWED — the divergence is the EAGER materialization shape, not the per-grammar locus, not hand-authorship (the files are `@generated`)." Grammar-neutral, no leak, no hand-written mislabel. ACCEPT. |
| 8 | RT17-007 BackendShape runtime-clean across grammars (1c:78) | **ACCEPT** | grep-empty for all 5 shape names over `crates/core/src/runtime/` + `generated/` — runtime carries no shape leak for ANY grammar. Grammar-neutral. ACCEPT. |

### 1a-substrate-evidence.md

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 9 | SUB17-004 value-API per-grammar vs lazy-ValueRef (table row 1a:81) | **ACCEPT** | The main spec-claim table row is GENERALITY-correct: "The core value API is per-grammar eager enums, not a single lazy `ValueRef<G>` projection" — it does NOT claim hand-written; the divergence is correctly framed as the eager materialization shape, and Files-touched names "the regenerated set is per-grammar `value.rs`/`view.rs`/`document.rs` (8 grammars), regen-gated." Grammar-neutral, all-8 breadth carried. ACCEPT. |
| 10 | Cross-Tree Value-API row (1a:94) + Structural-scan row (1a:96) | **ACCEPT** | Value-API row: "per-grammar eager typed enums; NO `ValueRef`/`value_from_ref` in core (grep empty)" — no hand-written mislabel; FieldSource walk compile-time-resolved fence carried. Scan row enumerates "ALL 8 generated grammars incl `json.rs:732`, `css_l4.rs:15982`, `css_pretty.rs`, `ebnf.rs`, `bnf.rs`, `csv.rs`, `google_sheets.rs`, `bbnf.rs`" with the wiring-state-not-design-property distinction. Live-verified. ACCEPT. |
| 11 | **Gaps-row "value APIs are hand-written per grammar" (1a:126)** | **REVISE** | CH2 Lock-14-framing DEFECT. The Gaps row reads: "No `ValueRef` lazy-projection generator in crates/core; value APIs are **hand-written per grammar**." LIVE TRUTH: `head -2 crates/core/src/runtime/{json,css_l4}/value.rs` = "`// @generated by xtask regen-{json,css}; do not edit by hand.`" — the per-grammar value APIs are template-emitted, NOT hand-written. "Hand-written per grammar" implies hand-authored per-grammar runtime, which would be a Lock-14 VIOLATION; the @generated reality is Lock-14 ALLOWED. This is structurally the SAME defect V3-CH1-V3-003 charged against 1D's SK17L-002 ("the value API is already a regen output … Lock-14 HONOURED not violated") and that 1D, 1C (RT17-002/004), and 1F-coherence (COH17-002, Gaps 1f:114) all folded. 1A folded the prose elsewhere (SUB17-004 row, Cross-Tree map) but left this Gaps row stale. FIX: replace "value APIs are hand-written per grammar" with "value APIs are **`@generated` per grammar but EAGER** (`// @generated by xtask regen-{json,css}`, `json/value.rs:1` + `css_l4/value.rs:1`), not hand-written and not lazy; the divergence is the EAGER materialization shape, Lock-14 HONOURED" — mirroring 1F's already-correct Gaps row (1f:114) and 1D's (1d:146). Localized to this one row; the inventory's table + map are correct. |
| 12 | SUB17-007 alphabet config-breadth grammar-as-data (1a:84) | **ACCEPT** | Correctly grammar-neutral: "generality is config-breadth, not proof-breadth: the `quote_classes` doc comment shows the field is JSON/CSS-motivated … JSON/CSS-exercised-only … not breadth-of-proof. Both grammar-as-data (Lock 14 honoured)." Live-verified at `alphabet.rs:19-37`. ACCEPT. |
| 13 | Substrate-Union Firewall sidecar row, all-8 enumeration (1a:104) | **ACCEPT** | Enumerates the `OnceCell<StructuralIndex>` carriers across all 8 grammars (json.rs:686 … bbnf.rs); the "Verify at fold: scope to ALL 8 carriers, not json.rs alone" note is the correct generality scope. Live-verified: all-8 doc-line anchors confirmed verbatim. No JSON-only narrowing. ACCEPT. |

### 1f-coherence-scan.md / 1f-anti-pattern.md / 1f-past-corpora.md

| # | Section / row | Disposition | CH2 finding |
|---|---|---|---|
| 14 | 1f-coherence COH17-002/008 + Gaps row 1f:114/116; 1f-anti AP17-002/004 all-8 (line 62); 1f-past-corpora all-8 enumeration (97-105) + DO-NOT-CARRY-UNDERCOUNT flag (85-95) | **ACCEPT** | 1F-coherence carries the all-8 census uniformly (1f:64-65,93,116), COH17-002 + Gaps row 1f:114 correctly say "GENERATED, not hand-written (the fold target is the generator)" — the framing 1A's Gaps row misses — and COH17-008 carries the breadth-of-config-not-proof note. 1f-anti-pattern (V3-carried) AP17-002 enumerates the OnceCell sidecar across all 8 carriers (line 62) and dispositions all grammar-named constructs as Lock-14-ALLOWED per-grammar surfaces; AP17-004 grammar-name-leak row confirms generic tape carries no route strings. 1f-past-corpora (V3-carried) enumerates all-8 carriers (97-105) and the DO-NOT-CARRY-UNDERCOUNT flag (85-95) is an exemplary CH2/CH3 cross-guard. No JSON-only mis-catalogue; no uncited grammar-name leak. ACCEPT. |

## Grammar-Coverage Matrix (CH2 firewall verification)

| Grammar | scan_structural wired (core, live) | value-API in core | catalogued generality verdict | CH2 |
|---|---|---|---|---|
| JSON | YES (json.rs scan=1) | eager `JsonValue` enums, `@generated`, no `value_from_ref` | JSON-empirical WITNESS (tape proven); grammar-neutral substrate | clean |
| CSS (css_l4) | YES (css_l4.rs scan=1) | eager `CssTypedValue` (value.rs:414), `@generated` | SK-V17 first-mover; by-exercise proof | clean |
| CSS (css_pretty) | YES (css_pretty.rs scan=1) | per-grammar `@generated` | grammar-neutral scan, per-grammar runtime | clean |
| Sheets (google_sheets) | YES (google_sheets.rs scan=1) | per-grammar runtime dir | by-construction (sheets_witness stub, no BackendRule/.bbnf); SK-V18 proof | clean |
| BBNF-self (bbnf) | YES (bbnf.rs scan=1) | per-grammar runtime dir | by-construction; no tape witness; SK-V18 proof | clean |
| EBNF | YES (ebnf.rs scan=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| BNF | YES (bnf.rs scan=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| CSV | YES (csv.rs scan=1) | per-grammar `@generated` | grammar-neutral scan | clean |
| math | **NO (math.rs scan=0)** | per-grammar runtime dir | holds `OnceCell` field (doc-comment :281/:285), NOT scan-wired | correctly catalogued (1D folded) |

CH2 verifies: no value-API/NEON/tape divergence is catalogued JSON-only when it is
grammar-neutral; Sheets and BBNF-self are explicitly covered (scan-wired + correctly
flagged by-construction-not-by-exercise for the value-API proof); the generic-crate
grammar-name-leak firewall is clean. The excavation spans the full
JSON/CSS/Sheets/BBNF-self matrix per Lock 14, and the math.rs field-only-no-scan
distinction is now accurate in every inventory.

## V3-Residual Closure Status

| V3 GENERALITY disposition | V4 status |
|---|---|
| 1D SK17L-008 "+math.rs" scan-census over-count (V3 sole REVISE) | CLOSED — struck; restated verified EIGHT-name census; math named as lone field-only/no-scan grammar (1d:33,94,108); live `grep -c scan_structural math.rs`=0 confirms |
| All-8 scan census uniform across inventories (V3 ACCEPT) | HELD — 1A/1C/1D/1F all carry all-8 with per-grammar line anchors |
| lazy-ValueRef<G> grammar-neutral catalogue (V3 ACCEPT) | HELD — grep-verified core has no ValueRef; skinny parametric over G:EventGrammar |
| Sheets/BBNF by-construction discrimination, SK17L-009 (V3 ACCEPT) | HELD — sheets_witness stub re-verified (no BackendRule/.bbnf) |
| zero grammar-name leak in generic crates (V3 ACCEPT) | HELD — simd-scan + runtime/tape grep EMPTY; substrate.rs data-binds |

## CH2 Verdict

The V4 cycle closes the sole V3 GENERALITY residual cleanly: the 1D SK17L-008
"+math.rs" scan-census over-count is struck and replaced with the verified
EIGHT-name wired census plus the math.rs field-only-no-scan distinction —
live-confirmed (`grep -c scan_structural math.rs`=0, doc-comment-only at :281/:285).
The all-8 grammar scan census is uniform and correct across every inventory; the
lazy-`ValueRef<G>` generalization is catalogued grammar-neutral and
grammar-parametric; the by-construction Sheets/BBNF-self discrimination (SK17L-009)
is intact and does not over-claim; the generic crates are grammar-name-leak-clean.
The excavation spans the full JSON / CSS / Sheets / BBNF-self matrix Lock 14 demands
— not CSS-only, not JSON-only.

ONE NEW residual REVISE surfaces: 1A's Gaps-row (`1a-substrate-evidence.md:126`)
carries the "value APIs are hand-written per grammar" framing — the structurally
identical Lock-14 mischaracterisation that V3-CH1-V3-003 charged against 1D and that
1D/1C/1F all folded to "@generated, not hand-written." 1A's own SUB17-004 table row
and Cross-Tree map are correct, so the defect is localized to this single Gaps row;
the fix is a one-row restatement mirroring 1F's already-correct Gaps row (1f:114).
The "hand-written" framing matters under CH2 because it inverts the Lock-14
disposition: a hand-authored per-grammar runtime would be a Lock-14 violation,
whereas the @generated template-emitted reality is Lock-14 ALLOWED.

Counts: 14 sections — 13 ACCEPT, 1 REVISE, 0 REJECT (≈93% ACCEPT). The prior cycle's
REVISE is resolved; one new structurally-identical Lock-14-framing residual remains
in a different inventory (1A Gaps row). Materially the GENERALITY axis is convergent
— the substantive firewall (all-8 census, value-API neutrality, by-construction
Sheets/BBNF, leak-clean generic crates) holds live-verified. One targeted V5 fold
(restate 1A:126 to "@generated per grammar but EAGER, not hand-written") closes the
≥95% gate. NOTE: this is the SECOND consecutive cycle with exactly one
Lock-14-framing REVISE of identical class (V3: 1D hand-written/math; V4: 1A
hand-written) — the orchestrator should ensure the cross-inventory "@generated not
hand-written" framing is swept across ALL Gaps rows in the V5 fold, not patched
one inventory at a time, lest a third instance surface.
