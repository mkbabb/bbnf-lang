# S-P2 V1 CH4 Cost

Disposition: ACCEPT.

Each candidate includes scalar-reference status and the needed parity level:

- existing checkasm parity: byte-class table, byte-class eq-set, bulk emit,
  structural terminator, prefix XOR, next-set-bit, escape mask, EOB clamp,
  scan parity, UTF-8/Unicode x4;
- existing scalar or smoke-only status: DotProd digit MAC, tape/view scalar
  operations, materialization ratio reporting;
- no parity needed: scalar-only tape cursor and ratio-report candidates.

`p2b-dav1d-process.md` adds the S-P3 admission checklist: P1 row/hot leaf,
scalar oracle, strict `primitive-checkasm`, same-wave consumer, Lock 1
retention lifetime, Lock 14 policy owner, REDRESS non-reopen proof, and cold
same-workload measurement plan.

No SIMD microbench or ISA availability is treated as row admission.
