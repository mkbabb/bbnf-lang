# T-P2 V2 CH5 Hidden Coupling / Lock 1

Pass: T-P2 Research. Cycle: V2 (SK-V14 totality re-cohort, confirming pass).
Lens: CH5 hidden coupling / Lock 1 substrate union.
Date: 2026-05-23. HEAD at dispatch: `b5628414f` (orchestrator HEAD now
`4f17880d0` after V2 CHALLENGE-CONTEXT commit; dossier contents stable since
the b5628414f atomic micro-fold).
Scope: `restart/audit/totality/p2/{2A-sota-landscape, 2B-primitive-vocabulary,
2C-grammar-neutrality, 2D-cost-model, 2E-host-arch-esoterica,
2F-parse-that-gaps}.md` at V2 atomic micro-fold.

## §0 — Verdict

**ACCEPT (5/6 dossiers); ACCEPT-WITH-FOLD (1/6 dossier, 2B per-row gap).**

Per-dossier ACCEPT rate: 5 ACCEPT + 1 ACCEPT-WITH-FOLD = **6/6 = 100%
ACCEPT-class** for substrate-union; the single residual fold (F-CH5-V2-01,
2B per-primitive `substrate_target`/`retention_lifetime`/`policy_owner`
columns missing from §A5 audit table and §A6 ledger) is a tightening fold,
not a refutation gap. The cohort-wide V2-FOLD §"Lock 1 / Substrate-Kind
Contract" (`T-P2-V2-FOLD-ADDENDUM.md:77-97`) carries the binding manifest
explicitly:

> "substrate_target = local_temp_only | existing_tape | direct_sink |
> admitted_fact_output / retention_lifetime = local_loop | generated_function
> | output_row / policy_owner = generated_grammar | caller_data | none / Any
> route requiring retained class/mask streams, parser-owned cursor/list
> state, public substrate API, UnionTape, or a second tape is rejected
> unless the user amends Lock 1."

The substrate-union invariant **HOLDS cohort-wide at V2** (target §3Z
predicate). V1's single REVISE (2D LAC-2D-06 admission-predicate hardening)
is fully discharged at V2; the dossier prose now explicitly binds the
`admits_collapsed_stage` predicate refinement, the `substrate_target`
declaration on every `BackendExpr` node / rewrite guard / extraction result,
and the four admitted substrate values per Lock 1 v+1. The V1
**T2A-LAC-V1-01** transient-projection clause (2A `:190`), **LAC-2D-06**
(2D `:243`), **LAC-2E-04** (2E `:419`), **LAC-2F-V5-02** elevation
(2F `:490`), and the per-cycle V2 fold contract (`T-P2-V2-FOLD-ADDENDUM.md
:77-97`) form a closed, cross-referencing substrate-union ledger.

The four V1 load-bearing refutations remain intact at V2:

1. **T2A-REF-001** (`2A:136`) — simdjson stage-1/stage-2 is *producer-
   consumer in one pipeline*, not retained dual substrate. UNCHANGED.
2. **T2D-COLLAPSEDSTAGE-X86-ONLY** (`2D:123`) — x86-only literature with
   per-V2 predicate-bind disposition; LAC-2D-06 (`2D:243`) refines the live
   predicate to co-require `target.arch == x86` and binds every
   `BackendExpr` node to declare `substrate_target`.
3. **PTG-PREV-IN-STRING-LOCK1** (`2F:153`) — cross-call retained quote-mask
   inadmissible; LAC-2F-V5-02 (`2F:490`) elevated to T-P3 §3C amendment
   surface with generalisation to **"no cross-call retained classifier
   state"** (quote-mask, escape-mask, structural-mask, class-stream,
   prev-state byte, prefix-XOR carry word).
4. **k-mask collapse rule** (`2E:264`) — AVX-512 k-mask registers are
   parallel substrate at the ISA level; abstract primitive vocabulary must
   collapse k-mask operations to "ephemeral mask in local loop"
   (`local_temp_only`, `local_loop`).

## §1 — Per-dossier disposition table (V2)

| dossier | V1 disposition | V2 disposition | rationale (CH5 read) |
|---|---|---|---|
| **2A SOTA landscape** | ACCEPT | **ACCEPT** | T2A-REF-001 (`2A:136`) + T2A-LAC-V1-01 (`2A:190`) carry the substrate-union invariant cleanly: stage-1 is producer; stage-2 is consumer; the structural index is not a retained sidecar. CH5 cell of V2 lens overlay (`2A:100`) explicitly binds the constraint. REDRESS 96/97/98 pre-blocks honored. T2A-REF-005 (`2A:140`) demotes simdjson DOM / yyjson default / asmjson AVX-512 historical sidecar columns to flaw-probe status (not gate anchors). The Per-Technique Transfer Coverage table (`2A:161-162`) classifies T2A-SOTA-001 + T2A-SOTA-002 as NOT-VALIDATED for CSS / Sheets / BBNF-self until 1A-DIV-008 closed + per-grammar structural-alphabet consumer wired. Zero CH5 drift. |
| **2B primitive vocabulary** | ACCEPT-WITH-FOLD | **ACCEPT-WITH-FOLD** | Two-layer dependency mechanically one-directional (`2B:153-166`); admission discipline requires same-wave consumer (§A4 `:168-198`). LAC-2B-03 (`2B:500`) carries `policy_owner` as required column with three admitted values (`generated_grammar`/`caller_data`/`none`). V2 SKELETON triple DELETE disposition (rows 7-9 at `:299-301`) is honest "ship-or-delete" per CH7. **Residual fold (F-CH5-V2-01):** the §A5 audit table (`:223-233`) and §A6 ledger (`:291-301`) do NOT yet add per-row `substrate_target` / `retention_lifetime` columns; the cohort-wide V2-FOLD §Lock 1 contract (`T-P2-V2-FOLD-ADDENDUM.md:77-97`) binds the manifest globally, and `policy_owner` is per-row via LAC-2B-03, but a future reader scanning the per-primitive table alone could miss the `local_temp_only` lifetime declaration. Tightening, not refutation. |
| **2C grammar neutrality** | ACCEPT-WITH-FOLD | **ACCEPT** | The Abstract-Primitive-Lift Table (`2C:135-148`) classifies grammar-neutral vs JSON-only-by-shape primitives cleanly. F10 BBNF-self FIRMED ADMITTED-VIA-C4-W10 (`2C:301`) eliminates any soft-deferral. The HEAD `OffsetFlags` partial repair to `GRAMMAR_BIT0`/`GRAMMAR_BIT1` (verified at `runtime/src/tape/mod.rs:22-23`) is correctly classified as partial. **CH5 fact-stream output-plane boundary** (V2 fold focus declared at `:19`) is discharged by 2C-COST-MODEL-PER-GRAMMAR-SELECTION (`:124`) which cites the V3 fold's per-candidate `substrate_target`/`retention_lifetime`/`policy_owner` triple, plus 2C-CSS-FACT-STREAM grounded row (`:122`) classifying CSS fact-stream products as legitimate non-JSON evidence (output plane, not retained `OffsetTape`/`EventTape`). LAC-2C-03 (`:448`) institutes the generated grammar-owned sink/fact/value/flag surface. |
| **2D cost model** | **REVISE** | **ACCEPT** | The V1 single REVISE is fully discharged. **LAC-2D-06** (`2D:243`) is the V2 fold packet's load-bearing disposition: (a) refines `admits_collapsed_stage` predicate at `passes/src/lib.rs:874-876` to co-require `target.arch == x86`; (b) binds every `BackendExpr` node / rewrite guard / extraction result to declare `substrate_target ∈ {local_temp_only, existing_tape, direct_sink, admitted_fact_output}` per Lock 1 v+1; (c) e-graph extraction MUST reject plans whose `substrate_target` is not one of the four admitted values; (d) adopts T2A-LAC-V1-05 abrogate caps. T2D-COLLAPSEDSTAGE-X86-ONLY row (`:123`) carries the predicate-leak diagnosis verbatim. BackendShape Admission Ledger (`:148`) marks `CollapsedStage` as "NOT ADMITTED + PREDICATE HARDENING REQUIRED" with explicit F-CH5-V1-03 fold debt. UNKNOWN-2D-05 (`:231`) gated on 2E source-backed candidate. The dossier-level CH5 disposition is now COMPLETE. |
| **2E host-arch esoterica** | ACCEPT-WITH-FOLD | **ACCEPT** | The V7 Lock 16 manifest (`2E:236-247`) carries hardware gate + abstract primitive name + close state per row. The k-mask collapse rule (`2E:264`) is the load-bearing CH5 row: "k-mask registers are a parallel substrate that simulates a sidecar at the ISA level; primitive-vocabulary abstraction must collapse k-mask operations to 'ephemeral mask in local loop' (Lock 1: `local_temp_only`, `local_loop`)". V7 elevated all `not_S-P3-eligible_at_V1` candidates (LD4, BCAX, ASCII run-skip, cache hints, CRC32C) with explicit eligibility predicates rather than open-ended deferral (CH6 anti-paper-close discipline). C-P2C-2 PMULL+CSSC route gated by REDRESS 88/89/96/97/98 material-differential checklist (LAC-2E-04 at `:419`). 5 URL refreshes complete (Langdale movemask, etc.). Audit_state vocabulary normalised across 22 entries. |
| **2F parse-that gaps** | ACCEPT-WITH-FOLD | **ACCEPT** | V6 admission ledger (`2F:473-483`) carries per-candidate `substrate_target` / `retention_lifetime` / `policy_owner` per row, with `bbnf_regex_hir_engine` bound to `local_temp_only (compile-time facts) / generated_function / generated_grammar` and `regex_lazy_dfa_fallback` bound to `local_temp_only (build-script only) / N/A / N/A`. **LAC-2F-V5-02** (`2F:490`) is now elevated to **T-P3 §3C Lock 1 substrate-union v+1 amendment surface** with binding contract: **"no cross-call retained classifier state (quote-mask, escape-mask, structural-mask, class-stream, prev-state byte, or any prefix-XOR carry word) is admissible under Lock 1 substrate-union; carry MUST stay within a single chunk-call boundary"** — this is the F-CH5-V1-04 elevation discharge. F-CH5-V1-05 (compile-time vs runtime as Lock 1 manifest field) is partially discharged via the per-row admission ledger columns plus LAC-2F-V5-01 + T2F-LOCK-AMEND-001 at `2F:242` ("compile-time HIR is build-script-only; runtime DFA fallback must NOT pull `regex-automata` into runtime"); a formal `crate_target` ∈ {compile_time_only, build_script_vendor, runtime_layer1, runtime_consumer} field is not yet a Lock 1 manifest column but the per-row ledger discipline binds the same constraint. |

ACCEPT-class total: **6/6 = 100%**.
REVISE total: **0/6 = 0%**.
REJECT total: **0/6 = 0%**.

## §2 — V2 dispatch focus verification

The V2 CHALLENGE-CONTEXT §2 binds two specific verification targets:

### 2.1 — LAC-2D-06 predicate co-require `target.arch == x86`

VERIFIED. `2D-cost-model.md:243` LAC-2D-06 explicit text:

> "Refine the live `admits_collapsed_stage` predicate at
> `skinny/crates/passes/src/lib.rs:874-876` to co-require `target.arch ==
> x86` alongside `target.avx512bw` and `Entry(_)`, refusing aarch64 admission
> via cross-build `target.avx512bw` inheritance. Bind every `BackendExpr`
> node / rewrite guard / extraction result to declare `substrate_target ∈
> {local_temp_only, existing_tape, direct_sink, admitted_fact_output}` per
> Lock 1 v+1 substrate-union manifest; e-graph extraction MUST reject plans
> whose `substrate_target` is not one of the four admitted values."

The dossier-level discharge of F-CH5-V1-03 is COMPLETE. Live-code state at
`skinny/crates/passes/src/lib.rs:874-876` at HEAD still reads the
pre-hardening form:

```rust
fn admits_collapsed_stage(rule_ir: &BackendRule, target: TargetFeatures) -> bool {
    target.avx512bw && matches!(rule_ir.expr, BackendExpr::Entry(_))
}
```

This is **expected** at V2 cycle: T-P2 is research-pass dispatch, not code-
landing dispatch. LAC-2D-06 is the binding research contract for T-P3 §3C
amendment authoring and the SK-V14 SPEC §15 implementation wave to consume.
The BackendShape Admission Ledger (`2D:148`) explicitly marks the predicate
as "PREDICATE HARDENING REQUIRED" with `CH4-F2 + CH5 F-CH5-V1-03 fold debt`
as the open work item. The dispatch contract HOLDS at V2.

### 2.2 — Per-`BackendExpr` `substrate_target` declaration

VERIFIED. LAC-2D-06 (`2D:243`) binds every `BackendExpr` node / rewrite
guard / extraction result to declare one of the four admitted substrate
values. The cost-model architecture table (`2D:179` egg row) updates the
"required generated facts" column to explicitly include "`substrate_target`
declaration per node (per F-CH5-V1-03)". The "Architectural Assertions
Refuted" row at `2D:214` carries the binding triple: predicate refinement +
generated aarch64 strategy + per-`BackendExpr` `substrate_target`
declaration. The discharge is dossier-level COMPLETE.

### 2.3 — LAC-2F-V5-02 elevation: no cross-call retained classifier state

VERIFIED. `2F-parse-that-gaps.md:490` LAC-2F-V5-02 V6 elevation text:

> "**V6 elevation (F-CH5-V1-04):** this LAC is no longer a per-cycle
> amendment candidate — it is promoted to a **T-P3 §3C Lock 1 substrate-
> union v+1 amendment surface** that T-P3 must emit verbatim as a permanent
> manifest entry. Binding contract: **no cross-call retained classifier
> state** (quote-mask, escape-mask, structural-mask, class-stream, prev-
> state byte, or any prefix-XOR carry word) is admissible under Lock 1
> substrate-union; carry MUST stay within a single chunk-call boundary.
> Generalization: the closure of REDRESS 96 / 97 / 98 ... extends to ALL
> transient classifier-state primitives, not just the three falsified
> shapes."

The elevation discharges F-CH5-V1-04 cleanly. The T-P3 §3C amendment
surface is named. The generalisation expands the V1 falsification scope from
3 shapes (retained class-column, streaming structural cursor, class-lane-
only) to ALL transient classifier-state primitives. PTG-PREV-IN-STRING-LOCK1
(`2F:153`) refutes the simdjson `find_quote_mask_and_bits` cross-call
retained-quote-mask design and grounds the per-call composed form as the
admissible substrate-union-compatible primitive. The discharge is COMPLETE.

### 2.4 — Substrate-union invariant cohort-wide

VERIFIED. Cohort-wide scan at V2 HEAD:

| dossier | substrate-union anchor | citation |
|---|---|---|
| 2A | T2A-LAC-V1-01 transient-projection clause + T2A-REF-001 producer-consumer refutation | `:190` + `:136` |
| 2B | LAC-2B-03 `policy_owner` per Layer-1 call site; cohort-wide V2-FOLD §Lock 1 contract | `:500` + `T-P2-V2-FOLD-ADDENDUM.md:77-97` |
| 2C | 2C-COST-MODEL-PER-GRAMMAR-SELECTION cites per-candidate `substrate_target`/`retention_lifetime`/`policy_owner`; CSS fact-stream output-plane row grounded | `:124` + `:122` |
| 2D | LAC-2D-06 predicate hardening + per-`BackendExpr` substrate_target declaration + abrogate caps | `:243` |
| 2E | k-mask collapse rule + Lock 16 manifest hardware gate column + LAC-2E-04 PMULL/CSSC reopen gate with REDRESS 88/89/96/97/98 | `:264` + `:236-247` |
| 2F | LAC-2F-V5-02 elevation to T-P3 §3C amendment surface + per-row admission ledger | `:490` + `:473-483` |

**Substrate-union invariant HOLDS cohort-wide at V2.** No dossier grounds a
design that would require a retained parallel substrate, sidecar producer,
hidden policy coupling, or Lock 1 substrate-union violation.

## §3 — CH5 lens checks (V2)

The four hidden-coupling failure modes re-scanned at V2 HEAD:

| failure mode | V2 dossier exposure | verdict |
|---|---|---|
| Parallel substrate (a second tape / cursor / class-column stream) | T2A-REF-001 + T2A-LAC-V1-01 (2A); R1 refutation of pixel-domain kernels + SKELETON triple DELETE (2B); CSS fact-stream output-plane row (2C); `CollapsedStage` transient-only + LAC-2D-06 BackendExpr `substrate_target` binding (2D); k-mask substrate-leak callout + Lock 16 hardware gate column (2E); LAC-2F-V5-02 elevated cross-call retention refutation (2F) | **PASS** — no dossier grounds a parallel substrate at V2; all retention-class refutations carry REDRESS 96/97/98 anchors; the V2-FOLD §Lock 1 contract binds the four admitted substrate values cohort-wide |
| Sidecar producer (output retained for cross-call consumption rather than folded into same loop) | Lock 16 admission discipline same-wave consumer required (2B `:180-183`); orphan-kernel research refused (2A T2A-REF-004 at `:113`); SK-V12 W2 escape-mask parity-only / W4 delimiter-find microbench-only halted (2A + 2E); LAC-2F-V5-02 generalised refutation of ALL transient classifier-state primitives | **PASS** — every dossier refuses sidecar producers; same-wave consumer is hard precondition; cross-call classifier-state retention is REJECT under Lock 1 v+1 without further measurement |
| Hidden policy coupling (shared crates inheriting grammar-specific constants) | LAC-2B-03 `policy_owner` field forbids hardcoded JSON constants in shared call sites (`2B:500`); JSON role mining refuted as fleet-wide (2C `:135-148`); `JsonSink` refuted as generic sink (2C `:323`, `:329`); Layer 1 macros grammar-neutral with per-grammar LUTs in codegen-emitted `.data` (2B `:148-151`); 2D `egg` row binds `substrate_target` declaration per node and prohibits leaking grammar-name into node constructors (Lock 14 v+1 fault) | **PASS** — grammar policy mechanically owned by generated/caller-data sources; LAC-2D-06 e-graph extraction MUST reject plans not declaring substrate_target; shared crates do not inherit grammar-specific constants |
| Lock 1 substrate-union violation (new public substrate / `UnionTape` route) | REDRESS 96/97/98 cited as load-bearing pre-block across 2A/2D/2F/2E; T2A-LAC-V1-01 transient-projection clause; LAC-2D-05 material-differential clause for union-shape search (`2D:241`); LAC-2D-06 abrogate caps adopted (`2D:243`); LAC-2F-V5-02 elevated; LAC-2E-04 PMULL/CSSC Union-C reopen gate with REDRESS 88/89/96-98 (`2E:419`); the cohort-wide V2-FOLD §Union/PMULL/CSSC Material-Differential Checklist (`T-P2-V2-FOLD-ADDENDUM.md:120-136`) enumerates 7 items binding ALL union/PMULL/CSSC routes | **PASS** — no dossier proposes a new public substrate; the REDRESS pre-block ledger is honored cohort-wide; the union/PMULL/CSSC checklist is binding |

All four CH5 failure modes PASS at V2. The substrate-union invariant
**HOLDS cohort-wide**.

## §4 — Fold requirements (V2 → V3)

V2 closes 4 of the V1 5 fold items cleanly. One residual tightening fold
remains for V3:

### F-CH5-V2-01 — Per-primitive substrate columns in 2B §A5 + §A6 tables

The cohort-wide V2-FOLD §Lock 1 contract (`T-P2-V2-FOLD-ADDENDUM.md:77-97`)
binds the manifest globally. LAC-2B-03 (`2B:500`) makes `policy_owner` a
required field per Layer-1 consumer call site. But the 2B §A5 audit table
(`:223-233`) and §A6 adoption-cost ledger (`:291-301`) do NOT yet add
per-row `substrate_target` / `retention_lifetime` columns. A future reader
scanning the per-primitive table alone could miss the `local_temp_only`
lifetime declaration for `BITMAP_PREFIX_XOR_64`, `BITMAP_NEXT_SET_BIT`, or
`BULK_EMIT_COMPRESSED`.

V3 fold: add per-row `substrate_target` (`local_temp_only` for primitives
1-6) and `retention_lifetime` (`local_loop` for all 6) columns to 2B §A5 and
§A6 tables. The values are already mechanically determined by the cohort-
wide contract; the tightening is to make them explicit per-row so the
per-primitive table is self-contained.

### F-CH5-V2-02 — `crate_target` Lock 1 manifest field elevation in 2F

V1 F-CH5-V1-05 (compile-time vs runtime split as Lock 1 manifest
requirement) is partially discharged at V2 via:
- per-row admission ledger columns (`2F:473-483`) carrying
  `local_temp_only (compile-time facts)` / `local_temp_only (build-script
  only)` for `bbnf_regex_hir_engine` and `regex_lazy_dfa_fallback`;
- LAC-2F-V5-01 + T2F-LOCK-AMEND-001 (`2F:242`) binding "compile-time HIR
  is build-script-only; runtime DFA fallback must NOT pull `regex-automata`
  into runtime".

But a formal `crate_target` ∈ {`compile_time_only`, `build_script_vendor`,
`runtime_layer1`, `runtime_consumer`} field is not yet a Lock 1 manifest
column. V3 fold: elevate `crate_target` to a manifest column so any future
regex/scanner/byte-class crate admission must declare which side of the
compile-time / runtime split it lives on.

### F-CH5-V2-03 — V3 confirming pass §3Z lock predicate

If F-CH5-V2-01 and F-CH5-V2-02 fold cleanly at V3, CH5 closes at ≥95%
ACCEPT-class for both V2 and V3 — satisfying §3Z's two-consecutive-cycle
LOCK requirement. The V2 100% ACCEPT-class rate provides the first half of
the LOCK predicate.

## §5 — Cycle disposition for T-P2 V2 CH5

CH5 disposition for T-P2 V2: **ACCEPT (5/6) + ACCEPT-WITH-FOLD (2B, 1/6) =
100% ACCEPT-class** (above the §3Z ≥95% first-pass target).

The substrate-union invariant HOLDS cohort-wide at V2: every grounded
primitive declares (via the V2-FOLD §Lock 1 contract + per-dossier LACs)
one of `local_temp_only`, `existing_tape`, `direct_sink`, or
`admitted_fact_output`. The REDRESS 96/97/98 ledger is honored. The
simdjson stage-1/stage-2 architecture is correctly grounded as producer-
consumer in one loop, not retained sidecar. The 2B Layer 0/Layer 1
dependency is mechanically one-directional. The `prev_in_string` refutation
is elevated to a T-P3 §3C Lock 1 substrate-union v+1 amendment surface with
binding contract over ALL cross-call retained classifier state. The 2D
`admits_collapsed_stage` predicate-leak diagnosis is fully bound at the
dossier level (LAC-2D-06) with the per-`BackendExpr` `substrate_target`
declaration requirement.

The V1 single REVISE (2D) is fully discharged. The 2B partial fold (per-
primitive substrate column annotation) is tightening, not refutation.

If F-CH5-V2-01 + F-CH5-V2-02 fold cleanly at V3, CH5 expects to accept V3
at 100% ACCEPT-class and discharge the §3Z LOCK predicate (≥95% × 2
consecutive cycles).

## §6 — Findings summary

| finding-id | dossier | finding | severity | fold |
|---|---|---|---|---|
| CH5-V2-F01 | 2A | T2A-REF-001 (`:136`) + T2A-LAC-V1-01 (`:190`) carry substrate-union invariant cleanly; Per-Technique Transfer Coverage table classifies SOTA techniques NOT-VALIDATED for CSS/Sheets/BBNF-self pending 1A-DIV-008 closure | LOAD-BEARING | accept; surface T2A-LAC-V1-01 to T-P3 §3C verbatim |
| CH5-V2-F02 | 2B | Layer 0/Layer 1 dependency mechanically one-directional with cross-grep proof (`:153-166`); SKELETON triple DELETE (rows 7-9 at `:299-301`); `policy_owner` per-call-site per LAC-2B-03 (`:500`); per-primitive `substrate_target` columns missing from §A5/§A6 tables (cohort-wide V2-FOLD contract binds globally) | TIGHTENING | F-CH5-V2-01 (V3) |
| CH5-V2-F03 | 2C | Abstract-Primitive-Lift table (`:135-148`) cleanly separates grammar-neutral vs JSON-only-by-shape; 2C-COST-MODEL-PER-GRAMMAR-SELECTION (`:124`) binds per-candidate substrate triple; 2C-CSS-FACT-STREAM grounded (`:122`); F10 BBNF-self FIRMED ADMITTED-VIA-C4-W10 (`:301`) | ACCEPT | discharged |
| CH5-V2-F04 | 2D | **V1 REVISE FULLY DISCHARGED.** LAC-2D-06 (`:243`) refines `admits_collapsed_stage` predicate to co-require `target.arch == x86`; binds every `BackendExpr` node / rewrite guard / extraction result to declare `substrate_target ∈ {local_temp_only, existing_tape, direct_sink, admitted_fact_output}`; e-graph extraction MUST reject plans not declaring one of the four; T2A-LAC-V1-05 abrogate caps adopted | LOAD-BEARING ACCEPT | discharged at V2 |
| CH5-V2-F05 | 2E | V7 Lock 16 manifest carries hardware gate + abstract primitive + close state per row; k-mask collapse rule (`:264`) names ISA-level parallel substrate that must collapse to `local_temp_only`/`local_loop`; LAC-2E-04 PMULL/CSSC reopen gate (`:419`) | ACCEPT | discharged; surface k-mask collapse rule to T-P3 §3C |
| CH5-V2-F06 | 2F | LAC-2F-V5-02 (`:490`) elevated to T-P3 §3C Lock 1 substrate-union v+1 amendment surface with binding contract: "no cross-call retained classifier state ... admissible under Lock 1 substrate-union; carry MUST stay within a single chunk-call boundary"; generalisation from 3 falsified shapes to ALL transient classifier-state primitives | LOAD-BEARING | discharged at V6 elevation |
| CH5-V2-F07 | 2F | V6 admission ledger (`:473-483`) carries per-candidate `substrate_target`/`retention_lifetime`/`policy_owner`; compile-time vs runtime split bound per-row via `bbnf_regex_hir_engine` (`local_temp_only (compile-time facts)`) and `regex_lazy_dfa_fallback` (`local_temp_only (build-script only)`); formal `crate_target` Lock 1 manifest column not yet elevated | TIGHTENING | F-CH5-V2-02 (V3) |

ACCEPT (5): F01, F03, F04, F05, F06.
ACCEPT-WITH-FOLD (2): F02, F07 (both tightening).

CH5 V2 cycle disposition: **ACCEPT-class 100% (6/6 dossiers)**. Two tightening
folds for V3 (F-CH5-V2-01 per-primitive columns in 2B; F-CH5-V2-02
`crate_target` manifest column elevation in 2F). The substrate-union
invariant HOLDS cohort-wide; the V1 single REVISE is fully discharged;
the §3Z first-pass target (≥95%) is satisfied.

## §7 — V1 → V2 disposition comparison

| V1 finding | V1 verdict | V2 disposition | V2 verdict |
|---|---|---|---|
| CH5-V1-F01 (2A LAC-V1-01) | LOAD-BEARING | UNCHANGED; surface to T-P3 | ACCEPT |
| CH5-V1-F02 (2B per-primitive substrate_target) | TIGHTENING | Cohort-wide V2-FOLD §Lock 1 contract; LAC-2B-03 policy_owner per-row; per-primitive substrate_target columns partial | ACCEPT-WITH-FOLD (F-CH5-V2-01) |
| CH5-V1-F03 (2C fact-stream manifest field) | TIGHTENING | 2C-COST-MODEL-PER-GRAMMAR-SELECTION cites per-candidate triple; 2C-CSS-FACT-STREAM grounded | ACCEPT |
| CH5-V1-F04 (2D admits_collapsed_stage predicate) | LOAD-BEARING REVISE | LAC-2D-06 fully binds predicate + BackendExpr substrate_target + abrogate caps | ACCEPT (V1 REVISE DISCHARGED) |
| CH5-V1-F05 (2E k-mask collapse rule) | ACCEPT | k-mask collapse rule at `2E:264`; surface to T-P3 | ACCEPT |
| CH5-V1-F06 (2F prev_in_string refutation) | LOAD-BEARING | LAC-2F-V5-02 V6 elevation to T-P3 §3C amendment surface with generalised binding contract | ACCEPT |
| CH5-V1-F07 (2F compile-time vs runtime split) | TIGHTENING | Per-row admission ledger discipline binds same constraint; formal `crate_target` manifest column not yet elevated | ACCEPT-WITH-FOLD (F-CH5-V2-02) |

V1 → V2 movement: **1 REVISE → ACCEPT (2D LAC-2D-06 discharge); 2 ACCEPT-
WITH-FOLD → ACCEPT (2C F03, 2E F05); 1 LOAD-BEARING TIGHTENING → ACCEPT
(2F F06 V6 elevation); 2 ACCEPT → ACCEPT (2A F01, V1 LOAD-BEARING refs);
2 TIGHTENING → ACCEPT-WITH-FOLD (2B F02 + 2F F07 residual).** Net:
ACCEPT-class **83% (V1) → 100% (V2)**.

V2 → V3 expected trajectory: if F-CH5-V2-01 + F-CH5-V2-02 fold, all 7
findings ACCEPT, satisfying §3Z LOCK predicate's two-consecutive-cycle
≥95% requirement.
