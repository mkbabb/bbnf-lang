# T-P2 V2 CH3 Regression / REDRESS Review

Pass: T-P2 Totality Research.
Cycle: V2.
Lens: CH3 regression / REDRESS.
Agent: CH3.
Date: 2026-05-21.
Ownership: `restart/audit/totality/p2/hardening/V2/CH3.md` only.

## Verdict

ACCEPT.

V2 resolves the CH3 REDRESS discipline defects from V1. The shared V2 fold
addendum converts the prior broad reopen language into mechanical route gates:
REDRESS-119 is reopened only through a row-specific matrix, Union/PMULL/CSSC
labels are blocked unless a material-differential checklist passes, REDRESS
121-127 are classified by prerequisite/comparator/admission state, and proof-only
or fixpoint history is carried as evidence rather than close authority.

## Findings

1. REDRESS-119 row-specific reopening is now mechanical. The addendum covers all
   13 residual direct rows (`twitter`, `canada`, `github_events`,
   `update_center`, `mesh`, `random`, `gsoc-2018`, `instruments`, `numbers`,
   `unicode_mixed`, `unicode_escapes`, `distinct_values`,
   `y_string_unicode`) and gives each row a prior proof, proposed material
   differential, same-wave consumer, strict comparator/oracle, and old-route
   block condition (`restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md:99`).
   2A carries that rule forward by stating that direct rows remain eligible only
   through the addendum matrix and that "decision engine", "union", or "ASM" is
   not itself a material differential
   (`restart/audit/totality/p2/2A-sota-landscape.md:100`,
   `restart/audit/totality/p2/2A-sota-landscape.md:107`).

2. Union/PMULL/CSSC replay is blocked unless a named material differential
   exists. The addendum checklist requires citing REDRESS 88/89/96/97/98,
   forbids global replacement of `bitmap_prefix_xor_64` or
   `bitmap_next_set_bit`, forbids retained class-column/cursor/sidecar/
   `UnionTape` replay, requires the named row consumer to delete or bypass the
   old scalar cost source, and requires scalar/checkasm/microbench plus strict
   row gate and rollback criteria
   (`restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md:120`). 2B and 2E repeat
   the same pre-shortlist fence for `Union-C`, `PMULL+CSSC`, and
   `SIMD-first union` routes
   (`restart/audit/totality/p2/2B-primitive-vocabulary.md:224`,
   `restart/audit/totality/p2/2E-host-arch-esoterica.md:166`).

3. REDRESS 121-127 are no longer ambiguous close shorthand. The addendum
   classifies 121 as a partial Lock 14 prerequisite, 122 as prerequisite-only,
   123 as scaffold-only, 124 as comparator infrastructure, 125 as a candidate
   admitted only through 127, 126 as a route-production split, and 127 as one
   CSS row admit with union/ASM still routed
   (`restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md:138`). 2A and 2B mirror
   this taxonomy and keep escape-mask correctness and ASCII run-skip microbench
   evidence out of production admission claims
   (`restart/audit/totality/p2/2A-sota-landscape.md:107`,
   `restart/audit/totality/p2/2B-primitive-vocabulary.md:209`).

4. Prior fixpoint history is not used as a close excuse. The skinny ledger says
   REDRESS 119 was a measured fixpoint, not direct `GO`, with no source
   intervention or row movement (`skinny/REDRESS.md:3495`,
   `skinny/REDRESS.md:3524`), and REDRESS 120 keeps those 13 rows exhausted
   unless a future pass names a material differential with fresh profile and
   micro-proof evidence (`skinny/REDRESS.md:3542`,
   `skinny/REDRESS.md:3549`). V2 honors that split: 2A treats REDRESS-119 as
   history-only under the addendum matrix, while 2F says REDRESS 119/120 cannot
   close by changing comparator meaning
   (`restart/audit/totality/p2/2A-sota-landscape.md:100`,
   `restart/audit/totality/p2/2F-parse-that-gaps.md:239`).

5. I found no V2 route reopened without a named material-differential gate.
   Candidate reopen language in 2A, 2B, 2D, 2E, and 2F is consistently framed as
   source-backed, conditional, micro-proven, or architecture pressure until a
   row-local consumer, strict oracle, substrate/retention fields, and measured
   row movement or architectural block exist
   (`restart/audit/totality/p2/2B-primitive-vocabulary.md:123`,
   `restart/audit/totality/p2/2D-cost-model.md:43`,
   `restart/audit/totality/p2/2E-host-arch-esoterica.md:108`,
   `restart/audit/totality/p2/2F-parse-that-gaps.md:198`).

## Required Redress If Any

None for CH3. V2 is safe to advance from the regression/REDRESS lens.

## Evidence Checked

- `restart/audit/totality/p2/T-P2-V2-FOLD-ADDENDUM.md`: provenance register,
  REDRESS-119 matrix, Union/PMULL/CSSC checklist, REDRESS 121-127 taxonomy,
  admission ledger, source-present orphan states, and abrogate gates.
- `restart/audit/totality/p2/2A-sota-landscape.md`: SOTA reopen framing,
  REDRESS carry-forward, and candidate admission states.
- `restart/audit/totality/p2/2B-primitive-vocabulary.md`: primitive ledger,
  REDRESS taxonomy, orphan-state normalization, and material-differential
  checklist.
- `restart/audit/totality/p2/2C-grammar-neutrality.md`: same-wave consumer and
  primitive-admission posture, plus CSS row as evidence rather than full
  generality close.
- `restart/audit/totality/p2/2D-cost-model.md`: decision-engine/union replay
  fences and substrate-kind gates.
- `restart/audit/totality/p2/2E-host-arch-esoterica.md`: PMULL/CSSC route
  disposition, hardware-gate manifest, and source-present SIMD/ASM states.
- `restart/audit/totality/p2/2F-parse-that-gaps.md`: parse-that import
  conditionality, retained-substrate rejection, same-wave consumer requirements,
  and direct-digest comparator discipline.
- `skinny/REDRESS.md`: REDRESS 119-127 source ledger and prior PMULL/CSSC/union
  rejection history.
- `skinny/RESULTS.md`: current JSON direct residual rows, CSS row admission, and
  strict comparator/telemetry surface.
- `restart/prompts/totality/PASS-2-RESEARCH.md`: CH3 lens scope and challenge
  output contract.
