# SK-V14 W2-C: Lock 14 Grammar-Neutrality

Date: 2026-05-24.
Scope: Analyze Lock 14 constraints and grep gates for a W2 `regen-css` implementation.
Output: this file.

## §1 — Findings (concrete, file:line cited)

- Lock 14 requires generic crates to carry zero grammar-specific code, grammar-named modules, grammar-specific public APIs, per-grammar feature flags, and hand-written per-grammar runtime files; allowed integration surfaces are grammar source, workspace metadata, and optional per-grammar declaration crates at `restart/locks/LOCKS.md:349-351`.
- Lock 14's generated-output allowance says generated CSS identifiers are valid only when emitted from a rostered generator using grammar source plus metadata and byte-equivalent regeneration, with hand-patching rejected at `restart/locks/LOCKS.md:360-364`.
- Lock 6 reinforces the same generated-artifact rule: any `// @generated by skinny bbnf-codegen` header must trace to a rostered `cargo xtask regen-{grammar}` emission and reject hand-patching at `restart/locks/LOCKS.md:183-190`.
- SK-V14 W2 is explicitly the first `regen-{grammar}` family instance, not a CSS-specific exception: `restart/skinny/tranches/sk-v14/SYNTHESIS.md:91-97` binds R4 to `cargo xtask regen-css` with a grammar-neutral xtask generator.
- The W2 owner paths include both a shared `regen.rs` and `regen_css.rs`; the latter is allowed only as the CSS facade for the first instance of the family at `restart/skinny/tranches/sk-v14/SPEC.md:463-467`.
- W2's exit gate says zero grammar-named branches in xtask itself and allows `regen_css.rs` as the only css-named identifier in the xtask binary at `restart/skinny/tranches/sk-v14/SPEC.md:491-496`. Current `skinny/xtask/src/main.rs` already contains legacy CSS row strings in gate/report validation, so the W2 plan must define the grep gate over newly added regen machinery or first isolate legacy gate-json CSS accounting.

## §2 — Recommendations (named falsifiability gates)

- `G-W2-LOCK14-NEW-DIFF`: run `git diff -- skinny/xtask/src/main.rs skinny/xtask/src/regen.rs skinny/xtask/src/regen_css.rs` and reject any newly added `match` or branch that selects by grammar behavior outside the roster entry.
- `G-W2-CSS-NAME-SCOPE`: run `rg -n 'regen_css|regen-css|css_l4|CssL4|CSS L4' skinny/xtask/src` and verify new CSS identifiers are limited to the facade, command strings, roster data, and legacy pre-existing gate-json rows.
- `G-W2-GENERIC-BRANCH-SCAN`: use the Lock 14 branch scan from `restart/skinny/tranches/sk-v14/SPEC.md:300-306` as a no-regression check for touched generic crates.
- `G-W2-ROSTERED-EMISSION`: every output path containing the skinny generated header must be owned by a `regen-css` roster entry and by a `check-css-l4-*` companion command.

## §3 — Risks (REDRESS entries to pre-block)

- A literal whole-file grep for css identifiers in `skinny/xtask/src/main.rs` will fail on pre-existing gate-json/report CSS rows; using it unqualified would create a false W2 rejection. The plan should compare new diff or isolate the regen module.
- Adding CSS behavior to `skinny/crates/codegen` generic lowering would violate Lock 14 and preempt W5. W2 should only expose rostered regeneration of the existing provider outputs.
- Any generated header that cannot be reproduced by `cargo xtask regen-css` repeats P-1 fake-generated recurrence.

## §4 — Sources (every external citation)

No external citations. Local repository sources only.
