# BA.W5 — All-Grammar Direct-To-Struct Migration (Subsumed)

This wave is subsumed by W5a, W5b, W5c, W5d, W5e per directive §5 (Wave-Numbering Convention) — sub-waves at letter suffix. Per `docs/tranches/BA/audit/W5-substrate-identity-decision.md` option (a) — the user override on substrate-identity disposition — BA owns the all-grammar direct-to-struct migration; Lock 1 honours at BA close (not deferred to BB).

## Sub-wave map

| Sub-wave | Grammar | Purpose | Closer-gate |
|---|---|---|---|
| `W5a.md` | JSON | Direct-projection emit replaces 86.07% `Vec<OpenFrame>::clone` pathology; demonstrates the canonical pattern | BA-G1: twitter ≤ 400 µs vs sonic-rs's 436 µs; BA-G2: ≤ 2 heap allocs/parse; `rg -n 'enum OpenFrame' crates/core/src/runtime/json/` returns 0 |
| `W5b.md` | CSS L4 | 14-variant typed-enum lands; lightning-css parity passes; bootstrap.css regression-bound | `rg -n 'enum OpenFrame' crates/core/src/runtime/css_l4/` returns 0; CSS L4 nextest 100% pass; `css_l4.rs ≤ 100,000` LOC |
| `W5c.md` | BBNF | Pratt operator-chain emission; self-host roundtrip passes; aggregator deletion (surgery #19) | `rg -n 'enum OpenFrame' crates/core/src/runtime/bbnf/` returns 0; self-host roundtrip 100% pass; BBNF aggregator namespaced |
| `W5d.md` | Sheets | Per-leaf direct emission (cell_ref, identifier, sheet_prefix, error); cssparser-class parity; arena-fallback resolved (surgery #18 Sheets-side) | `rg -n 'enum OpenFrame' crates/core/src/runtime/google_sheets/` returns 0; Sheets nextest 100% pass; arena-fallback comments resolved |
| `W5e.md` | Cohort (BNF, CSV, EBNF, CSS Pretty, Math) | Five hand-written direct-to-struct modules; SimpleStructBuilder template retires; strategy resolver collapses | `rg -n 'enum OpenFrame' crates/core/src/runtime/` returns 0 across all nine grammars (Lock 1 honoured at BA close); `EmitStrategy::OpenFrame` variant deletes |

## Cross-tranche disposition

Per the user override (option (a)), BA owns the all-grammar migration; BB.W1 retires per-grammar receivers under option (a). The carry-tag table BA→BB no longer carries per-grammar OpenFrame migration; the renumbered carry C1' is cohort hand-written → BB.W2 template-consolidation only (BA writes 5 hand-written direct-to-struct cohort modules; BB.W2 consolidates into 1 template per gap D).

The detailed wave specification is in W5a.md, W5b.md, W5c.md, W5d.md, W5e.md.
