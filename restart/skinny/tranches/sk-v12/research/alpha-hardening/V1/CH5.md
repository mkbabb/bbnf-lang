# SK-V12 Pass Alpha CH5 Hidden Coupling

Pass: Alpha SK-V11 -> SK-V12 CHALLENGE V1.
Date: 2026-05-20.
Lens: CH5 hidden coupling.
Output: this file only.

## Disposition

ACCEPT.

Critical findings: none.

The Alpha V1 packet does not introduce a hidden parallel substrate, sidecar
producer, renamed scanner route, Track 1 == Track 2 shortcut, typed/direct
output-plane relabel, hidden generic JSON policy, or non-JSON oracle coupling.
It correctly treats SK-V11 as an unchanged measured fixpoint, routes SK-V12 to
the generated non-JSON baseline first, and keeps JSON direct residuals
pre-blocked unless a future pass names fresh material evidence beyond REDRESS
114-119.

## Materials Read

- `restart/prompts/pass-contracts/PASS-ALPHA.md` Section 3.
- `restart/skinny/tranches/sk-v12/SYNTHESIS.md`.
- `restart/skinny/tranches/sk-v12/HANDOFF.md`.
- `restart/skinny/tranches/sk-v12/research/alpha/alpha-A-results-extraction.md`.
- `restart/skinny/tranches/sk-v12/research/alpha/alpha-B-competitor-deltas.md`.
- `restart/skinny/tranches/sk-v12/research/alpha/alpha-C-redress-digest.md`.
- `restart/skinny/tranches/sk-v12/research/alpha/alpha-D-validated-invalidated.md`.
- `restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md`.
- `restart/skinny/tranches/sk-v12/research/alpha/alpha-F-contract-draft.md`.
- `restart/skinny/tranches/sk-v11/research/close/close-plan.md`.
- `restart/skinny/tranches/sk-v11/research/close/close-research.md`.
- `restart/skinny/tranches/sk-v11/research/close/close-redress.md`.
- `skinny/REDRESS.md` through REDRESS 120, with specific review of REDRESS
  96, 97, 98, 102, and 111-120.

## Sub-Dispositions

| Surface | Disposition | Critical finding |
|---|---|---|
| Parallel substrate / sidecar producer / renamed W3 route | ACCEPT | None |
| Track 1 / Track 2 and non-JSON oracle independence | ACCEPT | None |
| Typed/direct output-plane honesty | ACCEPT | None |
| Hidden JSON policy in generic crates or generic runtime | ACCEPT | None |
| Non-JSON baseline before JSON direct companion work | ACCEPT | None |
| Same-wave gate/report consumer binding | ACCEPT | None |

## Findings

### CH5-1 - ACCEPT: W3 substrate, sidecar, and renamed-scanner routes remain closed

The SK-V12 packet carries the retired W3 substrate surface as negative
authority, not as a renamed implementation option. `SYNTHESIS.md` keeps W3 and
parse-only routes closed and forbids union/event/class-column/streaming-cursor,
class-lane, sidecar substrate, `UnionTape`, retained structural vectors,
W4-through-W3 cascade, parse-only SOTA claims, and renamed routes
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:62-67`). The pre-blocked route
section repeats the same ban and adds parser-owned projections, sidecar
freshness as behavior producer, new directives, BIR variants, `BackendShape`,
public substrate APIs, parser-owned sidecar/fact slots, and second retained
substrates (`restart/skinny/tranches/sk-v12/SYNTHESIS.md:196-215`).

Alpha-C preserves the historical lock: REDRESS 96 and 97 rejected two faithful
W3 substrate implementations, REDRESS 98 retired `G-W3-UNION-SUBSTRATE`, and
REDRESS 102 firewalled parse-only SOTA movement
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-C-redress-digest.md:73-88`).
The original REDRESS entries show why this is binding: the full class-column
substrate and the allocation-free streaming cursor both failed their measured
rows (`skinny/REDRESS.md:2795-2848`, `skinny/REDRESS.md:2850-2906`), REDRESS
98 retired the thesis rather than deferring it (`skinny/REDRESS.md:2908-2950`),
and REDRESS 102 found no live W3 substrate dispatch route while keeping
parse-only rows out of SOTA admission (`skinny/REDRESS.md:3040-3058`).

The shortlist also avoids scanner renames. E5 may only reopen a JSON direct row
after E1/E4 produce a non-JSON-proven generated template, and it explicitly
rejects `number_span_emit_slot`, `container_tail_next`, bounded string span,
escaped source fold, and output-digest host sink under a new name
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:238-251`,
`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:291-296`).

### CH5-2 - ACCEPT: Track 1 / Track 2 and non-JSON oracle independence are load-bearing

The generated non-JSON baseline must have generated Track 1, independent Track
2 or oracle, strict output equality, finite same-run throughput, provenance,
telemetry, and gate/report consumption
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:35-42`). The intervention must
consume the baseline, improve the same output plane, preserve oracle
independence, and pass strict equality
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:43-48`). The telemetry gate must
reject oracle coupling and generated Track 1 / Track 2 dishonesty
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:190-194`).

Alpha-E carries that into the concrete candidates. E1 requires generated CSS L4
Track 1 plus an independent same-plane oracle and forbids the oracle from
calling generated Track 1, generated SinkOnly helpers, generated typed helpers,
`sheets_witness`, or benchmark-private parser code
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:55-66`).
E2 and E3 require independent same-plane oracles and reject witness/parser
inventory as generated authority
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:107-135`,
`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:156-184`).

This is consistent with REDRESS 111, where the accepted W1a report lane
validated source provenance separately from `track2_independence_status` and
required shared-source/coupled-Track-2 negative fixtures
(`skinny/REDRESS.md:3284-3304`). W1a remains only gate/report infrastructure,
not baseline authority, because it did not create generated non-JSON Track 1 or
move a row (`skinny/REDRESS.md:3284-3288`).

### CH5-3 - ACCEPT: typed and direct planes are not being relabeled

Alpha-A keeps the output planes separate. Direct rows use digest output with
same-run sonic-rs strict direct and serde_json direct anchors; typed rows use
typed direct output, with Track 2 as a structural/product oracle rather than
the SOTA gate
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-A-results-extraction.md:42-55`).
The direct residual table is generated Track 1 SinkOnly digest versus
independent hand Track 2 SinkOnly digest, while the typed table is generated
typed product versus independent Track 2 structural or serde oracle
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-A-results-extraction.md:87-90`,
`restart/skinny/tranches/sk-v12/research/alpha/alpha-A-results-extraction.md:112-126`).

The close condition guards the four admitted direct rows and seven admitted
typed rows, and requires same-wave measured disposition before any guard can
demote (`restart/skinny/tranches/sk-v12/SYNTHESIS.md:49-53`). It also
pre-blocks direct digest evidence as typed product proof and direct/typed row
admission by analogy (`restart/skinny/tranches/sk-v12/SYNTHESIS.md:204-206`).
Alpha-F's refusal conditions reject direct admission without generated Track 1,
independent Track 2, strict same-run direct evidence, output-plane match,
provenance, and gate consumption, and reject typed admission from direct digest
evidence
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-F-contract-draft.md:177-197`).

### CH5-4 - ACCEPT: hidden generic JSON policy is treated as a blocker, not proof

The packet correctly identifies the current generic-codegen blocker from
REDRESS 112: skinny codegen still routes direct and typed emission through
`json_provider::ensure_runtime_profile`, which only accepts
`backend.grammar_name == "json"`, and no generated CSS L4 runtime exists
(`skinny/REDRESS.md:3311-3338`). SK-V12 does not paper over that. The close
condition requires no JSON policy leak into generic crates or runtime outside
generated per-grammar modules
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:35-42`), the telemetry gate must
reject JSON policy leakage
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:190-194`), and the pre-block list
forbids JSON policy in generic crates or runtime outside generated per-grammar
code (`restart/skinny/tranches/sk-v12/SYNTHESIS.md:210-214`).

Alpha-E also keeps JSON-provider emission out of baseline authority. E1 says
REDRESS 112 blocks routing through `json_provider::ensure_runtime_profile` as
a generality proof, and Lock 14 blocks JSON policy in generic crates or
runtime outside generated per-grammar modules
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:83-88`).
The "Explicitly Not Shortlisted" section rejects JSON-provider emission as
generic runtime proof and rejects old hand non-JSON runtimes, witness modules,
sidecars, or gate fixtures as generated Track 1 baseline authority
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:299-308`).

### CH5-5 - ACCEPT: JSON direct companion work cannot couple back into first-wave scope

The packet does not let a JSON direct row masquerade as the SK-V12 first
material target. `SYNTHESIS.md` requires the generated non-JSON baseline first
and pre-blocks JSON direct residual rows unless S-P1/S-P2 name fresh material
evidence beyond REDRESS 114-119
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:35-42`,
`restart/skinny/tranches/sk-v12/SYNTHESIS.md:57-61`). HANDOFF makes that
priority order binding and refuses JSON-only direct work before the generated
non-JSON baseline and measured intervention are satisfied or explicitly blocked
(`restart/skinny/tranches/sk-v12/HANDOFF.md:39-57`,
`restart/skinny/tranches/sk-v12/HANDOFF.md:87-100`).

E5 is therefore acceptable as a later conditional route, not a hidden first
wave. It may dispatch only after successful E1/E4 non-JSON generated template
evidence, must prove the JSON source delta is not a renamed W3-W7 route, must
show same-host plausibility for both generated Track 1 and independent Track 2,
and must have `gate-json` consume provenance and row movement in the same wave
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:238-290`).

### CH5-6 - ACCEPT: same-wave gate/report consumption stays visible

Telemetry is not allowed to become producer-only side evidence. `SYNTHESIS.md`
requires every new non-JSON row, field, or companion report to be consumed by a
same-wave gate; producer-only fields, stale run ids, oracle coupling, and JSON
policy leaks fail closed
(`restart/skinny/tranches/sk-v12/SYNTHESIS.md:65-67`). HANDOFF requires the
generated non-JSON baseline and intervention to be gate-consumed either in
`skinny/RESULTS.md` with all consumers updated or in a companion report with an
explicit gate command, and lists source provenance, independent oracle source,
same-wave consumer class, and fail-closed checks as required evidence
(`restart/skinny/tranches/sk-v12/HANDOFF.md:72-81`).

Alpha-E repeats that candidate by candidate: E1, E2, E3, and E4 all require the
generated benchmark row plus the non-JSON gate/report consumer in the same
wave, while E5 requires `gate-json` to consume provenance and row movement
(`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:64-78`,
`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:115-128`,
`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:162-175`,
`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:211-224`,
`restart/skinny/tranches/sk-v12/research/alpha/alpha-E-candidate-shortlist.md:272-286`).

## Non-Blocking Fold-Forward Requirements

These are not critical defects in Alpha V1 because S-P3 owns the implementation
packet, but they should be preserved as CH5 entry-gate requirements:

1. S-P3 should require E1/E2/E3 negative fixtures equivalent to the W1a
   coupled-Track-2 and shared-source failures. The docs require independence;
   the implementation packet should make the oracle-coupling proof executable
   for whichever non-JSON grammar is selected.
2. S-P3 should make the generic JSON-policy audit executable before any
   baseline redress: `json_provider` may be replaced or bypassed only with
   grammar-neutral codegen plus generated per-grammar runtime modules, and any
   generic-crate JSON policy must fail the gate.
3. E5 must remain sequenced after a positive generated non-JSON baseline and
   intervention. It is not an alternate first-wave JSON direct route and should
   REJECT at plan time if the non-JSON-proven material evidence is absent.

## Result

CH5 accepts Alpha V1 for the SK-V11 -> SK-V12 G-Alpha packet. No REVISE or
REJECT disposition is open for hidden coupling.
