# SK-V12 Pass Alpha Hardening V4 - Consolidated

Date: 2026-05-20.
Cycle: V4 under `USER-PIN-W1-CSS-L4-SOTA.md`.

## Disposition

ACCEPT.

The pin-aware SK-V12 Pass Alpha packet converges at V4. All six hardening
lenses pass after the G-Alpha presentation fold.

## Lens Results

| Lens | Disposition | V4 finding |
|---|---|---|
| CH1 correctness | PASS | Strict lightningcss admission, symmetric CSS fact stream, pass order, measurable close conditions, citations, rollback facts, and G-Alpha consistency are correct. |
| CH2 generality / Lock 14 | PASS | CSS L4 is executable and first; Sheets/BBNF-self remain fallback-only; `GrammarConfig` legality, JSON template fences, and public substrate limits hold. |
| CH3 regression / REDRESS | PASS | G-Alpha consistency, JSON guard refresh, W1a local guard inheritance, zero carried orphan, REDRESS adjacency, rejected patch paths, W0 revalidation, and SIMD/ASM preconditions pass. |
| CH4 cost / tranche budget | PASS | The G-Alpha seed preserves the accepted S-P1/S-P2/S-P3 plus W0-W5 split, 20/15/30 caps, hand LOC caps, generated LOC/O(N) discipline, and rollback actions. |
| CH5 hidden coupling | PASS | Stale G-Alpha coupling, W1a guard shortcut, pre-pin authority coupling, CSS plane/normalization asymmetry, public substrate leakage, SIMD orphan loopholes, and JSON guard shortcuts are closed. |
| CH6 anti-paper-close | PASS | The packet does not claim premature `G-Alpha PASS`, does not skip G-Alpha/S-P1/S-P2/S-P3, requires CSS redress before fallback, binds telemetry, and prevents future-phase close promises. |

## Accepted G-Alpha Contract

G-Alpha may now present the pin-aware contract:

- CSS L4 is the authoritative first target.
- Sheets and BBNF-self are fallbacks only after a measured CSS L4 redress
  attempt.
- CSS admission is strict
  `generated_track1_mbps > lightningcss_mbps + 1` on the same corpus, same
  output plane, same host, and strict equality semantics.
- The CSS output plane is one canonical fact stream shared symmetrically by
  generated Track 1, independent Track 2/oracle, and lightningcss.
- G-Alpha authorizes only SK-V12 S-P1 Profile, then S-P2 Research, then S-P3
  Synthesis-Plan under the user pin. It does not authorize behavior source
  edits or implementation waves.
- Downstream S-P3 must materialize replacement `SPEC.md` and
  `DISPATCH-PROMPT.md` before W0-W5 wave authority exists.
- ADMIT and FIXPOINT both require the carried aarch64 orphan set to be
  admitted, removed, or inventory-demoted with evidence.
- Union and ASM-gen categories are unblocked at category level under the user
  pin, while REDRESS 96/97/98 and 88/89/90 remain historical implementation
  evidence requiring material differential, CHALLENGE, and measurement.

## Next Authority

SK-V12 S-P1 Profile under the user pin. S-P1 must profile fresh against the
SK-V12-open baseline, cover the 17 JSON corpora and CSS L4 target corpus, and
produce the six-profile cohort before S-P1 hardening decides convergence.
