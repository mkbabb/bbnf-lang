# SK-V14 W5A CHALLENGE V4 CH2: Non-JSON Generality and Lock 14
Disposition: ACCEPT
Acceptance score: 95/100
Findings:
- The W5A plan remains grammar-neutral at the runtime-generation boundary. It introduces one `RuntimeGenerationRequest` path carrying grammar source plus workspace metadata into codegen, parses required runtime-generation constructs as source facts, routes `regen-css` and JSON checks through that request, and leaves CSS provider/template deletion to W5B (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:18`).
- CSS L4 is not granted a special branch. The parser/contract gate requires import metadata, directives, comma sequence, whitespace modifiers, projections, raw host/value-expression spans, and `@{...}` capture to parse as source facts without `grammar_id == "css_l4"` or equivalent profile-specific generic-branch behavior (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:39`-`40`; `restart/skinny/tranches/sk-v14/SPEC.md:662`-`674`).
- Sheets and BBNF-self are covered by the same request path rather than adjacent fixture or JSON-only proof. The plan requires both to fail closed with named, source-located unsupported constructs or to emit generated-role witnesses through the same parser/contract (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:42`; `restart/skinny/tranches/sk-v14/SPEC.md:665`-`677`).
- The unsupported-construct requirement is concrete enough for redress. The accepted names include `BBNF-UNSUPPORTED-PROJECTION`, `BBNF-UNSUPPORTED-WHITESPACE-MODIFIER`, `BBNF-UNSUPPORTED-IMPORT-RESOLUTION`, `BBNF-UNSUPPORTED-DIRECTIVE`, and `BBNF-UNSUPPORTED-HOST-CAPTURE`; generic parser errors such as `unexpected token '-'` are explicitly pre-blocked as insufficient evidence (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:42`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:125`-`126`).
- The temporary Lock 14 guard does not permit provider/template creation or deletion. W5A owns `skinny/crates/bbnf-bench/src/lock14_baseline.rs` only for a temporary no-deletion/no-new-provider guard, while CSS provider files and CSS template directories are explicit non-owner paths (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:26`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:29`-`35`). The count and A/D/R gates require provider modules to remain at 8, CSS template directories to remain at 7, and staged/unstaged provider/template additions, deletions, or renames to fail (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:43`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:92`-`95`).
- The W5A/W5B boundary is consistent across plan and SPEC. W5A may leave existing provider count non-zero but cannot add provider modules or template directories; W5B owns provider/template deletion after W5A admits (`restart/skinny/tranches/sk-v14/SPEC.md:667`-`678`, `restart/skinny/tranches/sk-v14/SPEC.md:701`-`709`).
- Existing Lock 14 baseline code makes the W5A guard a real implementation obligation, not a paper gate. Current SK-V14 owner-path allowances include W0, W2, and W4 only, and the baseline validates dirty, unstaged, staged, and parent diffs against the allowed owner paths (`skinny/crates/bbnf-bench/src/lock14_baseline.rs:1081`-`1156`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:1160`-`1171`). W5A therefore needs a scoped allowance, but the plan's non-owner paths and count/A/D/R gates prevent that allowance from opening provider/template creation or deletion.
- Lock 14 v+1 compatibility holds. Lock 14 allows generated grammar-named runtime output only when emitted from grammar source plus workspace metadata, while excluding hand-coded provider enums, generic-crate grammar branches, grammar-named generic APIs, and proof fixtures routed through generic roots (`restart/locks/LOCKS.md:349`-`364`). The W5A request contract, non-JSON witnesses, and no-provider/template guard match that boundary.
- V3 already delivered a clean CH2 result, and V4 finds no regression. The V3 consolidated packet recorded CH2 as ACCEPT with no required folds and summarized the same-path Sheets/BBNF-self and temporary Lock 14 guard coverage (`restart/skinny/tranches/sk-v14/research/skv14-waveW5A-challenge/V3/HARDENING-SKV14-W5A-V3-CONSOLIDATED.md:11`-`30`).
Required folds:
- NONE
Evidence:
- Read: `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md`, `restart/skinny/tranches/sk-v14/research/skv14-waveW5A-challenge/V3/HARDENING-SKV14-W5A-V3-CONSOLIDATED.md`, `restart/skinny/tranches/sk-v14/research/skv14-waveW5A-challenge/V3/CH2.md`, `restart/skinny/tranches/sk-v14/SPEC.md`, `restart/locks/LOCKS.md`, and `skinny/crates/bbnf-bench/src/lock14_baseline.rs`.
- Command/gate references accepted for CH2: exact tests `w5a_named_unsupported_constructs_are_source_located` and `w5a_sheets_bbnf_fail_closed_through_runtime_contract` plus nonzero pass assertions (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:63`-`70`); `cargo xtask check-json`, `cargo xtask regen-css`, and all seven `check-css-l4-*` companions through the same request path (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:71`-`80`, `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:113`); request-path grep and old call-boundary absence (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:87`-`91`); provider/template count and staged/unstaged A/D/R rejection gates (`restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:92`-`95`).
