# SK-V9 S-P1 V2 CH2 Generality / Lock 14

Disposition: ACCEPT.

The V2 rerun names grammar-neutral generated parser and substrate surfaces:
`dispatch_value`, direct object/array projection, string/unescape materializers,
view accessors, structural scan, and typed projection. It does not introduce a
JSON-only directive, BIR, substrate, policy rule, or sidecar producer.

The fused parse-only symbol is treated as too coarse for primitive design, which
preserves Lock 14 and prevents speculative JSON-role attribution.
