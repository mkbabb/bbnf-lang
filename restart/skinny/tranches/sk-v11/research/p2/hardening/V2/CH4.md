# SK-V11 S-P2 CH4 Cost
Pass: S-P2 CHALLENGE. Cycle: V2.
Date: 2026-05-19.
Scope: scalar-reference, checkasm/parity, micro-proof, same-wave consumer, feature/fallback, output-plane, and reject-boundary review.
Output: this file.
Disposition: ACCEPT.
Accept rate contribution: 1.

## Findings

1. Accepted - V2 folds the admission tuple into the common SIMD/ASM process. PASS-2 makes scalar-reference status, checkasm/parity expectation, and same-wave-consumer notes mandatory (`restart/prompts/skinny/PASS-2-RESEARCH.md:119`-`124`), and the orchestrator non-negotiables require scalar reference, checkasm before wiring, same-wave consumer, and no contrivance (`restart/prompts/ORCHESTRATOR.md:197`-`212`). P2-B now states the full sequence: scalar oracle, differential cell, feature gate, micro-prove-first, same-wave consumer, and proof-only demotion when no consumer exists (`restart/skinny/tranches/sk-v11/research/p2/p2b-dav1d-process.md:242`-`259`). Its candidate table then carries per-row scalar-ref status, strict parity/checkasm, micro-proof, same-wave consumer, feature/fallback, and reject boundary for `HEX_QUARTET_X4_PROOF`, `STRING_SPECIAL_BLOCK_CALLER_MICROPROOF`, `BYTE_CLASS_TBL_CLASSIFIER`, `DIGIT_SPAN_UDOT`, `WHITESPACE_BYTE_SET_SKIP`, `MOVEMASK_EXHAUSTIVE_GATE`, `CONTAINER_DISPATCH_CLASSIFIER`, and `OUTPUT_DIGEST_HASH_ORACLE` (`restart/skinny/tranches/sk-v11/research/p2/p2b-dav1d-process.md:261`-`270`). This closes the V1 CH4 tuple gap.

2. Accepted - The local checkasm cost boundary is correctly represented. P2-B cites the in-tree harness shape: scalar reference versus candidate, cloned inputs, source mutation checks, alignment sweep, stack canary, signal guard, deterministic random inputs, and strict promotion by `BBNF_SIMD_STRICT=1` (`restart/skinny/tranches/sk-v11/research/p2/p2b-dav1d-process.md:32`-`93`). The local references support that claim: `CHECKASM-REPORT.md` maps `call_ref` + `call_new` and dst comparison to bbnf-simd (`skinny/crates/bbnf-simd/CHECKASM-REPORT.md:41`-`51`), records strict-mode promotion (`skinny/crates/bbnf-simd/CHECKASM-REPORT.md:58`-`63`, `77`-`79`), and exposes primitive-checkasm as the strict gate (`skinny/crates/bbnf-simd/CHECKASM-REPORT.md:207`-`216`). The tests contain scalar anchors and strict controls (`skinny/crates/bbnf-simd/tests/checkasm_parity.rs:112`-`115`, `425`-`502`) plus dedicated byte-class and x4/string cells (`skinny/crates/bbnf-simd/tests/checkasm_byte_class_from_eq_set_64.rs:138`-`162`, `skinny/crates/bbnf-simd/tests/checkasm_byte_class_from_table_64.rs:13`-`49`, `skinny/crates/bbnf-simd/tests/checkasm_utf8_block.rs:59`-`63`). CH4 accepts the tuple because V2 requires strict candidate-specific coverage before any product row counts.

3. Accepted - P2-A now carries cost gates on comparator-derived candidates instead of leaving them as architecture prose. `byte_class_mask64_transient`, `bounded_plain_string_end`, `whitespace_run_skip64`, `separator_pair_probe16_direct`, `digit_block_accumulate_16`, and `output_digest_hash_oracle` each name scalar-reference shape, same-wave consumer/proof, output plane, feature/fallback, and reject boundary (`restart/skinny/tranches/sk-v11/research/p2/p2a-sota-teardown.md:47`-`72`, `74`-`101`, `103`-`124`, `126`-`148`, `150`-`172`, `174`-`200`). P2-A also demotes quote state, hex decode, movemask, and live-mask handling to support/proof surfaces that cannot move to S-P3 alone (`restart/skinny/tranches/sk-v11/research/p2/p2a-sota-teardown.md:202`-`212`). The digest surface is explicitly non-parser/output-only, not generic parser semantics (`restart/skinny/tranches/sk-v11/research/p2/p2a-sota-teardown.md:174`-`200`, `245`-`247`).

4. Accepted - P2-C no longer overclaims arch inventory as admission. Its aarch64 rows now name scalar refs, same-wave consumers, fallback, micro-proof, and reject boundaries for TBL byte-set/classifier, UDOT digit-span, widened string-special block, x4 escape proof, and whitespace byte-set skip (`restart/skinny/tranches/sk-v11/research/p2/p2c-arch-esoterica.md:25`-`69`). The V1 x4 overclaim is corrected: P2-C says current x4 coverage is smoke only, needs a scalar x4 oracle and strict valid/invalid/mixed/alignment/surrogate coverage, and production requires a real new direct/typed escaped-segment caller rather than the already-consuming `unescape_string` path (`restart/skinny/tranches/sk-v11/research/p2/p2c-arch-esoterica.md:52`-`60`). Movemask, `EXT`, PMULL, CTZ, SHA3 ternary, PRFM/STNP, and cache hints are support/inventory rows with no standalone row movement (`restart/skinny/tranches/sk-v11/research/p2/p2c-arch-esoterica.md:71`-`79`).

5. Accepted - P2-D supplies the missing scalar-only parity and fallback tuple for substrate/consumer-shape candidates. D1-D5 now state scalar references and candidate shape (`restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:29`-`35`), then bind each candidate to output-plane declaration, scalar-output parity plan, micro-proof/reject boundary, and fallback/no-op plan (`restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:37`-`47`). This is adequate for scalar candidates where checkasm is N/A and product parity is the cost gate. The D5 sparse-flag lane remains acceptable only as an internal encoding replacement with an existing retained-view or typed product consumer; lazy counters and parse-only rows cannot admit it (`restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:35`, `47`, `67`-`69`).

6. Accepted - P2-E carries a complete parse-that admission tuple and concrete cost boundaries. The common rule requires CH4 fields, exact commands, strict no-regression, `>= 1.0%` useful movement, and `0.5%` guard-row slack (`restart/skinny/tranches/sk-v11/research/p2/p2e-parse-that-gaps.md:33`-`41`). The four retained gaps each include scalar reference, output plane, same-wave product consumer, row/proof set, strict parity/checkasm, fallback, and reject boundary: `pt_byte_set_run_skip`, `pt_bounded_plain_string_end`, `pt_digit_run_span_accumulate`, and `pt_escaped_string_segments` (`restart/skinny/tranches/sk-v11/research/p2/p2e-parse-that-gaps.md:43`-`48`). P2-E also correctly demotes `container_dispatch`, `simd_movemask`, and `output_digest_hash` out of parse-that primitive status (`restart/skinny/tranches/sk-v11/research/p2/p2e-parse-that-gaps.md:27`-`29`, `92`).

7. Accepted - P2-F is adequate for CH4 as an abstraction crosswalk because it does not try to admit standalone behavior rows. C1-C7 carry scalar/parity gates and same-wave consumer/reject boundaries at the abstraction level (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:32`-`40`), while C8 and C9 are explicitly non-parser/oracle/accounting surfaces (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:42`-`47`). The mandatory non-JSON benchmark requirement includes generated Track 1 before/after throughput, independent Track 2 or oracle, strict output equality, primitive self-time, c/B where PMU is available, strict checkasm/parity for SIMD/ASM, scalar or no-op fallback, no sidecar allocation, no generic-crate grammar names, and same-wave generated non-JSON consumer (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:77`-`82`). This satisfies CH4 for S-P2; S-P3 still must attach LOC budget, risk class, wave alignment, and hard cap when it sequences waves.

## Accepted Facts For S-P3

1. `HEX_QUARTET_X4_PROOF`, `MOVEMASK_EXHAUSTIVE_GATE`, `OUTPUT_DIGEST_HASH_ORACLE`, and C9 output-plane accounting are not standalone parser row movers. S-P3 may use them only as proof/oracle/support surfaces unless a wave names a real same-wave generated direct, typed, or non-JSON consumer and the full tuple.
2. Any AArch64 SIMD/ASM body shortlisted by S-P3 must carry a scalar oracle, strict candidate-specific checkasm/parity under `BBNF_SIMD_STRICT=1`, same-host micro-proof, feature gate, scalar/no-op fallback, same-wave consumer, output-plane declaration, and reject boundary before product measurements count.
3. Scalar-only code-shape candidates satisfy checkasm as N/A only when they carry product-output parity against generated Track 1 plus independent Track 2 or oracle, a micro-proof-first plan, a no-op fallback, and an explicit row-movement/reject floor.
4. Proof-only or oracle-only rows may support a wave, but cannot close a behavior gate by primitive parity, benchmark-only digest speed, parse-only movement, or reuse of an already-consuming caller.

## Required Folds

None. CH4 accepts V2 for S-P2 convergence. The remaining cost work belongs to S-P3 wave sequencing: concrete LOC budgets, risk class, owner paths, hard caps, and per-wave falsifiability gates.
