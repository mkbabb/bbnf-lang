# T-P2 V2 Hardening Consolidated

Pass: T-P2 Research.
Cycle: V2.
Date: 2026-05-21.
Scope: six-lens CHALLENGE consolidation for the V2 totality research fold.
Output: this file.

## Verdict

`G-T-P2-V2-CHALLENGE`: REVISE.

V2 resolves the broad V1 defects in REDRESS discipline, Lock 14 transfer,
Lock 1 substrate ownership, and anti-paper-close posture. It does not yet
converge because CH1 found residual provenance/counting defects and CH4 found
that the admission ledger is not consistently executable across the primitive,
hardware, and parse-that dossiers.

Acceptance rate: 4/6 = 66.7%.
Consecutive accepted cycles: 0.

| Lens | Disposition | Consolidated reason |
|---|---|---|
| CH1 correctness / provenance | REVISE | V2 pins the main moving upstreams, repairs W3C/OASIS/OpenFormula citation posture, downgrades Sneller, and records parse-that local authority, but several source-register rows still use generic repository roots and `primary_sources_cited` counts are not consistently auditable. |
| CH2 generality / Lock 14 | ACCEPT | V2 defines a mechanical Lock 14 transfer contract covering generated provider registration, leak scans, generated sink/fact/value/flag surfaces, primitive policy manifests, CSS plus Sheets/BBNF-self witnesses or negative controls, and generated decision-engine facts. |
| CH3 regression / REDRESS | ACCEPT | V2 carries a row-specific REDRESS-119 reopen matrix, a Union/PMULL/CSSC material-differential checklist, REDRESS 121-127 taxonomy, and no route closes from prior fixpoint history. |
| CH4 cost / implementation realism | REVISE | The shared ledger schema has the right fields, but 2B, 2E, and 2F do not consistently instantiate row-local LOC/risk/rollback/abrogate/state fields, the state vocabulary is mixed with disposition labels, and e-graph/generated-LOC abrogate thresholds remain too elastic. |
| CH5 hidden coupling / substrate ownership | ACCEPT | V2 makes `substrate_target`, `retention_lifetime`, and `policy_owner` canonical, fences `CollapsedStage` and parse-that runtime masks to local/admitted output lifetimes, and rejects new directives, BIR variants, `BackendShape` variants, public substrate APIs, sidecars, and `UnionTape` replays. |
| CH6 anti-paper-close | ACCEPT | V2 separates citation, path presence, parity, and microbench evidence from admission. Strict comparator equality, same-wave consumers, row movement, or architectural-block evidence remain required for future gates. |

## Required V3 Fold

V3 must address both REVISE lenses before another challenge cycle:

- Pin or source-date every external library-source row still expressed as a
  generic repository root. At minimum: bind 2D's simdjson row to the V2 SHA;
  bind 2F's RE2, Rust regex, fast_float, and simdjson rows to the V2 addendum
  SHAs; and either add memchr/xxHash SHA/source-date authority or downgrade
  them to non-counted background.
- Normalize `primary_sources_cited` across 2A-2F by making each count equal a
  reproducible source-register count or by adding explicit `counted_source_ids`
  lists.
- Populate the minimum per-technique admission ledger rows in 2B, 2E, and 2F,
  or move them into a shared table, with scalar reference, strict
  checkasm/parity command, same-wave consumer, expected row gate, LOC budget,
  risk class, rollback path, abrogate threshold, state, substrate target,
  retention lifetime, and policy owner.
- Reserve `admissibility_state` for the state machine
  `source_backed -> scalar_backed -> checkasm_backed -> micro_proven ->
  production_wired -> row_admitted | measured_rejected |
  architectural_block`. Put `conditional`, `inventory`, `NOT-VALIDATED`, and
  related terms in a separate disposition/blocker field or define an explicit
  mapping.
- Make decision-engine abrogate thresholds numeric or dereferenceable: e-graph
  node/iteration/memory caps and generated LOC growth must resolve to row-local
  upper bounds or a named SPEC budget.

## Disposition

Do not dispatch T-P3. Fold V2 CH1 and CH4 into V3 research artifacts, commit the
fold, then run a fresh six-lens V3 CHALLENGE. The V2 ACCEPT lenses remain useful
evidence, but they do not count toward convergence until a full accepted cycle
is achieved.
