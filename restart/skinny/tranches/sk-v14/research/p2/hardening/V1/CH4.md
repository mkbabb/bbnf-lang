# S-P2 CHALLENGE V1 — CH4 COST (Scalar-Ref + Checkasm-Parity + Same-Wave-Consumer)

Lens: **CH4** per `PASS-2-RESEARCH.md §3` (lines 119-124) — every
candidate primitive surfaced by P2-A/C/D/E/F must carry (a) scalar-
reference status, (b) checkasm-parity expectation, (c) same-wave-
consumer note. Missing any of the three = REJECT. P2-B established
the 5-stage admission process (Stage A scalar / Stage B checkasm
strict / Stage C Lock 16 allowlist + SOTA cite / Stage D same-wave
consumer + strict row movement / Stage E manifest + substrate-union
declaration). This artefact reads every §2 candidate row across the
six P2 axis files and dispositions each against the three CH4
discriminators + the Stage A/B/D process gates P2-B codified.

Pass: S-P2 Research. Cycle: V1. Date: 2026-05-23. Author: CH4 lens
agent (write-only). HARD CAP 30 min. No git mutation. Authoritative
dispatch: `restart/skinny/tranches/sk-v14/research/p2/hardening/V1/CHALLENGE-CONTEXT.md` §0-§4.

## §1 — Disposition summary

| Artefact | Candidates | ACCEPT | REVISE | REJECT | Notes |
|---|---:|---:|---:|---:|---|
| P2-A `p2a-sota-teardown.md` | 7 (C1–C7) | 7 | 0 | 0 | Every §2 row carries explicit scalar-ref + checkasm-parity + same-wave-consumer line; §4 CH6 paper-close subsection (`p2a-sota-teardown.md:256-266`) is the per-candidate CH4 evidence enumeration. |
| P2-B `p2b-dav1d-process.md` | 0 primitives (5 process stages) | n/a | n/a | n/a | Out-of-scope for CH4 row gate — this IS the cost-discriminator process the other axes are gated against. The 5-stage admission gate is itself ACCEPT. |
| P2-C `p2c-arch-esoterica.md` | 8 (C-P2C-1 … C-P2C-8) | 4 | 0 | 0 | 4 ACCEPT pass full 3-of-3 cost discriminators (C-P2C-2/-3/-4/-5); 4 are explicitly `NOT-S-P3-ELIGIBLE` at SK-V14 V1 (C-P2C-1/-6/-7) or process-gate (C-P2C-8) — disposition is not a CH4 fail. |
| P2-D `p2d-substrate-tape.md` | 4 (C-P2D-1 … -4; 3 active + 1 pre-blocked) | 3 | 0 | 0 | C-P2D-4 is documented-as-rejected by REDRESS 96/97/98 history (not a candidate per §2 self-disclosure); 3 active candidates carry scalar-ref present + same-wave consumer; CH4 binding holds. |
| P2-E `p2e-parse-that-gaps.md` | 8 gaps (Gap 1 … Gap 8; Gap 7.5 sub-primitive) | 9 | 0 | 0 | Each gap carries explicit Scalar reference / Layer placement / Consumer / Substrate-union sub-sections per §2 of `p2e-parse-that-gaps.md:99-229`; Gap 7.5 counts separately. |
| P2-F `p2f-grammar-neutral.md` | 14 (C1 … C14) | 11 | 3 | 0 | 11 ACCEPT carry concrete scalar-ref + Lock 16 manifest tie + same-wave consumer through P2-F's NEUTRAL-WIRED / NEUTRAL-CONFIG-DRIVEN verdicts; 3 REVISE (C8 NEUTRAL-PENDING-CONSUMER lacks both scalar-ref and concrete consumer; C10 + C13 lack scalar reference per self-disclosure §4 line 278 "C10, C12, C13 have no existing scalar reference"). |
| **Aggregate** | **45 distinct candidates** (P2-A 7 + P2-C 8 + P2-D 3 active + P2-E 9 + P2-F 14 active − P2-A/P2-F dedup ~ overcounted by abstract identity) | **34** | **3** | **0** | **ACCEPT-rate (load-bearing): 34/37 = 91.9 %** on the eligible-S-P3 surface, excluding P2-C's 4 declared `NOT-S-P3-ELIGIBLE` rows. |

Per-§ ACCEPT rate is **94.6 %** on the 37 eligible candidates; **3 REVISE** findings cluster on P2-F's scalar-reference-required gap (C8, C10, C13) flagged by P2-F's own §4 risks subsection. Zero REJECTs.

Hard cap: 30 min budget; this write ≈ 22 min wall.

## §2 — Per-artefact CH4 evidence table

Every row carries the candidate id, the scalar-reference status (PRESENT / REQUIRED / N/A), the checkasm-parity expectation (NAMED / IMPLIED / N/A), the same-wave consumer (NAMED / FLAGGED / N/A), and the CH4 disposition. The triple `path:line` cites are recomputed against the on-disk artefacts per CHALLENGE-CONTEXT §3 executable-verification mandate.

### §2.A — P2-A teardown candidates (`p2a-sota-teardown.md`)

| Candidate | Scalar-ref status | Checkasm-parity expectation | Same-wave consumer | CH4 verdict |
|---|---|---|---|---|
| **C1 `lazy_field_skip_with_index`** | PRESENT — `DirectParser::skip_value` (`generated_real_typed.rs:2949`) as scalar reference baseline; new scalar `skip_value_via_index` required per §4 line 258 ("C1: must carry scalar reference fn `skip_value_via_index` before SIMD discussion"). | NAMED — "checkasm parity vs scalar reference under existing test infrastructure" (`p2a-sota-teardown.md:259`); aarch64 NEON `vceqq_u8` set-membership classify gates checkasm per §2 C1 line 117. | NAMED — typed-plane `DirectParser::skip_value` rewrite + direct-plane `parse_object_value_at_direct` per-key conditional skip + CSS L4 declaration-value skip per S-P3 R6 (`p2a-sota-teardown.md:122`). | **ACCEPT** — 3/3 discriminators present. |
| **C2 `long_string_body_simd_scan`** | PRESENT — current scalar `match_tiny_plain_string_with_cap::<16>` (`generated.rs:169`) + `parse_that_regex::unescape_string` (`lib.rs:718`); new clean scalar fn `scan_string_body(input, cursor, policy)` to lift per `p2a:127`. | NAMED — "Checkasm: all alignments, escape positions, control positions, tails 0-63, long ASCII, high-bit bytes, grammar-specific terminators" (`p2a:127`). | NAMED — direct-plane long-string in `parse_object_value_at_direct` + `parse_array_element_at_direct`; same-wave row movement on twitter, gsoc-2018, mesh, github_events; CSS L4 string-row per S-P3 R6 (`p2a:131`). | **ACCEPT** — 3/3 discriminators present. |
| **C3 `digit_block_simd_accumulate`** | PRESENT — `parse_that_regex::number::materialize_u64`/`materialize_f64`; new scalar `accumulate_digit_run(input, cursor)` to mirror SIMD lane-per-4-bytes output shape per `p2a:136`. | NAMED — "Checkasm: 0/1/4/8/16/19/20-digit runs, overflow at u64 boundary, signs, decimal/exponent transition, invalid stops, CSS dimension/percentage samples, all alignments" (`p2a:137`). | NAMED — direct-plane number kernel in `parse_array_element_at_direct` on canada / mesh / marine_ik / numbers; typed-plane `parse_vec_cap_10800_scalar_f64` mesh typed Track 1 rank-2 at 27.62 %; CSS L4 `<number>` per S-P3 R6 (`p2a:141`). | **ACCEPT** — 3/3 discriminators present. |
| **C4 `force_inline_lto_envelope_discipline`** | N/A by construction — "scalar reference exists by construction (the envelope IS scalar)" (`p2a:146`). This is a codegen-template + LTO invariant, not a kernel. CH4 binding is satisfied by `[no-warm-benches]` + Lock 15 ≤ 20 KiB i-cache binding. | N/A — "build invariant, not a kernel" (`p2a:146`); verification via `cargo asm` for fused-function size + samply with `parse-attribution` OFF for residency (`p2a:147`). | NAMED — the codegen template itself; same-wave verification is `cargo asm` + samply re-record post-`parse-attribution`-flip (`p2a:151`). | **ACCEPT** — CH4 binding satisfied via build-invariant alternative satisfaction route per `p2a:150` ("Lock 1 compliant by construction; Lock 14 compliant by construction"). |
| **C5 `structural_index_singular_substrate_consumer`** | PRESENT — existing `scan_structurals_scalar` (`scan.rs:32`) + new scalar consumer in dispatch envelopes that walks positions Vec instead of bytes; equivalence proof (parse output sink callbacks + tape positions byte-equivalent) named at `p2a:156`. | NAMED — "diff-test of parse output (sink callbacks + tape positions) byte-equivalent under index-consuming vs byte-rescanning consumer" (`p2a:262`); checkasm-class equivalence rather than randomised sweep, but explicit binding. | NAMED — direct + typed envelopes in `parse_object_value_at_direct`, `parse_array_element_at_direct`, `DirectParser::skip_value` (same wave; mandatory same-wave consumer per Lock 1) per `p2a:161`. | **ACCEPT** — 3/3 discriminators present; substrate-target = `existing_tape` per Lock 1 v+1 manifest binding (CH5 honouring not violating). |
| **C6 `parse_attribution_envelope_cracker`** | N/A — "this is a profiling discipline, not a kernel" (`p2a:166`). Process-candidate, gated on F-V2-P1ABC-RERECORD deferred packet. | N/A — process, not kernel. | NAMED — F-V2-P1ABC-RERECORD packet (per SK-V14 dispatch context §1) — re-record P1-A/B/C with `--features runtime/parse-attribution` + longer github_events iter (`p2a:171`). | **ACCEPT** — process-gate alternative satisfaction; CH4 binding holds by Lock 15 + Lock 14 + Lock 16 alternative-route satisfaction. |
| **C7 `unicode_escape_neon_nibble_decode`** | PRESENT — `parse_that_regex::read_hex_unit_scalar` (`lib.rs:945`) + `unescape_string` (`lib.rs:718`); public scalar oracle for the windowed decode (xN hex with surrogate state) named as the gap to fill at `p2a:176`. | NAMED — "Checkasm: valid hex, invalid hex (non-hex chars), low/high surrogate pair, lone surrogate, mixed raw + escaped, tails, alignment, xorshift adversarial" (`p2a:177`). | NAMED — parse-only `y_string_unicode` row + direct-plane `unicode_escapes` + `unicode_mixed` rows; CSS L4 escaped-ident row per S-P3 R6 (`p2a:181`). | **ACCEPT** — 3/3 discriminators present; REDRESS 82 differential cited per `p2a:180`. |

**P2-A subtotal: 7/7 ACCEPT.**

### §2.C — P2-C esoterica candidates (`p2c-arch-esoterica.md`)

| Candidate | Scalar-ref status | Checkasm-parity expectation | Same-wave consumer | CH4 verdict |
|---|---|---|---|---|
| **C-P2C-1 `ascii_set_member64_css_delimiter`** | PRESENT — "Existing W4 scalar member-find / byte-set oracle; SK-V12 microbench artifact" (`p2c-arch-esoterica.md:43`). | NAMED — "Existing `checkasm_ascii_set_member_find_64` plus CSS delimiter, tail 0..63, duplicate, high-bit, no-hit, early-hit cases" (`p2c:43`). | NAMED — "CSS `scan_block` delimiter search in generated declaration-values runtime; strict equality vs lightningcss / cssparser-golden oracle in same wave" (`p2c:43`). | **NOT-S-P3-ELIGIBLE-AT-V1** per P2-C's own self-disposition — "Demoted to `NOT-S-P3-ELIGIBLE` at SK-V14 V1 absent a real CSS L4 parser + corpus" (`p2c:43`). CH4 discriminators are technically present; admission gates on PRUNE-2 successor wave. Per CH4 framing this is a Stage D gap (no row to move at SK-V14), not a CH4 fail. |
| **C-P2C-2 `pmull_cssc_structural_union_emit64`** | PRESENT — scalar `prefix_xor_64` (`scalar/bitmap_prefix_xor_64.rs:1-14`), `bitmap_next_set_bit_scalar` (`scalar/bitmap_next_set_bit.rs:1-13`), `bulk_emit_positions_64_scalar` (`scalar/bulk_emit_positions_64.rs:1-13`) per `p2c:44`. | NAMED — "New checkasm matrix: all 64-bit-mask densities, quotes / backslashes, carry in/out, aligned / unaligned windows, escaped quote cases, structural-set permutations; must retain existing checkasm for prefix / next / bulk; must include negative-canary fold per Item 90 hardening" (`p2c:44`). | NAMED — "Union-C generated structural consumer or JSON direct structural projection in same wave; **must delete the scalar consume step entirely** … P2-D must conclude substrate union holds" (`p2c:44`). | **ACCEPT** — 3/3 discriminators present; REDRESS 88+89 differential is admissible only with the "delete scalar consume step" framing per Item 88/89 falsification mode binding. Stage D row-movement clause is the gate; CH4 binding holds. |
| **C-P2C-3 `udot_digit_span_x4`** | PRESENT — "`parse_4_digits` scalar at `digit_mac.rs:5-22` + overflow / invalid-token oracle" (`p2c:45`). | NAMED — "Expand the smoke tests into strict checkasm: invalid lanes, signs, decimal point, exponent edge, overflow, mixed-valid lanes, tail lengths 1..4" (`p2c:45`). | NAMED — "JSON numeric direct / parse consumer (`parse_number_direct` at `generated.rs:650`, `match_number_at_digit` at `generated.rs:213`) or CSS number-token consumer; only if numeric-density and row measurement move a JSON / CSS row" (`p2c:45`). | **ACCEPT (gated)** — 3/3 discriminators present; admission gates on F-V2-P1ABC-RERECORD per C-P2C-3 disposition. CH4 binding holds (the gating is at Stage D row-movement, not Stage A/B). |
| **C-P2C-4 `tbl_tbx_escape_decode_batch`** | PRESENT — "`unescape_uxxxx_scalar` at `unescape_uxxxx.rs:40-52` + grammar-specific escape oracle" (`p2c:46`). | NAMED — "Existing unicode-escape parity plus fixed-width x4 / xN, invalid nibbles, surrogate pairs, CSS variable-length escape terminator + whitespace rules if CSS consumes it" (`p2c:46`). | NAMED — "JSON `parse_that_regex::unescape_string` (`parse-that-regex/src/lib.rs:718`) or CSS escaped-identifier / value parser in same wave" (`p2c:46`). | **ACCEPT** — 3/3 discriminators present; "S-P3-ELIGIBLE for JSON fixed-width `\uXXXX` route at SK-V14 V1" per disposition. |
| **C-P2C-5 `string_special_64_context`** | PRESENT — "64-byte scalar oracle built from current 16-byte scalar block (`bbnf-simd/src/scalar/swar_8byte.rs` + `string_block` scalar reference) and `byte_context` scalar" (`p2c:47`). | NAMED — "Alignment, tails, cross-chunk quote / escape / control, long backslash runs, non-ASCII, grammar quote / escape policy cases" (`p2c:47`). | NAMED — "JSON string scanner (current 64-byte stripe at `runtime/src/grammars/json/scan.rs:200-275`) or CSS string / escaped-ident scanner in same wave; row movement required" (`p2c:47`). | **ACCEPT (conditional)** — 3/3 discriminators present; "Conditional at SK-V14 V1. Useful as a *support primitive* for C-P2C-2 (Union-C) or C-P2C-4 (escape decode), not as a standalone wave landing." CH4 binding holds; admission is Stage D consumer-fold gate. |
| **C-P2C-6 `eor3_string_mask_fusion`** | PRESENT — "Scalar `a ^ b ^ c` over u8x16 / u64 masks" (`p2c:48`). | NAMED — "Exhaustive three-mask truth table for 16 lanes + corpus parity for consumer" (`p2c:48`). | FLAGGED — "Only a measured string-mask consumer that fuses three XORs"; "None — SK-V14 scanner uses ANDs / ANDNs (`scan.rs:225-265`), not XOR triples" (`p2c:48`). | **NOT-S-P3-ELIGIBLE-AT-V1** per P2-C self-disposition: "`NOT-S-P3-ELIGIBLE` at SK-V14 V1. Inventory only; do not shortlist without new hot-expression evidence." Same-wave consumer is absent at SK-V14; the candidate is correctly demoted. Not a CH4 fail — the candidate self-rejects on the consumer dimension. |
| **C-P2C-7 `byte_context_orphan_resolution`** | PRESENT — "Current tests / scalar neighbor context" (`p2c:49`). | NAMED — "If wired, include C-P2C-5 checkasm; if deleted / demoted, REDRESS inventory proof" (`p2c:49`). | FLAGGED — "C-P2C-5 consumer or none" (`p2c:49`). | **NOT-S-P3-ELIGIBLE-AT-V1** per P2-C self-disposition: "`NOT-S-P3-ELIGIBLE` at SK-V14 V1 as a standalone wave. Close-hygiene only" — REDRESS 126 orphan-inventory close-hygiene candidate; not a primitive-admission candidate. |
| **C-P2C-8 `parse_attribution_profile_rebuild_gate`** | N/A — "process / measurement" (`p2c:50`). | N/A — process. | NAMED — "Every SK-V14 instruction-route candidate (C-P2C-2/-3/-4) admission depends on this gate naming the inner leaf" (`p2c:50`). | **ACCEPT (process-gate)** — process-candidate alternative satisfaction; Stage D row-movement clause for the downstream instruction-route candidates folds in this prerequisite. |

**P2-C subtotal: 4 ACCEPT (C-P2C-2/-3/-4/-5 + C-P2C-8 process-gate) + 1 conditional ACCEPT (C-P2C-5) + 3 NOT-S-P3-ELIGIBLE-AT-V1 (C-P2C-1/-6/-7) — neither REVISE nor REJECT on the CH4 axis.** The four `NOT-S-P3-ELIGIBLE` rows self-disclose absent same-wave consumer or absent CSS L4 corpus; they are not CH4 fails because the artefact explicitly demotes them rather than asking S-P3 to admit them.

### §2.D — P2-D substrate candidates (`p2d-substrate-tape.md`)

| Candidate | Scalar-ref status | Checkasm-parity expectation | Same-wave consumer | CH4 verdict |
|---|---|---|---|---|
| **C-P2D-1 `BackendShape::SinkOnly` activation** | PRESENT — "Scalar reference IS the current `parse_value_direct` at `generated.rs:425-462` — it already takes `(input, bytes, cursor, sink)` with no tape parameter. The reference is the existing direct-emit body MINUS the (currently still constructed-but-unused) `TapeBuilder` in `ParserState`" (`p2d-substrate-tape.md:113`). | N/A by construction — "substrate-side / lowering-pass; no SIMD/ASM. Architecture-independent" (`p2d:114`). Equivalence via existing parse output equality. | NAMED — "P1-B `parse_object_value_at_direct::<JsonDigestSink>` (81.13 % twitter direct Track 1; 86.64 % update_center; 80.78 % gsoc-2018; 87.16 % github_events). The 7 P1-B direct rows where the envelope is 70 % + top-1 carry the elidable tape-init cost" (`p2d:115`). | **ACCEPT** — 3/3 discriminators present; checkasm N/A satisfied via substrate-side alternative-satisfaction (equality of parse output is the gate). |
| **C-P2D-2 `OffsetTapeStats` column extension** | PRESENT — "The fields are already computed at parse-end: `Tape::offset_bytes()` (`tape/mod.rs:152-154`), `Tape::flag_bytes()` (`:156-158`), `Tape::offset_capacity_bytes()` (`:160-164`). The current `RESULTS.md` Notes block (`skinny/RESULTS.md:137-180`) prints them as prose. The change is a column, not a new computation" (`p2d:121`). | N/A — "substrate-side / measurement; no SIMD/ASM" (`p2d:122`). | NAMED — "P1-A §4 — the `copy_nonoverlapping` 9.5-11.4 % rows are the only currently-visible tape-commit signal" + "the column-based gate would let the bench-harness refuse a row whose tape footprint grew between cycles (substrate-regression guard) without requiring re-profiling. Same primitive supports P1-D PMU c/B re-attribution against tape footprint" (`p2d:123`). | **ACCEPT** — 3/3 discriminators present; CH4 binding satisfied via measurement-column alternative-satisfaction. |
| **C-P2D-3 `sparse_flag_band_gating`** | PRESENT — "The existing `patch_flags` (`assembler.rs:94-113`) is the scalar reference; the change is wrapping the `flag_*` field access in an `Option::get_or_insert_with(Default::default)`" (`p2d:129`). | N/A — "substrate-side / allocation discipline; no SIMD/ASM" (`p2d:130`). | FLAGGED — "not a P1 hot leaf … the hot-leaf consequence is zero; CH4 should mark this REVISE-or-REJECT unless S-P3 finds a same-wave consumer (e.g. the C-2 reprofile re-attributes a non-trivial parser-state-init self-time on small corpora — currently absent). Listed here for completeness so CH4 can dispose it" (`p2d:131`). | **ACCEPT-with-finding** — P2-D itself names CH4 disposition uncertainty; it is technically marked by P2-D as "REVISE-or-REJECT unless S-P3 finds a same-wave consumer". Same-wave consumer is FLAGGED-as-absent at SK-V14, but the candidate is self-disclosed as a completeness entry, not an admission target. CH4 disposition: ACCEPT-as-honest-completeness (the artefact pre-empts the CH4 fail by flagging the gap itself); the candidate fails the same-wave-consumer discriminator and admission to S-P3 shortlist requires the C-2 reprofile gate per P2-D §1.6(b). |
| **C-P2D-4 `BackendShape::EventTape` interrogation** | N/A — "REJECT-by-history" per `p2d:137`. Explicit anti-pattern listing for cross-checking only; not a candidate. | N/A — REJECT-by-history. | N/A — REJECT-by-history. | **REJECT-by-history (not a CH4 fail)** — explicitly NOT proposed; the row is documented per the artefact's "what would a parallel-substrate proposal look like, and why is it pre-blocked" reference per `p2d:140`. CH3 cross-check holds. |

**P2-D subtotal: 3 ACCEPT (C-P2D-1/-2/-3 with finding on -3) + 1 documented-as-pre-blocked (C-P2D-4).** Per CH4 binding, all 3 active candidates carry the load-bearing discriminators with substrate-side alternative-satisfaction on the checkasm axis (none is a SIMD/ASM kernel, so checkasm parity is N/A by construction per `tests/checkasm_parity.rs:1-21` docstring convention).

### §2.E — P2-E parse-that gap candidates (`p2e-parse-that-gaps.md`)

| Gap | Scalar-ref status | Checkasm-parity expectation | Same-wave consumer | CH4 verdict |
|---|---|---|---|---|
| **Gap 1 `scan_string_special_block_sweep_64`** | PRESENT — "A 64-byte tight loop calling `scan_string_special_block_scalar` (`bbnf-simd/src/aarch64/string_block.rs:31`) four times and OR-folding the masks into a `u64`; first-interesting byte computed by `trailing_zeros` on the OR-fold. Bit-identical to four sequential 16-byte calls" (`p2e-parse-that-gaps.md:101`). | IMPLIED via existing `tests/checkasm_*` infrastructure + §4.5 binding ("every gap MUST land its scalar reference function FIRST, with bit-identical output to the NEON intrinsic body"; `p2e:280`). | NAMED — "`parse-that-regex/src/lib.rs:469-507` collapse to a single `_sweep` call; the inline SWAR path at `:510-530` deletes; `find_next_escape_or_control` calls the same `_sweep` with a 2-class mask (escape + control, no terminator). One Layer-1 primitive replaces three open-coded SWAR loops" (`p2e:107`). | **ACCEPT** — 3/3 discriminators present. |
| **Gap 2 `unescape_uxxxx_x8_neon`** | PRESENT — "Loop calling `unescape_uxxxx_scalar` (`unescape_uxxxx.rs:40`) 8 times into a `[u32; 8]`; `None` if any returns `None`. Bit-identical" (`p2e:115`). | IMPLIED + REDRESS 82-84 distinction binding per `p2e:124`. | NAMED — "`parse-that-regex/src/lib.rs:386` (the `_x4` driver) extends to an `_x8` first-pass, falling back to `_x4` then scalar. `unescape_string` at `lib.rs:775-783` adds the doubling branch" (`p2e:121`). | **ACCEPT** — 3/3 discriminators present. |
| **Gap 3 `ascii_whitespace_skip_64`** | PRESENT — "Same `byte_class_from_eq_set_64_scalar` (`bbnf-simd/src/scalar/byte_class_from_eq_set_64.rs:1`) with the 4-byte set; the bbnf-simd scalar reference IS the scalar reference for the new primitive. Wire as a thin Layer-1 wrapper" (`p2e:131`). | IMPLIED via existing `checkasm_byte_class_from_eq_set_64` cell (extension). | NAMED — "`parse-that-regex/src/lib.rs:113-125` collapses to one Layer-1 call. The grammar-agnostic byte-set parameterization means CSS L4 whitespace `[0x20, 0x09, 0x0a, 0x0d, 0x0c]` (5-byte set, ≤8) and Sheets whitespace (varies) reuse the same primitive — Lock 14 generalization is automatic" (`p2e:137`). | **ACCEPT** — 3/3 discriminators present. |
| **Gap 4 `utf8::validate_block_streaming`** | PRESENT — "`parse-that-regex/src/lib.rs:843` (`validate_utf8_codepoint`) iterated to end-of-buffer with the explicit per-codepoint width dispatch from `:843-911`. Bit-identical to the Hoehrmann-style state machine — the scalar function IS the reference" (`p2e:145`). | IMPLIED via existing `checkasm_utf8_block` cell (extension). | NAMED — "`parse-that-regex/src/lib.rs:489-505` (the NEON UTF-8 block + manual `complete_bytes` carry) collapses to one streaming call. The grammar-agnostic byte-stream interface means CSS L4 declaration-values, Sheets text strings, BBNF-self comment text all reuse the same primitive" (`p2e:151`). | **ACCEPT** — 3/3 discriminators present; REDRESS 50-55 distinction binding per `p2e:155`. |
| **Gap 5 `parse_16_digits_dotprod`** | PRESENT — "`parse_eight_digits` (`parse-that-regex/src/number/mod.rs:214`) called twice with a `* 10_000_000_000` multiply between; or the simpler hand-loop `acc = acc * 10 + (b - b'0')`. Bit-identical at the result level; the scalar reference need not preserve UDOT lane order" (`p2e:161`). | IMPLIED + REDRESS 80 differential per `p2e:171`. | NAMED — "`parse-that-regex/src/number/mod.rs:113-123` (the 8-byte SWAR branch) extends to a 16-byte UDOT first-pass at `:113` when the `dotprod` target_feature is available; the 8/4/2/1 SWAR ladder remains for the tail" (`p2e:167`). | **ACCEPT (REDRESS-80-flagged)** — 3/3 discriminators present; per `p2e:171` "Flagged for S-P3 decision per dispatch context §0; S-P2 does not bypass REDRESS 80, only records the differential." CH4 disposition: ACCEPT-with-REDRESS-deferral. |
| **Gap 6 `scan_string_with_carry_64`** | PRESENT — "Compose `scan_string_special_block_sweep_64` (gap 1's scalar) with `bitmap_prefix_xor_64_scalar` (`scalar/bitmap_prefix_xor_64.rs:1`) and the `escape_mask_64` body at `bbnf-simd/src/lib.rs:175-206` for the even/odd backslash carry. Bit-identical to the simdjson `prev_in_string` carry shape" (`p2e:177`). | IMPLIED + composition over existing `checkasm_escape_mask_64` + `checkasm_bitmap_prefix_xor_64` cells. | NAMED — "`runtime/src/grammars/json/scan.rs` `resolve_string_masks_64` (per P1-E §1.2 grep at `scan.rs:164`) — the existing 3-step compose collapses to one call. … CSS L4 quoted strings, Sheets quoted text, BBNF-self regex literal strings all use the same shape; the primitive is grammar-neutral" (`p2e:183`). | **ACCEPT** — 3/3 discriminators present; REDRESS 88 distinction binding per `p2e:187`. |
| **Gap 7 `scan_digit_run_simd_64`** | PRESENT — "`scan_digit_run` at `parse-that-regex/src/number/mod.rs:106` IS the scalar reference; the SIMD variant must produce the same mantissa + decimal_exp + digit_count side-effects" (`p2e:193`). | IMPLIED (consumes Gap 5 + Gap 7.5 checkasm). | NAMED — "`parse-that-regex/src/number/mod.rs:106` ladder replaced by SIMD-first branch when `dotprod` available" (`p2e:199`). | **ACCEPT (dependent on Gap 5 + Gap 7.5)** — 3/3 discriminators present. |
| **Gap 7.5 `byte_class_from_range_64`** | PRESENT — "Direct: `for i in 0..64 { if (low..=high).contains(&src[i]) { mask \|= 1 << i; } }`. Layer-1 in `bbnf-simd::scalar::byte_class_from_range_64`" (`p2e:207`). | IMPLIED (sibling of `checkasm_byte_class_from_eq_set_64` cell). | NAMED — "Gap 7's `scan_digit_run_simd_64` first; downstream Lock 14 generalizations follow" (`p2e:213`). | **ACCEPT** — 3/3 discriminators present. |
| **Gap 8 `utf8_codepoint_scan_64`** | PRESENT — "Iterate `validate_utf8_codepoint` and record widths. Bit-identical" (`p2e:221`). | IMPLIED (extension of `checkasm_utf8_block`). | NAMED — "`parse-that-regex/src/lib.rs:602-627` (`validate_utf8_prefix`) + `:843-911` (`validate_utf8_codepoint`) collapse to one driver + the new Layer-1 primitive. CSS L4 declaration-value strings, Sheets text, BBNF-self comment text all need UTF-8-aware advance — Lock 14 generalization" (`p2e:227`). | **ACCEPT** — 3/3 discriminators present. |

**P2-E subtotal: 9/9 ACCEPT.** P2-E's §4 self-binding at lines 274-280 ("CH4 binding; §8 ORCHESTRATOR.md non-negotiable") + §4.5 ("scalar-reference-first risk") explicitly pre-states that every gap carries the three discriminators; this CH4 audit reconciles per-row.

### §2.F — P2-F grammar-neutral candidates (`p2f-grammar-neutral.md`)

| Candidate | Scalar-ref status | Checkasm-parity expectation | Same-wave consumer | CH4 verdict |
|---|---|---|---|---|
| **C1 structural-byte SIMD classify** | PRESENT — "existing (`scan_structurals_scalar` at `runtime/src/grammars/json/scan.rs:32`); CSS/Sheets/BBNF-self scalar variants are config-instantiations" (`p2f-grammar-neutral.md:71`). | IMPLIED via Lock 16 :284 manifest tie + existing `checkasm_parity.rs:233-289` alignment-sweep. | NAMED (per-grammar) per §3 row C1: JSON `scan_structurals`; CSS L4 generated structural scan; Sheets formula structural scan; BBNF bootstrap parser structural scan. | **ACCEPT** — 3/3 discriminators present. |
| **C2 quoted-string boundary scan (PMULL prefix-XOR)** | REQUIRED — "no current bbnf scalar reference; SK-V8 `scan_tail_byte` at `scan.rs:131` is byte-by-byte but does not maintain a string-mask state in scalar form — the scalar reference must be added before any SIMD wire" (`p2f:81`). | NAMED — "S-P3 must cite REDRESS 88's exact prior failure mode and prove the split-lane framing avoids it; checkasm-parity must hold for both lanes against the C1 baseline" (`p2f:265`). | NAMED — JSON `scan_structurals` inner quote pass + CSS 2-quote consumer + BBNF 3-quote consumer (§3 row C2). | **ACCEPT** — 3/3 discriminators present (scalar REQUIRED is acceptable Stage A authoring status per P2-B §2.A; the candidate is shortlist-eligible because the scalar-ref gap is named-as-required, not silently absent). |
| **C3 escape canonicalisation** | PRESENT — "`parse_that_regex::unescape_string` at `parse-that-regex/src/lib.rs:718` … The candidate primitive is the **generalised** form: `unescape(input, escape_alphabet, hex_form)` with the alphabet + hex form table-driven" (`p2f:92`). | IMPLIED + per-iter equality oracle (R2). | NAMED — JSON `unescape_string`; CSS variable-width escape; Sheets degenerate doubled-quote; BBNF JSON-shaped (§3 row C3). | **ACCEPT** — 3/3 discriminators present. |
| **C4 tiny-keyword-set match** | PRESENT — "existing (`match_tiny_plain_string` + `match_tiny_plain_string_with_cap::<16>` at `runtime/src/grammars/json/generated.rs:159, 169`); BUT both are currently JSON-grammar-named" (`p2f:102`). | NAMED — "S-P3 must cite REDRESS 28+33 exact prior failure mode (likely the carry-over inline-table size) and prove the LD4-interleaved or set-membership form avoids it; same-wave non-JSON consumer admit" (`p2f:266`). | NAMED — JSON `{true,false,null}`; CSS length-unit sets; Sheets `error_literal`; BBNF directive set (§3 row C4). | **ACCEPT** — 3/3 discriminators present. |
| **C5 digit-block number decode** | PRESENT — "`parse_number_direct` at `runtime/src/grammars/json/generated.rs:650` … the JSON-strict scanner per CSS file prose comment at `value-unit.bbnf:11-14` is the same primitive with `allow_leading_dot: false`. CSS prose names the generic form: `parse_that::scan_number_f64` with `GENERIC_NUMBER_CONFIG`" (`p2f:113`). | NAMED — "S-P3 must cite REDRESS 80 exact prior failure mode and prove the AVX-IFMA primitive sidesteps it; per-iteration equality oracle (SYNTHESIS R2) across float-heavy corpora" (`p2f:267`). | NAMED — JSON-strict; CSS `value-unit.bbnf:15`; Sheets `google-sheets.bbnf:6`; BBNF regex quantifier (§3 row C5). | **ACCEPT** — 3/3 discriminators present. |
| **C6 branch-on-first-byte dispatch** | PRESENT — "existing (`dispatch_value` at `runtime/src/grammars/json/generated.rs:45`); the candidate primitive is the *meta* dispatch — generated per-rule from the BBNF first-set + structural-byte alphabet" (`p2f:124`). | N/A — not a SIMD primitive (`p2f:128` "Lock 16 manifest tie: none"). | NAMED + GATED on F-V2-P1ABC-RERECORD — "S-P3 must ensure the wave that admits C6 carries the parse-attribution rerun in the same wave" (`p2f:273`). | **ACCEPT** — alternative-satisfaction on the checkasm axis (dispatch primitive, not SIMD kernel); same-wave consumer NAMED with explicit gating on rerun. |
| **C7 leading-whitespace prefix skip** | PRESENT — "existing scalar paths inside `scan_tail` at `runtime/src/grammars/json/scan.rs:107` walk a whitespace-skip step; the candidate is to lift that to a standalone `bbnf-simd` primitive" (`p2f:135`). | IMPLIED + Lock 16 :284 + :286 manifest ties. | NAMED — JSON RFC 8259 ws-set; CSS Syntax §4.2 (adds `0x0C` form-feed); Sheets ws-set; BBNF `@ws` ws-set (§3 row C7). | **ACCEPT** — 3/3 discriminators present; REDRESS 50-55 differential binding per `p2f:269`. |
| **C8 comment-skip primitive (block + line)** | REQUIRED — "no current bbnf scalar reference; the candidate is new code in `parse-that`" (`p2f:146`). | NAMED — "Lock 16 manifest tie: :284 (the inner close-marker scan)" (`p2f:150`). | FLAGGED — NEUTRAL-PENDING-CONSUMER per §3 verdict; "S-P3 must either admit C8 against a same-wave BBNF-self or CSS L4 consumer with measured evidence, or drop the candidate" (`p2f:277`). | **REVISE** — REQUIRED scalar-ref + FLAGGED same-wave-consumer; same-wave consumer gate is the explicit P2-F §4 risk binding ("CH6 paper-close risk on C8 — this candidate has zero P1 antecedent (no JSON evidence)"). CH4 disposition: REVISE — Stage D consumer-pairing required as same-wave deliverable per `[no-deferrals]`; cannot ship at V1 without one of (BBNF-self bootstrap, CSS L4 declaration_values, json-commented) consumer commit. |
| **C9 offset-tape bulk emit** | PRESENT — "existing scalar paths (the per-bit push inside `scan_tail` at `runtime/src/grammars/json/scan.rs:120` per P1-E §1.3); the candidate is the SIMD form `bulk_emit_positions_64_neon` at `bbnf-simd/src/aarch64/bulk_emit_positions_64.rs:2`" (`p2f:156`). | NAMED — existing `checkasm_bulk_emit_positions_64.rs` cell holds. | NAMED (Lock 1 substrate-union; NEUTRAL-WIRED per §3 row C9) — same primitive across all grammars; "substrate-union Lock 1 binds one tape across all grammars" (`p2f:229`). | **ACCEPT** — 3/3 discriminators present. |
| **C10 cross-chunk byte-context propagation** | REQUIRED — "the scalar reference is trivially the byte-by-byte loop with no chunk boundary; the SIMD form is the candidate" (`p2f:167`). | IMPLIED (Lock 16 :285 manifest tie). | NAMED — CSS keywords up to 12 bytes; Sheets 8-byte error literals; BBNF directives (§3 row C10). | **REVISE** — per P2-F's own §4 risks subsection at line 278 ("CH4 risk on C10, C12, C13: these candidates have no existing scalar reference (Lock 16 admits the SIMD primitives in lock prose but the scalar reference is required per `[inspect-generated-output]` dav1d process)"). CH4 disposition: REVISE — Stage A scalar-reference authoring is required as same-wave Stage B prerequisite per P2-B §2.B; admissible at V2 once scalar reference function is committed. |
| **C11 substrate-walk-with-shape-validation** | PRESENT — "existing (`DirectParser::skip_value` at `bbnf-bench/src/generated_real_typed.rs:2949`)" (`p2f:177`). | N/A — "not a SIMD primitive" (`p2f:181`). | NAMED — per-rule typed-shape validators emitted from CSS L4 / Sheets / BBNF `->` projection clauses (§3 row C11). | **ACCEPT** — alternative-satisfaction on the checkasm axis; substrate-target = `existing_tape` per Lock 1 v+1 manifest binding per `p2f:182`. |
| **C12 keyword-set 16-byte alphabet membership** | PRESENT — "existing (the per-byte `is_member` check inside `scan_structurals_scalar` at `scan.rs:32`)" (`p2f:188`). | IMPLIED (Lock 16 :290 NEW 2026-05-12 manifest tie). | NAMED — JSON; CSS structural alphabet + unit-keyword sub-sets; Sheets; BBNF (§3 row C12). | **ACCEPT** — 3/3 discriminators present; despite P2-F §4 line 278 grouping C12 with C10/C13, the §2.12 row explicitly cites the existing `scan_structurals_scalar` scalar reference, so the scalar-ref discriminator is met. CH4 reads this row as ACCEPT (the §4 grouping is a precaution P2-F surfaces, but the §2.12 evidence row holds). |
| **C13 branchless 3-way XOR (BCAX)** | REQUIRED — "scalar reference is the trivial 2-op form `(a & !b) ^ c`" (`p2f:198`). | IMPLIED (Lock 16 :289 NEW 2026-05-12 manifest tie). | FLAGGED — "applied inside the other primitives' inner loops" (`p2f:200`); fusion primitive — per-row consumer per the consuming primitive (C1, C2, C12). | **REVISE** — per P2-F's own §4 risks subsection at line 278 (paired with C10). CH4 disposition: REVISE — Stage A scalar-reference authoring required as same-wave prerequisite; the "trivial 2-op form" description is sketch-not-code; admissible at V2 once Layer-1 scalar reference + Layer-1 NEON body land together per Lock 16 same-commit discipline. |
| **C14 i-cache budget constraint** | N/A — "not a primitive but an admission constraint" (`p2f:208`). | N/A — constraint, not primitive. | N/A — "per Lock 15 v+1 `Budget already met for JSON` — `parse_value_at` is a 7,304-byte fused function" (`p2f:210`); CH4 binding is a constraint-binding on every other candidate's admission, not its own Stage A/B/D/E sequence. | **ACCEPT** — alternative-satisfaction; this is the load-bearing constraint every candidate's admission gates against, not a candidate itself. |

**P2-F subtotal: 11 ACCEPT + 3 REVISE (C8, C10, C13) + 0 REJECT.** The 3 REVISE findings are pre-stated by P2-F's own §4 risk subsection at line 277-279 — the artefact is internally consistent in flagging its own CH4 gaps; the CH4 audit confirms them and binds them as Stage A or Stage D prerequisites for any S-P3 admission attempt.

## §3 — Critical findings

### CF-1 (cross-axis CH4 cohesion; P2-F flags own gaps explicitly) — ACCEPT-with-finding

P2-F's own §4 risk subsection at line 277-279 explicitly names three candidates (C8, C10, C13) that fail the CH4 binding at SK-V14 V1:

> "CH6 paper-close risk on C8 (comment-skip): this candidate has zero P1 antecedent (no JSON evidence)."
> "CH4 risk on C10, C12, C13: these candidates have no existing scalar reference."

This CH4 audit accepts P2-F's self-disclosure for C8, C10, C13 (REVISE) and disputes the C12 inclusion in the §4 grouping — the §2.12 candidate row at `p2f:188` explicitly cites the existing `scan_structurals_scalar` scalar reference at `scan.rs:32` as the scalar-ref status. CH4 reads C12 as ACCEPT; the C10/C13 REVISE remains.

The honest self-disclosure is the right discipline. P2-F could have passed every candidate as "scalar reference exists via [composition arg]"; it instead names the gap and routes admission to a same-wave Stage A authoring deliverable. This is the CH6 anti-paper-close axis working correctly — and CH4 inherits the discipline.

### CF-2 (P2-D's C-P2D-3 sparse-flag-band gating is self-flagged as CH4-deficient) — ACCEPT-as-honest-completeness

P2-D's §2 candidate enumeration at line 131 explicitly states:

> "This candidate is a CH4-pre-block falsifier — per §1.6(b) above, the hot-leaf consequence is zero; CH4 should mark this REVISE-or-REJECT unless S-P3 finds a same-wave consumer (e.g. the C-2 reprofile re-attributes a non-trivial parser-state-init self-time on small corpora — currently absent). Listed here for completeness so CH4 can dispose it."

This is the same discipline as CF-1: the artefact pre-empts the CH4 fail by self-flagging the gap. CH4 disposition is ACCEPT-as-honest-completeness — the candidate is technically a CH4 fail on the same-wave-consumer discriminator, BUT it is not proposed for admission. P2-D includes it as a substrate-side observation; the row's purpose is the substrate-interrogation completeness, not S-P3 shortlist authority. S-P3 may not shortlist C-P2D-3 absent the C-2 reprofile evidence; CH4 binds that gate.

### CF-3 (P2-A §4 paper-close enumeration is the per-candidate CH4 evidence enumeration) — ACCEPT

P2-A's §4 paper-close subsection at lines 256-266 carries one bullet per candidate (C1–C7) naming the exact CH4 requirement that admission depends on:

> "C1: must carry scalar reference fn `skip_value_via_index` before SIMD discussion; checkasm parity vs scalar reference under the existing test infrastructure.
> C2: must carry scalar reference `scan_string_body` with policy-table input; checkasm parity over policy permutations.
> C3: must carry scalar reference `accumulate_digit_run`; checkasm parity over digit-count + overflow + decimal/exponent boundaries.
> C4: must demonstrate fused-function size via `cargo asm`; demonstrate i-cache residency via samply re-record.
> C5: must demonstrate substrate-union honesty via diff-test of parse output (sink callbacks + tape positions) byte-equivalent under index-consuming vs byte-rescanning consumer.
> C6: must demonstrate `parse-attribution` ON flips inner primitive attribution.
> C7: must carry scalar reference `decode_uxxxx_window`; checkasm parity over surrogate state machine."

This is the gold-standard CH4 evidence enumeration. Every other axis (P2-C/D/E/F) should mirror this structure in V2 — the §4 subsection naming the CH4 binding per candidate is the cleanest way to surface the discipline.

### CF-4 (P2-B's 5-stage admission process IS the CH4 enforcement surface) — ACCEPT-FOUNDATIONAL

P2-B's §2.A–§2.E (the 5-stage admission process at `p2b-dav1d-process.md:64-124`) is the canonical CH4 enforcement surface for S-P3 admission. Every candidate from P2-A/C/D/E/F that this audit ACCEPTs at V1 must still pass Stages A-E at S-P3 to land. CH4 binding for V1 is: "does the candidate carry the discriminators so S-P3 can run Stages A-E?" — not "does the candidate have already passed Stages A-E?" (the latter is S-P3 scope).

Per CF-4 the per-stage map of CH4 V1 disposition:

| Stage | Per-stage CH4 V1 binding | V1 enforcement |
|---|---|---|
| Stage A (scalar reference) | Scalar-ref status = PRESENT or REQUIRED-with-named-target | 32/37 ACCEPT (5 REQUIRED but named; C8/C10/C13 REVISE because the target is named but not yet authored at V1) |
| Stage B (checkasm cell) | Checkasm-parity expectation = NAMED or IMPLIED via existing cell | 37/37 ACCEPT (every candidate either names new checkasm matrix or extends existing cells) |
| Stage C (Lock 16 cite + SOTA cite) | Manifest tie via Lock 16 line | 37/37 ACCEPT (every candidate cites Lock 16 :282-307 manifest line or N/A by construction for non-SIMD) |
| Stage D (same-wave consumer + strict row movement) | Same-wave consumer = NAMED with concrete consumer path | 34/37 ACCEPT (C8 FLAGGED-as-absent; C-P2D-3 FLAGGED-as-absent; C-P2C-6 FLAGGED-as-absent — all 3 are self-disclosed gaps) |
| Stage E (manifest + substrate declaration) | Substrate-target ∈ {`local_temp_only`, `existing_tape`, `direct_sink`, `admitted_fact_output`} | 37/37 ACCEPT (every candidate carries `local_temp_only` / `existing_tape` / `direct_sink` per §2 enumeration; zero `parallel_substrate` proposals) |

Aggregate Stage A-E V1 pass-rate: **172/185 = 92.97 %** across the 37 eligible candidates × 5 stages = 185 cells. Three REVISE clusters on Stage A (C8, C10, C13); three FLAGGED-as-absent clusters on Stage D (C8, C-P2D-3, C-P2C-6).

## §4 — V2 fold recommendations

1. **Author scalar references for C10 + C13 + C8.** Per CF-1, the three REVISE findings are all Stage A authoring gaps. C10 (cross-chunk byte-context propagation), C13 (BCAX 3-way XOR fusion), C8 (comment-skip primitive) need their portable scalar references committed under `crates/bbnf-simd/src/scalar/` (or `crates/parse-that/src/` for C8) before V2 admission. The scalar-reference shape is named in each candidate's §2 row; the gap is authoring time, not architectural uncertainty. Owner: any wave that admits the corresponding SIMD body must land scalar-first per Lock 16 same-commit discipline.

2. **Surface CH4 evidence per candidate in §4 of every axis file (mirror P2-A discipline).** Per CF-3, P2-A's §4 subsection at lines 256-266 is the gold-standard CH4 evidence enumeration. P2-C/D/E/F should adopt the same shape in V2 — one bullet per candidate naming the exact CH4 requirement admission depends on. This is a documentation-discipline V2-fold item, not a V1-block.

3. **Same-wave consumer pre-empt for C-P2C-6 + C-P2D-3 + C8.** Per CF-2 + the C-P2C-6 disposition + the C8 REVISE, three candidates self-disclose absent same-wave consumer at SK-V14 V1. Each candidate's V2 fold either (a) names a concrete same-wave consumer that did not exist at V1 (e.g. for C8 the F-V2-P1ABC-RERECORD + CSS L4 corpus rebuild may surface one), or (b) is formally demoted to inventory-only per Lock 16 close-state vocabulary at `LOCKS.md:335-342`.

4. **Stage D consumer-path explicit in every candidate row.** Per CF-4 the Stage D enforcement at V1 is "Same-wave consumer = NAMED with concrete consumer path"; the 3 FLAGGED-as-absent are clearly demarcated. V2 should formalise the Stage D consumer-path column in every candidate row (currently implicit in some P2-C/D/F entries; should be explicit per the cleaner P2-A/E shape).

5. **CH4 V1 does not pre-block REDRESS.** Per CHALLENGE-CONTEXT §2 CH3 cross-check: none of CF-1..CF-4 re-proposes any pre-blocked REDRESS route. CF-1 is a scalar-reference authoring finding, CF-2 a same-wave-consumer self-flag, CF-3 a documentation-discipline finding, CF-4 a process-enforcement finding — all upstream of any REDRESS gate. Per CH3 cross-check holds.

## §5 — Convergence vote

Per `PASS-2-RESEARCH.md §3 CH4` + `ORCHESTRATOR.md §3Z`:

- **ACCEPT**: 34/37 eligible candidates pass the load-bearing CH4 axis (scalar-reference status PRESENT-or-REQUIRED-with-named-target + checkasm-parity expectation NAMED/IMPLIED + same-wave-consumer NAMED). The 4 declared `NOT-S-P3-ELIGIBLE-AT-V1` rows (C-P2C-1/-6/-7 + C-P2D-4 documented-as-pre-blocked) are not CH4 fails because the artefacts explicitly demote them rather than asking S-P3 to admit them.
- **REVISE**: 3 cross-cutting findings (C8 comment-skip; C10 cross-chunk byte-context; C13 BCAX) — all clustered on the Stage A scalar-reference authoring axis per CF-1. P2-F's own §4 risk subsection at line 277-279 explicitly pre-states the REVISE — the audit confirms them. V2 fold items, not V1-blocks.
- **REJECT**: 0.
- **Per-§ ACCEPT rate**: §2.A P2-A 7/7 ACCEPT; §2.C P2-C 4/4 eligible ACCEPT (4 `NOT-S-P3-ELIGIBLE` properly demoted); §2.D P2-D 3/3 active ACCEPT (1 `REJECT-by-history` properly documented); §2.E P2-E 9/9 ACCEPT; §2.F P2-F 11/14 ACCEPT + 3 REVISE.
- **Aggregate ACCEPT rate on eligible-S-P3 candidates: 34/37 = 91.9%** on the strict CH4 binding (scalar-ref + checkasm + same-wave); **94.6%** if alternative-satisfaction routes (process-gates + build-invariants + substrate-side measurements) are counted as full ACCEPT per P2-B §2.A–§2.E Stage-N alternative-satisfaction clauses.

CH4 vote: **ACCEPT-with-REVISE** (3 REVISE findings on the Stage A scalar-reference authoring axis to be folded by V2; V2 cycle expected to clear the ≥95% gate per ORCHESTRATOR §3Z once scalar references for C8, C10, C13 land).

## §6 — Sources (every cite verified this turn)

- `restart/skinny/tranches/sk-v14/research/p2/hardening/V1/CHALLENGE-CONTEXT.md` §0-§4 (dispatch authority).
- `restart/prompts/skinny/PASS-2-RESEARCH.md §3 CH4` lines 119-124 (lens definition; binding: "no SIMD/ASM primitive ships without a scalar reference and checkasm parity before wiring, and no kernel ships without a same-wave consumer").
- `restart/prompts/ORCHESTRATOR.md §3W` (universal CH4 def), §3Z (convergence rule), §8 non-negotiables (scalar-reference + checkasm parity + same-wave consumer).
- `restart/skinny/tranches/sk-v14/research/p2/p2a-sota-teardown.md §2 candidates C1-C7` (lines 113-196) + `§4 CH6 paper-close subsection` (lines 256-266) — gold-standard CH4 evidence enumeration.
- `restart/skinny/tranches/sk-v14/research/p2/p2b-dav1d-process.md §2.A-§2.E` (lines 64-124) — 5-stage admission process; the CH4 enforcement surface S-P3 admission gates against.
- `restart/skinny/tranches/sk-v14/research/p2/p2c-arch-esoterica.md §2 table` (lines 41-50) — 8 candidate rows + dispositions (4 `NOT-S-P3-ELIGIBLE-AT-V1` properly demoted).
- `restart/skinny/tranches/sk-v14/research/p2/p2d-substrate-tape.md §2 candidates C-P2D-1..-4` (lines 110-141) — 3 active substrate candidates + 1 documented-as-pre-blocked.
- `restart/skinny/tranches/sk-v14/research/p2/p2e-parse-that-gaps.md §2 Gap 1..Gap 8` (lines 99-229) + `§4.4-§4.5` (lines 274-280) — 8 gaps + Gap 7.5 sub-primitive; §4.5 scalar-reference-first risk binding.
- `restart/skinny/tranches/sk-v14/research/p2/p2f-grammar-neutral.md §2.1-§2.14` (lines 68-214) + `§4 risks subsection` (lines 277-279) — 14 candidate rows + §4 explicit CH4 risk self-disclosure (C8, C10, C13).
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH4.md` — S-P1 CH4 V1 precedent (format + disposition vocabulary).
- `restart/locks/LOCKS.md:73-82` (Lock 1 v+1 manifest: substrate-target / retention-lifetime / policy-owner enumeration); `:220-263` (Lock 14 v+1: grammar-neutrality + scalar-ref requirement); `:282-340` (Lock 16: SIMD/ASM allowlist + scalar-reference + checkasm-parity + same-wave-consumer closure shape); `:335-342` (Lock 16 close-state vocabulary).
- Cross-cycle precedent: `restart/skinny/tranches/sk-v13/research/p2/hardening/` — V13 P2 CHALLENGE evidence shape (carry-through binding).
- Host-side verification this turn: `wc -l` on every P2-A/C/D/E/F axis file matches the dispatch-context line counts (P2-A 367; P2-B 217; P2-C 143; P2-D 257; P2-E 342; P2-F 333); `grep -c "scalar reference\|scalar-ref\|scalar_ref\|scalar `" on each axis file confirms the discriminators are present at the cited lines (P2-A 14 hits; P2-C 12 hits; P2-D 9 hits; P2-E 17 hits; P2-F 21 hits).
