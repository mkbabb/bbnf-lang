# BD.W6 — BD Close + Carry Ledger Closure

**Name**: W6 — BD Close + Carry Ledger Closure
**Opens after**: BD.W3 + BD.W4 + BD.W5 close (all sister crates published; fleet-wide fixtures landed; cross-backend parity verified)
**Hard gate**: PROGRESS / FINAL artefacts; carry ledger CLOSED (no successor); 13-lock cross-reference verified empty-cell-free; all BD-G gates green; the trilogy (BA + BB + BC + BD) closes
**Status**: planned

## §1 Deliverable

Hereupon BD closes. The closure ceremony walks the seven preceding waves (BD.W0 through BD.W5), verifies every BD-G gate has a closure citation, walks the 13-lock cross-reference table empty-cell-free, records the FINAL.md attestation, and CLOSES the carry ledger. There is no successor. There is no BE. There is no BF. There is no BG. The plan ends here. Phase 5 hardening verifies the close.

The wave is the closure ceremony; no new substrate lands. The substrate landed in W0-W5; W6 verifies, documents, hands off NOTHING (the carry ledger is empty post-close). The hand-off is to Phase 5 hardening, which is a verification phase, not a continuation phase.

## §2 Milestones

### §2.1 PROGRESS.md commit

Mechanism: `docs/tranches/BD/PROGRESS.md` records the wave-by-wave timeline. Each wave's open + close timestamp + closer-gate verification. The format mirrors `docs/tranches/BC/PROGRESS.md` (per BC.W6 §2.5, `docs/tranches/BC/waves/W6.md:48-54`).

```markdown
# BD Tranche Progress

## Wave Timeline

| Wave | Open | Close | Closer-gate verification |
|---|---|---|---|
| W0 | <timestamp> | <timestamp> | path-ts cdylib builds; smoke test passes |
| W1 | <timestamp> | <timestamp> | @bbnf-lang/runtime ships; BD-G1 met (8 ms median) |
| W2 | <timestamp> | <timestamp> | @bbnf-lang/runtime-wasm ships; BD-G2 met (2.5 ms median) |
| W3 | <timestamp> | <timestamp> | sister crates published; cargo-semver-checks clean |
| W4 | <timestamp> | <timestamp> | fleet-wide fixtures materialised; CI matrix expanded |
| W5 | <timestamp> | <timestamp> | 81-cell parity matrix green |
| W6 | <timestamp> | <timestamp> | this wave |
```

Files: `docs/tranches/BD/PROGRESS.md` (create).

Sub-gate: PROGRESS.md exists; every wave row is populated; cross-references resolve.

### §2.2 FINAL.md commit

Mechanism: `docs/tranches/BD/FINAL.md` records the closing artefacts: the perf gate metrics, the publication ledger, the parity matrix attestation, the 13-lock verification, the carry ledger closure, the close posture.

```markdown
# BD Tranche Final

## Closing Attestation

Hereupon BD closes. The trilogy (BA + BB + BC + BD) closes. The 13 architectural locks are settled.

### Perf gate metrics

| Gate | Target | Measured | Anchor |
|---|---|---|---|
| BD-G1 | ≤ 8 ms twitter.json on Node 20.x | <measured> ms median | `docs/tranches/BD/audit/W1-bd-g1-ratification.md` |
| BD-G2 | ≤ 2.5 ms twitter.json WASM on Node 20.x | <measured> ms median | `docs/tranches/BD/audit/W2-bd-g2-ratification.md` |

### Publication ledger

| Crate / Package | Published | Version | Path |
|---|---|---|---|
| egraph-derive | crates.io | 0.1.0 | `crates/egraph-derive/` |
| egraph | crates.io | 0.1.0 | `crates/egraph/` |
| csp-solver | crates.io | 0.1.0 | `crates/csp-solver/` |
| bbnf-regex | crates.io | 0.1.0 | `crates/bbnf-regex/` |
| path-core | crates.io | 0.1.0 | `crates/path-core/` |
| path | crates.io | 0.1.0 | `crates/path/` |
| @bbnf-lang/path-ts | npm | 0.1.0 | `crates/path-ts/` |
| @bbnf-lang/runtime | npm | 0.1.0 | `npm/runtime/` |
| @bbnf-lang/runtime-wasm | npm | 0.1.0 | `npm/runtime-wasm/` |

### Parity matrix attestation

81-cell parity matrix passed at BD.W5. Per `docs/tranches/BD/audit/W5-cross-backend-parity-spec.md`.

### Carry ledger CLOSED

No successor. Phase 5 hardening verifies the close.
```

Files: `docs/tranches/BD/FINAL.md` (create).

Sub-gate: FINAL.md exists; every section is populated; the publication ledger matches W3 closure; the parity matrix attestation references W5; the carry ledger closure is explicit.

### §2.3 13-Lock cross-reference verification

Mechanism: walk the 13-lock cross-reference table from `docs/tranches/BD/BD.md`; verify every cell names the wave that addresses the lock; verify no empty cells. The verification document records the per-lock attestation:

```markdown
# 13-Lock Verification

## Per-Lock Attestation

| Lock | Tranche.Wave | Verification |
|---|---|---|
| L1 | BA.W0 + BA.W5 + BB.W1 (closed); BC.W6 (ratified) | Tape + columnar dead; substrate is rust-typed-enums, JS discriminated unions, WASM linear-memory structs |
| L2 | BA.W2 (closed); BC.W0 (ratified at IR contract) | Layout lowering canon; old terms (TypeDesc, StructLayout) retired |
| L3 | BA.W4 + BB.W2 (closed) | Cursor + byte-skip unified; eager fast path elides cursor calls |
| L4 | BB.W3 (closed) | Per-domain orthogonal optimisation; CSP / e-graph / miners output-piped |
| L5 | BD.W0 + BD.W1 + BD.W2 + BD.W5 (production ratification) | IR + per-backend lower; the typed IR contract supports Rust + TS + WASM in production |
| L6 | BC.W1 (closed); BD.W1 + BD.W2 (ratified at xtask emit) | xtask emits committed source; no proc-macro façade except path-ts shell |
| L7 | BD.W0 + BD.W3 (production ratification) | path consolidation; path triplet (path, path-core, path-ts) published |
| L8 | BC.W6 (closed for native); BD.W1 + BD.W2 (engineering-bounded for cross-FFI/WASM) | Surpass sonic-rs / simdjson / lightning-css; native gates met; cross-FFI bounded |
| L9 | BC.W4 (closed); BD.W1 + BD.W2 (ratified) | Slice-borrow primary; bumpalo + owned escape hatches; TS uses Uint8Array views; WASM uses linear-memory ptr+len |
| L10 | BB.W3 (closed) | Pratt + SIMD auto-detected; no annotations |
| L11 | BD.W3 (production ratification) | Path-deps for incubating sister crates; sister crates graduated to crates.io |
| L12 | (closed in pre-BA ceremony) | ser + gorgeous archive BEFORE BA.W0 |
| L13 | BC.W3 (closed at crate level); BD.W4 (ratified at fixture level) | No god directories; cohesive encapsulation at every level |

All 13 locks honoured. No empty cells. The trilogy (BA + BB + BC + BD) closes.
```

Files: `docs/tranches/BD/audit/W6-lock-verification.md` (create).

Sub-gate: every lock has at least one wave cell; no empty cells; cross-reference verified against the wave-by-wave closure.

### §2.4 Carry ledger closure record

Mechanism: BD has no successor. The carry ledger CLOSES at BD.W6. The closure record names: every BC→BD carry was consumed at a BD wave; no BD→BE / BD→BF / BD→BG carries exist; the absence of forward carries IS the gate.

Files: `docs/tranches/BD/audit/W6-carry-ledger-closure.md` (create).

Sub-gate: the closure record names the consumed carries (BC→BD.C1, BC→BD.C2, BC→BD.C3) + the wave where each closed (BD.W0/W1/W2 for C1; BD.W3 for C2; BD.W4 for C3); names the absence of forward carries; `rg -n "BD→B[A-Z]\." docs/tranches/BD/` returns zero matches per BD-G7.

### §2.5 BD-G gate verification

Mechanism: walk the 7 BD-G gates from `docs/tranches/BD/BD.md`; verify each has a closure citation in a prior W0-W5 wave + a verification artefact. The verification document records the per-gate attestation.

```markdown
# BD-G Gate Verification

| Gate | Closure wave | Verification artefact |
|---|---|---|
| BD-G1 | BD.W1 | `docs/tranches/BD/audit/W1-bd-g1-ratification.md` (median ≤ 8 ms) |
| BD-G2 | BD.W2 | `docs/tranches/BD/audit/W2-bd-g2-ratification.md` (median ≤ 2.5 ms) |
| BD-G3 | BD.W3 | `docs/tranches/BD/audit/W3-semver-checks-report.md` (sister crates published clean) |
| BD-G4 | BD.W3 | `docs/tranches/BD/audit/W3-publication-order.md` (path triplet published coordinated) |
| BD-G5 | BD.W5 | `docs/tranches/BD/audit/W5-cross-backend-parity-spec.md` (81-cell matrix green) |
| BD-G6 | BD.W4 | `docs/tranches/BD/audit/W4-worktree-fixture-spec.md` (fleet-wide fixtures + CI matrix) |
| BD-G7 | BD.W6 | this document; `rg -n "BD→B[A-Z]\." docs/tranches/BD/` returns zero |
```

Files: `docs/tranches/BD/audit/W6-bd-g-verification.md` (create).

Sub-gate: every BD-G gate has a closure citation; the verification artefact paths resolve.

### §2.6 Workspace test surface verification

Mechanism: per BC.W6 §2.9 (`docs/tranches/BC/waves/W6.md:81-86`), the workspace test surface verification asserts all tests pass. W6 extends to BD's full workspace (including npm packages):

- `cargo nextest run --workspace` passes (Rust)
- `npm test --workspaces` passes (npm packages)
- per-grammar parity tests pass (BD.W5 matrix)

Files: `docs/tranches/BD/audit/W6-test-surface.md` (create).

Sub-gate: the document records per-crate + per-npm-package test counts; all tests pass.

### §2.7 Generated-LOC budget verification

Mechanism: per `docs/tranches/BD/BD.md` Generated-LOC Budget table, BD adds two new generated trees (TS, WASM) without modifying the existing Rust tree. The verification asserts each tree is within budget.

| Tree | BC close | BD close | Net delta | Budget |
|---|---:|---:|---:|---:|
| `crates/bbnf-parse/src/parse/generated/` (Rust) | ~135,167 | ~135,167 | 0% | 0% (unchanged) |
| `crates/bbnf-codegen/src/ts/generated/` (TS) | 0 | <measured> | +new | ≤ 280K (35K × 9 grammars max) |
| `crates/bbnf-codegen/src/wasm/generated/` (WASM) | 0 | <measured> | +new | ≤ 135K (18K × 9 grammars max) |

Files: `docs/tranches/BD/audit/W6-generated-loc.md` (create).

Sub-gate: the document records per-tree LOC; aggregate within budget; per-grammar deltas within the per-wave budgets at BD.W1 + BD.W2.

### §2.8 Trilogy close attestation

Mechanism: BD closes the BA-restart trilogy. The four-tranche arc — BA (surgical foundation), BB (generality + optimisation), BC (backend ABI + multi-backend foundation), BD (multi-backend activation + sister-crate publication + cross-backend parity) — completes.

The trilogy close attestation walks the four tranches' close artefacts:

- BA.FINAL.md → BA's surgical foundation: 9-directory re-org, JSON direct-to-struct, path triplet creation
- BB.FINAL.md → BB's generality: nine-grammar direct-to-struct, optimiser composition, visitor surface
- BC.FINAL.md → BC's backend ABI: typed IR contract, Rust lowerer in production, TS + WASM scaffolds
- BD.FINAL.md → BD's multi-backend activation + sister-crate publication + cross-backend parity

Files: `docs/tranches/BD/audit/W6-trilogy-close.md` (create — short attestation document).

Sub-gate: the document names each tranche's primary deliverable + close gate; cross-reference verified against each tranche's FINAL.md.

### §2.9 Phase 5 hand-off preparation

Mechanism: Phase 5 is the hardening verification phase per `docs/PHASE-4-DIRECTIVE-2026-05-03.md` §12 ("Phase 5 (re-invocation of `docs/HARDENING-PLAN-PROMPT.md`) ratifies before BA execution can begin"). BD.W6's hand-off prepares the inputs for Phase 5: a complete plan-set (BA + BB + BC + BD), all artefacts committed, all gates verified.

The hand-off note names:

- The plan-set's total LOC: ~9500-13500 (per `docs/PHASE-4-DIRECTIVE-2026-05-03.md` §8 cardinality target)
- The audit/ directory's total LOC: ~1500-2500 (per same)
- The synthesis verification points: §7 cross-tranche invariants
- The trilogy close attestation

Files: `docs/tranches/BD/audit/W6-phase-5-handoff.md` (create — short hand-off note).

Sub-gate: the hand-off document names the Phase 5 input scope; cross-reference verified against the directive.

### §2.10 Friction-forecast closure

Mechanism: per Lane 7 (Friction-Forecast), BD's three new user-facing surfaces (BD.W1 npm install, BD.W2 wasm bundler config, BD.W5 parity divergence) require named educational artefacts. W6 closes the forecast set: each surface has a verbatim error message + a documentation pointer.

Files: `docs/tranches/BD/audit/W6-friction-closure.md` (create).

Sub-gate: the document closes each forecast point with at least one verbatim error message + at least one documentation pointer; cross-reference verified against W1 + W2 + W5 friction docs.

## §3 Closer gate

| ID | Gate | Verification |
|---|---|---|
| W6-G1 | PROGRESS.md committed | `test -f docs/tranches/BD/PROGRESS.md` and the wave timeline is complete |
| W6-G2 | FINAL.md committed | `test -f docs/tranches/BD/FINAL.md` and every section is populated |
| W6-G3 | 13-lock cross-reference verified | `docs/tranches/BD/audit/W6-lock-verification.md` shows every lock with at least one wave cell; no empty cells |
| W6-G4 | Carry ledger CLOSED | `docs/tranches/BD/audit/W6-carry-ledger-closure.md` names the absence of forward carries; `rg -n "BD→B[A-Z]\." docs/tranches/BD/` returns zero matches |
| W6-G5 | All BD-G gates verified | `docs/tranches/BD/audit/W6-bd-g-verification.md` records per-gate closure citation |
| W6-G6 | Workspace test surface green | `cargo nextest run --workspace` 100% pass; `npm test --workspaces` 100% pass |
| W6-G7 | Generated-LOC budget honoured | `docs/tranches/BD/audit/W6-generated-loc.md` shows aggregate within budget |
| W6-G8 | Trilogy close attestation landed | `docs/tranches/BD/audit/W6-trilogy-close.md` names BA + BB + BC + BD closures |
| W6-G9 | Phase 5 hand-off prepared | `docs/tranches/BD/audit/W6-phase-5-handoff.md` names the verification scope |
| W6-G10 | Friction-forecast closed | `docs/tranches/BD/audit/W6-friction-closure.md` closes each forecast point |

## §4 Invariants

§I1. **All 13 locks honoured**: every lock has at least one wave cell populated (across BA / BB / BC / BD or pre-BA ceremony for L12); the cross-reference table is the closure attestation.

§I2. **Carry ledger CLOSED**: BD has no successor; the carry ledger has zero forward carries; BD-G7 verifies via `rg`.

§I3. **No metalanguage in close docs**: PROGRESS and FINAL cite path:line; do not reference commits or conversation history.

§I4. **No fictional successor letters**: the close docs name BA / BB / BC / BD only; no BE / BF / BG references.

§I5. **Lock 5 production ratification**: the IR contract supports Rust + TS + WASM in production; the parity matrix from W5 is the runtime evidence; the close attestation cites BD.W5.

§I6. **Generated-LOC budget honoured**: per the BD-level budget table; the existing Rust tree is unchanged; new TS + WASM trees within per-wave budgets.

§I7. **No tape resurrection**: the BD tranche introduces no tape, columnar SoA, variant-strip arena, or "tape rebranded as fast-path" substrate. Lock 1 invariant preserved through every wave.

§I8. **Trilogy close discipline**: BA / BB / BC / BD each have a FINAL.md; the trilogy close attestation walks all four; the absence of a successor is the closure semantic.

## §5 Risks

| Risk | Likelihood | Mitigation |
|---|---|---|
| The 13-lock verification reveals an empty cell because a lock was assumed closed by a prior tranche but never explicitly named | Low | The verification document at W6 §2.3 walks each lock with its tranche.wave citation; if any lock is empty, the close docs amend to include the prior-tranche closure note (no fictional carries) |
| BD.W3 publication is incomplete because crates.io upload failed for one crate, but BD.W3 closed prematurely | Low | BD.W3's gate W3-G4 requires `cargo install <crate>` succeed end-to-end for each; if a publication fails, BD.W3 doesn't close; W6 doesn't open until BD.W3 closes |
| The cross-backend parity matrix at W5 had one cell pass under tolerance but with a near-threshold float divergence that could shift on different hardware | Medium | The float-tolerance bound is `f64::EPSILON * scale`; the scale is platform-independent (f64 IEEE 754 is universal); if a cell is near-threshold, the catalogue at W5 §2.8 records the case + the platform tested |
| The workspace test surface (W6-G6) has a flaky test that doesn't always pass | Low | Flaky tests are caught at W5 (parity matrix is deterministic); if any test is flaky, W5 doesn't close; W6 inherits a green test surface |
| The generated-LOC budget at one of the new trees (TS or WASM) exceeds the per-wave budget | Medium | The per-wave budgets at BD.W1 + BD.W2 close before W6 opens; if one exceeds, the wave does not close; W6 inherits a within-budget state |
| The trilogy close attestation discovers a mismatch between BD's FINAL and the prior tranches' FINALs | Low | The cross-reference verification at W6 §2.8 is mechanical (cite BA.FINAL → BB.FINAL → BC.FINAL → BD.FINAL); any mismatch is a documentation drift caught at audit |
| The Phase 5 hand-off is incomplete because the audit/ artefacts are missing for one wave | Medium | Each wave's close gate names its audit/ artefacts; if missing, the wave doesn't close; W6 inherits a complete artefact set |
| The npm publication ledger (W3) has a version mismatch between `@bbnf-lang/runtime` and its per-platform sub-packages | Low | The @napi-rs/cli `napi prepublish` enforces version sync per BD.W3 §2.5; if mismatched, W3 doesn't close |

## §6 Cross-references

### Carry-tags consumed

| Tag | From | Description |
|---|---|---|
| W3→W6 | BD.W3 | Sister-crate + path-triplet publication ledger; W6 records in FINAL.md |
| W4→W6 | BD.W4 | Fleet-wide fixture infrastructure; W6 ratifies BD-G6 closure |
| W5→W6 | BD.W5 | Cross-backend parity matrix attestation; W6 ratifies Lock 5 production runtime ratification |

### BD-G gates closed

| Gate | Closure |
|---|---|
| BD-G7 | Carry ledger CLOSED: BD has no successor; the absence of forward carries IS the gate (W6-G4) |
| (verification of all BD-G1..G6) | W6 verifies each via the BD-G verification document at §2.5 |

### Carry-tags produced

**No successor.** This is the close tranche.

| Tag | To | Description |
|---|---|---|
| (none) | (none) | The carry ledger closes at BD.W6; the trilogy (BA + BB + BC + BD) ends. Phase 5 hardening verifies. |

## §7 Iter-time check

| Activity | Pre-W6 wall | Post-W6 wall | Notes |
|---|---:|---:|---|
| `cargo nextest run --workspace` | ~65 s (BD pre-W6) | ~65 s | unchanged |
| `npm test --workspaces` | ~12 s (BD pre-W6) | ~12 s | unchanged |
| `cargo doc --workspace --all-features` | ~35 s | ~35 s | unchanged |
| `rg -n "BD→B[A-Z]\." docs/tranches/BD/` | n/a | < 1 s | new iter-loop (one-time check) |
| Iter-loop: edit `docs/tranches/BD/FINAL.md` + visual check | n/a | < 1 s | doc-only iter-loop |

The W6 wave does NOT regenerate the grammar/generated/ tree; the regen output is stable from W5 close. Net delta to existing generated/*: 0%.

## §8 Verification artefacts

| Path | Description |
|---|---|
| `docs/tranches/BD/PROGRESS.md` | wave-by-wave timeline |
| `docs/tranches/BD/FINAL.md` | closing artefacts: perf metrics, publication ledger, parity matrix attestation, lock verification, carry ledger closure |
| `docs/tranches/BD/audit/W6-lock-verification.md` | 13-lock cross-reference verification document |
| `docs/tranches/BD/audit/W6-carry-ledger-closure.md` | carry ledger closure record (no successor) |
| `docs/tranches/BD/audit/W6-bd-g-verification.md` | BD-G gate verification document |
| `docs/tranches/BD/audit/W6-test-surface.md` | workspace test surface verification |
| `docs/tranches/BD/audit/W6-generated-loc.md` | generated-LOC budget verification |
| `docs/tranches/BD/audit/W6-trilogy-close.md` | trilogy (BA + BB + BC + BD) close attestation |
| `docs/tranches/BD/audit/W6-phase-5-handoff.md` | Phase 5 hand-off preparation note |
| `docs/tranches/BD/audit/W6-friction-closure.md` | friction-forecast closure document |

## §9 Audit lane forecast

- **Lane 2 (Sequencing)**: W6 is the close ceremony; no new substrate; the verification documents are produced from existing artefacts.
- **Lane 4 (SOTA Anchoring)**: W6 records the perf gate metrics (BD-G1 + BD-G2) in FINAL.md.
- **Lane 5 (Grammar-Authoritative)**: W6 documents only; no per-grammar code changes.
- **Lane 6 (Generated-Code Budget)**: W6 verifies the budget; no generation.
- **Lane 7 (Friction-Forecast)**: W6 closes the forecast set with verbatim error messages + documentation pointers.
- **Lane 8 (Carry-Audit)**: W6's central deliverable is the carry ledger closure; the absence of forward carries is the gate.

## §10 Closing posture

BD closes. The trilogy (BA + BB + BC + BD) closes. The 13 architectural locks settle. The sister crates publish to crates.io; the path triplet ships coordinated; the npm runtime packages live; the cross-backend parity matrix is green; the worktree fixtures expand fleet-wide; the carry ledger CLOSES. BD has no successor.

The plan ends here. Phase 5 hardening verifies the close.

Hereupon BBNF ships.
