# AZ-IV.W1 - Grammar Generality + Test Redress

**Name**: W1 - Grammar Generality + Test Redress
**Opens after**: W0 - Truth And Canonical Regen
**Agents**: 5 parallel writer slots, 9 sub-units (W1.6/W1.7/W1.8/W1.9 sequenced inside owner worktrees alongside W1.1-W1.5)
**Hard gate**: zero failing workspace tests; no grammar overfit; no silent fallback; runtime parity green for JSON, CSS L4, Sheets, BBNF; TS backend builds + typechecks (Node-execute lands in W5)
**Status**: complete

## Scope

This wave merges grammar-generality activation (Fermat's OVERFIT-HARD class), legacy-shim deletion (Heisenberg's view/color + recover_*), and complete failing-test redress per B1 — runtime API changes (W1.7 dedup, W1.8 substrate panic) break tests; landing redress in the same wave avoids a balloon mid-tranche.

1. Eliminate every `from_rule_name(&str) -> Kind` discriminator impl in production runtime (`crates/core/src/runtime/{bbnf,bnf,csv,css_pretty,ebnf,google_sheets,math}/arena.rs`) per `audit/HARDENING-2026-05-01-fermat.md` F1. Replacement: `StructRegistry`-projected discriminator field populated at codegen.
2. Eliminate the `(layout.kind, layout.rule_name.as_str())` builder dispatches at `crates/core/src/runtime/json/builder.rs:262`, `crates/core/src/runtime/css_l4/builder.rs:325, :521` per Fermat F2/F3. Replacement: typed `OpenFrame::from_layout(layout, &registry)` selection.
3. Delete `crates/core/src/runtime/css_l4/builder.rs:790-833 leak_static_str` rule-name allowlist; rule names that need static lifetimes go through one canonical interner.
4. Replace `crates/core/src/pipeline/compile.rs:219-224` rule-name → `GrammarAuditTag` aliasing with `GrammarAuditTag::Custom(entry_name)` for every grammar (Fermat F6).
5. Architectural patch for `recover_modifier` / `recover_binary_op` / `wrap.rs:89-99`: alt_dispatch emitter pushes modifier and binary-operator tokens as typed Span leaves; lower path deletes its three byte-recovery functions. Substrate at `crates/core/src/backend/rust/emitter/shapes/alt_dispatch/branches.rs:227-298` is ready (Fermat F8).
6. Replace `crates/core/src/backend/rust/emitter/shapes/substrate.rs:70-78` fallback to `JsonStructBuilder` with `panic!` (Fermat F4 + Heisenberg F7).
7. Replace `EmitStrategy::for_grammar` 9-arm allowlist (`crates/ir/src/registry/strategy.rs:143-262`) with manifest-driven binding registry: each grammar contributes one `[package.metadata.bbnf-grammars.<ident>]` row.
8. Delete `crates/core/src/backend/rust/view/color.rs`, fold `peel.rs` into `named_types.rs`, retire `runtime/view.rs:35` re-export. Production CSS uses `runtime::css_l4::CssColor`. Legacy decoder migrates to test-support only (per Heisenberg F4 + Amendment 2).
9. Close Sheets parity to 133/133 from regenerated tempdir output. The 122→115/133 regression detected during hardening pass 3 must be root-caused before any new feature lands.
10. Close CSS lightningcss parity, including named-color payload materialization (depends on W0 `Map { fn_id }` preservation) and Tailwind non-timeout behavior (Tailwind perf carry routes to W4).
11. Keep JSON sonic-rs parity 5/5 GREEN through the discriminator-projection migration; BBNF self-host parity stays 95/95.
12. Make TS backend buildable: syntax/typecheck pass on tempdir output, EOF enforcement, discriminated unions, repeat loops, host function wiring. **Node-executable proof lands in W5** (TS binding wave); W1's gate is build-time correctness only.
13. **Failing-test redress.** W0 produces `W0-failing-test-census.txt`. Every entry resolves under one of two dispositions before W1 closes:
    - **Fix** the test to conform to the new API (per `commit-discipline`, `feedback_no-workarounds`).
    - **Delete** the test only if (a) the bug surface is gone (e.g., test asserts on a deleted internal API), (b) the test is redundant (covered by another named test), or (c) the test was always-flaky and the wave's redress can replace it with a stable equivalent. Delete commits carry the per-test justification table specified below.

## Triumvirate Dispatch

Dispatch a triumvirate if any of these occurs:

- a parity surface needs a new `from_rule_name` arm, a new literal rule-name projection table, or a new `EmitStrategy::for_grammar` parser-name arm to land (per `audit/HARDENING-2026-05-01-fermat.md`);
- a regression test demonstrates that deleting a `from_rule_name` arm cannot be replaced by a `StructRegistry` lookup without a type-inference pass extension;
- CSS or Sheets parity requires a normalizer escape hatch, rule-name projection table, or synthetic payload default instead of grammar-derived structure;
- a failing test cannot be fixed AND does not match a delete-justification per Scope item 13 (e.g., the test asserts a real bug we do not have a fix for, and the bug surface is not deletable);
- TS parity needs a separate parser implementation from the emitted backend;
- shape generality cannot be proven without literal parser-name branches;
- three diagnostic loops fail to isolate a parity class.

Empty/no-evidence returns get one verbatim redispatch with the same worktree pointer. A second empty/no-evidence return triggers mandatory triumvirate. Three diagnostic-loop iterations without isolating root cause auto-trigger triumvirate. JSONL transcript quiet >15 minutes is an orchestrator-side auto-trigger; record the condition and dispatch time in `PROGRESS.md` alongside the wave's evidence ledger.

## File Bounds

| File | Access |
|---|---|
| `crates/core/src/runtime/{bbnf,bnf,csv,css_pretty,ebnf,google_sheets,json,math}/arena.rs` | modify |
| `crates/core/src/runtime/{json,css_l4}/builder.rs` | modify |
| `crates/core/src/runtime/path.rs` | modify-carve (path semantic alphabet stable; W2 owns Path IR) |
| `crates/core/src/runtime/view.rs` | modify (delete `runtime/view.rs:35` color re-export) |
| `crates/core/src/runtime/css_l4/**` | modify |
| `crates/core/src/backend/rust/view/**` | modify/delete |
| `crates/core/src/backend/rust/emitter/shapes/substrate.rs` | modify (panic! replaces fallback) |
| `crates/core/src/backend/rust/emitter/shapes/alt_dispatch/branches.rs` | modify (typed-leaf push activation) |
| `crates/core/src/lower/expression/{repeat,pratt,wrap}.rs` | modify (delete `recover_*` functions and byte-scan branch) |
| `crates/ir/src/registry/strategy.rs` | modify (manifest-driven binding) |
| `crates/ir/src/registry/struct.rs` | modify (extend with `compound_kind_for_layout`) |
| `crates/core/src/pipeline/compile.rs` | modify-carve (audit-tag aliasing replacement, line 219-224) |
| `crates/core/src/grammar/host.rs` | modify-carve (host directive decoders typed) |
| `Cargo.toml` | modify (`[package.metadata.bbnf-grammars]` block) |
| `crates/core/src/backend/ts/**` | modify (build-time correctness; Node-execute is W5) |
| `grammar/json/**` | modify-carve |
| `grammar/css/**` | modify |
| `grammar/google-sheets/**` | modify |
| `grammar/bbnf/**` | modify-carve |
| `crates/core/src/grammar/generated/**` | modify generated |
| `crates/core/tests/**` | modify (parity + redress) |
| `crates/core/tests/no_grammar_name_branch.rs` | create (CI static AST scan) |
| `crates/core/tests/synthetic_grammar_strategy.rs` | create (manifest-driven strategy regression) |
| `crates/core/tests/common/legacy_color_payload.rs` | create (test-support if any tests still need legacy Color decoder) |

Do NOT touch: rewrite/ruler production activation (W4 owns), benchmark thresholds (W6), new path crates (W2 owns), parent-pointer sidecars (W2 owns AscentStrategy), TS sub-crate (W5).

## Disjointness

W1.1 (overfit elimination + builder dispatches) owns runtime arena/builder. W1.2 (Sheets parity) owns `grammar/google-sheets/**` + Sheets tests. W1.3 (CSS parity) owns `grammar/css/**` + CSS tests + view/color deletion. W1.4 (TS build-time) owns `backend/ts/**`. W1.5 (shape generality + audit-tag aliasing) owns `backend/rust/emitter/shapes/**` (modify-carve) + `pipeline/compile.rs:219-224`. W1.6 (alt_dispatch typed-leaf push) sequences inside W1.5's owner worktree. W1.7 (per-grammar arena/builder dedup) sequences inside W1.1's owner worktree (the registry projection lands once, then arena/builder consume). W1.8 (substrate.rs panic + manifest strategy) sequences inside W1.5's owner worktree. **W1.9 (failing-test redress) is per-area:** Sheets-test redress in W1.2 worktree; CSS-test redress in W1.3 worktree; runtime-API-test redress in W1.1 worktree; backend-ts-test redress in W1.4 worktree; cross-cutting test redress in W1.5 worktree. The 5 parallel writer slots stay disjoint at the file level.

## Worktree Plan

| Agent unit | Sibling worktree absolute path | CARGO_TARGET_DIR |
|---|---|---|
| AZ-IV.W1.1 Runtime Overfit Elimination + Test Redress (runtime API) | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-runtime` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-runtime/target/w1-runtime` |
| AZ-IV.W1.2 Sheets Parity 133/133 + Test Redress (sheets) | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-sheets` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-sheets/target/w1-sheets` |
| AZ-IV.W1.3 CSS Parity + view/color Deletion + Test Redress (css) | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-css` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-css/target/w1-css` |
| AZ-IV.W1.4 TS Backend Build-Time + Test Redress (backend-ts) | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-ts` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-ts/target/w1-ts` |
| AZ-IV.W1.5 Shape Generality + Audit-Tag Fix + Substrate Panic + Manifest Strategy + Cross-Cutting Test Redress | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-shape` | `/Users/mkbabb/Programming/bbnf-wt-aziv-w1-shape/target/w1-shape` |

W1.6/W1.7/W1.8/W1.9 are sequenced inside the worktrees above per Disjointness.

## Agent Units

### AZ-IV.W1.1 Runtime Overfit Elimination + Test Redress (runtime API)

- Mechanism: extend `crates/ir/src/registry/struct.rs` with `compound_kind_for_layout(layout: &StructLayout) -> CompoundKindId` (registry-projected discriminator). At codegen, populate the layout's `compound_kind` field. Delete every `from_rule_name(&str) -> Kind` impl. Replace `(layout.kind, rule_name)` builder dispatches with `OpenFrame::from_layout(layout, &registry)`. Delete `leak_static_str`. Sequenced redress: every test that constructed `JsonValue::Object(JsonObjectId)` directly or asserted on `from_rule_name` return values gets fixed-or-deleted in the same worktree.
- Files: `crates/core/src/runtime/{bbnf,bnf,csv,css_pretty,ebnf,google_sheets,json,math}/arena.rs`, `crates/core/src/runtime/{json,css_l4}/builder.rs` (lines 262, 325, 521, 790-833), `crates/ir/src/registry/struct.rs`, `crates/core/tests/runtime_*.rs`, `crates/core/tests/json_*.rs`, generated outputs.
- Sub-gate: `crates/core/tests/no_grammar_name_branch.rs` returns zero match arms keyed on a literal rule-name string under `crates/core/src/runtime/**` or `crates/core/src/backend/rust/emitter/shapes/**` (excluding `generated/` and `#[cfg(test)]`); JSON, CSS L4, Sheets, BBNF parity remain GREEN through the discriminator-projection migration; runtime-API tests in this worktree's slice are 100 % pass.

### AZ-IV.W1.2 Sheets Parity 133/133 + Test Redress (sheets)

- Mechanism: root-cause the 122→115/133 regression first; then drive parity to 133/133 from regenerated tempdir output. No synthetic tag/payload fallbacks; no hand-coded serializer lexeme maps duplicating grammar facts. Sheets-test redress runs alongside.
- Files: `grammar/google-sheets/**`, `crates/core/tests/*sheets*.rs`, generated Sheets output.
- Sub-gate: full Sheets parity suite green (133/133), no `#[ignore]` without spec-conforming justification, no synthetic-default-payload escape hatch.

### AZ-IV.W1.3 CSS Parity + view/color Deletion + Test Redress (css)

- Mechanism: make lightningcss parity field-level where claimed; eliminate early-return payload gaps + rule-name projection tables + defensive default payload synthesis. Delete `crates/core/src/backend/rust/view/color.rs` (290 LOC zero production consumers per Heisenberg F4); fold `peel.rs` into `named_types.rs`; promote `view/named_types.rs` to `crates/core/src/backend/rust/named_types.rs`; retire `runtime/view.rs:35` re-export. Production CSS continues to flow through `crate::runtime::css_l4::CssColor`. Legacy `Color`/`ColorSpace`/`COLOR_PAYLOAD_BYTES` decoder migrates to `crates/core/tests/common/legacy_color_payload.rs` if any tests still reference it.
- Files: `grammar/css/**`, `crates/core/src/runtime/css_l4/**`, `crates/core/src/backend/rust/view/**`, `crates/core/src/runtime/view.rs:35`, `crates/core/tests/*css*.rs`, `crates/core/tests/*lightningcss*.rs`.
- Sub-gate: CSS normalize/bootstrap/named-color payload tests green from generated typed structures; normalizers are bounded diagnostics, not the parity proof; named_color depends on W0 `Map { fn_id }` preservation; Tailwind non-timeout behavior routes to W4; `rg "view/color|view::color" crates/core/src` returns zero hits.

### AZ-IV.W1.4 TS Backend Build-Time + Test Redress (backend-ts)

- Mechanism: repair discriminated union, repeat loop, EOF enforcement, host function wiring at the codegen level. Build-time correctness only — Node execution proof is W5 owner.
- Files: `crates/core/src/backend/ts/**`, `crates/core/tests/backend_ts.rs`, `crates/core/tests/pipeline_compile_request.rs`.
- Sub-gate: tempdir TS syntax + typecheck pass on representative grammars (JSON, CSS L4, Sheets, BBNF); `pipeline_compile_request::ts_backend_emits_discriminated_union` passes; backend-ts tests green.

### AZ-IV.W1.5 Shape Generality + Audit-Tag Fix + Substrate Panic + Manifest Strategy + Cross-Cutting Test Redress

- Mechanism: prove shape detector/emitter equivalence on non-JSON grammar facts. Replace `pipeline/compile.rs:219-224` rule-name aliasing with `GrammarAuditTag::Custom(entry_name)` (Fermat F6). Migrate host directive decoders to typed `BbnfCompoundKind`-driven flow (Fermat F10). Replace `substrate.rs:70-78` fallback with `panic!`. Replace `EmitStrategy::for_grammar` 9-arm allowlist with manifest-driven binding registry (`[package.metadata.bbnf-grammars.<ident>]`). Cross-cutting test redress (anything not in W1.1-W1.4 slices).
- Files: `crates/core/src/backend/rust/emitter/shapes/**` (modify-carve), `crates/core/src/pipeline/compile.rs:219-224`, `crates/core/src/grammar/host.rs`, `crates/core/src/backend/rust/emitter/shapes/substrate.rs`, `crates/ir/src/registry/strategy.rs`, `Cargo.toml`, `crates/core/tests/synthetic_grammar_strategy.rs` (new), `crates/core/tests/no_grammar_name_branch.rs` (new), cross-cutting test files identified by W0 census.
- Sub-gate: every active `ShapeTag` executes on two real grammars or one real grammar plus non-JSON synthetic IR, or is removed from the active denominator; `substrate_path` panics with structured error; synthetic-grammar test passes; cross-cutting tests in this worktree's slice are 100 % pass.

### AZ-IV.W1.6 BBNF Alt-Dispatch Typed-Leaf Push (sequenced inside W1.5)

- Mechanism: activate the alt_dispatch typed-leaf substrate at `crates/core/src/backend/rust/emitter/shapes/alt_dispatch/branches.rs:227-298` so the emitter pushes modifier and binary-operator tokens as typed Span leaves; delete `recover_modifier`, `recover_binary_op`, and the `wrap.rs:89-99` source-byte scanner (Fermat F8). Substrate exists; the W1 task is activation.
- Files: `crates/core/src/backend/rust/emitter/shapes/alt_dispatch/branches.rs`, `crates/core/src/lower/expression/repeat.rs` (delete `recover_modifier`), `crates/core/src/lower/expression/pratt.rs` (delete `recover_binary_op`), `crates/core/src/lower/expression/wrap.rs` (delete byte-recovery branch), generated BBNF output.
- Sub-gate: `cargo xtask regen --check` passes for `bbnf` and `bnf` grammars without source-byte recovery in the lower path; BBNF self-host parity remains 95/95 GREEN; `rg "recover_modifier|recover_binary_op" crates/core/src/lower/` returns zero hits.

### AZ-IV.W1.7 Per-Grammar Discriminator Projection (sequenced inside W1.1)

- Mechanism: same as W1.1 mechanism, scoped specifically to the discriminator-projection codegen lift. Lands `compound_kind_for_layout` in `crates/ir/src/registry/struct.rs`; the codegen populates `layout.compound_kind` so runtime reads it directly. Per-grammar value-enum dedup (the Arena<G>/Builder<G> structural skeleton) is W5's job, not W1's; W1 just retires the 7 `from_rule_name` impls.
- Files: same as W1.1.
- Sub-gate: `rg "fn from_rule_name" crates/core/src/runtime/` returns zero hits in `arena.rs` (test-support and `#[cfg(test)]` allowed).

### AZ-IV.W1.8 Substrate Panic + Manifest Strategy (sequenced inside W1.5)

- Mechanism: same as W1.5 mechanism, scoped specifically to the substrate.rs panic + EmitStrategy manifest-binding lift. Add `[package.metadata.bbnf-grammars.<ident>]` rows for each production grammar (JSON, CSS L4, CSS Pretty, Sheets, BBNF, BNF, EBNF, CSV, Math). Resolver reads the parsed manifest. The synthetic grammar `__test_strategy_synth__` proves no Rust arm is required.
- Files: `crates/core/src/backend/rust/emitter/shapes/substrate.rs`, `crates/ir/src/registry/strategy.rs`, `Cargo.toml`, `crates/core/tests/synthetic_grammar_strategy.rs` (new).
- Sub-gate: substrate.rs panics with structured error message naming the offending binding string; W0 manifest gate already enforces well-formed binding paths; synthetic-grammar test passes.

### AZ-IV.W1.9 Failing-Test Redress (per-area, sequenced inside W1.1-W1.5)

- Mechanism: every failing test in the W0 census resolves under fix-or-justified-delete. **Fix** is the default — bring the test to spec conformance with the new API. **Delete** requires per-test commit-body justification (table format below). Categories of acceptable deletion: (a) bug-surface-gone (test asserts on a deleted internal API); (b) covered-by-named-test (replacement test name cited); (c) always-flaky-and-replaced (replacement test name cited).
- Per-test commit body delete-justification table format:

  ```markdown
  ## Deleted tests

  | Test | File:Line | Why | Replacement |
  |---|---|---|---|
  | `JsonValue_object_construct_legacy` | `tests/json_legacy.rs:42` | API removed by W1.7 dedup; bug surface gone | covered by `runtime_arena_dedup::test_object_kind` |
  | `bbnf_self_old_dispatch` | `tests/bbnf_self_legacy.rs:108` | Refers to dispatch surface deleted at AZ-III `d316f40e` | none — bug surface gone |
  | `flaky_sheets_serialize` | `tests/sheets_format.rs:215` | Always-flaky on macOS arm64 (timing-dependent); replacement is `sheets_format_deterministic_serialize` | covered |
  ```
- Files: any test file under `crates/core/tests/`, `crates/ir/tests/`, `crates/csp-solver/tests/`, etc. as named by the W0 census.
- Sub-gate: `cargo nextest run --workspace --cargo-profile ax-iter --no-fail-fast` reports zero failures; ignored tests carry the owner-named comment with deadline commit, reason, and follow-up ticket; ignores without that triplet fail the close-honesty checklist.

## Hard Gate

1. Regenerated tempdir outputs are used for parity. Evidence: `docs/tranches/AZ-IV/audit/W1-parity-matrix.txt` shows each parity suite ran against `cargo xtask regen --tempdir` output, not checked-in `crates/core/src/grammar/generated/**`.
2. `cargo nextest run --workspace --cargo-profile ax-iter --no-fail-fast` reports **zero failures**. Evidence: `docs/tranches/AZ-IV/audit/W1-nextest-pass.txt`.
3. **Every `#[ignore]`** carries an owner-named comment with deadline commit, reason, and follow-up ticket. Evidence: `docs/tranches/AZ-IV/audit/W1-ignore-justifications.txt` enumerates each ignore with the triplet.
4. **Every deleted test** has a per-test commit-body justification per the W1.9 table format. Evidence: `git log --grep "## Deleted tests"` returns a body for every deletion commit.
5. JSON sonic-rs parity 5/5 GREEN; BBNF self-host 95/95 GREEN; Sheets 133/133 GREEN; CSS lightningcss field-level parity GREEN (named_color + Tailwind activation depend on W0 + W4 respectively, but the parity tests pass at W1 close from regenerated output).
6. TS backend builds + typechecks at tempdir level on representative grammars; backend-ts tests pass; Node execution lands in W5.
7. Shape generality tests execute generated parser behavior on at least two grammars (JSON + non-JSON), not only `syn::parse2`.
8. **No grammar-name branch in production runtime path**: `crates/core/tests/no_grammar_name_branch.rs` returns zero literal-rule-name match arms.
9. **Manifest-driven `EmitStrategy::for_grammar`**: `crates/core/tests/synthetic_grammar_strategy.rs` proves a synthetic grammar registered only via manifest round-trips codegen without adding a Rust arm.
10. **Substrate-path hard-fail**: `substrate.rs:70-78` no longer falls back to `JsonStructBuilder`; it `panic!`s with the offending binding string.
11. **`from_rule_name` and rule-name builder dispatch eliminated**: all 7 `from_rule_name(&str) -> Kind` impls in `crates/core/src/runtime/{bbnf,bnf,csv,css_pretty,ebnf,google_sheets,math}/arena.rs` are deleted; `(layout.kind, layout.rule_name.as_str())` matches eliminated; `leak_static_str` allowlist deleted.
12. **`view/color` shim deleted**: `crates/core/src/backend/rust/view/color.rs`, `peel.rs`, and the `runtime/view.rs:35` re-export are gone.
13. **BBNF source-byte recovery functions deleted**: `recover_modifier`, `recover_binary_op`, and the `wrap.rs:89-99` byte-recovery branch are gone after the alt_dispatch typed-leaf push lands.

## Format And Lint Cadence

Run `cargo fmt --all -- --check`, focused `cargo clippy --profile ax-iter`, focused nextest for touched parity suites, and `git diff --check` after integration batches and before close. Final close runs the full workspace nextest sweep cited in Hard Gate 2.

## Verification Artefacts

- `docs/tranches/AZ-IV/audit/W1-parity-matrix.txt` (regenerated tempdir parity per gate 1)
- `docs/tranches/AZ-IV/audit/W1-nextest-pass.txt` (workspace zero-fail evidence per gate 2)
- `docs/tranches/AZ-IV/audit/W1-ignore-justifications.txt` (ignore-triplet evidence per gate 3)
- `docs/tranches/AZ-IV/audit/W1-deleted-test-ledger.md` (consolidated delete-justification table)
- `docs/tranches/AZ-IV/audit/W1-no-overfit-scan.txt` (output of `crates/core/tests/no_grammar_name_branch.rs`)
- `docs/tranches/AZ-IV/audit/W1-synthetic-strategy.txt` (output of `crates/core/tests/synthetic_grammar_strategy.rs`)
- `docs/tranches/AZ-IV/audit/W1-view-color-deletion.txt` (rg evidence for shim removal)
- `docs/tranches/AZ-IV/audit/W1-recover-deletion.txt` (rg evidence for source-byte recovery removal)
- `docs/tranches/AZ-IV/audit/W1-ts.txt` (TS build-time + tempdir typecheck output)
- `docs/tranches/AZ-IV/audit/W1-shape-generality.txt`

## Commit Plan

Every dispatch carries `HARD CAP: N min. At 0.9N commit, at N halt.` Defaults: research 20, plan 15, redress 30, audit 25 (per `docs/precepts/instructions/ORCHESTRATION.md` §Triumvirate). HARD CAP expands on overrun (no pre-allocated triumvirate budget; expand cap and record reason in `PROGRESS.md`).

Use `commit-discipline` before each commit. Expected scopes: `runtime/discriminator-from-registry`, `runtime/builder-from-layout`, `runtime/leak-static-str-delete`, `runtime/view-color-delete`, `runtime/runtime-view-reexport-delete`, `lower/recover-modifier-delete`, `lower/recover-binary-op-delete`, `lower/wrap-byte-recovery-delete`, `emitter/substrate-panic`, `registry/manifest-strategy`, `pipeline/audit-tag-aliasing`, `grammar/sheets-parity-133`, `grammar/css-parity`, `backend/ts-build-time`, `tests/no-grammar-name-branch`, `tests/synthetic-strategy`, `tests/redress-{cluster}`, `tests/delete-with-justification`. Commit bodies are required for generated output, deletion, gate-changing parity, and **every** test-deletion commit (the per-test justification table is mandatory).

## Dependencies

- **Depends on**: W0 - Truth And Canonical Regen (failing-test census, `Map { fn_id }` preservation for named_color, regen totality).
- **Blocks**: W2 - Path IR + Typed Path<G,T> + AscentStrategy.
