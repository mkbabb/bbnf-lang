---
challenge: CH2
pass: T-P2-research
cycle: V3
lens: generality / Lock 14
generated_at: 2026-05-21T14:51:29-04:00
verdict: ACCEPT
---

# T-P2 V3 CH2 - Generality / Lock 14

## Verdict

**ACCEPT.** V3 preserves the V2 Lock 14 grammar-neutrality contract. The V3
fold repairs source counting, provenance pins, executable admission fields, and
numeric abrogate caps; it does not narrow the research packet to JSON-only
evidence or remove CSS, Sheets, BBNF-self, generated-policy, or witness
pressure.

## Evidence

- V2 already accepted CH2: the consolidated V2 challenge records generated
  provider registration, leak scans, generated sink/fact/value/flag surfaces,
  primitive policy manifests, CSS plus Sheets/BBNF-self witnesses or negative
  controls, and generated decision-engine facts as the accepted Lock 14
  transfer contract
  (`restart/audit/totality/p2/hardening/HARDENING-T-P2-V2-CONSOLIDATED.md:25`).
  The V3 addendum says it supplements rather than replaces the V2 Lock 14,
  Lock 1, REDRESS, material-differential, and anti-paper-close contracts
  (`restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md:11`).

- The `counted_source_ids` repair is a provenance/counting repair, not a
  JSON-only narrowing. 2C's counted set remains explicitly non-JSON: CSS
  Syntax, Selectors, CSS Values, CSS Variables, OpenFormula, V2 fold authority,
  and local evidence
  (`restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md:40`,
  `restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md:51`;
  `restart/audit/totality/p2/2C-grammar-neutrality.md:8`).

- The canonical Lock 14 surface remains live in 2C: generated provider
  registry, grammar-shape leak scan, generated sink/fact/value/flag surfaces,
  primitive policy manifests, CSS plus negative-control transfer, and
  decision-engine facts
  (`restart/audit/totality/p2/2C-grammar-neutrality.md:91`-`96`). 2C also keeps
  CSS declaration-values as `ADMITTED-EVIDENCE` only, while CSS selectors,
  extended CSS values, Sheets formulas, and BBNF-self remain `NOT-VALIDATED`
  until generated facts, row consumers, strict oracles, and telemetry exist
  (`restart/audit/totality/p2/2C-grammar-neutrality.md:121`-`126`).

- The V3 executable admission ledger keeps generated policy and non-JSON
  pressure in gate-consumed fields: `same_wave_consumer_path`,
  `expected_row_or_feature_gate`, `substrate_target`, `retention_lifetime`, and
  `policy_owner`
  (`restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md:89`). The first ledger
  row requires a CSS scan-block consumer, strict lightningcss/cssparser
  equality, CSS row movement, and `policy_owner=generated_grammar` before the
  delimiter primitive can advance
  (`restart/audit/totality/p2/T-P2-V3-FOLD-ADDENDUM.md:91`).

- The owner dossiers preserve the same constraints rather than treating the V3
  ledger as JSON admission. 2B requires shared SIMD policy from generated
  grammar data or caller data and same-wave JSON/CSS/Sheets/BBNF-self row
  movement (`restart/audit/totality/p2/2B-primitive-vocabulary.md:42`-`54`).
  2D keeps backend rewrites, CSP constraints, and costs on generated grammar
  facts, with CSS plus Sheets or BBNF-self proof fixtures required before
  fleet-wide claims (`restart/audit/totality/p2/2D-cost-model.md:45`,
  `restart/audit/totality/p2/2D-cost-model.md:190`). 2E requires
  caller-supplied grammar policy and production row consumers for hardware
  primitives (`restart/audit/totality/p2/2E-host-arch-esoterica.md:96`,
  `restart/audit/totality/p2/2E-host-arch-esoterica.md:142`). 2F fences
  parse-that/regex APIs to grammar-neutral facts and generated grammar modules
  rather than grammar-named generic helpers
  (`restart/audit/totality/p2/2F-parse-that-gaps.md:55`,
  `restart/audit/totality/p2/2F-parse-that-gaps.md:245`).

## Required Repairs

None for CH2.

## Carry-Forward

This ACCEPT is research-pass hardening only. T-P3/G-Omega still must implement
and gate-consume the generated manifests, leak scans, strict CSS/JSON oracles,
Sheets or BBNF-self witnesses, same-wave consumers, and telemetry before
claiming fleet-wide Lock 14 closure.
