# SK-V12 Pass Alpha Hardening V1 - Consolidated

Pass: Pass Alpha SK-V11 -> SK-V12 re-bracket.
Cycle: V1 under `USER-PIN-W1-CSS-L4-SOTA.md`.
Date: 2026-05-20.

Disposition: REVISE.

V1 is directionally aligned with the user pin: CSS L4 is authoritative,
Sheets/BBNF-self are fallback-only after a CSS redress attempt, JSON rows are
guard state, and the union plus ASM-gen categories are reopened only as
measured routes with REDRESS material-differential requirements. It does not
converge because every lens found bounded contract defects that must fold into
V2 before G-Alpha.

## Lens Results

| Lens | Disposition | Blocking finding |
|---|---|---|
| CH1 correctness | REVISE | Alpha-B/E use `>= lightningcss_mbps + 1` where the pin requires strict `>`, and result/guard tables need local resolving citations. |
| CH2 generality / Lock 14 | REVISE | CSS L4 emission is not yet isolated from the JSON-named generic template path; E4 union needs a generated-private boundary. |
| CH3 regression / REDRESS | REVISE | ADMIT lacks the zero-orphan SIMD disposition, Alpha-E has comparator drift, and generic runtime/codegen edits need refreshed JSON guard measurement. |
| CH4 cost / wave alignment | REVISE | E1+E2 are not credible as one redress; pin-aware 20/15/30 caps, split triggers, micro-prove budgets, generated LOC ceilings, and union/ASM-gen wave slots are missing. |
| CH5 hidden coupling | REVISE | CSS Track 1 / oracle / lightningcss output plane is under-specified, ADMIT can bypass orphan disposition, and E4 can leak public tape/event surface. |
| CH6 anti-paper-close | REVISE | The packet jumps to S-P3 instead of G-Alpha -> S-P1 -> S-P2 -> S-P3, candidate minute caps and G-Alpha table are missing, and rollback/rejected-patch coverage is uneven. |

## Accepted Core

- CSS L4 remains the first target; stale Sheets-first W1 authority is removed.
- REDRESS 111 report-only and REDRESS 112/113 future-phase shapes remain
  blocked except for the explicit CSS mandate.
- REDRESS 88/89/90 and 96/97/98 remain historical specific rejections; the
  categories are open only with material differential, CHALLENGE, scalar or
  reference proof, microbench, parity/checkasm where applicable, and same-wave
  consumer.
- `parse_only` remains diagnostic and JSON direct/typed rows remain guard
  state, not the CSS close route.

## Required V2 Folds

1. Normalize every CSS candidate gate to
   `generated_track1_mbps > lightningcss_mbps + 1`, or define one
   gate-consumed integer rounding rule and use it everywhere.
2. Add local resolving citations beside Alpha-A, `SYNTHESIS.md`, and
   `HANDOFF.md` result-state summaries and JSON guard-floor tables.
3. State that CSS L4 emission uses a grammar-neutral generator/config path
   plus generated CSS runtime output; `json_templates/generated.rs` may be
   edited only to preserve or extract JSON parity, not as a polymorphic CSS
   provider.
4. Add a generic-crate branch scan rejecting grammar-name, corpus-name,
   object/array-role, field-name, string-role, layout-role, or CSS-specific
   branches in generic crates.
5. Define the CSS L4 output representation shared by Track 1, Track 2/oracle,
   equality, and lightningcss throughput. The equality adapter must be
   symmetric and gate-consumed, not a bbnf-only or comparator-only bridge.
6. Add zero-carried-orphan aarch64 SIMD disposition to ADMIT as well as
   FIXPOINT: each known orphan is admitted with a consumer, removed, or
   explicitly inventory-demoted with evidence.
7. Tighten the JSON guard shortcut: if generic runtime, codegen,
   generated-output, bench, report, or gate roots move, the wave requires a
   refreshed JSON guard run or measured REDRESS demotion.
8. Split or prove the CSS path before redress. E1 CSS baseline plus E2
   `GrammarConfig` legality is over-budget as one wave unless S-P3 supplies a
   materially smaller owner/path table.
9. Add a pin-aware cost/cap matrix for E1-E5: wave slot, hand LOC, generated
   LOC ceiling, risk, 20/15/30 minute caps, mandatory CHALLENGE, same-wave
   consumer, microbench/checkasm budget, split trigger, and failure action.
10. Seed the <=12-wave ADMIT/FIXPOINT manifest: W0 revalidation, Lock14/CSS
    baseline, escape-mask correctness, union attempt, ASM-gen attempt,
    zero-orphan disposition, and close.
11. Bound E4 union to generated CSS-local/private or existing internal
    surfaces. Reject new public substrate API, directive, BIR variant,
    BackendShape, `UnionTape`, generic event side vector, retained cursor/list,
    or parser-owned fact slot.
12. Restore pass order: G-Alpha -> SK-V12 S-P1 under pin -> S-P2 under pin ->
    S-P3 under pin -> wave triumvirates. Reusing pre-pin S-P1/S-P2 evidence
    requires explicit measured revalidation.
13. Make rollback uniform for E1-E5, including
    `/tmp/skv12-waveW{n}-rejected.patch` or the exact wave-specific rejected
    patch artifact and dependent-wave blocking rule.
14. Expand the G-Alpha seed into an intervention table covering target row,
    role, LOC, minute cap, REDRESS adjacency, close contribution, predicted
    close state, and failure action.

V1 does not converge. V2 must patch Alpha A-F, `SYNTHESIS.md`, and
`HANDOFF.md`, then rerun six-lens hardening before G-Alpha presentation.
