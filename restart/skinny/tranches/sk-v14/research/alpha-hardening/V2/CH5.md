# CH5 HIDDEN COUPLING — Pass Alpha V2 Disposition

Lens scope per `restart/skinny/tranches/sk-v14/research/alpha-hardening/V1/CHALLENGE-CONTEXT.md:149-160` carried through V2 per `restart/skinny/tranches/sk-v14/research/alpha-hardening/V2/CHALLENGE-V2-ADDENDUM.md`. The CH5 charge: no parallel substrate, no sidecar producer, no renamed-scanner Lock 1 violation, no Track 1 ≡ Track 2 dishonesty; substrate union holds. V2 overlay: fold-verify the V1 BINDING REJECT (E-3) + five REVISEs (F-14, F-15, F-16, C-1, E-4) per `V1/HARDENING-ALPHA-V1-CONSOLIDATED.md §2.1-§2.4`, then fresh-scan the V2 artefacts for any new hidden-coupling vector introduced under fold pressure. Authority unchanged: `restart/locks/LOCKS.md:73-82` (Lock 1 substrate-ceiling fold, 2026-05-21 amendment).

## §0 — Disposition summary

- **ACCEPT-rate: 100 %** (46 of 46 §-level dispositions across the eight artefacts; all six V1 findings folded; zero new findings).
- **REJECT count: 0.**
- **REVISE count: 0.**
- **Critical findings: 0.**
- **Fold-verification scoreboard: 6 FOLD-LANDED / 0 FOLD-PARTIAL / 0 FOLD-MISSING.**

The substrate-union architecture stands as authored. Every V1 fold landed verbatim or with substantively-equivalent strengthening; the V2 cycle introduced no new parallel substrate, no renamed scanner, no Track 1 ≡ Track 2 collapse, no sidecar producer, and no public-substrate-API slip. The Lock-1 triad (`substrate_target` / `retention_lifetime` / `policy_owner`) propagates correctly from the LOCKS.md authority through SYNTHESIS §4, HANDOFF §7, α-E §6 (C-4 owner-paths + same-wave consumer plan), and α-C §2 (P-7 triple gate). The `track2_entry_point` column is wired into the SK-V14 RESULTS schema with the `xtask gate-json` rejection rule; the UnionTape / second-tape verbatim refusal lands at HANDOFF §7 with explicit `LOCKS.md:73-82` citation; the C-4 module-path discipline names `runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` as pre-emptive REJECT targets.

CH5 converges at 100 % for V2. The lens carries forward to V3 as the confirming pass per `ORCHESTRATOR.md §3Z` two-consecutive-cycle rule.

## §1 — V1 fold-verification table

| Fold | Origin | Target artefact + section | V2 evidence | Status |
|---|---|---|---|---|
| **E-3** (BINDING) | CH5 V1 §2 REJECT | α-E §6 C-4 owner-paths + same-wave consumer plan | `alpha-E-candidate-shortlist.md:459-476` adds a "Per-shape Lock-1 triad declaration (mandatory; CH5 §2 REJECT remediation, V1 hardening)" block. Verbatim text: "The same-wave consumer plan adds a `substrate_target=existing_tape \| retention_lifetime=generated_function \| policy_owner=generated_grammar` triple as a REQUIRED column in the wave's REDRESS entry for every CSP-selectable shape the wiring exercises." Allowed-value enumeration cites `LOCKS.md:73-82` directly. Abrogation path bound to `[abrogate-before-patch]` + C-4 architectural-block escalation (§11). `xtask gate-json` rejection wired through the same gate path that rejects empty equality columns from C-2. | **FOLD-LANDED** |
| **F-14** | CH5 V1 §2 REVISE #1 | SYNTHESIS §4 G-SIMD-GRAMMAR-POLICY clause | `SYNTHESIS.md:311-319` appends verbatim: "Every SIMD consumer wired by C-4 declares `substrate_target`, `retention_lifetime`, and `policy_owner` per `LOCKS.md:73-82`; `xtask gate-json` rejects any row whose REDRESS lacks the triple." The clause inherits the SK-V13 SIMD-policy body (no public substrate API, no retained sidecar classifier state, scalar parity, checkasm/differential coverage) and extends with the triad-declaration discipline as instructed. | **FOLD-LANDED** |
| **F-15** | CH5 V1 §2 REVISE #2 | SYNTHESIS §2 telemetry schema | `SYNTHESIS.md:240` adds the `track2_entry_point` row verbatim: "**NEW (CH5)** — symbol path of the Track 2 oracle entry point; `xtask gate-json` rejects any row where the Track 1 and Track 2 entry-point symbol paths share a common ancestor in `runtime::tape::` beyond the public `Tape` / `OffsetFlags` types." Column inserted between `Track 1 Mbps` / `Track 2 Mbps` and `comparator_plane`; rejection rule mechanically inspectable. | **FOLD-LANDED** |
| **F-16** | CH5 V1 §2 REVISE #3 | HANDOFF §7 refusal list | `HANDOFF.md:230-233` adds the verbatim Lock 1 clause as a new refusal bullet: "proposes `UnionTape`, a second tape, a public substrate API, a retained class/mask stream, or parser-owned cursor/list state; only G-Omega may amend Lock 1's substrate-union closure (`LOCKS.md:73-82`)." The wording lifts the LOCKS.md verbiage exactly (`LOCKS.md:80-81`); the G-Omega amendment route is named explicitly. | **FOLD-LANDED** |
| **C-1** | CH5 V1 §2 REVISE #4 | α-C §2 P-7 falsifiability gate | `alpha-C-redress-digest.md:348-385` restructures P-7's gate into the triple check requested: (a) distinct symbol paths via `xtask gate-json` per-row column (preserves the V1 mechanical check); (b) distinct concrete `Sink` types via compile-time `TypeId::of::<Track1::Sink>() != TypeId::of::<Track2::Sink>()` inside a `const _: () = assert!(...);` form (or `static_assertions::assert_type_ne_all!`); (c) distinct buffer addresses via `Track1::tape() as *const _ as usize` vs `Track2::tape() as *const _ as usize` recorded into the per-iter equality column on iter 0, with `xtask gate-json` rejecting any row where the two addresses are equal. The gate closing-paragraph cites `LOCKS.md:80-81` verbatim and labels the triple as the mechanical correlate of the renamed-scanner clause. | **FOLD-LANDED** |
| **E-4** | CH5 V1 §2 REVISE #5 | α-E §6 C-4 same-wave consumer plan | `alpha-E-candidate-shortlist.md:499-509, 530-532` adds a "Post-wave hot-leaf module-path discipline (CH5 §2 REVISE #5, V1 hardening)" sub-clause. Verbatim instruction: "The post-wave hot leaf's module path traces to `runtime::tape::` (an existing same-tape variant) or to a generator-emitted module whose template provenance is named in REDRESS; module paths under `runtime::ext::`, `runtime::sidecar::`, `runtime::union::`, or `runtime::cursor::` are REJECT pre-emptively." Closing prose ties the discipline to the "renamed-scanner-with-shared-buffer slip the CH5 charge names". The discipline is reiterated in the falsifiability-gate block (lines 530-532) as a hard REJECT for hot-leaf-changed-but-bad-module-path cases. | **FOLD-LANDED** |

All six V1 CH5 dispositions land verbatim or with substantive strengthening (the α-C P-7 triple gate actually exceeds the V1 ask — it adds explicit Lock-1 `substrate_target` enumeration as a fourth substrate-distinct correlate that the V1 prescription did not name).

## §2 — Per-artefact V2 disposition table

| Artefact | § | Disposition | Reason |
|---|---|---|---|
| SYNTHESIS.md | §0.1 Close condition | ACCEPT | Same-plane / same-corpus / same-equality bind preserves Track separation; unchanged from V1 ACCEPT. |
| SYNTHESIS.md | §0.2 Goalset row enumeration | ACCEPT | Per-plane gate still bars plane collapse; V2 numeric reconciliation (F-1) does not alter CH5 surface. |
| SYNTHESIS.md | §0.3 R-target goalset | ACCEPT | R8 "distinct parse_only path" remains inside `generated_json`; no parallel-substrate slip. |
| SYNTHESIS.md | §0.4 P-1..P-7 pre-blocks | ACCEPT | P-7 "Track 1 ≡ Track 2 dishonesty" carries the SK-V13 plane-collapse pre-block forward verbatim (lines 145-148). |
| SYNTHESIS.md | §0.5 wave-by-wave deferral | ACCEPT | Deferral remains contracted to PASS-ALPHA §4.4; no hidden-coupling concession. |
| SYNTHESIS.md | §1 Corrected diagnosis | ACCEPT | OffsetFlags + Tape carried grammar-neutral; no plane mixing. |
| SYNTHESIS.md | §2 Telemetry binding | ACCEPT | **F-15 LANDED.** `track2_entry_point` column wired (line 240) with `xtask gate-json` rejection rule for symbol-path co-ancestry under `runtime::tape::`. The V1 REVISE closes. |
| SYNTHESIS.md | §3 Candidate shortlist | ACCEPT | C-4 row (line 274) now names "per-shape Lock-1 triad (`substrate_target`, `retention_lifetime`, `policy_owner`) declared in REDRESS" as part of the C-4 falsifiability cell; risk reads "VERY HIGH (Lock-1 substrate-ceiling surface)" — surface intent is unambiguous. |
| SYNTHESIS.md | §4 S-P3 constraints | ACCEPT | **F-14 LANDED.** G-SIMD-GRAMMAR-POLICY clause (lines 311-319) appends the triad-declaration discipline + `xtask gate-json` enforcement. The V1 REVISE closes. |
| SYNTHESIS.md | §5 Pre-blocked routes | ACCEPT | "parser-owned sidecars, hidden Track 1 ≡ Track 2 coupling, stale comparator sidecars" preserved (lines 362-363). |
| SYNTHESIS.md | §6 Close posture | ACCEPT | Plane-honest prose; no hidden-coupling implication. |
| HANDOFF.md | §1 Bracket verdict | ACCEPT | Substrate-pillar carry-forward language unchanged from V1. |
| HANDOFF.md | §3 Honest baseline | ACCEPT | OffsetFlags + Tape grammar-neutral pillar preserved. |
| HANDOFF.md | §6 Next-move | ACCEPT | CH5 review bound at line 168; F-7 cap-discipline echo + F-8 G-Omega restoration touch §6 without disturbing CH5 surface. |
| HANDOFF.md | §7 Refusal conditions | ACCEPT | **F-16 LANDED.** UnionTape / second-tape / public substrate API / retained class/mask stream / parser-owned cursor/list state verbatim refusal lands at lines 230-233 with explicit `LOCKS.md:73-82` citation + G-Omega amendment route. The V1 REVISE closes. |
| HANDOFF.md | §8 V1 disposition | ACCEPT | PENDING flag honest. |
| alpha-A | §1-§4 per-plane row tables | ACCEPT | Plane-separated tabulation preserved through V2 row-count reconciliations (A-1 + A-2). |
| alpha-A | §5 telemetry | ACCEPT | Lazy-tape materialisation lines preserved grammar-neutral; A-3 LOC-budget addition does not disturb CH5 surface. |
| alpha-A | §6 audit verdict summary | ACCEPT | Per-plane bookkeeping clean. |
| alpha-A | §7 forward pointers | ACCEPT | Each handoff preserves plane separation. |
| alpha-B | §1.1 per-plane comparator binding | ACCEPT | STANDS from V1 (α-B not re-authored in V2 per ADDENDUM §0). |
| alpha-B | §2 parse_only rebound overlay | ACCEPT | STANDS from V1. |
| alpha-B | §3-4 direct / typed overlays | ACCEPT | STANDS from V1. |
| alpha-B | §5 CSS L4 overlay | ACCEPT | STANDS from V1. |
| alpha-B | §6 comparator-availability gap | ACCEPT | STANDS from V1. |
| alpha-B | §8 escalations | ACCEPT | STANDS from V1. |
| alpha-C | §1 per-entry dispositions | ACCEPT | Per-row REDRESS classification preserves plane separation. |
| alpha-C | §2 P-1..P-6 pre-blocks | ACCEPT | Provider-module collapse + comparator rebind correctly bound. |
| alpha-C | §2 P-7 pre-block | ACCEPT | **C-1 LANDED.** P-7 falsifiability gate (lines 348-385) restructured into the triple check: distinct symbol paths + distinct concrete `Sink` types via `TypeId` inequality + distinct buffer addresses via `as *const _ as usize` recorded into per-iter equality column. Closing prose cites `LOCKS.md:80-81` verbatim. The V1 REVISE closes. |
| alpha-C | §3 pattern-level summary | ACCEPT | Round-trip eligibility per P-7 correctly captured (line 396 reads "YES iff distinct entry points + buffers"). |
| alpha-C | §4 reopen obligations | ACCEPT | R8 distinct parse_only path scoped inside `generated_json`. |
| alpha-C | §5 closing posture | ACCEPT | No hidden-coupling artefact. |
| alpha-D | §2 V-1..V-8 VALIDATED | ACCEPT | STANDS from V1 (α-D not re-authored in V2 per ADDENDUM §0). |
| alpha-D | §3 INVALIDATED | ACCEPT | STANDS from V1. |
| alpha-D | §4 DEMOTED | ACCEPT | STANDS from V1. |
| alpha-D | §5 STILL-OPEN | ACCEPT | STANDS from V1. |
| alpha-D | §6 net ledger | ACCEPT | STANDS from V1. |
| alpha-E | §3 C-1 Lock-14 refactor cluster | ACCEPT | Trait-based dispatch + ONE grammar-agnostic generator + delete-then-emit; "no renamed `RuntimeProvider`" pre-block carried; E-7 forward-invariant clause does not alter CH5 surface. |
| alpha-E | §4 C-2 comparator rebind | ACCEPT | Comparator binding lives in `bbnf-bench`, not in any production substrate; E-12 envelope-ceiling expansion confined to harness scope. |
| alpha-E | §5 C-3 regen-css pipeline | ACCEPT | xtask emits committed source artefacts per Lock 6; E-6 family-shape binding strengthens grammar-neutrality. |
| alpha-E | §6 C-4 W8 + W9 scaffold → load-bearing | ACCEPT | **E-3 LANDED.** Per-shape Lock-1 triad declaration mandatory at lines 459-476; allowed-value enumeration cites `LOCKS.md:73-82`; abrogation path bound to `[abrogate-before-patch]` + architectural-block escalation. The V1 BINDING REJECT closes. |
| alpha-E | §6 C-4 same-wave consumer plan | ACCEPT | **E-4 LANDED.** Post-wave hot-leaf module-path discipline at lines 499-509 + 530-532; `runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` named as pre-emptive REJECT targets. The V1 REVISE closes. |
| alpha-E | §6 C-4 pre-blocked routes | ACCEPT | "No new substrate API; no parallel sidecar producer; no new BIR variant" preserved (lines 555-558); E-8 no-grammar-branch + two-grammar-family clauses strengthen the architecture without surfacing a slip. |
| alpha-E | §7 C-5 clean revert | ACCEPT | Pure deletion + ledger revert; E-10 scribe-contract clarification does not alter CH5 surface. |
| alpha-E | §8 consolidated pre-blocks | ACCEPT | P-7 carried verbatim (lines 702-703: "Track-1 ≡ Track-2 dishonesty; parallel substrate / sidecar producer. C-4 same-tape union variants only; no new sidecar."). |
| alpha-E | §9 concurrency matrix | ACCEPT | Single-writer ledger discipline preserves audit-trail integrity; E-13 C-4-strictly-after-C-1 update tightens sequencing without opening a coupling vector. |
| alpha-E | §10 cost / caps / telemetry | ACCEPT | E-2 cap-discipline reconciliation + E-9 grammar-keyed hot-leaf clause strengthen the telemetry surface; neither introduces a CH5 slip. |
| alpha-E | §11 convergence / escalation | ACCEPT | Architectural-block escalation honoured per C-4 fail-mode (lines 789-791); E-3 abrogation path inherits the escalation. |
| DISPATCH-CONTEXT | all §α-X | ACCEPT | STANDS from V1 (DISPATCH-CONTEXT not re-authored in V2 per ADDENDUM §0). |

46 of 46 §-level dispositions ACCEPT. V2 CH5 ACCEPT-rate is 100 %.

## §3 — Fresh-finding scan

CH5 V2 mandate per ADDENDUM §1.2: "Look for issues the V1 lens did NOT catch. The V2 cycle is itself a CHALLENGE — agents may introduce new defects while folding."

### §3.1 — Parallel-substrate scan

Surveyed all mentions of `sidecar`, `parallel substrate`, `UnionTape`, `second tape`, `public substrate`, `class/mask`, `renamed scanner`, `shared buffer`, `shared sink`, `shared dispatcher`, `cursor list`, `cursor/list` across the five re-authored V2 artefacts plus α-B / α-D / DISPATCH-CONTEXT (which STAND from V1).

Every occurrence is a refusal / pre-block / guard — none authorise a new substrate. Notable refusals:
- `SYNTHESIS.md:308, 315, 362-363` — pre-block lists name "parser-owned sidecars, hidden Track 1 ≡ Track 2 coupling, stale comparator sidecars".
- `HANDOFF.md:212, 230-231` — refusal conditions name "no sidecar classifier state" + "proposes `UnionTape`, a second tape, a public substrate API, a retained class/mask stream, or parser-owned cursor/list state" as REJECT vectors.
- `alpha-C-redress-digest.md:81` — single descriptive mention naming W9's CSS scaffold as "no public `UnionTape`, no substrate API; the 'union projection' is a hardcoded `UNION_PROJECTION_NORMALIZED_ASCII` vs `UNION_PROJECTION_RAW_BYTES` branch in CSS config" — DEMOTE-AUDIT verdict, not an authorisation.
- `alpha-E-candidate-shortlist.md:193, 455, 504, 531, 556, 702-703` — pre-blocked routes + module-path REJECT pre-emptions + P-7 reiteration.

No new parallel substrate. No new sidecar producer. No public-substrate-API surface slip.

### §3.2 — Track 1 ≡ Track 2 collapse scan

The `track2_entry_point` column (F-15) + the α-C §2 P-7 triple gate (C-1) together close the Track-1-≡-Track-2 surface mechanically. The V2 cycle did not introduce any new bench harness scaffolding that would short-circuit either check. The `xtask gate-json` rejection logic is wired into the same gate path that rejects empty equality columns from C-2 (per α-E §6 same-wave consumer plan, lines 472-477), confirming a single enforcement spine rather than orthogonal gate paths.

No Track 1 ≡ Track 2 collapse vector surfaced.

### §3.3 — Renamed-scanner Lock 1 violation scan

The module-path discipline (E-4) names `runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` as pre-emptive REJECT targets, closing the renamed-scanner-with-shared-buffer slip the V1 CH5 charge identified. The α-C §2 P-7 triple gate's clause (c) (distinct buffer addresses) is the runtime correlate; the clause (b) (distinct concrete `Sink` types via `TypeId` inequality) is the compile-time correlate. The two work in tandem to catch a renamed scanner sharing either the buffer or the sink type even when the symbol path is fresh.

No renamed-scanner violation surfaced.

### §3.4 — Cross-fold interaction scan (V2-specific)

Three V1 folds touch the same architectural surface (E-3 owner-paths triad in α-E §6; F-14 G-SIMD triad in SYNTHESIS §4; C-1 P-7 triple in α-C §2). Verify they cohere rather than contradict:

- The triad enumeration in α-E §6 lines 464-468 cites `LOCKS.md:73-82` for allowed values. The triad enumeration in SYNTHESIS §4 line 317-318 cites `LOCKS.md:73-82` for the same authority. The α-C §2 P-7 clause (c) at line 377-379 cites `LOCKS.md:73-82` for the `substrate_target ∈ {existing_tape, direct_sink, admitted_fact_output}` distinctness check. All three citations resolve to the same LOCKS.md lines; the allowed-value sets agree (`local_temp_only` is excluded from α-C's Track-2 enumeration because Track 2 must retain its tape across iters, which is correct — local_temp_only would imply a per-iter scratch buffer, incompatible with the bench harness contract). The triad propagates coherently.

- The `track2_entry_point` column rejection rule (F-15, SYNTHESIS §2 line 240) rejects rows where Track 1 and Track 2 entry-point symbol paths share a common ancestor in `runtime::tape::` beyond the public `Tape` / `OffsetFlags` types. The α-C P-7 clause (a) rejects rows where Track 1 and Track 2 entry points share a symbol. The two checks are complementary, not redundant: F-15 catches symbol-path co-ancestry under runtime::tape::; clause (a) catches outright symbol identity. A future row could pass clause (a) (different symbols) but fail F-15 (both symbols ancestor in `runtime::tape::assembler::common`) — F-15 is the stricter gate. The coherence holds.

- The UnionTape refusal (F-16, HANDOFF §7 lines 230-233) is the SK-V14-local re-statement of `LOCKS.md:73-82` verbatim, with the G-Omega amendment route named explicitly. The α-E §6 C-4 owner-paths triad (E-3, lines 459-476) operationalises this at the wave level — every CSP-selectable shape must declare the triple, abrogating if it cannot. The two work as a refusal-plus-operationalisation pair, not as overlapping constraints.

No cross-fold contradiction; no new defect introduced under fold pressure.

### §3.5 — Convergence

Per `ORCHESTRATOR.md §3Z`, convergence requires ≥ 95 % ACCEPT for two consecutive cycles. V1 CH5 was 86.96 %; V2 CH5 is 100 %. The V2 ACCEPT-rate clears the convergence floor with a 13.04 pp margin; if V3 holds the line at ≥ 95 %, the CH5 lens locks the bracket per the two-cycle rule.

## §4 — Recommended folds for V3

**None.** Every V1 CH5 disposition landed FOLD-LANDED; the fresh-finding scan surfaced zero new REJECT and zero new REVISE; the cross-fold interaction scan confirmed coherent propagation of the Lock-1 triad across SYNTHESIS, HANDOFF, α-C, and α-E. V3 is the confirming pass per `ORCHESTRATOR.md §3Z` two-consecutive-cycle rule; the CH5 V3 lens should re-disposition the same 46 sections against the same artefacts and (under the realistic stability assumption) converge again at 100 %.

CH5 verdict for V2: the architecture is sound; the language is hardened to the Lock 1 charter; the substrate-union closure carries forward without slip. The six V1 findings are fully addressed; no new finding surfaced. V3 dispatches as the lock-in pass.
