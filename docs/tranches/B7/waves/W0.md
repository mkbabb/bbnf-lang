# B7.W0 — bbnf-lang internal: simd-scan divan + test-surface partition + doc scrubs

**Opens after**: tranche open
**Repo**: `bbnf-lang`
**Agents**: 3 parallel (A1, A9, A10)
**Status**: planned

## Scope

W0 closes three bbnf-lang internal modernization items in parallel:

1. **simd-scan divan completion.** The last bench in the bbnf-lang
   workspace still on a non-divan harness ports to divan.
2. **Test-surface partition.** The `bench_lsp::bench_lsp_actions`
   16.5 s test re-routes from the routine `[[test]]` surface to
   the `[[bench]]` surface; the simd-scan proptest fuzzers
   (`json_alphabet_skewed`, `css_with_digraphs`, etc.) sample-cap
   so they no longer saturate the warm `cargo nextest run --profile
   ax-iter` wall.
3. **Doc scrubs.** Three doc-side scrubs land:
   - T1.a: AZ-II.md wave-merge (W1+W2 → merged Stage-A/B atomic
     wave + FINAL).
   - T1.b: BB.W0 e-graph rule hit-rate measurement gate.
   - T1.c: AY-III deferred status + AZ-I.W4 + AZ-II.W2 absorb-
     clauses + REMAINING-TRAJECTORY annotation.
   - B7 plan document set authoring.

## Phase sub-items

### B7.W0.1 — simd-scan divan migration (A1, 30 min cap)

Owner: A1.

Files: `crates/simd-scan/Cargo.toml`,
`crates/simd-scan/benches/stage1_throughput.rs`.

Mechanics: add divan as a dev-dep; declare `[[bench]] name =
"stage1_throughput" harness = false` in `Cargo.toml`; rewrite the
bench file so every benchmark function uses `#[divan::bench]` (or
`#[divan::bench(args = [...])]` for parameterized cases) and no
manual `std::time::Instant` loop remains.

Sub-gate: `cargo bench -p simd-scan` runs under divan's harness;
divan's per-bench ns/iter format appears in the output. `grep -n
'std::time::Instant' crates/simd-scan/benches/stage1_throughput.rs`
returns no hits. `cargo nextest run -p simd-scan --profile ax-iter`
green.

### B7.W0.2 — Test-surface partition (A9, 3 h cap)

Owner: A9.

Files: `crates/lsp/Cargo.toml`, `crates/lsp/tests/bench_lsp.rs`,
`crates/lsp/benches/`, `crates/simd-scan/Cargo.toml`,
`crates/simd-scan/tests/fuzz.rs`.

Mechanics:

- Re-route `bench_lsp::bench_lsp_actions` from `crates/lsp/tests/
  bench_lsp.rs` to a new `crates/lsp/benches/bench_lsp.rs`
  registered as `[[bench]]` in `crates/lsp/Cargo.toml`. The `tests/`
  variant retires; the benches/ variant carries the same body.
- Sample-cap the simd-scan proptest fuzzers in
  `crates/simd-scan/tests/fuzz.rs` via either `PROPTEST_CASES=64`
  in `crates/simd-scan/Cargo.toml`'s `[env]` section, or per-fn
  `#[proptest_attr(cases = 64)]` per the proptest crate's
  attribute API. Each fuzz test runs in < 1 s post-cap.

Commit cadence: A9 commits the bench re-categorization separately
from the fuzz sample-cap so the orchestrator can revert one
independently if either surface introduces flake.

Sub-gate: `bbnf-lsp::bench_lsp::bench_lsp_actions` absent from
`cargo nextest run --workspace --profile ax-iter` output. simd-scan
fuzz tests sample-capped. Warm `cargo nextest run --workspace
--profile ax-iter` 3-run median ≤ 14 s post-A1 + A9 cherry-pick.

### B7.W0.3 — T1.a doc scrub: AZ-II wave merge (A9, folded into 3 h)

Owner: A9.

Files: `docs/tranches/AZ-II/AZ-II.md` (wave-schedule + W1/W2
narrative sections only), `docs/tranches/AZ-II/waves/W1.md`,
`docs/tranches/AZ-II/waves/W2.md`.

Mechanics: declare 3-wave shape (W0 + W1-merged + W2 FINAL).
W1-merged describes the Stage-A + Stage-B atomic wave with byte-
equal close gate. W2 FINAL describes tape deletion + parity recode
+ BA handoff. A9 does NOT touch the §Hard gates §AZ-II.W2 absorbs
durable AY-III gates subsection — A10 owns it.

Sub-gate: AZ-II.md wave-schedule declares 3-wave shape; W1.md and
W2.md describe the merged shape; orchestrator-side cross-check
against A10's W2 absorb-clause subsection shows no overlap.

### B7.W0.4 — T1.b + T1.c + B7 plan docs (A10, 90 min cap)

Owner: A10.

Files: `docs/tranches/BB/{BB.md,waves/W0.md}`,
`docs/tranches/AY-III/{AY-III.md,PROGRESS.md}`,
`docs/tranches/AZ-I/AZ-I.md`,
`docs/tranches/AZ-II/AZ-II.md` (hard-gates section only),
`docs/tranches/REMAINING-TRAJECTORY.md`,
`docs/tranches/B7/{B7.md,PROGRESS.md,AGENT_DISPATCH.md,
waves/{W0,W1,W2}.md,FINAL.md}` (create).

Three commits:

- T1.b BB.W0 e-graph hit-rate gate (BB.md + waves/W0.md).
- T1.c AY-III deferred + AZ-I.W4 + AZ-II.W2 absorb-clauses +
  REMAINING-TRAJECTORY annotation.
- B7 plan + PROGRESS + AGENT_DISPATCH + waves + FINAL skeleton.

Sub-gate: `find docs/tranches/B7/ -type f | wc -l` ≥ 6 (B7.md,
PROGRESS, AGENT_DISPATCH, waves/{W0,W1,W2}.md, FINAL.md);
`cargo nextest run --workspace --profile ax-iter --no-fail-fast`
returns 1477+/1477+ (no source changes; doc-only); `cargo xtask
regen --check` exit 0 across 9 grammars.

## Hard gate

1. `cargo bench -p simd-scan` runs under divan harness; no
   `std::time::Instant` in `crates/simd-scan/benches/
   stage1_throughput.rs`.
2. `bbnf-lsp::bench_lsp::bench_lsp_actions` absent from `cargo
   nextest run --workspace --profile ax-iter` output.
3. simd-scan fuzz tests sample-capped (`PROPTEST_CASES=64` or
   `#[proptest_attr]`).
4. Warm `cargo nextest run --workspace --profile ax-iter` 3-run
   median ≤ 14 s.
5. `cargo nextest run --workspace --profile ax-iter --no-fail-
   fast` returns 1477+/1477+.
6. `cargo xtask regen --check` exit 0 across all 9 grammars.
7. T1.a wave-merged AZ-II docs on master clean.
8. T1.b BB.W0 hit-rate gate on master clean.
9. T1.c AY-III deferred-status header + absorb-clauses on master
   clean.
10. B7 plan docs (B7.md + PROGRESS + AGENT_DISPATCH +
    waves/{W0,W1,W2}.md + FINAL.md) on master clean.

## Verification artefacts

- `docs/benchmarks/post-B7-W0-walls.txt` — 3-run median wall
  capture, post-A1 + A9 + A10 cherry-pick.
- Commit hashes for each W0 sub-phase (A1: 1; A9: 3; A10: 3).

## File bounds (wave-level disjoint check)

| Agent | Files (write) |
|-------|---------------|
| A1 | `crates/simd-scan/Cargo.toml` (subset: dev-dep + `[[bench]]` lines), `crates/simd-scan/benches/stage1_throughput.rs` |
| A9 | `crates/lsp/Cargo.toml`, `crates/lsp/tests/bench_lsp.rs`, `crates/lsp/benches/`, `crates/simd-scan/Cargo.toml` (disjoint subset: `[env]` section for `PROPTEST_CASES`), `crates/simd-scan/tests/fuzz.rs`, `docs/tranches/AZ-II/AZ-II.md` (wave-schedule + W1/W2 narrative), `docs/tranches/AZ-II/waves/W1.md`, `docs/tranches/AZ-II/waves/W2.md` |
| A10 | `docs/tranches/BB/{BB.md,waves/W0.md}`, `docs/tranches/AY-III/{AY-III.md,PROGRESS.md}`, `docs/tranches/AZ-I/AZ-I.md`, `docs/tranches/AZ-II/AZ-II.md` (hard-gates section only), `docs/tranches/REMAINING-TRAJECTORY.md`, `docs/tranches/B7/**` |

A1 + A9 share `crates/simd-scan/Cargo.toml` on disjoint sections
(A1 declares the dev-dep + `[[bench]]` registration; A9 declares
the `[env] PROPTEST_CASES = "64"` block). Per SPEC §Parallelism
"`mod.rs` entries are disjoint hunks when each agent edits non-
overlapping module-declaration lines"; the same applies here. The
orchestrator verifies at cherry-pick time that each agent's diff
touches only its declared section.

A9 + A10 share `docs/tranches/AZ-II/AZ-II.md` on disjoint sections
(A9 owns wave-schedule + W1/W2 narrative; A10 owns the §Hard gates
§AZ-II.W2 absorbs durable AY-III gates subsection). The
orchestrator verifies at cherry-pick time that each agent's diff
touches only its declared region.

## Dependencies

- **Depends on**: tranche open (master clean at HEAD `6d11f3b5`).
- **Blocks**: tranche close (W0 + W1 + W2 must all close for B7
  to close).
