# HARDENING 2026-05-03 Lane 05 — Cohort Validation

Lane: Cohort-Validation.  
Scope: read `docs/HARDENING-AUDIT-PROMPT.md` §Cohort-Validation, `docs/tranches/meta-audit/05-validation.md`, predecessor AZ-IV audit outputs, and current source under `crates/`.  
Current checkout verified at `baf7df2d07cd`; pre-existing dirty files were outside this lane's write scope. This lane edits only this file.

## Method

Claims below are top claims from the predecessor cohort, sampled across DEEP-A/B/C/D, DEEPX-1..8, POST-CLOSE-A/B/C/D, DEEP-SYNTHESIS, and `SYNOPSIS-2026-05-03.md`. Outcomes:

- **VALIDATED** — current source / benchmark canon / git history still supports the claim.
- **NARROW** — the central claim holds, but only in a narrower form than written.
- **STALE** — the claim's exact wording/count is no longer true at current HEAD.

## Per-Claim Validation Table

| # | Verbatim predecessor claim | Verification command | Outcome |
|---|---|---|---|
| 1 | “DEEP-A counted 9 emission sites with `rule_type: ::bbnf_ir::TypeDesc::Span`; the actual count (verified by grep against master `40e1835d`) is **10 sites** across emitter shapes” (`DEEPX-1-json.md`) | `rg -n "rule_type: ::bbnf_ir::TypeDesc::Span" crates/core/src/backend/rust/emitter -g '*.rs' --count-matches`; `rg -n "rule_type: ::bbnf_ir::TypeDesc::Span" crates/core/src/backend/rust/emitter -g '*.rs' \| sort -u` | **STALE**. Current unique emitter-shape count is **9**, not 10: `object.rs` 1, `arglist.rs` 1, `array/mod.rs` 2, `alt_dispatch/mod.rs` 1, `unordered.rs` 1, `flat/struct_direct.rs` 1, `pratt/struct_direct.rs` 1, `wrap/struct_direct.rs` 1. The defect remains; the exact count drifted. |
| 2 | “The DEEP-A finding — every JSON parse fn entry constructs a runtime `__layout: StructLayout { rule_type: TypeDesc::Span, fields: vec![] }` even though the static `REGISTRY` ... has every rule's typed projection populated.” (`DEEPX-1-json.md`) | `sed -n '260,405p' crates/core/src/grammar/generated/json.rs`; `sed -n '1506,1522p' crates/core/src/grammar/generated/json.rs`; `sed -n '1648,1660p' crates/core/src/grammar/generated/json.rs`; `sed -n '1768,1780p' crates/core/src/grammar/generated/json.rs` | **VALIDATED**. `REGISTRY` carries typed JSON layouts (`Bool`, `F64`, `Named`, `Vec`, `Tuple`, `HeterogeneousAltJoin`), while parse-time `object`, `array`, and `pair` literals still use `rule_type: TypeDesc::Span` and `fields: Vec::new()`. |
| 3 | “Compile-time-resolved typed projection (the `StructLayout` produced by `project_types`) is never consumed at parse time.” (`DEEPX-7-sotapath.md`) | `rg -n "StructRegistry\|populate_struct_registry\|project_types\|rule_type" crates/ir/src crates/core/src/backend/rust/emitter/registry_emit.rs crates/core/src/grammar/generated/json.rs \| head -120`; `for f in json css_l4 google_sheets bbnf; do rg -c "fields: ::std::vec::Vec::new\\(\\)" crates/core/src/grammar/generated/${f}.rs; done` | **VALIDATED**. `project_types` population remains in `crates/ir/src/passes/types/registry.rs`; generated parsers still allocate empty parse-time layouts: JSON 3, CSS L4 134, Sheets 26, BBNF 38. |
| 4 | “86.07% of inclusive samples on `bbnf_value_twitter` are `Vec<OpenFrame>::clone` from `<JsonStructBuilder as StructBuilder>::checkpoint`.” (`SYNOPSIS-2026-05-03.md`) | `sed -n '96,126p' crates/core/src/runtime/json/builder.rs`; `sed -n '238,260p' crates/core/src/runtime/json/builder.rs`; `rg -n "86\\.07\|Vec<OpenFrame>\|checkpoint" .profiles docs/tranches/AZ-IV/audit/DEEP-B-performance-profile.md 2>/dev/null \| head -80` | **NARROW**. Current source validates the mechanism: `JsonStructCheckpoint` owns `stack: Vec<OpenFrame<'p>>`, and `checkpoint()` still does `stack: self.stack.clone()`. The exact 86.07% sample share is present in the audit doc, but the cited `.profiles/samply/post-AZ-IV/deep-B/bbnf_value_twitter/` directory is not present in this checkout, so the percentage itself was not independently re-opened. |
| 5 | “The generated parser calls `builder.checkpoint()` 26 times per `parse_wrap_JsonParser_value`.” (`DEEPX-1-json.md`) | `for f in css_l4 google_sheets bbnf json; do printf '%s ' "$f"; rg -c "\\.checkpoint\\(" crates/core/src/grammar/generated/${f}.rs; done` | **VALIDATED** for JSON. Current generated counts are JSON 26, CSS L4 1407, Sheets 126, BBNF 217. |
| 6 | “CSS L4 (DEEPX-2 verified: 1 407 `checkpoint()` sites in `css_l4.rs` ...).” (`SYNOPSIS-2026-05-03.md`) | `rg -c "\\.checkpoint\\(" crates/core/src/grammar/generated/css_l4.rs` | **VALIDATED**. Current count is **1407**. |
| 7 | “BBNF (DEEPX-4 verified: 217 checkpoint sites in `bbnf.rs`, identical magnitude class).” (`SYNOPSIS-2026-05-03.md`) | `rg -c "\\.checkpoint\\(" crates/core/src/grammar/generated/bbnf.rs` | **VALIDATED**. Current count is **217**. |
| 8 | “`bbnf_get_twitter` is 4196× sonic_get_twitter because the bench harness ... calls `JsonParser::parse(&input)` ... then walks the document via `Document::get<&str>(path)`.” (`DEEPX-1-json.md`) | `sed -n '1,120p' crates/core/benches/json/value.rs`; `jq '.. | objects | with_entries(select(.key|test("bbnf_get_twitter|sonic_get_twitter|bbnf_value_twitter|sonic_value_twitter"))) | select(length>0)' docs/benchmarks/post-AZ-IV.json` | **VALIDATED**. Current bench still parses eagerly then calls `parsed.get(p)`. `post-AZ-IV.json` still records `bbnf_get_twitter = 1,396,000 ns`, `sonic_get_twitter = 332.7 ns`, ratio 4196.5×. |
| 9 | “There is no `JsonParser::get<T>(input, path)` static entry that calls `parse_with` directly.” (`DEEPX-1-json.md`) | `rg -n "JsonParser::get\|impl JsonParser\|pub fn get\|fn get<\|parse_with::<\|parse_with\\(" crates/core/src/grammar/generated/json.rs crates/core/src/runtime/json crates/core/benches/json/value.rs` | **VALIDATED**. `JsonDocument::get` exists, `runtime/json/parse_with.rs` exists, but no static `JsonParser::get` entry exists in generated JSON parser code. |
| 10 | “`Document::get<T>(path)` continues to walk the materialised tree.” (`POST-CLOSE-D-pathforward.md`) | `sed -n '145,165p' crates/core/src/runtime/json/document.rs`; `sed -n '320,380p' crates/core/src/runtime/json/document.rs`; `sed -n '70,105p' crates/core/src/runtime/json/parse_with.rs` | **VALIDATED**. `JsonDocument::get` calls `T::query(self, path)` and `walk_path` over `JsonValue`; `parse_with` still finalises a `JsonDocument` then calls `doc.get::<T>(LegacyPath::new(&legacy))`. It does not project directly into the caller slot. |
| 11 | “The typed `path!` macro is at `runtime/path.rs:155-163`; the typed `path!(Json, ...)` ... is not yet built — `crates/bbnf-path` ... does not exist as a workspace member at HEAD.” (`DEEPX-1-json.md`) | `rg --files crates | rg 'bbnf-path|path_macro'`; `sed -n '1,130p' crates/bbnf-path/src/lib.rs`; `sed -n '1,115p' crates/core/tests/path_macro_compile.rs`; `sed -n '1,120p' crates/core/tests/path_macro_errors/unknown_field.stderr`; `rg -n "bbnf-path" Cargo.toml crates/core/Cargo.toml` | **STALE**. Current HEAD has `crates/bbnf-path`, workspace membership, proc-macro docs, positive compile fixtures, and negative stderr. The older “does not exist” claim was true at the audited base, not now. |
| 12 | “Compile-time grammar-aware diagnostics” are part of the “better than SOTA” claim. (`DEEPX-7-sotapath.md`) | `sed -n '1,120p' crates/core/tests/path_macro_errors/unknown_field.stderr`; `sed -n '1,130p' crates/bbnf-path/src/lib.rs` | **VALIDATED**. Negative fixture stderr shows a compile-time diagnostic for `path!(Json, "nope")` naming the bad segment and valid variants. |
| 13 | “Return type INFERRED from terminal TypeDesc — no turbofish for common case.” (`DEEPX-7-sotapath.md`) | `sed -n '1,130p' crates/bbnf-path/src/lib.rs`; `sed -n '131,155p' crates/core/src/path/ir.rs`; `rg -n "TypedPath<.*\\(\\)|TypedPath<Json, \\(\\)>" crates/core/tests/path_macro_compile.rs crates/bbnf-path/src/path_macro.rs` | **NARROW**. The type parameter exists in `TypedPath<G, T>`, but the proc-macro docs state `T` is currently `()` and terminal-type extraction lands later. Current positive macro tests use `TypedPath<Json, ()>`, `TypedPath<CssL4, ()>`, and `TypedPath<Bbnf, ()>`. |
| 14 | “`path!(Sheets, ...)` works end-to-end through the bbnf-path proc-macro + the `__path_plan::lookup` codegen + the `SheetsPathQuery` trait.” (`DEEPX-3-sheets.md`) | `sed -n '80,150p' crates/bbnf-path/src/registry.rs`; `sed -n '1,70p' crates/core/src/runtime/google_sheets/parse_with.rs`; `sed -n '1,80p' crates/core/src/runtime/google_sheets/document/path_query.rs`; `rg -n "path!\\(Sheets|TypedPath<Sheets|Sheets," crates/core/tests crates/bbnf-path crates/core/src -g '*.rs'` | **NARROW**. Marker support (`Sheets => google_sheets`), `parse_with<TypedPath<Sheets, T>>`, and `SheetsPathQuery` are present. No positive `path!(Sheets, ...)` proc-macro test was found; current tests construct `TypedPath<Sheets, f64>` manually. |
| 15 | “The Flat-shape lazy `#[ignore]` closes here” / “two ignored Sheets tests” remain a named carry. (`DEEPX-7-sotapath.md`, `POST-CLOSE-A-legacy.md`) | `rg -n "#\\[ignore\\]|Flat-shape|W3 carry|parse_with" crates/core/tests/parse_with_google_sheets.rs crates/core/src/runtime/google_sheets/parse_with.rs` | **VALIDATED**. Current source has two ignored Flat-shape lazy tests: one in `crates/core/tests/parse_with_google_sheets.rs` and one in `crates/core/src/runtime/google_sheets/parse_with.rs`. |
| 16 | “Eager `parse(input)` collapses to `parse_with(input, &EMPTY_PATH)`. The cross-grammar `__EAGER_EMPTY_PATH<Json,_>` literal retires.” (`HARDENING-AUDIT-PROMPT.md`, derived from DEEP cohort) | `rg -n "fn parse\\(|__EAGER_EMPTY_PATH|markers::Json" crates/core/src/backend/rust/emitter/grammar.rs crates/core/src/grammar/generated/{json,css_l4,google_sheets,bbnf}.rs crates/core/src/runtime/json/parse_with.rs` | **VALIDATED as still-open defect**. The emitter still hardcodes `TypedPath<crate::path::markers::Json, &'static str>` in eager parse scaffolding. Generated JSON, CSS L4, Sheets, and BBNF each contain `__EAGER_EMPTY_PATH` plus `markers::Json` in their parse entry. |
| 17 | “The substrate-audit test ... shipped at W5.4 with **32 zero-caller `pub` substrates** out of 886 enumerated. W6.2 did not close it.” (`POST-CLOSE-B-substrate.md`) | `cargo test -p bbnf-ir --test substrate_audit substrate_audit_zero_caller_substrates --profile ax-iter -- --nocapture` | **NARROW**. The failure still reproduces with **32 zero-caller substrates** and whitelist size 0. The enumerated total is now **883**, not 886. The current test completed in 3.42s after compile and failed as expected. |
| 18 | “Era archaeology citations” in the project-to-struct archaeology resolve. (`DEEPX-5-projfail-archaeology.md`, related cohort docs) | `grep -hEo '\`[0-9a-f]{7,40}\`' docs/tranches/AZ-IV/audit/DEEPX-5-projfail-archaeology.md docs/tranches/AZ-IV/audit/DEEPX-6-tranchehist.md docs/tranches/AZ-IV/audit/POST-CLOSE-C-carries.md docs/tranches/AZ-IV/audit/POST-CLOSE-D-pathforward.md \| tr -d '\`' \| sort -u > /tmp/bbnf_audit_hashes.txt; while read h; do git cat-file -e "$h^{commit}" ...; done < /tmp/bbnf_audit_hashes.txt` | **VALIDATED**. All **57** extracted commit hashes resolve as commits in current git history, including the key bibliography entries (`f419b6d3`, `094ce8e8`, `031cff59`, `feffe271`, `a13840a0`, `4f42f6bb`, `e090f2cb`, `f9afa87d`, `e0e0af30`, `43ea56bb`, `85cf83e7`, `94620aaa`, `0744c9f9`, `aa31fe56`, `078c8276`, `d4b8de18`, `df33e160`, `40092b28`). |
| 19 | “Canonical post-AZ-IV letter sequence is AZ-IV (closed) → BA (direct-projection codegen) → BB (rule-discovery) → BC (cleanup) → BD+ (TS/WASM). The fictional `AZ-V` is removed from all close-state docs ... residual references in the audit corpus are historical context.” (`SYNOPSIS-2026-05-03.md`) | `rg -n "AZ-V|post-AZ-IV|fictional|BA|BB|BC|BD" docs/tranches/AZ-IV/FINAL.md docs/tranches/AZ-IV/PROGRESS.md docs/tranches/AZ-IV/audit/DEEP-D-tranche-reordering.md docs/tranches/AZ-IV/audit/DEEP-SYNTHESIS.md docs/tranches/AZ-IV/audit/SYNOPSIS-2026-05-03.md docs/tranches/BA/BA.md docs/tranches/BB/BB.md docs/tranches/BC/BC.md docs/GESTALT.md` | **NARROW**. Current BA/BB/BC top-level specs encode the canonical order. Residual `AZ-V` references still appear in historical audit-corpus docs, especially `DEEP-D-tranche-reordering.md`, but current forward specs route to BA/BB/BC/BD+. |
| 20 | “`bbnf_get_twitter` ≤ 5× sonic via `JsonParser::get(input, path)` static entry ... closes Hard Gate 7 by ≥ 3 orders of magnitude.” (`DEEPX-1-json.md`) | `rg -n "JsonParser::get|pub fn get|bbnf_get_twitter|sonic_get_twitter" crates/core/src/grammar/generated/json.rs crates/core/benches/json/value.rs docs/benchmarks/post-AZ-IV.json` | **VALIDATED as unresolved prescription**. The static entry still does not exist; benchmark source still exercises eager parse; the close matrix still records the 4196× gap. The predecessor prescription remains live rather than landed. |

## Validation Notes

1. The deepest count drift is the emitter-site count: the predecessor's “10 hardcoded `rule_type: TypeDesc::Span` emission sites” should not be re-cited as current truth. Cite **9 emitter sites** for current source, while separately citing generated parser empty-layout counts for grammar breadth.
2. SOTA path macro status improved since the older DEEPX-1 base: `crates/bbnf-path` now exists and validates paths at compile time. The stronger “terminal TypeDesc return inference” and positive production-registry wildcard coverage remain narrower than the SOTA claim.
3. The performance mechanism claims remain structurally current: eager `get` still parses first, `JsonStructBuilder::checkpoint` still clones `Vec<OpenFrame>`, and substrate-audit still fails on 32 zero-caller substrates.
4. All archaeology hashes sampled from the project-to-struct and tranche-history corpus resolve. No stale hash citation was found in this lane.
