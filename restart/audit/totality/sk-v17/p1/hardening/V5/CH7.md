---
lens: CH7 OVERFIT-PRUNE
pass: T-P1-excavation
cycle: V5
generated_at: 2026-05-29T19:40:00Z
reviewer: CH7 (V5)
subject_files:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1b-codegen-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1e-locks-evidence.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
  - restart/audit/totality/sk-v17/p1/1f-past-corpora.md
mandate: >
  No contrivance; the excavation must distinguish the genuinely-generalizable
  tape model from any CSS/JSON-special-cased current state; crates/core is the
  fold TARGET, not measured-as-skinny. Disposition each section
  ACCEPT/REVISE/REJECT with file:line + concrete fix.
master_head: 445925167154de73540e3ea3283d0170371de790
counts:
  accept: 8
  revise: 0
  reject: 0
sections_reviewed: 8
prior_cycle: V4
prior_cycle_disposition: 8 ACCEPT / 0 REVISE / 0 REJECT (100%)
prior_cycle_revises_folded_into_v5_inventories:
  - V4-CH-1A-Gaps-row-1a126-lock14-mischaracterisation   # 1A V5 REVISE: "value APIs are hand-written per grammar" → @generated-but-EAGER
  - V4-CH4-LAC-1E-SKV17-04-path-b-LayoutFacts-priced-live   # 1E V5 REVISE: LayoutFacts grep-zero in crates/, skinny/prior-totality-only
  - V4-CH5-S8-1e-REDRESS-53-anchor-578                      # 1E V5 REVISE: re-anchor to :577/:825/:839 named lines
---

## §0 Posture

CH7 OVERFIT-PRUNE asks one question of every catalogued divergence: does the
finding measure the *genuinely-generalizable* substrate model, or has the
inventory over-fit to the CSS/JSON-special-cased current state — by mislabelling
a grammar-neutral fact as grammar-specific, by inventing a "gap" that is a
scope/convergence question, by *understating* an already-general construct, or
by *over-crediting* breadth-of-config as breadth-of-proof? The complementary
failure is contrivance: a divergence dressed up as a defect when it is a
fold-direction artefact (crates/core is the fold TARGET; it is allowed to lag
the SKINNY-proven model), or a genuine spec tension *dressed away* into a clean
convergence story.

This is the V5 cycle. The V4 CH7 lens returned 8/8 ACCEPT — the second
consecutive ≥95% cycle from the OVERFIT vantage, and V4 declared the lens at the
§4 convergence criterion. V5 is the cycle in which the OTHER lenses' V4 REVISEs
folded into the 1X inventories: 1A folds a CH-driven Lock-14 Gaps-row
mischaracterisation (the same class V3 CH1 charged against 1D); 1E folds two V4
REVISEs (CH4 LAC-04 path-(b) `LayoutFacts` mispricing, CH5-S8 REDRESS-53 anchor).
CH7's V5 obligation is therefore twofold: (1) confirm the lens carries no orphan
REVISE of its own (it does not — V4 closed clean); (2) re-run the contrivance and
breadth-over-credit sweep over the V5-touched rows and confirm the cross-lens
folds did not introduce a NEW over-fit or contrivance. Every citation below is
re-resolved live at master HEAD `445925167154de73540e3ea3283d0170371de790` by
Read + grep over the live trees; no recalled LOC.

## §1 Live re-verification of the V5-touched rows (OVERFIT sweep)

CH7's distinct discipline is to confirm that each folded change is a
*breadth-of-fact* correction, not a new inflation, deflation, or contrivance.
All anchors below re-resolved this cycle.

| V5-touched row | Inventory | Live re-verification at HEAD `445925167` | OVERFIT verdict |
|---|---|---|---|
| **1A Gaps-row Lock-14 fold** (`1a:140`) — restated "value APIs are hand-written per grammar" → "`@generated by xtask regen-{json,css}` … EAGER, NOT hand-written — Lock-14 ALLOWED (hand-written would be the Lock-14 VIOLATION); the divergence is the EAGER materialization shape" | 1A | `head -1 crates/core/src/runtime/json/value.rs` = `// @generated by xtask regen-json; do not edit by hand.`; `css_l4/value.rs:1` = `// @generated by xtask regen-css; …`. `grep -c value_from_ref crates/core/src/runtime/json/value.rs` = **0**. The restated row mirrors `1f-coherence-scan.md:79/116` and `1d-skinny-lessons.md:89/147`. | **NOT a contrivance, NOT over-fit.** This is the inverse of an over-fit: the V4 prose Gaps-row had OVER-stated the defect class (claiming a Lock-14 VIOLATION shape — hand-authorship — where the live state is Lock-14-ALLOWED generated-but-eager). The fold *removes* a spurious defect inflation. The genuinely-general divergence (EAGER vs lazy `ValueRef<G>` materialization shape) survives intact; the special-case framing (hand-written-per-grammar) is correctly struck. 1A's own Table (SUB17-004 `1a:95`) + Cross-Tree map (value-API row `1a:108`) already carried the correct "eager … not lazy `ValueRef`" framing, so the localization to one prose row is honest. |
| **1E LAC-04 path-(b) re-price** (`1e:181`) — path-(b) "narrow-to-side-table" was V4-priced "~0 LOC (LayoutFacts already live)"; re-priced "NOT ~0 LOC … `LayoutFacts` is the SKINNY/prior-totality side-table … absent from crates/core … `grep -rn LayoutFacts crates/`=0 … ANY crates/core realisation is a non-zero generator-side surface (UNKNOWN)" | 1E | `grep -rn LayoutFacts crates/` = **0** (re-run live). `grep -n LayoutFacts skinny/crates/passes/src/lib.rs` = `:85` (`pub layout_facts: LayoutFacts`), `:91` (`pub struct LayoutFacts`), `:109/:110/:395` — present ONLY in skinny. | **OVERFIT-EXEMPLARY correction.** The V4 path-(b) had committed the precise over-credit CH7 polices: pricing a fold-target capability (a side-table that closes Lock-2 at ~0 LOC) against a construct that does NOT exist in the fold target. The re-price grounds the cost on a live `grep`-zero anchor and explicitly bars claiming Lock-2 closure by `LayoutFacts` ALONE (the `LOCKS.md:162-166` v+1 note). The path-(a) 960-site/medium figure reproduces exactly (`grep -rn StructLayout crates/` = **960** re-run live). This is breadth-of-fact, not contrivance. |
| **1E CH5-S8 REDRESS-53 anchor** (`1e:141/190`) — re-anchored from `:578` (adjacent sidecar-vector continuation clause) to the verbatim naming lines `:577`/`:825`/`:839` | 1E | The 1f-past-corpora ledger (`1f-past-corpora:59`) independently confirms "REDRESS-53 is NAMED at `:577`/`:657`/`:825`/`:839` — `:578` is the continuation clause." The 1E ledger row (`1e:141`) and U2 (`1e:190`) now both cite `:577`/`:825`/`:839`. | **Not an OVERFIT axis per se (a citation-precision fix, CH1/CH5 territory)**, but CH7 confirms the re-anchor did not deflate the REDRESS-53 pre-block into a non-fact: the parallel-retained-index pre-block remains a load-bearing fold fence scoped to all 8 `OnceCell<StructuralIndex>` carriers, neither inflated nor dissolved. Clean. |
| **1D / 1C / 1F math.rs eight-not-nine census** (carried from V4 §3-B, re-verified) | 1C/1D/1F-anti-pattern | `grep -c scan_structural crates/core/src/grammar/generated/math.rs` = **0**; `grep -c OnceCell math.rs` = **2** (both doc-comments `:281`/`:285`). The eight scan-wired carriers each return `grep -c scan_structural` = **1** (json/css_l4/ebnf/bnf/csv/css_pretty/google_sheets/bbnf, re-run live). | **Breadth-of-fact, held clean across the V5 fold.** The V3 false-ninth-carrier inflation remains struck; the EIGHT-name wired census is restated consistently in 1C RT17-005 (`1c:79`), 1D SK17L-008 (`1d:95`), and 1f-anti-pattern AP17-002 (`1f-anti-pattern:70`). No re-inflation. |

## §2 Section dispositions (V5 inventories)

### 1a-substrate-evidence.md — ACCEPT

The Lock-1 substrate-union spine is OVERFIT-clean and the lone V5-folded REVISE
(the Gaps-row Lock-14 mischaracterisation, §1) is a defect-inflation REMOVAL, not
a new over-fit. The genuinely-generalizable model (SoA `Tape` +
`ValueRef<'doc,'input,K,G>` + `select_classifier`) stays cleanly separated from
the CSS/JSON-special-cased current state (eager `OpenFrame`/`CssTypedValue`/
no-`value_from_ref`); crates/core is consistently the fold TARGET. Re-verified
live: `TapeStructBuilder` grep-zero outside `runtime/tape/`; `StructLayout`=**960**
exactly (SUB17-006 `1a:97`, the OVERFIT-correct *up*-calibration from the V2
40-120 LOC under-estimate); `value_from_ref` absent in core json (=0).
SUB17-007's `StructuralAlphabet` row (`1a:98`) is the OVERFIT-exemplary
breadth call: it classes the richer config (digraphs, quote_classes) as
"config-breadth, not proof-breadth" — re-verified live at `alphabet.rs:33-37`
where the `quote_classes` doc-comment itself names `b'"'` for JSON and
`b'\''`+`b'"'` for CSS, JSON/CSS-exercised-only, NOT breadth-of-proof. No
contrivance; no breadth over-credit.

### 1b-codegen-evidence.md — ACCEPT

The BackendShape canon excavation is OVERFIT-clean and its V5 first-cycle
addition BSHAPE17-009 (the NEON shared-classifier row) is the most directly
mandate-relevant new finding in the set — and it is handled WITHOUT over-credit.
The prompt subject names "NEON JSON-only vs the shared-classifier generalization"
as a divergence to excavate; BSHAPE17-009 (`1b:77`) resolves it correctly to
**impl-exceeds-spec, not a gap**: the shared classifier is ALREADY grammar-general
in core. Re-verified live: `scan_structural(input, &StructuralAlphabet)` at
`simd-scan/lib.rs:80`; `KernelShape::select(alphabet)` (the `select_classifier`
analogue) at `alphabet.rs:118`; per-grammar `GRAMMAR_STRUCTURAL_ALPHABET` emitted
at `profile.rs:43/:100`; the runtime constructs the alphabet per-call inside
`ensure_structural_index` `get_or_init` (`support.rs:472-485`, re-read live — a
retained-OUTPUT scan cache, NOT cross-call classifier state); all 8 generated
grammars consume it (`grep -rln scan_structural crates/core/.../generated` = 8,
re-confirmed per-file = 1). NEON aarch64-baseline (`neon.rs:47`); avx2/avx512/wasm
cfg-gated to non-aarch64 (`avx2.rs`/`wasm.rs` headers re-read). The OVERFIT
discipline is exactly right: this is NOT inflated into "the spec must build the
classifier" (a contrivance) — the divergence is narrative-only, the spec's
JSON-scanner framing must absorb the alphabet-parametrised shared form, 0 LOC
fold. BSHAPE17-003's single `EmitStrategy::StructDirect` (re-verified
`strategy.rs:107`) is named "the SinkOnly/struct-builder lineage to absorb," not
inflated into a Lock-10 violation. BSHAPE17-008's data-bound `builder_path`
resolution is correctly ruled Lock-14-ALLOWED DATA. The V5 symbol-anchor fix
(BSHAPE17-006: lookup method is `StructRegistry::layout`/`layout_by_name`
`:331`/`:337`, not the struct-decl line `:313`) is a CH1-class precision
correction that does not disturb the OVERFIT-relevant pre-block fact. No
contrivance.

### 1c-runtime-evidence.md — ACCEPT

The runtime census is materially sound and OVERFIT-clean. RT17-001 (`1c:75`)
draws the offset-tape-admissible-vs-AV.04-dense-class-column distinction
explicitly (carried clean from the V3/V4 §3-A fold), reserving "columnar SoA is
dead" for the dense class-column it actually names. The eager `OpenFrame`
builders are correctly the fold-deletion target; RT17-007 (`1c:81`) refuses the
contrived runtime-side BackendShape leak (re-verified: grep-empty for the 5 shape
names over `crates/core/src/runtime/`). RT17-002 (`1c:76`) carries the same
generated-but-EAGER framing the 1A Gaps-row was folded onto — consistent, no
hand-written mischaracterisation. RT17-005's all-8 + math.rs-excluded census
(`1c:79`) is breadth-of-fact, re-verified. No contrivance.

### 1d-skinny-lessons.md — ACCEPT

The JSON-empirical vs grammar-neutral split (`1d:99-111`) remains the strongest
generality firewall in the set and is the literal embodiment of the CH7 mandate's
"distinguish the genuinely-generalizable tape model from CSS/JSON-special-cased
current state." Every lesson is tagged JSON-empirical (witness) vs grammar-neutral
(durable, fold). SK17L-009 (`1d:96`) holds the projection-generality framing
correctly: "by-construction on JSON+CSS only," `sheets_witness` a bench-surface
stub "cannot serve as a projection exercise," Sheets/BBNF generality is SK-V18 —
this is the precise anti-over-credit posture (breadth-of-config, not
breadth-of-proof). SK17L-008's eight-not-nine math.rs census is breadth-of-fact
(re-verified §1). The eager-tree (SK17L-003) and registry-indirection (SK17L-004)
pre-blocks stay anchored to the construct, not a symbol list. The
crates/core-is-fold-target discipline is explicit (`1d:71-73`: the disproved-route
SHAPES are "UNWIRED dead code awaiting SK-V18 fold … the fold TARGET, not skinny
artefacts; the monotonic direction is skinny-proven → crates/core"). No
contrivance.

### 1e-locks-evidence.md — ACCEPT

The locks audit and the 6 amendment candidates are evidence-backed and
OVERFIT-clean, and the two V5-folded REVISEs are the cycle's most OVERFIT-relevant
corrections (§1). LAC-1E-SKV17-04 (`1e:181`) is now the OVERFIT-exemplary call: it
re-prices path-(b) off a `grep -rn LayoutFacts crates/`=0 live anchor (re-verified
this cycle), explicitly classing the side-table as skinny/prior-totality-only and
its crates/core materialisation as a non-zero UNKNOWN — the correction of a
fold-target over-credit (pricing an absent construct as live). The L14 row
(`1e:119`) rules the per-grammar `CssStructBuilder`/`CssTypedValue` the
fold-deletion target, not a leak; the L16 row (`1e:120`) names core's multi-arch
scan "BROADER than the proven aarch64 set — a fold-scope question … not a defect"
(refusing contrivance into a Lock-16 violation). Candidate ownership (6 LACs,
disposition deferred to 3C) is correctly bounded; the candidate table carries the
explicit "scanned axes" enumeration (`1e:163-166`). No over-fit; the cost
calibration is breadth-of-fact.

### 1f-coherence-scan.md — ACCEPT

Carries the §3-A offset-tape-admissible-vs-AV.04-dense-class-column fold clean
(COH17-001 `1f-coherence:78`, U-COH17-002 `1f-coherence:125`), re-verified against
`LOCKS.md:75` carrying both clauses and the skinny `Tape`'s sparse-side-vector
shape. COH17-008 (`1f-coherence:85`) correctly classes the richer
`StructuralAlphabet` "JSON/CSS-exercised-only (breadth-of-config, NOT
breadth-of-proof)" — the anti-over-credit call, re-verified at `alphabet.rs:35-37`.
COH17-007 routes the FactStream question to an UNKNOWN (U-COH17-001) rather than
contriving a contradiction (`1f-coherence:84`). COH17-002 carries the
generated-but-EAGER framing consistent with the 1A fold. No contrivance.

### 1f-anti-pattern.md — ACCEPT

The CH5-firewall counterpart, OVERFIT-clean. AP17-001 refuses to contrive a Lock-1
violation from the unwired tape ("NOT a same-tree second substrate"). AP17-002's
8-carrier OnceCell census explicitly excludes math.rs as the symbol-vs-doc-comment
class (`1f-anti-pattern:70`, re-verified `grep -c scan_structural math.rs`=0,
`OnceCell`=2 doc-comments). AP17-003 names the 817-LOC CSS builder a
fold-deletion target, not a Lock-13 violation — re-verified live: `wc -l
crates/core/src/runtime/css_l4/builder.rs` = **817**; the pending-field count
(`1f-anti-pattern:71`) "six `pending_*` Vecs (`:74-79`) plus one `pending_value:
Option` (`:71`) = SEVEN pending_ fields, NONE … `Vec<Vec>`" reproduces EXACTLY at
the live source (`pending_value: Option` at `:71`; `pending_rules/decls/selectors/
values/blocks/components: Vec<T>` at `:74-79`; the additional `span_starts: Vec<u32>`
at `:73` is correctly NOT counted as a `pending_*` field). AP17-004 (grammar-name
leaks) rules the emitter `substrate.rs` builder-path references Lock-14-ALLOWED
DATA. No contrivance, no count over/under-statement.

### 1f-past-corpora.md — ACCEPT

The do-not-redrive ledger and Direction-Monotonicity Note (`1f-past-corpora:66-75`)
remain the strongest fold-target-not-skinny guardrails in the set. PC17-006
(`1f-past-corpora:63`) names the §9 "second substrate" block calling
`StructLayout`/`TapeStructBuilder`/`TapeCursor` FORBIDDEN-IN-SKINNY and forbids
relocating them into skinny — the monotonic skinny→totality direction is explicit
and is the literal CH7 mandate ("crates/core is the fold TARGET, not
measured-as-skinny"). The V5 REDRESS-53 named-line re-anchor (`:577`/`:657`/`:825`/
`:839`, `:578` the continuation clause) is carried here independently and
correctly. The DO-NOT-CARRY-UNDERCOUNT flag (`1f-past-corpora:87`) pre-fences the
prior V2 false-negative. No over-fit.

## §3 Dispositions

No REVISE, no REJECT this cycle. CH7 carried zero orphan REVISE into V5 (V4 closed
8/8 ACCEPT). The two cross-lens V4 REVISEs that folded into the V5 inventories
(the 1A Lock-14 Gaps-row, the 1E LAC-04 path-(b) + CH5-S8 anchor) are both, from
the OVERFIT vantage, *removals* of a spurious inflation/over-credit — the precise
direction CH7 polices — and both are grounded on live `grep`-zero anchors
re-verified this cycle (`value_from_ref`=0 + `@generated` headers; `LayoutFacts
crates/`=0). The contrivance-detection sweep over the V5-touched and V5-new rows
(the BSHAPE17-009 NEON-already-general addition; the math.rs eight-not-nine
census; the StructuralAlphabet config-breadth classification; the 817-LOC CSS
builder seven-pending-field count) found every entry to be a breadth-of-fact
statement, not an inflation, deflation, or contrivance. The mandate's three
firewalls all hold:

- **No contrivance.** Every broader-than-proven construct (multi-arch scan, richer
  `StructuralAlphabet`, AoS-vs-SoA, the 960-site StructLayout surface, the
  multi-arch simd-scan crate) is classed as scope/convergence/config-breadth
  pressure or breadth-of-fact cost, never inflated into a Lock violation; and no
  genuine spec tension (the exactly-one-encoding closure obligation, REDRESS-53
  re-entry risk, the StructRegistry per-leaf fence) is dressed away into a clean
  convergence story — each is carried as a catalogued divergence or fenced UNKNOWN.
- **Generalizable separated from special-cased.** The SoA `Tape` + `ValueRef<G>` +
  `select_classifier` model is consistently separated from the eager
  `OpenFrame`/`CssTypedValue`/no-`value_from_ref` current state; the JSON-empirical
  vs grammar-neutral split (1D) and the config-breadth-not-proof-breadth calls
  (1A SUB17-007, 1F COH17-008) are the load-bearing distinctions, all live-verified.
- **crates/core is the fold TARGET.** Every inventory's Direction-Monotonicity
  discipline holds; PC17-006 forbids reverse-relocation; the unwired tape is named
  "dormant fold target," not a defect (1A/1C/1F firewall rows).

## §4 Counts

- Sections reviewed: 8 (1A, 1B, 1C, 1D, 1E, 1F-coherence, 1F-anti-pattern,
  1F-past-corpora).
- ACCEPT: 8 — all eight inventories. 1A and 1E carry the folded V4 cross-lens
  REVISEs (the Lock-14 Gaps-row; LAC-04 path-(b) + CH5-S8 anchor), both verified
  clean and live-anchored.
- REVISE: 0.
- REJECT: 0.

## §5 OVERFIT-PRUNE verdict

The V5 inventories pass the CH7 mandate decisively and the lens converges for a
THIRD consecutive cycle (V3: 6 ACCEPT / 2 REVISE; V4: 8 ACCEPT / 0 REVISE; V5:
8 ACCEPT / 0 REVISE). The two cross-lens REVISEs folded into the V5 inventories
are, from the OVERFIT vantage, corrections in CH7's own direction-of-travel —
the 1A Gaps-row struck a spurious Lock-14-VIOLATION inflation (the live state is
Lock-14-ALLOWED `@generated` code, header re-verified), and the 1E LAC-04
path-(b) re-price struck a fold-target over-credit (pricing the absent
`LayoutFacts` side-table as live, `grep -rn LayoutFacts crates/`=0 re-verified).
Neither introduces a new over-fit; both ground the corrected claim on a live
`grep`-zero anchor at HEAD `445925167`.

The V5-new finding most central to the mandate — BSHAPE17-009, "NEON JSON-only vs
the shared-classifier generalization" — is resolved without contrivance: the
shared `select_classifier(alphabet)` is ALREADY grammar-general across 8 grammars
in core (impl-exceeds-spec, 0 LOC fold, narrative-only), every anchor
(`lib.rs:80`, `alphabet.rs:118`, `profile.rs:43`, `support.rs:472-485`, 8-grammar
scan census) re-verified live. The genuinely-generalizable model is cleanly
separated from the CSS/JSON-special-cased current state, and crates/core is
consistently the fold TARGET.

No orphan REVISE remains; no new defect surfaced; no recalled LOC (all citations
re-resolved live). Against the §4 convergence criterion (≥95% ACCEPT for two
consecutive cycles, zero open critical defects, no orphan unresolved REVISE),
the CH7 lens is at convergence on a clean 8 ACCEPT / 0 REVISE / 0 REJECT — 100%,
its third consecutive ≥95%-ACCEPT cycle. From the OVERFIT-PRUNE vantage T-P1
SK-V17 is converged; V5 does not approach the V≤5 hard ceiling as a BLOCKED state.
