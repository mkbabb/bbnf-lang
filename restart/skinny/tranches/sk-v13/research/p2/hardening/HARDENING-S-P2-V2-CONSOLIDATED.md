# SK-V13 S-P2 V2 Hardening Consolidated

Pass: S-P2 Research. Cycle: V2.
Date: 2026-05-21.
Scope: consolidated CH1-CH6 challenge verdict for the SK-V13 S-P2 V2 research fold.
Output: this file.

## Verdict

`G-S-P2-V2-CONVERGED`: FAIL.

V2 resolves the V1 challenge set almost completely. Five lenses accept. CH1
returns one narrow revise: P2-F still exposes CSS row/fact-stream scopes as
immediate S-P3 eligibility even though the current CSS profile is
timer/fact-sink dominated and not a parser-hot-leaf proof.

| Lens | Disposition | Load-bearing reason |
|---|---|---|
| CH1 correctness | REVISE | C7, D1, orphan SIMD inventory, dav1d lineage, comparator strictness, and Arm ISA overclaim are resolved; remaining blocker is that P2-F's CSS rows 1-6 need a non-primitive or conditional route-production stamp before S-P3 reads them as primitive eligibility. |
| CH2 generality / Lock 14 | ACCEPT | P2-F now cross-reads P2-A and maps C1-C8 into grammar-neutral, conditional, JSON-overfit, inventory, or `NOT-S-P3-ELIGIBLE` verdicts. |
| CH3 regression / REDRESS | ACCEPT | V2 preserves material differentials and does not reopen rejected REDRESS routes under renamed candidates. |
| CH4 cost / micro-proof | ACCEPT | Candidate costs, scalar/checkasm/same-wave gates, micro-prove-first requirements, narrow CSS profiling, generated-size risk, and resolver-time constraints are explicit enough for S-P3, subject to preservation. |
| CH5 hidden coupling / Lock 1 | ACCEPT | V2 preserves one tape or sink-only projection and rejects sidecar substrates, retained structural side vectors, parser-owned cursors/lists, aux density tables, public substrate APIs, and Track-2-as-substrate proof. |
| CH6 anti-paper-close | ACCEPT | dav1d is bounded out of gate authority, Arm citations are narrowed to named features/intrinsics, inventory/drop decisions are fenced from S-P3, and candidate tables carry scalar-reference, parity/checkasm, and consumer expectations. |

Acceptance rate: 5/6 = 83.3%.
Consecutive accepted cycles: 0.

## Fold Actions For V3

1. Update P2-F's CSS row/fact-stream entries so S-P3 cannot treat missing-CSS-hot-leaf
   row scopes as primitive admissions.
2. Either classify CSS rows 1-6 as non-primitive row-production scopes, or stamp
   each as conditional route-production requiring fresh narrow CSS parser profile
   or same-wave strict lightningcss/cssparser row movement.
3. Preserve all accepted V2 folds: do not reopen C7, D1, orphan SIMD inventory,
   dav1d lineage, comparator strictness, Arm ISA citations, REDRESS material
   differentials, or Lock 1 substrate boundaries.

## Cycle Disposition

S-P2 returns to a narrow V3 research fold owned by P2-F. If V3 resolves the CSS
eligibility wording and the six lenses accept, V4 must still run as the
confirmation cycle before S-P2 can converge.
