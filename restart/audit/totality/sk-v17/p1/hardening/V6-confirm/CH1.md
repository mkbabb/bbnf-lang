---
lens: CH1-CORRECTNESS
pass: T-P1-excavation
cycle: V6-confirm
subject: SK-V17 T-P1 EXCAVATION
generated_at: 2026-05-29T22:30:00Z
master_head: 445925167154de73540e3ea3283d0170371de790
reviewer_method: "CONFIRMING re-review of the V5-folded on-disk artefacts (restart/audit/totality/sk-v17/p1/1{a,b,c,d,e,f}*.md). Read PASS-1-EXCAVATION.md §3 CH1 + ORCHESTRATOR §3W/§3Z (carried via prompt); Read V5/CH1.md + HARDENING-T-P1-SKV17-V5-CONSOLIDATED.md to carry the single residual REVISE CH1-V5-001. Re-anchored every load-bearing file:line/SHA live at HEAD 445925167 via git rev-parse + grep -n/-c + sed -n + wc -l + ls over crates/core/src/{runtime,grammar/generated}, crates/ir/src/registry/{struct,strategy}.rs, crates/simd-scan/src/{lib,alphabet}.rs, skinny/crates/{runtime,ir,passes,codegen}, restart/{ARCHITECTURE,locks/LOCKS}.md, restart/skinny/tranches/sk-v17/SPEC.md. No recalled LOC; every load-bearing claim re-anchored at source."
inventories_reviewed:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1b-codegen-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1e-locks-evidence.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
  - restart/audit/totality/sk-v17/p1/1f-past-corpora.md
prior_cycle_dispositions_carried:
  - "CH1-V5-001 (1B frontmatter live_truth_method brace-glob `{eager,offset,event,collapsed}_tape.rs` names the non-existent collapsed_tape.rs; actual lowerer is collapsed_stage.rs) — V5 recorded it with a concrete fix but the fix was NOT applied to the on-disk artefact; STILL PRESENT at V6 (verified live). Re-flagged REVISE."
disposition_counts:
  accept: 7
  revise: 1
  reject: 0
sections_dispositioned: 8
---

## CH1 CORRECTNESS — Confirming Scope + Method

This is the CONFIRMING CHALLENGE cycle (V6-confirm) for SK-V17 T-P1 EXCAVATION.
Per the contract, a confirming cycle ACCEPTs where the V5-folded artefact is
correct + complete and flags only genuine residual defects. CH1 verifies, for the
current on-disk artefacts: every spec-claim ↔ implementation row resolves at the
cited file:line/SHA; the tape-substrate / eager-OpenFrame divergence is accurately
mapped; SKINNY-proven facts are cited correctly; and the single V5 residual REVISE
(CH1-V5-001) is checked against the on-disk state.

Master HEAD confirmed `445925167154de73540e3ea3283d0170371de790` (`git rev-parse HEAD`).

**Verdict in brief.** Every load-bearing substrate / value-API / BackendShape / NEON
fact re-anchors verbatim at master `445925167`. The two prior-folded V4 math.rs
corrections hold live: `crates/core/src/grammar/generated/math.rs` carries
`scan_structural`=0, `ScanState` (`:287-289`) declares only `nospace_bits: u64`
(`:288`) + `nospace_start: isize` (`:289`), and lines 281/285 are doc-comment text
inside the `ScanState` doc-block — math is the lone non-scanning generated grammar
of 8 wired (json/ebnf/bnf/csv/css_l4/css_pretty/google_sheets/bbnf). The
V5-localized author folds hold: `@generated` headers live
(`runtime/json/value.rs:1` = `// @generated by xtask regen-json`,
`runtime/css_l4/value.rs:1` = `regen-css`), `StructRegistry::layout` at `struct.rs:331`
/ `layout_by_name` at `:337` (not the decl `:313`), `LayoutFacts` grep-zero in
`crates/`, REDRESS-53 at SPEC `:577/:657/:825/:839`.

**One genuine residual defect — CH1-V5-001 — persists in the on-disk artefact.** The
V5 consolidated hardening doc recorded CH1-V5-001 as "non-load-bearing, recorded with
a concrete fix" but the fix was **never applied** to `1b-codegen-evidence.md`. The
frontmatter `live_truth_method` still reads
`wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs=17 each`
— the brace-glob expands `collapsed` to the non-existent `collapsed_tape.rs`
(`ls skinny/crates/codegen/src/lower/` confirms the file is `collapsed_stage.rs`;
`ls collapsed_tape.rs` = `No such file or directory`). The 17-LOC fact is true of
`collapsed_stage.rs` (`wc -l` = 17) and the body cites it correctly. Because this is
a confirming cycle that re-reviews the on-disk artefact — and the defect is verifiably
still present — §3 (1B) is re-dispositioned **REVISE**, not ACCEPT. No REJECT: no
inverted impl verdict, no unresolved load-bearing recall, no SKINNY-proof mis-citation.

---

## §1 — 1A substrate-evidence.md spec↔impl table + Gaps-row + counts — ACCEPT

All eight SUB17 rows + Cross-Tree map + Substrate-Union Firewall re-anchored live:

- **SUB17-001** SoA `Tape<'input>` six members at `skinny/crates/runtime/src/tape/mod.rs:94-100`
  (`source: &'input [u8]`, `offsets: Vec<u32>`, `flag_cursors: Vec<u32>`,
  `flag_values: Vec<u8>`, `payloads: PayloadArena`, `id: TapeId`) — **verbatim** (`sed -n '94,100p'`).
- **SUB17-002/003** core AoS `TapeRec` `#[repr(C, align(4))]` (`record.rs:102`) `pub struct TapeRec`
  (`:103`); 16-byte + align-4 const-asserts (`:120-121`) — **verbatim**.
- **SUB17-006** `StructLayout` Lock-2-retired (`LOCKS.md:160` = "Layout lowering is the
  canonical IR pass name ... replaces ... StructLayout") yet live; `grep -rn StructLayout
  crates/` = **960** (re-ran, exact) — confirmed.
- **SUB17-007/008** `scan_structural(input: &[u8], alphabet: &StructuralAlphabet)`
  `simd-scan/src/lib.rs:80`; `KernelShape::select(alphabet: &StructuralAlphabet)` `alphabet.rs:118`
  — verbatim. SUB17-009 fence `begin_compound(&StructLayout)` `tape/mod.rs:185` reading
  `(layout.rule_id & 0x1F)` `:186` — verbatim.

The Gaps-row Lock-14 `@generated`-not-hand-written relabel (the V5 1A-owned fold) holds:
the per-grammar value APIs carry live `@generated` headers (`runtime/json/value.rs:1`,
`runtime/css_l4/value.rs:1`); the divergence is correctly framed as the EAGER
materialization shape, not the generation provenance.

DISPOSITION (§1): **ACCEPT** — every row resolves; the Gaps-row Lock-14 relabel is
correct and source-anchored.

---

## §2 — 1A SKINNY-proven citations + tape↔OpenFrame map — ACCEPT

SKINNY-proven inputs resolve at source: `Tape<'input>` SoA `:94-100`;
`ValueRef<'doc,'input:'doc,K=AnyKind,G:EventGrammar=AnyGrammar>` `tape/mod.rs:175`
(1A's `ValueRef<'doc,'input>` shorthand is a faithful elision); `value_from_ref`
`skinny/crates/runtime/src/grammars/json/value.rs:143`; `select_classifier` aarch64
`skinny/crates/bbnf-simd/src/dispatch.rs:42`. The tape ↔ eager-OpenFrame map is
accurate: dead AoS `TapeStructBuilder` (`tape/mod.rs:58`, UNWIRED) vs live eager
`OpenFrame` builders (`json/builder.rs`, `css_l4/builder.rs`). No Track-1≡Track-2
dishonesty; the tape is catalogued dormant, not a sidecar.

DISPOSITION (§2): **ACCEPT**.

---

## §3 — 1B codegen-evidence.md spec↔impl table + frontmatter glob — REVISE

The nine BSHAPE17 rows + Cross-Tree Codegen Map re-anchored; load-bearing facts resolve:

- **BSHAPE17-001/002** `enum BackendShape` (`skinny/crates/ir/src/lib.rs:340`) +
  `derive_backend_shape` (`skinny/crates/passes/src/lib.rs:392`) exist ONLY in skinny;
  `grep -rn 'enum BackendShape\|derive_backend_shape' crates/` = **0** — verbatim/exit-clean.
- **BSHAPE17-003** `pub enum EmitStrategy {` `strategy.rs:104`; `is_struct_direct`
  `matches!(self, EmitStrategy::StructDirect { .. })` `:224`; `PRODUCTION_MANIFEST_TABLE`
  `:134` with **exactly 9** in-table `ManifestStrategyEntry {` rows
  (`:136,142,148,154,160,166,172,178,184`; the other two `{`-hits are struct-decl `:314`
  + impl `:327`) — the "9 rows" claim is **verbatim-correct**.
- **BSHAPE17-004** lowerer LOC: `eager_tape.rs`/`offset_tape.rs`/`event_tape.rs`/`collapsed_stage.rs`
  = 17 each (`wc -l`), `sink_only.rs` = 270 — **confirmed**. The body row cites the real
  filenames `{eager_tape,offset_tape,event_tape,collapsed_stage}.rs` and `collapsed_stage.rs:15-17`
  (`sed -n '15,17p'` = `pub fn lower_rule(rule: &BackendRule) -> String { super::tape_plan::render_rule(rule, ...TapeFlavor::Collapsed) }`) — **body correct**.
- **BSHAPE17-005** Cross-Tree CollapsedStage row cites `collapsed_stage.rs:15-17` — **correct**.
- **BSHAPE17-006** `StructRegistry::layout` `struct.rs:331`, `layout_by_name` `:337`, decl
  `:313` — verbatim (the V5 symbol-anchor fold holds). `begin_compound(&StructLayout)`
  `tape/mod.rs:185`, `:186` — verbatim.
- **BSHAPE17-009** shared classifier already general: `scan_structural(input, &StructuralAlphabet)`
  `lib.rs:80`, `KernelShape::select(alphabet)` `alphabet.rs:118`, 8 generated grammars wired
  — confirmed. impl-exceeds-spec verdict sound.

**Residual defect (CH1-V5-001) — STILL PRESENT, re-flagged REVISE.** The 1B frontmatter
`live_truth_method` brace-glob still reads
`wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs=17 each`.
The brace-glob `{...}_tape.rs` expands `collapsed` to the **non-existent**
`collapsed_tape.rs` (`ls skinny/crates/codegen/src/lower/collapsed_tape.rs` =
`No such file or directory`; the real file is `collapsed_stage.rs`). V5/CH1.md flagged
this exact defect, and HARDENING-T-P1-SKV17-V5-CONSOLIDATED.md §"Residual REVISE"
recorded it with a concrete fix — but the fix was **never applied** to the on-disk
artefact (verified live this cycle: the glob is unchanged at `1b-codegen-evidence.md`
frontmatter `live_truth_method`). Because this is a confirming cycle that re-reviews
the on-disk artefact, the persisting defect re-disposes §3 to REVISE.

- **file:line**: `restart/audit/totality/sk-v17/p1/1b-codegen-evidence.md` frontmatter
  `live_truth_method` (the `wc -l skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_tape.rs=17 each`
  brace-glob), contradicted by `ls skinny/crates/codegen/src/lower/`.
- **Fix**: enumerate the real filenames —
  `wc -l skinny/crates/codegen/src/lower/{eager_tape,offset_tape,event_tape,collapsed_stage}.rs = 17 each, sink_only=270`.
  The body BSHAPE17-004/005 citations of `collapsed_stage.rs:15-17` are already correct.
  No verdict/census change; the four-scaffold + 270-LOC-sink_only census stands.

DISPOSITION (§3): **REVISE** — every body row resolves verbatim (the 9-row manifest,
grep-zero BackendShape/derive selectors, the symbol-anchor `:331`/`:337`, the NEON
impl-exceeds-spec, and the body's correct `collapsed_stage.rs:15-17` citations); the one
frontmatter brace-glob defect (CH1-V5-001) persists unfixed and is re-flagged.

---

## §4 — 1C runtime-evidence.md + math.rs fold — ACCEPT

The prior V4 math.rs fold holds live. 1C's Exec Summary + RT17-005 + Cross-Tree scan row
state `math.rs` `scan_structural`=0 AND NO `structural_index` field: its `ScanState`
carries only `nospace_bits`/`nospace_start`, and the two `OnceCell` mentions are
doc-comment text. Re-anchored live: `grep -c scan_structural math.rs` = **0**;
`grep -n "OnceCell\|nospace\|struct ScanState" math.rs` = doc-comments `:281`/`:285`,
`pub struct ScanState` `:287`, `nospace_bits: u64` `:288`, `nospace_start: isize` `:289`.

Other base citations confirmed: 8 scan-wired grammars (`grep -rln scan_structural
crates/core/src/grammar/generated` = bbnf/bnf/css_l4/css_pretty/csv/ebnf/google_sheets/json);
real `structural_index: OnceCell<...>` FIELDS in json/css_l4 contrast math's doc-comments;
`@generated` headers live; `begin_compound(&StructLayout)` `tape/mod.rs:185`.

DISPOSITION (§4): **ACCEPT** — the math.rs fold is live-verified; the 8-grammar census,
the real-field contrast, and every line citation are sound.

---

## §5 — 1D skinny-lessons.md RESULTS/REDRESS/corpus + math.rs fold — ACCEPT

The prior V4 math.rs fold holds live: SK17L-008 + frontmatter state the corrected
"EIGHT wired; math.rs carries NO `structural_index` field — its `ScanState` struct
(math.rs:287-290) declares only `nospace_bits`/`nospace_start`; the two `OnceCell`
mentions at :281/:285 are doc-comment text" form; re-anchored live (matches verbatim).

Re-anchored: skinny `BackendShape` enum `ir/src/lib.rs:340`; `derive_backend_shape`
`passes/src/lib.rs:392`; `value_from_ref` `grammars/json/value.rs:143`; `StructLayout`
Lock-2-retired `LOCKS.md:160` + live + 960 sites; SPEC pre-block anchors `:793-795`
(per-leaf no-registry-lookup), x86/AVX bar `:806`, D6 `:854` — re-verified verbatim.

DISPOSITION (§5): **ACCEPT** — the math.rs fold is live-verified; RESULTS/REDRESS/corpus
citations resolve; the eager-vs-lazy (not generated-vs-hand-written) framing is intact.

---

## §6 — 1E locks-evidence.md + LAC-04 / CH5-S8 folds — ACCEPT

Both V5 folds that touched 1E hold live:

- **CH4 LAC-04 path-b re-pricing.** `grep -rn LayoutFacts crates/` = **0** (re-ran,
  confirmed) — so path-(b) "lock re-scope to side-table" is non-zero against the fold
  target; `LayoutFacts` is named in `LOCKS.md:160` as "the public side-table" but is
  skinny/prior-totality-only in realisation. The qualified pricing is **correct**.
- **CH5-S8 REDRESS-53 re-anchor.** `grep -n REDRESS-53 SPEC` resolves at `:577`
  (parallel-vector naming), `:657`, `:825` (W2 row), `:839` (shortlist condition) — all
  **verbatim-confirmed**; the rows cite `:577`/`:825`/`:839`. **Correct.**

Verified invariants re-anchored: 5-shape canon `{EagerTape,OffsetTape,EventTape,SinkOnly,CollapsedStage}`
`LOCKS.md:107-108` — verbatim; scan wired all 8; ONE tape construct UNWIRED
(`TapeStructBuilder` `tape/mod.rs:58`); L02 drift (`StructLayout` 960); L10 CollapsedStage
UNKNOWN-2D-05 `ARCH:1206`; SK-V17 aarch64-only `SPEC:806`. The six LOCKS-amendment-candidate
rows each carry supporting path:line.

DISPOSITION (§6): **ACCEPT** — both V5 REVISEs (LAC-04 path-b, REDRESS-53 anchor) are
live-verified; every lock verdict + candidate resolves; LayoutFacts=0 and
REDRESS-53@:577/:825/:839 confirmed.

---

## §7 — 1F triad (coherence + anti-pattern + past-corpora) + math.rs fold — ACCEPT

The math.rs root fold lands in 1F-anti-pattern: the Sidecar row carries "the 9th
generated grammar `math.rs` is NOT a carrier — `grep -c scan_structural math.rs` = 0
and its two `OnceCell` mentions at `math.rs:281,285` are doc-comment text, not a
`structural_index` field." Confirmed live.

1F-coherence rows re-anchored: COH17-001 `LOCKS.md:75` carries BOTH the offset-tape-admissible
clause ("The projection may be an offset tape, event tape, or collapsed-stage event sink")
AND the "Columnar SoA stays buried" clause — verbatim; COH17-002 `@generated` value-API,
no `value_from_ref` in core; COH17-004 CollapsedStage x86-pinned anchors; COH17-006
StructLayout 960. 1F-anti-pattern: parallel-substrate firewall (one UNWIRED tape), the
817-LOC CSS builder god-module with six `pending_*` Vecs + one `pending_value: Option`
= seven fields, the StructRegistry do-not-redrive fence — all resolve. 1F-past-corpora is
consistent with the live census.

DISPOSITION (§7): **ACCEPT** — the math.rs fold lands in 1F-anti-pattern; every coherence
+ anti-pattern + past-corpora row resolves; the offset-tape-admissible vs column-buried
distinction is correctly anchored to the dual `LOCKS.md:75` clauses.

---

## §8 — Prior-cycle disposition fold integrity — ACCEPT (with carried REVISE noted)

The two V4 math.rs REVISE dispositions are folded and live-re-verified at source
(§4/§5/§7), and the V5-localized author folds (1A Gaps-row Lock-14 relabel, 1B
symbol-anchor `:331`/`:337`, 1E LAC-04 path-b + CH5-S8 REDRESS-53) are folded and
live-verified (§1/§3/§6). The frontmatter `prior_cycle_dispositions_folded` lists map
to real V4/V3/V5 disposition IDs with accurate fix descriptions; no prior load-bearing
disposition is mis-folded or orphan.

The lone exception is **CH1-V5-001 itself**: V5 recorded it as "fix concrete, carry
forward" but did not apply the fix to the on-disk 1B frontmatter. The fold integrity of
all *load-bearing* dispositions is intact; the single non-load-bearing frontmatter-glob
fix was left unapplied and is re-flagged in §3. This §8 ACCEPT covers the fold integrity
of the load-bearing dispositions; the carried CH1-V5-001 is the §3 REVISE.

DISPOSITION (§8): **ACCEPT**.

---

## Disposition Summary

| § | Subject | Disposition | Finding |
|---|---|---|---|
| §1 | 1A spec↔impl table + Gaps-row Lock-14 fold + counts | **ACCEPT** | — |
| §2 | 1A SKINNY citations + tape↔OpenFrame map | **ACCEPT** | — |
| §3 | 1B codegen spec↔impl table + frontmatter glob | **REVISE** | CH1-V5-001 STILL PRESENT: 1B frontmatter `wc -l` brace-glob names `collapsed_tape.rs`; the file is `collapsed_stage.rs`; body cites it correctly. V5 recorded the fix but never applied it. |
| §4 | 1C runtime census + math.rs fold | **ACCEPT** | — |
| §5 | 1D skinny-lessons RESULTS/REDRESS/corpus + math.rs fold | **ACCEPT** | — |
| §6 | 1E locks + LAC-04 / CH5-S8 folds | **ACCEPT** | — |
| §7 | 1F triad (coherence + anti-pattern + past-corpora) + math.rs fold | **ACCEPT** | — |
| §8 | Prior-cycle disposition fold integrity | **ACCEPT** | — |

**Counts**: ACCEPT 7 · REVISE 1 · REJECT 0. (ACCEPT-rate 7/8 = 87.5%.)

**Verdict**: Every load-bearing substrate / value-API / BackendShape / NEON fact
re-anchors verbatim at master `445925167`: SoA `Tape` six-member (`tape/mod.rs:94-100`),
AoS `TapeRec` 16-byte align-4 const-asserts (`record.rs:102-103,120-121`), `ValueRef`
generic + `value_from_ref:143`, `TapeStructBuilder` UNWIRED, `BackendShape` /
`derive_backend_shape` skinny-only (grep-zero in `crates/`), `EmitStrategy::StructDirect`
+ 9 in-table manifest rows, `StructRegistry::layout`/`layout_by_name` `:331`/`:337`,
`begin_compound(&StructLayout)` `:185-186`, `StructLayout`=960, `LayoutFacts`=0 in
`crates/`, scan-wired-8 census (math.rs `scan_structural`=0, `ScanState` `:287-289`
nospace-only, OnceCell `:281/:285` doc-comments), `@generated` value headers,
REDRESS-53 `:577/:825/:839`, SPEC x86-bar `:806`, ARCH UNKNOWN-2D-05 `:1206`, LOCKS
`:75`/`:107-108`/`:160`. The single residual defect CH1-V5-001 — the 1B frontmatter
`wc -l` brace-glob mis-naming `collapsed_stage.rs` as the non-existent `collapsed_tape.rs`
— **persists unfixed in the on-disk artefact** because the V5 consolidated recorded the
fix but never applied it; it is re-flagged REVISE on §3. The defect is non-load-bearing
(frontmatter-shorthand only; the 17-LOC fact is true and the body cites
`collapsed_stage.rs:15-17` correctly) but it is a genuine residual citation defect, so
this confirming cycle cannot ACCEPT-all. No REJECT: no inverted impl verdict, no
unresolved load-bearing recall, no SKINNY-proof mis-citation. The fix is a one-line
frontmatter glob edit and touches no load-bearing substrate / value-API / BackendShape
/ NEON claim.
