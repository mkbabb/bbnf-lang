# SK-V15 T-P1 V5 CH2 Generality

## Verdict

ACCEPT.

The V5 FNV transcript and citation fold preserve the V4 CH2 accepted
generality surfaces. `P1-1B-D9` / `P1-1B-D10` remain grammar-neutral Lock 14
failures with Sheets / BBNF-self / CSS or non-JSON metadata proof receivers.
`RC-01` through `RC-11` remain bounded receiver rows. Primitive/kernel rows
remain non-admitting until scalar oracle, aarch64 or scalar-delegate
implementation, strict parity, and same-wave consumer proof exist. The FNV/hash
surface remains telemetry/quarantine evidence only, not CSS Value API proof,
retained document identity, same-substrate proof, production equality proof, or
a JSON-only lesson.

This CH2 report makes no cohort `§3Z` lock claim. V5 context says a clean V5 can
close known orphan REVISEs but does not by itself create two consecutive clean
cycles because V4 was REVISE
(`restart/audit/totality/p1/hardening/V5/CHALLENGE-CONTEXT.md:26-30`), while
the orchestrator requires two consecutive `>=95%` cycles or user pin and
escalates at V5 if convergence is absent
(`restart/prompts/ORCHESTRATOR.md:118-126`).

## Evidence

Authority:

- CH2 requires Lock 14 generality: no grammar-neutral fact catalogued as
  JSON-only, 1C flags every grammar-named module in generic runtime, 1D
  separates JSON-empirical from grammar-neutral findings, and no grammar-name
  leak passes uncited
  (`restart/prompts/totality/PASS-1-EXCAVATION.md:110-114`).
- The universal CH2 lens requires grammar-neutral interventions that work for
  CSS L4 / Sheets / BBNF-self, not JSON only
  (`restart/prompts/ORCHESTRATOR.md:81-85`).
- V5 scope is the six live inventories `1A` through `1F-coherence-scan`;
  `1F-anti-pattern.md` and `1F-past-corpora.md` are historical/superseded
  (`restart/audit/totality/p1/hardening/V5/CHALLENGE-CONTEXT.md:11-24`;
  `restart/audit/totality/p1/1F-coherence-scan.md:86-108`).
- V4 CH2 was ACCEPT on exactly this surface: D9/D10 stayed grammar-neutral,
  receiver rows stayed non-JSON, and primitive rows were not demoted
  (`restart/audit/totality/p1/hardening/V4/CH2.md:3-10`;
  `restart/audit/totality/p1/hardening/HARDENING-T-P1-V4-CONSOLIDATED.md:24-32`;
  `restart/audit/totality/p1/hardening/HARDENING-T-P1-V4-CONSOLIDATED.md:43-57`).
- V5 explicitly requires preservation of D9/D10, receiver rows, primitive rows,
  FNV quarantine, and generated-header non-provenance surfaces
  (`restart/audit/totality/p1/hardening/V5/CHALLENGE-CONTEXT.md:54-61`), with
  CH2 focused on preventing the FNV fold from demoting D9/D10, receiver rows, or
  primitive rows into JSON-only lessons
  (`restart/audit/totality/p1/hardening/V5/CHALLENGE-CONTEXT.md:69-71`).

Live inventory evidence:

- `1F` folds the FNV transcript as a current-source hash-sidecar/telemetry row,
  not as generality proof. COH-016 says all seven CSS generated runtime files
  are listed at root-resolving FNV path:line positions, then states the result is
  telemetry-only unless W10 proves otherwise and is not CSS Value API proof,
  retained document identity, same-substrate evidence, or a production equality
  arbiter (`restart/audit/totality/p1/1F-coherence-scan.md:89`;
  `restart/audit/totality/p1/1F-coherence-scan.md:91-101`). The owner/receiver
  map routes CSS generated FNV hashes to W10 quarantine with the same proof
  boundary (`restart/audit/totality/p1/1F-coherence-scan.md:154`), and the gaps
  table keeps production FNV quarantine open
  (`restart/audit/totality/p1/1F-coherence-scan.md:177`).
- `1D` keeps FNV/hashes scoped: JSON closed-enum FNV products are pending
  JSON-empirical quarantine (`restart/audit/totality/p1/1D-skinny-lessons.md:104`,
  `restart/audit/totality/p1/1D-skinny-lessons.md:129`); `RC-08` is W10 FNV
  quarantine with bench-only/delete disposition and no production equality
  arbiter (`restart/audit/totality/p1/1D-skinny-lessons.md:181`); the
  product-builder/hash primitive row is delete or bench-only quarantine
  (`restart/audit/totality/p1/1D-skinny-lessons.md:211`); and FNV production
  guard remains UNKNOWN/required
  (`restart/audit/totality/p1/1D-skinny-lessons.md:230`).
- `1B` still carries `P1-1B-D9` and `P1-1B-D10` as generic pass/codegen leaks,
  not JSON-only lessons. The live divergence row says recognizer mining is
  JSON-punctuation-coded and materialization role mining is JSON-role-coded
  (`restart/audit/totality/p1/1B-codegen-evidence.md:53`); the fold rows state
  they are grammar-neutral Lock 14 leaks with Sheets/BBNF-self proof receivers
  (`restart/audit/totality/p1/1B-codegen-evidence.md:71`,
  `restart/audit/totality/p1/1B-codegen-evidence.md:79`); and the divergence
  rows require Sheets/BBNF-self/CSS or non-JSON generated metadata proof
  (`restart/audit/totality/p1/1B-codegen-evidence.md:107-108`).
- `1D` repeats the same D9/D10 classification. `G-10` says recognizer and
  materialization mining are JSON-shaped generic pass logic, not JSON-only
  empirical lessons, with Sheets and BBNF-self as proof receivers
  (`restart/audit/totality/p1/1D-skinny-lessons.md:147`). `CH2-FOLD-004`
  repeats that D9/D10 are grammar-neutral Lock 14 failures, not JSON-only
  empirical lessons (`restart/audit/totality/p1/1D-skinny-lessons.md:190`).
- The receiver carrier remains grammar-neutral where CH2 needs it. `RC-07`
  routes D8/D9/D10 to CSS plus Sheets-or-BBNF-self generated metadata proof and
  rejects branches without non-JSON proof
  (`restart/audit/totality/p1/1D-skinny-lessons.md:180`). `RC-10` requires
  scalar oracle, non-JSON witness or scoped claim, REDRESS pre-block check, and
  generated consumer (`restart/audit/totality/p1/1D-skinny-lessons.md:183`).
  `RC-11` requires scalar reference, aarch64 implementation or scalar-delegate
  decision, strict parity, and final source-present primitive disposition
  (`restart/audit/totality/p1/1D-skinny-lessons.md:184`).
- The primitive/kernel table remains non-admitting and named by row, not
  JSON-only. It enumerates byte-class, bitmap, UTF-8, string, cursor/tape,
  product-builder/hash, and parse-that rows
  (`restart/audit/totality/p1/1D-skinny-lessons.md:196-217`). Rows requiring
  non-JSON or grammar-neutral proof include byte-class consumers
  (`restart/audit/totality/p1/1D-skinny-lessons.md:200-201`), UTF-8 validation
  (`restart/audit/totality/p1/1D-skinny-lessons.md:206`), parse-that rows
  (`restart/audit/totality/p1/1D-skinny-lessons.md:212-217`), and the
  product-builder/hash quarantine row
  (`restart/audit/totality/p1/1D-skinny-lessons.md:211`).
- The cited P2 sources support that posture: SIMD/ASM admission requires scalar
  oracle, target path, checkasm parity, same-wave consumer, and manifest/locks
  (`restart/skinny/tranches/sk-v15/research/p2/p2b-dav1d-process.md:24-32`),
  source-present primitives must close as wired/deleted/scalar-delegate/blocker
  and orphan files do not close Lock 16
  (`restart/skinny/tranches/sk-v15/research/p2/p2b-dav1d-process.md:41`), and
  individual primitive claims are not grammar-neutral until policy source and
  consumer prove it
  (`restart/skinny/tranches/sk-v15/research/p2/p2b-dav1d-process.md:60-67`).
  Parse-that candidate APIs may expose no JSON/CSS/Sheets/BBNF names in generic
  APIs and JSON-only wording is rejected
  (`restart/skinny/tranches/sk-v15/research/p2/p2e-parse-that-gaps.md:223-236`).
- `1C` satisfies the runtime-census side of CH2: it flags generic runtime root
  grammar-name leaks (`restart/audit/totality/p1/1C-runtime-evidence.md:61`),
  marks runtime root and Pattern H as Lock 14 failures
  (`restart/audit/totality/p1/1C-runtime-evidence.md:90-94`), and carries
  runtime root/profile/CSS-row leaks as open divergence rows
  (`restart/audit/totality/p1/1C-runtime-evidence.md:118-123`).
- `1F` carries the owner/receiver map for runtime root, codegen roster, pass
  recognizer mining, pass materialization mining, Pattern H, CSS generated body,
  CSS generated FNV hashes, and Lock 14 exclusions
  (`restart/audit/totality/p1/1F-coherence-scan.md:144-155`). `1E` preserves the
  gate discipline: Lock 14 is drifted because scan exclusions hide leak-bearing
  files (`restart/audit/totality/p1/1E-locks-evidence.md:103`), and Lock 14/16
  gates must report included roots, exclusions, primitive source status, and
  strict checkasm/parity evidence
  (`restart/audit/totality/p1/1E-locks-evidence.md:197-200`).

Targeted check:

```sh
rg -n "JSON-only|json-only" \
  restart/audit/totality/p1/1A-substrate-evidence.md \
  restart/audit/totality/p1/1B-codegen-evidence.md \
  restart/audit/totality/p1/1C-runtime-evidence.md \
  restart/audit/totality/p1/1D-skinny-lessons.md \
  restart/audit/totality/p1/1E-locks-evidence.md \
  restart/audit/totality/p1/1F-coherence-scan.md
```

The only live hits are negative/scoped uses: `1D:114` says the current scan
token universe is too JSON-only and therefore a grammar-neutral gate failure;
`1D:147`, `1D:190`, `1B:71`, and `1B:79` explicitly reject JSON-only routing
for D9/D10. No live inventory line demotes D9/D10, receiver rows, primitive
rows, or FNV/hash rows into a JSON-only lesson.

## Findings

| id | disposition | finding | evidence |
|---|---|---|---|
| CH2-V5-001 | ACCEPT | The V5 FNV citation fold is a telemetry/quarantine strengthening, not a generality demotion or proof laundering. | `restart/audit/totality/p1/1F-coherence-scan.md:89`, `restart/audit/totality/p1/1F-coherence-scan.md:91-101`, `restart/audit/totality/p1/1F-coherence-scan.md:154`, `restart/audit/totality/p1/1D-skinny-lessons.md:181`, `restart/audit/totality/p1/1D-skinny-lessons.md:211`, `restart/audit/totality/p1/1D-skinny-lessons.md:230`. |
| CH2-V5-002 | ACCEPT | `P1-1B-D9` / `P1-1B-D10` remain grammar-neutral Lock 14 failures with non-JSON proof receivers. | `restart/audit/totality/p1/1B-codegen-evidence.md:71`, `restart/audit/totality/p1/1B-codegen-evidence.md:79`, `restart/audit/totality/p1/1B-codegen-evidence.md:107-108`, `restart/audit/totality/p1/1D-skinny-lessons.md:147`, `restart/audit/totality/p1/1D-skinny-lessons.md:190`, `restart/audit/totality/p1/1F-coherence-scan.md:150-151`. |
| CH2-V5-003 | ACCEPT | Receiver rows remain bounded and non-JSON where required; FNV is separate quarantine, not a substitute for D9/D10 proof. | `restart/audit/totality/p1/1D-skinny-lessons.md:170-184`, especially `restart/audit/totality/p1/1D-skinny-lessons.md:180-184`. |
| CH2-V5-004 | ACCEPT | Primitive/kernel rows are enumerated and non-admitting until scalar oracle, aarch64/scalar-delegate implementation, strict parity, and same-wave consumer proof exist. | `restart/audit/totality/p1/1D-skinny-lessons.md:196-217`; `restart/skinny/tranches/sk-v15/research/p2/p2b-dav1d-process.md:24-32`; `restart/skinny/tranches/sk-v15/research/p2/p2b-dav1d-process.md:41`; `restart/skinny/tranches/sk-v15/research/p2/p2e-parse-that-gaps.md:223-236`. |
| CH2-V5-005 | ACCEPT | 1D still separates JSON-empirical, grammar-neutral, and CSS audit-demoted lessons; 1C/1F/1E still surface grammar-name leaks instead of passing them uncited. | JSON rows at `restart/audit/totality/p1/1D-skinny-lessons.md:122-132`; grammar-neutral rows at `restart/audit/totality/p1/1D-skinny-lessons.md:134-147`; CSS rows at `restart/audit/totality/p1/1D-skinny-lessons.md:149-159`; runtime census at `restart/audit/totality/p1/1C-runtime-evidence.md:61`, `restart/audit/totality/p1/1C-runtime-evidence.md:90-94`, `restart/audit/totality/p1/1C-runtime-evidence.md:118-123`; owner/receiver map at `restart/audit/totality/p1/1F-coherence-scan.md:144-155`; gate carrier at `restart/audit/totality/p1/1E-locks-evidence.md:197-200`. |

## Required Fold

None. CH2 is ACCEPT for SK-V15 T-P1 V5. Preserve `1D` rows `G-10`, `RC-07`,
`RC-08`, `RC-10`, `RC-11`, the primitive/kernel receiver table, and the `1F`
Lock 14 owner/receiver map in any consolidation. Do not broaden scoped JSON or
FNV/hash quarantine language into fleet-wide generality or production proof.
