# Omega-C Locks Amendments — Pass Omega V10 SK-V18 Totality

Date: 2026-06-01.
Cycle: V10 (astral synthesis above the three converged totality passes).
Scope: Ω-C — the SK-V18 GENERALIZATION-discipline `restart/locks/LOCKS.md`
amendment, the G-Omega gate object.
Disposition: LOCKS AMENDMENT REQUIRED, STAGED ONLY, G-Omega gated.

## Verdict

SK-V18 totality requires ONE proposed `LOCKS.md` amendment: the SK-V18 T-P3 3C
GENERALIZATION-discipline crystallisation addendum. It is NOT a lock-count change
and NOT a new `BackendShape`. It is a governance-strengthening addendum, by
ADDITION only, that folds the converged SK-V18 GENERALIZATION discipline — the
discipline the SK-V15 and SK-V17 addenda left unbound — into the locks surface so
the SK-V18 implementation waves can consume it as machine-checked admission gates.

This Ω-C pass CONSUMES the T-P3 3C disposition (it does not re-derive it). The
final staged diff is the V10-hardened consolidation OF the converged
`restart/audit/totality/p3/3C-locks-v+1-diff.md` (six clauses expanded with
present-tense-honesty + ownership-pointer refinements folding the V1–V3 CHALLENGE
corrections; no clause dropped, added, or REVERSED). The diff body is NOT
byte-identical to `3C-locks-v+1-diff.md` (md5 differs; 6 of 11 clauses changed);
`git apply --check` exits 0 and the 11-clause / 9A-11M-0R-1D disposition is
preserved. Re-verified at HEAD.

The staged diff lives at `restart/audit/totality/astral/V10/locks-diff.md`. The
live `restart/locks/LOCKS.md` is NOT touched; the CRUD merge executes POST-G-Omega,
after user sign-off.

## Disposition — 9 ACCEPT / 11 MODIFY / 0 REJECT / 1 DEFER

T-P3 disposed all 21 SK-V18 amendment candidates (1E×7, 1A×1, 2C×3, 2D×4, 2E×3,
2F×3) into 11 lock-addendum clauses, ZERO silent drops, EXACTLY one disposition
per candidate (`restart/audit/totality/p3/3C-locks-crystallisation.md:48`-`49`,
`:117`-`120`):

- **9 ACCEPT**: LAC-1E-V5-01 (named-primitive (a)-(d) gate), LAC-1E-V5-02
  (relocated-seam firewall), LAC-1E-V5-03 (neutrality-proof), LAC-1E-V5-05
  (verbatim-blob), LAC-1E-V5-06 (green-by-exclusion), LAC-2D-V3-01 (un-fork
  definition), LAC-2D-V3-02 (relocated-seam lock clause), LAC-2D-V3-04
  (CollapsedStage slot), LAC-2F-V3-01 (single-substrate + mask unification).
- **11 MODIFY**: LAC-1E-V5-04 + LAC-2E-V6-01 (folded → aarch64-ONLY),
  LAC-1E-V5-07 + LAC-2C-SK18-03 (folded → Pattern-H re-census), 1A-LOCK1-AMEND-001
  + LAC-2D-V3-03 (folded → cursor-generality re-anchor + e-graph regression-guard),
  LAC-2C-SK18-01 + LAC-2C-SK18-02 (folded → neutrality-proof scoping),
  LAC-2E-V6-02 + LAC-2F-V3-02 (folded → retarget-not-author), LAC-2E-V6-03
  (folded → single-substrate movemask).
- **0 REJECT**: no candidate is dismissed.
- **1 DEFER**: LAC-2F-V3-03 (re-scope the "gap" frame) — an AUDIT-SCOPE assertion,
  not a LOCKS clause; folded into the single-substrate clause as a one-line
  audit-scope note so it is not silently dropped.

The two load-bearing ACCEPTed candidates are disposed FIRST in the matrix
(`3C-locks-crystallisation.md:119`-`120`).

## Load-Bearing Accepted Clauses

1. **Named-primitive (a)-(d) gate** (LAC-1E-V5-01 → Lock 14/16/8): every
   hand-shaped kernel admitted under the §6 honest-finding escape MUST pass a
   machine-checked four-conjunct gate — (a) grammar-INVOKED-by-name, (b)
   emitted-output VARIES under invoking-rule mutation, (c)
   `verbatim_blob_present == false`, (d) PROFILE-PROVEN-NARROW-LEAF. Any one
   failing conjunct ⇒ relabeled hand-written blob ⇒ REJECT. This is the single
   largest paper-close surface, currently SK-V18-SPEC-only; the addendum lifts it
   to a lock discipline. Evidence: `restart/skinny/tranches/sk-v18/SPEC.md:358`-`390`;
   `restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md:257`-`266`;
   `restart/audit/totality/p1/1E-locks-evidence.md:147`.

2. **Relocated-seam firewall + un-fork** (LAC-1E-V5-02 + LAC-2D-V3-01 +
   LAC-2D-V3-02 → Lock 5/14/1/10): the un-forked emitter is ONE `render(program)`
   body dispatching ONLY on the cost-model-derived
   `program.policy_summary.backend_shape` (`emit_shape_source == lowered_program`),
   with `RuntimeEmitterKind` DELETED and `generator_grammar_branch_count == 0`.
   md5-distinctness is NECESSARY-NOT-SUFFICIENT (it proves the output differs per
   grammar, not that the SELECTION is grammar-derived); the PLANNED full-row
   `PartialEq` co-gate `runtime_target_rows_collapsed` is mandatory because the
   arm-census grep cannot see a per-grammar branch relocated into a neutral data
   table. iburg-faithful (LOPLAS 1992, DOI 10.1145/151640.151642). Evidence:
   `restart/skinny/tranches/sk-v18/SPEC.md:179`-`185`,`:247`,`:1100`-`1145`;
   `restart/audit/totality/p1/1E-locks-evidence.md:148`,`:106`.

## DEFER Re-Entry Trigger

LAC-2F-V3-03 (the lone DEFER) is an AUDIT-SCOPE assertion: the balanced-delimiter
scan is present in BOTH `parse-that/rust/parse_that/src/parsers/scan/` AND skinny
`bbnf-simd/src/`, so the V2 "balanced-scan gap" was a scope error, not a parse-that
absence. It is folded into the single-substrate clause as a one-line note.

Re-entry trigger: ANY future 2F-class re-audit that cites a "balanced-scan gap"
MUST `ls` both `parse-that/.../scan/` and `skinny/crates/bbnf-simd/src/` in its
evidence header before declaring the gap. Receiver: SK-V19 / next-cycle T-P1 1E
author (`3C-locks-crystallisation.md:144`,`:176`).

The companion totality bundle — the `crates/simd-scan` second `NibbleLut`/`WideLut`
classifier convention (`crates/simd-scan/src/lib.rs:67`) and the cursor-generality
`<G>` strike — is the SK-V19 scanner-unification single-priced disposition
(D-SKV18-L01 / COH18-015 / MP.SK19.SCANNER-UNIFY); the skinny `rg = 0` green is NOT
a totality single-substrate proof.

## Invariant Check (re-verified at HEAD)

```sh
# 16 numbered locks
grep -nE '^[0-9]+\. \*\*' restart/locks/LOCKS.md | head -16
#   1..16 at :75,160,170,179,181,183,200,202,260,269,319,328,336,349,436,453

# Five BackendShape variants in code (no sixth)
rg -n 'BackendShape::(EagerTape|OffsetTape|EventTape|SinkOnly|CollapsedStage)' \
   skinny/crates/codegen/src/lower/mod.rs        # :18-24, the five lowerings
rg -n 'all_backend_shapes\(\) -> \[BackendShape; 5\]' \
   skinny/crates/ir/src/cost.rs                  # :334

# Two PLANNED co-gate symbols are absent (PLANNED, not live)
rg -c runtime_target_rows_collapsed skinny/crates skinny/xtask    # 0
rg -c bbnf_simd_single_mask_convention skinny/crates             # 0

# Staged diff applies cleanly against live LOCKS.md at HEAD
awk '/^```diff$/{flag=1;next} /^```$/{flag=0} flag{print}' \
  restart/audit/totality/astral/V10/locks-diff.md | git apply --check -   # exit 0
```

Results at HEAD:
- 16 numbered locks confirmed; addendum adds no Lock 17.
- Five `BackendShape` variants confirmed in code; no sixth shape.
- `runtime_target_rows_collapsed` rg=0 and `bbnf_simd_single_mask_convention`
  rg=0 — both written as PLANNED SK-V18-SPEC gates, never as live symbols.
- Staged `restart/audit/totality/astral/V10/locks-diff.md` body is the V10-hardened
  consolidation OF the converged T-P3 `3C-locks-v+1-diff.md` (folding the V1–V3
  CHALLENGE corrections); the diff body DIFFERS from `3C-locks-v+1-diff.md` by the
  hardening deltas (md5 differs; 6 of 11 clauses changed), no clause dropped, added,
  or REVERSED; `git apply --check` exits 0 against live `restart/locks/LOCKS.md` and
  the 11-clause / 9A-11M-0R-1D disposition is preserved.
- Insertion anchor: AFTER the SK-V17 Lock 16 clause (`LOCKS.md:622`) and BEFORE
  the `## v+1 Governance Boundary` heading (`LOCKS.md:625`); the SK-V15
  (`:581`-`607`) and SK-V17 (`:610`-`622`) addenda are neither restated nor edited.

## Non-Negotiable Boundaries

- Preserve exactly 16 numbered locks. No Lock 17.
- Preserve exactly five `BackendShape` variants
  `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`. No sixth shape;
  CollapsedStage is a SHAPE SLOT ONLY with a `diagnostic-only / author-declared`
  body.
- Apple M5 Max / aarch64 is the ONLY admission host; the whole x86 surface is a
  P1 DELETION target; x86/AVX-512/GFNI is totality-SECONDARY pressure closing no
  skinny row.
- No public substrate API, retained sidecar, new directive, new BIR variant, lock
  retirement, or sixth shape enters through this amendment.
- Amendment by ADDITION only; the SK-V15/SK-V17 addenda are not renumbered or
  edited.

## Proposed Locks Diff

See `restart/audit/totality/astral/V10/locks-diff.md` — the staged, G-Omega-gated
diff the user reads before authorizing the LOCKS merge. STAGED ONLY; the live
`restart/locks/LOCKS.md` is untouched until POST-G-Omega sign-off.
