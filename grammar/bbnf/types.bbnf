// Type annotations for value expressions and @host return types.
// Includes primitive types and arbitrary identifiers (for IrNode, etc.).
type_name = "u8" | "u16" | "u32" | "u64"
          | "i32" | "i64"
          | "f32" | "f64"
          | "bool" | "usize"
          | /[_a-zA-Z][_a-zA-Z0-9]*/ ;
