# BC.W6 — BC Close

**Name**: W6 — BC Close
**Opens after**: BC.W5 close (sister crate API freeze; `bbnf-regex` endpoint reconciliation; worktree fixture closure)
**Hard gate**: final perf gates BC-G1..G3 met (sub-sonic JSON, sub-lightning CSS); PROGRESS / FINAL committed; IR contract published at `docs/codegen-IR-CONTRACT.md`; carry ledger to BD.W0 named explicitly
**Status**: planned (consumes the sub-waved W0a/W0b/W0c, W1a/W1b, W3a/W3b/W3c/W3d/W3e, W5a/W5b/W5c/W5d closure artefacts)

## §1 Deliverable

Hereupon BC closes. The final perf gates BC-G1..G3 land (`JsonParser::parse(twitter.json)` ≤ 380 µs beating sonic-rs's 436 µs by 13%; `parse(bootstrap.css)` ≤ 3.0 ms beating lightningcss's 4.16 ms by 27%; `parse(canada.json)` ≤ 2.8 ms beating sonic-rs's 3.144 ms by 11%). The IR contract is published at `docs/codegen-IR-CONTRACT.md`. The 13-Lock cross-reference table is verified with no empty cells. PROGRESS.md and FINAL.md commit. The carry ledger to BD.W0 is named explicitly: TS + WASM activation, host-fn per-backend resolution, sister crate publication, worktree fixture infrastructure.

The wave is the closure ceremony; no new substrate lands. The substrate landed in W0-W5; W6 verifies, documents, hands off.

## §2 Milestones

### §2.1 Final perf gate verification: JSON twitter

Mechanism: run the JSON twitter benchmark on M1 Pro; assert `JsonParser::parse(twitter.json)` ≤ 380 µs per BC-G1 (`audit/SOTA-2026-05-03.md:50-58`, sonic-rs 436 µs by ≥ 13%). Capture the bench output to `docs/tranches/BC/audit/W6-bench-twitter.json`.

Files: `docs/tranches/BC/audit/W6-bench-twitter.json` (create).

Sub-gate: the bench output records the wall + samples + sonic-rs comparison; the gate passes; samply trace shows the OpenFrame retiral from BA.W5 is preserved post-BC refactor.

### §2.2 Final perf gate verification: CSS L4 bootstrap

Mechanism: run the CSS L4 bootstrap.css benchmark; assert `parse(bootstrap.css)` ≤ 3.0 ms per BC-G2 (`audit/SOTA-2026-05-03.md:131-136`, lightningcss 4.16 ms by ≥ 27%). Capture the bench output.

Files: `docs/tranches/BC/audit/W6-bench-bootstrap.json` (create).

Sub-gate: the bench output records the wall + samples + lightningcss comparison; the gate passes; the BB.W1 OpenFrame retiral for CSS L4 is preserved post-BC refactor.

### §2.3 Final perf gate verification: JSON canada

Mechanism: run the JSON canada.json benchmark; assert `parse(canada.json)` ≤ 2.8 ms per BC-G3 (`audit/SOTA-2026-05-03.md:50-58`, sonic-rs 3.144 ms by ≥ 11%). Capture the bench output.

Files: `docs/tranches/BC/audit/W6-bench-canada.json` (create).

Sub-gate: the bench output records the wall + samples + sonic-rs comparison; the gate passes.

### §2.4 IR contract publication

Mechanism: `docs/codegen-IR-CONTRACT.md` lands at BC.W0; W6 verifies the published version is the final canonical specification. The contract is the boundary between optimiser-output and per-backend-lowerer-input; the Rust lowerer consumes only typed IR; the TS + WASM scaffolds compile against the same contract.

Files: `docs/codegen-IR-CONTRACT.md` (verify; no modification).

Sub-gate: the contract document is the published reference for the BC tranche and the input for BD; cross-reference verified against the wave-by-wave closure; no metalanguage refs.

### §2.5 PROGRESS / FINAL commit

Mechanism: `docs/tranches/BC/PROGRESS.md` records the wave-by-wave progress through BC; `docs/tranches/BC/FINAL.md` records the closing artefacts: the perf gate metrics, the IR contract publication, the 13-lock cross-reference verification, the carry ledger to BD, the close posture.

Files: `docs/tranches/BC/PROGRESS.md` (create), `docs/tranches/BC/FINAL.md` (create).

Sub-gate: PROGRESS records each wave's open/close timestamp + closer-gate verification; FINAL records the final perf metrics + the IR contract publication + the carry ledger.

### §2.6 13-Lock cross-reference verification

Mechanism: walk the 13-lock cross-reference table from BC.md; verify every cell names the wave that addresses the lock; verify no empty cells. The verification is the BC tranche's closure attestation per Lock 4 (which BC.md's table closes via BB.W3 reference) and Lock 5 (which BC closes via W0+W1+W2+W4) and Lock 7 (which BC closes via W5) and Lock 11 (which BC closes via W5) and Lock 13 (which BC closes via W3).

Files: `docs/tranches/BC/audit/W6-lock-verification.md` (create).

Sub-gate: every lock has at least one BC wave cell populated (or a prior-tranche closure note); no empty cells; cross-reference verified against the wave-by-wave closure.

### §2.7 Carry ledger to BD.W0 named explicitly

Mechanism: name BD.W0's preflight carries explicitly. BC→BD.C1: TS + WASM emitter scaffolds at `bbnf-codegen/src/{ts,wasm}/`; BD activates them in production with host-fn per-backend resolution. BC→BD.C2: sister crates frozen; BD may promote any to crates.io. BC→BD.C3: worktree fixture contract closure for parallel-agent dispatch infrastructure.

Files: `docs/tranches/BC/audit/W6-carry-ledger.md` (create).

Sub-gate: the carry ledger document names each BC→BD carry-tag with its owning BC wave + the receiving BD wave (BD.W0 for all three); BD is named explicitly; no fictional successor letters.

### §2.8 Generated-LOC budget verification

Mechanism: per BC-G10, the post-BC `crates/core/src/grammar/generated/` (post-W3 split: `crates/bbnf-parse/src/parse/generated/`) net delta from BB ≤ +2%. Verify per-file LOC at BC close.

Files: `docs/tranches/BC/audit/W6-generated-loc.md` (create).

Sub-gate: the per-file LOC table records pre-/post-BC LOC; aggregate net delta ≤ +2%; per-grammar deltas within the W4 visitor budget.

### §2.9 Workspace test surface verification

Mechanism: `cargo nextest run -p bbnf-parse -p bbnf-codegen -p bbnf-runtime -p bbnf-ir -p bbnf-analysis -p bbnf-path -p bbnf` 100% pass; per-grammar parity tests against sonic-rs, lightningcss, simdjson, cssparser, serde_json all pass. Bench harness produces post-BC.json archetype.

Files: `docs/tranches/BC/audit/W6-test-surface.md` (create), `docs/benchmarks/post-BC.json` (create).

Sub-gate: the test-surface document records the per-crate test counts; all tests pass; the post-BC.json bench archetype is the canonical record for BC close.

### §2.10 Cohort-grammar perf attestation

Mechanism: cohort grammars (BNF, CSV, EBNF, CSS Pretty, Math) per `audit/MODULES-2026-05-03.md:625-628` get template-driven shrinkage at BB.W2; performance is not the canonical metric for these grammars. BC.W6 attests that the cohort grammars' parse fns continue to function correctly post-BC refactor; no perf gate against external SOTA (per `docs/tranches/PLAN-INPUT-2026-05-03.md:341` — cohort grammars exist for grammar-author breadth, not perf flagship).

Files: `docs/tranches/BC/audit/W6-cohort-attestation.md` (create).

Sub-gate: the document records each cohort grammar's parse-fn correctness post-BC refactor; no external SOTA comparison required; the attestation is structural (parse + emit round-trip) only.

### §2.11 Friction-forecast closure

Mechanism: per Lane 7 (Friction-Forecast), BC.W6 closes the forecast set: the IR contract syntax (BC.W0 §2.9), the visitor friction (BC.W4 §2.8), the core split breaking change (BC.W3). Each forecast names: the API surface, the user mental model required, the point of greatest confusion, the educational artefact (cookbook entry, doc page, error message hint).

Files: `docs/tranches/BC/audit/W6-friction-closure.md` (create).

Sub-gate: the document closes each forecast point with at least one verbatim error message + at least one cookbook-entry pointer; cross-reference verified against W0 + W3 + W4 friction docs.

## §3 Closer gate

| ID | Gate | Verification |
|---|---|---|
| W6-G1 | BC-G1 met | `JsonParser::parse(twitter.json)` ≤ 380 µs on M1 Pro per `docs/tranches/BC/audit/W6-bench-twitter.json` |
| W6-G2 | BC-G2 met | `parse(bootstrap.css)` ≤ 3.0 ms on M1 Pro per `docs/tranches/BC/audit/W6-bench-bootstrap.json` |
| W6-G3 | BC-G3 met | `parse(canada.json)` ≤ 2.8 ms on M1 Pro per `docs/tranches/BC/audit/W6-bench-canada.json` |
| W6-G4 | IR contract published | `test -f docs/codegen-IR-CONTRACT.md` and contract is the canonical reference |
| W6-G5 | PROGRESS + FINAL committed | `test -f docs/tranches/BC/PROGRESS.md && test -f docs/tranches/BC/FINAL.md` |
| W6-G6 | 13-lock cross-reference verified | `docs/tranches/BC/audit/W6-lock-verification.md` shows every lock with at least one populated cell |
| W6-G7 | Carry ledger to BD.W0 named | `docs/tranches/BC/audit/W6-carry-ledger.md` names BC→BD.C1/C2/C3 with owning + receiving wave |
| W6-G8 | Generated-LOC budget within +2% | `docs/tranches/BC/audit/W6-generated-loc.md` per-file LOC table; aggregate delta ≤ +2% |
| W6-G9 | All workspace tests pass | `cargo nextest run` 100% pass per `docs/tranches/BC/audit/W6-test-surface.md` |

## §4 Invariants

§I1. **Lock 8 (surpass sonic-rs / simdjson / lightning-css)**: BC-G1, BC-G2, BC-G3 all met; final perf gates surpass the SOTA anchors per `audit/SOTA-2026-05-03.md`.

§I2. **All 13 locks honoured**: every lock has at least one wave cell populated (in BC, prior tranches, or pre-BA ceremony for L12); the cross-reference table is the closure attestation.

§I3. **No metalanguage in close docs**: PROGRESS and FINAL cite path:line; do not reference commits or conversation history.

§I4. **No fictional successor letters**: the carry ledger names BD only; BD's existence as a future tranche is implied by the carry-tag, not by a draft document. BD has not been drafted; this is named explicitly per Lane 8 (Carry-Audit).

§I5. **Lock 5 (IR + per-backend lower)**: the IR contract is published at `docs/codegen-IR-CONTRACT.md`; the Rust lowerer consumes only typed IR; the TS + WASM scaffolds compile against the same contract; cross-backend visitor isomorphism verified at W4.

§I6. **Generated-LOC budget honoured**: net delta from BB close ≤ +2% per BC-G10; the IR contract is the boundary, not the regen surface; the visitor surface adds modest LOC bounded by the per-grammar record alphabet.

§I7. **Cohort-grammar attestation**: cohort grammars (BNF, CSV, EBNF, CSS Pretty, Math) per `audit/MODULES-2026-05-03.md:625-628` parse correctly post-BC; no external SOTA comparison required (per `docs/tranches/PLAN-INPUT-2026-05-03.md:341`); structural attestation only.

§I8. **No tape resurrection**: the BC tranche introduces no tape, columnar SoA, variant-strip arena, or "tape rebranded as fast-path" substrate. Lock 1 invariant preserved through every wave.

## §5 Risks

| Risk | Likelihood | Mitigation |
|---|---|---|
| BC-G1 (JSON twitter ≤ 380 µs) misses because BB.W3's optimiser path didn't deliver the expected speedup | Low | Per the §5 BC.md risk table, BC.W5 entry pre-flight verifies BB.W3's deliverables; if BB.W3 missed, BC.W6 perf gates may miss; mitigation is BB.W3's CSP/e-graph/miner output samply checkpoints. |
| BC-G2 (CSS L4 bootstrap ≤ 3.0 ms) misses because the visitor surface adds runtime overhead | Low | The visitor surface is opt-in; bench tests run without visitor invocation; the perf path is the parse path, not the visitor path. |
| BC-G3 (JSON canada ≤ 2.8 ms) misses because canada is a deeply-nested JSON workload that exercises the OpenFrame retiral less effectively than twitter | Medium | The canada workload's structural index is dominated by repeated number arrays; the direct-projection emit (BA.W5) handles this case; BB.W3's SIMD scanner auto-detection further accelerates; if the gate misses, root-cause traced before close. |
| The IR contract publication is incomplete (some node kinds lack documented Layout/TypeDesc resolution rules) | Low | BC.W0 §2.1 mandates the contract document name every IR node kind; W6 verifies the published version is canonical; if incomplete, W0 must be amended before W6 closes. |
| The carry ledger to BD names BD as the receiving tranche, but BD has not been drafted | Low (this is expected) | Per Lane 8 (Carry-Audit), BD's drafting status is named explicitly: BD has NOT been drafted; the carry-tag is the forward commitment; BD's drafting happens post-BC close. The named BD wave (BD.W0) is the future-state preflight. |
| Workspace test surface fails because the post-W3 split breaks an integration test | Medium | The W3 closer gate (W3-G4) verified all tests pass; W6 re-verifies as part of close; if a regression appears between W3 and W6 (e.g., introduced by W4 visitor or W5 freeze), the wave is rolled back to the pre-regression state. |
| The bench harness produces non-deterministic timings due to thermal throttling on M1 Pro | Low | Bench runs use multi-sample averaging (Criterion default ≥ 100 samples); thermal throttling is bounded; the 13% / 27% / 11% gain margins (BC-G1, BC-G2, BC-G3) are larger than M1 Pro's typical thermal-throttling variance (~5%); the gates have signal even under throttling. |
| The 13-lock cross-reference verification reveals an empty cell because BC carries no work for that lock and the prior-tranche closure note is missing | Low | The verification document at W6 §2.6 names every lock + every wave that addresses it OR a prior-tranche closure note; if any lock is empty, BC.md's table is amended to include the prior-tranche closure note (e.g., L1 closed in BA.W0+W5 + BB.W1; L3 closed in BA.W4 + BB.W2). |
| The carry ledger's BD-not-drafted naming is misinterpreted as a fault per Lane 8 | Low (this is expected) | The ledger explicitly names "BD has NOT been drafted at BC close" and points to the receiving wave (BD.W0); per Lane 8's discipline, a future-state preflight is acceptable when the carry-tag is named with both owning + receiving wave; the user's TS/WASM punt is the policy basis. |

## §6 Cross-references

### Carry-tags consumed

| Tag | From | Description |
|---|---|---|
| BC.W5→BC.W6 | BC.W5 | Sister crates frozen; bench harness has stable inputs; BC perf gates can be measured without sister-crate API drift |
| BC.W4→BC.W6 | BC.W4 | Cross-backend visitor isomorphism; the W4 cross-backend test passes; W6 records the closing attestation |
| BC.W3→BC.W6 | BC.W3 | Post-split workspace; per-crate tests pass; W6 verifies the union test surface |
| BC.W2→BC.W6 | BC.W2 | TS + WASM scaffolds compile + smoke-test pass; W6 records the carry to BD for activation |
| BC.W1→BC.W6 | BC.W1 | Rust lowerer consumes typed IR exclusively; regen-equality verified; W6 records the closing perf metrics |
| BC.W0→BC.W6 | BC.W0 | IR contract published; W6 confirms the publication is canonical |

### BC-G gates closed

| Gate | Closure |
|---|---|
| BC-G1 | `JsonParser::parse(twitter.json)` ≤ 380 µs on M1 Pro, beating sonic-rs's 436 µs by ≥ 13% (W6-G1) |
| BC-G2 | `parse(bootstrap.css)` ≤ 3.0 ms on M1 Pro, beating lightningcss's 4.16 ms by ≥ 27% (W6-G2) |
| BC-G3 | `parse(canada.json)` ≤ 2.8 ms on M1 Pro, beating sonic-rs's 3.144 ms by ≥ 11% (W6-G3) |
| BC-G10 | Generated-file LOC stable: post-BC `crates/core/src/grammar/generated/` net delta from BB ≤ +2% (W6-G8) |

### Carry-tags produced

| Tag | To | Description |
|---|---|---|
| BC→BD.C1 | BD.W0 | TS + WASM emitter scaffolds exist at `bbnf-codegen/src/{ts,wasm}/`; BD activates them in production; host-fn per-backend resolution (TS: `runtime.parseHexColor`; WASM: indexed extern import) is BD scope. The scaffolds compile, produce trivial-grammar output, and fail gracefully on host-fn shim sites with named error strings; BD lands the host-fn resolution table per backend. **BD has NOT been drafted at BC close**; BD.W0 is the receiving wave per Lane 8 (Carry-Audit). |
| BC→BD.C2 | BD.W0 | Sister crates frozen (egraph, egraph-derive, csp-solver, bbnf-regex); `cargo publish --dry-run` produces clean output for each; BD may promote any to crates.io. The endpoint reconciliation lands one canonical `bbnf-regex` path; BD operates against the published crate. |
| BC→BD.C3 | BD.W0 | Worktree fixture contract closure supports parallel-agent dispatch infrastructure for BD execution; `xtask worktree-init` materialises `data/{json,css,bbnf,sheets}` + `grammar/<name>/rewrites/*.ron` for every grammar; BD's parallel agent layout consumes this contract. |

## §7 Iter-time check

| Activity | Pre-W6 wall | Post-W6 wall | Notes |
|---|---:|---:|---|
| `cargo xtask regen --check` | ≤ 22 s (W5 close) | ≤ 22 s | BC close gate (≤ 22 s on M1 Pro per the §Build/iter time gate of BC.md) |
| `cargo nextest run --workspace` | ~50 s | ~50 s | unchanged |
| `cargo bench` (canonical bench harness) | n/a | ~3 min | new iter-loop introduced for final perf gate verification; one-time run for W6 closure |
| `cargo doc --workspace` | ~35 s | ~35 s | unchanged |
| Iter-loop: edit `docs/tranches/BC/FINAL.md` + `cargo doc --no-deps` | n/a | < 1 s | doc-only iter-loop |

The W6 wave does NOT regenerate the grammar/generated/ tree; the regen output is stable from W5 close.

## §8 Dependencies

- **Depends on**: BC.W5 close (sister crate API freeze; bench harness stable); BC.W4 close (visitor surface formalisation); BC.W3 close (post-split workspace); BC.W2 close (TS + WASM scaffolds); BC.W1 close (Rust lowerer consumes typed IR); BC.W0 close (IR contract published).
- **Blocks**: BD.W0 (consumes BC→BD.C1, BC→BD.C2, BC→BD.C3 carries; BD has NOT been drafted at BC close per Lane 8 Carry-Audit).

## §9 Closing posture

BC closes. The IR contract is the boundary; the Rust lowerer ships in production; the TS + WASM scaffolds ship as compiled-against-the-contract scaffolds; the core crate fractures into three cohesive sub-crates; the sister crates freeze and become crates.io candidates; the `bbnf-regex` endpoint reconciliation lands one canonical path; the worktree fixture contract closes. Final perf gates surpass sonic-rs / simdjson / lightning-css per Lock 8. The 13 locks are settled; BC verifies them in seven waves and hands BD a ratified backend ABI. The carry to BD names TS + WASM activation, sister crate publication, and worktree fixture infrastructure for parallel-agent dispatch. **BD has NOT been drafted at BC close**; BD.W0 is the receiving preflight per Lane 8 (Carry-Audit).

Hereupon BD opens.

## §10 BC-tranche attestation

BC.W6 is the close attestation. Every BC-G gate has a closure citation. Every wave has a closer-gate verification. Every lock has a wave cell or a prior-tranche closure note. Every carry-tag has owning + receiving wave names. The 13-lock cross-reference is verified empty-cell-free. PROGRESS.md records the wave-by-wave timeline; FINAL.md records the closing artefacts (perf gates, IR contract publication, lock verification, carry ledger). The BC tranche closes as the third of the BA-restart trilogy: BA the surgical foundation, BB the generality + optimisation, BC the backend ABI + multi-backend foundation. The carry to BD is the eventual TS/WASM emergence; BD opens with the named carry-tags as its preflight.
