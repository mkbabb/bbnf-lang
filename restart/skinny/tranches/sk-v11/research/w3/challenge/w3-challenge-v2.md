# SK-V11 Wave W3 CHALLENGE V2

Date: 2026-05-20.

Plan challenged:

- `restart/skinny/tranches/sk-v11/research/w3/w3-plan-number-span-emit-slot.md`
  after the CHALLENGE V1 redress commit.

Disposition: **ACCEPT — redress authorized**.

All six lenses accept the revised W3 plan. The acceptance is scoped to the JSON
direct-plane numeric closure/fixpoint route; W2 remains BLOCKED and the
non-JSON axis remains routed to W8/W9.

## Lens Results

| Lens | Disposition | Conditions |
|---|---|---|
| CH1 correctness/measurability | ACCEPT | Gate/report owner paths, row floors, verification commands, and `gate-json` consumption are now falsifiable. |
| CH2 generality/Lock 14 | ACCEPT with caveats | `gate.rs` / `report.rs` changes must remain W3 provenance-only; `sink_direct.rs` may be edited only to preserve the JSON direct template. W2 BLOCKED is not non-JSON proof. |
| CH3 regression/REDRESS | ACCEPT | REDRESS 31/39/46/80, W0 clamp bypass, parse-only evidence, and SK-V9 substrate routes remain blocked. |
| CH4 cost/LOC/cap | ACCEPT | Added gate/report tests and conditional template preservation still fit the W3 cap if `numbers` remains optional and UDOT stays out of scope. |
| CH5 hidden coupling/Track 2 | ACCEPT | The plan now discloses Track 2 as an independent caller with a shared parse-that numeric scanner/materializer and makes generated number-slot parity mandatory. |
| CH6 anti-paper-close | ACCEPT | W3 provenance, caller delta, row floors, non-`gate_only` consumer metadata, and `gate-json` consumption prevent helper-only/manual row movement. |

## Redress Conditions

Redress must:

1. Keep the source patch within the revised owner paths.
2. Avoid `sink_direct.rs` unless preserving the generated JSON direct template
   is necessary; no generic number policy or non-JSON route may move.
3. Add W3 gate consumption for selected direct rows before claiming admission.
4. Add the generated `parse_direct` root/object/array number-slot parity test.
5. Measure `mesh/direct_to_struct` with native Criterion and `gate-json`; claim
   `numbers` only with W3 provenance and floor clearance.
6. Record any no-row movement or missed floor as REDRESS rejection, not a
   paper-close.
