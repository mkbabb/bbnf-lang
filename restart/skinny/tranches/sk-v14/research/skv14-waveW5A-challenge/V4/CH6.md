# SK-V14 W5A CHALLENGE V4 CH6: Consumers and Revert
Disposition: ACCEPT
Acceptance score: 95/100
Findings:
- ACCEPT: Same-wave consumers are mandatory, executable, and not deferred. The W5A plan requires `cargo xtask regen-css` plus all seven `check-css-l4-*` companions to exercise the new request path, and it adds a grep gate that fails if `regen.rs` still calls `emit_runtime_profile(target.profile)` at the xtask boundary.
- ACCEPT: CSS/JSON/Sheets/BBNF all prove the same request path. CSS is the production consumer through `regen-css` and seven companion checks; JSON must pass `check-json`, a whole-directory no-diff/hash proof, and an in-code equality test against `emit_from_source("json", source)`; Sheets/BBNF-self must use the same parser/contract and either fail closed with named source-located unsupported constructs or emit generated-role witnesses.
- ACCEPT: The plan blocks paper close. It pairs named exact tests with nonzero-pass log assertions, request-path greps, provider/template count and staged/unstaged A/D/R checks, JSON runtime no-diff evidence, exact `RESULTS.md`/rolling-delta no-diff maintain, LOC accounting, and downstream path no-diff checks.
- ACCEPT: The revert protocol is complete for the W5A risk slice. It escrows the rejected patch, reverts `grammar_provider.rs`, parser/runtime-generation construct support, `lib.rs` request entrypoint edits, `regen.rs`/`regen_css.rs` routing, and the W5A Lock 14 guard as one slice, retains the existing provider/template mesh, and requires REDRESS to name the failed construct/proof/metadata/JSON/Sheets/BBNF/guard surface.
- ACCEPT: Current code confirms the plan's consumer tests are necessary rather than ceremonial. `regen_css.rs` enumerates seven CSS targets over fifteen CSS source inputs plus workspace metadata, but `regen.rs` still only hashes those inputs and calls `codegen::emit_runtime_profile(target.profile)`; JSON currently travels through a separate `emit_from_source("json", source)` path that W5A must preserve while routing through the new request.
Required folds:
- NONE
Evidence:
- `restart/skinny/tranches/sk-v14/SPEC.md:662` requires grammar source plus workspace metadata to enter codegen for runtime emission instead of remaining freshness-only inputs.
- `restart/skinny/tranches/sk-v14/SPEC.md:663` through `restart/skinny/tranches/sk-v14/SPEC.md:668` require grammar-neutral construct parsing, CSS migration, JSON unchanged proof, Sheets/BBNF same-contract proof, Lock 14 guard, and `regen-css` plus all seven CSS companions.
- `restart/skinny/tranches/sk-v14/SPEC.md:672` through `restart/skinny/tranches/sk-v14/SPEC.md:680` define the W5A exit gates, including same-path CSS emission, JSON unchanged-output, Sheets/BBNF named proof, no new provider/template surface, and the <=1.0k LOC cap.
- `restart/skinny/tranches/sk-v14/SPEC.md:682` through `restart/skinny/tranches/sk-v14/SPEC.md:684` bind `cargo xtask regen-css` and the seven `check-css-l4-*` companions as production consumers in the W5A commit.
- `restart/skinny/tranches/sk-v14/SPEC.md:693` through `restart/skinny/tranches/sk-v14/SPEC.md:696` define the one-slice revert and REDRESS attribution requirement; `restart/skinny/tranches/sk-v14/SPEC.md:698` through `restart/skinny/tranches/sk-v14/SPEC.md:699` block W5B/W6/W7 and new-admit waves on W5A rejection.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:39` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:45` require the request-path migration, CSS construct parsing, JSON unchanged-output proof, Sheets/BBNF named same-path proof, provider/template count preservation, exact maintain no-diff, and LOC cap.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:61` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:81` list exact grammar/codegen tests, nonzero-pass assertions, `check-json`, `regen-css`, seven CSS companion commands, `gate-json`, and exact maintain no-diff.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:87` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:106` add executable grep/count/LOC/downstream no-diff gates, including the fail-if-present `emit_runtime_profile(target.profile)` grep.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:111` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-plan.md:113` require rejected-patch escrow, one-slice revert, provider/template retention, REDRESS attribution, and CSS/JSON/Sheets/BBNF same-request consumer proof.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-C-css-companion-emission.md:213` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-C-css-companion-emission.md:230` recommend one source-consuming request, replacing `emit_runtime_profile`, parsing CSS L4 constructs without `grammar_id == css_l4`, and proving every CSS companion observes source deltas through the new contract.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:72` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:100` require `check-json` migration through the same source+metadata request and in-code equality with the current source path.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:131` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-D-json-unchanged-output.md:141` identify paper-close risks: JSON-only proof, freshness-only hashing, incomplete JSON roster, CSS grammar-name exceptions, and generic Sheets/BBNF unsupported claims.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-E-sheets-bbnf-witness.md:94` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-E-sheets-bbnf-witness.md:107` require Sheets/BBNF tests to call the same contract and assert exact unsupported code, grammar id, path, offset, and source hash.
- `restart/skinny/tranches/sk-v14/research/skv14-W5A-E-sheets-bbnf-witness.md:125` through `restart/skinny/tranches/sk-v14/research/skv14-W5A-E-sheets-bbnf-witness.md:130` reject reuse of existing `sheets_witness` or SK-V13 witness JSON as W5A proof.
- `skinny/xtask/src/regen_css.rs:5` through `skinny/xtask/src/regen_css.rs:23` define the fifteen CSS source inputs and workspace metadata inputs; `skinny/xtask/src/regen_css.rs:25` through `skinny/xtask/src/regen_css.rs:75` enumerate all seven CSS L4 targets and companion command names.
- `skinny/xtask/src/regen_css.rs:77` through `skinny/xtask/src/regen_css.rs:118` route `regen-css` and every CSS companion through `regen::write_targets` / `regen::check_target`.
- `skinny/xtask/src/regen.rs:14` through `skinny/xtask/src/regen.rs:33` show the current CSS call boundary still uses `codegen::emit_runtime_profile(target.profile)` for both write and check paths.
- `skinny/xtask/src/regen.rs:61` through `skinny/xtask/src/regen.rs:74` show source and metadata are currently hashed for freshness only, which W5A explicitly forbids as the final proof shape.
- `skinny/xtask/src/main.rs:19` through `skinny/xtask/src/main.rs:33` expose `check-json`, `regen-css`, and all seven CSS companion commands; `skinny/xtask/src/main.rs:159` through `skinny/xtask/src/main.rs:165` show current `check-json` uses `emit_from_source("json", &source)`.
- `skinny/crates/codegen/src/lib.rs:98` through `skinny/crates/codegen/src/lib.rs:107` define the current JSON source-consuming path; `skinny/crates/codegen/src/lib.rs:117` through `skinny/crates/codegen/src/lib.rs:120` define the profile-only runtime path that W5A must remove from the xtask boundary.
