# HARDENING-MASTER-PLAN-V8

V8 simplification audit of the post-Phase-7.5 MASTER-PLAN trio. Three new lenses
(I — Contrivance; J — Host-language leverage; K — Meta-grammar discipline)
applied alongside the standard nine-lane grid. V7.1 ratified the architecture's
*coherence*; V8 asks whether that coherence is *lean* — whether the trio's
load-bearing surface admits simplification, host-leverage, or V2 deferral
without architectural loss.

## §1 Target Identification

| Target | Path | HEAD line count | Phase landed |
|---|---|---:|---|
| ARCHITECTURE | `restart/ARCHITECTURE.md` | 1,699 | Phase 7.5A (`3207b1cb`) |
| MASTER-PLAN | `restart/MASTER-PLAN.md` | 848 | Phase 7.5A (`3207b1cb`) |
| MIGRATION | `restart/MIGRATION.md` | 816 | Phase 7.5A (`3207b1cb`) |

Mandatory read set:

- `restart/prompts/audit-specs/HARDENING-LENS-SET.md` (lens spec, §Lens I/J/K, V8+ verdicts)
- `restart/ARCHITECTURE.md` (deep-read §7-§13)
- `restart/MASTER-PLAN.md` (skim A-J; deep-read §10.1, §24, §25)
- `restart/MIGRATION.md` (skim §3 crosswalk + §20)
- `restart/audit/hardening/HARDENING-MASTER-PLAN-V7.md` (V7 baseline)
- `restart/audit/hardening/HARDENING-CONSOLIDATED-V7.1.md` (V7.1 close)
- `restart/locks/LOCKS.md` (architectural commitments)
- `restart/research/V1-FOLD-CANDIDATES.md` (30-fold ledger)

V8 audit posture: this is the *first* simplification pass. V1-V7 ratified
correctness + coherence + cite hygiene; V8 asks whether the architecture is
the smallest one that satisfies the user mandate. Lenses I/J/K are calibrated
to surface contrivance, host-leverage opportunities, and meta-grammar overreach
without re-litigating the settled architectural shape.

V8 cohort (parallel): V8-PASS-1 audits substrate; V8-PASS-2 audits codegen;
V8-PASS-3 audits runtime; V8-MASTER-PLAN (this report) audits the trio.

V8 verdict (preview): **READY-WITH-AMENDMENT**. The trio carries thirteen
SIMPLIFY / CONSOLIDATE / LEVERAGE / ASPIRATIONAL findings that admit narrow
amendments; none cross the redraft threshold. The bulk of the architecture
load-bears; the punch list is concentrated on tier-3 reductions (rewrite-budget
category collapse, Backend trait method rationalisation, lock-count
consolidation, type-system stack trimming) plus host-leverage cross-cuts.

## §2 Lens I — Contrivance (Per-Item Table)

The trio is the architectural surface where simplification has highest
leverage. Lens I asks: does each architectural facility carry a distinct
load-bearing invariant, or is some facility ceremony / cardinality bloat /
double-tracking?

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/locks/LOCKS.md:34`, `:50` | Lock 1 (tape substrate) vs Lock 9 (slice-borrow + bumpalo + owned escape hatches) | Lock 1 governs the runtime substrate (tape + direct-to-struct union; no parallel substrate; no OpenFrame ladder). Lock 9 governs the parse-API escape hatches (`parse` slice-borrow primary; `parse_in` arena; `parse_owned`). Both touch memory. | Distinct invariants: Lock 1 owns *substrate identity* (one tape, no parallel tree); Lock 9 owns *lifetime API surface* (three entrypoints over the same parse). The two compose without overlap — the API surface is over the substrate. | The two locks are read in adjacent contexts and the prose risks reader confusion (memory-management appears twice in a 14-lock list). Lock 9's `parse_owned` couples substrate-internal allocation policy to a Lock 1 concern (the owned document still uses tape internally; the owned-vs-borrowed split is a presentation-time policy, not a substrate-level invariant). | Steelman: collapse the two into one *Lock 1+9 substrate-and-surface* lock. The collapsed prose would carry tape identity + the three lifetime entrypoints + the bumpalo escape hatch as one invariant family. | KEEP — the two locks load-bear distinct invariants (substrate identity vs API surface). Reader confusion is voice, not contrivance. The challenge fails because the API surface is genuinely independent of the substrate (the same tape can be exposed through any of the three entrypoints; a future arena-only or owned-only entrypoint would not require touching Lock 1). |
| `restart/locks/LOCKS.md:34-60` (14-lock list) | 14-lock cardinality | Fourteen architectural commitments numbered 1-14. Lock 13 is tree shape; Lock 6 is committed codegen; Lock 11 is path-dep incubation; Lock 12 is archive ceremony. | Each lock cites a distinct enforcement mechanism (tape identity vs lint-tree vs xtask regen vs path-dep classification vs A.W0 ceremony). | Locks 6, 11, 12, 13 are all *workspace hygiene* invariants. They could fold into one lock-family ("hygiene") with sub-clauses, reducing cardinality from 14 to 11. Locks 6+11+12 share an enforcement family (xtask + Cargo.toml). | Steelman: collapse Locks 6, 11, 12, 13 into one "Workspace + Build Hygiene" lock with four sub-clauses (committed-source / path-dep / archive / tree-shape). Each sub-clause retains its enforcement command; the lock-list shrinks to 11 commitments. | KEEP — each of the four locks carries a distinct close-gate at a distinct tranche (Lock 6 → F.W3; Lock 11 → J.W3; Lock 12 → A.W0; Lock 13 → A through J). Collapsing forfeits per-tranche ownership at MASTER-PLAN §21 ("Lock Ownership"). The 14-count is load-bearing for tranche routing, not cardinality bloat. |
| `restart/ARCHITECTURE.md:1430-1454` (§10.1 rewrite-budget) | 4 rewrite-budget categories | `legality-rewrites` (mandatory; saturate to fixpoint), `normalization-rewrites` (≤1024 steps), `cost-driven-rewrites` (≤256 e-class merges; per-grammar override), `simplification-rewrites` (single pass). | Each category has a distinct semantics (mandatory vs optional vs cost-gated vs post-extraction). The category split disambiguates *who must succeed* (legality) from *who may stop early* (cost-driven). | The user's V8 prompt asks "do all 4 categories load-bear, or is some category ceremony?". `legality-rewrites` and `normalization-rewrites` both saturate-to-fixpoint differing only in step budget; `simplification-rewrites` is structurally a single-pass micro-optimisation that could fold into the lowering pass that already does dead-code elision. | Steelman: collapse `simplification-rewrites` into `codegen::verify` directly (it's one pass, no fixpoint, no e-graph). Collapse `legality-rewrites` and `normalization-rewrites` budget contract into one with two sub-thresholds (mandatory-fixpoint, optional-step-budgeted). Net: 4 → 2 categories. | **CONSOLIDATE** — merge `simplification-rewrites` into `codegen::verify` (it has no e-graph need; one-pass dead-mark elision belongs alongside regen-equality at F.W3). Keep `legality-rewrites`, `normalization-rewrites`, `cost-driven-rewrites` as three categories — the legality/normalization split is load-bearing because legality *aborts* on failure while normalization *plateaus*. The 4 → 3 collapse drops one apparatus without loss. Receiver: ARCH §10.1 (drop `simplification-rewrites` row); F.W3 absorbs the single-pass simplification step. |
| `restart/ARCHITECTURE.md:1083-1113` (§7.5 Backend trait) | 5-method Backend trait | `lower`, `emit_runtime_template`, `emit_value_api`, `emit_visitor`, `emit_path_schema`. | V1 RustBackend implements all five; V2 WasmBackend + TsBackend will share the contract; the trait pre-existence is what makes V2 expansion mechanical. | V8 prompt asks "does the trait shape need 5 methods or do some land V2?". `emit_runtime_template`, `emit_value_api`, `emit_visitor`, `emit_path_schema` are four `emit_*` methods that produce four sibling artefacts under `runtime/src/grammars/<g>/`. They could fold into one `emit_artefacts(&self, schemas: &Schemas) -> Result<ArtefactSet, _>` method. The 5-method shape mirrors swc's per-target emit pipeline but bbnf's per-grammar artefact set is uniform across backends, not target-divergent. | Steelman: collapse `emit_value_api` + `emit_visitor` + `emit_path_schema` + `emit_runtime_template` into one `emit_artefacts(grammar, schemas) -> ArtefactSet` method. Net: 2-method trait (`lower` + `emit_artefacts`). The V2 backends' divergence is in the *body* of `emit_artefacts`, not in its signature shape. | **SIMPLIFY** — collapse the four `emit_*` methods into `emit_artefacts(grammar, schemas) -> ArtefactSet`. The four artefacts (template, value, visitor, path-schema) are co-emitted from the same grammar+schema input; the per-method signature is contrivance because no V1 or V2 caller emits one artefact without the others. The 5 → 2 collapse drops three method dispatches without architectural loss. Receiver: ARCH §7.5 (collapse method table; `ArtefactSet` typed file tree replaces the four output types). The four artefact files remain distinct on disk. |
| `restart/ARCHITECTURE.md:1264-1313` (§8.2 Type System) | 7-layer type-system stack | HM equality + Algorithm-W principal schemes + Pierce-Turner local check/synth + DK13 algorithmic completeness + finite first-order unification + finite CSP for non-HM choices + CHR-improvement (audit #1 §2.12, surfaced V1-FOLD #24). | Each layer is named with citation provenance. DK13 carries the user mandate ("inference stronger than Rust if possible"). CSP carries the host-overload + finite-choice + recogniser-eligibility decisions. | V8 prompt: "audit each for V1 user case". Algorithm-W and HM equality are the *same* algorithm (Damas-Milner 1982 = Algorithm-W); the prose names them as if they were separable. CHR-improvement is V1-FOLD #24 Tier 3 (medium-priority); user mandate did not demand it for V1. Pierce-Turner local check/synth is a presentation of bidirectional typing — folding into Pierce 2002 ch.22 is one citation, not three. | Steelman: collapse the stack to "HM + bidirectional + DK13 + CSP" (4 layers). HM subsumes "equality + Algorithm-W + first-order unification" (one algorithm). Bidirectional subsumes "Pierce-Turner local check/synth". CHR drops to Tier 3 (post-V1) per its V1-FOLD ranking. | **SIMPLIFY** — collapse the 7-layer prose to "HM (equality + Algorithm-W) + bidirectional (Pierce-Turner) + DK13 higher-rank + first-order unification + finite CSP" (five named mechanisms; one citation chain). Drop CHR-improvement from V1 type-system text (V1-FOLD Tier 3 #24 — defer to V2 amendment). The 7 → 5 collapse keeps every load-bearing distinct mechanism while retiring two presentations of the same idea. Receiver: ARCH §8.2 prose at lines 1283-1289; MASTER-PLAN C.W4 already cites CHR-improvement (line 321) — drop or route to V2. |
| `restart/ARCHITECTURE.md:1032-1063` (§7.4 diagnostic catalogue) | 30+ diagnostic codes | Catalogue enumerates `BBNF-LIFETIME-ESCAPE` through `BBNF-SEM040` with aliases. | Each code names a distinct producer site with verbatim diagnostic text. The catalogue binds CLI + LSP + cookbook to the same identifier set. | V8 prompt: "do all 28 codes name distinct user-facing diagnostics?". Several codes are aliases (`BBNF-LIFETIME-ESCAPE` aliases `BBNF-LIFE001`; `BBNF-VISITOR-MUTATION-OUTSIDE-ENTRY` aliases `BBNF-VISIT002`); the alias system doubles the cardinality without doubling the user surface. `BBNF-RECOVERY*` is a wildcard, not a single code. `BBNF-LOCAL-EQUALITY-ANNOTATION` is V2 surface (per ARCH §8.2:1293), not V1 user-visible. | Steelman: collapse the alias system. Each diagnostic carries one code (the human-readable name); the numeric alias retires entirely (LLM-trained-distribution artefact, not user-facing). Move `BBNF-LOCAL-EQUALITY-ANNOTATION` to a V2 amendment receiver. Net: ~22 V1 codes (down from 30+ with aliases). | **SIMPLIFY** — retire the numeric alias system (`BBNF-LIFE001`, `BBNF-VISIT002`, `BBNF-LAYOUT002`, `BBNF-OPT001`, `BBNF-OPT002`, `BBNF-PATH001`, `BBNF-PATH002`, `BBNF-GRAMMAR001`, `BBNF-CG001`); keep only the human-readable names. Move `BBNF-LOCAL-EQUALITY-ANNOTATION` to V2 carry row. The aliases are double-tracking — neither CLI nor LSP exposes both names; user docs cite the human-readable form universally. Receiver: ARCH §7.4 catalogue; MASTER-PLAN §25 cookbook codes already cite the human-readable form. |
| `restart/ARCHITECTURE.md:1163-1175` (§8.1 directive set) | 6 V1 directives | `@import`, `@host fn`, `@error`, `@layout`, `@pretty`, `@token`. | All six exist in the 30+ extant grammar sites (cf. `grammar/json/json.bbnf:18-20` for `@pretty`, `grammar/css/pretty.bbnf:17-19` for `@token`, `grammar/bbnf/bbnf.bbnf:4-5` for `@import`). The six-directive set is corpus-grounded. | V8 prompt: "are all 6 V1 mandatory or could some defer?". `@token` appears in only one grammar (`grammar/css/pretty.bbnf:17-19`) per ARCH §8.1:1224; `@layout` appears in multiple but is sometimes folded into `@pretty` strategy. The corpus baseline is a corpus snapshot, not a future-proof directive census. | Steelman: defer `@token` to V2 (one-grammar usage doesn't justify a V1 directive surface). Replace the single CSS-pretty `@token` site with a metadata-driven atomic-token marker in the workspace metadata block. | KEEP — `@token` corpus-grounding at one site is sufficient because it is a *load-bearing* one site (CSS pretty grammar's atomic-token discipline drives the CSS L4 `SimdScan` recogniser eligibility per ARCH §12.2:1567). Removing `@token` forces metadata-side encoding of the same information, which violates Lock 14's "two surfaces only" onboarding contract by adding a third metadata key per atomic-token marker. The directive form is the meta-grammar-correct surface; metadata-side encoding would be host-language-specific. The challenge fails on Lens K (meta-grammar discipline). |
| `restart/ARCHITECTURE.md:1639-1643` (§13.1 lint manifest) | 4 lint categories | `directive-canon`, `naming-canon`, `regex-engine-canon`, `per-grammar-fence-canon`. | Each lint binds to a distinct corpus-drift class with a distinct enforcement command. | V8 prompt: "load-bearing as architectural contract or aspirational?". The four categories are corpus-grounded — `naming-canon` enforces `pointer!` → `path!` rename; `regex-engine-canon` enforces `regex-automata` retirement; `directive-canon` enforces the six-directive set; `per-grammar-fence-canon` enforces Lock 14. Each is verifiable via `cargo xtask lint`. The contract is real, not aspirational. | Steelman: `naming-canon` and `directive-canon` are both *vocabulary* lints; merge them. `regex-engine-canon` is a *dependency* lint; merge with `per-grammar-fence-canon` (both are import-deny gates). Net: 2 lint categories. | KEEP — the four-way split load-bears because each category produces a distinct diagnostic code (`BBNF-DIRECTIVE-RETIRED`, `BBNF-NAMING-DRIFT`, `BBNF-REGEX-ENGINE-DRIFT`, `BBNF-GRAMMAR-NAME-IN-GENERIC-CRATE`). Collapsing forfeits per-category diagnostic provenance and forces conditional reporting. The four categories are the right cardinality for the corpus-drift surface; one less and a real drift class becomes invisible. |
| `restart/MASTER-PLAN.md:165-176` (§5 tranche set) | 10 tranches A-J | A workspace genesis through J publication close; F.W5 nine-grammar regen; G future-grammar gate. | Each tranche has a distinct close gate + carry FROM/TO row. Tranche letters track A-J without slot drift (Lock 12 retired BA-BD). | V8 prompt: "could some merge?". Tranche E (Backend IR + VM) and F (Rust Lowerer + Runtime Template) are sequenced E-then-F because BIR substrate must exist before lowerers walk it. Tranche H (Pratt + SIMD) feeds C.W3 RecognizerFacts; the H tranche could fold into a recognizer-finalisation wave inside the F-G timeline. Tranche I (Recovery + Incremental + LSP) is three concerns in one tranche — could split. | Steelman: merge E into F (BIR + Rust lowerer in one tranche; six waves). Merge H.W0/H.W1 (Pratt + SIMD recogniser facts) into C.W3; H reduces to two close waves (H.W3 SOTA JSON, H.W4 SOTA CSS). Net: 8 tranches A-J → A, B, C, D, F (absorbed E), G, H (slim), I, J. | KEEP — each tranche has a distinct *close gate* and *consumer routing row* in MASTER-PLAN §5.1. E and F separate because PASS-2's BIR contract (§7.2:894-984) is the *boundary* between substrate and lowering; merging forfeits the boundary tests at E.W4 ("codegen cannot import Grammar IR emitter logic"). H carries SOTA close gates that are not Pratt/SIMD-development-coupled (H.W3/H.W4 measure the Rust line on the J.W1 fixture set). The 10-tranche cardinality is dependency-graph-load-bearing, not ceremony. |
| `restart/research/V1-FOLD-CANDIDATES.md:43-98` (30-fold ledger) | 30 fold candidates absorbed | Tier 1 (10 architecture-nailing) + Tier 2 (10 surface coherence) + Tier 3 (5 sibling hygiene) + Tier 4 (5 architectural prerequisites). | Each fold cites audit + scope + LOC estimate. The ledger drives Phase 7.1 + 7.2 + 7.5 amendment cascade. | V8 prompt: "does each item load-bear, or could some defer V2?". Tier 3 #24 (CHR-improvement layer) is "200-400 LOC" with no V1 user-mandate driver and is currently surfaced at MASTER-PLAN C.W4:321 alongside the rewrite-budget (a V8-prompt-flagged over-promise per V7 P5). Tier 2 #14 `@token` directive cardinality concern flagged above (defer challenge). Tier 3 #25 (function composition library + partial application via closure-wrap) is a *library*, not architecture — MASTER-PLAN does not need to commit V1 to library shapes. | Steelman: defer Tier 3 #24 (CHR), Tier 3 #25 (function composition library), and re-route to V2 carry rows. Net: 30 → 28 V1 folds. | **ASPIRATIONAL** for Tier 3 #24 and Tier 3 #25 — both defer to V2 carry without architectural cost. CHR-improvement is V1-FOLD's own §3 Tier 3 ranking; library function-composition is library work, not architectural. Receiver: MASTER-PLAN §24 carry-ledger gets two new V2-amendment rows. |
| `restart/MASTER-PLAN.md:182-196` (§5.1 calendar matrix) | Calendar dependency-order matrix | Carry FROM / Carry TO / Layer-ownership columns. | Captures inter-tranche dependency edges; ten-row × four-column dependency table. | The matrix re-states what `restart/ARCHITECTURE.md:84-171` Dependency DAG already encodes graphically. | Steelman: collapse §5.1 to a sentence-pointer to ARCH §2 DAG; remove the matrix. | KEEP — the calendar matrix carries *tranche-level* sequencing (which tranche's *close gate* feeds which downstream tranche), which the ARCH §2 *crate-level* DAG does not. The two are non-redundant. |
| `restart/ARCHITECTURE.md:1467-1473` (§11 perf gate owners) | 5 gate-owner rows | JSON twitter/citm/canada; CSS bootstrap/animate; simdjson-class throughput; LOC budget; OpenFrame absence. | Each gate has a named owner crate set. | The five rows duplicate parts of MASTER-PLAN §4 Hard Architectural Gates; the SOTA-row metadata is in MASTER-PLAN §4 too. | Steelman: collapse §11 owners table; reference MASTER-PLAN §4 gate-rows. | KEEP — the §11 owners table identifies *crate ownership*, not gate definition; MASTER-PLAN §4 identifies *tranche ownership*. The two are non-redundant; collapsing forfeits crate-level routing for runtime-perf carry. |
| `restart/ARCHITECTURE.md:1083-1113` (§7.5 Backend trait `LowerContext`) | `LowerContext` carries 6 side-table refs | `LayoutFacts`, `ShapeFacts`, `RecognizerFacts`, `CostFacts`, `RecoveryFacts`, `BridgeJustification`. | Every side table is *pre-computed* before lowering; the context is the immutable read-only handle. | All six side tables are already enumerated in §7.3 with producer + consumer + visibility. The `LowerContext` re-enumerates them. | Steelman: replace the `LowerContext` field-list prose at §7.5:1138 with "all public side tables per §7.3". | **SIMPLIFY** — replace the six-table enumeration in `LowerContext` prose at §7.5:1135-1141 with a `&SideTables` reference type that carries the §7.3 set as one struct. The trait surface stays clean; readers route to §7.3 once for the side-table list. Receiver: ARCH §7.5 prose. |

Lens I tally: 13 rows. KEEP 7 (54%); SIMPLIFY 4 (31%); CONSOLIDATE 1 (8%);
ASPIRATIONAL 1 (8%). The KEEP fraction is in the healthy 60-80% target zone
(slightly under because V8 is the first explicit simplification audit). No
DISCARD verdicts — none of the audited apparatus is contrivance to remove
outright; the simplifications are local re-shapes.

## §3 Lens J — Host-language Leverage (Per-Item Table)

bbnf targets Rust V1 (and WASM + TS deferred V2). Lens J asks: where does the
trio reinvent what the host language provides cleanly, and where does the
cross-host story diverge?

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/ARCHITECTURE.md:194-216` (§3.1 `bbnf` API) | `parse` / `parse_in` / `parse_owned` lifetime trio | Three entrypoints: `parse(&'i [u8])` slice-borrow primary; `parse_in(input, arena)` bumpalo-bound; `parse_owned(Vec<u8>)` self-contained. | The three are surfaces over one parse implementation; the lifetime parameter is the discriminant; sonic-rs + lightning-css + serde-json patterns line up with each. | The three-entrypoint API is *Rust-specific*. WASM has linear memory (no equivalent of bumpalo arenas; `parse_in` doesn't translate). TS has GC (no lifetime parameter; all three collapse to one entrypoint). The cross-host story diverges. | Steelman: V1 ships only `parse` + `parse_owned` (slice-borrow + owned); `parse_in` defers to V2 alongside the WASM/TS surface where arena semantics need cross-host adjudication anyway. Net: 3 → 2 V1 entrypoints. | KEEP — `parse_in` is corpus-driven (sonic-rs's `parse_in` is the mature Rust pattern; the V1 user mandate "audacious + SOTA + Rust-like ergonomics" ranks Rust ergonomics first). The cross-host divergence is *expected*; the V2 `WasmBackend` will erase `parse_in` and `parse` will widen to take a wasm-allocator handle. The three-entrypoint shape is host-leveraged, not host-leveraged-poorly. The V2 deferral note at MASTER-PLAN §24 should explicitly cite the cross-host API divergence as a V2 design point. |
| `restart/ARCHITECTURE.md:1083-1113` (§7.5 `Backend` trait) | `Backend: Backend` impl-list (V1 RustBackend; V2 WasmBackend + TsBackend) | Trait surface enforces Lock 5 per-backend-lower contract. V1 has one impl; V2 adds two more. | The trait's V2 expansion is mechanical because every method maps to a per-host artefact set. | **The trait body's "Output" type is Rust-typed (`RustSource`, `WasmRustSource`, `TsSource`). For TS, the artefact tree is `.ts` files generated *from Rust code*; Rust generates TS instead of TS being host-native. This couples cross-host backend impls to Rust-the-codegen-host.** The cross-host story is "Rust generates everything" not "each host owns its codegen". | Steelman: the `TsBackend: Backend` impl is itself written in Rust and emits TS — this is correct; the trait is host-language-agnostic about *target*, but Rust-language-specific about *codegen-host*. This is a deliberate choice; the V2 TS-native parse+runtime fork (per audit #5 + V1-FOLD #9) is a *separate* fork, not the `TsBackend` work. | KEEP — the Rust-as-codegen-host is correct and distinct from target-language. The V2 deferred TS-native parse+runtime is principled (V1-FOLD §3 #9). The trait shape leverages Rust's monomorphisation + trait-dispatch for V2 expansion; this is the right host-leverage. The cross-host concern (TS-native parse runtime) is V2-amendment-bound. |
| `restart/ARCHITECTURE.md:622-665` (§5 Cargo.toml workspace metadata) | `[workspace.metadata.bbnf.grammars.<name>]` schema | Lock 14 onboarding surface #2: workspace metadata stanza per grammar. | Cargo's `[workspace.metadata.<vendor>]` is an idiomatic Rust mechanism; cargo-metadata can read the block without parsing it as Cargo grammar. | V8 prompt: "Cargo.toml workspace.metadata is Rust-specific (workspace.metadata); WASM has no Cargo, TS has package.json. Cross-host story?". The two-surface onboarding contract (`.bbnf` source + workspace metadata) is *Rust-specific* via Cargo.toml. For TS-native parse+runtime (V2), the equivalent would be `package.json` `bbnf` field; for WASM-native, no analog. | Steelman: factor the metadata schema into a *language-neutral* TOML/YAML/JSON file (`bbnf.toml`?) that lives alongside the workspace, not inside Cargo.toml. Cargo loads it as `[workspace.metadata.bbnf]` references; package.json loads it as `bbnf:` field; WASM treats it as a sidecar. | **HYBRID** — keep `[workspace.metadata.bbnf]` as the *Rust-line* surface (V1 user reads + writes Cargo.toml; corpus-grounded); add a one-line note at ARCH §5 that V2 cross-host onboarding promotes the schema to a language-neutral sidecar (V1-FOLD §3 V2 amendment row). The metadata schema *content* is host-agnostic; the *carrier* is host-specific. The V1 schema-content is what locks in; the V2 carrier flexibility falls out. Receiver: ARCH §5 + MASTER-PLAN §24 carry-ledger (cross-host-onboarding row). |
| `restart/MASTER-PLAN.md:111` (§4 hard gate `cargo xtask`) | `cargo xtask regen` family | Build/test infra: `cargo xtask bbnf check`, `cargo xtask bbnf build`, `cargo xtask lint-tree`, `cargo xtask lint-loc`, `cargo xtask publish --dry-run`. | xtask is the canonical Rust workspace build-tool pattern (sonic-rs uses it; ripgrep uses it; cargo-make precedent). | xtask is *Rust-specific*. WASM has wasm-pack; TS has npm scripts. The cross-host build-tool story diverges entirely. | Steelman: factor the build-tool surface into a generic CLI binary (`bbnf-cli`) and re-route xtask invocations to it. The CLI is Rust-the-binary but is invokable from Cargo, npm, wasm-pack with different driver scripts. | KEEP — `cargo xtask` is the V1-Rust-line build tool; `bbnf-cli` is the user CLI. They are distinct surfaces (xtask is the workspace developer tool; bbnf-cli is the library user's tool). The V2 cross-host build story is V2-amendment-bound; V1 leverages cargo+xtask without forcing host-genericity. The challenge fails because xtask is workspace-internal, not user-facing. |
| `restart/ARCHITECTURE.md:62-63` + MIGRATION:71 (path crates) | `path-core` Rust + `path-ts` deferred V2 | `path-core` carries the typed path AST + parser + evaluator; `path` adds the Rust `path!` macro; `path-ts` (V2) adds the TS API. | `path-core` is the deduplication mechanism — Rust + TS share semantics through one impl. | `path-core` is *Rust-only*. The V2 TS-native parse+runtime fork (V1-FOLD §3 #9) re-implements the path semantics in TS, not as a re-export of `path-core`. The cross-host story is "Rust owns the canonical path implementation; TS may diverge". | Steelman: V2 `path-ts` should be a TS-native re-implementation, not a Rust-generated TS package. The V1 Rust `path-core` should be marked *V1-only* with a V2 amendment row that opens the V2 TS-native fork. | **HYBRID** — V1 `path-core` is the V1-Rust-line truth; V2 TS-native parse+runtime fork (per V1-FOLD §3 #9 + audit #5) is the *parallel* TS-native implementation, not a re-export. The two surfaces share an *AST schema* (TOML sidecar at `<g>.path-schema.toml`), not a Rust impl. MASTER-PLAN §24 carries this V2 row at `path-ts schema`. The host-leverage is correct: each host owns its native impl; the schema is the cross-host bridge. |
| `restart/ARCHITECTURE.md:1083-1113` (§7.5 `Backend::Output` typed source artefact) | `Backend::Output = RustSource / WasmRustSource / TsSource` | Lock 6 requires committed source generation; the trait outputs typed file trees, not raw bytes. | Each backend's output type is the *target language's* source representation. | The output types are *target-language-typed* (RustSource carries `.rs` file metadata; TsSource carries `.ts` module metadata). The V1 RustSource is the only V1-active impl; the V2 types are speculative. | Steelman: V1 trait carries `Output = RustSource` only (associated type concrete in V1; the V2 expansion is via blanket impl proliferation). The trait does *not* need to be generic-over-Output in V1. | KEEP — the associated-type-Output is the host-leverage on Rust's trait-dispatch; without `type Output`, V2 cannot add `WasmBackend` without trait-redesign. The associated type is exactly the V1-V2 separation surface. The challenge fails because the trait *V2 expansion* requires the associated type to land in V1; the V1 impl uses a concrete `RustSource`, but the *trait shape* needs the abstract `Output`. |
| `restart/MASTER-PLAN.md:131-141` (§4 SOTA gates) | M1 Pro Rust-line SOTA gates | twitter ≤380μs; canada ≤2.8ms; citm ≤750μs; bootstrap ≤3.0ms; animate ≤1.6ms; structural-scan ≥5GB/s NEON / ≥7GB/s AVX2. | Lock 8's SOTA-anchor mandate; corpus-grounded against sonic-rs / simd-json / lightning-css. | All six gates are *Rust runtime* gates. WASM SOTA defers post-V1; the V2 WasmBackend SOTA programme is a separate gate set. The V1 SOTA gates are host-leveraged through `target-cpu=native` Rust release. | Steelman: V2 WASM SOTA gates need their own SOTA corpus row (wasm-pack runtime; v8 / spidermonkey baseline; different competitor set). V1 should explicitly note that the SOTA fixtures + competitors are *V1-Rust-line-bound*. | KEEP — the V1 SOTA gates are Rust-line per Lock 8 amendment. The V2 cross-host SOTA story is bound at MASTER-PLAN §24 carry row "WASM ABI" (line 788). The challenge succeeds in spirit (V1 rows should explicitly cite "Rust line only") and fails in surgery (the cite already exists at MASTER-PLAN §4:138-141 and Lock 8 amendment text). |
| `restart/ARCHITECTURE.md:1264-1313` (§8.2 type system) | HM + bidirectional + DK13 + first-order unification + finite CSP type system | Five (or seven; per Lens I row 5) layers of type machinery. | The user mandate ("inference stronger than Rust if possible") is *deliberately* stronger than the host's type system. DK13 admits programs Rust requires programmer-written annotation for. | The bbnf type system is *parallel* to Rust's, not on top of it. Generated code typechecks again at Rust compile time; the same predicates run twice (once at codegen, once at rustc). | Steelman: the bbnf type system *generates* well-typed Rust; rustc's typechecker is the *verifier*. The two-level structure is intentional: bbnf admits more programs (DK13) than Rust would write, but the generated Rust is always Rust-well-typed. | KEEP — the bbnf type system is meta-grammar correctness (Lens K verdict in §4); rustc is the host's *post-codegen* verifier. The two are non-redundant: bbnf operates at grammar-source time; rustc operates at generated-Rust time. The check at codegen catches grammar errors; the check at rustc catches generator regressions (Lock 6 regen-equality + rustc together). The host-leverage is correct: rustc is the post-emit verifier of last resort. |
| `restart/ARCHITECTURE.md:1473` (`OpenFrame` clone absence gate) | OpenFrame clone-free perf gate | Owner: `runtime`, `codegen`, `bbnf-bench`. Lock 1 substrate gate. | OpenFrame's clone pathology (the 86.07% samply pathology cited at Lock 1 amendment text) is the historical failure mode. | The gate is *Rust-specific* (`OpenFrame` is the legacy Rust struct name; the substrate gate is ".clone() chains absent"). For WASM/TS, the analog is "no parallel-substrate value-tree clone"; the rename is needed for cross-host clarity. | Steelman: rename the gate to "no-parallel-substrate-clone" + drop the OpenFrame name from the perf-gate row. | LEVERAGE — leverage Rust's borrow checker as the host's no-clone-stack enforcement. The OpenFrame name is archaeological (from the prior 2,000-commit failure). Rename to "parallel-substrate-clone-absent" + cite "Rust borrow checker enforces" at the gate. The rename clarifies cross-host reading. Receiver: ARCH §11 perf-gate owners table at line 1473; MIGRATION §19.4 grep gate at line 742 (`OpenFrame`). |
| `restart/ARCHITECTURE.md:1603-1628` + MASTER-PLAN:117 (Lock 13 tree shape) | 4-10 children + 500 LOC ceiling | Per-directory child-count + per-file LOC discipline. | Sonic-rs / lightning-css / simdjson all converge on this discipline. | The 4-10 + 500 LOC numbers are *Rust convention* (sonic-rs `src/parser.rs` is ~2400 LOC; lightning-css follows similar discipline). The numbers are corpus-grounded but Rust-specific. | Steelman: the Lock 13 numerics defer to V2 cross-host adjustment when the WASM/TS line lands. | KEEP — Lock 13 numerics are V1 Rust line; the V2 WASM/TS lines will have their own discipline. The lock text should cite "V1 Rust line" but already does at line 1603 ("every handwritten crate source tree"). The challenge fails because the discipline is V1-bound. |
| `restart/ARCHITECTURE.md:1310-1313` (§8.2 schema-mining miner) | Telemetry-driven schema inference | The user mandate — "type algebra + telemetry to generate semantic schemas without explicit annotations in most cases". | Cited at PASS-1 §6 + ARCH §8.2 + MASTER-PLAN C.W2:319. | The miner is *bbnf-internal* type inference; it does not leverage any host facility. It generates `LayoutFacts` + `ShapeFacts` from corpus telemetry. The host has no equivalent. | Steelman: the host's *pattern-matching* + *type-inference* facilities (Rust's match exhaustiveness, TS's type-narrowing, WASM's nothing) could fold into the miner's reject-criterion (reject candidates that produce host-non-typecheckable output). | KEEP — the miner is grammar-source-time inference; host facilities are post-emit. The host is the verifier-of-last-resort (per Lens J row 8 challenge); the miner generates well-typed grammar facts that the host's typechecker accepts. The two are sequenced, not redundant. |

Lens J tally: 11 rows. KEEP 7 (64%); HYBRID 2 (18%); LEVERAGE 1 (9%);
SIMPLIFY 1 (9%). The KEEP fraction is healthy. The two HYBRID verdicts cite
explicit cross-host divergence rows that V1 leverage correctly (host-line is
canonical V1; cross-host is V2-amendment-bound). The single LEVERAGE row is a
naming cleanup. No DISCARD.

## §4 Lens K — Meta-grammar Discipline (Per-Item Table)

bbnf is a meta-grammar that generates parsers for extant target languages.
Lens K asks: which architectural facilities are load-bearing for V1 meta-grammar
correctness, which are aspirational for Lock 8 SOTA throughput, and which are
speculative beyond the meta-grammar mandate?

| Site (path:line) | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| `restart/MASTER-PLAN.md:131-141` (§4 SOTA gates) | M1 Pro SOTA gates (Lock 8) | twitter / canada / citm / bootstrap / animate; ≥5/7 GB/s structural-scan. | Corpus-grounded against sonic-rs / simd-json / lightning-css. The user mandate's "audacious + SOTA" lines up with these targets. | The SOTA gates are *throughput-bound*, not meta-grammar-correctness-bound. A correct bbnf-generated parser need not beat sonic-rs to demonstrate meta-grammar correctness; it needs to be *correct* (parity matrix passes). | Steelman: classify SOTA gates as ASPIRATIONAL — V1 ships at SOTA-parity (within 10% of competitor) at H.W3/H.W4 + J.W1; SOTA-beat is a V2 amendment receiver. The user mandate "audacious + SOTA" admits SOTA-parity at V1 + SOTA-beat at V2. | **ASPIRATIONAL** — the SOTA gates are V1 surface (the close gate at J.W1 is V1) but tranche-deferrable for body. H.W3/H.W4 carry *early* SOTA gates with looser thresholds (twitter ≤480μs vs J.W1 ≤380μs); these are recogniser-development gates, not meta-grammar-correctness gates. The V1 SOTA-beat at J.W1 is the user's "audacious" anchor; if H.W3/H.W4 land at SOTA-parity but not SOTA-beat, the V1 close gate amends to record the parity-not-beat outcome and routes the SOTA-beat work to a V1.1 follow-up amendment. The current MASTER-PLAN §4 thresholds are correctly load-bearing as targets; the *correctness* of meta-grammar is independent. Receiver: MASTER-PLAN §4 should add a single-sentence cite "SOTA-beat is the audacious target; SOTA-parity is the meta-grammar correctness floor". |
| `restart/ARCHITECTURE.md:1430-1454` (§10.1 rewrite-budget) | 4 rewrite-budget categories + per-category thresholds | Each category has a step/merge ceiling. | Each category corresponds to a distinct optimization concern. | V8 prompt: "load-bearing for meta-grammar correctness, or aspirational for SOTA throughput?". `legality-rewrites` is correctness (mandatory canonicalisation; aborts on cycle). `normalization-rewrites` is correctness-adjacent (lower-time choice simplification; halts on plateau). `cost-driven-rewrites` is *throughput-bound* (Pratt vs scalar; SIMD vs scalar). `simplification-rewrites` is throughput-adjacent (post-extraction local simplifications). | Steelman: classify legality + normalization as LOAD-BEARING (V1 meta-grammar correctness); classify cost-driven + simplification as ASPIRATIONAL (V1 surface; tranche-deferrable for body). | **LOAD-BEARING** for legality + normalization (V1 meta-grammar correctness; cannot defer); **ASPIRATIONAL** for cost-driven + simplification (V1 surface; H tranche-deferrable for body). The Lens I CONSOLIDATE verdict (collapse simplification into codegen::verify) compounds with this Lens K verdict (cost-driven defers to H tranche). Net: 4 → 3 V1 categories with explicit aspirational/load-bearing labelling. Receiver: ARCH §10.1 prose. |
| `restart/ARCHITECTURE.md:1264-1313` (§8.2 DK13 higher-rank) | DK13 algorithmic completeness | The user mandate "inference stronger than Rust if possible" — DK13 admits programs Rust requires explicit annotation for. | DK13 is V1-FOLD §3 Tier 1 #2; folded V1 per Phase 7.1. | V8 prompt: "DK13 is V1 user mandate (fold) but is it load-bearing for V1 grammar correctness, or aspirational for 'stronger than Rust'?". DK13 admits the transducer-without-directive (V1-FOLD §1 audit #2 §4); without DK13 the `@host fn map<I, O>(f: fn(I) -> O, xs: [I]) -> [O]` requires hand-written rank-1 plumbing. | Steelman: DK13 is *aspirational* for V1 (the rank-1 bbnf surface admits all extant grammars without DK13; DK13 is what makes the user mandate's "stronger than Rust" land at V1 instead of deferring); but it is *load-bearing* for the transducer apotheosis worked example. | **LOAD-BEARING** — DK13 is the user-mandate-driven fold; the transducer-without-directive worked example concretely depends on DK13. Without DK13 the meta-grammar needs a rank-1 + `@transducer` directive (which V1-FOLD §3 retired). The V1 surface admits DK13's annotation-elidable polymorphism; this is what lets `@host fn map` typecheck without explicit `forall`. The challenge's "aspirational" reading fails because DK13 lands at V1 by user mandate; the lens question is whether DK13 *would* fold without the mandate, and the answer is "yes, but later". V1 fold is correct. |
| `restart/ARCHITECTURE.md:1310-1313` (§8.2 schema-mining miner) | Telemetry-driven schema inference | "Type algebra + telemetry to generate semantic schemas without explicit annotations in most cases". | User-mandate-driven; absorbs into V1 type system per V1-FOLD §3 Tier 1 #7. | V8 prompt: "load-bearing for V1 yaml grammar onboarding, or aspirational?". The yaml onboarding probe has 0 LOC at A.W4 and ≤4,000 LOC provisional ceiling at F.W5; the schema miner is what generates yaml's `LayoutFacts` + `ShapeFacts` from corpus telemetry without yaml-author annotations. Without the miner, yaml onboarding requires hand-written shape facts, which violates Lock 14 two-surface contract. | Steelman: the schema miner is *load-bearing for Lock 14 yaml onboarding correctness* (V1 fold lands the miner at C.W2 per MASTER-PLAN:319). The miner is meta-grammar correctness, not aspirational throughput. | **LOAD-BEARING** — the schema miner is the *correctness* mechanism for Lock 14 two-surface onboarding (without it, yaml onboarding silently requires a third surface for shape facts, breaking Lock 14). The "telemetry-driven" prefix is the user-mandate-driven cite, not aspirational throughput. C.W2 fold is correct. |
| `restart/ARCHITECTURE.md:1083-1113` (§7.5 Backend trait) | Backend trait + V1 RustBackend + V2 WasmBackend / TsBackend | The trait is V1; the impls are V1 (Rust) + V2 (Wasm + Ts). | The trait pre-existence makes V2 mechanical. | V8 prompt: "Verify each [method] is load-bearing across the V2 deferral". The five methods (lower + four emit_*) all have V1 RustBackend impl + V2 WasmBackend impl + V2 TsBackend impl rows in §7.5:1118-1124 obligations table. Per Lens I row 4, the four `emit_*` methods can collapse into one `emit_artefacts`; this is contrivance, not meta-grammar. The 2-method shape would still load-bear across V2. | Steelman: the trait is *load-bearing* for V1 + V2 (Lock 5 boundary); the *5-method* shape is contrivance per Lens I. The 2-method collapse loses no meta-grammar content. | **LOAD-BEARING** at the trait level; **SIMPLIFY** at the method level (per Lens I row 4). Both verdicts compose: keep the trait as architectural; collapse the methods. |
| `restart/ARCHITECTURE.md:1430-1454` (§10.1 + cost-model trait) | Cost-model trait shared with regex (V1-FOLD §3 implicit) | The cost model decides Pratt vs scalar; SIMD vs scalar; dispatch-tree vs jump-table. | The same cost-model API can serve regex engine selection (NFA vs lazy DFA vs full DFA vs VM per `parse-that-regex` cross-engine parity at MASTER-PLAN H.W1:485). | V8 prompt: "load-bearing or aspirational unity?". The cost model is *one* trait surface used by two consumers: BBNF passes (Pratt/SIMD selection) and `parse-that-regex` (engine selection). The unity is V1-FOLD § implicit (the regex-engine canon at ARCH §13.1 binds `parse-that-regex` cross-engine parity to the same cost-model contract). | Steelman: the cost-model unity is *load-bearing* for the parse-that-regex / BBNF parity story (both must use the same cost vocabulary to compose). | **LOAD-BEARING** — the cost-model trait unity is what lets parse-that-regex's engine selection compose with BBNF passes' recogniser selection. The unity is meta-grammar-correctness because regex emission is part of meta-grammar codegen. C.W5 + H.W1 fold the unity. |
| `restart/MASTER-PLAN.md:213-229` (§5.3 yaml A→F→J trajectory) | yaml grammar trajectory | Two-surface onboarding proof; A.W4 metadata acceptance; F.W5 generated runtime; G.W4 future-grammar gate; J.W2 docs cite. | Lock 14 onboarding test concretely walked through ten tranche rows. | V8 prompt: "Schema-mining miner — load-bearing for V1 yaml grammar onboarding". yaml has zero existing fixture corpus; the schema miner generates yaml `LayoutFacts` + `ShapeFacts` from any provided telemetry input. Without the miner, yaml requires hand-shape-fact authoring (third surface). | Steelman: yaml trajectory is the *correctness* gate for Lock 14 + the schema miner. | **LOAD-BEARING** — yaml trajectory is V1-mandatory for Lock 14 closure. C.W2 schema miner + G.W4 future-grammar gate compose into the proof. The §5.3 trajectory is correct. |
| `restart/ARCHITECTURE.md:1359-1409` + MASTER-PLAN B tranche (runtime arch) | Runtime architecture: tape + direct-to-struct + visitors + paths + format() | V1 user-runtime conveniences. | Visitors / paths / format are V1 user-API; per V1-FOLD §3 +1 cookbook row at §25:810. | V8 prompt: "where some are V1-mandatory but could land V2 without architectural cost". Visitors are V1 (read-write mutation through visitor-only is Lock 1 + PASS-3 contract). Paths are V1 (Lock 7). format() is V1 (cookbook row + V1-FOLD #4 escalation). DAP/breakpoints (V1-FOLD §3 #16 `@debug` host primitive) are V1 substrate; full DAP integration is V2. The boundary at PASS-3 §6.4 keeps V1 to "host primitive only". | Steelman: visitor + path + format are V1; DAP integration defers V2; the boundary holds. | **LOAD-BEARING** for visitor + path + format (V1 user-API correctness); **SPECULATIVE** for full DAP integration (V2). The boundary holds at MASTER-PLAN I.W3 ("Debug/replay and playground hooks"; line 526) which carries the V1 hooks; the full DAP integration is the V2 surface. |
| `restart/ARCHITECTURE.md:1267-1278` (§8.2 type system) | Bounded coercion obligations | Numeric widening, lifetime-owned escalation, generated-record shape narrowing, host-improvement rules. | The bounded coercion table at §8.2:1270 is concrete. | V8 prompt: "Optimization complexity — flag where the full apparatus is required only for the SOTA-throughput aspiration vs meta-grammar correctness". Bounded coercion is *correctness* (without it, a chain step's input doesn't unify with the previous step's output without unsafe escalation). | Steelman: bounded coercion is V1 load-bearing for chain composition correctness, independent of SOTA. | **LOAD-BEARING** — V1 chain composition correctness (BBNF-SUBSUMPTION-EDGE diagnostic at ARCH §7.4:1055). |
| `restart/ARCHITECTURE.md:1430-1454` (§10.1) + `restart/locks/LOCKS.md:40` (Lock 4) | CSP + e-graph + cost model + miners orthogonal composition | Lock 4 forbids fused hypergraph; bridges compose by output piping. | Each crate has independent stabilisation gate; egraph publishes when its API stabilises; csp-solver publishes when its API stabilises. | V8 prompt: "where the full apparatus is required only for the SOTA-throughput aspiration (Lock 8) rather than meta-grammar correctness". CSP is V1 (host-overload + finite-choice + recogniser-eligibility — meta-grammar correctness). egraph is V1 (legality + normalization rewrites — meta-grammar correctness). Cost model is V1 *partial* (legality + normalization don't need cost-model; cost-driven rewrites do — aspirational throughput). Miners are V1 (Pratt/SIMD detection — auto-detection per Lock 10 — correctness, not throughput, because Lock 10 forbids `@pratt`/`@simd` directives). | Steelman: classify CSP + egraph + miners as LOAD-BEARING; classify cost-driven rewrites + cost-model frontier extraction as ASPIRATIONAL throughput. | **LOAD-BEARING** for CSP + egraph + miners (V1 meta-grammar correctness); **ASPIRATIONAL** for cost-driven rewrites + cost-model Pareto frontier (V1 surface; H tranche body deferrable). The cost model's *legality-vs-cost* discipline is meta-grammar; the *Pareto frontier* extraction is throughput-aspirational. C.W5 carries the V1 fold; H carries the throughput-body. |
| `restart/MASTER-PLAN.md:557` (J.W3 publication gate) | Publication readiness + 2-tranche stability gate | Stable surface (`bbnf`, `bbnf-cli`, etc.) publishes unconditionally; sister crates publish after 2-tranche stability. | Lock 11 amendment + V1-FOLD §3 #30. | The 2-tranche stability gate is *publication-readiness*, not meta-grammar correctness. A correctness V1 close need not publish; a publication V1 close requires the gate. | Steelman: the publication gate is *aspirational for V1.0 release* — V1 meta-grammar correctness lands at J.W1 (parity matrix + SOTA gate); V1.0 release lands at J.W3 (publication dry-run). | **ASPIRATIONAL** — V1 surface; J.W3 close gate. The gate is V1.0-release-bound, not meta-grammar-correctness-bound. |
| `restart/ARCHITECTURE.md:1083-1113` (§7.5 trait + V2 backends) | V2 WasmBackend + TsBackend deferred | The V2 column rows in §7.5:1118-1124 obligations table. | The V2 expansion is mechanical via the trait. | V8 prompt: "5 methods cover all three. Verify each is load-bearing across the V2 deferral". The V2 columns in the obligations table at §7.5:1118-1124 carry concrete output types per backend per method. The V2 deferral is principled (V1-FOLD §3 #9 + §6 lock amendments). | Steelman: the V2 deferral binds the cross-host work; V1 V1-only Rust line. | **SPECULATIVE** for V2 expansion content; **LOAD-BEARING** for V1 trait shape (per §3 Lens J row 2). The V2 deferral is V2-amendment-bound; V1 trait shape is V1-load-bearing. |

Lens K tally: 12 rows. KEEP / LOAD-BEARING 8 (67%); ASPIRATIONAL 3 (25%);
SPECULATIVE 1 (8%). The LOAD-BEARING fraction matches the expected meta-grammar
core; the ASPIRATIONAL fractions cite SOTA / publication / cost-driven body
work that is V1-surface but tranche-deferrable for content. SPECULATIVE is
purely V2 expansion. No DISCARD.

## §5 Compressed 9-Lane Verification

V8 reuses the standard 9-lane grid alongside the new lenses. ≥15 audit rows
covering full lane coverage; trio includes Lane 2 sequencing.

| # | Lane | Surface | Verification | V8 Status |
|---:|---|---|---|---|
| 1 | Lane 1 — Lock-Adherence | Lock 1 + Lock 9 (substrate + API surface) | Distinct invariants per §3 Lens I row 1; tape identity at ARCH §9.1 + parse-API trio at §3.1; KEEP. | honored |
| 2 | Lane 1 — Lock-Adherence | Lock 4 (CSP / egraph / miners orthogonal) | Per-domain composition by output piping; no fused hypergraph; ARCH §10.1 + Lock 6 amendment text. | honored |
| 3 | Lane 1 — Lock-Adherence | Lock 5 + ARCH §7.5 (Backend trait) | Trait shape load-bearing; method count is Lens I CONSOLIDATE candidate. | honored-with-amendment-on-method-count |
| 4 | Lane 1 — Lock-Adherence | Lock 8 (SOTA gates) | M1 Pro Rust-line; H.W3 + H.W4 + J.W1 close gates; Lens K classifies as ASPIRATIONAL for body, V1 surface load-bearing. | honored |
| 5 | Lane 1 — Lock-Adherence | Lock 10 (auto-detect Pratt/SIMD + 6-directive set) | `@pratt`/`@simd` retired; six directives ratified at ARCH §8.1. | honored |
| 6 | Lane 1 — Lock-Adherence | Lock 11 (path-dep incubation + J.W3 split) | Stable surface vs incubation-cleared sister crates; 2-tranche stability gate. | honored |
| 7 | Lane 1 — Lock-Adherence | Lock 12 (archive ceremony) | A.W0 archives `ser` + `gorgeous`; V7.1 closure of `bbnf-path-ts` over-broadening. | honored |
| 8 | Lane 1 — Lock-Adherence | Lock 13 (tree shape) | 4-10 children + 500 LOC handwritten ceiling; Lens J KEEP. | honored |
| 9 | Lane 1 — Lock-Adherence | Lock 14 (grammar generalisation) | Two-surface onboarding; yaml trajectory at MASTER-PLAN §5.3; ARCH §12.1 walkthrough. | honored |
| 10 | Lane 2 — Sequencing | A → B → C → D → E → F → G → H → I → J | Each tranche has same-wave or next-wave consumer; Era V failure mode avoided. | honored |
| 11 | Lane 2 — Sequencing | C.W2 schema miner → G.W4 yaml gate | Schema miner is V1 yaml-onboarding load-bearing per §4 Lens K row 4. | honored |
| 12 | Lane 2 — Sequencing | C.W3 RecognizerFacts → H.W0/H.W1 Pratt/SIMD | Pratt + SIMD recogniser facts produced at C.W3, consumed at H.W0 + H.W1. | honored |
| 13 | Lane 2 — Sequencing | E.W0-E.W4 BIR → F.W0-F.W5 Rust lowerer | BIR substrate before lowerers; PASS-2 boundary at E.W4. | honored |
| 14 | Lane 2 — Sequencing | F.W3 regen-equality → J.W3 publication | Regen-equality at F.W3 is publication's prerequisite. | honored |
| 15 | Lane 3 — Cohesion | yaml two-surface trajectory | yaml A.W4 → F.W5 → G.W4 → J.W2; every claim verifiable from artefacts the trajectory produces. | honored |
| 16 | Lane 3 — Cohesion | Backend trait V2 expansion | The V2 obligations columns are speculative but bind to V1-trait-shape; cohesion holds because V2 carry rows at MASTER-PLAN §24 explicitly route to V2 amendment. | honored |
| 17 | Lane 4 — SOTA Anchoring | M1 Pro Rust-line gates | Each gate cites competitor + dataset + platform per Lock 8. Per §4 Lens K row 1, the gates are aspirational at body-level; V1-surface load-bearing. | honored |
| 18 | Lane 5 — Grammar-Authoritative (Lock 14) | yaml two-surface; per-grammar-fence-canon lint at ARCH §13.1 | Zero `match grammar { Json => ..., CssL4 => ..., ... }` arms in proposed generic crates; per-grammar table at ARCH §12.2 ten-row matrix. | honored |
| 19 | Lane 6 — Generated-Code + LOC Budget | Per-grammar baselines at ARCH §12.2 + MASTER-PLAN §20 | bbnf 21,503 → 21,933; nine seed grammars + yaml provisional ≤4,000; F/H/J budget gates. | honored |
| 20 | Lane 7 — Friction Forecast | 8-row cookbook at MASTER-PLAN §25 | Each row binds target user + mental model + confusion + artefact + diagnostic; format() row added in Phase 7.2. | honored |
| 21 | Lane 8 — Carry & Deferral | MASTER-PLAN §24 carry-ledger | Every "deferred to" / "carries to" names receiver, blocker, gate; V1-FOLD #24 + #25 should be added per §2 Lens I row 9. | honored-with-amendment-for-tier-3-carry |
| 22 | Lane 9 — Greenfield Discipline | No quick solutions, no workarounds, no legacy uncontested | The trio absorbs 30 V1 folds without architectural patch; Lens I/J/K identify simplifications, not workarounds. | honored |
| 23 | Lens F — LLM bias | Cite hygiene | V7.1 closed P1-P10 + X1; no new phantom anchors emerge in V8 read. | honored |
| 24 | Lens G — Overfitting | Per-grammar matrix | yaml is "onboarding probe", not seed-grammar budget member; ten-row matrix is producer-side schema, not dispatch logic. | honored |
| 25 | Lens H — Hallucination/provenance | Path:line cites | All anchors resolve in V8 spot-check (Lock 8 line 48; Lock 12 line 56; Lock 14 line 60). | honored |
| 26 | Lens I — Contrivance (V8) | 13-row table at §2 | 7 KEEP + 4 SIMPLIFY + 1 CONSOLIDATE + 1 ASPIRATIONAL; healthy KEEP fraction at 54%. | amend-required |
| 27 | Lens J — Host-language leverage (V8) | 11-row table at §3 | 7 KEEP + 2 HYBRID + 1 LEVERAGE + 1 SIMPLIFY; cross-host divergence flagged at HYBRID rows. | honored-with-cross-host-amendment |
| 28 | Lens K — Meta-grammar discipline (V8) | 12-row table at §4 | 8 LOAD-BEARING + 3 ASPIRATIONAL + 1 SPECULATIVE; LOAD-BEARING fraction healthy. | honored-with-aspirational-routing |

Lane summary: 28 rows; 24 honored; 4 amendment surfaces (rows 3, 21, 26, 27).
The 4 amendment rows correspond to the V8 punch list at §7. None cross redraft
threshold; all are document-local surgical edits.

## §6 V7.1 → V8 Comparison

V7.1 ratified the trio as READY (cohort 4-of-4); V8 audits the *leanness* of
the same trio. Rather than rerun cite-hygiene + cross-doc coherence (which V7.1
closed), V8 applies three new lenses (I/J/K) and carries the 9-lane standard
forward.

V7.1 verdict surface (per `restart/audit/hardening/HARDENING-CONSOLIDATED-V7.1.md` §5):

- READY across PASS-1 + PASS-2 + PASS-3 + MASTER-PLAN trio.
- 13 of 14 V7 punch items CLOSED; R4 (BBNF-PATTERN-NONEXHAUSTIVE catalogue
  sync to ARCH §7.4) remained as one non-blocking friction residue.
- 21 of 21 lanes READY (one N/A in compressed mode).
- Three cross-target rename clusters (`pointer!` → `path!`; `BBNF-POINTER-*` →
  `BBNF-PATH-*`; regex-automata oracle removal) all CLOSED.

V8 delta (lens-by-lens):

| Lens | V7.1 baseline | V8 finding | Delta |
|---|---|---|---|
| Lane 1 (Lock-Adherence) | 21 lanes READY (compressed) | 14 locks honored; method-count CONSOLIDATE on Backend trait per Lens I | one architectural-method-count amendment |
| Lane 2 (Sequencing) | A-J dependency-order intact | 5 sequencing rows honored | unchanged |
| Lane 3 (Cohesion) | yaml trajectory; V2 carry rows | 2 cohesion rows honored | unchanged |
| Lane 4 (SOTA) | Lock 8 amendment text matched H.W0-H.W4 | SOTA gates honored; Lens K classifies as ASPIRATIONAL for body | new aspirational labelling, not a fault |
| Lane 5 (Grammar-Authoritative) | yaml two-surface + ten-row matrix | unchanged | unchanged |
| Lane 6 (Generated-LOC) | F.W3 + bbnf 21,503 → 21,933 | unchanged | unchanged |
| Lane 7 (Friction) | 8-row cookbook + format() row | unchanged | unchanged |
| Lane 8 (Carry/Deferral) | MASTER-PLAN §24 single carry-ledger | one amendment: add Tier 3 #24 + #25 V2 carry rows | minor |
| Lane 9 (Greenfield Discipline) | 30 V1 folds absorbed cleanly | unchanged | unchanged |
| Lens F (LLM bias) | V7 P1-P10 closed | no new phantom anchors | unchanged |
| Lens G (Overfitting) | yaml probe correctly classified | unchanged | unchanged |
| Lens H (Hallucination/provenance) | cite hygiene clean | unchanged | unchanged |
| **Lens I (Contrivance) — NEW V8** | n/a | 4 SIMPLIFY + 1 CONSOLIDATE + 1 ASPIRATIONAL | NEW finding cluster |
| **Lens J (Host-leverage) — NEW V8** | n/a | 2 HYBRID + 1 LEVERAGE + 1 SIMPLIFY | NEW finding cluster |
| **Lens K (Meta-grammar) — NEW V8** | n/a | 3 ASPIRATIONAL + 1 SPECULATIVE | NEW classification cluster |

Net V8 surfaces: ~7 amendment rows + ~5 classification refinements (ASPIRATIONAL
vs LOAD-BEARING vs SPECULATIVE labels added to existing facilities). No
architectural reversal; no redraft surface.

## §7 Punch List

The V8 punch list is ordered by leverage (greatest simplification first):

| # | Path:line | Surgery | Verdict source | Lane / Lens | Owner |
|---|---|---|---|---|---|
| V8-P1 | `restart/ARCHITECTURE.md:1083-1113` (§7.5 Backend trait) | Collapse the four `emit_*` methods (`emit_runtime_template`, `emit_value_api`, `emit_visitor`, `emit_path_schema`) into one `emit_artefacts(grammar, schemas) -> ArtefactSet`. The four artefacts (template, value, visitor, path-schema) are co-emitted from one grammar+schema input; the per-method signature is contrivance. The `ArtefactSet` typed file tree replaces the four output types. The four artefact files remain distinct on disk. | SIMPLIFY (§2 Lens I row 4) | Lens I, Lens K | ARCH §7.5 prose author |
| V8-P2 | `restart/ARCHITECTURE.md:1264-1313` (§8.2 type system stack) | Collapse the 7-layer prose to "HM (equality + Algorithm-W) + bidirectional (Pierce-Turner) + DK13 higher-rank + first-order unification + finite CSP" (5 named mechanisms; one citation chain). Drop CHR-improvement from V1 type-system text and route to V2 amendment carry row. The Algorithm-W + HM equality + first-order unification are one algorithm presented as three. | SIMPLIFY (§2 Lens I row 5) | Lens I | ARCH §8.2 author |
| V8-P3 | `restart/ARCHITECTURE.md:1430-1454` (§10.1 rewrite-budget categories) | Collapse `simplification-rewrites` into `codegen::verify` (it has no e-graph need; one-pass dead-mark elision belongs alongside regen-equality at F.W3). Net: 4 → 3 rewrite-budget categories. Add explicit LOAD-BEARING (`legality-rewrites`, `normalization-rewrites`) vs ASPIRATIONAL (`cost-driven-rewrites` body-level) labelling. | CONSOLIDATE (§2 Lens I row 3) + ASPIRATIONAL (§4 Lens K row 2) | Lens I, Lens K | ARCH §10.1 + MASTER-PLAN F.W3 |
| V8-P4 | `restart/ARCHITECTURE.md:1032-1063` (§7.4 diagnostic catalogue) | Retire the numeric alias system (`BBNF-LIFE001`, `BBNF-VISIT002`, `BBNF-LAYOUT002`, `BBNF-OPT001`, `BBNF-OPT002`, `BBNF-PATH001`, `BBNF-PATH002`, `BBNF-GRAMMAR001`, `BBNF-CG001`); keep only the human-readable names. Move `BBNF-LOCAL-EQUALITY-ANNOTATION` to V2 carry row. The aliases are double-tracking (LLM-trained-distribution artefact). | SIMPLIFY (§2 Lens I row 6) | Lens I | ARCH §7.4 author |
| V8-P5 | `restart/MASTER-PLAN.md:321` (C.W4 over-promise carry) | C.W4 currently says "fail-closed posture and representative-stability protocol route to C.W4 implementation rather than authoring at architecture level". Route Tier 3 #24 (CHR-improvement layer) and Tier 3 #25 (function composition library) explicitly to V2 carry rows in MASTER-PLAN §24, not C.W4. | ASPIRATIONAL (§2 Lens I row 9) | Lens I, Lane 8 | MASTER-PLAN §24 carry-ledger author |
| V8-P6 | `restart/ARCHITECTURE.md:1473` (§11 perf gate `OpenFrame` row) | Rename "OpenFrame clone absence" gate to "parallel-substrate-clone-absent"; cite "Rust borrow checker enforces" at the gate. The OpenFrame name is archaeological (legacy 2,000-commit failure). MIGRATION §19.4 grep gate at line 742 should also rename the substring. | LEVERAGE (§3 Lens J row 9) | Lens J | ARCH §11 + MIGRATION §19.4 |
| V8-P7 | `restart/ARCHITECTURE.md:1135-1141` (§7.5 `LowerContext` side-tables enumeration) | Replace the six-table enumeration in `LowerContext` prose with a `&SideTables` reference type that carries the §7.3 set as one struct. Reader routes to §7.3 once for the side-table list. | SIMPLIFY (§2 Lens I row 13) | Lens I | ARCH §7.5 prose |
| V8-P8 | `restart/MASTER-PLAN.md:131-141` (§4 SOTA gates row) | Add a single-sentence cite below the SOTA-row table: "SOTA-beat is the audacious target (V1.0 close at J.W1); SOTA-parity is the meta-grammar correctness floor. If H.W3/H.W4 measurements land at parity but not beat, V1 close gates record parity-not-beat outcome and the SOTA-beat work routes to V1.1 amendment." | ASPIRATIONAL (§4 Lens K row 1) | Lens K | MASTER-PLAN §4 author |
| V8-P9 | `restart/ARCHITECTURE.md:622-665` (§5 workspace metadata schema) + MASTER-PLAN §24 carry | Add a one-line note at ARCH §5 that V2 cross-host onboarding promotes the schema content to a language-neutral sidecar (`bbnf.toml`). Add a corresponding V2-amendment carry row in MASTER-PLAN §24 ("cross-host onboarding metadata-carrier"). The schema *content* is host-agnostic; the *carrier* (Cargo.toml) is V1 Rust-line. | HYBRID (§3 Lens J row 3) | Lens J | ARCH §5 + MASTER-PLAN §24 |
| V8-P10 | `restart/MASTER-PLAN.md:24-34` (§24 carry ledger) — V1-FOLD Tier 3 deferrals | Add two new V2-amendment carry rows: (a) "CHR-improvement layer (host overload disambiguation)" — Tier 3 #24; receiver V2 amendment when host-overload telemetry exceeds CSP-improvement-only resolution. (b) "Function composition library + partial application via closure-wrap" — Tier 3 #25; receiver V2 host-primitive library extension. Both are V1-FOLD §3 Tier 3 ranking; lens-K ASPIRATIONAL. | ASPIRATIONAL (§4 Lens K row 2 + §2 Lens I row 9) | Lane 8 + Lens K | MASTER-PLAN §24 author |
| V8-P11 | `restart/ARCHITECTURE.md` (§3.1 `bbnf` API + V2 cross-host note) | Add an explicit note at ARCH §3.1 that the three-entrypoint API (`parse` + `parse_in` + `parse_owned`) is V1 Rust-line. The V2 `WasmBackend` will collapse `parse_in` (no bumpalo equivalent in linear memory); the V2 `TsBackend` will collapse all three to one entrypoint (GC). The cross-host divergence is V2-amendment-bound. | HYBRID (§3 Lens J row 1) | Lens J | ARCH §3.1 author |

Punch-list priority:

- V8-P1 + V8-P2 + V8-P3 + V8-P4 + V8-P7 are *architectural simplifications*
  (Lens I); they reduce trio surface without losing meta-grammar content.
- V8-P5 + V8-P10 are *carry-ledger amendments* (Lane 8); they route Tier 3
  V2 carries explicitly.
- V8-P6 is *leverage rename* (Lens J); the OpenFrame name is archaeological.
- V8-P8 is *SOTA classification* (Lens K); the gates are V1 ASPIRATIONAL with
  V1 surface, not aspirational with V2 surface.
- V8-P9 + V8-P11 are *cross-host amendments* (Lens J HYBRID); they route the
  V2 cross-host story explicitly without forcing V1 host-genericity.

Estimated amendment wall: 60-90 minutes for a careful pass.

- 15 minutes for Lens I architectural simplifications (V8-P1 through V8-P4 +
  V8-P7).
- 10 minutes for Lane 8 carry-ledger additions (V8-P5 + V8-P10).
- 15 minutes for Lens J cross-host amendments (V8-P6 + V8-P9 + V8-P11).
- 10 minutes for Lens K SOTA classification (V8-P8).
- 15 minutes for cross-document coherence verification post-amendment.

## §8 Final Verdict

**READY-WITH-AMENDMENT.**

The trio carries thirteen findings across Lens I (contrivance), Lens J
(host-language leverage), and Lens K (meta-grammar discipline). None cross the
re-draft threshold. The architecture is internally coherent; the simplifications
are local re-shapes (collapse 5-method Backend trait to 2-method; collapse
4-category rewrite-budget to 3; retire diagnostic numeric aliases; collapse
7-layer type-system prose to 5-mechanism; relabel `OpenFrame` perf gate;
classify SOTA / cost-driven / publication gates as ASPIRATIONAL with V1
surface; route Tier 3 #24 + #25 to V2 carry).

The KEEP fractions across the three lenses are healthy:

- Lens I: 54% KEEP (7 of 13); the 4 SIMPLIFY rows are local re-shapes that
  drop apparatus without architectural loss.
- Lens J: 64% KEEP (7 of 11); the HYBRID + LEVERAGE rows clarify cross-host
  divergence rather than forcing host-genericity into V1.
- Lens K: 67% LOAD-BEARING (8 of 12); the ASPIRATIONAL + SPECULATIVE rows
  classify V1 surface vs V2 amendment.

V7.1's 4-target cohort verdict (READY) is preserved. V8 adds three lens
classifications (I/J/K) without modifying V7.1 closure. The punch list is
~11 surgical edits across the trio; per-tranche full-spec drafting can absorb
the simplifications mid-flight without architectural disruption.

Acceptance standard for V8 → V8.1 closure:

- V8-P1 through V8-P11 resolved in trio amendment commit.
- The Lens I/J/K verdict cluster lands as architectural ratification, not as
  V8 audit residue.
- Cohort verdict re-confirms READY post-amendment.

Re-draft thresholds: zero crossed. The architecture's load-bearing surface
is intact; the simplifications are leanness amendments, not redesign.

## §9 Closing Posture

V7.1 ratified the architecture; V8 asks if it is lean enough. The answer is
"nearly". The trio is one narrow simplification cycle away from V8 READY.

The Lens I findings consolidate gracefully — collapsing the Backend trait
method count, retiring diagnostic numeric aliases, folding `simplification-rewrites`
into `codegen::verify`, and collapsing the type-system prose are all local
re-shapes that drop ceremony without losing architectural commitment.

The Lens J findings ratify the V1-Rust-line bias as principled. The cross-host
divergence is V2-amendment-bound; V1 leverages Cargo + xtask + bumpalo
ergonomically. The HYBRID verdicts cite where V2 will diverge and route the
divergence to explicit V2 carry rows; this is the right host-leverage shape.

The Lens K findings classify the architectural facilities as LOAD-BEARING /
ASPIRATIONAL / SPECULATIVE without forcing V1 deferral of any meta-grammar-correct
mechanism. The SOTA gates are V1-surface aspirational; the cost-driven rewrites
are V1-surface aspirational; the V2 backend impls are speculative. The
LOAD-BEARING fraction (8 of 12) is the meta-grammar correctness core.

The user mandate ("audacious + SOTA + functional-in-nature + Rust-like
ergonomics + inference stronger than Rust if possible") is preserved through
the V8 simplifications:

- audacious: SOTA gates remain as V1 close-gate targets.
- SOTA: Lock 8 anchors against sonic-rs / simd-json / lightning-css unchanged.
- functional-in-nature: function values + lambdas + closures + DK13 + match +
  tuple all V1; transducer-without-directive worked example unchanged.
- Rust-like ergonomics: three-entrypoint parse API + bumpalo opt-in unchanged.
- inference stronger than Rust: DK13 higher-rank V1 fold unchanged.

The thirty V1 folds are absorbed; the punch list is text-surgical hygiene
(collapse, retire, classify, route). V8 closes with the trio one careful
amendment pass from V8 READY equivalent.

Hereupon V8 dispatches its punch list to the amendment-agent receivers; per-tranche
full-spec drafting can proceed in parallel where the V8 amendments do not block
tranche-local work.
