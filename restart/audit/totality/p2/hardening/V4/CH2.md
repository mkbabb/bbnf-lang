---
challenge: CH2
pass: T-P2-research
cycle: V4
lens: generality / Lock 14
generated_at: 2026-05-21T15:02:26-04:00
verdict: ACCEPT
---

# T-P2 V4 CH2 - Generality / Lock 14

## Verdict

**ACCEPT.** V4 preserves the Lock 14 grammar-neutrality contract while resolving
the V3 executable-ledger ambiguity in the safe direction: rows without strict
commands or first consumers are marked non-shortlist, not admitted by JSON-only
evidence or generic-crate policy. CSS, Sheets, BBNF-self, generated-policy, and
caller-data pressures remain binding through the inherited V2/V3 contracts and
the current owner dossiers.

## Evidence

- The V4 addendum explicitly supplements the V2 and V3 addenda and does not
  weaken their Lock 14, Lock 1, REDRESS, material-differential,
  anti-paper-close, state-machine, or numeric abrogate contracts
  (`restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:11`). It frames the V4
  changes as executable-ledger deltas, non-shortlist blockers, and REDRESS-slice
  ownership repairs, not as a new JSON-only admission model
  (`restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:16`-`22`,
  `restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:41`-`45`).

- The exact V3 CH2 acceptance condition remains intact: V3 had already
  preserved grammar-neutral transfer by keeping executable rows
  generated-policy or caller-data owned, retaining non-JSON CSS/Sheets pressure,
  and avoiding any narrowing to JSON
  (`restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md:27`,
  `restart/audit/totality/p2/hardening/V3/CH2.md:14`-`18`). V4 adds no
  contrary override.

- The V4 executable ledger delta marks weak rows as non-shortlist until the
  missing strict command, first consumer, or policy split exists. This is
  load-bearing for CH2: `tbl_tbx_escape_decode_batch` is blocked until JSON
  fixed-width and CSS variable-width escape parity exist; DOTPROD digit parsing
  is blocked until scalar policy, overflow/invalid-lane parity, and a first
  consumer exist; PMULL/CSSC union is blocked until a combined strict matrix and
  exact first consumer exist
  (`restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:49`-`54`). Those rows
  therefore cannot leak JSON-specific proof into fleet-wide grammar claims.

- The REDRESS-slice ownership table is row-local rather than generic-category
  policy. It keeps JSON direct rows scoped to strict sonic row gates, keeps CSS
  ASCII run-skip scoped to strict lightningcss/cssparser movement, blocks
  string/unicode routes on grammar-policy leaks, and blocks parse-that imports
  until snapshot/license/HIR mapping closes without a new BIR or API
  (`restart/audit/totality/p2/T-P2-V4-FOLD-ADDENDUM.md:66`-`79`). That preserves
  grammar-neutrality while making non-shortlist status explicit.

- The owner dossiers still carry the non-JSON and generated-policy pressure. 2C
  requires a generated provider registry, grammar-shape leak scan, generated
  sink/fact/value/flag surfaces, primitive policy manifests, CSS plus
  Sheets/BBNF-self transfer, and generated decision-engine facts before Lock 14
  closure (`restart/audit/totality/p2/2C-grammar-neutrality.md:82`-`96`).
  2B says shared SIMD policy must come from generated grammar data or caller
  data and that proof-only primitives remain downgraded until a
  JSON/CSS/Sheets/BBNF-self row moves or is measured rejected
  (`restart/audit/totality/p2/2B-primitive-vocabulary.md:30`-`46`,
  `restart/audit/totality/p2/2B-primitive-vocabulary.md:53`-`60`).

- The potentially risky generic-crate surfaces remain fenced. 2E requires
  caller-supplied alphabets/policy for byte-class and hardware primitives and
  records `policy_owner` as generated grammar, caller data, or none
  (`restart/audit/totality/p2/2E-host-arch-esoterica.md:115`-`139`,
  `restart/audit/totality/p2/2E-host-arch-esoterica.md:243`-`253`). 2F fences
  parse-that/regex imports to grammar-neutral facts, scanner plans, and
  generated grammar consumers; JSON/CSS hard-coded alphabets, `JsonSink`, JSON
  flag names, and `\uXXXX`-only semantics are explicitly non-admissible in
  shared APIs (`restart/audit/totality/p2/2F-parse-that-gaps.md:54`-`61`,
  `restart/audit/totality/p2/2F-parse-that-gaps.md:162`-`171`,
  `restart/audit/totality/p2/2F-parse-that-gaps.md:245`-`250`).

## Required Repairs

None for CH2.

## Carry-Forward

This ACCEPT is research-pass hardening only. T-P3/G-Omega still must implement
and gate-consume the generated manifests, grammar-shape leak scans, strict
CSS/JSON oracles, Sheets or BBNF-self witnesses, same-wave consumers,
non-shortlist blocker clearances, and telemetry before claiming fleet-wide
Lock 14 closure.
