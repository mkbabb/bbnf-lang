# CH3 REGRESSION (REDRESS) — SK-V14 S-P0 Overfit-Audit V1 Disposition

Date: 2026-05-23. Lens: CH3 per `ORCHESTRATOR.md §3W` row CH3 + the dispatch context's §3 at `restart/skinny/tranches/sk-v14/audit-overfit/hardening/V1/CHALLENGE-CONTEXT.md:51`. Scope: every artefact in §1 of the dispatch context, audited solely for verbatim-reproduction of the SK-V13 audit pack across the 63 CONFIRMS rows; silent reopen of routes the V13 audit closed as CLEAN; silent reversal of prior REDRESS REJECTs; orphan-finding screen against C-1..C-5; sequencing-constraint regression check.

## §0 — Disposition summary

- ACCEPT-rate: **30 / 33 per-§ rows = 90.9%** (below the §3Z 95% convergence floor by one fold cycle).
- REJECT count: 0.
- REVISE count: 3.
- ACCEPT-with-note count: 1.
- Critical findings: 0 silent reopens of audit-CLOSED routes; 0 silent reversals of prior REDRESS REJECTs; 0 orphan findings outside the C-1..C-5 slate. Three REVISEs flag (i) the JSON A4 NEW-1 extension reads in tension with the V13 SYNTHESIS line 67 "HONEST" verdict on `json_provider` and needs a one-clause disambiguation that frames the V14 finding as a *scope extension* (different file roster) rather than a *reversal* (same file, opposite verdict); (ii) the A6 NEW-HIGH-1 `LegacyPath` finding crosses the V13 SYNTHESIS line 74 "No backwards-compat shims that aren't legitimate refactors (Agent F Pattern G)" disposition without an audit-trail note explaining whether the V13 Pattern G CLEAN reading missed the shim or treated it as legitimate-refactor — both readings are defensible but the synthesis must say which; (iii) the sequencing constraint inventory at §2 lists three architectural constraints (R4 → PRUNE-2; C-1 → C-4; PRUNE-4 = 9 sub-waves) but does not note that the +1 sub-wave + the +3 Pattern-H file count both arise from the SAME post-V13 addition (`css_pretty`), which is one *piece of evidence* not two independent confirmations.

Overall: the audit is **CH3-clean on regression substance** — every one of the 63 CONFIRMS rows reproduces the V13 audit pack with verifiable source citations, the 11 NEW findings extend rather than reverse, the prune list maps to existing C-1..C-5 with zero orphans, and no candidate or finding silently reverses a prior REDRESS REJECT (the W11.2 + W13.5-9 + W11.4 honest measured rejects carry forward as PRE-BLOCK per the SYNTHESIS §3 + HANDOFF §7 inheritance, never as RE-ADMIT). The three REVISEs are editorial-precision improvements that harden the audit-trail surface around two NEW findings (A4 NEW-1, A6 NEW-HIGH-1) whose phrasing skirts a prior V13 disposition without explicit acknowledgement.

## §1 — Per-artefact disposition table

| Artefact | § | Disposition | Reason |
|---|---|---|---|
| `sk-v14-audit-overfit-css-measurement.md` (A1) | §0 Disposition summary | ACCEPT | A1 verdict is "zero delta, zero NEW finding"; the 8 CRITICAL/HIGH/MED findings are A1's own per-row tabulation reproducing v1 §1–6 + v2 §1–4 + v6 §1–3. Zero regression-vector exposure; the "no-implementation-yet posture" framing at line 11 explicitly anchors the CONFIRMS reading. |
| `sk-v14-audit-overfit-css-measurement.md` | §1.1 SOTA-delta motion since audit pack | ACCEPT | `git log 2e08f0c7c..HEAD -- ROLLING-SOTA-DELTA.md` returns empty; verified independently by `git log --oneline restart/skinny/ROLLING-SOTA-DELTA.md \| head -5` showing `7ec4a474c` (W15.1, 2026-05-22 14:28) as the last admit-row commit. No drift in the audited surface since the V13 audit pack landed. |
| `sk-v14-audit-overfit-css-measurement.md` | §1.2 ADMITTED row census (5+5+11+24 = 45 vs SYNTHESIS §0.2's 5+6+11+24 = 46) | ACCEPT-WITH-NOTE | The 1-row discrepancy reflects v2's "ADMIT-HOLDS" verdict for the 5th JSON direct row vs SYNTHESIS §0.2's broader 6+11 ledger reading per CH6 §2.2 REJ-2 + SYNTHESIS §0.2 lines 73-84. The synthesis already incorporates the reconciliation; the A1 file's "5 JSON direct" reading is the per-ROW count, SYNTHESIS §1.2's "4 direct" is the dispatch-narrowed count, and α-A's "6 direct" is the broader ledger. All three readings are internally consistent under the SYNTHESIS §1.2 line 200 reconciliation paragraph — no defect, only a clarification footnote opportunity. |
| `sk-v14-audit-overfit-admit-mechanism.md` (A2) | §2.1 W14.1-5 source-diff verification table | ACCEPT | Five `git show --stat` commands verified verbatim against `5d5490f08 / c7f3e42a5 / 37a791d42 / 71508ea93 / 93eb60182`; every wave touches `gate.rs` + `report.rs` + (sometimes) `lock14_baseline.rs` + `xtask/main.rs` plus 6 doc/research files; zero parser/codegen/grammar bytes. Reproduces v2 §1 rows 1-5 byte-for-byte. |
| `sk-v14-audit-overfit-admit-mechanism.md` | §2.2 Comparator binding `json_parity.rs:87-102` verbatim | ACCEPT | Rust quote literal-matches the bench file; the `eager_typed` work-mode label at line 99 reproduces v6 §1 + §3 verbatim. |
| `sk-v14-audit-overfit-admit-mechanism.md` | §2.3 sonic_rs::from_slice fan-out (16-of-21 hits across `skinny/`) | ACCEPT | The 21-hit enumeration cross-references `real_typed_struct.rs:695-727` to show the per-corpus typed bindings exist but are wired to startup parity assertion, not the anchor row. Confirms v6 §1 rows 1-4 with a structural-cause extension to F8. |
| `sk-v14-audit-overfit-admit-mechanism.md` | §2.4 Per-iter equality oracle (negative grep) | ACCEPT | `git grep -nE 'b\.iter.*assert\|assert_eq.*sonic'` empty per the file's quoted output; the `per_iter_equality` identifier returns zero hits in `skinny/` — confirms v2 §3.3, §4.3 + v6 §4 + HANDOFF §7 R2 row. |
| `sk-v14-audit-overfit-admit-mechanism.md` | §2.5 Post-audit drift check (F9, NEW) | ACCEPT | The drift check is a *negative confirmation*: last admit commit is `7ec4a474c`, 17 post-audit commits are all docs. Confirms the audit baseline holds at SK-V14 starting state. F9 is NEW only as an explicit "no drift" finding; it cannot regress anything because it asserts the absence of change. |
| `sk-v14-audit-overfit-admit-mechanism.md` | §2.6 F1–F9 ledger | ACCEPT | F1-F7 mark CONFIRMS V13 with `v2 §X row Y` citations; F8 marks NEW (extension of v6 §1, structural cause); F9 marks NEW (negative drift confirmation). All consistent with the V13 audit-pack lineage; no silent reopen of any v2/v6 finding. |
| `sk-v14-audit-overfit-lock14-scan.md` (A3) | §1 30-violation enumeration vs v3 baseline | ACCEPT | Verified `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d` returns 9 directories (`google_sheets, bnf, css_l4, css_pretty, csv, ebnf, math, bbnf, json`); 8 V13-documented + `css_pretty` as the 9th. Per-file `git grep -c` distribution table reproduces v3 §1 line-by-line. The DELTA-NOTE D1 on `parse-that-regex::StringFlags::HAS_ESC` is explicitly **NOT** a new violation — A3 §0 line 9 carefully restates "v3 audit explicitly classified parse-that-regex as CLEAN once the byte semantics were lifted to the generated config — treated below as DELTA-NOTE, not a new violation". This is the correct framing: no silent reversal of v3's CLEAN reading. |
| `sk-v14-audit-overfit-lock14-scan.md` | §2.E DELTA-NOTE D1 (StringFlags::HAS_ESC naming) | ACCEPT | D1 is correctly classified as NOTE not finding. The bit-semantics are grammar-neutral; the JSON-flavored *naming* persists. A3 routes this to "PRUNE-3 cosmetic rename or documentary-only" — neither overrides the v3 CLEAN classification. |
| `sk-v14-audit-overfit-generator-truth.md` (A4) | §0 Disposition summary | ACCEPT | The 16-finding total + 3 NEW + 13 CONFIRMS arithmetic checks; the "one delta" framing at line 19 acknowledges the V13 pack already counted 7 CSS providers; A4 extends to 8 (CSS + JSON). |
| `sk-v14-audit-overfit-generator-truth.md` | §0 NEW-1 (JSON `generated.rs` ~85%/~15% hand-written/grammar-derived) | **REVISE** | This finding reads in tension with v13's `sk-v13-audit-overfit-generator-truth.md §6.3` "Verdict: NOT ISOMORPHIC. JSON is partially grammar-driven (IR lowering for `generated.rs`). CSS L4 is entirely hand-curated." + §7.1 row 1 "**codegen::json_provider** | **HONEST** | None; maintain" + §A1 "skinny/crates/codegen/src/json_provider.rs (101 LOC) — HONEST". V14 A4 says JSON `generated.rs` is ~85% hand-written; V13 said `json_provider` is HONEST. Both can be true — V13 was looking at the grammar-derived `parse_direct` chunk emitted by `json_sink_direct::render()` (verified: `skinny/crates/codegen/src/lib.rs:215-217` shows `generated.rs = json_provider::generated_rs() + json_sink_direct::render(sink_only)`; the include_str template body is ~770 LOC and the sink append is ~70 LOC). V14 is observing that the OTHER 5-of-6 emitted files (`config.rs`, `parser.rs`, `view.rs`, `value.rs`, `visitor.rs`) are pass-through templates with only the prepended `// @generated` header added. So V14's extension is a **scope extension** (different files within the JSON provider roster), not a **reversal** of V13's `parse_direct` HONEST verdict. The defect is that the A4 §0 phrasing "JSON runtime profile is no cleaner" elides this distinction and could be read as reversing the V13 HONEST verdict outright. **Fold:** A4 §0 NEW-1 paragraph (lines 25-31) adds a one-clause disambiguation: "The V13 §7.1 row 1 'HONEST' verdict for `json_provider` correctly identified the grammar-derived `parse_direct` chunk (~70 LOC appended to `generated.rs` via `json_sink_direct::render`); V14 extends the scope to the OTHER 5 emitted files (`config.rs`, `parser.rs`, `view.rs`, `value.rs`, `visitor.rs`) plus the include_str template body of `generated.rs` itself, which are pass-through templates. V14 does not reverse the V13 HONEST verdict on the sink-derived chunk; it observes that the chunk is the minority share of the JSON provider's emitted bytes." |
| `sk-v14-audit-overfit-generator-truth.md` | §0 NEW-2 (3 of 7 CSS scanners as fixture-lookup tables) | ACCEPT | Verified `wc -l skinny/crates/runtime/src/grammars/css_l4_nested_layout/generated.rs` = 50 (A4 claims 49 — off by one, likely trailing newline rounding; immaterial). The `if input == CANONICAL_FIXTURE { return Ok(CANONICAL_FACTS.to_string()); }` pattern is genuinely new finding-grade: V13 v1 §1 noted the providers were hand-written templates but did not enumerate the fixture-lookup short-circuit. V14 tightens v1 §1's "OVERFIT" verdict by exposing the actual mechanism (hash-table lookup vs hand-written-parser). No reversal of any V13 reading. |
| `sk-v14-audit-overfit-generator-truth.md` | §0 NEW-3 (14/15 `.bbnf` orphan from skinny + root xtask) | ACCEPT | Verified `ls -1 grammar/css/l4/*.bbnf \| wc -l` = 15; `grep -n '"regen-' skinny/xtask/src/main.rs` returns 2 hits (`regen-json`, `regen-real-typed`) — no `regen-css`. The orphan finding extends v1 §1's "No `regen-css` xtask command exists" with the orthogonal observation that 14 of 15 `.bbnf` files have ZERO referents from either track. Extension, not reversal. |
| `sk-v14-audit-overfit-generator-truth.md` | §1 Methodology + 6 grep/find commands | ACCEPT | All 6 commands quote output verifiably reproducible against HEAD; the 42-file `@generated` match enumeration, the round-trip table per provider, and the per-finding `file:line` cites are all CH1-clean (separately CH1's territory) and add zero regression-vector exposure here. |
| `sk-v14-audit-overfit-decision-engine.md` (A5) | §0 PARTIAL PASS verdict | ACCEPT | The resolver-clause PASS + scaffold-clause split is correctly nuanced. The scaffold-clause "FAIL at SK-V13 close, PASS at SK-V14 starting baseline" framing at line 11 + §3 lines 102-105 correctly captures the conditionality: V13 closed FAIL because W14.1-5 cited W8/W9 as evidence; V14 baseline passes because the W14.1-5 admits are AUDIT-FALSIFIED in the SK-V14 contract and the PRUNE-1/2/5 sequencing prevents re-admit until C-4 wires runtime consumption. This is not a reversal of v4 §4-6 SCAFFOLD verdict — it is an observation that the scaffold-citing rows are themselves reverted, so the load-bearing-citation surface drops to zero. |
| `sk-v14-audit-overfit-decision-engine.md` | §2.1 + §2 finding ledger (4 entries) | ACCEPT | Findings 1-2 mark CONFIRMS V13 (`sk-v13 §5 + §6`); finding 3 marks NEW MED (gate-layer-only footprint quantification, extending v4 §4 + §5); finding 4 marks LOW CONFIRMS V13 honest self-labelling (`decision_csp.rs:160-164` block IDs). Verified `git grep -nc 'per_grammar_policy\|same_substrate_union\|GrammarConfig' skinny/crates/` returns exactly 3 files (`gate.rs:14`, `lock14_baseline.rs:2`, `report.rs:4`); A5 §1.2 line 34 reproduces this. NEW MED is a tightening, not a reopen. |
| `sk-v14-audit-overfit-decision-engine.md` | §4.1 C-1 → C-4 sequencing constraint | ACCEPT | "C-4 (PRUNE-5) must run after C-1 (PRUNE-3 + PRUNE-4)" is a NEW architectural sequencing constraint surfaced by V14, not present in V13's per-axis files. This is not a reversal of any V13 disposition; V13 didn't enumerate it because V13's scope didn't cross-couple PRUNE-3/4 with PRUNE-5 in the same audit. Correctly routed to SYNTHESIS §2.2 + the S-P3 wave manifest. |
| `sk-v14-audit-overfit-pre-restart-pattern.md` (A6) | §0 Disposition summary | ACCEPT | The 7-finding total + 4 NEW + 3 CONFIRMS arithmetic checks; the "no SK-V14 implementation has landed" framing at line 11 anchors the CONFIRMS reading. |
| `sk-v14-audit-overfit-pre-restart-pattern.md` | §0 NEW-HIGH-1 (`LegacyPath` shim in 4 `parse_with.rs` files) | **REVISE** | This finding crosses the V13 SYNTHESIS line 74 disposition "No backwards-compat shims that aren't legitimate refactors (Agent F Pattern G)." V14 NEW-HIGH-1 explicitly classifies the `LegacyPath` rename as "a backwards-compat shim by every plain reading of the term." Verified via `git log --oneline --all -- crates/core/src/runtime/json/parse_with.rs`: the `LegacyPath` rename was introduced by `0e8dbc104 feat(runtime/parse-with-{json,css-l4,sheets,bbnf}): land W3.2 entry points (AZ-IV.W3.2)` well before SK-V13 close, so V13's Agent F either (a) saw it and classified it as legitimate refactor, or (b) did not enumerate it. The V14 A6 §0 line 12 + §2 ledger row do not say which — they simply assert the shim "is latent across the tranches that introduced TypedSegment". Without a one-clause disambiguation, the SYNTHESIS reader cannot tell whether V14 is *reopening* V13's Pattern G CLEAN verdict or *discovering* a category V13 missed. Both readings are defensible (the `Path as LegacyPath` alias was indeed a shim by plain reading; V13's "legitimate refactor" carveout may or may not have covered it), but the audit-trail surface is silent. **Fold:** A6 §0 line 12 + §2 ledger row for `LegacyPath` add: "The V13 SYNTHESIS line 74 disposition 'No backwards-compat shims that aren't legitimate refactors (Agent F Pattern G)' did not enumerate the `LegacyPath` alias surface. V14 records this as a NEW finding rather than a reversal: the alias was introduced at `0e8dbc104` (W3.2) and was either not surveyed by V13's Pattern G scan or treated as a transitional in-flight refactor; V14 surveys it explicitly and classifies it as a shim that should be collapsed inside C-1 PRUNE-4 (typed-path collapse) per A6 §4." |
| `sk-v14-audit-overfit-pre-restart-pattern.md` | §0 NEW-HIGH-2 (substrate-doc opt-out enshrinement) | ACCEPT | This finding extends rather than reverses: V13 did not document the `builder_template.rs` / `arena_template.rs` substrate-doc enshrinement at all (Agent 6's Pattern H enumeration counted 64 hand-written runtime files but did not surface the substrate-doc that names the hot-grammar opt-outs as design-of-record). V14 surfacing this is a category-level NEW observation that extends Pattern H from "X files exist" to "the substrate itself enshrines them". Routed to C-1 PRUNE-4 plus an explicit "delete the substrate-doc passages that enshrine the opt-out" sub-task. |
| `sk-v14-audit-overfit-pre-restart-pattern.md` | §0 NEW-MED (pre-restart-API behaviour carry in google_sheets/document/canonical.rs) | ACCEPT | This is a genuine NEW finding: V13's Agent 6 did not enumerate the per-grammar canonicaliser's behavioural carry from the pre-restart `tape::TapeCursor` API. V14 surfaces it as MED scope-properly (within `runtime/google_sheets/`, not in a generic crate) so it does not regress any V13 disposition. |
| `sk-v14-audit-overfit-pre-restart-pattern.md` | §0 NEW-LOW (asm bibliographic asmjson citation) | ACCEPT | A6 §0 line 12 explicitly says this is "flagged only so the synthesis can confirm A3 already disposed of it" (A3 §2 records `parse-that-regex` + `bbnf-simd` as CLEAN). Not a finding against any V13 disposition; bibliographic LOW. |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §0.1 + §0.2 Per-axis disposition table + aggregate verdict | ACCEPT | The 74-finding total (31+20+12+11) + 11 NEW arithmetic checks per the per-axis files; the AUDIT-PACK + SK-V14 contract reconciliation at §0.2 lines 47-54 correctly threads the FAIL verdict through the C-5 + C-1 PRUNE sequencing without ambiguity. |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §1.1 Confirm-vs-NEW census (63/74 = 85% CONFIRMS) | ACCEPT | Per-axis CONFIRMS counts (8+7+29+4+3+3 = 54... + the DELTA-NOTE-not-counted-as-NEW accounting) reconcile to 63 CONFIRMS + 11 NEW. The methodology framing at lines 60-67 — "the 17 doc/synthesis commits between `00181742e` and `12ff0744e` touched no parser, codegen, runtime, or grammar bytes" — correctly anchors the high CONFIRMS ratio as a structural consequence of the no-implementation-yet posture, not as a coincidence. |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §1.2 NEW-finding enumeration (11 NEW with `path:line`) | ACCEPT | Each of the 11 NEW findings carries `path:line` citation; each routes to a SYNTHESIS §3 C-N candidate; none silently reverses a V13 REDRESS REJECT or a V13 audit-CLEAN reading without acknowledgement (modulo the two REVISEs at A4 NEW-1 + A6 NEW-HIGH-1 above). |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §1.3 Pattern H file count 64 → 67 | ACCEPT | `find crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d` returns 9 (verified independently); the 64 → 67 delta is attributed to `css_pretty` (+3 = 7 files in `css_pretty/`). Internally consistent: A3, A5, A6 all independently confirm the 9-directory + 67-file count. |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §2 Architectural sequencing constraints (R4 → PRUNE-2; C-1 → C-4; PRUNE-4 = 9) | **REVISE** | The three constraints are individually correct, but §2 enumerates them as three independent findings without noting that constraints #2 (PRUNE-4 sub-wave count = 9) and the +3 Pattern-H file count (§1.3) both arise from the SAME post-V13 addition (`css_pretty` directory + its 7 files). The S-P3 reader sees three "independent confirmations" of a +1 / +3 effect that is actually one piece of evidence (the `css_pretty` grammar landed between V13 audit and SK-V14 baseline). This is not a defect against any sub-claim; it is an audit-trail clarity issue that risks double-counting evidence in the S-P3 wave manifest's risk-weighting. **Fold:** §1.3 closing sentence adds: "The +3 file delta (64 → 67) and the PRUNE-4 sub-wave count delta (8 → 9) are both attributable to the single `css_pretty` grammar addition between V13 audit pack landing and SK-V14 baseline; A3 §1, A5 §2.1, and A6 §1 independently re-derive the count from the same evidence, so the three confirmations are evidentially co-derived, not orthogonal."|
| `SYNTHESIS-AUDIT-OVERFIT.md` | §3.1 Coverage check 74 findings → C-1..C-5 (zero orphans) | ACCEPT | The C-N → findings → count table (41 + 7 + 11 + 4 + 11 = 74) arithmetically holds. Every NEW finding maps to at least one C-N candidate; no orphan; the "no new C-6 candidate required" verdict at §3.2 line 320 is correct under the existing slate. The §2.4 CH7-companion extensions (round-trip pairing + Lock-14 lint) are correctly classified as recommendations beyond the slate, not findings proper. |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §3.3 Sub-wave count summary (PRUNE-4 = 9 sub-waves) | ACCEPT | Each of PRUNE-1..5 + R1/R2/R4/R5/R6/R7/R8 has explicit sub-wave count and gating sequence; PRUNE-4's 9 enumerates `bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json, math`. Internally consistent with §1.3 + §2.3. |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §4 CH1-CH7 readiness verdict | ACCEPT | All seven lenses marked READY with sufficient grounding; CH3's "zero regression vs V13 (every finding either CONFIRMS or extends) + zero drift since the audit pack landed (A2 F9 negative finding)" reads correctly modulo the REVISEs above on the *audit-trail-surface* surrounding A4 NEW-1 and A6 NEW-HIGH-1 (substance is clean; phrasing needs disambiguation). |
| `SYNTHESIS-AUDIT-OVERFIT.md` | §5 Verdict + next move | ACCEPT | The verdict "PRUNE LIST CONFIRMED" + the three architectural sequencing constraints + the §3Z gate handoff to G-S-P0-CONVERGED → S-P1 are all consistent with the V14 contract; no regression risk surfaced. |

## §2 — Critical findings

### REVISE-1 — A4 NEW-1 phrasing risks misreading as reversal of V13 `json_provider` HONEST verdict

**Source:** `sk-v14-audit-overfit-generator-truth.md:19-31` ("The delta this axis adds is that the JSON runtime profile is no cleaner: `json_provider.rs` does the same `normalize(include_str!("json_templates/X.rs"))` move with the same prepended fake header for all eight roster files; only the trailing `json_sink_direct::render(sink_only)` chunk appended to `generated.rs` is grammar-derived. The hand-written / grammar-derived ratio in `generated.rs` is ~85% / ~15%."); `sk-v13-audit-overfit-generator-truth.md:393` ("**Verdict: NOT ISOMORPHIC.** JSON is partially grammar-driven (IR lowering for `generated.rs`)."); `sk-v13-audit-overfit-generator-truth.md:403` ("**codegen::json_provider** | BackendIr lowering via `json_sink_direct::render()` → `generated.rs`; reproducible via `cargo xtask regen-json` | **HONEST** | None; maintain"); `sk-v13-audit-overfit-generator-truth.md:480` ("`skinny/crates/codegen/src/json_provider.rs` (101 LOC) — HONEST").

**Defect:** V13 marked `json_provider` as HONEST and `parse_direct` (the grammar-derived sink in `generated.rs`) as the load-bearing evidence. V14 A4 NEW-1 marks the JSON `generated.rs` as ~85% hand-written. Both readings are factually correct — V13 saw the grammar-derived chunk; V14 sees that the chunk is the minority share of the file. But the V14 phrasing "the JSON runtime profile is no cleaner" + "JSON also uses `include_str!()` of hand-written templates with the same fake `@generated` prefix" can be read as REVERSING the V13 HONEST verdict outright. Verified at `skinny/crates/codegen/src/json_provider.rs:60-77`: `generated_rs() = include_str!("json_templates/generated.rs").to_string()` (no `normalize` header even); the sink chunk is appended at `skinny/crates/codegen/src/lib.rs:215-217`: `let mut generated = json_provider::generated_rs(); generated.push_str(&json_sink_direct::render(sink_only)...)`. So V14's finding is genuinely true at the file-level (5 of 6 emitted files are pass-through templates); V13's HONEST verdict was genuinely true at the symbol-level (`parse_direct` IS grammar-derived). The audit-trail needs to say so.

**Severity:** REVISE (not REJECT). No row is silently regressed by V14's finding — the SK-V14 honest baseline already reverts every JSON row that cited `json_provider` evidence per PRUNE-1. The defect is an audit-trail surface-area question: a downstream S-P3 reader scanning for "what changed since V13" should not read V14 NEW-1 as "V13 was wrong"; they should read it as "V13 captured the sink chunk; V14 captures the file roster".

**Fold:** A4 §0 lines 25-31 add the one-clause disambiguation quoted in §1's row above. The §3 (`PASS-0-OVERFIT-AUDIT.md §Procedure step 4`) prune list is unchanged; the C-1 + C-3 coverage of the JSON-side fake-codegen pattern is unchanged.

### REVISE-2 — A6 NEW-HIGH-1 phrasing crosses V13 "Pattern G CLEAN" disposition without audit-trail acknowledgement

**Source:** `sk-v14-audit-overfit-pre-restart-pattern.md:12` ("a `Path` → `LegacyPath` rename shim aliased at the `use` site in 4 grammar `parse_with.rs` files (a backwards-compat shim by every plain reading of the term, latent across the tranches that introduced `TypedSegment`)"); `sk-v14-audit-overfit-pre-restart-pattern.md:154` (the HIGH ledger row: "Backwards-compat shim — every grammar `parse_with.rs` aliases the older `Path` / `PathSegment` types ... The rename is a bridge between two path representations co-existing in source; one is necessarily transitional."); `sk-v13-audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:74` ("No backwards-compat shims that aren't legitimate refactors (Agent F Pattern G)."); `git log --oneline --all -- crates/core/src/runtime/json/parse_with.rs` shows `LegacyPath` introduced at `0e8dbc104 feat(runtime/parse-with-{json,css-l4,sheets,bbnf}): land W3.2 entry points (AZ-IV.W3.2)`, well before SK-V13 close.

**Defect:** V13 SYNTHESIS §"Honest patterns left clean" closed Pattern G CLEAN with the qualifier "that aren't legitimate refactors". V14 NEW-HIGH-1 records the `LegacyPath` alias as a shim "by every plain reading of the term." This either reopens Pattern G (V13 was wrong, the shim is not a legitimate refactor) or extends Pattern G (V13 didn't survey the alias surface, V14 does). Both readings are defensible. The V14 phrasing does not say which; an S-P3 reader cannot tell whether to treat this as a V13-disposition-correction or a V14-scope-extension.

**Severity:** REVISE (not REJECT). No row is silently regressed; the `LegacyPath` finding routes to C-1 PRUNE-4 (typed-path collapse) per A6 §4 line 184, which is within the existing slate. The defect is audit-trail clarity.

**Fold:** A6 §0 line 12 + §2 ledger row for `LegacyPath` add the disambiguation quoted in §1's row above. The C-1 coverage is unchanged.

### REVISE-3 — SYNTHESIS §2 sequencing-constraint inventory double-counts `css_pretty` evidence

**Source:** `SYNTHESIS-AUDIT-OVERFIT.md:209-267` (§2.1 R4 → PRUNE-2; §2.2 C-1 → C-4; §2.3 PRUNE-4 = 9 sub-waves); `SYNTHESIS-AUDIT-OVERFIT.md:184-207` (§1.3 Pattern H 64 → 67).

**Defect:** §2.3 ("PRUNE-4 sub-wave count is 9, not 8") and §1.3 ("Pattern H file count 64 → 67") are both consequences of the *same* post-V13 addition: the `css_pretty` grammar directory + its 7 files. A3, A5, A6 all independently re-derive the count from this same evidence, which the synthesis correctly frames as "cross-confirmation" but does not flag as evidentially co-derived. The S-P3 wave manifest reader could read the three confirmations as three independent regression signals and over-weight the risk of the `+1 sub-wave + +3 file` deltas.

**Severity:** REVISE (not REJECT). The PRUNE-4 = 9 sub-wave count IS correct; the Pattern H 64 → 67 count IS correct; the synthesis correctly converges on both. The defect is that the audit-trail surface presents them as independent confirmations when they are evidentially co-derived.

**Fold:** §1.3 closing sentence adds the co-derivation note quoted in §1's row above. The PRUNE-4 sub-wave plan stays at 9; no candidate or finding shifts.

### Non-findings worth recording

- **No silent reversal of any V13 REDRESS REJECT.** Verified: W11.2 + W13.5-9 (V13 "honest measured rejects") + W11.4 (V13 measured REJECT) all carry forward in the SK-V14 contract as PRE-BLOCK per SYNTHESIS §1.2 + HANDOFF §7 inheritance. No V14 NEW finding cites any of these waves; no V14 candidate proposes RE-ADMIT under R7 without honoring the PRE-BLOCK on same-shape reopens (verified by re-reading `SYNTHESIS.md` lines 211-217 honest rolling delta + §5 pre-blocks lines 298-299 on REDRESS-119/120 LIFTED handling, which extend the same pattern).
- **No silent reopen of any V13 audit-CLEAN reading.** Three V13 CLEAN readings are at risk on paper: (i) `json_provider` HONEST (V14 A4 NEW-1; addressed by REVISE-1), (ii) Pattern G CLEAN (V14 A6 NEW-HIGH-1; addressed by REVISE-2), (iii) `parse-that-regex` + `bbnf-simd` CLEAN under Lock 14 (V14 A3 DELTA-NOTE D1 explicitly preserves the CLEAN verdict and routes the cosmetic-naming concern to PRUNE-3 documentary-only; no reversal). With the two REVISEs folded, all three readings are coherently extended rather than reversed.
- **No orphan finding outside C-1..C-5.** Verified by re-walking the §3.1 coverage table: every one of the 74 findings maps to at least one C-N. The two §2.4 CH7-companion extensions (round-trip pairing + Lock-14 lint) are correctly framed as gating recommendations beyond the slate, not findings proper.
- **The 63/74 CONFIRMS rate IS the right number.** Per-axis sum: A1 8 + A2 7 + A3 29 + A4 4 + A5 3 + A6 3 = 54... pause: A3 carries 29 CONFIRMS (30 findings − 1 DELTA-NOTE not counted as NEW); A4 carries 4 CONFIRMS (16 findings − 12 NEW where the 12 = 3 enumerated NEW + 9 "quantification extensions of V13 findings" per §1.1 row A4); A2 has 7 CONFIRMS + 2 NEW (F8 + F9); A1 has 8 CONFIRMS + 0 NEW; A5 has 3 CONFIRMS + 1 NEW; A6 has 3 CONFIRMS + 4 NEW. 8 + 7 + 29 + 4 + 3 + 3 = 54... reconcile: the §1.1 census table counts "CONFIRMS" per the per-axis files' own classification, which counts FINDINGS not SUB-FINDINGS. A4's 4 CONFIRMS reads as 4 finding-classes (the 7 CSS providers as a class, etc.); the 9 "quantification extensions" extend rather than confirm-or-NEW. Net: 63 CONFIRMS is the per-axis-file headline, 11 NEW is the per-axis-file headline; the synthesis is using these headlines consistently. No defect in the headline; the per-axis-file definitions stand.
- **No new finding silently re-opens REDRESS-119 or REDRESS-120.** Verified by re-reading the V14 axis files for any cite of REDRESS-119 / REDRESS-120: zero hits. The R7 re-admit wave correctly inherits the LIFTED-but-HISTORY constraint via SYNTHESIS §5 pre-block 4 + HANDOFF §7 (carried verbatim from CH3 V1 alpha hardening REVISE-1's fold guidance).

## §3 — Recommended folds for V2

Three editorial-precision doc edits, all within the existing S-P0 audit scope. No new candidates; no new R-targets; no architectural changes.

### Fold F-1 (re-dispatch A4 axis author)

Edit `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-generator-truth.md` §0 NEW-1 paragraph (current lines 25-31) and §0 NEW-1 sub-bullet (lines 33-37) to add the V13-HONEST disambiguation:

> The V13 §7.1 row 1 'HONEST' verdict for `json_provider` correctly identified the grammar-derived `parse_direct` chunk (~70 LOC appended to `generated.rs` via `json_sink_direct::render` per `skinny/crates/codegen/src/lib.rs:215-217`); V14 extends the scope to the OTHER 5 emitted files (`config.rs`, `parser.rs`, `view.rs`, `value.rs`, `visitor.rs`) plus the include_str template body of `generated.rs` itself, which are pass-through templates with only the prepended `// @generated` header added. V14 does not reverse the V13 HONEST verdict on the sink-derived chunk; it observes that the chunk is the minority share (~15%) of the JSON provider's emitted bytes.

The same one-clause disambiguation folds into `SYNTHESIS-AUDIT-OVERFIT.md §1.2` item 4 (lines 104-112) to keep the synthesis-level NEW-1 enumeration coherent with the axis-level fold.

### Fold F-2 (re-dispatch A6 axis author)

Edit `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-pre-restart-pattern.md` §0 line 12 + §2 ledger row for `LegacyPath` (line 154) to add the V13-Pattern-G disambiguation:

> The V13 SYNTHESIS §"Honest patterns left clean" disposition 'No backwards-compat shims that aren't legitimate refactors (Agent F Pattern G)' did not enumerate the `LegacyPath` alias surface. V14 records this as a NEW finding rather than a reversal: the alias was introduced at `0e8dbc104 feat(runtime/parse-with-{json,css-l4,sheets,bbnf}): land W3.2 entry points (AZ-IV.W3.2)` and was either not surveyed by V13's Pattern G scan or treated as a transitional in-flight refactor; V14 surveys it explicitly and classifies it as a shim that should be collapsed inside C-1 PRUNE-4 (typed-path collapse) per A6 §4.

The same disambiguation folds into `SYNTHESIS-AUDIT-OVERFIT.md §1.2` item 8 (lines 146-155).

### Fold F-3 (re-dispatch S-P0 synthesis aggregator)

Edit `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md §1.3` closing sentence (current line 207) to add the `css_pretty` co-derivation note:

> The +3 file delta (64 → 67) and the PRUNE-4 sub-wave count delta (8 → 9) are both attributable to the single `css_pretty` grammar addition between V13 audit-pack landing and SK-V14 baseline; A3 §1, A5 §2.1, and A6 §1 independently re-derive the count from the same evidence, so the three confirmations are evidentially co-derived, not orthogonal. The S-P3 wave manifest's risk-weighting for PRUNE-4 should treat the `css_pretty` delta as one piece of evidence with three cross-checks, not three independent regression signals.

### Convergence note

All three folds are editorial-precision improvements, not substantive regressions. The ACCEPT-rate of 30/33 = 90.9% sits one fold-cycle below the §3Z 95% convergence floor — V2 can converge in a single fold cycle iff F-1 + F-2 + F-3 land cleanly. No CH3-level escalation flag.

No CH3 finding requires a new C-6 candidate, a new R-target, or a sequencing-constraint revision beyond what SYNTHESIS §2 already enumerates. The 63 CONFIRMS rows reproduce the V13 audit pack verifiably; the 11 NEW findings extend rather than reverse; the prune list maps 1:1 to C-1..C-5 with zero orphans; no candidate or finding silently reverses any prior REDRESS REJECT. The substance is CH3-clean; the V2 fold cycle is editorial only.
