# HARDENING-S-P2-V2-CONSOLIDATED
Date: 2026-05-17.
Pass: S-P2 Research - substrate-ceiling cohort.
Cycle: V2.

Cycle verdict: REVISE.
ACCEPT-rate: 0/6 (0%).
REJECT list: none.
REVISE list: CH1, CH2, CH3, CH4, CH5, CH6.
Convergence status: not converged. S-P2 does not satisfy the
`>=95% ACCEPT x 2` rule: V1 was 0/6 ACCEPT, V2 is 0/6 ACCEPT, and every V2
lens leaves unresolved REVISE work. Under `restart/prompts/ORCHESTRATOR.md:104-123`, S-P2 cannot advance to S-P3 until
the dispositions fold and the consecutive-ACCEPT criterion or explicit user pin
holds.

No SK-V8 implementation wave dispatch is authorized by this consolidation.
No S-P3 dispatch is authorized by this consolidation.

## Lens Disposition Table

| Lens | Disposition | Score | Basis |
|---|---:|---:|---|
| CH1 CORRECTNESS | REVISE | 82 | Comparator columns and sonic-rs architecture claims remain wrong enough to invalidate strict evidence (`hardening/V2/CH1.md:11-51`). |
| CH2 GENERALITY | REVISE | 86 | `StructuralAlphabet`, `facts`, and JSON-density selector language still leak grammar policy or global byte roles into generic posture (`hardening/V2/CH2.md:11-27`). |
| CH3 REGRESSION | REVISE | 84 | `parse_only` demotion, strict comparator refusal, residual labeling, and measured-path proof are not folded into executable gates (`hardening/V2/CH3.md:11-81`). |
| CH4 COST | REVISE | 76 | Tier A is underpriced and over-promises Tier B string-boundary deletion, non-JSON proof, same-wave consumers, and SC-5 gate work (`hardening/V2/CH4.md:12-100`). |
| CH5 HIDDEN COUPLING | REVISE | 84 | Low-density fallback, inconsistent consumers, broad facts, and `UnionTape` wording leave parallel-substrate/API apertures open (`hardening/V2/CH5.md:11-81`). |
| CH6 ANTI-PAPER-CLOSE | REVISE | 82 | V2 can still be mistaken for convergence, strict-win rhetoric remains mixed, and SC-4's knee metric is not executable (`hardening/V2/CH6.md:11-23`). |

## Blocking Findings By Fold Theme

### 1. Strict Evidence And Comparator Discipline

The V2 fold still violates strict-vs-strict comparator discipline. CH1 shows
that post-comparator delta columns are shifted: twitter is `-25.1%` versus
sonic-strict and `-35.8%` versus simdjson DOM, but SYNTHESIS and SC-5 label
`-35.8%` as sonic strict (`hardening/V2/CH1.md:11-31`;
`restart/skinny/tranches/sk-v8/SYNTHESIS.md:130-138`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-5-k-classification-adjudication.md:42-47`).
The same anchor drift mislabels citm and unicode evidence (`hardening/V2/CH3.md:51-66`; `restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-4-string-plane-gap.md:184-206`).

SC-1 and SC-5 still let non-strict rows wear strict-win language. CH6 flags
SC-1's mixed table of no-anchor, SK-V6, and sidecar evidence, and SC-5's list
of "faster than sonic-rs strict" rows that includes historical or sidecar-only
signals (`hardening/V2/CH6.md:16-18`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-1-offset-tape-teardown.md:99-114`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-5-k-classification-adjudication.md:42-51`).
This must be repaired before any strict admission claim because the governing
packet allows only same-run strict anchors for admission and treats sidecars as
planning signals (`restart/skinny/tranches/sk-v8/SPEC.md:44-55`;
`restart/prompts/skinny/PASS-2-RESEARCH.md:214-219`).

SC-4 also overclaims sonic-rs architecture. SC-2 cautions that sonic-rs should
be treated as skip-scan/single-pass without a persistent document-wide
structural index absent exact source proof (`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-2-two-stage-sota.md:28-43`,
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-2-two-stage-sota.md:148-164`),
while SC-4 still groups sonic-rs with simdjson as whole-document index evidence
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-4-string-plane-gap.md:116-139`).

### 2. `parse_only`, Gate Schema, And Measured-Path Regression

SC-5 correctly recommends demoting `parse_only` from the SOTA scoreboard,
keeping all residual deltas, adding a plane-matched `tape_vs_tape` telemetry
row, and making comparator plane an executable gate predicate
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-5-k-classification-adjudication.md:166-199`,
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-5-k-classification-adjudication.md:210-230`).
The packet has not folded that into the gate surface. SPEC still freezes the
outcome enum to `{A,C,G,K,L,N-direct}` and keeps the 17 `parse_only` rows as
candidate parse residuals (`restart/skinny/tranches/sk-v8/SPEC.md:57-73`,
`restart/skinny/tranches/sk-v8/SPEC.md:119-137`; `hardening/V2/CH3.md:11-29`; `hardening/V2/CH4.md:83-100`).

The strict comparator rule is described but not executable. W0 rejects missing
fields and malformed sidecars, but V2 does not require `gate-json` to refuse
strict admission on mismatched `comparator_plane`, non-strict
`comparator_strictness`, deferred strictness, or stale sidecar freshness
(`hardening/V2/CH3.md:30-49`;
`restart/skinny/tranches/sk-v8/SPEC.md:75-111`,
`restart/skinny/tranches/sk-v8/SPEC.md:264-282`). That leaves a route for
candidate rows to pass with validation, comparator, or sidecar work outside the
measured path.

W3 measured-path proof is likewise incomplete. SC-2 requires strict validation
to remain in the same path, SC-5 keeps `tape_vs_tape` as telemetry only until
same-run structural-index competitors exist, and SPEC says `tape_vs_tape` is
not a production same-wave consumer (`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-2-two-stage-sota.md:318-342`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-5-k-classification-adjudication.md:178-190`;
`restart/skinny/tranches/sk-v8/SPEC.md:405-410`). V2 still does not require W3
selected rows to prove UTF-8/control/escape validation, sparse tape facts, and
structural-index competitor work occur in the measured row (`hardening/V2/CH3.md:68-81`).

### 3. Lock 14 And Grammar-Neutral Substrate Semantics

V2 improved the old grammar-semantic names, but Lock 14 is still not clean.
CH2 identifies the remaining defect: SC-6 assigns an event class per structural
byte, which is too global for grammars where the same byte has different
parser-state meanings (`hardening/V2/CH2.md:11-16`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-6-lock1-amendment-generalisation.md:339-371`,
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-6-lock1-amendment-generalisation.md:399-465`).
SC-3's safer formulation is byte-set plus opaque class ordinals consumed by
generic code, with meaning interpreted in generated grammar modules
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:286-302`).

The `facts` lane is also still an escape hatch. SC-3 admits
"EventTape-required recovery and layout facts" without a closed neutral
vocabulary or owner matrix (`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:157-194`,
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:257-261`;
`hardening/V2/CH2.md:17-22`;
`hardening/V2/CH5.md:52-67`). As written, recovery or
layout policy for JSONL, indentation-sensitive grammars, CSS recovery, or user
grammars can be smuggled into generic tape/runtime code under neutral names,
violating SPEC's Lock 14 gate (`restart/skinny/tranches/sk-v8/SPEC.md:227-244`).

Finally, SC-1 and SC-4 still use JSON corpus shape as generic selector policy:
SC-1 preserves `OffsetTape` via recursive descent for low-quote-density rules,
and SC-4 uses the JSON string-fraction knee as a universal success criterion
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-1-offset-tape-teardown.md:362-364`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-4-string-plane-gap.md:307-342`;
`hardening/V2/CH2.md:23-27`). That is research signal,
not a grammar-neutral fast-path criterion.

### 4. Tier Scope, Cost, And Same-Wave Consumers

The W3 story still sells more than Tier A prices. SPEC and SC-2 describe a
lead hypothesis that deletes rediscovery of every structural byte and string
boundary and rewrites the parser as a class-column cursor-walker
(`restart/skinny/tranches/sk-v8/SPEC.md:392-403`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-2-two-stage-sota.md:296-316`).
SC-4's recommendation relies on quote/backslash/parity masks and string bounds
from the union cursor (`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-4-string-plane-gap.md:281-295`).
But SC-3 Tier A explicitly excludes quote/backslash/parity masks,
CostFacts/template parity, non-JSON grammars, and broader lowerer bodies
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:359-367`)
and routes string-index closure to Tier B or multi-wave work
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:421-437`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-4-string-plane-gap.md:365-370`;
`hardening/V2/CH4.md:12-35`).

Tier A touches generic substrate/codegen/SIMD surfaces but leaves the
non-JSON proof unpriced. SPEC requires generic edits to prove CSS L4, Sheets,
and BBNF-self do not need JSON structural roles (`restart/skinny/tranches/sk-v8/SPEC.md:227-244`), while SC-3 Tier A excludes
non-JSON grammars and budgets only scalar/checkasm parity, JSON runtime tests,
and a gate refresh (`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:405-419`;
`hardening/V2/CH4.md:37-57`).

The production consumer set is inconsistent. SC-2 and SC-3 describe direct and
SinkOnly consumption of the union, and SC-6 requires cursor, `ValueRef`, `path!`,
and touched retained-view/direct consumers to migrate in the same wave
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-2-two-stage-sota.md:307-316`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:262`;
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-6-lock1-amendment-generalisation.md:610-620`).
SC-3 Tier A only lists JSON retained `OffsetTape`, existing JSON `EventTape`
patching, `ValueRef::offset()`, and JSON `consume_structural` sites
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:361-367`;
`hardening/V2/CH4.md:58-81`;
`hardening/V2/CH5.md:30-50`).

### 5. Lock 1, Hidden Coupling, And No New Substrate

The core Lock 1 invariant is not yet airtight. SC-1 R2 says number-heavy
maintain failure may keep `OffsetTape` via recursive descent for low-density
rules through `derive_backend_shape`
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-1-offset-tape-teardown.md:360-364`).
CH5 correctly treats that as the old producer path surviving beside the union
candidate unless it is an explicit rejection or reroute outcome
(`hardening/V2/CH5.md:11-29`).

SC-6 still leaves a `UnionTape` node option in co-routed architecture text,
even though the same section says no new `BackendShape` variant and the union
is only representation replacement (`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-6-lock1-amendment-generalisation.md:301-312`,
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-6-lock1-amendment-generalisation.md:622-627`;
`hardening/V2/CH5.md:68-81`). That wording is a new
substrate/API aperture and must be deleted.

The accepted core remains: one producer, one retained `Tape`, no independent
`StructuralIndex`, no parser-owned cursor/facts, no parallel offset append path,
no new BBNF directive, no new BIR variant, and no new substrate surface
(`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:115-120`,
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-3-union-substrate-design.md:352-357`;
`restart/skinny/tranches/sk-v8/SPEC.md:430-436`). V3 must preserve that core and
remove the fallback/API apertures.

### 6. Anti-Paper-Close And Convergence Governance

V2 cannot advance S-P2. ORCHESTRATOR and PASS-2 require `>=95% ACCEPT` for two
consecutive cycles, with no unresolved REVISE, or an explicit user pin
(`restart/prompts/ORCHESTRATOR.md:118-123`;
`restart/prompts/skinny/PASS-2-RESEARCH.md:155-158`). V1 did not converge
(`hardening/HARDENING-S-P2-V1-CONSOLIDATED.md:5-7`), and V2 has six open REVISE
dispositions. HANDOFF still says the next framework step is CHALLENGE "then
S-P3 Synthesis-Plan" while also saying SC-1..SC-6 authorize no W3 plan
(`restart/skinny/tranches/sk-v8/HANDOFF.md:67-75`; `hardening/V2/CH6.md:11-15`). V3 must make the convergence guard explicit so a
future single ACCEPT cycle after V2 is not mistaken for convergence.

SC-4 also keeps an unmeasurable admission metric: moving the string-fraction
knee from about 0.14 "toward" 1.0 (`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-4-string-plane-gap.md:307-310`,
`restart/skinny/tranches/sk-v8/research/p2-substrate-ceiling/SC-4-string-plane-gap.md:372-375`;
`hardening/V2/CH6.md:20-23`). "Toward 1.0" is not a
pass/fail rule and cannot close a plane under the no-deferrals rule
(`restart/prompts/ORCHESTRATOR.md:211`).

## Required V3 Fold Actions

These actions are required, concrete, and non-deferrable. V3 must preserve
strict-vs-strict comparator discipline, grammar-neutral Lock 14, no new
directive/BIR/substrate, and no deferrals.

1. Correct every comparator-delta claim in SYNTHESIS, SC-1, SC-4, SC-5, and any
   packet summary. Recompute against the authoritative column order; split
   same-run sonic/serde strict anchors from simdjson/yyjson/RapidJSON sidecars
   and historical SK-V6 signals. Remove all non-strict "strict win" language,
   including the twitter, citm, unicode_escapes, numbers, marine_ik, and
   instruments misclassifications called out by CH1/CH3/CH6.

2. Rewrite SC-4's comparator-architecture section. Separate simdjson's retained
   document-wide stage-1 index from sonic-rs skip-scan/single-pass behavior, or
   add exact upstream source anchors proving the stronger sonic-rs claim. Until
   then sonic-rs may be used only as a same-run strict performance anchor and
   local skip-scan comparator, not as persistent-index architecture proof.

3. Fold SC-5 into SPEC, SYNTHESIS, and HANDOFF. Make `parse_only` a substrate-
   guard non-SOTA class or equivalent explicit non-admission state; keep every
   strict comparator delta and named residual visible; amend SPEC's outcome enum
   and Section 0.5 postures accordingly; and route `tape_vs_tape` only to W0/W1
   telemetry/gate-binding with owner paths, LOC, focused tests, comparator-
   plane refusal tests, and one gate refresh. `tape_vs_tape` must not count as
   W3's production same-wave consumer.

4. Add executable `gate-json` refusal rules: strict admission requires matching
   `comparator_plane`, accepted `comparator_strictness`, same-run freshness, and
   strict validation in the measured row. `Strictness=deferred`,
   `parse_utf8=view-boundary`, stale sidecars, C++ sidecar-only evidence, or
   plane mismatch may be guard telemetry only.

5. Revise SC-6 and SC-6-L1-R1 so generic code consumes only generated byte sets
   plus opaque structural-class ordinals. Event-role meaning must be interpreted
   inside generated grammar modules, keyed by parser state plus class/byte, so a
   reused punctuation byte is not forced into one global role. Keep Lock 14
   grammar-neutral: no generic JSON roles, no public generic grammar API, no
   grammar-name branches, no new directive.

6. Replace SC-3's broad `EventTape-required recovery/layout facts` allowance
   with either a closed neutral fact vocabulary or opaque generated fact ids.
   Add a fact admission matrix with fact name/id, producer, consumer, owner file,
   cursor domain, lifetime, and challenge gate. Include JSONL record-boundary
   and indentation-sensitive examples proving generic runtime code does not
   encode JSON policy.

7. Replace SC-1 and SC-4 JSON-density selector language. "Low-quote-density"
   and the universal 0.14-to-1.0 knee are not generic fast-path policy. Express
   them as per-grammar CostFacts/RecognizerFacts telemetry and per-plane gates.
   If SC-4 keeps a knee metric, it must name an in-repo command, row set,
   numeric target, formula, maintain budget, and pass/fail rule; otherwise it is
   diagnostic only.

8. Split the W3 hypothesis everywhere into two named scopes: Tier A structural-
   class cursor migration, and Tier B string-boundary / quote-backslash-parity /
   CostFacts-template union. Tier A must not claim it stops re-walking strings,
   deletes string-boundary work, or closes the string plane unless it includes
   the Tier B work and fits SPEC's 650-LOC template-parity cap plus verification
   budget.

9. Add an S-P3-ready W3 cost and owner table for Tier A before any later W3
   challenge can consume it: exact files, source LOC, generated-output audit,
   strict row/plane targets, same-wave production consumer, named parser tests,
   exact scalar oracle and checkasm commands, full-gate rerun budget, revert
   slice, and explicit touched/proven-untouched rows for `path!`, retained view,
   direct/SinkOnly, generated Track 1, and Track 2. Price the SPEC Section 2.1
   CSS L4, Sheets, and BBNF-self proof inside Tier A if generic crates change.

10. Repair Lock 1 hidden coupling. Rewrite SC-1 R2 so number-heavy maintain
    failure rejects or reroutes the union candidate; it must not preserve an
    `OffsetTape`-via-recursive-descent producer inside the same retained-index
    design. Delete SC-6's `UnionTape node` option. The only admitted fold is
    representation replacement of `OffsetTape` and retained `EventTape`; no new
    substrate node, BackendShape variant, BIR variant, BBNF directive, public
    substrate type, or old offset append constructor may survive.

11. Align SC-2, SC-3, SC-6, SPEC, and HANDOFF on the Tier A production consumer
    set. Either remove direct/SinkOnly/path claims from Tier A or add exact
    owner paths and same-wave verification for `path!`, direct/SinkOnly
    generated loops, retained view, cursor, and `ValueRef`. The invariant is one
    producer, one retained `Tape`, no parser-owned facts/cursors, and no
    telemetry-only row counted as a production consumer.

12. Add an explicit S-P2 convergence guard to SYNTHESIS/SPEC/HANDOFF and the
    next S-P2 dispatch. A V3 ACCEPT cycle, if it happens, is only the first
    possible ACCEPT cycle after V2 REVISE unless the user explicitly pins S-P2
    final. S-P3 remains blocked until `>=95% ACCEPT` holds for two consecutive
    cycles with zero open critical defects and no unresolved REVISE, or the user
    pins final.

## Cycle Disposition

V2 hardening must fold into V3 before any S-P3 dispatch. Hardening without
folding is paper-hardening under `restart/prompts/ORCHESTRATOR.md:112-123`.

No SK-V8 implementation wave dispatch is authorized. G-Alpha remains the only
packet-level decision point, and even a closed G-Alpha authorizes W0 only under
the current SK-V8 packet (`restart/skinny/tranches/sk-v8/SYNTHESIS.md:239-245`;
`restart/skinny/tranches/sk-v8/SPEC.md:534-541`;
`restart/skinny/tranches/sk-v8/HANDOFF.md:5-7`).
