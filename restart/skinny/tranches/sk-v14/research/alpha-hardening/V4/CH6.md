# CH6 ANTI-PAPER-CLOSE — Pass Alpha V4 Disposition

Lens authority: `restart/prompts/ORCHESTRATOR.md:88` (CH6 row);
`restart/skinny/tranches/sk-v14/research/alpha-hardening/V1/CHALLENGE-CONTEXT.md:164-176`
(disposition focus); `restart/skinny/tranches/sk-v14/research/alpha-hardening/V2/CHALLENGE-V2-ADDENDUM.md`
(overlay binding); V4 cycle binding per the SK-V14 orchestrator
dispatch — V4 is the micro-repair pass after V3 NOT-CONVERGED on two
CH1 orphan REVISEs against V3-fold defects, landed at commit
`5e00b6d270844300c4d28a4ff1915030601b5fe7`.

CH6 V3 disposition under inheritance: `V3/CH6.md` — 100 % (42/42); 0
REJECT; 0 REVISE; zero new findings; the CH6 recurrence vector closed
in three layers at V3 close (V2 F-2 attribution declaration + V2 F-9
triumvirate constraint binding + V3 commit `5e2ae78b4` per-agent fold
attribution).

V4 overlay verifies two surfaces:

- **V2 + V3 baseline preservation.** The five CH6-routed V2 anchors
  (F-1 numeric reconciliation; F-2 sole-author HANDOFF §4; F-8
  G-Omega chain; F-9 thirteenth triumvirate constraint; E-5 hot-leaf
  naming) plus the two V3-NEW CH6-CLEAN folds (F-V3-α-E-1 round-trip
  gate metadata-derivation; F-V3-α-F-1 carry-over guard broadening)
  must read functionally-equivalent at V4 HEAD vs the V3-converged
  commit `5e2ae78b4`.
- **V4 delta CH6 character.** The two V4 micro-folds (F-V4-α-E-1
  cargo-metadata|jq command repair with executable verification;
  F-V4-α-F-1 HANDOFF §7 citation anchor repair) must introduce no
  paper-close pattern, no "documented" / "planned" / "future-wave"
  deferral, no attribution gap. The V4 atomic commit `5e00b6d27`
  itself is a strong anti-paper-close exemplar: V3's broken-but-
  plausible-looking `.workspace_metadata...|keys[]` was paper-perfect
  but execution-broken (returned jq `null`); V4 fixed it by mandatory
  executable verification AND the agent's commit body shows the actual
  command output ("VERIFIED executable: enumerates 9 grammars (bbnf,
  json, css_l4, css_pretty, google_sheets, ebnf, bnf, csv, math)
  against live workspace"). This is the discipline pattern the V3
  aggregator's methodological note recommends institutionalizing.

## §0 — Disposition summary

- ACCEPT-rate: **100 %** (42/42 dispositioned §-rows; V2 + V3 baseline
  inherited untouched on every CH6-routed cell; both V4 micro-fold
  sites CH6-clean; zero new findings).
- REJECT count: 0.
- REVISE count: 0.
- Critical findings: 0.

V2 + V3 baseline preservation roll-up:

| Anchor | Site | V4 status | Evidence |
|---|---|---|---|
| F-1 numeric reconciliation | `SYNTHESIS.md:73-84 + :194-195 + :200-209` and `HANDOFF.md:79-89` | **HELD verbatim** | V4 commit `5e00b6d27` touches only `HANDOFF.md:196` (citation correction inside §7) and `alpha-E:362-388` (jq command repair); the F-1 SYNTHESIS § sites are not in the diff. F-1 numeric reconciliation paragraph at `SYNTHESIS.md:73-84` reads "+2 direct extension rows (**marine_ik**, **instruments**) and +4 typed extension rows (**random** W13.3, **instruments** W13.4, **numbers** W13.1, **unicode_basic** W13.2, with **update_center** adjusted under W15.1)" exactly as V2/V3 landed. |
| F-2 sole-author attribution | `HANDOFF.md:118-127` | **HELD verbatim** | The V4 diff does not touch lines 118-127. The block reads "Pass Alpha V1 α-A / α-C / α-D / α-F artefacts in tree were authored by α-F ... per CH6 §2.2 REJ-1 disposition (b) (CONSOLIDATED §2.1 F-2). CHALLENGE V1 caught the divergence; V2 reconciles." V4 commit body extends the per-cycle attribution pattern by explicitly naming α-E as F-V4-α-E-1 owner and α-F as F-V4-α-F-1 owner. |
| F-8 G-Omega chain | `HANDOFF.md:159-160` + hard-cap echo `:162-165` | **HELD verbatim** | The V4 diff does not touch lines 159-165. Next-move chain reads `ready-for-CHALLENGE-V1 → G-Alpha → S-P0 → S-P1/S-P2/S-P3 ∥ Pass Omega → G-Omega → Wave 0 (PRUNE-1)` exactly as V2/V3 landed. |
| F-9 thirteenth triumvirate constraint | `SYNTHESIS.md:342-344` | **HELD verbatim** | V4 commit does not touch SYNTHESIS.md at all. The clause reads "every wave fans out as research → plan → redress in distinct commits per `[triumvirate-discipline]` + ORCHESTRATOR §8; a wave that lands a single research-plan-redress mega-commit fails the gate at S-P3." V4's commit `5e00b6d27` is itself a single atomic two-file micro-fold commit (NOT a wave), so the triumvirate constraint is preserved for downstream S-P3 wave dispatch — the constraint binds wave-level execution, not cycle-level hardening micro-fold generation. |
| E-5 hot-leaf naming | `alpha-E:531-547` | **HELD verbatim** | The V4 diff window on alpha-E is lines 362-388 (§5 C-3 round-trip gate); §6 falsifiability gate is untouched. Pre-wave row binding to `json/numbers/direct_to_struct/main`, post-wave W11.1-emitted symbol naming "at S-P3 dispatch time, not after redress", and module-path discipline (`runtime::ext::` / `runtime::sidecar::` / `runtime::union::` / `runtime::cursor::` ⇒ REJECT pre-emptively) all hold. |
| F-V3-α-E-1 (V3 CH6-clean) round-trip gate metadata-derivation | `alpha-E:362-388` | **STRENGTHENED, NOT REGRESSED** | V3 introduced metadata-derived enumeration; V4 repaired the jq schema path (`.metadata.bbnf.grammars[].ident` is correct vs V3's `.workspace_metadata.bbnf.grammars \| keys[]` which returned jq null), added `--no-deps`, added `git diff --exit-code`, and removed the stale 8-grammar parenthetical. The intent (metadata-derivation as the single source of truth for grammar enumeration) is preserved; the mechanical executability of the gate is strengthened from "looks correct but broken" to "actually executes against live workspace producing 9 grammars". |
| F-V3-α-F-1 (V3 CH6-clean) carry-over guard broadening | `HANDOFF.md:192-197` | **STRENGTHENED, NOT REGRESSED** | V3 broadened the carry-over guard from 41 to 47 rows; V4 corrected the citation anchor at `HANDOFF.md:195-196` from `SYNTHESIS.md §1.3` (post-PRUNE rolling delta 0/17, wrong target) to `SYNTHESIS.md §0.2` reconciliation block (lines 73-84, actual per-row breakdown source). The 47-row guard population is preserved; the strict-subset disclosure is preserved; the citation now points to the SOURCE of the per-row breakdown rather than the rolling delta downstream of it. |

V4 delta CH6-character roll-up:

| V4 fold | V4 site | CH6 character | Evidence |
|---|---|---|---|
| F-V4-α-E-1 | `alpha-E:362-388` | **CH6-CLEAN, EXEMPLAR** | The V4 jq path repair is the textbook inverse of paper-close. V3's `.workspace_metadata.bbnf.grammars \| keys[]` was paper-perfect (looked plausible, cited correct authority `LOCKS.md:220`) but execution-broken (returned jq `null`). V4 corrects to schema-correct `.metadata.bbnf.grammars[].ident`, adds `--no-deps`, adds `git diff --exit-code`, and removes the stale "currently `{json, css_l4, google_sheets, bbnf, csv, ebnf, bnf, math}`" 8-grammar parenthetical so the metadata-derived enumeration is the single source. The V4 commit body explicitly carries the executable verification output ("enumerates 9 grammars (bbnf, json, css_l4, css_pretty, google_sheets, ebnf, bnf, csv, math) against live workspace") — measured evidence, not paper claim. The phrasing change from "adding a 9th grammar" to "admitting an additional grammar" makes the gate roster-count-agnostic. CH6 character: stronger gate (now actually runs), not weaker. |
| F-V4-α-F-1 | `HANDOFF.md:195-196` | **CH6-CLEAN** | Single-line citation correction from `SYNTHESIS.md §1.3` to `SYNTHESIS.md §0.2` reconciliation block (lines 73-84). The V4 commit body declares the correction was "Verified by reading both candidate sections; sec1.2 back-references sec0.2 confirming sec0.2 is source" — measured verification of citation correctness, not paper claim. The 47-row carry-over guard population, the strict-subset disclosure, and the wider-ledger language all hold. CH6 character: stronger refusal (now cites the source containing the per-row breakdown), not weaker. |

V4 converges at the 100 % ceiling forecast for CH6. The
two-consecutive-cycle convergence chain (V2 → V3 → V4) is now bound
by three CH6-clean cycles. The V4 cycle further institutionalizes the
executable-verification discipline the V3 aggregator's methodological
note prescribed.

## §1 — Per-artefact disposition table

V3 dispositions inherited verbatim where V4 left the site untouched
(per V2 addendum §1 + V3 dispatch addendum + V4 cycle binding, every
V4-untouched cell holds its V3 ACCEPT). Rows marked V4-NEW are sites
the V4 micro-fold edited.

| Artefact | § | V3 disp. | V4 disp. | Reason |
|---|---|---|---|---|
| SYNTHESIS.md | §0 Authority | ACCEPT | ACCEPT | Untouched at V4; V3 holds. |
| SYNTHESIS.md | §0.1 Close condition | ACCEPT | ACCEPT | Untouched; R10 verbatim. |
| SYNTHESIS.md | §0.2 Goalset row enumeration | ACCEPT (F-1 FOLD-LANDED) | ACCEPT | Untouched at V4; reconciliation paragraph at `:73-84` holds verbatim. |
| SYNTHESIS.md | §0.3 R-target goalset | ACCEPT | ACCEPT | Untouched. |
| SYNTHESIS.md | §0.4 Pre-blocked routes P-1..P-7 | ACCEPT | ACCEPT | Untouched. |
| SYNTHESIS.md | §0.5 Wave-by-wave gates (deferred) | ACCEPT | ACCEPT | Untouched; CONTRACTED deferral per PASS-ALPHA §4.4 holds. |
| SYNTHESIS.md | §1.1 Pillars table | ACCEPT | ACCEPT | Untouched. |
| SYNTHESIS.md | §1.2 Does not survive table | ACCEPT (F-1 FOLD-LANDED) | ACCEPT | Untouched at V4; rows at `:194-195` + reconciliation paragraph at `:200-209` hold verbatim. |
| SYNTHESIS.md | §1.3 Honest rolling delta | ACCEPT | ACCEPT | Untouched at V4. (Note: V4 F-V4-α-F-1 in HANDOFF §7 corrects a citation that previously pointed to §1.3 to instead point to §0.2 — the §1.3 content itself is unchanged; the V4 fold relocates an upstream pointer to the correct target.) |
| SYNTHESIS.md | §2 Telemetry binding | ACCEPT | ACCEPT | Untouched. |
| SYNTHESIS.md | §3 Candidate shortlist | ACCEPT | ACCEPT | Untouched. |
| SYNTHESIS.md | §4 S-P3 constraints | ACCEPT (F-9 FOLD-LANDED) | ACCEPT | Untouched at V4; thirteenth triumvirate constraint at `:342-344` holds verbatim. |
| SYNTHESIS.md | §5 Pre-blocked + unblocked | ACCEPT | ACCEPT | Untouched. |
| SYNTHESIS.md | §6 Close posture | ACCEPT | ACCEPT | Untouched. |
| HANDOFF.md | §1 Bracket verdict | ACCEPT | ACCEPT | Untouched. |
| HANDOFF.md | §2 Authority list | ACCEPT | ACCEPT | Untouched. |
| HANDOFF.md | §3 Honest baseline | ACCEPT (F-1 FOLD-LANDED) | ACCEPT | Untouched at V4; reconciliation at `:79-89` holds verbatim. |
| HANDOFF.md | §4 Pre-S-P0 readiness | ACCEPT (F-2 FOLD-LANDED) | ACCEPT | Untouched at V4; sole-author declaration at `:118-127` holds verbatim. V4 commit `5e00b6d27` extends the per-cycle attribution pattern by explicitly naming α-E (F-V4-α-E-1) and α-F (F-V4-α-F-1) as V4 fold owners in the commit body — closing any residual V1 staging-race language at the V4 generation. |
| HANDOFF.md | §5 Pass sequence | ACCEPT | ACCEPT | Untouched. |
| HANDOFF.md | §6 Next-move | ACCEPT (F-8 FOLD-LANDED) | ACCEPT | Untouched at V4; next-move chain at `:159-160` + hard-cap echo at `:162-165` hold verbatim. |
| HANDOFF.md | §7 Refusal conditions | ACCEPT (V3-NEW, CH6-CLEAN) | **ACCEPT (V4-NEW, CH6-CLEAN)** | F-V4-α-F-1 at `:195-196` corrects the citation anchor from `SYNTHESIS.md §1.3` (wrong target — post-PRUNE rolling delta 0/17) to `SYNTHESIS.md §0.2` reconciliation block (lines 73-84, the actual dispatch-vs-ledger reconciliation source). The 47-row carry-over guard population is preserved; the strict-subset disclosure is preserved; the wider-ledger framing is preserved. The V4 commit body verifies the correction by reading both candidate sections. CH6 character: stronger refusal (now cites the section containing the per-row breakdown), not weaker. |
| HANDOFF.md | §8 V1 disposition | ACCEPT | ACCEPT | Untouched; PENDING framing remains contracted, not paper-closed. |
| alpha-A | §0–§7 | ACCEPT | ACCEPT | STAND per V4 dispatch (alpha-A untouched at V4); V3 dispositions inherited. |
| alpha-B | §0–§8 | ACCEPT | ACCEPT | STAND per V2 + V3 + V4 inheritance. |
| alpha-C | §1–§4 | ACCEPT | ACCEPT | STAND per V4 dispatch (alpha-C untouched at V4); V3 dispositions inherited. |
| alpha-D | §1–§6 | ACCEPT | ACCEPT | STAND per V2 + V3 + V4 inheritance. |
| alpha-E | §0–§4 + §7–§11 | ACCEPT | ACCEPT | Untouched at V4 outside §5 round-trip gate. |
| alpha-E | §5 C-3 round-trip gate (BINDING expansion site) | ACCEPT (V3-NEW, CH6-CLEAN) | **ACCEPT (V4-NEW, CH6-CLEAN, EXEMPLAR)** | F-V4-α-E-1 at `:362-388` repairs the V3-introduced cargo-metadata|jq enumeration command. V3's `.workspace_metadata.bbnf.grammars \| keys[]` was schema-incorrect (returned jq null against live workspace); V4 corrects to `.metadata.bbnf.grammars[].ident`, adds `--no-deps`, adds `git diff --exit-code`, removes the stale 8-grammar parenthetical, and generalises "9th grammar" phrasing to "admitting an additional grammar". The metadata-derivation intent (V3 CH6-clean fold) is preserved; the gate's mechanical executability is upgraded from "looks correct" to "actually executes against live workspace producing 9 grammars (bbnf, json, css_l4, css_pretty, google_sheets, ebnf, bnf, csv, math)". The V4 commit body carries the actual command output as measured evidence — the textbook inverse of paper-close. The bypass-header detector clause at `:388-396` is untouched. |
| alpha-E | §6 C-4 falsifiability gate | ACCEPT (E-5 FOLD-LANDED) | ACCEPT | Untouched at V4; pre-wave + post-wave hot-leaf naming at `:531-547` holds verbatim. |
| DISPATCH-CONTEXT.md | §0–§4 | ACCEPT | ACCEPT | STAND per V2 + V3 + V4 inheritance. |

## §2 — V2 + V3 baseline preservation (binding overlay)

Per the V4 dispatch addendum, the V4 micro-repair pass verifies the
V2 + V3 100 % CH6 baseline holds with no regression. The five
CH6-routed V2 anchors plus the two V3-NEW CH6-clean fold sites were
re-read at V4 HEAD; all hold functionally-equivalent (the two
V3-introduced fold sites were strengthened by V4, not regressed).

### V2 F-1 (numeric divergence reconciliation): HELD verbatim

The three V2 sites untouched by V4:

- **SYNTHESIS §0.2** at `SYNTHESIS.md:73-84` — the reconciliation
  paragraph headed "Numeric-divergence reconciliation (per CH6 §2.2
  REJ-2)" reads identically to V2/V3: dispatch-context 4+7 vs α-A/α-D
  6+11 surfaced; +2 direct extension rows (marine_ik, instruments) and
  +4 typed extension rows (random W13.3, instruments W13.4, numbers
  W13.1, unicode_basic W13.2, with update_center W15.1 adjusted) named;
  v6 §1 rows 3-4 AUDIT-FALSIFIED classification preserved; PRUNE-1
  ledger revert bound to the wider 6+11 population.
- **SYNTHESIS §1.2** at `SYNTHESIS.md:194-195` (table rows) +
  `:200-209` (reconciliation paragraph) — table rows read "4 JSON
  direct admits (dispatch §1) / 6 under broader ledger" and "7 JSON
  typed admits (dispatch §1) / 11 under broader ledger"; reconciliation
  paragraph cites `α-A:117-122` + `α-A:161-169` + `α-D:281-291` +
  `α-D:353-368`.
- **HANDOFF §3** at `HANDOFF.md:79-89` — combined-entry bullet reads
  the wider 47-row population with named extension rows.

The numeric divergence remains surfaced and bound to the wider revert
scope; no paper-close pattern re-introduced. V4 F-V4-α-F-1 corrects a
DOWNSTREAM citation anchor in §7 to point at this very §0.2
reconciliation — reinforcing the F-1 anchor as the canonical
reconciliation site.

### V2 F-2 (HANDOFF §4 sole-author attribution): HELD verbatim

`HANDOFF.md:118-127` reads exactly as V2/V3. The V4 commit
`5e00b6d27` extends the per-cycle attribution discipline by explicitly
naming α-E as F-V4-α-E-1 owner and α-F as F-V4-α-F-1 owner in the
commit body, with per-fold rationale citing the originating V3
finding (CH1 V3 REV-1; CH1 V3 REV-2) and the V3 CONSOLIDATED §2
prescription. The V4 micro-fold attribution is therefore fresh
per-agent and atomic — the V1 staging-race language closure now binds
three cycles (V2 + V3 + V4) of fresh per-cycle per-agent attribution.

### V2 F-8 (HANDOFF §6 G-Omega next-move chain): HELD verbatim

`HANDOFF.md:159-160` reads exactly as V2/V3:

> "**Next-move:** `ready-for-CHALLENGE-V1 → G-Alpha → S-P0 →
> S-P1/S-P2/S-P3 ∥ Pass Omega → G-Omega → Wave 0 (PRUNE-1)`."

The G-Omega gate, S-P1/S-P2/S-P3 ∥ Pass Omega parallelism, and
PRUNE-1 wave anchor remain in the operationally consulted line.
F-7's hard-cap echo at `:162-165` (30-min lens / 20-15-30 R-P-R
cadence; 45-min C-4 uplift) is also untouched. No downstream agent
reading the one-liner can dispatch past G-Omega.

### V2 F-9 (SYNTHESIS §4 thirteenth constraint): HELD verbatim

`SYNTHESIS.md:342-344` reads exactly as V2/V3:

> "every wave fans out as research → plan → redress in distinct
> commits per `[triumvirate-discipline]` + ORCHESTRATOR §8; a wave
> that lands a single research-plan-redress mega-commit fails the
> gate at S-P3."

The S-P3 binding remains intact. The V4 commit `5e00b6d27` is itself
a single atomic two-file micro-fold commit, NOT a wave — the
triumvirate-discipline constraint binds wave-level execution
(research/plan/redress), not the cycle-level hardening micro-fold
generation. The V4 commit body provides per-fold rationale internally
(F-V4-α-E-1 with executable verification of the repaired command;
F-V4-α-F-1 with citation cross-read verification), discharging the
intent of the constraint for cycle-level work. The constraint is
preserved verbatim for downstream S-P3 wave dispatch.

### V2 E-5 (α-E §6 C-4 hot-leaf naming): HELD verbatim

`alpha-E-candidate-shortlist.md:531-547` reads exactly as V2/V3:
pre-wave row binding to one of (a) `RESULTS.md` Hot-leaf column for
`json/numbers/direct_to_struct/main` reading `parse_value_at` OR (b)
v2 §3.1 numeric-array dispatch trace + W11.1 commit SHA. Post-wave
naming of the W11.1-emitted number-specialised symbol "at S-P3
dispatch time, not after redress". Hot leaf unchanged ⇒ REJECT;
module path under `runtime::ext::` / `sidecar::` / `union::` /
`cursor::` ⇒ REJECT pre-emptively. The gate is still mechanically
binary; the lens still pre-verifies the symbol pair.

### V3 F-V3-α-E-1 (round-trip gate metadata-derivation): STRENGTHENED, NOT REGRESSED

The V3-introduced metadata-derivation intent is preserved at
`alpha-E:362-388`. V4 F-V4-α-E-1 repairs the broken jq command body:
V3's `.workspace_metadata.bbnf.grammars | keys[]` was schema-incorrect
(against live workspace, that path returns `null`); V4 corrects to
`.metadata.bbnf.grammars[].ident` (schema-correct against
`cargo metadata --format-version 1 --no-deps` output), adds
`--no-deps` (avoids dependency-graph noise), adds `git diff --exit-code`
(makes the round-trip assertion machine-checkable rather than visual),
and removes the stale 8-grammar parenthetical so the metadata-derived
enumeration is the sole source-of-truth for grammar count at the gate
site. The V4 commit body carries the actual command output —
"enumerates 9 grammars (bbnf, json, css_l4, css_pretty, google_sheets,
ebnf, bnf, csv, math) against live workspace" — as measured evidence
that the repaired command works. Cross-reference to C-1's forward
invariant at `alpha-E:170-176` is preserved. Citation of `LOCKS.md:220`
as the workspace-metadata clause Lock 14 names is preserved. CH6
binding strengthened: the gate's executable surface upgrades from
"looks correct but broken" to "actually executes against live
workspace producing the expected output".

### V3 F-V3-α-F-1 (HANDOFF §7 carry-over guard broadening): STRENGTHENED, NOT REGRESSED

The V3-introduced 47-row carry-over guard population is preserved at
`HANDOFF.md:192-197`. V4 F-V4-α-F-1 corrects the single citation
anchor on line 195-196 from `SYNTHESIS.md §1.3` (post-PRUNE rolling
delta 0/17, wrong target — that section contains the post-PRUNE
baseline projection, not the per-row reconciliation breakdown) to
`SYNTHESIS.md §0.2` reconciliation block (lines 73-84, the actual
dispatch-vs-ledger reconciliation block carrying the per-row
breakdown). The 47-row guard population, the strict-subset disclosure
("V1 dispatch §1 narrower bind of 4 direct + 7 typed is a strict
subset"), and the wider-ledger framing all hold. The V4 commit body
explicitly verifies the citation correction: "Verified by reading both
candidate sections; sec1.2 back-references sec0.2 confirming sec0.2 is
source." CH6 binding strengthened: the refusal guard now cites the
section containing the per-row breakdown evidence rather than the
downstream rolling delta projection.

## §3 — V4 delta fresh-finding scan

CH6 ANTI-PAPER-CLOSE fresh scan across the V4 delta (per V4 dispatch
overlay). Sites scanned: every line added or modified by the
F-V4-α-E-1 + F-V4-α-F-1 fold packets per `git diff
5e2ae78b4..5e00b6d27 -- restart/skinny/tranches/sk-v14/HANDOFF.md
restart/skinny/tranches/sk-v14/research/alpha/alpha-E-candidate-shortlist.md`.

**Result: zero new findings.**

The V4 micro-folds preserve CH6 character throughout — in fact
strengthen it. Specifically:

- **F-V4-α-E-1 cargo-metadata|jq repair** (`alpha-E:362-388`) is the
  textbook inverse of paper-close and a strong anti-paper-close
  exemplar. V3's `.workspace_metadata.bbnf.grammars | keys[]` was
  paper-perfect (looked plausible, cited correct authority
  `LOCKS.md:220`) but execution-broken (returned jq `null` against
  the live workspace — verifiable by running the V3 command directly).
  V4 fixed it via mandatory executable verification AND the agent's
  commit body shows the actual command output ("VERIFIED executable:
  enumerates 9 grammars (bbnf, json, css_l4, css_pretty,
  google_sheets, ebnf, bnf, csv, math) against live workspace"). The
  V4 corrections (`.metadata.bbnf.grammars[].ident`, `--no-deps`,
  `git diff --exit-code`, removal of 8-grammar parenthetical, "admitting
  an additional grammar" generalisation) are all mechanically motivated
  by what the live workspace produces. This is precisely the discipline
  pattern the V3 aggregator's methodological note recommends
  institutionalizing (V3 CONSOLIDATED §1.2): "executable-verification
  dispatch for any shipped shell command". CH6 binding satisfied — the
  fold introduces measured evidence, not paper claim; the gate becomes
  more mechanical, not less.

- **F-V4-α-F-1 citation anchor correction** (`HANDOFF.md:195-196`) is
  a single-line citation repair from `SYNTHESIS.md §1.3` to
  `SYNTHESIS.md §0.2` reconciliation block (lines 73-84). The V4
  commit body declares the correction was "Verified by reading both
  candidate sections; sec1.2 back-references sec0.2 confirming sec0.2
  is source" — measured cross-read verification of citation
  correctness, not paper claim. The 47-row carry-over guard
  population, the strict-subset disclosure, and the wider-ledger
  framing all hold. The fold relocates the refusal guard's evidence
  pointer to the canonical reconciliation source. CH6 binding
  satisfied — the refusal guard cites the actual per-row evidence
  source.

- **V4 commit body attribution discipline.** The atomic commit
  `5e00b6d27` explicitly names α-E as F-V4-α-E-1 owner and α-F as
  F-V4-α-F-1 owner, with per-fold rationale citing the originating V3
  CH1 finding (CH1 V3 REV-1; CH1 V3 REV-2). The commit declares
  "Other files STAND: SYNTHESIS, alpha-A, alpha-B, alpha-C, alpha-D,
  DISPATCH-CONTEXT, V2-DISPATCH-CONTEXT, V3 hardening files,
  ORCHESTRATOR-PROMPT" — every untouched artefact named, no
  implicit-edit risk. The closing-paragraph forward pointer
  ("V5 confirming pass at the V max=5 §3Z ceiling closes the bracket;
  SK-V14 contract locks at V5 convergence") is a forecast bound to a
  measurable test (per-lens disposition output across V5 lens
  dispatch), not a paper-close.

- **The V3 aggregator's methodological note carries forward.** The
  V4 commit body explicitly cites the V3 aggregator's methodological
  recommendation ("V3's lens-depth divergence (CH7 documentary OK vs
  CH1 executable BROKEN) recommends explicit executable-verification
  dispatch for any shipped shell command") and reports that the V4
  α-E redispatch carried this mandate ("V4 alpha-E dispatch carried
  this mandate and produced verified output") and that V4 CHALLENGE
  lenses will inherit the same mandate ("V4 CHALLENGE lenses will
  inherit the same mandate"). This is the institutionalization the
  V3 aggregator prescribed: executable verification is now a binding
  fold-discipline at V4 close. CH6 binding strengthened.

- **No deferral outside the contracted PASS-ALPHA §4.4 boundary**
  appears anywhere in the V4 delta. Grep over `git diff
  5e2ae78b4..5e00b6d27 -- restart/skinny/tranches/sk-v14/HANDOFF.md
  restart/skinny/tranches/sk-v14/research/alpha/alpha-E-candidate-shortlist.md`
  for paper-close patterns (`planned` / `designed` / `future-wave` /
  `future tranche` / `to be` / `will be` / `complete` / `wired` /
  `verified` / `deferred` / `outstanding` / `TBD` / `TODO` / `FIXME` /
  `stub` / `shim` / `fallback`) returns zero matches in additions
  (`+` lines that are not file-mode markers). The V4 deltas are pure
  tightenings: command repair (mechanical) + citation correction
  (factual).

- **No agent self-report of "complete" / "wired" / "verified"
  unsubstantiated** appears anywhere in the V4 delta. The
  F-V4-α-E-1 fold's repaired command is mechanically falsifiable (the
  shell loop runs and produces empty `git diff --exit-code` output
  for every iterated grammar OR exits non-zero); the F-V4-α-F-1
  fold's corrected citation anchor is verifiable by reading both
  candidate sections (the V4 commit body declares this cross-read was
  performed and reports the result).

- **The CH6 recurrence vector** (V1 staging-race paper-close attested
  in V1 CH6 REJ-1) remains closed in four layers at V4 close:
  (i) V2 F-2 declares α-F sole V1 author of α-A / α-C / α-D and the
  SYNTHESIS + HANDOFF pair; (ii) V2 F-9 encodes triumvirate-distinct-
  commit discipline as a binding S-P3 constraint; (iii) V3 commit
  `5e2ae78b4` provides fresh per-agent attribution for both V3
  micro-folds in the commit body itself; (iv) V4 commit `5e00b6d27`
  provides fresh per-agent attribution for both V4 micro-folds AND
  carries the V3 aggregator's executable-verification mandate
  forward. The CH6 lens has no remaining surface to challenge.

- **The contracted §4.4 deferral** (SYNTHESIS §0.5) and the
  contracted-pending §8 V1 disposition (HANDOFF §8) remain CH6-clean
  per V2 + V3 + V4 disposition inheritance; both are explicitly
  deferred under PASS-ALPHA §4.4 boundary + ORCHESTRATOR §3Z
  convergence rule respectively, and the deferral language is
  unchanged at V4.

## §4 — V4 anti-paper-close exemplarity (methodological surface)

The V4 cycle itself crystallizes the CH6 ANTI-PAPER-CLOSE lens. The
V3 → V4 transition is the canonical demonstration of why this lens
exists:

- **V3's failure mode.** F-V3-α-E-1 landed a metadata-derivation
  intent that was paper-perfect (cited Lock 14 §220 correctly,
  named a plausible cargo-metadata|jq path, structured the shell loop
  correctly) but execution-broken. The fold passed every documentary
  CH lens at V3 (CH2 metadata-derivation grammar-neutrality, CH7
  generated-output discipline, CH6 anti-paper-close on its prose
  surface) precisely because all those lenses operated on the WORDS
  of the fold, not on the COMMAND'S OUTPUT.

- **V3 CH1's catch.** CH1 V3 REV-1 caught the defect because CH1's
  lens authority demands "every claim cites file:line, commit SHA,
  RESULTS row, or REDRESS entry that resolves" — including, by
  V3-era extension, that shell commands shipped as falsifiability
  gates actually execute. CH1 V3 executed the V3 command and found
  it returned `null`.

- **V4's discipline institutionalization.** The V4 commit body
  carries the V3 aggregator's methodological recommendation forward
  as a binding fold-discipline: "explicit executable-verification
  dispatch for any shipped shell command. V4 alpha-E dispatch carried
  this mandate and produced verified output; V4 CHALLENGE lenses will
  inherit the same mandate." The V4 commit body shows the actual
  command output (9 grammars enumerated against live workspace) —
  measured evidence, not paper claim.

This is exactly the inverse of paper-close. Paper-close ships words
that look like verification; executable-verification ships commands
that produce measurable output. The V4 cycle institutionalizes the
latter discipline. CH6 binding is therefore strengthened by V4, not
merely preserved.

## §5 — Recommended folds for V5

**None required from CH6.**

V4 closes the V2 → V3 → V4 three-cycle convergence chain at the CH6
100 % ceiling. No V4 finding, no V2/V3 baseline regression, no fresh
paper-close pattern, no new attribution gap. The V4 cycle further
strengthens CH6 character by institutionalizing the
executable-verification discipline the V3 aggregator's methodological
note prescribed. CH6 disposition for V4: **ACCEPT-COMPLETE.**

The V4 aggregator may proceed with the V4 CONSOLIDATED authoring
expecting CH6 contribution of 42/42 ACCEPT, 0 REJECT, 0 REVISE.
Under the V3 CONSOLIDATED §3.1 V4 point forecast (≥ 99 %, point
100 %), CH6 contributes the convergence margin verbatim. The V5
confirming pass at the V ≤ 5 ceiling per `ORCHESTRATOR.md §3Z`
remains the binding two-consecutive-cycle close for the SK-V14 Pass
Alpha bracket; CH6 expects to contribute 42/42 at V5 absent fresh
defects.

The V1 staging-race recurrence vector that CH6 V1 REJ-1 exposed is
now closed at four layers by V4 close: V2 F-2 (attribution
declaration) + V2 F-9 (triumvirate constraint binding) + V3 commit
`5e2ae78b4` (fresh per-agent V3 micro-fold attribution) + V4 commit
`5e00b6d27` (fresh per-agent V4 micro-fold attribution with
executable-verification discipline carried forward). The
numeric-divergence recurrence vector that CH6 V1 REJ-2 exposed is
closed at three layers: V2 F-1 (reconciliation paragraph in
SYNTHESIS §0.2 + §1.2 + HANDOFF §3) + V3 F-V3-α-F-1 (HANDOFF §7
carry-over guard widening to bind the 47-row population) + V4
F-V4-α-F-1 (HANDOFF §7 citation anchor correction pointing the guard
to the canonical SYNTHESIS §0.2 reconciliation source). Both CH6 V1
BINDING REJECTs are fully remediated at V4 close with no residual
paper-close surface.

The V4 cycle additionally surfaces a methodological strengthening
worth carrying into the SK-V14 ORCHESTRATOR-PROMPT or
PASS-ALPHA.md §3 lens-discipline section (outside this lens's scope
to write, but worth surfacing for the V4 aggregator): the V3 → V4
transition demonstrates that executable-verification is a binding
fold-discipline for any shipped shell command, and the
documentary-depth CH lenses (CH2, CH6, CH7) operate on prose surface
and cannot catch command-output defects. CH1 carries the
executable-verification mandate; the V4 institutionalization should
be made permanent at the ORCHESTRATOR level.

CH6 confirms: SK-V14 Pass Alpha bracket is V4-clean; the V5
confirming pass at the V ≤ 5 ceiling per `ORCHESTRATOR.md §3Z`
expects to lock the SK-V14 contract; the orchestrator proceeds to
S-P0 per the SK-V14 ORCHESTRATOR-PROMPT pin at V5 convergence.
