# CH2 Generality - Pass Omega V3 W2R

Verdict: ACCEPT.

W2R preserves CH2 / Lock 14 generality. The correction narrows W2 to the
skinny CSS L4 mirror and moves root CSS L4 runtime collapse to W6.0, where it
is one member of the same nine-grammar Pattern H runtime-root collapse. This is
a wave-ownership repair, not a CSS exception in generic surfaces and not a
row-admission change.

## Checks

| Check | Disposition |
|---|---|
| W2 skinny-only scope generalizes | ACCEPT. W2 is scoped as the first `regen-{grammar}` family instance, not fleet-wide closure. Its CSS-specific command is limited to skinny mirror recovery and does not authorize root runtime generation. |
| W6.0 root-runtime split generalizes | ACCEPT. CSS L4 is moved into the same W6 Pattern H collapse class as the other eight runtime dirs, after the generic W5 substrate exists. |
| W6.1-W6.8 remaining grammar list | ACCEPT. The set is complete: `bbnf`, `bnf`, `css_pretty`, `csv`, `ebnf`, `google_sheets`, `json`, `math`; no missing or extra grammar is introduced. |
| No CSS special-case leak | ACCEPT. The packet does not add generic-crate CSS branches, public CSS-specific generic APIs, feature flags, or grammar-shaped policy mining. CSS-specific text is confined to ownership routing for existing CSS L4 runtime surfaces. |
| No new row admit | ACCEPT. The packet repeatedly binds no `RESULTS.md` movement, no CSS/JSON SOTA movement, and no W8/W9/W10 new-admit dispatch before PRUNE closure. |
| Lock 14 grammar-neutrality | ACCEPT. W2R changes dispatch and ownership text only; zero lock amendment is required. |

## Evidence

- REDRESS-183 rejects W2 because the dual-tree gate required
  `crates/core/src/runtime/css_l4/` regeneration, but no current generator owns
  that root runtime tree.
- W2R makes W2 skinny-only: emit `skinny/crates/runtime/src/grammars/css_l4_*`,
  run the skinny destructive round-trip, retain bypass-header detection, and
  forbid W2 from touching `crates/core/src/runtime/css_l4/` or claiming
  Pattern H closure.
- W6.0 owns CSS L4 root-runtime collapse after W5 creates the generic generator
  substrate. W6.1-W6.8 retain the eight remaining Pattern H runtime dirs.
- Omega-C records zero `LOCKS.md` delta and explicitly says W2R reinforces
  generated-output truth by preventing W2 from claiming root runtime generation
  before W6 owns it.

## Defects

None.
