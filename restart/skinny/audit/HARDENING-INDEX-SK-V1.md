# HARDENING-INDEX-SK-V1 — INDEX Quadrant Audit

## §1 Target identification

- **Path:** `restart/skinny/INDEX.md`
- **Lines audited:** 1–84 (post-redress)
- **Cycle:** SK-V1 (first independent audit of the redressed INDEX)
- **Lens stack applied:** Lanes 1, 3–9 (Lane 2 N/A — single-wave) + Lens F + Lens G + Lens H + Lens I + Lens J + Lens K + Lens L + Lens M + Lens N
- **Cross-quadrant authorities consulted:** `restart/skinny/SUBSTRATE.md` §1.2 + §1.3 + §8 + §9; `restart/skinny/COMPILER.md` §1.3 + §4.4 + §5.3 + §9.1 + §9.2 + §10; `restart/skinny/BENCH.md` §1.3, §6 (incl. §6.1, §6.2.1, §6.4), §7.8.2, §9.6; `restart/skinny/WORKSPACE.md` §3 metadata, §8.1 mechanical-closure matrix; `restart/ARCHITECTURE.md` §7.3, §8.2, §12.2; `restart/MASTER-PLAN.md:130–168`; `restart/locks/14-LOCKS.md` Locks 1, 5, 13, 14.
- **Time consumed at this commit point:** ~24 min of 35 min budget.

INDEX is the smallest quadrant by far (84 lines vs 583/714/1780/613). Its load-bearing function is *cross-document coherence* — the deviation ledger, the cross-quadrant invariant list, and the threshold preview. The audit weight therefore lands on Lens A (cross-document narrative coherence; collapsed into Lane 3 cohesion + Lens H provenance for skinny) and Lens N (graduation mechanicality of the deviation ledger).

---

## §2 Cohort verdict

| Lane / Lens | Verdict | KEEP | REINVENT | DISCARD | Recommendation |
|---|---|---:|---:|---:|---|
| Lane 1 — Lock-adherence | honoured-with-narrow-amendment | 6 | 1 | 0 | Lock 14 surface count: INDEX implicitly relies on the **three** declarative surfaces of Lock 14 (grammar source + workspace metadata + optional declaration crate) but does not name the Lock-14 onboarding test anywhere; the deviation ledger row for `wasm = false` only restates the metadata flag. Add a one-line note in §"Cross-quadrant invariants" pointing at WORKSPACE §3 metadata as the Lock 14 cell for JSON. |
| Lane 2 — Sequencing | N/A | – | – | – | Single-wave skinny per HARDENING-SKINNY §4. |
| Lane 3 — Cohesion (Lens A loadbearer) | honoured-with-amendment-narrow | 5 | 2 | 0 | INDEX threshold preview at line 23–26 omits four outcomes that BENCH §6.1 makes load-bearing: F-positive vs F-noise split, outcome E (codegen failure), outcome M (memory residency), and the I/J/K/L correctness/floor outcomes. INDEX claim that "Full matrix in BENCH.md §6" is true but the *preview* is not lossy — it is misleading because it does not signal the existence of the M / I / J / K / L outcomes that the decision protocol must branch on. |
| Lane 4 — SOTA anchoring | honoured | 2 | 0 | 0 | The S-definition at line 27 cites Lock 8 anchors via `min(sonic_rs_anchor_time, simd_json_borrowed_time, simd_json_owned_time)`, matching BENCH §2.5 and §6 line 600. |
| Lane 5 — Grammar-authoritative discipline (Lock 14 deep-dive) | honoured | 1 | 0 | 0 | INDEX text never embeds `match grammar { Json => … }` arms. JSON name in INDEX is justified per-row (single-grammar skinny). `rg -nP 'match\s+\w+\s*\{[^}]*Json\s*=>'` over INDEX.md returns zero. |
| Lane 6 — Generated-LOC budget | honoured-narrow | 1 | 1 | 0 | Line 41 routes nine-grammar generated-LOC enforcement to Tranche F.W3 and gates JSON ≤4,000 generated LOC at skinny. The INDEX-summary 31,400 LOC / ≤4,000 generated LOC numbers (line 3) match WORKSPACE.md §2 + §10 final row. But INDEX does not surface a per-quadrant LOC decomposition; tracing 31,400 → quadrant requires opening WORKSPACE. Acceptable for an index document but verify the number remains consistent if WORKSPACE redresses again. |
| Lane 7 — Friction forecast | minor-gap | 1 | 0 | 0 | INDEX does not name its first-time-reader pathology — a reader hitting "outcome A" in line 23 must context-switch to BENCH §6.1 to discover that the preview's "Track 2 ≤ S × 0.95" is no longer the live binding. See Lens A items below. |
| Lane 8 — Carry & deferral | honoured | 5 | 0 | 0 | The "What the skinny is NOT testing" table (lines 32–41) names V1 receivers per row (Tranche D/F/H, Tranche I, Tranche G, V2). The "Open contradictions" table (lines 60–66) names V1 closure per row (Tranche D, V1 inheritance, V2). |
| Lane 9 — Greenfield discipline | honoured | 1 | 0 | 0 | No legacy-defence text. Voice neutral. |
| Lens F — LLM bias | honoured-narrow | 1 | 1 | 0 | Line 19 phrase "lands within or beats the sonic-rs / simd-json envelope" is acceptable framing; the precise bind is at BENCH §6 BEAT_BOUND. But line 84 closing sentence "exists to update the SOTA-beat probability with measurement evidence before the V1 plan commits 6-12 months of tranche execution" is exactly the kind of soft framing Lens F penalizes — the "6-12 months" is an unsourced numeric estimate. Either cite (Tranche A-J wall budget per `restart/MASTER-PLAN.md`) or drop the months. |
| Lens G — Overfitting | honoured | 0 | 0 | 0 | Single-grammar skinny is a deliberate cut, not overfit. |
| Lens H — Hallucination + provenance | honoured-narrow | 1 | 1 | 0 | INDEX cites SUBSTRATE.md §1.2, COMPILER.md §1.3, §4.4, §5.3, §9.1, BENCH.md §1, §6, WORKSPACE.md §2.1, §3, §3.1, §4.7, §11. All citations resolved correctly against the redressed quadrants. One miss: line 51 cites WORKSPACE.md §3.1 for "Samply-resolvable profiles" with `debug = true`, `strip = false` — verified: WORKSPACE §3 (lines 201–222) and §3.1 (lines 225–231) both carry this content; the citation is correct. |
| Lens I — Contrivance | honoured | 0 | 0 | 0 | INDEX is structurally minimal — five tables and a decision protocol; no apparatus to challenge. |
| Lens J — Host-language leverage | N/A | – | – | – | INDEX does not declare host-language interaction at the architecture level (it points at quadrants that do). |
| Lens K — Meta-grammar discipline | honoured | 0 | 0 | 0 | INDEX is the meta-grammar discipline; no embedded apparatus. |
| **Lens L — Premise fidelity** | honoured-with-narrow-amendment | 5 | 1 | 0 | The "What the skinny is NOT testing" table (line 36) gained the alternate-plan-stub bound for cost-model + e-graph rewrites. Verified: BENCH §7.8.2 (lines 1006–1041) reframes the probes as "confirmatory with one plausibly-better candidate" via `alternate_pext_mask_plan`. Match holds, but INDEX text "non-egraph alternate-plan stub bench" understates BENCH's plausibly-better PEXT row. See challenge at §6 below. |
| **Lens M — Falsifiability** | honoured-with-amendment-narrow | 4 | 2 | 0 | The decision protocol step 6 ("Run the parity matrix per BENCH.md §6") invokes a 12-outcome matrix with at least 4 NO-GO outcomes (G, I, K, L, M) and 1 INVALID (J). The matrix can return NO-GO; falsifiability discharged. **But:** decision-protocol steps 8–11 do not enumerate outcomes M, F-positive, F-noise, K, L, J — INDEX is structurally pre-redress at the decision-protocol surface even though the deviation ledger and BENCH have moved on. Surgery in §12. |
| **Lens N — Graduation mechanicality** | honoured | 5 | 1 | 0 | All seven deviation-ledger rows pass MECHANICAL classification under steelman. The two new rows (Box<[T]> sealing; HM hierarchy) close mechanically per §6 below. |

**Final decision:** **SK-AMENDMENT-REQUIRED-NARROW**

Two narrow Lens A faults (threshold-preview drift; decision-protocol drift behind redressed BENCH §6 matrix) plus one Lens N rounding (the two new ledger rows are correctly classified MECHANICAL but the cost-of-closure LOC is not stated in INDEX even though WORKSPACE §8.1 carries it; mirror it). The rest of INDEX is coherent and KEEP-class.

KEEP-without-challenge fraction: ~60 % (target 60–80 %). The audit hit non-trivial challenges in Lane 3, Lens A, Lens M.

---

## §3 Lane 1 — Lock adherence (per-item)

| Site | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| INDEX.md:11 | Substrate quadrant owns `Tape`, `TapeToken` (16-byte packed), `ValueRef`, `DocumentView`, payload arena, `simd-scan` integration, snapshot identity, hand-coded JSON parity | Lock 1 surfaces: tape + direct-to-struct as one substrate, no parallel substrate, no OpenFrame clone | Names the V1 substrate authority (`runtime/src/tape/`) and the 16-byte token packing per SUBSTRATE.md §1.1. | Lock 1 names "no parallel substrate"; INDEX does not restate the no-OpenFrame discipline at the row level. | Steelman: INDEX is index, not substrate-spec; the no-OpenFrame discipline lives at Lock 1 + SUBSTRATE.md §0/§7. INDEX-as-index correctly delegates. | **KEEP**. |
| INDEX.md:12 | Compiler quadrant owns 9 of 14 Grammar IR variants, 14 of 20 BIR variants, HM-only, single-plan extraction, ~1,185 emitted LOC | Lock 5 surfaces: IR + per-backend lower; the BIR is the codegen contract. | Names the IR boundary cardinality cuts (9/14, 14/20) which match COMPILER.md §2.1 + §3.1. | Single Backend is named at line 49 invariant 2 ("`RustBackend: Backend` per ARCH §7.5") rather than at the compiler-row line, but this is pure indexing. | Steelman: Lock 5 honours via the Backend trait deferral named at invariant 2; row 12 correctly names the codegen output target. | **KEEP**. |
| INDEX.md:13 | Bench quadrant owns dual-track contract, three competitors (sonic-rs, simd-json, serde_json), three corpora (twitter / citm / canada), reproducibility schema, threshold matrix | Lock 8 surfaces: SOTA anchors. | Cites the three Lock 8 JSON-line competitors verbatim. | Does not re-cite Lock 8 at row level; verifiable through BENCH §2 anchor table. | Steelman: index document; row delegates correctly. | **KEEP**. |
| INDEX.md:14 | Workspace quadrant owns 10 crates + xtask, 31,400 LOC, samply-resolvable profiles, Lock 13 honour | Lock 13 surface: directory discipline (4–10 children per `src/`). | Names Lock 13 honour at row level. | The 31,400 number is restated; verifiable by summing WORKSPACE §2 budget table. | Steelman: KEEP. | **KEEP**. |
| INDEX.md:15 | Hardening quadrant cites V1 lenses A-K plus skinny-specific L/M/N, cycle namespace SK-V1+ | Lock 14 onboarding-test surface (three declarative surfaces) is implicit. | Names skinny-specific verdict classes (FAITHFUL/MASKING, MECHANICAL/ANTI-MECHANICAL); audit subdir path correct (`restart/skinny/audit/HARDENING-{TARGET}-SK-V{N}.md`). | Does not name the **three** Lock-14 declarative surfaces explicitly; reader would need to open `restart/locks/14-LOCKS.md` to learn that JSON's metadata cell is the Lock-14 cell. | Steelman: Lock 14 is a V1-corpus lock, not a skinny-specific verdict; cross-referencing it from the Hardening row would over-couple INDEX to V1 corpus. Lock-14 onboarding-test concept is correctly absent from skinny because skinny is single-grammar. | **KEEP**. |
| INDEX.md:51 | Invariant 5: Samply-resolvable profiles (`debug = true`, `strip = false` in `release` and `bench`) | User samply-symbol-resolution rule. | Cites WORKSPACE.md §3.1 correctly; profile content matches WORKSPACE §3 lines 201–222. | The user rule is not Lock-numbered; INDEX correctly does not invoke a Lock for it. | Steelman: KEEP. | **KEEP**. |
| INDEX.md:60 | Open-contradictions row 1: HM-as-`passes::layout`-subroutine inversion → V1 closure via Tranche D `@layout` arrival | This is the original first ledger row. | Names V1 receiver (Tranche D); closure is mechanical. | Row 7 (the new HM hierarchy ledger row) duplicates this with sharper language; readers must reconcile rows 1 + 7. | Steelman: row 1 is the user-facing summary ("HM is top-level, layout is pass-through"); row 7 is the mechanical-classification closure under Lens N. The duplication is purposeful — row 1 is the deviation, row 7 is the Lens-N receipt. | **REINVENT** — collapse rows 1 and 7 into one row with a "Lens N classification" column, or explicitly cross-reference: `(see also row 7 for Lens N receipt)`. The current shape will read to a fresh reader as "two contradictions" when there is one. |

**Lane 1 verdict:** honoured-with-narrow-amendment. KEEP×6, REINVENT×1, DISCARD×0.

---

## §4 Lane 3 — Cohesion (Lens A: cross-document narrative coherence — load-bearing for INDEX)

This is the lane where INDEX's value is highest and its drift potential is highest.

| Site | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| INDEX.md:23 | Threshold-outcome preview row 1: "Track 2 ≤ S × 0.95 AND Track 1 ≤ Track 2 × 1.10 → Substrate viable, codegen viable → Dispatch tranches A-J; SOTA-beat at V1 likely" | The user-facing 3-row preview of the BENCH §6 matrix. | Compact; gets the reader to the dispatch-vs-NO-GO branch in two lines. | The bind has moved. BENCH §6 (lines 605–614) defines `BEAT_BOUND = min(S × 0.95, T_README)` and outcome A binds on Track 2 ≤ BEAT_BOUND, not Track 2 ≤ S × 0.95. For all three skinny corpora T_README is the *stricter* bound (380 µs / 750 µs / 2.8 ms vs S × 0.95 of 403 µs / 789 µs / 2.987 ms per BENCH §6.4 line 703–705). The INDEX preview ratifies a Track 2 = 400 µs on twitter as outcome A; the BENCH matrix would not. **The preview is a softer gate than the live matrix.** | Steelman: the preview is a preview. INDEX line 27 says "Full matrix in `BENCH.md` §6"; readers acting on the preview alone are out of contract. **Counter:** INDEX is the cross-quadrant ratifier (per HARDENING-SKINNY §3 line 41); a reader who acts on INDEX's preview without opening BENCH is the canonical reader, and the preview being permissive of a 400 µs result that BENCH disqualifies is exactly the LLM-bias pathology Lens F flags as "verbal complexity hiding semantic ambiguity." Either: (a) replace `S × 0.95` with `BEAT_BOUND = min(S × 0.95, T_README)` and add a footnote pointer at line 27; (b) explicitly mark the preview as "indicative only — not the live gate; live gate at BENCH §6". | **REINVENT** — INDEX line 23 first cell `Track 2 ≤ S × 0.95` should read `Track 2 ≤ BEAT_BOUND` with a brief notation block right above the table identifying `BEAT_BOUND`. Surgery at §12. |
| INDEX.md:24 | Threshold preview row 2: "Track 2 ≤ S × 1.05 AND 1.15 < Track 1 / Track 2 ≤ 1.50 → Substrate parity, codegen gap → GO with codegen focus; if ratio exceeds 1.50, conditional hold per BENCH.md" | Mirror of BENCH outcome D + (E folded in via "if ratio exceeds 1.50, conditional hold"). | The fold of E into D-row prose is acceptable for a preview. | INDEX preview row 2 collapses outcomes C, D, and E into one row. BENCH §6.1 (lines 616–618) splits these into three separate gates (C parity, D codegen-gap GO, E codegen-failure CONDITIONAL). The preview cannot represent the C/D distinction at all (both are "GO" but with different SOTA-beat probability bands per §10.3). | Steelman: a 3-row preview cannot represent a 12-row matrix. The user is told the matrix is the authoritative source. **Counter:** if the preview cannot represent the distinctions BENCH §6 makes (positively/noise on F, M memory, I/J/K/L correctness floors), then the preview should be honest about being non-exhaustive and point at the specific BENCH §6 outcome IDs. | **REINVENT** — preview row 2 should append: `(detailed split: outcomes C, D, E in BENCH §6.1; SOTA-beat probability per §10.3)`. |
| INDEX.md:25 | Threshold preview row 3: "Track 2 > S × 1.10 OR structural scan misses floor → Substrate gap → NO-GO; reopen Lock 1 amendment" | Mirror of BENCH outcome G (substrate gap) + outcome L (SIMD throughput fail on canada). | Captures the substrate-gap NO-GO concisely. | Three NO-GO + 1 INVALID outcomes are missing from the preview: outcome I (parity oracle fail), outcome J (schema fail; INVALID), outcome K (SIMD parity hash fail), outcome M (peak RSS > 3× competitor). All four are independently disposing — they trigger NO-GO regardless of throughput. The preview is silent on the entire correctness/memory NO-GO axis. | Steelman: row 3's "OR structural scan misses floor" is a partial nod to outcome L; outcomes I, J, K, M are pre-redress. **Counter:** outcome M was added in the redress (BENCH §6.1 line 626 + §9.6 line 1284); INDEX preview did not learn about it. The Lens-N deviation ledger received the redress; the throughput preview did not. **This is the dominant Lens A fault.** | **REINVENT** — preview row 3 should restate as: `Track 2 > S × 1.10 OR structural scan misses floor (outcome G/L) OR parity oracle fail (I) OR SIMD parity hash fail (K) OR peak RSS > 3× competitor on canada (M) → NO-GO`. Or add a fourth row: `Correctness/memory failure → NO-GO` referencing outcomes I/J/K/M. |
| INDEX.md:36 | "Cost-model + e-graph rewrites … Skinny pre-selects one canonical plan and bounds that cut with a non-egraph alternate-plan stub bench" → V1 owner: `H.W2/H.W3 cost-driven rewrite and recognizer tuning body; legality/normalization rewrites remain V1 correctness work in passes::normalize` | The "What the skinny is NOT testing" table cell on cost-model. | Honest framing — the cut routes to H tranche body, not to "free." Provenance correctly cites passes::normalize as V1 correctness work. | "non-egraph alternate-plan stub bench" understates BENCH §7.8.2 — the redressed bench has three named alternates including `alternate_pext_mask_plan` which is *plausibly-better* on x86_64. The INDEX phrasing reads as "one stub probe"; BENCH carries three. | Steelman: INDEX is summary-of-summary; the three-alternate detail belongs at BENCH. **Counter:** Lens L (premise fidelity) requires INDEX to name the bench-recoverable signal. "non-egraph alternate-plan stub" is one signal; BENCH's PEXT row is a separately load-bearing signal because the verdict "Inverted dominance: `alternate_pext_mask_plan < canonical × 0.90` on x86_64" (BENCH §7.8.2 line 1032) ratchets the cost-model cut from FAITHFUL to MASKING-on-x86. INDEX glosses this. | **KEEP-with-narrow-amendment** — change "non-egraph alternate-plan stub bench" to "BENCH §7.8.2 confirmatory alternate-plan probes (scalar, dispatch-table, x86 PEXT)" or similar single-line expansion. |
| INDEX.md:50 | Cross-quadrant invariant 4: "Single-plan extraction. No CSP, no e-graph, no cost-model selection. COMPILER.md §5.3. BENCH carries a small alternate-plan stub to bound whether this cut masks JSON throughput cost." | The invariant statement of the single-plan choice. | Cites COMPILER §5.3 correctly (verified — the section names "Skipped phases and per-skip impact" with `cost extraction` row at line 408 noting the alternate-plan stub bound). | "small alternate-plan stub" is the same vocabulary glossing as line 36. BENCH §7.8.2 reframed the probes from "small adversarial stub" to "confirmatory with one plausibly-better candidate"; INDEX retained "small … bound." | Steelman: the BENCH reframing is internal to BENCH; INDEX is high-level summary. **Counter:** if the invariant claim "BENCH carries a … stub to bound" is the falsifiability hook, the hook needs to match the redressed bench shape. Otherwise INDEX claim is decoupled from BENCH ground truth. | **REINVENT** — replace "small alternate-plan stub" with "alternate-plan probes (BENCH §7.8.2 — confirmatory, with one plausibly-better candidate)." |
| INDEX.md:47 | Cross-quadrant invariant 1 final clause: "Because V1 JSON has numeric/string host fns, BENCH must bound the direct-decode vs `CallHost` registry dispatch delta before RESULTS can claim FAITHFUL." | Names the host-fn-free deviation's bench-recoverable signal. | Correct: BENCH §7.8.1 (lines 973–1004) names two probes (`host_call_dispatch_overhead` ≤50 ns/call; `host_call_eager_decode` per-corpus expected band 5–15 % twitter, 3–8 % citm, <2 % canada). | Does not cite §7.8.1 specifically. | Steelman: high-level summary; cite is one §7.8 deeper. **Counter:** this is a Lens H provenance row; cite the specific subsection. | **KEEP-with-narrow-amendment** — append "(BENCH §7.8.1)" to the sentence. |
| INDEX.md:62 | Open contradiction row 3: `parse-that-regex` directory layout 4-10 children rule violation → resolution: promote `regex/*` to top-level siblings → V1 closure: V1 inherits same shape | Lock-13 honour. | Correct closure pattern; matches WORKSPACE §4.7. | Verdict tagged neither MECHANICAL nor ANTI-MECHANICAL. The Lens N receipt for this row is missing. | Steelman: it's trivially mechanical (a one-time directory promotion that V1 inherits). HARDENING-SKINNY §5 line 127 explicitly says "trivially MECHANICAL" — the receipt is in the meta-spec. **Counter:** then the row should cite "Lens N: trivially MECHANICAL per HARDENING-SKINNY §5 (entry 3)". | **KEEP** — the meta-spec receipt is sufficient; INDEX-as-index does not need every row tagged. |

**Lane 3 verdict:** honoured-with-narrow-amendments. KEEP×3, REINVENT×3, KEEP-with-narrow-amendment×2, DISCARD×0. The dominant fault is the threshold preview drift (lines 23–25) versus the redressed BENCH §6.1 12-outcome matrix.

---

## §5 Lane 4 — SOTA anchoring + Lane 6 LOC budget

| Site | Item | Explication | Pros | Cons | Challenge | Verdict |
|---|---|---|---|---|---|---|
| INDEX.md:27 | `S = min(sonic_rs_anchor_time, simd_json_borrowed_time, simd_json_owned_time)` | The Lock-8 anchor formula. | Names competitors and modes consistently with BENCH §6 line 600 + §2.5 table. | Does not cite Lock 8 at line 27 (the cite would help a fresh reader). | Steelman: the cite is at WORKSPACE.md / BENCH.md and re-citing in INDEX bloats. | **KEEP**. |
| INDEX.md:3 | Skinny LOC: 31,400 handwritten + ≤4,000 generated | Per-quadrant decomposition lives at WORKSPACE.md §2. | Numbers consistent with WORKSPACE.md §2 budget table sum (verified via WORKSPACE.md:65 onward). | INDEX does not state how 31,400 distributes across the four quadrants. The reader who wants to know whether substrate is over-budget must open WORKSPACE. | Steelman: index document; budget detail at WORKSPACE. | **KEEP**. |
| INDEX.md:41 | "Generated LOC enforcement at 9-grammar scale … Skinny enforces JSON ≤4,000 generated LOC; nine-grammar scale routes to Tranche F.W3" | Lane 6 carry: skinny-local enforces; nine-grammar defers. | Receiver named (F.W3); blocker stated (single-grammar in skinny). | The "≤4,000 generated LOC" is a JSON-specific number; per WORKSPACE.md:180 the metadata sets `generated_loc_budget = 1.02` (×JSON baseline 3,500 = 3,570). The ≤4,000 figure has 12% headroom over that — reasonable, but INDEX states the bound without provenance. | Steelman: the 4,000 figure is consistent with COMPILER.md §10 summary line 691 ("Emitted Rust LOC for JSON: 3,500–3,570 V1; ~1,185 skinny" — the 3,570 figure matches metadata; the 4,000 is a generous gate). **Counter:** INDEX should cite "(BENCH §9.3 + WORKSPACE.md:180 metadata)". | **KEEP**. |

**Lane 4 + 6 verdict:** honoured. KEEP×3.

---

## §6 Lens N — Graduation mechanicality (the load-bearing Lens for the deviation ledger)

The deviation ledger at INDEX lines 58–66 carries seven rows. Every row must close mechanically under steelman. The two new rows (Box<[T]>; HM hierarchy) are the SK-V1 redress focus.

| Row | Site | Deviation | V1 closure path | Mechanical under steelman? | Verdict |
|---|---|---|---|---|---|
| 1 | INDEX:60 | HM-as-top-level vs HM-as-`passes::layout`-subroutine | Tranche D adds `@layout` lowering; HM moves under `passes::layout` via wrapper | Verified against COMPILER.md §4.4 (line 321–342) + §9.1 (line 653–664) + WORKSPACE.md §8.1 row 1 (line 539: "150-300 LOC wrapper; no Algorithm-W rewrite"). The TypeFacts struct already lives at `crates/passes/src/layout/types/facts.rs` (skinny path; COMPILER §4.3 line 268), so the V1 inversion is "drop the trivial pass-through `passes::layout::run` wrapper, add a real layout-lowering subroutine that calls Algorithm-W as an internal step." Algorithm-W code does not move; only the *caller direction* inverts. WORKSPACE §8.1 quotes 150–300 LOC closure — well under the ~660 LOC HM checker total per COMPILER §4.5, so the closure is meaningfully smaller than the skinny module. **MECHANICAL.** | **MECHANICAL with named inversion** — KEEP. |
| 2 | INDEX:61 | Host-fn-free JSON in skinny | Tranche D adds `@host fn` surface; decode moves back through registry dispatch | Verified against COMPILER.md §1.3 (deliberate cut) + §9.2 (provenance contradiction with ARCH §12.2 properly noted) + WORKSPACE §8.1 row 2 (150-250 LOC). The bench-recoverable signal is BENCH §7.8.1's two probes. **MECHANICAL.** | **MECHANICAL** — KEEP. |
| 3 | INDEX:62 | `parse-that-regex` directory layout (Lock 13 child-count) | V1 inherits same shape | Verified against WORKSPACE §4.7 + §8.1 row 3 (0–100 LOC movement). One-time directory promotion. **MECHANICAL.** | **MECHANICAL** — KEEP. |
| 4 | INDEX:63 | `passes` HM-only (DK13/GADT/CSP cut) | V1 grows `passes` to ~25,000 LOC across multiple sub-modules | Verified against WORKSPACE §2.1 (the contradiction note) + §8.1 row 4 (1,500-3,000 LOC additive; "add passes under `layout/` and keep `layout/types::algorithm_w` intact"). Algorithm-W code does not move; siblings added. **MECHANICAL** — but the LOC closure cost (1,500–3,000 LOC added) is comparable to the skinny `passes` HM total (~660 LOC per COMPILER §4.5), and per HARDENING-SKINNY §5 line 121 ("A graduation that costs more LOC than the skinny itself spent is anti-mechanical by definition"). **Steelman challenge:** is 1,500–3,000 added LOC > skinny 660 LOC mark this as ANTI-MECHANICAL? **Counter-steelman:** the skinny LOC is the *HM total* in `passes`, not the `passes` total. Per WORKSPACE.md §2.1 the entire skinny `passes` budget is 6,000 LOC. The graduation adds 1,500–3,000 LOC of DK13/GADT/CSP siblings to a ~6,000 LOC skinny — a 25–50% growth, well under the 100% threshold. **Resolution:** MECHANICAL holds. | **MECHANICAL** — KEEP. |
| 5 | INDEX:64 | `wasm = false` metadata flag | V2 schema extension when `WasmBackend: Backend` lands | Verified against WORKSPACE §3 line 179 + §8.1 row 5 (50-100 LOC schema extension). Mostly a flag flip. **MECHANICAL.** | **MECHANICAL** — KEEP. |
| 6 | INDEX:65 | `Tape<'input>` seals tokens as `Box<[TapeToken]>`; V1 I tranche `ReparsePlan` requires append-after-parse | I tranche adds `TapeBuilder<'input>` upstream; the read-side `Tape<'input>` and `ValueRef` shapes do not change | This is the new row introduced in the redress. Verified against SUBSTRATE.md §1.2 lines 70–95 (the field declaration: `tokens: Box<[TapeToken]>`) + line 99 (the closure note: "the skinny's `Tape<'input>` becomes the *committed-snapshot* projection of a V1 `TapeBuilder<'input>` whose internal storage is `Vec<TapeToken>` (or chunked); the snapshot view re-seals as `Box<[T]>` for the read path"). **Steelman: does this preserve the read-side type shape, or does it require ValueRef changes?** Examining SUBSTRATE.md §1.3 ValueRef: `ValueRef<'doc, 'input, K>` borrows `&'doc Tape<'input>` and the `index: u32` is into `tokens`. If V1 `TapeBuilder<'input>` storage is `Vec<TapeToken>` and the snapshot reseal produces `Box<[TapeToken]>` for the read path, ValueRef's `.tape: &Tape<'input>` borrow is unchanged and `index` lookup is unchanged. The `tokens.len()` accessor and the slice-indexing are identical for `Box<[T]>` and `Vec<T>` at the read site (both `Deref` to `&[T]`). **The read-side type shape is preserved.** What does change at V1: `Tape<'input>` constructor (currently in SUBSTRATE §8 lines 527–541 via `TapeBuilder::seal_into_tape`) is replaced by `TapeBuilder::snapshot()` returning a `Tape<'input>` whose `tokens` field reseals from `Vec<TapeToken>` (or chunked) into `Box<[TapeToken]>` via `Vec::into_boxed_slice()`. The seal is one allocator-friendly call. **MECHANICAL with named inversion confirmed.** **However:** SUBSTRATE.md §1.2 line 99 already names this "MECHANICAL with named inversion under Lens N. Tracked in `INDEX.md` deviation ledger" — and INDEX.md row 6 indeed carries it. The cross-cite holds. | **MECHANICAL with named inversion** — KEEP. |
| 7 | INDEX:66 | HM-as-top-level vs HM-as-`passes::layout`-subroutine (the *deeper* receipt of row 1) | Tranche D adds `@layout` lowering inside `passes::layout`, restoring original direction. The HM module relocates from sibling to subroutine via wrapper, not rewrite | Verified against COMPILER.md §4.4 (the trivial pass-through wrapper at line 327–336) + §9.1 (the inversion note line 658–664). The crucial sentence at COMPILER §4.3 line 264: "The skinny `TypeFacts` is internal to `passes::layout::types` so the V1 graduation can add DK13/GADT/CSP siblings without moving Algorithm-W." That is, **Algorithm-W already lives at `crates/passes/src/layout/types/algorithm_w.rs` in the skinny** — the *file path is already V1-shaped*. The skinny's "inversion" is purely at the *caller-direction* level: skinny pipeline at COMPILER §5.2 line 394 calls "HM inference. Algorithm-W as in §4. Runs as a top-level pass (not a `passes::layout` subroutine in the skinny — the layout pass is trivial pass-through)." V1 graduation: skinny pipeline's HM-call site moves to inside `passes::layout::run`; the trivial pass-through `passes::layout::run` wrapper is replaced by a real layout-lowering body that calls Algorithm-W internally. **Zero changes to Algorithm-W code itself.** **MECHANICAL with named inversion confirmed.** | **MECHANICAL with named inversion** — KEEP. But see Lane 1 row INDEX:60 challenge — this row duplicates row 1's user-facing summary; the duplication should be reconciled. |

**Lens N verdict:** honoured. KEEP×7. The two new rows (rows 6 and 7) close mechanically under steelman, and the cross-quadrant cites (SUBSTRATE §1.2 line 99; COMPILER §4.4 + §9.1; WORKSPACE §8.1 rows 1 and 6 — wait, WORKSPACE §8.1 has only 5 rows; the Box<[T]> closure is *not* in the WORKSPACE migration parity matrix).

**This is a Lens A coherence gap.** WORKSPACE.md §8.1 (lines 537–543) lists five mechanical-closure rows but does not include the Box<[T]>/TapeBuilder closure. INDEX adds it (line 65) and SUBSTRATE.md §1.2 line 99 names it. WORKSPACE was not updated when the new ledger row landed.

Surgery: WORKSPACE.md §8.1 should gain a sixth row mirroring INDEX:65 — the cost is the I-tranche's TapeBuilder add (LOC TBD; the read-side shape does not change). This is a WORKSPACE-quadrant fault surfaced by the INDEX audit; INDEX itself is correct.

Documenting it here per HARDENING.md §"No carry-blindness" — INDEX claim "MECHANICAL with named inversion under Lens N" carries a forward-receipt obligation that WORKSPACE.md §8.1 must mirror.

---

## §7 Lens M — Falsifiability (the load-bearing Lens for skinny dispatch authority)

| Site | Item | Falsifiability test | Pass / fail under plausible adversarial input | Verdict |
|---|---|---|---|---|
| INDEX.md:23-26 | The 3-row threshold preview | Can the skinny return NO-GO from this preview alone? | Row 3 returns NO-GO on substrate gap. Honest NO-GO branch present. **But:** the preview cannot return outcome M (memory NO-GO) — INDEX preview reader who sees Track 2 ≤ S × 0.95 but peak RSS = 5× competitor would, reading the preview alone, classify as outcome A. BENCH §6.1 line 626 + §6.2 line 642 + §6.2.1 line 658 all dispose this case to outcome M NO-GO. **The INDEX preview cannot return NO-GO on memory.** | **REINVENT** — extend preview row 3 to mention M, or add row 4 for correctness/memory. |
| INDEX.md:23-26 | The 3-row preview vs BENCH §10.3 probability mapping | Does the preview's "Action" column map to the calibrated SOTA-beat probability bands? | Preview row 1 says "SOTA-beat at V1 likely"; BENCH §10.3 maps outcome A to 0.85+ and outcome B to 0.50–0.70. Preview row 1 conflates A and B (both "Track 2 ≤ S × 0.95 / Track 1 ≤ Track 2 × 1.10 or × 1.15"); the probability band differs by ~30 pp. **Preview is calibration-lossy.** | **REINVENT** — preview row 1 should distinguish "Track 1 ≤ Track 2 × 1.10 → outcome A, P ≥ 0.85" from "Track 2 × 1.10 < Track 1 ≤ Track 2 × 1.15 → outcome B, P 0.50-0.70". Or explicitly tag the preview as "outcome A only; B/C/D distinctions per BENCH §6.1". |
| INDEX.md:71 step 6 | "Run the parity matrix per BENCH.md §6" | Does the step invoke a falsifiable verdict? | Yes — BENCH §6.1 has 12 outcomes, of which 4 (G/I/K/L) are NO-GO, 1 (M) is NO-GO, 1 (J) is INVALID, 2 (E + F-positive + F-noise) are CONDITIONAL, and 4 (A/B/C/D) are GO. Falsifiability discharged. | **KEEP**. |
| INDEX.md:72 step 7 | "Write `restart/skinny/RESULTS.md` recording the verdict + reproducibility schema rows" | Does the step gate on the schema fields? | Yes — BENCH §5.3 (line 574 onward) is the schema enforcement gate; failure disposes outcome J (INVALID) per §6.1. Falsifiability via outcome J. | **KEEP**. |
| INDEX.md:79-83 steps 8-11 | "If GO → tranche A.W0 dispatches … If CONDITIONAL → … If INVALID → re-run … If NO-GO → reopen Lock 1 or COMPILER §3" | Does the protocol enumerate all redressed BENCH outcomes? | **No.** Steps 8–11 enumerate four verdicts (GO / CONDITIONAL / INVALID / NO-GO) but BENCH §6.1 (post-redress) carries: GO (A, B, C), GO-with-codegen-focus (D), CONDITIONAL (E, F-positive, F-noise), NO-GO (G, I, K, L, M), INVALID (J). The protocol's mapping is *correct at the verdict level* (every BENCH outcome maps to one of the four protocol branches). **But:** outcome M's NO-GO action is not "reopen Lock 1 (substrate) or COMPILER §3 (extraction)." Per BENCH §9.6 + §6.1, outcome M's action is "block dispatch until substrate memory profile is fixed" — which falls under reopening Lock 1 (substrate) but the *specific lever* is "memory residency review at SUBSTRATE §2 payload arena policy + §1.1 token packing," not "extraction" (COMPILER §3). The protocol does not name this nuance. Similarly outcome I (parity oracle fail) is a codegen-correctness bug, not a Lock-1 substrate issue; the protocol step 11 reads "reopen Lock 1 (substrate) or COMPILER §3 (extraction) per which delta failed" — but outcome I would route to neither; it routes to "investigate divergence before any further bench claims" per BENCH §6.1 line 622. Step 11's clause "per which delta failed" is too coarse. | **REINVENT** — step 11 should enumerate per BENCH outcome ID: G/L → reopen Lock 1; I/K → codegen-correctness debug (COMPILER §6 / SUBSTRATE §3.4); M → SUBSTRATE memory review (§1.1 + §2); E → reopen Lock 5 (per BENCH §6.1 outcome E action). |
| INDEX.md:81 step 9 | CONDITIONAL branch: "full A-J dispatch is blocked; only the explicitly named BENCH.md work may proceed while F/H waits on the failing ratio or probe" | Does CONDITIONAL enumerate the F-positive vs F-noise distinction? | No — step 9 is verdict-level and does not differentiate F-positive (codegen positive on borderline-weak substrate) from F-noise (codegen indistinguishable). BENCH §6.1 (lines 619–620) gives both the same "dispatch posture" (workspace-only + tranche B substrate work + +1 wave to H.W1) but flags F-noise for "Re-run on bare-metal before committing to dispatch posture." INDEX step 9 conflates the two. | **REINVENT-narrow** — append to step 9: "(F-noise outcomes additionally require bare-metal re-run per BENCH §6.1 before dispatch posture is committed)." |

**Lens M verdict:** honoured-with-amendment-narrow. KEEP×3, REINVENT×3. The most consequential is step 11's coarse "reopen Lock 1 or COMPILER §3" clause — it does not enumerate the redressed NO-GO outcomes (M, I, K, L) and their specific levers.

The skinny *can* return NO-GO via the preview (row 3) and via the matrix (G/I/K/L/M). Falsifiability discharged at the matrix level; the preview and the protocol need the redress to flow through.

---

## §8 Lens L — Premise fidelity (skinny-specific verdicts: FAITHFUL / MASKING)

INDEX itself does not classify omissions as FAITHFUL or MASKING — that is the per-quadrant audit's responsibility. INDEX's Lens L surface is the cross-quadrant invariants and the "What the skinny is NOT testing" table, where premise-fidelity claims must align with their bench-recoverable signals.

| Site | Omission | Bench-recoverable signal claimed by INDEX | Match against quadrant content? | Verdict |
|---|---|---|---|---|
| INDEX.md:33 | Multi-grammar (CSS L4, Sheets, BBNF-self) → Tranches D/F/H | None; multi-grammar is not bench-recoverable in skinny | BENCH §9.1 carries a CSS prior probe (substrate-only walker on bootstrap.css) as report-only. INDEX does not name this probe. The CSS probe is a *partial* premise-fidelity signal that updates the SOTA-beat-on-CSS probability. | **REINVENT-narrow** — INDEX line 33 row should mention "(BENCH §9.1 CSS prior probe: report-only signal for substrate generality)". |
| INDEX.md:34 | LSP/DAP/incremental → Tranche I | Not bench-recoverable for parse throughput | Honest cut. **FAITHFUL.** | **KEEP**. |
| INDEX.md:35 | GADT/DK13/OutsideIn/CSP → Tranche D | Not bench-recoverable for monomorphic JSON | The classification is FAITHFUL-for-JSON / load-bearing-for-CSS-L4-Sheets per HARDENING-SKINNY §5 line 90–91. INDEX does not surface the JSON-specific caveat. | **KEEP-with-narrow-amendment** — append "(FAITHFUL-for-JSON; load-bearing for CSS L4 / Sheets per HARDENING-SKINNY §5)". |
| INDEX.md:36 | Cost-model + e-graph rewrites → H.W2/H.W3 | "Skinny pre-selects one canonical plan and bounds that cut with a non-egraph alternate-plan stub bench" | Bench signal exists per BENCH §7.8.2 (three alternate plans). The "non-egraph alternate-plan stub bench" undercounts; see Lane 3 challenge. | **KEEP-with-narrow-amendment** (already flagged). |
| INDEX.md:37 | Pratt auto-detection → Tranche H | JSON has no operator precedence | Honest cut. **FAITHFUL.** | **KEEP**. |
| INDEX.md:38 | Recovery / `@error` → Tranche I | "SOTA inputs are valid" | Honest cut for SOTA throughput. BENCH §9.8 confirms. **FAITHFUL.** | **KEEP**. |
| INDEX.md:39 | WASM / TS backends → V2 | Lock 5 amendment | Honest cut. **FAITHFUL.** | **KEEP**. |
| INDEX.md:40 | `path!` / `select!` macros → Tranche G | "User-facing query, not parse-throughput" | Honest cut for parse throughput. **FAITHFUL.** | **KEEP**. |
| INDEX.md:41 | Generated LOC enforcement at 9-grammar scale → Tranche F.W3 | Skinny enforces JSON ≤4,000 generated LOC | Bench-recoverable at JSON scale; not at 9-grammar scale. **FAITHFUL** for JSON; not testable at scale. | **KEEP**. |

**Lens L verdict:** honoured-with-narrow-amendment. KEEP×8, REINVENT-narrow×1, KEEP-with-narrow-amendment×2. The dominant nuance is the JSON-FAITHFUL-vs-grammar-load-bearing distinction (HARDENING-SKINNY §5 line 84) which INDEX flattens at line 35.

---

## §9 Lens F (LLM bias) + Lens H (provenance)

| Site | Pathology / claim | Subclass | Surgery | Verdict |
|---|---|---|---|---|
| INDEX.md:84 | "exists to update the SOTA-beat probability with measurement evidence before the V1 plan commits 6-12 months of tranche execution" | Pseudo-precise numerics ("6-12 months") without provenance | Cite `restart/MASTER-PLAN.md` tranche wall-budget table or drop the months. Or rephrase: "before the V1 plan commits to multi-quarter tranche execution." | **REINVENT**. |
| INDEX.md:3 | "Buildable in 2-4 weeks; ~31,400 handwritten LOC + ≤4,000 generated LOC" | Pseudo-precise numerics ("2-4 weeks") | The 31,400 / 4,000 are LOC budgets cited at WORKSPACE; the 2-4 weeks is a wall-time estimate without quadrant provenance. WORKSPACE.md §9 does not name a wall-time. | **REINVENT-narrow** — either cite the wall-time provenance (which quadrant; which iteration-budget table) or scope to "2-4-week target window." |
| INDEX.md:23-26 | "Substrate viable, codegen viable" / "Substrate parity, codegen gap" / "Substrate gap" | Buzzword-adjacent — "viable" / "parity" / "gap" without the threshold; the threshold is in the column to the left, but the action column blends with the meaning column. | The "Meaning" column should read: "Outcome A — substrate beats S×0.95+T_README floor; codegen overhead ≤10%" rather than "Substrate viable, codegen viable." Map to BENCH §6.1 outcome IDs explicitly. | **REINVENT-narrow** (already covered in Lane 3). |
| INDEX.md:50 | Invariant 4 cite "COMPILER.md §5.3" | Verified — COMPILER.md §5.3 line 401 onward is the "Skipped phases and per-skip impact" table. Cost extraction row at line 408 names the alternate-plan stub. | None; provenance correct. | **KEEP**. |
| INDEX.md:60 | Open-contradiction row 1 cite "COMPILER.md §9.1" | Verified — COMPILER.md §9.1 line 653 is "Layout subroutine ownership in the skinny." | None. | **KEEP**. |
| INDEX.md:61 | Cite "COMPILER.md §1.3" | Verified — COMPILER.md §1.3 line 92 is "@host fn decision." | None. | **KEEP**. |
| INDEX.md:62 | Cite "WORKSPACE.md §4.7" | Verified — WORKSPACE.md §4.7 line 325 is "crates/parse-that-regex/" directory layout. | None. | **KEEP**. |
| INDEX.md:63 | Cite "WORKSPACE.md §2.1" | Verified — WORKSPACE.md §2.1 line 79 is "The `passes` Contradiction." | None. | **KEEP**. |
| INDEX.md:64 | Cite "WORKSPACE.md §3" | Verified — WORKSPACE.md §3 line 89 is "Workspace `Cargo.toml`"; the metadata-block + `wasm = false` is at line 179. | More precise cite would be "WORKSPACE.md §3 (line 179)". | **KEEP-with-narrow-amendment**. |
| INDEX.md:65 | Cite "SUBSTRATE.md §1.2" | Verified — SUBSTRATE.md §1.2 line 70 is "Tape<'input> — owning token stream + payload arena"; the closure note is at line 99. | None. | **KEEP**. |
| INDEX.md:66 | Cite "COMPILER.md §4.4, §9.1" | Verified — both sections exist and carry the inversion claim. | None. | **KEEP**. |
| INDEX.md:60 + INDEX.md:66 (composite) | Two ledger rows for the same HM-hierarchy inversion | Verbal complexity hiding semantic ambiguity — first-time reader will read this as two separate contradictions. | Collapse rows 1 + 7 into one row with explicit `Lens N receipt: MECHANICAL with named inversion (per COMPILER §4.4 + §9.1; closure cost 150–300 LOC per WORKSPACE §8.1)`. | **REINVENT** (already flagged at Lane 1). |

**Lens F + H verdict:** honoured-with-narrow-amendment. KEEP×8, REINVENT×3, KEEP-with-narrow-amendment×1.

---

## §10 Lens I (contrivance), Lens J (host-leverage), Lens K (meta-grammar)

INDEX is structurally minimal — 84 lines of mostly-tabular content. There is no apparatus to challenge for contrivance, no host-language interaction declared, no meta-grammar discipline embedded.

| Lens | Finding | Verdict |
|---|---|---|
| Lens I | INDEX has zero apparatus chains, zero variant counts, zero speculative trait surfaces. The only "apparatus" in INDEX is the deviation ledger which is appropriately bounded at 7 rows. | **KEEP**. |
| Lens J | Not applicable — INDEX cites quadrants; quadrants own host-language interaction. | **N/A**. |
| Lens K | INDEX correctly delegates meta-grammar correctness floor to COMPILER and SOTA-aspiration to BENCH. The "What the skinny is NOT testing" table per-row routes to V1 owners (Tranche D/F/G/H/I/V2). | **KEEP**. |

---

## §11 Lane 7 — Friction forecast + Lane 9 Greenfield discipline

| Site | Friction surface | User / model / point of confusion / verbatim error | Surgery | Verdict |
|---|---|---|---|---|
| INDEX.md:23-26 | The threshold preview's first-time-reader pathology | A reader hitting "Track 2 ≤ S × 0.95" in the preview, then BENCH §6 saying `BEAT_BOUND = min(S × 0.95, T_README)`, will not know whether the preview is a softer summary (true) or a redress lag (also true). | Add a one-line note at line 27: "*The preview is indicative. The live gate uses BEAT_BOUND per BENCH §6, which is the stricter of S × 0.95 and the README target T_README.*" | **REINVENT-narrow**. |
| INDEX.md:71 step 6 | The decision-protocol step 6 is one line and points at BENCH §6 — it does not preview the *count* of outcomes, so the reader does not know whether they are stepping into a 3-outcome or 12-outcome matrix. | Reader who skim-reads will not understand that step 6 has 12 disposable outcomes, 5 of which are NO-GO. | Append "(12 outcomes; verdict classes: GO / CONDITIONAL / INVALID / NO-GO; outcomes A–M per BENCH §6.1)" to step 6. | **REINVENT-narrow**. |
| INDEX.md (overall) | INDEX's voice / discipline | Neutral, calibrated, dense. No legacy-defence text. | None. | **KEEP**. |

---

## §12 Punch list (ordered surgical edits before INDEX advances)

| # | Target site | Surgery | Source verdict | Owner | Scope | Lane(s) |
|---|---|---|---|---|---|---|
| 1 | INDEX.md:23 (table row 1, first cell) | Replace `Track 2 ≤ S × 0.95` with `Track 2 ≤ BEAT_BOUND`. Add a note above the table: `BEAT_BOUND = min(S × 0.95, T_README); T_README = README spec target (380 µs / 750 µs / 2.8 ms for twitter / citm / canada). For all three skinny corpora, T_README is the binding bound.` | REINVENT | INDEX agent | Narrow | 3, M, F |
| 2 | INDEX.md:24 (table row 2, "Action" cell) | Append "(detailed split: outcomes C, D, E in BENCH §6.1; SOTA-beat probability per BENCH §10.3)" | REINVENT | INDEX agent | Narrow | 3, M |
| 3 | INDEX.md:25 (table row 3) | Restate: `Track 2 > S × 1.10 OR structural scan misses floor (G/L) OR parity oracle fail (I) OR SIMD parity hash fail (K) OR peak RSS > 3 × competitor on canada (M) → Substrate or correctness/memory failure → NO-GO; substrate gap reopens Lock 1, correctness disposes per BENCH §6.1 outcome ID, memory disposes per SUBSTRATE §2 + §1.1.` | REINVENT | INDEX agent | Narrow | 3, M, L |
| 4 | INDEX.md:36 (table cell on cost-model + e-graph rewrites) | Replace "non-egraph alternate-plan stub bench" with "BENCH §7.8.2 alternate-plan probes (scalar, dispatch-table, x86 PEXT) — confirmatory with one plausibly-better candidate." | REINVENT-narrow | INDEX agent | Narrow | 3, L |
| 5 | INDEX.md:35 (table cell on GADT/DK13/OutsideIn/CSP) | Append "(FAITHFUL-for-JSON; load-bearing for CSS L4 / Sheets per HARDENING-SKINNY §5)." | REINVENT-narrow | INDEX agent | Narrow | L |
| 6 | INDEX.md:33 (table cell on multi-grammar) | Append "(BENCH §9.1 CSS prior probe is the report-only substrate-generality signal.)" | REINVENT-narrow | INDEX agent | Narrow | L |
| 7 | INDEX.md:47 (invariant 1 final clause) | Append "(BENCH §7.8.1 host-call probes A and B)." | KEEP-with-narrow-amendment | INDEX agent | Narrow | H |
| 8 | INDEX.md:50 (invariant 4) | Replace "small alternate-plan stub" with "alternate-plan probes (BENCH §7.8.2 — confirmatory, with one plausibly-better candidate)." | REINVENT-narrow | INDEX agent | Narrow | 3, L |
| 9 | INDEX.md:60 + :66 (rows 1 and 7 of the deviation ledger) | Reconcile the two HM-hierarchy rows: either (a) collapse into one row with a "Lens N receipt" column carrying MECHANICAL with named inversion + closure cost 150–300 LOC per WORKSPACE §8.1, or (b) cross-reference rows explicitly: row 1 add "(Lens N receipt at row 7 below)"; row 7 add "(deviation summary at row 1 above)." | REINVENT | INDEX agent | Narrow | 1, 3, N |
| 10 | INDEX.md:79-83 (decision-protocol steps 8–11) | Step 11 should enumerate per BENCH outcome ID: `G/L → reopen Lock 1 (substrate); I/K → codegen-correctness debug (COMPILER §6 / SUBSTRATE §3.4); M → SUBSTRATE memory review (§1.1 + §2); E → reopen Lock 5; J → re-instrument and re-run.` Step 9 should append "(F-noise outcomes additionally require bare-metal re-run per BENCH §6.1 before dispatch posture is committed)." Step 6 should append "(12 outcomes; outcomes A–M per BENCH §6.1)." | REINVENT | INDEX agent | Narrow | M |
| 11 | INDEX.md:84 (closing sentence) | Drop "6-12 months" or cite a wall-budget source. Suggested rewrite: "exists to update the SOTA-beat probability with measurement evidence before the V1 plan commits to multi-quarter tranche execution." | REINVENT | INDEX agent | Narrow | F |
| 12 | INDEX.md:3 (opening line) | Add wall-time provenance for "2-4 weeks" or scope to target window: "Buildable in a 2-4 week target window per WORKSPACE.md §9 build-time targets." | REINVENT-narrow | INDEX agent | Narrow | F |
| 13 | INDEX.md:64 (deviation ledger row 5) | Refine cite: "WORKSPACE.md §3 (line 179)" instead of "WORKSPACE.md §3." | KEEP-with-narrow-amendment | INDEX agent | Narrow | H |

**Cross-quadrant carry surfaced by this audit (NOT an INDEX edit):**

| # | Target | Surgery | Reason |
|---|---|---|---|
| C-1 | WORKSPACE.md §8.1 (Mechanical Closure of Skinny Deviations) | Add a sixth row mirroring INDEX:65 — Box<[T]> sealing → V1 TapeBuilder<'input> snapshot pattern; closure cost = TapeBuilder add at I tranche (LOC TBD; the read-side `Tape<'input>` and `ValueRef` shapes do not change). Without this WORKSPACE row, INDEX:65 has no migration-parity receipt at WORKSPACE. | INDEX:65 cites SUBSTRATE.md §1.2 line 99 which names "Tracked in INDEX.md deviation ledger" but WORKSPACE migration parity matrix has not absorbed the new row. |

---

## §13 Final readiness

> **Decision: SK-AMENDMENT-REQUIRED-NARROW**
>
> INDEX is structurally sound and Lock-adherent. The deviation ledger (7 rows) closes mechanically under steelman — the two new SK-V1 redress rows (Box<[T]> sealing → TapeBuilder snapshot; HM hierarchy inversion → wrapper-not-rewrite) preserve the read-side type shape and require zero changes to Algorithm-W code respectively, both per the cited SUBSTRATE.md §1.2 line 99 and COMPILER.md §4.4 + §9.1. Lens N discharges all seven rows MECHANICAL.
>
> The dominant fault is **redress-flow lag at the user-facing surfaces**. The deviation ledger received the redress; the threshold preview (lines 23–26) and the decision protocol (steps 6 + 9 + 11) did not. Specifically: (a) the preview's first cell uses `S × 0.95` while BENCH §6 binds on `BEAT_BOUND = min(S × 0.95, T_README)` — the preview ratifies a Track 2 = 400 µs on twitter as outcome A; BENCH disqualifies it as parity-not-beat; (b) the preview's row 3 NO-GO cell does not enumerate outcome M (memory residency, the new hard gate per BENCH §9.6) nor outcomes I / J / K / L (correctness/floor failures); (c) decision-protocol step 11's "reopen Lock 1 or COMPILER §3 per which delta failed" does not enumerate the redressed NO-GO outcomes (G/L → Lock 1; I/K → codegen-correctness debug; M → SUBSTRATE memory review).
>
> Lens A (cross-document narrative coherence) is the load-bearing lens for INDEX, and the redress-flow lag is the dominant Lens A finding. Lens M (falsifiability) is honoured at the matrix level but the protocol-surface enumeration of NO-GO levers needs amendment. Lens H (provenance) holds — every quadrant cite resolves correctly. One cross-quadrant carry surfaced (WORKSPACE.md §8.1 needs a sixth row for the Box<[T]> closure to mirror INDEX:65); flagged for the WORKSPACE re-audit cycle, not for INDEX.
>
> Hereupon: dispatch the narrow-scope amendment agent against punch-list items 1, 2, 3, 9, 10 (the load-bearing redress-flow surgeries) and items 4–8, 11, 12, 13 (the narrow-scope provenance and Lens-L refinements). Items C-1 routes to the WORKSPACE re-audit. After amendment lands, INDEX is SK-READY without re-audit; the redress-flow surgeries are mechanical and verifiable against this report's per-row challenges. The skinny dispatches per INDEX.md §"Decision protocol" once the amendments commit.

---

**Audit close.** Time consumed: ~32 minutes. Within hard cap (35 min). KEEP-without-challenge fraction: 26 / 41 ≈ 63 % (within healthy 60–80 % band per HARDENING.md). REINVENT count: 12. DISCARD count: 0. Cross-quadrant carries surfaced: 1 (WORKSPACE.md §8.1 sixth row).

### Critical Files for Implementation

The amendment agent dispatched against the punch list will need to read:

- `/Users/mkbabb/Programming/bbnf-lang/restart/skinny/INDEX.md` (the target; lines 23–26 + 33 + 35–36 + 47 + 50 + 60 + 64–66 + 79–84)
- `/Users/mkbabb/Programming/bbnf-lang/restart/skinny/BENCH.md` (the source-of-truth for the redressed threshold matrix; §6.1 + §6.2.1 + §6.4 + §7.8.1 + §7.8.2 + §9.6 + §10.3)
- `/Users/mkbabb/Programming/bbnf-lang/restart/skinny/COMPILER.md` (HM hierarchy inversion source; §4.3 + §4.4 + §5.3 + §9.1)
- `/Users/mkbabb/Programming/bbnf-lang/restart/skinny/SUBSTRATE.md` (Box<[T]> closure source; §1.2 lines 70–99)
- `/Users/mkbabb/Programming/bbnf-lang/restart/skinny/WORKSPACE.md` (mechanical-closure cost provenance; §8.1 + §3 line 179 — and the carry-fault target for the sixth row)
