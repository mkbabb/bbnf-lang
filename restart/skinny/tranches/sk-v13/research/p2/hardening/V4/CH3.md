# SK-V13 S-P2 V4 CH3 Regression / REDRESS

Verdict: ACCEPT.

## Evidence

- V4 confirms the unchanged V3 packet. The V3 consolidated verdict records CH3
  as ACCEPT and states that REDRESS material differentials remain preserved for
  later S-P3 wave selection
  (`restart/skinny/tranches/sk-v13/research/p2/hardening/HARDENING-S-P2-V3-CONSOLIDATED.md:19`-`26`).
  V3 is explicitly the first accepted cycle, with V4 required only as the
  consecutive confirmation cycle
  (`restart/skinny/tranches/sk-v13/research/p2/hardening/HARDENING-S-P2-V3-CONSOLIDATED.md:28`-`47`).

- No historical REDRESS reject is reopened by a renamed CSS scope. P2-F defines
  `CSS-ROW-SCOPE-CONDITIONAL` as generated CSS parity row/fact-stream work, not
  primitive admission, and requires fresh narrow CSS parser profiling or
  same-wave strict lightningcss/cssparser row movement
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:44`-`47`).
  The six CSS expansion scopes carry that same verdict and consumer/gate shape
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:69`-`74`,
  `:123`-`:132`, `:158`-`:161`, `:240`-`:245`).

- REDRESS 119/120 addendum implications are carried honestly. The addendum makes
  the old direct fixpoints history only and makes every residual direct row
  wave-eligible, but still requires prior-fixpoint citation plus a fresh
  material differential
  (`restart/skinny/USER-PIN-ADDENDUM-2026-05-21-FULL-SOTA.md:58`-`74`).
  P2-A, P2-B, P2-D, P2-E, and P2-F all preserve that distinction: profile
  signals are not admissions, and direct reopens need strict same-harness
  comparator evidence
  (`restart/skinny/tranches/sk-v13/research/p2/p2a-sota-teardown.md:124`-`128`;
  `restart/skinny/tranches/sk-v13/research/p2/p2b-dav1d-process.md:137`-`139`;
  `restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:400`-`404`;
  `restart/skinny/tranches/sk-v13/research/p2/p2e-parse-that-gaps.md:146`;
  `restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:187`-`191`).

- Union and structural routes still preserve material-differential requirements.
  P2-D says structural SIMD is scanner signal only and does not reopen retained
  SIMD-position vectors, streaming cursors, class columns, or parser-owned
  cursors (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:62`-`85`).
  Its admissible D2/D3 routes name the differential against REDRESS 96/97/98:
  codegen-time same-tape projection or mask-to-tape writeback where the retained
  projection is the tape
  (`restart/skinny/tranches/sk-v13/research/p2/p2d-substrate-tape.md:187`-`227`,
  `:229`-`:276`). P2-F repeats that union attempts are legal only as
  codegen-private tape/fact routing over one substrate and must carry REDRESS
  96/97/98 citations
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:147`-`149`,
  `:174`-`:178`).

- SIMD/ASM history remains bounded. P2-B keeps `a64_ascii_set_run_skip` as a
  future CSS scanner consumer and forbids counting SK-V12 W4 as row movement
  (`restart/skinny/tranches/sk-v13/research/p2/p2b-dav1d-process.md:64`-`76`).
  P2-C keeps PMULL/CSSC admissible only as a new SIMD-first union route with
  material differential and same-wave consumer, not as stale default-body
  replacement (`restart/skinny/tranches/sk-v13/research/p2/p2c-arch-esoterica.md:16`,
  `:57`-`:65`). P2-F keeps standalone prefix/next/bulk bitmap primitives,
  `byte_context`, cache hints, EOR3/BCAX, LD4/TBX/SMIN/SMAX, and D1 capacity out
  of S-P3 without fresh evidence and a same-wave consumer
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:162`-`166`,
  `:180`-`:185`).

- Row movement gates remain measurable. CSS admissions require strict equality,
  lightningcss throughput, and an independent oracle per the addendum
  (`restart/skinny/USER-PIN-ADDENDUM-2026-05-21-FULL-SOTA.md:76`-`87`), while
  JSON row admissions require strict sonic-rs same-plane equality and no silent
  demotion (`restart/skinny/USER-PIN-ADDENDUM-2026-05-21-FULL-SOTA.md:89`-`94`).
  P2-F carries those gates into S-P3 by classifying CSS rows as conditional
  row-production scopes and by requiring same-wave strict row movement for CSS
  and direct-row reopens
  (`restart/skinny/tranches/sk-v13/research/p2/p2f-grammar-neutral.md:151`-`170`,
  `:187`-`:202`).

## Blockers / Fold Requirements

No CH3 blocker for V4 confirmation.

Carry forward these S-P3 requirements:

1. CSS rows 1-6 remain conditional row-production scopes, not primitive
   admissions; plans need fresh narrow CSS parser profiling or same-wave strict
   lightningcss/cssparser row movement.
2. REDRESS 119/120 are history only. Every direct-row reopen must cite the prior
   fixpoint, name a fresh material differential, and use strict same-harness
   comparator evidence.
3. Union and structural SIMD plans must cite REDRESS 96/97/98, reject sidecar or
   parser-owned structural projections, and make the existing tape or sink-only
   fact stream the measured consumer.
4. REDRESS 88/89/126-adjacent SIMD work must not land as support inventory:
   scalar reference, checkasm/parity, material differential, same-wave consumer,
   and zero-orphan disposition remain required.

## Disposition

CH3 confirms V3 acceptance for S-P2 V4. Route to the V4 aggregator as ACCEPT;
the items above are S-P3 preconditions, not V4 revision blockers.
