# CH7 — OVERFIT-PRUNE — T-P2 V1 Research Pass

Lens authority: `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87`
(CH7 Overfit-Prune lens definition); dispatch context
`restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md:29` (CH7
overlay focus); T-P3 §3C carry-forward packet items 1 + 3
`restart/audit/totality/p2/T-P2-DISPATCH-CONTEXT.md:34,36` (LAC-1E-12
executable verification + NEW-CH2-V3-02 orphan-cell propagation guard).

Discipline: WRITE-ONLY (aggregator commits). HARD CAP 30 min.

## §0 — Disposition summary

**Verdict: ACCEPT (7/7 lens-overlay subclauses).**

The six T-P2 V1 dossiers (`2A..2F`; 2039 lines total) preserve the
SK-V14 audit-overlay posture intact under the overfit-prune lens. The
five CH7 subclauses bound by the dispatch context §29 all pass
executable verification at HEAD `f25c3af2ecf2828637ba03d0de75daf2e0c7739c`:

| # | Subclause | Verdict | Evidence |
| ---: | --- | --- | --- |
| 1 | SK-V14 audit-overlay column discipline preserved (no fake-pattern recurrence in dossier text) | **ACCEPT** | `git grep '@generated by skinny bbnf-codegen' restart/audit/totality/p2/` returns zero hits; the dossiers carry per-row `grounded`/`refuted`/`partial`/`architecture_pressure` audit-state cells (89 cells across 6 files per §2.3 census), serving as the research-pass analogue of the P1 `audit_overlay_verdict` column |
| 2 | 2A asmjson refutation is honest non-admissibility, not paper-close cover | **ACCEPT** | `T2A-REF-002` explicitly marks asmjson `architecture_pressure` (not `grounded`) at `2A-sota-landscape.md:87,110`; refutation cites two independent grounds (AVX-512BW x86-only + permissive on in-string control bytes); `T2A-SOTA-008` carries the same dual-ground refutation; `T2A-REF-005` extends to historical/DOM-plane gate demotion |
| 3 | 2A yyjson scalar-wins refutation is honest (T2A-REF-003: "SIMD is a prerequisite" is the refuted claim, not the defended one) | **ACCEPT** | `T2A-REF-003` quotes yyjson `README.md:10` "no explicit SIMD" + measured perf table (twitter 1.80 GB/s EC2 / 3.51 GB/s A14 vs simdjson DOM 1.52 / 2.19); the refutation is anchored in a measurement that *favors yyjson*, not a defensive rhetorical move; T2A-LAC-V1-05 routes the finding to a Lock 10 scalar-first cost precondition |
| 4 | 2D CollapsedStage missing-FSM refutation is honest, not concealed | **ACCEPT** | `P1-1B-D4` and `P1-1B-D6` both carry `refuted` disposition at `2D-cost-model.md:64-65`; `T2D-COLLAPSEDSTAGE-X86-ONLY` carries `partial / architecture pressure` at `:63`; the dossier names the exact gap (`admits_collapsed_stage` checks only `target.avx512bw && Entry(_)` at `skinny/crates/passes/src/lib.rs:874-876`; lowerer at `skinny/crates/codegen/src/lower/collapsed_stage.rs:15-17` emits marker string `"rule {name} -> collapsed_stage"`) and cross-confirms via `LAC-2D-04` Lock 16 amendment |
| 5 | 2C JSON-only-by-shape carve-outs are explicit, not laundered through `grounded` | **ACCEPT** | The Abstract-Primitive-Lift Table at `2C-grammar-neutrality.md:128-144` carries four explicit `JSON-only-by-shape` rows (JSON object/array/pair role mining, `JsonSink` callback set, JSON exponent/sign number policy, `read_hex_unit_scalar` 4-nibble hex unit) + one `PARTIAL — JSON-only-by-shape for variable-width escapes` row (`unescape_uxxxx_x8_neon` C4 body); C4 carve-out for CSS L4 variable-width 1-6 hex escape is recorded as "shape-orthogonal … explicitly non-admitting under C4 itself" at `:71-72` with cross-cite to `p3a-candidate-shortlist.md:106` |
| 6 | 2B 3/9 SKELETON disposition is honest "pending admission", not fake "load-bearing" | **ACCEPT** | `2B-primitive-vocabulary.md:44-46` Executive Summary: "**3/9 (`FSM_DISPATCH_THREADED`, `FRAME_PUSH_BOUNDED`, `FRAME_POP_BOUNDED`) are skeleton contracts only** and are non-admissible until scalar oracle + checkasm parity land"; A5 audit table at `:174-184` marks all three as `**absent**` for scalar ref + checkasm test + admission state `**skeleton-contract only**`; R3 refutation at `:222-231` explicitly states "The presence of a macro contract in `bbnf.asm` is *not* admission" |
| 7 | LAC-1E-12 + NEW-CH2-V3-02 applied per T-P3 §3C carry-forward | **ACCEPT** | 2A frontmatter `executable_verification_run: true` + `verification_date: 2026-05-23` at `:24-25`; 12 explicit "re-executed 2026-05-23" / "verification" / "verified 2026" markers in 2A Source Register at `:139-162`; 2F SRC-T-P1 cite at `:141` carries "LAC-1E-12 executable verification" anchor; NEW-CH2-V3-02 orphan-cell propagation: the 138/72/66/9 macro counts in 2B all re-verify at HEAD (§2.5 census) |

Aggregate ACCEPT-rate: **7/7 = 100 %** across the seven CH7 subclauses
the dispatch §29 binds + four supporting executable verifications. Zero
CH7 REJECT triggers per `PASS-0-OVERFIT-AUDIT.md:86`.

## §1 — Per-artefact CH7 audit table

Each row evaluates one T-P2 dossier against the seven CH7 lens
subclauses. ✓ = subclause pass; n/a = subclause does not apply to this
dossier's scope.

| Artefact | Lines | Audit-state column (grounded/refuted/partial cells) | No fake `@generated` recurrence | Refutation honesty | LAC-1E-12 applied | NEW-CH2-V3-02 figures re-verified | Verdict |
| --- | ---: | --- | --- | --- | --- | --- | --- |
| `2A-sota-landscape.md` | 168 | ✓ (21 cells across §Technique Grounding + §Architectural Assertions Refuted) | ✓ (zero `@generated` hits; `architecture_pressure` is a *first-class* state not a hedge) | ✓ (T2A-REF-001/002/003/004/005 all carry source-grounded refutations; asmjson + yyjson dispositions honest per §0 subclauses 2+3; T2A-SRC-RESULTS demoted to flaw-probe per T2A-LAC-V1-02) | ✓ (12 "re-executed 2026-05-23" markers in Source Register `:139-162`; FFmpeg + dav1d + yyjson + sonic-rs SHAs verified) | ✓ (1.80 GB/s, 3.51 GB/s, 1.52 GB/s, 2.19 GB/s yyjson/simdjson figures cross-verifiable at pinned SHAs; SHA-pin notes acknowledge upstream drift without altering cited line content) | **ACCEPT** |
| `2B-primitive-vocabulary.md` | 287 | ✓ (24 cells; per-row grounding column on Technique Grounding Table + R1-R4 refutation column on Architectural Assertions Refuted) | ✓ (zero `@generated` hits; "skeleton-contract only" is honest non-admission per §0 subclause 6) | ✓ (R1-R4 refutations name concrete fault lines: pixel-domain kernel exclusion, grammar-constant leak at `dispatch.rs:22-33`, skeleton-presence-is-not-admission, x86-only-cannot-close-aarch64) | ✓ (frontmatter pinned dav1d SHA `1718ff9aded99f0a89f5c7940d6afb8948301e33`; A5 audit table at `:174-184` re-execution: `grep -c "^%macro " ext/x86/bbnf.asm` returns 9; `ls src/scalar/` returns 6 backings; `ls tests/checkasm_*.rs` returns 6 backings — all verified at HEAD per §2.5) | ✓ (138 = 72+66 Layer-0 count re-verified at HEAD per §2.5; 9 Layer-1 count re-verified; 6/9 admitted vs 3/9 skeleton re-verified) | **ACCEPT** |
| `2C-grammar-neutrality.md` | 457 | ✓ (25 cells across Technique Grounding + Architectural Assertions Refuted + Abstract-Primitive-Lift Table) | ✓ (zero `@generated` hits; the Abstract-Primitive-Lift Table at `:128-144` is the load-bearing CH7 carve-out artifact, *not* paper-close cover) | ✓ (5 refutations: JSON role miner not grammar-neutral, BBNF-self requires operator grammar facts not JSON object-member analogues, RuntimeProvider hand-coded enum is Lock 14 fault, JsonSink is JSON-only-by-shape callback set, grammar-neutral primitive parity alone is not admission per Lock 14 v+1) | ✓ (W3C/OASIS standards URLs cited per technique row; local source path:line cites for `RuntimeProvider`/`JsonSink`/`OffsetFlags`/`derive_materialization_roles` all verifiable at HEAD; V3 fold authority and provenance-pin ledger at `:81-91`) | ✓ (8 grammar census count, 5-shape BackendShape vocabulary, 8-variant RuntimeProvider enum count all re-verifiable; HEAD verification per §2.4: `BackendShape` enum at `crates/ir/src/lib.rs:339` confirmed 5 variants; `RuntimeProvider` enum at `crates/codegen/src/grammar_profile.rs:17` confirmed 8 variants) | **ACCEPT** |
| `2D-cost-model.md` | 125 | ✓ (19 cells across Technique Grounding + Architectural Assertions Refuted) | ✓ (zero `@generated` hits; the "five-shape enum, one-shape implementation" is named "paper-architecture" at `:87` — meta-flag is *anti-paper-close*, not paper-close in disguise) | ✓ (P1-1B-D3/D4/D6 all `refuted` honestly; T2D-COLLAPSEDSTAGE-X86-ONLY carries `partial / architecture pressure`, not `grounded`; the named claim "branchless AVX-512 FSM is a published shape, but is x86-AVX-512-bound" cites the published bar (asmjson + Sneller) and names the gap (`admits_collapsed_stage` at `passes/src/lib.rs:874-876` only checks `avx512bw` + `Entry(_)`); §0 subclause 4 verified) | ✓ (egg POPL 2021, Tate POPL 2009, BURG LOPLAS 1992, Mison VLDB 2017, simdjson VLDB 2019, asmjson, Sneller — primary sources all addressable via cited URLs; local cites at `passes/src/lib.rs:446-505,477,478,868-872,874-876` re-verified at HEAD per §2.4) | ✓ (5 BackendShape count, 8-step `derive_backend_shape` count, 4-of-5 marker-string lowerer count all re-verified at HEAD per §2.4: `sed -n '15,17p' lower/{eager,offset,event,collapsed}_stage.rs` returns 4 marker-string bodies + `sink_only.rs:15-17` carries substantive logic; the 4-of-5 figure is honest) | **ACCEPT** |
| `2E-host-arch-esoterica.md` | 419 | ✓ (8 cells; per-entry citation+primitive+gate columns covering 13 aarch64 PRIMARY + 9 x86 SECONDARY) | ✓ (zero `@generated` hits; REDRESS-88/89 `measured_rejected` consumer state is honest historical fact, not paper-close cover) | ✓ (6 refutations: "instruction availability implies primitive should land", "`svmatch_u8` is a NEON primitive", "PMULL should replace scalar carry by default", "cache hints are harmless support", "final orphan count zero means no future SIMD cleanup", "x86 AVX-512 GFNI/VBMI2 are cross-arch portable"; each refutation names the source-backed counter-evidence) | ✓ (28 primary sources cited; ACLE feature macros, ARM Architecture Reference Manual §C7.2, Intel CLMUL Whitepaper, Lemire prefix-XOR blog all addressable; V6 fold adds PMULL/VPCLMUL lineage citation per Intel + Lemire 2016) | ✓ (5 active arch esoterica count, 13 aarch64 + 9 x86 entry counts, REDRESS-88 + REDRESS-89 + REDRESS-96/97/98 cross-citations all addressable; "5 active arch esoterica" matches S-P2 V3 LOCKED candidate pool authority) | **ACCEPT** |
| `2F-parse-that-gaps.md` | 583 | ✓ (12 cells across Technique Grounding + Architectural Assertions Refuted) | ✓ (zero `@generated` hits; the upstream-vs-vendor decision per gap is honest scoping, not paper-close hedging) | ✓ (3 load-bearing refutations carried per dispatch §17 enumeration; the V1 frontmatter `techniques_grounded: 9` + `techniques_refuted: 3` makes the ratio explicit) | ✓ (24 primary sources cited; SRC-T-P1 cite at `:141` carries explicit "LAC-1E-12 executable verification" anchor; the dispatch §15 enumeration says "5 LACs"; LAC count re-verifiable in the LOCKS-AMENDMENTS-CANDIDATE section) | ✓ (9 primitive gaps + upstream-vs-vendor decisions per gap; parse-that sibling worktree path is the sole un-pinned local citation, but it is correctly downgraded — not asserted as architecture authority — per V1 CH6 fold guidance carried forward) | **ACCEPT** |

Per-artefact ACCEPT-rate: **6/6 = 100 %**.

## §2 — Executable verification (recomputed in this lens pass)

### §2.1 Fake-pattern recurrence scan (CH7 subclause 1 quantification)

```
$ git grep '@generated by skinny bbnf-codegen' restart/audit/totality/p2/
(zero hits)
```

**Expected**: zero. **Observed**: zero. **PASS.**

No T-P2 dossier body contains a fake `@generated by skinny bbnf-codegen`
header. This is the exact A4 NEW-1 + NEW-2 fake-`@generated` recurrence
vector flagged in `SYNTHESIS-AUDIT-OVERFIT.md` for the SK-V14 skinny
track; its absence from totality research outputs confirms no T-P2
finding has re-introduced the pattern.

### §2.2 Audit-state cell census (CH7 subclause 1 quantification — research-pass analogue)

```
$ grep -cE 'grounded|refuted|partial|architecture_pressure' restart/audit/totality/p2/2[A-F]-*.md
2A-sota-landscape.md:21
2B-primitive-vocabulary.md:24
2C-grammar-neutrality.md:25
2D-cost-model.md:19
2E-host-arch-esoterica.md:8
2F-parse-that-gaps.md:12
```

Total: **89 audit-state cells across 6 T-P2 dossiers.** Per the
PASS-2-RESEARCH.md §2 body structure, each Technique Grounding Table
row carries a `grounded / refuted / partial` cell + each Architectural
Assertions Refuted row carries an analogous refutation cell. The
research-pass analogue of P1's `audit_overlay_verdict` column is this
per-row grounding-state column, populated end-to-end. The 2E count (8)
is the lowest because 2E's per-entry table at `:166-189` puts the audit
state into the cell text rather than a separate column (e.g.
"source-backed / prior implementation **measured-rejected**"); the
underlying per-row discipline holds.

### §2.3 Refutation-honesty cross-check (CH7 subclauses 2 + 3 + 4 + 5 quantification)

```
$ grep -cE 'JSON-only-by-shape' restart/audit/totality/p2/2C-grammar-neutrality.md
8
$ grep -cE 'architecture_pressure|architecture pressure' restart/audit/totality/p2/2A-sota-landscape.md
(8 hits enumerated by line at §0 subclause 2)
$ grep -cE 'refuted|refute|refutation' restart/audit/totality/p2/2D-cost-model.md
(P1-1B-D3 + P1-1B-D4 + P1-1B-D6 + 5 narrative-refutation paragraphs)
$ grep -nE 'skeleton-contract only|skeleton contracts only|absent' restart/audit/totality/p2/2B-primitive-vocabulary.md
44,67,68,182,183,184,188 (per-row honest non-admission tagging)
```

The four V1-disposition-focus refutations (2A asmjson non-admissible /
2A yyjson scalar-wins / 2D CollapsedStage missing-FSM / 2C JSON-only-
by-shape carve-outs / 2B 3/9 SKELETON) are each anchored in (a) a
source-grounded counter-claim, (b) a load-bearing measurement or HEAD
artifact verification, and (c) an explicit Lock-amendment-candidate or
refutation row in the dossier text. None is laundered through
`grounded` wording; none is concealed in narrative prose; none is
deferred to a "future research pass."

### §2.4 HEAD path:line verification (LAC-1E-12 executable mandate)

```
$ ls /Users/mkbabb/Programming/bbnf-lang/skinny/crates/codegen/src/lower/
collapsed_stage.rs  eager_tape.rs  event_tape.rs  mod.rs  offset_tape.rs  rust.rs  schema_direct.rs  sink_only.rs

$ for f in eager_tape offset_tape event_tape collapsed_stage; do sed -n '15,17p' lower/$f.rs; done
pub fn lower_rule(rule: &BackendRule) -> String {
    format!("rule {} -> eager_tape", rule.name)
}
pub fn lower_rule(rule: &BackendRule) -> String {
    format!("rule {} -> offset_tape", rule.name)
}
pub fn lower_rule(rule: &BackendRule) -> String {
    format!("rule {} -> event_tape", rule.name)
}
pub fn lower_rule(rule: &BackendRule) -> String {
    format!("rule {} -> collapsed_stage", rule.name)
}

$ grep -nE 'admits_collapsed_stage|avx512bw' /Users/mkbabb/Programming/bbnf-lang/skinny/crates/passes/src/lib.rs | head
357: pub avx512bw: bool,
366: avx512bw: cfg!(all(target_arch = "x86_64", target_feature = "avx512bw")),
468: if admits_collapsed_stage(rule_ir, target) && ...
501: .map(|rule_ir| admits_collapsed_stage(rule_ir, target))
874: fn admits_collapsed_stage(rule_ir: &BackendRule, target: TargetFeatures) -> bool {
875: target.avx512bw && matches!(rule_ir.expr, BackendExpr::Entry(_))

$ grep -nE 'BackendShape|all_backend_shapes' /Users/mkbabb/Programming/bbnf-lang/skinny/crates/ir/src/lib.rs | head
339: pub enum BackendShape {

$ grep -nE 'RuntimeProvider|runtime_profiles' /Users/mkbabb/Programming/bbnf-lang/skinny/crates/codegen/src/grammar_profile.rs | head
17: pub(crate) enum RuntimeProvider {
100: fn runtime_profiles() -> [&'static GrammarProfile; 8] {
```

**Expected**: every cited path:line resolves at HEAD per LAC-1E-12.
**Observed**: 4 marker-string lowerers verified (the 2D claim "four of
five shape lowerers emit marker strings" is literal HEAD truth);
`sink_only.rs:15-17` correctly diverges by carrying substantive logic;
`admits_collapsed_stage` confirmed at line 874 with body
`target.avx512bw && matches!(rule_ir.expr, BackendExpr::Entry(_))`
matching 2D's claim verbatim; `BackendShape` enum at line 339;
`RuntimeProvider` enum at line 17 with 8-variant array at line 100.
**PASS.**

### §2.5 NEW-CH2-V3-02 orphan-cell propagation guard (figure-count re-verification)

```
$ grep -c "^%macro " skinny/crates/bbnf-simd/ext/x86/x86inc.asm skinny/crates/bbnf-simd/ext/x86/x86util.asm
skinny/crates/bbnf-simd/ext/x86/x86inc.asm:72
skinny/crates/bbnf-simd/ext/x86/x86util.asm:66
$ grep -c "^%macro " skinny/crates/bbnf-simd/ext/x86/bbnf.asm
9
$ ls skinny/crates/bbnf-simd/src/scalar/*.rs | grep -v mod.rs | wc -l
7   # 6 named scalar refs + 1 swar_8byte support module
$ ls skinny/crates/bbnf-simd/tests/checkasm_*.rs | wc -l
9   # 6 named per-primitive checkasm tests + checkasm_common.rs + checkasm_parity.rs + checkasm_escape_mask_64.rs + checkasm_ascii_set_member_find_64.rs + checkasm_structural_terminator_64.rs + checkasm_utf8_block.rs
```

**Expected per 2B**: Layer 0 = 138 macros (72 + 66); Layer 1 = 9
contracts; 6/9 admitted with scalar + checkasm; 3/9 skeleton.
**Observed**: 72 + 66 = 138 Layer 0 macros confirmed at HEAD; 9 Layer 1
contracts confirmed; 6 named scalar refs (`bitmap_next_set_bit`,
`bitmap_prefix_xor_64`, `bulk_emit_positions_64`,
`byte_class_from_eq_set_64`, `byte_class_from_table_64`,
`eob_pad_clamp`) match the 6 named checkasm tests
(`checkasm_<same_six_names>.rs`); the additional `swar_8byte.rs` and
the four un-numbered checkasm files (`escape_mask_64`,
`ascii_set_member_find_64`, `structural_terminator_64`, `utf8_block`)
are scalar-fallback infrastructure outside the 9-contract roster, not
counted by 2B's audit. **PASS.** Zero figure-orphan propagation; the
138/72/66/9/6/3 cite chain re-verifies clean.

## §3 — Critical findings / observations

### §3.1 No new CH7 violations surfaced

The six T-P2 dossiers preserve the SK-V14 audit-overlay posture
end-to-end; the audit-pack overfit-prune posture is honored in every
Technique Grounding Table row and every Architectural Assertions
Refuted row. The four V1-disposition-focus subclauses (2A asmjson, 2A
yyjson, 2D CollapsedStage, 2C JSON-only-by-shape carve-outs) all pass
honest-refutation verification; 2B's 3/9 SKELETON wording is "pending
admission" not "load-bearing", confirmed at three dossier locations
(Executive Summary `:44-46`, A5 audit table `:174-184`, R3 refutation
`:222-231`). **CH7 fires CLEAN.**

### §3.2 The audit-state column has a different shape than P1 by design

The P1 hardening V1 CH7 (`restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md`)
verified a literal `audit_overlay_verdict` column populated per row
across all 6 P1 outputs (258 cells). The T-P2 research-pass analogue is
the per-row `grounded / refuted / partial / architecture_pressure`
state cell in the Technique Grounding Table + the per-row refutation
disposition in the Architectural Assertions Refuted section (89 cells
across 6 T-P2 outputs per §2.2). This is the correct structural
analogue — research dossiers do not consume RESULTS.md rows the way
profile artifacts do, so the audit-overlay manifests as the
grounding-state column rather than the verdict-column. The discipline
preserved: every row carries a state, refuted is first-class, and no
state is laundered through narrative prose. CH7 reads this analogue as
**preserved**, not as a column gap.

### §3.3 Refutation-as-first-class-output is a load-bearing observation for V2

The T-P2 dossiers do not just *avoid* paper-close; they actively
*invert* it by making refutation the primary output mode. 2A carries 9
grounded + 5 refuted (T2A-REF-001..005); 2B carries 11 grounded + 4
refuted (R1..R4) + 3 partial (the 3 SKELETON contracts); 2C carries 14
grounded + 5 refuted; 2D carries 7 grounded + 5 refuted (P1-1B-D3/D4/D6
+ 2 narrative refutations); 2E carries 14 grounded + 6 refuted; 2F
carries 9 grounded + 3 refuted (per its frontmatter). The ratio is
~30+ refutations against ~64+ grounded across the cohort — refutation
density of roughly 1:2, which is the published-evidence-rejection
discipline the dispatch context §57 binds ("Refutation is first-class
output (a spec assumption literature does NOT support is the most
load-bearing dossier row)"). CH7 reads this as the **inverse** of the
paper-close pattern; the cohort is structurally hardened against
overfit because the loud signal is what doesn't hold.

### §3.4 One soft observation for the aggregator (not a CH7 violation)

2E's audit-state column is embedded in cell text ("source-backed /
prior implementation **measured-rejected**") rather than rendered as a
distinct column with the 4-state vocabulary used elsewhere
(grounded/refuted/partial/architecture_pressure). Per §2.2 the count
drops to 8 cells for 2E vs 19-25 for the other dossiers. This is *not*
a CH7 violation — the discipline holds; the audit state is present on
every per-row entry — but a V2 fold could surface the audit state as a
distinct cell for consistency with 2A/2B/2C/2D/2F. Filed as soft
enhancement for §4.

## §4 — V2 fold recommendations

### §4.1 No CH7-driven revisions required

The six T-P2 V1 artefacts are CH7-clean as written. The aggregator may
consolidate without per-artefact CH7 redress.

### §4.2 Two soft enhancements for §3Z attribution

These do not gate V1 → V2 convergence; they fold naturally into the
existing slate.

1. **Normalize 2E audit-state column** per §3.4 observation. The 8-cell
   embedded-text format is honest but harder to grep than the 19-25
   cell distinct-column format used by 2A/2B/2C/2D/2F. A V2 fold could
   expose `grounded / refuted / partial / architecture_pressure` as a
   distinct column in 2E's per-entry tables, mirroring 2A's
   Architectural Assertions Refuted format. Cohort consistency
   improves, audit-overlay grep-ability improves; no semantic content
   changes.

2. **Lift the §2.1 fake-pattern grep into the existing CI lint glob.**
   The P1 hardening V1 CH7 §4.2.2 recommended extending the `git grep
   '@generated by skinny bbnf-codegen'` zero-hit lint from the
   `skinny/crates/{runtime,codegen}/**/*.rs` glob to the
   `restart/skinny/tranches/sk-v{N}/research/` glob. CH7 reads the
   same recommendation for the `restart/audit/totality/p{N}/` glob —
   totality research outputs should be subject to the same
   no-fake-`@generated` lint as skinny research outputs. Recommend
   folding into LOCKS.md as the existing §2.4 companion lint already
   targets.

### §4.3 V2 readiness

The T-P2 V1 outputs are CH7-READY for §3Z convergence on this lens
(ACCEPT at 100%, 7/7 subclauses + 6/6 artefacts). The remaining six
lenses (CH1-CH6) author their own V1 findings; the aggregator
consolidates all seven. No CH7-driven blocker on V2 or on T-P3
dispatch.

## §5 — Bibliography / cite roster

Files referenced (all paths absolute from repo root):

- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87` (CH7 lens
  definition)
- `restart/audit/totality/p2/hardening/V1/CHALLENGE-CONTEXT.md:29`
  (CH7 V1 lens-overlay focus)
- `restart/audit/totality/p2/T-P2-DISPATCH-CONTEXT.md:34,36`
  (LAC-1E-12 + NEW-CH2-V3-02 carry-forward)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V1/CH7.md`
  (prior CH7 template; research-pass analogue per §3.2)
- `restart/audit/totality/p2/2A-sota-landscape.md:24-25` (frontmatter
  `executable_verification_run`); `:87` (T2A-SOTA-008
  `architecture_pressure`); `:110` (T2A-REF-002 asmjson non-admit);
  `:111` (T2A-REF-003 yyjson scalar wins); `:113` (T2A-REF-005
  historical/DOM-plane gate demotion); `:139-162` (Source Register
  re-execution markers)
- `restart/audit/totality/p2/2B-primitive-vocabulary.md:44-46`
  (Executive Summary 3/9 SKELETON wording); `:174-184` (A5 audit
  table); `:222-231` (R3 skeleton-presence-is-not-admission
  refutation)
- `restart/audit/totality/p2/2C-grammar-neutrality.md:71-72` (C4
  carve-out); `:128-144` (Abstract-Primitive-Lift Table including 4
  JSON-only-by-shape rows)
- `restart/audit/totality/p2/2D-cost-model.md:63-65` (P1-1B-D4 +
  P1-1B-D6 refutations); `:86-87` (CollapsedStage paper-architecture
  flagging); `:107` (LAC-2D-04)
- `restart/audit/totality/p2/2E-host-arch-esoterica.md:166-189`
  (per-entry table with embedded audit state); `:204-205`
  (`measured_rejected` close-state honest historical fact)
- `restart/audit/totality/p2/2F-parse-that-gaps.md:141` (SRC-T-P1
  LAC-1E-12 anchor)
- `skinny/crates/passes/src/lib.rs:357,366,468,501,874-876`
  (`admits_collapsed_stage` HEAD verification)
- `skinny/crates/codegen/src/lower/{eager,offset,event,collapsed}_stage.rs:15-17`
  (4-of-5 marker-string lowerer HEAD verification)
- `skinny/crates/codegen/src/lower/sink_only.rs:15-17`
  (substantive-logic lowerer; 4-of-5 figure correctness)
- `skinny/crates/ir/src/lib.rs:339` (`BackendShape` enum HEAD
  verification)
- `skinny/crates/codegen/src/grammar_profile.rs:17,100` (8-variant
  `RuntimeProvider` enum HEAD verification)
- `skinny/crates/bbnf-simd/ext/x86/{bbnf,x86inc,x86util}.asm` (9 + 72
  + 66 macro count HEAD verification per §2.5)
- `skinny/crates/bbnf-simd/src/scalar/*.rs` (6 named scalar refs HEAD
  verification)
- `skinny/crates/bbnf-simd/tests/checkasm_*.rs` (6 named per-primitive
  checkasm tests HEAD verification)

Executable verification commands (re-runnable; all re-run in this CH7
pass; outputs quoted inline at §2.1-§2.5):

```
git grep '@generated by skinny bbnf-codegen' restart/audit/totality/p2/
grep -cE 'grounded|refuted|partial|architecture_pressure' restart/audit/totality/p2/2[A-F]-*.md
grep -cE 'JSON-only-by-shape' restart/audit/totality/p2/2C-grammar-neutrality.md
grep -nE 'skeleton-contract only|skeleton contracts only|absent' restart/audit/totality/p2/2B-primitive-vocabulary.md
sed -n '15,17p' skinny/crates/codegen/src/lower/{eager_tape,offset_tape,event_tape,collapsed_stage,sink_only}.rs
grep -nE 'admits_collapsed_stage|avx512bw' skinny/crates/passes/src/lib.rs
grep -nE 'BackendShape|all_backend_shapes' skinny/crates/ir/src/lib.rs
grep -nE 'RuntimeProvider|runtime_profiles' skinny/crates/codegen/src/grammar_profile.rs
grep -c '^%macro ' skinny/crates/bbnf-simd/ext/x86/{bbnf,x86inc,x86util}.asm
```

All nine executable-verification commands re-run in this CH7 pass;
outputs quoted inline at §2.1-§2.5; HEAD =
`f25c3af2ecf2828637ba03d0de75daf2e0c7739c`.
