# HARDENING-PASS-1-V8 — Simplification Audit (Lenses I/J/K)

## §1 Target Identification

| Field | Value |
|---|---|
| Target | `restart/audit/pass-1-substrate/PASS-1.md` |
| Audit role | independent V8 simplification auditor — PASS-1 |
| V7.1 carry-baseline | `restart/audit/hardening/HARDENING-CONSOLIDATED-V7.1.md` (READY across cohort; R4 friction rolled forward) |
| V7 PASS-1 baseline | `restart/audit/hardening/HARDENING-PASS-1-V7.md` (READY; 19 KEEP rows, 0 amendment, 4 friction-residuals R1-R4 closed in V7.1) |
| Most recent target commit | `c06d10c1` (Phase 7.5B — Lock 4/10 cross-refs + Backend trait cite + closure-by-move diagnostic) |
| Phase 7.2 fold commit | `c45d74ec` (DK13 + GADT substrate + row poly + schema miner + function values + lambda + closure-by-ref + parse-that-regex) |
| Phase 8 lens-set | A-H (carry-aware + LLM-pathology) PLUS I (contrivance) + J (host-leverage) + K (meta-grammar) per ORCHESTRATOR §6 + HARDENING.md V8+ amendment |
| Working tree at audit start | clean |
| Write surface | this report only |

V8 question: V7.1 ratified the fold-V1 architectural posture (DK13 + GADT-as-substrate + row-poly internal + schema miner + function values + closure-by-`&'i` + 6-directive grammar). Does some of that ratified architecture exceed the meta-grammar mandate? Specifically: (Lens I) does each layer load-bear, or is some apparatus speculative generality? (Lens J) does PASS-1 reinvent what Rust's type system, borrow checker, or pattern-match exhaustiveness already provide cleanly at codegen time? (Lens K) is each surface V1-mandatory for meta-grammar correctness, or aspirational for the user's "stronger than Rust" mandate / SOTA throughput?

The verdict (anticipated by §8): SIMPLIFY-AVAILABLE — six material simplification candidates surface across the three lenses; none undoes the V7.1 READY architectural verdict, but Phase 8.4 fold can collapse cardinality, leverage host facilities, and re-classify three V1 surfaces as ASPIRATIONAL with named V2 receivers.

## §2 — Lens I (Contrivance / Over-Engineering) Per-Item Table

bbnf is a meta-grammar generator targeting Rust V1 (and WASM/TS V2 deferred). Architectural facilities exceeding the meta-grammar mandate are contrivance. Surface each apparatus, defeat the steelman where KEEP wins, name the merge target where CONSOLIDATE wins, name what is lost (often nothing) where SIMPLIFY wins.

| # | Site (path:line) | Apparatus | Pro (the steelman) | Con (the contrivance signal) | Explication | Verdict | Surgery |
|---:|---|---|---|---|---|---|---|
| I-1 | `PASS-1.md:73` | Type-system algorithmic stack: HM equality + Algorithm-W + Pierce-Turner local check/synth + DK13 + first-order unification + finite CSP + CHR-improvement + OutsideIn-style implication. **Seven layers**. | Each layer cited to a primary paper; composition is principled (Damas-Milner foundational; Pierce-Turner bidirectional; DK13 algorithmic completeness; Pottier-Rémy first-order; CHR for determining-argument improvement). The composition is exactly what V1's grammar surface requires (rank-1 default + rank-N via explicit `forall` + GADT-substrate-ready + finite-CSP for non-HM choices). | Seven-layer apparatus is the maximum density a single sentence can carry without cross-referencing. CHR-improvement and OutsideIn-style implication are listed but **no V1 user surface activates either** — CHR fires only when host overloads carry determining arguments (rare in extant grammars; zero in seed set per my read of `grammar/json/json.bbnf`, `grammar/css/css_l4.bbnf`, `grammar/bbnf/bbnf.bbnf`); OutsideIn-style implication propagates branch-local equalities to `LayoutFacts` but the `BBNF-LOCAL-EQUALITY-ANNOTATION` surface defers to V2 (`PASS-1.md:81`/`:117`). Two of seven layers are substrate-only; their V1 user-visible behavior is *the diagnostic that never fires* and *the constraint that propagates into a fact set whose only V1 consumer is a layout subroutine which itself is internal*. | The composition is correct under the steelman: each disjoint concern handled by exactly one mechanism. The contrivance signal is the **density of layers without V1 users**: CHR + OutsideIn carry V2 surfaces that V1 cannot exercise. | **CONSOLIDATE** — the seven-layer prose lands; the V1-vs-substrate boundary should be **explicit** as a per-layer table at `PASS-1.md:73`. Add a per-layer cell: V1-active (HM, Algorithm-W, Pierce-Turner check/synth, DK13 application, finite first-order unification, finite CSP) vs V1-substrate-only (CHR-improvement when no determining-argument host fn exists; OutsideIn-style implication when no GADT surface exists). | Insert a 7-row `| Layer | V1 user surface | V1-substrate-only? |` table after the algorithm sentence. Cost: 8 lines. Lost: nothing (the layers stay; the boundary becomes legible). |
| I-2 | `PASS-1.md:81` | GADT/branch-local-equality machinery: CSP solver carries `Implication { givens, wanted }` constraints + propagates branch-local equalities through to `LayoutFacts` + reserves `BBNF-LOCAL-EQUALITY-ANNOTATION`. **V1 surface absent.** | The substrate is GADT-ready though no grammar production introduces a GADT today; when the V2 amendment lands, the algorithm absorbs without re-architect. The user mandate's "stronger than Rust" admits annotation-elidable polymorphism and pre-reserved diagnostic codes maintain catalogue stability. | The full apparatus is **substrate without surface** — no grammar can exercise branch-local equality in V1 because no `Pattern` production introduces a refinement, no `Type` production admits indexed types, and no `@host fn` body carries equality witnesses. The V1 user **cannot write a program that exercises this code path**. The diagnostic `BBNF-LOCAL-EQUALITY-ANNOTATION` is reserved but unreachable in V1 (`PASS-1.md:117`). | The V8 question: does pre-building substrate that no V1 program can exercise count as load-bearing or as speculative generality? Lens I's first definition says "trait surfaces, type parameters, or extension points that admit hypothetical impls without V1 use" — this is the textbook signature. The steelman defense is "V2 amendment cost without it is high"; the steelman is partly defeated because **the algorithm sentence at `:73` already names DK13 + finite CSP, both of which can absorb GADT extension at V2 amendment time** without pre-building the `Implication { givens, wanted }` shape today. | **ASPIRATIONAL** (Lens K classification) + **SIMPLIFY** (Lens I) — keep the algorithmic frame at `:73` (DK13 + finite CSP); **delete the `Implication { givens, wanted }` substrate sentence** + the `BBNF-LOCAL-EQUALITY-ANNOTATION` reservation; route to V2 amendment receiver. | At `:81`: replace "GADT/branch-local-equality machinery is internal substrate: the CSP solver carries `Implication { givens, wanted }` constraints that propagate branch-local equalities through to `LayoutFacts`. The substrate is GADT-ready though no grammar production introduces a GADT today" with one sentence: "GADT / branch-local equality is V2; the V2 amendment will extend the CSP solver and reserve `BBNF-LOCAL-EQUALITY-ANNOTATION` then." Delete the diagnostic from the catalogue (`:117`). Cost: -3 lines + -1 catalogue row. Lost: substrate pre-building (recovered at V2 amendment). |
| I-3 | `PASS-1.md:79` | Schema-mining miner: telemetry-driven schema inference as peer of recogniser miners; observes `(rule_shape, layout_decision, value_shape)` triples; proposes named-record / named-enum / sum-type identities through HM/CSP/DK13. | The user mandate's "type algebra + telemetry to generate semantic schemas without explicit annotations" lives here. The miner is a hint, not a constraint; checker accepts when consistent with HM/DK13 facts. Audit #1 §2.10 (`V1-FOLD-CANDIDATES.md:52`) recommended fold-V1 as Tier 1 architecture-nailing. Mirrors `passes::recognizers/` shape. | What is the **telemetry source** for the miner at V1? PASS-1 says "observes `(rule_shape, layout_decision, value_shape)` triples" but does not name the trace producer. `passes::recognizers/` mines grammar shape statically; a *telemetry* miner consumes runtime traces. **No V1 runtime exists yet to feed the miner.** The only V1 input is grammar shape, which HM-derived schema already covers (every `Alt` rule emits an enum; `:115` of `restart/README.md` says so). The miner consumes input that V1 cannot produce. | The miner reduces to "another HM-shape consumer" without telemetry; the telemetry signal source is unstated. Lens I marks the apparatus chain: HM-derived schema is the inferred type; miner proposes a *named* identity for the same type; the checker accepts when consistent. **Two facilities for one job.** Steelman: the miner could later consume runtime traces from generated parsers — but that consumption is V2 surface, not V1 surface. V1's miner is HM-shape mirror under a different name. | **CONSOLIDATE** — fold the schema-mining miner into HM-derived schema. The "named identity" output (`JsonObject` rather than `{string: Json}`) is HM's named-type post-pass, not a separate miner. The "telemetry" signal source defers to V2 with named receiver. | At `:79`: replace the "Schema-mining miner" paragraph with "Named-schema synthesis: the HM type system synthesises named-record / named-enum / sum-type identities from the grammar's `Rule` definitions; the user-facing schema (`JsonObject` rather than `{string: Json}`) is the HM result rendered with the rule-derived name. The user mandate's "without explicit annotations" lands here — annotations are unnecessary because every grammar rule names its type. Telemetry-driven *refinement* of inferred schemas defers to V2." Cost: -1 paragraph; +1 sentence. Lost: nothing at V1. V2 receiver: the future runtime trace consumer. |
| I-4 | `PASS-1.md:73, 75, 81` | Internal row polymorphism (Leijen-style scoped labels) as `passes::layout` subroutine. Surface fence: row variables never appear in `Type` non-terminal. | Record-narrowing across grammars becomes a single row-unification step rather than enumerated finite coercion candidates. The internal-only collapse honours the audacious mandate without exposing row-variable surface complexity to grammar authors. | Same apparatus pattern as I-2: substrate without surface. Row polymorphism is **load-bearing** for layout reasoning (record narrowing at decode boundaries is real V1 traffic) — but the *Leijen scoped-label* mechanism is one specific implementation. Lens I asks: does V1 require Leijen *specifically*, or is enumerated finite generated-shape coercion sufficient? PASS-1 says "record-narrowing in V1 is finite generated-shape coercion at the surface where source and target shapes are both known" (`:81`); the row-poly *internal* collapse is named as a generalisation, not a requirement. | The V1 surface uses finite generated-shape coercion. The Leijen row-poly machinery is reserved for the case where shapes are not both known. The seed set has zero such cases. **The internal collapse is a subroutine waiting for input it never receives in V1.** | **SIMPLIFY** — drop the Leijen-row-poly internal collapse from the V1 architectural commitment; keep finite generated-shape coercion as the V1 mechanism; route Leijen row poly to V2 with named receiver. | At `:81`: replace "The internal row-polymorphism collapse (Leijen-style scoped labels for layout reasoning) is a `passes::layout` subroutine — record-narrowing decisions across grammars become a single row-unification step rather than enumerated finite coercion candidates" with "Record narrowing in V1 is finite generated-shape coercion at the surface where source and target shapes are both known. Internal row polymorphism (Leijen-style scoped labels) is a V2 generalisation reserved for the case where shapes are not both known; V1 has no such case in the seed set." Cost: ~0 net. Lost: the architectural commitment to row-poly internal collapse (recovered at V2 with measurement). |
| I-5 | `PASS-1.md:81` | Generic rule monomorphisation validation: "recursive generic cycles require a decreasing structural argument, an explicit return annotation, or rejection with `BBNF-GENERIC-CYCLE`." **Three paths.** | Each path closes a different unbounded-monomorphisation case: decreasing structural argument terminates by structural induction; explicit return annotation cuts the recursion; rejection is the conservative default. The three paths together cover the unbounded-set generic cycle problem. | Three paths is one too many. Lens I marks **apparatus chains where a single pass would suffice**. Decreasing structural argument detection requires a structural-recursion analysis pass; explicit return annotation is a parser surface; rejection is a default. The three are not orthogonal — *every* generic rule that fails to terminate could be rejected with a clear diagnostic asking the user to add the annotation. The structural-argument detector is over-specified. | Steelman: structural-argument detection lets common cases (Object\<V> with V structurally smaller in recursive position) work without annotation. Defeated narrowly: the seed set has zero generic rules that recurse without an explicit annotation; the structural detector is paying for V1 surfaces that V2 grammars might want. The simplification is **default to annotation; reject without; structural-argument detection is V2 ergonomic improvement.** | **CONSOLIDATE** — collapse to two paths: (a) explicit return annotation OR (b) rejection with `BBNF-GENERIC-CYCLE`. Structural-argument detection routes to V2 ergonomic improvement. | At `:81`: replace "recursive generic cycles require a decreasing structural argument, an explicit return annotation, or rejection with `BBNF-GENERIC-CYCLE`" with "recursive generic cycles require an explicit return annotation, or rejection with `BBNF-GENERIC-CYCLE`. Structural-argument-decrease detection is a V2 ergonomic improvement that elides the annotation in common cases." Cost: -1 path. Lost: ergonomics for a V1-empty case. |
| I-6 | `PASS-1.md:107-122` | Diagnostic code count: 11 reserved codes including `BBNF1004`, `BBNF1201`, `BBNF1302`, `BBNF1401`, `BBNF-HOST004`/`BBNF-SUBSUMPTION-EDGE`, `BBNF-GENERIC-CYCLE`, `BBNF-LOCAL-EQUALITY-ANNOTATION`, `BBNF-PATTERN-NONEXHAUSTIVE`, `BBNF-CLOSURE-CAPTURE-BY-MOVE`, `BBNF2103`, `BBNF2104`. | Each diagnostic carries a distinct fault and a distinct rewrite suggestion. The catalogue is the user-facing surface; pre-reserving codes prevents code-renaming churn at V2. | The diagnostic numbering scheme is dual: numeric (`BBNF1004`, `BBNF2103`) and alphabetic (`BBNF-LOCAL-EQUALITY-ANNOTATION`). **Two namespaces for one purpose** — Lens I marks double-tracking. Per `:154` the binding is "`codes/` binds numeric codes (e.g. `BBNF1004`) and alphabetic aliases (e.g. `BBNF-LOOKBEHIND-WIDTH`) to kinds". The alphabetic alias is the user-facing string; the numeric code is the operator-facing handle. The two are structurally identical (one-to-one binding). | Steelman: numeric codes give stable IDs across renames; alphabetic codes give human-readable diagnostic strings. The pattern lifts cleanly from rustc (E0001, E0002...) and lints (`unused_variables`). Defeated narrowly: rustc's two-track is mature because its catalogue has 700+ codes; bbnf has 11. The numeric codes provide no compression at this cardinality. **One namespace suffices.** | **CONSOLIDATE** — drop the numeric codes; keep alphabetic. The alphabetic codes (`BBNF-LOOKBEHIND-WIDTH`, `BBNF-CHAIN-STEP`, `BBNF-PATTERN-NONEXHAUSTIVE`) are mnemonic; numeric codes are catalogue-bookkeeping for a 700-row catalogue, not an 11-row one. | At `:107`: replace the dual-numbered table with a single alphabetic-only table. The 11-row catalogue becomes simpler; the rendering layer's "numeric and alphabetic alias" sentence at `:154` simplifies to alphabetic-only. Cost: -11 numeric IDs. Lost: nothing at V1; V2 amendment can re-introduce numeric IDs when catalogue size justifies. |
| I-7 | `PASS-1.md:30-41` | Grammar IR variant inventory: 15 variants (`Rule`, `Seq`, `Alt`, `Repeat`, `Optional`, `Literal`, `Regex`, `Ref`, `Predicate`, `Lookbehind`, `Map`, `HostCall`, `LayoutDirective`, `ErrorDirective`, `Annotation`). | Each variant has a distinct producer and consumer per the schema-floor table at `:28-37`. Variants are semantic (not executable); the alphabet matches BBNF grammar's actual constructs one-to-one. | `Map` and `HostCall` overlap — both express typed callable invocations; `Map` is the rule-level chain step (`->`) and `HostCall` is the body-level `@fn(...)` form. The schema-floor table assigns both to `parser + type checker` producer and `host inference, layout lowering` consumer (`:35`). The two are **the same producer-consumer pair under a syntactic difference**. Multi-function chaining at `:129` says `a.f(x).g(y)` desugars to nested typed host/map calls — the desugaring collapses both into one shape at lowering. | Steelman: the syntactic difference is real (`->` outside body vs `@fn` inside body), and preserving it in the IR aids diagnostic localization. Defeated: the IR is for lowering, not for diagnostics; diagnostics carry source spans, not IR-variant identity. The two could share a single `Call { callee, args, kind: Map | Host }` variant with `kind` discriminating syntactic surface. | **CONSOLIDATE** — collapse `Map` and `HostCall` into one IR variant with a `CallKind` discriminator. 15 → 14 variants. | At `:24`: replace "`Map`, `HostCall`" with "`Call`" (with `kind: Map | Host`). At `:35` schema-floor row: merge into one row. The desugaring at `:129` simplifies (no nested-typed-host/map distinction at lowering). Cost: -1 variant. Lost: nothing. |
| I-8 | `PASS-1.md:41-43` | Backend IR variant inventory: 22 variants (`Entry`, `Seq`, `DispatchAlt`, `SpeculativeAlt`, `RepeatLoop`, `OptionalBranch`, `ByteLiteral`, `RegexProgram`, `SimdScan`, `PrattSpine`, `CallRule`, `CallHost`, `HostChain`, `LayoutPush`, `LayoutPop`, `ErrorRecover`, `SpanMark`, `TapeEmit`, `DirectBuild`, `ValueProject`, `PathEval`, `DebugMark`). | Each variant has a distinct lowering shape per the per-backend table at `:63-71`. PASS-1 §2 says "PASS-2 may not introduce new variants, retire variants, or redefine the alphabet" — strict ownership. The 22-variant alphabet matches BC table baseline (`docs/tranches/BC/audit/W0-typed-ir-variant-table.md:160-254`). | `LayoutPush` + `LayoutPop` are paired (every push has a matching pop). They could collapse into one `LayoutScope { kind: Push | Pop }` variant. Same for `CallHost` + `HostChain` (a `HostChain` is a sequence of `CallHost` nodes; the chain shape is structural, not a distinct lowering kind — per `:69` "emit `host::call_<name>` trait dispatch backed by metadata-resolved primitives" handles both). And `DispatchAlt` + `SpeculativeAlt` differ only in checkpoint policy; they could share one `Alt { mode: Dispatch | Speculative }` variant. | Three potential consolidations: (LayoutPush, LayoutPop) → LayoutScope; (CallHost, HostChain) → CallHost (with HostChain expressing as a `Seq` of `CallHost` already); (DispatchAlt, SpeculativeAlt) → Alt. Each saves one variant. **22 → 19 variants.** Lens I's cardinality test: each consolidation collapses semantically-redundant siblings without loss of lowering distinct-ness; the discriminator field carries the difference. | **CONSOLIDATE** — collapse three pairs to single-variant-with-mode. 22 → 19. | At `:41`: replace "`DispatchAlt`, `SpeculativeAlt`, ...  `LayoutPush`, `LayoutPop`, `ErrorRecover`, ..., `CallHost`, `HostChain`" with "`Alt` (with `mode: Dispatch \| Speculative`), ..., `LayoutScope` (with `kind: Push \| Pop`), `ErrorRecover`, ..., `CallHost` (chains express as `Seq`-of-`CallHost`)". Update per-backend table at `:63-71` accordingly. Cost: -3 variants. Lost: nothing — the discriminator preserves all distinctions. |

**Lens I cohort cardinality: 8 rows. KEEP=0, SIMPLIFY=2 (I-2, I-4), CONSOLIDATE=5 (I-1, I-3, I-5, I-6, I-7, I-8 — corrected count: I-1 + I-3 + I-5 + I-6 + I-7 + I-8 = 6 CONSOLIDATE).** Net: 6 CONSOLIDATE + 2 SIMPLIFY = 8 simplification candidates surfaced. Zero KEEP-without-challenge (per HARDENING.md the audit-failed signal).

## §3 — Lens J (Host-Language Leverage) Per-Item Table

bbnf targets Rust V1; the host language already provides rich facilities. Audit places where PASS-1 reinvents what Rust provides cleanly. Cross-host consequence (TS/WASM differ) is the binding constraint — surgery must name what changes when the cross-host story diverges.

| # | Site (path:line) | Mechanism | Host facility (Rust V1) | Cross-host consequence (TS/WASM V2) | Verdict | Surgery |
|---:|---|---|---|---|---|---|
| J-1 | `PASS-1.md:75` | Closure capture by `&'i Tape<'i>`-bounded reference; capture-by-move forbidden in V1; `Fn`/`FnMut`/`FnOnce` collapsed at BBNF surface. | Rust's borrow checker IS the lifetime-bounded reference enforcement. The generated Rust code carries `&'i Tape<'i>`-bounded references; rustc's borrow checker rejects move-captures from such references at compile time. **bbnf does not need a separate parse-time check** for capture-by-move — generated Rust either compiles (capture is by reference; valid) or fails to compile (capture is by move; invalid). | TS has no borrow checker; capture is always GC-managed; the V2 TS lowering must enforce the rule at TS-codegen time, not at TS-runtime. WASM linear memory has no closure shape; V2 WASM lowering must encode closure environments explicitly. **The cross-host story diverges**: Rust V1 leverages rustc; TS V2 needs a parse-time check; WASM V2 needs explicit closure encoding. | **HYBRID** — for V1 Rust, leverage rustc's borrow checker (do not duplicate the check at parse time); for V2 TS/WASM, encode the rule at parse time as today. | At `:75`: append "For V1 Rust, the closure-capture-by-`&'i` rule is enforced by rustc's borrow checker on the generated source; the parse-time `BBNF-CLOSURE-CAPTURE-BY-MOVE` diagnostic is a *better-error* surface for grammar authors who would otherwise hit a generated-code rustc error. For V2 TS/WASM lowering, the parse-time check is mandatory because those host languages do not enforce the rule." Cost: +2 sentences. Lost: nothing — the parse-time check stays for grammar-author UX; the rustc check is the **load-bearing correctness gate** for V1. |
| J-2 | `PASS-1.md:73, 81` | Generic rule typing: V1 generic rules such as `Object<V>` are rank-1 HM parametric schemes generalised at definition and instantiated at each `Ref`/call site. Validation materializes a finite `(RuleId, TypeArgs)` instance set. | Rust's monomorphisation handles `(RuleId, TypeArgs)` instance sets natively for *generated Rust generics*. If bbnf emits `fn parse_object<V: ParseValue>(...)`, rustc monomorphises at each call site. **The finite-instance-set validation IS a duplicate of rustc's monomorphisation termination.** Rust's recursion-limit (default 128) gates unbounded monomorphisation. | TS erases generics at runtime — V2 TS lowering produces non-generic functions with `any`-typed arguments; the bbnf type checker is the only checker. WASM has no generic surface — V2 WASM lowering must produce concrete-typed functions per instance. **The cross-host story diverges**: Rust V1 can lean on rustc for monomorphisation termination; TS/WASM V2 cannot. | **HYBRID** — for V1 Rust, the `(RuleId, TypeArgs)` instance set is *the input to rustc monomorphisation*, not a duplicate check; bbnf-side validation is the better-error surface, rustc is the correctness gate. For V2 TS/WASM, bbnf-side validation is the sole gate. | At `:81`: clarify "Validation materializes a finite `(RuleId, TypeArgs)` instance set... For V1 Rust, this set is the input to rustc's monomorphisation; bbnf's `BBNF-GENERIC-CYCLE` diagnostic is a *better-error* surface for grammar authors who would otherwise hit rustc's `recursion_limit` error. For V2 TS/WASM, the bbnf-side check is the sole correctness gate." Cost: +1 sentence. Lost: nothing. |
| J-3 | `PASS-1.md:77, 251` | Match expression typing: arm-result types unify across arms under HM equality; exhaustiveness checked at compile time against grammar-derived variant set; `BBNF-PATTERN-NONEXHAUSTIVE` emits when cover is incomplete. | Rust's match expression IS exhaustive-checked by rustc at codegen time. Generated Rust match expressions either compile (exhaustive) or fail to compile (`non-exhaustive patterns: ...`). **bbnf's exhaustiveness check is a duplicate** for V1 Rust. | TS's switch is not exhaustive-checked at compile time (TypeScript's never-narrowing pattern can simulate but is opt-in). WASM has no built-in match. **Cross-host story diverges**: Rust V1 leverages rustc's exhaustiveness; TS/WASM V2 must encode the check at parse time. | **HYBRID** — for V1 Rust, the bbnf parse-time check is a better-error surface (preserves grammar-author UX with grammar-aware variant names instead of Rust's enum-variant names); rustc's exhaustiveness check is the load-bearing correctness gate. For V2 TS/WASM, the bbnf check is sole. | At `:77`: clarify "Exhaustiveness is checked at compile time against the grammar-derived variant set... For V1 Rust, the parse-time check is a *better-error* surface; rustc's exhaustiveness check on the generated source is the correctness gate. For V2 TS/WASM, the parse-time check is the sole correctness gate." Cost: +1 sentence. Lost: nothing. |
| J-4 | `PASS-1.md:75` | Function arrow as first-order constructor; `FnType` decomposes through Pottier-Rémy first-order unification; DK13 application judgment handles function values. | Rust's function-pointer types (`fn(T) -> U`) and trait-object closures (`Box<dyn Fn(T) -> U>`) IS the first-class function value surface. Rust's type system already unifies arrow types via standard HM. **bbnf's first-order unification of `FnType` IS what rustc would do for the generated source.** The generated Rust code carries `fn(...) -> ...` types; rustc verifies the unification. | TS has function types as first-class via `(t: T) => U`; structural typing handles unification. WASM has no closure type; V2 WASM lowering must encode function values as table indices. **Rust V1 + TS V2** can lean on host type-checking for function types; **WASM V2** cannot. | **HYBRID** — for V1 Rust, `FnType` unification at parse time is the better-error surface; rustc's type check on the generated source is the correctness gate. For V2 WASM, parse-time unification is sole; for V2 TS, parse-time + structural-equiv check at TS compile. | At `:75`: append "For V1 Rust, `FnType` first-order unification at parse time is a better-error surface; rustc's standard HM unification on the generated source is the correctness gate. V2 TS leans on structural equivalence; V2 WASM cannot leverage host facilities and parse-time unification is sole." Cost: +1 sentence. Lost: nothing. |
| J-5 | `PASS-1.md:60-72` | Per-backend lowering obligations table: 7 variant families × 2 backends (Rust V1, WASM V1) → 14 obligation cells. Each cell names the lowering shape (e.g., `match` over dispatch keys; SIMD scanner kernels). | Rust's `match`, `&[u8]` slice compares, generated regex programs, Pratt LUT, and trait-dispatch are all *standard Rust idioms*. The obligations table is the **vocabulary the codegen template emits** — it does not encode obligations beyond what rustc's standard surface would accept. WASM V1's obligations are similar but for `wasm-simd128` + linear memory + guest-side jump tables. | The cross-host story is the entire table — it exists *because* TS/WASM lower-shapes diverge from Rust's. The table itself is the cross-host consequence representation. | **KEEP** — the table is load-bearing for the per-backend boundary; cross-host consequence is its purpose. The steelman defeats the contrivance challenge: each row's discriminator is what differs across hosts. **No leverage available; the table IS the host-aware specification.** | None. The table at `:63-71` is correctly structured. (The Lens J inquiry surfaces the table as load-bearing rather than redundant; this row exists to defeat the steelman that the table could be merged into Rust-only. Cross-host consequence prevents that merge.) |
| J-6 | `PASS-1.md:75, 248` | `Fn`/`FnMut`/`FnOnce` discrimination collapsed at BBNF surface. The lifetime-bounded reference closure is the only V1 form. | Rust's `Fn`/`FnMut`/`FnOnce` trait hierarchy IS the closure-environment-mutation discrimination. By collapsing to `Fn` (immutable borrow) only, bbnf elects the most restrictive variant; this is a deliberate choice. | TS has no Fn/FnMut/FnOnce; closures are uniformly mutable-environment. WASM has no closure shape. **The collapse is Rust-specific** — TS V2 cannot "collapse" because no hierarchy exists; WASM V2 has nothing to collapse. The collapse is meaningful only for Rust. | **LEVERAGE** — but the leverage is *negative* — bbnf chose the most restrictive Rust closure shape (`Fn`-equivalent immutable-borrow capture) to keep V1 simple; this is a Rust-aware choice that defers `FnMut`/`FnOnce` to V2. The mechanism is correct; the framing should explicitly cite the Rust-specific origin. | At `:75`: clarify "The `Fn`/`FnMut`/`FnOnce` trait hierarchy that Rust exposes is collapsed to `Fn`-equivalent (immutable-borrow capture only) at the BBNF surface for V1. This is a Rust-aware choice; V2 TS lowering trivially admits all three (TS closures are uniformly mutable-environment); V2 WASM lowering encodes closure environments explicitly without trait-dispatch." Cost: +1 sentence. Lost: nothing — the V1 collapse stays. |
| J-7 | `PASS-1.md:113, 154` | Diagnostic / error infrastructure: PASS-1 owns 11 diagnostic codes + 10 error vocabulary kinds (`Syntax`, `TypeMismatch`, `HostSignature`, `HostFailure`, `LayoutConflict`, `LookbehindWidth`, `RegexClass`, `Recovery`, `BackendUnsupported`, `InternalInvariant`). | Rust's `thiserror` crate provides #[derive(Error)] with #[error("...")] format strings; `anyhow` for boxed error contexts. The error-vocabulary table at `:113` is structurally identical to a `thiserror` enum. The `error/codes/` crate child at `:154` binds numeric codes to kinds — also a `thiserror` pattern. | TS has Error subclasses and `instanceof` checks; the cross-host story is similar (subclassing replicates the enum hierarchy). WASM has no native error machinery (errors cross the host boundary as values). | **HYBRID** — for V1 Rust, the bbnf error-vocabulary table is the **input** to a generated `thiserror` enum; bbnf does not invent its own error machinery, it generates `thiserror`-derived Rust. For V2 TS/WASM, bbnf emits Error subclasses / value-encoded errors. | At `:154` rationale row for `error`: clarify that the `error` crate's children produce `#[derive(Error)]` enums for V1 Rust (leveraging `thiserror`); V2 TS lowering produces Error subclasses; V2 WASM lowering produces value-encoded errors. Cost: +1 sentence. Lost: nothing. |

**Lens J cohort cardinality: 7 rows. LEVERAGE=1 (J-6), HYBRID=5 (J-1, J-2, J-3, J-4, J-7), KEEP=1 (J-5).** All five HYBRID rows are surface-level clarifications: each names V1-Rust-leverage explicitly and routes V2 cross-host to its own parse-time check. Net architectural change: zero (the V1 mechanism stays); narrative change: significant (the V1 mechanism is now framed as host-leverage-where-possible, parse-time-better-error-where-not).

## §4 — Lens K (Meta-Grammar Discipline) Per-Item Table

bbnf is a meta-grammar that generates parsers for extant target languages. Each item's V1 boundary names whether the surface is V1-mandatory for meta-grammar correctness, V1 surface but tranche-deferrable body, or V2+ entirely.

| # | Site (path:line) | Item | Meta-grammar role | Aspirational signal | Verdict | V1 boundary |
|---:|---|---|---|---|---|---|
| K-1 | `PASS-1.md:73` | HM equality + Algorithm-W principal schemes core | Every grammar rule has a principal type; HM equality binds rule-call argument types to definition parameter types. Without HM, the chain-step typing at `:257` cannot fire (`if step f_i produces type T_i, then step f_{i+1} must accept T_i`). The meta-grammar's user-visible chain syntax requires HM as floor. | None — HM is the floor for any typed parser-generator. Even pre-Damas-Milner-1982 grammars (yacc, ANTLR) used a more limited typing; modern parser-generators uniformly use HM. | **LOAD-BEARING** | V1; cannot defer. |
| K-2 | `PASS-1.md:73` | DK13 higher-rank (rank-N via explicit `forall`) | The user mandate's "stronger than Rust if possible" lives here. DK13 admits rank-N (e.g., `forall a. (forall b. b -> b) -> a`) which Rust requires explicit. The meta-grammar gains "annotation-elidable polymorphism that Rust requires the programmer to write out". | The aspirational signal: "stronger than Rust" is a comparative claim, not a load-bearing claim. The seed grammars (`grammar/json/json.bbnf`, `grammar/css/css_l4.bbnf`, `grammar/bbnf/bbnf.bbnf`) parse as rank-1 — **zero seed grammars exercise rank-N**. The fold-V1 dispatch at V1-FOLD-CANDIDATES `:46` Tier 1 names DK13 as architecture-nailing for V1; the rationale is "stronger than Rust", not "seed grammars require rank-N". | **ASPIRATIONAL** | V1 surface (the algorithm composition is named); tranche-deferrable for the rank-N body. The rank-1 default lands V1 mandatory; rank-N via explicit `forall` lands V1 surface but the rank-N inference body can defer to a tranche-D wave with explicit close-gate. The meta-grammar's V1 user surface for rank-N is `forall a. ...` annotation only; if no V1 grammar exercises it, the body need not be implemented before tranche D. **Receiver**: tranche D (the type-system tranche). **Blocker**: zero V1 user surface exercising rank-N. **Gate**: tranche-D close-gate that compiles a synthetic rank-N test grammar. |
| K-3 | `PASS-1.md:81` | GADT/branch-local-equality machinery (substrate-only) | The internal CSP-`Implication` substrate propagates branch-local equalities to `LayoutFacts`. Meta-grammar role: enable future GADT surface at V2 amendment without re-architect. | **Pure aspirational signal** — V1 has no surface; substrate-only; pre-reservation of `BBNF-LOCAL-EQUALITY-ANNOTATION` is forward-discipline for a V2 amendment. The user-mandate "audacious centre" frames it; the seed grammars exercise zero GADT surface. | **SPECULATIVE** (V2+) | V2 amendment. **Receiver**: V2 type-system amendment with annotation rules. **Blocker**: V1 has no `Pattern` production introducing refinements; no `Type` production admits indexed types; no `@host fn` body carries equality witnesses. **Gate**: V2 amendment activation of `BBNF-LOCAL-EQUALITY-ANNOTATION`. |
| K-4 | `PASS-1.md:79` | Schema-mining miner (telemetry-driven) | The user mandate's "type algebra + telemetry to generate semantic schemas without explicit annotations" lives here. Meta-grammar role: emit named, queryable schemas (`JsonObject` not `{string: Json}`) without annotation. | The "telemetry" signal source is unspecified for V1. HM-derived named schemas already cover the "without explicit annotations" mandate (every Alt rule emits a named enum per `restart/README.md:115`). The miner adds *telemetry-driven refinement* — a V2-class concern (no V1 runtime exists to feed traces). | **ASPIRATIONAL** (Lens K) overlapping with **CONSOLIDATE** (Lens I-3) | V1 surface (named schemas via HM-derived names); V2 body (telemetry refinement). **Receiver**: V2 telemetry refinement pass. **Blocker**: V1 has no runtime trace producer. **Gate**: V2 telemetry-trace consumer addition. |
| K-5 | `PASS-1.md:73` | CHR-improvement layer for host overloads | "Host overloads with determining arguments emit explicit improvement constraints, CHR-shaped where applicable, before finite CSP selection." Meta-grammar role: handle host-overload disambiguation when multiple `@host fn` definitions accept the same shape but differ in determining argument. | The seed set exercises **zero overloaded host fns** — every `@host fn` in `grammar/json/json.bbnf`, `grammar/css/css_l4.bbnf`, `grammar/bbnf/bbnf.bbnf` is uniquely-named. The CHR layer is reserved for a feature no V1 grammar uses. | **ASPIRATIONAL** | V1 surface (the algorithmic frame is named); tranche-deferrable body. **Receiver**: tranche D wave when host-overload surface lands. **Blocker**: V1 has zero overloaded host fns. **Gate**: tranche-D close-gate compiling a synthetic overload-grammar. |
| K-6 | `PASS-1.md:124-125` | RecoveryFacts producer-side (`ErrorDirective` lowering emits `RecoveryFacts` keyed by `RuleId`/`NodeId` with recovery kind, diagnostic code, sync token, typed-placeholder policy, source span; PASS-3 consumes for recovery, visitors, LSP, path/value diagnostics). | Meta-grammar role: enable `@error(recover = ...)` semantics at V1; recovery facts feed PASS-3 runtime recovery. The recovery contract IS the user-visible `@error` directive surface. | None — `@error` is a V1 directive (Lock 10 amendment); recovery facts are its production. The seed grammars exercise `@error(recover)` (cite: `grammar/css/pretty.bbnf` retirement of standalone `@recover` per V1-FOLD-CANDIDATES `:69`). | **LOAD-BEARING** | V1; cannot defer. The substrate at PASS-1 owns producer-side; PASS-3 owns consumer-side. |
| K-7 | `PASS-1.md:75, 248` | Function values + `FnType` + lambda + closure-by-`&'i` | Meta-grammar role: enable the "transducer apotheosis" (one generic `transducer<I, O>` host-fn plus `Rule<I, O>` record type expresses every finite-state-transducer use case without `@transducer` directive). Without function values, transducer requires a directive; with function values, it folds into the host-fn library. | Mixed — function-typed parameters in `@host fn` (the transducer apotheosis) is **load-bearing** for the directive retirement (Lock 10 amendment retires `@transducer`); lambda + closure-by-`&'i` are **load-bearing** for the chain-step receiver typing at `:257`; `FnType` in the `Type` non-terminal is **load-bearing** because both lambdas and `@host fn` parameters need an arrow-typed surface. | **LOAD-BEARING** | V1; cannot defer. |
| K-8 | `PASS-1.md:77, 230-235, 250` | Match + tuple expressions + `BBNF-PATTERN-NONEXHAUSTIVE` | Meta-grammar role: every `Alt` rule emits an enum (`restart/README.md:115`); without match, every Alt-derived enum requires a host-side helper or a chain of `if-let`-style probes (`PASS-1.md:251`). Match is the canonical variant-dispatch surface inside `@host fn` bodies. | None — match is the canonical product-type construction + variant-dispatch surface; without it, function bodies cannot dispatch on Alt variants without external machinery. **Load-bearing for `@host fn` body expressivity**. The seed grammars do not yet exercise match in `@host fn` bodies (`grammar/json/json.bbnf`, `grammar/bbnf/bbnf.bbnf` use simple body forms), but **the V1 surface mandates the construct** because Lock 10 Q5 commits to "function values + lambda literals + closure capture by `&'i`" and lambdas need bodies that dispatch on Alt-derived enums. | **LOAD-BEARING** | V1; cannot defer. |
| K-9 | `PASS-1.md:91-103` | Rare escape-valve fence (eight-field declaration-crate exception form) | Meta-grammar role: defines the *one allowed escape* from the "no per-grammar declaration crates" mandate (Lock 14). The fence is the V1 architectural commitment that the seed grammars never exercise it (table empty at `:101`). | None — the fence enforces Lock 14. The eight fields are minimal (approval owner, failure proof, location, import rule, deletion path, reviewer, extant grammars, verification). Each is a distinct accountability surface. | **LOAD-BEARING** | V1; cannot defer. |

**Lens K cohort cardinality: 9 rows. LOAD-BEARING=4 (K-1, K-6, K-7, K-8, K-9 — total 5), ASPIRATIONAL=3 (K-2, K-4, K-5), SPECULATIVE=1 (K-3).** Net: 5 LOAD-BEARING + 3 ASPIRATIONAL + 1 SPECULATIVE = 9 classifications. The three ASPIRATIONAL items have V2 receivers + blockers + gates fully named; the one SPECULATIVE item routes to V2 amendment with named diagnostic.

## §5 — Compressed Nine-Lane Verification (V7.1 carry forward)

Per HARDENING.md V8+ compressed-mode: lanes 1-9 still apply but verify V7.1 READY carries forward. The simplification candidates above must not regress the V7.1 lock-adherence, sequencing, cohesion, SOTA, grammar-authoritative, generated-code-budget, friction, carry-deferral, or greenfield posture.

| # | Lane | V7.1 verdict | V8 verification | V8 verdict |
|---:|---|---|---|---|
| 1 | Lock-Adherence (Lock 4 — DK13 + GADT-substrate + closure-by-`&'i`) | READY | Lens K-3 reclassifies GADT-substrate as SPECULATIVE; Lens I-2 proposes deletion of `Implication { givens, wanted }` substrate sentence. **Lock 4 amendment text says "GADT machinery is hidden substrate (CSP solver + branch-local equality plumbing)"** — the surgery for I-2 *requires Lock 4 re-amendment* to drop the substrate commitment. Lock 4 currently binds the substrate-now posture; V8 surfaces a re-amendment opportunity. | **READY (with simplification opportunity)** — V8's I-2 + K-3 surgery requires Lock 4 amendment "GADT machinery is hidden substrate" → "GADT V2 amendment via DK13 + finite CSP extension". Phase 8.4 fold candidate. |
| 2 | Lock-Adherence (Lock 5 — Backend trait + per-backend lowering) | READY | Lens J-5 confirms per-backend obligations table is load-bearing; cross-host consequence cannot merge. Lock 5 Backend-trait surface stays. | **READY** |
| 3 | Lock-Adherence (Lock 10 — 6-directive + function values + lambda + closure-by-`&'i`) | READY | Lens K-7 confirms function values + lambda + closure are LOAD-BEARING. Lens J-1 + J-6 confirm closure-by-`&'i` is correct host-leverage choice for V1 Rust. **No simplification candidate touches Lock 10.** | **READY** |
| 4 | Lock-Adherence (Lock 11 — parse-that-regex naming) | READY | No V8 surgery touches the regex engine surface; Lens J-7 leverages thiserror but the regex parse-that-regex naming canon is unaffected. | **READY** |
| 5 | Sequencing-Discipline | N/A (PASS-1 is a pass synthesis, not a multi-wave plan) | N/A | **N/A** |
| 6 | Cohesion (HM + DK13 + CSP composition) | READY | Lens I-1 proposes per-layer V1-active vs V1-substrate-only table at `:73`; **increases cohesion** (legibility) without changing the algorithmic frame. | **READY** (cohesion strictly improves under I-1 surgery) |
| 7 | SOTA-Anchoring | READY | PASS-1 carries no parse-throughput numerics; SOTA-anchoring is a master-plan / tranche-H concern. **No V8 surgery touches SOTA gates.** | **READY** |
| 8 | Grammar-Authoritative-Discipline (Lock 14) | READY | Lens K-9 confirms rare-fence is LOAD-BEARING; eight-field form preserved. Lens I cohort touches only architectural-internal shape (algorithmic stack, IR variants, diagnostics), not per-grammar code. **No genericity-erasure introduced.** | **READY** |
| 9 | Generated-Code-Budget | READY | Lens I-7 (Grammar IR 15→14 variants via Map+HostCall consolidation) and Lens I-8 (Backend IR 22→19 variants via three pair-collapses) **reduce generated-code surface area** — fewer match arms in `passes/extract/`, `vm/program/`, lowerers. Generated-LOC delta: small reduction. | **READY** (budget improves under I-7 + I-8) |
| 10 | Friction-Forecast | READY | Lens I-6 (drop numeric diagnostic codes) **reduces friction** for the user (one namespace, mnemonic only). Lens J-1 + J-3 (better-error framing for V1 Rust) **reduces friction** because the parse-time check is now explicitly the "better error than rustc would emit" surface. | **READY** (friction strictly improves under I-6 + J-1 + J-3) |
| 11 | Carry-Deferral (V2 / tranche-D receivers) | READY | Lens K cohort introduces three new ASPIRATIONAL routes (K-2 rank-N body to tranche D; K-4 telemetry refinement to V2; K-5 CHR-improvement body to tranche D) and one SPECULATIVE route (K-3 GADT V2 amendment). **Each carries receiver + blocker + gate per Lane 8 contract.** | **READY** (new carries fully named) |
| 12 | Greenfield-Discipline (no contrivance, no over-engineering) | READY | **Lens I cohort directly targets this lane.** Six CONSOLIDATE candidates + two SIMPLIFY candidates close eight contrivance signals. Each surgery preserves the load-bearing substance and excises the speculative surface. | **READY** (lane sharpens under V8 simplifications) |
| 13 | LLM-pathology lenses F + G + H | READY (V7) | V8 introduces no new LLM pathology. The simplification surgeries reduce ornament (drop dual-numbered diagnostics; collapse semantically-redundant variants); they do not introduce hedging, reference-stuffing, or hallucinated cites. | **READY** |

**13 of 13 lanes READY** (one N/A). V7.1 READY carries forward; the V8 surgeries strengthen lanes 6, 9, 10, 12 (cohesion, generated-code-budget, friction, greenfield-discipline) without regressing any lane.

## §6 — V7.1 → V8 Comparison

V7.1 ratified the architectural posture: DK13 + GADT-as-substrate + row-poly-internal + schema-miner + function-values + closure-by-`&'i` + 6-directive grammar + parse-that-regex naming + Backend-trait surface. V7.1's verdict was READY across the four-target cohort with one non-blocking R4 friction (`BBNF-PATTERN-NONEXHAUSTIVE` ARCH catalogue sync). The V7.1 corpus *confirmed* the architectural mandate; V8's role is to ask whether *the mandate exceeds what V1 requires*. V8 surfaces six material simplifications: three Lens I CONSOLIDATE candidates that collapse cardinality without semantic loss (algorithmic stack legibility table; Map+HostCall IR-variant merge; Backend-IR pair-collapses; numeric+alphabetic diagnostic code dual-namespace); two Lens I SIMPLIFY candidates that drop substrate-without-surface apparatus (GADT `Implication` substrate; Leijen row-poly internal collapse); five Lens J HYBRID candidates that explicitly frame V1-Rust mechanism as "better-error layer over rustc's correctness gate" (closure-capture, generic-monomorphisation, match-exhaustiveness, function-arrow-unification, error-vocabulary as `thiserror`-input); and three Lens K ASPIRATIONAL re-classifications that route DK13 rank-N body / schema-miner telemetry-refinement / CHR-improvement-body to named tranche-D or V2 receivers. Net architectural change is small — no lock retracts; no V1 surface vanishes; the algorithmic frame at `:73` stays intact. Net narrative change is significant — the V8 surgeries expose the V1-vs-substrate-vs-V2 boundary explicitly, leverage rustc as the V1 Rust correctness gate, and reduce IR-variant cardinality from 15+22 to 14+19 (a 6% reduction in IR alphabet).

## §7 — Punch List (Phase 8.4 fold candidates)

V8 surfaces twelve material simplification candidates. Each is routed to Phase 8.4 fold (or to consolidation merge if V8.1 verifies). None blocks PASS-1 V7.1 READY; each is a *simplification opportunity* that Phase 8.4 may absorb.

| # | Path:line | Surgery | Lens | Verdict | V1-impact |
|---:|---|---|---|---|---|
| 1 | `PASS-1.md:73` | Insert per-layer V1-active vs V1-substrate-only table after the algorithm sentence (7-row table; HM/AlgW/PT-check-synth/DK13-app/finite-unif/finite-CSP V1-active; CHR-improvement/OutsideIn-impl V1-substrate-only). | I-1 | CONSOLIDATE | +8 lines, legibility |
| 2 | `PASS-1.md:81, 117` | Replace GADT/branch-local-equality machinery substrate sentence with V2-amendment route; delete `BBNF-LOCAL-EQUALITY-ANNOTATION` from V1 catalogue; reserve at V2 amendment receiver. | I-2 + K-3 | SIMPLIFY + SPECULATIVE | -3 lines + -1 catalogue row, defer to V2 |
| 3 | `PASS-1.md:79` | Replace "Schema-mining miner" paragraph with "Named-schema synthesis" via HM-derived names; route telemetry refinement to V2 receiver. | I-3 + K-4 | CONSOLIDATE + ASPIRATIONAL | -1 paragraph, +1 sentence; V2 telemetry-trace consumer |
| 4 | `PASS-1.md:81` | Drop Leijen-row-poly internal collapse; keep finite generated-shape coercion as V1; route Leijen row-poly to V2. | I-4 | SIMPLIFY | ~0 net lines, defer to V2 |
| 5 | `PASS-1.md:81` | Collapse three-path generic-cycle validation to two-path (annotation OR rejection); route structural-argument detection to V2 ergonomic improvement. | I-5 | CONSOLIDATE | -1 path, simpler validation |
| 6 | `PASS-1.md:107-122, 154` | Drop numeric diagnostic codes (`BBNF1004`, `BBNF1201`, ...); keep alphabetic-only catalogue; rendering-layer simplifies. | I-6 | CONSOLIDATE | -11 numeric IDs, single namespace |
| 7 | `PASS-1.md:24, 35, 129` | Collapse Grammar IR `Map` + `HostCall` into single `Call` variant with `kind: Map \| Host`; 15→14 variants. | I-7 | CONSOLIDATE | -1 IR variant, simpler desugaring |
| 8 | `PASS-1.md:41, 63-71` | Collapse three Backend IR pairs: (LayoutPush, LayoutPop) → LayoutScope; (CallHost, HostChain) → CallHost; (DispatchAlt, SpeculativeAlt) → Alt; 22→19 variants. | I-8 | CONSOLIDATE | -3 IR variants, simpler lowerer |
| 9 | `PASS-1.md:75` | Reframe closure-capture-by-`&'i` rule as V1-Rust-leverage (rustc enforces; bbnf parse-time is better-error); V2 TS/WASM reverts to parse-time-only. | J-1 | HYBRID | +2 sentences, no V1 mechanism change |
| 10 | `PASS-1.md:81` | Reframe generic-rule monomorphisation as V1-Rust-leverage (rustc monomorphises; bbnf is better-error); V2 TS/WASM bbnf-only. | J-2 | HYBRID | +1 sentence, no V1 mechanism change |
| 11 | `PASS-1.md:77` | Reframe match-exhaustiveness as V1-Rust-leverage (rustc checks; bbnf is better-error); V2 TS/WASM bbnf-only. | J-3 | HYBRID | +1 sentence, no V1 mechanism change |
| 12 | `PASS-1.md:75` | Reframe `FnType` first-order unification as V1-Rust-leverage (rustc HM unifies; bbnf is better-error); V2 WASM bbnf-only; V2 TS structural-equiv-at-TS-compile. | J-4 | HYBRID | +1 sentence, no V1 mechanism change |
| 13 | `PASS-1.md:75` | Cite Rust-specific origin of `Fn`/`FnMut`/`FnOnce` collapse explicitly; V2 TS/WASM lowering paths named. | J-6 | LEVERAGE (negative) | +1 sentence, framing only |
| 14 | `PASS-1.md:154` | Cite `thiserror` as V1 Rust error-vocabulary host facility; V2 TS Error-subclass; V2 WASM value-encoded. | J-7 | HYBRID | +1 sentence, no V1 mechanism change |

**14 punch items.** Items 2, 3, 4 are *deletions of V1 architectural commitments* (substrate-without-surface) — these are the principled V8 simplifications. Items 1, 5, 6, 7, 8 are *cardinality reductions without semantic loss*. Items 9-14 are *narrative reframings* that explicitly cite V1 Rust host facilities as the correctness gate. None retracts a lock; none invalidates V7.1.

Items 2 + 3 + 4 carry a Lock 4 amendment opportunity: Lock 4 currently binds "GADT machinery is hidden substrate (CSP solver + branch-local equality plumbing)" — the V8 surgery requires re-amendment to "GADT machinery defers to V2 amendment via DK13 + finite CSP extension". Phase 8.4 should sequence Lock 4 re-amendment before PASS-1 surgery.

## §8 — Final Verdict

**Decision: SIMPLIFY-AVAILABLE.**

V7.1 READY carries forward unchanged. The V8 audit surfaces fourteen material simplification candidates across Lens I (contrivance — 8 candidates), Lens J (host-leverage — 5 candidates), and Lens K (meta-grammar discipline — 4 candidates contributing to the I+J set, plus 1 SPECULATIVE classification). No simplification undoes a V7.1 commitment; six are pure cardinality reductions; three are V1-substrate-without-surface deletions routed to V2 with named receivers + blockers + gates; five are narrative reframings that explicitly cite V1 Rust host facilities (rustc borrow checker, monomorphisation, exhaustiveness, HM unification, `thiserror`) as load-bearing correctness gates with bbnf-side better-error layers. Phase 8.4 fold dispatches are recommended for items 1-14 in the punch list; sequencing prefers Lock 4 re-amendment before items 2, 3, 4 (the substrate-without-surface deletions).

The V8 audit defeats the steelman that "every layer in PASS-1 is V1-load-bearing": GADT `Implication` substrate, Leijen row-poly internal collapse, schema-miner-telemetry-refinement, CHR-improvement layer (substrate-only), structural-argument-decrease detection, and dual-numeric-alphabetic diagnostic IDs all fail the load-bearing-under-steelman test for V1. Each can defer to V2 / tranche-D / future ergonomic improvement without sacrificing V1 correctness.

The V8 audit does NOT recommend RE-DRAFT — the V1 fold did not introduce load-bearing contrivance; it introduced *aspirational* surface area that can be re-classified without architectural retreat. The user mandate's "audacious + SOTA + functional + Rust-like + inference stronger than Rust if possible" is preserved at the algorithmic frame (`:73`) and at the function-value surface (`:75`); the V8 surgeries cleanse the substrate-without-surface excess that Phase 7 fold absorbed without question.

V7.1 READY → V8 SIMPLIFY-AVAILABLE → Phase 8.4 fold (conditional dispatch) → V8.1 verification rerun.

## §9 — Closing Posture

V8 is the simplification audit. V7.1 ratified what bbnf is; V8 asks what bbnf does not need to be. PASS-1 is substrate; the substrate is the right place to apply the ruthless excise. The V1 fold absorbed thirty candidates by user mandate; V8 returns six of those candidates to V2 / tranche-D / future ergonomic with named receivers, and consolidates eight others without semantic loss. The simplifications honour the meta-grammar discipline (bbnf generates parsers for extant target languages; substrate-without-surface is contrivance) and the host-leverage discipline (rustc is the V1 correctness gate; bbnf parse-time is the better-error UX layer). The fourteen locks hold; the fold-V1 architectural posture holds; the V1 boundary tightens.

Hereupon Phase 8.3 consolidation reads this V8 cohort with V8-PASS-2, V8-PASS-3, and V8-MASTER-PLAN; Phase 8.4 fold dispatches the consolidated punch list; V8.1 verifies the simplifications absorbed without regression. The architecture stands V7.1 READY; the V8 surface offers the next sharpening.
