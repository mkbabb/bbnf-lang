---
agent: 1C
pass: T-P1-excavation
cycle: V5-SKV18-totality
cycle_self_label: TOTALITY-EXCAVATION (post SK-V18 GENERALIZATION certification)
generated_at: 2026-06-01T00:00:00Z
spec_surfaces_audited: [ARCHITECTURE.md §9 + §4.3 + §9.1 + §9.2, LOCKS.md Lock 1 + Lock 14, sk-v18/SPEC.md, sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md]
files_audited_count: 142
live_truth_method: "wc -l + rg symbol/header scans + md5 byte-identity + path:line reads over skinny/crates/runtime/src, skinny/crates/codegen/src, crates/core/src/runtime, crates/core/src/grammar/generated"
divergence_count:
  implemented: 8
  unimplemented: 11
  impl_exceeds_spec: 1
  unknown: 4
  partial: 2
  enumeration_note: "Per-row bucket map across the Spec-Claim table (C1-C12), the Divergences section (D1-D8), the new css_types.rs census row (CH2-V4-006), and Open Questions (U1-U4) per CH1-V4-F8. implemented(8) = C4, C9, C10, C11, D4, D1-as-impl-divergence, D2-as-impl-divergence, D3-as-impl-divergence (the R1/R3/R4 rows are IMPLEMENTED-AS-DIVERGENCE — the surface exists, divergent from spec). unimplemented(11) = C1, C6, C8, C2-DIVERGES, C3-DIVERGES, C7-DIVERGES, D5, D6, D7, D8, css_types.rs (CSS-named module in generic core crate, CH2-V4-006). impl_exceeds_spec(1) = C12 (DOWNGRADED from IMPL-EXCEEDS-SPEC to header-scoped partial per CH6-V4-007 — see partial below; the surviving impl_exceeds_spec is the grammar-recognizer-plane comparative note only). partial(2) = C5 (PARTIALLY-IMPL phantom <G>), C12 (@generated-header present; generator round-trip provenance UNKNOWN, routed to 1A-UNK-003 / 1D U-1). unknown(4) = U1, U2, U3, U4. NOTE: the heterogeneous verdict tokens (DIVERGES, IMPLEMENTED-AS-DIVERGENCE) are folded into implemented/unimplemented by impl-surface-existence per the 1A enumeration model; the totals reconcile to the 12 C + 8 D + 1 css_types + 4 U = 25 catalogued rows."
locks_amendment_candidates: 0
---

# 1C — RUNTIME-LAYER Evidence Inventory (T-P1 Totality Excavation)

## Executive Summary (<=200 words)

Two runtime trees exist with OPPOSITE generation truth. `crates/core/src/grammar/generated/<g>.rs` — the 9 totality grammars (bbnf/bnf/css_l4/css_pretty/csv/ebnf/google_sheets/json/math) — are GENUINELY xtask-emitted combinator parsers from `.bbnf` (css_l4.rs 108406 LOC / 191 `parse_` fns; header "AUTO-GENERATED from `[workspace.metadata.bbnf.grammars]`"). The real generator EXISTS for these. But `crates/core/src/runtime/<g>/` is 67 hand-written runtime template files (Pattern H), and the SK-V18 SinkOnly/OffsetTape/EventTape consumption taxonomy ARCHITECTURE.md §9 describes lives ONLY in the **skinny** tree.

In skinny, the SK-V18 audit findings are LIVE: the CSS provider is a verbatim `&str` const courier `CSS_GENERATED_RS` (`runtime_generator.rs:701-1611`, 911 LOC) — the CSS generator DOES NOT EXIST; only JSON projects truly (`json_sink_direct::render`). 7 byte-identical `css_l4_*/generated.rs` replicas (md5 `b654562c…`) differ only in `config.rs` identity strings. A two-emitter fork (`RuntimeEmitterKind{CompiledLowering, RequestFacts}`) is the JSON/CSS divergence. The `G:EventGrammar` AXIS of `ValueRef<…,G:EventGrammar=AnyGrammar>` is a phantom test-only generic (the `K`=Kind axis is real — `_kind: PhantomData<fn()->K>` at `tape/mod.rs:178`, dispatched by `JsonNodeKind`/`RootKind`; only the `G` axis is decorative, see C5/D5). ARCHITECTURE.md §9:1932 "0/9 @generated markers" is STALE — crates/core runtime carries 67.

## Spec-Claim <-> Implementation Table

| # | Spec claim (path:line) | Implementation (path:line) | Verdict | Note |
|---|---|---|---|---|
| C1 | §9.2 "Generated per-grammar runtime lives under `runtime/src/grammars/<grammar>/` … files are template-emitted … not hand-written production crates" (`restart/ARCHITECTURE.md:2077-2091`) | `crates/core/src/runtime/{bbnf,bnf,css_l4,css_pretty,csv,ebnf,google_sheets,json,math}/` = 67 hand-written `.rs` across 9 dirs (`restart/ARCHITECTURE.md:1921-1932` self-documents this as Pattern H) | UNIMPLEMENTED | crates/core runtime is hand-written; §9.2 template shape is the V1 target. §9 admits "live HEAD has zero generated runtime files" at :1912-1919. |
| C2 | §9 Pattern H census: "`@generated` markers 0/9" for every crates/core runtime dir (`restart/ARCHITECTURE.md:1923-1932`) | `rg -ln '@generated' crates/core/src/runtime/` returns 67 files; `crates/core/src/runtime/json/value.rs:1` = "// @generated by xtask regen-json; do not edit by hand." | DIVERGES (spec STALE) | §9 table asserts 0/9 markers; live tree carries 67 `@generated` headers. Marker present but FILES are still the hand-roster (markers do not make them generator output). |
| C3 | §9 Live Pattern H: Lock-14 leak scan "HEAD currently returns 30 sites across 15 files" (`restart/ARCHITECTURE.md:2217-2219`) | `rg -n 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/core/src/runtime/` = 12 sites in 4 `parse_with.rs` (json, css_l4, google_sheets, bbnf) | DIVERGES (count stale within scope) | `restart/ARCHITECTURE.md:2217-2219` scopes the 30/15 figure to `crates/core/src/runtime/{json,bbnf,css_l4,google_sheets}/` (the SAME 4 dirs) and self-cites `1C-runtime-evidence:125`. The live runtime-only scan returns 12/4, so the 30/15 spec count is STALE/over-counted WITHIN the same scope — NOT a wider-scan difference (the asserted cause is dropped; see U2). Leak REAL: `__shape_support_CssL4Parser`, `parse_CssL4Parser_stylesheet` (`crates/core/src/runtime/css_l4/parse_with.rs:4,33,36`). |
| C4 | §9.1 "SK-V18 converges to ONE tape encoding: the PROVEN-AND-BENCHED SoA `Tape<'input>` (`skinny/crates/runtime/src/tape/mod.rs:94`), not the AoS `TapeRec`" (`restart/ARCHITECTURE.md:1961`) | `skinny/crates/runtime/src/tape/mod.rs:98` `pub struct Tape<'input> { source, offsets: Vec<u32>, flag_cursors, flag_values, payloads: PayloadArena, id }` — SoA confirmed | IMPLEMENTED | Genuine SoA tape; the CLEAN substrate the SK-V18 audit confirms (§2 "the unified `Tape`/`ValueRef`/`PayloadArena` substrate (Lock 1 holds)"). |
| C5 | §9.2 lazy `ValueRef<G:EventGrammar>` "the one grammar-parametric `ValueRef<'doc,'input,K,G:EventGrammar>` projection (`skinny/crates/runtime/src/tape/mod.rs:175`) is the unified materialization plane" (`restart/ARCHITECTURE.md:1990-2008`) | `skinny/crates/runtime/src/tape/mod.rs:175` `pub struct ValueRef<'doc,'input:'doc,K=AnyKind,G:EventGrammar=AnyGrammar>` — `G` defaulted to `AnyGrammar`; `_grammar: PhantomData<fn() -> G>`; no real-type instantiation outside `#[cfg(test)]` | PARTIALLY-IMPL (phantom) | Struct exists; `G` decorative. SK-V18 addendum-4 REJECT: "instantiate-or-delete" (R5). |
| C6 | §9 materialization model `{ OffsetTape \| EventTape \| SinkOnly \| CollapsedStage }` (`restart/ARCHITECTURE.md:2043-2049`); Output-Plane taxonomy `EagerTape/OffsetTape/EventTape` + `SinkOnly` (`restart/ARCHITECTURE.md:1898-1903`) | crates/core/src/runtime/tape/ exposes `arena/cursor/record/mod`, NO `SinkOnly`/`OffsetTape`/`EventTape` variants; skinny json uses `parse_direct<S: JsonSink>` sink trait + offset `TapeBuilder` (`skinny/.../json/generated.rs`, `json/parser.rs`) | UNIMPLEMENTED (named variants) | 4-plane consumption taxonomy is V1-target prose; live tape exposes arena/cursor/record. Skinny realises a JSON sink + offset tape only. |
| C7 | §4.3 crate layout `runtime/src/{tape, document, builder, visitor, grammars, support}/` (`restart/ARCHITECTURE.md:502-510`) | crates/core actual: `runtime/{tape, <9 grammar dirs>}/`; NO top-level `document/`/`builder/`/`visitor/`/`support/`; those concerns are per-grammar `document.rs`/`builder.rs`/`view.rs` | DIVERGES | §4.3 shared-module layout not realised; each grammar dir carries its own builder/document/view (Pattern H replication vector). |
| C8 | SK-V18: ONE grammar-driven generator emits JSON+CSS+Sheets from `.bbnf` (`SYNTHESIS-AUDIT-OVERFIT.md:23-39`) | skinny `runtime_generator.rs:16-26` forks: `CompiledLowering` → JSON `json_sink_direct::render(sink_only)` (:37) vs `RequestFacts` → CSS verbatim couriers (:91-94) | UNIMPLEMENTED | Un-forked single generator is SK-V18 G3 target; HEAD is the two-emitter fork (R3). Sheets has no generator path (24-LOC `sheets_witness` stub only). |
| C9 | SK-V18 addendum-1: verbatim `&str` literal in codegen = hand-written (`SYNTHESIS-AUDIT-OVERFIT.md:50`); R1 "CSS const-`&str` courier (generator does not exist)" | `skinny/crates/codegen/src/runtime_generator.rs:701` `const CSS_GENERATED_RS: &str = r#"` … `:1611` `"#;` = 911 verbatim LOC; emitted `normalize(CSS_GENERATED_RS)` at :91 | IMPLEMENTED-AS-DIVERGENCE (R1 LIVE) | Audit named `CSS_GENERATED_RS` exactly; verified :701-1611. Sibling couriers `CSS_MOD_RS:598`, `CSS_PARSER_RS:612`, `CSS_SINK_RS:665` — 4 verbatim CSS blobs. |
| C10 | SK-V18 addendum-2 / R4: "7 byte-identical css_l4 replicas" (`SYNTHESIS-AUDIT-OVERFIT.md:88`) | md5 of `skinny/crates/runtime/src/grammars/css_l4_*/generated.rs` = `b654562ccff46ed62dd48e9ace325830` × 7; `parser.rs`/`sink.rs`/`mod.rs` also identical; only `config.rs` differs (ROW_ID/REQUEST_PROFILE, all ENTRY_RULE="stylesheet") | IMPLEMENTED-AS-DIVERGENCE (R4 LIVE) | Confirmed by md5. The 7 differ ONLY in config identity → disk evidence is collapse-to-ONE CSS grammar (matches R-A0-2 "one `stylesheet.bbnf`, byte-identical output"). |
| C11 | Lock 1 "Tape is the substrate, properly unioned with direct-to-struct … one materialisation surface; one Visitor pattern; no parallel substrate" (`restart/locks/LOCKS.md:75`) | skinny: single `Tape`/`ValueRef`/`TapeBuilder`/`PayloadArena`; CSS provider routes into EXISTING offset tape (`CSS_GENERATED_RS` body :702 `use crate::tape::{OffsetFlags, Tape, TapeBuilder, ValueRef}`), no second cursor/sidecar | IMPLEMENTED | Lock 1 substrate-union holds at runtime (audit §A5 "substrate REAL"); divergence is CODEGEN provenance, not substrate. |
| C12 | 9 totality grammars genuinely generated (totality goal; tee-up `SYNTHESIS-AUDIT-OVERFIT.md:276-279`) | `crates/core/src/grammar/generated/`: bbnf 21557 LOC/55 fns, bnf 3304/7, css_l4 108406/191, css_pretty 9052/17, csv 1702/6, ebnf 7659/15, google_sheets 13861/39, json 3505/11, math 875/3 — all "AUTO-GENERATED from `[workspace.metadata.bbnf.grammars]`" | partial / @generated-header present; generator round-trip provenance UNKNOWN | DOWNGRADED from IMPLEMENTED (IMPL-EXCEEDS-SPEC) per CH6-V4-007: the ONLY live witness is the `//! AUTO-GENERATED from [workspace.metadata.bbnf.grammars]` header comment + LOC/fn counts — NO regen round-trip, generator manifest, or byte-equivalence proof. A closure verdict resting on the `@generated`/`AUTO-GENERATED` header alone is the EXACT provenance fallacy C2/D4 (this same inventory) rejects ("markers do not equal generator output"). The round-trip provenance closure is routed to 1A-UNK-003 (`1A-substrate-evidence.md:188`) / 1D U-1 (`1D-skinny-lessons.md:213-225`), kept OPEN. COMPARATIVE NOTE (not a closure verdict): crates/core's grammar/recognizer plane is AHEAD of skinny's runtime plane (which still lacks the un-forked CSS generator). |

## Divergences Catalogued

### D1 — CSS provider is a verbatim const courier; the CSS generator does not exist (R1, HIGH)
`skinny/crates/codegen/src/runtime_generator.rs:701` `const CSS_GENERATED_RS: &str = r#"` … `:1611` `"#;` — 911 LOC of hand-written Rust embedded as a raw string literal, emitted byte-for-byte via `normalize(CSS_GENERATED_RS)` at `:91`. Sibling CSS couriers: `CSS_MOD_RS:598`, `CSS_PARSER_RS:612`, `CSS_SINK_RS:665`. Contrast the JSON path `emit_compiled` (`:29-74`) which calls `json_sink_direct::render(sink_only)` (`:37`) — a true projection from `SinkOnlyProgram`. **The CSS `.bbnf` is never consumed to produce `generated.rs`**; `render_request_facts_config` (`:105-134`) emits only 8 request-IDENTITY constants (ROW_ID, ENTRY_RULE, source_hash, counts), NOT the parser. ARCHITECTURE.md §9 explicitly acknowledges the gap ("§9 generated runtime template prose below states the V1 target", :1914).

### D2 — Two-emitter grammar-family fork (R3, HIGH)
`skinny/crates/codegen/src/grammar_provider.rs:40-42` `pub enum RuntimeEmitterKind { CompiledLowering, RequestFacts }`. Dispatch at `runtime_generator.rs:16-26`: `CompiledLowering` → JSON real lowering; `RequestFacts` → CSS const courier. The grammar-family fork SK-V18 addendum-3 ("one grammar-agnostic emitter; flag grammar-family forks") REJECTs. SK-V18 G3 un-forks this.

### D3 — 7 byte-identical css_l4 runtime replicas (R4, HIGH)
`skinny/crates/runtime/src/grammars/css_l4_{at_rules_and_media, declaration_values, declaration_values_extended, nested_layout, stylesheet_selectors, vendor_and_custom_atrules, visual_functions}/generated.rs` — all md5 `b654562ccff46ed62dd48e9ace325830`. `parser.rs` (md5 `77d1fa6e…`), `sink.rs` (`2b46c086…`), `mod.rs` (`efa04189…`) likewise identical across all 7. Only `config.rs` varies (ROW_ID/REQUEST_PROFILE per `css_l4_*/config.rs:2-4`). All 7 carry ENTRY_RULE="stylesheet" → ONE grammar replicated 7× under distinct profile names (~5460 LOC of replication per SK-V18 P3).

### D4 — ARCHITECTURE.md §9 `@generated`-marker census STALE (DIVERGES, spec defect; loc_delta ≈0 — spec-table re-count, no source LOC)
`restart/ARCHITECTURE.md:1923-1932` asserts "0/9" `@generated` markers and Total "0/9". Live: `rg -ln '@generated' crates/core/src/runtime/` = 67 files; e.g. `crates/core/src/runtime/json/value.rs:1` "// @generated by xtask regen-json; do not edit by hand." Marker now PRESENT (files remain hand-roster — markers do not equal generator output). The §9 table predates the regen-header landing.

### D5 — Phantom `G: EventGrammar` generic (R5, HIGH; loc_delta ≈ −10..−40 — DELETE the `<G>` axis + `_grammar` field across `ValueRef` decls, preserve the `K` axis)
`skinny/crates/runtime/src/tape/mod.rs:175` `pub struct ValueRef<'doc,'input:'doc, K=AnyKind, G:EventGrammar=AnyGrammar>` with `_grammar: PhantomData<fn() -> G>`. `G` defaults to `AnyGrammar`, never instantiated with a real grammar type outside `#[cfg(test)]` (`_proof_compiles::<JsonEventGrammar>` is test-only). SK-V18 addendum-4 "instantiate-or-delete". The `K` (Kind) axis IS real; only the `G` axis is decorative.

### D6 — crates/core runtime crate-layout divergence (C7, DIVERGES; loc_delta ≈ −4000..−5000 for the 9×→1× shared-module fold of the ≈6867-LOC Pattern-H surface across 67 hand-written files — `find crates/core/src/runtime -mindepth 2 -name '*.rs' -not -path '*tape*'` — collapsing the 9× per-grammar `document.rs`/`builder.rs`/`view.rs` into shared `runtime/src/{document,builder,visitor}/` (estimate: 9 near-identical copies fold to 1 shared module + thin per-grammar deltas, so ≈8/9 of the duplicated carrier retires; receiver = SK-V19 totality adoption fold, matching U-1's verify_action — bounded per CH4-V3-010, NOT priced as a SK-V18 wave). CH4-V2-009: this is the TOTALITY `crates/core` tree, a DIFFERENT surface from the skinny css_l4 7-replica collapse the SK-V18 P3 −5460 budget covers (`SYNTHESIS-AUDIT-OVERFIT.md:169`); the P3 figure is an ANALOGY for the de-dup pattern, NOT this row's budget.)
§4.3 (`restart/ARCHITECTURE.md:502-510`) specifies shared `runtime/src/{document, builder, visitor, support}/`. Live crates/core has NO such shared dirs; `document.rs`/`builder.rs`/`view.rs` are replicated PER grammar dir. One `builder/`, one `visitor/` is the V1 target; HEAD is 9× duplication (the Pattern H surface).

### D7 — Metalang leak in generated JSON runtime (R15, MEDIUM; loc_delta ≈0 — rename-only `parse_w11_1_number_*`→`parse_number_*` at source, SK-V18 P5)
`skinny/crates/runtime/src/grammars/json/generated.rs:801,841,881,955,1007,1019,1031` reference `parse_w11_1_number_*` — a tranche/corpus tag (`w11_1` = SK-V13 W11.1) leaked into shipped runtime function names. SK-V18 P5 PRUNE renames `parse_w11_1_number_*` → `parse_number_*` at source (`json_sink_direct.rs`).

### D8 — Sheets has no generator path; only a 24-LOC witness stub (UNIMPLEMENTED; loc_delta ≈ +200 — emit a real Sheets runtime through the un-forked generator, SK-V18 PROVE wave; the cost basis is the SK-V18 PROVE-wave row `restart/skinny/tranches/sk-v18/SPEC.md:443` = `≈ +200 Sheets adoption (≈+30 grammar-root + xtask; the rest generated, named + diff-audited)`, NOT the separate G3 `≤450` un-fork band at `:440` — re-keyed to the owning PROVE wave per CH4-V3-008, G3-band line corrected `:442`→`:440` per CH4-V5-007)
`skinny/crates/runtime/src/grammars/sheets_witness/event_grammar_witness.rs` = 24 LOC; `mod.rs` = 1 LOC. NO `runtime_generator.rs` branch emits a Sheets runtime from `.bbnf`. SK-V18 PROVE wave requires Sheets to emit THROUGH the un-forked generator. The §9.2 prose flagging this stub "cannot serve as exercise" (`restart/ARCHITECTURE.md:2003`) is accurate.

### D9 — grammar-named CSS module in the generic core crate (CH2-V4-006; UNIMPLEMENTED; loc_delta ≈ −66 relocate-or-delete)
`crates/core/src/css_types.rs:1` "Host shims for the CSS L4 grammar's `-> parse_hex_color(...)` map" (66 LOC) is a CSS-L4-named host-shim module living IN the generic `core` crate (`crates/core/src/...`, 1C's audited runtime tree), and `restart/locks/LOCKS.md:349` names it VERBATIM as "the failure mode this lock prevents from recurring." The other Pattern-H runtime leaks (C2/C3 `__shape_support_CssL4Parser`, `parse_CssL4Parser_stylesheet`) are catalogued, but this grammar-named MODULE was not. Lock 14 (c) does NOT apply — it admits only a separate `crates/<grammar>/` declaration crate, whereas `css_types.rs` is in the generic `crates/core/src/`. Cross-ref `1F-coherence` COH18-006 / `1F-anti-pattern` `css_types.rs` row / U-COH18-002. Disposition: SK-V19 relocate-to-`crates/css/`-declaration-crate (admissible) or delete; not admissible as-is.

## Gaps / Missing Primitives

- **G-A — CSS-from-`.bbnf` lowering primitive (absent).** No code path lowers the CSS `.bbnf` to `generated.rs`; entire CSS runtime is the `CSS_GENERATED_RS` const. The JSON `SinkOnlyProgram → json_sink_direct::render` projection has NO CSS analogue. Central SK-V18 G2 gap.
- **G-B — Grammar-agnostic single emitter (absent).** `RuntimeEmitterKind` is a two-branch fork, not one grammar-neutral emitter. No shared `emit_runtime(grammar, facts)` handling JSON+CSS+Sheets uniformly.
- **G-C — Real `EventGrammar` instantiation (absent outside tests).** The `G` type parameter has no production consumer; the unified grammar-parametric materialization plane §9.2 claims is not yet exercised by ≥2 real grammars.
- **G-D — Named BackendShape consumption variants in crates/core tape (absent).** crates/core/src/runtime/tape exposes `arena/cursor/record/mod` but NOT `SinkOnly`/`OffsetTape`/`EventTape`/`CollapsedStage`; skinny realises a JSON sink + offset tape only.
- **G-E — Shared runtime support modules (absent).** §4.3 `runtime/src/{document,builder,visitor,support}/` shared dirs do not exist; concerns are per-grammar-replicated (Pattern H).

## Open Questions (UNKNOWN -> verify_action)

- **U1 — Is `crates/core/src/runtime/<g>/` reachable/built, or dead alongside the genuinely-generated `grammar/generated/<g>.rs`?** Two runtime planes coexist (hand-written `runtime/<g>/` vs generated `grammar/generated/<g>.rs`). verify_action: `rg -n 'mod runtime' crates/core/src/lib.rs` + trace which plane the bench/test harness parses through; if `runtime/<g>/` is unreferenced it is a deletion candidate, not a divergence.
- **U2 — Why does the §9:2217 "30 sites across 15 files" figure exceed the live runtime-only 12/4?** `restart/ARCHITECTURE.md:2217-2219` scopes the 30/15 figure to the SAME 4 dirs (`crates/core/src/runtime/{json,bbnf,css_l4,google_sheets}/`) the live scan covers AND self-cites `1C-runtime-evidence:125` — so the gap is a STALE spec count (or a circular self-citation), NOT a wider-`crates/`-scan artefact. verify_action: re-run `rg -n 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/core/src/runtime/{json,bbnf,css_l4,google_sheets}/` and reconcile the live 12/4 against the spec's 30/15; if they still disagree at the same scope, the §9 figure is stale and the spec line must be re-counted.
- **U3 — Are the 7 css_l4 skinny profiles backed by 7 distinct `.bbnf` roots or one?** Disk evidence (identical generated.rs, all ENTRY_RULE="stylesheet") says ONE; SK-V18 R-A0-2 defers collapse-vs-differentiate to S-P3. verify_action: `find skinny -name '*.bbnf' -path '*css*'` + diff roots; if one root, the 7 profiles are a packaging artefact, not 7 grammars.
- **U4 — Is the skinny "3 grammars" target (json+css+sheets) subsumed by crates/core's 9-grammar generated plane?** crates/core has 9 REAL generated grammar parsers; skinny has JSON-real + CSS-courier + Sheets-stub. verify_action: determine whether SK-V18's "3 grammars" is the skinny-runtime scope while crates/core/grammar/generated already proves the 9-grammar generator (runtime-vs-recognizer plane distinction). Bears on whether totality fold REUSES crates/core's generator for skinny runtime.

## (1E-only) LOCKS-amendment candidates
NONE. Scope 1C is RUNTIME-LAYER evidence; LOCKS-amendment candidates are surfaced by 1E (LOCKS-layer) only. 1C holds no amendment authority. Lock 1 substrate-union and Lock 14 generated-template clauses are CITED as spec surfaces; their amendment disposition is out of 1C scope.
