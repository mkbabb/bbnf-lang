---
lens: CH5
name: HIDDEN COUPLING
pass: T-P3-synthesis
cycle: V1
generated_at: 2026-05-21T19:30:06Z
files_audited:
  - restart/prompts/totality/PASS-3-SYNTHESIS.md
  - restart/audit/totality/p3/3A-architecture-synthesis.md
  - restart/audit/totality/p3/3B-master-plan-reconciliation.md
  - restart/audit/totality/p3/3C-locks-crystallisation.md
  - restart/audit/totality/p3/3C-locks-v+1-diff.md
  - restart/audit/totality/p3/3D-skinny-fold.md
  - restart/audit/totality/p3/3E-grammar-generalisation.md
  - restart/audit/totality/p3/3F-migration-handoff.md
scope: "CH5 hidden coupling only"
---

# CH5 Hidden Coupling - T-P3 V1

## Verdict

ACCEPT.

T-P3 V1 passes the CH5 hidden-coupling lens. No proposed delta implies a
parallel substrate, sidecar producer, renamed-scanner Lock 1 violation,
Track 1 equals Track 2 dishonesty, or accepted-amendment coupling. The
substrate union is coherent across 3A, 3C, 3E, and 3F: 3A proposes the
taxonomy, 3C makes it enforceable in Lock 1/8/14/16, 3E uses it for
non-JSON generality without adding a sixth shape, and 3F carries it into
MIGRATION/HANDOFF only behind G3, Pass Omega, and G-Omega.

## Evidence

| check | disposition | evidence |
|---|---|---|
| Pass contract | ACCEPT | PASS-3 defines CH5 as the guard against parallel substrate, sidecar producer, renamed-scanner Lock 1 violation, Track 1 equals Track 2 dishonesty, and accepted-amendment coupling (`restart/prompts/totality/PASS-3-SYNTHESIS.md:122`). It also bars new directives, BIR variants, and new substrates from being silently synthesized into the spec (`restart/prompts/totality/PASS-3-SYNTHESIS.md:214`). |
| 3A substrate union | ACCEPT | 3A explicitly preserves the no-new-directive/BIR/public-substrate/retained-sidecar/BackendShape-expansion gate (`restart/audit/totality/p3/3A-architecture-synthesis.md:27`). Its direct route requires same schedule, same-plane digest equality, independent Track 2 parity, and same-run comparators (`restart/audit/totality/p3/3A-architecture-synthesis.md:37`). It also separates transient scanner/capacity inputs, comparator sidecars, admitted fact-stream outputs, and retained runtime substrate (`restart/audit/totality/p3/3A-architecture-synthesis.md:38`), and keeps parse-that scanner outputs local-temp or existing-tape/direct-sink unless G-Omega approves a new public substrate/BIR surface (`restart/audit/totality/p3/3A-architecture-synthesis.md:42`). |
| 3B planning watchpoint | ACCEPT | 3B's fresh union material-differential wave is planning pressure, not current substrate permission: it requires row movement or architectural-block evidence (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:135`), and 3B says implementation waves must retain same-wave consumers, LOC budgets, and abrogate gates (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:144`). This remains acceptable only under 3C's Lock 1 fence. |
| 3C accepted amendments | ACCEPT | 3C's executive summary says the LOCKS diff adds no directive, BIR variant, BackendShape, public substrate API, or new lock, and keeps Pass Omega/G-Omega boundaries explicit (`restart/audit/totality/p3/3C-locks-crystallisation.md:23`). The disposition matrix accepts transient-mask and retained-sidecar prohibition into Lock 1 (`restart/audit/totality/p3/3C-locks-crystallisation.md:67`), routes non-JSON telemetry through BENCH gate consumers (`restart/audit/totality/p3/3C-locks-crystallisation.md:68`), keeps CSS fact streams as evidence not closure (`restart/audit/totality/p3/3C-locks-crystallisation.md:70`), and requires `substrate_target`, `retention_lifetime`, and `policy_owner` for primitive/scanner/union rows (`restart/audit/totality/p3/3C-locks-crystallisation.md:77`). |
| 3C Lock 1 fence | ACCEPT | The proposed Lock 1 hunk says Track 2 is a substrate-ceiling probe, not a second substrate, and does not authorize hidden runtime identity, parser-owned sidecars, or parallel representation (`restart/audit/totality/p3/3C-locks-v+1-diff.md:59`-`63`). Fact streams are output-plane contracts, not retained internal sidecars (`restart/audit/totality/p3/3C-locks-v+1-diff.md:75`-`78`). Every e-graph candidate, backend rewrite, imported scanner plan, union candidate, and SIMD consumer must declare allowed target/lifetime/owner fields, and any retained class/mask stream, parser-owned cursor/list state, public substrate API, `UnionTape`, or second tape is rejected unless G-Omega amends Lock 1 (`restart/audit/totality/p3/3C-locks-v+1-diff.md:82`-`90`). |
| Comparator and Track planes | ACCEPT | Lock 8 makes JSON parse/direct/typed rows separate gates, treats CSS as one fact-stream row rather than full closure, and restricts sidecars to strict anchors with matching corpus, output plane, host, strictness, freshness, status, and gate-consumed provenance (`restart/audit/totality/p3/3C-locks-v+1-diff.md:140`-`156`). Direct digest or SIMD sub-hash acceleration is admissible only when Track 1, Track 2, serde, and sonic strict equality hold for the same semantic output plane (`restart/audit/totality/p3/3C-locks-v+1-diff.md:168`-`173`). |
| Renamed scanner / parse-that | ACCEPT | Lock 10 requires regex/HIR facts where regex or scanner plans influence backend-shape or scanner selection, and blocks opaque-pattern-only SIMD/scanner/backend admission (`restart/audit/totality/p3/3C-locks-v+1-diff.md:209`-`215`). Lock 16 routes parse-that facades, generated scanners, and collapsed-stage code through manifest rows that include strict checkasm/parity, substrate target, retention lifetime, policy owner, same-wave consumer, rollback, and final disposition (`restart/audit/totality/p3/3C-locks-v+1-diff.md:337`-`345`). |
| 3D skinny fold | ACCEPT | 3D folds JSON offset tape, direct SinkOnly projection, and CSS fact-stream rows into one substrate family with fenced output planes while preserving the ban on parallel substrates (`restart/audit/totality/p3/3D-skinny-fold.md:68`). It also routes the decision-engine replacement as not a new directive, BIR, BackendShape, or substrate (`restart/audit/totality/p3/3D-skinny-fold.md:75`) and verifies that non-JSON import consumes existing five shapes rather than creating hidden variants (`restart/audit/totality/p3/3D-skinny-fold.md:89`). |
| 3E generality | ACCEPT | 3E keeps the five BackendShape variants and makes selection, primitive policy, sinks, flags, providers, and costs generated from grammar data (`restart/audit/totality/p3/3E-grammar-generalisation.md:23`-`38`). The matrix treats `CollapsedStage` only as a generated transient emitted strategy, never a retained sidecar (`restart/audit/totality/p3/3E-grammar-generalisation.md:75`). Future grammar onboarding fails closed if it needs a new directive, BIR variant, BackendShape, public substrate API, retained sidecar, or hand-coded generic behavior (`restart/audit/totality/p3/3E-grammar-generalisation.md:114`-`117`). 3E-D05 classifies CSS fact streams as admitted output planes, not retained sidecars and not a sixth BackendShape (`restart/audit/totality/p3/3E-grammar-generalisation.md:140`). |
| 3F migration and handoff | ACCEPT | 3F narrows Lock 1 closure to admitted grammar-runtime substrate with fenced non-substrate fact streams (`restart/audit/totality/p3/3F-migration-handoff.md:50`-`55`). It replaces rename/defer-only rows with archive/removal/source-present gates (`restart/audit/totality/p3/3F-migration-handoff.md:77`), reclassifies hand-owned per-grammar runtime surfaces as generated/rostered/archived/fixture-only (`restart/audit/totality/p3/3F-migration-handoff.md:79`), and adds a CSS telemetry/fact-output plane instead of forcing CSS into EventTape or retained substrate categories (`restart/audit/totality/p3/3F-migration-handoff.md:80`). Its proposed handoff blocks source/generated/gate/RESULTS/REDRESS edits until G3, Pass Omega, and G-Omega conditions are met (`restart/audit/totality/p3/3F-migration-handoff.md:110`-`120`, `restart/audit/totality/p3/3F-migration-handoff.md:141`-`155`). |

## Repairs

Required blocking repairs: none.

Carry-forward guardrails for Pass Omega and later S-P3/Omega work:

1. Preserve 3C's exact Lock 1 target/lifetime/owner vocabulary. Do not relax
   it into generic "scanner facts" or "runtime facts" wording.
2. Read 3B MP.NW10 only as a fresh material differential inside the existing
   tape/direct/admitted-output discipline, or as a G-Omega-approved Lock 1
   amendment/block. It is not permission for `UnionTape`, a second tape, or a
   retained class-column substrate.
3. Keep CSS lightningcss/source sidecars comparator-only. Every future CSS row
   must emit output-plane provenance and must not add a runtime dependency on
   comparator sidecars.
4. Keep Track 2 language precise: Track 2 is an independent parser-path and
   substrate-ceiling probe over the same runtime APIs, not an independent
   substrate and not evidence that Track 1 equals Track 2.

## Cycle Disposition

CH5 disposition for T-P3 V1: ACCEPT.
