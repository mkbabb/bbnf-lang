---
agent: 2D
pass: T-P2-research
cycle: V3-SKV18-totality
generated_at: 2026-06-01T00:00:00Z
t_p1_inventories_consumed: [1A, 1B, 1C, 1D, 1E, 1F]
primary_sources_cited: 11
techniques_grounded: 9
techniques_refuted: 4
locks_amendment_candidates: 4
sk_cycle: SK-V18
host_close_route: Apple-M5-Max-aarch64
scope: 2D-cost-model + 5-shape-BackendShape + R-A-un-fork + emit_shape_source-firewall
prior_cycle_dispositions_folded:
  accepted:
    - "V2 five-shape BackendShape canon (EagerTape/OffsetTape/EventTape/SinkOnly/CollapsedStage) — re-verified live at lower/mod.rs:18-24, SPEC.md:75-78."
    - "V2 LAC-2D-04 — exactly five shapes, no sixth — carried verbatim (1E no-candidates axis confirms)."
    - "V2 equality-saturation / BURG / Mison / simdjson / OR-Tools source spine."
  superseded:
    - "V2 'zero-rule e-graph scaffold' verdict — SK-V15 LANDED one asserted rewrite (NormalizeDirectSinkCost, instantiated backend_egraph.rs:75, struct+impl :191-:193); the activation refutation no longer holds at HEAD, re-grounded below."
    - "V2 'four marker-string lowerers' verdict — SK-V15 W8/W9 landed real lowerers (collapsed_stage.rs:16 now delegates to tape_plan::render_rule); the marker refutation no longer holds."
  revised:
    - "V2 SK-V15 wave-owner columns (W7/W8/W9) re-keyed to the LIVE SK-V18 §8 wave ledger (P4/P5/G1/G2/G3/G4/G5-G6/PROVE/H1 — SPEC.md:436-444); the cost-model R-A un-fork is wave_owner=G3 (≤450 hand LOC, HIGH-risk, ≤90 min wall / 30 min redress, documented larger cap if §5-risk-1 seam fires). The cost-model work is now DERIVED-not-new — the engine exists, G3 RELOCATES the discriminator, it does not rebuild the engine."
---

## Executive Summary

This V3 extension folds the SK-V18 GENERALIZATION lens onto the cost-model
spine the V2 cycle grounded. Two V2 refutations are now STALE at HEAD: SK-V15
SK-V15 landed one asserted e-graph rewrite (`NormalizeDirectSinkCost`,
instantiated `backend_egraph.rs:75`, struct+impl `:191-:193`) run through a
`BackoffScheduler` (`:73`) and extracted via a
`DecisionCostModel`, and W8/W9 landed real lowerers (CollapsedStage delegates to
`tape_plan::render_rule`, no longer a marker). The Decision Engine therefore
EXISTS as the egg-style equality-saturation pipeline the literature defends
(Willsey et al. POPL 2021; Tate et al. POPL 2009) with a BURG-style
dynamic-programming cost extractor (Fraser/Hanson/Proebsting LOPLAS 1992) and an
OR-Tools-style CSP feasibility finalizer. SK-V18 R-A does not rebuild this
engine: it RELOCATES the *consumer* of `cost.chosen` — deleting
`RuntimeEmitterKind` and re-dispatching the single `render(program)` body on the
already-cost-model-derived `BackendShape`. This is defensible against the
published generator-architecture record: a code-generator generator dispatches
emission on the lowered/selected pattern, never on a source-language family tag.
The `emit_shape_source == lowered_program` firewall is the bbnf-specific binding
of that record's separation-of-stages invariant: the emitter reads its shape
from the lowered program, never from a `RuntimeTarget` field. The five-shape
canon holds; no sixth shape; CollapsedStage stays aarch64-gated diagnostic.

## Technique Grounding Table

| spec-claim or T-P1-divergence-id | primary source cited | state | bbnf-specific note |
|---|---|---|---|
| **R-A**: an un-forked generator should dispatch emission on the cost-model-DERIVED `BackendShape`, NOT on a `RuntimeEmitterKind` source-family discriminator (`D-1E-V5-02`; SPEC §6 `:1070-1098`). | Fraser, Hanson, Proebsting, "Engineering a Simple, Efficient Code-Generator Generator", ACM LOPLAS 1(3) 1992, DOI <https://doi.org/10.1145/151640.151642> (iburg: the matcher dispatches code emission on the SELECTED bottom-up rewrite pattern via dynamic-programming cost, never on a front-end tag). | **grounded** | iburg's architecture IS this claim: the tree-pattern matcher selects a rule by cost, then emits from the *selected rule's* action — the source program never re-discriminates the emitter. bbnf's `select_lowering(cost.chosen)` (`lower/mod.rs:18`) already mirrors iburg dispatch-on-selected-pattern; the `RuntimeEmitterKind` fork (`grammar_provider.rs:39-43`) is the anomalous SECOND discriminator iburg architecture forbids. DELETE is the iburg-faithful move. **LEDGER NEGATIVE-WITNESS: the relocated-seam/un-fork/`RuntimeEmitterKind` route has ZERO REDRESS prior — `rg -ic 'relocated.seam\|RuntimeEmitterKind\|un.fork' skinny/REDRESS.md` == 0 — so R-A is SK-V18-NOVEL and owes no ledger id. It is NOT a revival of REDRESS 96/97/98 `G-W3-UNION-SUBSTRATE`: that ledger family retires a retained SUBSTRATE CURSOR (a streamed/materialized SIMD structural cursor threaded through retained parsing); R-A relocates an EMITTER DISCRIMINATOR (which lowered-shape the single `render(program)` reads) — a different object entirely, with the `emit_shape_source` firewall + R16 co-gate as the binding distinction.** |
| **R-A cost-derivation**: the shape decision must be grammar-DERIVED (a cost model over rule shapes, `cost.chosen`), not a config field (SPEC `:1073-1074`). | Willsey, Nandi, Wang, Flatt, Tatlock, Panchekha, "egg: Fast and Extensible Equality Saturation", PACMPL 5 (POPL) 2021, DOI <https://doi.org/10.1145/3434304>; Tate, Stepp, Tatlock, Lerner, "Equality Saturation", POPL 2009. | **grounded; live** | LIVE: `backend_egraph::select_with_rewrite_policy` (`:40-87`) builds an `EGraph<DecisionNode>`, unions the ranked candidates into one e-class, runs a `BackoffScheduler` (`:73`) with ONE asserted rewrite `NormalizeDirectSinkCost` (instantiated `:75`, struct+impl `:191-:193`), and extracts the cheapest via `Extractor::new(&graph, &DecisionCostModel)` (`:84-87`). This is the egg pipeline the V2 cycle predicted; W7 activated it. The decision is grammar-derived per rule. |
| **`emit_shape_source == lowered_program` firewall** (`D-1E-V5-10`; SPEC `:1115-1122`, conjunct 5): the un-forked `render(program)` reads its shape ONLY from `program.policy_summary.backend_shape`, NEVER from a `RuntimeTarget`/`RuntimeProfileContract` field. | Phase/stage separation invariant of the classic compiler back-end (Aho/Lam/Sethi/Ullman, *Compilers: Principles, Techniques, and Tools*, 2e 2006, Ch. 8 — instruction selection consumes the lowered IR, not the source); reinforced by iburg's IR-tree-only matcher (LOPLAS 1992). | **grounded** | The published record forbids a back-end stage reaching back past its input IR to the source representation. bbnf's firewall is the machine-checkable bbnf encoding: grep the `render(program)` body for any read of `target.profile`/`target.emitter`/`target.output_labels`/`target.profile_contract` == 0. Without it, the relocated seam (a per-grammar branch riding a neutral data column) passes the arm-census grep — the firewall is the binding distinction between an honest un-fork and a paper-close. |
| **R16 row-collapse co-gate** (`runtime_target_rows_collapsed`; `D-1E-V5-10`; SPEC `:1132-1145`): a full-row `RuntimeTarget: PartialEq` derive is the structural co-gate that catches a fork relocated into a neutral data table. | Structural-equality / value-equivalence reasoning (no novel cite needed — `derive(PartialEq)` is std Rust); the *necessity* is grounded by the relocated-seam risk (R-A §5-risk-1). | **grounded (mechanism), partial (live)** | The +1-line `PartialEq` derive recurses into BOTH nested structs (`frontend_requirements`, `output_labels`) automatically (both already derive `PartialEq,Eq` at `grammar_provider.rs:45`/`:91`). `runtime_target_rows_collapsed` is a PLANNED gate (SPEC `:247`), NOT a live symbol — `rg runtime_target_rows_collapsed skinny/crates skinny/xtask` == 0 (1E CH1-V3-F12). The grounding defends the MECHANISM; the symbol lands at P3/G3. |
| **5-shape BackendShape canon** is exactly `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` (`D-1E-V5` verified-invariant). | iburg/BURG finite-rule-set selection (LOPLAS 1992); simdjson staged materialization (Langdale/Lemire, "Parsing Gigabytes of JSON per Second", VLDBJ 2019, arXiv:1902.08318); Mison consumer-known projection (Li et al., VLDB 2017). | **grounded** | LIVE exactly-five at `lower/mod.rs:18-24` and `passes/src/lib.rs:531-563` (candidate generation enumerates exactly the five with priority steps P1-P8). SK-V18 adds NO sixth shape (1E no-candidates axis). The finite candidate set is the iburg/BURG rule-set analogue; the materialization classes are simdjson/Mison-grounded. |
| **derive_backend_shape algorithm** (Lock 10; `D-1E-V5` L10 over-stated→re-grounded): per-rule candidate generation → e-graph saturation → cost extraction → CSP feasibility finalize. | egg (POPL 2021) + BURG dynamic-programming cost (LOPLAS 1992) + OR-Tools CP-SAT feasibility model (Google OR-Tools CP-SAT docs, <https://developers.google.com/optimization/cp/cp_example>). | **grounded; live** | LIVE pipeline: `derive_backend_shape_with_diagnostics` (`passes/src/lib.rs:401`) → `choose_backend_shape` (`:473`) → `backend_candidates` (`:508`, the five specs with `hard_pruned`/`shape_rank`/`priority_fired`) → `backend_egraph::select` (`:498`) → `decision_csp::finalize_rule` (`:500`). The three-stage published architecture (candidate gen / cost extraction / feasibility) is faithfully realized. |
| **CollapsedStage as branchless staged FSM**, aarch64-gated diagnostic — not the M5 Max close route (`D-1E-V5-04`; SPEC `:75`,`:130`). | Langdale/Lemire two-stage SIMD parser (VLDBJ 2019); 2E aarch64 grounding: NEON/AdvSIMD TBL-classify (`SRC-A64-NEON`, `SRC-LOCAL-CLASSIFY`), PMU cold-parse evidence (`SRC-A64-PMU`); x86 AVX-512 is `X86FutureBackendContrast` diagnostic-only (2E `:81`). | **partial / diagnostic** | LIVE: CollapsedStage lowerer delegates to `tape_plan::render_rule(.., Collapsed)` (`collapsed_stage.rs:16`) — no longer a marker string. It is admitted only when `target.collapsed_stage_author_declared` (`passes/src/lib.rs:658`). The staged-FSM class is grounded; the aarch64 close path is the 2E TBL-classify baseline; x86 AVX-512 FSM literature is architecture-pressure that cannot close any row. |
| **SinkOnly direct projection** (the G1 R-C target — JSON projects via `SinkOnlyExpr` AST-walk, not a fixed-literal `render()`). | Li, Katsipoulakis, Chandramouli, Goldstein, Kossmann, "Mison: A Fast JSON Parser for Data Analytics", PVLDB Vol 10(10) 2017, DOI <https://doi.org/10.14778/3115404.3115416>, <https://www.microsoft.com/en-us/research/publication/mison-fast-json-parser-data-analytics/> (consumer-known speculative projection skips general materialization). | **grounded** | SinkOnly is the only currently-substantive lowerer (`sink_only.rs:68-96`, the rich `SinkOnlyExpr` IR). Mison grounds direct consumer-known projection avoiding a general value tree — exactly SinkOnly's contract. G1's R-C makes JSON emit THROUGH this walk (`D-1E-V5` L05); G3's un-forked `SinkOnly`-shape renderer then inherits a derived body. |
| **CSP feasibility finalizer** (Lock 10): a CSP layer filters candidate shapes for hardware/parity/consumer/budget feasibility after cost extraction. | Google OR-Tools CP-SAT (variables, constraints, objective, SAT/UNSAT status), <https://developers.google.com/optimization/cp/cp_example>. | **grounded; live partial** | LIVE: `decision_csp::finalize_rule` (`passes/src/lib.rs:500`) runs after `backend_egraph::select`. The V2 cycle flagged the live CSP as tautological / grammar-named (`csp_named_grammars`); that critique survives as a SK-V18 P5/P4 hardening target (metalang leak + green-by-exclusion), NOT a shape-canon defect. The feasibility-LAYER claim is grounded; the live facts need grammar-neutralization. |

## Architectural Assertions Defended

| assertion | defence | SK-V18 adoption rule |
|---|---|---|
| **R-A — DELETE `RuntimeEmitterKind`, dispatch on the lowered `BackendShape`.** | The published code-generator-generator record (iburg, LOPLAS 1992) dispatches emission on the cost-SELECTED pattern, never on a source-family tag. bbnf already has BOTH discriminators live: the iburg-faithful `select_lowering(cost.chosen)` (`lower/mod.rs:18`) AND the anomalous `RuntimeEmitterKind{CompiledLowering,RequestFacts}` fork (`grammar_provider.rs:39-43`). The fork is the architectural redundancy; deleting it leaves the single cost-derived discriminator the literature mandates. | G3 deletes the enum + both variants (grep `RuntimeEmitterKind|CompiledLowering|RequestFacts` over `codegen/src/`+`xtask/src/` == 0); one `render(program)` serves JSON+CSS dispatched on `BackendShape`. Entry-gates G1 ∧ G2 ∧ P4 ∧ P3. **Cost (SPEC §8 `:443`): wave_owner=G3, ≤450 hand source/test/gate LOC, risk_class=HIGH (un-fork relocated-seam), ≤90 min wall / 30 min redress, documented larger cap if §5-risk-1 fires; rollback path = revert the discriminator deletion to the live `RuntimeEmitterKind` fork (the engine is untouched — G3 is a PATH change, not an engine rebuild, so rollback is byte-local); abrogate threshold = if the un-forked `render(program)` cannot reach byte-equivalent G1/G2-shipped output without a relocated seam, ABROGATE the in-place un-fork and DELETE-rebuild the renderer rather than patch a data-column fork (per [abrogate-before-patch]).** |
| **The shape decision stays grammar-DERIVED (a cost model over rule shapes), not a config field.** | The egg equality-saturation pipeline (Willsey POPL 2021) + BURG dynamic-programming cost extraction (Fraser/Hanson/Proebsting LOPLAS 1992) are LIVE in `backend_egraph` (`EGraph`/`BackoffScheduler`/`Extractor`/`DecisionCostModel`, one asserted `NormalizeDirectSinkCost` rewrite). `cost.chosen` is the extractor's verdict, not a `RuntimeTarget` strategy slot. | G3's per-`BackendShape` renderer reads its shape from `program.policy_summary.backend_shape` (`sink_only.rs:48`) — the lowered cost verdict — never from a config. |
| **`emit_shape_source == lowered_program` is the binding firewall (the un-fork's fifth conjunct).** | The classic back-end separation-of-stages invariant (Aho et al. Ch. 8; iburg IR-tree-only matcher): a back-end stage consumes its lowered IR input, never the source representation. bbnf's firewall is that invariant made grep-checkable. **Without it, conjuncts 1-4 pass under a green gate while the fork rides a neutral per-profile column** (the §5-risk-1 relocated seam). | The `render(program)` body reads NO `target.profile`/`target.emitter`/`target.output_labels`/`target.profile_contract`/`contract.emitter`. md5-distinctness is NECESSARY-NOT-SUFFICIENT; the R16 `runtime_target_rows_collapsed` full-row `PartialEq` co-gate is the only check that catches a fork relocated into data. |
| **The five-shape candidate set is closed; no sixth shape.** | iburg/BURG select over a FINITE rule set; the five materialization classes (eager/offset/event tape + sink + collapsed-stage) cover the simdjson/Mison published materialization spectrum (eager DOM ↔ lazy offset tape ↔ event stream ↔ direct sink ↔ branchless staged FSM). A sixth would need a published class outside that spectrum. | Carry V2 LAC-2D-04 verbatim. The all-five gate must see exactly `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`; a sixth is a hard REJECT. |
| **CollapsedStage stays an aarch64-gated diagnostic, not the M5 Max close route.** | 2E grounds the aarch64 baseline as NEON/AdvSIMD TBL-classify + PMU cold-parse evidence; x86 AVX-512 staged-FSM literature is `X86FutureBackendContrast` diagnostic-only (2E `:81`). The Langdale/Lemire two-stage SIMD class is grounded, but admission requires `collapsed_stage_author_declared` + a scalar oracle + strict checkasm + a same-wave consumer (Lock 16). | CollapsedStage remains in the five-shape canon; aarch64 admission needs the 2E TBL-classify-class scalar reference + checkasm parity. x86 cannot close. |

## Architectural Assertions Refuted

| refuted assertion | why it is refuted | consequence |
|---|---|---|
| **(STALE V2) The Decision Engine is a zero-rule scaffold.** | SK-V15 LANDED: `select_with_rewrite_policy` runs ONE asserted `NormalizeDirectSinkCost` rewrite (instantiated `backend_egraph.rs:75`, struct+impl `:191-:193`) through a `BackoffScheduler` (`:73`), extracts via `DecisionCostModel` (`:84-87`). The V2 zero-rule verdict (`rules: …; 0] = []`) describes only the `enable_rewrites == false` branch (`:79`); the live path enables the rewrite. | The SK-V18 cost model is NOT "to be built". It EXISTS. R-A relocates its consumer; it does not rebuild the engine. The cost-model work in SK-V18 is DERIVED-not-new. |
| **(STALE V2) Four marker-string lowerers prove nothing.** | SK-V15 W8/W9 LANDED: `collapsed_stage.rs:16` delegates to `tape_plan::render_rule(.., Collapsed)`; `lower/mod.rs:18-24` routes all five through real `ShapeLowering` impls. The `format!("rule {} -> <shape>")` markers are gone. | SK-V18 inherits real lowerers. The G1/G2 work is JSON `SinkOnlyExpr`-projectionizing + CSS lowering, not lowerer-from-scratch. |
| **md5-distinctness of generated files proves the un-fork.** | md5-distinctness is satisfiable while a per-grammar branch rides a neutral `RuntimeTarget` data column (the relocated seam). The arm-census grep is syntactically incapable of seeing it. | md5-distinct is NECESSARY-NOT-SUFFICIENT; the `emit_shape_source == lowered_program` firewall + R16 `runtime_target_rows_collapsed` co-gate are mandatory (SPEC `:1132`, §5-risk-1). |
| **A `ProjectionSpec` value field can hold the un-forked emit strategy.** | A `ProjectionSpec` selected by `target.profile` IS the relocated-seam shape unless EVERY field is lowering-derived (R-A Candidate B, SYNTHESIS `:45-48`). It re-creates the fork in data. | R-A Candidate B is ABSORBED into A's per-`BackendShape` renderers (each declares its own roster). The strategy must come from the lowered program, never a profile-selected value. |

## Open Research Questions

| UNKNOWN | verify_action |
|---|---|
| **UNKNOWN-2D-V3-01**: Does the live CSP finalizer (`decision_csp::finalize_rule`) carry grammar-named or tautological facts that the SK-V18 P4/P5 hardening must neutralize before G3 reads `cost.chosen`? | Re-grep `csp_named_grammars` + the `json_*`/`css_*` fact names at HEAD; bind any survivor to P5 (metalang purge) / P4 (green-by-exclusion). The V2 CSP-tautology critique is a SK-V18 hardening target, not a shape defect — confirm it does not corrupt the shape verdict G3 dispatches on. |
| **UNKNOWN-2D-V3-02**: Is `cost.chosen` for the CSS rules even POPULATED today, or does CSS bypass `derive_backend_shape` entirely (the const-courier `:91`)? | The R-A dispatch needs a non-const CSS `BackendShape` input (G2 must LOWER CSS first — SYNTHESIS coupling §2.1). Confirm CSS rules reach `derive_backend_shape_with_diagnostics` post-G2; if CSS never produces a `cost.chosen`, G3 inherits a const arm and the un-fork is hollow. |
| **UNKNOWN-2D-V3-03**: Will the precedence-tower (Sheets, PROVE) stress the cost model's GENERALITY — does `derive_backend_shape` assign a sensible shape (OffsetTape/EventTape) to recursive `CallRule`/`RepeatLoop` chains it has never seen? | PROVE measures whether `choose_backend_shape` over the 7-level tower selects a viable shape WITHOUT a grammar-name special-case. If the cost model can only shape JSON/CSS rule patterns, the tower exposes a cost-model overfit (a §6 finding). |
| **UNKNOWN-2D-V3-04** (now BOUND by LAC-2D-V3-04 — a fence, not an open net-win; LEDGER-FENCED against REDRESS 96/97/98): the transient-mask property of the `Collapsed` lowerer body is the binding admission gate. CollapsedStage occupies the five-shape canon as a SHAPE SLOT ONLY; the body is `diagnostic-only / author-declared` until proven. A `Collapsed` lowerer threading a per-call staged-FSM mask through retained parsing is STRUCTURALLY the streamed-cursor class REDRESS 96/97/98 retired (`G-W3-UNION-SUBSTRATE`, `skinny/REDRESS.md:2795-2944`: on the M5 Max wide-issue core the scalar `consume_structural`/delimiter path is cheaper than materializing or streaming a SIMD structural cursor through retained parsing — both faithful implementations regressed every must-improve row, retired-not-blocked at `:2928-2933`). Its promotion past `diagnostic-only` must clear that retired prior, not merely satisfy the generic transient-mask CH5 gate. | Inspect the `Collapsed` flavor output of `tape_plan::render_rule`; confirm `ParserState + CollapsedStagePlan` carries no retained per-grammar side stream (only a per-call staged FSM state — the transient-mask proof CH5 requires) AND that it carries (or can carry) the 2E TBL-classify scalar oracle + checkasm differential AND that any net-win claim clears the REDRESS 96/97/98 scalar-cheaper-than-SIMD-cursor finding on the M5 Max host. Per LAC-2D-V3-04 this is a BINDING gate: an undocumented hand-tuned `Collapsed` loop is Lock-16 INADMISSIBLE and the body must report diagnostic-only or be DELETED, leaving the slot inert. |

## LOCKS-AMENDMENTS-CANDIDATE

| candidate | target | proposed amendment | supporting evidence |
|---|---|---|---|
| **LAC-2D-V3-01** | Lock 10 / Lock 14 / R-A | "Un-forked emitter" means: the single `render(program)` body dispatches on the cost-model-DERIVED `BackendShape` (`cost.chosen`), `RuntimeEmitterKind` and both variants are DELETED, and `generator_grammar_branch_count == generator_grammar_type_count == 0`. Dispatch-on-source-family is REJECT; dispatch-on-cost-selected-shape is the iburg-faithful architecture. | iburg LOPLAS 1992 (DOI 10.1145/151640.151642); live `select_lowering(cost.chosen)` `lower/mod.rs:18` vs anomalous fork `grammar_provider.rs:39-43`; SPEC §6 `:1100-1110`; 1E `D-1E-V5-02`. **Ledger negative-witness: `rg -ic 'relocated.seam\|RuntimeEmitterKind\|un.fork' skinny/REDRESS.md` == 0 — the un-fork is SK-V18-NOVEL, owes no REDRESS id, and is NOT the REDRESS 96/97/98 substrate-cursor retirement (an emitter discriminator is a different object than a retained structural cursor). Manifest: wave_owner=G3, loc_estimate ≤450 hand LOC, risk_class=HIGH, rollback path = restore the deleted discriminator (PATH-local revert), abrogate threshold = DELETE-rebuild the renderer if a relocated data-column seam re-appears, final disposition = relocate-consumer.** |
| **LAC-2D-V3-02** | Lock 1 / Lock 5 / Lock 14 / `emit_shape_source` | Bind the relocated-seam firewall as a lock clause: the un-forked `render(program)` reads its shape ONLY from `program.policy_summary.backend_shape`, NEVER from any `RuntimeTarget`/`RuntimeProfileContract` field (`emit_shape_source == lowered_program`). md5-distinctness is NECESSARY-NOT-SUFFICIENT; the R16 `runtime_target_rows_collapsed` full-row `PartialEq` co-gate is mandatory (the arm-census grep cannot see a fork relocated into a neutral data table). | Back-end stage-separation invariant (Aho et al. Ch. 8; iburg IR-only matcher); SPEC `:1115-1145`,`:247`; 1E `D-1E-V5-10` (co-binds 1E LAC-1E-V5-02 with the published stage-separation cite this 2D pass adds). **Manifest: wave_owner=G3 (the firewall lands with the un-fork), loc_estimate = +1 `RuntimeTarget: PartialEq` derive + the grep gate (≈ +10 gate LOC), risk_class=MED (gate-only), rollback path = remove the co-gate (the firewall is additive — removal cannot break shipped output), abrogate threshold = if the full-row `PartialEq` co-gate proves unable to catch a relocated seam, escalate to a structural row-collapse assertion (`runtime_target_rows_collapsed`) rather than weaken the firewall, final disposition = live-regression-guard.** |
| **LAC-2D-V3-03** | Lock 10 / cost model (REVISE V2 LAC-2D-01/02/03) | The V2 amendments demanding "≥1 asserted rewrite + nonzero work + non-tautological CSP" are now SATISFIED at HEAD (`NormalizeDirectSinkCost` live, instantiated `backend_egraph.rs:75`, struct+impl `:191-:193`). Re-key them from "activation requirement" to "regression-guard invariant": the engine must KEEP ≥1 rewrite and a feasibility-bearing CSP across SK-V18; a regression to zero-rule re-opens the V2 scaffold finding. | Live e-graph pipeline `backend_egraph.rs:40-87` (instantiation `:75`, struct+impl `:191-:193`); egg POPL 2021; the V2 2D dossier (this file, superseded section). **Manifest: wave_owner=G3 (regression-guard rides the un-fork wave; no new authoring), loc_estimate ≈ 0 (the engine exists — this is an invariant, not a build), risk_class=LOW, rollback path = N/A (a guard cannot be rolled back — its failure means the engine regressed), abrogate threshold = a regression to a zero-rule e-graph re-opens the V2 scaffold finding and blocks the wave, final disposition = live-regression-guard.** |
| **LAC-2D-V3-04** | Lock 16 / CollapsedStage (carry V2 LAC-2D-05, re-aimed aarch64) | CollapsedStage is admitted into the five-shape canon as a SHAPE SLOT ONLY; its lowerer body is `diagnostic-only / author-declared` until BOTH (a) a transient-mask proof — the `ParserState + CollapsedStagePlan` carries no retained per-grammar side stream, only a per-call staged FSM state — AND (b) the 2E aarch64 TBL-classify-class scalar oracle + strict checkasm parity + `collapsed_stage_author_declared` + a same-wave consumer land. This is a BINDING admission gate, not an open question: an undocumented hand-tuned `Collapsed` loop is Lock-16 INADMISSIBLE. A `Collapsed` lowerer threading a per-call FSM mask through retained parsing is the streamed-cursor shape REDRESS 96/97/98 RETIRED (`G-W3-UNION-SUBSTRATE`, the M5 Max scalar-cheaper-than-SIMD-cursor finding); promotion past `diagnostic-only` must clear that retired prior, not merely the generic transient-mask gate. x86 AVX-512 staged-FSM literature is `X86FutureBackendContrast` architecture-pressure that cannot close any row. | 2E `:73` (TBL-classify grounded), `:81` (x86 diagnostic-only); live `collapsed_stage.rs:16`, `passes/src/lib.rs:658` (`collapsed_stage_author_declared` gate); REDRESS 96/97/98 `G-W3-UNION-SUBSTRATE` retired (`skinny/REDRESS.md:2795-2944`, finding `:2928-2933`); 1E `D-1E-V5-04`. **Manifest (cost SPLIT committed-vs-conditional): (a) COMMITTED inert-slot cost = ≈0 LOC, wave_owner=none (the shape slot exists in the five-shape canon today with the body reverted to `diagnostic-only`; no consumer, no build budgeted); (b) CONDITIONAL rebuild cost = ≤450 hand LOC (scalar twin + checkasm extension), wave_owner=G5/G6 GATED on a profiled CollapsedStage hot leaf + a transient-mask proof + a scalar oracle + strict checkasm + a same-wave consumer + clearing the REDRESS 96/97/98 retired prior — NOT a committed G5/G6 build. risk_class=MED-HIGH (conditional only). rollback path = revert to `diagnostic-only` (the shape slot stays, the body reverts to author-declared). abrogate threshold = if the `Collapsed` lowerer cannot prove transient-mask + scalar-oracle + clear REDRESS 96/97/98, DELETE the lowerer body and keep the slot inert rather than ship a hand-tuned loop. final disposition = diagnostic-only-pending-G5/G6-evidence.** |

### No-candidates axes scanned (explicit)

- **6th BackendShape variant**: NONE — five-shape canon holds verbatim (`lower/mod.rs:18-24`; SPEC `:75-78`); 1E no-candidates axis concurs. SK-V18 adds no shape.
- **New cost axis beyond the live `{perf_cost, capacity_cost, static_size_cost, shape_rank, freshness}`**: NONE — the live `BackendCandidate`/`DecisionCost` axes (`backend_egraph.rs:21-23`,`:242-244`) are sufficient for the five-shape selection; SK-V18 hardens facts, adds no axis.
- **New decision-engine stage**: NONE — the candidate-gen / e-graph-extract / CSP-finalize three-stage pipeline is the BURG+egg+OR-Tools published architecture; SK-V18 relocates the CONSUMER (R-A), adds no stage.

## Source Index

Primary external sources (all verified real this pass): Fraser, Hanson,
Proebsting, "Engineering a Simple, Efficient Code-Generator Generator", ACM
LOPLAS 1(3) 1992, DOI 10.1145/151640.151642 (iburg dynamic-programming
cost-driven dispatch-on-selected-pattern — the R-A grounding); Willsey, Nandi,
Wang, Flatt, Tatlock, Panchekha, "egg: Fast and Extensible Equality Saturation",
PACMPL 5 (POPL) 2021, DOI 10.1145/3434304; Tate, Stepp, Tatlock, Lerner,
"Equality Saturation", POPL 2009; Li, Katsipoulakis, Chandramouli, Goldstein,
Kossmann, "Mison: A Fast JSON Parser for Data Analytics", PVLDB Vol 10(10) 2017,
DOI 10.14778/3115404.3115416; Langdale, Lemire, "Parsing Gigabytes of JSON per
Second", The VLDB Journal 28(6) 2019 / arXiv:1902.08318; Google OR-Tools CP-SAT
documentation, <https://developers.google.com/optimization/cp/cp_example>; Aho,
Lam, Sethi, Ullman, *Compilers: Principles, Techniques, and Tools*, 2e 2006,
Ch. 8 (back-end stage-separation invariant — the `emit_shape_source` firewall
grounding). 2E re-cites: Arm NEON Intrinsics Reference (`SRC-A64-NEON`); Apple
M5 Max PMU cold-parse evidence (`SRC-A64-PMU`); x86 AVX-512 `X86FutureBackendContrast`
diagnostic-only (2E `:81`).

Primary local sources: `restart/skinny/tranches/sk-v18/SPEC.md` (§6 G3 `:1070-1188`,
firewall `:1115-1145`, telemetry `:1158-1166`); `restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md`
(R-A `:38`, couplings §2, risks §5); `restart/audit/totality/p1/1E-locks-evidence.md`
(D-1E-V5-02/04/10, LAC-1E-V5-02/04); `restart/audit/totality/p2/2E-host-arch-esoterica.md`
(`:73`,`:81`); `skinny/crates/codegen/src/lower/mod.rs:18-24`;
`skinny/crates/codegen/src/lower/collapsed_stage.rs:16`;
`skinny/crates/codegen/src/lower/sink_only.rs:48`,`:68-96`;
`skinny/crates/codegen/src/grammar_provider.rs:39-43`,`:110-111`;
`skinny/crates/codegen/src/runtime_generator.rs:16-25`,`:91`;
`skinny/crates/passes/src/lib.rs:401`,`:473`,`:508`,`:658`;
`skinny/crates/passes/src/backend_egraph.rs:40-87`,`:75` (instantiation),`:191`/`:193` (struct+impl),`:84-87`;
`skinny/crates/passes/src/decision_csp.rs`; `skinny/xtask/src/regen.rs:5-19`.

Superseded-verdict note (V2→V3): the V2 dossier (above this extension's date)
refuted the Decision Engine as a "zero-rule scaffold" and the lowerers as "marker
strings". Both refutations are STALE at HEAD — SK-V15 W7/W8/W9 landed the asserted
rewrite and real lowerers. This V3 extension supersedes those two verdicts and
re-keys the V2 amendment candidates from activation-requirements to
regression-guard invariants (LAC-2D-V3-03). The five-shape canon and the
CollapsedStage-aarch64-diagnostic verdict carry forward unchanged.
