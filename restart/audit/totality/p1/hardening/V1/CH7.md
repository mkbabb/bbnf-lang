---
agent: CH7
pass: T-P1-excavation
cycle: V1
lens: OVERFIT-PRUNE
disposition: REVISE
generated_at: 2026-05-23T00:00:00-04:00
inputs_audited:
  - restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §CH7 (lens definition; lines 62-87)
  - restart/audit/totality/p1/hardening/V1/CHALLENGE-CONTEXT.md (dispatch §0-§4)
  - restart/audit/totality/p1/1A-substrate-evidence.md (V6; 112 lines)
  - restart/audit/totality/p1/1B-codegen-evidence.md (V6; 116 lines)
  - restart/audit/totality/p1/1C-runtime-evidence.md (V6; 203 lines)
  - restart/audit/totality/p1/1D-skinny-lessons.md (V1; 161 lines)
  - restart/audit/totality/p1/1E-locks-evidence.md (SK-V14; 162 lines)
  - restart/audit/totality/p1/1F-anti-pattern.md (V6; 120 lines)
  - restart/audit/totality/p1/1F-coherence-scan.md (V6; 117 lines)
  - restart/audit/totality/p1/1F-past-corpora.md (V6; 159 lines)
  - restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md (S-P0 prune list)
  - restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-pre-restart-pattern.md (Pattern H baseline)
  - restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-lock14-scan.md (30-violation A3 census)
  - restart/locks/LOCKS.md (governance text; specifically :40-90 surrounding the alleged CH7 binding cite)
  - restart/prompts/totality/PASS-1-EXCAVATION.md §3 (CH-lens registry; lines 91-138)
---

## Lens Contract

CH7 Overfit-Prune (`restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87`)
lenses every artefact for: (a) generated-vs-hand-written discipline (no
`@generated` headers on hand-coded source); (b) Lock 14 generic-crate
compliance (no JSON/CSS/Sheets string literals, byte literals, function
names, enum variants, or match arms in nominally-generic code); (c) every
admit lands via a real parser/codegen/SIMD source change with strict-vs-strict
comparator and per-iteration equality oracle; (d) every "generated" output
passes a delete + regen ⇒ byte-equivalent round-trip; (e) no SCAFFOLD-ONLY
landing counts as an admit. CH7 REJECT triggers immediate plan revise OR
redress revert with REDRESS entry; CH7 may not be carried as "acknowledged
but not blocking".

For the T-P1 totality excavation pass, CH7 is specialised to a documentary
lens over the 8 inventories: (i) SK-V14 audit-corrected baseline propagation
(does every inventory cite the audit-zero floor + 30-violation A3 census +
67-file Pattern H without recurrence-vector silence?); (ii) no fake-pattern
recurrence in inventory text (does prose use the literal `@generated by
skinny bbnf-codegen` header, or any of the seven SK-V14 P-list pre-blocks,
as a productive admit rather than a diagnostic citation?); (iii) the 1F
coherence-scan correctly flags the LOCKS.md:46 CH7-binding leak (i.e., that
LOCKS.md has no CH7 binding clause despite the orchestrator running CH7 on
every CHALLENGE phase); (iv) 1C 67-file Pattern H baseline matches S-P0 A6
exactly.

## Findings

### §1 — Per-inventory CH7 lens audit

| disposition | target | finding | required revision |
|---|---|---|---|
| ACCEPT | `restart/audit/totality/p1/1A-substrate-evidence.md:9` `v6_fold_note` + `:33-38` SK-V14 binding rows 1A-SUB-016/017/018 | 1A propagates SK-V14 audit-corrected baseline cleanly: substrate-union finding bound by orchestrator dispatch is named (1A-SUB-016); dispatch-envelope mis-attribution is recorded (1A-SUB-017/018); both reference S-P1 CH5 V3 binding + S-P1 CH2 census. No fake-pattern recurrence (literal `@generated` header used only diagnostically inside path:line citations of `json/parser.rs:1`). No paper-close inflation: SinkOnly fact remains "admitted direct evidence". | None. |
| ACCEPT | `restart/audit/totality/p1/1B-codegen-evidence.md:7-8` `hardening_inputs_folded` + `v6_metadata_fold` | 1B explicitly folds five prior consolidated hardening cycles; binds SK-V14 8-provider census (`P1-1B-D13`) to Lock 14 recurrence vector; revises three older divergences with SK-V14 truth (CostFacts expanded, ARCH §7.4 prose retired, lower-rust W7 fail-closed). The W7 fail-closed row at `:47` is a CH7-positive impl-exceeds-spec finding rather than overfit. No fake-pattern recurrence. | None. |
| ACCEPT | `restart/audit/totality/p1/1C-runtime-evidence.md:8` `live_truth_method` + `:38` executive summary + `:42-55` Pattern H census table | 1C is the inventory that reproduces the SK-V14 S-P0 A6 Pattern H baseline exactly. The per-grammar census `bbnf 8 / bnf 7 / css_l4 7 / css_pretty 7 / csv 7 / ebnf 7 / google_sheets 10 / json 7 / math 7 = 67` at `:38` matches `sk-v14-audit-overfit-pre-restart-pattern.md:46-56` (which records `google_sheets/ = 10`). Executable cross-check confirms (§2 below). Zero generation markers correctly identified; Lock 14 leak audit table at `:118-126` quantifies cleanly. | None. |
| REVISE | `restart/audit/totality/p1/1F-coherence-scan.md:63` COH-011; `:75` (Divergences row); `1F-anti-pattern.md:75` AP-016; `1F-past-corpora.md:83` PC-017 | The three 1F inventories all report the per-grammar Pattern H census as `bbnf=8, bnf=7, css_l4=7, css_pretty=7, csv=7, ebnf=7, google_sheets=6, json=7, math=7 = 67` — but the breakdown sums to **63**, not 67, because `google_sheets` is **10** at HEAD (verified: `find crates/core/src/runtime/google_sheets -type f -name '*.rs' \| wc -l` returns 10; per S-P0 A6 baseline `sk-v14-audit-overfit-pre-restart-pattern.md:53` `google_sheets/ = 10`). The asserted total `= 67` is correct; the per-grammar `google_sheets=6` is a transcription error that arithmetically fails its own claim. 1C carries the correct `google_sheets=10` at `:53` and breakdown `8+7+7+7+7+7+10+7+7 = 67`. | Correct `google_sheets=6` → `google_sheets=10` in three 1F rows (COH-011, AP-016, PC-017); the `= 67` totals are correct and require no change. |
| REVISE | `restart/audit/totality/p1/1F-coherence-scan.md:64` COH-012; `:83` (Divergences row); `:100` (V2 Planning Metadata row); `:109` (Gaps table); `:117` (U-COH-012) | COH-012 asserts that `restart/locks/LOCKS.md:46` "declares Lock 14 + CH7 Overfit-Prune lens binding" — verified false. `LOCKS.md:46` reads in full: "The plan must reflect these sixteen architectural commitments faithfully. Any wave that violates one is a fault. Locks 1–14 are the original architectural commitments; Locks 15 and 16 land 2026-05-12 after the V9.2 lazy-tape refutation and the six-agent comparative-profile cohort (see `restart/skinny/tranches/shared/SOTA-BEAT-DESIGN.md` for the empirical synthesis)." `grep -n "CH7\|Overfit" restart/locks/LOCKS.md` returns **zero hits**. The CH7 binding does not exist anywhere in LOCKS.md — the dispatch-context-flagged "LOCKS.md:46 CH7 binding lens-registry leak" is real: LOCKS.md is silent on CH7 entirely (per 1E LAC-1E-12 at `:97,120` and 1E gap row at `:145`). 1F COH-012 correctly identifies the *direction* of the lens-registry gap (PASS-1-EXCAVATION.md §3 registers only CH1-CH6; CH7 binding required by SK-V14) but it **misattributes the supporting citation**, fabricating a "Lock 14 + CH7 Overfit-Prune lens binding" string into LOCKS.md:46 that does not exist. This is itself a CH7-relevant pattern: a coherence-row asserts a counter-surface claim from a source that does not contain it. | Replace `\`restart/locks/LOCKS.md:46\` declares "Lock 14 + CH7 Overfit-Prune lens binding"` with `\`restart/locks/LOCKS.md\` carries no CH7 binding clause (LOCKS.md is silent on CH7; the SK-V14 contract relies on PASS-0-OVERFIT-AUDIT.md:62-87 as the lens authority)`. Same correction in the four other occurrences (`:83`, `:100`, `:109`, `:117`). Add a verify_action `grep -n "CH7\|Overfit" restart/locks/LOCKS.md` returning zero hits as the supporting evidence. |
| ACCEPT | `restart/audit/totality/p1/1D-skinny-lessons.md:21-52` first_cycle_additions | 1D enumerates 32 SK-V14 binding additions including SK14-AUDIT-ZERO, SK14-AUDIT-REVERSAL-40-ROWS, the five pillar rows (W5/W6/W7/OFFSET-FLAGS-TAPE/BBNF-SIMD), the five falsified-fake-pattern rows (W8/W9/CSS-fixture/FAKE-GENERATED/COMPARATOR-MISBINDING), the LOCK14-VIOLATION-CENSUS, PATTERN-H-67-FILES, DISPATCH-ENVELOPE-MIS-ATTRIBUTION, the three sequencing constraints (R4→PRUNE-2, C1→C4, PRUNE4-NINE-SUBWAVES), and the indefatigable-close clause. All seven SK-V14 P-list pre-blocks land in the Spec-Claim ↔ Implementation Table at `:114-115` as explicit binding constraints, with REDRESS path:line and audit-overfit synthesis cites. No fake-pattern recurrence; every `@generated` mention is a diagnostic citation of the A4 NEW-1/NEW-2 recurrence vector. | None. |
| ACCEPT | `restart/audit/totality/p1/1E-locks-evidence.md:8-22` SK-V14 binding anchors + `:97-101` D-1E-12..16 + `:120-124` LAC-1E-12..16 | 1E binds all four SK-V14 audit-corrected baseline vectors as explicit divergence/LAC pairs: D-1E-12 (CH7 binding silence) + D-1E-13 (R4 round-trip discipline) + D-1E-14 (CSS L4 FactStream classification gap) + D-1E-15 (Pattern H 67 recurrence vector) + D-1E-16 (audit-overlay column binding). LAC-1E-12 explicitly cites `LOCKS.md (no CH7 mention)` as the negative evidence — this is the correct anti-fabrication phrasing. Three V4-carried hardening cycles fold cleanly. Pattern H census at `:100` carries `+3 css_pretty over V13's 64` co-derivation; the SK-V14 9-sub-wave count is named. | None. |
| ACCEPT | `restart/audit/totality/p1/1F-anti-pattern.md:35-43` first_cycle_additions + `:71-78` AP-012..019 | 1F-anti-pattern covers all SK-V14 binding anti-patterns at the live-code layer: AP-012 (8 hand-written providers — Pattern P-6), AP-013 (JSON `json_provider::normalize` fake `@generated` mirror — Pattern P-1 extension), AP-014 (4 of 7 CSS L4 generators are `CANONICAL_FIXTURE` short-circuits — Pattern P-1), AP-015 (14 of 15 `.bbnf` orphan), AP-016 (Pattern H 67), AP-017 (W8/W9 SCAFFOLD-ONLY at 3 bench files), AP-018 (`LegacyPath` rename shims), AP-019 (substrate templates enshrine hot-grammar opt-out). The literal `@generated` token appears only inside `include_str!()` diagnostic citations and SK-V14 reference notes; no productive recurrence. Subject to the AP-016 `google_sheets=6` typo correction (above). | None additional beyond the Pattern H breakdown correction shared with COH-011/PC-017. |
| ACCEPT | `restart/audit/totality/p1/1F-past-corpora.md:42-50` first_cycle_additions + `:75-83` PC-009..017 | 1F-past-corpora promotes the SK-V14 P-list (P-1..P-7) to first-class binding pre-blocks (PC-009..PC-015), plus PC-016 audit-zero baseline + PC-017 Pattern H 67. SK-V13 G1-G7 pre-blocks are explicitly overlaid by the SK-V14 falsification verdict at `:85-98`. No fake-pattern recurrence — each P-list row is a "do not re-derive" ledger entry, not an admit. Subject to the PC-017 `google_sheets=6` typo correction (above). | None additional beyond the Pattern H breakdown correction shared with COH-011/AP-016. |

### §2 — Executable verification (re-run at HEAD)

#### §2.1 Dispatch directive: cargo metadata grammar census

```
$ cd /Users/mkbabb/Programming/bbnf-lang
$ cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'
bbnf
json
css_l4
css_pretty
google_sheets
ebnf
bnf
csv
math
```

**Expected**: 9 grammars (per S-P0 A6, S-P1 CH7 V3 standalone-closed, dispatch §0). **Observed**: 9 grammars; `css_pretty` present (5th line). **PASS.** This reproduces the SK-V14 S-P1 CH7 V1/V2/V3 §2.1 grammar census exactly. The 9 grammars match the 9 runtime directories below.

#### §2.2 Dispatch directive: runtime directory census

```
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime \
    -mindepth 1 -maxdepth 1 -type d | wc -l
9
```

**Expected**: 9 (per dispatch §0). **Observed**: 9. **PASS.** The nine directories are `{bbnf, bnf, css_l4, css_pretty, csv, ebnf, google_sheets, json, math}` — matches 1C `:38` and S-P0 A6 baseline exactly.

#### §2.3 LOCKS.md CH7-binding existence audit (CH7-specific dispatch focus)

```
$ grep -n "CH7\|Overfit" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md
(exit 1; zero hits)
```

**Expected (per 1F COH-012 cite)**: a "Lock 14 + CH7 Overfit-Prune lens binding" declaration at LOCKS.md:46. **Observed**: zero CH7/Overfit hits anywhere in LOCKS.md. **FAILS the 1F citation; PASSES the underlying coherence finding (the LOCKS.md CH7 silence is real).** The 1F COH-012 row needs revision per §1 above; the lens-registry gap itself stands.

#### §2.4 Pattern H per-grammar file census (1C vs 1F triplet)

```
$ for g in bbnf bnf css_l4 css_pretty csv ebnf google_sheets json math; do
    n=$(find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime/$g \
        -type f -name '*.rs' | wc -l | tr -d ' ')
    echo "  $g = $n"
  done

  bbnf = 8
  bnf = 7
  css_l4 = 7
  css_pretty = 7
  csv = 7
  ebnf = 7
  google_sheets = 10
  json = 7
  math = 7
total = 67
```

**Expected per dispatch directive**: 1C 67-file Pattern H baseline matches S-P0 A6 exactly. **Observed**: 1C's `:38` and `:42-55` census matches HEAD byte-for-byte. 1F-coherence-scan COH-011 (`:63`), 1F-anti-pattern AP-016 (`:75`), and 1F-past-corpora PC-017 (`:83`) all report `google_sheets=6`, which mathematically yields 63, not the asserted 67. **1C PASSES; 1F triplet FAILS the per-grammar breakdown (asserted total is right; breakdown is wrong).**

#### §2.5 Fake-pattern recurrence scan across the 8 inventories

```
$ grep -c '@generated by skinny bbnf-codegen' \
    /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p1/1*.md
1A-substrate-evidence.md: 0
1B-codegen-evidence.md: 0
1C-runtime-evidence.md: 0
1D-skinny-lessons.md: 0
1E-locks-evidence.md: 0
1F-anti-pattern.md: 0
1F-coherence-scan.md: 0
1F-past-corpora.md: 0
```

**Expected**: zero productive admits of the literal fake-`@generated` header (CH7 forbids ANY admit citing fake-pattern recurrence). **Observed**: zero hits across all 8 inventories. The literal token appears only in diagnostic prose (e.g., 1F-anti-pattern `:73-74` describes the recurrence vector, but does not quote the literal). **PASS.**

#### §2.6 Audit-zero baseline propagation across inventories

```
$ grep -c "audit-corrected\|audit-overlay\|AUDIT-FALSIFIED\|audit-zero\|SK-V14 binding\|audit-overfit" \
    /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p1/1*.md
1A-substrate-evidence.md: 5
1B-codegen-evidence.md: 7
1C-runtime-evidence.md: 1
1D-skinny-lessons.md: 16
1E-locks-evidence.md: 24
1F-anti-pattern.md: 8
1F-coherence-scan.md: 23
1F-past-corpora.md: 14
```

**Expected**: every inventory cites the SK-V14 audit-corrected baseline at least once (no silent-stale carry). **Observed**: every inventory has ≥1 SK-V14-binding citation. 1C carries only one, but it is load-bearing (`:8` `live_truth_method` includes "SK-V14 S-P0 A6 Pattern H baseline (67 main + 48 skinny) reproduced exactly"). **PASS** with note that 1C's binding-citation density is lower than peers but is concentrated in the executive-summary anchor.

#### §2.7 67-file census ledger consistency (1C ↔ S-P0 A6)

```
$ grep -n "67 hand-written\|Pattern H 67\|67-file" \
    /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p1/1*.md \
    /Users/mkbabb/Programming/bbnf-lang/restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-pre-restart-pattern.md
```

**Observed**: 1C `:38` matches `pre-restart-pattern.md:153` ("67 hand-written per-grammar runtime files across 9 directories under `crates/core/src/runtime/`") byte-for-byte. 1D, 1E, 1F-anti-pattern, 1F-past-corpora all cite the 67 total; only the 1F triplet (1F-coherence-scan, 1F-anti-pattern, 1F-past-corpora) carries the broken `google_sheets=6` breakdown. **1C ↔ S-P0 A6 lock-step confirmed.**

### §3 — Cross-cutting CH7 observations

#### §3.1 LOCKS.md CH7 binding leak is real; only the 1F citation is fabricated

The dispatch context's "Special V1 attention" item flags `LOCKS.md:46 CH7 binding lens-registry leak — verify these are correctly catalogued`. CH7 verifies:

- The leak itself is real: LOCKS.md has zero CH7 references (§2.3); PASS-1-EXCAVATION.md §3 registers only CH1-CH6; the SK-V14 contract relies on `PASS-0-OVERFIT-AUDIT.md:62-87` as the de facto CH7 binding source.
- 1E correctly catalogues the leak as D-1E-12 + LAC-1E-12 with the anti-fabrication phrasing `LOCKS.md (no CH7 mention)`.
- 1F-coherence-scan COH-012 catalogues the leak *direction* correctly (PASS-1-EXCAVATION §3 omits CH7; SK-V14 contract cites CH7) but fabricates a "Lock 14 + CH7 Overfit-Prune lens binding" string into LOCKS.md:46 as the supporting counter-surface. The fabricated cite must be corrected to `LOCKS.md is silent on CH7; PASS-0-OVERFIT-AUDIT.md:62-87 is the lens authority`.

This is itself a CH7-relevant finding because it shows the coherence-scan inventory adopting a citation pattern (asserting prose into a source that does not contain it) the lens is built to prevent. T-P1 must REVISE 1F before T-P3 disposition.

#### §3.2 1C-vs-1F-triplet Pattern H breakdown asymmetry

The same Pattern H total (67) lands in five inventories (1C, 1D, 1E, 1F-anti-pattern, 1F-past-corpora) plus 1F-coherence-scan. Only 1C carries the per-grammar breakdown that arithmetically sums to 67 (`google_sheets=10`). The 1F triplet's `google_sheets=6` is a stale carry that fails its own claim. The dispatch context's "1C 67-file Pattern H baseline matches S-P0 A6 exactly" check passes for 1C; the 1F triplet must align to 1C / S-P0 A6.

This is not a CH7 REJECT (the total is correct and the leak doesn't introduce a fake admit), but it is a REVISE because the breakdown is observably wrong and any downstream consumer that audits the per-grammar count by summing the breakdown will get 63 ≠ 67.

#### §3.3 No SCAFFOLD-ONLY admits in any inventory

The CH7 lens forbids SCAFFOLD-ONLY admits (research artefacts without source wiring counted as production admits). The inventories themselves are research artefacts; none of them claim production admit status. All eight inventories explicitly route their findings through T-P3 disposition or wave consumers and carry CH4 cost metadata + verify_actions on UNKNOWNs. CH7 reads this as clean.

#### §3.4 Lock 14 generic-crate compliance in inventory text

CH7 also lenses the inventory prose itself for grammar-name leaks. Every inventory names specific grammars (`json`, `css_l4`, `bbnf`, `google_sheets`, etc.) in the course of cataloguing Lock 14 violations — this is the legitimate diagnostic use of grammar names (cataloguing the leak surface to be remediated, not perpetrating the leak). Per CH7 lens definition `:75-77`, the prohibition is "JSON/CSS/Sheets string literals, byte literals, function names, enum variants, or match arms in nominally-generic code", i.e., the live source tree — not in audit text describing the leak. Inventory prose passes.

#### §3.5 Audit-overlay column gap is correctly flagged as a wave deliverable, not an admit

1F-past-corpora PC-016 records the 0/17 + 0/17 + 0/17 + 0/24 audit-zero baseline as a binding empirical floor; 1E D-1E-16 records the 4-column schema gap (`track2_entry_point`, `comparator_plane`, `per_iter_equality`, `audit_overlay_verdict`) as a SK-V14 C-2 wave deliverable. Neither inventory claims the columns are populated; both correctly route to the gate-side discipline. CH7 reads this exactly as S-P1 CH7 V3 §2.5 does for the skinny tree: the gap is the target of R1/R2 + audit-overlay scribe, not a re-introduced overfit pattern.

## Cycle Disposition

**REVISE.** Seven of nine inventory targets ACCEPT; two require corrections:

1. **1F-coherence-scan COH-012 (and its four in-file cross-references at `:83`, `:100`, `:109`, `:117`).** Replace the fabricated `restart/locks/LOCKS.md:46 declares "Lock 14 + CH7 Overfit-Prune lens binding"` citation with `restart/locks/LOCKS.md carries no CH7 binding clause; PASS-0-OVERFIT-AUDIT.md:62-87 is the lens authority`. The underlying coherence finding (PASS-1-EXCAVATION.md §3 missing CH7 registration) is correct and stands.

2. **1F-coherence-scan COH-011 (`:63`, `:82`), 1F-anti-pattern AP-016 (`:75`, `:99`), 1F-past-corpora PC-017 (`:83`, `:120`).** Correct the per-grammar Pattern H breakdown from `google_sheets=6` to `google_sheets=10` so the breakdown `8+7+7+7+7+7+10+7+7` arithmetically sums to the asserted total `67`. The `= 67` totals are correct and match 1C `:38` + S-P0 A6 baseline `sk-v14-audit-overfit-pre-restart-pattern.md:53` exactly; only the breakdown is broken.

CH7 dispatch directives (cargo metadata = 9 grammars; runtime dirs = 9) PASS at HEAD. 1C 67-file Pattern H baseline matches S-P0 A6 byte-for-byte. No fake-pattern recurrence in any inventory text. Audit-zero baseline propagates across all 8 inventories. SK-V14 binding fold is present in every inventory front-matter or executive summary. The LOCKS.md CH7-binding leak is real and correctly catalogued by 1E LAC-1E-12 (anti-fabrication phrasing); only 1F's supporting citation needs fixing.

ACCEPT-rate (lens-overlay): **7 ACCEPT / 9 targets** = 77.8% (V1 first cycle on this lens for the totality-track T-P1 pass; below the §3Z ≥95% threshold; second-cycle V2 must redress the two REVISE items to converge).

## Bibliography

- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62-87` (CH7 lens definition)
- `restart/audit/totality/p1/hardening/V1/CHALLENGE-CONTEXT.md §0-§4` (dispatch context)
- `restart/audit/totality/p1/1A-substrate-evidence.md` (V6; 112 lines)
- `restart/audit/totality/p1/1B-codegen-evidence.md` (V6; 116 lines)
- `restart/audit/totality/p1/1C-runtime-evidence.md` (V6; 203 lines; 67-file Pattern H authoritative)
- `restart/audit/totality/p1/1D-skinny-lessons.md` (V1; 161 lines)
- `restart/audit/totality/p1/1E-locks-evidence.md` (SK-V14; 162 lines; LAC-1E-12 anti-fabrication phrasing)
- `restart/audit/totality/p1/1F-anti-pattern.md` (V6; 120 lines; google_sheets=6 typo)
- `restart/audit/totality/p1/1F-coherence-scan.md` (V6; 117 lines; google_sheets=6 typo + COH-012 fabricated cite)
- `restart/audit/totality/p1/1F-past-corpora.md` (V6; 159 lines; google_sheets=6 typo)
- `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-pre-restart-pattern.md:46-56,153` (S-P0 A6 Pattern H baseline; google_sheets=10)
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md` (S-P0 prune list; 74 findings)
- `restart/skinny/tranches/sk-v14/research/p1/hardening/V3/CH7.md` (S-P1 CH7 V3 standalone-closed)
- `restart/locks/LOCKS.md:40-90` (verified zero CH7 references; CH7-binding leak is real)
- `restart/prompts/totality/PASS-1-EXCAVATION.md:91-138` (CH1-CH6 registry; CH7 absent)

Executable verification commands (re-run at HEAD):

```
cd /Users/mkbabb/Programming/bbnf-lang
cargo metadata --format-version 1 --no-deps | jq -r '.metadata.bbnf.grammars[].ident'
find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 1 -maxdepth 1 -type d | wc -l
grep -n "CH7\|Overfit" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md
for g in bbnf bnf css_l4 css_pretty csv ebnf google_sheets json math; do n=$(find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime/$g -type f -name '*.rs' | wc -l | tr -d ' '); echo "  $g = $n"; done
grep -c '@generated by skinny bbnf-codegen' /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p1/1*.md
grep -c "audit-corrected\|audit-overlay\|AUDIT-FALSIFIED\|audit-zero\|SK-V14 binding\|audit-overfit" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p1/1*.md
```

All six verifications executed at HEAD; outputs quoted inline at §2.1-§2.7.
