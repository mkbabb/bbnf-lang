# SK-V11 S-P2 CH5 Hidden Coupling
Pass: S-P2 CHALLENGE. Cycle: V1.
Date: 2026-05-19.
Scope: hidden coupling and Lock 1/Lock 14 review of S-P2 candidates.
Output: this file.
Disposition: REVISE.
Accept rate contribution: 0.

## Findings

1. **Major - P2-D satisfies the required substrate-union conclusion, but only in the existing-offset-tape sense.** PASS-2 requires CH5 to reject second scans, retained cursors, aux density tables, and parser-owned structural projections (`restart/prompts/skinny/PASS-2-RESEARCH.md:126-131`). Lock 1 allows direct-only `SinkOnly` with no queryable document identity and says a retained structural projection is the tape, not a sidecar (`restart/locks/LOCKS.md:52`). P2-D does make the required conclusion: retained tape and retained structural projection are one substrate, Track 1 and Track 2 both use `runtime::tape::TapeBuilder`, and direct/typed planes are consumers rather than alternate retained substrates (`restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:12`, `restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:14`, `restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:18-20`). This is consistent with `skinny/RESULTS.md`, which names Track 1 as `runtime::generated_json::parse`, Track 2 as an independent parser over `runtime::tape`, and says Track 2 uses `runtime::tape::TapeBuilder` without calling Track 1 (`skinny/RESULTS.md:144-145`). The fold must preserve that reading and not use "substrate union holds" to revive the retired W3 union-substrate experiment.

2. **Critical - Retained structural-position/class-lane variants are rejected, even when phrased as tape projection.** P2-A's `live_mask_to_tape_or_sink` allows a retained class column "unless it is the tape projection itself" (`restart/skinny/tranches/sk-v11/research/p2/p2a-sota-teardown.md:136-149`), and P2-B's `BYTE_CLASS_TBL_CLASSIFIER` says it may return masks or structural positions (`restart/skinny/tranches/sk-v11/research/p2/p2b-dav1d-process.md:260`). Those wordings leave a hidden-carrier path back to W3. REDRESS 96 rejected a co-indexed event-class byte column and move-consumed structural-position vector after green correctness (`skinny/REDRESS.md:2797-2848`); REDRESS 97 rejected the allocation-free streaming cursor/class lane variant (`skinny/REDRESS.md:2852-2906`); REDRESS 98 retired the W3 union-substrate thesis because consuming the structural cursor was slower than source-byte rediscovery (`skinny/REDRESS.md:2910-2940`); REDRESS 102 firewalls W3 from behavior/source/generated-output row movement (`skinny/REDRESS.md:3042-3058`). CH5 rejects any S-P3 candidate that materializes structural positions, a retained class lane, a streaming cursor, or a parser-owned projection under these names.

3. **Major - `tape_sparse_flag_delta_lane` is acceptable only as an encoding change for existing tape-owned facts.** P2-D D5 keeps one offset tape and the public `flags_at(cursor)` contract, but proposes tape-owned deltas or a compact flag stream (`restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:34`). That can remain within Lock 1 only if it replaces the current sparse flag representation without adding fact semantics, density tables, or parser-owned projection state. REDRESS 50 rejected dense and sparse parser-time aux side tables even though retained traversal probes improved, because the governing parse plane regressed (`skinny/REDRESS.md:715-740`). P2-D also admits that lazy materialization counters are shape facts, not behavior admissions (`restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:16`), and `RESULTS.md` records zero payload bytes while preserving direct NO-GO rows (`skinny/RESULTS.md:96-142`). D5 must not be sold as direct-row closure or as a new retained projection.

4. **Major - Track 1/Track 2 independence is currently honest, but V2 must make the output-plane declaration mandatory per candidate.** `RESULTS.md` states direct rows are generated Track 1 `SinkOnly` versus independent hand Track 2 `SinkOnly` (`skinny/RESULTS.md:6`, `skinny/RESULTS.md:12`, `skinny/RESULTS.md:17`), and retained Track 2 never calls generated Track 1 (`skinny/RESULTS.md:144-145`). Typed rows are more nuanced: several `real_typed_struct` rows use Track 2 as a structural/oracle context rather than the SOTA gate (`skinny/RESULTS.md:7`, `skinny/RESULTS.md:10`, `skinny/RESULTS.md:21`). P2-F correctly requires non-JSON generated parser benchmarks to report generated Track 1, an independent Track 2 or oracle, strict output equality, no source sidecar allocation, no generic-crate grammar names, and a same-wave consumer (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:68-73`). V2 must carry that plane declaration into each S-P3 candidate packet; otherwise a shared generated parser, shared hidden sidecar, or structural oracle mislabeled as same-plane Track 2 becomes Track 1 == Track 2 dishonesty.

5. **Critical - Hidden JSON policy in generic crates remains a Lock 14 blocker for non-JSON claims.** Lock 14 forbids grammar-named modules, types, match arms, feature flags, or per-grammar behavior in generic crates (`restart/locks/LOCKS.md:78`). PASS-2 also says JSON policy never enters a generic crate (`restart/prompts/skinny/PASS-2-RESEARCH.md:232-236`). P2-F finds current codegen still calls `json_provider::ensure_runtime_profile` for generated emission (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:22`) and requires replacement with a grammar-agnostic generated-runtime template before claiming CSS/Sheets/BBNF-self generality (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:95`). Any S-P3 primitive that gets generality by hiding JSON string, number, escape, or structural policy in `bbnf-simd`, `parse-that-regex`, `codegen`, or generic runtime is rejected. The same applies to any new directive, BIR variant, or `BackendShape`: Lock 10 forbids directive-driven materialization/SIMD policy (`restart/locks/LOCKS.md:70`), and P2-D D2 is acceptable only because it preserves existing `SinkOnly`/`DirectBuild` without a directive, BIR variant, sidecar, or alternate parser (`restart/skinny/tranches/sk-v11/research/p2/p2d-substrate-tape.md:31`).

6. **Minor - Output digest/hash candidates are CH5-admissible only as gate/output consumers, not parser substrate.** P2-E correctly excludes `output_digest_hash` as parse-that or bbnf-simd parser vocabulary (`restart/skinny/tranches/sk-v11/research/p2/p2e-parse-that-gaps.md:27`, `restart/skinny/tranches/sk-v11/research/p2/p2e-parse-that-gaps.md:111-115`), and P2-F frames C8 as a benchmark/oracle sink rather than parser semantics (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:65`). This guard is necessary because digest acceleration can otherwise become a benchmark-private side substrate. Any digest/hash packet must stay outside generic parser semantics and prove same-output equivalence on the declared output plane.

## Required folds

1. Narrow P2-A `live_mask_to_tape_or_sink`, P2-B `BYTE_CLASS_TBL_CLASSIFIER`, P2-B `CONTAINER_DISPATCH_CLASSIFIER`, and P2-F C1/C7/C9 language to "transient masks or first offsets consumed in the same loop by the existing `TapeBuilder`, generated `SinkOnly`, or generated typed consumer." Delete any retained class-column, structural-position vector, streaming cursor, or parser-owned projection interpretation.

2. State in V2 that P2-D's "substrate union holds" means the existing offset tape plus direct/typed consumer union holds. It does not reopen W3, `UnionTape`, class-lane-only repair, structural cursor, or parse-only structural-scan admission.

3. Constrain D5 to a pure internal encoding replacement for the existing tape sparse flags. It must preserve `flags_at(cursor)`, add no new fact semantics, add no aux density table, and carry a same-wave retained or typed consumer before being used as row-moving evidence.

4. Add a per-candidate output-plane declaration for S-P3 packets: generated Track 1 path, independent Track 2 or oracle path, same-output proof, and explicit statement that Track 2 does not call Track 1 or read a hidden shared sidecar.

5. Add a Lock 14 policy-boundary column to the V2 candidate matrix. For every primitive, name which facts are grammar metadata, which code is generated per grammar, and which generic crate API remains grammar-neutral. Reject any route that requires a new directive, BIR variant, `BackendShape`, JSON provider policy in generic codegen, or JSON-specific parse-that/bbnf-simd semantics.

## Accepted facts

None for S-P3 until the required folds are made.
