# α-E — SK-V18 Candidate Shortlist (the GENERALIZATION cycle) — V5

**Pass:** PASS-ALPHA (bracket SK-V17→SK-V18), **cycle V5**, agent α-E.
**Entry HEAD:** `318d9c046` (V4 cohort + V4 CHALLENGE committed). SK-V17 closed `f6a38445b`; V3 audit `7dbe44c22`.
**Seed:** `restart/audit/skinny-impl-overfit/V3/CONSOLIDATED-AUDIT.md` + `AGENT-{1..6}` + `restart/prompts/SK-V18-GENERALIZATION-HANDOFF.md §3`.
**Subject:** SK-V18 is the **inflection backtrack** — ONE grammar-driven generator emitting JSON + CSS + Sheets from `.bbnf`, over the unified tape/`ValueRef` substrate, shared value-API trait, PROVEN on a 3rd grammar (Sheets), **preserving the >SOTA**. NOT a new-feature cycle. **PRUNE-before-GENERALIZE** (standing order, V3 §backlog).

This artefact is the candidate shortlist α-E owns per PASS-ALPHA §2. It is *not* the wave plan (§4.4, authored downstream by S-P3). It shortlists candidate interventions, each carrying: owner path(s) · scalar-ref status · checkasm status · same-wave consumer · falsifiability gate (named corpus rows + Mbps thresholds + the three SK-V18 generalization gates) · LOC budget · risk · pre-blocks.

## FOLD LEDGER (every CHALLENGE REVISE on α-E, resolved in place — orphan-free)

### V4→V5 FOLD (the V4 CHALLENGE pass `research/alpha-hardening/V4/` — CH1..CH7)

The V4 CHALLENGE pass dispositioned α-E with **TWO αE-specific REVISEs** (CH1 §αE + CH3, the *same* orphan-fold defect; CH2 §8.1, a projection-tuple sharpening that touches αE) and **ACCEPT on every other lens** — CH4 §αE (`CH4.md:377-395` "no orphan V3 CH4 REVISE; the V4 αE fold ledger … TALLY accept=6 revise=0"); CH5 §αE (`CH5.md:220-229` "αE tally: ACCEPT ×4, REVISE ×0 — both open V3 REVISEs concretely folded"); CH6 #8 (`CH6.md:78,266-300` "alphaE candidate shortlist gates + sequencing — ACCEPT" on all four lens-rounds); CH7 §αE (carried). The two αE REVISEs are folded below as **F15** (the x86-scope orphan) + **F16** (the projection-tuple sharpening); **neither adds or removes a candidate** — F15 widens P1's deletion scope + close gate crate-wide, F16 widens one structural machine-check's projected column set. The shortlist remains additive-by-deletion, exactly 5 (A, B1–B4), no re-opened REDRESS pre-block.

| # | source | disposition | fold |
|---|---|---|---|
| **F15** | CH1 §αE (`V4/CH1.md` §αE + Cross-artefact note); CH3 (`V4/CH3.md:316-345`, TALLY 5A/2R); seeded by CH5 V3 §C.5 (`V3/CH5.md:112-157`, twin F.7) | **REVISE αE P1 row + P1 exit gate (the sole CH1 V4 REVISE; one of two CH3 V4 REVISEs)** — the V4 FOLD-1 (the **SECOND x86 surface**) landed in αC/SYNTHESIS/HANDOFF but was **NOT propagated into αE** | The V1–V4 αE P1 row scoped x86 deletion to **`bbnf-simd/src/x86_64/` only** (24 files, 847 LOC). CH5's V3 sweep found — and CH1/CH3 V4 **re-verified LIVE at HEAD `318d9c046`** (reproduced this pass) — a **SECOND x86 surface the whole cohort omitted**: (a) `skinny/crates/bbnf-simd/ext/x86/` = **4 files / 3554 LOC** vendored x86 ASM (`bbnf.asm`, `x86util.asm`, `x86inc.asm`, `LICENSE-VENDOR`); (b) `skinny/crates/bbnf-simd/build.rs` = **102 LOC** nasm-rs assembler driver; (c) `skinny/crates/bbnf-simd/Cargo.toml:19` `nasm-rs = "0.3"` build-dep; (d) `skinny/crates/bbnf-simd/src/lib.rs:247` contract reference (`"Contract documented in ext/x86/bbnf.asm"`). **The defect:** αE's P1 **exit gate** at `:93` (`grep -rE '_mm…|x86_64|avx|gfni|sve' bbnf-simd/src → 0` + `find …/src/x86_64 → 0`) is **`src/`-scoped** — it reads **0 (GREEN) while ~3656 LOC of x86 ASM survive** under `ext/x86/` + `build.rs`. This is a **P4-class FALSE-GREEN** (worse than the un-satisfiable count it is the structural twin of, per CH1: a count error red-on-clean is conservative; a `src/`-scoped P1 gate is **green-on-dirty** — it would certify P1 closed while the x86 tree lives, falsifying the R10 "x86 gone" binding pin + the αC §2.6 x86/AVX/SVE PERMANENT pre-block). **The fold (mechanical, already authored in αC:135-186 / SYNTHESIS:246, propagate verbatim):** (i) the **P1 action/owner-path row** names the crate-wide x86 surface — `src/x86_64/` (847 LOC) **+ `ext/x86/` (3554 LOC) + `build.rs` (102 LOC nasm driver) + `Cargo.toml:19` `nasm-rs` dep + `lib.rs:247` contract ref + the `lock14_baseline.rs` `"diagnostic-x86"` entry**; (ii) the **P1 exit gate** moves from `src/`-scoped to **crate-wide**: `grep -riE 'avx|gfni|sve|x86|nasm' skinny/crates/bbnf-simd/` → **only aarch64-neutral comments** (the live crate-wide grep = 686 lines today, must collapse to the residual neutral-comment floor) AND `find skinny/crates/bbnf-simd/ext/x86 -type f` → **0** AND `find …/src/x86_64 -type f` → **0** AND no `nasm-rs` in `Cargo.toml`; (iii) the **P1 LOC Δ** is corrected from **−847** to **≈ −4500** (847 `.rs`+`.asm` `src/` + 3554 `ext/x86/` ASM + 102 `build.rs` + the nasm dep line); the SUMMARY-table net re-rolls accordingly. **Why REVISE not REJECT (held from CH5 C.5):** `build.rs:38-40` early-returns on non-`x86_64`, and `ext/x86/` is referenced by **no aarch64 admission path** (the `lib.rs:247` ref is a doc-comment in a contract block, not a call site) — so the surface is **DORMANT**, not load-bearing; deleting it is pure prune, zero aarch64 risk, and brings the cohort to full x86-scope consistency. The fix touches **no other disposition** (sequencing, candidate count, >SOTA all unchanged). |
| **F16** | CH2 §8.1 (`V4/CH2.md:294-447`, the sole CH2 V4 REVISE; TALLY 29A/1R) | **REVISE the relocated-seam structural machine-check projection** (touches αE:196 / :145 / :226 — the B2/B4 distinct-grammar gates + cross-cutting note 4) | The F13 (V3→V4) fold correctly moved the relocated-overfit-seam defense FROM the arm-census regex (syntactically incapable on a neutral-identifier data-table) TO the **P3 collapse structural row-count check** — picking the right MECHANISM. CH2 §8.1 **empirically refuted the PROJECTION** (disk-reproduced this pass at HEAD `318d9c046`, `skinny/xtask/src/regen_css.rs`): the prescribed projection `sort -u over (source_roots, entry_rule)` = **1** (GREEN) — but the SAME 7 css_l4 `RuntimeTarget` rows carry **7 DISTINCT** values in `fact_schema` (`"css-l4-at-rules-media-facts-v1"` … `"css-l4-visual-function-facts-v1"`), and likewise in `row_id` / `output_plane` / `output_dir`. The `RuntimeTarget` struct (`regen.rs:6`, ~12 fields) is projected onto **exactly the 2 columns that are INVARIANT across all 7 rows**, discarding the 5 where the per-profile divergence demonstrably lives. **Why this is a generality hole, not cosmetics:** a relocated per-grammar branch does NOT need to vary `(source_roots, entry_rule)` — it can ride `fact_schema` / `output_plane` / `emitter` (all per-row). An un-forked emitter that internally dispatches on `target.fact_schema` to select a different generated body per CSS profile IS the relocated seam, and it **sails through `runtime_target_rows_collapsed == true`** because those 7 distinct values are invisible to a `(source_roots, entry_rule)` `sort -u`. The contract's "N **collapsed-identical** rows" framing is the giveaway — the live rows are NOT collapsed-identical; they differ in 5 columns; the check reports collapse=1 precisely BECAUSE it discards the columns the divergence is in. This is the SAME necessary-not-sufficient lineage (V1 md5 → V2 grep-alphabet → V3 grep-cannot-fire-on-data-table → V4 row-count-projects-too-narrow) carried one level deeper. **The fold (projection-tuple correction, NOT architectural — the P3 collapse is still the right mechanism):** every relocated-seam structural-check restatement at **αE:196 (B4 distinct-grammar gate), :145 (B2 distinct-grammar gate), :226 (cross-cutting note 4)** is corrected to project onto the **per-grammar config tuple modulo the generated-artefact path columns**: *"all `RuntimeTarget` rows sharing one `grammar_name` MUST be byte-identical in EVERY field except the generated-artefact path columns (`output_dir`, `expected_files`)."* The projection is stated **BY EXCLUSION** so the operative list cannot drift to a strict subset of its own prose (CH2 V5 §8.1): enumerate-by-exclusion over the live `regen.rs:6` 12-field struct, the operative non-path set is `profile` / `source_inputs` / `metadata_inputs` / `emitter` / `entry_rule` / `source_roots` / `check_command` / `frontend_requirements` / `output_labels` — the prior explicit enumeration `fact_schema`/`row_id`/`output_plane` was a STRICT SUBSET that **OMITTED `profile`** (the 7-distinct per-profile discriminator that differentiates the 7 css_l4_* configs) **plus `source_inputs`/`metadata_inputs`** (`fact_schema`/`row_id`/`output_plane` are per-profile content the `profile` discriminator selects, not struct fields). Machine-check: a tiny xtask assertion (or `awk`/`jq`) over the rows for each `grammar_name`, asserting `count(distinct config-tuple-minus-(output_dir,expected_files)) == 1`. **P3 (collapse the 7 css_l4_* replicas) must PRESERVE profile-distinctness** — `profile` is the per-config discriminator, so the collapse erases it ONLY where the profiles are genuinely one grammar; where they are distinct grammars they differentiate by distinct `.bbnf` roots, never by erasing `profile`. **Under live HEAD this FAILS today** (7 distinct `fact_schema`) — which is correct: the gate must be **RED pre-P3** and only go GREEN after the 7 profiles genuinely collapse to one CSS config. The "collapsed-identical" framing is corrected to: *"a data-table whose rows for one `grammar_name` are NOT identical modulo `output_dir` IS the relocated seam — the per-grammar-config-tuple collapse catches it; a `(source_roots, entry_rule)`-only projection does NOT, because the divergence rides `fact_schema`/`output_plane`/`emitter`."* The arm census + type census (self-disclosing tokens) are retained as NECESSARY-NOT-SUFFICIENT; the §0.4 prose obligation ("every residual routing entry names the `.bbnf` rule it derives from", reviewed at admission) remains the human backstop. **REVISE not REJECT:** the Lock-14 spine is correct, the P3 collapse mechanism is the right defense, and the threat IS policeable by it — only the projected column set was too narrow, repeated across the F13 sites, now widened to the columns a relocated branch can actually ride. |

**V4 ACCEPTs preserved (not re-opened):** the falsifiability triple (CH1 §αE "every OTHER falsifiability gate is measurable against its actual close condition"; CH5 §αE; CH7 §αE); A/B1/B2/B3/B4 architecture + sequencing + no-second-substrate pre-blocks (CH3 "no candidate re-opens any of the six pre-block families architecturally, candidate count unchanged at 5, REDRESS-W2-1 correctly the G3 SUBJECT not a re-open"; CH5 §αE; CH6 #8); the honest-finding anti-paper-close escape, gated (CH6 §10 ACCEPT — "the V4 folds did not touch this gate"); the Sheets-source-named + Pratt-shape litmus (CH6 #11 ACCEPT); the corrected checkasm 12+2 count (CH4/CH5/CH7 confirm α-E correct in four places); the F1–F14 prior folds. The two V4 αE REVISEs (CH1 §αE / CH3 = F15; CH2 §8.1 = F16) are folded above to zero-orphan.

### V3→V4 FOLD (the V3 CHALLENGE pass `research/alpha-hardening/V3/` — CH1..CH7)

The V3 CHALLENGE pass dispositioned **α-E ACCEPT on every lens** — CH1 (`CH1.md:181-211` "Every gate is measurable; every figure verifies. ACCEPT"); CH2 §5 (`CH2.md:182-229` ACCEPT ×6/REVISE ×0); CH3 (`CH3.md:53-56` 5-candidate additive-by-deletion, clean); CH4 (`CH4.md:65-66` "The V3 αE cost ground truth is accurate in full. No stale count, no wrong owner-path, no un-satisfiable gate remains"); CH5 §E (`CH5.md:169-191,284-286` ACCEPT on B3/B1/B2/B4 + sequencing + same-wave); CH6 #7 (`CH6.md:98,246` ACCEPT); CH7 §5 (`CH7.md:187-203,313` ACCEPT — "αE is the artefact that does the checkasm count CORRECTLY in four places"). The V3 cohort closed at **CH1 6A/1R, CH2 29A/1R, CH6 12A/0R, CH7 7A/1R** — the two open V3 REVISEs are folded below; **neither is a defect IN α-E** (one is a cross-artefact verification-surface over-claim α-E:185 shares; the other is an αD-only stale count α-E already carries correctly).

| # | source | disposition | fold |
|---|---|---|---|
| F13 | CH2 §8.1 (`V3/CH2.md:306-377`) | REVISE (the **single open V3 cross-artefact REVISE**; touches α-E:185 among five sites) | the **xtask-root arm-census grep's stated REACH is scoped honestly**. The F10/V3 fold widened the neutrality grep to `skinny/xtask/src` and α-E:185 claimed this means "a `RuntimeTarget` strategy table in DATA cannot carry the relocated branch." CH2 §8.1 **empirically refuted** this (disk-reproduced this pass): the live `RuntimeTarget` table (`xtask/src/regen_css.rs:33-` `TARGETS`) carries per-profile config as **neutral-identifier DATA** — `grammar_name: "css_l4"`, `profile: "css_l4_at_rules_and_media"`, `emitter: RuntimeEmitterKind::RequestFacts`, per-row `fact_schema`/`row_id`/`output_plane` strings — with **zero** `match grammar { Json => … }` arm syntax; the prescribed arm-census regex returns **NO MATCH** over `xtask/src` (verified: `rg -nE 'Json\s*=>\|CssL4\s*=>\|(GoogleSheets\|Sheets)\w*\s*=>\|Bbnf\w*\s*=>' skinny/xtask/src` → 0). A *neutral-identifier* data-table is by definition one that does NOT use the `Json =>` token — so the regex is **syntactically incapable** of catching the threat it was claimed to cover. **The fold (verification-surface attribution, NOT architectural):** (a) the xtask-rooted arm-census grep is **retained** for its real value — it catches a metadata branch that *self-discloses a grammar token* (`Json =>` etc.) anywhere in either root, plus the codegen `match grammar` arm + the re-emitted grammar-named type; (b) every "a relocated branch into a neutral-identifier data-table cannot escape / is caught" reach-claim at α-E:185 (and the B1/B2/B4 gate restatements + cross-cutting note 4) is **corrected to the accurate scope** — the grep catches a *self-disclosing* token, NOT a neutral-identifier strategy table; (c) the **relocated-overfit-seam threat is machine-checked structurally by the P3 collapse close-gate**, NOT the regex: the xtask `RuntimeTarget` table must carry exactly **one row per distinct `(source_roots, entry_rule)` pair** — verifiable by `sort -u` over `(source_roots, entry_rule)` (disk-verified this pass: all 7 css_l4 rows share `source_roots: CSS_L4_ROOTS` + `entry_rule: "stylesheet"` ⇒ collapse to 1), plus the §0.4 prose obligation "every residual CSS routing entry names the `.bbnf` rule it derives from," reviewed at admission. Per CH2 §8.1: REVISE not REJECT — the Lock-14 spine is correct, the threat IS policed (by P3 collapse), the grep IS worth running; only one over-stated machine-check reach claim, repeated across the gate restatements, is corrected to match what the regex can actually do. **[V5 NOTE per F16: the V3-prescribed P3-collapse projection `(source_roots, entry_rule)` is ITSELF too narrow — the 7 css_l4 rows SHARE `(source_roots, entry_rule)` but carry 7 DISTINCT `fact_schema`/`output_plane`/`row_id`, where a relocated branch rides. F16 widens the collapse to the FULL per-grammar config-tuple modulo `output_dir`/`expected_files`. The MECHANISM (P3 structural collapse) is unchanged; only the PROJECTED COLUMN SET is widened.]** |
| F14 | CH1 §αD / CH7 §4 (`V3/CH1.md:129-180,279`; `V3/CH7.md:311,166-171,328-338`) | REVISE on **αD only** (NOT α-E) — carried here as a no-op confirmation | the αD `:85` "18 differential harnesses" / "`tests/checkasm_*.rs (18)`" is stale (disk = 14 = 12 single-kernel + 2 aggregate). **α-E is the count-correct reference** — it already carries the disk-true **12 single-kernel + 2** in four places (`alphaE:79` candidate-A checkasm-status; `:179` B4 checkasm-status; `:213` cross-cutting-3; the F4 fold-ledger line). CH7 §5 explicitly names α-E "the dispositive evidence the αD `18` is a defect" (`CH7.md:313`). **No change to α-E** — this REVISE is filed against αD's V4 row; α-E requires no fold. Recorded here only to certify zero orphan in the α-E artefact and that α-E does NOT inherit the stale count. |

**V3 ACCEPTs preserved (not re-opened):** the falsifiability triple (CH1 §αE; CH2 §5.1; CH7 §5); A/B1/B2/B3/B4 architecture + sequencing + no-second-substrate pre-blocks (CH3 5-candidate clean; CH5 §E; CH6 #7); the honest-finding anti-paper-close escape, now itself gated (CH6 §9 ACCEPT after V3, `:309-323`); the F1–F12 V1/V2 folds (CH2/CH4/CH5/CH7 confirm substantively landed + ground-truth-correct); the Sheets-source-named + Pratt-shape litmus (CH2 §5.5; CH3 §; CH6 §10); the corrected checkasm 12+2 count (CH1/CH4/CH7 confirm α-E correct in four places); the canonical four-grammar Lock-14 alphabet (CH2 §6.4 verified against `LOCKS.md` item 14). CH1/CH4/CH6/CH7 were 100% ACCEPT on the α-E cluster (no α-E REVISE); CH2's sole REVISE (§8.1/F13) is a verification-surface attribution shared across five artefacts, not an α-E candidate defect.

### V2→V3 FOLD (the V2 CHALLENGE pass `research/alpha-hardening/V2/`)

The V2 CHALLENGE pass dispositioned α-E **ACCEPT on CH1/CH2(6)/CH3/CH4/CH6/CH7**, with **ONE REVISE (CH5 E.1, the sole V2 α-E REVISE)** plus **two NON-blocking owner-path/LOC fold-ins (CH4 §1, §5)** and **one cross-artefact verification-surface REVISE (CH2 §8.1)** that also touches the α-E neutrality grep. Each is folded below with a `[FOLD CHn-V2]` tag at the exact gate/line it touches. The V2 cohort was already convergence-ready (CH1/CH3/CH6/CH7 = 100% ACCEPT; CH2 = 24A/1R cross-artefact; CH4 = ACCEPT with 2 non-blocking riders; CH5 = 22A/1R); these folds discharge the residual REVISEs to zero-orphan.

| # | source | disposition | fold |
|---|---|---|---|
| F9 | CH5 E.1 (`V2/CH5.md:159-187`) | REVISE B3 (sole V2 α-E REVISE) | the **SHARED-TRAIT grep** (B3 gate #2) is now **test-excluded + canonical-trait-named**, mirroring the F6 phantom-grep exclusion exactly: `grep -rn 'impl .* SharedValueTrait .* for' --include='*.rs' crates/runtime/src/grammars \| grep -v 'tests\.rs\|#\[cfg(test)\]'` → BOTH families present in NON-test code. Closes the V2-fresh false-green seam (a `#[cfg(test)] impl SharedValueTrait for CssTestNode` would otherwise satisfy the un-excluded recipe). The SYNTHESIS:394 close gate (`shared_value_trait_instantiations` = "≥2 real production instantiations, test-only does NOT count") was already safe; this aligns the α-E research recipe to it. |
| F10 | CH2 §8.1 (`V2/CH2.md:307-352`) | REVISE (cross-artefact, touches α-E neutrality grep) | the **neutrality grep alphabet** in every B-candidate gate is corrected to the **canonical four-grammar Lock-14 alphabet** (`LOCKS.md:349`): `Json\s*=>\|CssL4\s*=>\|(GoogleSheets\|Sheets)\w*\s*=>\|Bbnf\w*\s*=>` — `GoogleSheets` UN-abbreviated (the `Sheets\w*` start-anchor would MISS a `GoogleSheets =>` arm), `Bbnf` included for SK-V19 forward-safety. AND the **scan-root set** is widened to include the xtask workspace-metadata surface (`skinny/xtask/src/`, the `RuntimeTarget`/`source_roots`/`entry_rule` tables) so a per-grammar strategy table in DATA cannot carry a relocated *self-disclosing-token* `match grammar` branch the codegen-scoped grep misses. **[V4 NOTE per F13: the widened root catches a self-disclosing token, NOT a neutral-identifier data-table — that case is the P3 collapse structural gate's job, see F13.]** AND the canonical grammar-named-*type* census (`rg 'JsonParser\|CssL4Parser\|GoogleSheetsParser'`) is added as the second surface (the `SheetsEventGrammar`/`JsonEventGrammar` witnesses are exactly grammar-named types a generated emitter could re-emit). |
| F11 | CH4 §1 (`V2/CH4.md:83-85`) | NON-blocking fold-in | the P1 LOC label is corrected from **−742** to **−847** (742 `.rs` + 105 `.asm`), since F8 deletes the `.asm` (`byte_class_from_eq_set_64.asm`) too. Net-LOC conclusion unchanged (it was a floor — "if anything understated"). |
| F12 | CH4 §5 (`V2/CH4.md:215-224`) | NON-blocking fold-in | the G6 registration/relabel/retire owner path is corrected from `bbnf-simd/src/aarch64/dispatch.rs:67-85` to **`bbnf-simd/src/dispatch.rs`** (disk-verified: `find … -name dispatch.rs` = `src/dispatch.rs` only; the `aarch64/dispatch.rs` directory prefix does not exist). The line numbers (NEON :68-73, scalar :80-85) and the kernel-BODY paths (`aarch64/bitmap_prefix_xor_64.rs:2`, etc.) are correct; only the dispatch directory prefix is fixed. SYNTHESIS:174 already cites it correctly. |

**V2 ACCEPTs preserved (not re-opened):** the falsifiability triple (CH7 §5, CH2 §5.1); A/B1/B2/B4 architecture + sequencing + no-second-substrate pre-blocks (CH3 100%, CH5 E.2, CH6 §7); the honest-finding anti-paper-close escape (CH6 §9); the F6/F7 B3 folds (CH5 confirms substantively landed); the Sheets-source-named + Pratt-shape litmus (CH2 §5.5, CH6, CH7). CH1/CH3/CH6/CH7 were 100% ACCEPT on the full α-E cluster.

### V1→V2 FOLD LEDGER (every V1 CHALLENGE REVISE on α-E, resolved in place — orphan-free)

The V1 CHALLENGE pass (`research/alpha-hardening/V1/`) dispositioned α-E **ACCEPT on the triple + A + B1 + B2 + B3-structure**, with **REVISE on B2 (one gate), B3 (two gates), B4 (four items)** and one census alignment carried from CH7. Each is folded below with a `[FOLD CHn]` tag at the exact gate/line it touches:

| # | source | disposition | fold |
|---|---|---|---|
| F1 | CH1 (`CH1.md:102-126,193-197`) | REVISE B2 gate#1 + B4 gate#4 | CSS PRESERVED->SOTA pinned to the **N=200 `css_canon_bench` close-ledger Mbps medians** (bootstrap 2473.1 / animate 2937.9 / tailwind 2773.4 / material 2618.5 Track1; lightningcss 1119.1 / 1247.7 / 828.5 / 1312.0 — `skv17-W5-close-ledger.md:99-102`), −3% against THOSE per-row Mbps, NOT the "1.9–3.3×" range and NOT the live N=80 reproduction. |
| F2 | CH2 §5.5 item 1 (`CH2.md:251-260`) | REVISE B4 | Sheets source named as `grammar/google-sheets/google-sheets.bbnf` (verified the only real Sheets `.bbnf`; NOT in the skinny tree); PROVE owner-set gains "wire a skinny Sheets grammar root + xtask `RuntimeTarget`"; acknowledges the grammar's **Pratt operator-precedence + error literals + cell references** — shapes NO JSON/CSS rule exercises (this makes the litmus non-hollow by construction). |
| F3 | CH2 §5.5 item 2 (`CH2.md:262-270`) | REVISE B4 | md5-distinct is **necessary-not-sufficient**; B4 gate gains a companion grammar-neutral generator-body grep (`rg 'Json\s*=>\|CssL4\s*=>\|Sheets\w*\s*=>' skinny/crates/codegen/src` → **0**), the canonical Lock-14 command (`LOCKS.md:349` "ZERO `match grammar` arms in generic crates"). |
| F4 | CH4 §5 (`CH4.md:204-218,232-236`) | REVISE B4 | checkasm inventory corrected from "18" to the **disk-verified 12 single-kernel differentials + 2 harness/aggregate files** (`checkasm_common.rs`, `checkasm_parity.rs`) — `ls checkasm_*.rs`=14; a gate asserting "18 present" would be un-satisfiable on a clean tree (the exact P4-class false gate this cycle fixes). |
| F5 | CH4 §5 (`CH4.md:173-182,232-236`) | REVISE B4 | G6 NEON-body LOC **bounded by a committed body-count ceiling** — PMULL `bitmap_prefix_xor_64` FIRST (V3 §6); every other kernel **retired/relabelled UNLESS a same-wave hot-path consumer exists**; the "+150 per body" is no longer an unstated multiplicand. |
| F6 | CH5 E.1 item 1 (`CH5.md:240-258`) | REVISE B3 | phantom-generic gate **excludes `#[cfg(test)]`/test-file matches** (else the standing test-only `JsonEventGrammar` line false-greens it); **DELETE is the DEFAULT** branch (`abrogate-before-patch`); INSTANTIATE is the **burden-of-proof** branch because `CssEventGrammar` does NOT exist at HEAD (only `Json`+`Sheets` witnesses, verified) — authoring it is a new grammar-named coupling surface, un-budgeted in "±0 LOC". |
| F7 | CH5 E.1 item 2 (`CH5.md:260-272`) | REVISE B3 | shared-trait gate gains a **preserve-rich-ast structural condition**: a both-impl grep is necessary-not-sufficient; JSON's `get(key)` + typed `Kind` accessors + visitor must remain reachable THROUGH the shared trait (not bypassing it) — guards the LCD-flattening false-green. |
| F8 | CH7 §2 (`CH7.md:66-73,262`) | REVISE αA (carried) | x86 census aligned to **24 files (23 `.rs` + 1 `.asm` `byte_class_from_eq_set_64.asm`)**, 742 LOC, 14 `unimplemented!`; P1 deletes the `.asm` too. |

**V1 ACCEPTs preserved (not re-opened):** the falsifiability triple (CH2 §5.1, CH7 §5); A/B1/B2 architecture (CH2/CH3/CH4/CH5/CH6/CH7 all ACCEPT); the PRUNE-before-GENERALIZE sequencing + P4-before-B1 entry-gate (CH2 §5.6, CH4 §6.2, CH5 E.2, CH6 §7); B4 G6 acceleration-wiring-at-admission same-wave-consumer discipline (CH5 E.3, CH6 §11); no-second-substrate pre-blocks on every candidate (CH3, CH5 E.2). CH3 was **100% ACCEPT** on α-E (zero regression re-open).

---

## 0. Method + the SK-V18-specific falsifiability triple

PASS-ALPHA §4.4 demands a falsifiability gate per candidate. For a *generalization* cycle (not a speed cycle), the gate is a **triple** — every GENERALIZE candidate must satisfy all three, or it is REJECTED to REDRESS:

1. **PRESERVED->SOTA** — the grammar-DERIVED parser must reproduce the SK-V17 cold real-corpus headline (JSON `>` sonic-rs strict; CSS `>` lightningcss, *honestly framed* per H1). Threshold: **no regression beyond −3% on the named per-row Mbps vs the SK-V17 hand-written baseline.** A grammar-derived parser that loses the speed is not done (HANDOFF §0). **[FOLD F1: the CSS baseline is the N=200 `css_canon_bench` close-ledger per-row Mbps, NOT a ratio range, NOT N=80.]**
2. **GRAMMAR-DERIVATION-PROOF** — the emitted `generated.rs` must be a *projection* of the `.bbnf`, not a const-`&str` courier. Falsifier: the new CHALLENGE `verbatim-blob` lens (a `@generated` file that is a verbatim `&str` in codegen = hand-written, REJECT). Operational test: **mutate the `.bbnf` → the regenerated `generated.rs` changes correspondingly** (a const courier cannot pass this).
3. **DISTINCT-GRAMMAR-OUTPUT** — N grammars ⇒ N **non-identical** `generated.rs` (diff-census). Falsifier: today the 7 CSS replicas are byte-identical (`diff` = 0 at `f6a38445b`; `css_l4_at_rules_and_media/generated.rs` ≡ `css_l4_visual_functions/generated.rs` IDENTICAL). After SK-V18, JSON ≠ CSS ≠ Sheets at the byte level. **[FOLD F3: md5-distinct is NECESSARY-NOT-SUFFICIENT — the generator body must ALSO be grammar-neutral (no `match grammar` arms), gated by the canonical Lock-14 grep.] [FOLD F10: that grep uses the canonical four-grammar alphabet `Json|CssL4|(GoogleSheets|Sheets)\w*|Bbnf\w*`, scans BOTH `skinny/crates/codegen/src` AND `skinny/xtask/src`, and pairs the `match grammar`-arm census with a grammar-named-*type* census (`rg 'JsonParser|CssL4Parser|GoogleSheetsParser|BbnfBootstrap'` → 0).] [FOLD F13: the xtask-rooted arm-census catches a *self-disclosing grammar-token* branch (`Json =>` etc.) anywhere — it does NOT catch a *neutral-identifier* `RuntimeTarget` strategy table (the regex is syntactically incapable, disk-confirmed: arm-census over `xtask/src` = 0 matches against the live `grammar_name: "css_l4"` DATA table). The relocated-overfit-seam into a neutral-identifier data-table is machine-checked STRUCTURALLY by the P3 collapse close-gate, NOT the regex.] [FOLD F16: the P3 structural collapse projects onto the FULL per-grammar config tuple MODULO the generated-artefact path columns (`output_dir`/`expected_files`) — `fact_schema`/`row_id`/`output_plane`/`emitter`/`entry_rule`/`source_roots`/`check_command`/`frontend_requirements` collapse to ONE distinct config-tuple per `grammar_name`. The V3-prescribed `(source_roots, entry_rule)`-only projection (F13) was too narrow: disk-confirmed the 7 css_l4 rows SHARE `(source_roots, entry_rule)` (collapse=1, GREEN) but carry 7 DISTINCT `fact_schema`/`output_plane`, so a relocated branch riding `fact_schema` sails through the old projection. The MECHANISM (P3 structural collapse) is unchanged from F13; only the PROJECTED COLUMN SET is widened.]**

These three are the load-bearing gates; the per-row Mbps thresholds (§4.1 telemetry) ride on top of #1.

**Ordering rule (PRUNE-before-GENERALIZE):** every GENERALIZE candidate (B-series) has an entry-gate dependency on the PRUNE candidate (A-series) that clears its surface. The shortlist is sequenced; a GENERALIZE candidate may not dispatch until its named PRUNE predecessor closes.

---

## SHORTLIST — 5 candidate clusters (PRUNE-cluster A, then GENERALIZE-clusters B1–B4)

The V3 backlog enumerates 13 line-items (P1–P5, G1–G6, PROVE, H1). Per `[single-plan-execution]` + `[no-deferrals]` they execute in ONE SK-V18 cycle, but they cluster into **5 candidate interventions** (the PASS-ALPHA §2 "≤5" bound), sequenced PRUNE→GENERALIZE→PROVE. Each cluster below is one candidate; its sub-items are the same-wave commit set.

---

### CANDIDATE A — PRUNE the overfit/wrong-arch/contrivance surface (P1+P2+P3+P4+P5)

**Disposition: ACCEPT (mandatory entry-gate for all of B1–B4). Risk: LOW.** *(V1+V2+V3: ACCEPT across CH1/CH2/CH3/CH4/CH5/CH6/CH7; CH4 V3 "cost ground truth accurate in full".)*

This is the prune cluster. It carries **zero** generalization risk (pure deletion + gate-tightening) and is the standing-order predecessor to every B candidate. Bundled because each sub-item clears a surface a later GENERALIZE candidate must touch, and none preserves any >SOTA-bearing code.

| sub | action | owner path:line | LOC Δ |
|---|---|---|---|
| **P1** | DELETE the WHOLE x86 surface crate-wide (aarch64-only mandate; deletion list reach-matched to the verify grep) — **BOTH surfaces** **[FOLD F8 + V5 R-1/CH6 V4 §1]** | **(1)** `skinny/crates/bbnf-simd/src/x86_64/` (742 `.rs` LOC + 105 `.asm` LOC, 24 files, 14 `unimplemented!`); remove `pub mod x86_64;` `bbnf-simd/src/lib.rs:5`; remove the `#[cfg(target_arch="x86_64")]` dispatch arms `lib.rs:285-288`; remove `("crates/bbnf-simd/src/x86_64","diagnostic-x86")` `lock14_baseline.rs:2463`. **(2)** `skinny/crates/bbnf-simd/ext/x86/` (3554 LOC vendored ASM) AND `bbnf-simd/build.rs` (102 LOC nasm driver, delete-or-neutralize) AND the `nasm-rs="0.3"`/`build="build.rs"` deps in `Cargo.toml:8,19` (+`:14-16` comments) AND re-home `src/lib.rs:247` `ext/x86/bbnf.asm` ref AND scrub in-crate doc surfaces OR scope the grep to source+manifest. **(3) COMPILE-COUPLED removal/decoupling sites the verify grep ALSO fires on (V5 R-2/CH5 F.6 widening — a deletion list narrower than the grep ships a RED-by-construction gate, the same hazard V5 R-1 fixed for `Cargo.toml`/`lib.rs`):** `tests/checkasm_parity.rs` carries **11 `x86_64` tokens, 9 of them ACTIVE compile-coupled `bbnf_simd::x86_64::…::*_scalar(…)` call sites** (`:458,:464,:467,:477,:478,:484,:493,:497,:502`) resolving into `src/x86_64/` — **deleting `src/x86_64/` WITHOUT decoupling these BREAKS THE BUILD; DECOUPLE-OR-DELETE the x86_64 block (and the `:672` `#[ignore]` `sk_v3_intrinsic_parity_x86_64` harness)**; and `src/scalar/byte_class_from_eq_set_64.rs` carries residual x86 doc strings (`:10,:12,:15` "AVX-512 BW"/"AVX2") — **CLEAN to aarch64/scalar-neutral**. | **≈ −4500** (847 + 3554 + 102) **[FOLD F11 + V5 R-1 + V5 R-2/F.6]** |
| **P2** | DELETE the OLD contrived CSS bench path (warm 85–357B SHA-fixtures, more-work-comparator) | `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` `lightningcss_facts:528`, `measure_mbps:3091`, the per-fixture SHA256/byte/projection scaffolding `:59-203,:1988+,:2502`; and `benches/nonjson_css_l4.rs` | ~−700 |
| **P3** | COLLAPSE the 7 byte-identical CSS replicas → **7 `generated.rs` (6370 LOC) → 1 (or N-distinct)** **[FOLD: CH4 §1 file-count phrasing]** (OR differentiate `source_roots`/`entry_rule` so output truly diverges — decide in B2). **[FOLD F13 + V5 R-2/CH2 V4 §8.1: this collapse close-gate is ALSO the structural machine-check for the relocated-overfit-seam — all `RuntimeTarget` rows for one `grammar_name` must be byte-identical modulo the generated-artefact path columns (`output_dir`/`expected_files`): `count(distinct config-tuple-minus-output_dir) == 1` over `fact_schema`/`row_id`/`output_plane`/`emitter`/`entry_rule`/`source_roots`. A `(source_roots,entry_rule)`-only `sort -u` is INSUFFICIENT — disk-verified the 7 css_l4 rows DO share `CSS_L4_ROOTS` + `entry_rule:"stylesheet"` BUT carry 7 DISTINCT `fact_schema`/`output_plane`/`row_id`, where a relocated branch rides; the gate is correctly RED pre-collapse, GREEN only post-collapse.]** | `skinny/xtask/src/regen_css.rs:5-22,23,33-` (`TARGETS` table; shared `CSS_L4_ROOTS`/`entry_rule:"stylesheet"` across all 7 rows); 6 of 7 `crates/runtime/src/grammars/css_l4_*/` dirs (~30 ancillary files in 6 collapsed dirs) | ≈ −5460 (6×910) |
| **P4** | FIX the Lock-14 gate exclusion hole — move `runtime_generator.rs`, `json_sink_direct.rs`, `json_typed_direct.rs`, `json_templates/`, `grammar_provider.rs` from the weak `SKV15_W2_EXTRA_COVERAGE_ROOTS:2442` INTO strict `GENERIC_SCAN_ROOTS:2409`; extend `FORBIDDEN_GENERIC_TOKENS:2420` with `CSS_`/`_RS` template-const patterns | `skinny/crates/bbnf-bench/src/lock14_baseline.rs:2409,2420,2442` | +~15 |
| **P5** | PURGE the metalang bench-wave-id leak from shipped JSON runtime | `crates/runtime/src/grammars/json/generated.rs` 7× `parse_w11_1_number_*` (`:801,841,881,955,1007,1019,1031`) — fix at the *generator/template* source so regen --check stays clean | runtime: rename only |

- **Scalar-ref status:** N/A (P1 deletes the `src/x86_64/` stub tree with 0 real intrinsics + 14 `unimplemented!` AND the `ext/x86/` vendored ASM, which is DORMANT — `build.rs:38-40` non-`x86_64` early-return, no aarch64 admission caller, V5 R-1/F15; P2–P5 touch no kernels). The aarch64 scalar references that ARE the spec (checkasm) are untouched.
- **Checkasm status:** the **12 aarch64 single-kernel differential harnesses + `checkasm_common.rs`** signal-trampoline/stack-canary discipline are preserved untouched; **`tests/checkasm_parity.rs` is DECOUPLED, not deleted (V5 R-2/CH5 F.6):** only its x86_64 reference block (9 active `bbnf_simd::x86_64::…::*_scalar(…)` call sites + the `#[ignore]` x86 harness) is removed so the test crate compiles after `src/x86_64/` deletion — the aarch64 parity assertions in the file are retained. **[FOLD F4: count corrected from "18" to 12+2 — α-E is the count-correct reference per CH7 §5; F14 confirms no inheritance of the αD-only stale "18".]**
- **Same-wave consumer:** P4's tightened gate is its own consumer (`accepts_current_allowlist` must still pass — but now *meaningfully*). P3's collapse consumer is the runtime `lib.rs` `pub mod generated_*` roster + `regen.rs::write_targets`/`check_target`, AND the per-grammar config-tuple collapse structural check **[FOLD F13+F16]**. P5's consumer is `regen --check`.
- **Falsifiability gate:**
  - P1 exit (CRATE-WIDE, NOT `src/`-scoped — V5 R-1/CH6 V4 §1; deletion list reach-matched to the grep, V5 R-2/CH5 F.6): `grep -riE --include='*.rs' --include='Cargo.toml' 'avx|gfni|sve|x86|nasm' skinny/crates/bbnf-simd/` → only aarch64-neutral comments (every active hit on the (1)+(2)+(3) removal list — including the 9 `tests/checkasm_parity.rs` `bbnf_simd::x86_64::…` call sites and the `src/scalar/byte_class_from_eq_set_64.rs` doc strings); `find crates/bbnf-simd/src/x86_64 crates/bbnf-simd/ext/x86 -type f` → **0** (incl. the `.asm` AND the vendored ASM tree); `build.rs` + `Cargo.toml` carry no `nasm`/`x86` active token; invariant §5.3 holds; **`cargo build` AND `cargo test --no-run` clean — the `checkasm_parity.rs` decoupling (3) is what keeps the `src/x86_64/` deletion build-sound (without it the test crate fails to compile against the deleted `bbnf_simd::x86_64::…` paths).**
  - P3 exit: post-collapse `find crates/runtime/src/grammars/css* -name generated.rs | wc -l` matches the *true* CSS grammar count (1, or N-distinct if differentiated); **DISTINCT-GRAMMAR-OUTPUT gate #3** holds (no two `generated.rs` byte-identical); **AND the relocated-seam structural check [FOLD F13 + V5 R-2/CH2 V4 §8.1; field set widened per F16/CH2 V5 §8.1]: `count(distinct config-tuple-minus-(output_dir,expected_files)) == 1` per `grammar_name`, enumerate-by-exclusion over the live `regen.rs:6` 12-field `RuntimeTarget` struct — the operative non-path columns are `profile`/`source_inputs`/`metadata_inputs`/`emitter`/`entry_rule`/`source_roots`/`check_command`/`frontend_requirements`/`output_labels` (the prior list OMITTED `profile` — the 7-distinct per-profile discriminator — plus `source_inputs`/`metadata_inputs`; `fact_schema`/`output_plane`/`row_id` are per-profile content the `profile` discriminator selects), NOT a `(source_roots,entry_rule)`-only projection — the 7 css_l4 rows must collapse to ONE css_l4 config with no per-profile divergence in any column that could carry a relocated branch, and P3 must PRESERVE profile-distinctness where the 7 profiles are genuinely distinct grammars (collapse only when truly one grammar; otherwise differentiate by distinct `.bbnf` roots — do NOT erase the `profile` discriminator).**
  - P4 exit: `accepts_current_allowlist` is GREEN **and** meaningful — re-run after temporarily re-introducing a `JsonSink` token into `runtime_generator.rs` must now turn it RED (proving coverage). The gate must no longer pass by exclusion.
  - P5 exit: `grep -rc 'parse_w11_1_number\|_w[0-9]' crates/runtime/src/grammars/json/generated.rs` → **0**; `regen --check` clean.
- **LOC budget:** net **≈ −10800** (P1 ≈ −4500 = `src/x86_64/` 847 incl `.asm` + `ext/x86/` 3554 + `build.rs` 102 **[FOLD F11 + V5 R-1/CH6 V4 §1: BOTH x86 surfaces]** + P3 ≈ −5460 + P2 ~−700 + P4 +~15 + P5 rename-only; disk-recompute = −10600…−10800, if anything understated). Per `[generated-size-budget]` this is a *reduction*, no overflow risk.
- **Risk: LOW.** No >SOTA-bearing code deleted (V3 C3: the old path "did NOT produce the headline numbers"; those came from `css_canon_bench`, which is KEPT). x86 is dead (0 intrinsics). The only care item: P3 must decide collapse-vs-differentiate (defer the *which* to B2, but the *replica deletion* lands here).
- **Pre-blocks (must NOT re-open):** AZ-IV eager value-tree; StructRegistry per-leaf; fact-stream-as-output; 24-broadcast; FNV-in-runtime; **x86/AVX/SVE** (P1 enforces, never re-add). P2 must not delete `css_canon_bench.rs`/`w2_rich_cssom_bench.rs` (the honest harness — H1 keeps it).

---

### CANDIDATE B1 — UN-FORK the generator + PROJECT JSON from grammar (G3 + G1)

**Disposition: ACCEPT. Risk: MEDIUM. Entry-gate: CANDIDATE A closed (P4 gate must scan the emitter).** *(V1+V2+V3: ACCEPT across all lenses; CH2 §5.2 V3 ACCEPT.)*

The structural core of the inflection. Retire the `RuntimeEmitterKind` JSON-vs-CSS fork into ONE program-driven emit path, and make `json_sink_direct::render` actually project the JSON parser from the `SinkOnlyProgram`/grammar (the current hand-written template becomes the byte-for-byte **parity oracle**). G1+G3 are bundled because un-forking is meaningless unless at least one grammar genuinely projects — JSON is the closer of the two (its spine is already grammar-shaped, V3 A1/AGENT-3 §5), so it is the first to be made real, and proves the unified emitter works before CSS rides it (B2). CH2 §5.2 V3 verified the defect: `json_sink_direct::render` consults `program` only in `validate`/`render_header` (`:19,26,30,39,78`) then emits fixed `out.push_str(...)` literal bodies (`render_entry :96`, `render_value_dispatch :124`) — a hand-written template, not a projection.

- **Owner paths:** `skinny/crates/codegen/src/grammar_provider.rs:40` (`RuntimeEmitterKind` — retire the 2-variant fork); `skinny/crates/codegen/src/runtime_generator.rs:16-26` (dispatch), `:33-66` (JSON assembly), `:195` `JSON_PARSE_ONLY_GENERATED_RS`; `skinny/crates/codegen/src/json_sink_direct.rs:4,80,97,100,125,128,251,252,326,327,368` (`render` must consume `SinkOnlyProgram.rules`/`shape.fields`, not `push_str` constant text); `skinny/crates/codegen/src/json_templates/*` (6 files / 1149 LOC byte-identical copies — become the regression oracle, then deleted once derivation reproduces them).
- **Scalar-ref status:** N/A (codegen path, no SIMD kernel). The JSON *scanner* (`json/scan.rs`) is NOT touched here — that is B4/G5.
- **Checkasm status:** N/A.
- **Same-wave consumer:** the unified emitter's only consumer is `xtask regen` → the shipped `crates/runtime/src/grammars/json/generated.rs`. The same commit that lands the program-driven `render()` must regenerate `json/generated.rs` and pass `regen --check` + the JSON bench (`generated_real_typed.rs`).
- **Falsifiability gate (the triple):**
  1. **PRESERVED->SOTA:** JSON `parse_only` on `twitter`/`canada`/`citm`/`github` rows must stay `>` sonic-rs strict, **within −3%** of the SK-V17 hand-written baseline (the unconditional proof row per V3 A1/A5). **Thinnest-margin tripwire (CH4 §2): `apache_builds/parse_only` is +1.4% over sonic-strict — a derived parser that drops 1.4% loses >SOTA on that row.** Telemetry §4.3 schema emitted.
  2. **GRAMMAR-DERIVATION-PROOF:** mutate `grammar/json/*.bbnf` (e.g. add a rule alias) → regenerated `json/generated.rs` reflects it; `json_sink_direct.rs` contains **0** `parse_object_direct`/`JsonSink`-named raw-string literal bodies after the change (V3 F2/AGENT-4). The `verbatim-blob` CHALLENGE lens passes.
  3. **SINGLE-EMITTER-PATH:** `grep -c 'RuntimeEmitterKind\|CompiledLowering\|RequestFacts'` in codegen → **0** (the fork is gone); one `render(program: &SinkOnlyProgram)` path. Companion grammar-neutral grep (the **canonical four-grammar Lock-14 alphabet + widened scan roots, [FOLD F10]**): `rg 'Json\s*=>\|CssL4\s*=>\|(GoogleSheets\|Sheets)\w*\s*=>\|Bbnf\w*\s*=>' skinny/crates/codegen/src skinny/xtask/src` → **0** (the xtask root catches a *self-disclosing grammar-token* branch relocated into metadata; **[FOLD F13: it does NOT catch a neutral-identifier strategy table — that case is covered by the P3 `sort -u` row-count structural check, not this regex]**), AND the grammar-named-*type* census `rg 'JsonParser\|CssL4Parser\|GoogleSheetsParser\|BbnfBootstrap' skinny/crates/codegen/src skinny/xtask/src` → **0**.
- **LOC budget:** net **≈ −800** (delete `JSON_*_RS` consts + `json_templates/` 1149-LOC copies; the projecting renderer is smaller than the verbatim blobs). **Softest budget in the shortlist (CH4 §2) — true LOC unknown until written.** Same-wave consumer regen MUST show `json/generated.rs` line-count within **±5%** of today's (it must reproduce the oracle) — the binding cost-control.
- **Risk: MEDIUM.** JSON is the >SOTA holdout with a real hot kernel; the projection must reproduce the hand-written hot loop *exactly* or the speed regresses. Mitigation: keep `json_templates/` as the byte-for-byte oracle through the wave; the derived output must `diff`-match it before the oracle is deleted (`[clean-regen-discipline]`).
- **Pre-blocks:** no AZ-IV eager tree; no StructRegistry per-leaf; no fact-stream output. Must NOT re-introduce a grammar-named branch into the now-generic emitter (Lock 14; P4 gate enforces). (G3 single-emitter is the SK-V17 REDRESS-W2-1 SUBJECT admitted to discharge — NOT a re-open, per CH3/CH4 §2.)

---

### CANDIDATE B2 — DERIVE CSS from grammar lowering (G2)

**Disposition: ACCEPT. Risk: LOW (per V3 A2). Entry-gate: B1 closed (unified emitter exists) + P3 closed (replicas collapsed).** *(V1+V2+V3: ACCEPT across all lenses; CH2 §5.3 V3 ACCEPT; CH1 REVISE folded as F1.)*

The CSS inflection wave: retire `CSS_GENERATED_RS` (the ~910-LOC hand-written recursive-descent scanner emitted verbatim as a `const &str`, `runtime_generator.rs:701`→`:1611`) and route CSS through the same grammar-lowering path JSON now uses (B1). The `.bbnf` (`grammar/css/l4/stylesheet.bbnf`) finally feeds the CSS parser.

- **Owner paths:** `skinny/crates/codegen/src/runtime_generator.rs:701` (`CSS_GENERATED_RS`, 910 LOC), `:598` (`CSS_MOD_RS`), `:612` (`CSS_PARSER_RS`), `:665` (`CSS_SINK_RS`) (~590 LOC combined), `:76-104` (`emit_request_facts` — retire); `skinny/xtask/src/regen_css.rs:23,33-` (`CSS_L4_ROOTS`/`entry_rule`/`TARGETS`); `grammar/css/l4/*.bbnf` (the source that must now drive emission); `crates/runtime/src/grammars/css_l4_*/generated.rs` (the regenerated output).
- **Scalar-ref status:** N/A at the codegen layer. **Key V3 finding (A2/AGENT-2 §inflection):** the CSS hot path is *already scalar* and runs cache-resident — **there is NO fragile hand-tuned kernel to preserve** (`find_css_significant`/`find_comment_close` are `#[cfg(test)]`-only at `lib.rs:574,598,608` inside the parity-guard module `:500-501`), which is precisely why this is LOW risk. The >SOTA does not depend on hand-shaping.
- **Checkasm status:** N/A (the CSS NEON kernels are B4/G6; this wave is recognizer structure only).
- **Same-wave consumer:** `xtask regen` → `css_l4_*/generated.rs`; the honest `css_canon_bench.rs` + the `assert_rich_strict_equality` 9-field parity oracle (`nonjson_css_l4.rs:451`, the ONE honest artefact KEPT from the old file) on the real 71KB–495KB corpus.
- **Falsifiability gate (the triple):**
  1. **PRESERVED->SOTA — [FOLD F1: pinned to the N=200 `css_canon_bench` close-ledger per-row Mbps]:** the derived CSS recognizer must hold Track1 Mbps **within −3%** of the SK-V17 N=200 close-ledger medians (`skv17-W5-close-ledger.md:99-102`), per row:

     | corpus | Track1 baseline (Mbps) | lightningcss (Mbps) | ratio | −3% floor (Track1) |
     |---|---|---|---|---|
     | bootstrap | 2473.1 | 1119.1 | 2.210× | ≥ 2398.9 |
     | animate | 2937.9 | 1247.7 | 2.355× | ≥ 2850.0 |
     | tailwind | 2773.4 | 828.5 | 3.348× | ≥ 2690.2 |
     | material | 2618.5 | 1312.0 | 1.996× | ≥ 2540.0 |

     **honestly framed per H1** (lazy-rich-summary vs eager-full-CSSOM — see CANDIDATE B3 H1-rider). The live N=80 reproduction (animate 2.145× / bootstrap 2.905× / tailwind 1.911× / material 1.975×) is a **cross-check only, NOT the gate reference** (CH1 §αB/αE: the two sample planes must not be mixed). The derived recognizer must also pass `assert_rich_strict_equality` (9-field) on the real corpus.
  2. **GRAMMAR-DERIVATION-PROOF:** mutate `stylesheet.bbnf` → `css_l4_*/generated.rs` changes; `grep -c 'CSS_GENERATED_RS\|const.*_RS.*r#'` in codegen → **0**. The `verbatim-blob` lens passes (this is the centrepiece the lens was authored to catch).
  3. **DISTINCT-GRAMMAR-OUTPUT:** CSS `generated.rs` ≠ JSON `generated.rs` byte-wise (trivially true once both derive) **AND** the generator body stays grammar-neutral on the **canonical four-grammar alphabet + xtask metadata surface [FOLD F10]** (`rg 'Json\s*=>\|CssL4\s*=>\|(GoogleSheets\|Sheets)\w*\s*=>\|Bbnf\w*\s*=>' skinny/crates/codegen/src skinny/xtask/src` → 0, per F3+F10) **AND** the relocated-seam structural check **[FOLD F13 + V5 R-2/CH2 V4 §8.1]**: all `RuntimeTarget` rows sharing `grammar_name == "css_l4"` collapse to ONE distinct config-tuple MODULO the generated-artefact path columns (`output_dir`/`expected_files`) — enumerate-by-exclusion over the live `regen.rs:6` 12-field struct: `count(distinct (profile, source_inputs, metadata_inputs, emitter, entry_rule, source_roots, check_command, frontend_requirements, output_labels)) == 1` (the prior tuple OMITTED `profile`/`source_inputs`/`metadata_inputs`; `fact_schema`/`row_id`/`output_plane` are per-profile content the `profile` discriminator selects), NOT a `(source_roots, entry_rule)`-only `sort -u` (disk-confirmed the 7 css_l4 rows SHARE `(source_roots, entry_rule)` = `CSS_L4_ROOTS`/`"stylesheet"` but carry 7 DISTINCT `profile` + per-profile `source_inputs`/`metadata_inputs`/`fact_schema`/`output_plane`, so a relocated branch riding `profile`/`fact_schema` sails through the narrow projection — the full-config-tuple collapse is RED today, GREEN only post-collapse; P3 PRESERVES profile-distinctness where the profiles are genuinely distinct grammars). If P3 chose *differentiate* over *collapse*, the N CSS profiles must point at distinct `.bbnf` roots (`color.bbnf`/`media.bbnf`/`selectors.bbnf` exist) so their `generated.rs` truly diverge AND each row names the distinct config-tuple it derives from (the §0.4 prose obligation).
- **LOC budget:** net **≈ −1500** (delete `CSS_GENERATED_RS` 910 LOC + `CSS_MOD_RS`/`CSS_PARSER_RS`/`CSS_SINK_RS` ~590 LOC from codegen; the lowering-derived emission is the shared B1 renderer parameterized by the CSS program). Per `[generated-size-budget]`: if the derived CSS `generated.rs` *exceeds* the hand-written one by `>`20%, halt and trace the generator O(N) regression.
- **Risk: LOW.** No hot kernel to preserve (V3 A2). Honest-finding clause (HANDOFF §6): IF a grammar-derived CSS recognizer CANNOT preserve the >SOTA without hand-shaping, surface it — the hand-shaping becomes a *named, grammar-parameterized primitive* (a registered scanner the grammar invokes), not a silent blob, AND not a relabeled hand-written blob (CH6 §9: the named primitive must be invoked from the `.bbnf`, parameterized, with a checkasm/scalar ref — else REJECT, REDRESS). Do not paper-close.
- **Pre-blocks:** no const-string courier (the thing being retired); no `RuntimeEmitterKind` resurrection; no second CSS tape (Lock 1 — the CSS at-rule tag must keep riding the existing sparse `flag_cursors`/`flag_values` pair, `tape/mod.rs:96-98`).

---

### CANDIDATE B3 — SHARED Value/Document/Cursor trait + instantiate-or-delete phantom `ValueRef<G>` (G4 + H1-rider)

**Disposition: ACCEPT. Risk: MEDIUM. Entry-gate: B1 + B2 closed (both grammars derive; now unify their value surface).** *(V1: ACCEPT-structure; V2 CH5 E.1 two REVISEs folded as F6/F7; V3 CH2 §5.4 + CH5 §E.1 ACCEPT.)*

The value-API isomorphism. Today JSON exposes a recursive document tree (`JsonValue`, `get(key)`, `JsonVisitor`, `DocumentView`) and CSS a flat rule/decl/typed-token stream (`CssTypedNode`, no visitor, does NOT implement `DocumentView`) — they share the `at_cursor` *pattern* hand-copied, with NO shared trait (V3 D2/F4). Define ONE cross-grammar `Value`/`Document`/`Cursor` trait family both instantiate; INSTANTIATE-OR-DELETE the phantom `ValueRef<G: EventGrammar=AnyGrammar>` (`tape/mod.rs:175` — `G` never bound to a real type; `EventGrammar` methods have 0 non-test call sites). Carries the **H1** honesty-rider: re-frame CSS >SOTA as lazy-rich-summary vs eager-full-CSSOM OR add a symmetric materialization-depth comparator.

**[FOLD F6 — DELETE is the DEFAULT branch.]** Per `abrogate-before-patch`: the phantom `<G>` should be **DELETED** unless the shared trait genuinely needs a type-level grammar parameter. INSTANTIATE is the **burden-of-proof** branch, NOT a symmetric alternative — because `CssEventGrammar` **does NOT exist at HEAD** (only `json/event_grammar_witness.rs` `JsonEventGrammar` + `sheets_witness/event_grammar_witness.rs` `SheetsEventGrammar`, both test-only witnesses, verified). Choosing INSTANTIATE requires authoring a new `CssEventGrammar` witness — a new grammar-named coupling surface that is **un-budgeted in the "±0 LOC"** estimate and must be justified before it is animated. The contract default: DELETE the `G` parameter; the shared trait carries no phantom type.

- **Owner paths:** `skinny/crates/runtime/src/tape/mod.rs:175` (`ValueRef<G>` — DELETE `G` by default), `:227` (`DocumentView` — generalize to the shared trait); `skinny/crates/runtime/src/tape/event_grammar.rs` + `tape/event_grammar_tests.rs:89` (the inert `EventGrammar` trait — wire or delete); `crates/runtime/src/grammars/json/{value,view,visitor}.rs` + `json/event_grammar_witness.rs`; `crates/runtime/src/grammars/css_l4_*/generated.rs:45,95,115` (`CssNode`/`CssTypedNode`); `crates/runtime/src/grammars/sheets_witness/event_grammar_witness.rs` (the inert witness — becomes real in CANDIDATE B4).
- **Scalar-ref / checkasm status:** N/A (value-API trait layer, no kernel).
- **Same-wave consumer:** the shared trait's consumers are JSON's `value_from_ref`/`DocumentView` impl AND CSS's `CssNode::value()` — both must implement the SAME generated trait in the same commit (no orphan trait). Per `[phantom-generic]`: if the unified trait does NOT need a type-level `G`, **DELETE** `ValueRef<G>`'s `G` parameter (the default, abrogate-before-patch) rather than animate it; if it DOES (burden of proof), bind `G` to a *production* `JsonEventGrammar`/`CssEventGrammar`/`SheetsEventGrammar` at ≥1 NON-test call site.
- **Falsifiability gate:**
  1. **PHANTOM-GENERIC resolved — [FOLD F6: test-excluded grep]:**
     - DELETE branch (DEFAULT): `grep -c 'G: EventGrammar' crates/runtime/src/tape/mod.rs` → **0** (the `G` parameter is gone).
     - INSTANTIATE branch (burden-of-proof): `grep -rn 'ValueRef<.*EventGrammar>' --include='*.rs' crates/runtime/src | grep -v 'tests\.rs\|#\[cfg(test)\]'` → **≥1** production (NON-test) instantiation. The standing test-only `JsonEventGrammar` line (`event_grammar_tests.rs:89`) must NOT satisfy the gate (the false-green CH5 E.1 caught).
  2. **SHARED-TRAIT — [FOLD F9: test-excluded + canonical trait name, mirroring the :140 phantom-grep exclusion]:** both JSON and CSS `impl <SharedValueTrait>` in **NON-test** code — `grep -rn 'impl .* SharedValueTrait .* for' --include='*.rs' crates/runtime/src/grammars | grep -v 'tests\.rs\|#\[cfg(test)\]'` returns BOTH families (today only JSON implements `DocumentView`). The exclusion is mandatory: a `#[cfg(test)] impl SharedValueTrait for CssTestNode` would otherwise false-green this gate exactly as the standing test `JsonEventGrammar` line would have false-greened the phantom gate before F6. (Equivalently, inherit the SYNTHESIS:394 `shared_value_trait_instantiations` definition — "≥2 real **production** instantiations; test-only `_proof_compiles` does NOT count" — which already binds the machine-checked close gate; this aligns the research recipe to it.)
  3. **PRESERVE-RICH-AST structural gate — [FOLD F7: both-impl is necessary-not-sufficient]:** JSON's `get(key)` + typed `Kind` accessors + the visitor must remain reachable **THROUGH** the shared trait (not via a bypass). Operational test: the JSON view-time tests still compile + pass against the trait surface (not a side-channel); the trait is NOT a lowest-common-denominator collapse that discards JSON's richness. A both-impl grep is necessary; the richness-preservation is a SEPARATE checked condition.
  4. **H1-HONESTY:** the CSS >SOTA claim in `RESULTS.md` carries the explicit materialization-depth framing ("lazy rich-summary `>` eager full-CSSOM") OR a symmetric comparator row is added. The `timed-plane-symmetry` CHALLENGE lens passes on the kept `css_canon_bench`.
  5. **PRESERVED->SOTA:** no value-API regression — JSON `parse_full_traversal`/`path_lookup` rows + CSS rich-summary rows stay within −3% (the trait must be zero-cost; if it adds vtable/dispatch cost, the abstraction is wrong).
- **LOC budget:** net **≈ ±0** (a trait + 2–3 impls replaces hand-copied surface; roughly LOC-neutral). **Caveat (F6):** if the INSTANTIATE branch is taken, the new `CssEventGrammar` witness adds un-budgeted LOC — the DELETE default keeps ±0. Preserve-rich-ast: the JSON visitor/navigable cursor MUST survive the unification (do NOT flatten JSON to CSS's thinner surface — V3 notes JSON is the richer; the trait must accommodate both depths).
- **Risk: MEDIUM.** The trait must be zero-cost (no vtable in the hot path) AND must not flatten the rich JSON AST (`[preserve-rich-ast]` is non-negotiable). The two value APIs are genuinely divergent in surface (recursive tree vs flat stream); the shared trait is an *abstraction over both*, not a lowest-common-denominator collapse.
- **Pre-blocks:** no AZ-IV eager value-tree (the trait stays lazy/tape-backed); no StructRegistry per-leaf indirection; preserve-rich-ast (no JSON flattening); Lock 1 (one substrate — the trait rides the existing `Tape`).

---

### CANDIDATE B4 — PROVE on Sheets via the generator ONLY + scanner/NEON honesty (PROVE + G5 + G6)

**Disposition: ACCEPT. Risk: MEDIUM-HIGH (the honest litmus). Entry-gate: B1 + B2 + B3 closed (one emitter, two grammars derived, shared trait exists).** *(V1: ACCEPT-substance; V2 CH2 §5.5 + CH4 §5 four REVISEs folded as F2/F3/F4/F5; V3 CH2 §5.5 + CH5 §E.4 + CH6 §10 ACCEPT.)*

The generalization litmus + the remaining acceleration honesty. Three same-wave items: **PROVE** Sheets from a 25-LOC stub to a real third grammar **via the generator ONLY** (no hand-written `_GENERATED_RS`); **G5** migrate JSON's bespoke string-aware NEON scanner (`json/scan.rs`, the legacy holdout) onto the neutral alphabet-parametric kernel CSS already rides; **G6** wire-or-retire the CSS NEON honestly into the HOT path (today `find_css_significant`/`find_comment_close` are dead at admission, only `#[cfg(test)]`) + the 5 scalar-passthrough kernels + the aarch64 ASM backlog. Bundled because Sheets is the proof that B1–B3 generalized, and the scanner/NEON items are the substrate-honesty tail that the Sheets grammar will exercise.

**[FOLD F2 — the Sheets source is named + it is a genuinely different shape.]** The Sheets grammar is `grammar/google-sheets/google-sheets.bbnf` (verified the only real Sheets `.bbnf`; it lives under `grammar/`, NOT in the benched skinny tree — the skinny tree has NO Sheets grammar root and NO xtask Sheets target today). The PROVE owner-set therefore MUST: (a) name `grammar/google-sheets/google-sheets.bbnf` as the source, (b) wire a skinny Sheets grammar root + an xtask `RuntimeTarget` for Sheets, (c) acknowledge the grammar uses **Pratt operator-precedence + error literals + cell references** — shapes that NO JSON/CSS rule exercises. This is the genuine generality stress AND the good news: it makes the litmus **non-hollow by construction** (Sheets is not a "third JSON"). Honest-finding clause (HANDOFF §6): if the generator cannot lower Pratt precedence, that is a genuine finding to surface — the named, grammar-parameterized precedence primitive becomes the result, not a hand-written blob and not a paper-close.

- **Owner paths:**
  - PROVE: `grammar/google-sheets/google-sheets.bbnf` (the SOURCE, F2); a skinny Sheets grammar root + `xtask/src/regen*.rs` Sheets `RuntimeTarget` (NEW, F2 — and its config-tuple must be a distinct `grammar_name` from css_l4's AND from json's, so the per-grammar config-tuple collapse structural check **[FOLD F13 + V5 R-2/F16]** counts a genuine third grammar config, not a relabeled CSS row); `crates/runtime/src/grammars/sheets_witness/` (25-LOC stub → full generated runtime).
  - G5: `crates/runtime/src/grammars/json/scan.rs:201-...` (bespoke NEON, JSON-density-tuned `sampled_structural_capacity:56`) → migrate onto `runtime/src/runtime_simd.rs` bridge + `bbnf_simd::prim::byte_class_from_eq_set_64` (the kernel CSS already uses).
  - G6: `runtime/src/runtime_simd.rs:112,169` (`find_comment_close`/`find_css_significant` — wire into the hot recognizer or retire honestly); the 5 scalar-passthrough `_neon` kernel BODIES at `bbnf-simd/src/aarch64/bitmap_prefix_xor_64.rs:2`, `aarch64/bitmap_next_set_bit.rs`, `aarch64/bulk_emit_positions_64.rs`, `aarch64/byte_class_from_table_64.rs`, `aarch64/eob_pad_clamp.rs:4` (where the body is rewritten) + the registration/relabel/retire site **`bbnf-simd/src/dispatch.rs` [FOLD F12: corrected from `aarch64/dispatch.rs` — disk-verified the only `dispatch.rs` is `src/dispatch.rs`; line numbers NEON `:68-73`, scalar twins `:80-85` are correct]**; `bbnf-simd/src/aarch64/digit_mac.rs:27,40` (orphan UDOT `parse_4_digits_dotprod`, `grep`=0 runtime callers — wire into number parsing or delete).
- **Scalar-ref status:** STRONG and the spec — every aarch64 kernel has a scalar reference declared the executable spec (V3 A4 §5). G6's 5 passthrough kernels currently have scalar bodies under a `_neon` suffix (`bbnf-simd/src/dispatch.rs:68-73` NEON entries point at `aarch64/<kernel>::<kernel>_neon`, scalar twins `:80-85` **[FOLD F12]**); SK-V18 either gives them real NEON bodies (PMULL prefix-XOR FIRST, V3 §6, with the scalar ref as the checkasm oracle) OR renames them honestly (drop the `_neon` suffix). The `_neon`/`_simd`-suffix-truth lens (V3 AGENT-4 §9) gates this.
- **Checkasm status — [FOLD F4: corrected count; α-E is the count-correct reference per CH7 §5, F14]:** **12 single-kernel differential harnesses + `checkasm_common.rs` + `checkasm_parity.rs` = 14 `checkasm_*.rs` files total** (disk-verified: `ls checkasm_*.rs | wc -l`=14; the prior "18" was an overcount that would make any "18 present" gate un-satisfiable on a clean tree — the exact P4-class false gate this cycle fixes; the αD-only stale "18" is a SEPARATE V3 REVISE, F14, that α-E does NOT inherit). The harness discipline (signal trampoline, 1 KiB stack-canary, x19–x28 callee-saved sentinel) is KEPT and EXTENDED. **Each new NEON body adds exactly 1 differential harness** (current N=12 single-kernel → N=12+k where k = committed bodies). Any new NEON body lands WITH its checkasm differential in the same commit (scalar-ref-as-spec, alignment + set-size + adversarial-seed sweeps). The `acceleration-wiring` CHALLENGE lens requires the kernel be reached AT ADMISSION (hot path), not `#[cfg(test)]`.
- **Same-wave consumer (the load-bearing rule, prevents the V5 orphan-kernel pattern):**
  - PROVE: Sheets `generated.rs` consumed by a Sheets bench row + the shared value-API trait (B3) instantiated for Sheets.
  - G5: JSON scanner migration lands WITH the JSON `parse_only` bench as consumer (must not regress).
  - G6: each wired NEON kernel lands WITH its hot-path caller in the same commit (e.g. PMULL `bitmap_prefix_xor_64` WITH the JSON string-quote-carry / CSS comment-mask caller; UDOT `digit_mac` WITH the number scanner). **A kernel with no admission-path consumer is RETIRED (or honestly relabelled, dropping `_neon`), not shipped.**
- **Falsifiability gate (the triple, sharpest here):**
  1. **DISTINCT-GRAMMAR-OUTPUT (the litmus) — [FOLD F3: md5-distinct is NECESSARY-NOT-SUFFICIENT; FOLD F10: canonical four-grammar alphabet + widened roots + type census; FOLD F13+F16: + the P3 config-tuple collapse covers the neutral-identifier data-table the regex AND the `(source_roots, entry_rule)`-only projection both cannot]:** `diff crates/runtime/src/grammars/{json,sheets_witness}/generated.rs` → **NON-zero** AND `diff css.../generated.rs sheets.../generated.rs` → **NON-zero** (three md5-distinct `generated.rs`) **AND** the generator body is grammar-neutral on the **canonical Lock-14 alphabet** (`LOCKS.md:349`): `rg 'Json\s*=>\|CssL4\s*=>\|(GoogleSheets\|Sheets)\w*\s*=>\|Bbnf\w*\s*=>' skinny/crates/codegen/src skinny/xtask/src` → **0** (`GoogleSheets` UN-abbreviated — the prior `Sheets\w*` start-anchor would MISS a `GoogleSheets =>` arm; `Bbnf` included for SK-V19 forward-safety; the xtask root catches a *self-disclosing grammar-token* metadata branch) **AND** the grammar-named-*type* census `rg 'JsonParser\|CssL4Parser\|GoogleSheetsParser\|BbnfBootstrap' skinny/crates/codegen/src skinny/xtask/src` → **0** (a generated emitter could re-emit a `SheetsEventGrammar`/`JsonEventGrammar`-shaped named type — the arm census alone misses it) **AND [FOLD F13+F16] the neutral-identifier-data-table threat is machine-checked by the per-grammar config-tuple collapse** (all `RuntimeTarget` rows sharing one `grammar_name` collapse to ONE distinct config-tuple modulo the generated-artefact path columns `output_dir`/`expected_files`, enumerate-by-exclusion over the live `regen.rs:6` 12-field struct: `count(distinct (profile, source_inputs, metadata_inputs, emitter, entry_rule, source_roots, check_command, frontend_requirements, output_labels)) == 1` per `grammar_name` — the prior tuple OMITTED `profile` (the 7-distinct per-profile discriminator) + `source_inputs`/`metadata_inputs`; `fact_schema`/`row_id`/`output_plane` are per-profile content `profile` selects; disk-confirmed the arm-census regex is syntactically incapable of firing on a `grammar_name: "css_l4"` DATA table AND the prior `(source_roots, entry_rule)`-only projection missed the 7 distinct `profile`/`fact_schema`/`output_plane` rows, so the full-config-tuple collapse, not the regex and not the narrow projection, polices a per-profile strategy table carrying a relocated branch riding `profile`/`fact_schema`; P3 PRESERVES profile-distinctness where the profiles are genuinely distinct grammars). Three distinct files produced by a generator that still `match grammar { Json => …, CssL4 => …, GoogleSheets => … }` internally is the self-disclosing Lock-14 violation the arm-census catches; a branch RELOCATED into a neutral-identifier data-table is the violation the per-grammar config-tuple collapse catches. THREE non-identical `generated.rs` from ONE grammar-NEUTRAL generator = generalization is real. If Sheets `generated.rs` cannot be produced without hand-writing a `_GENERATED_RS` block, the generator is still overfit and B1/B2 iterate (HANDOFF §6 honest-finding).
  2. **GRAMMAR-DERIVATION-PROOF:** Sheets has **zero** hand-authored runtime Rust — `grep -c 'const.*_RS.*r#' codegen/src` for any Sheets blob → **0**; Sheets falls entirely out of the B1 generator from `google-sheets.bbnf`.
  3. **ACCELERATION-WIRING (G6):** any kernel labelled `_neon` is reached at admission — `grep -rn 'find_css_significant\|find_comment_close\|<wired_kernel>' crates/runtime/src` returns ≥1 NON-`#[cfg(test)]` caller on the hot path. The SK-V17 W3 overstatement ("NEON structural-index acceleration of the CSS scan" — dead at admission, V3 C1) is corrected: wired-or-retired honestly. A kernel honestly relabelled (no `_neon`) is NOT a NO-GO; only a `cfg-test-only` *acceleration claim* is.
  4. **PRESERVED->SOTA — [FOLD F1: CSS baseline pinned to N=200 medians]:** G5 must not regress JSON `parse_only` (the migration onto the neutral kernel must match or beat the bespoke scanner — if the neutral kernel is slower, expose the JSON string-mask path AS a parametric kernel rather than regress, V3 F5). Threshold: JSON `parse_only` within −3% on `twitter`/`canada`/`citm`/`github`. **CSS rows (if B4 re-runs the canonical harness) hold the SAME N=200 per-row floors as B2 gate#1 (bootstrap ≥2398.9 / animate ≥2850.0 / tailwind ≥2690.2 / material ≥2540.0 Mbps Track1), NOT a ratio range, NOT N=80.**
- **LOC budget — [FOLD F5: G6 bounded by a committed body-count ceiling]:** PROVE +~200 (Sheets `.bbnf` is referenced, not authored; the generated runtime falls out of B1 ⇒ near-zero hand LOC; the skinny grammar-root + xtask target is +~30); G5 net −~100 (bespoke scanner retired onto shared kernel); **G6: PMULL `bitmap_prefix_xor_64` is the ONE committed real NEON body (+~150 with its checkasm differential); every OTHER kernel (the remaining 4 passthroughs + UDOT/TBX/CSSC) is RETIRED or honestly relabelled UNLESS a same-wave hot-path consumer exists for it.** The "+150 per body" is therefore bounded: committed ceiling = +~150 (one body) unless a consumer materializes for more, in which case each adds +1 checkasm + ~150 LOC under the same-wave-consumer gate. Net **≈ +250** (capped). Per `[generated-size-budget]`: Sheets `generated.rs` line-count must be O(grammar-size), traced if anomalous.
- **Risk: MEDIUM-HIGH.** This is where "does it generalize?" is answered honestly. The Sheets grammar IS a genuinely different shape (Pratt + error literals + cell refs, F2) — the `distinct-grammar-output` + grammar-neutral-body gate + the per-grammar config-tuple collapse structural check (F13+F16) guards the hollow-litmus failure. G5 carries real >SOTA-regression risk (JSON is the speed holdout). G6 PMULL/UDOT are real asm — full checkasm discipline mandatory (current N=12 differentials + each new body adds 1).
- **Pre-blocks:** x86/AVX/SVE/nasm (P1 crate-wide per V5 R-1/F15 — aarch64 ASM only); no orphan kernel (same-wave consumer mandatory, the V5 pattern); FNV-in-runtime; no `_neon` suffix on a scalar passthrough without honest relabel; Lock 1 (Sheets rides the existing tape, no third substrate); broadcast (Sheets cell-corpus must be cold per-parse, not 24-broadcast — CH6 §10/αC pre-block).

---

## SUMMARY TABLE

| # | candidate | V3 items | risk | LOC Δ | entry-gate | the gate that falsifies it |
|---|---|---|---|---|---|---|
| **A** | PRUNE overfit/x86/contrivance | P1–P5 | LOW | ≈ −10800 (P1 ≈ −4500 BOTH x86 surfaces incl `.asm`+`ext/x86/`+`build.rs`, F11/V5 R-1) | (first) | x86=0 crate-wide (both `src/x86_64/` 24 files AND `ext/x86/` 3554 LOC AND nasm `build.rs`/`Cargo.toml` dep, CH6 V4 §1); replicas collapsed (per-`grammar_name` config-tuple-minus-`output_dir` distinct=1 over all non-path columns, F13/V5 R-2); Lock-14 gate meaningful; metalang purged |
| **B1** | un-fork emitter + project JSON | G3+G1 | MED | ≈ −800 | A | single-emitter-path + neutral body (self-disclosing arm/type census); grammar-derivation-proof; JSON >sonic preserved (apache +1.4% tripwire) |
| **B2** | derive CSS from lowering | G2 | LOW | ≈ −1500 | B1, P3 | verbatim-blob retired; CSS >lightningcss preserved (N=200 per-row floors, H1-framed); distinct+neutral+row-count (F13) |
| **B3** | shared value trait + kill phantom `<G>` | G4+H1 | MED | ≈ ±0 | B1, B2 | phantom-generic resolved (DELETE default, test-excluded grep); shared trait both-impl + rich-ast reachable; H1 honesty |
| **B4** | PROVE Sheets + scanner/NEON honesty | PROVE+G5+G6 | MED-HIGH | ≈ +250 (capped) | B1,B2,B3 | 3 distinct + neutral-body + per-`grammar_name` config-tuple collapse over all non-path columns (F13/V5 R-2) generated.rs; Sheets 0-hand-LOC from google-sheets.bbnf; accel-wired-at-admission; JSON preserved |

**Net LOC across all 5:** ≈ **−12850** (disk-recomputed incl BOTH x86 surfaces per F11/V5 R-1: A ≈ −10600…−10800 [P1 ≈ −4500 = `src/x86_64/` 847 + `ext/x86/` 3554 + `build.rs` 102, + P3 ≈ −5460 + P2 ~−700], B1 −800, B2 −1500, B3 ±0, B4 +250 capped ⇒ −12650…−12850; CH4 §6.1 + CH6 V4 §1). A generalization cycle that DELETES far more than it adds — the inflection backtrack collapses two forked hand-written parsers + replicas + BOTH x86 surfaces into one generator.

---

## CROSS-CUTTING NOTES (for α-F contract draft + S-P3 wave plan)

1. **Sequencing is binding (PRUNE-before-GENERALIZE):** A → B1 → B2 → B3 → B4. Each B entry-gates on its predecessor. A green Lock-14 gate (P4) must land BEFORE B1, so the un-forked emitter is actually scanned for neutrality as it is built (otherwise B1 could re-leak under a blind gate). **[CH6 §5: the entry-gate dependency must be carried as an EXPLICIT exit-gate-blocks-successor clause into S-P3, so a broken G1 predecessor halts G2/G3/G4/PROVE rather than marching on.]**
2. **The honest-finding escape (HANDOFF §6, sharpened per CH6 §9):** if B2 proves CSS, or B4 proves Sheets, CANNOT preserve >SOTA / cannot lower (e.g. Pratt) without hand-shaping, that is a genuine finding — the hand-shaping becomes a *named, validated, grammar-parameterized primitive* **invoked from the `.bbnf`, parameterized, with a checkasm/scalar reference** (e.g. a registered balanced-delimiter or precedence scanner the grammar invokes), NOT a silent hand-written `_RS` blob, NOT a relabeled hand-written blob, and NOT a paper-close. A "primitive" that is a relabeled blob without `.bbnf`-invocation + parameterization + a reference is REJECTED to REDRESS. This is the abrogate-before-patch / pluggable-components discipline.
3. **The kept-honest artefacts (do NOT prune):** `css_canon_bench.rs` + `w2_rich_cssom_bench.rs` (the genuinely cold, real-corpus, N≥200 harness — H1/H2); the 9-field `assert_rich_strict_equality` oracle; the `@generated` headers; `validate_unique_targets`/`validate_generated_roster` roster gates; the **12 checkasm single-kernel differentials + `checkasm_common.rs` + `checkasm_parity.rs`** hardening; the substrate (Lock 1 — one `Tape`/`ValueRef`/`PayloadArena`, the genuine foundation).
4. **Telemetry binding (§4.3):** every B candidate's PRESERVED->SOTA gate emits the full per-row schema (Track 1/2 Mbps, sonic-strict/simdjson-DOM deltas, hot-leaf, strictness/output-plane). **yyjson/asmjson/RapidJSON emit honest `None` on aarch64 (FFI not wired — only simd-json + sonic-rs are wired, `Cargo.toml:23`); the gate must NOT be read to require an un-run engine's number (CH1 §0.6 fix).** The `xtask gate-json` must reject any row missing the columns AND must add the generalization-status columns (`verbatim_blob_present==false`, `emitter_fork_present==false`, `phantom_generic_resolved∈{instantiated,deleted}`, `generated_md5_distinct`, `generator_grammar_count==3`, `acceleration_at_admission∈{admission,scalar-passthrough-labeled,retired}` NOT `cfg-test-only`, `corpus_in_timer==true`, `materialization_framing`) so the three generalization gates are machine-checkable per row. **[FOLD F10+F13+F16: the `generator_grammar_branch_count==0` column consumes the canonical four-grammar Lock-14 arm-census grep `Json|CssL4|(GoogleSheets|Sheets)\w*|Bbnf\w*` over BOTH `skinny/crates/codegen/src` AND `skinny/xtask/src` (catching a *self-disclosing grammar-token* branch wherever it lives), paired with the grammar-named-*type* census `JsonParser|CssL4Parser|GoogleSheetsParser|BbnfBootstrap` → 0; AND a SEPARATE structural column `runtime_target_config_tuple_collapsed == true` machine-checks, per `grammar_name`, that all `RuntimeTarget` rows collapse to ONE distinct config-tuple MODULO the generated-artefact path columns (`output_dir`/`expected_files`) — i.e. over `(fact_schema, row_id, output_plane, emitter, entry_rule, source_roots)`. The prior `runtime_target_distinct_config_rows` over `(source_roots, entry_rule)` ALONE is FALSE-GREEN on the live table (7 css_l4 rows share `(source_roots, entry_rule)` but carry 7 distinct `fact_schema`) and is REPLACED — a relocated branch riding `fact_schema`/`output_plane`/`emitter` escapes the narrow projection; the full-config-tuple collapse is the relocated-seam defense.]**
5. **Relocated-overfit-seam — the F13+F16 correction (binding for α-F + S-P3):** the V3 contract mis-attributed the relocated-seam machine-check to the xtask-rooted arm-census grep (CH2 §8.1/V3 refuted it: the regex returns 0 over the neutral-identifier `RuntimeTarget` DATA table). F13 moved the defense to the P3 structural row-count check. **F16 (CH2 §8.1/V4) then found the V3-prescribed structural PROJECTION `(source_roots, entry_rule)` was ITSELF too narrow** — disk-confirmed the 7 css_l4 rows share `(source_roots, entry_rule)` (collapse=1, GREEN) but carry 7 DISTINCT `fact_schema`/`output_plane`/`row_id`, so a relocated branch riding `fact_schema` sails through. The corrected attribution: (i) the arm-census grep catches a *self-disclosing grammar-token* branch wherever relocated — KEPT, NECESSARY-NOT-SUFFICIENT; (ii) the relocated-seam into a *neutral-identifier* strategy table is machine-checked STRUCTURALLY by the **P3 collapse close-gate projecting onto the FULL per-grammar config-tuple modulo `output_dir`/`expected_files`** (`fact_schema`/`row_id`/`output_plane`/`emitter`/`entry_rule`/`source_roots` collapse to ONE distinct tuple per `grammar_name`), NOT `(source_roots, entry_rule)` alone; the check is RED today (7 distinct `fact_schema`), GREEN only post-P3-collapse — which is correct. Plus the §0.4 prose obligation "every residual routing entry names the `.bbnf` rule it derives from," reviewed at admission. This is a verification-surface projection fix, NOT architectural — the Lock-14 spine + the P3 collapse MECHANISM are correct; only the projected column set widened.
6. **Risk-weighted close prediction:** A (LOW) + B2 (LOW) close with high confidence (no kernel to preserve). B1/B3 (MED) carry the zero-cost-projection / zero-cost-trait risk (B1's apache_builds +1.4% is the thinnest tripwire). B4 (MED-HIGH) is the litmus + the only real >SOTA-regression surface (G5 JSON scanner). If B4's Sheets litmus fails, SK-V18 does NOT paper-close — it surfaces "generator is still JSON+CSS-overfit," iterates B1/B2, and B4 re-enters (V≤5 ceiling).
7. **No candidate re-opens a pre-blocked route:** verified against the V3 pre-block list (AZ-IV eager, StructRegistry per-leaf, fact-stream-output, 24-broadcast, FNV-runtime, x86/AVX/SVE/nasm — now crate-wide per V5 R-1/F15). CH3 was 100% ACCEPT on the α-E architecture at V3, and the V4 CH3 REVISE is the F15 P1-scope orphan (folded), not an architectural re-open. The shortlist is additive-by-deletion; it removes overfit, it does not resurrect rejected architecture.
8. **N=80 vs N=200 plane discipline (CH1):** the contract bar is the **N=200 `css_canon_bench` close-ledger medians** (bootstrap 2.210× / animate 2.355× / tailwind 3.348× / material 1.996×). The live N=80 reproduction (animate 2.145× / bootstrap 2.905× / tailwind 1.911× / material 1.975×) is a cross-check only. The two sample-size planes must NEVER be silently mixed in the SPEC's −3% gate; every PRESERVED->SOTA CSS floor references the N=200 per-row Mbps (B2 gate#1 table).
9. **V4→V5 convergence (this cycle):** the V4 CHALLENGE pass returned α-E at **CH1 §αE = REVISE (the x86-scope orphan), CH2 §8.1 = REVISE (the projection-tuple), CH3 = REVISE (same x86-scope orphan), CH4 §αE = ACCEPT, CH5 §αE = ACCEPT, CH6 #8 = ACCEPT, CH7 §αE = ACCEPT.** The two distinct V4 REVISEs are folded: **F15** (CH1 §αE / CH3 — the SECOND x86 surface NOT propagated into αE's P1 row + exit gate; re-verified live at HEAD `318d9c046`: `ext/x86/` 3554 LOC + `build.rs` 102 LOC + `Cargo.toml` nasm dep + `lib.rs:247` ref; P1 widened crate-wide, exit gate moved `src/`-scoped → crate-wide, LOC corrected −847 → ≈ −4500); **F16** (CH2 §8.1 — the relocated-seam structural projection `(source_roots, entry_rule)` was too narrow; disk-confirmed the 7 css_l4 rows share `(source_roots, entry_rule)` but carry 7 distinct `fact_schema`; the projection widens to the full per-grammar config-tuple modulo `output_dir`/`expected_files` across αE:145/:196/:226 + cross-cutting notes 4/5). No candidate was added or removed (still exactly 5: A, B1–B4); the shortlist remains additive-by-deletion. No re-opened REDRESS pre-block. The three load-bearing generalization gates (PRESERVED->SOTA / GRAMMAR-DERIVATION-PROOF / DISTINCT-GRAMMAR-OUTPUT) are unchanged in substance — F15 widens P1's x86 deletion scope + close gate, F16 widens one structural machine-check's projected column set; both are course-corrections to enforcement reach, not architecture. The prior V3→V4 folds (F13 relocated-seam attribution; F14 αD-only checkasm "18", α-E carries 12+2 correctly) remain landed.
