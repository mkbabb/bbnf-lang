---
agent: CH7
pass: T-P3-synthesis
cycle: V2
lens: OVERFIT-PRUNE
disposition: REVISE
generated_at: 2026-05-23T23:55:00-04:00
inputs_audited:
  - restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §CH7 (lens definition; lines 62-87)
  - restart/audit/totality/p3/hardening/V2/CHALLENGE-CONTEXT.md (V2 dispatch §0-§4)
  - restart/audit/totality/p3/hardening/V1/CH7.md (V1 REVISE; six repair items across two findings + 1 optional hygiene)
  - restart/audit/totality/p3/3A-architecture-synthesis.md (V2 91 lines)
  - restart/audit/totality/p3/3B-master-plan-reconciliation.md (V2 247 lines)
  - restart/audit/totality/p3/3C-locks-crystallisation.md (V2 208 lines; was 186 at V1)
  - restart/audit/totality/p3/3C-locks-v+1-diff.md (V2 390 lines; was 366 at V1)
  - restart/audit/totality/p3/3D-skinny-fold.md (V2 284 lines; was 264 at V1)
  - restart/audit/totality/p3/3E-grammar-generalisation.md (V2 284 lines; was 282 at V1)
  - restart/audit/totality/p3/3F-migration-handoff.md (V2 327 lines)
  - restart/locks/LOCKS.md (HEAD: 16-lock count + 5-shape canon at Lock 10 line 164)
  - restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md (canonical T-P2 V3 32:69 = 31.7%)
  - restart/audit/totality/p2/hardening/HARDENING-T-P2-V5-CONVERGED.md (LOCK cycle; no numeric refutation density)
  - git log 144606e64 (V2 atomic micro-fold) + aea580279 (V2 CHALLENGE-CONTEXT)
---

## Lens Contract

CH7 Overfit-Prune (`restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md:62`-`87`)
lenses every artefact for: (a) generated-vs-hand-written discipline (no
`@generated` headers on hand-coded source); (b) Lock 14 generic-crate
compliance; (c) every admit lands via a real source change with
strict-vs-strict comparator + per-iter equality; (d) every "generated"
output passes a delete + regen round-trip; (e) no SCAFFOLD-ONLY landing
counts as an admit. CH7 REJECT triggers immediate plan revise OR redress
revert; CH7 may not be carried as "acknowledged but not blocking".

V2 dispatch (`CHALLENGE-CONTEXT.md:28`) specialises CH7 to the V1→V2
discharge surface: F-V2-CH7-3C Pattern H census `-maxdepth 2` drop +
F-V2-CH7-3F `31:64 → 32:69` cohort-density correction across 5 sites +
V5→V3 attribution + meta-CH7 self-correction. LAC-1E-12 + NEW-CH2-V3-02
executable verification mandate (per T-P1 V5 + T-P2 V3 institutionalisation)
applies on every cited path:line.

## Verdict

**REVISE.**

V2 substantially discharges the V1 CH7 repair surface but two of the
three V1-named census-command sites went un-amended and the 3C diff
preface-body carries a still-stale `31:69 = 31.7%` numerator. V1's
repair surface enumerated **nine** discrete sites: five `31:64` sites in
3F (lines 71, 107, 131, 280, 315), one `31:69 = 31.7%` arithmetic
mismatch at 3C diff line 69, and three Pattern H census command sites
(3C diff V4-7 hunk + 3B-MP-D03 + 3F-MIG-003). V2 discharged six of
nine: all five 3F refutation-density sites are now `32:69 = 31.7%` with
re-attribution to T-P2 V3 CONSOLIDATED at `:76,172,187,295`; the 3C
diff V4-7 hunk and 3C crystallisation L14-pattern-h-census row both
correctly drop `-maxdepth 2` and explain the 63-vs-67 gap. **Three
sites remain non-conforming at V2 HEAD `aea580279`:**

1. **3C diff line 69 preface body** still reads `SK-V14 cohort 31:69 =
   31.7% refutation density preservation` — the arithmetic is still
   broken (31/69 = 44.9%, not 31.7%) and the canonical paired form is
   `32:69 = 31.7%`. The V2 fold commit `144606e64` touched the 3C
   crystallisation Appendix + V4-1/V4-3/V4-4/V4-7 hunk metadata but did
   not amend the V4-1 hunk body's preface-paragraph numerator on
   line 69.
2. **3B-MP-D03 line 124** still binds the census command as
   `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name
   '*.rs' \| wc -l` (and the code-block illustration at line 217 is the
   same stale form). The V2 fold commit corrected the 3C twin but did
   not propagate to 3B.
3. **3F-MIG-003 line 123** still binds the census command as
   `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name
   '*.rs' | wc -l`. Same propagation gap.

Both defects are doc-level, narrow, and convergence-blocking under
CH7's contract ("cannot be carried as acknowledged but not blocking").
The 3C diff stale numerator at the preface — the very block that
institutionalises CH7-binding for LOCKS.md — is the most acute
recurrence of the T-P1 V1 COH-012 counter-surface-fabrication pattern
the dispatch context flagged as the V2 close target ("counter-surface
fabrication pattern closed"). The Pattern H propagation gap leaves two
of three sibling sites binding a command whose output (63) contradicts
the lock's own asserted figure (67) — exactly the failure mode LAC-1E-12
was authored to prevent.

The architectural CH7-positive substrate is unchanged from V1 and
remains strong: 16-lock count preserved (LAC-1E-12 lands as preface
clause); 5-shape `BackendShape` canon preserved at Lock 10 line 164;
audit-overlay 4-column schema lands cleanly at 3C-L08 + 3B-MP-D07 +
3D-FOLD-014; SKELETON DELETE refusal architecture present in both
3B-MP-NW-SK14 + 3F-MIG-007; no fake `@generated` recurrence as
productive admit; no SCAFFOLD-as-load-bearing admit; no gate-relabel
admit; LAC-1E-14 `FactStream` as 5th SUBSTRATE not 6th BackendShape
preserved verbatim across 4 sites in 3F (V1 optional hygiene
implicitly discharged — the loose "5th BackendShape variant"
parenthetical no longer appears anywhere in 3F). T-P2 V5 attribution
to "LOCK confirmation cycle" + numerator pair from V3 CONSOLIDATED is
correctly disentangled at all five 3F sites.

## Findings

### §1 — Per V1 repair-item discharge audit

| V1 repair item | V2 site | V2 status | finding | V3 repair |
|---|---|---|---|---|
| 1(a) 3F: replace `31:64` with `32:69` at 3F:71 | `restart/audit/totality/p3/3F-migration-handoff.md:71` | **DISCHARGED** | Site reads "doubly inadmissible per T-P2 V3 LOCK cohort refutation density 32:69 = 31.7% (`HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295`)". V3 attribution correct; canonical pair correct. | None. |
| 1(a) 3F: 3F:107 | `restart/audit/totality/p3/3F-migration-handoff.md:107` | **DISCHARGED** | Site reads "**REJECTED** per T-P2 V3 LOCK cohort refutation density 32:69 = 31.7% (1:2 anti-paper-close pattern; canonical T-P2 V3 figure at `restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295`)". | None. |
| 1(a) 3F: 3F:131 | `restart/audit/totality/p3/3F-migration-handoff.md:131` | **DISCHARGED** | Same canonical pair + re-attribution to T-P2 V3 figure at `:76,172,187,295`. | None. |
| 1(a) 3F: 3F:280 | `restart/audit/totality/p3/3F-migration-handoff.md:280` | **DISCHARGED** | Site reads "formalises T-P2 V3 LOCK cohort refutation density 32:69 = 31.7% (`HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295`) as a re-proposal gate". | None. |
| 1(a) 3F: 3F:315 | `restart/audit/totality/p3/3F-migration-handoff.md:315` | **DISCHARGED** | Site reads "cites T-P2 V3 LOCK cohort refutation density 32:69 = 31.7% (`HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295`) as the LOCKS-strengthening evidence". | None. |
| 1(b) 3C diff V4-1 hunk preface body line 69: replace `31:69 = 31.7%` with `32:69 = 31.7%`, re-attribute to `HARDENING-T-P2-V3-CONSOLIDATED.md:76` | `restart/audit/totality/p3/3C-locks-v+1-diff.md:69` | **NOT DISCHARGED** | Line 69 still reads "+SK-V14 cohort 31:69 = 31.7% refutation density preservation; anti-paper-close". Arithmetic still broken (31/69 = 44.9%); canonical pair still missing. This is the LOCKS-preface CH7-binding clause body — the most load-bearing site in the entire packet for CH7 self-binding. The V2 fold commit `144606e64` "F-V2-CH6-3C LOAD-BEARING anchor `34a28f5c1`→`e12c5323d` + 12 per-hunk transcripts + Appendix" touched 3C extensively but did not amend the V4-1 hunk body. | (a) Edit `restart/audit/totality/p3/3C-locks-v+1-diff.md:69` to read `+SK-V14 cohort 32:69 = 31.7% refutation density preservation; anti-paper-close`. (b) Append re-attribution to evidence list at `:78`-`:80` already citing `HARDENING-T-P2-V3-CONSOLIDATED.md:48` — extend to `:48,76`. (c) Verify_action: `grep -n "32:69 = 31.7%" restart/audit/totality/p3/3C-locks-v+1-diff.md` returns ≥1 hit. |
| 2 3C diff V4-7 hunk body: drop `-maxdepth 2` | `restart/audit/totality/p3/3C-locks-v+1-diff.md:272` | **DISCHARGED** | Hunk body at V4-7 reads `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l` followed by "The bound command MUST omit `-maxdepth 2` so the four files living at depth 3 under `google_sheets/document/{path_query.rs, mod.rs, canonical.rs, view.rs}` are counted; verified at HEAD `e12c5323d`: the corrected command returns 67 ... while the `-mindepth 2 -maxdepth 2` form returns 63 and contradicts the asserted figure". Source-authority alignment confirmed. | None. |
| 2 3B-MP-D03 paragraph: drop `-maxdepth 2` | `restart/audit/totality/p3/3B-master-plan-reconciliation.md:124` (+ `:217` code-block illustration) | **NOT DISCHARGED** | Line 124 still binds "committed `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l` transcript". Line 217 (the code-block illustration that ends the bound-command demonstration) is the same stale form. The 3C twin was amended at V2 but 3B was not — propagation gap. | (a) Edit `:124` to drop `-maxdepth 2` matching the 3C V4-7 binding. (b) Edit `:217` code-block to the same. (c) Append to the same row's rationale "Bound census command verified at HEAD `aea580279`: corrected form (no `-maxdepth 2`) returns 67; `-maxdepth 2` form returns 63 missing 4 files at depth 3 under `google_sheets/document/`." |
| 2 3F-MIG-003 paragraph line 123: drop `-maxdepth 2` | `restart/audit/totality/p3/3F-migration-handoff.md:123` | **NOT DISCHARGED** | Line 123 still binds "Per-tranche Pattern H census via `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l` cited at every wave commit". Propagation gap from 3C twin. | (a) Edit `:123` to drop `-maxdepth 2`. (b) Append parenthetical "(MUST omit `-maxdepth 2` per `restart/audit/totality/p3/3C-locks-v+1-diff.md:272-279`; verified HEAD `aea580279` corrected form → 67, `-maxdepth 2` form → 63)." |
| 2 LAC-1E-15 source row in 3C: add verified-at-HEAD note | `restart/audit/totality/p3/3C-locks-crystallisation.md:121` | **DISCHARGED** | Row body extended: "Bound census command verified at HEAD `e12c5323d`: `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l` → 67 (matches asserted Pattern H total); `-maxdepth 2` variant → 63 and misses the 4 google_sheets/document/* files at depth 3. Routed into hunk 3C-L14-pattern-h-census with the no-`-maxdepth-2` command form." | None (though for V3 the SHA may refresh from `e12c5323d` to `aea580279` if any LOCKS-surface text has shifted; current re-run at HEAD `aea580279` returns 67 unchanged, so no body edit required). |
| 3 Optional hygiene: tighten 3F-MIG-004 parenthetical | `restart/audit/totality/p3/3F-migration-handoff.md:104,125,311,327` | **DISCHARGED (implicit)** | All four 3F LAC-1E-14-mentioning sites use "5th admitted-product category at the Lock 1 SUBSTRATE manifest" or "5th admitted-product category" + explicit "NOT a 6th `BackendShape` variant" disambiguation. `grep -n "5th BackendShape variant" restart/audit/totality/p3/3F-migration-handoff.md` returns zero hits at V2 HEAD. The loose V1 parenthetical no longer appears. | None. |

### §2 — V2-specialised CH7 audit (per dispatch §2 focus)

| dispatch focus | V2 status | evidence |
|---|---|---|
| F-V2-CH7-3C Pattern H census drop `-maxdepth 2` (now returns 67) | **PARTIAL** | The 3C V4-7 hunk body and 3C crystallisation L14-pattern-h-census + LAC-1E-15 row all correctly drop `-maxdepth 2` and bind the no-`-maxdepth-2` form. Live verification at HEAD `aea580279`: `find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l` returns **67**; `find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l` returns **63**; the 4-file gap is at `google_sheets/document/{path_query, mod, canonical, view}.rs` (depth 3). The 3C-amended form matches the asserted 67. BUT the sibling sites 3B-MP-D03 (`:124` + `:217`) and 3F-MIG-003 (`:123`) still bind the stale `-maxdepth 2` form. Dispatch §2.7 claim "F-V2-CH7-3C Pattern H census drop `-maxdepth 2` (now returns 67)" is true for the 3C anchor but the propagation to its two sibling lock-text mirrors is incomplete. |
| F-V2-CH7-3F numerator 31:64 → 32:69 at 5 sites + V3-CONSOLIDATED re-attribution (V5 was confirmation cycle; counter-surface fabrication pattern closed) | **DISCHARGED at 3F**, **NOT DISCHARGED at 3C diff line 69** | 3F lines 71, 107, 131, 280, 315 all carry `32:69 = 31.7%` re-attributed to `HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295`. The V5→V3 attribution correction is sound (V5 CONVERGED is silent on the numerator pair; `grep -nE "31.7\|32:69" restart/audit/totality/p2/hardening/HARDENING-T-P2-V5-CONVERGED.md` returns zero hits — verified). However the 3C diff V4-1 hunk preface body line 69 still carries `31:69 = 31.7%` (arithmetic-broken; not the canonical pair). The counter-surface fabrication pattern is closed at 3F but recurs at exactly the site that institutionalises CH7 binding for LOCKS.md — a meta-CH7 self-collision on the LAC-1E-12 carrier itself. |
| LAC-1E-12 + NEW-CH2-V3-02 institutionalisation held at V2 fold sites | **DISCHARGED** | `grep -c "verified at HEAD" restart/audit/totality/p3/3C-locks-crystallisation.md` = 11 (LAC-1E-15 row + Pattern H census row + Appendix re-execution transcript + 8 V3-carried disposition rows with grep -n re-execution evidence). `restart/audit/totality/p3/3C-locks-v+1-diff.md` Appendix cites Pass Omega CRUD-3 `e12c5323d` consistently. The executable verification mandate is materially enforced at the 3C surface. (The 3B-MP-D03 + 3F-MIG-003 propagation gap above is precisely a violation of this mandate at sibling sites — the bound census command does NOT verify what the lock asserts at those two sites.) |
| §3Z: V1 70% → V2 100% expected (first ≥95%); V3 confirming → 2-cycle LOCK | **NOT MET at V2** | V1 disposition was REVISE with 2 blocking findings + 1 optional hygiene = ~70% ACCEPT-rate at finding level (5 of 7 ACCEPT-equivalent CH7-positive checks held; 2 REVISE). V2 discharges 6 of the 9 V1-named repair sites (66.7%) and the optional hygiene (implicit). However, 3 of 9 repair sites remain non-conforming (line 69 stale numerator + 3B-MP-D03 + 3F-MIG-003), so V2 cannot reach ≥95% ACCEPT-rate at the V1-repair-discharge level. V2 ACCEPT-rate at finding level: **~78%** (7 of 9 V1 repair items discharged; 7 of 8 V2 CH7-positive carry-forward checks held; meta-CH7 pattern survives at 3C diff line 69). LOCK at V3 requires both the three remaining sites and confirming ≥95% × 2 consecutive cycles per §3Z. |

### §3 — Executable verification (re-run at V2 HEAD `aea580279`)

#### §3.1 16-lock count preservation

```
$ grep -nE "^## (Lock |Gestalt)" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md
44:## Gestalt — sixteen locks

$ grep -nE "^[0-9]+\. \*\*" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md | wc -l
16

$ grep -c "Lock 17" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-crystallisation.md
0
$ grep -c "Lock 17" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-v+1-diff.md
0
$ grep -nE "NOT Lock 17" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-crystallisation.md
29:... Carrier: in-preface clause, NOT Lock 17 — preserves 16-lock count.
117:... Carrier per T-P1 V5 §6.1 disposition: in-preface clause, NOT Lock 17 — preserves 16-lock count per PASS-3 §8.1.
```

Verifies: 16 numbered locks at HEAD; Gestalt heading at `:44`; zero
affirmative Lock 17 claims; four explicit "NOT Lock 17" disambiguation
sites across 3C (two in crystallisation + two in diff).

#### §3.2 5-shape BackendShape canon

```
$ grep -nE "EagerTape.*OffsetTape.*EventTape.*SinkOnly.*CollapsedStage" /Users/mkbabb/Programming/bbnf-lang/restart/locks/LOCKS.md
164:10. **Pratt + SIMD auto-detected; ...** ... `LayoutFacts.backend_shape ∈ {EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}` ...
```

Verifies: Lock 10 line 164 binds the 5-shape canon at HEAD; LAC-1E-14
V4-3 hunk inserts at Lock 1 manifest level (`:71`-`:73`), does not
modify Lock 10. 3F lines 104, 125, 311, 327 all carry the 5-shape canon
verbatim with explicit "NOT a 6th `BackendShape` variant"
disambiguation.

#### §3.3 T-P2 V3 canonical refutation density at V2 sites

```
$ grep -nE "32:69|31:64|31:69" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3F-migration-handoff.md
71:    - "SKELETON triple DELETE proposal (FSM_DISPATCH_THREADED + FRAME_PUSH/POP_BOUNDED) — doubly inadmissible per T-P2 V3 LOCK cohort refutation density 32:69 = 31.7% ..."
107:| 3F-MIG-007 | ... 32:69 = 31.7% (1:2 anti-paper-close pattern; canonical T-P2 V3 figure at ...HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295) ...
131:7. ... 32:69 = 31.7% (1:2 anti-paper-close pattern; canonical T-P2 V3 figure at ...HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295) ...
280:| positive | ... 32:69 = 31.7% (...HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295) as a re-proposal gate ...
315:| CH6 | ... cites T-P2 V3 LOCK cohort refutation density 32:69 = 31.7% (...HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295) ...

$ grep -nE "31:64|31:69" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-v+1-diff.md
69:+SK-V14 cohort 31:69 = 31.7% refutation density preservation; anti-paper-close

$ grep -nE "32:69|31.7%" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p2/hardening/HARDENING-T-P2-V5-CONVERGED.md
(zero hits)

$ grep -nE "32:69" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p2/hardening/HARDENING-T-P2-V3-CONSOLIDATED.md
76:| **CH6 ANTI-PAPER-CLOSE** | ... | Cohort refutation 32:69 = 31.7% density (1:2 anti-paper-close pattern preserved). ...
172:- **CH6** cohort refutation 32:69 = 31.7% density preserved (1:2
187:| 1 | **Refutation discipline INSTITUTIONALIZED** | Cohort 32:69 = 31.7% density preserved across V2+V3 ...
198:| Refutation ratio | 31:64 | 32:64+ | **32:69 (1:2 density)** | extended + locked |
295:1. **Refutation discipline INSTITUTIONALIZED** (cohort 32:69 ratio; CH6 lens).
```

Verifies: (a) all five 3F sites carry the canonical `32:69 = 31.7%` +
V3 re-attribution — DISCHARGED. (b) 3C diff line 69 still carries
stale `31:69 = 31.7%` — NOT DISCHARGED. (c) V5 CONVERGED is silent on
the numerator (V1's V5→V3 attribution correction is sound). (d)
canonical T-P2 V3 figure is `32:69` at 5 sites in the V3 CONSOLIDATED
document.

#### §3.4 Pattern H file count vs bound command at V2 HEAD

```
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l
67
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' | wc -l
63
$ find /Users/mkbabb/Programming/bbnf-lang/crates/core/src/runtime -mindepth 3 -type f -name '*.rs'
.../google_sheets/document/path_query.rs
.../google_sheets/document/mod.rs
.../google_sheets/document/canonical.rs
.../google_sheets/document/view.rs

$ grep -nE "mindepth 2 -maxdepth 2" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3B-master-plan-reconciliation.md
124:... committed `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l` transcript ...
217:find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' | wc -l

$ grep -nE "mindepth 2 -maxdepth 2" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3F-migration-handoff.md
123:... `find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' | wc -l` cited at every wave commit ...

$ grep -nE "mindepth 2 -maxdepth 2" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-v+1-diff.md
278:+    while the `-mindepth 2 -maxdepth 2` form returns 63 and contradicts the
```

Verifies: 67 total `.rs` files at depth ≥ 2 (matches asserted Pattern H
total); 63 at depth exactly 2; 4-file gap at `google_sheets/document/`
depth 3. The 3C diff V4-7 hunk binding is correct (uses no-`-maxdepth-2`
form; the `:278` mention is the diagnostic body explaining the 63
contradiction). BUT 3B-MP-D03 (`:124` + `:217`) and 3F-MIG-003 (`:123`)
still bind the stale `-maxdepth 2` form — the same two sibling sites
V1 named for repair.

#### §3.5 V2 Appendix re-execution transcript discipline (LAC-1E-12 institutionalisation)

```
$ grep -c "verified at HEAD\|e12c5323d" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-crystallisation.md
33

$ grep -nE "^## V3-Merged Re-Execution" /Users/mkbabb/Programming/bbnf-lang/restart/audit/totality/p3/3C-locks-crystallisation.md
189:## V3-Merged Re-Execution Transcript Appendix (LAC-1E-12 mandate)
```

Verifies: 33 "verified at HEAD" / `e12c5323d` mentions in 3C
crystallisation; new Appendix `## V3-Merged Re-Execution Transcript
Appendix (LAC-1E-12 mandate)` at line 189 — V2 fold packet `144606e64`
F-V2-CH6-3C "12 per-hunk transcripts + Appendix" discharged. LAC-1E-12
executable verification mandate is materially institutionalised at the
3C anchor (though violated at the 3B-MP-D03 + 3F-MIG-003 sibling sites
where the bound command produces 63 not 67).

### §4 — CH7-specialised carry-forward checks (V1 § 3 carry-forward + V2 specialisations)

| check | result | evidence |
|---|---|---|
| Counter-surface fabrication (T-P1 V1 COH-012 pattern) | **DETECTED-AT-3C-DIFF-LINE-69** | The 3C diff V4-1 hunk preface body — the very block institutionalising CH7 binding for LOCKS.md — still asserts the arithmetic-broken `31:69 = 31.7%` cohort density. The mismatch (31/69 = 44.9% ≠ 31.7%) is itself the COH-012 pattern (asserting a quantitative claim into a document body where the arithmetic does not validate). The dispatch context's "counter-surface fabrication pattern closed" claim is true at 3F (5 sites discharged) but **false at the 3C anchor**. This is the meta-CH7 self-collision: LAC-1E-12 institutionalises executable verification, and the surrounding-context arithmetic fails self-verification. | `restart/audit/totality/p3/3C-locks-v+1-diff.md:69`. |
| Fabricated baselines | NONE detected | Audit-zero 0/17 × 3 JSON planes + 0/24 CSS L4; 40-admit AUDIT-FALSIFIED cohort; Pattern H 67-file census; 30-violation A3 census — all trace to SK-V14 SYNTHESIS / S-P0 audit-overfit / 1C-runtime-evidence / 1E-locks-evidence with verified path:line citations and reproduce at HEAD. | `3D:162` FOLD-3D-014; `3F:110` HANDOFF-003; `3A:40` D08; `3B:124` MP-D03 (Pattern H 67 figure asserted correctly; only the bound command is stale). |
| Hand-coded "generated" headers | NONE-ADMITTED | Every `@generated` token in the packet is diagnostic. SK-V13 W1b CSS L4 pathology cited as DISPROVED and routed to R4 + W2 + W4 PRUNE-2 for revert + regen-css xtask round-trip. | `3D:109`; `3C-diff:151-177` (V4-4 hunk); `3B:126` (MP-D05 R4). |
| Lock 14 generic-crate compliance | PROPOSED-FORWARD-CORRECTLY (with two stale sibling-site bindings) | Packet does not produce new generic-crate violations; routes the existing 30-violation A3 census + 8-arm `RuntimeProvider` to SK-V14 W5 PRUNE-3. LAC-1E-15 Pattern H census institutionalises forward-direction prevention at 3C anchor. 3B-MP-D03 + 3F-MIG-003 sibling-site command-staleness is a Lock 14 *enforcement-text* defect, not a Lock 14 *primary-violation* defect, but the dispatch §2 LAC-1E-12 institutionalisation claim is materially weaker at those two sites. | `3B:130` (MP-D09); `3F:102` (3F-MIG-002); `3C-diff:252-279` (V4-7 anchor). |
| Strict-vs-strict comparator + per-iter equality | INSTITUTIONALISED | Lock 8 v+1 audit-overlay 4-column binding holds (3C-L08 V2-amended); `xtask gate-json` REJECTS rows missing any column; comparator-plane provenance mandatory. | `3C-diff:188-208` (V4-5 hunk); `LOCKS.md:130-137`. |
| Round-trip clean check | INSTITUTIONALISED | Lock 6 + Lock 14 v+1 regen-roundtrip hunk (LAC-1E-13). Every `// @generated` file requires trace to rostered xtask emission + byte-equivalent regen + reject hand-patching. | `3C-diff:151-177` (V4-4 hunk); `3C:35,118,135,159` (LAC-1E-13 routing). |
| SCAFFOLD-as-load-bearing pattern | NONE-ADMITTED | W7 PRUNE-5 receiver wires W8/W9 SCAFFOLD to LOAD-BEARING (3F-MIG-005); F-V2-P1ABC-RERECORD Stage-0 W10 UNCONDITIONAL closes orphan-kernel risk (3D-FOLD-012); W8/W9 SCAFFOLD-ONLY pre-block in 3B refuted cohort. | `3F:105`; `3D:160`; `3B:82`. |
| Gate-relabel-as-admit pattern | NONE-ADMITTED | The 40-admit gate-relabel cohort (5 parse_only + 4 direct + 7 typed + 24 CSS) is correctly classified DISPROVED across 3D-§2 and 3F-HANDOFF-003 with revert receivers (W1 PRUNE-1 + W4 PRUNE-2) named. | `3D:110`; `3B:82`; `3F:110`. |
| LAC-1E-12 preface-vs-Lock 17 carrier discipline | PRESERVED | Zero affirmative "Lock 17" claims across 3C; 4 explicit "NOT Lock 17" disambiguation sites; 16-lock canon at LOCKS.md:44 unchanged. | `3C:29,117`; `3C-diff:16,71`. |
| LAC-1E-14 5th-SUBSTRATE-not-6th-BackendShape discipline | PRESERVED | 4 sites in 3F (`:104,125,311,327`) + 3C-L01-factstream-fifth-category hunk + 3A-D07 + 3E-D05 + 3D §1/§2 rows all explicitly disambiguate. V1 optional hygiene (loose "5th BackendShape variant" parenthetical) implicitly discharged — zero hits at V2 HEAD. | `grep -n "5th BackendShape variant" restart/audit/totality/p3/3F-migration-handoff.md` returns 0 hits. |
| V5→V3 attribution correction (5 3F sites) | DISCHARGED | All 5 3F sites cite "T-P2 V3 LOCK cohort refutation density 32:69 = 31.7%" with anchor at `HARDENING-T-P2-V3-CONSOLIDATED.md:76,172,187,295`. V5 attribution at the disposition level ("LOCK") is sound (T-P2 V5 CONVERGED is the cycle that LOCKED V3); only the numerator pair sourced from V3. | `3F:71,107,131,280,315`. |

## Repair Requirements (V3)

1. **3C diff V4-1 hunk preface body line 69: replace `31:69 = 31.7%` with `32:69 = 31.7%`.**
   - Site: `restart/audit/totality/p3/3C-locks-v+1-diff.md:69`.
   - Current text (verbatim): `+SK-V14 cohort 31:69 = 31.7% refutation density preservation; anti-paper-close`.
   - Required: `+SK-V14 cohort 32:69 = 31.7% refutation density preservation; anti-paper-close`.
   - Append T-P2 V3 anchor to evidence list at `:78`-`:80`: extend `HARDENING-T-P2-V3-CONSOLIDATED.md:48` to `:48,76,172,187,295`.
   - Verify_action: `grep -n "32:69 = 31.7%" restart/audit/totality/p3/3C-locks-v+1-diff.md` returns ≥1 hit; `grep -n "31:69" restart/audit/totality/p3/3C-locks-v+1-diff.md` returns 0 hits.
   - **Severity: HIGH (meta-CH7 self-collision; the surrounding LAC-1E-12-binding text fails its own executable-arithmetic check).**

2. **3B-MP-D03 + code-block illustration: drop `-maxdepth 2`.**
   - Sites: `restart/audit/totality/p3/3B-master-plan-reconciliation.md:124` (prose) + `:217` (code-block illustration).
   - Current text (`:124`, verbatim): `committed \`find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l\` transcript`.
   - Required: `committed \`find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l\` transcript`.
   - Same correction at `:217`.
   - Append to MP-3B-V1-D03 rationale: "Bound census command verified at HEAD `aea580279`: corrected form (no `-maxdepth 2`) returns 67 matching the asserted Pattern H total; `-maxdepth 2` form returns 63 missing 4 files at depth 3 under `google_sheets/document/`. The bound command MUST omit `-maxdepth 2` per `restart/audit/totality/p3/3C-locks-v+1-diff.md:272-279`."
   - Verify_action: `grep -nE "mindepth 2 -maxdepth 2" restart/audit/totality/p3/3B-master-plan-reconciliation.md` returns 0 hits.
   - **Severity: HIGH (LAC-1E-12 executable-verification-mandate violation at sibling lock-text site).**

3. **3F-MIG-003 line 123: drop `-maxdepth 2`.**
   - Site: `restart/audit/totality/p3/3F-migration-handoff.md:123`.
   - Current text (verbatim): `\`find crates/core/src/runtime -mindepth 2 -maxdepth 2 -type f -name '*.rs' \| wc -l\` cited at every wave commit`.
   - Required: `\`find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' \| wc -l\` cited at every wave commit`.
   - Append parenthetical: "(MUST omit `-maxdepth 2` per `restart/audit/totality/p3/3C-locks-v+1-diff.md:272-279`; verified HEAD `aea580279` corrected form → 67, `-maxdepth 2` form → 63 missing 4 files at depth 3 under `google_sheets/document/`)."
   - Verify_action: `grep -nE "mindepth 2 -maxdepth 2" restart/audit/totality/p3/3F-migration-handoff.md` returns 0 hits.
   - **Severity: HIGH (same as repair 2).**

No other V3 repair required. The 9 V1-named repair sites become 9 sites
DISCHARGED at V3 close (6 already at V2 + 3 above). All other CH7-positive
substrate is intact and unchanged from V1.

## Cycle Disposition

**REVISE, not REJECT.** V2 substantially advanced the V1 repair surface:
6 of 9 V1-named repair sites are DISCHARGED (5 × 3F refutation-density
correction + 3C V4-7 census command + 3C L14 + LAC-1E-15 row), the
optional hygiene is implicitly DISCHARGED (loose "5th BackendShape
variant" parenthetical no longer appears), and the V2 fold packet's
F-V2-CH6-3C 12-per-hunk-transcript + Appendix work materially
institutionalises the LAC-1E-12 executable-verification mandate at the
3C anchor. **But three sites remain non-conforming**: the 3C diff V4-1
hunk preface body line 69 still carries the arithmetic-broken
`31:69 = 31.7%` numerator (a meta-CH7 self-collision at the very block
that institutionalises CH7 binding for LOCKS.md), and the 3B-MP-D03 +
3F-MIG-003 sibling sites still bind the stale `-maxdepth 2` census
command form. Both defects are doc-level, narrow, and convergence-
blocking under CH7's "cannot be carried as acknowledged but not
blocking" contract.

V2 ACCEPT-rate at the V1-repair-discharge level: **~78%** (7 of 9
discharged) — does NOT meet the ≥95% LOCK-eligible threshold for §3Z.
V2 ACCEPT-rate at the CH7-positive carry-forward level: **~91%** (10 of
11 checks held; the counter-surface fabrication carry-forward fails at
3C diff line 69). Combined V2 ACCEPT-rate: **~85%** — first cycle above
the 80% threshold but does not satisfy §3Z cohort LOCK (≥95% × 2
consecutive cycles).

V3 path to LOCK: discharge the three Repair Requirements above
(3-edit V3 fold packet; all single-line corrections); confirming V3
expected to reach ≥95% across all CH7 checks; V3+V4 then provides the
2-cycle §3Z confirmation window. The V5 ceiling per §3W remains.
