# SK-V14 T-P1 Totality Excavation — V3 CHALLENGE Consolidated

Aggregator: SK-V14 T-P1 V3 hardening aggregator (write-only).
Date (UTC): 2026-05-23.
Scope: seven-lens CHALLENGE V3 over the four V3-amended T-P1 inventory
artefacts (`1A-substrate-evidence.md`, `1C-runtime-evidence.md`,
`1D-skinny-lessons.md`, `1F-anti-pattern.md`) atop the four V2-LOCKED
artefacts (`1B-codegen-evidence.md`, `1E-locks-evidence.md`,
`1F-coherence-scan.md`, `1F-past-corpora.md`) committed at HEAD
`0a9f1288c` (T-P1 V3 atomic micro-fold over V2 baseline `87816a2cd`).
Authority: `restart/prompts/totality/PASS-1-EXCAVATION.md §3 + §5`
(CH1-CH7 specialisations + convergence rule); `restart/prompts/ORCHESTRATOR.md
§3W` (universal CH-lens registry) + `§3Z` (cohort LOCK = ≥95 % × 2
consecutive cycles + zero orphan REVISEs; V≤5 ceiling);
`restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §CH7` (Overfit-Prune
lens binding; LAC-1E-12 anti-fabrication template); prior V2 consolidator
at `restart/audit/totality/p1/hardening/HARDENING-T-P1-V2-CONSOLIDATED.md`
(818 lines; format reference + V3 fold-packet authority); dispatch
`restart/audit/totality/p1/hardening/V3/CHALLENGE-CONTEXT.md` §0-§4.
Input ledger: seven V3 lens dispositions under
`restart/audit/totality/p1/hardening/V3/`
(`CH1.md` 418 lines, `CH2.md` 114, `CH3.md` 107, `CH4.md` 202, `CH5.md`
86, `CH6.md` 244, `CH7.md` 299 — 1470 lens lines + 40 CHALLENGE-CONTEXT
lines).

This consolidator supersedes the V3 stub previously occupying this path
(a 45-line interim placeholder authored before the seven V3 lenses
returned with full cycle data).

## §0 — V3 cycle verdict

### §0.1 Per-lens dispositions (verbatim from each CH file's verdict)

| Lens | Definition | Sub-axes | ACCEPT | REVISE | REJECT | Per-lens ACCEPT-rate | V2 rate | V1 rate | Verdict |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| CH1 CORRECTNESS | every row's spec path:line ↔ impl path:line ↔ verdict triangle resolves against HEAD; cited symbol presence verified; cited negative-search results reproduce; cite-rebinds carry executable verification | 8 inventories | 8 | 0 | 0 | **100 %** | 100 % | 62.5 % | ACCEPT (**2-cycle LOCK V2+V3**; all V3 cite-rebinds executable-verified; CH7-V2-failure-mode structurally addressed; two non-blocking V4 polish notes: AP-009 "24→27 hits" count refresh + 1E:33-35 sustained-UNKNOWN paragraph carry-forward) |
| CH2 GENERALITY | Lock 14 holds; no JSON-only divergence catalogued when grammar-neutral substrate fact; 1C runtime census flags every grammar-named module in a generic crate; 1D separates JSON-empirical from grammar-neutral; no grammar-name leak passes uncited; pass-layer grammar-shape leaks distinct from codegen-layer name leaks | 12 rows | 11 | 1 | 0 | **91.7 %** | 91.7 % | 60 % | **REVISE — single residual orphan cell** at `1C-runtime-evidence.md:40` (executive summary still reads "126 grammar-named type reexports" while every structural row reads 127; mechanical re-extraction at V3 HEAD confirms 127 exactly; single-token V3 fold miss; V4 mechanical repair) |
| CH3 REGRESSION (REDRESS) | no T-P1 inventory re-opens a REDRESS route already rejected, blocked, or admitted-then-falsified; 1D pre-block list + 1E LAC ledger correctly identify SK-V14 pre-blocks; no admitted REDRESS row mis-catalogued as unimplemented | 9 findings | 8 | 1 | 0 | **88.9 %** | 100 % | 66.7 % | **REVISE — single LOW precision-nit** at `1D-skinny-lessons.md:140` (W13.9 labelled `MEASURED-REJECT` but live `REDRESS.md:4734` reads `CORRECTNESS-REJECT`; substance regression-discipline-correct since `CORRECTNESS-REJECT > MEASURED-REJECT`; V4 mechanical one-cell fix) |
| CH4 COST | every divergence carries LOC-delta + risk class under orchestrator six-field schema (loc_budget / risk / wave / hard_cap / same_wave_consumer / evidence_basis); 1E amendment candidates state wave-alignment hint; V3 cite-rebinds are cost-neutral by construction | 8 artefacts | 8 | 0 | 0 | **100 %** | 100 % | 100 % | ACCEPT (**3-cycle LOCK V1+V2+V3**; NEW V3 finding: every cite-rebind in F-V3-CH7-1/CH7-2/CH5-1/CH1-1 is cost-neutral — zero LOC-band rescale required, zero hard-cap-multiplier disturbance, zero wave-alignment perturbation; cost-neutrality discipline now a load-bearing CH4 invariant for cite-rebind class) |
| CH5 HIDDEN COUPLING | no parallel substrate / sidecar / retained cursor / second source scan; substrate union holds; no renamed-scanner closure; 1F anti-pattern scan catches live couplings | 9 findings | 9 | 0 | 0 | **100 %** | 100 % | 42.9 % | ACCEPT (**2-cycle LOCK V2+V3**; CH5-V3-008 residual housekeeping flag at 1D row 117 sub-case enumeration carrying pre-rebind cite list verbatim — non-blocking cross-reference annotation; V4 refresh) |
| CH6 ANTI-PAPER-CLOSE | self-reports of "resolved/wired/honoured/proved/implemented pre-block" require live-evidence citation; every UNKNOWN carries verify_action; no divergence deferred to "a later inventory" | 22 row classifications | 22 | 0 | 0 | **100 %** | 100 % | 68.4 % | ACCEPT (**2-cycle LOCK V2+V3**; critical finding: V3 fold structurally addresses CH7-V2-failure-mode at PROTOCOL level via LAC-1E-12 procedural addendum + NEW-CH2-V2-03 enumeration discipline; empirically validated by AP-009 V3 self-correction + 1C 126→127 repair surfaced by V3 CH2) |
| CH7 OVERFIT-PRUNE | SK-V14 audit-corrected baseline propagation; no fake-pattern recurrence in inventory text; executable verification mandate applies to every cite-bearing micro-fold INCLUDING forward-carried cites from prior cycles; meta-CH7 self-test for dispatch-context-inherited fabrication | 5 dispatch checks (9 full-inventory targets) | 5 | 0 | 0 | **100 %** (dispatch overlay) / **100 %** (full inventory) | 66.7 % | 77.8 % | ACCEPT (**first ≥95 % cycle**; V1 77.8 % → V2 66.7 % → V3 100 % — cleanest convergence of the cohort's deepest-regressed lens; meta-CH7: V2 dispatch context itself contained CH7-class error ["zero hits" assertion]; V3 correctly names and overturns this; recursive anti-fabrication discipline now load-bearing in inventory text) |

### §0.2 Aggregate ACCEPT-rate

Two aggregation methods (per `ORCHESTRATOR.md §3Z`):

- **Sub-axis-weighted (load-bearing for §3Z convergence):**
  (8+11+8+8+9+22+5) / (8+12+9+8+9+22+5) = **71 / 73 = 97.3 %**.
- **Per-lens mean (informational; equal weight per lens):**
  (100 + 91.7 + 88.9 + 100 + 100 + 100 + 100) / 7 = **97.2 %**.

Both aggregates land **above** the §3Z ≥95 % floor (sub-axis 97.3 %
vs 95 %; per-lens 97.2 % vs 95 %). The V2 → V3 trajectory is **+2.5 pp
sub-axis** (V2 94.8 % → V3 97.3 %), achieved by closing CH7 from 66.7 %
to 100 % and clearing the V2 CH7 binding REVISE cluster. V3 is the
**first cohort-wide ≥95 % cycle** for T-P1. §3Z requires the second
consecutive ≥95 % cycle for cohort LOCK; V4 must hold the floor with
zero new orphan REVISEs.

### §0.3 REJECT roster

**Zero REJECT findings** across all 7 lenses on the V3 cycle (matches
V1 + V2 zero-REJECT posture). Both V3 REVISEs (CH2 1-cell + CH3 1-cell)
admit mechanical correction in a V4 fold; neither falsifies a
substantive inventory claim. Both are single-token edits.

### §0.4 REVISE roster (deduplicated across lenses)

Two REVISE-class findings (both single-token mechanical edits) +
three LIGHT non-blocking polish notes carried forward across lenses:

1. **F-V4-CH2-1** (CH2 single REVISE; 1C executive-summary 126→127
   propagation miss): The V3 fold corrected `1C-runtime-evidence.md`
   structural rows to "127 grammar-named type reexports" but the
   executive summary at `1C-runtime-evidence.md:40` retains the V2
   figure "126". Live mechanical re-extraction at V3 HEAD
   (`grep -c '^pub use' crates/core/src/runtime/mod.rs` over the
   25-71 window minus the 6 in-window neutrals) returns **127**
   exactly. Single-token orphan-cell propagation defect; verify with
   `rg -n '\b126\b' restart/audit/totality/p1/1C-runtime-evidence.md`
   returns empty before V4 commit (NEW-CH2-V3-02 orphan-cell
   propagation guard).

2. **F-V4-CH3-1** (CH3 single REVISE; 1D W13.9 reject-class
   precision-nit): `1D-skinny-lessons.md:140` reads "W13.5-W13.9 are
   MEASURED-REJECT at REDRESS.md:4621/4645/4674/4704/4734". Live
   `REDRESS.md:4734` reads `CORRECTNESS-REJECT`, not
   `MEASURED-REJECT`. Since `CORRECTNESS-REJECT > MEASURED-REJECT` in
   the regression-discipline ordering, the substance is preserved (W13.9
   IS rejected) but the precise verdict-class is mis-labelled. Repair
   text: "W13.5-W13.8 MEASURED-REJECT at :4621/4645/4674/4704; W13.9
   CORRECTNESS-REJECT at :4734".

3. **F-V4-CH1-1** (CH1 V3 non-blocking; AP-009 count refresh): The
   V3 AP-009 inline rebind note at `1F-anti-pattern.md:69` reads
   "grep returns 24 hits" while HEAD `grep -c 'lightningcss_facts'
   skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` returns 27 (root
   def at `:636` + 6 siblings + 20 call sites). Load-bearing claim
   (symbol exists, V2 dispatch's "zero hits" assertion overturned)
   holds; only the literal count is understated by 3. Cosmetic refresh.

4. **F-V4-CH6-1** (CH6 V3 carry-forward; 1E:33-35 sustained-UNKNOWN
   paragraph): The V2 → V3 carry from CH1 SC-V2-4 + CH6 1E `:33-35`
   asks for an executive-summary cross-reference paragraph listing
   L03 + L16 + the two NEW SK-V14 UNKNOWNs at
   `1E-locks-evidence.md:33-35`. CH6 V3 dispositions it as discharged
   per substantive Open Questions presence at `:157-162`; CH1 V3
   re-flags as cosmetic carry-forward only. V4 paragraph addition
   closes both CH1 and CH6 polish flags simultaneously.

5. **F-V4-CH5-1** (CH5 V3 housekeeping; 1D row 117 + 1A:84 + 1A
   frontmatter:10 sub-case enumeration refresh): Track 2 + CSS sidecar
   sub-case cites in 1D row 117 sub-case enumeration list pre-rebind
   cite lines (V2 numbering) verbatim. Cross-reference annotation
   update to V3 line anchors (post-rebind `:7,26,45` Track 2 + post-
   rebind `:648, :2691, :1082+` CSS sidecar). Cosmetic; substantive
   classifications hold at HEAD.

**No structural addition to T-P3 §3C required this cycle.** The
LAC-1E-12 procedural addendum (executable-verification-on-cite-carry)
already landed in V3 inventory text per CH7 §3.3 institutionalization
(three independent sites: `1A-substrate-evidence.md:10`,
`1F-anti-pattern.md:55`, `1F-anti-pattern.md:69`). V3 fold discharged
the structural addition itself; V4 carries the polish.

## §1 — Cross-lens convergence (final V3)

Three convergence patterns surface where two or more lenses
independently land on the same finding, ratifying the V3 fold's
structural fixes and pre-staging V4 + V5 confirming cycles:

### §1.1 — Stale cite cluster CASCADE — CH5 + CH6 + CH7 V2 REVISE fully discharged at V3

Three V2 lenses (CH5 §CH5-V2-008 ACCEPT-with-caveat; CH6 §New Finding
CH6-V2 surfaced-to-CH1; CH7 §3.1 binding REVISE) all landed on the
identical fabricated-cite cluster `nonjson_css_l4.rs:222,234,299,504`
carried verbatim from V1 CH5-004 into V2 1A-SUB-014 + 1F-anti-pattern
AP-020 + 1F-anti-pattern AP-009. The V3 fold packet (F-V3-CH7-1 +
F-V3-CH7-2 per `HARDENING-T-P1-V2-CONSOLIDATED.md §3.1-§3.2`)
rebound all five string sites (1F `:55, :80, :105` + 1A `:10, :67`)
to the executable-verified set `:648, :1082, :1203, :1354, :1511,
:1661, :1815, :1964, :2691`. CH7 V3 §2.1 HEAD grep returns exactly
those nine line numbers; AP-009 `lightningcss_facts` rebind at `:636`
(definition) + 6 siblings + 20 call sites verified at V3 HEAD.
Three-lens convergence on **fully discharged** at V3:

- CH5 V3 88.9 % → 100 % (caveat closed; CH5-V3-008 residual is row-117
  sub-case enumeration housekeeping, not the V2 cite cluster).
- CH6 V3 100 % sustained (V2 surfaced-to-CH1 routing was correct; CH1
  V3 carry-forward discharged at protocol level).
- CH7 V3 66.7 % → 100 % (binding REVISE fully discharged; meta-CH7
  self-correction landed in inventory text).

Per V2 §1.1 cascade prediction, this was the highest-priority V3 fold;
delivery confirmed.

### §1.2 — CH7-V2-failure-mode RECURSIVE SELF-CORRECTION (strongest anti-fabrication structural win)

The V3 dispatch context (`hardening/V3/CHALLENGE-CONTEXT.md` §1)
inherited a CH7-class error from V2: it asserted that
`lightningcss_facts` had "zero hits" at HEAD and used that assertion
as the binding basis for the V2 AP-009 REVISE. HEAD verification at
V3 returns **27 hits** (root def at `:636` + 6 sub-grammar siblings +
20 call sites). The V3 CH7 fold caught this **in the dispatch context
itself** and self-corrected via two mechanisms:

1. **Inline rebind note at `1F-anti-pattern.md:69`** explicitly names
   the V2 dispatch error: "CH7 V2 dispatch assertion `lightningcss_facts
   has zero hits` was itself off — grep returns 24 hits". (Per CH7 V3
   §3.5: count understated by 3 vs HEAD's 27; load-bearing claim
   unaffected.)
2. **Protocol-level fold via CH6 V3** — CH6 V3 NEW finding institutes
   the LAC-1E-12 procedural addendum (executable-verification-on-cite-
   carry) at THREE independent inventory sites
   (`1A-substrate-evidence.md:10`, `1F-anti-pattern.md:55`,
   `1F-anti-pattern.md:69`), cross-binding the cite-rebind discipline
   to the LAC-1E-12 governance candidate at `1E:120 + §1.5 :126-128`.

This is the **strongest single anti-fabrication structural win of the
campaign**: the V2 failure mode was V→V+1 cite-carry without
re-verification, and V3 closed the loop not just by rebinding the
cites but by institutionalizing the verify-at-HEAD discipline IN THE
INVENTORY TEXT ITSELF, so any V4+ cite-bearing micro-fold inherits
the mandate mechanically. Recursive empirical validation: AP-009 V3
self-correction caught the V2 dispatch error, and 1C 126→127 V3 repair
caught the V2 arithmetic error — both via the institutionalized
discipline, not author vigilance. Two cross-lens cascades (CH7 § dispatch
self-correction + CH2 § orphan-cell repair) confirm the discipline
operates at the mechanical level.

### §1.3 — CH4 cost-neutrality discipline (NEW V3 finding) — load-bearing for all cite-rebind classes

CH4 V3 surfaces a NEW invariant: **every cite-rebind in the V3 fold
packet (F-V3-CH7-1, F-V3-CH7-2, F-V3-CH5-1, F-V3-CH1-1) is
cost-neutral by construction.** Verified at V3 commit:

- Zero LOC-band rescale required (line-count deltas: 1A +4, 1C +13, 1D
  +4, 1F-anti-pattern +14 = +35 lines total; well within V2 1.2-1.4×
  hard-cap multiplier on the 240 LOC cap; per CH4 V3 §5).
- Zero hard-cap-multiplier disturbance (no V3 fold packet exceeded the
  V2 LOC budget by more than 0.15×).
- Zero wave-alignment perturbation (every V3 rebind targets cites
  inside an already-cost-bound row; no new wave routing introduced).

Cost-neutrality is now a **load-bearing CH4 invariant for the cite-
rebind class**: any future cite-rebind that DOES require LOC rescale
or wave perturbation must surface as a CH4 REVISE, not pass through as
mechanical correction. V4 + V5 cycles inherit this discipline. CH4
3-cycle LOCK ratifies the discipline at the §3Z gate level.

## §2 — V3 strengthening packet (what V3 fold discharged)

The V3 micro-fold (commit `0a9f1288c`; 4 inventories amended in one
atomic pass over V2 baseline `87816a2cd`) discharged the V2 CONSOLIDATED
§3 fold packet across all 5 fold groups, moving 2 lenses (CH2 → 91.7 %
of a moving target, CH7 → 100 %) into the ≥95 % zone and confirming
4 lenses (CH1 + CH4 + CH5 + CH6) into 2-cycle LOCKS:

### §2.1 — CH1 CORRECTNESS: V2 100 % → V3 100 % (2-cycle LOCK V2+V3)

All five V2 fold targets discharged at HEAD `0a9f1288c`:
- **F-V3-CH7-1 (AP-020 + 1A-SUB-014 cite rebind)**: 5 sites verified
  bit-for-bit at HEAD (1F `:55, :80, :105` + 1A `:10, :67`).
- **F-V3-CH7-2 (AP-009 `lightningcss_facts` rebind + V2 dispatch
  self-correction)**: V3 inline rebind note at `1F-anti-pattern.md:69`
  carries the verify command + count (24, understated by 3 vs HEAD's
  27; non-blocking V4 polish per §0.4 F-V4-CH1-1).
- **F-V3-CH5-1 (Track 2 + proof-witness cite refresh)**: Track 2
  `:5,24,43` → `:7,26,45` at `1D-skinny-lessons.md:157`; proof-witness
  `:9` → `:29-33` at `1D:117`; all three sites verified at HEAD.
- **F-V3-CH1-1 (1D row 100→117 cosmetic)**: `1D-skinny-lessons.md:157`
  carries "row 117 substrate-union ratification" at V3 HEAD.
- **F-V3-CH2-1 (1C reexport 126→127)**: structural rows + per-grammar
  breakdown all read 127 at V3 HEAD; orphan executive-summary cell at
  `:40` is the single residual V4 polish item (F-V4-CH2-1).

CH1 V3 trajectory: 2-cycle LOCK satisfied at V3 commit (V2 + V3 both
100 %). V4 confirming cycle: re-verify the 5 V3 cite-rebinds remain
HEAD-anchored after V4 fold + verify F-V4-CH1-1 count refresh + F-V4-
CH6-1 paragraph addition land without new cite drift. CH1 LOCK
extension to 3-cycle expected at V4.

### §2.2 — CH2 GENERALITY: V2 91.7 % → V3 91.7 % (single residual orphan cell at 1C:40)

V2 single REVISE (1C reexport count 126→127) substantively discharged:
- Structural rows at `1C-runtime-evidence.md:91, :123, :124, :200` all
  read "127 grammar-named symbols" with per-grammar breakdown bbnf 10,
  bnf 10, css_l4 **43**, css_pretty 10, csv 10, ebnf 10, google_sheets
  **11**, json 13, math 10 (sum 127 verified by V3 CH2 §2.2).
- 6 in-window + 4 out-window neutrals enumerated path:line per
  NEW-CH2-V2-03 discipline (in-window: `CompoundHandle, DtaError,
  GenericAtRule, ParseErr, StringHandle, StructBuilder` at `mod.rs:25-71`;
  out-window: `IntoPathSegment, Path, PathSegment` at `:72`,
  `RuntimeView` at `:76`).
- css_l4 41→43 + google_sheets 12→11 secondary corrections caught
  beyond named scope per V3 dispatch §1 (1C amended scope expanded).

**Single residual orphan cell**: executive summary at
`1C-runtime-evidence.md:40` retains V2's "126 grammar-named type
reexports". F-V4-CH2-1 single-token mechanical repair; verify with
`rg -n '\b126\b' restart/audit/totality/p1/1C-runtime-evidence.md`
returns empty before V4 commit. CH2 V4 trajectory: 100 % expected
after F-V4-CH2-1 + first ≥95 % cycle achieved.

### §2.3 — CH3 REGRESSION: V2 100 % → V3 88.9 % (LOW precision-nit on W13.9 reject-class)

Eight of nine V2 ACCEPT findings sustain at V3:
- REDRESS PASS-ADMIT cardinality (5 typed + 2 direct + 5 parse_only +
  24 CSS L4 = 36) byte-verified at `REDRESS.md:4293/4377/4463/4504/
  4542/4581/4995` (ADMIT) + `:4622/4646/4675/4705` (MEASURED-REJECT).
- 1D frontmatter at `:53-75` carries explicit V2-fold comment naming
  both divergence-table axes + spec-claim-table axes distinct.
- W13.5-W13.8 MEASURED-REJECT verified at `:4621/4645/4674/4704`.

**Single LOW precision-nit**: `1D-skinny-lessons.md:140` reads
"W13.5-W13.9 are MEASURED-REJECT" but `REDRESS.md:4734` reads
`CORRECTNESS-REJECT` not `MEASURED-REJECT` for W13.9. Substantive
regression-discipline-correct (W13.9 IS rejected; `CORRECTNESS-REJECT
> MEASURED-REJECT` ordering preserved); only reject-class label needs
splitting. F-V4-CH3-1 single-token mechanical fix; verify with
`grep -n 'CORRECTNESS-REJECT' restart/audit/totality/REDRESS.md`
returns `:4734` exactly. CH3 V4 trajectory: 100 % expected after
F-V4-CH3-1 + first ≥95 % cycle achieved.

### §2.4 — CH4 COST: V1 + V2 + V3 = per-lens 3-cycle LOCK SATISFIED

CH4 alone among the seven lenses crosses **3-cycle LOCK** at V3 commit
— V1 100 % + V2 100 % + V3 100 %. V3 NEW finding: cost-neutrality
discipline for cite-rebind class (§1.3). All four V3 fold packets
(F-V3-CH7-1/CH7-2/CH5-1/CH1-1) verified cost-neutral:
- Line-count deltas: 1A +4, 1C +13, 1D +4, 1F-anti-pattern +14 = +35
  total; well inside V2 240 LOC hard cap × 1.4× multiplier.
- Zero new wave routing.
- Zero new same-wave-consumer obligation.

CH4 standalone-closed at V3; no V4 CH4 dispatch required. T-P3 §3C
carry-forward governance items unchanged from V2: (a) adopt LAC-1E-15
per-tranche framing as load-bearing Pattern H budget; (b) codify
1.2-1.4× hard-cap multiplier convention in Lock 8 V+1 wording; (c)
**NEW V3**: codify cite-rebind cost-neutrality invariant alongside.

### §2.5 — CH5 HIDDEN COUPLING: V2 100 % → V3 100 % (2-cycle LOCK V2+V3)

V2 cite-staleness caveat (CH5-V2-008) fully discharged:
- Track 2 `bbnf-bench/src/track2/json.rs:5,24,43` → `:7,26,45` at
  V3 `1D:157` + `1F-anti-pattern.md:71` (live verified at HEAD).
- Proof-witness `runtime/src/lib.rs:9` → `:29-33` at V3 `1D:117`
  (cfg-gated `pub mod json_event_grammar_witness` + `pub mod
  sheets_witness` confirmed at HEAD).
- AP-011 rebind preserves substrate-coupling classification.

**CH5-V3-008 residual housekeeping**: 1D row 117 sub-case enumeration
carries pre-rebind cite list verbatim (cross-reference annotation
gap, not classification gap). F-V4-CH5-1 cosmetic refresh; CH5 V4
trajectory: 100 % expected sustained → 3-cycle LOCK at V4.

### §2.6 — CH6 ANTI-PAPER-CLOSE: V2 100 % → V3 100 % (2-cycle LOCK V2+V3)

All 19 V2 ACCEPT findings sustain at V3, plus 3 NEW V3 findings:
- **LAC-1E-12 procedural addendum institutionalization** (§1.2): three
  independent V3 inventory sites carry the verify-at-HEAD directive
  bound to the LAC-1E-12 governance candidate.
- **NEW-CH2-V2-03 enumeration discipline**: V3 1C fold enumerates 6
  in-window + 4 out-window neutrals path:line, satisfying the
  generalized "K-neutrals enumeration" anti-paper-close discipline.
- **NEW-CH2-V3-02 orphan-cell propagation guard**: residual 1C:40
  126→127 miss surfaces a NEW V3 sub-finding that any "N grammar-named
  X" fold must `rg -n` verify the old figure returns empty before
  commit; promote to V4 fold discipline.

CH6 V3 → CH6 V4 trajectory: 100 % sustained expected; 2-cycle LOCK
extension to 3-cycle at V4. The only non-CH6 surface item is the
1E:33-35 sustained-UNKNOWN paragraph (CH1 + CH6 cross-flag; F-V4-CH6-1
discharges both simultaneously).

### §2.7 — CH7 OVERFIT-PRUNE: V2 66.7 % → V3 100 % (first ≥95 % cycle; cleanest convergence of the cohort)

The deepest-regressed V2 lens (V1 77.8 % → V2 66.7 %) cleanly converges
to 100 % at V3. Both V2 binding REVISE items fully discharged plus the
meta-CH7 dispatch-context self-correction landed:

- **F-V3-CH7-1 (AP-020 + 1A-SUB-014 cite rebind)**: 9 cite lines
  rebound at 5 string sites; HEAD verification §2.1 returns exactly
  the cited line numbers.
- **F-V3-CH7-2 (AP-009 `lightningcss_facts` rebind)**: symbol confirmed
  at `:636` definition + 6 siblings at `:656/:681/:708/:723/:739/:755`
  + 20 call sites. V2 dispatch "zero hits" error explicitly named and
  overturned in inline rebind note at `1F-anti-pattern.md:69`.
- **Self-test (V3 introduces no new fabrication)**: every V3-added
  cite HEAD-verified before commit; three V3 sites carry inline
  verify-at-HEAD command (1A `:10`, 1F-anti-pattern `:55, :69`).
- **Zero drift on V2-LOCKED axes**: 1B + 1E + 1F-coherence + 1F-past
  byte-identical to V2 `87816a2cd` (CH7 §2.4 git diff confirmation).
- **Executable-verification mandate institutionalized**: LAC-1E-12
  procedural addendum bound to inventory text at three sites (§1.2).

**Single V3 residual numerical imprecision (non-blocking)**: AP-009
inline note count "24 hits" understates HEAD's 27 by 3. Per CH7 V3
§3.8 not REVISE-routed (load-bearing claim on symbol existence holds;
count is annotation only). F-V4-CH1-1 cosmetic refresh.

CH7 V4 trajectory: 100 % sustained expected; first ≥95 % at V3,
second consecutive ≥95 % at V4 → CH7 standalone LOCK at V4. Cohort
LOCK turn on V5 confirming for CH2 + CH3.

## §3 — V4 fold packet (5 LIGHT items, 4 inventories touched)

Five V4 fold packets prescribed below. All five are **LIGHT** single-
line edits (one paragraph addition for F-V4-CH6-1; four single-token
substitutions otherwise). Total V4 fold surface ~15 min wall across 5
LIGHT packets. No HEAVY or MEDIUM wave-program work surfaces this
cycle; the V3 cite-rebind cluster fully cleared the cite-class load.

### §3.1 — F-V4-CH2-1 — 1C executive-summary 126→127 (LIGHT)

**Closes:** CH2 single residual REVISE (orphan executive-summary cell
at `1C-runtime-evidence.md:40`).

**Scope:** Replace at `1C-runtime-evidence.md:40` "126 grammar-named
type reexports" with "127 grammar-named type reexports" (single-token
mechanical substitution). The structural rows + per-grammar breakdown
+ V3 dispatch text all already read 127; the executive summary is the
single propagation miss.

**Verification before V4 commit:** `rg -n '\b126\b'
restart/audit/totality/p1/1C-runtime-evidence.md` returns empty
(NEW-CH2-V3-02 orphan-cell propagation guard discipline). If any
hit remains, repeat scan until empty.

**Cost:** ≈ 2 min wall (LOW). Single cell update.

**Convergence impact:** CH2 11/12 → 12/12 (100 %); first ≥95 % cycle
achieved at V4.

### §3.2 — F-V4-CH3-1 — 1D W13.9 reject-class split (LIGHT)

**Closes:** CH3 single residual REVISE (1D row 140 reject-class
precision-nit).

**Scope:** Replace at `1D-skinny-lessons.md:140` "W13.5-W13.9 are
MEASURED-REJECT at REDRESS.md:4621/4645/4674/4704/4734" with
"W13.5-W13.8 MEASURED-REJECT at :4621/4645/4674/4704; W13.9
CORRECTNESS-REJECT at :4734".

**Verification before V4 commit:** `grep -n 'CORRECTNESS-REJECT'
restart/audit/totality/REDRESS.md` returns `:4734` exactly;
`grep -n 'W13\.9' restart/audit/totality/REDRESS.md:4730,4740`
confirms reject-class line content. If MEASURED-REJECT also appears
at `:4734`, escalate to CH3 V4 disposition rather than mechanical
fold.

**Cost:** ≈ 3 min wall (LOW). Single cell, one cite split.

**Convergence impact:** CH3 8/9 → 9/9 (100 %); first ≥95 % cycle
achieved at V4.

### §3.3 — F-V4-CH1-1 — 1F-anti-pattern AP-009 count refresh (LIGHT)

**Closes:** CH1 V3 non-blocking cosmetic flag (AP-009 inline rebind
note count understatement).

**Scope:** Replace at `1F-anti-pattern.md` AP-009 inline rebind note
"grep returns 24 hits" with "grep returns 27 hits" (count refresh to
match HEAD `grep -c 'lightningcss_facts'
skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` = 27).

**Verification before V4 commit:** `grep -c 'lightningcss_facts'
skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` returns 27; cite
substitution preserves "grep returns N hits" structure.

**Cost:** ≈ 2 min wall (LOW). Single token correction.

**Convergence impact:** CH1 cosmetic clean-up; no per-lens rate change.

### §3.4 — F-V4-CH6-1 — 1E:33-35 sustained-UNKNOWN paragraph (LIGHT)

**Closes:** CH1 V3 carry-forward (1E:33-35 sustained-UNKNOWN
paragraph) + CH6 V3 cross-reference flag.

**Scope:** Add to `1E-locks-evidence.md:33-35` an executive-summary
paragraph listing the four sustained-UNKNOWN routes:
- L03 (sustained UNKNOWN; route → V+1 SK-V14 audit-overlay disposition)
- L16 (sustained UNKNOWN; route → V+1 SK-V14 audit-overlay disposition)
- NEW SK-V14 UNKNOWN #1 (Lock 1 fact-stream taxonomy; route → T-P3 §3C)
- NEW SK-V14 UNKNOWN #2 (audit-overlay gap; route → T-P3 §3C)

Cross-reference to the Open Questions table at `1E:157-162` + §1.5
LAC-1E-12 promotion candidacy block at `:126-128`. The substantive
material exists at `:157-162` per V2 CH6 ACCEPT; F-V4-CH6-1 adds the
executive-summary anchor for CH1 cosmetic close.

**Verification before V4 commit:** `grep -n 'L03\|L16\|sustained-UNKNOWN'
restart/audit/totality/p1/1E-locks-evidence.md` returns hits at both
`:33-35` (new paragraph) and `:157-162` (Open Questions table).

**Cost:** ≈ 5 min wall (LOW). Single paragraph addition.

**Convergence impact:** CH1 + CH6 polish flags both closed; no
per-lens rate change.

### §3.5 — F-V4-CH5-1 — 1D row 117 + 1A:84 + 1A frontmatter:10 sub-case cite refresh (LIGHT)

**Closes:** CH5 V3 housekeeping (sub-case enumeration cross-reference
annotation refresh).

**Scope:** Three single-line edits:
- `1D-skinny-lessons.md:117` sub-case enumeration: update Track 2 cite
  references from V2 `:5, :24, :43` to V3 `:7, :26, :45` (post-rebind
  HEAD anchors).
- `1A-substrate-evidence.md:84` sub-case: update CSS sidecar cite
  references to V3 `:648, :2691, :1082+` (post-rebind HEAD anchors).
- `1A-substrate-evidence.md:10` frontmatter cross-cite: update line-
  anchor cross-cites from "row 100" → "row 117" (V2 numbering → V3
  numbering propagation; mirrors F-V3-CH1-1 at `1D:157`).

**Verification before V4 commit:** `grep -n ':5,24,43'
restart/audit/totality/p1/1D-skinny-lessons.md` returns empty;
`grep -n ':222,234,299,504'
restart/audit/totality/p1/1A-substrate-evidence.md` returns empty;
`grep -n 'row 100'
restart/audit/totality/p1/1A-substrate-evidence.md` returns empty.

**Cost:** ≈ 3 min wall (LOW). Three cite-annotation updates.

**Convergence impact:** CH5 cross-reference annotation clean-up; CH5
2-cycle LOCK extension to 3-cycle at V4.

### §3.6 — V4 fold discipline: NEW-CH2-V3-02 orphan-cell propagation guard (PROCEDURAL)

**Closes:** Structural class-defect surfaced by V3 CH2 91.7 % residual
(executive summary still reading 126 while structural rows read 127).

**Scope (V4 fold-time discipline, not inventory edit):** Any V4 fold
that changes a numerical figure in inventory text MUST be followed
immediately by a `rg -n '\b<old-figure>\b'` scan over the amended file
+ all cross-cited files. If the scan returns any hit, the fold is
incomplete and the discipline requires repeating until the scan
returns empty. Adopt as V4 dispatch-context discipline rule.

**Cost:** Zero LOC at T-P1 (discipline carried as V4 dispatch-context
rule); commits to T-P3 §3C as governance addendum to LAC-1E-12 (zero-
to-light LOC at T-P3).

**Convergence impact:** Prevents recurrence of the V3 propagation
defect class in V4+; structural fix bound to the V3-institutionalized
executable-verification mandate.

## §4 — V4 dispatch shape

### §4.1 — Inventories requiring V4 micro-fold

Three of eight inventories require V4 fold edits (down from V3's
four-inventory amend scope):

| Inventory | V3 lens findings | V4 fold packets | Edit count | Est. wall |
|---|---|---|---:|---:|
| 1A-substrate-evidence.md | CH5 sub-case enumeration cross-ref refresh + line-anchor "row 100"→"row 117" carry | F-V4-CH5-1 (2 cells at :10, :84) | 2 cells | ~2 min |
| 1C-runtime-evidence.md | CH2 single residual orphan cell (exec summary 126→127) | F-V4-CH2-1 (1 cell at :40) | 1 cell | ~2 min |
| 1D-skinny-lessons.md | CH3 reject-class precision-nit (W13.9 split) + CH5 sub-case Track 2 cite refresh | F-V4-CH3-1 (1 cell at :140) + F-V4-CH5-1 (1 cell at :117 sub-case enum) | 2 cells | ~3 min |
| 1E-locks-evidence.md | CH1 + CH6 sustained-UNKNOWN executive-summary paragraph | F-V4-CH6-1 (1 paragraph at :33-35) | 1 paragraph | ~5 min |
| 1F-anti-pattern.md | CH1 AP-009 count refresh 24→27 | F-V4-CH1-1 (1 cell at AP-009 rebind note) | 1 cell | ~2 min |

**Four inventories pass V4-unchanged**: 1B (V2-LOCKED through V3;
CH7 ACCEPT verified zero-drift at V3 §2.4), 1F-coherence-scan (V2-
LOCKED through V3), 1F-past-corpora (V2-LOCKED through V3; PC-017
google_sheets=10 + PC-008 anchor intact). (Note: 1E gets a paragraph
addition for F-V4-CH6-1 — it is V2-LOCKED only with respect to
structural content; the F-V4-CH6-1 paragraph is the inventory's
single V4 edit.)

**No HEAVY or MEDIUM wave-program work surfaces.** Total V4 fold
surface: ~15 min wall across 5 LIGHT packets distributed over 4
inventories (1A, 1C, 1D, 1E, 1F-anti-pattern). All packets are
single-line mechanical edits except F-V4-CH6-1 (one paragraph
addition).

### §4.2 — V4 dispatch recommendation

Dispatch a single T-P1 V4 micro-fold packet to the four inventories
requiring V4 edits in one parallel agent fan-out (per V4 inventory;
~2-5 min per agent). Each agent receives the inventory's specific
fold-packet subset from §4.1 plus the cross-lens convergence context
from §1 (stale cite cluster fully discharged; CH7-V2-failure-mode
recursive self-correction landed; cost-neutrality invariant
established).

After V4 micro-fold commit, the V4 aggregator dispatches a V4
CHALLENGE cycle over the seven lenses, expecting all seven lenses to
hold ≥95 % per the §3 fold packet sizing (CH4 at 3-cycle LOCK already
satisfied; CH1/CH5/CH6 expected to extend 2-cycle LOCKS to 3-cycle;
CH7 expected second consecutive ≥95 % cycle; CH2/CH3 expected first
≥95 % cycle).

### §4.3 — V4 CHALLENGE-CONTEXT discipline carry-forward

V4 dispatch context must extend the V3 institutionalized
executable-verification mandate with the NEW-CH2-V3-02 orphan-cell
propagation guard (per §3.6): any V4 fold that changes a numerical
figure must be followed by a `rg -n '\b<old-figure>\b'` empty-scan
verification. This closes the V3 propagation-defect loophole at the
dispatch contract level pending T-P3 §3C governance disposition.

The three V3-institutionalized inline verification phrases
(`1A-substrate-evidence.md:10`, `1F-anti-pattern.md:55, :69`) become
the V4 baseline; any V4 cite-bearing prose must carry equivalent
verify-at-HEAD command.

## §5 — §3Z gate evaluation

### §5.1 — V3 cycle gate state

Per `ORCHESTRATOR.md §3Z` (≥95 % × 2 cycles, zero orphan REVISEs):

- V3 is the **third** cycle (V1 = 68.6 % sub-axis, V2 = 94.8 % sub-axis,
  V3 = 97.3 % sub-axis).
- Sub-axis aggregate **97.3 %** (71/73) **above** floor; per-lens
  mean **97.2 %** (681.6/7) above floor. Both aggregation methods
  clear ≥95 %.
- **2 orphan REVISEs** across 2 lenses (CH2: 1 single-token at 1C:40;
  CH3: 1 single-token at 1D:140); CH1/CH4/CH5/CH6/CH7 all discharge
  ACCEPT.
- **4 lenses (CH1+CH4+CH5+CH6)** at 2-cycle LOCK satisfied at V3 (V2+V3
  both ≥95 %); CH4 alone at 3-cycle LOCK (V1+V2+V3 all 100 %).
- **1 lens (CH7)** at first ≥95 % cycle on V3 (66.7 % → 100 %; needs
  V4 confirming).
- **2 lenses (CH2 + CH3)** at first ≥95 % cycle blocked by single-cell
  REVISE; V4 mechanical fold lifts both to first ≥95 %.

**Cycle verdict: COHORT NOT-YET-LOCKED — V4 + V5 REQUIRED.** V3 cleared
the ≥95 % floor at the cohort level but the cohort-wide "× 2
consecutive cycles" sub-clause cannot close at V3 because two lenses
(CH2 + CH3) carry single-cell orphan REVISEs that prevent counting V3
as the first ≥95 % cohort cycle for §3Z LOCK chain entry. V4 must (a)
clear the 2 orphan REVISEs (CH2-1 + CH3-1) AND (b) re-confirm CH7 +
all 2-cycle LOCKS hold ≥95 %; V5 then provides the second consecutive
cohort-wide ≥95 % cycle for §3Z LOCK close.

### §5.2 — V4 forecast (light-fold-only)

With the five light packets (§3.1-§3.5, ~15 min wall total):

| Lens | V1 rate | V2 rate | V3 rate | Expected V4 rate (light-only) | Net |
|---|---:|---:|---:|---:|---|
| CH1 | 62.5 % | 100 % | 100 % | 100 % (8/8) | F-V4-CH1-1 + F-V4-CH6-1 close cosmetic carry-forward; 3-cycle LOCK extension |
| CH2 | 60 % | 91.7 % | 91.7 % | 100 % (12/12) | F-V4-CH2-1 closes single residual orphan cell; first ≥95 % cycle achieved |
| CH3 | 66.7 % | 100 % | 88.9 % | 100 % (9/9) | F-V4-CH3-1 closes single LOW precision-nit; first ≥95 % cycle achieved |
| CH4 | 100 % | 100 % | 100 % | 100 % (8/8) | No V4 work; 3-cycle LOCK extension to 4-cycle |
| CH5 | 42.9 % | 100 % | 100 % | 100 % (9/9) | F-V4-CH5-1 closes cross-ref annotation housekeeping; 3-cycle LOCK extension |
| CH6 | 68.4 % | 100 % | 100 % | 100 % (22/22) | F-V4-CH6-1 paragraph addition; 3-cycle LOCK extension |
| CH7 | 77.8 % | 66.7 % | 100 % | 100 % (5/5 dispatch; 9/9 full inv) | No V4 work; second consecutive ≥95 % cycle → CH7 standalone LOCK |

**Expected sub-axis-weighted V4 aggregate:** ≈ 100 % (73/73 if every
fold packet lands cleanly; conservative 98-100 % allowing for any V4
fold introducing one new minor finding).

**Expected per-lens mean V4:** ≈ 100 %.

**V4 outcome under light-fold-only:** all 7 lenses ≥95 %; zero orphan
REVISEs; **first cohort-wide LOCK-eligible ≥95 % cycle achieved at
V4** (V3 was first ≥95 % cohort-wide but with 2 orphan REVISEs
preventing LOCK chain entry). V5 then provides the second consecutive
cohort-wide ≥95 % cycle for §3Z LOCK close.

### §5.3 — V5 forecast and predicted §3Z LOCK close path

Under V4 light-fold-only:

- V5 inherits V4's ≈ 100 % sub-axis aggregate + 100 % per-lens ACCEPT
  on all seven lenses.
- V5 work surface: re-verify V4 light-fold edits land cleanly at new
  HEAD; surface any new cite drift introduced by V4 textual edits;
  reconfirm the ten executable verifications from CH7 §2.1-§2.6 still
  PASS at V5 HEAD; verify NEW-CH2-V3-02 orphan-cell propagation guard
  scan returns empty for every V4-touched numerical figure.
- V5 expected outcome: ≥95 % on both aggregation methods with zero
  new orphan REVISEs. **§3Z cohort LOCK on the second consecutive
  ≥95 % cycle.** T-P3 §3C dispatch gate opens per
  `PASS-1-EXCAVATION.md §5`.

**Predicted §3Z LOCK close path: V3 (first ≥95 % cohort-wide cycle
with 2 single-cell orphan REVISEs) → V4 (orphan REVISEs cleared +
first LOCK-eligible cohort-wide ≥95 %) → V5 (confirming verification
re-pass + second consecutive ≥95 % cycle) → cohort LOCK at V5
ceiling.** V≤5 ceiling honoured exactly; one V4 surprise (any new
orphan REVISE) would push to V6 and BLOCKED escalation per §3Z V≤5
ceiling rule.

**V≤5 ceiling trajectory feasibility**: tight margin. V4 has 5 LIGHT
fold packets across 4 inventories, all single-token / single-paragraph
mechanical edits; risk of new REVISE introduction is LOW but
non-zero. NEW-CH2-V3-02 orphan-cell propagation guard discipline
(§3.6) is the proximate mitigation; pre-commit `rg -n` verification
on every V4 numerical-figure change is mandatory.

### §5.4 — §3Z LOCK criteria for V4+V5 chain

Per `ORCHESTRATOR.md §3Z`, V4+V5 cohort LOCK criteria are:

1. **V4: Sub-axis aggregate ≥95 %** on the post-V4 lens dispositions
   (V3 baseline 97.3 % → V4 target ≥95 %; expected 100 % per §5.2).
2. **V4: Zero orphan REVISEs.** The 2 V3 REVISEs (CH2-1 + CH3-1) both
   resolve to ACCEPT via the §3 fold packet; F-V4-CH1-1 + F-V4-CH5-1
   + F-V4-CH6-1 close non-blocking carry-forward.
3. **V4: No new orphan REVISEs surfaced by V4 work.** V4's edits to
   the four amended inventories must not introduce new cite drift,
   new tally inconsistency, or new closure-wording-without-evidence.
   NEW-CH2-V3-02 orphan-cell propagation guard discipline (§4.3) is
   the mechanical prevention; CH7 V3 §3.2 self-test re-runs cleanly
   at V4 commit.
4. **V5: Confirming ≥95 % cycle** with all 7 lenses re-ACCEPT and
   zero new REVISEs.

**Predicted V4 lens ACCEPT-rate vector for first LOCK-eligible cohort
cycle:** CH1 100, CH2 100, CH3 100, CH4 100 (3-cycle LOCK already
satisfied), CH5 100, CH6 100, CH7 100. Sub-axis-weighted aggregate
~100 %; per-lens mean 100 %.

**Likely V4 → V5 → LOCK trajectory:** V4 closes 2 REVISEs + 3
non-blocking carry-forwards in ~15 min wall + one CHALLENGE V4
dispatch cycle (~30-60 min lens dispatch + ~30 min aggregator); V5
re-runs the seven CHALLENGE V4 lenses against the V4-folded artefacts
(~30-60 min wall lens dispatch + ~30 min aggregator); V5 ratifies the
second consecutive ≥95 % cycle and discharges §3Z cohort LOCK at V5
ceiling. T-P3 §3C dispatch context can be staged in parallel with V5
to land immediately on V5 LOCK.

## §6 — T-P3 §3C carry-forward

Three structural items carry to T-P3 §3C for governance disposition,
all surfaced or strengthened by V3 cycle:

1. **LAC-1E-12 procedural addendum (institutionalize executable-
   verification on cite-carry)**: V3 institutionalized at three
   inventory sites (§1.2). T-P3 §3C disposes whether the addendum
   lands as Lock 17/Lock 18 numbering, in-preface CH7-binding clause,
   or LAC-1E-12 expansion. Cost envelope: 20-60 LOC docs within V2
   `60-180 LOC docs / low risk / 240 LOC hard cap` frame (CH4 V3 §5).

2. **NEW-CH2-V2-03 enumeration discipline (K-neutrals path:line
   inside cited window)**: V2 surfaced + V3 ratified by 1C in-window/
   out-window enumeration. T-P3 §3C disposes as generalised
   anti-paper-close discipline for any "N grammar-named X subtract-
   from-K" cite. Cost envelope: 10-30 LOC docs.

3. **NEW-CH2-V3-02 orphan-cell propagation guard (`rg -n '\b<old-
   figure>\b'` empty-scan before V+1 commit)**: V3 surfaced by 1C:40
   126→127 propagation miss. T-P3 §3C disposes as V4+ dispatch-
   context discipline rule + LAC-1E-12 procedural addendum extension.
   Cost envelope: 5-15 LOC docs.

All three items are PROPOSALS at T-P1 V3 (institutionalized in V3
inventory text where applicable); T-P3 disposes governance carrier
per CH6 §1.5 rule (T-P1 proposes; T-P3 disposes; Pass Omega merges).

## §7 — Sources

V3 lens dispositions (all verified existing at write-time):
- `restart/audit/totality/p1/hardening/V3/CH1.md` (418 lines)
- `restart/audit/totality/p1/hardening/V3/CH2.md` (114 lines)
- `restart/audit/totality/p1/hardening/V3/CH3.md` (107 lines)
- `restart/audit/totality/p1/hardening/V3/CH4.md` (202 lines)
- `restart/audit/totality/p1/hardening/V3/CH5.md` (86 lines)
- `restart/audit/totality/p1/hardening/V3/CH6.md` (244 lines)
- `restart/audit/totality/p1/hardening/V3/CH7.md` (299 lines)
- `restart/audit/totality/p1/hardening/V3/CHALLENGE-CONTEXT.md` (40 lines)

V3 T-P1 inventory artefacts under review (HEAD `0a9f1288c`):
- `restart/audit/totality/p1/1A-substrate-evidence.md` (113 lines; +4
  from V2; F-V3-CH7-1 cite rebind at `:10, :67`)
- `restart/audit/totality/p1/1B-codegen-evidence.md` (116 lines;
  V2-LOCKED; zero V3 diff)
- `restart/audit/totality/p1/1C-runtime-evidence.md` (206 lines; +13
  from V2; F-V3-CH2-1 reexport 126→127 + css_l4 41→43 + google_sheets
  12→11; in-window + out-window neutrals enumerated path:line)
- `restart/audit/totality/p1/1D-skinny-lessons.md` (182 lines; +4 from
  V2; F-V3-CH5-1 proof-witness + Track 2 rebind; F-V3-CH1-1 row
  100→117 cosmetic)
- `restart/audit/totality/p1/1E-locks-evidence.md` (166 lines; V2-LOCKED;
  zero V3 diff; F-V4-CH6-1 paragraph addition pending)
- `restart/audit/totality/p1/1F-coherence-scan.md` (127 lines; V2-LOCKED;
  zero V3 diff)
- `restart/audit/totality/p1/1F-anti-pattern.md` (123 lines; +14 from
  V2; F-V3-CH7-1 AP-020 rebind at `:55, :80, :105` + F-V3-CH7-2 AP-009
  rebind at `:69, :94` + F-V3-CH5-1 AP-011 Track 2 rebind at `:71`)
- `restart/audit/totality/p1/1F-past-corpora.md` (159 lines; V2-LOCKED;
  zero V3 diff)

V2 baseline aggregator:
- `restart/audit/totality/p1/hardening/HARDENING-T-P1-V2-CONSOLIDATED.md`
  (818 lines; V2 ACCEPT-rate 94.8 % sub-axis / 94.0 % per-lens; 4
  REVISE findings clustered into 5 fold-class groups; V3 fold packet
  §3.1-§3.5 discharged by V3 atomic micro-fold at HEAD `0a9f1288c`)

V1 baseline aggregator:
- `restart/audit/totality/p1/hardening/HARDENING-T-P1-V1-CONSOLIDATED.md`
  (V1 ACCEPT-rate 68.6 % sub-axis / 68.3 % per-lens; 22 REVISE
  findings clustered into 11 fold-class groups; V2 fold packet
  discharged at HEAD `87816a2cd`)

Binding authorities (carry-forward from V1+V2):
- `restart/prompts/totality/PASS-1-EXCAVATION.md §3 + §5` (CH1-CH7
  specialisations; §5 convergence + T-P3 dispatch gate)
- `restart/prompts/ORCHESTRATOR.md §3W` (universal CH1-CH6 lens
  registry) + `§3Z` (≥95 % × 2 consecutive cycles + zero orphan
  REVISEs convergence rule; V≤5 cohort ceiling)
- `restart/prompts/skinny/PASS-0-OVERFIT-AUDIT.md §CH7` (Overfit-Prune
  lens definition; LAC-1E-12 anti-fabrication phrasing template; SK-V14
  contract relies on this as the de facto CH7 binding source since
  LOCKS.md is silent on CH7)
- `restart/skinny/tranches/sk-v14/SYNTHESIS.md` + `HANDOFF.md` (SK-V14
  contract; R1-R10 + C-1..C-5 + PRUNE-1..PRUNE-7)
- `restart/skinny/tranches/sk-v14/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md`
  (S-P0 prune list; 74 findings; google_sheets=10 baseline at `:210`)
- `restart/skinny/tranches/sk-v14/audit-overfit/sk-v14-audit-overfit-pre-restart-pattern.md:46-56,153`
  (S-P0 A6 Pattern H baseline; google_sheets=10)
- `restart/locks/LOCKS.md` (governance text; verified zero CH7/Overfit
  references via `grep -n` at V3 dispatch + at every V3 lens dispatch
  time; LAC-1E-12 anti-fabrication phrasing template)
- `restart/ARCHITECTURE.md:764-765` (nine extant grammars enumeration;
  verified at V3 HEAD; matches `find crates/core/src/runtime -mindepth
  1 -maxdepth 1 -type d | wc -l` = 9)

On-disk artefacts verified at V3 lens dispatch time (per CHALLENGE-
CONTEXT §3 executable-verification mandate; CH7 §2.1-§2.6 + CH1
verification pass):
- `crates/core/src/runtime/` (9 grammar directories, 67 hand-written
  .rs files; `find` reproduces 1C `:38` baseline byte-for-byte;
  per-grammar census `8+7+7+7+7+7+10+7+7=67`)
- `crates/core/src/runtime/mod.rs:25-77` (53-line reexport block;
  mechanical extraction returns 127 grammar-named symbols after
  subtracting 6 in-window + 4 out-window neutrals; bbnf 10, bnf 10,
  css_l4 43, css_pretty 10, csv 10, ebnf 10, google_sheets 11, json 13,
  math 10 = 127)
- `crates/core/src/runtime/google_sheets/` (10 files; google_sheets=10
  baseline carry-forward)
- `grep -n "CH7\|Overfit" restart/locks/LOCKS.md` (zero hits; V2
  COH-012 fix carry-forward verified)
- `grep -c '@generated by skinny bbnf-codegen' restart/audit/totality/p1/1*.md`
  (zero productive admits; 1 diagnostic citation in 1E:79)
- `skinny/crates/bbnf-bench/src/nonjson_css_l4.rs` (3,644 LOC at V3
  HEAD; `fixture_sidecar_facts` callsite at `:648`, definition at
  `:2691`; `same-plane-source-sidecar` writers at `:1082, 1203, 1354,
  1511, 1661, 1815, 1964`; `lightningcss_facts` 27 hits — root def at
  `:636` + 6 siblings at `:656/:681/:708/:723/:739/:755` + 20 call
  sites)
- `skinny/crates/bbnf-bench/src/track2/json.rs:7,26,45` (HEAD line
  positions; V2 cites at `:5,24,43` rebound in V3 F-V3-CH5-1)
- `skinny/crates/runtime/src/lib.rs:29-33` (cfg-gated `pub mod
  json_event_grammar_witness` + `pub mod sheets_witness`; V2 cite at
  `:9` rebound in V3 F-V3-CH5-1)
- `restart/audit/totality/REDRESS.md:4734` (CORRECTNESS-REJECT for
  W13.9; V3 1D `:140` mis-labels as MEASURED-REJECT — F-V4-CH3-1
  repair target)
