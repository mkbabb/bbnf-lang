# T-P1 V2 Hardening Consolidation

Pass: T-P1 Excavation. Cycle: V2.
Date: 2026-05-21.
Scope: consolidation of `restart/audit/totality/p1/hardening/V2/CH1.md`
through `CH6.md` against the folded V2 excavation artifacts.

## Verdict

Disposition: REVISE.

V2 materially folded the V1 hardening set. CH2 generality and CH3
regression both accept. CH1, CH4, CH5, and CH6 still require a narrow V3
fold before T-P1 can enter acceptance-counted convergence. No V2 lens
issued REJECT.

## Lens Dispositions

| Lens | Disposition | Load-bearing result |
|---|---|---|
| CH1 correctness | REVISE | Broken V1 path citations, `LayoutFacts.backend_shape`, CSS/SinkOnly regression framing, and most negative-search hygiene are fixed. Remaining issues are uncaptured command-derived count/test claims in 1C, Lock 14 citation drift in 1E, uncaptured Lock 13 `wc` counts in 1E, and substantive 1D claims citing prior hardening instead of primary evidence. |
| CH2 generality / Lock 14 | ACCEPT | V2 distinguishes grammar-name leaks from grammar-shape leaks, expands the generic-crate census, preserves GrammarConfig as partial repair only, and records CSS L4 / Sheets / BBNF-self implications without claiming universal closure. |
| CH3 regression / REDRESS | ACCEPT | V2 preserves admitted JSON SinkOnly and CSS evidence, keeps REDRESS-119/120 as history only, maintains SK-V13 hard pre-blocks, and does not silently demote admitted rows. |
| CH4 cost / hard-cap realism | REVISE | 1A-1E now carry the required planning schema. 1F carries complete ID-keyed planning metadata, but the divergence tables still require readers to join across tables without saying that metadata is authoritative. |
| CH5 hidden coupling | REVISE | V2 folds `StructuralIndex`, CSS source-sidecar comparator, proof-witness root coupling, and retained parallel-substrate fences. It still lacks an explicit 1F anti-pattern row for Track 2 shared runtime substrate helpers. |
| CH6 anti-paper-close | REVISE | UNKNOWN rows now carry verify actions and most over-closure wording is weakened. Remaining issues are the JSON-only "single substrate" lesson in 1D and Lock 1 wording in 1E, both of which read broader than the evidence proves. |

## Required V3 Fold

1. In `1C-runtime-evidence.md`, either capture command output for the
   runtime file/test/LOC claims or downgrade them to scan-derived estimates
   and UNKNOWN verify actions.
2. In `1E-locks-evidence.md`, cite `restart/locks/LOCKS.md:78` for the
   Lock 14 rule; use `restart/locks/LOCKS.md:1-17` only for the scoped
   SK-V9 allowance. Downgrade uncaptured Lock 13 `wc -l` counts or cite a
   committed transcript.
3. In `1D-skinny-lessons.md`, replace substantive citations to V1 hardening
   files with primary source, RESULTS, REDRESS, or SK-V13 citations. Change
   the single-substrate lesson to "proved for JSON; grammar-neutral rule
   candidate" unless non-JSON live substrate evidence is added.
4. In all three 1F files, state directly above each divergence table that
   the ID-keyed `V2 Planning Metadata` table is the authoritative CH4 carrier,
   or merge those fields inline.
5. In `1F-anti-pattern.md`, add a Track 2 shared-substrate-helper row citing
   `skinny/crates/bbnf-bench/src/track2/json.rs:5-8`, `:24-32`, and `:43`;
   classify it as independent parser authority with shared runtime substrate
   helpers, not parser-authority dishonesty and not retained parallel substrate.
6. In `1E-locks-evidence.md`, change Lock 1 to "partial / honoured for
   scoped JSON lazy-offset evidence" and keep the T-P3 substrate consumer as
   future verification, not closure.

## V3 Entry Rule

V3 must preserve the accepted V2 corrections: admitted CSS fact-stream
evidence, admitted generated JSON SinkOnly evidence, the live-but-partial
`LayoutFacts.backend_shape` classification, the SK-V13 hard-pre-block versus
fresh-differential-unblocked split, and UNKNOWN verify actions for uncaptured
negative searches.
