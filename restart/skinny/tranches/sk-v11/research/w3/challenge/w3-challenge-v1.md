# SK-V11 Wave W3 CHALLENGE V1

Date: 2026-05-20.

Plan challenged:

- `restart/skinny/tranches/sk-v11/research/w3/w3-plan-number-span-emit-slot.md`

Disposition: **REJECT — route back to plan**.

The W3 plan has a real generated direct consumer and plausible scalar scope, but
it is not redress-authorized as written. Three of six lenses rejected because
row admission is not gate-consumable under the owner paths and the plan
overstates Track 2 as an independent numeric scanner oracle.

## Lens Results

| Lens | Disposition | Load-bearing finding |
|---|---|---|
| CH1 correctness/measurability | REJECT | The `mesh` and `numbers` floors are correct and the consumer is real, but `gate-json` cannot admit W3 direct rows without gate/report ownership. Current direct admission logic only has W2 and W10 routes. |
| CH2 generality/Lock 14 | ACCEPT with caveats | W3 may proceed only as a JSON direct-plane closure/fixpoint wave. W2 remains BLOCKED and the non-JSON axis remains open for W8/W9. |
| CH3 regression/REDRESS | ACCEPT with caveats | The plan does not reopen REDRESS 31/39/46/80 or SK-V9 W3 substrate routes, but scanner-policy drift needs exact serde/sonic and number-class backstops. |
| CH4 cost/LOC/cap | ACCEPT with tight scope | A generated-runtime scalar refactor fits the cap, but it is more likely to be too small/no-op than too broad. |
| CH5 hidden coupling/Track 2 | REJECT | Track 2 is an independent caller, not an independent numeric scanner/materializer; both tracks use `match_number_span_from_first` and shared materializers. |
| CH6 anti-paper-close | REJECT | The row gate is not machine-consumable without `gate.rs` / report metadata edits, so manual row movement or helper-only admission would be a paper-close. |

## Required Plan Redress

The revised W3 plan must:

1. Add `skinny/crates/bbnf-bench/src/bin/gate.rs` and any required
   `skinny/crates/bbnf-bench/src/report.rs` support to owner paths.
2. Define W3 gate consumption for selected direct rows: primary
   `mesh/direct_to_struct` floor `8675`; optional `numbers/direct_to_struct`
   floor `2425` with explicit W0-clamp/W3 provenance.
3. Make exact generated `parse_direct` root/object/array number-slot parity
   tests mandatory, including `-0`, `-0.0`, i64/u64 limits,
   overflow-to-f64, subnormal, exponent forms, invalid suffix ownership, and
   slot class dispatch.
4. Disclose Track 2 as `independent caller, shared parse-that
   scanner/materializer`; do not call it an independent number-scanner oracle.
5. Specify the exact Criterion/gate commands and the `>= 1.0%` same-host
   caller movement calculation before redress.
6. Keep W2 BLOCKED explicit and carry the non-JSON axis to W8/W9; W3 cannot
   close grammar generalization.

Redress remains forbidden until a revised plan passes CHALLENGE.
