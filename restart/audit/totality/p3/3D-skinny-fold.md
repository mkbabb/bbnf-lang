---
agent: 3D
pass: T-P3-synthesis
cycle: V4-SKV18-totality
cycle_self_label: SK-V18-totality
generated_at: 2026-06-01T00:00:00Z
t_p1_inventories_consumed: [1A, 1B, 1C, 1D, 1E, 1F]
t_p2_dossiers_consumed: [2A, 2B, 2C, 2D, 2E, 2F]
v1_surface_targeted: n/a (proposal-only; Pass Omega CRUD applies post-G-Omega)
proposed_deltas_count: 12
delta_summary:
  carried_from_prior_cycle:
    - 3D-D01-json-guard-scope (re-anchored SK-V15→SK-V18 wave plan)
    - 3D-D02-css-broadcast-demotion (re-anchored; SK-V18 broadcast already PRUNED — kept as standing gate)
    - 3D-D03-css-provider-before-retirement (re-anchored; CSS now MEASUREMENT-VALID not contrived)
    - 3D-D04-lock14-lock16-gate-exclusion (re-anchored to P4 strict GENERIC_SCAN_ROOTS)
    - 3D-D05-pattern-h-provenance (re-anchored; carried as SK-V19 U-1 carry, not SK-V18 wave)
    - 3D-D06-decision-engine-scaffold (re-anchored; Decision now LOAD-BEARING — kept as selection-DEPTH gate)
    - 3D-D07-fnv-bench-quarantine (re-anchored; G-5 SPLIT: bench-quarantine clean, production telemetry honest)
    - 3D-D08-substrate-sidecar-lock (re-anchored to SK-V18 R16/REDRESS-51/53 pre-block)
    - 3D-D09-non-json-receiver-onboarding (re-anchored; Sheets is the SK-V18 PROVE negative control)
    - 3D-D10-prune-before-rebuild-sequencing (re-anchored SK-V15 W0-W11 → SK-V18 P1-P5/G1-G6/PROVE/H1)
  removed: []
  answered:
    - "SK-V18 inflection thesis (1D G-9): >SOTA real, impl hand-written/forked/replicated — naive grammar-walk regresses to lightningcss; folded into new 3D-D11"
    - "SK-V18 one-generator non-existence (1D D-1/G-6): generator forks on grammar family; folded into new 3D-D11"
    - "SK-V18 R16 relocated-seam co-gate (1D G-12): structural row-collapse over arm-grep; folded into new 3D-D12"
    - "SK-V18 G6=WIRE / NEON RETARGET-not-wire-as-is (1D G-10/D-6); folded into 3D-D08 + 3C cross-ref"
    - "SK-V18 x86 crate-wide prune (1D D-4/G-2); folded into 3D-D10 PRUNE cluster"
    - "G-5 production-telemetry SPLIT (1D G-5 CH5-V3-005); folded into 3D-D07"
  newly_added:
    - 3D-D11-one-generator-inflection-thesis
    - 3D-D12-r16-relocated-seam-cogate
prior_cycle_dispositions_folded:
  accepted:
    - "Prior V3 monotonic-fold direction preserved verbatim: skinny WIN → V1-authoritative proposal input; skinny REJECTION → locks-strengthening evidence; totality NEVER dictates live skinny."
    - "Prior CH2 generality (CSS plus Sheets/BBNF-self, no sixth shape) carried; SK-V18 names Sheets as the explicit PROVE negative control."
    - "Prior CH3 REDRESS delete-before-provider pre-blocks carried; SK-V18 adds REDRESS items 51/53/246/247 four-item pre-block."
    - "Prior CH7 overfit-prune (broadcast, gate-exclusion, FNV, wrong-host, provider-order) carried; SK-V18 PRUNE cluster P1-P5 lands all five fail-closed BEFORE rebuild."
  rejected: []
  revised:
    - "Wave-plan re-anchor: every prior 3D delta re-keyed from SK-V15 W0-W11 to the SK-V18 12-wave manifest (P1-P5 PRUNE / G1-G6 GENERALIZE / PROVE / H1 HONESTY)."
    - "CSS posture: SK-V15 carried CSS as CONTRIVED (fake admit); SK-V18 1D J-3/G-9 carry CSS as MEASUREMENT-VALID but DIRECTIONAL/not-re-locked (loadavg 4.35, H1 css_canon_bench re-lock gate). 3D-D02/D03 demotion language softened from contrived to directional-pending-re-lock."
    - "Decision Engine posture: SK-V15 carried it as scaffold; SK-V18 1D D-10/G-3 carry it LOAD-BEARING (SK-V15 W7/W8/W9 admitted operation-plan renderers + e-graph rewrite count). 3D-D06 reframed from scaffold-rejection to selection-DEPTH-under-Sheets-tower open gate (1E-L10)."
    - "G-5 FNV: SK-V15 carried it bench-only quarantine; SK-V18 1D G-5 SPLITS — bench quarantine clean (KEEP), production emit_full_parse input_fnv64 is LIVE non-equality telemetry (honest, NOT bench-quarantined). 3D-D07 reworded to bench-quarantine-clean + non-equality-arbiter, not bench-only."
    - "V1-FOLD (CH1-V1-C3-xref): 3D-D12 relocated-seam cross-ref re-keyed from '3C D-L06/D-L14' to '3C D-SKV18-L05-L10-unfork' (the relocated-seam co-gate clause; D-SKV18-L06 is the verbatim-blob clause)."
    - "V1-FOLD (CH4-V1 3D-D12): the CH4 cost cell scoped explicitly to the skinny P3 +1-line RuntimeTarget:PartialEq derive; the totality 9-row scaling is cross-ref'd to SK-V19 R16 (≈+217, 3B MP.SK19.UNFORK / 3A-D11b), not charged to the +1-line."
    - "V1-FOLD (CH3-V1-R2, 3D primary owner): the SK-V16/V17 REDRESS reconcile RETIMED from SK-V19-entry to a Pass-Omega-V6 / pre-W-PRUNE blocker; 3D-D08 + the CH3 open question now state G2/G4/G6 entry is BLOCKED until the SK-V16/V17 pre-block reconcile is on the committed ledger (mirrored 3B CH3 / 3F-MH-003)."
    - "V3-FOLD (CH4-V3, 3D:155): the W-PRUNE summary row's internally inconsistent component sum repaired — P3 re-stated ≈ −5500 (= −5460 replica bodies + ~−40 collapsed rows + 1 PartialEq derive) per SPEC:435 so the P1+P2+P3+P4+P5 components sum to the SPEC per-wave −10685 under the ≈ tilde to the −10800 headline (the bare −5460 in a component-sum context was the last artefact carrying it; now aligned with 3B §13.7-P3 / 3B-D04 / 3F-MH-009)."
    - "V3-FOLD (CH2-V3-R01, consistency): 3D-D04 (3D:89,:124) already carries the certified SK-V18 SPEC token form `{GENERATED_RS, CSS_GENERATED_RS, EventGrammar, *EventGrammar}`; the cluster-A artefacts (3A-D11/3C/3E) were back-ported onto this same form, so all five loci + the binding v+1 hunk now carry the IDENTICAL string."
---

## Executive Summary

3D folds skinny into totality in one direction only: skinny lessons inform V1
proposal inputs; totality never dictates live skinny (`restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:110`-`112`).
SK-V18 is the INFLECTION cycle, and the durable lesson the V1 spec must absorb is
its inversion of SK-V13: the >SOTA is now MEASUREMENT-VALID — JSON 51/51 strict
cold beats sonic-rs bench-row-backed (`restart/audit/totality/p1/1D-skinny-lessons.md:185`),
and CSS canonical cold beats lightningcss 1.66–3.38× but DIRECTIONAL, not yet
re-locked (loadavg 4.35; H1 `css_canon_bench` gate; `restart/audit/totality/p1/1D-skinny-lessons.md:187`).
What is DISPROVED is the EMISSION story: one generator does not exist (two forked
couriers + 7 byte-identical css_l4 replicas), `RuntimeEmitterKind` forks on
grammar family, the Lock-14 gate is green-by-exclusion, x86 lingers, the phantom
`<G>` is test-only (`restart/audit/totality/p1/1D-skinny-lessons.md:202`-`208`). The SK-V18
finding the totality spec MUST reflect: a naive grammar-walk regresses to
lightningcss's 94.1% scan architecture, so >SOTA survives ONLY via named,
`.bbnf`-invoked, grammar-derived, (a)-(d)-gated primitives — never a paper-close
(`restart/audit/totality/p1/1D-skinny-lessons.md:48`-`49`). V4 re-anchors all ten prior 3D
deltas onto the SK-V18 12-wave manifest and adds two: 3D-D11 (one-generator
inflection thesis) and 3D-D12 (R16 relocated-seam co-gate).

## V4 Delta Summary

| bucket | disposition |
|---|---|
| carried from prior cycle | `3D-D01` through `3D-D10` remain the proposal-only skinny-fold set, RE-ANCHORED from the SK-V15 W0-W11 wave numbering to the SK-V18 12-wave manifest (P1-P5 / G1-G6 / PROVE / H1). No live V1 spec surface is edited by 3D (`restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:63`-`66`). |
| removed | None. No skinny lesson is silently dropped; the SK-V15-specific wave labels are revised, not the lessons. |
| answered | The SK-V18 inflection thesis (1D `G-9`), one-generator non-existence (1D `D-1`/`G-6`), R16 relocated-seam (1D `G-12`), G6=WIRE NEON retarget (1D `G-10`/`D-6`), x86 prune (1D `D-4`/`G-2`), and the G-5 production-telemetry split are answered by 3D-D07/D08/D10/D11/D12. |
| newly added | `3D-D11-one-generator-inflection-thesis`; `3D-D12-r16-relocated-seam-cogate`. |

## Monotonic Fold Register

| fold class | SK-V18 skinny lesson | totality treatment |
|---|---|---|
| win → V1-authoritative proposal input | The unified `Tape`/`ValueRef`/`PayloadArena` substrate is the genuine CLEAN foundation; `ValueRef` is a cursor into the one tape, no second substrate, no retained sidecar (1D `G-1`, `D-11`; `restart/audit/totality/p1/1D-skinny-lessons.md:126`-`128`, `:197`). | V1 may cite the single-tape/`ValueRef`-cursor substrate as Lock-1-authoritative at GREATER fidelity than the spec currently asserts; it is what S-P0 explicitly KEEPs (`restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:109`). |
| win → V1-authoritative proposal input | The canonical COLD bench `css_canon_bench` (corpus-in-timer, N≥50, real corpus, no broadcast) is the honest measurement plane; the warm micro-fixture path (`nonjson_css_l4.rs:3091 measure_mbps`) did NOT produce the numbers and is a P2 deletion target (1D `J-3`; `restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:112`, `:138`). | V1 absorbs the cold-corpus-in-timer canonical bench as the authoritative >SOTA harness; warm/cached benches are non-admitting (no-warm-benches feedback). |
| win → V1-authoritative proposal input | The >SOTA cold proofs are honest: JSON 51/51 strict-vs-sonic cold per-iter equality is bench-row-backed (1D `J-1`/`D-9`); CSS cold beats lightningcss DIRECTIONALLY (1D `J-3`/`G-9`). | V1 cites JSON as the load-bearing same-plane >SOTA proof and CSS as DIRECTIONAL pending the H1 `css_canon_bench` re-lock; CSS must NOT carry an un-caveated "MEASUREMENT-VALID" closure word on its half (`restart/audit/totality/p1/1D-skinny-lessons.md:67`, `:187`). |
| win → V1-authoritative proposal input | The one-generator-inflection FINDING: >SOTA is real, impl is forked/replicated; a naive grammar-walk regresses to lightningcss, so >SOTA survives ONLY via (a)-(d)-gated named primitives (1D `G-9`, `G-11`; `restart/audit/totality/p1/1D-skinny-lessons.md:207`). | V1 absorbs the named-primitive escape as the single largest paper-close surface, admissible ONLY under (a) grammar-INVOKED-by-name ∧ (b) output VARIES under invoking-rule mutation ∧ (c) `verbatim_blob_present==false` ∧ (d) PROFILE-PROVEN-NARROW-LEAF (new 3D-D11). |
| rejection → locks-strengthening evidence | The generator does not exist: two forked couriers (`runtime_generator.rs:16,91,701`), JSON 7× `push_str`, 7 byte-identical css_l4 `generated.rs` (md5 `b654562c…`), `RuntimeEmitterKind` forks on grammar family (1D `D-1`/`D-2`/`D-3`/`G-6`/`C-1`). | Lock 6/Lock 14 wording must treat the current emission route as a forked courier until G1+G2+G3 prove a grammar-DERIVED body; the R16 structural row-collapse co-gate (1D `G-12`) is the only catcher of the relocated-seam (new 3D-D12; cross-ref 3C `D-SKV18-L05-L10-unfork`, the relocated-seam co-gate clause — CH1-V1-C3-xref; `D-SKV18-L06` is the verbatim-blob clause, not the seam). |
| rejection → locks-strengthening evidence | The Lock-14 gate is green-by-exclusion — leak surfaces sit in the weak `SKV15_W2_EXTRA_COVERAGE_ROOTS` + `diagnostic-x86` exclusion (1D `D-7`/`G-7`; `skinny/crates/bbnf-bench/src/lock14_baseline.rs:2442,2463`). | Lock 14/16 gate wording must require strict `GENERIC_SCAN_ROOTS` inclusion of `runtime_generator.rs`, the JSON sink/typed/template surfaces, plus `FORBIDDEN_GENERIC_TOKENS ⊇ {GENERATED_RS,CSS_GENERATED_RS,EventGrammar,*EventGrammar}` (the `*EventGrammar` glob is alias-immune per CH2-V1-R02, catching `SHEETS_EventGrammar`/`JsonEventGrammar` re-injects), with the gate landing BEFORE G2/G3 (3D-D04; cross-ref 3C `D-SKV18-L14-green-by-exclusion` / `D-SKV18-L16`). |
| rejection → locks-strengthening evidence | x86 lingers crate-wide (24 files `src/x86_64/` + `ext/x86/` vendored ASM + nasm `build.rs`) under an aarch64-only admission platform (1D `D-4`/`G-2`; `restart/skinny/tranches/sk-v18/SPEC.md:130`-`133`). | Lock 15/Lock 16 host wording carries aarch64-only as PROVED admission policy; x86/AVX-512/SVE2 are diagnostic-only and cannot close an M5 Max row (T-P2 `T2A-V18-REFUTE-002`, `T2A-V18-REFUTE-001` dav1d, `MatchSetSve2` REFUTED on host; `restart/audit/totality/p2/2A-sota-landscape.md:182`, `:181`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:131`). The x86 excision is the P1 PRUNE (3D-D10). |
| rejection → locks-strengthening evidence | The metalang leak `parse_w11_1_number` ×7 contaminates shipped JSON runtime symbols (1D `D-8`/`C-4`; `skinny/crates/runtime/src/grammars/json/generated.rs:801,841,881`). | Lock 6 generated-state cleanliness wording requires `regen --check` exit 0 with no `w[0-9]+`/corpus/`sk_v` tag in shipped runtime; rename at source is the P5 PRUNE (3D-D10). |
| generalization gap → 3E cross-ref | Sheets proves generalization REAL via the precedence tower through the un-forked generator ONLY; if it needs a shim, generalization is NOT real and is surfaced HONESTLY (negative-control `N`), never stub-proved (1D `U-2`, SPEC §0.1.9; `restart/skinny/tranches/sk-v18/SPEC.md:143`-`152`). | 3D routes the non-JSON receiver story to 3E while preserving the fold: V1 absorbs Sheets as the explicit PROVE negative control; live skinny remains governed by the SK-V18 wave gates (3D-D09; cross-ref 3E 3E-D07). |
| generalization gap → 3E/SK-V19 carry | The totality `crates/core/` tree carries 67 hand-written files / 6867 LOC at 9× scale; whether SK-V19 inherits the SAME forked-emitter problem is UNKNOWN (1D `U-1`; `restart/audit/totality/p1/1D-skinny-lessons.md:213`-`225`). | 3D carries Pattern-H provenance as a SK-V19-entry census obligation (line-1 `@generated` provenance + md5-distinctness across the 9 grammars), NOT a SK-V18 wave; 3D-D05 reframed from a SK-V15 wave to the SK-V19 carry. |

## SK-V18 Wave-Plan Re-Anchor Map

Every prior 3D delta was keyed to the SK-V15 W0-W11 numbering. The SK-V18
certified manifest is a 12-wave PRUNE-before-rebuild plan: W-PRUNE = P1-P5
(DELETE x86 / warm bench / 7 replicas / fix gate / purge metalang), then
G1-G6 (GENERALIZE: JSON projection / CSS lowering / un-fork emitter / value-API
trait + `<G>` delete / json-scan retire / NEON retarget), then PROVE (Sheets
negative control), then H1 (HONESTY: quiet re-lock + framing disclosure)
(`restart/skinny/tranches/sk-v18/SPEC.md:16`-`17`, `:58`-`174`). This map re-keys the
delta receivers; the lessons are unchanged.

| prior SK-V15 wave | SK-V18 receiver wave(s) | re-anchor note |
|---|---|---|
| W0 baseline / W1 broadcast demotion | W0 lock + P2 (warm-bench prune) | SK-V18 broadcast is already PRUNED (P2 deletes `measure_mbps`); CSS is now MEASUREMENT-VALID, so the demotion gate is STANDING, not active repair. |
| W2 gate fix | P4 (Lock-14 strict scan roots; lands BEFORE G2/G3) | gate-exclusion fix is a PRUNE pre-condition, not a mid-rebuild wave. |
| W3 codegen leaks | P5 (metalang purge) + G1 (JSON projection diff-control) | leak purge is PRUNE; projection truth is G1. |
| W4 Pattern H | SK-V19 entry census (1D `U-1`) | the 67-file Pattern-H carry is a totality `crates/core/` SK-V19 concern, not a SK-V18 benched-tree wave. |
| W5/W6 CSS rebuild/retime | G2 (CSS lowering + named primitive) + H1 (quiet re-lock) | CSS body becomes grammar-DERIVED at G2; the same-run `track1_rich/lightningcss > 1.0×` gate fires at G2-exit; H1 re-confirms directionally. |
| W7-W9 Decision/lowerers | G3 (un-fork emitter, dispatch on lowered `BackendShape`) | the 5-shape decision spine is LOAD-BEARING (1D `G-3`); G3 dispatches ON it; selection DEPTH under the Sheets tower is the open L10 stressor. |
| W10 FNV | retained as standing Lock-16 gate (no SK-V18 wave) | G-5 SPLIT: bench quarantine clean, production telemetry honest non-equality. |
| W11 no-orphans | G4 (`<G>` delete) + G5/G6 (no-orphan kernel) + close-audit (#11/#12) | phantom delete + same-wave-consumer SIMD admission. |

## Proposed Delta Table

| proposed delta | source T-P1/T-P2 finding-id cited | affected V1-surface section | rationale |
|---|---|---|---|
| `3D-D01-json-guard-scope`: V1 proposal note that JSON 51/51 strict cold is a same-plane >SOTA proof-of-concept, not CSS or fleet closure. Preserve parse_only/direct/typed plane names; carry CSS as DIRECTIONAL pending H1 re-lock. | T-P1 `J-1`, `J-2`, `D-9` (`restart/audit/totality/p1/1D-skinny-lessons.md:185`-`186`, `:115`-`118`); T-P2 plane split (`restart/audit/totality/p2/2A-sota-landscape.md:68`). | `ARCHITECTURE.md` output-plane taxonomy; `LOCKS.md` Lock 1/Lock 8 scope; `SPEC.md` SK-V18 close conditions 6 (`restart/skinny/tranches/sk-v18/SPEC.md:104`-`128`). | Skinny win becomes V1-authoritative only with scope qualifiers: JSON is clean same-plane proof; CSS is MEASUREMENT-VALID but DIRECTIONAL not re-locked (`restart/audit/totality/p1/1D-skinny-lessons.md:187`). The SK-V18 inversion of SK-V13 (no fake admit) is the headline V1 must reflect. |
| `3D-D02-css-broadcast-demotion`: V1 carries broadcast-admission detection as a STANDING gate; repeated CSS throughput tuples are non-admit without distinct row-local typed measurements; require anti-broadcast telemetry fields. | T-P2 `T2A-V1-REFUTE-CSS-001` (`restart/audit/totality/p2/2A-sota-landscape.md:64`); SK-V18 P2 warm-bench prune (`restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:97` R13, `:162`-`164`). | `LOCKS.md` Lock 8 telemetry discipline; `MIGRATION.md` fact-stream/telemetry receiver; SK-V18 comparator classes (`restart/skinny/tranches/sk-v18/SPEC.md:175`-`205`). | SK-V18 has ALREADY pruned the warm broadcast bench (P2); the demotion becomes a standing gate that keeps future CSS rows honest, binding `measurement_row_id`/`broadcast_group_id`. The SK-V18 canonical bench (`css_canon_bench`, no broadcast) is the V1-authoritative replacement. |
| `3D-D03-css-provider-before-retirement`: receiver rule that the CSS const courier (`CSS_GENERATED_RS`), 7 replicas, and fact-stream-only CSS `parse()` retire only in the same wave as, or after, grammar-DERIVED CSS body proof + the same-run `track1_rich/lightningcss` retime. | T-P1 `C-1`, `C-2`, `C-3` (`restart/audit/totality/p1/1D-skinny-lessons.md:188`-`190`); T-P2 `T2A-V1-REFUTE-CSS-002/003/004` (`restart/audit/totality/p2/2A-sota-landscape.md:65`-`67`). | `ARCHITECTURE.md` output-plane taxonomy; `MIGRATION.md` generated-provider/fact-stream rows; SK-V18 close conditions 1/6 (`restart/skinny/tranches/sk-v18/SPEC.md:63`-`69`, `:104`-`128`). | Delete-before-provider lock strength: the CSS courier retires only when G2 emits a grammar-DERIVED body whose 94.1% scan does NOT regress to a tree-walk (lightningcss's own architecture). The SAME-RUN comparison (pre-G2 baseline captured at G2 entry) replaces the un-re-locked absolute ratio (`restart/skinny/tranches/sk-v18/SPEC.md:106`-`118`). |
| `3D-D04-lock14-lock16-gate-exclusion`: strengthen V1 so the Lock-14 gate moves `runtime_generator.rs` + JSON sink/typed/template surfaces into strict `GENERIC_SCAN_ROOTS`, extends `FORBIDDEN_GENERIC_TOKENS`, drops `diagnostic-x86`, and lands BEFORE G2/G3; self-exempting scans REJECT. | T-P1 `D-7`, `G-7` (`restart/audit/totality/p1/1D-skinny-lessons.md:108`-`111`, `:203`); T-P2 gate-exclusion (`restart/audit/totality/p2/2C-grammar-neutrality.md:148`). | `LOCKS.md` Lock 14 / Lock 16; SK-V18 close condition 8 + P4 prune (`restart/skinny/tranches/sk-v18/SPEC.md:135`-`141`, `:170`-`175`). | The gate reads CLEAN only because the leak surfaces sit in the WEAK extra-coverage roots, so it cannot evidence generic cleanliness. V1 should encode gate-exclusion detection as an admission precondition. The `re-inject-a-SHEETS_GENERATED_RS-token` falsifier is the proof of coverage (cross-ref 3C D-L14). |
| `3D-D05-pattern-h-provenance-skv19-carry`: V1 proposal that Pattern-H close (the 67 hand-written `crates/core/` files at 9× scale) requires line-1 `@generated` provenance + md5-distinctness across the 9 grammars, asserted at SK-V19 ENTRY, not a SK-V18 benched-tree wave; header-only or destructive delete without same-wave replacement rejects. | T-P1 `U-1` (`restart/audit/totality/p1/1D-skinny-lessons.md:213`-`225`); T-P2 Pattern-H provenance (`restart/audit/totality/p2/2C-grammar-neutrality.md:147`). | `ARCHITECTURE.md` live Pattern-H status; `LOCKS.md` Lock 14 Pattern-H census; `MIGRATION.md` Pattern-H receiver. | SK-V18 is the skinny-tree generator-inflection; the 67-file Pattern-H carry is a TOTALITY `crates/core/` SK-V19 concern. V1 should preserve the 67-file / 6867-LOC census as the SK-V19 repair surface and require the same forked-emitter falsifier (md5-distinctness, root provenance) that SK-V18 P3 applies to the 7 replicas (cross-ref 3E CH5). |
| `3D-D06-decision-engine-selection-depth`: V1 proposal that the Decision Engine is LOAD-BEARING (e-graph rewrite count, falsifiable CSP, all-five lowerers as operation-plan renderers) but its SELECTION DEPTH under the Sheets precedence tower remains the open L10 stressor until G3 exists. Preserve exactly five `BackendShape` variants. | T-P1 `D-10`, `G-3` (`restart/audit/totality/p1/1D-skinny-lessons.md:119`-`125`, `:199`); T-P2 five-shape canon (`restart/audit/totality/p2/2D-cost-model.md:62`). | `LOCKS.md` Lock 10 decision/cost clauses; `MASTER-PLAN.md` Decision load-bearing receiver; SK-V18 close conditions 2/9 (`restart/skinny/tranches/sk-v18/SPEC.md:71`-`78`, `:143`-`152`). | SK-V15 W7/W8/W9 moved this from scaffold to load-bearing (prior-cycle 3D rejected it as scaffold; that is REVISED). The remaining V1 obligation is selection DEPTH: whether G3 can render recursive `CallRule`/`RepeatLoop` chains for the Sheets tower (`1E-locks-evidence.md:90`). Keep five variants; require executable row movement or explicit gate rejection. |
| `3D-D07-fnv-bench-quarantine-split`: V1 proposal that FNV/closed-enum products have TWO clean surfaces — (a) the bench-side FNV quarantine is genuinely bench-only and KEPT; (b) the production `emit_full_parse input_fnv64` is LIVE non-equality / non-substrate / non-document-identity telemetry, honest and NOT a runtime equality arbiter. | T-P1 `J-4`, `G-5` (split per CH5-V3-005) (`restart/audit/totality/p1/1D-skinny-lessons.md:201`); production path:line at `skinny/crates/runtime/src/grammars/css_l4_declaration_values/generated.rs:393`. | `LOCKS.md` Lock 16 primitive manifest / correctness floor; `MIGRATION.md` primitive manifest receiver. | Prior 3D carried FNV as bench-ONLY; SK-V18 1D G-5 SPLITS it: the production `input_fnv64` is live telemetry on the Track-1 path, NOT bench-quarantined. V1 must word it as "never an equality arbiter / never substrate / never document-identity", not "bench-only", so the honest telemetry use is not mis-stated and cannot leak into correctness proof. |
| `3D-D08-substrate-sidecar-lock`: V1 proposal that retained structural sidecars, parser-local event/structural cursors, public `UnionTape`, and any sixth `BackendShape`/substrate remain blocked; the NEON G6 retarget is RETARGET-onto-the-live-recursive-shell, never a wire-as-is dead-flat kernel or a parser-local second scanner. | T-P1 `G-1`, `G-10`, `D-6`, REDRESS pre-block items 51/53/246/247 (`restart/audit/totality/p1/1D-skinny-lessons.md:166`-`171`, `:206`); T-P2 `find_css_significant` dead-at-admission + `MatchSetSve2` REFUTED (`restart/audit/totality/p2/2E-host-arch-esoterica.md:80`, `:219`, `:131`). | `ARCHITECTURE.md` output-plane taxonomy; `LOCKS.md` Lock 1 / Lock 16; SK-V18 close condition 10 (`restart/skinny/tranches/sk-v18/SPEC.md:154`-`165`). | SK-V18 preserves the structural-projection direction while rejecting retained sidecars. The G6=WIRE decision RULE is the neutral lesson (a hot leaf above the WIRE threshold warrants NEON retarget over honest-retire), but the dead `find_css_significant` kernel was written FLAT and the hot leaf recurses — so G6 RETARGETS, never wires-as-is (T-P2 explicitly REFUTES "wire as-is"). REDRESS 51/53/246/247 fence the second-scanner/structural-stream routes (cross-ref 3C D-L01/D-L16). CH3-V1-R2: because the four-item pre-block is complete only for the SK-V15-W11 ledger (1D U-5; SK-V16/V17 rejects structurally invisible), G2/G4/G6 entry is BLOCKED until the SK-V16/V17 REDRESS reconcile is on the committed ledger as a Pass-Omega-V6 / pre-W-PRUNE blocker (mirrored at 3B CH3 / 3F-MH-003). |
| `3D-D09-sheets-negative-control-onboarding`: V1 proposal that generic claims touching providers/generators/lowerers/egraph/CSP/cost/xtask/gates require CSS L4 plus Sheets (the SK-V18 PROVE negative control) or an intrinsic-block record before fleet-wide wording; a Sheets emission needing a shim is the negative-control fail `N`, surfaced honestly. | T-P1 `U-2`, `G-9` (`restart/audit/totality/p1/1D-skinny-lessons.md:226`-`233`, `:205`); T-P2 Sheets future-grammar (`restart/audit/totality/p2/2C-grammar-neutrality.md:72`). | `LOCKS.md` Lock 14 future-grammar gate; `ARCHITECTURE.md` generated-runtime target; `MIGRATION.md` generated-provider receiver. | The monotonic generalization bridge: JSON+CSS success is preserved, Sheets is the SK-V18 explicit negative control whose 7-level precedence tower JSON+CSS structurally lack. The BINDING FALLBACK is the lesson V1 must encode: if Sheets cannot emit via the generator ONLY, generalization is NOT real — surface honestly, never stub-prove a `_GENERATED_RS` Sheets block (`restart/skinny/tranches/sk-v18/SPEC.md:150`-`152`; cross-ref 3E 3E-D07). |
| `3D-D10-prune-before-rebuild-sequencing`: V1 receiver constraint that SK-V18 is PRUNE-before-REBUILD: P1 x86, P2 warm bench, P3 7 replicas, P4 gate (lands BEFORE G2/G3), P5 metalang → G1 JSON projection, G2 CSS lowering, G3 un-fork, G4 trait+`<G>` delete, G5 json-scan retire, G6 NEON retarget → PROVE Sheets → H1 quiet re-lock. T-P3 proposes only; it does not dispatch waves. | Dispatch constraint (`restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:55`-`57`); SK-V18 wave order + sequencing constraints (`restart/skinny/tranches/sk-v18/SPEC.md:45`-`51`; `restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:150`-`185`). | `MASTER-PLAN.md` wave reconciliation surface; `MIGRATION.md` proposal boundary; SK-V18 close conditions 7/11 (`restart/skinny/tranches/sk-v18/SPEC.md:130`-`133`, `:167`-`169`). | PRUNE lands FIRST — it reduces the surface for GENERALIZE and makes the Lock-14 gate trustworthy BEFORE the emitter rebuild (net LOC ≈ −10800; deletes far more than it adds). This prevents totality prose from paper-closing live skinny. There is no overflow route; non-fit work is intrinsic-block, REDRESS/revert, or G-Omega wave-graph amendment. |
| `3D-D11-one-generator-inflection-thesis` (NEW): V1 proposal that the SK-V18 inflection thesis is a durable totality finding — one generator does NOT yet exist (two forked couriers + 7 replicas), >SOTA is real but rides hand-written content, and a naive grammar-walk regresses to lightningcss's 94.1% scan; therefore >SOTA survives ONLY via the §6 named-primitive escape gated (a) grammar-INVOKED-by-name ∧ (b) output-VARIES-under-rule-mutation ∧ (c) `verbatim_blob_present==false` ∧ (d) PROFILE-PROVEN-NARROW-LEAF. | T-P1 `D-1`, `G-6`, `G-9`, `G-11` (`restart/audit/totality/p1/1D-skinny-lessons.md:82`-`86`, `:202`, `:205`, `:207`); T-P2 candidate R-B/R-C tree-walk REJECTED (`restart/skinny/tranches/sk-v18/research/p2/SYNTHESIS-RESEARCH.md:39`-`40`, `:51`). | `ARCHITECTURE.md` generator architecture + `BackendShape` seam; `LOCKS.md` Lock 6 generated-source / Lock 14 generic-crate; `MASTER-PLAN.md` one-generator goal; SK-V18 close conditions 1/6 (`restart/skinny/tranches/sk-v18/SPEC.md:63`-`69`, `:104`-`128`). | This is the central SK-V18 lesson the V1 spec must absorb: the round-trip (`delete + regen ⇒ byte-equivalent`) passes BUT over hand-written content — a courier-swap or replica-relabel must NOT pass. The named-primitive escape is the single largest paper-close surface; V1 must encode the (a)-(d) machine-checked gate so a tree-walk that inflates the flat scan into a combinator descent (the genuine tension) is REJECTED before close. |
| `3D-D12-r16-relocated-seam-cogate` (NEW): V1 proposal that the relocated-seam risk (un-forking the visible `RuntimeEmitterKind` enum while leaving a per-grammar branch in a neutral data table) is caught ONLY by the structural `runtime_target_rows_collapsed` co-gate — the R16 +1-line `RuntimeTarget: PartialEq` full-row derive recursing into both `frontend_requirements` (#11) and `output_labels` (#12) — never by arm-grep. | T-P1 `G-12`, `D-3`, R16 row-collapse mechanism (`restart/audit/totality/p1/1D-skinny-lessons.md:208`, `:94`-`98`, `:151`-`154`); T-P2 3-co-gate conjunction (`restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:59`-`63`). | `LOCKS.md` Lock 6 regen / Lock 14 generated-provider; `ARCHITECTURE.md` generator dispatch surface; SK-V18 close conditions 2/3 (`restart/skinny/tranches/sk-v18/SPEC.md:71`-`85`). | The distinct-grammar-output addendum is a 3-co-gate CONJUNCTION, NOT an md5 check: {md5-distinct ∧ `branch_count==0` ∧ `type_count==0` ∧ `rows_collapsed==true`}. V1 must require the structural co-gate because md5-distinctness alone proves nothing about the un-fork (a REFUTED assertion per the dispatch); the seam can relocate into a neutral identifier table that arm-grep cannot see (cross-ref 3C `D-SKV18-L05-L10-unfork`, the relocated-seam co-gate clause — CH1-V1-C3-xref). COST SCOPE (CH4-V1): the skinny P3 cost is the +1-line `RuntimeTarget: PartialEq` derive; the totality 9-grammar full-row scaling (recursing into all 9 `idents` rows) is the SK-V19 R16 work (≈+217 LOC, cross-ref 3B MP.SK19.UNFORK / 3A-D11b / 3C D-SKV18-L13), NOT charged to the skinny +1-line. |

## Consequences And CH4 Coverage

| delta | LOC | propagation count | risk | wave alignment | consumer/gate | hard-cap fit | fail action |
|---|---:|---:|---|---|---|---|---|
| `3D-D01` | 0-120 doc | 3 | medium | W0/H1 guard | Pass Omega CRUD guard review + H1 re-lock guard | Fits doc-only T-P3; no implementation overflow. | Pass Omega blocks overgeneralized wording; G-Omega amends before CRUD. CSS DIRECTIONAL until H1 cites a re-locked row. |
| `3D-D02` | 60-180 | 4 | medium | P2 (pruned) | Standing broadcast-detection gate; LOCKS/MIGRATION receivers | Fits; P2 already deleted the warm bench. | Standing gate rejects hidden one-to-N stamps; intrinsic-block or REDRESS any live touch. |
| `3D-D03` | grammar-DERIVED CSS body in G2 + retime | 4 | high | G2/H1 | grammar-DERIVED body proof + same-run `track1_rich/lightningcss` retime | Fits G2 scope; courier retires only same-wave-as/after body proof. | Intrinsic-block CSS close, REDRESS/revert premature courier delete, or G-Omega wave amendment. |
| `3D-D04` | 120-340 | 3 | high | P4 (BEFORE G2/G3) | Lock 14/16 gate with included/excluded roots + re-inject falsifier | Fits P4 gate scope; no hidden runtime repair. | Self-exempting scan rejects; intrinsic-block or G-Omega gate amendment. |
| `3D-D05` | 0 SK-V18 (SK-V19 census carry) | 4 | high (regression) | SK-V19 entry | Pattern-H provenance + md5-distinctness census across 9 grammars | Fits proposal-only; no SK-V18 implementation. | Non-fit becomes intrinsic-block at SK-V19, REDRESS/revert live deletes, or G-Omega wave amendment. |
| `3D-D06` | doc + selection-depth proof in G3 | 4 | high | G3 | Decision spine, e-graph rewrite count, all-five gate, Sheets-tower depth | Fits G3 dispatch scope; five-shape canon preserved. | Gate-consumed rejection, intrinsic-block, or G-Omega for sixth-shape/directive/BIR. |
| `3D-D07` | 80-220 | 3 | medium | standing Lock-16 | Lock 16 manifest: bench-quarantine + non-equality-telemetry rows | Fits doc/gate scope. | FNV never an equality/substrate/identity arbiter; block production equality use or REDRESS/revert. |
| `3D-D08` | 80-350 per primitive/consumer row | 3 | high | G5/G6 (one seam with G2) | Lock 1 substrate gate + Lock 16 primitive manifest; non-`#[cfg(test)]` caller census | Fits per named primitive; no sixth shape, no retained sidecar, no wire-as-is. | Intrinsic-block retained-sidecar/wire-as-is drift; REDRESS items 51/53/246/247 fence; G-Omega for new substrate. |
| `3D-D09` | 120-400 per receiver/gate | 4 | high | PROVE | Lock 14 future-grammar gate requiring CSS plus Sheets; negative-control `N` route | Fits per receiver/gate; fleet wording waits for the Sheets witness. | Sheets shim ⇒ `N` (generalization NOT real), surfaced honestly; intrinsic-block fleet claim; G-Omega receiver amendment. |
| `3D-D10` | 0 T-P3 implementation | 3 | high (regression) | P1-P5/G1-G6/PROVE/H1 | Pass Omega CRUD, G-Omega dispatch, SK-V18 entry/exit-gate chain | Fits proposal-only T-P3; implementation cost lives in the waves with no overflow route. | Entry-gate blocks dispatch; REDRESS/revert skipped prune/retire proof; net LOC ≈ −10800. |
| `3D-D11` | 0-200 doc + (a)-(d) gate predicates | 4 | high | G1/G2/G3/G6 | §6 named-primitive (a)-(d) machine-checked gate; tree-walk-regression falsifier | Fits doc/gate scope; the four predicates are machine-checked, not prose. | Tree-walk that regresses the flat scan REJECTS; intrinsic-block paper-close; G-Omega for any new primitive class. |
| `3D-D12` | skinny P3: +1-line `RuntimeTarget: PartialEq` derive + co-gate doc (the totality 9-row scaling is SK-V19 R16 ≈+217, cross-ref 3B MP.SK19.UNFORK — NOT charged here) | 3 | high | P3/G3 (skinny); SK-V19 (9-row scaling) | 3-co-gate conjunction {md5-distinct ∧ branch==0 ∧ type==0 ∧ rows_collapsed} | Fits P3 row-collapse + G3 un-fork scope; SK-V19 owns the 9-grammar scaling. | Relocated seam in a neutral table REJECTS via the structural co-gate; arm-grep alone is non-evidence. |

## Cost And Non-Fit Fold

| receiver | cost posture | non-fit disposition |
|---|---|---|
| W-PRUNE (P1-P5) | Pure deletion + gate-tightening; net LOC ≈ −10800 headline (P1 x86 ≈ −4500, P2 warm bench ≈ −700, P3 ≈ −5500 [= −5460 replica bodies + ~−40 collapsed rows + 1 `PartialEq` derive, per `restart/skinny/tranches/sk-v18/SPEC.md:435`], P4 gate-fix ≈ +15, P5 metalang rename ≈ 0) — the P1+P2+P3+P4+P5 components sum to the SPEC-authoritative per-wave −10685 under the ≈ tilde to the −10800 headline (matching the figure 3B §13.7-P3 / 3B-D04 / 3F-MH-009 already carry). A `P` wave carries zero generalization risk and deletes no >SOTA-bearing code (`restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:150`-`179`; `restart/skinny/tranches/sk-v18/SPEC.md:232`-`234`). | A prune that would delete >SOTA-bearing code is REDRESS/revert; P4 that cannot prove coverage via the re-inject falsifier is intrinsic-block. |
| G1-G3 emitter rebuild | G1 JSON projection (diff-match `json_templates/` byte-for-byte BEFORE oracle delete), G2 CSS lowering (grammar-DERIVED body + named primitive, doubles as G6 seam), G3 un-fork (DELETE `RuntimeEmitterKind`, dispatch on lowered `BackendShape`). Carrier costs reconcile through 3B/G-Omega (`restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:115`-`132`). | A projection that fails the byte-for-byte diff-match is a re-stringification, not a real projection — REDRESS; an emitter that relocates the seam fails the R16 co-gate (3D-D12). |
| G4-G6 trait/scan/NEON | G4 thin `Cursor`/`DocumentView` trait + DELETE `<G>` (preserve JSON rich nav by the byte-equal falsifier, NOT by-construction), G5 retire zero-sampled `json/scan.rs`, G6 RETARGET NEON onto the live recursive shell with a non-`#[cfg(test)]` caller census + `simd_admission_profile_sampled` (`restart/skinny/tranches/sk-v18/SPEC.md:87`-`97`, `:154`-`165`). | An LCD-flatten of JSON's rich navigation is §5-risk-4 REJECT; a wire-as-is of the dead flat kernel is REDRESS (the hot leaf recurses); a source-census-only NEON claim without the profile conjunct is `dead`, not `admission`. |
| PROVE / H1 | PROVE: Sheets via the generator ONLY (negative control); a shim ⇒ `N`. H1: quiet `css_canon_bench` re-capture (`host_loadavg < 1.0`), CSS framing disclosed `lazy-rich-vs-eager-cssom` (`restart/skinny/tranches/sk-v18/SPEC.md:143`-`152`, `:187`-`205`). | A Sheets stub-prove is forbidden (surface `N` honestly); an H1 figure under `host_loadavg >= 1.0` is `S` (directional residual), never `A`; an unqualified "beats CSSOM" behind a re-label without the materialization-depth asymmetry disclosed is REJECT (R-A0-1). |

## Open Questions

| challenge lens | question | receiver | blocker | gate |
|---|---|---|---|---|
| CH1 correctness | Which exact V1 prose lines still state or imply the one-generator EXISTS, or carry an un-caveated "CSS MEASUREMENT-VALID" closure after Pass Omega CRUD? | Pass Omega CRUD receiver for ARCHITECTURE, LOCKS, MASTER, MIGRATION, HANDOFF. | Line-level wording cannot be named until 3A/3B/3C/3E/3F final V4 proposal text freezes. | Pass Omega path:line CRUD review + G-Omega application gate; every affected line resolves to forked-courier-until-G3 or CSS-DIRECTIONAL-until-H1 (`restart/audit/totality/p3/T-P3-DISPATCH-CONTEXT.md:63`-`69`). |
| CH2 generality | Is Sheets sufficient as the SOLE SK-V18 non-JSON negative control, or must BBNF-self also gate fleet wording? | 3E grammar-generalisation receiver + Lock 14 future-grammar gate. | The SK-V18 SPEC names Sheets as the PROVE control; BBNF-self is a 3E/SK-V19 receiver, not a SK-V18 wave. | Lock 14 gate: fleet wording requires CSS plus Sheets (SK-V18) or an intrinsic-block record; BBNF-self deferred to the SK-V19 fold (cross-ref 3E 3E-D08). |
| CH3 regression | How should Pass Omega carry the SK-V18 REDRESS four-item pre-block (items 51/53/246/247) alongside the prior SK-V15 REDRESS rows? | Pass Omega CRUD + SK-V18 REDRESS pre-block receiver. | The SK-V16/V17 REDRESS coverage is UNKNOWN (1D `U-5`); the committed ledger ends at SK-V15 W11, so an SK-V16/V17-era streamed-cursor/second-scanner reject is structurally invisible to the four-item pre-block. | CH3-V1-R2 RETIME: the pre-block gate fences the second-scanner/structural-stream routes at path:line; the SK-V16/V17 uncaptured-reversal reconcile is a **Pass-Omega-V6 / pre-W-PRUNE blocker** (per U-5's "before Pass Omega ratification"), NOT an SK-V19-entry obligation — because SK-V18 G2/G4/G6 abut items 51/53/247 and run during SK-V18. G2/G4/G6 entry is BLOCKED until the SK-V16/V17 pre-block reconcile is on the committed ledger (3D-D08 / 3F-MH-003) (`restart/audit/totality/p1/1D-skinny-lessons.md:244`-`248`,`:171`). |
| CH4 cost | Does the SK-V18 net −10800 LOC PRUNE-before-rebuild fit the wave caps without hiding GENERALIZE work in the PRUNE deletions? | 3B MASTER cost reconciliation, mirrored by 3D/3E. | The PRUNE deletes far more than the campaign adds, but G1-G3 emitter-rebuild carriers must reconcile through 3B/G-Omega. | 3B cost matrix names per-wave LOC, consumer/gate, hard-cap fit, fail action, and no-overflow route; non-fit becomes intrinsic-block, REDRESS, or G-Omega amendment (`restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:115`-`132`). |
| CH5 hidden coupling | How will G6 prove the NEON kernel reached the hot path AT ADMISSION rather than as a dead source-census-only caller? | 3C Lock 1/Lock 16 receiver + G6 wave. | The dead `find_css_significant` kernel is `#[cfg(test)]`-only and was written FLAT; the hot leaf recurses, so a wire-as-is fails. | Both binding conjuncts: (i) non-`#[cfg(test)]` `generated.rs` caller census ∧ (ii) `simd_admission_profile_sampled == true` (non-zero self-time in the `css_canon_bench` sample); a census-only proof is `dead`, not `admission` (`restart/skinny/tranches/sk-v18/SPEC.md:158`-`165`; `restart/audit/totality/p2/2E-host-arch-esoterica.md:80`). |
| CH6 anti-paper-close | What is the minimum evidence bundle for the §6 named-primitive (a)-(d) gate so the largest paper-close surface stays machine-checked, not prose? | PROVE/close-audit receiver with 3F handoff wording. | The §6 escape (1D `G-11`, R-A0-3) is prose-reviewed today, not machine-checked. | (a) grammar-INVOKED-by-name ∧ (b) output VARIES under invoking-rule mutation ∧ (c) `verbatim_blob_present==false` ∧ (d) PROFILE-PROVEN-NARROW-LEAF, each machine-checked; the `css_balanced_component_scan` neutrality is the FORCED-honest CSS-scoped rename, not a fabricated cross-grammar caller (`restart/audit/totality/p2/2C-grammar-neutrality.md:215`). |
| CH7 SK-V18 addendum | Where should the six SK-V18 addenda (verbatim-blob, distinct-grammar-output 3-co-gate, single-emitter-path, phantom-generic, timed-plane-symmetry, acceleration-wiring) surface in V1 after CRUD? | 3C/3F proposal receivers + Pass Omega CRUD. | Surface placement depends on accepted LOCKS and HANDOFF/MIGRATION wording after V4. | CH7 addendum gate exposes all six addenda + the R16 structural co-gate + the §6 (a)-(d) escape before G-Omega authorizes implementation (`restart/skinny/tranches/sk-v18/audit-overfit/SYNTHESIS-AUDIT-OVERFIT.md:48`-`71`). |
