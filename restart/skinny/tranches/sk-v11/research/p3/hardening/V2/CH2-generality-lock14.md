# SK-V11 S-P3 V2 CH2: Generality / Lock 14

Pass: S-P3 Synthesis-Plan.
Cycle: V2.
Lens: CH2 GENERALITY / Lock 14.
Date: 2026-05-20.
Output: this file.
Scope: evaluate whether the committed V2 packet's W1a/W1b/W2 split still makes non-JSON generated direct/typed measurement executable, blocks JSON-only policy leaks, and keeps CSS/Sheets/BBNF-self measurable rather than prose.
Disposition: ACCEPT.

## Verdict

ACCEPT.

V2 satisfies CH2. The orchestrator's CH2 lens requires Lock 14 to hold: no grammar-name leak, and candidate plans must work for CSS L4 / Sheets / BBNF-self rather than only JSON (`restart/prompts/ORCHESTRATOR.md:81-88`). The S-P3 prompt specializes that to a SPEC generality gate requiring a non-JSON proof for generic-crate edits and failing any wave that lets JSON policy enter a generic crate (`restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md:116-120`). S-P2 convergence makes the same point load-bearing for S-P3: non-JSON generality must be measured through a generated direct/typed parser; prose or JSON-only telemetry is insufficient (`restart/skinny/tranches/sk-v11/research/p2/hardening/HARDENING-S-P2-CONVERGED.md:23-32`).

The V2 packet carries that obligation through W1a, W1b, and W2 without turning Lock 14 into a documentation claim.

## Checks

| Check | Assessment | Evidence |
|---|---|---|
| W1a is gate/report infrastructure, not a disguised non-JSON proof | ACCEPT | W1a is scoped to C9 accounting and Lock 14 gate/report infrastructure with no parser row movement (`restart/skinny/tranches/sk-v11/SPEC.md:283-286`). Its tasks require the gate/report path to consume non-JSON benchmark evidence, add rejecting fixtures for grammar id, domain, output plane, comparator/oracle, Track 2/oracle independence, run id, host, feature mask, same-wave consumer class, and producer-only telemetry, while keeping JSON `gate-json` green (`restart/skinny/tranches/sk-v11/SPEC.md:299-307`). Its exit gate rejects missing required non-JSON fields and producer-only telemetry, forbids JSON row movement, and explicitly claims no generated non-JSON baseline authority (`restart/skinny/tranches/sk-v11/SPEC.md:308-315`). |
| W1b creates a generated non-JSON baseline plus independent oracle | ACCEPT | W1b is now a separate generated non-JSON baseline and oracle lane, with no intervention admission (`restart/skinny/tranches/sk-v11/SPEC.md:326-330`). Its entry gate selects exactly one target, preferring CSS L4 declaration values, then Sheets, then BBNF-self, and names the independent oracle/Track 2 path (`restart/skinny/tranches/sk-v11/SPEC.md:345-347`). Its exit gate requires generated Track 1 for the selected non-JSON workload, independent oracle or Track 2, strict output equality, baseline throughput with run id/host/flags/sample count/output plane/oracle status, no JSON policy in generic crates or runtime outside generated per-grammar modules, and no behavior row admission (`restart/skinny/tranches/sk-v11/SPEC.md:357-367`). |
| W2 consumes the baseline for an admitted generated direct/typed intervention | ACCEPT | W2 cannot create the first measurable non-JSON row; it consumes W1b's baseline (`restart/skinny/tranches/sk-v11/SPEC.md:402-409`). Its entry gate names one generated non-JSON direct or typed intervention, the scalar oracle, independent Track 2/oracle, baseline Mbps, target threshold, and generic-codegen Lock 14 proof (`restart/skinny/tranches/sk-v11/SPEC.md:397-400`). The exit gate requires generated non-JSON Track 1 and independent Track 2/oracle for the selected workload, strict output equality, at least `ceil(W1b_css_baseline_mbps * 1.01)`, strict scalar differential/checkasm for any SIMD body, JSON guard floors if refreshed, and no JSON policy in generic crates or runtime outside generated per-grammar code (`restart/skinny/tranches/sk-v11/SPEC.md:411-421`). |
| JSON-only policy leaks are blocked at the right boundary | ACCEPT | The global SPEC non-negotiables forbid JSON policy in generic crates or runtime outside generated per-grammar modules and require every generic/codegen/runtime-outside-JSON edit to carry a same-wave CSS L4, Sheets, or BBNF-self proof (`restart/skinny/tranches/sk-v11/SPEC.md:163-183`). The Lock 14 gate forbids generic branches selecting JSON/corpus/object/array/field/layout roles; requires byte-set, delimiter, terminator, escape, numeric, and dispatch facts to be generated metadata; and makes the live `json_provider` path a replace/bypass/untouched gate before any non-JSON generality claim passes (`restart/skinny/tranches/sk-v11/SPEC.md:229-245`). This matches the S-P2 carry-forward risk that current codegen still calls `json_provider::ensure_runtime_profile` and must replace it with a grammar-agnostic generated-runtime template before CSS/Sheets/BBNF-self claims (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:10-17`, `restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:111-113`). |
| CSS/Sheets/BBNF-self are measurable surfaces, not prose placeholders | ACCEPT | P2-F requires a generated non-JSON direct/typed benchmark before any C1-C7 S-P3 shortlist; its preferred/fallback target order is CSS L4 declaration values, Sheets formulas, then BBNF-self, with required generated Track 1 before/after throughput, independent Track 2/oracle throughput, strict output equality, primitive self-time, c/B where available, checkasm/parity where applicable, fallback behavior, no source sidecar allocation, no generic-crate grammar names, and same-wave generated non-JSON consumer (`restart/skinny/tranches/sk-v11/research/p2/p2f-grammar-neutral.md:70-91`). P3-C carries this into executable gates: non-JSON claims reject unless they name a generated row, independent oracle/Track 2, before/after Mbps, and gate-consumed grammar id (`restart/skinny/tranches/sk-v11/research/p3/p3c-falsifiability-gates.md:50-57`), while W1b and W2 get concrete gate rows with strict equality, throughput, oracle independence, and gate consumption (`restart/skinny/tranches/sk-v11/research/p3/p3c-falsifiability-gates.md:75-80`). P3-D binds `css_l4`, `sheets`, and `bbnf_self` as allowed grammar/domain values and requires non-JSON grammar id, workload, comparator/oracle, Track 1, Track 2 or independent oracle, profile artifact, strict output proof, and same-wave consumer to be gate-consumed (`restart/skinny/tranches/sk-v11/research/p3/p3d-telemetry-schema.md:154-172`, `restart/skinny/tranches/sk-v11/research/p3/p3d-telemetry-schema.md:257-261`). |
| W1a/W1b/W2 sequencing preserves attribution | ACCEPT | P3-B orders W1a before W1b because non-JSON telemetry must be gate-consumed before a generated baseline row can become authority, W1b before W2 because a baseline and independent oracle must exist before an intervention can claim improvement, and W2 before later generic C1-C7 behavior waves because SK-V11 requires exercised non-JSON generality rather than prose (`restart/skinny/tranches/sk-v11/research/p3/p3b-wave-sequencing.md:78-86`). The dispatch prompt repeats the same dependency order: W1a gate-consumption first, W1b exactly one generated non-JSON baseline plus independent oracle, W2 the preferred non-JSON admitted intervention consuming that baseline (`restart/skinny/tranches/sk-v11/DISPATCH-PROMPT.md:65-72`). |

## Residual Watch Item

No V2 REVISE is required. If W1b selects Sheets or BBNF-self instead of CSS, the wave CHALLENGE must demand the same concreteness the V2 packet gives CSS: selected generated Track 1 path, independent oracle/Track 2 path, output plane, strict semantic equality, baseline Mbps, W2 improvement threshold or measured fallback disposition, gate command, and same-wave generated direct/typed consumer. V2 already has the necessary hooks through W1b selection, P3-D allowed values, P3-C unmeasurable-gate rejection, and SPEC §2.2, so this remains a watch item rather than a revision.

