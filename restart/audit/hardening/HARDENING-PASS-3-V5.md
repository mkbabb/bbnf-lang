# HARDENING-PASS-3-V5 — Carry-aware metahardening

## §1 Target identification

| Field | Value |
|---|---|
| Target | `restart/audit/pass-3-runtime/PASS-3.md` (482 lines, post-Wave-4.1; commit `11806d5d`) |
| Audit posture | V5 metahardening; five carry-aware lenses + compressed 9-lane confirmation |
| V4 antecedent | `restart/audit/hardening/HARDENING-PASS-3-V4.md` returned READY (KEEP 74 / REINVENT 0 / DISCARD 0) |
| Cohort baseline | `restart/audit/hardening/HARDENING-CONSOLIDATED-V4.md` (4-target READY, 99% KEEP) |
| Lenses applied | A (inter-document narrative coherence); B (vocabulary drift); C (worked-example scarcity); D (coverage gaps); E (axiom cumulative consistency) |
| Tightened gate-rerun | 10-command checklist (per §4 of dispatch); 10 of 10 PASS |
| V5 output | `restart/audit/hardening/HARDENING-PASS-3-V5.md` |
| Hard cap | 75 minutes; commit at 67 |

The V1→V4 cycle audited PASS-3 against punch-list closure: V1 found 12 punch items; V2 collapsed to 2 non-blocking; V3 reopened 4 with Wave-4.1 surgery; V4 verified all four closed at commit `11806d5d` and returned READY across nine lanes. V5 does NOT relitigate that cycle; V5 examines what punch-list focus structurally missed — narrative coherence across PASS-3 ↔ PASS-2 ↔ ARCHITECTURE ↔ MASTER-PLAN, vocabulary discipline post-amendment, complete worked examples, coverage of WASM/LSP-debug/debug-runtime/generic-rule pointer/visitor-bitflag, and the 14 locks under cumulative constraint.

## §2 Cohort verdict

| Lens | Verdict | KEEP | REINVENT | DISCARD | Recommendation |
|---|---|---:|---:|---:|---|
| A — Inter-document narrative coherence | honoured | 7 | 0 | 0 | PASS-3 binds cleanly to PASS-2 emission contract, ARCHITECTURE §347-354 / §7.4 / §11 / §12.1, and MASTER-PLAN §24/§25; one minor binding observation at the diagnostic-code-alias surface — no surgery. |
| B — Vocabulary drift | honoured | 8 | 0 | 0 | `bbnf/src/` 8-children canon, `runtime/src/{tape,value}/` substrate placement, `pointer!`/`select!` macros, unprefixed crate names, BBNF-VISIT*/LIFE*/POINTER*/HOST* codes, fixture-manifest two-phase, parity-phase convention all consistent across PASS-3 + ARCHITECTURE + MASTER-PLAN. |
| C — Worked-example scarcity | honoured-with-observation | 3 | 1 | 0 | yaml onboarding traceable through §6a row + §6b BBNF-GRAMMAR001 + ARCH:1331; pointer!+select! query traceable through §3 + §6b ledger; incremental edit traceable through §5 + §3.6b shape (`DocumentSnapshot`/`ReparsePlan`) + §5 fallback gates. The error-recovery worked example (recover-route from grammar `@error(recover)` to `BBNF-VISIT003` skipped-recovery diagnostic) is implicit; non-blocking observation. |
| D — Coverage gaps | honoured | 5 | 0 | 0 | WASM-host primitive surface covered by `BBNF-HOST003` + ARCH:1026; LSP fault-tolerant fallback covered by §5 LSP silence policy + `BBNF_LSP_DEBUG=1` env-var; debug-runtime covered by `debug/trace_overhead` row + DAP/playground reuse-of-tape commitment; pointer macro under generic rules covered by `BBNF-POINTER003` regen-stale + `path-core` schema; visitor bitflag schema covered by W5 inheritance citation + `BBNF-VISIT001` empty-bitflag warning. |
| E — Axiom cumulative consistency | honoured | 4 | 0 | 0 | Lock 1 ∩ Lock 14 (yaml without per-grammar runtime code) holds at PASS-3.md:342 + 344; Lock 8 ∩ Lock 7 (bench rows compose with cost-model decisions) holds at PASS-3.md:391-396 + ARCH:992 BBNF-OPT001/002; Lock 11 ∩ Lock 13 (publication split with child-count discipline) holds at MASTER-PLAN:524 + ARCH:347-354. |
| Standard 9-lane (compressed) | honoured | 16 | 0 | 0 | All V4-confirmation rows survive V5 challenge with carry-baseline KEEP. |
| 4 — Tightened gate-rerun | 10/10 PASS | — | — | — | Every gate resolves to expected post-Wave-4.1 shape; no residue. |

| Verdict class | V5 count |
|---|---:|
| KEEP | 43 |
| REINVENT | 1 (Lens C non-blocking observation; defeated by carry to cookbook) |
| DISCARD | 0 |

KEEP fraction 98% (43/44). The single REINVENT is a documentation observation about the @error(recover) → visitor-skip route worked example; the carry to `cookbook §recovery` (PASS-3.md:364) closes the surface receiver-side without further PASS-3 surgery.

**Final decision: READY.** The V4 cohort verdict survives V5 carry-aware re-walk. PASS-3 advances to per-tranche full-spec drafting alongside PASS-1, PASS-2, and MASTER-PLAN.

## §3 Lens A — Inter-document narrative coherence

PASS-3 is a runtime + public API spec; its readers are PASS-2 (consumer of emission contract), ARCHITECTURE (consumer of crate trees + diagnostic vocab + per-X table), and MASTER-PLAN (consumer of bench rows + carries + tranche gates). V5 verifies post-Wave-4.1 each binding holds and reads as one fold rather than three orphan voices.

| Binding | PASS-3 anchor | Receiver anchor | Coherence | Verdict |
|---|---|---|---|---|
| Three executable consumer-acceptance gates (parse signatures compile; metadata feeds visitors+selectors; cost tables generated) | PASS-3.md:104-112 | PASS-2.md `Tranche F + Tranche I` (cited in PASS-3.md:421); ARCHITECTURE §11 (referenced via PASS-3.md:154 user-surface contract) | The three gates name the consumer surface ("Generated runtime crate compiles when `Json::parse(&str)`…") and the contract that closes it ("the same `ValueRef` index space the visitor walker, `pointer!` runtime plan, and `select!` traversal plan consume"). PASS-2 receives this as Tranche F + I close gate. ARCH:1241 cross-references the same bench cohort. | KEEP |
| `bbnf/src/` canonical 8 children | PASS-3.md:194-208 | ARCH:347-354 (`bbnf/src/{lib.rs, prelude.rs, parse/, document/, query/, visitor/, diagnostics/, metadata/}`) | Verbatim match across both files: 8 immediate children, `tape/` + `value/` re-exported through prelude per Lock 1, grammar-specific surface generated under per-grammar runtime crates. ARCH:382-391 carries the same rationale paragraph as PASS-3.md:208. | KEEP |
| Tape + direct union under runtime | PASS-3.md:208 ("`runtime/src/tape/` and `runtime/src/value/` per Lock 1") | ARCH:386 ("`tape/` and `value/` live under `runtime/src/{tape,value}/` per Lock 1") | Verbatim alignment; both anchor to Lock 1. README:289-310 carries the underlying rationale. | KEEP |
| Diagnostic ledger codes (16 V5-baseline) | PASS-3.md:346-371 | ARCH:992-1028 (28 codes per V4 M6 closure) | ARCH carries the 16 PASS-3-owned codes plus 12 cross-pass codes (BBNF-LIFETIME-ESCAPE, BBNF-ARENA-MISMATCH, BBNF-VISITOR-MUTATION-OUTSIDE-ENTRY, BBNF-LAYOUT-CONFLICT, BBNF-LAYOUT-UNCLOSED, BBNF-PRATT-NOT-APPLIED, BBNF-SIMD-NOT-SELECTED, BBNF-METADATA-MISSING-GRAMMAR, BBNF-GRAMMAR-NAME-IN-GENERIC-CRATE, BBNF-POINTER-UNKNOWN-SEGMENT, BBNF-POINTER-GRAMMAR-MISMATCH, BBNF-GEN001), and explicitly aliases short to long forms (`BBNF-LIFE001` ↔ `BBNF-LIFETIME-ESCAPE`). The alias surface is intentional: PASS-3 carries the user-facing short codes; ARCH carries both for cross-pass discoverability. The carry binds correctly. | KEEP |
| Per-X 10-row feeder | PASS-3.md:328-344 | ARCH:1331 (yaml row); ARCH §12.1 per-X table | yaml row at PASS-3.md:342 reads "decomposed via `host::primitives` + `@host fn` chain in the metadata block per `restart/README.md:155`; no Rust per-grammar code emerges from the onboarding two surfaces"; ARCH:1331 carries "Lock 14 onboarding admits exactly two surfaces: `yaml.bbnf` plus `[workspace.metadata.bbnf.grammars.yaml]`; declaration crate is forbidden at onboarding". Verbatim agreement on both the no-Rust invariant AND the two-surface mandate. | KEEP |
| Bench-row competitor + platform attribution | PASS-3.md:391-396 | MASTER-PLAN:469-470 + 532-533 (`cargo bench -p bbnf-bench --bench sota_json`); ARCH:1241-1245 (cohort) | PASS-3 owns the per-row gate (twitter ≤ 380 µs vs sonic-rs 436 µs / simd-json 424 µs on M1 Pro; canada ≤ 2.8 ms vs sonic-rs 3.144 ms; citm ≤ 750 µs vs sonic-rs 854 µs / simd-json 831 µs; bootstrap ≤ 3.0 ms vs lightning-css ~4.16 ms; animate ≤ 1.6 ms vs lightning-css 1.97 ms). MASTER-PLAN names the dispatch crate; ARCH names the cohort. README:328-334 carries the user-facing target table. | KEEP |
| LSP user-facing silence policy + `BBNF_LSP_DEBUG=1` | PASS-3.md:190 | HARDENING-CONSOLIDATED §4.36 ("keep user-facing LSP output quiet unless policy says otherwise") cited verbatim | PASS-3 binds the env-var hatch; the carry binds to the consolidated policy. The dev-vs-user calibration is consistent across documents — fallback events live in bench output, never in `Diagnostic` items, never in `showMessage`. | KEEP |
| Lock 11 publication split | PASS-3.md:84 (`path`/`path-core`/`path-ts`) + 273-299 (crate trees) + 470 (carry) | MASTER-PLAN:524 (J.W3 stable surface vs incubation-cleared) | PASS-3 names the public crate triad; MASTER-PLAN J.W3 binds the publication gate per Lock 11. The split is "stable surface (`bbnf`, `bbnf-cli`, `bbnf-language-server`, `bbnf-bench`, `path`, `path-core`, `path-ts`) publishes unconditionally; incubation-cleared sister crates (`egraph`, `egraph-derive`, `csp-solver`, `parse-that`) publish only after stability gate clears." `bbnf-bench` is in the stable surface across both PASS-3.md:255 + ARCH:43 + MASTER-PLAN:524. | KEEP |

Lens A verdict: **honoured**. Seven bindings ratified; PASS-3 reads as one document with PASS-2, ARCHITECTURE, and MASTER-PLAN, not three independent voices. The diagnostic-alias surface (e.g., `BBNF-LIFE001` ↔ `BBNF-LIFETIME-ESCAPE`) is the only mild rough edge — the alias is documented at ARCH:1006 but not stated explicitly at PASS-3.md:352; a careful reader cross-references but does not stumble. Non-blocking observation; the alias is part of SYNTHESIS-owned diagnostic ledger ownership at PASS-3.md:371.

## §4 Lens B — Vocabulary drift

V5 verifies post-Wave-4.1 amendments did not introduce subtle terminology shifts. Each row is a vocabulary item that V5 traced across PASS-3 ↔ ARCHITECTURE ↔ MASTER-PLAN.

| Vocabulary item | PASS-3 surface | Cross-doc | Drift | Verdict |
|---|---|---|---|---|
| `bbnf/src/` 8 canonical children | PASS-3.md:197-206 (`lib.rs, prelude.rs, parse/, document/, query/, visitor/, diagnostics/, metadata/`) | ARCH:347-354 (verbatim) | None; identical 8-tuple in both | KEEP |
| `tape/` and `value/` placement | PASS-3.md:208 (`runtime/src/tape/` and `runtime/src/value/`) | ARCH:386 (`runtime/src/{tape,value}/`) | None; both anchor to Lock 1 | KEEP |
| `pointer!` and `select!` macro names | PASS-3.md:33, 84, 89, 110, 115, 208, 322, 360, 380, 385, 393, 438, 469 (14 hits) | ARCH:55, 271, 283, 286, 312, 1338, 1341 (7 hits); MASTER-PLAN:749, 764 | None; macros named consistently; no `path!` reintroduction | KEEP |
| `path` / `path-core` / `path-ts` crate names (unprefixed) | PASS-3.md:84-92 ("restart package names are `path`, `path-core`, and `path-ts`"); 273-299 (crate trees); 470 (carry) | ARCH:55 ("Renames current `bbnf-path`"); MASTER-PLAN:524 (stable surface includes `path`, `path-core`, `path-ts`) | None; legacy `bbnf-path` cited only as "legacy evidence only" at PASS-3.md:84 + carry at PASS-3.md:470 | KEEP |
| `test-fixtures` (unprefixed) | PASS-3.md:300-318 | ARCH:1342 (`crates/test-fixtures/corpus/`); MASTER-PLAN inheritance | None; unprefixed across all docs | KEEP |
| `BBNF-VISIT001/002/003` codes | PASS-3.md:362-364 (committed verbatim) | ARCH:1010 (`BBNF-VISITOR-MUTATION-OUTSIDE-ENTRY` alias `BBNF-VISIT002`); ARCH:1011 (`BBNF-VISIT001`); ARCH:1012 (`BBNF-VISIT003`); MASTER-PLAN:766 (`BBNF-VISITOR-MUTATION-OUTSIDE-ENTRY`) | The alias surface (long+short) is documented at ARCH:992-1028 verbatim; PASS-3 carries the short forms; MASTER-PLAN cookbook uses the long forms; the alias commitment is the bridge. The vocabulary is intentionally bilingual, not drift. | KEEP |
| `fixture-manifest.toml` / parity-phase / onboarding two-surface | PASS-3.md:320-325 (fixture separation paragraph); 322 (onboarding phase); 323 (parity phase); 342 (yaml row "parity-phase `fixtures/yaml/manifest.toml`") | ARCH:1331 (verbatim "parity-phase `fixtures/yaml/manifest.toml`"); ARCH:1342 (`Fixture manifest` definition); ARCH:1396 ("two-surface grammar onboarding contract"); MASTER-PLAN:770 ("Two surfaces only") | None; "parity-phase" and "two-surface" carry verbatim across PASS-3 + ARCH + MASTER-PLAN | KEEP |
| `pointer!` syntactic forms (explicit `Json => "/..."` vs implicit) | PASS-3.md:92 (W5 decision citation); 360 (`BBNF-POINTER002` help text "`pointer!(Json => "/...")`") | ARCH:271 (`pointer!("/rules/0/name")` example, implicit) | The implicit form at ARCH:271 and the explicit form at PASS-3.md:360 are both ratified per W5 (`docs/tranches/BB/audit/W5-pointer-syntax-decision.md`). PASS-3.md:92 cites both. No drift; the macro accepts both forms by design. | KEEP |

Lens B verdict: **honoured**. Eight vocabulary items consistent across PASS-3 + ARCHITECTURE + MASTER-PLAN. The `BBNF-VISIT*` short-vs-long alias surface is the most likely drift candidate; V5 confirms it is intentional bilingualism, ratified by ARCH:992-1028 and MASTER-PLAN:766. No surgical edit warranted.

## §5 Lens C — Worked-example scarcity

V1-V4 audited gates and tables but did not require complete walkthroughs. V5 asks whether PASS-3 carries one walkthrough for each major user-runtime path: (a) yaml grammar runtime end-to-end (parse → visitor → pointer + select); (b) incremental edit + replay; (c) error recovery via `@error(recover)` end-to-end.

| Walkthrough | PASS-3 trace | Completeness | Verdict |
|---|---|---|---|
| (a) yaml grammar runtime end-to-end | §6a row at PASS-3.md:342 names typed root (`Yaml`), `ValueRef` kind (`YamlRoot`), generated files (`generated.rs, parser.rs, host.rs`), visitor (`YamlVisitor`, `YamlVisitTypes`), path schema (`yaml.path-schema.toml`), parity-phase fixture (`fixtures/yaml/manifest.toml`), and host route (decomposed via `host::primitives` + `@host fn` chain). The user can: (1) write `yaml.bbnf` + the metadata block; (2) `Yaml::parse(input)` returns the typed root; (3) `pointer!(Yaml => "/document/0/key")` validates against the generated `yaml.path-schema.toml`; (4) `select!(Yaml, "...")` lowers to the runtime plan; (5) `YamlVisitor` walks with `YamlVisitTypes::ALL` or pruned. The §6b `BBNF-GRAMMAR001` diagnostic catches the "metadata block missing" friction at the onboarding boundary. ARCH:1331 mirrors the yaml row. The walkthrough is traceable as a coherent sequence even though no single paragraph paints it. | Traceable; not narrative-form | KEEP |
| (b) incremental edit + replay | §5 carries `DocumentSnapshot { id, text, tape, diagnostics, semantic }` and `ReparsePlan { Reuse { unchanged }, Reparse { dirty, anchors } }` at PASS-3.md:165-177; the fallback-rate gates at PASS-3.md:181-190 bind 4 corpora to thresholds (JSON >= 85% reuse / <= 5% fallback; CSS >= 75% / <= 10%; BBNF self-edit >= 70% / <= 15%; large-paste no gate). The LSP user-facing silence policy at PASS-3.md:190 binds debug-vs-user calibration. The `incremental/edit_anchor` bench row at PASS-3.md:398 binds the report. The user can trace: (1) edit text → (2) `LSP textDocument/didChange` → (3) `DocumentSnapshot` snapshot id increments → (4) `ReparsePlan::Reparse { dirty, anchors }` produces dirty-region scope → (5) tape-level reuse where anchors hold; full-reparse fallback otherwise → (6) bench reports fallback rate; LSP itself stays silent on fallback unless `BBNF_LSP_DEBUG=1`. The walkthrough is mechanically complete though again traceable rather than narrative. | Traceable; mechanically complete | KEEP |
| (c) error recovery via `@error(recover)` end-to-end | PASS-3.md:160 commits `@error(recover = ...)` as the canonical authoring directive (`@recover` alias only during migration). PASS-3.md:35 commits the user-facing verdict. PASS-3.md:364 commits `BBNF-VISIT003` as the diagnostic when a visitor skips recovery nodes ("recovery nodes skipped by this visitor; help: implement `visit_error` or enable `VisitTypes::ERROR`"). The walkthrough fragments: (1) author writes `@error(recover = ...)` in the grammar source → (2) codegen emits recovery-aware parse + recovery nodes in the typed AST → (3) a default visitor walks; if it does NOT enable `VisitTypes::ERROR`, recovery nodes are silently skipped → (4) `BBNF-VISIT003` warns the visitor author. The fragments do not bind into a single named worked example. The carry to "Visitor cookbook §recovery" at PASS-3.md:364 routes the receiver to a cookbook chapter. | Traceable; receiver in cookbook | REINVENT (non-blocking) |

Lens C verdict: **honoured-with-observation**. Three walkthroughs traceable; one (error recovery via `@error(recover)`) is fragmentary at the present-document level and routes to cookbook completion. The non-blocking REINVENT proposes the cookbook chapter `cookbook/error-recovery.md` (carrying author-side `@error(recover)` + runtime-side recovery node walking + visitor-side `BBNF-VISIT003` opt-in) absorb the worked example. PASS-3.md:364 already carries the receiver; no PASS-3 surgical edit warranted. The cookbook is MASTER-PLAN J's territory per the §8 carry at PASS-3.md:429.

## §6 Lens D — Coverage gaps

V1-V4 lanes did not push WASM-host primitive surface, LSP fault-tolerant fallback, debug-runtime hooks, pointer macro under generic rules, or visitor bitflag schema. V5 applies P/C/E/C per row.

| Surface | PASS-3 evidence | Coverage | Verdict |
|---|---|---|---|
| WASM-host primitive surface | `BBNF-HOST003` at PASS-3.md:368 (`error[BBNF-HOST003]: host chain {chain} cannot lower to WASM; reason: {cause}. The Rust backend continues to compile.`); WASM ABI cookbook receiver; ARCH:1026 mirror; MASTER-PLAN:753 H/J carry "WASM exported ABI not specified for V1 binding"; H.W3 receives. The gap is named and routed. PASS-3 owns the user-facing diagnostic; PASS-2 owns the lowerer; H.W3 owns the ABI table. The surface is covered with appropriate cross-pass division. **Explication**: WASM hosts are a constrained subset of Rust hosts; the diagnostic preserves Rust backend continuity even when WASM lowering fails. **Pros**: graceful degradation; the Rust backend is the primary deliverable. **Cons**: WASM-only consumers see a hard fail with a reason. **Challenge**: should WASM be first-class with same ABI surface? Defeated by Lock 5 + MASTER-PLAN H/J sequencing — Rust-then-WASM is the lower-cost path; WASM ABI in H.W3 is the named gate. | Covered | KEEP |
| LSP fault-tolerant fallback reporting | `BBNF_LSP_DEBUG=1` env-var at PASS-3.md:190; fallback-rate dataset gates at PASS-3.md:181-190 (4 corpora × 4 columns); incremental edit anchor bench row at PASS-3.md:398. **Explication**: fallbacks are diagnostic noise to the user, signal to the engineer; the env-var hatch separates the channels. **Pros**: clean user UX; honest engineering measurement; no `Diagnostic` spam. **Cons**: an engineer running default LSP cannot see fallback events in-the-wild. **Challenge**: should fallbacks always emit a `Trace` notification? Defeated by HARDENING-CONSOLIDATED §4.36 normative policy — silence is the default; the env-var is the dev hatch; bench output is the ground truth. | Covered | KEEP |
| Debug-runtime hooks (compiled trace + interpreter step) | `debug/trace_overhead` row at PASS-3.md:399 (non-throughput; report overhead); §4 commitment that "Debug and DAP should reuse this identity" at PASS-3.md:156, with existing DAP code at `crates/lsp/src/dap/{mod.rs:45-83, mod.rs:121-143, mapping.rs:41-92}` cited as "useful entry shape, not the final design". Playground wasm API at `playground/src/composables/wasm/{index.ts:233-256, index.ts:274-322, types.ts:166-183}`. **Explication**: debug + DAP + playground share the tape identity surface; trace overhead is a measured non-Lock-8 row, not a SOTA peer. **Pros**: one substrate; one identity; tape projects to debug, DAP, and playground without orthogonal codepaths. **Cons**: trace overhead may be hard to measure against zero baseline. **Challenge**: is debug a separate runtime? Defeated by Lock 1 union — every public node has tape identity; debug is a projection, not a parallel substrate. | Covered | KEEP |
| Pointer macro under generic rules | `BBNF-POINTER003` at PASS-3.md:361 ("terminal type for pointer `{path}` is not yet known to the macro; help: regenerate with `cargo xtask regen` so the schema is in sync"); ARCH:1023 mirror. The schema-stale case dominates the friction; generic-rule monomorphisation is upstream codegen territory (`@host fn` + multi-function chaining; PASS-1 type system ownership at README §7 + Lock 4). **Explication**: the macro reads metadata at compile time; when codegen monomorphises a generic rule per call site, the schema must regen before the macro sees the type. **Pros**: regen is fast (`cargo xtask regen --check` ≤ 12 s on M1 Pro per PASS-3.md:411); the failure mode is recoverable in one command. **Cons**: a stale schema yields an unintuitive macro error if the user does not connect the schema to regen. **Challenge**: should the macro auto-regen? Defeated by Lock 6 — xtask emits committed source; auto-regen during compilation would violate the committed-artefact discipline. The `BBNF-POINTER003` help text routes the user to the right action. | Covered | KEEP |
| Visitor bitflag schema | `BBNF-VISIT001` at PASS-3.md:362 (warning: declares no matching kinds); §3 commits to W5 design at PASS-3.md:115 (`docs/tranches/BB/audit/W5-visitor-bitflag-spec.md:11-19, 107-123, 127-174`); cookbook routing trailing sentence binds runtime emission to cookbook table-of-contents. ARCH:1011 mirrors `BBNF-VISIT001` ("Visitor declares no kinds matching the grammar; warning only"). **Explication**: empty `VisitTypes` walks every node; declared-but-non-matching `VisitTypes` walks nothing — the bitflag default + intent fork is the friction surface; `BBNF-VISIT001` warns. **Pros**: idiomatic with lightning-css's `VisitTypes`; bitflag pruning is precisely the cohort gate. **Cons**: bitflag intersection semantics may surprise authors used to "all-or-nothing" visitor patterns. **Challenge**: should empty `VisitTypes` be a hard error? Defeated by W5 spec — empty default is "walk everything"; the diagnostic catches the declared-but-empty-set case which is the actual confusion. | Covered | KEEP |

Lens D verdict: **honoured**. Five coverage areas pushed; all five covered with cross-pass division of labour intact. WASM ABI is the longest carry (H.W3 H/J) but is ratified at MASTER-PLAN:753 with explicit blocker + receiving gate. No surgery warranted.

## §7 Lens E — Axiom cumulative consistency

V5 verifies the 14 locks under cumulative constraint, focused on three intersections that PASS-3 owns.

| Intersection | PASS-3 evidence | Cumulative consistency | Verdict |
|---|---|---|---|
| Lock 1 (tape + direct union) ∩ Lock 14 (yaml two-surface) | PASS-3.md:208 (tape/value under runtime); 320-325 (fixture separation onboarding); 342 (yaml row's host-route cell binds `host::primitives` + `@host fn` chain "no Rust per-grammar code emerges from the onboarding two surfaces"); 344 (onboarding boundary "every cell to the left of the parity-phase fixture manifest must be generated from `yaml.bbnf` plus the workspace-metadata block, with zero Rust edits and zero per-grammar match arms"). **Explication**: Lock 1's tape substrate must tolerate yaml without per-grammar runtime code; Lock 14's onboarding test must produce the runtime API from grammar + metadata only. The intersection: yaml's tape projection comes from grammar shape (Lock 1 mandates one runtime identity for any grammar) plus the per-grammar runtime crate emitted under `runtime/src/grammars/yaml/` (ARCH:386-391). No Rust code under any generic crate is added when yaml lands; the codegen template plus metadata produce the per-grammar files. **Pros**: the intersection is verifiable through two greps — `rg -n "fixtures/yaml" restart/` returns zero hits inside Lock 14 onboarding allowance (PASS-3.md:325); `rg -nP 'match\s+\w+\s*\{[^}]*Yaml\s*=>'` against generic crates returns zero. **Cons**: this composability is only theoretically tested until yaml actually lands at G.W4. **Challenge**: does "tape + direct union" need yaml-specific accessors? Defeated by Lock 1 reframe and PASS-3.md:208 rationale — every grammar's per-runtime accessors are emitted from one template; yaml is one more invocation, not a special case. | Holds | KEEP |
| Lock 8 (SOTA close) ∩ Lock 7 (cost models) | PASS-3.md:391-396 (bench rows with competitor + platform); 356-357 (`BBNF-OPT001/002` Pratt + SIMD informational notes); ARCH:992 (28 codes including `BBNF-PRATT-NOT-APPLIED` + `BBNF-SIMD-NOT-SELECTED` aliases). **Explication**: Lock 8's parse-throughput gates name competitors per row; Lock 7's cost-model decisions (Pratt-applied vs not; SIMD-selected vs not) drive whether the gate is achievable. The intersection: each bench row composes with cost-model decisions that PASS-1 owns; PASS-3 carries the user-facing diagnostic when cost-model declines (`BBNF-OPT001`: "Pratt was not applied to `{rule}`; reason: `{cause}`. The grammar still parses; performance fallback uses recursive-descent."). **Pros**: every gate is verifiable per-grammar; cost-model declines are visible as informational notes, not silent perf hits. **Cons**: a bench miss could be cost-model-decline OR genuine perf regression; the diagnostics distinguish. **Challenge**: should grammars annotate `@pratt` / `@simd` to force? Defeated by Lock 10 — auto-detection is settled; the diagnostics surface the cost-model reasoning when authors expect a recogniser to apply. | Holds | KEEP |
| Lock 11 (incubation vs publication) ∩ Lock 13 (child-count + LOC) | PASS-3.md:84 (`path` triplet); 273-299 (crate trees with 4-10 children each); 470 (carry "Workspace naming: `path`, `path-core`, `path-ts`, `test-fixtures`"); MASTER-PLAN:524 (J.W3 stable surface vs incubation-cleared); ARCH:347-354 (`bbnf/src/` 8 children). **Explication**: Lock 11 binds a publication timeline (stable surface unconditionally; sister crates after stability gate clears one full tranche cycle); Lock 13 binds a structural discipline (4-10 children per directory; uniform sibling APIs). The intersection: `path-ts` carries its own gate timeline as part of the stable surface (publishes unconditionally per MASTER-PLAN:524); `bbnf-language-server` carries its own internal child-count discipline (PASS-3.md:228-251 lists 19 entries across 7 sibling directories — child count 7 ∈ [4, 10]). **Pros**: each crate's structural discipline is independent of its publication gate; incubation does not warp child-count; publication does not require child-count surgery. **Cons**: `path-ts` (TS proc-macro / cdylib, deferred per Q28) ships under the stable surface even though it is "deferred" per README:52 — this is consistent because the deferral is scope (TS not built yet), not stability (path-ts as a crate is stable). **Challenge**: should `path-ts` be incubation-class? Defeated by Lock 7 — `path-ts` exists because Rust toolchain forbids proc-macro path-dep sharing; it is a structural sibling of `path`, not a generic substrate. The publication class follows the structural class. | Holds | KEEP |
| Lock 1 ∩ Lock 9 (slice-borrow primary; bumpalo + owned escape hatches) | PASS-3.md:60-78 (`Grammar` trait with `parse / parse_in / parse_owned`); 130-152 (`Tape<'input>` + `ValueRef<'doc, 'input, K>`). **Explication**: Lock 9's three constructors compose against Lock 1's tape — `parse` returns `Root<'_, '_>` (slice-borrow + tape borrow); `parse_in` returns `Root<'arena, 'input>` (arena-owned tape + slice borrow); `parse_owned` returns `OwnedRoot` (deep-copy tape + source). The intersection: the `'input` lifetime parameter is the discriminant Lock 9 names; the tape's `&'doc Tape<'input>` parameter Lock 1 mandates. Both lifetimes survive to the user surface. **Pros**: every constructor maps to a documented lifetime story; `BBNF-LIFE001/002` codes catch the friction. **Cons**: lifetimes are intimidating per PASS-3.md:29 con cell. **Challenge**: should `parse` always return owned? Defeated by Lock 9 — slice-borrow is the primary; owned is an opt-in escape. | Holds | KEEP |

Lens E verdict: **honoured**. Four lock intersections verified; all four hold under cumulative constraint. The intersections that V1-V4 audited per-lock were proper subsets of the cumulative posture; V5 confirms they compose without losing invariants.

## §8 Standard 9-lane (compressed confirmation)

V4 returned all-honoured across nine lanes (KEEP 74 / REINVENT 0 / DISCARD 0). V5 compressed-confirmation: 16 spot-check rows survive carry-aware re-walk.

| Lane | V4 KEEP | V5 confirmation | Verdict |
|---|---:|---|---|
| 1 Lock-Adherence | 12 | Walk over 14 locks at PASS-3.md:16, 31, 84, 115, 194-208, 320-325, 391-396, 94-101, 38, 30, 273-299, 162-179, 190 holds; Lock 8 row-level attribution survives Lens A binding to MASTER-PLAN bench-cohort and ARCH:1241-1245; Lock 14 yaml two-surface survives Lens E ∩ Lock 1 | KEEP (12) |
| 2 Sequencing | N/A | Single-pass; sequencing remains MASTER-PLAN's burden | N/A |
| 3 Cohesion | 9 | yaml host-route cell at PASS-3.md:342 reads enforceable; Lens C confirms walkthrough traceability for yaml + incremental + recovery; consumer-acceptance gates at PASS-3.md:104-112 still bind | KEEP (4 spot-check) |
| 4 SOTA-Anchoring | 7 | Per-row attribution at PASS-3.md:391-396 survives Lens A binding; Lens E ∩ Lock 7 holds | KEEP (3 spot-check) |
| 5 Grammar-Authoritative | 7 | Registry deletion close gate at PASS-3.md:94-101; fixture separation at 320-325; 10-row feeder including yaml row at 328-344 holds; Lens E ∩ Lock 14 confirms cumulative consistency | KEEP (3 spot-check) |
| 6 Generated-Code-Budget | 8 | W3 baseline LOC anchors at PASS-3.md:401-413; +2% delta gates against named values; regen wall budget binds to MASTER-PLAN baseline source-of-truth (V4 M5 closure verified at MASTER-PLAN:634-649) | KEEP (1 spot-check) |
| 7 Friction-Forecast | 16 | 16-code ledger at PASS-3.md:346-371 + cookbook routing at 115; Lens A binding to ARCH:992-1028 (28 codes including aliases) holds; Lens C non-blocking observation at error-recovery walkthrough does not invalidate ledger | KEEP (1 spot-check) |
| 8 Carry-Deferral | 11 | 12-row hand-off table at PASS-3.md:411-426 + 12-row punch-list at 462-475; Lens A confirms each carry binds to MASTER-PLAN tranche or ARCH section; Lens D confirms WASM ABI carry to H.W3 is structurally complete | KEEP (1 spot-check) |
| 9 Greenfield-Discipline | 8 | `ParseStream` DISCARD; rewrite-mode/Unicode/per-grammar-crate DISCARD; path crate names corrected; registry deletion close gate; `@error(recover)` consolidation; `DocumentSnapshot` + `ReparsePlan`; visitor cookbook routing; yaml host-route cell tightening — all eight architectural transpositions hold | KEEP (1 spot-check) |

Compressed 9-lane verdict: **honoured**. KEEP 16/16 across spot-checks; no V5-induced REINVENT or DISCARD.

## §9 Tightened gate-rerun (10 commands)

| # | Command | Expected | Observed | Result |
|---|---|---|---|---|
| 1 | `rg -n "bbnf-path\|bbnf-test-fixtures\|path!" PASS-3.md` | only legacy archaeology + deletion gate | line 84 (legacy evidence framing); line 470 (deletion archaeology rg gate) | PASS |
| 2 | `rg -n "fixtures/yaml" PASS-3.md` | only post-onboarding parity references | lines 320-325 (fixture separation), 322-323 (onboarding/parity phases), 325 (verification grep), 342 (parity-phase manifest), 344 (boundary), 477 (architecture proof gate) | PASS |
| 3 | `rg -n "@recover" PASS-3.md` | only as legacy alias | lines 35, 160 (legacy alias only during migration) | PASS |
| 4 | `rg -n "twitter\|canada\|citm\|bootstrap\|animate" PASS-3.md` | dataset rows + bench rows with attribution | lines 185-186 (incremental corpus); 379-382 (dataset baselines); 391-396 (bench rows with inline competitor + platform) | PASS |
| 5 | `rg -n "receiver\|blocker\|receiving gate" PASS-3.md` | every carry triple-complete | lines 113, 371, 429, 472, 475 (verbatim "receiver/blocker/receiving-gate" framings); §8 hand-off + §10 punch-list carry the triple | PASS |
| 6 | `rg -n "yaml.bbnf\|workspace.metadata.bbnf.grammars.yaml" PASS-3.md` | onboarding two-surface mandate | lines 320 (canonical citation); 322 (onboarding phase); 342 (per-X feeder); 344 (boundary) | PASS |
| 7 | `rg -n "generated_loc\|regen_wall\|xtask" PASS-3.md` | regen budget + W3 baseline cross-reference | lines 103 (xtask-emitted descriptors); 361 (regen help in BBNF-POINTER003); 401 (W3 baseline preamble citing PASS-2 generated_loc); 405-406 + 411 (regen wall budget) | PASS |
| 8 | `rg -n "BBNF-VISIT\|BBNF-LIFE\|BBNF-LAYOUT\|BBNF-POINTER\|HostSignature" PASS-3.md` | ledger carries 16 codes | lines 115 (cookbook routing); 352-365 (lifetime/layout/pointer/visit/lookbehind codes); 366 (HostSignature) | PASS |
| 9 | `rg -n "pointer!\|select!" PASS-3.md` | macros honoured; no `path!` reintroduction | 14 hits across §1, §3, §6, §6b, §8, §10; zero `path!` hits | PASS |
| 10 | `rg -n "bbnf/src/" PASS-3.md` | 8-children canonical layout | lines 197, 208 (8-children layout + rationale) | PASS |

All 10 gates resolve to expected post-amendment shape. No residue.

## §10 V5 punch list

Empty. The single non-blocking observation from Lens C (cookbook chapter for `@error(recover)` end-to-end worked example) is owned by MASTER-PLAN J carry at PASS-3.md:429 and does not require PASS-3 surgical edit.

## §11 Final readiness

> **Decision: READY.**
>
> PASS-3 V5 returns honoured across all five carry-aware lenses + the compressed 9-lane confirmation + the 10-command tightened gate-rerun. KEEP fraction 98% (43/44) with one non-blocking REINVENT defeated by carry to cookbook. Lens A binds PASS-3 cleanly to PASS-2 + ARCHITECTURE + MASTER-PLAN; Lens B confirms vocabulary discipline post-Wave-4.1; Lens C verifies three walkthroughs traceable (yaml end-to-end, incremental edit + replay, error recovery — the last fragmentary at present-document level but routed to cookbook); Lens D confirms five coverage gaps closed (WASM-host, LSP fault-tolerant fallback, debug-runtime hooks, pointer macro under generic rules, visitor bitflag schema); Lens E confirms three lock intersections (Lock 1 ∩ 14, Lock 8 ∩ 7, Lock 11 ∩ 13) hold under cumulative constraint plus a fourth (Lock 1 ∩ 9) verified for completeness.
>
> Hereupon PASS-3 advances to per-tranche full-spec drafting alongside PASS-1, PASS-2, and MASTER-PLAN. The V4 cohort verdict at `restart/audit/hardening/HARDENING-CONSOLIDATED-V4.md` survives the carry-aware metahardening; no V5 surgery is warranted; the research-fold cycle (Phase 1 of `restart/prompts/RESEARCH-FOLD-ORCHESTRATOR.md`) may dispatch.

## §12 Provenance and methodology

Methodology per HARDENING.md §"Per-Item Discipline" + §"Methodology": Pro/Con/Explication/Challenge per row; KEEP requires defeating the steelman. Voice per `restart/README.md` §13: calibrated, archaic-permissive, no metalanguage, path:line citations, tables liberal. V5 carry-aware metahardening is independent: the V1-V4 history reports were read for the carry-baseline, not as the verdict source; the five lenses look for what punch-list-focused cycles structurally missed. The 10-command gate-rerun was independently re-walked from the present `restart/audit/pass-3-runtime/PASS-3.md` at commit `11806d5d`. Cross-document bindings to ARCH:347-354, ARCH:992-1028, ARCH:1241-1245, ARCH:1331, MASTER-PLAN:469-470, MASTER-PLAN:524, MASTER-PLAN:534, MASTER-PLAN:766-770 were independently verified.

## §13 Closing posture

PASS-3 is architecturally sound, materially complete, narratively coherent with its receivers, terminologically disciplined post-Wave-4.1, materially populated with traceable walkthroughs, structurally covered for every V1-V4-deferred coverage area, and cumulatively consistent across the 14 locks. The runtime + public API spec advances unencumbered. Hereupon Phase 1 (research deep-dives) of the research-fold cycle dispatches.
