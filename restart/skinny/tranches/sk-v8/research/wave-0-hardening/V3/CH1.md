# CH1 W0 V3 Hardening Challenge

VERDICT ACCEPT.

Confidence: 96%.

Scope: correctness review of SK-V8 W0 after `61d5d304 fix(sk-v8-wave0): fold hardening V2 gate blockers`, plus prior W0 commits back through the telemetry-gate wave. Lens: strict-vs-strict admission and outcome mapping, `parse_utf8` / `escape_complete` completeness, row identity binding, no new directive/BIR/substrate, and Lock 14 grammar-neutral discipline. This review edited only this report.

## Evidence

1. Strict admission is live-gated, not paper-gated. The orchestrator requires CH1 to verify measurable gates and strictness-plane correctness (`restart/prompts/ORCHESTRATOR.md:83`) and separately names the strict-vs-strict comparator gate as a W0 invariant (`restart/prompts/ORCHESTRATOR.md:208`). SPEC makes the same rule executable: strict admission must fail unless plane, strictness, freshness, and measured-row UTF-8/control/escape validation all hold (`restart/skinny/tranches/sk-v8/SPEC.md:73`, `restart/skinny/tranches/sk-v8/SPEC.md:76`, `restart/skinny/tranches/sk-v8/SPEC.md:77`). The live gate calls `report.validate_schema_v3().and_then(|_| report.validate_sk_v8_w0())` before writing or checking `RESULTS.md` (`skinny/crates/bbnf-bench/src/bin/gate.rs:308`, `skinny/crates/bbnf-bench/src/bin/gate.rs:319`), and the row validator calls the W0 admission boundary before comparator evidence acceptance (`skinny/crates/bbnf-bench/src/report.rs:328`, `skinny/crates/bbnf-bench/src/report.rs:369`, `skinny/crates/bbnf-bench/src/report.rs:370`).

2. The strict predicate now includes the missing validation fields. `StrictAdmissionEvidence` carries `parse_utf8`, `escape_complete`, row/comparator planes, strictness, freshness, and measured path (`skinny/crates/bbnf-bench/src/gate.rs:59`, `skinny/crates/bbnf-bench/src/gate.rs:62`, `skinny/crates/bbnf-bench/src/gate.rs:69`). `validate_strict_admission` rejects non-strict rows, non-strict comparators, `parse_utf8 != measured-row`, `escape_complete != yes`, plane mismatch, non-measured validation, stale/historical/absent freshness, and unsupported freshness (`skinny/crates/bbnf-bench/src/gate.rs:135`, `skinny/crates/bbnf-bench/src/gate.rs:145`, `skinny/crates/bbnf-bench/src/gate.rs:151`, `skinny/crates/bbnf-bench/src/gate.rs:154`, `skinny/crates/bbnf-bench/src/gate.rs:157`, `skinny/crates/bbnf-bench/src/gate.rs:160`, `skinny/crates/bbnf-bench/src/gate.rs:163`, `skinny/crates/bbnf-bench/src/gate.rs:172`). This directly closes the V1 fold item that `parse_utf8=view-boundary` and non-`yes` escape status must fail strict admission (`restart/skinny/tranches/sk-v8/research/wave-0-hardening/V1/HARDENING-W0-V1-CONSOLIDATED.md:35`).

3. Outcome handling is strict enough for W0 and preserves hard failures. SPEC reserves `K`/`S` as substrate-guard non-admission and forbids either from supporting strict SOTA admission (`restart/skinny/tranches/sk-v8/SPEC.md:95`, `restart/skinny/tranches/sk-v8/SPEC.md:98`). The shared parser recognizes the broader enum (`skinny/crates/bbnf-bench/src/gate.rs:114`), but W0 validation rejects unknown and non-W0 outcomes (`skinny/crates/bbnf-bench/src/report.rs:866`, `skinny/crates/bbnf-bench/src/report.rs:870`). Parse rows may render only hard failures or `S` (`skinny/crates/bbnf-bench/src/report.rs:361`), and the producer preserves `I/J/K/L/M` while demoting admission-capable parse outcomes to `S` (`skinny/crates/bbnf-bench/src/bin/gate.rs:353`, `skinny/crates/bbnf-bench/src/bin/gate.rs:355`, `skinny/crates/bbnf-bench/src/bin/gate.rs:360`). This preserves the V1 hard-failure requirement (`restart/skinny/tranches/sk-v8/research/wave-0-hardening/V1/HARDENING-W0-V1-CONSOLIDATED.md:38`) and the V2 accepted CH1 fold (`restart/skinny/tranches/sk-v8/research/wave-0-hardening/V2/HARDENING-W0-V2-CONSOLIDATED.md:33`).

4. The V2 row-identity blocker is folded. V2 rejected because `sk_v8.row_id` was not bound back to rendered `corpus` / `workload` (`restart/skinny/tranches/sk-v8/research/wave-0-hardening/V2/HARDENING-W0-V2-CONSOLIDATED.md:48`). Current validation parses `row.sk_v8.row_id` and compares it with `row.corpus` and `row.workload` before the rest of the report is accepted (`skinny/crates/bbnf-bench/src/report.rs:328`, `skinny/crates/bbnf-bench/src/report.rs:962`, `skinny/crates/bbnf-bench/src/report.rs:964`). The report-level validator also requires exactly the `SK_V8_OPEN_BASELINE` row count, rejects duplicate/unknown row ids, validates Track 1/Track 2 baseline deltas, and requires every baseline row to be present (`skinny/crates/bbnf-bench/src/report.rs:493`, `skinny/crates/bbnf-bench/src/report.rs:501`, `skinny/crates/bbnf-bench/src/report.rs:508`, `skinny/crates/bbnf-bench/src/report.rs:511`, `skinny/crates/bbnf-bench/src/report.rs:514`).

5. The V2 native/sidecar comparator blockers are folded. V2 required workload-matched native source, expected plane, strictness, same-run-native freshness, sidecar `n/a`, and finite Mbps (`restart/skinny/tranches/sk-v8/research/wave-0-hardening/V2/HARDENING-W0-V2-CONSOLIDATED.md:53`), plus sidecar source/freshness tightening (`restart/skinny/tranches/sk-v8/research/wave-0-hardening/V2/HARDENING-W0-V2-CONSOLIDATED.md:59`). Current validation requires each comparator field, finite positive Mbps when populated, native source/plane/strictness/freshness/sidecar semantics, all sidecar slots, sidecar DOM strictness, freshness agreement, rejected `sidecar-same-run` without structured manifest, and exact populated/absence source shapes (`skinny/crates/bbnf-bench/src/report.rs:973`, `skinny/crates/bbnf-bench/src/report.rs:989`, `skinny/crates/bbnf-bench/src/report.rs:1009`, `skinny/crates/bbnf-bench/src/report.rs:1049`, `skinny/crates/bbnf-bench/src/report.rs:1051`, `skinny/crates/bbnf-bench/src/report.rs:1059`, `skinny/crates/bbnf-bench/src/report.rs:1065`, `skinny/crates/bbnf-bench/src/report.rs:1071`, `skinny/crates/bbnf-bench/src/report.rs:1077`, `skinny/crates/bbnf-bench/src/report.rs:1083`, `skinny/crates/bbnf-bench/src/report.rs:1089`, `skinny/crates/bbnf-bench/src/report.rs:1109`, `skinny/crates/bbnf-bench/src/report.rs:1120`, `skinny/crates/bbnf-bench/src/report.rs:1133`, `skinny/crates/bbnf-bench/src/report.rs:1139`, `skinny/crates/bbnf-bench/src/report.rs:1145`, `skinny/crates/bbnf-bench/src/report.rs:1151`, `skinny/crates/bbnf-bench/src/report.rs:1157`, `skinny/crates/bbnf-bench/src/report.rs:1163`).

6. No new directive, BIR variant, or substrate is introduced by W0. SPEC forbids new directives, BIR variants, `BackendShape`, `UnionTape`, new/public/parallel substrate surfaces, JSON policy in generic crates, and non-strict strict admission (`restart/skinny/tranches/sk-v8/SPEC.md:234`, `restart/skinny/tranches/sk-v8/SPEC.md:235`, `restart/skinny/tranches/sk-v8/SPEC.md:236`, `restart/skinny/tranches/sk-v8/SPEC.md:237`, `restart/skinny/tranches/sk-v8/SPEC.md:238`, `restart/skinny/tranches/sk-v8/SPEC.md:240`, `restart/skinny/tranches/sk-v8/SPEC.md:242`, `restart/skinny/tranches/sk-v8/SPEC.md:243`, `restart/skinny/tranches/sk-v8/SPEC.md:244`). The W0 range `55c8e234..HEAD` has no diffs under the frozen behavior roots I checked: `skinny/crates/grammar/src`, `skinny/crates/bbnf/src`, `skinny/crates/bbnf-simd/src`, `skinny/crates/bbnf-simd/build.rs`, `skinny/crates/bbnf-simd/ext`, `skinny/crates/runtime/src`, `skinny/crates/ir/src`, `skinny/crates/passes/src`, and `skinny/crates/codegen/src`. Current Lock 14 validation also freezes those directive/asm/generic roots (`skinny/crates/bbnf-bench/src/lock14_baseline.rs:375`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:383`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:385`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:386`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:387`) and rejects working-tree or parent-diff changes there (`skinny/crates/bbnf-bench/src/lock14_baseline.rs:399`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:403`).

7. Lock 14 grammar-neutral discipline is now enforced at the gate layer W0 owns. PASS-2 requires grammar-neutral candidate expression and rejects JSON-only primitives (`restart/prompts/skinny/PASS-2-RESEARCH.md:102`, `restart/prompts/skinny/PASS-2-RESEARCH.md:103`); PASS-3 requires the SPEC's Lock 14 gate and no hidden parallel substrate (`restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md:117`, `restart/prompts/skinny/PASS-3-SYNTHESIS-PLAN.md:134`). The allowlist rejects unsupported classes, unsupported W0 mutability, and forbidden surface names (`skinny/crates/bbnf-bench/src/lock14_baseline.rs:343`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:353`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:359`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:365`). It checks the `BackendShape` enum still has exactly the five expected variants and forbids `UnionTape`/`union_tape` in the IR surface (`skinny/crates/bbnf-bench/src/lock14_baseline.rs:462`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:465`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:481`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:485`, `skinny/crates/bbnf-bench/src/lock14_baseline.rs:488`).

## Blockers

None for CH1. I did not find a material strictness, outcome, UTF-8/escape, row identity, directive/BIR/substrate, or Lock 14 blocker after `61d5d304`.

## Residual Risk

- `cargo xtask gate-json --advisory --check-results` exited 2 in my local workspace because the default local Criterion cache drifted from committed `SK-V8-open`: the validator reported `json/twitter/parse_only/main Track 1 moved 63.03% from SK-V8-open baseline`. I do not treat this as a W0 blocker because it demonstrates the baseline guard rejecting stale local measurement data; it is not a source change or a false acceptance.
- This CH1 ACCEPT is not full W0 convergence by itself. V2 says W0 needs the next qualifying cycle to return at least 95% ACCEPT across the cohort and still needs two consecutive qualifying ACCEPT cycles before W1-W6 dispatch (`restart/skinny/tranches/sk-v8/research/wave-0-hardening/V2/HARDENING-W0-V2-CONSOLIDATED.md:116`, `restart/skinny/tranches/sk-v8/research/wave-0-hardening/V2/HARDENING-W0-V2-CONSOLIDATED.md:118`).

## Commands Run

- `cargo test -p bbnf-bench w0_`: passed 10 report tests and 6 gate-bin tests.
- `cargo test -p bbnf-bench lock14`: passed 7 Lock 14 tests.
- `cargo xtask gate-json --advisory --check-results`: failed closed on local `SK-V8-open` baseline drift, as noted above.
- `git diff --name-status 55c8e234..HEAD -- <directive/BIR/substrate frozen roots>`: no output.
