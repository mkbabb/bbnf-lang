# SK-V14 W2R Corrective Packet: Regen-CSS Wave-Graph Cycle

Status: proposed Pass Omega V3 input.

Trigger: REDRESS-183 materially affects the SK-V14 wave graph.

Owner: SK-V14 orchestrator.

## 1. Finding

W2 rejected honestly. The SPEC requires `regen-css` to generate two runtime
trees, but one of those trees is not generatable until a later wave.

Evidence:

- `restart/skinny/tranches/sk-v14/SPEC.md:485` requires W2 to emit CSS L4
  runtime modules under both `skinny/crates/runtime/src/grammars/css_l4_*`
  and `crates/core/src/runtime/css_l4/`.
- `restart/skinny/tranches/sk-v14/SPEC.md:491` requires the destructive
  round-trip:
  `rm -rf skinny/crates/runtime/src/grammars/css_l4_* crates/core/src/runtime/css_l4/ && cargo xtask regen-css && git diff`.
- `restart/ARCHITECTURE.md:1800-1825` states that live HEAD has zero
  generated runtime files and that `crates/core/src/runtime/css_l4/` is one
  of the 67 hand-written Pattern H runtime files.
- `restart/MIGRATION.md:44-45` routes generated-provider collapse to W5 and
  Pattern H runtime-root collapse to W6 PRUNE-4.
- REDRESS-183 records the attempted W2 redress: skinny-side rostered
  `regen-css` can be made to pass local checks, but the core runtime tree is
  still handwritten and unowned by any current generator.

This is a wave-graph dependency cycle: W2 requires a core-runtime generator
whose owner is W6, while W6 requires W5, W5 requires W4, W4 requires W3, and
W3 requires W2 admitted.

## 2. Contradictory Clauses

The W2 section contains an advisory downstream note:

- `restart/skinny/tranches/sk-v14/SPEC.md:512-515` says W2 rejection blocks
  W4 and W8 but W3/W5/W6/W7/W9/W10 may proceed independently.

That note conflicts with hard entry gates:

- W3 requires W2 admitted at `SPEC.md:530-533`.
- W4 requires W2 and W3 admitted at `SPEC.md:583-586`.
- W5 requires W4 admitted at `SPEC.md:643-647`.
- W6 requires W5 admitted at `SPEC.md:705-708`.
- W7 requires W5 and W6 admitted at `SPEC.md:796-799`.
- W8 requires the full W2-W7 chain at `SPEC.md:858-862`.

The entry gates are controlling because `SYNTHESIS.md:29-33` and
`ORCHESTRATOR-PROMPT.md:110-131` bind prune-before-rebuild ordering. The
contradictory downstream note should be replaced, not treated as dispatch
authority.

## 3. Proposed Amendment

Pass Omega V3 should amend the SK-V14 wave graph locally.

### W2 after amendment

W2 remains the first `regen-{grammar}` family instance, but its exit gate is
skinny-side only:

- Add `cargo xtask regen-css` to `skinny/xtask`.
- Consume the 15 CSS L4 `.bbnf` files and workspace metadata.
- Emit the seven existing skinny CSS L4 runtime profile directories under
  `skinny/crates/runtime/src/grammars/css_l4_*`.
- Add and run the seven `check-css-l4-*` companion commands.
- Run a destructive skinny-only round-trip:
  `rm -rf skinny/crates/runtime/src/grammars/css_l4_* && cargo xtask regen-css && git diff --exit-code -- skinny/crates/runtime/src/grammars`.
- Keep the bypass-header detector against both skinny and root runtime trees:
  any `@generated by skinny bbnf-codegen` match under `crates/core/src/runtime`
  must still fail unless owned by the W6 generator.

W2 may not move CSS SOTA rows, touch `crates/core/src/runtime/css_l4/`, or
claim Pattern H closure.

### W6 after amendment

W6 PRUNE-4 absorbs the core-runtime CSS L4 collapse as its first sub-wave:

- W6.0: CSS L4 core-runtime collapse. After W5 stands up the generic
  generator, emit `crates/core/src/runtime/css_l4/` from grammar source and
  metadata, then pass the destructive core-runtime round-trip for that tree.
- W6.1-W6.8: the eight remaining Pattern H grammar runtime dirs:
  `bbnf`, `bnf`, `css_pretty`, `csv`, `ebnf`, `google_sheets`, `json`,
  and `math`.

The W6 total remains nine sub-waves and keeps the existing cap: <=90 minutes
per sub-wave, aggregate <=810 minutes. W6.0 inherits the same cap and the
same clean-regen discipline.

### W4 after amendment

W4 PRUNE-2 keeps its W2 admission dependency, but W2 admission now means
skinny-side `regen-css` plus companion checks. W4 still deletes the seven CSS
L4 templates and reverts 24 CSS rows before any new CSS admission.

## 4. Rejected Alternative

Do not resequence W5/W6 before W2. That would globally rewrite the PRUNE
chain:

- W4 depends on W2 and W3.
- W5 depends on W4.
- W6 depends on W5.
- W7 depends on W5 and W6.

Moving W5/W6 before W2 would require re-authoring the whole PRUNE-1..5
ordering. The local split preserves the intended sequence while moving the
core-runtime obligation to its actual owner.

## 5. Risk Class

Risk: LOW.

Reason: this packet changes wave ownership and exit-gate text only. It does
not amend Lock 1, Lock 10, Lock 14, Lock 16, BackendShape, substrate union,
or the generated-output allowance. The live architecture already routes
Pattern H runtime collapse to W6; W2R only removes the contradictory early
requirement from W2.

## 6. Pass Omega V3 Routing

Inputs:

- T-P3 V4 LOCK packet already consumed by Pass Omega V2.
- Pass Omega V2 spec surfaces at HEAD.
- REDRESS-183.
- This W2R corrective packet.

Primary Omega outputs:

- Omega-A: verify the amended wave graph is acyclic.
- Omega-C: verify no locks amendment is required and publish a no-op
  `locks-diff.md`.
- Omega-D: produce `SPEC.md` and `MASTER-PLAN.md` amendment diff text.
- Omega-F: produce HANDOFF/MIGRATION wording that records W2R and keeps
  current dispatch blocked until G-Omega closes.

CRUD routing after G-Omega:

- CRUD-2 owns `restart/MASTER-PLAN.md`.
- CRUD-4 owns `restart/HANDOFF.md` and `restart/MIGRATION.md`.
- The SK-V14 SPEC amendment is tranche-local and should be applied with the
  same G-Omega authorization packet because it changes dispatch authority.
- CRUD-3 is read/no-op unless Omega-C discovers a lock implication.

## 7. G-Omega Gate Packet

Before any SPEC or V1 surface patch merges, present:

- REDRESS-183 and this W2R packet.
- Omega V3 challenge consolidated verdict.
- No-op locks diff or proposed locks diff if Omega-C contradicts this packet.
- Proposed SPEC diff.
- Proposed MASTER-PLAN diff.
- Proposed HANDOFF/MIGRATION diff.
- CRUD operation list.

Until G-Omega authorizes the amendment, do not rerun W2 and do not dispatch
W3 or later waves.
