# Phase 7.2 PASS-2 Fold Classification

This note records the verify-then-patch classification for the Phase 7.2 fold items routed to the PASS-2 codegen surface by the Phase 7.2 fold dispatch. Phase 7.1 landed the Backend trait at `restart/ARCHITECTURE.md` §7.5 (lines 1067-1144), the 6-directive grammar at Lock 10, the parse-that-regex naming canon at Lock 11, the egraph-decoupling DK13 fold at Lock 4 + Lock 6, and the lock amendments. PASS-2 is the codegen consumer; it absorbs the Backend trait surface, function-value lowering, sibling-crate naming canon, and egraph decoupling.

## §A — Backend trait surface

| # | Verification result on `PASS-2.md` | Disposition | Surgery |
|---|---|---|---|
| A1 — Backend trait integration | Line 5 references "per-backend lowerers" + Lock 5; lines 119-130 carry a `BackendLowerer` trait surface that pre-dates Phase 7.1's formal `Backend` trait at ARCH §7.5. PASS-2.md never names `Backend` (the Phase-7.1 trait), `RustBackend`, `WasmBackend`, or `TsBackend`. The current `BackendLowerer` is an 8-method internal surface; the Phase-7.1 `Backend` trait is the V1/V2 contract boundary that gates `lower` / `emit_runtime_template` / `emit_value_api` / `emit_visitor` / `emit_path_schema`. The two co-exist: `BackendLowerer` is the `Backend::lower` per-rule decomposition, but PASS-2 must position itself as the `RustBackend: Backend` impl carrier. | ADD | Insert a §3-adjacent paragraph after the `BackendLowerer` definition (post line 130) declaring that PASS-2's lowerer suite is the V1 `RustBackend: Backend` impl per ARCH §7.5; cite the trait surface at `restart/ARCHITECTURE.md:1078-1108`. The internal `BackendLowerer` carries the per-rule emission decomposition that `RustBackend::lower` invokes. |
| A2 — Per-backend obligation table | Line 52 (BIR variant table) carries a "TS status" column with all-`scaffold` entries; line 8 (verdict ledger) lists "TS scaffold capability"; line 17 carries the BD.W1 carry; line 226 names the `ts_stub/` lowerer tree. There is no top-level table that names: V1 = RustBackend, V2 = WasmBackend, V2 = TsBackend with their dispositions and the V2 amendment route. The carry ledger row at line 563 names "TS production" but does not bind V2 to the `WasmBackend` / `TsBackend: Backend` impl form. | ADD | Insert a per-backend obligation table after the §2 commitments (post line 49 or near the BackendLowerer paragraph) that names: V1 ships `RustBackend: Backend` impl (this PASS-2's surface); WASM ships `WasmBackend: Backend` impl in V2 (deferred post-V1 alongside Lock 11 V2 publication carry per ARCH §7.5 and §12.1); TS ships `TsBackend: Backend` impl in V2 (deferred post-V1 alongside the principled TS-native fork). |
| A3 — Trait method coverage | Per ARCH §7.5 (lines 1078-1108), the Backend trait carries five methods: `lower(bir, ctx)`, `emit_runtime_template(grammar)`, `emit_value_api(schema)`, `emit_visitor(schema)`, `emit_path_schema(schema)`. The `BackendLowerer` trait at PASS-2.md:119-130 carries 8 methods that decompose `lower`. The `runtime_template/` tree (lines 228-235) carries `files.rs`, `tape.rs`, `grammar.rs`, `host.rs`, `budgets.rs` — i.e., the substrate of `emit_runtime_template`. The runtime-template parameter schema at lines 134-153 covers the parameter set; the runtime emission table at lines 489-501 covers per-grammar emission rows. The five trait methods are implicitly satisfied; the explicit binding to ARCH §7.5 method names is missing. | ADD | Insert a method-coverage map adjacent to the trait surface paragraph: `Backend::lower` ↔ §3 lowerer trees (`codegen/src/lower/rust/*` for RustBackend); `Backend::emit_runtime_template` ↔ §3 `runtime_template/*` tree + §6 generated LOC table; `Backend::emit_value_api` ↔ generated `value.rs` per §3 runtime tree; `Backend::emit_visitor` ↔ `visitor_bitflags` runtime template parameter (line 151) + generated `visitor.rs`; `Backend::emit_path_schema` ↔ `<g>.path-schema.toml` plus `pointer!` glue (already at §4 PASS-3 handoffs row "Path-schema metadata"). |

## §B — Function-value lowering (audit #6)

| # | Verification result on `PASS-2.md` | Disposition | Surgery |
|---|---|---|---|
| B1 — Lambda + closure lowering | PASS-2.md is silent on lambda literal `\|x\| body` lowering; on the four-form bounded closure inventory at ARCH §8.4 (host chain, map, predicate, recovery); and on audit #6's option-3 verdict (inline at known call site). The §3.2 audit-6 row says option 3 is the V1 default. Phase 7.1 lock 4 amendment (line 40) folded function values + lambda + closure capture by `&'i` reference into V1; PASS-2 must describe the lowering. | ADD | Insert a §2 commitment row (post line 49) describing function-value + lambda lowering: option 3 (inline at known call site) is the V1 default per audit #6; the four bounded forms (host chain, map, predicate, recovery — ARCH §8.4 lines 1187-1207) are the V1 closure inventory; lambda literals `\|x\| body` lower to inlined BIR straight-line code at the call site; no new BIR variant. |
| B2 — Function-typed `@host fn` parameters | PASS-2.md is silent on F3 (function-typed parameters in `@host fn`). Phase 7.1 Lock 10 line 52 folded `@host fn map<T, U>(f: fn(T) -> U, xs: [T]) -> [U]` into V1. Lowering options per audit #6: option 1 (monomorphise per call site) is the V1 default when the function value is statically known; option 2 (vtable dispatch) is forbidden in V1 (incompatible with throughput per PASS-2.md:330-332 and §7 perf gates). | ADD | Insert a function-value lowering paragraph adjacent to the §6 generic monomorphisation budget gate (line 417) describing: function-typed `@host fn` parameters lower via option 1 (monomorphise at call site) when the function value is statically known; reuses the existing finite `(RuleId, TypeArgs)` instance set machinery; vtable dispatch (option 2) is forbidden in V1 (incompatible with throughput targets at lines 452-460); cite audit #6 §7.4 V1 recommendation. |
| B3 — Higher-rank lowering | PASS-2.md is silent on DK13 higher-rank monomorphisation. Phase 7.1 Lock 4 (line 40) folded DK13 algorithmic completeness into V1 type system. PASS-2 lowering: explicit `forall` annotations require monomorphic instantiation at the call site; rank-1 generic rules carry the existing instance-set proof at PASS-2.md:417; rank-2 (DK13-folded) higher-rank quantification monomorphises per call site identically — the finite-instance proof still gates emission. | ADD | Add a higher-rank monomorphisation row to the §3 lowering surface (adjacent to line 417 generic monomorphisation budget): explicit `forall` annotations (DK13 surface, ARCH/Lock 4) instantiate monomorphically at the call site; the finite `(RuleId, TypeArgs)` instance set machinery extends to higher-rank by carrying the explicit-quantifier instantiation alongside the rank-1 instances; cite Lock 4 line 40 + audit #1 §2.1. |
| B4 — Closure environment frame | PASS-2.md is silent on closure environment frames. Phase 7.1 Lock 4 (line 40) folded closure capture by `&'i` reference only; capture-by-move forbidden. Audit #6 §7.4 + audit #2 §3.3 + ARCH §8.4 lines 1187-1207 bind: stack-allocated environment frame; no heap allocation; lifetime bound by `&'i Tape<'i>`; `Fn`/`FnMut`/`FnOnce` discrimination not exposed at BBNF surface. | ADD | Insert a closure-environment paragraph adjacent to the function-value lowering paragraph: closure captures lower to stack-allocated environment frames bounded by `&'i Tape<'i>`; no heap allocation (incompatible with hot-path per PASS-2.md:330-332); capture-by-move is forbidden in V1 (Lock 4 line 40); the borrow-checker validates lifetime escape on the Rust backend; cite audit #2 §3.3 + ARCH §8.4 lines 1187-1207. |

## §C — Sibling crate alignment

| # | Verification result on `PASS-2.md` | Disposition | Surgery |
|---|---|---|---|
| C1 — parse-that-regex naming canon | PASS-2.md uses `parse-that/regex` (line 15, line 81). Phase 7.1 Lock 11 (line 54) renamed the legacy `bbnf-regex` crate to `parse-that-regex`; the canonical name is now `parse-that-regex` (the regex sub-crate of `parse-that`). PASS-2's `parse-that/regex` spelling is the workspace-relative module path; the published-crate name is `parse-that-regex`. Both spellings co-exist canonically per Lock 11; the relationship "parse-that-regex is the regex sub-crate of parse-that" must be documented when the lowering surface names it. | ADD | Insert a one-line note adjacent to line 81 (the `RegexDfa` payload contract) clarifying: `parse-that/regex` is the workspace-internal path; `parse-that-regex` is the published-crate name (the regex sub-crate of `parse-that`); both refer to the same regex engine substrate per Lock 11 line 54. No global rename; the existing `parse-that/regex` spelling stands as the workspace path. |
| C2 — regex-automata oracle removal | PASS-2.md carries `regex-automata` references at line 81 ("checked against a `regex-automata` oracle lane until parity is proven") and line 470 ("bespoke regex code remains checked against `regex-automata` until parity is proven"). The user mandate per audit #4 §4 + V1-FOLD-CANDIDATES item #23 (line 82) forbids `regex-automata` entirely, including as oracle. The amendment routes oracle pressure to "parse-that internal cross-engine parity (VM ↔ lazy DFA ↔ full DFA)". | REWRITE | Rewrite both lines (81, 470) to replace the `regex-automata` oracle citation with: `parse-that-regex` internal cross-engine parity (VM ↔ lazy DFA ↔ full DFA) on the same fixtures; cite Lock 11 + audit #4 §4. Strike the "until parity is proven" oracle clause. |
| C3 — RegexDfa → RegexProgram rename | PASS-2.md uses `RegexDfa` throughout (lines 34, 65, 81, 180, 457, 470, 482, 587). ARCH §7.2 (line 935) ratifies `RegexProgram` as the BIR final-23 spelling; PASS-2 cannot re-own BIR alphabet (PASS-2.md:85-114). The rename anchors at ARCH §7; PASS-2 follows. The 2026-04-13 archive note at the variant table (line 65) describes `RegexDfa` as a "compatibility-name slot." V1-FOLD-CANDIDATES item #18 (line 70) rates this a tier-2 fold. Phase 7.1 SYNTHESIS is the alphabet authority; PASS-2's job is to consume the canonical rename. Need to verify ARCH §7 currently uses `RegexProgram` (it does, at line 935). | RENAME | Replace all `RegexDfa` instances in PASS-2.md with `RegexProgram` throughout; the BIR variant ID (`12 | RegexProgram | ...`) carries the canonical name; lines 81, 180, 457, 470, 482, 587 follow. Cite ARCH §7.2 line 935 as the alphabet anchor. The variant-table compatibility note at line 81 retires (`RegexProgram` is now the alphabet spelling, not a compatibility slot). |

## §D — Egraph decoupling (audit #4)

| # | Verification result on `PASS-2.md` | Disposition | Surgery |
|---|---|---|---|
| D1 — egraph independence | PASS-2.md mentions "e-graph extraction" at line 32, line 378 (PASS-1 handoffs row), line 423 (e-graph + cost-model bridge implicitly via §7.3 in ARCH). PASS-2.md does not currently describe the post-Phase-7.1 fact: egraph and csp-solver compose at `passes::bridge`, not by direct Cargo-level dependency (Lock 6 line 44 amendment). PASS-2 codegen consumes BIR post-egraph-extraction; the bridge has settled by the time PASS-2 sees BIR. | ADD | Insert a one-line clarifier in §5 PASS-1 handoffs (post line 378 e-graph extraction row) stating: egraph + csp-solver compose at `passes::bridge` per Lock 6 line 44; the bridge settles before BIR reaches PASS-2; PASS-2 codegen never imports either crate directly and consumes BIR post-extraction only. Cite audit #4 §3 X-5. |

## Acceptance gates after the amendment commit

- `rg -n 'Backend trait\|RustBackend\|WasmBackend\|TsBackend' restart/audit/pass-2-codegen/PASS-2.md` returns matches binding PASS-2's lowerer suite to the V1 `RustBackend: Backend` impl, with V2 deferral rows for `WasmBackend` and `TsBackend`.
- `rg -n 'parse-that-regex' restart/audit/pass-2-codegen/PASS-2.md` returns at least one match clarifying the published-crate name relationship.
- `rg -n 'regex-automata' restart/audit/pass-2-codegen/PASS-2.md` returns zero (or deletion archaeology only); the oracle clauses retire.
- `rg -n 'bbnf-regex' restart/audit/pass-2-codegen/PASS-2.md` returns zero (the legacy name has no presence in PASS-2.md and must remain absent).
- `rg -n 'RegexProgram' restart/audit/pass-2-codegen/PASS-2.md` returns matches throughout the surface (variant table, runtime emission, perf gates, closing posture); `rg -n 'RegexDfa' restart/audit/pass-2-codegen/PASS-2.md` returns zero (or deletion archaeology only).
- `rg -nC2 'higher-rank\|DK13\|monomorphis' restart/audit/pass-2-codegen/PASS-2.md` returns the higher-rank lowering paragraph.
- `rg -nC2 "closure.*&.i\|capture.*reference" restart/audit/pass-2-codegen/PASS-2.md` returns the closure environment-frame paragraph.
- `rg -nC2 'passes::bridge' restart/audit/pass-2-codegen/PASS-2.md` returns the egraph decoupling clarifier.

## Phase 7.2 fold note

This classification predicates the Phase 7.2 narrow-scope amendment commit; the amendment lands surgical edits on `PASS-2.md` only and leaves PASS-2 sub-agent reports untouched, save for this note alongside.
