---
lens: CH7 OVERFIT-PRUNE
pass: T-P1-excavation
cycle: V6-confirm
generated_at: 2026-05-29T22:30:00Z
reviewer: CH7 (V6-confirm)
subject_files:
  - restart/audit/totality/sk-v17/p1/1a-substrate-evidence.md
  - restart/audit/totality/sk-v17/p1/1b-codegen-evidence.md
  - restart/audit/totality/sk-v17/p1/1c-runtime-evidence.md
  - restart/audit/totality/sk-v17/p1/1d-skinny-lessons.md
  - restart/audit/totality/sk-v17/p1/1e-locks-evidence.md
  - restart/audit/totality/sk-v17/p1/1f-coherence-scan.md
  - restart/audit/totality/sk-v17/p1/1f-anti-pattern.md
  - restart/audit/totality/sk-v17/p1/1f-past-corpora.md
mandate: >
  Confirming CHALLENGE cycle. No contrivance; the excavation must distinguish the
  genuinely-generalizable tape model from any CSS/JSON-special-cased current state;
  crates/core is the fold TARGET, not measured-as-skinny. ACCEPT where the
  V5-folded artefact is correct + complete; flag only genuine residual defects.
  Disposition each section ACCEPT/REVISE/REJECT with file:line.
master_head: 445925167154de73540e3ea3283d0170371de790
cycle_kind: confirming
counts:
  accept: 8
  revise: 0
  reject: 0
sections_reviewed: 8
prior_cycle: V5
prior_cycle_disposition: 8 ACCEPT / 0 REVISE / 0 REJECT (100%)
convergence_note: >
  V1 was a VOID infrastructure cycle (all agents crashed, 0 CH files) and does NOT
  count as a disposition cycle. The real cycles are V2 (61.9%) → V3 (85.3%) →
  V4 (93.3%) → V5 (98.7%). This V6-confirm cycle obtains the formal
  2nd-consecutive ≥95% for §3Z: V5 98.7% + this cycle 100% = two consecutive
  ACCEPT cycles from the OVERFIT vantage.
---

## §0 Posture

CH7 OVERFIT-PRUNE asks one question of every catalogued divergence: does the
finding measure the *genuinely-generalizable* substrate model, or has the
inventory over-fit to the CSS/JSON-special-cased current state — by mislabelling
a grammar-neutral fact as grammar-specific, by inventing a "gap" that is a
scope/convergence question, by *understating* an already-general construct, or
by *over-crediting* breadth-of-config as breadth-of-proof? The complementary
failure is contrivance: a divergence dressed up as a defect when it is a
fold-direction artefact (crates/core is the fold TARGET; it is allowed to lag
the SKINNY-proven model), or a genuine spec tension *dressed away* into a clean
convergence story.

This is the CONFIRMING cycle. The prior CHALLENGE wave climbed V2 61.9% → V3
85.3% → V4 93.3% → V5 98.7% (V1 was VOID — all agents crashed, 0 CH files, NOT a
real disposition cycle). The CH7 lens specifically ran V3 (6 ACCEPT / 2 REVISE),
V4 (8 ACCEPT / 0 REVISE), V5 (8 ACCEPT / 0 REVISE). V5 carried zero orphan
REVISE and the excavation is substantively converged. A confirming cycle ACCEPTs
where the V5-folded artefact is correct + complete; only genuine residual defects
are REVISE/REJECT. CH7's confirming obligation is twofold: (1) confirm the lens
carries no orphan REVISE (it does not — V5 closed clean); (2) re-run the
contrivance and breadth-over-credit sweep over the V5-folded on-disk rows and
confirm every load-bearing anchor still resolves live at master HEAD
`445925167154de73540e3ea3283d0170371de790`. Every citation below is re-resolved
this cycle by Read + grep over the live trees; no recalled LOC.

## §1 Live re-verification of every load-bearing CH7 anchor (confirming sweep)

All anchors re-resolved this cycle against master HEAD `445925167` over the live
trees. The table is the contrivance/over-credit firewall: each row confirms the
V5-folded inventory claim is a breadth-of-fact statement, not an inflation,
deflation, or contrivance.

| Anchor (inventory claim) | Live re-verification at HEAD `445925167` | OVERFIT verdict |
|---|---|---|
| **@generated value-API headers** (1A Gaps-row `1a:140`; 1C RT17-002 `1c:76,99`; 1D SK17L-002 `1d:89`) — "value APIs are `@generated by xtask regen-{json,css}` … EAGER, NOT hand-written — Lock-14 ALLOWED" | `head -1 crates/core/src/runtime/json/value.rs` = `// @generated by xtask regen-json; do not edit by hand.`; `css_l4/value.rs:1` = `// @generated by xtask regen-css; do not edit by hand.` | **Confirmed. NOT contrivance, NOT over-fit.** The folded V4 prose Gaps-row had OVER-stated the defect class (claiming a Lock-14-VIOLATION shape — hand-authorship — where the live state is Lock-14-ALLOWED generated-but-eager). The fold removed a spurious defect inflation; the genuinely-general divergence (EAGER vs lazy `ValueRef<G>` materialization shape) survives intact. The framing is identically carried across 1A/1C/1D/1F-coherence — no special-case residue. |
| **`value_from_ref` grep-zero in core json** (1C RT17-002 `1c:76`; 1D SK17L-002 `1d:89`; consolidated B/C tables) | `grep -c value_from_ref crates/core/src/runtime/json/value.rs` = **0** | **Confirmed.** The lazy `value_from_ref`/`ValueRef<G>` projection is genuinely absent from the fold target (spec-claims-unimplemented), correctly distinguished from the eager generated current state. No over-credit. |
| **`LayoutFacts crates/`=0 (1E LAC-04 path-(b) re-price)** `1e:181` — "NOT ~0 LOC … `LayoutFacts` is the SKINNY/prior-totality side-table … absent from crates/core … ANY crates/core realisation is a non-zero generator-side surface (UNKNOWN)" | `grep -rn LayoutFacts crates/` = **0** (re-run live). Present ONLY in skinny: `skinny/crates/passes/src/lib.rs:85` (`pub layout_facts: LayoutFacts`), `:91` (`pub struct LayoutFacts`), `:109/:110/:395/…` | **OVERFIT-EXEMPLARY correction, confirmed.** The V4 path-(b) had committed the precise over-credit CH7 polices: pricing a fold-target capability against a construct that does NOT exist in the fold target. The re-price grounds the cost on a live `grep`-zero anchor and bars claiming Lock-2 closure by `LayoutFacts` ALONE. Breadth-of-fact, not contrivance. |
| **`StructLayout crates/`=960 (1E LAC-04 path-(a); 1A SUB17-006; 1F L02)** | `grep -rn StructLayout crates/` = **960** (re-run live) | **Confirmed.** The OVERFIT-correct *up*-calibration from the V2 40-120 LOC under-estimate; path-(a) full-rename 960-site/medium reproduces exactly. Breadth-of-fact. |
| **math.rs eight-not-nine scan census** (1C RT17-005 `1c:79,102`; 1D SK17L-008 `1d:95`; 1F-anti-pattern AP17-002 `1f-anti-pattern:70`) | `grep -c scan_structural crates/core/src/grammar/generated/math.rs` = **0**; `grep -c OnceCell math.rs` = **2** (both doc-comments at `:281`/`:285`). The eight scan-wired carriers each return `grep -c scan_structural` = **1** (json/css_l4/ebnf/bnf/csv/css_pretty/google_sheets/bbnf, re-run live); `grep -rln scan_structural crates/core/src/grammar/generated/` = **8** | **Confirmed, held clean.** The V3 false-ninth-carrier inflation remains struck; the EIGHT-name wired census is restated consistently across 1C/1D/1F-anti-pattern. math.rs is the lone field-only no-scan generated grammar. No re-inflation, no symbol-vs-doc-comment slip. |
| **BSHAPE17-009 NEON shared-classifier already-general** (1B `1b:77`) — "impl-exceeds-spec, not a gap; the shared `select_classifier(alphabet)` is ALREADY grammar-general across 8 grammars" | `scan_structural(input: &[u8], alphabet: &StructuralAlphabet)` at `crates/simd-scan/src/lib.rs:80`; `KernelShape::select(alphabet)` (the `select_classifier` analogue) at `alphabet.rs:118`; per-grammar `GRAMMAR_STRUCTURAL_ALPHABET` emitted at `backend/rust/emitter/profile.rs:43`/`:100`; the runtime constructs the alphabet per-call inside `ensure_structural_index` `get_or_init` (`backend/rust/emitter/shapes/dispatcher/support.rs:472-485`, re-read live — a retained-OUTPUT scan cache, NOT cross-call classifier state); all 8 generated grammars consume it (`grep -rln scan_structural` = 8); NEON aarch64-baseline (`neon.rs:5`/`:35`/`:42`/`:47`); avx2/avx512/wasm `#![cfg]`-gated to non-aarch64 | **Confirmed without contrivance.** The most mandate-central V5-new finding. The divergence is narrative-only (the spec's JSON-scanner framing must absorb the alphabet-parametrised shared form, 0 LOC fold) — NOT inflated into "the spec must build the classifier." Every anchor re-resolved live; the `support.rs:472-485` cache is correctly characterised as a retained scan-OUTPUT cache, not a renamed-scanner cross-call state. |
| **BackendShape / derive_backend_shape grep-zero in fold target** (1B BSHAPE17-003; consolidated D table) | `grep -rn 'enum BackendShape' crates/` = **0**; `grep -rn derive_backend_shape crates/` = **0** (selector lives only in skinny). `EmitStrategy::StructDirect {` at `crates/ir/src/registry/strategy.rs:107` (the lone struct-builder lineage to absorb) | **Confirmed.** Spec-claims-unimplemented-in-fold-target is correctly drawn; the single `StructDirect` variant is "the SinkOnly/struct-builder lineage to absorb," not inflated into a Lock-10 violation. No contrivance. |
| **StructRegistry per-leaf fence symbols** (1B BSHAPE17-006/007; 1C RT17-006; 1D SK17L-004; consolidated F) | `StructRegistry::layout(rule_id)` at `crates/ir/src/registry/struct.rs:331`; `layout_by_name` at `:337`; `begin_compound(&StructLayout)` takes a pre-resolved layout reading only `layout.rule_id & 0x1F` (`crates/core/src/runtime/tape/mod.rs:185-186`) | **Confirmed.** The hot-path fence is anchored to the construct, not a symbol list; the FieldSource walk must stay compile-time projection-emission. The do-not-redrive fence is genuine, not dressed away. |
| **StructuralAlphabet config-breadth, not proof-breadth** (1A SUB17-007; 1F-coherence COH17-008 `1f-coherence:85`) | `crates/simd-scan/src/alphabet.rs:33-37` doc-comment names `b'"'` for JSON and `b'\''`+`b'"'` for CSS — JSON/CSS-exercised-only; `singletons`/`digraph_mask:[u64;4]`/`digraph_pairs`/`quote_classes` at `:19-37` | **Confirmed, anti-over-credit holds.** The richer config (digraphs, quote_classes) is classed "breadth-of-config, NOT breadth-of-proof" — the precise anti-over-credit posture. No breadth over-credit. |
| **817-LOC CSS builder / seven pending fields** (1F-anti-pattern AP17-003 `1f-anti-pattern:71`; consolidated B) | `wc -l crates/core/src/runtime/css_l4/builder.rs` = **817**; `pending_value: Option<CssTypedValue<'p>>` at `:71`; `pending_rules/decls/selectors/values/blocks/components: Vec<T>` at `:74-79` = SEVEN pending_ fields, NONE `Vec<Vec>`; `span_starts: Vec<u32>` at `:73` correctly NOT counted as a `pending_*` field | **Confirmed.** The fold-deletion target count reproduces EXACTLY; no count over/under-statement. |
| **REDRESS-53 named-line re-anchor** (1F-past-corpora `1f-past-corpora:59`; 1E `1e:141/190`) — re-anchored from `:578` (continuation clause) to `:577`/`:825`/`:839` | The 1f-past-corpora ledger row reads "REDRESS-53 is NAMED at `:577`/`:657`/`:825`/`:839` — `:578` is the continuation clause" | **Confirmed.** A citation-precision fix (CH1/CH5 territory); CH7 confirms the re-anchor did not deflate the REDRESS-53 pre-block into a non-fact — the parallel-retained-index fold fence remains load-bearing and scoped to the `OnceCell<StructuralIndex>` carriers. Clean. |
| **`TapeStructBuilder` grep-zero outside `tape/`; runtime carries no shape leak** (1A; 1C RT17-007 `1c:81,104`; consolidated A) | `grep -rn TapeStructBuilder crates/core/src \| grep -v runtime/tape/` = **0**; `grep -rn -E 'EagerTape\|OffsetTape\|EventTape\|SinkOnly\|CollapsedStage' crates/core/src/runtime/` = **0** | **Confirmed.** The unwired tape is a dormant fold target, not a same-tree second substrate; the runtime correctly carries no BackendShape symbol. RT17-007 refuses the contrived runtime-side leak. No contrivance. |
| **Lock-1 both-clauses + skinny SoA shape** (1C RT17-001 `1c:75`; 1F-coherence COH17-001) — offset-tape-admissible vs AV.04-dense-class-column-dead | `LOCKS.md:75` carries BOTH clauses verbatim ("the projection may be an offset tape, event tape, or collapsed-stage event sink" AND "columnar SoA is dead … Columnar SoA stays buried"); skinny `Tape<'input>` at `skinny/crates/runtime/src/tape/mod.rs:94-100` = `source`/`offsets: Vec<u32>`/`flag_cursors: Vec<u32>`/`flag_values: Vec<u8>`/`payloads`/`id` — sparse position-keyed side-vectors, NOT a dense kind-partitioned class column | **Confirmed.** The §3-A fold (V3→V4) holds clean: the convergence target is the admissible offset-tape; the dead shape is the AV.04 dense class-column; the two are distinct constructs. No clean-convergence dressing-away of a genuine tension. |

Every anchor re-resolved live. No drift, no orphan, no new over-fit.

## §2 Section dispositions (confirming)

### 1a-substrate-evidence.md — ACCEPT

The Lock-1 substrate-union spine is OVERFIT-clean. The V5-folded Gaps-row
Lock-14 relabel (`1a:140`) is confirmed a defect-inflation REMOVAL — the live
state is Lock-14-ALLOWED `@generated` code (headers re-verified verbatim), not a
hand-authorship violation. The genuinely-generalizable model (SoA `Tape` +
`ValueRef<'doc,'input,K,G>` + `select_classifier`) stays cleanly separated from
the CSS/JSON-special-cased current state (eager `OpenFrame`/`CssTypedValue`/
no-`value_from_ref`); crates/core is consistently the fold TARGET. Re-verified
live: `TapeStructBuilder` grep-zero outside `runtime/tape/`; `StructLayout`=**960**
exactly; `value_from_ref` absent in core json (=0). SUB17-007's
`StructuralAlphabet` row is the OVERFIT-exemplary breadth call — config-breadth,
not proof-breadth, re-verified at `alphabet.rs:33-37`. No contrivance; no breadth
over-credit.

### 1b-codegen-evidence.md — ACCEPT

The BackendShape canon excavation is OVERFIT-clean and BSHAPE17-009 (`1b:77`),
the most mandate-relevant finding in the set, is handled WITHOUT over-credit. The
prompt subject's "NEON JSON-only vs the shared-classifier generalization"
divergence resolves correctly to **impl-exceeds-spec, not a gap**: the shared
`select_classifier(alphabet)` is ALREADY grammar-general across 8 grammars in
core. Every anchor re-verified live (`lib.rs:80`, `alphabet.rs:118`,
`profile.rs:43/:100`, `support.rs:472-485`, `neon.rs:47`, 8-grammar scan census).
The OVERFIT discipline is exactly right: NOT inflated into "the spec must build
the classifier" — the divergence is narrative-only, 0 LOC fold. BSHAPE17-003's
single `EmitStrategy::StructDirect` (`strategy.rs:107`) is named "the
SinkOnly/struct-builder lineage to absorb," not a Lock-10 violation; BSHAPE17-008's
data-bound `builder_path` is Lock-14-ALLOWED DATA. No contrivance.

### 1c-runtime-evidence.md — ACCEPT

The runtime census is materially sound and OVERFIT-clean. RT17-001 (`1c:75`)
draws the offset-tape-admissible-vs-AV.04-dense-class-column distinction
explicitly, reserving "columnar SoA is dead" for the dense class-column it
actually names. The eager `OpenFrame` builders are correctly the fold-deletion
target; RT17-007 (`1c:81/104`) refuses the contrived runtime-side BackendShape
leak (re-verified: grep-empty for the 5 shape names over `crates/core/src/runtime/`).
RT17-002 (`1c:76/99`) carries the generated-but-EAGER framing consistent with the
1A fold — no hand-written mischaracterisation. RT17-005's all-8 + math.rs-excluded
census (`1c:79/102`) is breadth-of-fact, re-verified. No contrivance.

### 1d-skinny-lessons.md — ACCEPT

The JSON-empirical vs grammar-neutral split remains the strongest generality
firewall in the set and is the literal embodiment of the CH7 mandate. SK17L-002
(`1d:89`) holds the generated-but-EAGER-not-hand-written framing
(`@generated`-header anchored, re-verified). SK17L-008's eight-not-nine math.rs
census (`1d:95`) is breadth-of-fact (math.rs `scan_structural`=0; `OnceCell`=2
doc-comments at `:281/:285`; contrast json.rs:701 genuine field). SK17L-009's
projection-generality framing ("by-construction on JSON+CSS only"; `sheets_witness`
a bench-surface stub resolving to `skinny/crates/bbnf-bench/src/report.rs:800-802`
report fields, "cannot serve as a projection exercise") is the precise
anti-over-credit posture (breadth-of-config, not breadth-of-proof). The
eager-tree (SK17L-003) and registry-indirection (SK17L-004) pre-blocks stay
anchored to the construct, not a symbol list; both name the crates/core construct
"UNWIRED dead code (fold target)." The monotonic direction (skinny-proven →
crates/core) is explicit. No contrivance.

### 1e-locks-evidence.md — ACCEPT

The locks audit and the 6 amendment candidates are evidence-backed and
OVERFIT-clean. LAC-1E-SKV17-04 (`1e:181`) is the OVERFIT-exemplary call: path-(b)
re-priced off a `grep -rn LayoutFacts crates/`=0 live anchor (re-verified),
explicitly classing the side-table as skinny/prior-totality-only and its
crates/core materialisation as a non-zero UNKNOWN — the correction of a
fold-target over-credit (pricing an absent construct as live). path-(a)
960-site/medium reproduces exactly (`StructLayout`=960 re-run live). The L14 row
rules the per-grammar `CssStructBuilder`/`CssTypedValue` the fold-deletion target,
not a leak; the L16 row names core's multi-arch scan "BROADER than the proven
aarch64 set — a fold-scope question … not a defect" (refusing contrivance into a
Lock-16 violation). Candidate ownership (6 LACs, disposition deferred to 3C) is
correctly bounded. No over-fit; the cost calibration is breadth-of-fact.

### 1f-coherence-scan.md — ACCEPT

Carries the §3-A offset-tape-admissible-vs-AV.04-dense-class-column fold clean,
re-verified against `LOCKS.md:75` carrying both clauses and the skinny `Tape`'s
sparse-side-vector shape. COH17-008 (`1f-coherence:85`) correctly classes the
richer `StructuralAlphabet` "JSON/CSS-exercised-only (breadth-of-config, NOT
breadth-of-proof)" — the anti-over-credit call, re-verified at `alphabet.rs:35-37`.
The "core CSS structural scan IS wired — the missing primitive is the TAPE
CONSUMER, not the scan" row (`1f-coherence:118`) correctly distinguishes the
already-general scan from the absent consumer, refusing to contrive a scan gap.
COH17-002 carries the generated-but-EAGER framing consistent with the 1A fold.
No contrivance.

### 1f-anti-pattern.md — ACCEPT

The CH5-firewall counterpart, OVERFIT-clean. AP17-001 refuses to contrive a
Lock-1 violation from the unwired tape ("NOT a same-tree second substrate").
AP17-002's 8-carrier OnceCell census explicitly excludes math.rs as the
symbol-vs-doc-comment class (`1f-anti-pattern:70`, re-verified `grep -c
scan_structural math.rs`=0, `OnceCell`=2 doc-comments). AP17-003 names the
817-LOC CSS builder a fold-deletion target, not a Lock-13 violation — re-verified
live: `wc -l … css_l4/builder.rs` = **817**; the seven pending_ fields
(`pending_value: Option` at `:71` + six `pending_*: Vec` at `:74-79`, `span_starts:
Vec<u32>` at `:73` correctly NOT counted) reproduce EXACTLY. AP17-004 rules the
emitter `substrate.rs` builder-path references Lock-14-ALLOWED DATA. No
contrivance, no count over/under-statement.

### 1f-past-corpora.md — ACCEPT

The do-not-redrive ledger and Direction-Monotonicity Note remain the strongest
fold-target-not-skinny guardrails in the set. PC17-006 names the §9 "second
substrate" block calling `StructLayout`/`TapeStructBuilder`/`TapeCursor`
FORBIDDEN-IN-SKINNY and forbids relocating them into skinny — the monotonic
skinny→totality direction is explicit and is the literal CH7 mandate
("crates/core is the fold TARGET, not measured-as-skinny"). The V5 REDRESS-53
named-line re-anchor (`:577`/`:657`/`:825`/`:839`, `:578` the continuation clause)
is carried correctly (`1f-past-corpora:59`). The eager-value-tree row anchors the
crates/core CSS builder's eager `CssTypedValue`+`pending_*` Vecs (`builder.rs:74-79`,
`value.rs:414`) as the AZ-IV-pre-blocked fold-deletion target, not carried
forward. No over-fit.

## §3 Dispositions

No REVISE, no REJECT this cycle. CH7 carried zero orphan REVISE into V6-confirm
(V5 closed 8/8 ACCEPT). The confirming sweep re-resolved every load-bearing
anchor live at HEAD `445925167` (§1): the `@generated` value-API headers verbatim;
`value_from_ref`=0; `LayoutFacts crates/`=0; `StructLayout`=960; math.rs scan=0 /
OnceCell=2-doc-comments; the eight-grammar scan census; the BSHAPE17-009 NEON
anchors (`lib.rs:80`/`alphabet.rs:118`/`profile.rs:43,100`/`support.rs:472-485`/
`neon.rs:47`); `BackendShape`/`derive_backend_shape`=0 in core; the StructRegistry
fence symbols (`struct.rs:331/337`, `mod.rs:185-186`); the StructuralAlphabet
quote-class config-breadth doc; the 817-LOC / seven-pending CSS builder; the
REDRESS-53 named lines; `TapeStructBuilder`=0 outside `tape/`; the runtime
shape-symbol grep-zero; and `LOCKS.md:75`'s both-clauses with the skinny SoA
shape. The contrivance/breadth-over-credit sweep found every V5-folded row a
breadth-of-fact statement, not an inflation, deflation, or contrivance. The
mandate's three firewalls all hold:

- **No contrivance.** Every broader-than-proven construct (multi-arch scan, richer
  `StructuralAlphabet`, AoS-vs-SoA, the 960-site StructLayout surface, the
  multi-arch simd-scan crate) is classed as scope/convergence/config-breadth
  pressure or breadth-of-fact cost, never inflated into a Lock violation; and no
  genuine spec tension (the exactly-one-encoding closure obligation, REDRESS-53
  re-entry risk, the StructRegistry per-leaf fence) is dressed away into a clean
  convergence story — each is carried as a catalogued divergence or fenced UNKNOWN.
- **Generalizable separated from special-cased.** The SoA `Tape` + `ValueRef<G>` +
  `select_classifier` model is consistently separated from the eager
  `OpenFrame`/`CssTypedValue`/no-`value_from_ref` current state; the JSON-empirical
  vs grammar-neutral split (1D) and the config-breadth-not-proof-breadth calls
  (1A SUB17-007, 1F COH17-008) are the load-bearing distinctions, all live-verified.
- **crates/core is the fold TARGET.** Every inventory's Direction-Monotonicity
  discipline holds; PC17-006 forbids reverse-relocation; the unwired tape is named
  "dormant fold target," not a defect.

## §4 Counts

- Sections reviewed: 8 (1A, 1B, 1C, 1D, 1E, 1F-coherence, 1F-anti-pattern,
  1F-past-corpora).
- ACCEPT: 8 — all eight inventories.
- REVISE: 0.
- REJECT: 0.
- Accept rate: 8 / 8 = **100%**.

## §5 OVERFIT-PRUNE verdict

The V5-folded inventories pass the CH7 mandate decisively in this confirming
cycle. Every load-bearing OVERFIT anchor re-resolved live at master HEAD
`445925167` with zero drift; no orphan REVISE remained; no new defect surfaced;
no recalled LOC. The genuinely-generalizable model (SoA-offset `Tape` +
`ValueRef<G>` + `select_classifier`) is cleanly separated from the
CSS/JSON-special-cased current state (eager `OpenFrame` + `CssTypedValue` +
no-`value_from_ref`), and crates/core is consistently the fold TARGET — never
measured-as-skinny.

The two cross-lens REVISEs folded into the V5 inventories are, from the OVERFIT
vantage, corrections in CH7's own direction-of-travel: the 1A Gaps-row struck a
spurious Lock-14-VIOLATION inflation (live state is Lock-14-ALLOWED `@generated`
code, header re-verified), and the 1E LAC-04 path-(b) re-price struck a
fold-target over-credit (pricing the absent `LayoutFacts` side-table as live,
`grep -rn LayoutFacts crates/`=0 re-verified). Both ground the corrected claim on
a live `grep`-zero anchor. The V5-new BSHAPE17-009 finding most central to the
mandate — "NEON JSON-only vs the shared-classifier generalization" — resolves
without contrivance: the shared `select_classifier(alphabet)` is ALREADY
grammar-general across 8 grammars in core (impl-exceeds-spec, 0 LOC fold,
narrative-only), every anchor re-verified live.

This is a clean **8 ACCEPT / 0 REVISE / 0 REJECT — 100%** confirming cycle. V1
was a VOID infrastructure cycle (all agents crashed, 0 CH files) and does NOT
count as a disposition cycle; the real CH7 trajectory is V3 (6A/2R), V4 (8A/0R),
V5 (8A/0R), V6-confirm (8A/0R). Per ORCHESTRATOR §3Z, V5 98.7% + this cycle 100%
constitutes the formal **two consecutive ≥95%-ACCEPT cycles** from the
OVERFIT-PRUNE vantage. From CH7's vantage, T-P1 SK-V17 is CONVERGED.
