# AZ.W0 — Stale-Test Retirement + ebnf_prettify Diagnosis + AX.FINAL

**Opens after**: tranche open
**Agents**: 1 serial (retire + AX.FINAL) + 1 parallel (ebnf_prettify diagnosis)
**Hard gate**: `cargo test --workspace` compiles clean; `post-AX-W1-close.json` + `AX/FINAL.md` committed; ebnf_prettify recognizer bug diagnosed in `audit/AZW0-ebnf-diag.md` with proposed fix path.

## Scope

1. **Retire 5 stale W0a/W0b-era wire-contract test files** (invariant 14): files reference retired shape predicates + carved `GrammarProfile` fields. Retirement was blocked in the prior session by interactive permission denial; AZ.W0 formalizes the retirement with explicit commit authorization.
2. **Diagnose ebnf_prettify recognizer bug**: `EbnfParser::parse("digit = \"0\" ;")` fails at offset 0. Pre-existing. Not caused by W1r landings. Produce diagnostic identifying the specific divergence between `EbnfParser` (derived from `grammar/ebnf/ebnf.bbnf`) and `BbnfEmit` parsing the same source successfully.
3. **AX.FINAL artefacts**: capture `docs/benchmarks/post-AX-W1-close.json` over the 18-entry matrix at HEAD `9074a685`; author `docs/tranches/AX/FINAL.md` summarizing AX outcomes; append `docs/tranches/AX/PROGRESS.md` with the AX-closed entry.

## File bounds

| File | Access |
|------|--------|
| `crates/core/tests/bbnf_profile_wire_contract.rs` | delete |
| `crates/core/tests/grammar_profile_wire_contract.rs` | delete |
| `crates/core/tests/json_parity_shape_emit.rs` | delete |
| `crates/core/tests/gate_predicate_wire_contract.rs` | delete |
| `crates/core/tests/aw_v_w5_2_per_ref_routing.rs` | delete |
| `docs/benchmarks/post-AX-W1-close.json` | create |
| `docs/tranches/AX/FINAL.md` | create |
| `docs/tranches/AX/PROGRESS.md` | modify (append close entry) |
| `docs/tranches/AZ/audit/AZW0-ebnf-diag.md` | create |

**Do NOT touch**: any `.bbnf` grammar, any `crates/core/src/backend/`, any generated.rs, any Value API surface. Out of scope for W0.

## Phase sub-items

### AZ.W0.1 Retire 5 stale wire-contract tests

Mechanism: `git rm` 5 files in a single commit with body explaining invariant-14 retirement + linking each file to its retired predicate / carved field:

- `bbnf_profile_wire_contract.rs` → `GrammarProfile` fields carved in W0b.A (`b7aa41c0`).
- `grammar_profile_wire_contract.rs` → same root cause.
- `json_parity_shape_emit.rs` → `dta_run_JsonGrammar` walker symbol deleted in W0b.A (`a206b962`).
- `gate_predicate_wire_contract.rs` → `has_w4_classified` / `has_full_shape_coverage` / `has_shape_dispatcher_entrypoint` admission-widened in W0a.2.j (`5f0709fc`).
- `aw_v_w5_2_per_ref_routing.rs` → same predicates.

Files touched: 5 deletes. **Sub-gate**: `cargo test --workspace --no-run` compiles clean after the commit.

### AZ.W0.2 ebnf_prettify recognizer diagnosis (parallel)

Reproduce `EbnfParser::parse("digit = \"0\" ;")` failure. Instrument `EbnfParser` (derived from `ebnf.bbnf`) vs `BbnfEmit::parse(grammar/ebnf/ebnf.bbnf)` which succeeds. Identify which IR rule rejects at offset 0. Two likely causes: (a) grammar-level rule ordering changed post-W1r and EBNF grammar needs regen; (b) a shape-emitter produces divergent code for this particular entry-rule shape.

Files read: `grammar/ebnf/ebnf.bbnf`, generated `ebnf_prettify.rs` target output, `crates/core/src/backend/rust/emitter/shapes/*.rs` at relevant dispatch sites.

Files touched: 1 new diag doc under `docs/tranches/AZ/audit/AZW0-ebnf-diag.md`. **Sub-gate**: diag doc identifies collapse site + proposes fix scope (grammar vs codegen).

### AZ.W0.3 AX.FINAL bench capture + close doc

Bench: run 18-entry matrix at master HEAD `9074a685` via the standard bench-regression workflow. Cold per-parse, mimalloc, release profile. Save to `docs/benchmarks/post-AX-W1-close.json` with same schema as `post-AX-W0a-close.json` (`1241e7ac`).

FINAL.md: author `docs/tranches/AX/FINAL.md` summarizing AX outcomes — W0a cascade, W0b deletions, W0c doc rewrite, W1 grammar-derived rewrite, scope-reveals + how each was absorbed or deferred, handoff to AZ. Cite commit SHAs throughout.

PROGRESS.md append: single entry `## 2026-04-20 — AX closes; AZ opens` citing the bench file + FINAL.md + W1 close.

Files touched: create 2, modify 1. **Sub-gate**: all 3 files landed, `post-AX-W1-close.json` has 18/18 numeric entries (W0a.close schema).

## Hard gate

1. `cargo test --workspace --no-run --profile ax-iter` compiles clean after AZ.W0.1. Artefact: commit SHA + CI log.
2. `cargo test --workspace --profile ax-iter` runs clean after ebnf_prettify fix (if W0.2's diagnostic identifies a mechanical fix within W0 scope) OR ebnf_prettify tests `#[ignore]`'d with AZW0-ebnf-diag.md citation (if fix exceeds W0 scope; defer to dedicated AZ sub-wave).
3. `docs/benchmarks/post-AX-W1-close.json` exists with 18 entries + numeric `ns_per_iter` + `bytes_per_ns` per entry. Artefact: file at path.
4. `docs/tranches/AX/FINAL.md` committed. Artefact: file at path.
5. `docs/tranches/AX/PROGRESS.md` has AX-close entry. Artefact: grep for "AX closes; AZ opens" returns 1 match.

## Verification artefacts

- `docs/benchmarks/post-AX-W1-close.json` — 18-entry cold bench matrix.
- `docs/tranches/AX/FINAL.md` — AX tranche summary.
- `docs/tranches/AX/PROGRESS.md` — append entry.
- `docs/tranches/AZ/audit/AZW0-ebnf-diag.md` — ebnf_prettify root-cause.
- Commit SHAs: one for test retirement, one for AX.FINAL, one for ebnf diag.

## Dependencies

- **Depends on**: tranche open (AZ.md + wave specs + A1-A6 audit reports on master).
- **Blocks**: W1 (W0 clears compile blockers + establishes AX baseline for W1 bench deltas).

## Archaeology

W0b.D (`0d730c8f` + `6854f18b` + `e839378c`) deleted 8 DTA-coupled test suites but missed these 5 invariant-14 wire-contract tests — predicates retired in W0a.2.j's admission widening + W0b.A's profile-slot carving without the corresponding test retirements. AZ.W0 closes the audit-trail.
