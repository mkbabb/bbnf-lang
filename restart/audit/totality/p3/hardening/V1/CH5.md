---
lens: CH5
name: HIDDEN COUPLING
pass: T-P3-synthesis
cycle: V1
generated_at: 2026-05-23T00:00:00Z
files_audited:
  - restart/prompts/totality/PASS-3-SYNTHESIS.md
  - restart/audit/totality/p3/3A-architecture-synthesis.md
  - restart/audit/totality/p3/3B-master-plan-reconciliation.md
  - restart/audit/totality/p3/3C-locks-crystallisation.md
  - restart/audit/totality/p3/3C-locks-v+1-diff.md
  - restart/audit/totality/p3/3D-skinny-fold.md
  - restart/audit/totality/p3/3E-grammar-generalisation.md
  - restart/audit/totality/p3/3F-migration-handoff.md
scope: "CH5 hidden coupling only — V1 dispatch foci: LAC-2F-V5-02 elevation strengthens substrate-union; LAC-1E-14 FactStream as 5th SUBSTRATE category preserves 5-shape BackendShape canon; 3A D06 ratify-or-unify disposition; substrate-union holds across 3A surface deltas; 3C accepted amendments introduce no coupling."
---

# CH5 Hidden Coupling — T-P3 V1

## Verdict

ACCEPT.

T-P3 V1 passes the CH5 hidden-coupling lens. No proposed delta implies a
parallel substrate, sidecar producer, renamed-scanner Lock 1 violation,
Track 1 ≡ Track 2 dishonesty, or accepted-amendment coupling. The
substrate union is coherent across 3A, 3C, 3E, and 3F: 3A proposes the
taxonomy and forces explicit substrate-union disposition (D06 ratify-or-
unify; D07 four-plane output taxonomy); 3C makes them enforceable in
Lock 1 / 8 / 14 / 16 and **elevates LAC-2F-V5-02 as the strongest
amendment surface** to bar all cross-call retained classifier state;
3E uses the taxonomy for non-JSON generality without introducing a sixth
shape; and 3F carries it into MIGRATION/HANDOFF only behind G3, Pass
Omega, and G-Omega. The two V4 substrate-related hunks (V4-2 elevation,
V4-3 FactStream 5th substrate category) are *strengthenings* of the
existing Lock 1 manifest, not coupling introductions.

## Evidence

| check | disposition | evidence |
|---|---|---|
| Pass contract | ACCEPT | PASS-3 defines CH5 as the guard against parallel substrate, sidecar producer, renamed-scanner Lock 1 violation, Track 1 ≡ Track 2 dishonesty, and accepted-amendment coupling (`restart/prompts/totality/PASS-3-SYNTHESIS.md:122`-`125`). It also bars new directives, BIR variants, and new substrates from being silently synthesised into the spec (`restart/prompts/totality/PASS-3-SYNTHESIS.md:214`). |
| 3A substrate union | ACCEPT | 3A explicitly preserves the no-new-directive / no-new-BIR / no-new-public-substrate / no-retained-sidecar / no-BackendShape-expansion gate (`restart/audit/totality/p3/3A-architecture-synthesis.md:23`). Its direct route requires same-schedule + same-plane digest equality + independent Track 2 parity + same-run comparators (`restart/audit/totality/p3/3A-architecture-synthesis.md:38`). It separates transient scanner / capacity inputs, comparator sidecars, admitted fact-stream outputs, and retained runtime substrate (`restart/audit/totality/p3/3A-architecture-synthesis.md:38`-`39`) and keeps parse-that scanner outputs local-temp or existing-tape/direct-sink unless G-Omega approves a new public substrate / BIR surface (`restart/audit/totality/p3/3A-architecture-synthesis.md:44`). |
| 3A D06 substrate-union ratify-or-unify | ACCEPT | ARCH-3A-D06 forces explicit T-P3 §3C resolution of the two-cursor (`ParserState.cursor` over offset-tape + `DirectParser.cursor` over raw bytes) vs unified-cursor question rather than leaving the §9.2 union claim ambiguous (`restart/audit/totality/p3/3A-architecture-synthesis.md:38`, `:55`, `:74`, `:90`). The CH5 axis row at `:90` names T-P3 §3C disposition + Pass Omega CRUD-1 as the receiver and 1A-DIV-008 / 1D `:117` as the two-can-hold-under-different-definitions evidence base. Per the CH5 contract, forcing the resolution into the open is itself the anti-coupling move; ambiguity at §9.2 is precisely how a parallel-substrate slip would enter the spec. |
| 3A D07 four-plane substrate taxonomy | ACCEPT | ARCH-3A-D07 adds §9 "Output-Plane Substrate Taxonomy" with four explicit planes: (1) retained tape (Eager/Offset/Event), (2) direct-sink (SinkOnly), (3) admitted same-plane fact-stream output (CSS L4 declaration-values), (4) transient scanner/capacity (StructuralIndex, comparator sidecars). The fact-stream plane is admitted-evidence with `substrate_target = admitted_fact_output`; it is *not* a sixth BackendShape, *not* retained substrate, *not* full CSS closure (`restart/audit/totality/p3/3A-architecture-synthesis.md:39`, `:56`, `:75`, `:88`). This makes admitted CSS fact-stream evidence and comparator-plane sidecars visible to Pass Omega; the pre-D07 surface left them invisible and at risk of later promotion to a sixth shape. |
| 3B planning watchpoint | ACCEPT | 3B's fresh-union material-differential wave is planning pressure, not current substrate permission: it requires row movement or architectural-block evidence (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:135`); 3B's implementation waves must retain same-wave consumers, LOC budgets, and abrogate gates (`restart/audit/totality/p3/3B-master-plan-reconciliation.md:144`). This remains acceptable only under 3C's Lock 1 fence. No 3B NEW wave proposes `UnionTape`, a second tape, or retained-class-column substrate as permitted material. |
| 3C executive summary | ACCEPT | 3C's executive summary attests the LOCKS diff adds no directive, BIR variant, BackendShape, public substrate API, or new lock, and keeps Pass Omega / G-Omega boundaries explicit (`restart/audit/totality/p3/3C-locks-crystallisation.md:23`). The disposition matrix accepts transient-mask and retained-sidecar prohibition into Lock 1 (`restart/audit/totality/p3/3C-locks-crystallisation.md:67`), routes non-JSON telemetry through BENCH gate consumers (`restart/audit/totality/p3/3C-locks-crystallisation.md:68`), keeps CSS fact streams as evidence not closure (`restart/audit/totality/p3/3C-locks-crystallisation.md:70`), and requires `substrate_target`, `retention_lifetime`, and `policy_owner` on primitive/scanner/union rows (`restart/audit/totality/p3/3C-locks-crystallisation.md:77`). |
| **LAC-2F-V5-02 elevation = STRENGTHENING** | ACCEPT | Hunk V4-2 ("no cross-call retained classifier state, period") appends to Lock 1 substrate-union manifest at `restart/locks/LOCKS.md:90` (`restart/audit/totality/p3/3C-locks-v+1-diff.md:86`-`116`). The diff text generalises the closure of REDRESS 96/97/98 (retained class-column / streaming structural cursor / class-lane-only) to **all** transient classifier-state primitives — quote-mask, escape-mask, structural-mask, class-stream, prev-state byte, prefix-XOR carry word (`restart/audit/totality/p3/3C-locks-v+1-diff.md:94`-`107`). It binds `retention_lifetime ∈ {transient-single-call, retained-within-chunk, retained-across-call-boundary}` with the third value as the REJECT class. This is unambiguously a *strengthening*: it widens the existing substrate-union prohibition to cover unforeseen primitive shapes without introducing a new substrate, a new directive, a new BIR variant, or a new public API. The 3C matrix carries it as "STRONGEST AMENDMENT SURFACE" with ACCEPT-ELEVATED disposition (`restart/audit/totality/p3/3C-locks-crystallisation.md:124`, `:133`, `:157`). The elevation extends rather than replaces REDRESS 96/97/98 (`restart/audit/totality/p3/3C-locks-crystallisation.md:30`-`31`: "REDRESS 96/97/98 material-differential — already merged at HEAD; no v+1 delta" + "ELEVATED amendment … generalises REDRESS 96/97/98 to ALL transient classifier-state primitives"). Per-call composed primitives (e.g. `scan_string_with_carry_64`) remain admissible substrate-union-compatible (`restart/audit/totality/p3/3C-locks-v+1-diff.md:105`-`108`). No retained surface is created. |
| **LAC-1E-14 = 5th SUBSTRATE category, NOT 6th BackendShape** | ACCEPT | Hunk V4-3 appends `FactStream` as the 5th admitted-product category at the Lock 1 substrate manifest, alongside `OffsetTape`, `EventTape`, `SinkOnly`, `CollapsedStage` (`restart/audit/totality/p3/3C-locks-v+1-diff.md:118`-`143`). The diff body is explicit on the separation-of-axes: "The 5th category is a substrate-manifest classification only; it is NOT a 6th `BackendShape` variant. The 5-shape `BackendShape` search domain at Lock 10 holds: `{EagerTape, OffsetTape, EventTape, SinkOnly, CollapsedStage}`. Adding a 6th `BackendShape` variant remains G-Omega gated per Lock 10 v+1 and PASS-3 §8.1." (`restart/audit/totality/p3/3C-locks-v+1-diff.md:129`-`134`). The 3C executive summary records `FactStream` 5th category resolves the CSS L4 classification gap while preserving the 5-shape `BackendShape` canon at Lock 10 (`restart/audit/totality/p3/3C-locks-crystallisation.md:144`, `:158`). The matrix row LAC-1E-14 names the carrier note "5th *substrate* class, NOT 5th `BackendShape` variant" (`restart/audit/totality/p3/3C-locks-crystallisation.md:32`, `:119`, `:134`). 3C-L01-factstream-fifth-category routes the hunk explicitly through the substrate manifest, not the search domain. The 16-lock count and 5-shape `BackendShape` canon are both preserved (`restart/audit/totality/p3/3C-locks-v+1-diff.md:361`). |
| 3C accepted amendments introduce no coupling | ACCEPT | 51-candidate disposition matrix carries 38 ACCEPT + 13 MODIFY + 0 REJECT + 0 DEFER (`restart/audit/totality/p3/CHALLENGE-CONTEXT.md:15`). Each Lock 1 / Lock 8 / Lock 10 / Lock 14 / Lock 16 hunk routes through the existing manifest/search-domain/comparator/audit-overlay axes already in LOCKS at HEAD; the diff body never invokes a new directive, public substrate API, retained sidecar, BackendShape expansion, or new lock (`restart/audit/totality/p3/3C-locks-crystallisation.md:23`; `restart/audit/totality/p3/3C-locks-v+1-diff.md:361`-`366`). Closing posture at `restart/audit/totality/p3/3C-locks-v+1-diff.md:366`: "No implementation wave may use proposed v+1 wording as permission to … add a directive, add a BIR variant, add or retire a lock, expand `BackendShape` (the 5-shape canon at Lock 10 holds even with LAC-1E-14 5th substrate category folded), add a public substrate API, retain a sidecar, or dispatch SK-V14 W0 / S-P3 / SK-V13 wave admission before G-Omega closes." |
| 3C Lock 1 fence | ACCEPT | The proposed Lock 1 hunk treats Track 2 as a substrate-ceiling probe, not a second substrate, and does not authorise hidden runtime identity, parser-owned sidecars, or parallel representation (`restart/audit/totality/p3/3C-locks-v+1-diff.md:59`-`63`). Fact streams are output-plane contracts, not retained internal sidecars (`restart/audit/totality/p3/3C-locks-v+1-diff.md:75`-`78`). Every e-graph candidate, backend rewrite, imported scanner plan, union candidate, and SIMD consumer must declare allowed target / lifetime / owner fields; any retained class/mask stream, parser-owned cursor/list state, public substrate API, `UnionTape`, or second tape is rejected unless G-Omega amends Lock 1 (`restart/audit/totality/p3/3C-locks-v+1-diff.md:82`-`90`). |
| Comparator and Track planes | ACCEPT | Lock 8 makes JSON parse/direct/typed rows separate gates, treats CSS as one fact-stream row rather than full closure, and restricts sidecars to strict anchors with matching corpus, output plane, host, strictness, freshness, status, and gate-consumed provenance (`restart/audit/totality/p3/3C-locks-v+1-diff.md:140`-`156`). Direct digest or SIMD sub-hash acceleration is admissible only when Track 1, Track 2, serde, and sonic strict equality hold for the same semantic output plane (`restart/audit/totality/p3/3C-locks-v+1-diff.md:168`-`173`). Track 1 ≡ Track 2 dishonesty is structurally blocked: the equality is required at the output plane, not the substrate. |
| Renamed-scanner / parse-that boundary | ACCEPT | Lock 10 requires regex/HIR facts where regex or scanner plans influence backend-shape or scanner selection, and blocks opaque-pattern-only SIMD / scanner / backend admission (`restart/audit/totality/p3/3C-locks-v+1-diff.md:209`-`215`). Lock 16 routes parse-that facades, generated scanners, and collapsed-stage code through manifest rows that include strict checkasm/parity, substrate target, retention lifetime, policy owner, same-wave consumer, rollback, and final disposition (`restart/audit/totality/p3/3C-locks-v+1-diff.md:337`-`345`). ARCH-3A-D12 §7.5 + §10 parse-that / regex import boundary keeps SIMD scanner outputs local-temp-only or feeding existing tape / direct-sink with no new public substrate or BIR surface (`restart/audit/totality/p3/3A-architecture-synthesis.md:44`). A renamed scanner cannot re-enter as a new substrate. |
| 3D skinny fold | ACCEPT | 3D-FOLD-001 folds JSON offset tape, direct SinkOnly projection, and CSS fact-stream rows into one substrate family with fenced output planes while preserving the ban on parallel substrates (`restart/audit/totality/p3/3D-skinny-fold.md:144`). The SK-V12 CSS row carries V1-AUTH as "admitted evidence as fact-stream output plane; not a sixth BackendShape; not full CSS parity" (`restart/audit/totality/p3/3D-skinny-fold.md:92`). 3D routes the decision-engine replacement as not a new directive, BIR, BackendShape, or substrate (`restart/audit/totality/p3/3D-skinny-fold.md:75`) and verifies non-JSON import consumes existing five shapes rather than creating hidden variants (`restart/audit/totality/p3/3D-skinny-fold.md:89`). The 8-candidate Stage-A shortlist (C1..C8) is bound to Lock 1 + Lock 14 + Lock 16 with `substrate_target` triad per candidate; C2 `structural_index_singular_substrate_consumer` is named "substrate-union typed-skip" — the singular-substrate adjective is load-bearing (`restart/audit/totality/p3/3D-skinny-fold.md:160`). |
| 3E generality | ACCEPT | 3E keeps the five BackendShape variants and makes selection, primitive policy, sinks, flags, providers, and costs generated from grammar data (`restart/audit/totality/p3/3E-grammar-generalisation.md:23`-`38`). The matrix treats `CollapsedStage` only as a generated transient emitted strategy, never a retained sidecar. Future grammar onboarding (3E-D06) fails closed if it needs a new directive, BIR variant, BackendShape, public substrate API, retained sidecar, or hand-coded generic behaviour. 3E-D05 classifies CSS fact streams as admitted output planes, not retained sidecars and not a sixth BackendShape. L14-HC-07 "fact streams are output planes": "valid admitted output planes only with strict comparator/oracle provenance and gate-consumed telemetry. They are not hidden retained sidecars and do not create a sixth `BackendShape`." (`restart/audit/totality/p3/3E-grammar-generalisation.md:208`). |
| 3F migration and handoff | ACCEPT | 3F narrows Lock 1 closure to admitted grammar-runtime substrate with fenced non-substrate fact streams. It replaces rename/defer-only rows with archive/removal/source-present gates, reclassifies hand-owned per-grammar runtime surfaces as generated/rostered/archived/fixture-only, and adds a CSS telemetry/fact-output plane instead of forcing CSS into EventTape or retained substrate categories (`restart/audit/totality/p3/3F-migration-handoff.md:80`). 3F-MIG-004 binds CSS L4 row to `admitted_fact_output` substrate_target with comparator provenance mandatory until T-P3 §3C disposes (`restart/audit/totality/p3/3F-migration-handoff.md:104`, `:125`). Decision-engine receiver row 3F-MIG-005 explicitly distinguishes the W7 `same_substrate_union` enforcement-layer pass from the SK-V9 W3 retired retained-class-column-union data structure (PERMANENT-PRE-BLOCK per REDRESS 96/97/98) (`restart/audit/totality/p3/3F-migration-handoff.md:127`). Proposed handoff blocks source / generated / gate / RESULTS / REDRESS edits until G3, Pass Omega, and G-Omega conditions are met. CH2 axis row at `:311` correctly defers the BackendShape-canon question to T-P3 §3C disposition; CH5 axis row at `:327` confirms "5-shape BackendShape canon coherent across 3A + 3B + 3E … 3F MIG-004 fact-stream row defers BackendShape expansion to §3C disposition; does not unilaterally propose 5th variant." |

## Hidden-Coupling Sub-Lens Checks (V1 dispatch focus)

| sub-check | result | rationale |
|---|---|---|
| Parallel substrate (UnionTape / second tape / class-column substrate)? | NEGATIVE | 3C-L01-substrate-union-v+1-elevation explicitly REJECTs `UnionTape`, second tape, retained class/mask stream, parser-owned cursor/list state, public substrate API unless G-Omega amends Lock 1 (`restart/audit/totality/p3/3C-locks-v+1-diff.md:82`-`90`). LAC-2F-V5-02 elevation generalises this to ALL transient classifier-state primitives. 3D-FOLD-001 keeps JSON tape + direct + CSS fact-stream as ONE substrate family with fenced output planes. |
| Sidecar producer? | NEGATIVE | Lock 8 restricts sidecars to strict anchors with matching corpus, output plane, host, strictness, freshness, status, gate-consumed provenance (`restart/audit/totality/p3/3C-locks-v+1-diff.md:140`-`156`). 3F-MIG-004 binds CSS L4 fact-stream to `admitted_fact_output` substrate_target with comparator provenance mandatory. 3E L14-HC-07 attests fact streams are output planes, not hidden retained sidecars (`restart/audit/totality/p3/3E-grammar-generalisation.md:208`). |
| Renamed-scanner Lock 1 violation? | NEGATIVE | Lock 10 requires regex/HIR facts and blocks opaque-pattern-only SIMD/scanner/backend admission (`restart/audit/totality/p3/3C-locks-v+1-diff.md:209`-`215`). Lock 16 routes parse-that facades / generated scanners / collapsed-stage code through manifest rows with strict checkasm/parity, substrate target, retention lifetime, policy owner (`restart/audit/totality/p3/3C-locks-v+1-diff.md:337`-`345`). ARCH-3A-D12 keeps SIMD scanner outputs local-temp-only or feeding existing tape/direct-sink without a new public substrate or BIR surface. |
| Track 1 ≡ Track 2 dishonesty? | NEGATIVE | Lock 1 hunk: "Track 2 is a substrate-ceiling probe, not a second substrate, and does not authorise hidden runtime identity, parser-owned sidecars, or parallel representation" (`restart/audit/totality/p3/3C-locks-v+1-diff.md:59`-`63`). Lock 8 makes parse/direct/typed rows separate gates with strict-equality required at the semantic output plane, not at the substrate. |
| Substrate-union holds across 3A surface deltas? | POSITIVE | 3A deltas D01..D12 preserve the substrate-union fence + 5-shape canon + no-new-substrate gate (`restart/audit/totality/p3/3A-architecture-synthesis.md:23`). D06 ratify-or-unify forces the disposition into the open; D07 four-plane taxonomy classifies fact-stream + sidecars without expanding BackendShape; D11 binds `CollapsedStage` to x86 architecture pressure; D12 binds parse-that / regex import boundary. |
| 3C accepted amendments introduce coupling? | NEGATIVE | Zero REJECT + zero DEFER on 51 candidates; the V4 diff carries the explicit "adds no directive, BIR variant, BackendShape, public substrate API, or new lock" attestation (`restart/audit/totality/p3/3C-locks-crystallisation.md:23`); closing posture at `restart/audit/totality/p3/3C-locks-v+1-diff.md:366` reiterates the same fence. |
| LAC-2F-V5-02 elevation: strengthening vs introducing? | STRENGTHENING | Hunk V4-2 generalises an existing REDRESS-merged Lock 1 prohibition (REDRESS 96/97/98) to all transient classifier-state primitives. No new substrate, directive, BIR variant, or public API is added. The hunk widens the REJECT class for cross-call retention; admissible per-call composed primitives remain (`restart/audit/totality/p3/3C-locks-v+1-diff.md:105`-`108`). The matrix carrier classifies it as ELEVATION not ADDITION (`restart/audit/totality/p3/3C-locks-crystallisation.md:31`, `:124`). |
| LAC-1E-14 FactStream: 5th SUBSTRATE category vs 6th BackendShape? | 5th SUBSTRATE (manifest-level only) | Hunk V4-3 explicitly states "5th category is a substrate-manifest classification only; it is NOT a 6th `BackendShape` variant" and reiterates the Lock 10 search domain at five shapes (`restart/audit/totality/p3/3C-locks-v+1-diff.md:129`-`134`). LAC-1E-14 matrix row carries "5th *substrate* class, NOT 5th `BackendShape` variant" (`restart/audit/totality/p3/3C-locks-crystallisation.md:32`, `:119`, `:134`). 5-shape canon preserved across 3A + 3B + 3E + 3F (`restart/audit/totality/p3/3F-migration-handoff.md:327`). The two axes — Lock 1 substrate manifest and Lock 10 BackendShape search domain — are orthogonal; LAC-1E-14 touches only the manifest axis. |

## Repairs

Required blocking repairs: none.

Carry-forward guardrails for Pass Omega and later S-P3 / Omega work
(non-blocking; intended for the consolidated cycle handoff):

1. Preserve 3C's exact Lock 1 `substrate_target` / `retention_lifetime` /
   `policy_owner` vocabulary on every primitive / scanner / union row;
   do not relax it into generic "scanner facts" or "runtime facts"
   wording. The LAC-2F-V5-02 elevation specifically institutionalises
   `retention_lifetime ∈ {transient-single-call, retained-within-chunk,
   retained-across-call-boundary}` with the third value as the REJECT
   class.
2. Read 3B MP.NW10 only as a fresh material-differential candidate
   inside the existing tape / direct / admitted-output discipline, or as
   a G-Omega-approved Lock 1 amendment / block. It is not permission for
   `UnionTape`, a second tape, or a retained class-column substrate.
   3F-MIG-005's `same_substrate_union` enforcement-layer framing is the
   correct mental model.
3. Keep CSS lightningcss / source sidecars comparator-only. Every future
   CSS row must emit output-plane provenance and must not add a runtime
   dependency on comparator sidecars. The LAC-1E-14 FactStream 5th
   substrate category is manifest-level classification only and must
   not be promoted to a 6th `BackendShape` variant without G-Omega Lock
   10 amendment.
4. Keep Track 2 language precise: Track 2 is an independent parser-path
   and substrate-ceiling probe over the same runtime APIs, not an
   independent substrate and not evidence that Track 1 ≡ Track 2.
   Strict-equality remains required at the semantic output plane per
   Lock 8.
5. 3A D06 ratify-or-unify disposition is a T-P3 §3C deliverable that
   must propagate into ARCHITECTURE.md §9.2 prose + Lock 1 v+1 manifest
   wording post-G-Omega; ambiguity at §9.2 is the precise channel by
   which a parallel-substrate slip would re-enter the spec.

## Cycle Disposition

CH5 disposition for T-P3 V1: ACCEPT.

ACCEPT-rate: 13/13 evidence rows + 8/8 sub-lens checks (100%).
No REJECT, no REVISE, no DEFER, no orphan finding.

V1 advances toward the §3Z ≥95% × 2-consecutive-cycle LOCK criterion;
CH5 contributes a clean ACCEPT to the V1 cohort.
