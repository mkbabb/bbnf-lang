# AZ-II.W2 — FINAL (`crates/tape/` deletion + parity recoding + BA handoff)

**Status**: superseded (2026-04-28). The `crates/tape/` deletion +
view recode + parity harness recode fold into **AZ-II.cutover.C** per
`docs/tranches/AZ-I/audit/W2-CLOSE-AUDIT.md` §9. The §9 ledger of
tape-substrate prune candidates absorbs into cutover.A's hoist +
deletion sweep (`tape::dta` → `bbnf-ir::dta`; `tape::visitor` family
deletion; tape driver dead helpers).

**Opens after** (superseded): AZ-II.W1 close (byte-equal green)
**Agents**: 2 parallel (tape deletion; view / pprint / debug recode)
**Hard gate**: `crates/tape/` deleted from the tree; no non-test
reference to live tape symbols remains; view layer, pprint, and
`@debug` directive rewired to struct-tree walkers; parity harnesses
all on struct-vs-external-native comparison; full 17-entry matrix
at or above AZ-I close on the struct-only path; BA handoff contract
verified; `FINAL.md` lands.
**Status**: planned

## Scope

W2 is FINAL. It opens on a tree that already runs Stage B as the
default compiler (W1 close), with `crates/tape/` still on disk
but unreachable from any production parse path. W2 deletes the
crate, rewires every remaining downstream consumer to the struct
tree, and verifies the BA handoff contract.

1. Delete `crates/tape/` directory and every file under it.
2. Rewire every remaining consumer to the struct path:
   - `crates/core/src/backend/driver/` — view layer for BBNF.
   - `crates/pprint/` — pretty-printer tape-record rendering
     migrates to struct-tree walker.
   - `@debug` directive lowerer under `crates/core/src/backend/` —
     migrates from tape-replay to struct-tree walker.
   - Parity harnesses under `tests/` — rewired to
     struct-vs-external-native where not already done.
3. Retire `crates/core/src/runtime/mod.rs` alias shims (any
   re-exports that preserved tape compatibility during AZ-I
   W2/W3 or AZ-II W1).
4. Run the tape-deleted grep checks: `rg '^crates/tape/'` →
   zero; `rg '::bbnf::runtime::tape|bbnf::runtime::tape|use tape::|\btape::|\bTape(Rec|Builder|Cursor|Offset|Kind)\b|\bColumns\b|\bFinaliser\b|\bDTA\b|\bPSI\b|Fused(Build|Output)' crates --glob '*.rs'`
   → zero.
5. Confirm `cargo build -p bbnf --no-default-features` succeeds
   without `crates/tape/` existing.
6. Run the full 17-entry matrix on the tape-deleted struct-only
   path; capture samply fleet under
   `docs/benchmarks/profiles/AZ-II/W2/`.
7. Verify the BA handoff contract (AZ-II.md §Handoff contract to
   BA): all four grammars struct-only; tape deleted; registry
   closed fleet-wide; parity harnesses rewired; full matrix at
   AU parity; bootstrap reproducibility as permanent CI gate;
   parent-pointer decision surface open to BA.W0.
8. Author `docs/tranches/AZ-II/FINAL.md` with deltas, reversals
   taken (if any), and the BA handoff verification report.

## File bounds

| File | Access |
|---|---|
| `crates/tape/` | delete (entire directory) |
| `Cargo.toml` (workspace) | edit — remove `crates/tape` from members |
| `crates/core/src/backend/driver/*` | edit — struct-tree rewire |
| `crates/pprint/*` | edit — struct-tree walker |
| `crates/core/src/backend/debug.rs` | edit — `@debug` lowering to struct walker |
| `crates/core/src/runtime/mod.rs` | edit — retire alias shims |
| `tests/*_parity.rs` | edit — final rewires if any |
| `docs/benchmarks/AZ-II/W2/final-matrix.json` | create |
| `docs/benchmarks/profiles/AZ-II/W2/` | create |
| `docs/tranches/AZ-II/FINAL.md` | create |

**Do NOT touch**: `grammar/bbnf/bbnf.bbnf` (frozen at W1 close);
any runtime emitter code for the four grammars (frozen at W1
close); `tests/bbnf_bootstrap_reproducibility.rs` (permanent CI
gate, must continue passing through the W2 deletion).

## Phase sub-items

### AZ-II.W2.1 — Consumer rewire (view / pprint / @debug)

Walk every remaining live tape consumer per the pre-W2 grep.
Rewire to struct-tree equivalents:

- View layer: `tape-cursor advance` → `struct field access`.
- pprint: tape-record render → struct-node render via derived
  `StructLayout` metadata.
- `@debug`: tape-replay → struct-tree DFS walker with the same
  output shape.

Sub-gate: the live-symbol tape scan has zero matches at W2.1
close outside files explicitly owned by W2.2/W2.3.

### AZ-II.W2.2 — Tape crate deletion + workspace update

`rm -r crates/tape/`; remove from workspace `Cargo.toml` members
list; run `cargo build --workspace` to confirm no build break;
run the tape-deleted grep checks.

Sub-gate: `cargo build -p bbnf --no-default-features` green;
`crates/tape/` does not exist; all three grep checks pass.

### AZ-II.W2.3 — Alias shim retirement

Remove any re-exports or type aliases in
`crates/core/src/runtime/mod.rs` that preserved tape compatibility
during earlier waves.

Sub-gate: no `pub use tape::*`, `pub mod tape`, `pub use
runtime::tape::*`, or equivalent compatibility export remains.

### AZ-II.W2.4 — Final matrix + samply + FINAL.md

Run the 17-entry matrix three times (cold only); capture samply;
verify BA handoff contract; author `FINAL.md`.

Sub-gate: full matrix at AU parity or better; samply captures
present; `FINAL.md` lands with deltas + handoff report.

## Hard gate

1. `crates/tape/` deleted; workspace rebuilds cleanly.
2. Tape-deleted grep checks all pass (`^crates/tape/` → zero;
   live tape-symbol scan → zero outside archive).
3. `cargo build -p bbnf --no-default-features` green without
   `crates/tape/` existing.
4. View, pprint, `@debug` all rewired to struct-tree walkers.
5. Parity harnesses green on all four grammars.
6. Bootstrap reproducibility test
   (`tests/bbnf_bootstrap_reproducibility.rs`) green on the
   tape-deleted commit.
7. Full 17-entry matrix at or above AZ-I close on the struct-only
   path for all four grammars.
8. BA handoff contract verified (all seven points).
9. `FINAL.md` lands with deltas and reversal log.
10. `cargo nextest run --workspace --profile ax-iter` ≥ 1480 pass
    (post-B4 baseline per `docs/tranches/B4/FINAL.md` test
    summary).

## Reversal criteria

Any non-test consumer not rewired at the close commit → W2 does
not close; rewire the consumer before close.

Bootstrap reproducibility test fails on the W2 commit (possibly
due to the view-layer rewire reading from a different struct
field than the pre-W2 tape path) → revert the responsible rewire;
re-run; any remaining drift routes to a W2' sub-wave, not to BA.

## Dependencies

- **Depends on**: AZ-II.W1 close (byte-equal green).
- **Blocks**: BA open.
