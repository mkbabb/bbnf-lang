# CH2 GENERALITY — SK-V18 T-P2 (cycle V1)

Lens: CH2 GENERALITY. Lock 14 holds — every primitive/technique must be grounded
grammar-NEUTRALLY; 2C must show the technique transferring to CSS L4 / Sheets /
BBNF-self / the 9-grammar fleet, not JSON-only. A technique grounded JSON-only but
used fleet-wide is a REVISE. A neutrality-proof that leans on a JSON consumer which
does not exist in production (while the kernel is in fact fleet-used on CSS) is the
same defect.

Disposition: **REVISE** (the packet's GENERALITY framing is broadly sound and
mostly grammar-neutral, but three dossiers ground the §6 eq-set inner kernel's
grammar-neutrality on a JSON consumer that does not exist in production code).

## Verdict Census

13 CH2-relevant groundings/refutations enumerated: 8 ACCEPT, 4 REVISE, 1 REJECT.

## Critical Findings

| id | dossier | disposition | finding | falsifying / corroborating evidence |
|---|---|---:|---|---|
| CH2-V1-01 | 2B | **REVISE** | SKV18-A1 / Tech-Grounding row 1 assert the eq-set kernel `byte_class_from_eq_set_64` "already rides BOTH the JSON `scan_dispatch`/`byte_class_from_table_64` path AND the CSS `count_top_level_commas` path", offered as the genuine-neutrality proof of the §6 inner sub-kernel. The JSON consumer does not exist: `byte_class_from_eq_set_64` has ZERO non-test, non-bench callers in `skinny/crates/runtime/` outside `runtime_simd.rs`, whose ONLY callers (`QUOTES`/`COMMA`/`set_a`/`set_b`) are the CSS path. `scan_dispatch` consumes `byte_class_from_table_64` (the TBL classifier) — a DIFFERENT primitive. The two are conflated. Correction: the inner kernel's neutrality must be grounded structurally (caller-supplied byte set, names no grammar — which is true) NOT by a fictitious JSON consumer; per Lock 14's bbnf-simd clause the kernel has only a CSS production consumer and must record that honestly (the abstract neutrality stands; the "rides JSON too" empirical claim is false). | `rg byte_class_from_eq_set_64 skinny/crates/runtime/src/grammars/json/` = 0 hits; `runtime_simd.rs:44,56,199` are the only non-test runtime callers (all CSS); `find_ascii_set_member64` callers = report.rs (telemetry strings) + checkasm tests only. `json/scan.rs:22-29` `scan_structurals` → `scan_structurals_scalar` (no eq-set call). S-P1 `SYNTHESIS-PROFILE.md:75-79` JSON path scan-free, `json/scan.rs` G5-neutralize. |
| CH2-V1-02 | 2F | **REVISE** | PTG-2F-10 (state "GROUNDED + WIRED, Lock 16 FULLY SATISFIED") and Architectural Assertion 3 ("already rides BOTH the JSON `scan_dispatch`/`byte_class_from_table_64` path and the CSS `count_top_level_commas` path") repeat the same false dual-consumer claim. The eq-set kernel is live-wired on CSS only. Correction: keep the (correct) structural-neutrality argument (set is caller data) but strike the JSON-rides-it empirical claim; PTG-2F-10's "Lock 16 FULLY SATISFIED for this kernel TODAY" overstates the same-wave-consumer cell — its only live production consumer is CSS, the JSON one named is a different kernel. | same evidence as CH2-V1-01; `runtime_simd.rs:6-7` comment "the same kernel JSON's `scan_structurals` rides" is inaccurate — `scan_structurals` rides `byte_class_from_table_64`, not the eq-set kernel. |
| CH2-V1-03 | 2C | **REVISE** | V3 Tech-Grounding (SK-V18-2C-CSS-BALANCED-SCAN…) and V3 Assertion 2 state the INNER eq-set sub-kernel "IS genuinely neutral (proven by JSON's own `find_ascii_set_member64` consumer)". `find_ascii_set_member64` has ZERO runtime-grammar (non-test, non-bench) callers anywhere in skinny. Correction: the FORCED `css_balanced_component_scan` demotion of the SHELL is correct and well-grounded; but the INNER-kernel neutrality cannot cite a JSON `find_ascii_set_member64` consumer that does not exist in production. Re-ground on the structural fact (caller-supplied ≤8-byte set, kernel names no grammar) and disclose the inner kernel currently has only a CSS production consumer. | `rg find_ascii_set_member64 skinny/crates/runtime/` = 0; all 5 non-def references are report.rs telemetry + checkasm test harness. |
| CH2-V1-04 | 2A | **REVISE** | T2A-V18-JSON-SONIC-001 grounds the JSON >SOTA bar (strict-vs-sonic-rs-strict) and the SinkOnly direct path as the same plane — sound and grammar-appropriate. But the dossier carries the >SOTA-preservation framing under the "ONE grammar-driven generator emitting JSON+CSS+Sheets" GENERALIZATION header (Executive Summary, line 142-145) while every measured close row it cites is JSON-or-CSS only; Sheets/BBNF-self/the remaining 6 fleet grammars have NO grounded SOTA plane. The fleet-wide ">SOTA preserved" wording is scoped to 2 witnessed grammars. Correction (per 2C's own LAC-2C-SK18-02 scoping discipline, which 2A does not honour): qualify the GENERALIZATION header — SK-V18 grounds SOTA on JSON+CSS only; Sheets is a generality (not a SOTA) proof; the 9-grammar fleet SOTA is SK-V19. | `skinny/RESULTS.md` rows are JSON+CSS; no Sheets/CSV/Math/BNF/EBNF SOTA comparator grounded in 2A; 2C LAC-2C-SK18-02 binds exactly this scoping but 2A omits it. |
| CH2-V1-05 | 2C | ACCEPT | The Lock-14 self-gate falsification is real and grammar-NEUTRAL: LOCKS.md:349 asserts `rg 'JsonParser\|CssL4Parser\|BbnfBootstrap\|GoogleSheetsParser' crates/.../src/` "returns ZERO"; the live grep returns 13 sites (9 grammar-named `idents` rows in the generic `ir` crate `strategy.rs:137-185` + 2 analysis + 2 ir passes). The narrow 4-name regex catches only 4 of 9 idents (Csv/Math/Bnf/Ebnf/CssPretty escape). This is the totality relocated-seam, correctly refuted. | `crates/ir/src/registry/strategy.rs:137-185` verified (9 rows, all grammar-named); `rg ... = 13` verified live; the table is consumed via `for_grammar_with_manifest`/`:216`. |
| CH2-V1-06 | 2C | ACCEPT | `css_types.rs` Lock-14-(c) refutation is correct and grammar-neutral: the file lives in the GENERIC `crates/core/src/css_types.rs` (verified on disk), not a `crates/<grammar>/` declaration crate, so Lock 14 (c) does not admit it. Correctly routed to SK-V19 relocate-or-delete. | `ls crates/core/src/css_types.rs` confirmed (2373 B); LOCKS.md:349 names it verbatim as "the mess". |
| CH2-V1-07 | 2C | ACCEPT | The Sheets precedence-tower negative-control grounding is grammar-neutral and correctly cited: Pratt "Top Down Operator Precedence", POPL 1973, DOI 10.1145/512927.512931 VERIFIED (title/author/venue confirmed). The tower is the SOLE Sheets-distinctive construct JSON+CSS lack; lowers to existing `SinkOnlyExpr` vocabulary, needs no new IR primitive — a true generality stress, not a JSON-only technique. The `Nu8u8` 295×/21× demotion is sourced to SYNTHESIS-RESEARCH:42 (the source text confirms both figures). | DOI verified via ACM/web; SYNTHESIS-RESEARCH.md R-E row contains "295×"/"21×" verbatim. |
| CH2-V1-08 | 2C/2D | ACCEPT | The 5-shape `BackendShape` dispatch backbone is genuinely grammar-NEUTRAL: `select_lowering(cost.chosen)` dispatches exactly `{EagerTape,OffsetTape,EventTape,SinkOnly,CollapsedStage}` from the cost verdict, never a grammar tag (`lower/mod.rs:18-24` verified). The `emit_shape_source==lowered_program` firewall + `runtime_target_rows_collapsed` co-gate are the correct neutrality falsifiers (md5-distinct = necessary-not-sufficient). The `RuntimeEmitterKind{CompiledLowering,RequestFacts}` second discriminator R-A deletes is verified live (`grammar_provider.rs:40-43`). | `lower/mod.rs:18-24`, `grammar_provider.rs:40-43` verified; iburg LOPLAS 1992, egg POPL 2021, Mison VLDB 2017 cite-grounded. |
| CH2-V1-09 | 2C | ACCEPT | The single-file Sheets onboarding neutrality (import_closure as a derived DATA flag in `RuntimeFrontendRequirements`, NOT a `match grammar` arm) is grammar-neutral and verified: no `GoogleSheets =>`/`Sheets =>` arm exists in `codegen`/`passes`/`ir` generic crates. | `grep -E 'GoogleSheets...=>'` over generic crates = 0; struct confirmed `grammar_provider.rs:46-57`. |
| CH2-V1-10 | 2C | ACCEPT | The 9-grammar fleet onboarding test is correctly scoped: SK-V18 witnesses the un-fork on 3 (JSON+CSS+Sheets); with only one of Sheets/BBNF-self witnessed the claim is SCOPED to the witnessed grammars (LAC-2C-SK18-02). This is the correct GENERALITY discipline — fleet-wide wording requires SK-V19 adoption. The roster (9 generated grammars, 8 source roots) is verified on disk. | `crates/core/src/grammar/generated/` = 9 grammars; `grammar/` = 8 roots; LAC-2C-SK18-02 binds the scoping. |
| CH2-V1-11 | 2E | ACCEPT | x86/AVX-512 esoterica are correctly held grammar-neutral SECONDARY (never an M5 Max close route); aarch64-ONLY standing grounded; x86 28-file deletion target verified (matches 1E D-1E-V5-04). The two-fan OR-reduce and eq-set kernel carry the byte-set as caller data (Lock 14). No JSON-only-grounded-but-fleet-used defect in 2E's host-arch rows. | `find ...x86_64 ext/x86 -type f = 28` (1E); Lemire-2026 ARM-match post EXISTS (verified, SVE2-match-fastest + NEON-eq-fan in comments — 2E attributes "(comments)" honestly). |
| CH2-V1-12 | 2D | ACCEPT | The cost-model engine activation is grammar-neutral and the V2 "zero-rule scaffold" supersession is accurate at HEAD: `NormalizeDirectSinkCost` is a live `Rewrite` impl (`backend_egraph.rs:191-193`, applied `:75`); `collapsed_stage.rs lower_rule` delegates to `tape_plan::render_rule(.., Collapsed)`. Dispatch is on BackendShape, not grammar — no JSON-only overfit. | `backend_egraph.rs:75,191-193` + `collapsed_stage.rs:16` verified. |
| CH2-V1-13 | 2C | **REJECT** | V3 frontmatter `cycle: V3` and the SK-V15-lineage body assert the converged SK-V15 (V2) rows carry forward "verbatim, retained" with the V3 extension grounding the SK-V18 generalisation. The 2C Pattern-H census is internally CONTRADICTORY and one branch is stale-as-fact: the retained V2 body (Assertion 3, `SPEC.md:69-70`) asserts "the root runtime file count remains exactly 67" and SK-V15-2C-PATTERN-H-COLLAPSE cites `wc -l = 67`, while the V3 Open-Research row admits a `67→71` drift. The live census is 71 (`find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l = 71`). A retained-verbatim row asserting "exactly 67" as the live count, alongside a 71 reality, is a stale grounding presented as current fact — it must be struck or re-keyed to 71, not carried as both 67 (asserted) and 67→71 (open question). This is a generality-relevant defect because the Pattern-H collapse claim is the JSON+CSS+Sheets+BBNF fleet-collapse evidence. | `find crates/core/src/runtime -mindepth 2 -type f -name '*.rs' | wc -l` = 71; 2C carries both "exactly 67" (V2 Assertion 3) and "67→71 drift" (V3 OQ). |

## Spot-Verified Citations (CH2 most load-bearing)

- **Pratt, "Top Down Operator Precedence", POPL 1973, DOI 10.1145/512927.512931** — VERIFIED (title/author/venue confirmed via ACM record + web). 2C Sheets negative-control grounding is real and grammar-neutral.
- **Lemire, "The fastest way to match characters on ARM processors?", 2026-04-19** — VERIFIED EXISTS; SVE2 `match` named fastest, `vceqq_u8` NEON route in the comment section (2E attributes "(comments)" honestly).
- **`bbnf-simd/tests/checkasm_parity.rs:3`** — VERIFIED verbatim "Modelled on FFmpeg's `tests/checkasm/checkasm.h`" (2A/2B/2D process-discipline grounding).
- **`crates/ir/src/registry/strategy.rs:137-185`** — VERIFIED 9 grammar-named `idents` rows in the generic `ir` crate; the Lock-14 self-gate `rg ... = 13` (asserts ZERO) is RED (2C refutation accurate).
- **`crates/core/src/css_types.rs`** — VERIFIED present in the generic core crate (2C Lock-14-(c) refutation accurate).
- **`find_component_delim` 79.5% / 94.1% scalar-scan** — VERIFIED `SYNTHESIS-PROFILE.md:90,96,98`.
- **eq-set kernel JSON consumer (2B/2C/2F)** — FALSIFIED: `byte_class_from_eq_set_64` / `find_ascii_set_member64` have NO live production JSON (or non-CSS) consumer; only CSS `runtime_simd.rs` + tests/bench.

## Fold Requirements

REVISE folds (V2):
1. **2B** (CH2-V1-01): strike the empirical "rides BOTH the JSON `scan_dispatch`/`byte_class_from_table_64` path AND CSS" dual-consumer claim for `byte_class_from_eq_set_64`; re-ground the §6 inner-kernel neutrality on the structural fact (caller-supplied ≤8-byte set, kernel names no grammar) and disclose that its only live production consumer is CSS per Lock 14's bbnf-simd clause.
2. **2F** (CH2-V1-02): same correction in PTG-2F-10 and Assertion 3; downgrade "Lock 16 FULLY SATISFIED TODAY" same-wave-consumer cell to reflect the CSS-only live consumer; fix the `runtime_simd.rs:6-7` paraphrase ("the same kernel JSON's `scan_structurals` rides" is false — `scan_structurals` rides `byte_class_from_table_64`).
3. **2C** (CH2-V1-03): strike "proven by JSON's own `find_ascii_set_member64` consumer"; re-ground the inner-kernel neutrality structurally and disclose CSS-only live consumer.
4. **2A** (CH2-V1-04): qualify the GENERALIZATION ">SOTA preserved across JSON+CSS+Sheets" framing to JSON+CSS (the only grounded SOTA planes); cite 2C LAC-2C-SK18-02 scoping discipline; Sheets is a generality proof, not a SOTA plane, in SK-V18.

REJECT fold (V2):
5. **2C** (CH2-V1-13): re-key the retained-V2 "root runtime file count remains exactly 67" to the live 71 (or strike the absolute count and keep only the structural-provenance claim); resolve the internal 67-vs-71 contradiction — do not carry "exactly 67" as fact alongside the "67→71" open question.

## Convergence Impact

This CH2 result BLOCKS T-P2 V1 convergence: 4 REVISE + 1 REJECT folds are required.
The core GENERALITY backbone (5-shape cost-derived dispatch, the un-fork firewall,
the Lock-14 self-gate falsification, the Sheets precedence-tower negative control,
the 9-grammar scoping discipline) is grammar-neutral and sound; the defects are
localized to (a) a false JSON-consumer neutrality proof for the §6 eq-set kernel
repeated across 2B/2C/2F, (b) an unscoped fleet-wide >SOTA framing in 2A, and (c) a
stale 67-vs-71 Pattern-H census in 2C.

TALLY accept=8 revise=4 reject=1
