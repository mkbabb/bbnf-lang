# CH3 REGRESSION - T-P1 V1

## Lens Contract

CH3 checks that the P1 inventories do not reopen routes already rejected in `skinny/REDRESS.md`, that 1D/1E/1F identify the rejected-route pre-blocks, and that admitted REDRESS rows are not silently downgraded to unimplemented. The governing prompt names this exact scope at `restart/prompts/totality/PASS-1-EXCAVATION.md:116-119`; the orchestrator repeats it at `restart/prompts/ORCHESTRATOR.md:81-88`.

## Findings

| ID | Disposition | Inventory finding | Evidence | Required V2 action |
|---|---|---|---|---|
| CH3-001 | ACCEPT | 1D correctly preserves the core rejected-route ledger for pair-token fusion, alternate dispatch, skipless tokens, union class/cursor attempts, string micro-proof production, and docs-only direct closure. | 1D lists the strongest disproved routes at `restart/audit/totality/p1/1D-skinny-lessons.md:24`, details alternate/pair/skipless as pre-blocked at `restart/audit/totality/p1/1D-skinny-lessons.md:62`, and cites REDRESS rejection rows at `skinny/REDRESS.md:209-234`, `skinny/REDRESS.md:2910-2934`, and `skinny/REDRESS.md:3495-3527`. | Keep the 1D rejected-route rows; no regression fix needed. |
| CH3-002 | REVISE | The inventories understate the SK-V13 pre-block/unblocked split. 1D says parse rows and REDRESS-119 residuals are reopened, but the pre-block list in 1F is still mostly SK-V5/SK-V6 era and omits SK-V13's current hard pre-blocks. | SK-V13 marks all 51 JSON rows and fresh union attempts as unblocked only with strict fresh evidence at `restart/skinny/tranches/sk-v13/SYNTHESIS.md:95-106` and `restart/skinny/tranches/sk-v13/SYNTHESIS.md:255-260`, while pre-blocking diagnostic-only parse treatment, REDRESS-119-history closure, producer-only artifacts, stale comparator sidecars, grammar-name branches, sidecars, and Wave 0 before G-Omega at `restart/skinny/tranches/sk-v13/SYNTHESIS.md:237-253`. 1F's pre-block ledger stops at older route classes in `restart/audit/totality/p1/1F-past-corpora.md:30-37` and `restart/audit/totality/p1/1F-past-corpora.md:43-48`. | Add a current SK-V13 pre-block table to 1D or 1F: "unblocked with fresh evidence" is not the same as "route accepted"; list the hard pre-blocks from SK-V13 §5 explicitly. |
| CH3-003 | REVISE | 1A miscatalogues admitted SinkOnly direct evidence as a substrate divergence when it says direct parsing is divergent because it does not instantiate `TapeBuilder` or `Tape`. | 1A marks JSON direct parsing as diverged at `restart/audit/totality/p1/1A-substrate-evidence.md:39-40` and repeats the divergence at `restart/audit/totality/p1/1A-substrate-evidence.md:50`. REDRESS admits generated SinkOnly as the Track 1 direct workload, explicitly says Track 1 calls generated runtime while Track 2 is independent at `skinny/REDRESS.md:535-557`, and RESULTS confirms JSON direct uses generated SinkOnly with independent hand Track 2 at `skinny/RESULTS.md:145-147`. 1C gets the narrower classification right: SinkOnly direct parser is implemented, with a hand-written sink dependency, at `restart/audit/totality/p1/1C-runtime-evidence.md:43` and `restart/audit/totality/p1/1C-runtime-evidence.md:58`. | Revise 1A-SUB-001/010 and divergence 2 to separate "admitted SinkOnly direct shape exists" from "V1 shared event scheduling remains unaudited." Do not classify lack of `TapeBuilder` inside SinkOnly as regression by itself. |
| CH3-004 | REVISE | 1A and 1C risk demoting the admitted CSS L4 row by treating the fact-stream runtime primarily as a substrate outlier instead of as an admitted same-plane product row with a V1 substrate-classification gap. | 1A calls CSS fact-stream "not the Lock 1 substrate union" and estimates replacement/routing at `restart/audit/totality/p1/1A-substrate-evidence.md:44` and `restart/audit/totality/p1/1A-substrate-evidence.md:58`; 1C calls it not the claimed runtime artifact set at `restart/audit/totality/p1/1C-runtime-evidence.md:80` and leaves classification open at `restart/audit/totality/p1/1C-runtime-evidence.md:90`. REDRESS records PASS-ADMIT for `css_l4/declaration_values/direct_to_struct/main` at `skinny/REDRESS.md:3824-3840`, and RESULTS carries the admitted row plus overall campaign close at `skinny/RESULTS.md:94` and `skinny/RESULTS.md:145-148`. 1D and 1E correctly preserve that admission at `restart/audit/totality/p1/1D-skinny-lessons.md:38`, `restart/audit/totality/p1/1D-skinny-lessons.md:60`, and `restart/audit/totality/p1/1E-locks-evidence.md:72`. | Reword 1A/1C CSS rows so the admitted row remains ACCEPTED evidence; the V2 divergence should be "V1 lacks a substrate/telemetry category for admitted fact-stream product rows," not "replace or reroute the row." |
| CH3-005 | ACCEPT | 1E does not silently regress admitted rows. It keeps JSON NO-GO rows distinct from the SK-V12 CSS PASS-ADMIT and proposes row-plane accounting rather than blanket success. | 1E classifies Lock 8 as over-stated because RESULTS has JSON NO-GO rows plus CSS L4 admission, not universal SOTA closure, at `restart/audit/totality/p1/1E-locks-evidence.md:39` and `restart/audit/totality/p1/1E-locks-evidence.md:56`; its amendment candidate preserves CSS PASS-ADMIT while requiring row-plane accounting at `restart/audit/totality/p1/1E-locks-evidence.md:72`. This matches RESULTS `skinny/RESULTS.md:94` and `skinny/RESULTS.md:145-148`. | Keep the 1E L08/LAC-1E-04 shape. |
| CH3-006 | ACCEPT | 1F correctly prevents re-deriving older rejected routes and distinguishes closed direct-codegen honesty from direct throughput residuals. | 1F lists dispatch-table, skipless token, EventCursor sidecar, Class A tiny-string, bench-private SinkParser, and SinkOnly lowerer residual rows at `restart/audit/totality/p1/1F-past-corpora.md:30-35`, then explicitly says direct throughput is residual rather than proof that SinkOnly is absent at `restart/audit/totality/p1/1F-past-corpora.md:45-46`. REDRESS supports the direct-codegen close at `skinny/REDRESS.md:420-438`, `skinny/REDRESS.md:535-557`, and `skinny/REDRESS.md:662-683`. | Keep these rows; extend them with SK-V13 current pre-blocks per CH3-002. |

## Cycle Verdict

Disposition counts: ACCEPT 3, REVISE 3, REJECT 0.

CH3 verdict: REVISE. The inventories mostly preserve rejected-route history, but V2 must repair the SinkOnly and CSS admission wording and add the SK-V13 pre-block/unblocked split so future agents do not treat "fresh evidence may reopen" as "old rejected route is admitted."
